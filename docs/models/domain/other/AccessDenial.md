# AccessDenial

## На какой вопрос отвечает этот файл

Что это за модель `AccessDenial`.

## Назначение

`AccessDenial` — журнальная строка **отвергнутого по правам вызова к нашей
поверхности**: попытка обратиться к закрытой точке, у которой принципал не
предъявлен либо предъявленный не принят, — а также попытка под принятым
принципалом, которому операция не разрешена.

Это **носитель наблюдаемости** (`docs/concept.md`, П3): персистентная
запись, по которой держатель видит интерес к поверхности. Лог этой роли не
исполняет — он не запрашивается, не агрегируется и не переживает ротацию.

Модель не валидатор и не команда: она ничего не решает и ни на что не
влияет. Её единственный потребитель — разбор человеком.

## Почему это отдельная модель, а не отчёт об аномалии

`AnomalyReport` принять этот факт не может, и оба довода несущие:

- **структурно** — у отчёта `exchangeAccountId` обязателен, а у отвергнутого
  вызова биржи нет вовсе: он не про биржу и не про сделку;
- **по назначению** — отчёт фиксирует расхождение локального состояния с
  фактическим состоянием на бирже, а здесь никакого расхождения не
  происходит: вызов до контура не дошёл.

Больший след назван и обоснован: существующий носитель факт не принимает,
а без носителя клейм наблюдаемости остаётся заявленным
(`.claude/rules/design-simplicity.md` — упрощение не делает носителем то,
что фактом не является).

## Природа факта — происшествие

У каждой попытки свой момент, поэтому **дедупа нет**: каждая отвергнутая
попытка заводит свою строку. Природа факта — ось «один отчёт на что»
(`docs/models/domain/other/AnomalyReport.md`), и здесь она разрешается в
происшествие: состояния, которое держалось бы во времени, у отказа нет.

**Счётность обязательна.** Схлопывание попыток в одну строку сделало бы
частоту — единственную величину, ради которой строка и заводится, —
систематически заниженной.

## Структура

| Поле | Тип | Обязательно | Назначение |
|---|---|---|---|
| `id` | `Long` | да | Внутренний идентификатор. |
| `internalId` | `String` | да | Внешний идентификатор строки. |
| `surface` | `String` | да | Поверхность, к которой обратились: HTTP-метод и путь. Отвечает на «куда стучались». |
| `outcome` | `Outcome` | да | Класс отказа. |
| `principal` | `String` | нет | **Принятый** принципал — тот, чью личность контур удостоверил. Пусто ⟺ `outcome = PRINCIPAL_ABSENT`, и пустота покрывает обе тропы: принципал не предъявлен и предъявленный не принят. Заявленное, но не удостоверенное имя сюда не пишется — это была бы запись непроверенного как факта. |

Наследует поля аудита (`docs/models/domain/other/Auditable.md`) — все шесть
по бинарному правилу состава; биржевые остаются пустыми навсегда, и это
названная цена самого правила, а не дефект этой модели.

**Пустота биржевых полей здесь — «события не было», а не «не добыто»**
(`docs/rules/absent-value-semantics.md`). Требование «у локального события
биржевой момент тоже существует» на эту модель не распространяется: там
поле означает момент **в биржевом домене**, а у отвергнутого вызова
биржевого домена нет вовсе. Ходить за серверным временем источника ради
такой строки нечего — и незачем, и на тропе отвергнутых кредов нечем.

**Актор строки и её предмет — разные вопросы, и на разных тропах они
расходятся.** `createdBy` отвечает на «кто **инициировал ход**», `principal`
— на «кого **отвергли**»:

| Тропа | `createdBy` | `principal` |
|---|---|---|
| принципал не предъявлен либо не принят | **контур**: внешнего инициатора, которого контур удостоверил, нет | пусто |
| принципал принят, операция не разрешена | **имя принципала**: ход порождён внешним вызовом под предъявленным принципалом | то же имя |

**Совпадение на второй тропе не делает поля одним — и довод не в будущем, а
в первой тропе.** Там значения уже расходятся: актор — контур, предмет —
пусто. Схлопывать поля значило бы объявить, что строку отказа создал тот,
кого отвергли, — при том что на самой частой тропе его имени не существует
вовсе. Ссылаться на будущий ход, где отвергнутый и инициатор окажутся
разными лицами, здесь нечем: при посылке одного субъекта такой популяции
нет, и довод, опирающийся на неё, был бы выдуман, а не выведен.

## Енум `Outcome`

| Значение | Что означает | Писатель |
|---|---|---|
| `PRINCIPAL_ABSENT` | принципал не предъявлен **либо предъявленный не принят** | точка входа отказа аутентификации |
| `OPERATION_FORBIDDEN` | принципал предъявлен и принят, но операция ему не разрешена | точка входа отказа авторизации |

**Непринятые креды — тот же класс, что и непредъявленные, и это не
слияние двух фактов.** Разделяет значения то, **известен ли субъект**: у
непринятых кредов он неизвестен ровно так же, как у непредъявленных —
записать заявленное имя значило бы записать непроверенное как факт. Третье
значение различало бы то, чего мы не знаем.

**`OPERATION_FORBIDDEN` не достижим при посылке фазы 1 «к поверхности
обращается один субъект»**, и это записано, а не умолчано: право у
единственного принципала на всё (`docs/rules/api-access-policy.md` — там же
параметр, снимающий посылку). Значение заводится вместе с первым, а не
позже, потому что оно **часть контракта внешней поверхности**, а не
удобство: слить исходы дешевле сейчас и дороже потом
(`docs/rules/error-handling-policy.md`). Писатель у него назван и
достижим — недостижима сегодня сама популяция, а не актор.

**Достижимость возвращается вместе с посылкой:** появился второй субъект
поверхности — ветвь становится штатной, и ни модель, ни перечень, ни
писатель от этого не переписываются.

## Чего строка не несёт

- **Текста исключения и тела запроса** — по тому же доводу, по которому их
  не несёт ответ: вызывающий, не предъявивший себя, о контуре не узнаёт
  ничего, и симметрично — контур не хранит того, чего не понимает
  (`docs/rules/error-handling-policy.md`).
- **Секретов** — предъявленный токен, подпись, заголовок авторизации в
  строку не попадают ни в каком виде.
- **Сетевого адреса вызывающего.** Он не отвечает на «кто»: при
  непредъявленном принципале ответа на этот вопрос не существует вовсе.
  **Кандидат-потребитель у него назван, и он один** — решение о реакции на
  серию отказов: различить «много попыток из одного места» и «много мест»
  без адреса нечем. Поле заводится **вместе с тем решением**, а не
  превентивно: сегодня неизвестно даже, понадобится ли различение.

## Инварианты

- **Пустота `principal` и `PRINCIPAL_ABSENT` — одно и то же состояние**,
  выраженное дважды; расхождение между ними означает дефект писателя.
- **Строка заводится до ответа, а не после.** Ответ вызывающему —
  последнее, что делает тропа отказа: строка, заведённая после, потерялась
  бы ровно на том исходе, где отказ и интересен.
- **Отказ записи не превращается в доступ.** Если строку завести не
  удалось, вызов всё равно отвергается: ответ остаётся отказом, а сбой
  записи уходит во внутреннюю градацию
  (`docs/rules/error-handling-policy.md`). Обратный порядок — «не смогли
  записать, значит пропускаем» — был бы ошибкой в разрешающую сторону
  (П1 следствие 3), а молчаливый пропуск записи запрещён П3.
- **Ступени серия отказов не поднимает.** Это решение с названной ценой, а
  не пропуск; дом решения и условие его пересмотра —
  `docs/rules/api-access-policy.md`.

## Персистентность

Таблица `access_denials`, наследует поля аудита. Строковые колонки —
`varchar` со своей шириной у каждой (`internal_id` / `principal` — 64,
`surface` — 256, `outcome` — 32).

**Индексов два, и оба не про дедуп.** `uk_access_denial_internal_id` —
уникальность внешнего идентификатора строки, как у прочих моделей;
`ix_access_denial_created_at` (по убыванию) — под единственного читателя,
разбор человеком по времени. **Индекса под ключ дедупа нет и не заводится**:
дедупа нет по природе факта.

**Рост таблицы ничем не ограничен, и это названное ограничение.** Строку
заводит каждая отвергнутая попытка, а попытки инициирует тот, кто себя не
предъявил: поток запросов растит таблицу без потолка. Дедуп здесь запрещён
природой факта — он и есть то, чем частота измеряется, — поэтому ограничение
не снимается ключом. Что с этим делать, решается вместе с реакцией на серию
отказов: обеим нужна одна и та же наблюдённая частота
(`.claude/work/backlog.md`, задача о реакции на серию отказов доступа).

**Ширина поверхности усекается писателем.** Значение приходит от
вызывающего, и длина под его контролем; без усечения длинный путь ронял бы
запись на ограничении колонки — то есть отказ доступа стирал бы собственный
след, и тем надёжнее, чем длиннее путь.

## Связи

- Дом правила доступа — `docs/rules/api-access-policy.md`.
- Форма ответа на отказ — `docs/rules/error-handling-policy.md`.
- Поля аудита и область значений актора —
  `docs/models/domain/other/Auditable.md`.
