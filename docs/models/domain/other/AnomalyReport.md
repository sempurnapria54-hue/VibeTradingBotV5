# AnomalyReport

## На какой вопрос отвечает этот файл

Что это за модель `AnomalyReport`: структура, енумы, инварианты,
персистентность.

Статусы и переходы — в `docs/lifecycles/AnomalyReport.md`.

## Назначение

`AnomalyReport` — доменная модель для фиксации, сопровождения и
аудита аномалий, обнаруженных при сравнении локального состояния
системы и фактического состояния на бирже. Это **не** валидатор и
**не** команда, а журналируемый объект расследования и обработки
инцидента (поэтому — `docs/models/domain/other/`, а не `core`: модель не про
бизнес-цикл сделки, а про аудит/расследование инцидента, см.
`.claude/decisions/models-core-vs-other.md`).

## Структура

Java-модель, наследует поля аудита от `Auditable`.

| Поле | Тип | Обязательно | Назначение |
|---|---|---|---|
| `id` | `Long` | да | Внутренний идентификатор отчёта. |
| `internalId` | `String` | да | Межсервисный идентификатор отчёта. |
| `exchangeId` | `Long` | да | Биржа, на которой обнаружена аномалия. |
| `instrumentId` | `Long` | нет | Инструмент; `null`, если однозначно не определён. |
| `scope` | `HoldScope` | да | **Радиус** инцидента: `INSTRUMENT` / `EXCHANGE`; снимает неоднозначность `instrumentId = null`. Ярлыки уровня (L3/L4) с енума сняты — уровень живёт в error-политике, одному уровню отвечают разные радиусы (H6 `GAPS_CLOSE_5`, реконсилировано здесь H4 `GAPS_CLOSE_6`). Групповое значение упразднено (H14 `DOCS_CHECK_15`, §Инварианты). Общий enum с `HoldSignal` (дом — `docs/components/models/HoldSignal.md`). |
| `status` | `Status` | да | Текущий статус обработки (см. lifecycle). |
| `severity` | `Severity` | да | Критичность аномалии. |
| `kind` | `Kind` | да | **Природа факта:** `STATE` (факт есть состояние scope) / `EVENT` (факт есть отдельное происшествие). Дискриминатор идемпотентности: `STATE`-отчёт — процесс, который продолжают, `EVENT` — своя строка на событие (§Инварианты; H16 `DOCS_CHECK_10`, механизм ревизован H17 `GAPS_CLOSE_13`). |
| `code` | `String` | да | Машинно-читаемый код аномалии. |
| `message` | `String` | нет | Текст непредвиденной ошибки обработки; заполняется при `status = ERROR`. |
| `internalBefore` | `String` (jsonb) | нет | Снимок локального состояния до обработки. |
| `externalBefore` | `String` (jsonb) | нет | Снимок внешнего состояния (биржа) до обработки. |
| `internalAfter` | `String` (jsonb) | нет | Снимок локального состояния после обработки. |
| `externalAfter` | `String` (jsonb) | нет | Снимок внешнего состояния (биржа) после обработки. |

## Енумы

### `Status`
`CREATED`, `IN_PROGRESS`, `KILL_SWITCH_EXECUTED`, `COMPLETED`,
`ERROR`. Значения, переходы и какие поля заполняются на каждом шаге —
в `docs/lifecycles/AnomalyReport.md`.

### `Severity`

**Одна сущность, две тропы обработки; разводит их `severity`** (H6,
`GAPS_CLOSE_6`). Отдельной «журнальной» сущности не заводим: аудит-аномалия
и инцидент реактивной обработки — один объект расследования, различается
**поведение**, а не онтология.

**`Severity` отвечает ровно на один вопрос: критична аномалия или нет** (H9
`DOCS_CHECK_12`). Силу реакции холда она не выражает — это отдельная ось на
стороне холда (`ReactionClass`, `docs/components/models/HoldSignal.md`), и
переносить её сюда не нужно: производители, которым нужна мягкая
блокировка, отчёта не заводят вовсе.

- `CRITICAL` — критичная аномалия: **гоняется kill-switch** — полная
  реакция инструмента и ступень 2 биржевой лестницы
  (`docs/components/HoldService.md`,
  `docs/components/SafetyHoldCoordinator.md`); отчёт проходит
  `KILL_SWITCH_EXECUTED` и терминализуется только по подтверждённому
  снятию риска.
- `NON_CRITICAL` — некритичная: kill-switch **не гоняется**, процесс
  завершается **штатно** (`CREATED → COMPLETED` для журнальной аномалии,
  либо через `IN_PROGRESS`, если состояние держится).
  Типичные производители — сверка bills↔net и cross-ccy на финализации,
  несвежесть ставки/ключа группы, пометка «неисчислимо» на аварийном
  терминале.

**«Холд до ручного разбора» не тождественен kill-switch:** мягкий запрет
входов (`Instrument.ENTRY_BLOCKED`) — тоже холд с ручным снятием, но его
отчёт идёт `NON_CRITICAL`, потому что kill-switch в составе реакции нет;
блокировку задаёт состав реакции, не `severity` (§ниже). На биржевом
радиусе то же самое: ступень 1 (`Exchange.HOLD`) — холд с ручным снятием
при `NON_CRITICAL`-отчёте (`kind = STATE`), потому что kill-switch в
составе реакции нет; ступень 2 гоняет kill-switch ⇒ `CRITICAL`
(`docs/rules/exchange-hold.md`).

### `Kind`

**Природа факта — отдельная ось от `severity`** (H16 `DOCS_CHECK_10`).
`severity` отвечает на «гоняется ли kill-switch»; `kind` — на «один отчёт
на **что**».

- `STATE` — факт есть **состояние scope**, держащееся во времени
  (несвежесть ставки/ключа группы; реактивный холд любого scope —
  `docs/rules/exchange-hold.md`, `docs/rules/instrument-hold.md`). Пока состояние
  держится, второй отчёт по тому же ключу заводить нельзя — иначе журнал
  тонет в копиях каждого тика. **Поиск незавершённого отчёта применяется.**
- `EVENT` — факт есть **отдельное происшествие**, привязанное к своему
  моменту и своей сделке (расхождение сверки; cross-ccy движение;
  «неисчислимо» на терминале). Каждое такое событие — **своя строка**;
  схлопывать их нельзя, они обязаны быть **счётными**. **Поиска
  незавершённого для них нет.**

**Почему природа факта — дискриминатор.** «Один на факт» у холда означает
«один на **состояние**», у журнала — «один на **событие**»; одним
механизмом это не выражается. Прежняя редакция ставила общий анкер по
(`exchangeId`, `instrumentId`, `scope`, `severity`) и сталкивалась с
lifecycle: журнальная тропа завершается **сразу**
(`CREATED → COMPLETED`), значит охраняемое множество незакрытых в
установившемся режиме **пусто** и анкер не срабатывал никогда — у того
самого производителя, ради которого вводился.
Держать журнальные отчёты открытыми было нельзя тем более: все
`NON_CRITICAL`-коды шага 7 со `scope = INSTRUMENT` совпали бы по ключу
поиска и маскировали бы друг друга, а `RESULT_PROFIT_UNAVAILABLE` по
разным сделкам одного инструмента схлопнулся бы — это ровно те события,
которые надо считать поштучно.

**Торговое следствие — счётность `unknown`'ов.** Защита от смещения
(реш.3 `docs/decisions/pnl-finalization-mechanics.md`) строится на том,
что null-случай в расчёт ожидаемости не входит **как unknown** (сделка при
этом записана и из популяции не выводится — узел F `DOCS_CHECK_12`); такое
невключение корректно, **только если число unknown'ов известно**. При схлопывании
маркеров оно систематически занижено, причём тем сильнее, чем чаще отказ —
дефект скрывал бы сам себя. `kind = EVENT` у `RESULT_PROFIT_UNAVAILABLE`
это и обеспечивает (`docs/models/domain/aggregate/Deal.md` §«Рамка
R-выборки»).

**Почему не «`code` в ключ».** Отвергнуто и остаётся отвергнутым: по
разным сделкам одного инструмента отчёты с одинаковым `code` всё равно
схлопнулись бы. Ось не та — различать надо **природу**, а не тип.

**Severity не задаёт блокировку торговли.** Она отвечает **только** на
«отрабатывает ли kill-switch». Блокировку задаёт **состав реакции**
error-политики (`docs/rules/error-handling-policy.md` §«Перечень scope и
реакций»), а несёт её **статус scope** (`Instrument.Status.TRADE_BLOCKED` /
`ENTRY_BLOCKED`, `docs/rules/instrument-hold.md` §Enforcement). Прежняя
формулировка («`CRITICAL` → торговля остаётся запрещённой; `NON_CRITICAL` →
после kill-switch может быть разрешена») выводила блокировку из severity и
описывала обе ветки в kill-switch-терминах — снята: `NON_CRITICAL`-тропа
kill-switch не проходит вовсе, а мягкий холд ставится при
`NON_CRITICAL`-аномалии и снимается вручную. На биржевом радиусе
разведение то же: ступень 1 (`Exchange.HOLD`) — гейт входов при
`NON_CRITICAL`-отчёте (`kind = STATE`), ступень 2 —
kill-switch и `CRITICAL` (`docs/rules/exchange-hold.md`).

## Инварианты структуры

- `exchangeId` обязателен — каждая аномалия относится к конкретной
  бирже.
- `instrumentId` nullable — аномалия может относиться к внешней
  сущности, для которой локальный инструмент не определён.
- `scope` (`HoldScope`: `INSTRUMENT` / `EXCHANGE`) явно кодирует
  **радиус** инцидента и снимает неоднозначность `instrumentId = null` —
  которая **двузначна**: биржа-широкий радиус vs неопределённый
  инструмент. Enum общий с `HoldSignal` (сигнал из прохода) — дом енума
  `docs/components/models/HoldSignal.md`.
- **Групповой радиус отчёта упразднён; отчёт о несвежести заводится на
  инструмент** (H14 `DOCS_CHECK_15`, решение пользователя; **сужает**
  ратифицированную радиусную конструкцию `ANOM-Q5` и снимает H4
  `GAPS_CLOSE_6` / H15 `DOCS_CHECK_11` в части группового радиуса).
  Инцидент «строка ставки / ключ группы протухли» фиксируется **по одному
  отчёту на каждый затронутый инструмент**: `scope = INSTRUMENT`,
  `instrumentId` заполнен.
  - **Почему.** Групповая конструкция требовала собственного
    идентификатора субъекта — колонки `fee_group_key`, — а её состав не был
    задан нигде: голый `groupId` схлопывал бы две разные группы источника
    (`instType` + `groupId` — пара, `docs/components/InstrumentExternalRulesSyncJob.md`
    §«Матч по паре, не по голому `groupId`»), конкатенация не имела бы
    зафиксированного формата.
    Через **инструмент** достаётся и группа, и биржа: обе половины ключа
    резолвятся с навеса, а идентичность несёт уже существующая колонка
    `instrument_id`. Отдельная колонка не нужна **вовсе**.
  - **Цена принята явно:** одна проблема синхронизации порождает **N
    отчётов** вместо одного. Это тот же ратифицированный принцип, которым
    уже выражается радиус блокировки, — «радиус — набором строк, не
    отдельным статусом» (`docs/rules/instrument-hold.md`): мягкий холд
    группы и так ставится **набором** статусов инструментов, теперь так же
    фиксируется и журнал.
  - **Что заменяет прежний довод «N отчётов задваивали бы один факт».**
    Задвоение — это две строки об **одном** субъекте; здесь субъектов N, по
    одному на инструмент, и каждый несёт своё состояние (свой статус
    блокировки, своё снятие оператором). Схлопывание их в одну строку
    прятало бы, какие именно инструменты затронуты.
  - **Радиус `EXCHANGE` для несвежести больше не используется**:
    режим «вызов не прошёл» затрагивает все инструменты контура и даёт
    отчёт на каждый. Ратифицированная клауза `ANOM-Q5` «субъект состояния
    на радиусе биржи — биржа» **сужена**: она остаётся верной для
    реактивной тропы (биржевой холд ступени 2 —
    `SafetyHoldCoordinator`) и для
    `AnomalyJob`, но к несвежести ставок больше не применяется — там
    субъект состояния инструмент, потому что именно он несёт статус
    блокировки.
  - **Следствие для енума:** `HoldScope.INSTRUMENT_GROUP` производителей
    не остаётся ни одного (`HoldSignal` им не оперировал никогда), поэтому
    значение **не вводится** — енум остаётся двузначным
    (`docs/components/models/HoldSignal.md` §«Енум `HoldScope`»).
- **Идемпотентность `STATE`-отчёта держится незавершённым статусом, а не
  уникальным ключом** (H17 `GAPS_CLOSE_13`, решение пользователя;
  ревизует H16 `DOCS_CHECK_10` и H15 `DOCS_CHECK_11` в части механизма).
  Анкер-ограничение уникальности **снято целиком** — ни группового, ни
  негруппового частичного `UNIQUE` в схеме нет.

  **Отчёт — процесс с фазами**, а не запись-факт:

  | Фаза | Что происходит |
  |---|---|
  | снапшот «до» | состояние, в котором факт обнаружен |
  | выполнение команд | реакция (для `CRITICAL` — teardown через `SafetyHoldCoordinator`) |
  | снапшот «после» | состояние по завершении реакции |

  Производитель **перед созданием ищет незавершённый отчёт** своего кода и
  радиуса; нашёл — **продолжает его с нужной фазы**, не заводит второй.
  Это же и восстановление после рестарта: незавершённый отчёт сам
  описывает, на чём процесс прервался, и повторный проход доводит его до
  `COMPLETED`.

  - **Ключ поиска — составной по радиусу** (`ANOM-Q5`, закрыт
    2026-08-20; групповая строка снята H14 `DOCS_CHECK_15`):
    (`exchange_id`, `code`, `scope`) **плюс операнд
    идентичности радиуса**, резолвимый по `scope` — `INSTRUMENT` →
    `instrument_id`; `EXCHANGE` → без операнда. Без операнда идентичности
    поиск по (`exchange_id`, `code`, `scope`) находил бы незавершённый
    отчёт **другого инструмента** того же кода и сливал бы два инцидента в
    одну строку. Отчёты несвежести теперь тоже адресуются
    `instrument_id` — той же строкой ключа, что прочие
    `INSTRUMENT`-отчёты; отдельного операнда группы нет.
  - **Сравнение — в форме, корректной с пустыми значениями**
    (`is not distinct from`, не `=`): равенство с `null`-параметром не
    совпадает никогда, и незавершённый отчёт негруппового радиуса не
    находился бы — синк заводил бы копию каждый тик. Это тот же
    `NULL`-дефект, которым обоснован снос анкер-ключа, только переехавший
    из `UNIQUE` в `SELECT`.
  - **Поиск незавершённого применяется и к критическим (`CRITICAL`)
    `STATE`-отчётам**, не только к журнальной ветке синка: предикат
    поискового индекса включает `KILL_SWITCH_EXECUTED` — статус,
    принадлежащий только критической тропе. Формулировка lifecycle
    «несвежесть — единственный производитель этой ветки» говорит о ветке
    **внутри `NON_CRITICAL`**; таблица §«Производящая поверхность» — о
    виде `kind = STATE` **поперёк троп**. Оба верны в своих прочтениях, и
    разночтение разведено здесь явно (`ANOM-Q5`).
  - **Субъект состояния несвежести — инструмент** (H14 `DOCS_CHECK_15`;
    сужает клаузу `ANOM-Q5` «субъект состояния на радиусе биржи —
    биржа»). Протухший ключ группы в режиме «вызов не прошёл» остаётся
    нарушением контракта интеграции и ручным разбором
    (`docs/rules/error-handling-policy.md` §«Нарушение контракта
    интеграции»), но **схлопывание затронутых инструментов в один отчёт
    радиуса `EXCHANGE` снято**: состояние, которое отчёт журналирует,
    несёт **статус инструмента** — он же и снимается оператором
    поинструментно. Прежняя клауза остаётся верной для реактивной тропы
    (биржевой холд — обе ступени лестницы) и для `AnomalyJob`.

  - **Почему не ключ.** Уникальный ключ выражал «второго такого факта не
    заводить», но не выражал «доведи начатое до конца», и рестарт
    посередине реакции оставлял отчёт в незавершённой фазе без писателя.
    Плюс ключ был **мёртв на `scope = EXCHANGE`**: `instrument_id = null`,
    а `NULL`'ы в `UNIQUE` PostgreSQL по умолчанию различны — то есть на
    негрупповом радиусе анкера не существовало вовсе, и синк с часовым
    тактом заводил бы копию каждый тик до ручного разбора.
  - **Посылка отсрочки была ложной.** Клейм «в шаге 7 таких
    производителей нет (оба `STATE`-кода — групповые)» был опровергнут
    тремя носителями и снят; после H14 `DOCS_CHECK_15` вопрос отпал по
    другой причине — групповых `STATE`-кодов не осталось вовсе, оба
    заводятся радиусом `INSTRUMENT`.
  - **`feeGroupKey` полем не заводится** (H14 `DOCS_CHECK_15`): вместе с
    групповым радиусом упразднён и его идентификатор. Идентичность
    субъекта несёт `instrument_id`, через который резолвятся и группа, и
    биржа. Из ключа уникальности `feeGroupKey` ушёл раньше — вместе с
    самим ключом; теперь колонки нет ни в схеме, ни в модели.
  - **`ANOM-Q3` снимается вместе с ключом:** «два незакрытых факта разных
    `code` на одном ключе» — вопрос о ключе, которого больше нет. Поиск
    незавершённого отчёта идёт **по `code`**, поэтому `FEE_RATE_STALE` и
    `FEE_GROUP_KEY_STALE` не подавляют друг друга.
  - **`ANOM-Q4` снимается вместе с ключом** — вопрос был «чем анкеровать
    негрупповые `STATE`-отчёты».
  - **Число процессных писателей — три, и это факт, а не проблема**
    (`ANOM-Q5`, закрыт 2026-08-20 на трёх; четвёртый писатель, вводившийся
    интерим-редакцией лестницы под биржевую ступень 1, **снят** ревизией
    держателя `GAPS_CLOSE_18` вместе с реактивным путём к
    `Exchange.HOLD` — `docs/decisions/exchange-safety-ladder.md`): синк
    ставок (журнальная тропа, `FEE_*_STALE`),
    `SafetyHoldCoordinator` (реактивная, коды из
    `HoldSignal`), `AnomalyJob` (проактивная — шаг 8). Их наборы кодов не
    пересекаются, а каждый актор на одном инстансе сериализован
    (`JobExecutionGuard` / проход оркестратора) ⇒ межакторная гонка
    «два такта — два незавершённых отчёта» в фазе 1 **недостижима**.
    Достижимая тропа задвоения была другой — несовпадение гранулярности
    обхода (инструмент) с радиусом отчёта (группа/биржа) внутри одного
    тика; она закрыта **проходом радиуса** (за тик — не более одного
    отчёта на пару «код + радиус»,
    `docs/components/InstrumentExternalRulesSyncJob.md`) и составным
    ключом поиска (§выше). Проблемой число писателей станет вместе с
    мультиинстансом — страховка на том горизонте: частичный уникальный
    индекс по незавершённым (§Персистентность).
  - **Хвост 2 (популяция ограничения) — зависимый.** Для действующего
    механизма популяция **записана** — неразобранные строки (предикат
    поиска и поискового индекса, §Персистентность); прежняя посылка
    «популяция не названа» была ложной (H27 `DOCS_CHECK_14`). Содержание
    у вопроса остаётся только для формы «частичный уникальный индекс» —
    он оживает вместе с ней на горизонте мультиинстанса (`ANOM-Q6`,
    `.claude/work/questions/open-questions.md`).
  **Отчёты `kind = EVENT` процессом не являются:** каждое событие — своя
  строка, они обязаны быть счётными (§Енумы, `Kind`), и поиска
  незавершённого для них нет.
- `severity` и `code` — независимые измерения: `severity` —
  уровень опасности, `code` — конкретный тип аномалии.
- `message` заполняется только одновременно с `status = ERROR`,
  содержит текст непредвиденной ошибки обработки (техническое поле,
  не бизнес-описание аномалии).

## Производящая поверхность и коды шага 7

**Поверхность создания — две, по природе производителя** (H23,
`GAPS_CLOSE_7`; идентичность реактивной поверхности переведена на сигнал —
H5 `DOCS_CHECK_14`). Существующая точка входа рассчитана только на
реактивную тропу: `instrumentId`/`exchangeId` она берёт **из
`HoldSignal`** (прежнее чтение из `DealContext` снято вместе с уходом
контекста из подписи `HoldService.hold(...)` — карточка сделки не нужна
нигде по цепочке) и жёстко ставит `Severity.CRITICAL`. Производители
шага 7 в неё не проходят: сверка/cross-ccy/«неисчислимо» дают
`NON_CRITICAL`, а `InstrumentExternalRulesSyncJob` работает вне сделки.

| Поверхность | Кто зовёт | Что задаёт |
|---|---|---|
| Реактивная (существующая) | `SafetyHoldCoordinator`, `AnomalyJob` | `HoldSignal` — идентичность объекта (`instrumentId`/`exchangeId`) **из сигнала** (H5 `DOCS_CHECK_14`: `DealContext` из цепочки ушёл вместе с уходом из подписи `HoldService.hold(...)`); `severity = CRITICAL`, `kind = STATE`; слепки собирает `AnomalyReportService`, радиус приходит ему через координатора из того же сигнала |
| Журнальная (вводится шагом 7) | `FinalizeDealExitExecutor`, `RefreshBillsExecutor`, `MarkDealEmergencyClosedExecutor`, `InstrumentExternalRulesSyncJob`, `HoldService` (ветка `FREEZE` — биржевая ступень 1) | `exchangeId`, `scope`, опц. `instrumentId`, `code`, `severity = NON_CRITICAL`, **`kind`** (`EVENT` у producer'ов сделки, `STATE` у синка и `FREEZE`-холда), слепки |

**Процессные (`STATE`) отчёты ведут четыре актора** (`ANOM-Q5` закрыт на
трёх; четвёртый — `HoldService` в ветке `FREEZE`, введён вместе с первым
автоматическим триггером биржевой ступени 1, `GAPS_CLOSE_19`): синк
(журнальная тропа), `HoldService` (биржевая ступень 1),
`SafetyHoldCoordinator` (реактивная с kill-switch),
`AnomalyJob` (проактивная, полноценно — шаг 8). Поиск незавершённого
применяется ко **всем** `STATE`-отчётам, включая критические
(§Инварианты структуры).

Журнальная поверхность **не требует `DealContext`** и severity не
захардкоживает — иначе отчёты синка выразить нечем: у них нет сделки, а
`severity` у них `NON_CRITICAL`. После H14 `DOCS_CHECK_15` радиус этих
отчётов — `INSTRUMENT` (по одному на затронутый инструмент), поэтому
довод «у радиуса нет одного инструмента» отпал вместе с групповым
радиусом; довод про отсутствие сделки и про `severity` остаётся.

**Производителем отчёта серия неудачных исполнений не является** (H9
`DOCS_CHECK_12`, решение пользователя). Довод разведения поверхностей —
«существующая точка входа жёстко ставит `CRITICAL`; производители шага 7 в
неё не проходят» — остаётся верным **целиком**: мягкая ветка холда в
поверхность не заходит, потому что **отчёта не производит**. Серия
`FAILED`-строк — это **параметр**, который отслеживает наблюдатель, чтобы
повесить холд через `HoldService` (`docs/components/HoldService.md`,
`docs/rules/instrument-hold.md` §«Носитель серии»); аномалией она не
является. Отсюда:

- **поверхность не параметризуется и не расширяется** — `open(...)`
  по-прежнему жёстко ставит `CRITICAL`, `severity`/`kind` в неё не
  передаются;
- **`Severity` отвечает ровно на один вопрос** — критична аномалия или нет:
  `CRITICAL` ⇒ kill-switch и холд до ручного разбора, `NON_CRITICAL` ⇒
  работаем дальше. Класс реакции холда (`SOFT`/`FULL`) — **другая ось**, она
  живёт на стороне холда (`docs/components/models/HoldSignal.md`) и в отчёт
  не переносится;
- **`STATE`-ветку lifecycle внутри `NON_CRITICAL` ведут двое** — синк
  (несвежесть ставки) и `HoldService` в ветке `FREEZE` (биржевая
  ступень 1, `GAPS_CLOSE_19`); других ведущих у неё нет и не требуется
  (`docs/lifecycles/AnomalyReport.md` §«Кто управляет»).

**Реестр кодов шага 7 — журнальной поверхности** (`code` обязателен — до
сих пор ни одного значения для `NON_CRITICAL` объявлено не было). Коды
**реактивной** поверхности живут не здесь: их источник — `HoldSignal.code`
(§ниже, H8 `DOCS_CHECK_16`).

| `code` | Производитель | Факт | `scope` | `kind` |
|---|---|---|---|---|
| `PNL_RECONCILIATION_MISMATCH` | `FinalizeDealExitExecutor` (журнальный) + `HoldService` (отчёт состояния холда) | общее расхождение по сделке (`Σ|Δᵢ|` по **четырём** парам раздельной сверки) вышло за epsilon (H19 `DOCS_CHECK_12`, четвёртая пара и единый допуск — H7/H9 `GAPS_CLOSE_13`); сопровождается `Deal.reconciliationStatus = MISMATCHED`. **Уровень градации назначен** (решение держателя, `GAPS_CLOSE_19`): уровень 4, **ступень 1** биржевой лестницы — при **калиброванном** допуске; в разведочном режиме только журнальный отчёт, лестница не триггерится (`docs/rules/pnl-reconciliation.md` §«Реакция на расхождение»). Тот же код несёт `HoldSignal` `FREEZE`-тропы | `INSTRUMENT` (журнальный) / `EXCHANGE` (холд) | `EVENT` (журнальный) / `STATE` (холд) |
| `RESULT_CURRENCY_MISMATCH` | `FinalizeDealExitExecutor` / `MarkDealEmergencyClosedExecutor` (H9 `DOCS_CHECK_17`) | валюта записи закрытия (`Position.externalResultCurrency`) не совпала с расчётной валютой инструмента (H10 `DOCS_CHECK_10`); расчёт не блокируется | `INSTRUMENT` | `EVENT` |
| `UNCLASSIFIED_CASH_FLOW` | `FinalizeDealExitExecutor` / `MarkDealEmergencyClosedExecutor` (H9 `DOCS_CHECK_17`) | у сделки есть залинкованные строки категории `OTHER` — тип движения не распознан и не исключён (H14 `DOCS_CHECK_10`) | `INSTRUMENT` | `EVENT` |
| `SETTLE_CURRENCY_VIOLATION` | `RefreshBillsExecutor` | движение в валюте, отличной от расчётной валюты инструмента | `INSTRUMENT` | `EVENT` |
| `SETTLE_CURRENCY_UNAVAILABLE` | `RefreshBillsExecutor` / `FinalizeDealExitExecutor` / `MarkDealEmergencyClosedExecutor` / `MarkDealClosedExecutor` (H9 `DOCS_CHECK_17` — писателей валюты три) | расчётная валюта инструмента не резолвится — операнд сравнения/источник валюты результата пуст (H10 `DOCS_CHECK_10`) | `INSTRUMENT` | `EVENT` |
| `CROSS_CCY_RATE_UNAVAILABLE` | `RefreshBillsExecutor` | котировка для пересчёта cross-ccy движения не получена: `appliedRate` = `null`, слагаемое не вносится (H11 `DOCS_CHECK_10`) | `INSTRUMENT` | `EVENT` |
| `RESULT_PROFIT_UNAVAILABLE` | `MarkDealEmergencyClosedExecutor` | число на аварийном терминале «неисчислимо» (`null`) | `INSTRUMENT` | `EVENT` |
| `EMERGENCY_TERMINAL_UNVERIFIED` | `MarkDealEmergencyClosedExecutor` | сделка завершена аварийным терминалом: сверка bills ↔ net **не гонялась** (`reconciliationStatus = NOT_RUN` — обязанность не наступила, чаще всего потому, что движения не добывались: bills-звено на аварийной тропе не эмитится). **Одна запись на эпизод.** Клейм «обе ветви» **снят** (A2 `DOCS_CHECK_19`): ветвь с посчитанной сверкой отчёта не производит — там исход `MATCHED`/`MISMATCHED`. Клауза «сделка выведена из R-выборки» **снята** (узел F `DOCS_CHECK_12`): из популяции ничего не выводится, факт несёт поле `Deal.reconciliationStatus = NOT_RUN`, а отчёт остаётся журнальной записью о факте | `INSTRUMENT` | `EVENT` |
| `PLANNED_RISK_MISSING` | `FinalizeDealExitExecutor` / `MarkDealEmergencyClosedExecutor` | у сделки состоялись операции, но `Deal.plannedRiskAmount` пуст — сопровождается **`Deal.riskBenchmarkAvailability = MISSING`** (H13 `DOCS_CHECK_16`: две природы пустоты знаменателя разведены признаком, нормальная популяция несёт `NOT_APPLICABLE`) — **сумма по ногам входа пуста, потому что ног с плановым риском нет** (позиция создана вне приложения; H6/H11 `DOCS_CHECK_15` смысла кода не меняет): знаменатель R не определён, сделка помечается как `R-unknown` и **считается** поштучно; омиссионный член epsilon не участвует (H23/H7 `DOCS_CHECK_11`) | `INSTRUMENT` | `EVENT` |
| `CLOSE_OUTCOME_UNDETERMINED` | `FinalizeDealExitExecutor` / `MarkDealEmergencyClosedExecutor` | операнд торгового исхода (`Position.externalCloseType`) **пуст, потому что записи закрытия нет** ⇒ `Deal.closeOutcome = UNDETERMINED` (H2 `GAPS_CLOSE_13`). Холда не вызывает. Ветка «запись добыта, а поле пусто/вне перечня» сюда больше не приходит: контракт записи проверяется **на границе интеграции**, и её нарушение уводит сделку общим контуром в биржевую ступень 2 (`Exchange.TRADE_BLOCKED` + flatten) (H5 `DOCS_CHECK_15`, `docs/models/mapping/PositionCloseResult.md` §«Контракт записи проверяется здесь») | `INSTRUMENT` | `EVENT` |
| `BREAKDOWN_COMPLETENESS_NOT_ASSESSED` | `MarkDealEmergencyClosedExecutor` (P3 `DOCS_CHECK_24`: у штатного финализатора ветка **недостижима** — предикат эмиссии его звена требует терминального **исхода** добычи, а исчерпание бюджета уводит сделку ошибочной тропой) | добыча движений не выполнялась (`Deal.billsFetchedThrough` пуст) ⇒ сравнение с глубиной архивной выдачи не выполнялось ⇒ `Deal.breakdownIncomplete = NOT_ASSESSED` (H4 `GAPS_CLOSE_13`; **триггер один**, прежняя причина «границы окна не добыты» снята — A1 `DOCS_CHECK_23`); «не проверяли» отделено от «проверили, всё в порядке» | `INSTRUMENT` | `EVENT` |
| `PARTIAL_EXIT_ROUNDED_TO_FULL` | `SizeCalculator` (через исполнителя действия) | объявленная доля частичного выхода после округления вниз по `lotSz` оказалась меньше `minSz`, и остаток позиции тоже меньше `minSz` ⇒ выход исполнен **целиком**: «частично» на этом размере невыразимо. Исход объявляется, а не подставляется (C5 `DOCS_CHECK_20`, `docs/components/SizeCalculator.md` §«Reduce-only / algo») | `INSTRUMENT` | `EVENT` |
| `PARTIAL_EXIT_BELOW_MIN_SIZE` | `SizeCalculator` (через исполнителя действия) | объявленная доля меньше `minSz` при остатке ≥ `minSz` ⇒ действие **не исполнено**, шаг стратегии пропущен, сделка остаётся в текущем статусе (C5 `DOCS_CHECK_20`) | `INSTRUMENT` | `EVENT` |
| `FEE_RATE_STALE` | `InstrumentExternalRulesSyncJob` | возраст строки ставки группы > порога — **по одному отчёту на каждый инструмент группы** (H14 `DOCS_CHECK_15`) | `INSTRUMENT` | `STATE` |
| `FEE_GROUP_KEY_STALE` | `InstrumentExternalRulesSyncJob` | возраст ключа группы на инструменте > порога — **по одному отчёту на каждый затронутый инструмент**, включая режим «вызов не прошёл» (H14 `DOCS_CHECK_15`) | `INSTRUMENT` | `STATE` |

**Отчёты по конкретной сделке — всегда `EVENT`** (все строки реестра, кроме
двух последних и холдовой половины первой): это и есть разведение по
природе факта (§Енумы, `Kind`) — факт привязан к сделке и её моменту,
поэтому считается поштучно и не схлопывается.

**`STATE` производят факты состояния scope, и производителей у них в
шаге 7 двое** (A11 `DOCS_CHECK_21` — прежняя редакция говорила «только
несвежесть у синка» и «радиус всегда `INSTRUMENT`», что опровергалось
собственной строкой реестра выше):

| Производитель `STATE` | Факт | `scope` |
|---|---|---|
| `InstrumentExternalRulesSyncJob` | несвежесть ставки / ключа комиссионной группы (`FEE_RATE_STALE`, `FEE_GROUP_KEY_STALE`) | `INSTRUMENT` — групповой радиус упразднён, режим отказа влияет на **число** затронутых инструментов, а не на радиус отчёта (H14 `DOCS_CHECK_15`) |
| `HoldService`, ветка `FREEZE` | состояние биржевой ступени 1, поднятой по `PNL_RECONCILIATION_MISMATCH` (`GAPS_CLOSE_19`) | **`EXCHANGE`** — ступень 1 радиусом биржи и является |

Держатся `STATE`-отчёты, пока холд не снят.

**Тот же код бывает и `EVENT`, и `STATE`** — `PNL_RECONCILIATION_MISMATCH`
в первой строке реестра: журнальная половина (`INSTRUMENT` / `EVENT`)
фиксирует **факт расхождения по сделке**, холдовая (`EXCHANGE` /
`STATE`) — **состояние остановки контура**. Различает их не код, а пара
`kind` + `scope`.

**`RECONCILIATION_OPERAND_MISSING` снят из реестра** (решение держателя,
позиция С3 валидации `GAPS_CLOSE_16`, вариант a). Оба операнда допуска
персистятся write-once на ноге — `ctVal` (H5 `DOCS_CHECK_16`) и уровень стопа
(Р3, шестое число), — и резолвятся всегда по инварианту «шесть или ни
одного»; последний производитель кода (вырождение предиката селекции
attached-защиты) в резолве епсилона больше не участвует. Реестр не держит
значений без производителя. Нарушенный инвариант данных ловит
**детектирующий контур отказом операции**
(`docs/models/domain/aggregate/Deal.md` §«Обнаружение рассогласования пары
носителей»), а не журнальная запись. **Заводить код заново не нужно** —
защитной ветки у финализатора не осталось намеренно
(`docs/components/FinalizeDealExitExecutor.md` §epsilon).

**Кодов серии отказов в реестре нет** (H9 `DOCS_CHECK_12`). Серия отчёта
не производит, а `severity` класс реакции не выражает. Код причины холда
по исчерпанию бюджета — **один** (`RETRY_BUDGET_EXHAUSTED` на живой
тропе; `POST_MORTEM_HARVEST_EXHAUSTED` снят A3 `DOCS_CHECK_20` — у
посмертной добычи производителя холда нет), и живёт он как код холда
(`HoldSignal.code`), не как код аномалии;

**Реестр выше — реестр кодов ЖУРНАЛЬНОЙ поверхности** (H8
`DOCS_CHECK_16`). Клауза «кодов серии в реестре нет … живёт как код холда,
не как код аномалии» читалась как отрицание кода у **любого** отчёта этой
ветки — и это прочтение неверно, потому что отчёт там всё-таки заводится:

- **всякое** исчерпание бюджета попыток даёт `FULL` по инструменту
  (`docs/rules/instrument-hold.md` §«Форма реакции…»; H8 `DOCS_CHECK_16`,
  решение держателя `GAPS_CLOSE_16`: различителя нет, мягкая форма и
  «записанное отклонение» сняты) и доходит до `SafetyHoldCoordinator`, а
  его последовательность производит `AnomalyReport` **шагом 2,
  безусловно**;
- код этого отчёта — **`HoldSignal.code`**, то есть
  **`RETRY_BUDGET_EXHAUSTED`**: реактивная поверхность берёт `code`,
  идентичность и радиус **из сигнала** (§«Поверхность создания»;
  `docs/components/models/HoldSignal.md` §«Перечень `code`» — «`code`
  обязателен и попадает в `AnomalyReport.code` реактивного отчёта»);
- **двузначности код не даёт:** мягкой ветки у этого кода не осталось
  вовсе, поэтому `AnomalyReport` с `RETRY_BUDGET_EXHAUSTED` означает ровно
  одно — `FULL` по инструменту. Прежде однозначность держалась тем, что
  мягкая ветка отчёта не производила; теперь — тем, что ветка одна;
- **в реестр журнальных кодов ничего не вводится.** Ветка не «серия»
  (серия отчёта по-прежнему не производит) и не журнальная — она
  реактивная, а у реактивной поверхности собственный источник кода
  (`HoldSignal.code`, по исчерпанию бюджета однозначный). Реестр выше
  перечисляет производителей журнальной тропы и `SafetyHoldCoordinator` не содержит
  **законно**; клейм полноты у него ограничен этой поверхностью, и
  ограничение теперь записано.

**Сам код `RETRY_BUDGET_EXHAUSTED` объявлен** в
`docs/rules/instrument-hold.md` §«Серия неудач: реакция на исчерпание
бюджета» (H6 `DOCS_CHECK_14`: прежняя пара имён
`ENTRY_SIDE_FAILURE_SERIES` / `MANAGEMENT_SIDE_FAILURE_SERIES` снята
вовсе — резолв реакции идёт по типу исключения, стороне ноги
дискриминатор не нужен, и два кода требовали бы от call-site знать
сторону). Побочно снимается вопрос «кто ведёт `STATE`-отчёт серии» —
такого отчёта нет. (B10 `DOCS_CHECK_21`: у абзаца был потерян субъект —
предложение начиналось с «объявлен в», — а указатель вёл в
§«Носитель серии», где код не объявляется.)

**Поиск незавершённого отчёта идёт по `code`** (H17 `GAPS_CLOSE_13`).
Прежняя редакция выводила `code` из ключа анкера (H15 `DOCS_CHECK_9`), и
`FEE_RATE_STALE` с `FEE_GROUP_KEY_STALE`, деля один ключ, подавляли друг
друга — подавление факта не есть идемпотентность (`ANOM-Q3`). С переходом
на «незавершённый статус вместо ключа» вопрос снят вместе с ключом: два
разных `code` — два разных процесса, и повторный тик по **тому же** `code`
продолжает свой отчёт, а не заводит новый.

## Связи

- **Exchange** — биржа, на которой обнаружена аномалия (`exchangeId`).
- **Instrument** — инструмент аномалии (`instrumentId`, опционально).

## Персистентность

**Идемпотентность `STATE`-отчёта — незавершённым статусом, а не
уникальным ключом** (H17 `GAPS_CLOSE_13`, решение пользователя;
ревизует H15 `DOCS_CHECK_9`, H16 `DOCS_CHECK_10`, H15 `DOCS_CHECK_11` в
части механизма). **Частичных уникальных индексов-анкеров в схеме нет** —
ни группового, ни негруппового: производитель перед созданием ищет
незавершённый отчёт своего `code` и радиуса
(`status in ('CREATED', 'IN_PROGRESS', 'KILL_SWITCH_EXECUTED')`) и
продолжает его с нужной фазы (§Инварианты структуры, «Отчёт — процесс с
фазами»).

- **Поисковый индекс вместо уникального:**
  **`ix_anomaly_report_unfinished_state`** (имя названо здесь — место
  истины схемы сущности, B8 `DOCS_CHECK_20`) по (`exchange_id`, `code`,
  `scope`, `instrument_id`) `where kind = 'STATE' and
  status in ('CREATED', 'IN_PROGRESS', 'KILL_SWITCH_EXECUTED')` —
  **не уникальный**, его задача обслужить поиск незавершённого, а не
  запретить вторую строку; состав приведён к форме составного ключа
  поиска (`ANOM-Q5`: операнд идентичности радиуса — `instrument_id` при
  `INSTRUMENT`, без операнда при `EXCHANGE`; сравнение — `is not
  distinct from`). Колонка `fee_group_key` из состава **убрана вместе с
  групповым радиусом** (H14 `DOCS_CHECK_15`). Уникальности индекс не
  охраняет.
- **Частичный уникальный индекс по незавершённым — страховка следующего
  горизонта, не правка сейчас** (`ANOM-Q5`): нужен он только против
  межакторной гонки на **мультиинстансе** и припаркован на тот же
  горизонт (фаза 3), что advisory-замок оркестратора
  (`.claude/rules/tech-radar.md`, строка raw-JDBC `hold`). Внутритиковую
  тропу задвоения он не закрывает и в фазе 1 не вводится.
- **Почему ключ снят.** (1) Он был **мёртв на `scope = EXCHANGE`**
  (`instrument_id = null`, `NULL`'ы в `UNIQUE` различны), а такие
  производители в шаге 7 есть — реактивная тропа биржевого холда.
  (2) Он не выражал главного: «доведи начатое до конца» — рестарт посреди
  реакции оставлял отчёт в незавершённой фазе без писателя, и ключ этого
  не чинил.
- **Отчёты-события (`kind = EVENT`)** заводятся свободно и обязаны быть
  счётными (`docs/rules/idempotency-via-unique.md`,
  `docs/rules/persistence-representation.md`); поиска незавершённого для
  них нет.
- **`ANOM-Q5` закрыт** (2026-08-20): акторов три, межакторная гонка в
  фазе 1 недостижима; внутритиковая тропа задвоения закрыта проходом
  радиуса и составным ключом поиска (§Инварианты структуры). **`ANOM-Q6`
  — зависимый**: популяция действующего механизма записана (неразобранные
  строки — предикат выше); вопрос оживает только при вводе частичного
  уникального индекса (фаза 3,
  `.claude/work/questions/open-questions.md`). `ANOM-Q3` и `ANOM-Q4`
  сняты вместе с ключом.

**Этот раздел — место истины схемы** (`docs/rules/persistence-representation.md`
§«Место истины схемы», H16 `DOCS_CHECK_12`). Schema-дельта шага
(`docs/decisions/pnl-finalization-mechanics.md` §«Schema-дельта шага 7»)
— **сборка-указатель**: при расхождении составов ведёт этот раздел, а
сборка правится под него. Клейма «сборка приведена к этому составу»
здесь нет намеренно — он утверждал бы состояние **чужого** файла и
устаревал бы первой же правкой (B2 `DOCS_CHECK_18`).

**Колонки миграции шага 7** (полная schema-дельта —
`docs/decisions/pnl-finalization-mechanics.md` §«Schema-дельта шага 7»):

- `kind` — `varchar(64) not null` (единая норма длин, H18
  `DOCS_CHECK_15`), значение = `name()` доменного enum
  (codestyle §Слои моделей и enum'ы). **`not null` ставится сразу, тем же
  `ALTER`** — без `default` «на время миграции» и без промежуточного
  nullable-шага: таблица `anomaly_reports` пуста, потому что до конца
  фазы 1 проект считается незапущенным
  (`.claude/rules/pre-launch-schema-changes.md`, H17 `DOCS_CHECK_12`).
  Вопрос «чем заполняются существующие строки» здесь не возникает; если бы
  возникал, ответ **обязан** был бы быть назван — `kind` входит в предикат
  поискового индекса незавершённых, и неверное значение сразу же ломало бы
  поиск;
- **`fee_group_key` не заводится** (H14 `DOCS_CHECK_15`): колонка
  упразднена вместе с групповым радиусом отчёта — идентичность субъекта
  несёт `instrument_id`. Прежняя строка дельты (H15 `DOCS_CHECK_11`,
  `varchar(64)` nullable) снята;
- `scope` — расширяется `varchar(16)` → **`varchar(64)`** (H22
  `DOCS_CHECK_10`, длина приведена к единой норме H18 `DOCS_CHECK_15`).
  Прежний довод («`INSTRUMENT_GROUP` — ровно 16 символов, запас нулевой»)
  отпал вместе со значением, но `ALTER` остаётся: колонка и так в дельте
  шага, а норма длин строковых колонок теперь одна — `varchar(64)`
  (`docs/rules/persistence-representation.md` §«Строковые колонки:
  длины»).

Семантика NULL `instrument_id` перестала быть вопросом ключа (H17
`GAPS_CLOSE_13`): уникального ключа-анкера нет, поисковый индекс ничего не
охраняет — но в **поиске** `instrument_id` участвует как операнд
идентичности радиуса (единственный после H14 `DOCS_CHECK_15`), и сравнение
обязано быть `is not distinct from` (`ANOM-Q5`). `ANOM-Q3`, `ANOM-Q4`, `ANOM-Q5`
сняты/закрыты; `ANOM-Q6` — зависимый (оживает с уникальным индексом,
фаза 3).

Четыре поля `internalBefore` / `externalBefore` / `internalAfter` /
`externalAfter` хранятся в jsonb; в один столбец попадают разнородные
доменные объекты.

- **Локальные снимки** (`internalBefore`, `internalAfter`) —
  доменные модели из БД: активные `Deal`; активные `Position`;
  pending `Order`; pending/active `AlgoOrder`; статус инструмента;
  связанные служебные идентификаторы.
- **Внешние снимки** (`externalBefore`, `externalAfter`) — данные с
  биржи: активные позиции; pending orders; active algo orders;
  сущности, найденные по идентификаторам из БД; orphan-сущности
  (найденные на бирже без соответствия в БД).

Конкретная сериализация (формат, версионирование) — отдельная задача
(стандарт описания персистентности доменных моделей; форвард-заметка
в task-вопросах).

## Чего не хранит

- `dealId` — контекст сделки попадает в `internalBefore`/
  `internalAfter`.
- `tradingBlocked` — блокировка торговли живёт в статусе инструмента.
- Boolean-флаги по шагам обработки — прогресс отражён через `status`.
