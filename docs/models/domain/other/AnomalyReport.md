# AnomalyReport

## На какой вопрос отвечает этот файл

Что это за модель `AnomalyReport`.

## Назначение

`AnomalyReport` — журналируемый объект расследования: фиксация,
сопровождение и аудит аномалии, обнаруженной при сравнении локального
состояния с фактическим состоянием на бирже.

Это **носитель наблюдаемости** (`docs/concept.md`, П3): персистентная
запись, по которой восстанавливается причина сработавшей защиты.

Отчёт не валидатор и не команда.

**Отвергнутый по правам вызов к нашей поверхности этой моделью не
выражается:** биржа у отчёта обязательна, а у такого вызова её нет, и
расхождения с биржевым состоянием не происходит вовсе. Его носитель —
`docs/models/domain/other/AccessDenial.md`.

## Структура

| Поле | Тип | Обязательно | Назначение |
|---|---|---|---|
| `id` | `Long` | да | Внутренний идентификатор. |
| `internalId` | `String` | да | Внешний идентификатор отчёта. |
| `exchangeAccountId` | `Long` | да | **Биржевой счёт**, на котором наблюдено происшествие. Радиус отчёта — счёт, а не площадка: происшествие одного тенанта не описывает состояние другого (`docs/architecture/tenant-and-exchange.md` §«Торговая строка называет счёт, и радиусы читаются от него»). |
| `instrumentId` | `Long` | нет | Инструмент; пусто, если однозначно не определён. |
| `subjectExternalId` | `String` | нет | **Сущность-предмет** — величина ключа дедупа у отчёта БЕЗ блокировки. Пусто у отчёта с блокировкой: его состояние есть стоящая ступень объекта радиуса. |
| `scope` | `HoldScope` | да | **Радиус** инцидента: инструмент либо биржа. Снимает неоднозначность пустого инструмента. |
| `status` | `Status` | да | Статус обработки. |
| `severity` | `Severity` | да | Критичность. |
| `code` | `String` | да | Машинно-читаемый код аномалии. Значение приходит от тропы-производителя; закрытого реестра кодов нет, дом каждого значения — дом его тропы (`docs/components/models/HoldSignal.md`, дом конвенции о домах значений). |
| `message` | `String` | нет | Текст непредвиденной ошибки обработки. |
| `internalBefore`, `externalBefore` | JSONB | нет | Снимки локального и внешнего состояния до обработки. |
| `internalAfter`, `externalAfter` | JSONB | нет | Снимки после обработки. |

## Енумы

**`Status`** — `CREATED`, `IN_PROGRESS`, `KILL_SWITCH_EXECUTED`,
`COMPLETED`, `ERROR`.

**`Severity`** отвечает ровно на один вопрос — **критична аномалия или
нет**. Силу реакции холда она не выражает: это отдельная ось на стороне
холда.

| Значение | Что означает |
|---|---|
| `CRITICAL` | гоняется kill-switch; отчёт терминализуется только по подтверждённому снятию риска |
| `NON_CRITICAL` | kill-switch не гоняется, процесс завершается штатно |

**«Холд до ручного разбора» не тождественен kill-switch:** мягкий запрет
входов — тоже холд с ручным снятием, но его отчёт некритичен, потому что
kill-switch в составе реакции нет. Блокировку задаёт **состав реакции**, а
не критичность. То же на биржевом радиусе: мягкая ступень некритична,
ступень со сворачиванием — критична.

## Природа факта — свойство тропы, а не колонка

**Природа факта — отдельная ось от критичности.** Критичность отвечает на
«гоняется ли kill-switch», природа — на «один отчёт **на что**».

| Природа | Что означает | Дедуп |
|---|---|---|
| **состояние** | факт есть состояние объекта, держащееся во времени | применяется: пока состояние держится, второй отчёт по тому же ключу не заводится (ключ — ниже) |
| **происшествие** | факт привязан к своему моменту | не применяется: каждое происшествие — своя строка |

**Дискриминатор — выбор писателя, и в данных он не выражен.** Носителя-
колонки у природы нет, и заводить его не за чем: величина **известна
писателю на call-site** и не нужна ни одному читателю. Ни один запрос,
гейт, выборка или отчёт корпуса не спрашивает «состояние это или
происшествие» — спрашивают по объекту радиуса, коду и критичности.

**Дедуп состояния энфорсится двумя формами, и обе законны.** Операнд у
них один — **стоящее состояние объекта**, — а носитель разный:

| Форма | Кто гейтит | Когда применима |
|---|---|---|
| **по стоящей строке** — писатель состояния сам отказывается заводить вторую, пока по ключу стои́т своя | сервис отчёта | состояние наблюдается извне, и своего durable-признака у объекта нет: safety-ступени, признаки детекторов |
| **по признаку самого объекта** — писатель сверяет состояние объекта до и после хода и заводит строку на его **возникновение** | вызывающая тропа | у объекта есть свой durable-признак состояния (непустая корзина нераспознанных движений, стоящая ступень) |

Вторая форма строже первой: она различает «состояние держится» и
«состояние возникло заново» — по стоящей строке эти два неразличимы, и
повторное возникновение после разбора она бы погасила. Поэтому тропа со
своим признаком зовёт писателя происшествия и гейтит сама.

**Инвариант, на котором стои́т отсутствие колонки: один код — одна
природа.** Дедуп по стоящей строке ключуется кодом и критичностью, а
природу найденной строки не различает. Пока множества кодов двух троп не
пересекаются, это безопасно; при пересечении строка-происшествие погасила
бы законную строку-состояние **и** послужила бы ложным подтверждением
гистерезиса. Код заводит его тропа-производитель
(`docs/components/models/HoldSignal.md`), и заводить существующий код с
другой природой запрещено этим инвариантом.

**Операнд дедупа — стоящее состояние объекта, а не статус отчёта.**
Спрашивать «есть ли **незавершённый** отчёт» нельзя: журнальная
тропа создаёт отчёт сразу завершённым
(`docs/lifecycles/AnomalyReport.md`), поэтому
множество незавершённых пусто **по построению** и такой дедуп не
срабатывал бы никогда. Тем же доводом ниже объяснено, почему не годится
общий анкер, — и прежняя редакция не применяла его к собственному ключу.
Дом правила — `docs/rules/error-handling-policy.md`.

**Привязка к сделке — типичный признак происшествия, а не обязательный.**
Несущий признак один — **свой момент** против держащегося состояния.
Происшествия без сделки существуют штатно: снятие safety-ступени
держателем привязано к моменту, а не к сделке
(`docs/rules/manual-halt.md`), и природа происшествия для него — не
натяжка.

**Ключ дедупа у состояния** — «**объект радиуса** + `code` + `severity`»,
и вычисляется он **в окне наблюдения**:

- **объект радиуса**, а не радиус вообще: групповых значений у радиуса
  нет, состояние держится на конкретной бирже или
  инструменте;
- **`code`** — иначе ручная остановка схлопнулась бы с автоматической,
  стоящей на том же объекте, и различимость, ради которой коды заведены,
  пропала бы ровно в том случае, ради которого нужна;
- **`severity`** — она несёт **состав реакции** (критична аномалия или
  нет, а состав задаёт блокировку), поэтому
  эскалация мягкой ступени в полную — **другое состояние**, а не повтор
  того же: её строка заводится, и запуск kill-switch виден в журнале;
- **окно наблюдения** — операнд «состояние ещё держится». Носителя
  «последнее наблюдение» в модели нет, поэтому держание читается по
  времени создания стоящей строки, и по истечении окна наблюдение
  записывается заново. **Названное ограничение:** строка получается одна
  на окно, а не одна на всё время держания; дом остатка —
  `.claude/work/backlog.md` §«Хвост шага 8 (safety / AnomalyJob) —
  возврат по появлению носителя».

Это не противоречит «ключ по коду тоже не спасает» ниже: там речь об
отчётах-происшествиях **по разным сделкам** одного инструмента, у которых
дискриминатором служит сделка; у факта-состояния сделки нет по природе, и
дедуп ведётся по состоянию объекта.

**У отчёта без блокировки в ключ входит ещё одна величина — сущность,
о которой он.** Ключ выше предполагает, что состояние держится **на
объекте радиуса**; у отчёта, за которым блокировки нет вовсе
(журнальная тропа, `HoldSignal` фабрик `instrumentJournal` /
`exchangeAccountJournal`), состояние держится на **сущности-предмете** — заявке,
позиции, algo-заявке, — а инструмент в этот момент ни в какой ступени не
стои́т. Ключ поэтому несёт и её: «объект радиуса + сущность-предмет +
`code` + `severity`», в том же окне. Без сущности в ключе два разных
расхождения учёта по одному инструменту схлопнулись бы в одну строку —
тот же дефект, ради
которого в ключ введён `code`, только уровнем ниже.

**Тропа-производитель этого класса названа:** проактивная детекция,
детектор `A8` — локально терминальная сущность, живая на бирже
(`docs/components/AnomalyJob.md` §«Что ищет»). Отчёт с блокировкой
величины предмета не несёт: у него состояние и есть стоящая ступень
объекта радиуса.

**Почему природа, а не код, служит дискриминатором.** «Один на факт» у
холда означает «один на состояние», у журнала — «один на событие»; одним
механизмом это не выражается. Общий анкер по радиусу и критичности
сталкивался бы с жизненным циклом: журнальная тропа завершается сразу,
поэтому охраняемое множество незакрытых пусто, и анкер не срабатывал бы
никогда. Ключ по коду тоже не спасает: отчёты по разным сделкам одного
инструмента с одинаковым кодом схлопнулись бы.

**Торговое следствие — счётность неизвестных исходов.** Невключение
неисчислимого результата в расчёт ожидаемости корректно **только если
число таких случаев известно**. При схлопывании маркеров оно
систематически занижено, причём тем сильнее, чем чаще отказ, — дефект
скрывал бы сам себя.

## Инварианты

- **Групповой радиус выражается набором строк** — по отчёту на каждый
  затронутый инструмент: группового значения у радиуса нет.
- **Радиус реакции и адрес наблюдения — разные оси, и в ключ входят
  обе.** `scope` отвечает на «что блокируется» (инструмент либо биржа);
  `instrumentId` — на «где наблюдался факт». У биржевой ступени,
  поднятой признаком конкретного инструмента (расхождение экспозиции,
  живой риск без покрытия), заполнены обе: реакция биржевая, а адрес
  инструментный, и схлопывать такие строки в одну нельзя — иначе
  пропадает единственное место, где видно, **чем** ступень вызвана.
  Отсюда строк по биржевой ступени может быть несколько — по одной на
  адрес; «одна строка на объект-биржу» верно лишь для факта, у которого
  адреса нет вовсе (слепота прохода, чужой инструмент).
- **Отчёт принадлежит тропе, а не классу реакции**
  (`docs/rules/error-handling-policy.md`): производителем может быть и
  детектор, и сама тропа — у ручного вызова детектора нет по
  построению, — и не всякая мягкая реакция отчёт производит.

## Персистентность

Таблица `anomaly_reports`, наследует поля аудита. Снимки состояния —
JSONB-колонки. Строковые и енум-колонки — `varchar`, и **единого
номинала у них нет**: ширина каждой объявлена своей миграцией
(`scope`/`severity` — 16, `status` — 32, `internal_id`/`code`/
`subject_external_id` — 64, `message` — 1024).

**Ключ дедупа несёт индекс** `ix_anomaly_report_standing` (`V25`):
«биржевой счёт + `code` + `severity` + инструмент + предмет + время создания».
Без него проверка стоящей строки — скан журнала на каждой находке
каждого тика, а журнал растёт: «чем хуже состояние контура, тем дороже
проверка» кормило бы само себя.

## Связи

- Статусы и переходы — `docs/lifecycles/AnomalyReport.md`.
- Градация реакций — `docs/rules/error-handling-policy.md`.
- Сигнал реакции — `docs/components/models/HoldSignal.md`.
- Поиск нарушений инвариантов — `docs/components/AnomalyJob.md`.
