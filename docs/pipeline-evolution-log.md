# Pipeline Evolution Log

История изменений пайплайна работы с Claude на этом проекте: агенты, скиллы, процессы, концепция.

**Правила лога:**
- Append-only. Старые записи не редактируются.
- Каждая запись — заголовок с датой и кратким описанием + 2-5 строк сути.
- Сюда пишутся **сделанные** изменения, не запланированные.
- Записи добавляются в хронологическом порядке (новые — наверх).

---

## 2026-05-14 — backlog добавлен в правило связного обновления

Расширен принцип 4 «Связное обновление документации» в
working-with-claude.md: теперь явно фиксирует синхронизацию с
docs/backlog.md.

- Закрытие пункта backlog работой → пункт перемещается в «Закрытые»
  с датой и ссылкой на ADR/коммит.
- Новый открытый вопрос в ходе работы → сразу в backlog, не в чате
  и не в комментарии.

Соответствующие пункты добавлены в чек-листы playbook («Перед
коммитом», «В конце сессии Claude Code») и в проверки агента
knowledge-curator.

Триггер: при проектировании стандарта структуры docs/spec/
обнаружено, что правило связного обновления покрывает ADR,
спецификации и pipeline-evolution-log, но не backlog. Без явной
фиксации backlog-задачи накапливаются недо-обновлёнными.

---

## 2026-05-14 — терминологическая правка: entity → доменная модель

Зафиксировано предпочтение термина «доменная модель» (или «модель»)
вместо «entity» во всех контекстах проекта. Причина: «entity» в
Spring/Java-проекте нагружено JPA-аннотацией `@Entity`, что создаёт
двусмысленность между доменной моделью и персистентной сущностью.

Не все доменные модели проекта персистентны (DealContext,
CalculationContext — не имеют таблиц). Термин «доменная модель»
покрывает оба случая без подразумеваемой персистентности.

Триггер: пользователь обратил внимание на нагруженность термина
«entity» в обсуждении стандарта структуры docs/spec/.

---

## 2026-05-14 — правило про уточняющие вопросы

Добавлен пункт 11 в Custom Instructions Проекта: уточняющие
вопросы, требующие фактуры из репозитория (листинг папок,
содержимое файлов), не задаются пользователю. Вместо вопроса
предлагается разведочный промпт для Claude Code.

Триггер: в чате по стандарту docs/spec/ Claude задал пользователю
два вопроса про содержимое docs/domain/processes/, на которые
тот не мог ответить из головы. Правильное поведение было —
сразу предложить разведку через Claude Code.

---

## 2026-05-14 — Введён единый бэклог открытых задач

**Что:** создан `docs/planning/backlog.md` — единый список открытых задач,
ADR-кандидатов и тем для проработки. Структура: секции по теме (Документация /
Содержательные ADR / Архитектура / Тесты), внутри пометки приоритета
(Now / Next / Later / Someday). Закрытые задачи не удаляются, помечаются
с датой и ссылкой.

**Зачем:** до этого открытые пункты жили только в `setup-strategy-summary.md`,
который по природе исторический и задним числом не редактируется. По итогам
ADR-0001 добавилось 5 новых TBD-пунктов + крупная задача миграции
`docs/domain/processes/` — нужно единое место, отражающее текущее состояние
очереди.

**Связные изменения:**
- В `setup-strategy-summary.md` добавлена сноска со ссылкой на backlog;
  сам список TODO в summary не редактируется (исторический срез).
- ADR-0001 содержит 5 TBD-пунктов, продублированных в backlog.

**Источник:** чат ADR-0001 → решение о вынесении очереди в отдельный файл.

---

## 2026-05-14 — Initial setup

Запуск работы с Claude на проекте.

- Установлен Claude Code v2.1.141.
- Создана структура `.claude/agents/`, `.claude/skills/`.
- Подняты стартовые агенты: architect, domain-expert, risk-engineer, trading-risk-officer, knowledge-curator.
- Создан `CLAUDE.md`, `docs/working-with-claude.md`, `docs/playbook.md`.
- Принята схема поэтапной миграции документации в `docs/spec/`.
- Старые `codex/stage/`, `codex/tasks/`, `codex/Main.md` удалены. `codex/CodeStyle.md` и `codex/TechRadar.md` перенесены в `docs/conventions/`.

Контекст и обоснования — в первой стратегической сессии в чате (этот лог стартует здесь).
