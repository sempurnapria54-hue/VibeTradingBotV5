# Retention результатов расчёта рыночных данных

## На какой вопрос отвечает этот файл

Какое у нас правило хранения/чистки результатов расчёта рыночных данных
(индикатор / структура / фаза).

## Правило

Персистентные результаты расчёта рыночных данных (`IndicatorValue`,
`MarketStructure`) **не чистятся** — политик удаления / обрезки истории
сейчас не вводим. Оба типа хранят историю рядом строк (строка на свечу /
окно), а не upsert одной строки. `MarketPhase` под retention **не
попадает** — она не персистируется, вычисляется на лету
(`docs/decisions/market-phase-stateless.md`).

Основание: данных для решения о глубине хранения нет — **нет потребителя
истории**, который задал бы нужную глубину. Вводить retention-политику
превентивно — спекулятивно (инфраструктура от потребности, не на пустом
месте).

## Якорь пересмотра

Когда появится **потребитель истории** результатов (бэктест / аналитика /
иной), вернуться к политике retention и определить глубину/чистку под его
потребность.

## Связи

- Свежесть (отдельное правило: устаревание ≠ удаление) —
  `docs/rules/market-data-freshness.md`.
- Модели результатов — `docs/models/domain/other/IndicatorValue.md`,
  `docs/models/domain/other/MarketStructure.md`,
  `docs/models/domain/other/MarketPhase.md`.
- Ключевание / идентичность результатов —
  `docs/decisions/market-data-result-identity-keying.md`.
