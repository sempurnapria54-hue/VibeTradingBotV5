# RefreshPositionExecutor

## На какой вопрос отвечает этот файл

Кто исполняет добычу состояния позиции.

## Что делает

Спрашивает у источника текущую позицию по инструменту, резолвит доменный
статус и применяет его; причину закрытия ставит только если она ещё пуста.

Команда **двуногая**: живая позиция, затем — при её отсутствии либо смене
эпизода — история закрытых позиций.

## Обход внутри одной команды

```text
1) живая позиция
   найдена, тот же эпизод      → активна, обновить внешние поля → стоп
   найдена, другой эпизод      → смена эпизода: живую строку закрыть,
                                 завести строку нового эпизода → нога 2
   не найдена                  → живую строку закрыть → нога 2

2) история закрытых позиций — по каждой строке эпизода, закрытой и без
   добытого положения закрытия
   найдено    → наполнить поля положения закрытия на своей строке
                и подвинуть верхнюю границу окна линковки вперёд
   не найдено → факт не добыт: звено не завершается, повторяется
                по бюджету действия
```

**Нога 2 адресует эпизоды, а не одну позицию:** её предикат — «строка
закрыта и положения закрытия не несёт», поэтому она идемпотентна и
покрывает эпизод, схлопнувшийся и переоткрывшийся между тиками.

**Верхняя граница окна двигается монотонно вперёд** — по времени
**последней** записи: окно обязано накрывать движения всех эпизодов.

**Нижнюю границу окна эта команда не пишет** — у неё единственный
писатель, отправка первой ноги входа.

## Адресация записи истории

Ось — идентификатор эпизода; когда он не наблюдался, запись адресуется
инструментом и **временным окном от времени создания первой отправленной
ноги входа**. Однозначность держит инвариант «одна активная сделка на
инструмент».

**Несколько записей в окне — это несколько эпизодов** одной сделки;
каждая материализуется своей строкой.

**Известное ограничение:** у позиции вокруг чужого риска отправленной ноги
входа нет, значит нет и операнда нижней границы — подтропа не адресуема.

**Позиция, впервые увиденная уже закрытой, заводится этой ногой** сразу
закрытой, с полями положения закрытия из самой записи.

## Что пишет ещё

- **ось эпизода на ногах**: той же транзакцией, в которой эпизод
  материализован или наблюдён, проставляет идентификатор эпизода ногам с
  непустым филлом и пустой осью; write-once;
- **пересчитывает четвёрку чисел риска сделки**: меняемый здесь операнд —
  размер живого эпизода, а числа пересчитываются целиком тем, кто меняет
  любой их операнд.

## Терминала команда не выносит

Отсутствие записи закрытия — не «сущность потеряна»: статус позиции уже
определён первой ногой, а недобытый факт повторяется бюджетом действия.
Этим команда отличается от добычи заявки, где исчерпанный цикл и есть
основание терминала.

**Контракт добытой записи проверяется на границе интеграции**, а не
финализацией: нарушение даёт контролируемое исключение, отказ строки и
бросок.

## Связи

- Модель эпизода — `docs/models/domain/core/Position.md`.
- Числа риска — `docs/spec/deal-risk-numbers.json`.
- Окно линковки — `docs/models/domain/aggregate/Deal.md`.
- Принцип обхода — `docs/rules/command-lifecycle.md`.
