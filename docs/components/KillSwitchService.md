# KillSwitchService

## На какой вопрос отвечает этот файл

Кто триггерит аварийное снятие риска для реактивной реакции.

## Что делает

Тонкий триггер аварийного исполнителя. Снятие риска — состав **полных**
реакций: инструментной и биржевой ступени сворачивания. У мягких ступеней
такого шага нет, и сервис на них не зовётся.

Сам ход и сверку реального состояния делает исполнитель.

**Происхождение полной реакции сервису безразлично:** автоматический
детектор и ручной вызов держателя (`docs/rules/manual-halt.md`) приходят
одинаково — радиусом и триггерной сделкой. Отдельной тропы «ручной
kill-switch» нет.

## Два радиуса

| Радиус | Что делает | Что возвращает |
|---|---|---|
| **инструмент** | снятие риска по графу **триггерной** сделки | подтверждение закрытия риска — оно гейтит терминал отчёта |
| **биржа** | **каскадный обход** всех активных сделок биржи, по вызову исполнителя на каждую | агрегат подтверждений: успех только если подтвердились все |

**Агрегация консервативна:** неподтверждённая сделка делает
неподтверждённым весь каскад, и отчёт не терминализуется.

## Границы

- Класс реакции не выбирает и статус блокировки не ставит.
- Сделки по FSM не ведёт: каскад снимает риск, а статусы применяет
  enforcement петли.

## Связи

- Исполнение — `docs/components/KillSwitchExecutor.md`.
- Последовательность реакции — `docs/components/SafetyHoldCoordinator.md`.
- Ступени биржи — `docs/rules/exchange-hold.md`.
