# SafetyHoldCoordinator

## На какой вопрос отвечает этот файл

Кто держит последовательность полной реакции холда.

## Что делает

Оркеструет исполнителей полной реакции — блокировку с последующим снятием
живого риска. Сам ничего не исполняет напрямую.

Точка входа идемпотентна по статусу объекта блокировки; идентичность
объекта координатор берёт **из сигнала**.

**Мягкие формы через координатора не идут:** мягкий запрет входов
исполняет сам сервис блокировки, мягкая биржевая ступень ставится вручную
либо своим автоматическим триггером. До координатора доходит только
полная реакция.

## Последовательность

1. **жёсткий статус объекта блокировки — первым.** Это одновременно гейт
   и анкер идемпотентности: повторный сигнал того же объекта, уже
   стоящего в этом статусе, реакцию пропускает — снятие риска повторно не
   гоняется;
2. **снятие живого риска** — аварийный исполнитель;
3. **журнальный отчёт** с обеими парами снимков состояния;
4. **каскад активных сделок** в ошибку.

Инструментный и биржевой радиусы у полной реакции **одной формы** и
различаются только исполнителями статуса.

## Гейт терминала отчёта

Отчёт терминализуется только по **подтверждённому** снятию риска. До
подтверждения он остаётся в промежуточном статусе: журнал обязан
показывать, что реакция начата и не завершена.

## Политика отказов

Отказ любого шага не отменяет уже применённого: статус блокировки
остаётся, отчёт остаётся, следующий проход доводит недоделанное. Реакция
best-effort по составу и неидемпотентных шагов не содержит.

## Границы

- Класс реакции не выбирает — он приходит с сигналом.
- Снятие холда не делает: это ручная сервисная операция.
- Сделками не управляет: каскад применяет статус, а не ведёт FSM.

## Связи

- Сигнал — `docs/components/models/HoldSignal.md`.
- Точка входа реакций — `docs/components/HoldService.md`.
- Ступени биржи — `docs/rules/exchange-hold.md`.
- Снятие риска — `docs/components/KillSwitchExecutor.md`.
