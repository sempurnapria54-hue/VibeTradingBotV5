# HoldService

## На какой вопрос отвечает этот файл

Кто исполняет блокировку по требованию детекторов.

## Назначение

`HoldService` — **общий исполнитель блокировки**. Детектор, обнаруживший
основание, зовёт его, передавая **самодостаточный сигнал**: радиус, силу
реакции, машинный код причины и идентичность объекта блокировки.

Точка входа идемпотентна: повторный сигнал на объект, уже стоящий в
запрошенной ступени, реакции не запускает. Предикат анкера и
единственное исключение из него живут в доме анкера —
`docs/components/SafetyHoldCoordinator.md`; здесь он не переписывается. Сигнал — **параметр
вызова**, а не путешествующий объект: сервису не нужно знать, родился ли
холд проходом над сделкой.

## Кто зовёт

| Детектор | Когда | Что запрашивает |
|---|---|---|
| поиск нарушений инвариантов | при классификации аномалии | по классу аномалии |
| оркестратор сделок | при перехвате исключения исполнителя в проходе | по типу исключения |
| исполнители терминального ребра | при расхождении сверки в боевом режиме допуска, **после коммита терминала** | мягкую биржевую ступень, код причины `PNL_RECONCILIATION_MISMATCH`, отчёт (`docs/rules/pnl-reconciliation.md`) |
| исполнители терминального ребра | при достижении порога серии убытков (`Exchange.consecutiveLossCount` ≥ `globalConsecutiveLossLimit`), **после коммита терминала** | мягкую биржевую ступень, код причины `LOSS_STREAK_LIMIT_REACHED`, отчёт |
| **ручная операция остановки** | когда так решил держатель | ступень по паре «радиус × режим», код причины `MANUAL_HALT_REQUESTED`, отчёт (`docs/rules/manual-halt.md`) |

**Счёт детекторов в тексте не дублируется — он пересобирается из
таблицы.**

**Ручная постановка — та же тропа, только решение принимает человек.**
Отдельного пути у неё нет: она собирает сигнал фабрикой и заходит в эту
же идемпотентную точку входа, наследуя анкер, согласование эскалации и
состав реакции; собственного механизма остановки в системе не
появляется. Единственное, чем ручной полный вызов отличается от
автоматического, — право на доведение недоделанного снятия риска, и это
клауза дома анкера (`docs/components/SafetyHoldCoordinator.md`), а не отдельная тропа здесь. **Ручное снятие — вторая операция той же поверхности, и вот
она сюда не заходит**.

**Исчерпание бюджета попыток отдельным актором не является:** это тот же
перехват оркестратором.

**Нарушение контракта в добытой записи обнаруживается в слое
интеграции**, где ответ впервые разбирается, и выражается контролируемым
исключением; дальше реакция идёт общим путём — исполнитель команды,
отказ строки, бросок, перехват оркестратором. Финализация местом
обнаружения не является.

**Писатели статуса вне этого перечня существуют штатно.** Журнальная
тропа синка ставок ставит мягкий запрет входов сама, инструментам
затронутого радиуса. Правила единственности писателя нет.

**Что при этом не меняется:** сервис остаётся общим местом реакции, и
детектор, которому нечего добавить к штатной последовательности, зовёт
его, а не переписывает её у себя. Детектор, ставящий статус сам, берёт на
себя идемпотентность и эскалацию — и это его названная цена.

## Что делает вызов

1. приводит статус объекта блокировки к запрошенной ступени
   (идемпотентно — по предикату анкера, дом которого
   `docs/components/SafetyHoldCoordinator.md`);
2. согласует эскалацию — мягкий запрет входов в холд с kill-switch у
   инструмента, мягкий холд в сворачивание у биржи;
3. запускает состав реакции по её классу;
4. заводит отчёт, если его производит эта тропа.

**Отчёт принадлежит тропе, а не классу реакции:** реакцию несвежести
ставки отчётом снабжает её детектор — синк ставок; мягкую биржевую
ступень, поднятую ручной операцией, — сама тропа вызова.

**Поглощение по уже стоящей ступени гасит шаги 1-3, но не шаг 4.** Отчёт
идемпотентен по своему ключу, и второе основание, у которого свой
машинный код, заводит свою строку, хотя статус не переставляется и
реакция не гоняется. Дом правила и оба ключа —
`docs/rules/error-handling-policy.md`. Прежняя редакция говорила, что
поглощённый вызов «строки не заводит вовсе», и тем отменяла различимость
оснований, ради которой коды заведены.

## Границы

- Сервис не решает, **что** случилось: класс реакции приходит с
  сигналом.
- Сервис не ходит на биржу: снятие живого риска исполняет kill-switch.
- Снятие холда — ручная сервисная операция, не этот путь; **ручная
  постановка — этот**. Обе операции описаны в
  `docs/rules/manual-halt.md`: снятие идёт своей тропой, пишущей статус
  самостоятельно, и несёт идемпотентность само — по образцу писателей
  статуса вне перечня выше. Счётчика серии убытков снятие не трогает:
  его обнуляет только ценово-прибыльная закрытая сделка
  (`docs/rules/loss-streak-halt.md`).

## Связи

- Сигнал — `docs/components/models/HoldSignal.md`.
- Градация реакций — `docs/rules/error-handling-policy.md`.
- Инструментный scope — `docs/rules/instrument-hold.md`.
- Биржевая лестница — `docs/rules/exchange-hold.md`.
- Остановка по серии убытков — `docs/rules/loss-streak-halt.md`.
- Ручное управление остановкой (постановка и снятие) — `docs/rules/manual-halt.md`.
- Последовательность полной реакции — `docs/components/SafetyHoldCoordinator.md`.
