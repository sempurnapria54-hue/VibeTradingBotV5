# StrategyActionOrchestrator

## На какой вопрос отвечает этот файл

Кто выбирает следующее действие стратегии за проход.

## Что делает

Для пакета шага выбирает следующее действие, гейтит повтор и
маршрутизирует действие в исполнитель своего типа.

```java
Optional<StrategyAction> nextAction(StrategyStep step, DealContext dealContext,
                                    DealTranche tranche);
ActionPlan plan(StrategyStep step, StrategyAction action, DealActionState state,
                DealContext dealContext, DealTranche tranche);
```

Выбор действия и его продвижение разведены: `nextAction` отвечает, какое
действие пакета можно **начать** этим проходом (строки у него ещё нет),
`plan` продвигает действие, строка которого уже заведена.

## Порядок выбора действия

**Пакет шага исполняется целиком; за проход — одно действие.**

**Ключ порядка — риск-класс действия: устанавливающие защиту раньше
снимающих** (`docs/rules/live-risk-protection.md`). Риск-класс
оркестратор резолвит и так — он определяет ветку преконтроля и порядок
ног замещения. Порядок вставки ключом порядка не служит; уровень действия
остаётся уровнем грид-сетки.

**Действие, чьё предусловие не выполнено, исполнения не начинает:** строка
исполнения не заводится, бюджет попыток не расходуется, шаг остаётся
неисполненным до следующего прохода. Верхней границы ожидания нет — всё
это время позицию держит прежняя защита. Предусловие спрашивается у
исполнителя типа (`docs/components/StrategyActionExecutor.md`
§«Готовность отвечается ДО строки исполнения»), и три его исхода ведут
пакет по-разному: отложенное действие пакет **останавливает**,
неактуальное — **пропускается**.

**Начинается действие, у которого строки ещё нет.** Действие с заведённой
строкой продвигает `plan`, а не выбор: иначе пакет каждым проходом брал
бы первое действие и на нём же стоя́л — ступень из двух действий (снять
защиту, поставить новую) не доигрывалась бы никогда.

## Гейт повтора

Исполнение, ожидающее повтора, отдаёт команду только по наступлении
времени следующей попытки. Завершённые и отказавшие строки терминальны:
новая надобность — новая строка.

**«Новая надобность» — не повтор того же шага на том же эпизоде.** Шаг
стратегии применяется на эпизоде транша однократно, и его строки
исполнения служат носителем этого признака
(`docs/rules/strategy-step-once-per-episode.md`). Гейт повтора считает
**попытки исполнить** надобность; сколько раз надобность возникает —
предмет того правила, не этого. **Повтор отказавшей надобности** (новая
строка того же шага после `FAILED`) гейтится стоящей ступенью радиуса и
возобновляется её снятием — дом гейта:
`docs/rules/strategy-step-once-per-episode.md` §«Надобность после
исчерпания бюджета — гейт по стоящей ступени».

## Границы

- Статус исполнения сам не пишет — его двигают исполнители команд и учёт
  повторов.
- Состав команд действия не знает: это собственность исполнителя типа.
- Системные действия не ведёт.

## Связи

- Слои исполнения — `docs/processes/fsm-execution-layering.md`.
- Строка исполнения — `docs/models/domain/other/DealActionState.md`.
- Порядок ремодела — `docs/rules/replace-not-amend.md`.
