# SizeCalculator

## На какой вопрос отвечает этот файл

Кто рассчитывает размер действия.

## Что делает

Считает размер заявки или условной заявки и возвращает результат расчёта
размера. Цену не считает, но принимает рассчитанную цену входом.

```java
CalculatedSize calculate(CalculationContext context, CalculatedPrice price);
```

## Размер в контрактах

Размер решается в контрактах: считаются **оба** кандидата — по поактному
бюджету риска и по доле аллокации, — берётся **меньший**, и он
округляется вниз по шагу лота, снизу — минимальный торговый размер. Форма
и примеры — `docs/spec/order-sizing.json` (`contractsFromRisk`,
`contractsFromAllocation`, `desiredContracts`, `entryContracts`); который
из двух связывает — `riskCeilingBinds` и `bindingStopDistance` там же.

Размер контракта, шаг лота и минимальный размер — из справочных правил
инструмента.

## Вход: сайзинг под поактный потолок

Размер ограничен поактным потолком риска: убыток на стопе не превышает
`riskPerActionPercent × база риска`. Это **верхняя граница**, а не цель:
когда меньшим кандидатом оказывается доля аллокации, фактический риск
ноги выходит ниже потолка (дом политики — `docs/rules/risk-policy.md`).

**Считается закрытой формой, не подбором** — комиссия пропорциональна
размеру, поэтому размер решается из неравенства. Форма и примеры —
`docs/spec/order-sizing.json` (`perContractRisk`, `riskBudget`,
`contractsFromRisk`, `entryRiskAtStop`, `entryWithinRiskBudget`); эта спека
— **единственный дом закрытой формы сайзинга**.

Каждая нога комиссии — **по своей цене**: вход по цене входа, выход по
цене стопа. Оценка обеих по цене входа занижала бы комиссию выхода для
коротких позиций и давала бы им систематически больший размер.

Ставка читается аксессором справочных правил — без вызова биржи из
калькулятора и без отдельного поля контекста; ставка не резолвится —
вход блокирует преконтроль, калькулятор её не выдумывает.

- размер округляется по шагу лота и **снизу ограничен** минимальным
  торговым размером;
- если даже на минимуме убыток на стопе выше потолка — калькулятор
  возвращает минимум, а преконтроль блокирует вход;
- **сделочные потолки калькулятор не считает** — их проверяет преконтроль
  над готовым размером: подбирать размер под остаток бюджета значило бы
  выпускать ногу заведомо меньше объявленного, не сообщая об этом;
- **пол дистанции стопа калькулятор не проверяет** — его проверяет
  преконтроль над готовым уровнем (`docs/rules/risk-policy.md`); запас на проскок не закладывается — названное
  ограничение.

**Связывает меньший из двух пределов**, и какой именно — зависит от
дистанции стопа: размер равен `min(по риску, по аллокации)`. Дом политики
«поактный потолок — предел, а не цель сайзинга» и критерия, при каком
стопе какой предел связывает, — `docs/rules/risk-policy.md`.

## Reduce-only выход: пола минимального размера нет

Размер закрытия — доля живой экспозиции, округлённая вниз.

**Подъём размера до минимума менял бы объявленное действие:** частичный
выход стал бы бо́льшим, вплоть до полного, — система сняла бы риск,
который стратегия снимать не объявляла. Ошибка односторонняя и тихая: ни
одна из минимальных проверок «закрыли больше объявленного» не выражает.

Вместо пола — четыре исхода. Условия ветвления — форма
`exitOutcome` в `docs/spec/order-sizing.json` (`exitSize`, `exitRemainder`,
`exitSizeFinal`); здесь — что каждый исход означает.

| Исход | Что означает | Что объявляется |
|---|---|---|
| `PARTIAL` | штатный частичный выход объявленной доли; остаток жизнеспособен | ничего |
| `FULL_BY_FRACTION` | объявленная доля забрала экспозицию целиком; округление ни при чём | ничего: выход ровно такой, каким объявлен |
| `FULL` | «частично» на этом размере невыразимо, поэтому выходим целиком | журнальный отчёт `PARTIAL_EXIT_ROUNDED_TO_FULL` |
| `SKIPPED` | действие **не исполняется** | журнальный отчёт `PARTIAL_EXIT_BELOW_MIN_SIZE` |

**Ветвление идёт по остатку, а не по размеру выхода.** Признак полного
выхода по доле — нулевой остаток, и он не зависит от того, выразим ли сам
размер минимальным торговым: выход, забравший экспозицию целиком, ничего
не округлял, сколь бы мала эта экспозиция ни была.

**«Частично» невыразимо в двух случаях, и оба ведут в `FULL`:** размер
выхода ниже минимального торгового — и остаток ниже него. Во втором
случае объявленная доля исполнима, но оставила бы дробь, которую нечем ни
защитить, ни закрыть; правило, запрещающее её заводить, — дом
`docs/rules/no-partial-close.md`, форма и инвариант —
`docs/spec/order-sizing.json` (`exitSizeFinal`, `exitLeavesNoDust`).

**Полных выхода два, и они разведены значением.** Одно значение на оба
заставляло бы потребителя перевыводить, было ли округление, чтобы решить,
заводить ли отчёт, — то есть исход перестал бы быть достаточным для
единственного решения, ради которого он назван.

**Носитель исхода и его читатель.** Исход едет в `CalculatedSize.exitOutcome`,
итоговый размер — в `CalculatedSize.sizeContracts`
(`docs/components/models/CalculatedSize.md`); читает их per-type
`StrategyActionExecutor` действия выхода, когда решает, отправлять ли
рассчитанное действие и заводить ли журнальный отчёт. Калькулятор отчёт не
заводит и действие не отменяет — он только называет исход и размер.

## Защитная ступень — другой класс

**Последняя ступень набора получает остаток** — `lastLadderStepSize` в
`docs/spec/order-sizing.json`. Округление вниз применяется к предыдущим
ступеням, поэтому суммарное покрытие получается ровно равным экспозиции
**транша**: нетто-размер позиции в операнд не входит — на сетке он
относится к нескольким траншам сразу.

Селектор «последней» и операнд суммы — `docs/rules/live-risk-protection.md`.

**Ступень меньше минимального размера** (`ladderStepRejected`) **—
контролируемая ошибка расчёта**
(`PROTECTION_LADDER_STEP_BELOW_MIN_SIZE`), а не пропуск: шаг не
исполняется явно, с видимым отказом.

**Почему класс разведён.** У выхода «не исполнили» значит «ничего не
произошло»; у защиты то же «не исполнили» значит **«стоп не поставлен»**
— благоприятное умолчание, запрещённое
`docs/rules/absent-value-semantics.md`. Достижимость не краевая: вход
снизу упирается в минимальный размер, поэтому позиция меньше `N`
минимумов делает лестницу из `N` ступеней невыразимой.

## Границы

- Прямое частичное закрытие позиции не считает: полный выход идёт своей
  командой.
- Размер нельзя безопасно посчитать (нет размера контракта, шага лота или
  минимума) — контролируемая ошибка расчёта, а не приближение.

## Связи

- Потолки риска — `docs/rules/risk-policy.md`.
- Форма размера, исходов выхода и ступени лестницы, с примерами —
  `docs/spec/order-sizing.json`.
- Лестница защиты — `docs/rules/live-risk-protection.md`.
- Формы выхода — `docs/rules/no-partial-close.md`.
