# Тенант и биржа

## На какой вопрос отвечает этот файл

Как устроены тенант, его биржевые счета и площадка.

## Три сущности вместо одной

Сегодняшняя модель `Exchange` (`docs/models/domain/core/Exchange.md`)
совмещает **площадку** (имя, точка подключения) и **счёт** (база риска,
серия убытков, ступени холда). Второй тенант и вторая площадка развалят
это совмещение с двух сторон, поэтому сущностей три:

| Сущность | Владелец | Что несёт |
|---|---|---|
| `Exchange` — площадка | платформа (`auth`, справочник) | код площадки (`OKX`, `BYBIT`), точки подключения, возможности (типы контрактов, каналы), статус доступности площадки для платформы |
| `Tenant` | `auth` | идентичность тенанта, статус, владелец; место под тариф зарезервировано и не спроектировано |
| `ExchangeAccount` — биржевой счёт тенанта | `auth` (реестр и ключи), `trading-core` (торговое состояние) | тенант × площадка × метка; путь ключей в Vault; торговое состояние счёта — база риска, валюта базы, серия подряд убыточных сделок, ступень холда, счётчик слепых проходов |

Счёт — двухписательный по двум **разным** наборам полей: реестр и ключи
пишет `auth`, торговое состояние пишет `trading-core`. Физически это две
таблицы в двух базах с общим идентификатором счёта; правило «у таблицы
один писатель» соблюдено (`data-ownership.md`).

## Что переезжает на счёт тенанта

Всё, что сегодня лежит на `Exchange` как состояние счёта, переезжает на
`ExchangeAccount` без смены смысла: база риска и её движение
(`docs/rules/risk-policy.md`), счётчик серии и остановка
(`docs/rules/loss-streak-halt.md`), ступени биржевой лестницы
(`docs/rules/exchange-hold.md`), предел слепоты
(`docs/components/AnomalyJob.md`). Радиус «биржа» лестницы реакций
становится радиусом **счёта**: холд одного тенанта не трогает других.

Посылка «счёт принадлежит системе единолично» (`docs/concept.md`)
переформулируется на счёт тенанта: на биржевом счёте, подключённом к
платформе, торгует только платформа от имени этого тенанта. Чужая заявка
по-прежнему признак постороннего актора, и реакция та же.

## Ключи

- Ключи счёта лежат в Vault по пути, выводимому из идентификатора счёта;
  в базах платформы ключей нет ни в каком виде.
- Пишет ключи `auth` по действию владельца тенанта; коннектор читает их
  по идентификатору счёта из запроса, кэширует с коротким сроком и
  сбрасывает кэш по событию смены ключей.
- Роль Vault у коннектора ограничена префиксом путей счетов; у `auth` —
  правом записи в тот же префикс; у остальных сервисов доступа к ключам
  нет.
- Отвергнутые площадкой ключи — исходящий отказ доступа; его дом и
  реакция не меняются (`docs/rules/exchange-hold.md`).

## Пользователи и роли

- Учётные данные, сессии и токены — у провайдера идентичности
  (`services.md`). Платформа хранит только идентификатор пользователя.
- **Членство** — пользователь × тенант × роль. Роли в тенанте:
  `OWNER` (ключи, риск-аппетит, приглашения, активация стратегий),
  `TRADER` (стратегии и ручные операции без ключей и риск-чисел),
  `VIEWER` (чтение). Вне тенантов — `PLATFORM_ADMIN` (тенанты,
  площадки, доступность платформы; торговых прав ни в одном тенанте не
  имеет).
- Права проверяются на сервере на каждом вызове; фронт лишь скрывает
  недоступное. Как контекст тенанта и роль доходят до сервиса —
  `contracts.md`.
- Действие, изменяющее риск-аппетит или ключи, требует роли `OWNER` и
  оставляет запись аудита с актором.

## Риск на тенанта

Числа риск-аппетита — доля базы на действие, множитель потолка сделки,
предел серии убыточных, потолок одновременной суммы риска по всем
сделкам — принадлежат тенанту и назначаются его владельцем; пустое место
означает отказ (`docs/concept.md`, `docs/rules/risk-policy.md`). Хранит и
энфорсит их `trading-core`, потому что он единственный, кто принимает
риск. `auth` о числах риска не знает.

## Инструменты

Листинг площадки — платформенное знание: инструменты и их биржевые
правила собирает `market-data` по всему листингу каждой площадки.
Тенант инструментов не заводит — он выбирает их через стратегии. Плечо и
режим маржи — настройка **счёта на инструменте**, не инструмента.

## Связи

- Сервисы — `services.md`.
- Владение данными — `data-ownership.md`.
- Контекст тенанта в вызовах — `contracts.md`.
- Нынешняя модель площадки — `docs/models/domain/core/Exchange.md`.
- Доступ к поверхности — `docs/rules/api-access-policy.md`.
