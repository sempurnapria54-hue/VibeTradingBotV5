# Механика финализации P&L (шаг 7)

## На какой вопрос отвечает этот файл

Как механически добываются P&L-факты, кто считает и пишет число
`resultProfit` (штатно и аварийно), где durable-живёт посчитанное число,
откуда берётся ставка комиссии для сайзинга, как реагирует сверка bills↔net —
и почему именно так.

## Контекст

`GAPS_CLOSE_1` выбрал **источник** числа (net из positions-history + разбивка
из bills, `docs/decisions/result-profit-source.md`), но сознательно отложил
стадия-1/2 механику. `DOCS_CHECK_2` (`phase-1-step-7-docs-check-2.md`) вскрыл 11
гейтящих пробелов механики: добыча фактов не назначена (N6); носитель
staged-числа между двумя финализационными командами (N7); аварийный терминал без
владельца + неисполнимый контракт (N8); seam ставки комиссии в сайзинг (N9);
реакция сверки (N10); носители пути только name-level (N1-N5); непроверенный
инвариант агрегации (N11). Это решение закрывает **механику**; структуры-носители
материализуются отдельными доками (native / mapping / model), ссылаясь сюда.

## Решения

### 1. Добыча P&L-фактов — нога цикла + новая команда, замена `REFRESH_FILLS` (N6, N12)

- **Положение закрытия** (число) добывается **второй ногой evidence-cycle
  `REFRESH_POSITION_COMMAND`**: live `/account/positions` → при not-found
  `/account/positions-history` по `posId`. Отдельная команда
  `REFRESH_POSITIONS_HISTORY` **не вводится** — positions-history описывает
  **ту же сущность** `Position` после закрытия, а refresh-набор держит по
  одной команде на сущность (CMD-Q3). Добытое **приземляется полями на
  `Position`** (`docs/models/domain/core/Position.md` §«Положение
  закрытия»).
- **`REFRESH_BILLS_COMMAND`** вводится как новая команда — под **новую** сущность
  `DealCashFlow` (разбивка), по тому же принципу. **Эмитится звеном
  `REFRESH_DEAL_CONTEXT_ACTION`; инициатором действия выступает handler
  соответствующей тропы** (`ExitPendingHandler` штатно, `ErrorHandler`
  аварийно) **до** финализации/терминала отдельным проходом: её факт
  durable — строки `DealCashFlow` в БД. Прежняя формулировка («эмитится
  handler'ами») противоречила §5a того же решения и оставляла команду без
  анкера попыток — `applyFailureAccounting` был бы no-op, а у добычи
  bills не было бы бюджета и ратифицированной реакции на его исчерпание
  (H23 `GAPS_CLOSE_13`). Внутри команды проходит пагинация bills (7d→3m
  archive) — паритет evidence-cycle
  (`refresh-evidence-cycle-ownership.md`).
- `REFRESH_FILLS` **снимается** (N12): его единственная функция (пересчёт
  order-fill-метрик `accumulatedFillSize`/`averagePrice`/`fee` ordinary `Order`)
  покрыта `REFRESH_ORDER_COMMAND` (`OkxOrderResponse.accFillSz`/`avgPx`). Swap
  1-out-1-in в refresh-вокабуляре (целевой состав — 17,
  `docs/components/models/ServiceCommand.md`).
- **Оба факта durable ⇒ границу прохода FSM пересекают штатно**, и
  потребители читают их со строк (`Position`, `deal_cash_flows`), а не из
  чужой памяти.
- `FinalizeDealExitExecutor` **остаётся off-exchange** — интеграционное
  чтение делает refresh-команда, финализатор читает **уже приземлённый
  факт**. Латентное противоречие снято (executor «на биржу не ходит» ⟷
  факты некому добывать → их добывают refresh-команды).
- **Альтернативы отвергнуты:** (2) integration read вне command-layer
  (CMD-Q4-образец) — размывает «финализация не ходит на биржу», теряет командный
  retry/идемпотентность на гранулярности факта; (3) fetch внутри
  `FinalizeDealExitExecutor` — концентрирует fetch+compute, ломает паттерн
  «refresh populates → finalize consolidates».

> **Ревизия H13 `GAPS_CLOSE_6` (H1/H3, `GAPS_CLOSE_7`).** Промежуточная
> редакция объявляла `PositionCloseResultExternalSnapshot` **транзитным без
> durable-дома** и потому уводила его добычу во **вложенный шаг**
> финализирующего действия (`REFRESH_POSITIONS_HISTORY` внутри
> `FINALIZE_DEAL_EXIT_COMMAND` / `MARK_DEAL_EMERGENCY_CLOSED_COMMAND`), а снапшот держала
> **в памяти действия**. Обе посылки сняты:
>
> - **«негде хранить» ложно** — у факта есть естественный владелец,
>   `Position`; это данные о позиции, а не о финализации;
> - **«команда внутри команды» канон не допускает** —
>   `docs/rules/command-lifecycle.md` ослабляет атомарность только до
>   «несколько **эндпоинтов** внутри команды», а `ServiceCommandExecutionResult`
>   канала возврата снапшота не имеет. Аналогия «как REPLACE композирует свои
>   ноги» была ложным прецедентом: секвенс ног REPLACE ведёт **петля по
>   проходам** (`action-orchestration-vs-command.md`), а `replace-not-amend.md`
>   §Отвергнутые прямо отвергает «executor с двумя ногами».
>
> Цена промежуточной редакции была **исполнимой**: `REFRESH_BILLS_COMMAND` идёт
> отдельным проходом и к памяти чужого действия доступа не имеет, поэтому
> операнд `end` окна линковки, объявленный через «`externalUpdatedAt`
> снапшота», не существовал в момент, когда линковка исполняется (H1). С
> приземлением факта на `Position` операнд стал наблюдаемым.

### 2. Носитель staged-числа = поле `Deal`, пишет `FINALIZE_EXIT` (N7)

- **Разрыв GAPS_CLOSE_1:** число считалось в `FINALIZE_EXIT`, писалось
  `MARK_CLOSED` — durable-слот между двумя командами (разные проходы FSM, разные
  строки `DealFinalizationState`) не назначен; ломалось об идемпотентность/рестарт.
- **Решение:** `FINALIZE_DEAL_EXIT_COMMAND` **пишет `resultProfit`/`resultProfitCurrency`
  прямо на `Deal`** (persisted) в **одной транзакции** с durable-продвижением
  своего звена в исполнении `FINALIZE_DEAL_EXIT_ACTION` (+ персистит
  `DealCashFlow`; носитель исполнений —
  `docs/models/domain/other/DealActionState.md`, транзакционная клауза
  обобщена — `docs/decisions/command-action-boundary.md` §5).
  `MARK_DEAL_CLOSED_COMMAND` **читает `Deal.resultProfit`, ассертит непустоту** (инвариант
  чистого `CLOSED`), ставит `status = CLOSED`.
- **Durable-носитель = сами поля `Deal.resultProfit`** (nullable; заполнены на
  `FINALIZE_EXIT`, ассертятся на терминале). Рестарт-safe: после `COMPLETED` число
  уже на `Deal`; повторный `FINALIZE_EXIT` — no-op, `MARK_CLOSED` читает готовое.
  Транзакционная связка: `COMPLETED` ⟺ `resultProfit` persisted.
- **Ревизует framing GAPS_CLOSE_1** «`MARK_CLOSED` пишет число»: число пишет
  `FINALIZE_EXIT`, `MARK_CLOSED` терминализует + ассертит. Побочно: `resultProfit`
  может быть непустым на `EXIT_PENDING` до терминала — доброкачественно (транзитное
  финализационное состояние; §Персистентность `Deal` — nullable).

### 3. Аварийный терминал: владелец + провенанс-контракт (N8)

- **Владелец (part 1):** step-6 не назначил **запись** `EMERGENCY_CLOSED` —
  команды-терминала нет (`MarkDealErrorExecutor` пишет только `ERROR`;
  `MARK_DEAL_EMERGENCY_CLOSED_COMMAND` в enum отсутствовал). Вводится
  **`MARK_DEAL_EMERGENCY_CLOSED_COMMAND`** (команда + `MarkDealEmergencyClosedExecutor`;
  звено `FINALIZE_DEAL_ERROR_ACTION`), терминальное ребро
  `ERROR → EMERGENCY_CLOSED`, **симметрично `MARK_DEAL_CLOSED_COMMAND`**. Эмитит
  `ErrorHandler` после подтверждённого снятия live risk. Best-effort число —
  из полей положения закрытия на `Position` (их наполнил `REFRESH_POSITION_COMMAND`
  второй ногой; на аварийной тропе `ErrorHandler` его и так гоняет, чтобы
  доказать отсутствие live risk).
- **Провенанс-контракт (part 2) — best-effort, два провенанса разведены:**
  - **(a) реальная ликвидация/ADL** (позицию закрыла биржа): `realizedPnl` +
    `liqPenalty` доступны (`type` 3-6) → число считается **по той же
    формуле, что на чистой тропе**: net **плюс cross-ccy-слагаемое**
    Σ(`amount` × `appliedRate`). Best-effort относится к **доступности**
    числа, а не к его **составу** (H12 `DOCS_CHECK_10`; формулировка
    «пишем фактический realized net» снята H18 `DOCS_CHECK_11` — без
    слагаемого число молча завышалось ровно в той корзине, которая и так
    смещена).
  - **(b) отказ расчёта после исчерпания retry** (чистая тропа не смогла
    посчитать → ушла в `ERROR`): `ErrorHandler` перед терминалом гоняет
    `REFRESH_POSITION_COMMAND`, и её вторая нога ещё раз пробует добыть положение
    закрытия; net на `Position` есть → пишем его; **genuinely недоступен** →
    `resultProfit = null` с семантикой **«неисчислимо»** (НЕ ноль),
    сделка терминализуется **всё равно**, факт помечается (лог +
    `AnomalyReport`, `severity = NON_CRITICAL`).
  - **Маркер (без нового поля):** на `EMERGENCY_CLOSED` `resultProfit != null` =
    фактический net; `resultProfit == null` = «неисчислимо» — **отличимо от нуля**
    (ноль = посчитанный нулевой P&L). Nullability несёт маркер.
  - **Торговое обоснование (F-T1):** число **не зануляется** — недоступность
    помечается, не подменяется нулём; null-случай в расчёт ожидаемости не
    входит **как unknown** (не считается нулём) → левый хвост не усекается
    молча [Vince гл.1 с.15; Tharp гл.6 с.158-159]. **Сама сделка при этом
    из популяции не выводится** (узел F `DOCS_CHECK_12`): она записана, а
    «нет числа» — значение поля `resultProfit`, по которому потребитель
    отчёта фильтрует и которое считает поштучно
    (`docs/models/domain/aggregate/Deal.md` §«Признаки отбора для отчёта»).
  - **Пометки недостаточно — нужен добор числа (H6, `GAPS_CLOSE_3`).** Ревизует
    прежнее «пометки (`AnomalyReport` / лог) достаточно»: пометка фиксирует
    **факт** дыры, но не устраняет её **смещение**. Пропуск null'ов
    **outcome-коррелирован** — null возникает на аварийной тропе, а она
    кучкуется там же, где крупные убытки. Значит выборка «вычислимых» сделок
    смещена к благоприятным исходам, и простой drop не устраняет смещение, а
    **воспроизводит его омиссией**: ожидаемость завышается [Vince гл.5 с.63;
    Tharp формула 6-2, гл.6 с.158-159]. Само решение «не ноль» — **верно**
    (ноль был бы прямой ложью о числе); неверно было утверждение о
    достаточности пометки.
  - **Принятое направление — добор недостающего числа** до истечения окна
    positions-history (~3 мес; тот же эндпоинт, недоступность обычно
    **временна**). То есть null на `EMERGENCY_CLOSED` — не финальный вердикт, а
    **отложенный долг**: число дочитывается, пока окно источника живо.
    **Материализация добора** (кто дочитывает, на каком такте, что с
    просроченным окном) — **форвард шага ожидаемости / фазы 2**; здесь
    фиксируется **направление**, не механика. Отвергнуты как основной путь:
    worst-case импутация (искажает число ради метрики) и метрика счёта дыр
    рядом с ожидаемостью (честно раскрывает дыру, но не закрывает её — годится
    как дополнение, не как замена добора).
- Контракт становится **исполнимым**: «на `EMERGENCY_CLOSED` — фактический realized
  net если доступен; иначе `null` с маркером «неисчислимо»; **никогда не ноль**».

### 4. Ставка комиссии для сайзинга — seam на `InstrumentExternalRules`, дом на `TradeFeeRate` (N9)

**Доспецифицировано на `GAPS_CLOSE_3` (H1).** Первая редакция назвала ставку
«инструмент-level внешние данные» и положила её полями на навес инструмента.
Фактура офдока (прогон `integrator`, 2026-07-14) показала, что это неверно: ставка
— **group × tier-level**, и слой её добычи не был заведён вовсе (нет native, нет
маппинга, `groupId` отброшен) → `takerFeeRate()` оставался null, прогноз комиссии
молча выпадал. Ниже — итог.

- **Поверхность чтения не изменилась:** `SizeCalculator`/`RiskValidator` читают
  taker-ставку тем же аксессором `InstrumentExternalRules.takerFeeRate()` —
  **нового поля контекста не нужно**, **exchange-вызова из калькуляторов не
  нужно**. Дефект был в **наполнении**, не в поверхности.
  - **Уточнено на `GAPS_CLOSE_4` (H1).** Прежняя редакция формулировала это
    как «оба читают из **уже присутствующего**
    `CalculationContext.instrumentExternalRules`» — для `RiskValidator`
    **ложно**: его сигнатура `validate(CalculatedStrategyAction, DealContext)`,
    `CalculationContext` у него нет, навес он читает **напрямую** через
    `InstrumentExternalRulesDataService.findByInstrumentId`
    (`docs/components/RiskValidator.md` §Входы). Верно так: **аксессор** у обоих
    один и не двигался, а **гидрированных троп чтения навеса две** — через
    фабрику контекста и прямая. Довод `DOCS_CHECK_3` «обе линзы признали seam
    валидным» покрывал только первую тропу; на `DOCS_CHECK_4` это стоило гейта.
    Шаг 7 третьего читателя навеса **не добавляет** (H5 `DOCS_CHECK_16`,
    решение пользователя). Прежняя редакция заводила им
    `FinalizeDealExitExecutor` — за `ctVal`, без гидрации ставки (H7
    `DOCS_CHECK_11`); ход снят: `ctVal` момента постановки персистится на
    ноге (`Order.plannedContractValue`, write-once), и финализатор навес не
    читает вовсе. Читателей остаётся **два** (реш.5 §epsilon;
    `docs/components/InstrumentExternalRulesDataService.md` §Использование).
- **Дом значения — `TradeFeeRate`** (`docs/models/domain/other/TradeFeeRate.md`):
  одна строка на комиссионную группу, отдельная таблица с историей. На навесе
  инструмента — **только ключ группы** `externalFeeGroupId`.
- **Гидрация — на хранилищном слое** (H1, `GAPS_CLOSE_4`). Аксессоры
  `takerFeeRate()`/`makerFeeRate()` наливает
  `docs/components/InstrumentExternalRulesDataService.md` — граница
  domain ↔ persistence, **единственная точка, через которую проходят обе
  тропы**. Гидрируй фабрика контекста — прямая тропа `RiskValidator` отдавала
  бы негидрированный навес, `takerFeeRate()` = `null`, и
  `FEE_RATE_UNAVAILABLE` блокировал бы **каждый** risk-creating вход. Читатели
  о `TradeFeeRate` не знают.
  **Почему не полем навеса:** ставка — атрибут комиссионного уровня аккаунта
  (объём/VIP), а не справочника инструмента; копия на каждом инструменте —
  заготовка под расхождение при смене тира (N копий, N шансов отстать). Это
  ревизует **место хранения**, а не seam: (c) «дом ставки не на инструменте»
  был прав по факту, но правильный его объём — хранение, не поверхность чтения.
- **Ось резолва — пара (`instType`, `groupId`)**, не голый `groupId`: одно число
  значит разное при разном `instType`. Ключ группы приходит
  `/public/instruments` (офдок: «instType and groupId should be used together to
  determine a trading fee group»); отброс `groupId` снят
  (`docs/models/mapping/InstrumentExternalRules.md`). Перечень групп **не
  хардкодится** — офдок сам предписывает «actual return values shall prevail».
  - **Обе половины ключа — сырые биржевые значения** (H7, `GAPS_CLOSE_4`), не
    доменная проекция `instrumentType`. Довод — **коллизия `UNKNOWN`**:
    проекция схлопывает все нераспознанные сырые типы в одно значение, поэтому
    две разные группы источника столкнулись бы в одном ключе и правило истории
    «значение изменилось → новая строка» выродилось бы во флип-флоп между их
    ставками. Группа — ось **источника**, ключуется тем, чем её задаёт
    источник. Ратифицировано пользователем; заявка на доменную ось в
    `TradeFeeRate`/`InstrumentExternalRules` снята.
  - **Матч в синке — тоже по паре** (H8, `GAPS_CLOSE_4`): per-instrument матч
    идёт по (`externalInstrumentType`, `externalFeeGroupId`) навеса, а не по
    голому `groupId` (`docs/components/InstrumentExternalRulesSyncJob.md`
    §Такт). Голый `groupId` на контуре из нескольких `instType` дал бы **молча
    неверную ставку** — ровно тот отказ, от которого предостерегает абзац выше.
- **Такт (A′) — чтение раз-на-тик:** **один** вызов `trade-fee(instType=SWAP)`
  **до** цикла по инструментам → карта групп → per-instrument матч. Ось запроса —
  группа, а не инструмент, поэтому лимит (5 req / 2 s по User ID) неощутим.
  Синк — тем же `InstrumentExternalRulesSyncJob`: **один писатель**.
  - *(B) отдельная `TradeFeeSyncJob`* — отвергнута: её единственный настоящий
    довод (разные природы контуров — публичный/IP vs приватный/User ID) не
    окупает внесённую гонку двух писателей в один навес (`JobExecutionGuard` —
    per-job, джобы друг друга не видят). Вернуться, если приватный контур
    потребует своего такта.
  - *(C) читать на входе в сделку* — отвергнута: exchange-вызов на горячем
    пути — точка отказа **в момент входа**, а покупает (C) устранение
    **дрейфа ставки между тиками** — величины, близкой к нулю (ставка и тир
    меняются единицы раз в год); плюс ломает seam. Прежняя опора («цена
    ~5 bp против стопа 1-2%») снята (H12, `GAPS_CLOSE_4`; носитель-решение
    выровнен `GAPS_CLOSE_5`, H18): «стоп 1-2%» — не свойство системы, и
    сравнивался уровень ставки, а не дрейф; вывод обе поправки переживает.
    Развёрнутый довод — `docs/components/InstrumentExternalRulesSyncJob.md`
    §«Почему такт такой».
- **Свежесть — измеряется, не предполагается.** Возраст `modifiedAt` строки =
  время с последнего успешного чтения; возраст > порога (конфиг, стартово
  **24 ч**) → **холд инструментов группы**
  (`docs/rules/instrument-hold.md` §«Несвежесть ставки комиссии»).
  - **Измеритель держится на `refreshCount`** (H5, `GAPS_CLOSE_4`).
    Прежняя редакция утверждала, что `modifiedAt` двигается при каждом успешном
    чтении, — из механизма это **не следовало**: JPA auditing двигает метку на
    **изменении** сущности, а путь «значение совпало» (практически
    единственный: ставка меняется единицы раз в год) ничего не менял ⇒ UPDATE не
    выпускался, метка замирала на времени создания, и через 24 ч **здоровой**
    работы срабатывал холд. Введён **счётчик подтверждений `refreshCount`**:
    инкремент на каждом успешном чтении делает строку dirty ⇒ метка двигается.
    Свойство стало **записанным**, а не побочным.
  - **«Счётчик отказов не нужен» — довод перепроверен и держится** (H5,
    `GAPS_CLOSE_4`): возраст `modifiedAt` и есть время с последнего успеха, а
    при фиксированном такте оно однозначно переводится в число неудач подряд
    (часовой такт, порог 24 ч → 24 неудачи). Разница с прежней редакцией — в
    опоре: раньше довод стоял на незаписанном побочном эффекте (и потому был
    ложен), теперь — на `refreshCount`. Полный разбор —
    `docs/models/domain/other/TradeFeeRate.md` §«Свежесть измеряется, а не
    предполагается».
  - **Scope холда — инструмент, не биржа** (H3/H4, `GAPS_CLOSE_4`). Ратифицировано
    пользователем. Чтение `trade-fee` ломается на **исправной** бирже и
    обесценивает справочное число **одной группы** ⇒ радиус ущерба —
    инструменты этой группы (`docs/rules/error-handling-policy.md` §«Радиус
    ущерба задаёт scope»). Формулировка «холд биржи, сделки доживают»
    **снята**: она была вдобавок внутренне противоречива —
    exchange-блок-сет рубит `SUBMIT_ALGO_ORDER_COMMAND`, то есть `MAIN_PROTECTION`,
    ремодел трейлинга и reduce-only partial exit, так что «доживающие»
    сделки лишались управления.
  - **Реакция — мягкая, kill-switch снят** (H2, `GAPS_CLOSE_5`;
    реконсилировано здесь — H1, `GAPS_CLOSE_6`). Состав: **запрет новых
    входов** по инструментам протухшей группы + `AnomalyReport`; **живые
    сделки доживают под своим стопом** и сопровождаются штатно. Довод —
    радиус ущерба ограничивает и реакцию: ставка участвует только в
    сайзинге **нового** входа, `R` живой сделки задан стопом на бирже, а
    `resultProfit` считается из фактов ⇒ kill-switch действовал бы за
    радиусом. Прежняя редакция этого пункта («реакция — полная, уровня 4:
    … + kill-switch … Более слабого класса холда не заводим») — **носитель
    отвергнутой политики**, снята.
  - **Снятие — вручную** (H2, `GAPS_CLOSE_6`, решение пользователя; scope —
    фаза 1). Восстановившееся чтение холд не снимает: измеритель
    самоизлечим, холд — sticky-запись, снимает оператор. Довод —
    human-in-the-loop: триггер есть сбой пайплайна добычи фактов, который
    человек и так идёт разбирать. Авто-снятие по предикату свежести —
    **отложено, не отвергнуто**; горизонт — установившийся режим
    (`docs/rules/instrument-hold.md` §Снятие).
  - **Enforcement — отдельный статус `Instrument.Status.ENTRY_BLOCKED`**
    (H3, `GAPS_CLOSE_6`), не `TRADE_BLOCKED`: последний в оркестраторе
    уводит активные сделки в `ERROR`, то есть переиспользование воскресило
    бы снятое kill-switch-поведение (`docs/rules/instrument-hold.md`
    §Enforcement).
  - **Свежесть меряется у обеих половин резолва** — и у значения ставки, и
    у **ключа группы** на навесе (H9, `GAPS_CLOSE_6`): протухший
    `externalFeeGroupId` не должен молча резолвиться в чужую свежую строку
    (`docs/models/domain/other/InstrumentExternalRules.md` §«Свежесть ключа
    группы»).
  - Такт пересчёта тира офдок **не специфицирует** (а страница комиссий —
    JS-рендер, не читается), поэтому `level`/`ts` пишутся в строку как
    **датчик**: пусть на вопрос ответят собственные данные, а не догадка о
    поведении биржи. `level` при этом — **часть значения группы** (его смена
    рождает строку), иначе датчик не зафиксировал бы смену тира без смены
    ставки (H11, `GAPS_CLOSE_4`).
- **Null-политика:** ставка не резолвится → **реджект `FEE_RATE_UNAVAILABLE`**
  (`RiskValidator`); последняя известная ставка не затирается — на отказе синк
  **не пишет ничего** (история append-only; атрибуция «IGNORE-null» снята как
  рудимент редакции «ставка на навесе» — H10, `GAPS_CLOSE_4`,
  `docs/models/mapping/TradeFeeRate.md` §«Error policy»).
  Fallback из конфига **отвергнут**: подставленное число выглядит фактом, не
  будучи им; заниженная ставка → позиция больше положенной — та же болезнь, что
  H6/H2 (тихое оптимистичное смещение). Цена реджекта — пропуск одной сделки с
  громкой причиной; цена fallback'а — тихо превышенный риск на каждой.
  - **Оговорка о цене (H15, `DOCS_CHECK_4`).** Асимметрия «пропуск одной сделки
    vs риск на каждой» защитима для **реджекта**, но ею же прежде обосновывался
    **холд** — а холд не «одна сделка». С переездом холда на scope=инструмент
    цена стала конечной и адресной (инструменты одной группы, не вся биржа), но
    **нулевой она не является**: пропуск входов — цена по оси **возможностей**
    [Tharp гл.6 с.158-159; гл.11 с.279]. Форвард — §«Форвард-фокус: ось
    упущенных возможностей» в конце файла.
- **Инвариант organic-base-rates.** Запрос **без** `instId`/`instFamily`
  возвращает organic base rates — ставок market-maker incentive не видно, ответ
  приходит **валидный, но не тот**. Мы не участники программы ⇒ base rates для
  нас корректны. **Вход в MM-программу требует пересмотра оси запроса
  `trade-fee`.** Записано именно как условие, а не примечание: молчаливое
  допущение — ровно то, на чём горел H1.
- Прогноз по **taker** (worst-case: вход-маркет = taker, выход-по-стопу = taker →
  консервативно).
  - **Оговорка (H13, `DOCS_CHECK_4`, cross-cutting → шаг ожидаемости / фаза 2):**
    taker-консерватизм подан здесь как **бесплатный**, а корпус этого не
    поддерживает — «издержки: равный вред от занижения и завышения» [Kaufman
    гл.1, PDF с.114-119]. При **maker**-входе (лимитные входы в модели есть —
    `docs/components/RiskValidator.md`, `PriceMode.EXPLICIT`; `makerFeeRate()`
    хранится и не используется) прогноз по taker систематически
    **недосайзит**. В фазе 1 не чиним — это ось ожидаемости, а не механики
    финализации; помечено, чтобы не закрепилось как «выбора не было».
- **Знак ставки — снимается при маппинге** (H2, `GAPS_CLOSE_4`). Биржевая
  конвенция «минус = комиссия» (`docs/integrations/okx/contracts/trade-fee.md`)
  **не выходит за маппинг**: `× −1` в per-source-секции
  (`docs/models/mapping/TradeFeeRate.md` §«Знак ставки — снимается здесь») →
  ниже маппинга ставка есть **издержка** (комиссия положительна, ребейт
  отрицателен). Отсюда `+ commissions` в формуле риска верно **в обе стороны**,
  и `abs` на пути не появляется — **одним местом**, без дубля по формулам.
  - **Что это закрывало.** До `GAPS_CLOSE_4` аксессор отдавал сырую знаковую
    ставку, а `RiskValidator`/`SizeCalculator`/`per-trade-risk-policy` складывали
    её через `+ commissions` без `abs`: убыток на стопе **уменьшался**, бюджет
    риска **увеличивался** — оба члена в одну сторону ⇒ **позиция больше
    положенной**. Дословно тот отказ, ради которого отвергнут fallback (выше):
    тихое оптимистичное смещение, не видное ни в одном логе. Валидация его не
    ловила (отрицательное парсится), `FEE_RATE_UNAVAILABLE` — тоже (только на
    `null`).

### 5. Реакция сверки bills ↔ net (N10)

- **Число всегда авторитетно = positions-history net** (bills-sum его **не
  подменяет**). Расхождение — **сигнал целостности, не ошибка числа**.
  - **Уточнение (H5, `GAPS_CLOSE_6`): net авторитетен в своей валюте.**
    Биржевой `realizedPnl` посчитан **в settle-ccy** и издержку, уплаченную
    вне неё, не содержит. Поэтому заголовочное число = **net +
    cross-ccy-слагаемое** Σ(`amount` × `appliedRate`) по строкам
    cross-ccy-области — `rateStatus = APPLIED` **и** тип вне списка
    исключений **и** категория экономическая (§«Cross-ccy» ниже;
    конъюнкция — H11 `GAPS_CLOSE_13`, выровнено H2 `DOCS_CHECK_14`). Bills
    по-прежнему не подменяют net — они доносят слагаемое, которого в net
    физически нет.
- **Не блокирует финализацию:** сделка идёт в `CLOSED` с net-числом. Расхождение
  **сверх epsilon** → **`AnomalyReport`** (audit-аномалия для разбора; `scope =
  INSTRUMENT`, `severity = NON_CRITICAL` — тропа без kill-switch,
  `docs/lifecycles/AnomalyReport.md`) — **не** холд, **не** runtime-error
  уровней 1-2, **не** блок терминала.
- **Epsilon — якорь = оборот, не итог (H7, `GAPS_CLOSE_3`).** Допуск на
  округление между эндпоинтами: относительный член **0.5% от Σ`|amount|`**.
  Якорь относительного члена — **валовой оборот сделки**
  (Σ модулей движений), **не `|net|`.** Довод: сверка охраняет **ошибку
  композиции разбивки**, а такая ошибка масштабируется **валовыми потоками**
  (сколько движений сложили), а не их **итогом**. Привязка к `|net|` ломается с
  обоих концов: при большом net `0.5%·|net|` глотает реальную дыру, а при
  net ≈ 0 с большими валовыми потоками (типично: прибыльные и убыточные
  ноги гасят друг друга) относительный член вырождается в ноль и сверка
  срывается в ложные аномалии на голом округлении.
- **Тест — двухчастный, срабатывает меньший (H15 `DOCS_CHECK_10`).**
  Прежняя форма `max(0.01, 0.5%·Σ|amount|)` покрывала **только** ошибку
  композиции, а оба названных в доках **живых** режима отказа —
  **омиссионные и фиксированного размера**: выпадение entry-fee за нижнюю
  границу окна (`docs/models/domain/other/DealCashFlow.md` §«Нижняя
  граница под наблюдением») и неподобранные движения после терминала.
  Их величина оборотом **не масштабируется**, а допуск масштабируется ⇒ на
  сделке с крупным реализованным движением `0.5%` от оборота превышает
  целую комиссионную ногу, и систематическая омиссия становится
  ненаблюдаемой **именно на сделках с большим |R|** — там, где она дороже
  всего.
  - **Форма (H8 `DOCS_CHECK_11`, решение пользователя):**
    `epsilon = max( 0.01 settle-ccy, min( 0.5% × Σ|amount|, k × ожидаемая
    комиссия сделки ) )` — относительный член охраняет композицию,
    омиссионный член охраняет омиссию, а **пол общий**.
    - **Область применения — сделка, а не пара** (H9 `GAPS_CLOSE_13`,
      решение пользователя). Сверка раздельна по категориям (четыре пары),
      но **допуск один**: он сравнивается с **общим расхождением по
      сделке** `Σ|Δᵢ|`. Пер-парных якорей не вводится — они дали бы три-
      четыре калибруемые величины вместо одной, а funding-паре
      потребовался бы операнд (ставка фондирования), которого в модели
      шага нет: вариант перенёс бы гейт на другой шаг. Цена названа:
      омиссия внутри одной категории наблюдаема лишь до тех пор, пока
      суммарное расхождение превышает единый порог; калибровка `k`
      остаётся **предусловием `CODE`** (§«Предусловия `CODE` шага 7»,
      п. 8).
    - **Пол вынесен наружу.** Прежняя форма
      `min( max(0.01, 0.5%·Σ|amount|), k·комиссия )` держала пол внутри
      одного из кандидатов, и `min` его снимал: при
      `k × ожидаемая комиссия < 0.01` итоговый допуск уходил ниже
      величины, признанной неотличимой от округления, а у омиссионного
      члена своего пола нет и `k` провизорен ⇒ точка пробоя ничем не
      ограничена. Режим отказа торговый: систематический
      `PNL_RECONCILIATION_MISMATCH` на мелких сделках, то есть контроль,
      срабатывающий на каждой сделке, — а он «эквивалентен погашенному».
    - Пол — **не член сравнения, а шум-флор**: он принадлежит итоговому
      допуску, а не одному из кандидатов. Довод «допуск обязан быть уже
      самого дешёвого режима отказа» верен для членов, но к полу не
      применяется. Правка не вводит ни нового операнда, ни новой
      константы; вариант «дать омиссионному члену собственный пол»
      отвергнут — он заводит вторую копию той же константы, то есть два
      места калибровки.
  - **Операнд омиссионного члена — строка самой сделки, ставка не
    перечитывается** (H7 `DOCS_CHECK_11`, решение пользователя).
    Прогнозная комиссия обеих ног **уже входит** в `plannedRiskAmount`
    (закрытая форма сайзинга
    `budget / (ctVal × (|entry − stop| + rate × (entry + stop)))`),
    поэтому она извлекается вычитанием ценовой части:

    `ожидаемая комиссия сделки = Σ_i (plannedRiskAmount_i −
    |plannedEntryPrice_i − stop_i| × plannedSizeContracts_i × ctVal)`
    по ногам входа `i`

    - **Считается поногово** (H6 `DOCS_CHECK_15`, решение пользователя).
      Тождество верно ровно там, где риск и его операнды принадлежат
      **одной** ноге; при многоногом входе смешение уровней («риск с
      `Deal`, ценовая часть с ноги») даёт односторонний перекос в обе
      стороны — отрицательное вычитаемое при агрегате (допуск падает до
      шум-флора на самых крупных сделках) либо комиссию ~1/N при первой
      ноге. Сумма разностей и есть ожидаемая комиссия сделки.
    - **Где живут операнды.** `plannedRiskAmount`, `plannedEntryPrice`,
      `plannedSizeContracts` — все три поля **ноги входа** (`orders`;
      риск переехал туда H6/H11 `DOCS_CHECK_15`, операнды — H3
      `DOCS_CHECK_11`); на `Deal` живёт их **сумма**. Уровень стопа —
      `AlgoOrder.condition.trigger.stopLoss.value` либо элемент
      `Order.attachedAlgoOrders`, выбранный **записанным предикатом
      селекции** (статус + причина закрытия; H12 `DOCS_CHECK_15`) —
      защиты **этой же входной ноги**; `ctVal` —
      `InstrumentExternalRules`.
    - **Темпоральная привязка стопа — вход, и она держится структурой**
      (H10 `GAPS_CLOSE_13`). Тождество вычитания верно только с тем
      уровнем, под который считался `plannedRiskAmount`, поэтому операнд —
      защита входной ноги, а **не** «актуальная защита как последнее
      звено цепочки замещений» (`docs/decisions/replace-not-amend.md`).
      Ветка «стоп сдвинут трейлингом» здесь **не достижима**: трейлинг и
      перенос стопа в безубыток живут на **основной standalone-защите**,
      появляющейся после `PROTECTION_SWITCHED` и сопровождаемой в
      `MANAGING` (`docs/lifecycles/Deal.md` §Статусы), — это другая
      сущность, защитой входной ноги не являющаяся. Посылка проверена
      при закрытии; окажись ветка достижимой (например, если ремодел
      начнёт замещать защиту самой входной ноги), находка возвращается, и
      операнд придётся персистить отдельным числом.
    - **Почему так, а не пере-чтением `TradeFeeRate`.** Ставки может не
      быть **никогда** (`FEE_RATE_UNAVAILABLE`), она может быть
      **несвежей** (`FEE_RATE_STALE` + холд группы), а строка ставки —
      append-only история, и группа инструмента может смениться между
      входом и финализацией: какая строка «та самая», неопределимо. Это
      ровно тот отказ «guard считает по тому, чего нет», которым отвергнут
      вариант 2 выше, — и к принятому варианту он раньше не применялся.
      Persisted-операнды write-once ⇒ величина **воспроизводима
      постфактум** (тот же критерий, по которому введён `appliedRate`).
    - **Довод самоприменим, и он применён: `ctVal` тоже персистится**
      (H5 `DOCS_CHECK_16`, решение пользователя). Прежняя редакция
      объявляла ставку неперечитываемой «потому что persisted write-once
      воспроизводимо» — и через две строки предписывала финализатору
      **дочитывать `ctVal` из навеса**, который синк переписывает каждым
      тиком. Асимметрия снята вверх: `ctVal` момента постановки ноги
      садится пятым числом на саму ногу
      (`Order.plannedContractValue`, write-once,
      `docs/models/domain/core/Order.md` §«`plannedContractValue` — пятое
      число»). **Финализатор перестаёт быть читателем навеса вовсе** —
      прежняя регистрация «третий читатель» в
      `docs/components/InstrumentExternalRulesDataService.md`
      §Использование снята; читателей снова два. Ставку он не читал и
      раньше, вызовов биржи не добавляется.
      - **Что это чинит:** тихое искажение (смена `ctVal` между входом и
        финализацией ⇒ неверный допуск на уже закрытой сделке) и громкое
        (нерезолвимый `ctVal` уводил **штатно** завершившуюся сделку с
        корректным `resultProfit` в `EMERGENCY_CLOSED` — терминальная ось
        получала событие о несинхронизированном справочнике).
      - **Чего не сделано:** готовое число ожидаемой комиссии
        (`plannedCommission_i`) **не** персистится — вариант рассмотрен и
        отвергнут (решение пользователя): он выводим из пяти уже
        персистенных величин и уровня стопа.
    - **Ветка «планового риска нет».** Ног входа с непустым
      `plannedRiskAmount` нет вовсе (популяция H23 — позиции, созданные
      вне приложения) ⇒ сумма пуста (**не ноль**), омиссионный член **не
      участвует**, `epsilon` вырождается в `max(0.01, 0.5% × Σ|amount|)`,
      и это помечается `AnomalyReport` `PLANNED_RISK_MISSING` — слепота к
      омиссии на такой сделке **названа**, а не молчалива (образец ветки —
      `docs/models/domain/aggregate/Deal.md` §«Ветка "операнд пуст"»).
    - **Ветка «операнд допуска не резолвится» — описана, но производителя
      не имеет** (Р3 `GAPS_CLOSE_16`). Если операнд не резолвится при
      наступившей обязанности ⇒ признак остаётся `NOT_RUN` (третьего
      значения нет — H6 `DOCS_CHECK_16` отклонён решением держателя) +
      сделка уходит **ошибочной тропой** к `EMERGENCY_CLOSED` +
      `AnomalyReport` `RECONCILIATION_OPERAND_MISSING`, **без** биржевого
      холда (H2 `DOCS_CHECK_15`). Оба прежних производителя закрыты
      персистом: «`ctVal` пуст» — H5 `DOCS_CHECK_16`, «предикат селекции
      дал не ровно один элемент» — Р3 (уровень стопа персистится шестым
      числом). Оставить ветку защитной или снять вместе с кодом —
      **открыто**. Прежняя редакция («`NOT_RUN` + успешный терминал»,
      H25 `DOCS_CHECK_12`) снята: она давала на одной тропе исход,
      противоположный терминальному контракту, и возвращала `NOT_RUN`
      двузначность.
  - **Почему меньший, а не больший.** Допуск обязан быть уже самого
    дешёвого режима отказа, который он охраняет; максимум из двух даёт
    слепоту к тому из них, который меньше.
  - **Почему не «якорить относительный член на издержечные потоки»**
    (вариант 2): при полном выпадении издержечной строки якорь
    схлопывается — тот же отказ «guard считает по тому, чего нет»,
    который проект уже дважды чинил (мёртвый `ccy`-фильтр, пустой операнд
    сравнения).
  - **Величины остаются провизорными** (`0.01`, `0.5%`, `k`) — меняется
    **структура** теста, не калибровка. Побочно это снимает форвард
    «форма epsilon слепнет с ростом оборота» (`.claude/work/backlog.md`):
    омиссионный член от числа движений не растёт.
- **Величины — по-прежнему провизорны** (`0.01` / `0.5%` / `k` — паттерн
  «провизорное значение»): калибровка — пользователь / бэктест.
  `GAPS_CLOSE_3` менял **якорь** (то, к чему привязан относительный член),
  `GAPS_CLOSE_10` — **структуру теста** (два члена вместо одного); ни то ни
  другое калибровку не заменяет.
- **Инвариант валюты комиссии (H8, `GAPS_CLOSE_3`) — принят, не допущение.**
  **Комиссии платятся только в валюте расчёта (settle-ccy, USDT).** Оплата
  комиссии сторонней валютой (на OKX — скидка за оплату в OKB) **запрещена
  конфигурацией**; движение с `ccy ≠` расчётной валюте инструмента —
  **нарушение инварианта**, то есть аномалия, а не ожидаемый режим работы.
  - **Довод — переносимость, не деталь OKX.** Инвариант формулируется
    **нейтрально к источнику**: заголовочное число берётся net'ом в settle-ccy,
    поэтому издержка, уплаченная **вне** settle-ccy, в это число не входит — и
    `resultProfit` **завышает** реальный net на её величину. Это свойство
    **схемы «net в одной валюте»**, а не особенность OKX: любая площадка с
    оплатой комиссии сторонним токеном ломает число одинаково. Поэтому режим
    отключается на всех источниках, а не патчится per-source.
  - **Дом инварианта — `docs/rules/trading-constraints.md`** (реестр
    инвариантов контура, куда указывает
    `docs/rules/trading-configuration-ownership.md`): инвариант
    source-agnostic, стратегия его не переопределяет. Здесь — только довод.
  - **Детект — только постфактум по `ccy`; проактивного рычага нет.**
    Проверено против офдока (прогон `integrator`, 2026-07-14): в OKX API v5
    **нет поля/настройки «платить комиссию в OKB»** — скидка конфигурируется
    вне API; `feeType` (`account-config`) — **Spot-only** и на SWAP не
    применим, то есть **не рычаг OKB**. Спросить у источника «в какой валюте
    будут комиссии» **нечем** ⇒ инвариант наблюдаем **только** по факту, уже
    заложенным cross-ccy guard'ом на сверке. Это **граница источника, не
    пробел проектирования** — зафиксировано явно, чтобы следующий заход не
    искал несуществующую настройку. Рантайм-наблюдатель — AG3.4/RQ-4
    (`.claude/tests/source-api/okx/plan.md`).
  - **Механика (H5, `GAPS_CLOSE_6` — guard был мёртв, оживлён).** Прежняя
    редакция объявляла «не отбрасывается молча», но запись отсекалась
    **дважды раньше guard'а**: `ccy=USDT` стоял в query bills и `ccy` входил
    в предикат линковки. Нарушающая запись не приходила ⇒ строки не
    возникало ⇒ guard не срабатывал никогда, а расхождение сверки не
    возникало тоже (чужая издержка не входит ни в биржевой net, ни в Σ
    USDT-строк) — нарушение было невидимо **полностью**. Ратифицировано:
    - `ccy` **убран** из query и из критериев матчинга (матчинг — окно +
      `instId`);
    - ветка на записи: `ccy ≠` расчётной валюте инструмента → **персист +
      линковка + пересчёт + `AnomalyReport`**; запись не теряется;
    - поле `DealCashFlow.ccy` остаётся **обязательным** — без валюты
      различить движения нечем.
  - **Операнд сравнения — расчётная валюта инструмента** (H4,
    `GAPS_CLOSE_7`; ревизует редакцию `GAPS_CLOSE_6`). Сравнение с
    `Deal.resultProfitCurrency` **неисполнимо**: это поле пишет
    `FINALIZE_DEAL_EXIT_COMMAND`, то есть **после** прохода `REFRESH_BILLS_COMMAND`, и на
    момент записи оно `null` — guard сравнивал бы с пустым и снова был бы
    мёртв, ровно тем же отказом, который `GAPS_CLOSE_6` чинил. Settle-ccy —
    свойство **инструмента**: известно до входа и не зависит от исхода
    сделки. **Носитель расчётной валюты — определён** (H12
    `DOCS_CHECK_9`; дом пересмотрен H6 `DOCS_CHECK_11`): валюты
    персистятся полями самого **`Instrument`**
    (`docs/decisions/instrument-currencies-home.md`) и читаются с
    `DealContext.instrument`; `CCY-Q2` закрыт, имена полей окончательные.
  - **Итоговое число — в settle-ccy (USDT); чужая валюта пересчитывается
    сразу, по курсу из свечи на момент операции** (H4, `GAPS_CLOSE_7`;
    источник котировки пересмотрен H25 `DOCS_CHECK_11` — «момент обработки»
    снят, см. §«Механизм курса» ниже).
    `Deal.resultProfit` = net из positions-history +
    Σ(`amount` × `appliedRate`) по строкам cross-ccy-области —
    **`rateStatus = APPLIED` и тип вне списка исключений и категория
    экономическая (не `OTHER`)**, конъюнкция целиком (H11
    `GAPS_CLOSE_13`, выровнено H2 `DOCS_CHECK_14`; «по строкам чужой
    `ccy`» снято — оно затягивает в произведение строки с пустым
    `appliedRate`).
    Довод за само
    слагаемое: пометка аномалией фиксирует **факт** нарушения, но **смещения
    числа не устраняет** — без слагаемого число завышает результат ровно на
    величину издержки, уплаченной вне settle-ccy, и завышает молча. Это тот
    же урок, что применён к null-drop (реш.3): «пометка не заменяет
    починку». Валютные суммы-сверки при этом идут **только по строкам
    расчётной валюты** — складывать разные валюты нельзя
    (`docs/models/mapping/DealCashFlow.md` §«Область суммирования»).
  - **Механизм курса — свеча на момент операции** (CCY-Q1 закрыт
    `GAPS_CLOSE_7`; **источник котировки пересмотрен H25
    `DOCS_CHECK_11`**, решение пользователя). Курс берётся из **свечи на
    момент операции** — секундного разрешения при доступности, с
    деградацией к более грубому при его отсутствии; добывается на записи,
    проходом `REFRESH_BILLS_COMMAND`, и **применённый курс сохраняется**
    полем `DealCashFlow.appliedRate` — число, посчитанное по внешней
    котировке, обязано быть воспроизводимым (тот же принцип, по которому
    отвергнут fallback-ставки).
    - **Прежняя редакция брала тикер на момент обработки и прямо
      отвергала исторический бар** — довод был: ветка есть нарушение
      инварианта, а не рабочий режим, поэтому темпоральная точность
      самостоятельной ценности не несёт, а бар стоит ещё одного
      read-пути. **Довод снят** решением пользователя: он верен для
      *точности*, но не покрывает **устойчивость** котировки.
      `appliedRate` фиксируется **однократно** и при пересчёте не
      дрейфует, то есть однажды взятое число **навсегда** становится
      частью `resultProfit`; единичный тик одной площадки в тонком стакане
      — наименее робастный операнд, и разовая аномалия даёт
      **невосстановимую** ошибку. Свеча агрегирует торговлю интервала и к
      одиночному выбросу устойчива.
    - **Воспроизводимость от смены источника выигрывает, а не теряет.**
      Тикер на момент обработки воспроизвести нельзя вовсе — он зависит
      от того, когда прошёл `REFRESH_BILLS_COMMAND`; свеча на момент
      операции детерминирована самим движением, поэтому догон курса
      (§ниже, H24) даёт **то же** число, что дала бы первая попытка.
    - **Деградация разрешения фиксируется признаком**, а не выводится:
      «взят по секундной» и «взят по минутной за неимением секундной» —
      разные факты (`docs/models/domain/other/DealCashFlow.md` §Енум
      `RateStatus`).
    - **Отвергнут перенос ветки на финализацию:** пересчёт на записи
      снимает зависимость guard'а от факта закрытия — ровно того дефекта,
      из-за которого операнд был мёртв (H4).
    - **Цена принятого решения названа:** `REFRESH_BILLS_COMMAND` получает
      exchange-вызов свечей котировки на редкой ветке; при многих
      движениях в окне встаёт вопрос группировки запросов против квоты —
      хэнд-офф `integrator` (`docs/components/RefreshBillsExecutor.md`
      §«Носитель курса»). Контракт «финализация off-exchange» при этом
      **не нарушен** — вызов живёт в refresh-команде, а не в финализаторе.
  - **Неполученный курс — долг, а не только пометка** (H24
    `DOCS_CHECK_11`, решение пользователя). Реш.3 устанавливает принцип
    «пометка фиксирует **факт** дыры, но не устраняет её **смещение**;
    пометка не заменяет починку» — и к отказу котировки он применяется
    **так же**, как к null-drop. Прежняя редакция принимала для
    `CROSS_CCY_RATE_UNAVAILABLE` обратное («строка персистится, слагаемое
    не вносится, факт помечается, число остаётся завышенным»), и
    разделяющего довода не приводила — принцип выглядел применяемым
    выборочно. Симметрия восстановлена: строки в состоянии
    `RATE_UNAVAILABLE` образуют долг, следующее исполнение звена по этой
    сделке повторяет попытку, и догон возможен ровно потому, что
    состояние выражено **явным признаком**, а не пустотой поля (H9).
    Горизонт долга — терминал сделки; оставшийся после него долг и есть
    названная цена (`docs/components/RefreshBillsExecutor.md` §«Политика
    отказа котировки и догон»).
  - **Посылка «биржевой net не содержит издержку вне settle-ccy» —
    требует рантайм-подтверждения** (H13 `DOCS_CHECK_10`).
    Cross-ccy-слагаемое **прибавляется** к net на этой посылке. Она
    записана как факт («свойство схемы "net в одной валюте"»), но это
    утверждение о поведении **источника**: если `realizedPnl` эту
    издержку всё-таки содержит, слагаемое считается дважды и число
    **занижается**. Сестринская посылка того же класса (кумулятивность
    `realizedPnl` по partial-закрытиям, реш.6) помечена предположением и
    гейтит `CODE` — эта не была помечена вовсе. Помечена: провенанс —
    **предположение**, наблюдатель — вопросник **§AG3.4** контура
    source-api (`.claude/tests/source-api/okx/plan.md`), расширенный
    инспекцией `realizedPnl` на сделке с cross-ccy-издержкой; фикстура
    та же, лишнего прогона нет. Грунт довода: «ничего не предполагать —
    проверять всё», «равный вред от занижения и завышения издержек»
    [Kaufman гл.1, PDF с.110-119].
  - **Валюта результата — один авторитет** (H10 `DOCS_CHECK_10`, решение
    пользователя). `Deal.resultProfitCurrency` берётся из **расчётной
    валюты инструмента**, а не из `Position.externalResultCurrency`:
    иначе net приходил бы «в валюте записи источника», а
    cross-ccy-слагаемое — «в расчётной валюте инструмента», и при
    расхождении складывались бы **разные валюты молча**. Валюта от биржи
    остаётся **проверяемым признаком**: не совпала → `AnomalyReport`
    `RESULT_CURRENCY_MISMATCH`, расчёт **не блокируется**. Ветка «операнд
    пуст» задана по образцу `FEE_RATE_UNAVAILABLE` (реджект до взятия
    риска, пометка после) — полный разбор
    `docs/models/domain/aggregate/Deal.md` §«Валюта результата: один
    авторитет».
  - Guard остаётся при принятом инварианте: он ловит **нарушение**
    (конфигурацию сбили / площадка сменила поведение), а не штатный режим.

### 5a. Асимметрия троп отказа добычи (H15, `GAPS_CLOSE_7`)

Отказ добычи P&L-фактов обрабатывается **по-разному на штатной и аварийной
тропе**, и это различие надо кодировать явно — прежде оно нигде не было
записано, хотя вся конструкция на нём стоит.

| Тропа | Отказ добычи | Реакция |
|---|---|---|
| Штатная (`EXIT_PENDING` → `CLOSED`) | `REFRESH_POSITION_COMMAND` / `REFRESH_BILLS_COMMAND` не смогли прочитать | **ретрай по бюджету исполнения `REFRESH_DEAL_CONTEXT_ACTION`**; исчерпание уводит сделку ошибочной тропой в `ERROR` + **полный холд инструмента** (единая форма исчерпания на всех тропах — H6 `DOCS_CHECK_14` + H8 `DOCS_CHECK_16`, решение держателя; `docs/rules/instrument-hold.md` §«Форма реакции на исчерпание бюджета попыток» — с доводом отладки и условием пересмотра) |
| Аварийный терминал (`ERROR` → `EMERGENCY_CLOSED`) | **отказ канала добычи** (транспорт, парс, исчерпанный цикл) | **ретрай по тому же бюджету**; исчерпание → `FAILED` строки, это и есть durable-исход «недоступно» → `resultProfit = null` «неисчислимо» → **терминал ставится**. Радиусная реакция **не** поднимается (довод радиуса: обесценена отчётность, не управление риском) |
| Аварийный терминал (`ERROR` → `EMERGENCY_CLOSED`) | **дефект содержимого ответа** (`ControlledExchangeException`) | **как на штатной тропе**: `FAILED` сразу (non-retryable) + бросок ⇒ **полный биржевой холд**; сделка параллельно доходит до ошибочного терминала с `resultProfit = null` (H4 `DOCS_CHECK_15`) |

**Что изменилось в `_15` и почему** (H3, H4 `DOCS_CHECK_15`, решения
пользователя). Прежняя редакция держала одну строку — «жёсткий отказ
чтения (controlled/parse) приравнивается к "недоступно"», **нулевые
попытки**, — и обе правки бьют по ней:

- **Послабление для контролируемых исключений снято.** Правило
  контролируемых исключений действует на **обеих** тропах одинаково;
  приравнивание дефекта содержимого к «недоступно» было молчаливым
  проглатыванием нарушения контракта там, где оно наблюдается впервые.
  Разграничитель «дефект содержимого ответа против отказа канала добычи»
  (`docs/rules/error-handling-policy.md` §«Нарушение контракта интеграции
  — радиус по неизвестности поражения») остаётся в силе — снято именно
  то, что аварийная тропа приравнивала **обе** природы.
- **Нулевые попытки отменены.** Без расхода бюджета у «приравненного
  недоступно» не было **durable-носителя**: проход за проходом выводил ту
  же стадию из той же пустоты, эмитил ту же команду — сделка зависала в
  `ERROR`, то есть ровно тот исход, ради которого асимметрия и вводилась.
  Общее правило исчерпания распространено и сюда; прецедент — тот же
  выбор для легитимно-пустого исхода (H8 `DOCS_CHECK_14`).
  Дискриминатор `Deal.status = ERROR` сохраняется, но управляет **броском**
  (то есть радиусной реакцией), а не ростом счётчика
  (`docs/components/ServiceCommandExecutor.md` §«Контракт броска»).

- **Почему асимметрия, а не единообразие.** На штатной тропе отказ означает
  «факт ещё не получен» — сделка живая, ждать безопасно, и повтор дешевле
  неверного числа. На аварийном терминале ждать **нельзя**: сделка уже в
  `ERROR`, live risk снят, и единственное, что осталось, — довести её до
  терминала. Если жёсткий отказ там трактовать как провал действия,
  исполнение уходит в `FAILED`, `SystemActionExecutor` перестаёт эмитить
  команду, и сделка **зависает в `ERROR` вопреки инварианту** «сделка
  всегда доходит до терминала» (`docs/lifecycles/Deal.md`).
- **Якорь попыток добычи — строка исполнения `REFRESH_DEAL_CONTEXT_ACTION`**
  (вид SYSTEM, `docs/models/domain/other/DealActionState.md`; закрытие
  узла 3 `DOCS_CHECK_8`, `docs/decisions/command-action-boundary.md`).
  Прежнее предписание «добывающие `REFRESH_*` — под анкером
  `DealActionState` действия стратегии» было структурно неисполнимо
  (`strategy_action_id NOT NULL` + `UNIQUE(deal_id, strategy_action_id)`
  при эмиссии handler'ом вне strategy-action). Отдельная
  finalization-строка типа `FETCH_*` по-прежнему **не вводится** — сущность
  финализации упразднена вовсе, анкер даёт общая таблица исполнений.
  - **CODE-следствие:** handler'ы перестают эмитить добывающие `REFRESH_*`
    напрямую через `DealFsmSupport.systemCommand(...)` — только звеньями
    действия (иначе `applyFailureAccounting` остаётся no-op). Оговорка
    `docs/components/models/ServiceCommand.md` («без анкера») остаётся
    верной **только для cleanup**. Пункт — в `.claude/work/backlog.md`
    §Шаг 7.
- **`FINALIZE_DEAL_EXIT_COMMAND` не завершается без числа** (узел 4 `DOCS_CHECK_8`,
  вариант (а)): звено эмитится по терминальному исходу добычи и без числа
  не завершается; прежняя клауза исполнителя «пустота фактов — легитимная
  тропа "неисчислимо" на штатной тропе» снята — «неисчислимо» остаётся
  только аварийному контуру.
- **Полнота разбивки bills не гейтит выход.** Разбивка — **best-effort**:
  недобранные движения дают `AnomalyReport` на сверке, а не удержание
  сделки в `EXIT_PENDING` (`docs/components/ExitPendingHandler.md`
  §«Выходные проверки»); согласуется с «сверка — аудит-аномалия» (реш.5).
  Гейтит число (и окно линковки — `Deal.billsWindowEnd`) факт **закрытия**,
  добываемый `REFRESH_POSITION_COMMAND`.
- **Различение «пусто» vs «жёсткий отказ» — только на аварийной тропе.** На
  штатной различать незачем: оба исхода ведут в ретрай, а исчерпание — в
  `ERROR`, откуда работает аварийное правило.

#### Кто применяет приравнивание и что с холдом (H4 `DOCS_CHECK_10`)

Асимметрия была записана, но **исполнителя не имела**, и сталкивалась с
общим контуром учёта отказов: `attemptCount++` → `RETRY_PENDING` →
`FAILED`, а `FAILED` строки `REFRESH_DEAL_CONTEXT_ACTION` означал
«ошибочная тропа + **холд инструмента**» **без оговорки про тропу**. Один и
тот же отказ давал по одним клаузам «терминал с `null`», по другим —
kill-switch-холд. Достроено:

| Что | Ответ |
|---|---|
| Кто применяет приравнивание | **`ServiceCommandExecutor`** — единственная ветка учёта отказов; предикат — `Deal.status = ERROR` из `DealContext` (durable-факт, не знание вызывающего). Прежде приравнивание было сформулировано на `MarkDealEmergencyClosedExecutor`, который самого отказа **не наблюдает** |
| Сколько попыток до приравнивания | **штатный бюджет исполнения** (H3 `DOCS_CHECK_15`; прежнее «ноль» отменено). Исход «недоступно» выносится **исчерпанием**, и его durable-носитель — `FAILED` строки `REFRESH_DEAL_CONTEXT_ACTION`. Ждать по существу нечего, но носитель факта нужен: без него исход существовал только в памяти прохода |
| Что с холдом | на `Deal.status = ERROR` исчерпание бюджета добычи **радиусную реакцию не поднимает** (`RetryBudgetExhaustedException` не бросается), а `FAILED` **разрешает эмиссию терминала**. Довод радиуса: позиция закрыта, добыча обесценивает **отчётность**, не управление риском (`docs/rules/error-handling-policy.md` §«Радиус ущерба задаёт scope»). **Исключение — контролируемое исключение**: оно бросается и здесь, реакция — полный биржевой холд (H4 `DOCS_CHECK_15`) |
| Приоритет двух живых SYSTEM-исполнений | **добыча раньше терминала**: `FINALIZE_DEAL_ERROR_ACTION` (терминал) не эмитится, пока `REFRESH_DEAL_CONTEXT_ACTION` не вынесла исход. Порядок держит `ErrorHandler`, выдающий одно действие за проход |

**Почему не «разнести тропы отдельными типами действия»** (крен линзы,
вариант 1). Он же был креном H3 и там отвергнут: троп-контекстов больше
двух, а дискриминатор уже существует durable-полем `Deal.status` — новая
ось enum'а его дублировала бы
(`docs/components/SystemActionExecutor.md` §«Состав конкретного цикла»).
**Вариант «признать холд на аварийной тропе штатным»** отвергнут доводом
радиуса — тем же, которым снят kill-switch с несвежести.

### 6. Инвариант агрегации positions-history — рантайм-верификация (N11)

- **Инвариант:** одна сделка ↔ один `posId` ↔ **одна финализированная** запись
  positions-history, чей `realizedPnl` **кумулятивен по ВСЕМ** partial-закрытиям и
  доборам за жизнь позиции; читается **финализированной** (позиция полностью
  закрыта — `REFRESH_POSITION_COMMAND` показал flat/отсутствие).
- **Требует рантайм-верификации** (контур source-api, demo,
  `.claude/tests/source-api/okx/plan.md`): агрегирует ли OKX partial-выходы
  (partial TP `type` 1 → SL `type` 2) в **одну** запись на `posId`, и в какой
  момент запись финализирована (риск чтения слайса/нефинализированной записи →
  систематический недосчёт realized). До верификации инвариант — **предположение**,
  помеченное в контракт-доке и test-плане. **Гейтит корректность числа** →
  верификация до `CODE`.

## Носители (материализуются отдельно, стадия 2)

- **native `OkxPositionsHistoryResponse`** — `docs/models/integrations/okx/OkxPositionsHistoryResponse.md`.
- **boundary-снапшот `PositionCloseResultExternalSnapshot`** (не persisted;
  приземляется полями на **`Position`**, отдельной persisted доменной сущности
  нет) + маппинги native→snapshot→`Position`→`Deal` —
  `docs/models/mapping/PositionCloseResult.md`.
- **`DealCashFlow`** (доменная модель `other` + персистенция) —
  `docs/models/domain/other/DealCashFlow.md`; маппинг bills→`DealCashFlow` —
  `docs/models/mapping/DealCashFlow.md`.

## Следствия

- **Новые команды/executor'ы (целевая дельта `CODE` шага 7, в коде их ещё
  нет):** `REFRESH_BILLS_COMMAND`/`RefreshBillsExecutor`,
  `MARK_DEAL_EMERGENCY_CLOSED_COMMAND`/`MarkDealEmergencyClosedExecutor`; **снимается**
  `REFRESH_FILLS`/`RefreshFillsExecutor` (в коде пока присутствуют — H15,
  `GAPS_CLOSE_6`: формулировать целевым временем, не свершившимся); плюс
  `SystemActionExecutor` и переименование enum'ов
  (`docs/decisions/command-action-boundary.md`).
- **Не-схемная дельта `CODE` шага 7** — изменения без колонки-якоря,
  которые ни одна schema-дельта не ловит по построению (H13/H14
  `DOCS_CHECK_12`). Перечень:
  - **новый компонент `HoldService`** — общий исполнитель блокировки,
    которого зовут детекторы (перечень зовущих открыт, H12
    `GAPS_CLOSE_13`; синк ставок ставит холд сам)
    (`docs/components/HoldService.md`, H8 `DOCS_CHECK_12`);
  - **канал подъёма реакции — строится, и строится первым** (H1
    `DOCS_CHECK_15`; прежде дельта его не заказывала вовсе, хотя три дока
    назначали перехват исключения единственной точкой подъёма):
    1. **новый тип `RetryBudgetExhaustedException`** (идентичность
       блокируемого объекта, тип команды, `dealActionStateId`;
       первопричина — в `cause`);
    2. **бросок в `ServiceCommandExecutor`** после перевода строки
       исполнения в `FAILED` — вместо нынешнего
       `catch (RuntimeException) → return failure(...)`; плюс
       **`classify()` перестаёт схлопывать `ControlledExchangeException`
       в `VALIDATION_ERROR`** — иначе тип теряется до точки резолва
       (`docs/components/ServiceCommandExecutor.md` §«Контракт броска»);
    3. **выделенный `catch` в `DealOrchestratorJob`** вокруг шага
       диспетчеризации команд, поимённо по двум типам, **до** общего
       `catch (RuntimeException)`
       (`docs/components/DealOrchestratorJob.md` §«Перехват реакции»);
  - **снятие прежнего транспорта — только после пункта выше**:
    **снимается транспортное поле `DealTransition.holdSignal`** и путь
    «handler приложил сигнал → оркестратор прочитал»
    (в коде: `DealFsmSupport`, `DealTransition`,
    `DealOrchestratorJob.reactToHoldSignal`). Порядок обязателен и
    назван явно: прежний канал в коде **жив и работает**, и снятие его
    раньше постройки нового оставило бы шаг без пути подъёма холда
    вовсе (H1 `DOCS_CHECK_15`);
  - **ключ комиссионной группы `groupId`** заводится в трёх местах —
    поле `InstrumentOkxResponse`, поле
    `InstrumentExternalRulesExternalSnapshot`, строка маппера в
    `InstrumentExternalRules` (навес — JSONB, миграции не требует;
    `/public/instruments` уже читается). Без него
    `externalFeeGroupId` остаётся `null`, ставка не резолвится и
    `FEE_RATE_UNAVAILABLE` блокирует **каждый** risk-creating вход
    (H13 `DOCS_CHECK_12`);
  - **переименование полей валют в `InstrumentExternalSnapshot`** под
    доменные имена: `externalSettleCurrency` →
    `externalSettlementCurrency`, `externalBaseCurrency` /
    `externalQuoteCurrency` — сверить с доменными; снапшот транзиентный
    ⇒ ни миграции, ни бэкфилла, зато маппинг остаётся по имени и факт
    зовётся одинаково во всех слоях (H14 `DOCS_CHECK_12`);
  - **возврат `pnl` и `fee` positions-history** в
    `PositionCloseResultExternalSnapshot` — операнды раздельной сверки
    по категориям (H19 `DOCS_CHECK_12`,
    `docs/models/mapping/PositionCloseResult.md`);
  - **нормализация знака `fundingFee` при маппинге в доменный снапшот**
    — единственное место приведения (H20 `DOCS_CHECK_12`);
  - **носитель конфига «список исключений сверки по бирже»** —
    **`@ConfigurationProperties` per-exchange** (H16 `DOCS_CHECK_16`,
    решение пользователя; предусловие `CODE` п. 12). Колонки-якоря у него
    нет, поэтому строка живёт здесь, а не в schema-дельте. Состав: класс
    свойств с ключом по бирже, **непустой стартовый набор в конфиге по
    умолчанию** (иначе п. 2 не выполняется при первом запуске) и чтение из
    предиката области сверки
    (`docs/models/mapping/DealCashFlow.md` §«Область сверки задаётся
    списком исключений по бирже»);
  - **возврат `liqPenalty` positions-history** в
    `PositionCloseResultExternalSnapshot` и на `Position` — правый
    операнд **четвёртой** пары сверки (H7 `GAPS_CLOSE_13`,
    `docs/models/mapping/PositionCloseResult.md`); колонка-якорь у него
    есть (`positions.external_liquidation_penalty`), поэтому в
    schema-дельте он тоже назван;
  - **четыре поля планового риска на `CreateOrderCommandPayload`** —
    и **только** на нём (`plannedRiskAmount`, `plannedRiskCurrency`,
    `plannedEntryPrice`, `plannedSizeContracts`; `RISK-Q4` закрыт:
    входной алго-тропы не существует, `CreateAlgoOrderCommandPayload`
    полей не несёт) — payload runtime-объект, колонки у
    него нет (H19 `GAPS_CLOSE_13`; `docs/components/CreateOrderExecutor.md`
    §«Канал доставки», `docs/components/CreateAlgoOrderExecutor.md`);
  - **новое значение `Instrument.Status.ENTRY_BLOCKED`** плюс его
    enforcement в выборках скана и оркестратора — колонка
    `instruments.status` существует с `V1`, якоря нет
    (`docs/rules/instrument-hold.md` §Enforcement,
    `docs/components/EntryScannerJob.md`, `docs/lifecycles/Instrument.md`);
  - **вторая («журнальная») поверхность создания `AnomalyReport`** — точка
    входа без `DealContext` и без жёсткой `CRITICAL`: колонка `kind` в
    дельте есть, а **метода** — нет
    (`docs/models/domain/other/AnomalyReport.md` §«Производящая
    поверхность и коды шага 7»);
  - **ветка `ServiceCommandExecutor`, приравнивающая жёсткий отказ к
    «пусто» при `Deal.status = ERROR`** (нулевые попытки, `HoldService` не
    зовётся) — §«Кто применяет приравнивание»,
    `docs/components/MarkDealEmergencyClosedExecutor.md`;
  - **снятие read-пути fills целиком** — команда, executor, снапшот,
    маппер, native-DTO и методы границы (H26 `GAPS_CLOSE_13`,
    `docs/decisions/result-profit-source.md` §«Каскад снятия»);
  - **идентичность объекта блокировки внутрь `HoldSignal`** и уход
    `DealContext` из подписи `HoldService.hold(...)` (H13
    `GAPS_CLOSE_13`); фабрики сигнала получают параметр идентичности;
  - **идентичность протянута вниз по цепочке** (H5 `DOCS_CHECK_14`):
    подпись `SafetyHoldCoordinator.react(HoldSignal)` (вместо
    `react(signal, dealContext)`), реактивная поверхность
    `AnomalyReport` берёт `instrumentId`/`exchangeId` из сигнала;
  - **резолв класса реакции по типу исключения** (H6 `DOCS_CHECK_14`):
    отображение в `docs/components/HoldService.md` §«Момент вызова»;
    код холда серии — один, `RETRY_BUDGET_EXHAUSTED`; пары кодов по
    стороне ноги не существует;
  - **инкремент бюджета на исчерпанном цикле** refresh-команды (H8
    `DOCS_CHECK_14`; `docs/rules/command-lifecycle.md` следствие 3,
    `docs/components/ServiceCommandExecutor.md` §Retry);
  - **гейт эмиссии bills-звена — тропа** (H7 `DOCS_CHECK_16`, решение
    держателя `GAPS_CLOSE_16`: звено эмитится только на выходной тропе, в
    аварийный цикл не входит; ревизует составной предикат H9
    `DOCS_CHECK_14` «bills входит ⟺ `billsWindowEnd` непуст») и **сверка
    на ветви (a) аварийного терминала** (H21 `DOCS_CHECK_14`) — последняя
    сужена: гоняется, если движения уже добыты;
  - **валидация контракта записи закрытия — в слой интеграции** (H5
    `DOCS_CHECK_15`; ревизует H7/H10 `DOCS_CHECK_14`): обязательные поля
    (`type` внутри `1..6`, четыре правых операнда пар, `realizedPnl`,
    `ccy`) проверяются при разборе ответа и при нарушении дают
    `ExternalInvariantViolationException`; **вызова `HoldService` у
    финализаторов не остаётся** (`docs/models/mapping/PositionCloseResult.md`
    §«Контракт записи проверяется здесь», `docs/rules/exchange-hold.md`
    §«Что переводит в HOLD»);
  - **нормализация `direction` записи positions-history в доменный
    `Position.Direction`** — в слое интеграции, симметрично живой ноге
    (H7 `DOCS_CHECK_15`): поле снапшота меняет тип и имя
    (`externalDirection: String` → `direction: Position.Direction`),
    незнакомое значение — то же controlled-исключение;
  - **предикат селекции attached-защиты ноги входа** (H12
    `DOCS_CHECK_15`): операнд уровня стопа выбирается из
    `Order.attachedAlgoOrders` по статусу и причине закрытия — «первый
    элемент» операндом не является
    (`docs/components/FinalizeDealExitExecutor.md` §epsilon);
  - **`TimeFrame` расширяется значением `ONE_SECOND`** (H10
    `DOCS_CHECK_15`) + ветка `TimeFrameMapper`; легальность значения в
    `CandleGroup` и в дереве стратегии ограничена валидацией
    (`docs/models/domain/other/CandleGroup.md` §«Енум `TimeFrame`»);
  - **плановый риск переезжает на ногу входа** (H6/H11 `DOCS_CHECK_15`;
    состав расширен H5 `DOCS_CHECK_16` и Р3 `GAPS_CLOSE_16`):
    `CreateOrderCommandPayload` везёт **шесть** чисел (пятое —
    `plannedContractValue`, шестое — `plannedStopPrice`), пишет их исполнитель
    **на `Order`**, а на `Deal` той же транзакцией пересчитывает **две
    суммы** по ногам входа — заявленную и взятую (H2/H3 `DOCS_CHECK_16`,
    `docs/models/domain/aggregate/Deal.md` §«Плановый риск», §«Взятый
    риск»).
- **Полная schema-дельта шага 7** (H21, `DOCS_CHECK_8`; всё — `ALTER` /
  новые объекты, в `V1-V10` их **нет**):
  - **Длины строковых колонок шага — единая норма `varchar(64)`** (H18
    `DOCS_CHECK_15`, решение пользователя): категоризация по типу значения
    (валюта 16 / сырой код 32 / сырой идентификатор 64 / enum 32)
    **схлопнута**, всякая строковая колонка, вводимая или изменяемая этим
    шагом, — `varchar(64)`
    (`docs/rules/persistence-representation.md` §«Строковые колонки:
    длины»). Ниже типы приведены к норме; отклонений, которые H18
    `DOCS_CHECK_15` перечислял поимённо, не остаётся — они стали нормой.
  - `deals`: `ALTER` + **`incurred_risk_amount`** (`numeric(36,18)` —
    **взятый на входе** риск, Σ по ногам входа с весом исполненной доли; H3
    `DOCS_CHECK_16`; частичным выходом **не уменьшается**) +
    **`current_risk_amount`** (`numeric(36,18)` — **текущий** риск,
    `incurred × Position.externalSize / Σ accFillSz`; уменьшается частичным
    выходом, при полном — ноль; потребитель — сопровождение, не отчёт).
    Обе — решение держателя `GAPS_CLOSE_16` («три поля: входной плановый,
    входной фактический, текущий фактический»); не write-once,
    пересчитываются целиком (§«Взятый риск» модели: три писателя по ногам +
    `RefreshPositionExecutor` по размеру позиции для третьего поля).
    Плюс `planned_risk_amount` (`numeric(36,18)` — **сумма**
    плановых рисков ног входа, H6/H11 `DOCS_CHECK_15`; **не** write-once),
    `planned_risk_currency` (`varchar(64)`), `bills_window_begin`,
    `bills_window_end` (обе `timestamptz`; все nullable; типы — H13
    `DOCS_CHECK_14`, длины — H18 `DOCS_CHECK_15`) **плюс признаки
    отбора для отчёта** (узел F `DOCS_CHECK_12`): `close_outcome`
    (`varchar(64)` — торговый исход закрытия, четыре значения включая
    `UNDETERMINED`, H2 `GAPS_CLOSE_13`), `reconciliation_status`
    (`varchar(64)` — исход сверки разбивки, **три** значения:
    `NOT_RUN`/`MATCHED`/`MISMATCHED`; четвёртое (`OPERAND_MISSING`, H6
    `DOCS_CHECK_16`) рассмотрено и **не заводится** — решение держателя
    `GAPS_CLOSE_16`), **`risk_benchmark_availability`**
    (`varchar(64)` — енум `RiskBenchmarkAvailability`
    `AVAILABLE`/`NOT_APPLICABLE`/`MISSING`, H13 `DOCS_CHECK_16`, решение
    пользователя: две природы пустоты знаменателя `R` разведены значением;
    **колонка новая**), `breakdown_incomplete`
    (**`varchar(64)`** — енум `BreakdownCompleteness`
    `COMPLETE`/`INCOMPLETE_BY_WINDOW`/`NOT_ASSESSED`; тип изменён с
    `boolean` — H4 `GAPS_CLOSE_13`: третье состояние «не оценивалось»
    заведено значением, а не трёхзначной логикой); плюс индекс
    (`status`, `close_outcome`) под выборку отчёта **и `DROP INDEX
    ix_deal_status`** — прежний индекс становится избыточным префиксом
    нового (H28 `GAPS_CLOSE_13`). Довод: выборки как
    хранимого объекта нет, фильтры отчёта — значения полей сделки, и
    признак, вычисляемый на лету, фильтром эндпоинта быть не может
    (`docs/models/domain/aggregate/Deal.md` §«Признаки отбора для
    отчёта»). Операнды сравнения планового риска (`planned_entry_price`,
    `planned_size_contracts`) **сюда не входят** — их дом сущность ноги
    входа (H3 `DOCS_CHECK_11`, см. ниже);
  - **нога входа — только `orders`** (`RISK-Q4` закрыт 2026-08-20; состав
    расширен H6/H11 `DOCS_CHECK_15`, **H5 `DOCS_CHECK_16`** и **Р3
    `GAPS_CLOSE_16`**): `ALTER` + `planned_entry_price`,
    `planned_size_contracts`, **`planned_risk_amount`**,
    **`planned_contract_value`**, **`planned_stop_price`** (все пять этих —
    `numeric(36,18)`) и **`planned_risk_currency`** (`varchar(64)`) — все
    nullable, write-once, заполняются **все шесть или ни одной**.
    `planned_stop_price` — уровень стопа, под который считался риск ноги;
    без него омиссионный член сверки теряет операнд после снятия
    встроенной защиты доборной ноги (Р3).
    Reference-цена и заявленный размер — операнды
    разрыва «заявлено ↔ взято»; `planned_contract_value` — `ctVal` момента
    постановки, снимающий последнюю зависимость тождества допуска от
    изменчивого JSONB-навеса (§epsilon выше); **плановый риск ноги** —
    слагаемое знаменателя `R`, сумма которых живёт на `Deal`
    (`docs/models/domain/aggregate/Deal.md` §«Плановый риск»; место
    истины — `docs/models/domain/core/Order.md` §Персистентность,
    заведён H16 `DOCS_CHECK_14`). `algo_orders` колонок не получает —
    входной алго-тропы не существует, четвёрка колонок была бы мёртвой
    схемой с живым именем; условие возврата —
    `docs/models/domain/core/AlgoOrder.md` §Назначение. Пометка
    «гейтит `RISK-Q4`» снята;
  - `positions`: `ALTER` + `external_realized_profit`,
    `external_result_currency`, `external_close_average_price` (H26
    `DOCS_CHECK_10` — операнд калибровки проскока на тропе attached-SL,
    H21 `DOCS_CHECK_11`), `external_close_type`,
    `external_funding_cost` (H20 `DOCS_CHECK_11` — операнд
    де-микширования R-мультипликатора; знак нормализован при маппинге —
    H20 `DOCS_CHECK_12`), `external_realized_profit_gross` и
    `external_fee` (H19 `DOCS_CHECK_12` — правые операнды раздельной
    сверки разбивки по категориям), **`external_liquidation_penalty`**
    (H7 `GAPS_CLOSE_13` — правый операнд четвёртой пары, категория
    `LIQ_PENALTY`). Итого **восемь** колонок положения
    закрытия — шесть `numeric(36,18)`, `external_result_currency` и
    `external_close_type` — обе `varchar(64)` (типы — H13
    `DOCS_CHECK_14`, длины приведены к единой норме H18
    `DOCS_CHECK_15`); место истины перечня —
    `docs/models/domain/core/Position.md`
    §Персистентность;
  - `instruments`: `ALTER` + `external_settlement_currency`,
    `external_base_currency`, `external_quote_currency` (все три
    `varchar(64)`, nullable — H13 `DOCS_CHECK_14`, длина — H18
    `DOCS_CHECK_15`; H6
    `DOCS_CHECK_11` — дом валют инструмента переехал с навеса на
    сущность, `docs/decisions/instrument-currencies-home.md`). **Писатель
    — тропа заведения инструмента** (H10 `DOCS_CHECK_12`); бэкфилл не
    нужен, потому что заведённых инструментов на момент ввода нет
    (`.claude/rules/pre-launch-schema-changes.md`), а не потому, что
    «значение появится после ближайшего тика синка» — эта формулировка
    снята;
  - **исполнения — две таблицы** (H15 `DOCS_CHECK_14`, решение
    пользователя; носитель топологии V2 «общая таблица» пересмотрен
    явно): `deal_action_states` **переименовывается** в
    `deal_strategy_action_states` (`strategy_action_id` остаётся
    `NOT NULL`; `+target_entity_type` `varchar(64)` nullable,
    `+target_entity_id` `bigint` nullable — типы названы H19
    `DOCS_CHECK_15`; **+ шесть audit-колонок**, H15 `DOCS_CHECK_15`;
    снятие `uk_deal_action_state_deal_action`, два частичных уникальных
    индекса живых исполнений — nullable-операнд `target_entity_id`
    отсекается предикатом индекса, связка названа явно в модели;
    **`DROP COLUMN target`** — **бэкфилла нет**, H25
    `GAPS_CLOSE_13`); создаётся **`deal_system_action_states`**
    (`deal_id`, `system_action_type` `varchar(64)` `NOT NULL`, `status`
    `varchar(64)` `NOT NULL`, retry-поля, **шесть audit-колонок** того же
    состава; частичный уникальный индекс живых по (`deal_id`,
    `system_action_type`); target-колонок нет — цель всегда сделка);
    колонок `action_kind` нет ни в одной. **Обе таблицы аудируемы** —
    доменная модель признана наследником `Auditable` (H15
    `DOCS_CHECK_15`), частичных наборов и правки самого правила состава
    не вводится. Место истины —
    `docs/models/domain/other/DealActionState.md` §Инварианты /
    §Персистентность. Строки
    `deal_finalization_states` **не переносятся** — `DELETE` + `DROP
    TABLE` (правило и довод — `docs/models/domain/other/DealActionState.md`
    §«Правило переноса», H19 `DOCS_CHECK_10`). **Поисковые индексы обеих
    таблиц — по одному на таблицу** (H10 `DOCS_CHECK_16`):
    `ix_deal_strategy_action_state_deal` и `ix_deal_system_action_state_deal`
    по (`deal_id`), **полные, не частичные** — они восстанавливают покрытие,
    утраченное вместе со снимаемым `uk_deal_action_state_deal_action`, чей
    префикс его нёс; заменяющие ключи частичны по живым статусам, а читаются
    и терминальные строки (сборка `DealContext`, резолв цепочки REPLACE), и
    строки копятся до фазы 3. Индекса под due-for-retry **не вводится** —
    проход сделко-ориентирован, отдельной выборки по таблицам исполнений
    нет; довод и отвергнутая денормализация `nextRetryAt` на `Deal` —
    `docs/models/domain/other/DealActionState.md` §«Поисковые индексы»;
  - `anomaly_reports`: `ALTER` — **`+kind`** (`varchar(64)`,
    `STATE`/`EVENT`,
    дискриминатор природы факта, H16 `DOCS_CHECK_10`; **`not null` вводится
    напрямую, без `default` и без промежуточного nullable-шага** — таблица
    пуста, `.claude/rules/pre-launch-schema-changes.md`, H17
    `DOCS_CHECK_12`; `varchar(64)` — H18 `DOCS_CHECK_15`) и **`scope` →
    `varchar(64)`** (H22 `DOCS_CHECK_10` — расширение; длина по единой
    норме, H18 `DOCS_CHECK_15`). **Колонка `fee_group_key` не
    заводится** (H14 `DOCS_CHECK_15`, решение пользователя): групповой
    радиус отчёта упразднён, отчёт о несвежести ставок заводится **на
    инструмент** (по одному на каждый затронутый), идентичность несёт
    существующая `instrument_id` — через инструмент достаются и группа, и
    биржа. Вместе с радиусом **не вводится и значение
    `HoldScope.INSTRUMENT_GROUP`** — производителей у него не остаётся
    (`docs/components/models/HoldSignal.md` §«Енум `HoldScope`»).
    **Частичных уникальных индексов-анкеров нет** (H17 `GAPS_CLOSE_13`,
    решение пользователя): анкер-ограничение уникальности **снято**
    целиком — и групповой, и негрупповой. Взамен ставится **поисковый
    (не уникальный) индекс незавершённых** по (`exchange_id`, `code`,
    `scope`, `instrument_id`) `where kind = 'STATE' and
    status in ('CREATED','IN_PROGRESS','KILL_SWITCH_EXECUTED')` — H14
    `DOCS_CHECK_14`: прежде дельта несла здесь только отрицание, а
    индекс, требуемый моделью, в неё не попадал; операнд
    `fee_group_key` из состава убран вместе с колонкой (H14
    `DOCS_CHECK_15`). Идемпотентность
    `STATE`-отчёта
    держится **незавершённым статусом**: производитель перед созданием
    ищет незавершённый отчёт своего кода и радиуса (составной ключ
    поиска — `ANOM-Q5`) и **продолжает** его, в
    том числе после рестарта (`docs/models/domain/other/AnomalyReport.md`
    §«Отчёт как процесс с фазами»). Прежняя конструкция ключа была
    вдобавок мёртвой на `scope = EXCHANGE` (`instrument_id = null`,
    `NULL`'ы в ключе различимы), а посылка «производителей такого рода в
    шаге 7 нет» — ложна.
    **Место истины схемы — §Персистентность модели**
    (`docs/models/domain/other/AnomalyReport.md`), здесь —
    сборка-указатель (`docs/rules/persistence-representation.md`
    §«Место истины схемы»). Строка добавлена по H20
    `DOCS_CHECK_10`: перечень заявлен полным, а док модели требовал
    индекс миграцией того же шага;
  - новые таблицы: `deal_cash_flows` (вкл. `applied_rate`, `rate_status`
    `not null`, `exchange_id` `not null`, `external_instrument_id`,
    **три колонки ссылки на свечу курса**
    `applied_rate_candle_instrument` / `_timeframe` / `_open_time` —
    координатная запись, не FK (H11 `DOCS_CHECK_14`; **енум `TimeFrame`
    расширен значением `ONE_SECOND`** — H10 `DOCS_CHECK_15`, миграции не
    требует: значение хранится строкой),
    **`UNIQUE(exchange_id, external_bill_id)`** — ось биржи в ключе, H13
    `DOCS_CHECK_11`; **индекс по `deal_id`** — выборка разбивки сделки и
    сверка (H14 `DOCS_CHECK_14` — прежде дельта перечисляла индексы этой
    таблицы выборочно); индекс отбора несвязанных строк окна по
    (**`exchange_id`**, `external_instrument_id`, `external_created_at`)
    `where deal_id is null` — ось биржи внесена и в индекс, и в предикат
    отложенной линковки, H16 `DOCS_CHECK_15`; H11 `DOCS_CHECK_11`),
    `trade_fee_rates` (вкл. **индекс
    резолва актуальной строки группы** по (`exchange_id`,
    `external_instrument_type`, `external_fee_group_id`,
    `created_at DESC`) — H14 `DOCS_CHECK_14`; audit-имена обеих новых
    таблиц — `modified_at`/`modified_by` по фактическому
    `AuditableEntity`, H12 `DOCS_CHECK_14`); **строковые колонки обеих
    таблиц — `varchar(64)`** (единая норма, H18 `DOCS_CHECK_15`); **PK обеих
    — `bigint generated by default as identity`** (H11 `DOCS_CHECK_16`:
    прежняя редакция обоих доков объявляла `bigserial` против формы, которую
    несут все `V1`-`V10`. Правилом норма **не фиксируется** — решение
    держателя `GAPS_CLOSE_16`: правятся два дока шага, § общего правила в
    `persistence-representation.md` не заводится);
  - `strategy_actions`: миграция **значений** `action_type`
    (`CREATE→CREATE_ACTION` и т. д., суффиксное правило);
  - **Бэкфилл `instruments.external_modified_at`** (измеритель свежести
    ключа комиссионной группы) рассматривался и **отвергнут** (H21
    `DOCS_CHECK_10`): бэкфилл проставил бы метке значение, которого
    измерение не производило. Начальное состояние выражено **семантикой
    `NULL`** — «ключ не подтверждён», инструмент не участвует в
    entry-скане до первого успешного тика синка
    (`docs/models/domain/other/InstrumentExternalRules.md` §«Свежесть
    ключа группы»); колонка уже существует с `V1` и nullable, менять её
    не требуется. (Валютные колонки `instruments` — отдельная строка
    дельты выше, H6 `DOCS_CHECK_11`.)

- **Предусловия `CODE` шага 7** — то, без чего шаг на `CODE` не
  переводится (перечень сквозной, гейты собраны в одном месте):
  1. **Инвариант агрегации positions-history** (N11) верифицирован
     рантайм-прогоном — иначе число `resultProfit` может браться со
     слайса (`.claude/tests/source-api/okx/plan.md` §AG1).
  2. **Непустой список исключений сверки для OKX** (H10
     `DOCS_CHECK_11`): перечень `type`/`subType`, не принадлежащих
     экономике сделки, наполнен тем же прогоном. Пустой список
     отгружает контроль целостности числа **погашенным**
     (`docs/models/mapping/DealCashFlow.md` §«Область сверки»).
  3. **Закрыт** (2026-08-20, `RISK-Q4`): дом операндов планового риска —
     **только `orders`**, две колонки write-once; входной алго-тропы не
     существует (inspection по коду), условие возврата записано в
     `docs/models/domain/core/AlgoOrder.md` §Назначение. Гейта нет.
  4. **Закрыт** (2026-08-20, `ANOM-Q5`): процессных писателей три,
     межакторная гонка в фазе 1 недостижима (коды не пересекаются,
     каждый актор сериализован guard'ом); внутритиковая тропа задвоения
     закрыта проходом радиуса и составным ключом поиска; частичный
     уникальный индекс — страховка мультиинстанса (фаза 3)
     (`docs/models/domain/other/AnomalyReport.md` §Инварианты
     структуры). `ANOM-Q3`/`ANOM-Q4` сняты вместе с анкер-ключом;
     `ANOM-Q6` — зависимый. Гейта нет.
  5. **Носитель курса cross-ccy** (H25 `DOCS_CHECK_11`): эндпоинт свечей,
     разрешение и правило деградации — хэнд-офф `integrator`; до него
     пересчёт неисполним и **все** cross-ccy строки уходят в
     `RATE_UNAVAILABLE`. **Тип носителя разрешения от этого гейта не
     зависит и решён отдельно** (H10 `DOCS_CHECK_15`): доменный
     `TimeFrame` расширен значением `ONE_SECOND` — лестница деградации
     требует, чтобы множество значений носителя ей соответствовало, каким
     бы ни оказался ответ по доступности. При отрицательном ответе
     (секундных свечей нет) гейт п. 5 закрывается, а значение остаётся
     легальным только у координаты свечи курса
     (`docs/models/domain/other/CandleGroup.md` §«Енум `TimeFrame`»).
  6. **Оси адресации записи positions-history без `posId`** (H5
     `DOCS_CHECK_11`): какие оси принимает источник и как ведёт себя при
     нескольких записях в окне — **хвост `integrator`**, без него
     create-тропа не специфицирована. **Наш операнд под временную ось
     назначен** (H12 `DOCS_CHECK_12`): нижняя граница —
     `Order.externalCreatedAt` первой отправленной ноги входа; прежде здесь
     стояло «окно сделки», обе границы которого пишет ровно эта же нога.
     Гейт остаётся, но его содержание — только грунт источника, не наш
     пробел.
  7. **Знак и горизонт накопления `fundingFee`** (`AG1.7`, H20
     `GAPS_CLOSE_13`). Два факта источника, оба — условия корректности, а
     не калибруемые величины:
     - **фактический знак** `fundingFee` в записи positions-history
       определяет **реализацию** нормализации, тогда как доменная
       конвенция «`externalFundingCost` — издержка, положительна когда
       фондирование уплачено» принята **до** проверки: при ошибке
       де-микширование вычитает funding дважды, `R` занижается
       направленно и коррелированно с длительностью удержания
       (`docs/models/mapping/PositionCloseResult.md` §«Знак `fundingFee`»);
     - **горизонт накопления** («за жизнь позиции» или «за последнее
       закрытие»): при ответе «за последнее закрытие» третья пара сверки
       **неверна по построению**, а не по калибровке — это **условие
       снятия** (`docs/decisions/per-trade-risk-policy.md` §H25).

     Кейс уже заведён в плане тестов; в этот реестр он не попадал, хотя
     реестр объявляет себя сквозным.
     - **Знак `fee` и `liqPenalty` — тем же кейсом `AG1.7`, но гейта не
       образуют** (H15 `DOCS_CHECK_16`; статус гейта снят решением
       держателя `GAPS_CLOSE_16`: знак **проверяется прогоном** и правится
       по факту выяснения, а `CODE` на него не ждёт). Три из четырёх пар сверки сравнивают знаковые
       числа **двух разных эндпоинтов** (positions-history против bills), и
       статус посылки о знаке у трёх операндов разный без довода: у
       `fundingFee` расхождение конвенций названо, нормализовано в одном
       месте и гейтит; у `fee` знак **заявлен утверждением без прогона**; у
       `liqPenalty` не заявлен вовсе — три носителя пишут «сырой знак», и
       направление не называет ни один (проверено грепом по `docs/`).
       **Довод паритета с п. 9 отвергнут держателем:** сестринская посылка
       гейтит, потому что при ошибке третья пара неверна **по построению**;
       у знака `fee`/`liqPenalty` исход другой — меняется **реализация**
       (наличие отрицания в паре), а место приведения и доменная конвенция
       остаются. Такая правка вносится по факту прогона и `CODE` не
       блокирует.
       - **Цена ошибки у `liqPenalty` — детерминированный контроль.**
         Четвёртая пара сравнивает `externalLiquidationPenalty` **без
         отрицания** (третья — против `−externalFundingCost`). Если
         источник отдаёт штраф **положительной величиной**, Δ₄ = 2·|штраф|
         **на каждой** ликвидации ⇒ `MISMATCHED` детерминированно, в
         корзине `LIQUIDATION`/`FORCED_REDUCTION`. Это ровно критерий,
         которым проект уже дважды браковал конструкции: контроль,
         срабатывающий на каждом объекте своей популяции, эквивалентен
         погашенному. Популяция — левый хвост, на котором стоит
         `BiggestLoss`. Обратная ветка не лучше: если знак совпал
         случайно, а `balChg` у ликвидационного bill'а знаковый иначе,
         дефект тихий.
       - **Что добывается прогоном:** фактический знак `fee` и
         `liqPenalty` в записи positions-history и знак `balChg` у bill'а
         ликвидационного штрафа. Фикстура — та же, лишнего прогона нет.
         Носитель по итогам — `docs/models/mapping/PositionCloseResult.md`
         §«Контракт записи проверяется здесь».
       - **Посылка «источник может отдавать любой из двух знаков»
         достроена из общих соображений** и помечена непроверенной;
         проверена и держится посылка «знак не определён ни одним доком».
         Корпус ADL/ликвидационных каскадов объявляет это пробелом и
         достраивать запрещает.

  8. **Калибратор `k` и условие выхода из разведочного режима** (H9
     `DOCS_CHECK_13` — **не закрыто**). Величина `k` омиссионного члена
     epsilon отгружается провизорной, а названный калибратор («пользователь
     / бэктест») её произвести не может: бэктест не содержит движений
     аккаунта источника и допуска на **омиссию данных API** не даёт.
     Требуется назвать: чем `k` калибруется, что фиксирует исход и к чему
     привязан срок выхода из разведочного режима — по образцу п. 2 того же
     реестра. Без этого `reconciliationStatus` может стать систематическим
     `MISMATCHED`, а на нём стоит доверие ко всей R-выборке.
  9. **Посылка «биржевой net не содержит издержку вне settle-ccy»**
     (`AG3.4`) — **гейтит** (статус назван H19 `DOCS_CHECK_14`; прежде
     посылка статуса гейта не имела ни здесь, ни в носителе — реш.4
     §выше). Довод: сестринская посылка того же класса (кумулятивность
     `realizedPnl`, реш.6 — п. 1) помечена предположением **и гейтит**, а
     довода, отличающего эту, нет; при её ложности cross-ccy-слагаемое
     считается **дважды**, число занижается систематически, и ни одна из
     четырёх пар этого не видит (строки не в settle-ccy). Прогон — та же
     фикстура §AG1.

  10. **Посылка «след частичного принудительного эпизода остаётся
      строками `LIQ_PENALTY`»** (H13 `DOCS_CHECK_15`, решение
      пользователя) — **гейтит**. Утверждение перемаркировано из **факта**
      в **посылку**: `type` `5,6` — ADL, он закрывает позицию против
      контрагента, а не через страховой фонд, и ликвидационный штраф в нём
      не обязателен; при нулевом `liqPenalty` и отсутствии bill-записи
      штрафа частичный эпизод **не оставляет следа вовсе**, а признаки
      отбора остаются чистыми. Направление ущерба — **правый хвост**
      (ADL выбирает прибыльные позиции с высоким плечом), то есть
      систематическое занижение ожидаемости. Корпус ADL объявляет
      пробелом и достраивать запрещает — значит клауза была **достройкой**,
      и её статус обязан быть назван. Прогон — та же фикстура §AG1:
      порождает ли `type` `5,6` записи категории `LIQ_PENALTY` в bills.
      Носители — `docs/models/domain/aggregate/Deal.md` §«Принудительный
      эпизод», `docs/models/mapping/DealCashFlow.md` §«Резолв категории».
      **Операнд-хвост `integrator`** (какие bill-записи порождает `type`
      `5,6`) назначенным владельцем **не проверен**.
  11. **Значения поля `direction` записи positions-history** (H7
      `DOCS_CHECK_15`) — **хвост `integrator`, гейта не образует.**
      Решение определило, **где** живёт правило резолва (слой интеграции,
      снапшот несёт доменный `Position.Direction`) и что делает
      нераспознанное значение (controlled-исключение), но **каково**
      правило — факт источника: посылка «источник отдаёт не имена наших
      констант» назначенным владельцем не проверена. Таблица значений
      заводится в `docs/integrations/okx/contracts/position.md` §История
      по итогам сверки. Гейта нет: при любом ответе меняется содержимое
      таблицы маппинга, не конструкция.
  12. **Носитель конфига «список исключений сверки по бирже» — решён**
      (H16 `DOCS_CHECK_16`, решение пользователя). Прежде п. 2 требовал,
      чтобы список `type`/`subType` был **непуст**, но **где он живёт** не
      было решено, и структурный вопрос в этот реестр не вносился, хотя
      реестр объявляет себя сквозным: между «список наполнен» и «список
      читается кодом» лежала невыбранная структура, при которой `CODE`
      формально удовлетворял п. 2, не имея откуда список прочитать. Список
      — конъюнкт **четырёх из пяти** областей суммирования и
      cross-ccy-области, формирующей само число.
      - **Дом — `@ConfigurationProperties` per-exchange.** Механизм в
        проекте уже используется; перечень меняется **без релиза**, что и
        было решающим соображением: состав посторонних `type`/`subType`
        уточняется по мере наблюдения за источником, и ждать релиза под
        каждое уточнение нельзя — при неполном списке контроль целостности
        числа искажён.
      - **Цена названа:** значение живёт **вне репозитория**, поэтому
        перечень не версионируется вместе с кодом, а стартовый состав
        обязан лежать в конфиге по умолчанию (иначе п. 2 не выполняется
        при первом запуске). Ключ конфигурации — по бирже, чтобы вторая
        площадка не наследовала чужой список.
      - **Отвергнуты:** `docs/integrations/okx/rules/` + константы adapter'а
        (правка требует релиза) и реляционный носитель на `exchanges`
        (аудируемо, но связность максимальная — состав области сверки
        становится данными, а не конфигурацией). Обратная опция —
        отвергнутый ранее вариант «сверять только распознанные
        экономические категории» — убирает конфиг целиком и остаётся
        отвергнутой.
      - **Что остаётся гейтом:** только **содержание** перечня (п. 2,
        хвост `integrator`), не структура.
      - Не-схемная дельта носителя конфига в §Следствия **не значилась** —
        строка заведена вместе с этим пунктом.

  13. **Клеймы полноты шага непроверяемы до реализации системного слоя —
      названное ограничение, не гейт** (решение держателя 2026-08-23 по
      итогам inspection; разведка —
      `.claude/notes/2026-08-23-разрывы-спека-кода-на-тропе-живой-сделки.md`).
      Пункт заведён **не** чтобы задержать `CODE`, а чтобы «чисто» на
      `DOCS_CHECK` не читалось шире, чем оно есть.

      > Доки шага описывают `SystemActionType`, `REPLACE`-ремодел и
      > наблюдение состояния в `MANAGING`, **которых в коде нет**.
      > «Чисто» на `DOCS_CHECK` означает «**доки не противоречат друг
      > другу**», а не «спецификация реализуема».

      - **Что именно не обеспечено кодом:** системного слоя действий нет
        (`SystemActionType` как типа не существует, `SystemActionExecutor`
        отсутствует) ⇒ тропа «сопровождение» и `REFRESH_DEAL_CONTEXT_ACTION`
        описаны, но не построены; `StrategyActionType.REPLACE`/`CANCEL` без
        исполнителей ⇒ ремодела защиты нет; `ManagingHandler` не эмитит
        `REFRESH_*` ⇒ сделка из `MANAGING` не выходит;
        `attachedProtection` не доезжает до payload; шаг `EXIT` не
        исполним; `FAIL_SAFE` без потребителя.
      - **Следствие для гейта:** гейт `CODE` **не может открыться по
        клейму, который никто не проверял**. Прогон `DOCS_CHECK`
        проверяет согласованность доков между собой; реализуемость
        спецификации он не проверяет и проверять не может — у линз нет
        такой поверхности. Отчёт `DOCS_CHECK_17` обязан различать эти два
        утверждения явно (§Состояние гейтов отчёта).
      - **Решение держателя:** шаг 7 доводится до `CODE` **как
        запланировано**; пять разрывов уходят дельтой `CODE`
        (`.claude/work/backlog.md` §Шаг 7), контур сейчас не
        достраивается. Цена принята осознанно и записана здесь, чтобы не
        подразумеваться.

  **Негейтящие решения записываются здесь же** (H19 `DOCS_CHECK_14`:
  реестр, объявляющий себя сквозным местом сбора гейтов, обязан нести и
  «рассмотрено — не гейтит», иначе отсутствие кейса читается как «не
  рассматривали»): **`AG1.5`** (нижняя граница окна / entry-fee) и
  **`AG3.5`** (гранулярность bills, fee-эхо) — рассмотрены и **не
  гейтят**: их прогон и так запланирован **до** `CODE`
  (`docs/models/mapping/DealCashFlow.md` §«Под-случай fee-эхо»).
  **Под-вопрос `AG1.5` «entry-fee раньше `cTime` позиции» закрыт
  конструкцией** (H9 `DOCS_CHECK_16`, решение пользователя): нижнюю границу
  окна пишет **единственный** писатель — `SubmitOrderExecutor`,
  `Order.externalCreatedAt` первой отправленной ноги, — а он **не позже**
  комиссии исполнения этой же ноги, значит окно накрывает entry-fee при
  любом ответе источника. Условная ветка H27 `DOCS_CHECK_10` снята вместе с
  зависимостью. Сам кейс `AG1.5` остаётся: он несёт инвариант агрегации
  (п. 1) и служит фикстурой для `AG1.7`/`AG3.4`/`AG3.5`/`AG12.5`.
- **Дельта существующего executor'а:** `RefreshPositionExecutor` получает
  вторую ногу цикла (positions-history), запись полей положения закрытия
  на `Position` и запись **`Deal.billsWindowEnd`** (нижнюю границу этот
  executor не пишет — H9 `DOCS_CHECK_16`; писатель `SubmitOrderExecutor`,
  и это **дельта его** дока).
- **Реконсиляция:** `ServiceCommand` enum, `SystemActionExecutor`,
  `DealActionState` (вид SYSTEM + N7-нота),
  `FinalizeDealExitExecutor` (N6/N7/N10), `MarkDealClosedExecutor` (N7),
  `MarkDealErrorExecutor`/`ErrorHandler`/`ExitPendingHandler` (N6/N8/N12),
  `Deal.md` + `lifecycles/Deal.md` (N7/N8), `InstrumentExternalRules`/
  `SizeCalculator`/`RiskValidator`/`per-trade-risk-policy` (N9),
  `refresh-evidence-cycle-ownership`/`fills.md`/`risk-validator-scope`/handler'ы
  (N12), `result-profit-source` (ссылки), `coverage-manifest`.
- **N13** (funding как holding-cost на форварде) — отдельная форвард-нота
  (`per-trade-risk-policy.md` §Учёт комиссий + backlog), не здесь.

## Форвард-фокус: ось упущенных возможностей (фаза 3)

**Раздел заведён `GAPS_CLOSE_12`** (H22 `DOCS_CHECK_12`): на него ссылались
из §«Оговорка о цене», а секции в файле не существовало — признанная цена
осталась без носителя. Здесь она названа; **содержательный разбор — фаза 3**
(решение пользователя: ось упущенных возможностей относится к фазе 3, не к
фазе 2).

**Что признано и не измеряется.** Реакции проекта на отказы —
преимущественно «запрет новых входов + ручное снятие», то есть цена отказа
конвертируется именно в **упущенные возможности**. Система измеряет
ожидаемость того, что торговала, и не может измерить, чего не сделала:

- `Deal.md` §«Рамка R-выборки» говорит, что несостоявшиеся входы
  учитываются отдельным счётчиком частоты, — но такой счёт возможен **только
  для входов, дошедших до создания `Deal`**;
- входы, не состоявшиеся **до** создания сделки, не счётны ни одним
  механизмом: инструмент под `ENTRY_BLOCKED`/`TRADE_BLOCKED` **выпадает из
  выборки скана** (`EntryScannerJob`), инструмент с неподтверждённым ключом
  группы не меняет статуса и отчёта не заводит, контурный гейт «нет активной
  сделки ни по одному инструменту» отсекает кандидатов молча;
- грунт: «ожидаемость × возможности = ценность системы; сравнение валидно
  при **полном использовании возможностей**» [Tharp гл.6 с.142, 156-157;
  гл.11 с.271-275]; «весь годовой профит часто делает одна сделка — её
  нельзя пропустить» [Tharp гл.6 с.158-159], то есть распределение цены
  пропуска само тяжелохвостое; SR ∝ √(независимых ставок/год) [Carver ST
  гл.2 с.59-60].

**Смежное — разрешимость выборки** (H24 `DOCS_CHECK_12`, тоже **фаза 3**):
контур фазы 1 — один инструмент, одна активная сделка; сколько сделок нужно
R-выборке, чтобы отличить систему от шума, не оценено. До оценки числа
ожидаемости фазы 1 — операционные, не статистические.

**Что решается в фазе 3:** нужен ли счёт упущенных возможностей как
самостоятельная метрика, что считается «возможностью» (сделка или время
торговой недоступности), и какой минимальный объём выборки делает
ожидаемость пригодной для калибровки провизорных величин. До фазы 3 обе оси
— **названное ограничение**, а не пробел спецификации шага 7.

## Связи

- Источник числа (что за данные) — `docs/decisions/result-profit-source.md`.
- Инвариант «комиссии только в settle-ccy» (дом; H8) —
  `docs/rules/trading-constraints.md` §Валюта комиссии.
- Терминальный контракт — `docs/lifecycles/Deal.md` §«Терминальный контракт
  финализации».
- Риск-политика и комиссии — `docs/decisions/per-trade-risk-policy.md`.
- Финализационная механика шага 6 — `docs/decisions/deal-finalization-state-materialization.md`.
- Внутренняя error-градация — `docs/rules/error-handling-policy.md`,
  `docs/rules/runtime-error-classification.md`.
