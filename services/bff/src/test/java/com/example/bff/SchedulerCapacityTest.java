package com.example.bff;

import com.example.testsupport.SchedulerCapacityContract;

/**
 * Проба вместимости планировщика периметра: форма общая
 * ({@link SchedulerCapacityContract}), дерево и конфигурация — свои.
 *
 * <p><b>Измеритель среди джоб периметра — пульс потока</b>
 * ({@link com.example.bff.domain.jobs.StreamPulseJob}): его молчание фронт
 * показывает как «поток мёртв», то есть голодание тика смотрящему неотличимо
 * от мёртвого потребителя (docs/architecture/contracts.md §«Живые данные в
 * браузер»).
 *
 * <p><b>Объявленное число сегодня равно умолчанию каркаса, и проба нужна
 * именно поэтому.</b> Само объявление поведения не меняет; оно делает
 * инвариант <b>измеримым</b>. Вторая джоба периметра — а поверхность его
 * растёт шагом фронта — без правки числа вернула бы голодание <b>молча</b>,
 * потому что отказа при этом не происходит. С пробой она роняет сборку.
 */
class SchedulerCapacityTest extends SchedulerCapacityContract {
}
