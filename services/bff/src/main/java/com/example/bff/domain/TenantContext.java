package com.example.bff.domain;

/**
 * Контекст вызова: тенант и роль предъявителя.
 *
 * <p><b>Выводится из членств, а не приходит из браузера</b>
 * (docs/architecture/contracts.md §«Контекст тенанта в вызове»):
 * присланное значение сверять пришлось бы всё равно с членствами, то
 * есть исполнять этот же вывод.
 *
 * @param tenantId идентичность тенанта, чей контекст уезжает заголовком
 * @param role     роль предъявителя в этом тенанте; едет с первого дня и
 *                 сегодня не читается никем — заголовок заводится вместе
 *                 с тропой, чтобы не переписывать её при появлении
 *                 второго субъекта
 */
public record TenantContext(String tenantId, String role) {
}
