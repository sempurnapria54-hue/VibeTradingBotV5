<?xml version="1.0" encoding="UTF-8"?>
<!--
  strategy-engine — общий артефакт: интерпретатор определения стратегии
  (docs/architecture/services.md §«Общие артефакты монорепозитория»).
  Собирается в сервисы, сам не разворачивается.

  ЗАВОДИТСЯ ВМЕСТЕ С ПЕРВЫМ СВОИМ ПОТРЕБИТЕЛЕМ, а не «фазой 4», как
  говорила плановая заметка libs/README.md. Потребность предъявлена
  шагом 6 фазы 2: market-data резолвит фазу рынка по авторским клаузам
  потребителя (docs/components/MarketPhaseService.md), а грамматику
  условий держит интерпретатор. Копия грамматики внутри market-data была
  бы вторым носителем одной истины (.claude/rules/policy-home.md), и
  разошлась бы с оригиналом при первом же расширении каталога условий.

  НЕСЁТ ПОКА ТОЛЬКО ИНТЕРПРЕТАТОР. Детекторы и симулированный коннектор —
  другие модули той же библиотеки; они приезжают со своими шагами, как и
  здесь: носитель без потребителя не заводится.

  ЗАВИСИМОСТЕЙ МИНИМУМ — тот же довод, что у domain-model: библиотеку
  тянут в себя сервисы, и всякая зависимость здесь становится их
  обязательной. Spring тут только ради стереотипа бина: интерпретатор —
  коллаборатор сервисных бинов.
-->
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>com.example</groupId>
    <artifactId>strategy-engine</artifactId>
    <version>0.0.1-SNAPSHOT</version>
    <packaging>jar</packaging>
    <name>strategy-engine</name>
    <description>Интерпретатор определения стратегии</description>

    <properties>
        <maven.compiler.release>25</maven.compiler.release>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <lombok.version>1.18.42</lombok.version>
        <commons-lang3.version>3.19.0</commons-lang3.version>
        <commons-collections4.version>4.5.0</commons-collections4.version>
        <slf4j.version>2.0.17</slf4j.version>
        <spring.version>7.0.0</spring.version>
        <maven-compiler-plugin.version>3.14.1</maven-compiler-plugin.version>
        <maven-surefire-plugin.version>3.5.4</maven-surefire-plugin.version>
        <junit-jupiter.version>6.0.1</junit-jupiter.version>
        <assertj.version>3.27.6</assertj.version>
        <logback.version>1.5.21</logback.version>
        <!--
          Метка красных ожиданий, изъятая из умолчания прогона. Кейс, чьё
          ожидание взято из дома, а дерево кода несёт иначе, краснеет по
          построению — и его красный прогон И ЕСТЬ предъявление названного
          долга (.claude/tests/cases/strategy-engine-calculation.md
          §«Ожидание берётся из дома, даже когда сегодня оно не
          исполнено»). В умолчании реактора такой кейс сделал бы красным
          всё дерево, то есть подменил бы предмет сигнала; поэтому он
          помечен @Tag("debt") и прогоняется отдельно:
          mvn test -Dtest.excluded.groups=
        -->
        <test.excluded.groups>debt</test.excluded.groups>
    </properties>

    <dependencies>
        <dependency>
            <groupId>com.example</groupId>
            <artifactId>domain-model</artifactId>
            <version>${project.version}</version>
        </dependency>

        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <version>${lombok.version}</version>
            <scope>provided</scope>
        </dependency>

        <!--
          Commons Lang3 и Collections4 — требование конвенции
          (.claude/rules/codestyle.md §«Строгие правила»): проверки
          коллекций и отрицания пишутся ими, а не руками.
        -->
        <dependency>
            <groupId>org.apache.commons</groupId>
            <artifactId>commons-lang3</artifactId>
            <version>${commons-lang3.version}</version>
        </dependency>
        <dependency>
            <groupId>org.apache.commons</groupId>
            <artifactId>commons-collections4</artifactId>
            <version>${commons-collections4.version}</version>
        </dependency>

        <!--
          Логирование — только API: интерпретатор пишет предупреждение о
          неоцениваемом в этом контексте типе правила. Реализацию выбирает
          сервис, который библиотеку в себя собирает.
        -->
        <dependency>
            <groupId>org.slf4j</groupId>
            <artifactId>slf4j-api</artifactId>
            <version>${slf4j.version}</version>
        </dependency>

        <!--
          spring-context — ради @Service на интерпретаторе: он коллаборатор
          сервисных бинов и должен подниматься сканированием, а не
          собираться руками у каждого потребителя.
        -->
        <dependency>
            <groupId>org.springframework</groupId>
            <artifactId>spring-context</artifactId>
            <version>${spring.version}</version>
        </dependency>

        <!--
          ТЕСТОВЫЕ ЗАВИСИМОСТИ ЖИВУТ ЗДЕСЬ, а не у сервиса-потребителя:
          расчётный слой есть исполнимая форма спек (order-sizing,
          risk-at-stop, stop-distance), и его проверка не должна зависеть
          ни от одного сервиса — тот же довод, по которому сам слой уехал
          в библиотеку (docs/architecture/services.md §«Общие артефакты
          монорепозитория»).

          Правило «зависимость общего артефакта становится обязательной
          для всех сервисов» (.claude/rules/codestyle.md §«Новый модуль
          монорепозитория») на область test не распространяется: она не
          транзитивна и в сборку сервиса не попадает.

          Версии закреплены явно — родителя у модуля нет; совпадают с
          теми, что даёт spring-boot-starter-parent 4.0.0 сервисам, чтобы
          два дерева тестов не разъезжались по API.
        -->
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter</artifactId>
            <version>${junit-jupiter.version}</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.assertj</groupId>
            <artifactId>assertj-core</artifactId>
            <version>${assertj.version}</version>
            <scope>test</scope>
        </dependency>

        <!--
          Реализация логирования НА ТЕСТОВОМ ПУТИ — по предъявленной
          потребности, а не превентивно: у интерпретатора условий выход не
          только булев. Тип правила, чей операнд собирает не этот контекст,
          называет себя ЗАПИСЬЮ ЖУРНАЛА, и без неё «тип не исполняется»
          неотличимо от «предикат честно ложен» — а именно это различение и
          есть предмет группы `U12` документа
          `.claude/tests/cases/strategy-engine-condition.md`. С одним
          slf4j-api провайдера нет, логгер NOP, и наблюдать нечего.

          Сборку сервисов это не задевает: область test не транзитивна
          (.claude/rules/codestyle.md §«Новый модуль монорепозитория»).
          Версия — та же, что даёт spring-boot-dependencies 4.0.0 сервисам.
        -->
        <dependency>
            <groupId>ch.qos.logback</groupId>
            <artifactId>logback-classic</artifactId>
            <version>${logback.version}</version>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <!-- Версия закреплена явно: родителя у модуля нет (см. domain-model). -->
                <version>${maven-compiler-plugin.version}</version>
                <configuration>
                    <annotationProcessorPaths>
                        <path>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok</artifactId>
                            <version>${lombok.version}</version>
                        </path>
                    </annotationProcessorPaths>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <!-- Версия закреплена явно по той же причине: родителя у модуля нет. -->
                <version>${maven-surefire-plugin.version}</version>
                <!-- Метка красных ожиданий вне умолчания прогона — довод у свойства. -->
                <configuration>
                    <excludedGroups>${test.excluded.groups}</excludedGroups>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
