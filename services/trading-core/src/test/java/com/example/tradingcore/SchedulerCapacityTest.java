package com.example.tradingcore;

import com.example.testsupport.SchedulerCapacityContract;

/**
 * Проба вместимости планировщика торгового ядра: форма общая
 * ({@link SchedulerCapacityContract}), дерево и конфигурация — свои.
 *
 * <p><b>Измеритель среди семи джоб ядра — проактивная детекция</b>
 * ({@link com.example.tradingcore.domain.jobs.AnomalyJob}). Её выход —
 * измерение о НАБЛЮДАЕМОСТИ контура, а не собственный продукт: чистый проход
 * обнуляет счёт слепоты, ненаблюдённый его увеличивает, а <b>несостоявшийся
 * не двигает вовсе</b> — счёт остаётся нулевым, и читатель видит «наблюдение
 * идёт, аномалий нет» (docs/components/AnomalyJob.md §«Гейт полноты среза»).
 *
 * <p><b>Цена объявления названа: проходы ядра становятся одновременными.</b>
 * Пары его джоб пройдены ходом, поднявшим пул, и те, что разведения не
 * имели, разведены поимённо: проекция реестра против торговых колонок
 * строки счёта —
 * разведена признаком {@code @DynamicUpdate}
 * ({@link com.example.tradingcore.persistence.model.ExchangeAccountEntity}),
 * проактивная детекция против прохода сделки на строке {@code deals} —
 * разведена переводом писателей существующей строки на точечные
 * гардированные запросы (docs/models/domain/aggregate/Deal.md
 * §Персистентность). Остальное общее разведено гардом запроса либо
 * инвариантом базы.
 */
class SchedulerCapacityTest extends SchedulerCapacityContract {
}
