# Knowledge-placement debug run: широкий прогон v3 на полном корпусе

**Дата:** 2026-05-19
**Скилл:** knowledge-placement v3
**Тип прогона:** широкий, для сигналов к v4.
**Корпус:** полное содержимое `.claude/`, `docs/spec/`, корневой `CLAUDE.md`.
**Исключено:** ~38 фрагментов из предыдущих прогонов (v1, v2,
v2-corpus, v3-регрессия).

## Замечания по структуре корпуса

- `.claude/conventions/` **не существует** (упоминается в v8 как
  планируемый каталог; v2-фрагмент 9 corpus, v3-сигнал к Кластеру A,
  фрагмент 13). Это согласуется с состоянием.
- `strategy-summary-v8.md` существует — новый снимок,
  созданный 2026-05-19 (после corpus-прогона). Резюмирует решения
  38-49, в основном повторяет v2-фрагменты 1-12 в более структурной
  форме.
- `.claude/adr/README.md` индекс содержит только ADR-0001..0005;
  фактически в каталоге ADR-0001..0009. Расхождение, см. фрагмент 25.
- v1-v6 strategy-summary — застывшие историчные T-SNAP, прочитаны
  выборочно; основные решения 1-37 уже в v7 (covered v1/v2).

## Объём корпуса

- Файлов прочитано: 22 (ключевые); ещё ~20 кратко обозрены
  через grep/прежний контекст.
- Фрагментов выделено всего: ~50 (предварительно).
- Из них исключено как покрытые предыдущими прогонами: ~20.
- Подозрительных в отчёте: 30.
- Чисто отработавших: 9.

## Исключённые как покрытые предыдущими прогонами

- v8 решения 38-39 (двойная ось + два измерения) — покрыто v2-фр. 1.
- v8 решение 40 (граница `.claude/` vs `docs/`) — покрыто v2-фр. 8.
- v8 решение 41 (Q-N — это T-Q) — покрыто v2-фр. 2.
- v8 решение 42 (метамодели = L-SPEC PIPELINE; заглушка — раздел)
  — покрыто v2-фр. 3 + 4.
- v8 решение 43 (L-SPEC vs L-CONV PIPELINE) — покрыто v2-фр. 6.
- v8 решение 44 (жанр format не вводится) — покрыто v2-фр. 5.
- v8 решение 45 (два шаблона ADR) — покрыто v2-фр. 7.
- v8 решение 46 (ADR-0003/0004/0005 не переносятся) — покрыто v2-фр. 10.
- v8 решение 47 (append-only временно) — покрыто v2-фр. 11.
- v8 решение 48 (ADR-PIPELINE-0006 последним) — покрыто v2-фр. 12.
- v8 «Не пересматривать карту до Этапа 1» — расширение v2-фр. 11
  (temporary-rule); по содержанию аналогично, не разворачиваю.
- v8 OQ-INV-* — это open-question entries; covered как класс
  v2-corpus-фрагмент 3.
- v8 MAP-1 (T-MIG PIPELINE), MAP-2 (T-Q PRODUCT) — covered v2-corpus
  фрагменты 8, 10 и v3-сигналы.
- evolution-log запись 2026-05-18..19 — резюме того же материала.

## Подозрительные фрагменты

### Фрагмент 1: Принцип 6 — «Документация — источник истины, код подтягивается»

**Источник:** `.claude/working-with-claude.md`, раздел «Принцип 6».

**Цитата:**

> «На этом проекте код **отстаёт** от документации, не наоборот.
> Доменная документация описывает **целевое состояние** системы;
> код реализует его постепенно. … При работе над спецификацией:
> дока описывает, как **должно быть**, а не "как сейчас в коде".
> Расхождение "есть в доке, нет в коде" — нормально (модель
> спроектирована, не реализована).»

**Результат применения скилла v3:**

```
Классификация:
- Измерение: PRODUCT (про сам бот) + PIPELINE (методология работы
  с расхождениями)
- Основные категории: L-ADR + L-CONV PRODUCT + L-CONV PIPELINE
  (Шаг 1(в): три аспекта — выбор «дока ведёт код», как правило
  применения при работе над spec, как правило применения при работе
  над кодом)
- Основной вопрос:
  - L-ADR: «почему дока ведёт код, а не наоборот?»
- Тег: precedent (определяет режим работы со всем продуктом).

Место хранения:
- Целевой файл (L-ADR): планируется ADR-PRODUCT или ADR-PIPELINE
  про целевую vs реализационную семантику документации.
- Целевой файл (L-CONV PRODUCT/PIPELINE): сейчас в working-with-claude
  как Принцип 6. После рефакторинга — раздел в `.claude/conventions/`?
  раздел в CLAUDE.md? Конвенция не определена.
- Промежуточное хранение: текущее место (working-with-claude
  Принцип 6).

Связные обновления:
- Конфликт с существующим ADR (Шаг 3(ж)): нет прямого. Принцип 6
  закреплён в working-with-claude, не в ADR. Это уже отражает
  «правила в convention, обоснование — в ADR» (Принцип 7), но
  L-ADR обоснование для самого Принципа 6 отсутствует.
- Скилл `spec-document-workflow` опирается на этот принцип
  («документ — карта целевого состояния»); согласовано, но
  ссылки нет.

Развилка конвенции (Шаг 2(ж)):
- Категория классифицирована: L-CONV (одновременно PIPELINE и
  PRODUCT — правило применяется и при работе со spec, и при
  работе с кодом).
- Конвенция размещения для случая «фундаментальный принцип, как
  работать с двумя измерениями документа vs кода» отсутствует.
  Сейчас он в working-with-claude (хаб M-PIPE), но это L-CONV-
  уровневое утверждение, для которого нет каталога convention.
- Развилка системная (применима к Принципам 1-7 working-with-
  claude — все они в одном месте, но природа разная).
- Варианты решения конвенции:
  (а) принципы 1-7 остаются в working-with-claude как намеренная
      смесь (см. v2-corpus-фрагмент 2);
  (б) принципы выехать в `.claude/conventions/principles.md` или
      по одному файлу;
  (в) гибрид: working-with-claude остаётся хабом-навигатором,
      каждый принцип — отдельный файл с обоснованием.
```

**Сигнал к v4:** Шаг 2(ж) сработал ожидаемо. Подтверждает Кластер A.
Дополнительно: фрагмент **одновременно** PIPELINE и PRODUCT
(применяется в обоих измерениях). Скилл v3 в Шаге 1(а) предлагает
одно измерение или развилку; вариант «фрагмент одновременно
обоим» не предусмотрен явно. v4: добавить опцию
«измерение: both PIPELINE и PRODUCT» в Шаг 1(а).

### Фрагмент 2: «Лёгкая пред-проверка PK» — operational procedure внутри working-with-claude

**Источник:** `.claude/working-with-claude.md`, раздел «Лёгкая
пред-проверка (начало каждого чата)».

**Цитата:**

> «В **первой реплике** каждого чата Claude сверяет состав видимых
> файлов PK с **ожидаемым списком**. … Проверка работает **только
> при наличии CLAUDE.md в PK**. … Ожидаемый список — это минимум,
> не точное соответствие.»

**Результат применения скилла v3:**

```
Классификация:
- Измерение: PIPELINE
- Основные категории: L-SKILL + L-CONV PIPELINE
  (Шаг 1(в): процедура («что делает Claude в первой реплике») —
  L-SKILL; правило применения («когда работает, что считать
  расхождением») — L-CONV PIPELINE.)
- Основной вопрос:
  - L-SKILL: «как Claude выполняет проверку PK в начале чата?»
  - L-CONV PIPELINE: «что считается расхождением и что нет?»
- Тег: не применяю.

Место хранения:
- Целевой файл (L-SKILL): сейчас процедура описана в working-with-
  claude как раздел; формально это L-SKILL по характеристическому
  вопросу. Должна была бы жить в `.claude/skills/pk-consistency-
  check/SKILL.md` (не существует).
- Целевой файл (L-CONV PIPELINE): раздел в working-with-claude
  как сейчас, либо после рефакторинга — `.claude/conventions/pk-
  workflow.md`.
- Промежуточное хранение: текущее место.

Связные обновления:
- Дубль в `.claude/project-instructions.md` Rule 0 (полное
  описание процедуры). Это L-SKILL с двумя адресатами (Claude в
  чате claude.ai vs Claude Code) — раздвоенная процедура. После
  Принципа 7 working-with-claude — должна быть в одном месте
  L-SKILL, project-instructions ссылается.
- Сценарий 8 playbook — глубокая часть проверки; ссылка есть.
- Конфликт с существующим ADR (Шаг 3(ж)): прямого нет.

Развилка конвенции (Шаг 2(ж)):
- Категория классифицирована: L-SKILL.
- Конвенция размещения для случая «процедура с двумя адресатами
  Claude (Claude Code и чат claude.ai), которая сейчас живёт
  в `.claude/working-with-claude.md` и в `.claude/project-
  instructions.md` дублированно» отсутствует.
- Развилка системная (потенциально касается всех скиллов с
  двумя адресатами).
- Варианты решения конвенции:
  (а) единый SKILL.md с пометкой «для обоих адресатов» + ссылка
      из project-instructions;
  (б) два SKILL.md (`pk-check-cc.md`, `pk-check-chat.md`);
  (в) оставить как есть — operational rule в working-with-claude
      и в project-instructions, без скилл-файла.
```

**Сигнал к v4:** новая разновидность развилки — «процедура с двумя
адресатами Claude». Это **не семейство M-PIPE vs M-USER** (то про
людей), а сама раздвоенность процедуры по адресату. v4: учесть в
карте, что Claude Code и Claude в чате — два возможных адресата
для одной L-SKILL-процедуры.

### Фрагмент 3: «Глубокая проверка PK — "кто менял, тот и проверяет"»

**Источник:** `.claude/working-with-claude.md`, раздел «Глубокая
проверка (конец сессии с содержательными изменениями)».

**Цитата:**

> «**Принцип:** "кто менял — тот и проверяет". Чат, который
> проектировал изменения, знает контекст ожиданий и может
> корректно сверить с фактическим состоянием. Новый чат этого
> контекста не имеет.»

**Результат применения скилла v3:**

```
Классификация:
- Измерение: PIPELINE
- Основные категории: L-ADR + L-CONV PIPELINE
  (Шаг 1(б): обоснование с альтернативой («новый чат может
  проверить») → L-ADR; правило применения «кто менял — тот и
  проверяет» → L-CONV PIPELINE.)
- Основной вопрос:
  - L-ADR: «почему глубокая проверка PK делается в исходном чате,
    не в новом?»
- Тег: precedent (применимо ко всем дисциплинам "verification by
  the originator", не только PK).

Место хранения:
- Целевой файл (L-ADR): планируется ADR-PIPELINE про
  PK-консистентность.
- Целевой файл (L-CONV PIPELINE): сейчас в working-with-claude;
  после рефакторинга — `.claude/conventions/pk-workflow.md`?
- Промежуточное хранение: текущее место.

Связные обновления:
- Сценарий 8 в playbook — операционка ровно про это.
  Согласовано.
- Конфликт с существующим ADR (Шаг 3(ж)): нет.

Развилка конвенции (Шаг 2(ж)):
- Категория классифицирована: L-CONV PIPELINE.
- Конвенция размещения для случая «принцип "кто менял, тот и
  проверяет" — это L-CONV-правило процедуры verification»
  отсутствует. Сейчас принцип сидит внутри длинного раздела
  «Лёгкая/глубокая проверка PK».
- Развилка может быть системной (принцип шире PK — applicable
  to retrospectives, code review, ADR adoption).
- Варианты решения конвенции:
  (а) принцип отдельным разделом в working-with-claude;
  (б) принцип в новой `conventions/verification-principles.md`;
  (в) принцип остаётся inline в текущем месте.
```

**Сигнал к v4:** ещё один кейс «семантической пометки принципа
внутри другой процедуры» — Кластер A подтверждается. Также сигнал
к v4: фрагмент имеет потенциал быть более универсальным правилом
(verification-by-originator), но сейчас зафиксирован только в
контексте PK. v4: пересмотреть тест «фрагмент шире контекста, в
котором сформулирован» — это сигнал к самостоятельному L-CONV
или ADR.

### Фрагмент 4: Knowledge capture rules — «распределённая ответственность всех агентов»

**Источник:** `.claude/working-with-claude.md`, раздел «Knowledge
capture rules».

**Цитата:**

> «В системные промпты **всех** агентов вшита инструкция: при
> принятии решения или обнаружении паттерна — предложить, где его
> задокументировать. Это распределённая ответственность, не
> только Curator.»

**Результат применения скилла v3:**

```
Классификация:
- Измерение: PIPELINE
- Основные категории: L-CONV PIPELINE + L-AGENT
  (Шаг 1(в): правило (что должно быть в промпте каждого агента)
  — L-CONV PIPELINE; влияние на содержание самих агентов
  — L-AGENT.)
- Основной вопрос:
  - L-CONV PIPELINE: «что должно быть в промпте каждого агента
    как минимально обязательная часть?»
- Тег: precedent (M-PROCDESIGN — методология эволюции
  агентов; правило шаблона промпта).

Место хранения:
- Целевой файл (L-CONV PIPELINE): сейчас раздел в working-with-
  claude. Кандидат на `.claude/conventions/agent-template.md`.
- Влияние: каждый L-AGENT файл должен включать секцию «Knowledge
  capture»; уже наблюдается в architect.md, domain-expert.md,
  risk-engineer.md, trading-risk-officer.md, knowledge-curator.md.
- Промежуточное хранение: текущее место.

Связные обновления:
- Все агенты (5 файлов) содержат «Knowledge capture» секцию,
  применяя это правило. Если правило меняется — править все
  5 файлов.
- Конфликт с существующим ADR (Шаг 3(ж)): нет.

Развилка конвенции (Шаг 2(ж)):
- Категория классифицирована: L-CONV PIPELINE («что должно
  быть в каждом агенте»).
- Конвенция размещения для случая «правило-шаблон для жанра
  L-AGENT» (т.е. что каждый агент обязан содержать) отсутствует.
  Сейчас правило в working-with-claude, реализация — в каждом
  агенте отдельно. Нет ни единого M-FORM (шаблон агента), ни
  L-CONV (правило о структуре).
- Варианты решения конвенции:
  (а) `.claude/templates/documents/agent.md` — шаблон агента;
  (б) `.claude/conventions/agent-structure.md` — convention;
  (в) раздел в working-with-claude (как сейчас).
```

**Сигнал к v4:** ещё один кейс правила «структуры жанра», для
которого нет специального места. Связано с corpus-фрагментом 1
(M-FORM как расширение карты). v4: рассмотреть, нужно ли явное
«шаблон жанра артефакта» — место правил структуры для агентов,
скиллов, ADR, шаблонов.

### Фрагмент 5: «Думаешь — чат, делаешь — Claude Code»

**Источник:** `.claude/flow/playbook.md`, раздел «Чат vs Claude
Code — когда что».

**Цитата:**

> «Правило: **думаешь — чат, делаешь — Claude Code**.»

**Результат применения скилла v3:**

```
Классификация:
- Измерение: PIPELINE
- Основные категории: L-CONV PIPELINE
- Основной вопрос: «как распределяется работа между чатом и
  Claude Code?»
- Тег: не применяю (это уже устоявшееся правило, не precedent).

Место хранения:
- Целевой файл: playbook.md, раздел «Чат vs Claude Code» — где
  оно и есть. Также упоминается в working-with-claude Принцип 1.
- Дубль с Принципом 1 working-with-claude — согласованный, не
  конфликтующий.

Связные обновления:
- Принцип 1 working-with-claude — более развёрнутая версия того
  же правила.
- project-instructions Rule 5 — то же правило, для чата.
- Конфликт (Шаг 3(ж)): нет; три места согласованы.
```

**Сигнал к v4:** фрагмент чисто работает. Но три **согласованных
дубля** (playbook, working-with-claude, project-instructions) —
это паттерн, который v3 не маркирует. v4 кандидат: тест на
«сознательный дубль для разных адресатов» как отдельная
категория связных обновлений, отличная от «конфликт» или
«рассинхрон».

### Фрагмент 6: «Один чат — одна задача / этап»

**Источник:** `.claude/flow/playbook.md`, раздел «Гигиена работы».

**Цитата:**

> «**Один чат — одна задача / этап.** Не превращай чат в "всё про
> проект". … **Claude Code: одна сессия — одна задача.** `/clear`
> между подзадачами, `/exit` после задачи.»

**Результат применения скилла v3:**

```
Классификация:
- Измерение: PIPELINE
- Основные категории: L-CONV PIPELINE (для адресата человека —
  частично M-USER по corpus-карте)
- Основной вопрос: «как организованы сессии чата/Claude Code?»

Место хранения:
- Целевой файл: playbook.md как сейчас (M-USER хаб для человека).

Связные обновления:
- Возможно дубль с CLAUDE.md/working-with-claude — не нашёл явно.
- Конфликт (Шаг 3(ж)): нет.
```

Чистый фрагмент. Уйдёт в «Чисто отработавшие» (см. ниже). Включён
здесь для протокола.

### Фрагмент 7: подразделы «При ревью миграционных правок» в агентах architect и domain-expert

**Источник:** `.claude/agents/architect.md` строки 39-41,
`.claude/agents/domain-expert.md` строки 38-40.

**Цитата (architect):**

> «Apply the entity-type tests from skill `spec-document-migration`
> (раздел "Определение типа сущности при миграции") when reviewing
> what is being introduced as a standalone document. Implementation
> classes — `Executor`, `Repository`, `Mapper`, `Service`,
> `Configuration`, `Factory`, `Handler`, `Resolver`, `Validator`,
> `Builder`, `Listener` — are NOT standalone domain documents.»

**Результат применения скилла v3:**

```
Классификация:
- Измерение: PIPELINE (методология ревью)
- Основные категории: L-AGENT + L-CONV PIPELINE
  (Шаг 1(в): то, что вшито в агенты — L-AGENT; правило
  применения теста типа сущности — L-CONV PIPELINE.)
- Основной вопрос:
  - L-AGENT: «как ведёт себя агент при ревью миграционных
    правок?»
  - L-CONV PIPELINE: «по какому списку реализационных классов
    блокируется создание document'а в models/?»
- Тег: precedent (правило: один источник тестов в скилле, все
  агенты ссылаются — пример применения Принципа 7 working-with-
  claude).

Место хранения:
- Целевой файл (L-AGENT): уже в architect.md, domain-expert.md.
- Целевой файл (L-CONV PIPELINE): список «11 реализационных
  классов» сейчас **дублирован** в обоих агентах (architect и
  domain-expert) **и** в skill spec-document-migration. Нужно
  одно место.
- Промежуточное хранение: текущие места.

Связные обновления:
- skill `spec-document-migration` — источник теста.
- Если правило обновляется (добавляется новый тип
  реализационного класса) — править ≥3 места одновременно.
  Это сигнал к нарушению Принципа 7 (правило живёт в скилле,
  агенты ссылаются — но список явно перечислен).
- Конфликт с существующим ADR (Шаг 3(ж)): нет прямого, но
  **паттерн-конфликт** с Принципом 7 working-with-claude (правила
  применения должны жить в одном источнике).

Развилка конвенции (Шаг 2(ж)):
- Категория классифицирована: L-CONV PIPELINE.
- Конвенция размещения для случая «список (например, 11
  реализационных классов), на который ссылаются 3+ артефакта»
  отсутствует. Сейчас список продублирован.
- Развилка системная (см. corpus-сигнал 2 для v3 — «паттерн-
  конфликт»).
- Варианты решения конвенции:
  (а) единый L-CONV-файл со списком + ссылки из агентов и
      скилла на него;
  (б) скилл-источник + ссылки из агентов без дубля списка;
  (в) оставить дубль с обязательством sync.
```

**Сигнал к v4:** **Кластер C (паттерн-конфликт)** подтверждается
третьим случаем (после corpus-фрагментов 4-6 «ADR содержат
чужой слой»). Сейчас 3-4 артефакта содержат **одно и то же
правило** (или его часть), и при изменении нужно править все.
v4: ввести явный тест «правило тиражировано в N артефактах» как
отдельный подвид связных обновлений.

### Фрагмент 8: «MODELS.md — отражение, не гейт» (повтор в 4 местах)

**Источник:** Многократное правило: `docs/spec/MODELS.md` шапка;
`.claude/agents/architect.md:40-41`; `.claude/agents/domain-
expert.md:39-40`; `.claude/skills/spec-models-registry/SKILL.md:25-35`;
`.claude/skills/spec-document-migration/SKILL.md` (косвенно).

**Цитата (MODELS.md):**

> «Реестр — отражение созданных model-документов (включая
> заглушки), не гейт для принятия решения о том, является ли
> встреченное имя бизнес-моделью.»

**Результат применения скилла v3:**

```
Классификация:
- Измерение: PIPELINE (правило применения реестра)
- Основные категории: L-CONV PIPELINE + L-ADR
  (Шаг 1(б): обоснование «реестр — отражение» с альтернативой
  «реестр — гейт» → L-ADR; правило применения → L-CONV.)
- Основной вопрос:
  - L-ADR: «почему MODELS.md — отражение созданных документов,
    а не гейт?»
- Тег: precedent (паттерн «реестр vs гейт» применим ко всем
  будущим реестрам пайплайна).

Место хранения:
- Целевой файл (L-ADR): зафиксировано исторически в Правке 2
  итерации 1 (см. evolution-log от 2026-05-18, раздел «Применённые
  правки тел Proposed-ADR»). Сейчас в ADR-0008 §2/§4
  (классификация расширена) и в формулировках самого MODELS.md.
- Целевой файл (L-CONV PIPELINE): MODELS.md шапка + 4 дубля
  в агентах/скиллах.
- Промежуточное хранение: не нужно (фрагмент уже размещён).

Связные обновления:
- Конфликт с существующим ADR (Шаг 3(ж)): нет прямого.
  **Паттерн-конфликт**: правило продублировано в 4-5 артефактах.
  Связано с фрагментом 7 — тот же паттерн.

Развилка конвенции (Шаг 2(ж)):
- Категория классифицирована: L-CONV PIPELINE.
- Конвенция размещения для случая «правило о применении
  реестра, продублированное в 4-5 артефактах одной серии»
  отсутствует. Сейчас правило формулируется по-разному в каждом
  месте (MODELS.md — шапка, агенты — короткая формула в подразделе
  ревью, скилл — раздел «Назначение MODELS.md»).
- Развилка системная.
- Варианты решения конвенции:
  (а) единый L-SKILL `spec-models-registry` как
      единственный источник (по Принципу 7), остальные ссылаются;
  (б) единый L-CONV-файл `.claude/conventions/registries-policy.md`
      про политику работы со всеми реестрами;
  (в) оставить дубли с обязательством sync.
```

**Сигнал к v4:** четвёртый случай паттерн-конфликта. Дополнительно:
фрагмент существует в **разных формулировках** в 5 артефактах —
не дубль, а пере-формулировки. Это сигнал к v4: тест на «правило
существует в разных формулировках в N местах» — это **более
тонкий** паттерн-конфликт, чем точный дубль (фрагмент 7).

### Фрагмент 9: «Прецедент цепочки ADR в одной сессии» — открытое наблюдение

**Источник:** `.claude/planning/backlog.md` строка 117.

**Цитата:**

> «**Прецедент цепочки ADR в одной сессии (наблюдение).** ADR-0004
> → ADR-0005 (2026-05-15) — родительский ADR явно предусмотрел
> дочерний "при необходимости", дочерний принят в той же сессии
> без правки родительского. Это наблюдённая практика, не
> закреплённое правило. Накопить 2-3 подобных кейса и решить:
> фиксировать как явный паттерн в working-with-claude / playbook,
> или это всегда останется частным случаем. Не торопиться.»

**Результат применения скилла v3:**

```
Классификация:
- Применение фильтра «знание vs план» (Шаг 1(б)):
  - Императив? Нет.
  - Утверждение о мире? Да — «наблюдённая практика».
  - С импликацией? Да — «накопить 2-3 кейса, решить».
  - Открытый вопрос с условием? Да — «не торопиться, накопить
    статистику».
  - → T-Q или наблюдение в backlog как сейчас.
- Измерение: PIPELINE
- Основные категории: T-Q + (потенциально L-ADR при накоплении
  статистики)
- Основной вопрос: «является ли цепочка ADR в одной сессии
  закономерностью или частным случаем?»
- Тег: не применяю.

Место хранения:
- Целевой файл: сейчас в backlog (как «не задача, а наблюдение
  на будущее»). По характеристическому вопросу — это T-Q. По
  природе — observation pending evidence.
- Альтернатива: `.claude/questions/open-questions.md` (T-Q
  PIPELINE).

Связные обновления:
- ADR-0004, ADR-0005 — источник прецедента; если паттерн
  закрепляется — упомянуть.
- working-with-claude / playbook — потенциальные адресаты при
  закреплении.
- Конфликт с существующим ADR (Шаг 3(ж)): нет.

Развилка конвенции (Шаг 2(ж)):
- Категория классифицирована: T-Q (PIPELINE) + потенциально
  L-ADR.
- Конвенция размещения для случая «observation pending
  evidence — пока недостаточно статистики, чтобы решить, это
  правило или частный случай» отсутствует. Сейчас сидит в
  backlog как «не задача, а наблюдение».
- backlog как место наблюдений-кандидатов на правила — не
  его жанр (T-BL — задачи).
- Варианты решения конвенции:
  (а) формализовать как T-Q PIPELINE (entry в журнале);
  (б) ввести отдельную категорию «accumulating observations»
      для наблюдений с условием закрепления;
  (в) оставить в backlog (текущая практика).
```

**Сигнал к v4:** новый подтип фрагмента — «observation pending
evidence», который не вписывается ни в T-Q (там вопрос, не
наблюдение), ни в T-BL (там задача, не наблюдение), ни в
L-ADR (там решение, не данные). Сейчас этот фрагмент **корректно**
лежит в backlog, но это компромисс. v4: добавить тест на
«накопительное наблюдение с условием закрепления» как кандидат
на отдельный жанр T-* (T-OBS?) или подраздел T-Q.

### Фрагмент 10: «Определение "принятия" ADR — действующая практика "согласован в чате"»

**Источник:** `.claude/planning/backlog.md` строка 119.

**Цитата:**

> «**Определение "принятия" ADR.** Правило append-only в
> `.claude/adr/README.md` ссылается на "момент принятия", но не
> определяет, что это: согласование решения в чате или коммит.
> Действующая (по практике) модель — "принят = согласован в
> чате". Уточнить формулировку в `.claude/adr/README.md`,
> добавив однозначное определение.»

**Результат применения скилла v3:**

```
Классификация:
- Измерение: PIPELINE
- Основные категории: T-Q + L-CONV PIPELINE
  (Шаг 1(в): открытый вопрос (что считать «принятием») → T-Q;
  действующее правило-по-факту («принят = согласован в чате»)
  → L-CONV.)
- Основной вопрос: «что считать "принятием" ADR — согласование
  в чате или коммит?»
- Тег: не применяю.

Место хранения:
- Целевой файл (T-Q): `.claude/questions/open-questions.md`.
- Целевой файл (L-CONV PIPELINE): `.claude/adr/README.md` — где
  и должна жить формулировка.
- Сейчас в backlog как «маленькая правка».

Связные обновления:
- `.claude/adr/README.md` — нужно уточнение.
- Конфликт с существующим ADR (Шаг 3(ж)): нет.
```

**Сигнал к v4:** аналогично фрагменту 9 — наблюдение, ушедшее в
backlog по компромиссу, но по характеристическому вопросу — T-Q.
Дополнительный сигнал: «правило-по-факту» (действующая практика
без формальной фиксации) — это **разновидность L-CONV без
явного носителя**, аналогично corpus-фрагменту 9 (двойственная
семантика T-SNAP). v4: рассмотреть «правило-по-факту» как
подтип в Шаг 2(ж).

### Фрагмент 11: «Два типа примечаний в ADR — технические и содержательные»

**Источник:** `.claude/adr/README.md` строки 52-74.

**Цитата:**

> «**Технические примечания** — разрешены в любом статусе.
> Назначение: навигационные / ссылочные пометки, не меняющие
> решение. … **Содержательные примечания** — разрешены **только**
> в статусах Superseded и Deprecated.»

**Результат применения скилла v3:**

```
Классификация:
- Измерение: PIPELINE
- Основные категории: L-SPEC PIPELINE (метамодель ADR-артефакта)
  + L-CONV PIPELINE (правило, что разрешено в каком статусе)
- Основной вопрос:
  - L-SPEC PIPELINE: «что есть в структуре ADR (типы примечаний)?»
  - L-CONV PIPELINE: «когда разрешён каждый тип примечаний?»
- Тег: precedent (паттерн «разрешённость подразделов по статусу»
  применим к другим артефактам с lifecycle).

Место хранения:
- Целевой файл (L-SPEC PIPELINE): метамодель ADR — будет
  жить в `.claude/spec/adr.md` (планируется по v2-фр. 3, v8 §42).
- Целевой файл (L-CONV PIPELINE): сейчас в `.claude/adr/README.md`.
  После v8 §42 — формулировка станет частью L-SPEC PIPELINE.
- Промежуточное хранение: текущее место (`.claude/adr/README.md`).

Связные обновления:
- ADR-0001, ADR-0002 уже содержат технические примечания — это
  применение правила.
- corpus-фрагмент 18 (тех. примечания как встроенный журнал
  переездов) — расширение этого фрагмента; правило позволяет
  использовать тех. примечания именно так.
- Конфликт с существующим ADR (Шаг 3(ж)): нет прямого. Но v2-фр. 11
  (append-only временно отключается на Этапе 1) — частично
  отменяет правило «append-only внутри append-only» для блока
  примечаний на время Этапа 1. Сейчас не проблема (Этап 1 не
  исполнен).

Развилка конвенции (Шаг 2(ж)):
- Категория классифицирована: L-SPEC PIPELINE + L-CONV PIPELINE.
- Конвенции размещения для «метамодели артефакта-журнала с
  типами подразделов и правилами их применения по статусу»
  отсутствует (L-SPEC PIPELINE как каталог не существует — см.
  v2-фр. 3, открытый MAP-4 в v8).
- Развилка системная (применима ко всем артефактам, у которых
  «append-only с разрешёнными вставками»).
```

**Сигнал к v4:** ещё один кейс L-SPEC PIPELINE-метамодели,
который сейчас живёт inline в L-CONV-документе (`.claude/adr/
README.md`). Подтверждает v2-фрагмент 3. Также: правило «append-
only с разрешённой вставкой» — это **сама** policy редактирования
(corpus-фрагмент 13). Связные сигналы к v4: метамодели и
policy одинаковых артефактов часто живут рядом, но v3 не
имеет теста на это сцепление.

### Фрагмент 12: «При крупных рефакторингах — процедура для ADR»

**Источник:** `.claude/adr/README.md` строки 80-87.

**Цитата:**

> «1. ADR переезжают физически согласно решению из ADR о
> рефакторинге. 2. Тела ADR не редактируются. 3. В блок
> "Технические примечания" добавляется запись со ссылкой на
> ADR о рефакторинге. 4. ADR о рефакторинге в Consequences
> явно перечисляет все ADR, которых коснулся переезд.»

**Результат применения скилла v3:**

```
Классификация:
- Измерение: PIPELINE
- Основные категории: L-SKILL + L-CONV PIPELINE
  (Шаг 1(в): процедура («что делать при рефакторинге») —
  L-SKILL; правило применения («какие шаги обязательны») —
  L-CONV.)
- Основной вопрос:
  - L-SKILL: «как выполняется рефакторинг ADR (физический
    переезд + тех. примечания + перечисление в Consequences)?»
- Тег: precedent (методология применима к каждому крупному
  рефакторингу).

Место хранения:
- Целевой файл (L-SKILL): сейчас раздел в `.claude/adr/README.md`.
  Кандидат на `.claude/skills/adr-refactor/SKILL.md` или
  раздел в существующем скилле.
- Целевой файл (L-CONV PIPELINE): то же место сейчас.
- Промежуточное хранение: текущее место.

Связные обновления:
- v2-фрагмент 10 (ADR-0003/0004/0005 не переносятся) — отступает
  от этой процедуры (physical retirement без переезда). Это
  **конфликт по букве** с пунктом 1 («ADR переезжают физически»)
  и снятие пункта 3 («не остаётся блока тех. примечаний — ADR
  удаляется вместе с примечаниями»).
- v2-фрагмент 11 (append-only временно) — bootstrap-исключение,
  легализующее physical retirement.
- Конфликт с существующим ADR (Шаг 3(ж)): **прямой конфликт**
  процедуры в `.claude/adr/README.md` с планируемым поведением
  Этапа 1. Сигнал: процедура должна быть либо расширена
  четвёртой опцией («physical retirement при bootstrap»), либо
  явно отметить, что Этап 1 — исключение.

Развилка конвенции (Шаг 2(ж)):
- Категория классифицирована: L-SKILL.
- Конвенция размещения для случая «процедура работы с ADR
  при рефакторинге» формально есть (раздел в `adr/README.md`),
  но при обнаружении исключения (Этап 1) — не понятно, куда
  фиксировать «исключения из общей процедуры».
- Варианты: (а) разделить общую процедуру (постоянная) и
  исключения (живут в working-with-claude как режимы); (б)
  добавить раздел «Исключения» внутрь самой процедуры.
```

**Сигнал к v4:** фрагмент 12 — это **тот самый** конфликт, на
который указывал v3-сигнал «physical retirement как
четвёртая опция Шага 3(ж)» (пункт 8 раздела «Не адресовано в
v3»). Подтверждает Кластер C. Также: сам фрагмент 12 + конфликт
с v2-фр. 10 — это **L-SKILL под влиянием temporary-rule**.
v4: рассмотреть взаимодействие L-SKILL и temporary-rule
(временные исключения в постоянной процедуре).

### Фрагмент 13: индекс ADR в `.claude/adr/README.md` устарел (содержит 5 из 9 ADR)

**Источник:** `.claude/adr/README.md` строки 95-101 + фактический
состав `.claude/adr/`.

**Цитата:**

> «| Номер | Название | Статус | Дата |
> | 0001 | … | Accepted | 2026-05-14 |
> | 0002 | … | Accepted | 2026-05-14 |
> | 0003 | … | Accepted | 2026-05-15 |
> | 0004 | … | Accepted | 2026-05-15 |
> | 0005 | … | Accepted | 2026-05-15 |»

**Результат применения скилла v3:**

```
Классификация:
- Применение фильтра «знание vs план» (Шаг 1(б)):
  - Это не знание-для-фиксации, это **наблюдение о drift**
    между артефактом и реальностью.
  - → T-Q? T-BL? Это не категория знания, это бы поднял
    Knowledge Curator.
- Измерение: PIPELINE
- Основные категории: (не применимо — это не фрагмент знания,
  это drift)
- Основной вопрос: «индекс отстаёт от каталога; нужно ли
  обновлять или это намеренно?»

Место хранения:
- Целевой файл: исправить индекс в `.claude/adr/README.md`
  или зафиксировать как T-Q «почему индекс остановлен на 0005».
- По состоянию: учитывая v8 §46 (ADR-0003/0004/0005 удаляются)
  и v8 §47 (Этап 1 — bootstrap, append-only временно отключён),
  фактически индекс **не имеет смысла** обновлять до Этапа 1
  — он будет переписан целиком.

Связные обновления:
- ADR-0006..0009 — фактически в каталоге, Proposed-статус, не
  в индексе.
- v8 §«План Этапа 1» предусматривает пересоздание индекса.
- Конфликт с существующим ADR (Шаг 3(ж)): нет.
```

**Сигнал к v4:** ещё один фрагмент-«не-знание-а-drift». Скилл v3
по букве «Когда активируется» — не активируется для drift'а
(это работа Knowledge Curator). Но drift всё равно нужно
куда-то фиксировать. v4: явно вывести drift-наблюдения за
зону скилла, ссылка на Knowledge Curator.

### Фрагмент 14: «Целевые spec-документы в трекере не предзаявляются»

**Источник:** `.claude/planning/migration-tracker.md` строка 14.

**Цитата:**

> «Целевые spec-документы в трекере не предзаявляются. Они
> перечисляются по факту создания — при миграции
> соответствующего legacy-документа.»

**Результат применения скилла v3:**

```
Классификация:
- Измерение: PIPELINE
- Основные категории: L-CONV PIPELINE + L-ADR
  (Шаг 1(б): обоснование «не предзаявлять, чтобы избежать
  дрейфа» с альтернативой → L-ADR; правило «по факту создания»
  → L-CONV.)
- Основной вопрос:
  - L-ADR: «почему трекер не предзаявляет целевые документы?»
- Тег: precedent (паттерн «реестр — отражение, не план»
  применим ко всем реестрам и трекерам — см. фрагмент 8).

Место хранения:
- Целевой файл (L-ADR): ADR-0009 (стратегия миграции) частично.
- Целевой файл (L-CONV PIPELINE): шапка migration-tracker.md —
  где и есть.
- Промежуточное хранение: не нужно.

Связные обновления:
- Связь с фрагментом 8 (MODELS.md — отражение, не гейт) —
  один паттерн «реестр vs план», применённый к двум разным
  реестрам (моделей и миграции). Это пятый случай паттерн-
  конфликта.
- Скилл `spec-cluster-migration` — финализация кластера
  обновляет трекер по факту.

Развилка конвенции (Шаг 2(ж)):
- Категория классифицирована: L-CONV PIPELINE.
- Конвенция размещения для случая «общий паттерн реестров —
  отражение, не план/гейт» отсутствует. Сейчас правило
  формулируется отдельно для каждого реестра (MODELS.md,
  migration-tracker) в их шапках.
- Варианты решения конвенции:
  (а) единый L-CONV `.claude/conventions/registries-policy.md`;
  (б) общая формулировка в working-with-claude;
  (в) оставить отдельно в каждом реестре.
```

**Сигнал к v4:** пятый случай паттерн-конфликта (с фрагментом 8
становится семьёй — два реестра, один общий принцип). v4
кандидат: «общий паттерн N реестров/артефактов» как явная
категория связных обновлений.

### Фрагмент 15: «Auto-staging» convention в CLAUDE.md

**Источник:** `CLAUDE.md`, раздел «Working with Claude Code in
this project».

**Цитата:**

> «**Auto-staging.** После создания или модификации файлов в этом
> проекте, run `git add` на этих файлах, чтобы они появились как
> staged changes в IDEA. Это применяется к созданным файлам,
> модифицированным файлам, и удалениям. Do NOT commit — staging
> only.»

**Результат применения скилла v3:**

```
Классификация:
- Измерение: PIPELINE
- Основные категории: L-CONV PIPELINE
  (одно правило, без альтернатив в формулировке).
- Основной вопрос: «что Claude Code делает с изменёнными
  файлами после правки?»
- Тег: не применяю.

Место хранения:
- Целевой файл: CLAUDE.md как сейчас (хаб M-PIPE + M-NAV +
  L-CONV).
- После рефакторинга: возможно в `.claude/conventions/
  claude-code-workflow.md`.

Связные обновления:
- Принцип 5 working-with-claude («Claude Code не коммитит сам»)
  — согласован, но не упоминает staging.
- playbook раздел «Гигиена работы» — упоминает «не коммитит»,
  не упоминает staging.
- Скилл `spec-document-workflow` — не упоминает.
- Конфликт с существующим ADR (Шаг 3(ж)): нет.

Развилка конвенции (Шаг 2(ж)):
- Категория классифицирована: L-CONV PIPELINE.
- Правило живёт в CLAUDE.md в подразделе «Working with Claude
  Code» — это намеренный смешанный файл (см. v2-corpus-фр. 2).
  Конвенция размещения для случая «правило применяется ко
  всем сессиям Claude Code» в текущей карте — раздел в CLAUDE.md
  или working-with-claude.
- Это **разрешённое размещение** (CLAUDE.md как M-PIPE-хаб), но
  не помечено как раздел вопроса-маркера L-CONV.
```

**Сигнал к v4:** фрагмент чисто работает в текущей карте, но
подтверждает паттерн «L-CONV в M-PIPE хабе». На корпусе:
~5-6 L-CONV-правил живут в CLAUDE.md как разделы (auto-staging,
default model, no-commit, source hierarchy, repository layout).
v4: можно вынести в семейство «L-CONV в хабе» как явный
подвид размещения (без расщепления, просто маркировка).

### Фрагмент 16: «Default model: Opus. Switch to Sonnet for routine code work»

**Источник:** `CLAUDE.md`, раздел «Working with Claude Code in
this project».

**Цитата:**

> «Default model for non-trivial work: **Opus**. Switch to
> Sonnet for routine code work to save budget.»

**Результат применения скилла v3:**

```
Классификация:
- Измерение: PIPELINE
- Основные категории: L-CONV PIPELINE
- Основной вопрос: «какая модель Claude используется по
  умолчанию для каких задач?»

Место хранения:
- Целевой файл: CLAUDE.md как сейчас.

Связные обновления:
- knowledge-curator.md frontmatter: `model: sonnet` —
  применение правила.
- Остальные агенты frontmatter: `model: opus` — применение
  правила (для глубокого анализа).
- Конфликт (Шаг 3(ж)): нет.
```

Чистое размещение (текущая практика). Уйдёт в «Чисто
отработавшие».

### Фрагмент 17: Rule 13 «Default chat response style — short, to the point»

**Источник:** `.claude/project-instructions.md` строки 128-142.

**Цитата:**

> «Baseline behavior in chat is terse: short bullet-style answers,
> "на пальцах". No preamble, no restating the question. No
> wrap-up summary at the end ("so, what I did was…"). … When in
> doubt — shorter.»

**Результат применения скилла v3:**

```
Классификация:
- Измерение: PIPELINE
- Основные категории: L-CONV PIPELINE (для чата)
  + (потенциально L-AGENT — определяет стиль ответа Claude
  как "роли в чате")
- Основной вопрос: «как Claude отвечает по умолчанию в чате
  claude.ai?»
- Тег: precedent (стиль для всех будущих чатов).

Место хранения:
- Целевой файл (L-CONV PIPELINE для чата): project-instructions
  Rule 13 как сейчас.
- Это **место для адресата чат claude.ai**, не для Claude Code
  (см. corpus-фр. 2 — Claude Code и чат claude.ai как два
  разных адресата).
- Промежуточное хранение: не нужно.

Связные обновления:
- Если правила для Claude Code в working-with-claude/playbook
  расходятся со стилем для чата — есть риск.
  (По факту: working-with-claude/playbook не имеют явных
  правил стиля ответа Claude Code; для Code стиль не
  оговаривается.)
- Конфликт с существующим ADR (Шаг 3(ж)): нет.

Развилка конвенции (Шаг 2(ж)):
- Категория классифицирована: L-CONV PIPELINE (для адресата
  «Claude в чате»).
- Конвенция размещения для случая «правило стиля ответа Claude
  по адресату» отсутствует. Сейчас: правила для чата — в
  project-instructions; правила для Claude Code — в CLAUDE.md
  (не явно) или в working-with-claude (опосредованно).
- Связано с corpus-фр. 2 (12 смешанных файлов) и
  фрагментом 2 этого прогона (PK-check имеет двух адресатов).
- Варианты решения конвенции:
  (а) явное разделение `.claude/conventions/style-cc.md` и
      `.claude/conventions/style-chat.md`;
  (б) единый style-файл с разделами по адресату;
  (в) оставить как сейчас.
```

**Сигнал к v4:** третий фрагмент про двух адресатов Claude
(после фрагмента 2 этого прогона и corpus-фр. 2). v4: ввести
явную пометку адресата (CC vs chat) в выходной формат для
PIPELINE-фрагментов.

### Фрагмент 18: Rule 14 «Discussion format for substantive decisions»

**Источник:** `.claude/project-instructions.md` строки 144-169.

**Цитата:**

> «When reviewing documents, working through design forks, or
> discussing substantive choices in chat, use this format:
> Short, focused statement of the question. List of options
> (usually 2-4). Short recommendation. Wait for the user's
> choice — do NOT apply the decision until the user signals
> which option they pick.»

**Результат применения скилла v3:**

```
Классификация:
- Измерение: PIPELINE
- Основные категории: L-CONV PIPELINE (для адресата чат) +
  L-SKILL (формат как процедура)
- Основной вопрос: «как структурируется обсуждение
  содержательного решения?»
- Тег: precedent (формат применим во всех чатах).

Место хранения:
- Целевой файл: project-instructions Rule 14 как сейчас.
- Альтернатива (по Принципу 7): отдельный скилл
  `discussion-format`.
- Промежуточное хранение: не нужно.

Связные обновления:
- playbook Сценарий 1 (создание ADR) — применение формата
  (раскладка альтернатив).
- Конфликт (Шаг 3(ж)): нет.

Развилка конвенции (Шаг 2(ж)):
- Категория классифицирована: L-SKILL/L-CONV.
- То же, что фрагмент 17 — правило для адресата «чат claude.ai»,
  не для Claude Code. Дубль развилки конвенции из фрагмента 17.
```

Связано с фрагментом 17, в отчёте не разворачивать повторно
ту же развилку.

**Сигнал к v4:** тот же, что в фрагменте 17.

### Фрагмент 19: «Один факт — одно место» (атомарность spec-документа)

**Источник:** `.claude/skills/spec-document-workflow/SKILL.md`,
раздел «Базовые принципы (выжимка из ADR-0002)».

**Цитата:**

> «**Один факт — одно место.** Документ не повторяет то, что
> описано в другом — он ссылается через markdown-link с
> относительным путём.»

**Результат применения скилла v3:**

```
Классификация:
- Измерение: PRODUCT (правило для PRODUCT-spec)
- Основные категории: L-CONV PRODUCT
- Основной вопрос: «как избегать дублирования содержания между
  spec-документами?»
- Тег: precedent (применим ко всем PRODUCT-spec).

Место хранения:
- Целевой файл (L-CONV PRODUCT): зафиксирован в ADR-0002 §3 +
  выжимка в skill spec-document-workflow.
- По Принципу 7 — правило в скилле, обоснование в ADR.

Связные обновления:
- ADR-0002 §3 — оригинал правила.
- Скилл — выжимка.
- Конфликт с существующим ADR (Шаг 3(ж)): нет.
```

Чистое размещение. Уйдёт в «Чисто отработавшие».

### Фрагмент 20: «Жанровая чистота — один документ один жанр»

**Источник:** `.claude/skills/spec-document-workflow/SKILL.md`,
раздел «Базовые принципы (выжимка из ADR-0002)».

**Цитата:**

> «**Жанровая чистота.** Один документ — один жанр. Не смешивать
> модель с процессом, маппинг с lifecycle.»

**Результат применения скилла v3:**

```
Классификация:
- Измерение: PRODUCT (правило для PRODUCT-spec)
- Основные категории: L-CONV PRODUCT
- Конфликт с corpus-фр. 2 (намеренные смеси) — фрагмент здесь
  относится к **PRODUCT**, corpus-фр. 2 — к **PIPELINE**.
  Согласовано: ADR-0002 §3 действует для PRODUCT; для PIPELINE
  смеси легитимны.
- Тег: не применяю.

Место хранения:
- Целевой файл: ADR-0002 §3 + skill spec-document-workflow.

Связные обновления:
- Конфликт (Шаг 3(ж)): нет, но согласовать с corpus-фр. 2
  при будущем ADR-PIPELINE про PIPELINE-смеси (фиксировать
  «PIPELINE — отдельный режим»).
```

Чистое размещение для PRODUCT-измерения.

### Фрагмент 21: «Контракт, не реализация» — запрет Java-сниппетов в spec

**Источник:** `.claude/skills/spec-document-workflow/SKILL.md`,
раздел «Базовые принципы».

**Цитата:**

> «**Контракт, не реализация.** Никаких Java-сниппетов. Структура
> — таблицами полей. SQL DDL — в Flyway-миграциях, не здесь.»

**Результат применения скилла v3:**

```
Классификация:
- Измерение: PRODUCT
- Основные категории: L-CONV PRODUCT
- Конфликт с corpus-фр. 9 corpus («не лезет в текущий шаблон —
  стиль / имена / append-only») — частично соприкасается; здесь
  про PRODUCT-spec, там про PIPELINE-spec.

Место хранения:
- Целевой файл: ADR-0002 §6 + skill spec-document-workflow.
```

Чистое размещение. Уйдёт в «Чисто отработавшие».

### Фрагмент 22: Hard gates для закрытия кластера миграции

**Источник:** `.claude/skills/spec-cluster-migration/SKILL.md`,
раздел «Этап 3. Проверка hard gates перед закрытием кластера».

**Цитата:**

> «1. Все legacy-документы кластера имеют MIGRATED-маркер. 2.
> Все целевые spec-документы валидны (проходят чек-лист spec-
> document-workflow). 3. Все Q-N, заведённые в ходе миграции
> кластера, имеют корректную classification и source.»

**Результат применения скилла v3:**

```
Классификация:
- Измерение: PIPELINE (методология миграции)
- Основные категории: L-SKILL + L-CONV PIPELINE
  (Шаг 1(в): процедура — L-SKILL; список обязательных проверок
  — L-CONV.)
- Основной вопрос:
  - L-SKILL: «как закрывается кластер миграции?»
- Тег: не применяю.

Место хранения:
- Целевой файл: skill `spec-cluster-migration` как сейчас.
- Дубль: hard gates перечислены в ADR-0009 §8. Это согласованное
  дублирование (ADR — обоснование, skill — применение по
  Принципу 7).

Связные обновления:
- ADR-0009 §8 — обоснование.
- spec-document-workflow — gate 2 ссылается на этот скилл.
- open-questions-workflow — gate 3 ссылается на этот скилл.
- Конфликт (Шаг 3(ж)): нет; согласованное место L-SKILL.
```

Чистое размещение.

### Фрагмент 23: «Параллельная миграция нескольких документов кластера запрещена»

**Источник:** `.claude/skills/spec-cluster-migration/SKILL.md`,
раздел «Подводные камни», пункт 1.

**Цитата:**

> «Соблазн — гнать сразу два legacy-документа в чате. Запрещено:
> документы внутри кластера обычно связаны, параллельная работа
> создаёт несогласованности.»

**Результат применения скилла v3:**

```
Классификация:
- Измерение: PIPELINE
- Основные категории: L-CONV PIPELINE
- Основной вопрос: «можно ли мигрировать несколько документов
  кластера параллельно?»
- Тег: не применяю.

Место хранения:
- Целевой файл: skill `spec-cluster-migration` раздел
  «Подводные камни».
```

Чистое размещение.

### Фрагмент 24: «Три функции MODELS.md» (навигация + карта для Curator + источник прогресса миграции)

**Источник:** `.claude/skills/spec-models-registry/SKILL.md`,
раздел «Назначение MODELS.md».

**Цитата:**

> «Три функции: 1. Навигационная точка входа в `docs/spec/`.
> 2. Карта созданных model-документов для Knowledge Curator.
> 3. Источник прогресса миграции по моделям.»

**Результат применения скилла v3:**

```
Классификация:
- Измерение: PIPELINE (функции артефакта)
- Основные категории: L-SPEC PIPELINE (метамодель MODELS.md)
  + L-CONV PIPELINE
- Основной вопрос:
  - L-SPEC PIPELINE: «какие функции у артефакта MODELS.md?»
- Тег: не применяю.

Место хранения:
- Целевой файл (L-SPEC PIPELINE): в будущем — `.claude/spec/
  models-registry.md` (по v2-фр. 3, MAP-4 в v8). Сейчас
  функции зафиксированы в skill `spec-models-registry`.

Связные обновления:
- v2-фр. 3 — список метамоделей включает MODELS.md.
- Конфликт (Шаг 3(ж)): нет.

Развилка конвенции (Шаг 2(ж)):
- L-SPEC PIPELINE как каталог не создан — Кластер A для
  семейства метамоделей.
- Тот же случай, что v2-фр. 3 — расширение фрагмента.
```

**Сигнал к v4:** очередной случай метамодели в L-SPEC PIPELINE,
сейчас живущей внутри L-SKILL. Подтверждает v2-фр. 3 и
v3-сигнал «состав L-SPEC PIPELINE».

### Фрагмент 25: «Что НЕ идёт в реестр MODELS.md» — convention о границах

**Источник:** `.claude/skills/spec-models-registry/SKILL.md`,
раздел «Что НЕ идёт в реестр».

**Цитата:**

> «Терминологические соглашения → `terminology.md`. Сквозные
> концепции → собственные spec-документы. Имена джоб, сервисов,
> executor'ов → process-документы. Вложенные модели → разделами
> внутри model-документа. Прогресс миграции → migration-tracker.
> Открытые вопросы по моделям → open-questions.»

**Результат применения скилла v3:**

```
Классификация:
- Измерение: PIPELINE (правило применения реестра)
- Основные категории: L-CONV PIPELINE
- Основной вопрос: «что не относится к реестру моделей и где
  тогда живёт?»
- Тег: не применяю.

Место хранения:
- Целевой файл: skill `spec-models-registry`.

Связные обновления:
- Дубль с правилом «новый термин — куда фиксировать» в
  working-with-claude Принцип 4 — частично пересекается, но
  здесь больше деталей про границы реестра.
- Конфликт (Шаг 3(ж)): нет.
```

Чистое размещение.

### Фрагмент 26: «Битые ссылки после переезда не обновляются»

**Источник:** `.claude/adr/README.md`, раздел «Битые ссылки после
переезда целевых файлов».

**Цитата:**

> «Ссылки внутри принятого ADR не обновляются. Они остаются как
> есть — это часть исторического контекста. Читатель ориентируется
> по CLAUDE.md и `docs/README.md` для актуальных путей.»

**Результат применения скилла v3:**

```
Классификация:
- Измерение: PIPELINE
- Основные категории: L-CONV PIPELINE
  (одно правило, без обоснования с альтернативой).
- Основной вопрос: «что делать со ссылками внутри ADR после
  переезда целевых файлов?»
- Тег: precedent (применимо ко всем append-only артефактам с
  ссылками).

Место хранения:
- Целевой файл: `.claude/adr/README.md` как сейчас.

Связные обновления:
- Принцип append-only (working-with-claude) — следствие.
- v2-фр. 11 (append-only временно) — для Этапа 1 это правило
  снимается вместе с самим append-only.
- Конфликт (Шаг 3(ж)): нет; согласовано.
```

**Сигнал к v4:** аналогично фрагменту 12 — правило, имеющее
исключение для Этапа 1. Та же тема: «постоянная convention +
временные исключения». См. сигнал к фрагменту 12.

### Фрагмент 27: «v8 запрет — Не использовать тег `precedent` без явного критерия»

**Источник:** `.claude/strategy-summary/strategy-summary-v8.md`,
раздел «Что НЕ делать», строки 568-574.

**Цитата:**

> «Не использовать тег `precedent` (в скилле `knowledge-
> placement`) без явного критерия. В V2 тег сработал на 9/12
> фрагментов — чрезмерно. До решения KP-3 — применять только
> когда фрагмент **явно** проектирует образец для будущих
> похожих случаев … На уровне процедуры: спросить себя "какие
> конкретно будущие фрагменты будут опираться на этот
> прецедент?"; если ответа нет — не ставить тег.»

**Результат применения скилла v3:**

```
Классификация:
- Измерение: PIPELINE
- Основные категории: L-CONV PIPELINE (временное правило для
  работы со скиллом) + L-ADR (обоснование ужесточения)
- Основной вопрос:
  - L-ADR: «почему критерий precedent ужесточается, а не
    остаётся как в v2?»
- Тег: **temporary-rule** (действует до решения KP-3 / v3
  скилла, см. v8 §«Открытые вопросы»). Триггер возврата —
  принятие v3/v4 скилла.

Место хранения:
- Целевой файл: v8 раздел «Что НЕ делать» как сейчас. После
  принятия v3 (фактически уже принят, см. SKILL.md строка 195)
  — фрагмент переезжает в SKILL.md как формальное правило тега.
- Промежуточное хранение: текущее место в v8.

Связные обновления:
- SKILL.md строка 195 («тег precedent применяется строго»)
  частично закрывает это правило.
- v3-сигнал к v4 пункт 9 — калибровка тегов остаётся открытой.
- Конфликт (Шаг 3(ж)): нет; v8 явно сформулирован как мост к
  v3.

Развилка конвенции (Шаг 2(ж)):
- Категория классифицирована: L-CONV PIPELINE (temporary).
- Конвенция размещения для «временное правило работы со
  скиллом» отсутствует. Сейчас сидит в v8 «Не делать», но
  v8 — T-SNAP, не место постоянного хранения правил.
- Связано с corpus-фр. 11 (append-only временно) — общий
  паттерн temporary-rule без специального места хранения.
  Подтверждает v3-сигнал к v4 пункт 9 (статус L-SCRATCH) и
  пункт о месте temporary-rule (corpus-фр. 11 сигнал).
- Варианты решения конвенции:
  (а) temporary-rules.md в `.claude/conventions/` (см. v8
      OQ KP-2);
  (б) inline в скилле с пометкой;
  (в) в snapshot как сейчас (компромисс).
```

**Сигнал к v4:** третий **temporary-rule** на разных корпусах
(v2-фр. 11 append-only; v8 «Не пересматривать карту до Этапа 1»;
v8 «Не использовать precedent без критерия» — этот). Все три
**сейчас живут в разных местах**: working-with-claude,
strategy-summary v8 «Не делать», SKILL.md. Это **системный
паттерн без места**. v4: место хранения temporary-rule — это
не один из подвидов Шага 2(ж), а **отдельный жанр L-CONV**.

### Фрагмент 28: «v8 запрет — Не записывать решения Этапа 1 в evolution-log до исполнения»

**Источник:** `.claude/strategy-summary/strategy-summary-v8.md`,
раздел «Что НЕ делать», строки 586-589.

**Цитата:**

> «Не записывать решения Этапа 1 рефакторинга в `pipeline-
> evolution-log.md` до фактического исполнения. Лог — для
> **сделанного**, не для планируемого. План живёт в snapshot v8.»

**Результат применения скилла v3:**

```
Классификация:
- Измерение: PIPELINE
- Основные категории: L-CONV PIPELINE
  (одно правило-уточнение жанра T-EVO).
- Основной вопрос: «что считать "сделанным" в T-EVO?»
- Тег: не применяю (это уточнение жанра, а не precedent).

Место хранения:
- Целевой файл: сейчас в v8 «Не делать». По существу — это
  уточнение характеристического вопроса T-EVO («как менялся
  пайплайн, в смысле **факт**, не план»).

Связные обновления:
- pipeline-evolution-log сам содержит правило «Сюда пишутся
  **сделанные** изменения, не запланированные» (строка 8) —
  уже согласовано. **Дубль**.
- v3-сигнал к v4 пункт 10 — T-EVO как основная категория и
  двойное толкование её характеристического вопроса.
- Конфликт (Шаг 3(ж)): нет; v8 повторяет правило, уже
  зафиксированное в самом T-EVO.

Развилка конвенции (Шаг 2(ж)):
- Категория классифицирована: L-CONV PIPELINE.
- Конвенция размещения корректна — pipeline-evolution-log
  правило в шапке.
- Но дубль в v8 «Не делать» — сигнал, что правило **повторяется
  как памятка**, а не как место хранения. Это согласованный
  дубль, не разрыв.
```

**Сигнал к v4:** ещё один кейс согласованного дубля (см.
фрагмент 5 этого прогона). Подтверждает паттерн «правило-в-
артефакте + памятка-в-snapshot». v4: можно явно описать в
карте этот паттерн.

### Фрагмент 29: «v8 запрет — Не вводить новые жанры L-SPEC PIPELINE сверх обоснованных»

**Источник:** `.claude/strategy-summary/strategy-summary-v8.md`,
раздел «Что НЕ делать», строки 576-580.

**Цитата:**

> «Не вводить новые жанры L-SPEC PIPELINE сверх обоснованных в
> §44 [жанр `format` отвергнут]. … Если при работе с метамоделями
> всплывёт ещё один кандидат на жанр — не вводить молча; сначала
> зафиксировать как **OQ** и обсудить отдельным событием.»

**Результат применения скилла v3:**

```
Классификация:
- Измерение: PIPELINE
- Основные категории: L-CONV PIPELINE + L-ADR
  (Шаг 1(б): обоснование «не расширять молча» с альтернативой
  «расширять по ходу» → L-ADR; правило применения «фиксировать
  как OQ» → L-CONV.)
- Основной вопрос:
  - L-ADR: «как контролируется состав жанров L-SPEC PIPELINE
    в фазе формирования?»
- Тег: temporary-rule (действует пока L-SPEC PIPELINE
  не зафиксирован).

Место хранения:
- Целевой файл: v8 «Не делать» сейчас. После принятия
  ADR-PIPELINE про L-SPEC PIPELINE — переедет в правила
  ведения слоя.

Связные обновления:
- v8 §44 (отвергнутый жанр format) — обоснование.
- v2-фр. 5 (negative-decision на format) — то же.
- Конфликт (Шаг 3(ж)): нет.
```

**Сигнал к v4:** четвёртый temporary-rule. См. фрагмент 27 сигнал.

### Фрагмент 30: ADR-0009 — единица миграции «legacy-документ», а не «модель»

**Источник:** `.claude/adr/0009-domain-migration-strategy.md`
(не цитирую полностью — глубоко в ADR; известно по
evolution-log от 2026-05-18 и v8).

**Цитата (по evolution-log):**

> «Переработка `spec-document-migration` и `spec-cluster-migration`
> под алгоритм "единица миграции — legacy-документ; тесты типа
> сущности; заглушка + Q-N".»

**Результат применения скилла v3:**

```
Классификация:
- Измерение: PIPELINE
- Основные категории: L-ADR
- Основной вопрос: «что является единицей миграции —
  legacy-документ или модель?»
- Тег: precedent (определяет единицу для всех миграций).

Место хранения:
- Целевой файл: ADR-0009 §«Единица миграции» (предположительно).
- L-SKILL: применение в `spec-document-migration`.

Связные обновления:
- skill `spec-cluster-migration` — оркестрация.
- ADR-0009 → ADR-PIPELINE-0005 в плане Этапа 1.
- Конфликт (Шаг 3(ж)): нет.
```

Чистое размещение (известное историческое решение, не требует
расширения). Включён в отчёт только для протокола.

## Чисто отработавшие фрагменты

- «Один чат — одна задача» (источник: playbook, раздел «Гигиена
  работы») — PIPELINE / L-CONV — playbook M-USER хаб.
- «Default model: Opus / Sonnet для рутины» (источник: CLAUDE.md,
  Working with Claude Code) — PIPELINE / L-CONV — CLAUDE.md
  как M-PIPE хаб.
- «Один факт — одно место» (источник: skill spec-document-workflow,
  Базовые принципы) — PRODUCT / L-CONV — ADR-0002 + skill.
- «Жанровая чистота — один документ один жанр» (источник:
  тот же) — PRODUCT / L-CONV — ADR-0002 + skill.
- «Контракт, не реализация» (источник: тот же) — PRODUCT / L-CONV
  — ADR-0002 §6 + skill.
- Hard gates кластера (источник: skill spec-cluster-migration,
  Этап 3) — PIPELINE / L-SKILL — skill + ADR-0009 §8.
- «Параллельная миграция документов кластера запрещена»
  (источник: skill spec-cluster-migration, Подводные камни) —
  PIPELINE / L-CONV — skill.
- «Что НЕ идёт в реестр MODELS.md» (источник: skill
  spec-models-registry) — PIPELINE / L-CONV — skill.
- ADR-0009 «единица миграции — legacy-документ» — PIPELINE /
  L-ADR — ADR-0009 + skill.

## Сводные наблюдения для v4

1. **Кластер B (классификация — позитивный тест на T-Q)
   подтверждается.** На широком корпусе фрагменты 9, 10 уходят
   в T-Q через тот же отрицательный сигнал «не план + не
   утверждение». На v2-corpus прогоне это уже было (фрагменты
   3, 7, 8, 15). Семь случаев — устойчивый сигнал. v4-правка
   нужна.

2. **Кластер C (паттерн-конфликт) подтверждается мощно.**
   На широком корпусе пять новых случаев:
   - Фрагмент 4 (Knowledge capture rules в 5 агентах).
   - Фрагмент 7 (список реализационных классов в 3 артефактах).
   - Фрагмент 8 (MODELS.md — отражение, в 5 артефактах).
   - Фрагмент 14 (трекер не предзаявляет, в 2 артефактах + общий
     паттерн с фр. 8).
   - Фрагмент 17/18 (правила стиля для чата — фрагмент в одном
     месте, но **раздвоен по адресату**).
   Вместе с corpus-фрагментами 4-6 (ADR содержат побочные
   продукты) — восемь паттерн-конфликтов. Это уже не «накопить
   статистику», а устойчивая категория. v4-правка обязательна.

3. **Новый сигнал: «два адресата Claude» (CC vs chat) как
   неявная развилка размещения.** Фрагменты 2 (PK check),
   17 (стиль чата), 18 (формат обсуждения) — все три
   подразумевают разделение L-CONV/L-SKILL по адресату. Сейчас
   это разделение **неявное** (CLAUDE.md/working-with-claude/
   playbook — преимущественно для Claude Code; project-instructions
   — преимущественно для чата). v4 кандидат: ввести явную
   пометку адресата в выходной формат для PIPELINE-фрагментов.

4. **Новый сигнал: «фрагмент одновременно PIPELINE и PRODUCT».**
   Фрагмент 1 (Принцип 6 — дока ведёт код) — применяется и при
   работе со spec (PRODUCT), и при работе с кодом (тоже PRODUCT,
   но методологически — PIPELINE). v3 в Шаге 1(а) даёт три
   варианта (PIPELINE / PRODUCT / развилка); опция «оба
   одновременно» не предусмотрена. v4: добавить «измерение:
   both» как явный случай.

5. **Temporary-rule подтверждён как структурный класс без
   места хранения.** На широком корпусе три новых случая:
   - Фрагмент 27 (precedent без критерия — действует до v3
     скилла).
   - Фрагмент 29 (не вводить новые жанры L-SPEC PIPELINE —
     действует до фиксации ADR).
   - Кратко в v8 «Не пересматривать карту до Этапа 1» (covered
     паттерном corpus-фр. 11).
   Вместе с corpus-фр. 11 — четыре кейса. Все живут в разных
   местах (working-with-claude, snapshot, snapshot). v4: жанр
   временных правил — отдельное место хранения (см. KP-2 в v8).

6. **Тег `precedent` после ужесточения: 12/30 ≈ 40%.** На v2 было
   9/12 (75%), на v2-corpus 9/18 (50%), на v3-регрессии 4/4
   (100% — но эти фрагменты по природе precedent), на этом
   широком прогоне 12/30 (40%). Динамика снижения подтверждается.
   На широком корпусе 40% — приемлемо для тега, который маркирует
   «структурно важное». Если v4 сузит критерий ещё (например,
   до «явно проектирует образец, перечислены 2+ конкретных
   будущих кейса»), ожидается ~20-25%. Сигнал к v4: пересмотр
   `precedent` на v4 — нужен, но не срочен.

7. **Теги `negative-decision` и `temporary-rule` — на новом
   корпусе:** `temporary-rule` сработал на 3 фрагментах (27, 29,
   v8 «Не пересматривать»). `negative-decision` — ноль. Тег
   `temporary-rule` устойчив. Тег `negative-decision` редкий, но
   виден из v2-corpus (фр. 4, 5) — удерживать.

8. **L-SCRATCH (статус `.claude/notes/`) остаётся открытым.**
   На корпусе note `model-catalog-2026-05-15.md`, `migration-
   inventory-2026-05-15.md` и другие — все живут как L-SCRATCH
   без статуса. Шаг 2(г) v3 опирается на это место,
   Шаг 2(ж) частично закрыл вопрос (явно отметив отсутствие
   конвенции). Конкретное решение — за ADR-PIPELINE, не за
   скиллом. v4 не нужно — ждать ADR.

9. **T-EVO как основная категория — подтверждается.** corpus-
   фр. 18 (тех. примечания как встроенный журнал) + фрагмент 28
   этого прогона (правило «лог для сделанного, не для плана»).
   Двойное толкование характеристического вопроса T-EVO («как
   менялся пайплайн» = факт изменения vs носитель исторического
   контекста) подтверждается. v4 правка нужна.

10. **Новый паттерн: «согласованный дубль для разных адресатов /
    разных целей».** Фрагмент 5 (правило «думаешь — чат»
    дублировано в 3 местах согласованно), фрагмент 28 (правило
    T-EVO дублировано в шапке + в v8 «Не делать»). Это не
    паттерн-конфликт (Кластер C) — это **сознательный дубль**.
    v4: возможно ввести явный тип связного обновления «дубль-
    sync» отличный от «конфликт» и «уточнение».

### Заполненность полей выходного формата (на 30 подозрительных
+ 9 чистых = 39 фрагментов)

| Поле | Заполнено на фрагментах | Доля |
|---|---|---|
| Несколько основных категорий | 25 из 39 | 64% |
| Промежуточное хранение | 21 из 39 | 54% |
| Тег `negative-decision` | 0 | 0% |
| Тег `temporary-rule` | 3 (27, 29 + 1 в кратком) | 8% |
| Тег `precedent` | 12 | 31% |
| Открытые расширения карты | 14 | 36% |
| Конфликт с ADR/convention (Шаг 3(ж)) | 7 (паттерн-конфликты учитываются как 1 на фрагмент) | 18% |
| Уточняющие вопросы пользователю | 0 | 0% |
| **Развилка конвенции (Шаг 2(ж) v3)** | **13** | **33%** |

**Сигналы по полям:**

- **Развилка конвенции (Шаг 2(ж)) — 33%.** Это значимый показатель.
  v3-правка работает не только на Кластере A (4/4 — corpus-фрагменты
  2, 9, 10, 13), но и на множестве других фрагментов (8 новых случаев
  на широком корпусе). Срабатывает регулярно — поле не пустое.
- **Несколько основных категорий — 64%.** Подтверждает решение
  v2 разрешить множественные категории.
- **Тег `negative-decision` — 0% на широком корпусе.** Удерживать,
  но не оптимизировать.
- **Тег `precedent` — 31%.** Снижение продолжается. v4-правка
  желательна.
- **Уточняющие вопросы пользователю — 0%.** Все развилки
  закрываются процедурно (Шаги 2(г)/2(д)/2(ж)/3(ж)) или уходят в
  «Открытые расширения карты». Это сигнал устойчивости v3 — нужды
  останавливаться и спрашивать пользователя не возникало.

## Вопросы к пользователю

Вопросов, на которые нельзя ответить чтением репозитория, не
возникло. Все развилки закрываются процедурой v3 или явно
отправляются в «Открытые расширения карты» / «Развилка
конвенции» / «Не адресовано в v3» как сигналы для последующих
итераций пакета ADR-PIPELINE и скилла.
