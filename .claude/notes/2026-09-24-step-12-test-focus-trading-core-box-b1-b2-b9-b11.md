# Фокус `test` шага 12: проход по группам B1-B2 и B9-B11 ящика `trading-core`

## На какой вопрос отвечает этот файл

Какие находки дал проход фокуса `test` по группам B1-B2 и B9-B11 чёрного ящика `trading-core` 2026-09-24?

## Статус

Вход отчёту фокуса `test`, а не отчёт. Два прохода — субагенты захода 147;
вернулись после конверта сессии. Находки проверены ими чтением теста и
продуктового кода; заходом, пишущим отчёт, перепроверяются. Пути — от
`services/trading-core/src/test/java/com/example/tradingcore/box/`; `док:N` —
строки `.claude/tests/cases/trading-core.md`. Покрытие меток полное (кроме
объявленных непрогоняемыми B2.11, B2.15, B2.18, B9.9).

## Находки — B1-B2

1. **B2.2 — невалидность, подогнан, торгово значимый.** `DealPassWindowBoxTest.java:81-97`
   (док:365-373): дом (`application.yaml:196-198` «не попавшие в окно
   подбираются следующим») обещает подбор следующим тиком, а код
   (`findByStatusNotInOrderByIdAsc(…, PageRequest.of(0, limit))`) берёт то же
   окно; тест это закрепляет. Сделки за окном не сопровождаются, пока первые
   нетерминальны. Правка: ожидание дома под `debt`, находка владельцу.
2. **B2.12, B2.13 — невалидность, F-4 закреплён контрактом.** `DealPassBoxTest.java:295-301,
   320-331` (док:469-483, :2105): `503` соседа — ждут `ERROR`, дом
   (`docs/rules/runtime-error-classification.md`) требует пропуска; B3.9
   (`DealCommandBoxTest:214-230`, `debt`) на том же входе ждёт `ACTIVE`. Правка:
   ронять сборку ответом 4xx (`PeerReadException`).
3. **B2.16 — невалидность, спорно.** `DealPassBoxTest.java:378-381`: пинит `202`
   без `debt`, а док:2079 объявляет её долгом (`409`).
4. **B2.3 — невалидность, спорно.** `:137-153`: `DealContextService.build` к
   коннектору не ходит — отказ кредов сборку уронить не может; порядок не различён.
5. **B2.10 — невалидность, спорно.** `:268-285`: зелена и при прогоне FSM.
6. **B2.9 — невалидность.** `:250-262`: «заявок не снимает» — 0 против 0.
7. **B2.17 — невалидность частичная.** `:398-407`: «до обработчика» не различено.
8. **B1.11 — невалидность, спорно; детерминизм.** `EntryScanBoxTest.java:309-329`:
   порядок счетов (`findByStatusAndSafetyRung`) не задан и не пинится.
9. **B1.3 — изоляция.** `EntryScanBoxTest.java:139, 421-427`: сделка в `ACTIVE`
   прямой записью при существующей тропе.
10. **B2 целиком — изоляция.** `DealPassBoxTest.java:149, 170, 186, …`: статус и
    причины читаются из базы, хотя `DealApiResponse` их отдаёт (док:74-78).
11. **Перечень прямых записей — пробел дока** (док:1984-1991): не называет B1.3,
    B2.1 `setStatus` (`:110-113`), B2.2 `CLOSED` (`:92`), B2.17 (`:655-663`).
12. **B1.8 — пробел дока** (док:274): клетка «сделки нет», тест ждёт сделку.
13. **Мелкое.** B2.1 — `EMERGENCY_CLOSED` без проверки; B1.1 — `startsWith` без
    смещения; B2.7 — нет следа второго прохода; B2.2 — `isPositive` вместо
    точных; B1.13 — стаб `auth` не проверен.

## Находки — B9-B11

14. **B11.7 — невалидность, долг закреплён контрактом.** `SurfaceReadBoxTest.java:224`
    (док:1609, :2079-2081): `carriesErrorDto()).isFalse()`. Правка: `isTrue()`
    под `debt`, как B12.1, B12.2.
15. **B11.3, B11.6 — невалидность.** `SurfaceReadBoxTest.java:133, 198`
    (док:144-149): пинят `400` своего обработчика, ожидание — отрицание `500`.
16. **B10.4 — невалидность.** `ProjectionsBoxTest.java:126-151`: отказывающий
    инструмент последним — «проход не роняет» не измерено.
17. **B10.5 — невалидность.** `:178`: 0 против 0.
18. **B10.6 — невалидность.** `:200`: тип сверяется с `SWAP`, равным
    `Feed.INSTRUMENT_TYPE` — константа прошла бы.
19. **B9.7 — невалидность, спорно.** `OutboxRelayBoxTest.java:161-172`:
    `isInstanceOf(IllegalStateException.class)` на любой ошибке SQL.
20. **B10.3 — пробел покрытия.** `ProjectionsBoxTest.java:103-119`: отказ гейта
    свежести не проверен.
21. **B9.5, B10.3 — изоляция, пробел дока.** Сделка через `rows.insert` вне
    объявленного перечня.
22. **B11 — пробел дока.** Javadoc `SurfaceReadBoxTest.java:22-27` и
    `Rows.java:79-88` расходятся с док:1984-1989.
23. **B9.1 — пробел дока** (док:1333-1334); **B9.4 — спорно** (порядок меряет
    B9.5); **B9.8 — спорно** (строк 4 при окне 200).
24. **B10.7 — детерминизм.** `ProjectionsBoxTest.java:211-223`: `findByStatus`
    не упорядочен.
25. **Мелкое.** B9.3 — значения заголовков не сверены; B10.8, B11.5 — половина
    выхода; B10.4 — `isGreaterThan` вместо `+1`.
26. **Форма.** `.equals` на переменной — `OutboxRelayBoxTest.java:218`,
    `RelayWindowBoxTest.java:98`, `ProjectionsBoxTest.java:390`;
    `!MIGRATION_JOURNAL.equals(…)` — `Rows.java:71, 227`.

## Охват прохода

Прочитаны целиком `EntryScanBoxTest`, `EntryScanWindowBoxTest`,
`DisabledEntryScanBoxTest`, `DealPassBoxTest`, `DealPassWindowBoxTest`,
`OutboxRelayBoxTest`, `RelayWindowBoxTest`, `UnavailableBrokerBoxTest`,
`ProjectionsBoxTest`, `SurfaceReadBoxTest`, `TradingCoreBox`, `Rows`. Моков бинов
и `sleep` нет. Группы B3-B4, B7-B8, B12-B14 этой заметкой не покрыты.
