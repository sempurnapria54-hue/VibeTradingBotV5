# Snapshot v98

**Дата:** 2026-09-01.

## На какой вопрос отвечает этот файл

Где мы сейчас. **`GAPS_CLOSE_33` закрыт целиком: два решения держателя
исполнены, все 30 гейтящих находок закрыты, десять узлов чисты по обоим
предметам мини-петли, исполнимый критерий выхода в `CODE` пройден
впервые (30/30); гейт `CODE` держит второе условие — слоты грунта
пп. 2, 7, 16.** Сменяет v97.

## Состояние

Фаза 1 — `IN_PROGRESS`; шаги 1-6 `DONE`, шаг 7 — `DOCS_CHECK_34`
(закрытие `_33` завершено, следующий прогон не начат), шаги 8-11 `HOLD`.
Ветка `claude-audit`, `HEAD = 8859279`; вся дельта — в рабочем дереве
**staged, не закоммичена**.

**Режим — автономия до конца фазы 1**; решения этой сессии — Д227-Д243
в дайджесте.

## Что произошло

- **Два решения держателя по эскалации `_33`** —
  `.claude/decisions/measurement-repair-not-extension.md` (минимальный
  ремонт исполнимости, узлы 1/7/8 разблокированы; derive-дисциплина на
  перечни вне спек не распространяется) и
  `.claude/decisions/recovered-deal-linkage-window-bound.md` (граница
  окна восстановленной сделки — биржевое время открытия наблюдённой
  позиции; `external_created_at` остаётся nullable).
- **`GAPS_CLOSE_33`** — все 30 гейтящих закрыты, 12 прочих закрыто,
  17 припарковано с владельцами; пять снятых редакций — в реестре с
  популяциями. Отчёт (популяции узлов, пробы, исходы мини-петель) —
  `.claude/work/progress/phase-1-step-7-gaps-close-33.md`; хроника —
  chronicle шага.

## Что теперь держит механизм

- **Вся машинерия корпуса исполняется голым запуском с объявленной
  Windows-среды** (дом фактов среды —
  `.claude/tests/source-api/okx/code-preconditions.md` §«Среда контура»);
  незапуск JVM — код 2; препятствия базового гейта — на штатной тропе.
- **Реестр диспозиции** `.claude/work/code-gate-ledger.json` — 30/30
  закрыты, `code-gate-check.py` — ПРОЙДЕН.
- **Реестр снятых редакций** — 15 записей, 69 строк популяций.

## Чем проверяется корпус — десять команд

Все десять зелены голым запуском (числа печатают прогоны; ориентир:
554 примера, 6 популяций/64 члена, 227 указателей, 531 адрес/долг 56,
15 записей реестра). Код возврата 2 = «не измерялось».

## Что ратифицировано и не пере-решается

Всё из v97, плюс:

- **Граница окна восстановленной сделки — время открытия наблюдённой
  позиции; писатель — `DealOpeningService`** (решение держателя).
- **Ремонт исполнимости — не усиление измерения**; новые оси/формы
  детекторов заморожены до `CODE`, парковки поимённы (решение держателя).
- **Разбор истории цикла добычи защиты — три ноги по терминалам
  контракта; вторая ступень — после ноги живых; активацию обеих троп
  предъявления пишет один предикат.**
- **Транш `ENTRY_SUBMITTED` под сворачиванием уходит своим выходом.**
- **При двух стоящих радиусах `shutdownReason = EXCHANGE_HOLD`.**
- **Повтор отказавшей надобности гейтится стоящей ступенью.**
- **Блок-сет safety-ступени — код `INSTRUMENT_SAFETY_HOLD`, реакция —
  карв-аут.**
- **Популяция предиката стороны — вся первичная постановка, вход
  включая; рубежи различаются предметом.**
- **Слот AG6.2 — двухшаговый контракт: кандидаты (перепрогон) → состав
  (семантическая разметка, хэнд-офф `integrator`).**

## Что висит

1. **`DOCS_CHECK_34`** — прогон следствий закрытия `_33` по действующему
   мандату (линзы малыми партиями, отчёты инкрементально). Детектор:
   С2 (окно набрано: `_33` 53-63 % — если `_34` даст ≥ половины,
   поднимется), С3 (величина записана, 79 % при пороге 70 % — окно три
   прогона, `_34` второй), С1/С4 — окна живые.
2. **Гейт `CODE`, условие 2** — слоты грунта: п. 2 (перепрогон `AG6.2` →
   кандидаты; затем разметка состава — `integrator`), п. 7 (граница
   funding-интервала), п. 16 (отображение категорий). Владелец —
   `tester`.
3. **Держателю — три числа конфигурации при запуске** (риск на сделку,
   множитель потолка, предел серии убытков).
4. **`CODE`-дельта** — backlog §«Шаг 7 (сделки и P&L) — исполнительный
   хвост»; синхронизирована с закрытием (цикл добычи, триггер
   `ENTRY_SUBMITTED`, ось покрытия).
5. **Припарковано** — хвосты починки `_33`, форма 7 детектора
   указателей, ось актуальности реестра (`.claude/work/backlog.md`
   §«Средовой дефицит автономного RUN тестов (контур source-api)»,
   возврат — вход в `CODE`); прочие `_33` — там же,
   §«Припаркованные прочие находки `DOCS_CHECK_33`».
6. **Адресный долг:** `.claude/**` — 56 под храповиком; `docs/**` — 62.
7. **Гигиена и распил гигантов** — курационный заход после закрытия
   шага; туда же G-3/G-4/G-10/G-11.
8. **Пользователю — обновить Project Knowledge:** этот снапшот, дайджест
   (Д221-Д243), два решения держателя.

## Среда

Windows-хост (Git Bash, `py -3`), Docker-контур, Vault не-dev; **дом
фактов среды один** — `.claude/tests/source-api/okx/code-preconditions.md`
§«Среда контура — проверено прогоном, не выведено». Vault-токен
`.env.vault.test.local` протух (перевыпуск — шаг 9); `mvnw` в репо нет.
