# Snapshot v27

**Дата:** 2026-06-04.

## На какой вопрос отвечает этот файл

Где мы сейчас и как сюда пришли (срез после верификации правки
«плечо динамическое» (`DOCS_CHECK_7` — чисто) и закрытия
`GAPS_CLOSE_7`; шаг 2 готов к `CODE`, перед `CODE` — торговый совет
в отдельном чате).

## Состояние

Фаза 1 — `IN_PROGRESS`; шаг 1 — `DONE`; **шаг 2 (Стратегия) —
`GAPS_CLOSE_7`** (закрыт); шаги 3-11 — `HOLD`. Цепочка шага 2:
`DOCS_CHECK_1..5` / `GAPS_CLOSE_1..5` → `DOCS_CHECK_6` (чисто, шаг
готов к `CODE`) → `GAPS_CLOSE_6` (правка «плечо динамическое» по
решению чата 2026-06-04) → `DOCS_CHECK_7` (верификация правки —
чисто) → `GAPS_CLOSE_7` (два хвоста закрыты).

## Что изменилось относительно v26

### Шаг 2 дочищен: DOCS_CHECK_6 — чисто

Шестая проверка верифицировала `GAPS_CLOSE_5` (условие → JSONB на
`strategy_step`; гигиена М1-М3): применено согласованно, новых
пробелов 0, блокирующих вопросов 0. Итерации
`DOCS_CHECK`/`GAPS_CLOSE` по исходному материалу шага завершены.

### Плечо динамическое (решение чата 2026-06-04 — применено и верифицировано)

`GAPS_CLOSE_6`: `docs/rules/trading-constraints.md` — рабочее плечо
выводится **на сделку** из торговых правил (риск на сделку,
инвариант «ликвидация за стопом») и рыночных условий
(волатильность); **наших потолков нет**, единственный жёсткий предел
— биржевой `InstrumentExternalRules.externalMaxLeverage`. Снят
`StrategyDetail.maxLeverage`; `LEVERAGE_EXCEEDED` убран из
`RiskCheckCode` (`EXCHANGE_MAX_LEVERAGE_EXCEEDED` сохранён);
anomaly-кейс и адаптерные сверки перенацелены на
`externalMaxLeverage`. Семь файлов.

`DOCS_CHECK_7` верифицировал: правка легла согласованно, остаточных
ссылок на снятый потолок в `docs/` — 0, новых doc↔doc хвостов — 0.
Формула вывода плеча сознательно **не достроена** — логика
риск-движка, шаг 5. Осталось два негейтящих хвоста → `GAPS_CLOSE_7`.

### GAPS_CLOSE_7 — два хвоста закрыты

- **INSTR-Q2 освежён (не решён):** снята устаревшая премисса про
  конфиговый максимум плеча; добавлен аспект статика↔динамика —
  нужна ли статическому `Instrument.leverage` роль рабочего плеча
  при динамическом выводе на сделку, кто и когда выставляет плечо
  на бирже. Заголовок теперь — «Роль статического плеча `Instrument`
  при динамическом рабочем плече». Проработка — риск-движок (шаг 5)
  / торговый совет.
- **Нейминг runtime-полей `Position` выровнен:**
  `mapping/Position.md` — пять полей приведены к `external*`-префиксу
  конвенции `domain/core/Position.md` (`externalAverageEntryPrice`,
  `externalMarkPrice`, `externalLiquidationPrice`, `externalMargin`,
  `externalUnrealizedProfit`).

Прогресс — `phase-1-step-2-docs-check-6.md` / `docs-check-7.md` /
`gaps-close-7.md`. В таблице `phase-1.md` зафиксирован и закрыт
рассинхрон: статусы `GAPS_CLOSE_6` / `DOCS_CHECK_7` в неё не
проставлялись.

## Активные задачи

- **Шаг 2, `GAPS_CLOSE_7`** — закрыт. Итерационные progress-файлы
  шага 2 остаются в `progress/` до `DONE` шага.

## Текущий фронтир / следующее действие

**Шаг 2 готов к `CODE`, но перед `CODE` — торговый совет** (в
отдельном чате):

1. Спроектировать сам совет: как устроен (чат vs код), источники
   (книги + интернет), роль/триггер.
2. Совет валидирует торговую модель и задаёт вопросы — закрываем их.
3. Только после этого — `CODE` шага 2.

Коммит — за пользователем (все правки сессии staged, CC не коммитит).

## Открытые общие вопросы

`open-questions.md`: **15** (без изменений; INSTR-Q2 освежён, не
закрыт).

## Что в работе / PK

- Шаг 2 — `GAPS_CLOSE_7` закрыт; дальше торговый совет → `CODE`.
- **Project Knowledge:** последний снапшот теперь **`snapshot-v27`**
  (заменяет v26 в префлайте). `structure.md` / `naming.md` /
  `CLAUDE.md` / `place-knowledge.md` в этой сессии **не менялись** —
  обновить только указатель на снапшот.
- Затронуто в `GAPS_CLOSE_7` (всё staged):
  `.claude/work/questions/open-questions.md`,
  `docs/models/mapping/Position.md`,
  `.claude/work/roadmap/phase-1.md`,
  `.claude/work/progress/phase-1-step-2-gaps-close-7.md`, этот
  снапшот. Рядом staged с прошлых под-шагов: правка плеча (7 файлов
  `docs/`), `docs-check-6.md` / `docs-check-7.md`.
