# Snapshot v11

**Дата:** 2026-05-28.

## На какой вопрос отвечает этот файл

Где мы сейчас и как сюда пришли (срез на дату запуска прохода 2 миграции
архивных процессов).

## Состояние

С момента `snapshot-v10.md` (2026-05-27) запущена **миграция архивных
процессов** из `.claude-archive/2026-05-21/docs/domain/processes/`
(кластеры `Audit/`, `Calculation/`, `Deal management/`). **Проход 1
(карта артефактов) завершён, проход 2 (создание файлов в `docs/`) не
запущен.** Разбор результатов прохода 1 в чате породил два
методологических decision и уточнение backlog.

## Что изменилось относительно v10

**Активные задачи:**
- Миграция архивных процессов — проход 1 завершён. Карта артефактов
  составлена (`.claude/work/progress/progress-карта-артефактов.md`):
  материализуются 4 процесса (`market-data-calculation`,
  `deal-management`, `strategy-action-calculation`, `risk-evaluation`),
  ~58 компонентов, ~20 RVO, ~14 моделей, 7 новых сквозных правил +
  расширения уже мигрированных моделей. На каждый из 8 архивных доков —
  пара `progress-<имя>.md` + `tasks-<имя>.md`.

**Новые decisions:**
- `.claude/decisions/migration-triad.md` — триада принципов миграции из
  недоступного позднее источника (не возвращаемся к архиву; при
  неоднозначности — open-question/tasks, не угадываем; знаем куда —
  кладём).
- `.claude/decisions/process-materialization-criterion.md` — критерий
  материализации процесса как файла `docs/processes/<X>.md` (собственное
  содержание про оркестрацию ИЛИ точка композиции для других процессов).

**Расширения существующих файлов:**
- `.claude/processes/knowledge-classification.md` — раздел «Сквозные
  операции» расширен правилом про самодостаточность open-questions при
  недоступном источнике.
- `.claude/work/backlog.md` — зонтичный пункт «Активная миграция:
  архивные процессы» (появился в проходе 1); пункт 6 дополнен ремаркой про
  вывод audit-дока из миграции.

**Новые open-questions** (`.claude/work/questions/open-questions.md`):
- PROC-Q1 — существование `PositionContext` как RVO (добавлено в проходе 1).

**Закрытые задачи:**
- (нет с момента v10).

## Текущая структура

См. `.claude/rules/structure.md`. Каталоги не менялись с v10. Скиллы
классификации не менялись. В `decisions/` — два новых файла (см. выше).

## Активные задачи

- Миграция архивных процессов: проход 1 закрыт, проход 2 готов к запуску.
  Рабочие файлы — в `progress/` и `questions/tasks/` (16 файлов пар +
  карта).
- В backlog — зонтичный пункт активной миграции + cross-cutting миграции.

## Открытые общие вопросы

- DEAL-Q1, DEAL-Q2 (продуктовые, финализация сделки) и PROC-Q1
  (`PositionContext` как RVO). Остальные закрыты ранее; история — в
  соответствующих decisions.

## Что в работе

- Готов промпт прохода 2 миграции архивных процессов.
- Следующий шаг — отправка промпта прохода 2 в новом чате (переезд из-за
  объёма задачи).
