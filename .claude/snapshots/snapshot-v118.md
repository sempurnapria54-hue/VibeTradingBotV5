# Snapshot v118

**Дата:** 2026-09-04.

## На какой вопрос отвечает этот файл

Где мы сейчас. **Шаг 9 в `CODE`: доковая петля закрыта, оба выноса
держателю отвечены, измеритель стагнации поправлен.** Сменяет v117.

## Состояние

Фаза 1 — `IN_PROGRESS`; шаги 1-8 `DONE`, шаг 9 — **`CODE`**, шаги 10-11
`HOLD`. Ветка `claude-audit`. **Дельта закоммичена** — по указанию
держателя на эту серию задач.

**Режим — автономия до конца фазы 1**; решения — Д250-Д529 в дайджесте.

**Открытых выносов держателю нет.** `open-questions.md` не несёт ни одного
вопроса, гейтящего шаг 9.

## Что происходит дальше

**Кодовая фаза шага 9, заход 8 реестра гейтов.** Три компонента:

| Компонент | Торговая тропа | Чем гейтится |
|---|---|---|
| `ApiAccessPolicy` — `SecurityFilterChain` плюс точки входа отказа, запись `AccessDenial` | нет | ничем |
| `PrincipalAuditor` — резолв принципала в актора записи | нет | ничем |
| `CredentialsRejectionResolver` — опознание отказа кредов на границе | **да** | **ждёт грунта S0** — перечня auth-отказов источника |

Аппрув `CODE` шага 9 идёт по **четырём** фокусам: `conventions`,
`performance`, `disaster` и реактивированный **`security`**.

Подтверждение перехода со всеми четырьмя гейтами —
`.claude/work/progress/phase-1-step-9-code-transition.md`.

## Чем закончилась доковая петля шага 9

Пять прогонов, ряд гейтящих **5, 5, 7 → 4 → 2**. Две эскалации детектора,
обе отвечены держателем:

- **`PROC-Q4` = A** — цельный дизайн-проход вместо итераций. Сработал по
  своему предмету измеримо: класс «дома решения не существует» исчерпан,
  ряд упал на 71 %. Решение —
  `.claude/decisions/security-perimeter-design-pass.md`, исполнение —
  `phase-1-step-9-design-pass.md`.
- **`PROC-Q5` = C** — закрыть три находки `_5` и выйти в `CODE`. Отчёт
  закрытия — `phase-1-step-9-gaps-close-5.md`.

**Что вскрыл дизайн-проход:** под именем «безопасность» лежали **два
разных права** — входящий доступ (к нашей поверхности) и исходящий (к
источнику), — писавшиеся в один дом. Это и был механизм, который мерил С2.

## Что ратифицировано и не пере-решается

Всё из v104-v117, плюс:

- **`SEC-Q1` = один принципал** (Д525-Д527): число субъектов — **посылка
  фазы 1**, не свойство контура; параметр снятия — появление второго
  субъекта поверхности; встречный якорь заведён **без** механического
  триггера, и это названо (событие не совпадает с границей фазы).
- **Поле `principal` — принятый, а не предъявленный** (Д528): заявленное
  неудостоверённое имя не пишется.
- **Указатели ведут в носитель, а не в момент** (Д529): шаблон
  контроллера адресует дом политики, а не шаг роадмапа.
- **У доли сигнала С2 объявлена нижняя граница знаменателя — четыре
  гейтящих находки** (решение держателя): прогон с меньшим числом доли не
  даёт, сигнал не срабатывает и **обрывает окно**. Порог ½, окно и
  машинность правила не менялись — менялась область определения величины.
  Дом — `.claude/skills/stagnation-detection.md`, довод и проверка на
  обеих сериях — `.claude/decisions/stagnation-ratio-minimum-denominator.md`.

## Чем проверяется корпус

Девять команд плюс `code-gate-check.py` — все код 0. **Тесты: 297, 0
падений.** `src/` шагом 9 пока не менялся.

## Что висит

1. **Грунт S0** — перечень auth-отказов источника; гейтит один компонент,
   `CredentialsRejectionResolver`. Оживит прогон контура с заведомо
   неверным ключом на demo.
2. **Слот S0 в реестре предусловий контура не заведён** — сверка
   `preconditions-check.sh` шага 9 не покрывает; компонент при этом держит
   `code-gate-check.py`. Дом — `.claude/work/backlog.md`, задача о
   слоте S0 в реестре предусловий контура; владелец — `tester`.
3. **A12 `DOCS_CHECK_3`** — слой опровержения в продуктовом корпусе (29
   «прежняя редакция» в 25 файлах); дом — `.claude/work/backlog.md`,
   позиции носителей корпуса.
4. **Пересмотр посылки о единственном принципале** —
   `.claude/work/backlog.md`, задача о втором субъекте поверхности; оживит
   его появление.
5. **Реакция на серию отказов доступа** — `.claude/work/backlog.md`,
   оживит первый период живых прогонов.
6. **Девять припаркованных позиций хвоста шага 8** —
   `.claude/work/backlog.md`, хвост шага 8 (safety / AnomalyJob).
7. **Долг адресных перекрёстий `docs/**`** — 62 адреса; в доме доступа
   сведены к нулю.
8. **Провизорные числа калибровки** и **две несверенные посылки о
   контракте источника** — свои якоря в бэклоге и в контуре.
9. **Пользователю — обновить Project Knowledge:** этот снапшот, дайджест
   (Д250-Д529).

## Среда

Изменений с v117 нет. Дом фактов —
`.claude/tests/source-api/okx/code-preconditions.md`. **Maven в `PATH`
отсутствует**: рабочий бинарь — `/c/Program Files/JetBrains/IntelliJ IDEA
2026.1/plugins/maven/lib/maven3/bin/mvn`; лаунчер Python — `py -3`.
**`JAVA_HOME` среды указывает на JDK 11** — Maven без переопределения
падает `UnsupportedClassVersionError` до тестов; рабочая форма
`JAVA_HOME=C:/Users/RomanKrd/.jdks/corretto-25.0.3`.
