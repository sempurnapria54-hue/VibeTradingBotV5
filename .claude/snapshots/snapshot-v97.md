# Snapshot v97

**Дата:** 2026-08-31.

## На какой вопрос отвечает этот файл

Где мы сейчас. **Пакет решений держателя исполнен целиком: член популяции
выводится из предмета командой, гейт `CODE` стал исполнимым критерием по
классам находок, усиление измерения заморожено до кода; `GAPS_CLOSE_32`
закрыл все 32 гейтящие находки; `DOCS_CHECK_33` начат и не завершён.**
Сменяет v96.

## Состояние

Фаза 1 — `IN_PROGRESS`; шаги 1-6 `DONE`, шаг 7 — `DOCS_CHECK_33` (прогон
начат, отчёт не собран), шаги 8-11 `HOLD`. Ветка `claude-audit`,
`HEAD = caaf96c`; вся дельта сессии — в рабочем дереве **staged, не
закоммичена**.

**Режим — автономия до конца фазы 1**; решения этой сессии — Д208-Д220 в
дайджесте.

## Что произошло

- **Пакет держателя 2026-08-31 по эскалации С2+С3 — расширенный вариант
  из трёх частей** (происхождение и критерий перечня; исполнимый критерий
  выхода в `CODE` по классам; заморозка новых эпистемических этажей до
  входа в `CODE` с зафиксированной ценой) —
  `.claude/decisions/population-origin-and-code-gate.md`.
- **F7 — с Postman-коллекции снят статус обязательного аппрув-артефакта**
  (аппрув-артефакт контура — план) —
  `.claude/decisions/collection-not-approval-artifact.md`.
- **`GAPS_CLOSE_32`** — дизайн-проход плюс десять узлов: все 32 гейтящие
  находки `_32` закрыты, десять узлов из десяти чисты по обоим предметам
  мини-петли —
  `.claude/work/progress/phase-1-step-7-gaps-close-32.md`.
- **`DOCS_CHECK_33` начат и не собран.** Линза A отработала — 9 находок,
  6 гейтящих (`КОД` 3, `РИСК` 2, `НОСИТЕЛЬ` 1); её отчёт в подпапке
  прогона. Линзы B-G **дважды подряд** погибли на платформенных сбоях
  (обрыв ответа API / таймаут) и не написали ничего. Строка `_33` в
  реестре прогонов пуста.

## Что теперь держит механизм

Три носителя, заведённых пакетом держателя; редакция здесь не
пересказывается — она живёт в доме.

- **Контракт популяции** (`rule` / `derive` / `excludes` сверх `keys`) —
  `.claude/skills/closure-population.md`; оси доказаны батареей
  `spec-mutation-check.sh` (25-32).
- **Реестр диспозиции гейтящих находок** —
  `.claude/work/code-gate-ledger.json`, вход исполнимого критерия выхода
  в `CODE`; дом критерия —
  `.claude/processes/roadmap-step-execution.md` §Гейты.
- **Класс гейтящей находки** —
  `.claude/skills/classify-code-blocking.md`; ярлык обязателен у каждой
  гейтящей и каждой припаркованной находки.

## Чем проверяется корпус — десять команд

Все десять зелены на текущем дереве.

| Команда | Что меряет | Состояние |
|---|---|---|
| `bash tools/spec-run.sh` | примеры сходятся, популяции покрыты | 22 спеки, 545 примеров (19 контрпримеров), 6 популяций / 64 члена (15 недостижимых) |
| `bash tools/spec-mutation-check.sh` | величина что-то проверяет | 33 оси; 378 величин, переживающих нейтрализацию 0 |
| `python3 tools/spec-scope-check.py docs/spec` | области, дом, копия формы | 8 осей; A:0 B:0 C:0 |
| `python3 tools/spec-pointer-check.py` | указатель ведёт в дом величины | 12 осей; 218 указателей, чужих 0 |
| `python3 tools/population-derive-check.py` | перечень сходится с выведенным из предмета | 10 осей; 6 популяций, объявленных неполнот 0 |
| `python3 tools/anchor-check.py` | разрешимость §-адресов `.claude/**` | 27 осей; 513 адресов, новых неразрешимых 0, долг 56 под храповиком |
| `python3 tools/retired-check.py` | снятая редакция и популяция носителей | 19 осей; 869 носителей, 10 записей, 43 строки популяции, дефектов 0 |
| `bash tools/preconditions-check.sh` | реестр == план == фактика | СОШЛОСЬ |
| `python3 tools/code-gate-check.py` | классы гейтящих находок и их диспозиция | 15 осей; на реестре `_32` — ПРОЙДЕН |
| `mvn -o test-compile` | дерево собирается | собирается |

**Код возврата 2 у любой команды означает «не измерялось», а не «дефектов
нет».**

**Что критерий выхода говорит сегодня.** «ПРОЙДЕН» относится к **реестру
`_32`** — то есть к закрытию прошлого прогона, не к готовности шага.
Гейтящие находки `_33` в реестр не сведены (прогон не собран), а линза A
уже дала три `КОД` и два `РИСК`: по действующему критерию шаг к `CODE`
**не готов**.

## Что ратифицировано и не пере-решается

- **База риска следует за балансом в обе стороны, автоматически** (v94).
- **Нотинал укладывается в потолок с запасом** `0.01` (v94).
- **Точечных ручек в фазе 1 нет** (v94).
- **Дом закрытой формы риска один** — `docs/spec/risk-at-stop.json`.
- **Адресуемая единица эпизода — пара** `(externalId, externalCreatedAt)`.
- **FSM транша прогоняют два обработчика из трёх.**
- **Имена DTO источника — суффиксом** `{Сущность}{Биржа}Response`.
- **Писатель причины закрытия назван по ребру, значение — по маршруту.**
- **Судьба встроенной защиты выводится из пары «терминал + налив»**, а не
  из присутствия элемента в снапшоте.
- **Член популяции выводится из предмета командой; покрытым считается
  проверенный, не предъявленный** (Д208-Д211).
- **Признак применённости шага — исчерпание пакета действий** (Д214).
- **У транша заведено значение причины `EXTERNAL_CLOSE`; старшинство —
  первое** (Д213).
- **Аппрув-артефакт контура — план; коллекция необязательна** (F7).
- **Дом стыковой истины не бывает у того, кто сам сторона стыка** —
  критерий, а не жанр (Д219).

## Что висит

1. **`DOCS_CHECK_33` не собран.** Перепрогнать линзы B-G (мандат готов и
   лежит в `.claude/work/progress/phase-1-step-7-docs-check-33/_mandate.md`),
   собрать отчёт прогона, пересобрать реестр диспозиции под `_33`,
   приложить **явный раздел с вердиктом** по исполнимому критерию выхода
   в `CODE`, заполнить строку `_33` в реестре прогонов хроники.
   Ограничение среды: две попытки подряд легли на платформенных сбоях —
   запускать меньшими партиями, отчёт линзы писать инкрементально.
2. **Детектор стагнации на `_33`:** С2/С3 под сбросом (окно не набрано),
   С1 может сработать, С4 судится честно — кандидат в `GAPS_CLOSE_32`
   есть (первая формулировка ограничения `where` стояла на ложной посылке
   и заменена в том же проходе, Д210).
3. **Держателю — три числа конфигурации при запуске:** максимальный риск
   на сделку, максимальный множитель катастрофического потолка, предел
   серии подряд убыточных сделок.
4. **Грунт: три слота открыты** (пп. 2, 7, 16); состояние читается
   колонкой статуса реестра предусловий, числа больше нигде не
   дублируются. Носитель факта слота п. 2 заведён
   (`observations/AG6_2.md`), добор — прогоном, где среда позволяет.
5. **`CODE`-дельта** — `.claude/work/backlog.md` §«Шаг 7 (сделки и P&L) —
   исполнительный хвост»; приросла позициями этого захода.
6. **Припарковано заморозкой измерения** — две находки класса `ИЗМЕРЕНИЕ`
   и одна `НОСИТЕЛЬ` в `.claude/work/backlog.md` §«Припаркованные прочие
   находки `DOCS_CHECK_32`», каждая с владельцем и условием возврата.
7. **Адресный долг:** `.claude/**` — 56 под храповиком (прогон падает на
   новом неразрешимом и на строке долга, которой в корпусе больше нет);
   `docs/**` — 62, остаток упирается в распил двух гигантов.
8. **Гигиена и распил гигантов** — курационный заход после закрытия шага.
9. **Пользователю — обновить Project Knowledge:** этот снапшот,
   `docs/concept.md`, `.claude/rules/structure.md`, дайджест решений
   (непрочитанные пакеты, включая Д208-Д220).

## Среда

Без изменений против v94-v96: Linux, Docker-контур поднят, Vault
распечатан. **Дом фактов среды один** —
`.claude/tests/source-api/okx/code-preconditions.md`; здесь редакция не
держится.
