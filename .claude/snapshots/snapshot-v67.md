# Snapshot v67

**Дата:** 2026-07-15.

## На какой вопрос отвечает этот файл

Где мы сейчас. **Тема — шаг 7 «Сделки и P&L»: `GAPS_CLOSE_3` закрыл
все 8 находок `DOCS_CHECK_3` (H1-H8); доковых гейтов не осталось,
шаг готов к `DOCS_CHECK_4`.** Сменяет v66.

## Состояние

Фаза 1 — `IN_PROGRESS`; шаги 1-6 `DONE`, **шаг 7 «Сделки и P&L» —
`GAPS_CLOSE_3`** (закрыт; исход → `DOCS_CHECK_4`), шаги 8-11 `HOLD`.
Ветка `claude-audit`, `HEAD = a0e19d3 (Аудит доков)`.

**В staging, не закоммичено:** пакет `GAPS_CLOSE_3` (~40 файлов) +
методологический пакет прошлых сессий (см. v66 §Состояние — не
коммитился). CC не коммитит — ревью/коммит в IDEA за пользователем.

## Как сюда пришли (после v66)

`DOCS_CHECK_3` (2026-07-04) дал 8 находок, 3 гейтящих.
**`GAPS_CLOSE_3` (2026-07-15) закрыл все** — отчёт
`.claude/work/progress/phase-1-step-7-gaps-close-3.md`, хроника —
`phase-1-step-7-chronicle.md`. Ядро — CC, периферия — 3
параллельных субагента на непересекающихся наборах.

1. **H1 (N9 fee-wiring) — главный гейт, снят.** Проработан
   `integrator`'ом (ступень Предложение, прогон против офдока
   2026-07-14) → провалидирован в чате. **Гейт закрылся без новой
   фактуры от биржи:** `groupId` уже приходит `/public/instruments`
   — дыры не было, был **отброшенный ключ** (отброс сделан до
   changelog OKX 2025-11-21). Итог — `pnl-finalization-mechanics.md`
   реш.4 (переписан).
2. **Хранение ставки (уточнение пользователя — ревизует место, не
   seam).** Ставка оказалась **group×tier-level**, не
   инструмент-level ⇒ новая модель **`TradeFeeRate`** — одна строка
   на группу, таблица с историей, **не копия на каждом
   инструменте**; на навесе только ключ `externalFeeGroupId`;
   **seam не двинулся**. `modifiedAt` = измеритель свежести;
   `level`/`ts` — датчик такта тира; несвежесть > 24 ч (конфиг) →
   холд биржи.
3. **H2/H5/H6/H7/H8 + гигиена H4×5** — решения пользователя
   зафиксированы: native `fee` в used (безопасный суперсет,
   компонента `externalFee`); `OffsetDateTime`; добор числа вместо
   «пометки достаточно» (направление; материализация — фаза 2);
   epsilon переякорен на оборот Σ`|amount|`; инвариант settle-ccy —
   дом `docs/rules/trading-constraints.md` (source-agnostic, довод —
   переносимость).
4. **Новое решение** —
   `docs/decisions/source-model-change-absorption.md` (смена модели
   источника поглощается native-слоем; резолв по заполненности, не
   по дате).

**Три поправки к посылкам задания** (зафиксированы в отчёте):
посылка «съехавшая ставка вылезет аномалией на сверке» **не
подтвердилась** (сверка = bills ↔ positions-history, оба факта
биржи, прогноз в ней не участвует) ⇒ аспект такта `CODE` не гейтил;
`elpMaker` — unused, но лежит **внутри `feeGroup[]`**, а не в
плоской шестёрке; H2-рантайм числился гейтом без своего кейса в
тест-плане (заведён §AG3.5).

## Loose ends

- Staged-пакет (`GAPS_CLOSE_3` + методологический хвост v66) —
  ревью/коммит в IDEA.
- Ручные синхронизации после коммита — см. «После коммита».
- `tradingbot.iml`, `vault.hcl` — untracked, не трогать.

## Что дальше

1. **Шаг 7 → `DOCS_CHECK_4`** (подтверждающий прогон концепт-линз +
   trading). H1 задел mapping/sync-job/риск-слой и ввёл новую модель
   с новой таблицей — прогон обязателен.
2. **После чистого `DOCS_CHECK_4` — рантайм-фикстура §AG1.5**
   (порядок последовательный, решение пользователя): N11 (§AG1.5) +
   H2 (§AG3.5) + RQ-3 (§AG12.5) + RQ-4 (§AG3.4) — **один прогон**;
   без фикстуры RQ-1 (§AG12.4), RQ-2 (§M1.7). Гейтят `CODE`.
3. **Затем `CODE`** — исполнительный хвост в `backlog.md` §Шаг 7
   (носители, команды, fee-wiring, снятие `REFRESH_FILLS`).
4. Форвард фазы 2 — там же: H6 (добор числа), N13 (funding),
   конвергенция оптимистичных смещений левого хвоста (единый фокус
   шага ожидаемости).

## Среда

Тулчейн/инфра — без изменений (см. v62/v64): `mvn` 3.9.11 из
wrapper-dist, `JAVA_HOME=~/.jdks/corretto-25`; boot —
`SPRING_PROFILES_ACTIVE=test`, джобы `*_ENABLED=false`,
`VAULT_TOKEN`/`VAULT_URI` из `.env.vault.test.local`; docker
postgres:16 (5441 test), vault (8200).

## После коммита

1. **Custom Instructions:** без изменений с v66 — если текст из
   `.claude/chat/chat-project-instructions.md` ещё не перенесён в
   Settings → Custom Instructions, перенести (правился префлайт +
   режим чтения PK).
2. **PK:** заменить снапшот (v67 сменяет v66). Прочие PK-копии этой
   сессией не менялись.
