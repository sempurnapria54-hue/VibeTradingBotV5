# Snapshot v130

**Дата:** 2026-09-05.

## На какой вопрос отвечает этот файл

Где мы сейчас. **Шаг 5 фазы 2 закрыт; шаг 6 — `CODE`, docs-first пройден,
порт сервиса не начат.** Сменяет v129.

## Состояние

Фаза 1 — `FOLDED`. **Фаза 2 — `IN_PROGRESS`**: шаги 1-5 `DONE`, шаг 6 —
`CODE` (не закрыт), шаги 7-12 `HOLD`. Фазы 3-6, 7-8 и прод-рубеж — `HOLD`.

**Дельта в working tree, не в истории.** Ветка `claude-audit`; всё
описанное — staged, не закоммичено (коммит руками).

**Режим — автономия до прод-рубежа.** Решения — Д250-Д652 (итерация 28).
**Открытый вынос держателю один:** ARCH-Q1 в остатке — провайдер облака и
CI-система; гейтит прод-рубеж.

## Что закрыто

| Шаг | Что поставлено |
|---|---|
| 1 | монорепозиторий: `donor/`, скелет, вторая ось размещения |
| 2 | платформа: `deploy/base`, три оверлея, `deploy-layout-check.py`, спека контура окружения |
| 3 | `libs/domain-model` (82 класса), реактор, `EventEnvelope`, `InternalIdFactory`, доменный `Order.Side` |
| 4 | `services/auth`: схема `V1`, допуск контура, форма пути ключей, контур доступа на bearer |
| 5 | `services/connector-okx`: 36 операций, 36 эндпоинтов, 25 тестов; запись ключей в `auth`; общие носители адреса, состава секрета и причины статуса |

Хроники и отчёты — `.claude/work/history/2026-09-05-phase-2-step-{1..5}-*`.

## Где стои́т шаг 6 и что в нём уже решено

**Docs-first пройден целиком** — пять прогонов (6 → 4 → 1 → 0 находок,
плюс контакт с кодом), четыре закрытия. Артефакты — в
`.claude/work/progress/phase-2-step-6-*`.

**Ратифицировано шагом 6 (не пере-решать):**

- **глубина хранения — из класса ряда** (невосполнимое не чистится в
  `prod`, восполнимое чистится с бэкфиллом, производные — за свечами);
- **сбор идёт по требованию потребителя**, выраженному синхронной
  командой; полный листинг для советника — конфигурация, не требование;
- **производные ключуются идентичностью вычисления**, не настройкой
  стратегии, а **свежесть — толерантность читателя** (Д651, Д652);
- **ликвидации ждут WS**, стакан и тикер собираются шагом 6;
- **тикер-срез — три агрегатных чтения**, потому что тикер площадки
  марк-цену и индекс не отдаёт.

**Код шага 6, уже в дереве:** `MarketOrderBook`, `MarketTicker`,
`OrderBookLevel` в `libs/domain-model`; четыре публичных чтения в
коннекторе (тикеры листинга, книга, марк-цены, индексы) с DTO, снапшотами
и маппером; сквозное переименование ключа производных (21 файл).

**Модуль `services/market-data` откачен намеренно:** пустой модуль
объявлял бы сервис, которого нет. Заводится следующим заходом вместе с
первым кодом.

## Что дальше — вход в следующий заход

1. `services/market-data`: модуль, схема `V1` (гипертаблицы рядов),
   персистентность, порт джоб и сервисов (~2400 строк донора), сбор
   срезов, поверхность заявки и чтения.
2. **Порт джоб индикаторов и структуры — не механический:** в доноре они
   обходят настройки стратегий, чтобы узнать, что считать; теперь
   обходить надо заказанные идентичности. То же у свечей — группу заводит
   требование, а не онбординг.
3. Четыре фокуса, `SYNC_DOCS_FROM_CODE`, пост-хок концепт-гейт
   (`.claude/processes/roadmap-step-execution.md`
   §«6a. Пост-хок концепт-гейт для CODE-инкрементов» — применим:
   концепт-инкремент на `CODE` уже въехал).

## Что висит

1. **`подтверждение`** — ARCH-Q1 в остатке: провайдер облака и CI-система.
2. **Парковки шага 6** — в `.claude/work/backlog.md`: §«Сбор ликвидаций —
   по добытому контракту WS-канала» (грунт: тот же прогон, что у
   WS-слушателя).
3. **Парковки шага 5**, там же: §«Сброс кэша ключей по событию ротации»;
   §«Осиротевшие секреты счетов»; §«Манифесты развёртывания сервисов»
   (закрывает долг и шага 4).
4. **Парковки прежних шагов**, там же: §«WS-слушатель фактов
   исполнения»; §«Контекстный тест сервиса»; §«Подпись образов и политика
   допуска»; §«Транспорт внутри кластера»; §«Размеры томов данных»;
   §«Темы Kafka по производителям»; §«Сериализационная аннотация на
   доменной модели».
5. **Не проверено прогоном:** кластера и базы в среде нет; коннектор к
   живому OKX не ходил.
6. **Прежние хвосты** — S1 Vault, контракт броска диспетчера, рост
   таблицы отказов, A12, хвост шага 8, долг адресных перекрёстий
   `docs/**`, дубль эталона стратегии в корне.
7. **Пользователю:** переиндексировать проект в IDEA (реактор:
   `libs/domain-model`, `services/auth`, `services/connector-okx`,
   `donor`). Обновить Project Knowledge: этот снапшот, дайджест
   (итерация 28), `structure-digest.md`.

## Проверки на момент снапшота

Десять команд корпуса — код 0 (1236 живых носителей, 46 записей снятых
редакций, дефектов 0; 23 спеки / 580 примеров; 389 величин, переживающих
нейтрализацию — 0). `mvn -o test -DexcludedGroups=source-api-live` →
`BUILD SUCCESS`: `auth` 10, `connector-okx` 25, `tradingbot` 324.

## Среда

Без изменений против v129: сборка реакторная из корня, Maven — из
поставки IDEA, `JAVA_HOME=$HOME/.jdks/corretto-25.0.3`, лаунчер Python —
`py -3`. Кластера Kubernetes и базы для прогона нет; сеть доступна —
Maven докачивает артефакты.
