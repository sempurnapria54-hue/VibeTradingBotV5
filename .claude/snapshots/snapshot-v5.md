# Snapshot v5

**Дата:** 2026-05-25.

## На какой вопрос отвечает этот файл

Где мы сейчас и как сюда пришли (срез на дату).

## Состояние

С момента `snapshot-v4.md` (2026-05-25) чат ушёл не в
запланированную вторую обкатку на торговых сущностях, а в
доработку правил пайплайна. Итоги:

- Уточнены правила поведения в чате: добавлены «Переезд в новый
  чат» и «Отслеживание ресурса чата».
- Введены два правила для Claude Code: отчёт при классификации
  (`classification-report.md`) и синхронизация внешнего источника
  правды (`external-source-sync.md`).
- Размещение правила про отчёт само породило открытый вопрос Q3
  (правило vs сквозная операция процесса).
- Зафиксирован открытый вопрос Q4 — как разделить знание по
  адресатам (чат vs CC). Назначен следующей темой.

## Текущая структура

См. `.claude/rules/structure.md` (без изменений с момента v4).

Кратко — изменения относительно `snapshot-v4.md`:

**Правила:**
- `.claude/rules/chat-project-instructions.md` — добавлены два
  правила в секцию «Поведение»: «Отслеживание ресурса чата»
  (рабочий ресурс ~40-60K токенов, строка `Ресурс: ~N%` в каждом
  ответе, пороги 70% / 90%) и «Переезд в новый чат» (триггеры;
  два сценария — плановое завершение со снапшотом / продолжение
  разбора с промпт-артефактом).
- `.claude/rules/classification-report.md` — **новый**. На каждый
  фрагмент знания при классификации — отчёт о результате (форма
  зависит от объёма; действует на время обкатки концепции).
- `.claude/rules/external-source-sync.md` — **новый**. При правке
  файла с шапкой «источник правды снаружи» — напомнить о
  синхронизации. Перенесён из инлайна `CLAUDE.md`.

**CLAUDE.md:**
- Инлайн-правило про синхронизацию внешнего источника убрано
  (перенесено в `external-source-sync.md`). Указатель на новый
  файл пока не добавлен — решение отложено до разбора Q4.

**Открытые вопросы:**
- Добавлены Q3 (правило vs сквозная операция процесса) и Q4
  (разделение знания по адресатам чат / CC).

**Продуктовые каталоги:**
- Без изменений. `docs/*` — заготовки с `.gitkeep`.

**Рабочее:**
- `.claude/work/backlog.md` — пустой.
- Остальные `work/*` каталоги — пустые.

## Активные задачи

Нет.

## Открытые общие вопросы

Q1, Q2, Q3, Q4 (см. `.claude/work/questions/open-questions.md`).

- **Q1** — куда класть инвариант конкретного объекта. Открыт. B1
  закрыт отдельным decision; B3 и B7/B10/B12/B13 отложены.
- **Q2** — где первоисточник дублированного правила. Открыт,
  отложен до появления базовых торговых сущностей в `docs/`.
- **Q3** — куда класть правило, неотделимое от процесса: `rules/`
  или внутрь файла процесса как сквозную операцию. Открыт,
  копится до похожих случаев. Гипотеза критерия: «универсальное
  правило vs правило-сквозная-операция».
- **Q4** — как разделить знание по адресатам (чат vs CC). Открыт,
  назначен следующей темой разбора.

## Что в архиве

`.claude-archive/2026-05-21/`:
- `CLAUDE.md` — старый.
- `.claude/` целиком — старая инфраструктура.
- `docs/` целиком — старая продуктовая документация. Источник
  материала для запланированной второй обкатки —
  `.claude-archive/2026-05-21/docs/domain`.

Брать оттуда что-то — осознанное действие, не автоматическое.

## Что изменилось с предыдущего снапшота

- В `chat-project-instructions.md` добавлены правила «Переезд в
  новый чат» и «Отслеживание ресурса чата». Custom Instructions в
  Settings → требуют ручной синхронизации.
- Введено правило `classification-report.md` (отчёт CC при
  классификации). Его размещение породило Q3.
- Введено правило `external-source-sync.md`; одноимённое
  инлайн-правило убрано из `CLAUDE.md`. Указатель на новый файл
  из `CLAUDE.md` пока не добавлен (отложено до Q4).
- Зафиксированы Q3 и Q4 в `open-questions.md`.

## Что в работе

- **Q4 — следующая тема.** Разбор требует переезда в новый чат.
- **Q3** ждёт накопления похожих случаев.
- **Q1 и Q2** — без изменений с момента v4.
- Каркас классификации продолжает обкатываться. Скиллы
  `classify-area` и `classify-theme` — по-прежнему каркасы без
  содержания, ждут материала.
- Запланированная в v4 вторая обкатка на торговых сущностях
  (`Balance` → `Position` → `Order` → `AlgoOrder` → `Deal` →
  `Strategy` из `.claude-archive/2026-05-21/docs/domain`) в этом
  чате не запускалась — отложена за Q4.
