# Snapshot v6

**Дата:** 2026-05-25.

## На какой вопрос отвечает этот файл

Где мы сейчас и как сюда пришли (срез на дату).

## Состояние

С момента `snapshot-v5.md` (2026-05-25) разобран открытый вопрос
Q4 — разделение знания по адресатам (чат vs Claude Code). По
итогам:

- Оформлен decision `chat-vs-cc-knowledge-split.md`: граница
  адресатов и правила формулировок.
- Проведена ревизия `.claude/` — из файлов для CC вычищены
  формулировки, ориентированные на чат.
- Q4 закрыт и удалён из `open-questions.md`; установлена
  конвенция про удаление закрытых вопросов.

## Текущая структура

См. `.claude/rules/structure.md` (без изменений с момента v5).

Кратко — изменения относительно `snapshot-v5.md`:

**Решения:**
- `.claude/decisions/chat-vs-cc-knowledge-split.md` — **новое**.
  `chat-project-instructions.md` — единственный файл для чата;
  всё остальное в `.claude/` — для Claude Code. Чат читает файлы
  CC как справку через PK. Файлы CC формулируются в терминах CC
  (без активации «в чате», без прямого диалога с пользователем).

**Агенты:**
- `.claude/agents/knowledge-curator.md` — убран пункт про
  активацию в чате; «предлагает пользователю / показывает
  варианты / действует после подтверждения» → «возвращает в
  основной контекст»; в «Назначении роли» «предлагает размещение
  пользователю» → «возвращает размещение в основной контекст».

**Скиллы:**
- `recognize-knowledge.md` — «по ходу обсуждения в чате» → «по
  ходу разбора материала»; шаги процедуры «предложить
  пользователю / по согласию» → «сформулировать в отчёт /
  передать дальше»; в пометке «в обсуждении» → «в разборе».
- `classify-theme.md` — «обсудить с пользователем» → «вынести
  развилку в отчёт, не угадывать».

**Правила:**
- `external-source-sync.md` — «напомнить пользователю» → «в
  отчёте напомнить».

**Без правок по ревизии:** `place-knowledge.md`, `classify-area.md`,
`classify-type.md`, `classification-report.md`, `naming.md`,
`structure.md`, `processes/knowledge-classification.md` —
формулировок под критерии не найдено.

**Открытые вопросы:**
- Q4 удалён (закрыт decision'ом). Остаются Q1, Q2, Q3.

**Продуктовые каталоги:**
- Без изменений. `docs/*` — заготовки с `.gitkeep`.

**Рабочее:**
- `.claude/work/backlog.md` — пустой.
- Остальные `work/*` каталоги — пустые.

## Активные задачи

Нет.

## Открытые общие вопросы

Q1, Q2, Q3 (см. `.claude/work/questions/open-questions.md`).

- **Q1** — куда класть инвариант конкретного объекта. Открыт.
- **Q2** — где первоисточник дублированного правила. Открыт,
  отложен до появления базовых торговых сущностей в `docs/`.
- **Q3** — куда класть правило, неотделимое от процесса: `rules/`
  или внутрь файла процесса как сквозную операцию. Открыт,
  копится до похожих случаев.

## Что в архиве

`.claude-archive/2026-05-21/`:
- `CLAUDE.md` — старый.
- `.claude/` целиком — старая инфраструктура.
- `docs/` целиком — старая продуктовая документация. Источник
  материала для запланированной второй обкатки —
  `.claude-archive/2026-05-21/docs/domain`.

Брать оттуда что-то — осознанное действие, не автоматическое.

## Что изменилось с предыдущего снапшота

- Разобран Q4: оформлен decision `chat-vs-cc-knowledge-split.md`.
- Правки в `knowledge-curator.md`, `recognize-knowledge.md`,
  `classify-theme.md`, `external-source-sync.md` — формулировки
  переведены в термины CC.
- Q4 удалён из `open-questions.md`. Установлена конвенция:
  закрытый вопрос удаляется из `open-questions.md`, история
  закрытия — в соответствующем decision.

## Что в работе

- **Q3** ждёт накопления похожих случаев.
- **Q1 и Q2** — без изменений с момента v5.
- Каркас классификации продолжает обкатываться. Скиллы
  `classify-area` и `classify-theme` — по-прежнему каркасы без
  содержания (правка в `classify-theme` касалась только
  формулировки, не наполнения).
- Запланированная вторая обкатка на торговых сущностях
  (`Balance` → `Position` → `Order` → `AlgoOrder` → `Deal` →
  `Strategy` из `.claude-archive/2026-05-21/docs/domain`)
  по-прежнему отложена.
