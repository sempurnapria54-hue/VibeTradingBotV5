# Snapshot v76

**Дата:** 2026-08-02.

## На какой вопрос отвечает этот файл

Где мы сейчас. **Развилка «команда ↔ действие» решена целиком (4
валидации) и материализована `GAPS_CLOSE_8`; `DOCS_CHECK_9` прогнан
тремя линзами — НЕ чист → `GAPS_CLOSE_9`, но ядро материализации
подтверждено всеми линзами.** Сменяет v75.

## Состояние

Фаза 1 — `IN_PROGRESS`; шаги 1-6 `DONE`, **шаг 7 — `GAPS_CLOSE_9`**,
шаги 8-11 `HOLD`. Ветка `claude-audit`, `HEAD = dfc1d93` (закоммичен
весь пакет: валидации 3-4, `GAPS_CLOSE_8` — ~30 доков + свип 78 +
2 новых + 3 удалённых, отчёты `GAPS_CLOSE_8`/`DOCS_CHECK_9`, хроника,
backlog, roadmap-статус, правки процесса).

**В staging, не закоммичено:** только снапшот-ход — этот файл (v75 →
`history/snapshots/`) + строка дерева.

## Как сюда пришли (после v75)

1. **Валидация 4** — окно консолидации входа закрыто транзакцией (крен
   «принять повтор» отклонён; 0/1). Квалификация пользователя: не
   исключение, а **второй экземпляр паттерна N7**; клауза обобщена.
   Диагноз — эвристика 4 не применялась в обратном направлении → правка
   `design-fork.md` (отбрасывая вариант как «исключение из правила»,
   искать действующий прецедент того же исключения). **Развилка решена
   целиком**; трейл — `progress/phase-1-step-7-command-action-boundary.md`
   (закрыт, уедет в history при DONE шага).
2. **`GAPS_CLOSE_8`** — материализация решённого (узлы 1/3/4 + выводимые
   H17/H19-H21/H23/H25 + суффиксы). Отчёт —
   `progress/phase-1-step-7-gaps-close-8.md`.
3. **`DOCS_CHECK_9`** — три независимые линзы, **впервые с ограниченным
   каталогом поиска** (запрет `.claude/work/**` соблюдён, утечек нет).
   Отчёт — `progress/phase-1-step-7-docs-check-9.md`; хроника —
   `phase-1-step-7-chronicle.md`.

## Ратифицировано — не пере-решать

**Вся развилка «команда ↔ действие» — теперь в постоянном носителе
`docs/decisions/command-action-boundary.md`** (9 разделов: критерий
атомарности «одна ответственность за состояние»; 4 системных действия и
их границы, kill-switch и cleanup вне; строка = исполнение + частичные
ключи живых + «копить, очистка — фаза 3»; β-бюджет + предел per command +
стадия из фактов; транзакционная клауза записи в `Deal`; суффиксы
`_COMMAND`/`_ACTION` вкл. `StrategyActionType`; окно линковки — поля
`Deal` с названной ценой; узел 4 — вариант (а); реакция на исчерпание
добычи — управление-сайд с доводом отладки и **условием пересмотра**).
Плюс отражено в правилах: `command-lifecycle` (критерий),
`idempotency-via-unique` (NULL-семантика, уникальность среди живых),
`persistence-representation` (операнды ключа — колонки),
`instrument-hold` (посттерминальная добыча), `naming` (разведение
уровней). Упразднены: `DealFinalizationState` (модель+lifecycle),
`DealFinalizationCommandFactory`; введён `SystemActionExecutor`.

## Живой контекст: исход `DOCS_CHECK_9` (→ `GAPS_CLOSE_9`)

**Ядро материализации подтверждено всеми тремя линзами; фактика схемы
сходится полностью.** 19 находок (одна отведена верификацией — F-A8:
линза заявила отсутствие секции `naming.md`, grep её находит); гейтят 3.
Группы (детали, цитаты, крены — в отчёте):

1. **Реконсиляционные хвосты свипа** (узел A отчёта, 7 находок одного
   класса «потребитель не реконсилирован», все `выводимо`): контракт
   `account-bills.md` §Использование несёт **снятую редакцию окна** (A+C
   независимо; крен — снять прикладной блок из контракт-дока); native
   `openAvgPx` в used вопреки H23; **шесть носителей** с прямой эмиссией
   `REFRESH_BALANCE_COMMAND` (Precheck/EntryFinalized/ProtectionSwitched
   + DealContext/BalanceContainer/RiskValidator); lifecycle
   `AnomalyReport` (писатель cross-ccy); стейл-хвосты (`RuntimeTarget`,
   §Связи ревизованного decision, `triggerPx`); `Position.md` без
   ALTER-пометки; `Deal.md` без cross-ccy-слагаемого в формуле.
2. **H8 — конфликт, внесённый самим закрытием** (гейтит): «dealId
   фактически всегда заполнена» (модель) против ветки ожидания
   executor'а («персистит, но не линкует»). **Крен CC: предикат эмиссии
   звена** — `REFRESH_BILLS_COMMAND` эмитится только при непустом
   `billsWindowEnd`; ветка исчезает, клейм модели становится верным.
3. **Дисциплина SYSTEM-ключей** (H9/H10, мягко): target-дисциплина
   SYSTEM-строк не закреплена (крен — всегда `(DEAL, NULL)`, инвариант
   несёт ключ 2, предикат непустоты в ключ 1); судьба
   `TargetEntityType.BALANCE`/`NONE`; `CLOSE_POSITION_COMMAND` вычистить
   из матрицы, развести два режима повтора (anchored / loop
   re-emission); H19 — close в реестр `ExitPendingHandler`.
4. **Переоткрытые узлы `DOCS_CHECK_8`** (подтверждение, не шум) с
   **новыми гранями**: узел 7 / H11 (гейтит) — плюс у
   `REFRESH_BILLS_COMMAND` нет durable-носителя исхода пагинации;
   узел 5 / H12 (гейтит) — плюс `mapping/Instrument.md` указывает домом
   валют модель, которая отказывается; узел 2 / H13 — канал
   `plannedRiskAmount` (крен — поле в payload); узел 8 / H14 —
   epsilon-якорь уязвим к `OTHER`-валовке; узел 6 / H15 — анкер
   `AnomalyReport` невыразим на схеме для группы.
5. **Торговые**: H16 (мягко гейтит) — тропы `PRECHECK`/`ENTRY_SUBMITTED`
   → `CLOSED` **без входа** не имеют источника числа, инвариант
   непустоты безусловен → дорога к «нулю-как-fallback» (крен — карв-аут:
   непустота только при состоявшемся входе + критерий членства
   R-выборки); H17 — знак дыры posId-less закрытий (левый хвост) не
   назван; H18 — довод момента курса смешивает оси.

## Открытые вопросы

Узловые решения у пользователя: **узлы 2, 5, 6, 7, 8** `DOCS_CHECK_8`
(переоткрыты на переписанных доках) + **карв-аут H16**. Прочее без
изменений: `PNL-Q1`, `RISK-Q3`, `FEE-Q1`, `HOLD-Q2`, **N11/§AG1.5**
(рантайм-верификация, гейтит `CODE`), `triggerPx` (обесточен), три
предложения `GAPS_CLOSE_7` на валидации (несостоявшаяся сделка — требует
правки после H5; адресация парой; носитель расчётной валюты — сросся с
узлом 5/H12).

## Пайплайн

- **Ступень — `Предложение`, счёт гейта 0.** Валидация 4 в леджере (0/1,
  диагноз — эвристика 4 в обратном направлении). Правки процесса за
  сессию: `design-fork.md` (эвристики 3, 4, 6 + проверка принципа на
  ущерб), `naming.md` §Разведение уровней.
- **Ограничение каталога линз** сработало (первый прогон без утечек);
  новый артефакт наблюдения — F-A8: линза сослалась на состояние файла,
  которого нет на диске (вероятно, копия из её контекста) — отведена
  верификацией. Верификация CC несущих атрибуций остаётся обязательной.
- **Три кандидата в правку пайплайна** — без изменений, не
  ратифицированы (обратный свип; проверка покрытия; «операнд, канал и
  анкер»).

## Loose ends

- Staged-пакет (~106 файлов) — ревью/коммит в IDEA.
- **Custom Instructions**: правка `chat-project-instructions.md` (правило
  ресурса) всё ещё не вставлена в Settings — до вставки не действует.
- `GAPS_CLOSE_9`: **выводимая часть готова к закрытию без пользователя**
  (узел A целиком, H10, H18; H8 — по крену после валидации); H9 —
  предложение владельца; узловое — по решениям.
- Целевые, в коде их нет: суффиксы enum'ов, `SystemActionExecutor`,
  V2-топология `deal_action_states`, `ENTRY_BLOCKED`,
  `REFRESH_BILLS_COMMAND`, `MARK_DEAL_EMERGENCY_CLOSED_COMMAND`, вторая
  нога, поля `Position`/`Deal`, секция `service-command-retry` (NPE —
  пункт `CODE`).
- Проработка развилки закрыта — уедет в history вместе с артефактами
  шага при `DONE`.

## Что дальше (порядок)

1. **Решения пользователя**: узлы 2/5/6/7/8 + карв-аут H16 (+ валидация
   крена H8 и предложения H9).
2. **`GAPS_CLOSE_9`**: выводимая часть + узловое по решениям.
3. **`DOCS_CHECK_10`** — подтверждающий, независимыми линзами с
   ограниченным каталогом.
4. После чистого прогона — **рантайм-фикстура §AG1.5**, затем **`CODE`**
   (хвост — `backlog.md` §Шаг 7; CODE-пункт границы «команда ↔ действие»
   переписан целиком).

## Среда

Тулчейн/инфра — без изменений (см. v62/v64/v67).

## После коммита

1. **PK:** заменить снапшот (v76 сменяет v75).
2. **Custom Instructions:** перенести текст
   `.claude/chat/chat-project-instructions.md` в Settings (висит с
   позапрошлой сессии — см. Loose ends).
