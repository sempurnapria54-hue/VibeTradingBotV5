# Snapshot v246

**Дата:** 2026-09-12.

## На какой вопрос отвечает этот файл

Где мы сейчас. **Шаг 11 фазы 2 открыт; `DOCS_CHECK_1` проведён — стадия 0 не
чиста, гейтящих находок двадцать семь. Прогон был оборван посреди работы линз
и доведён разбором оборванной единицы.** Сменяет v245.

## Состояние

Фаза 1 — `FOLDED`. **Фаза 2 — `IN_PROGRESS`**: шаги 1-10 `DONE`, шаг 11 —
`DOCS_CHECK_1`, шаги 12-13 `HOLD`. Прод-рубеж — `HOLD`.

**Гейтящих выносов держателю нет.** Прогон проверил это по каждой из двадцати
семи гейтящих находок всеми тремя линзами; единственный вынос прежних шагов —
`BFF-Q1` с вердиктом «не гейтит, адресат шага 13».

**Дельта единицы открытия закоммичена держателем** (`12c4bbda`, вместе с
единицей ленты цикла); **дельта разбора и весь прогон — staged, не
закоммичены.** Ветка `claude-audit`. Дерево кода не тронуто ни единицей.

**Режим — автономия до прод-рубежа.** Решения — Д250-Д1489.

## Что сделано после v245

**Единица 1 — открытие шага 11** (оборванной сессией цикла): `HOLD` →
`DOCS_CHECK_1`, исход единственной сработавшей секции бэклога («Ориентиры к
дизайн-проходу по `statistics`» — взята в работу), обобщение примечания
роадмапа.

**Единица 2 — `DOCS_CHECK_1`**: начат той же сессией, оборван держателем,
доведён разбором. Голова — `.claude/work/progress/phase-2-step-11-docs-check-1.md`.

- **Линза A** — 17 находок, 15 гейтящих, стадия 0 не чиста (A1-A7).
- **Линза B** — 15 находок, 10 гейтящих.
- **Линза C** — 11 находок; семь сняты дедупликацией против A, две перекрыты
  и оставлены за свой вклад, две свои.
- **C9 закрыта прогоном:** девятнадцать вердиктов гейтинга переставлены на
  «шаг 11 фазы 2, `DOCS_CHECK_1`», операнд `STEP` команды конвенции — тоже.

**Единица 3 — разбор оборванной единицы** (задание держателя):
`.claude/work/progress/phase-2-step-11-interrupted-unit-review.md`. Линзы A и
B перезапущены **её же заданием** — промпты восстановлены из потока сессии
дословно; заведена хроника шага, перенесён отчёт прошлой единицы в историю,
правлено дерево знания.

## Четыре вещи, которые дальше нельзя пере-решать

**1. Раскладка границы — решение держателя.** `integration` делится по
**собеседнику** (`internal` — сосед по монорепозиторию, `external` — система
вне его), шина живёт слоем `internal/event`. Вызов соседа остаётся интеграцией
и в третий слой не выносится (Д1463).

**2. Дом общих артефактов один и лежит в `services/common/`.** Модели
группируются каталогом и разводятся по слою; сам `model` артефактом не
является. Артефакт опознаётся по `pom.xml`, а не по глубине (Д1476-Д1477).

**3. Домен формы сообщения не держит — ни в сигнатурах, ни транзитом.**
Признак механический и мерится грепом по двум осям: импорт пакета форм в
`**/domain/**` и слово `Message` в его теле; обе обязаны быть пусты. Прогона у
этого признака нет — он держится дисциплиной и этой записью.

**4. Открытие шага — своя единица, а не флип статуса.** Простановка статуса
**сама** оживляет секции бэклога с условием `шаг:2-<M>:открыт`, и каждая
обязана получить исход тем же ходом. Перечень печатает
`py tools/backlog-check.py --статус 2-<M>=DOCS_CHECK_1`. Разрешение на вход
выдано заранее **и на шаг 11, и на шаг 12**: `HOLD` означает «ни одного
под-шага не сделано», а не «ждём держателя».

## Вход следующей сессии

**Первая единица — `GAPS_CLOSE_1` шага 11.** Порядок закрытия предписан
находками, а не выбором закрывающего (голова прогона §Рекомендация):

1. **A4 идёт первой** — `docs/architecture/services.md` и
   `docs/architecture/data-ownership.md` отвечают **противоположно** о судьбе
   чтения журнала статистикой; от исхода зависят A5 (грант, спека
   `owner-role-grants.json`, её команда вывода) и C6 (дом правила агрегатов).
   Пока стоя́т обе формулировки, закрытие починит ту, до которой дотянется.
2. **A1-A3** — что потребляет `statistics` (классы, темы, группа, позиция
   чтения), чем держится дедуп, что становится хранилищем фактов, — вход
   дизайн-прохода шага; его секция бэклога уже `оживит=сейчас`.
3. **A7 идёт одним ходом с переименованием** — три машинные оси сцеплены, и
   между правками корпус красен при верных правках.

**Ловушки** v206-v245 остаются в силе целиком. Добавлены этой единицей:

- **фоновый субагент, оборванный вместе с сессией, находок не оставляет
  вовсе** — его транскрипт остаётся нулевой длины. Но **его промпт лежит в
  потоке сессии**, и перезапуск возможен дословно: `sessions\raw\*.ndjson`
  переживает обрыв и есть источник истины о том, что сессия успела;
- **отчёт может объявить исполненной правку, которой нет.** Клейм отчёта о
  собственной правке проверяется **дельтой по метке**
  (`git diff --name-only <метка> -- <область>`), а не чтением отчёта: сессия
  объявила перестановку девятнадцати вердиктов пятью местами и была оборвана
  до неё;
- **операнд `STEP` команды конвенции открытых вопросов переставляется вместе
  с вердиктами** — иначе прогон мерит вердикты против прошлого шага и молчит
  на устаревших (проба О3 окрестности);
- **метка отбивается `git write-tree` ДО первой правки единицы.** Отбитая
  после — запасной вывод от дерева последнего коммита, и перечень точек тогда
  шире собственной дельты; это объявляется в отчёте.

Обязательное перед завершением любой единицы:

1. **гейт инструментов корпуса** целиком — перечень в
   `.claude/skills/update-roadmap-progress.md` §«Гейт инструментов корпуса»,
   вместе с `backlog-check.py` (с подменой статуса, если статус проставляется);
2. **сборка — `bash tools/reactor-test.sh`**, если тронуто дерево кода;
3. **`py tools/code-gate-check.py`** — перед каждым кодовым заходом.

## Что висит

Всё, что живёт секциями бэклога с маркерами, печатает
`py tools/backlog-check.py` — в снапшоте не дублируется. Вне бэклога висит:

1. **Двадцать семь гейтящих находок `DOCS_CHECK_1`** — вход `GAPS_CLOSE_1`;
   части прогона в `progress/`.
2. **Продуктовая половина B2 у держателя** — `BFF-Q1`, оживитель — шаг 13.
3. **Стенд от дерева кода отстал:** образы четырёх сервисов не собирались,
   роли и базы не заводились, тем и подписки нет.
4. **Ни одна пара производитель-потребитель не встречалась на живом брокере** —
   форма провода проверена сквозным тестом у журнала и чтением у прочих.
5. **Спека и её реализация не связаны ничем механическим** — `SpecRunnerTest`
   гоняет спеки против их собственных примеров; тесты сервисов переписывают
   примеры руками. Наблюдение, задачи пока нет: держатель о нём знает.
6. **Пользователю:** обновить Project Knowledge (этот снапшот).

## Проверки на момент снапшота

**Гейт инструментов корпуса прогнан** на состоянии после всех правок — весь
перечень его дома, ненулевых кодов нет, кода 2 нет. Реактор этой единицей не
гонялся: дерева кода она не трогала. Числа прогонов здесь не пересказываются:
их печатает сам прогон (`.claude/rules/self-description-form.md`).

## Среда

Против v132 не изменилась; полное описание — там, перечень ловушек — в v206
(`.claude/work/history/snapshots/snapshot-v206.md` §Среда) и он остаётся в силе.
