# Snapshot v253

**Дата:** 2026-09-12.

## На какой вопрос отвечает этот файл

Где мы сейчас. **Шаг 11 фазы 2 в `GAPS_CLOSE_1`; узлы 1-3 приземлены, узел 4
прошёл раунд критики 1 — следующая единица — доработка узла 4.** Сменяет v252.

## Состояние

Фаза 1 — `FOLDED`. **Фаза 2 — `IN_PROGRESS`**: шаги 1-10 `DONE`, шаг 11 —
`GAPS_CLOSE_1`, шаги 12-13 `HOLD`. Прод-рубеж — `HOLD`.

**Гейтящих выносов держателю нет.**

**Дельта — staged, не закоммичена.** Ветка `claude-audit`. Дерево кода не
тронуто: единица читающая.

**Режим — автономия до прод-рубежа.** Решения — Д250-Д1512.

## Что сделано после v252

**Единица — раунд критики 1 узла 4** (независимый заход); отчёт —
`.claude/work/progress/phase-2-step-11-gaps-close-1-node-4-critique.md`.

**Вердикт — дорабатывать. Конструкция узла не оспорена:** правило («свидетельство
о полноте приёма производит тот, о чьей полноте оно свидетельствует») верно, дом
выбран по уровню, вынос сделан выносом, второй экземпляр объявлен полностью. Все
блокирующие — незакрытые концы правки.

- **К1 — предметная.** `docs/spec/audit-journal.json`, нота `continuityClaimable`
  по-прежнему объявляет снятый радиус («отдаётся всякой выдачей чисел сервиса —
  и строк журнала, и строк агрегатов») и ведёт за формой обязательства к
  `AuditRecord.md`, то есть к дому, который узел перенёс.
- **К2 — передача.** Перечень дельты кода пропустил единицу реестра
  «`audit-statistics`: модуль, контур доступа, манифест и сетевые политики». Она
  затронута: четвёртый `@Scheduled`-метод процесса требует поднять пул
  планировщика с 3 до 4 и переставить ожидание пробы `SchedulerCapacityTest`.
- **К3-К5 — клеймы и имена**, ставшие ложными правкой: «четвёртого файла
  владельца `audit-statistics` нет» при двух оставшихся; `docs/spec/audit-journal.json`
  остался размечен сервисом при сквозном доме; бэклог назвал спеку «домом
  сквозной формы».
- **К6** — разорванная разметка лид-жирного пассажа в `data-ownership.md`.
- **К7 (текстовая)** — таблицы свипов 3 и 5 не суть разбор своей выдачи: состав
  расходится в обе стороны, `Итого` сходится совпадением. Это и есть механизм,
  которым К1 прошла мимо свипа.

## Вход следующей сессии

**Следующая единица — доработка узла 4 по итогам критики** (её делает автор
закрытия, не критик). Предмет — К1-К6 плюс К7; после доработки — верификация,
затем приземление.

После узла 4 — очередной узел `GAPS_CLOSE_1` по остатку гейтящих находок: A6,
A7, A10, A14, A15, B4, B6-B10. Части прогона —
`.claude/work/progress/phase-2-step-11-docs-check-1*.md`.

- **B2 — продуктовая половина у держателя** (`BFF-Q1`, оживитель — шаг 13).
- **A7 идёт одним ходом** со сведением имени `audit-statistics` по корпусу: он
  же правит javadoc дерева кода, спеку грантов и имя спеки полноты.

**Ловушки** v206-v252 остаются в силе целиком; добавленная критикой —
свип-команды узла регистрозависимы и не берут термин, разорванный переносом
строки: у `grep -rIn` флаг `-I` задаёт «не двоичные», а не «без учёта
регистра». Прочие — `phase-2-step-11-chronicle.md` §«Ловушки, оставленные
узлом».

Обязательное перед завершением любой единицы:

1. **гейт инструментов корпуса** целиком — перечень в
   `.claude/skills/update-roadmap-progress.md` §«Гейт инструментов корпуса»,
   вместе с `backlog-check.py` (с подменой статуса, если статус проставляется);
2. **сборка — `bash tools/reactor-test.sh`**, если тронуто дерево кода;
3. **`py tools/code-gate-check.py`** — перед каждым кодовым заходом.

**Среда:** `python3` в этой оболочке разрешается в заглушку Windows Store и
возвращает код 49 — инструменты корпуса зовутся через `py`.

## Что висит

Всё, что живёт секциями бэклога с маркерами, печатает
`py tools/backlog-check.py` — в снапшоте не дублируется. Вне бэклога висит:

1. **Мини-петля узла 4 не закрыта** — доработка, верификация, приземление.
2. **Гейтящие находки `DOCS_CHECK_1`, не закрытые узлами 1-4** — вход
   остальных узлов `GAPS_CLOSE_1`.
3. **Стенд от дерева кода отстал:** образы четырёх сервисов не собирались,
   роли и базы не заводились, тем и подписки нет.
4. **Ни одна пара производитель-потребитель не встречалась на живом брокере.**
5. **Спека и её реализация не связаны ничем механическим** — наблюдение,
   задачи пока нет: держатель о нём знает.
6. **Пользователю:** обновить Project Knowledge (этот снапшот); прочитать
   блоки дайджеста Д1500-Д1512 — право вето постфактум.

## Проверки на момент снапшота

**Гейт инструментов корпуса прогнан** дважды — на входе критики и на состоянии
после записи отчётов: весь перечень его дома, ненулевых кодов нет, кода 2 нет.
Реактор этой единицей не гонялся: дерева кода она не трогала. Числа прогонов
здесь не пересказываются — их печатает сам прогон
(`.claude/rules/self-description-form.md`).

## Среда

Против v132 не изменилась; полное описание — там, перечень ловушек — в v206
(`.claude/work/history/snapshots/snapshot-v206.md` §Среда) и он остаётся в
силе.
