# Снапшот v461

**Дата:** 2026-09-26.

## На какой вопрос отвечает этот файл

Где мы сейчас.

## Состояние

Сменяет v460. Фаза 1 — `FOLDED`. **Фаза 2 — `IN_PROGRESS`**: шаги 1-11
`DONE`, **шаг 12 — `REVIEW`**, шаг 13 `HOLD`. Прод-рубеж — `HOLD`.

**Машина шага** — `CONCEPT → CONCEPT_REVIEW → CODE → DOCS → REVIEW → DONE`
(`.claude/processes/roadmap-step-execution.md`).

**Дельта — в рабочем дереве, не закоммичена** (поверх `6380c6c5`). Ветка
`claude-audit`.

**Режим — автономия до прод-рубежа.** Решения — Д250-Д2225
(`.claude/work/decision-digest.md`).

## Что сделано после v460

Одиночная сессия при остановленном цикле, работа шага 12 не велась
(решение держателя 2026-09-26 о цене сквозных прогонов; Д2218-Д2225).

- **Закрытие тестерской сессии — тронутыми классами.** Дом —
  `.claude/rules/session-work-unit.md` §«Объём закрывающего прогона»: сессия
  тестера с одной тестовой дельтой закрывается классами, которые изменила или
  добавила; правка общей обвязки набора не расширяет; модуль целиком —
  полным реактором при закрытии единицы; тот же прогон несёт приземляемая
  точка между группами. Пересказы — `tools/session-prompt.md`,
  `.claude/skills/update-roadmap-progress.md` §«Сборка — реактор».
- **Режим классов обёртки** — `bash tools/reactor-test.sh --modules <модуль>
  --classes <Класс>[,…]`, оси 11-13 (исполнение каждого класса — по отчёту
  surefire, не по логу: строку класса surefire печатает по `@DisplayName`).
  Живые прогоны: `ExitCascadePathTest` — 5 тестов, 2 минуты;
  `ExitCascadePathTest,TeardownEscalationPathTest` — 9 тестов, 4 минуты.
- **Постоянный лог реактора** — `target/reactor-test.log`; лента цикла
  (`tools/session_feed.py`) показывает его давность у реактора.
- **Секция общего стенда сквозного набора** — `.claude/work/backlog.md`
  §«Сквозные классы модуля `tests` делят поднятый стенд на группу».

## Вход следующей сессии

Единица — секция тестера `.claude/work/backlog.md` §«Клетки тропы активации за
копией определения ждут закрытия находки `F6`»; позиция в ней — пролог двух
инструментов для `E2.5`, `E2.6`, `E3.1`, `E7.5` документа
`e2e-safety-teardown` (хроника шага, заход 183, «Следующий ход»). Сессии этой
единицы с одной тестовой дельтой закрываются тронутыми классами; последняя —
полным реактором.

**Следующая единица после её закрытия** — секция тестера
`.claude/work/backlog.md`
§«Сквозные классы модуля `tests` делят поднятый стенд на группу»,
**до** кодовых заходов
`code-writer` по находкам (`F6`, `F7` документа `e2e-safety-teardown`,
`F7`-`F13` тропы выхода, долг доминирования).

## Что держателю решать

1. **Апрув `REVIEW` шага 12** — по условию держателя: правки кода по находкам
   сделаны, все тесты шага написаны и зелены. Открытыми остаются четыре клетки
   сквозной тропы снятия риска, секция общего стенда, клетки таблицы
   §«Кейсы, не прогоняемые сегодня» документа кейсов `trading-core` и секции
   бэклога с маркером `сейчас`.
2. **Плечо пар стенда** — вход по паре без назначенного плеча отвергается;
   к первому прогону на стенде плечо назначается точкой
   `PUT …/pair-settings/…` (число — риск-аппетит держателя).

Прочие ожидания держателя печатает `py tools/backlog-check.py` строкой
`держатель:`.

## Что висит

Висящее живёт секциями `.claude/work/backlog.md` и
`.claude/work/prod-checks.md` и печатается `py tools/backlog-check.py`; в
снапшоте не дублируется.

**Пользователю:** обновить Project Knowledge (этот снапшот).

## Ловушки

Ловушки живут в домах по своему вопросу; снапшот их не держит. В разделе
ловушек дома — оглавление: споткнулся — найди строку по словам области и
симптома, тело читай по номеру в `.claude/traps/<дом>-traps.md`.

- среда, оболочка, правка корпуса скриптом, площадка OKX —
  `.claude/skills/environment-commands.md` §«Ловушки и обходы»;
- код тестов — `.claude/skills/test-code.md` §«Ловушки и обходы»;
- письмо кейсов и их ревью — `.claude/skills/test-design.md`,
  `.claude/skills/test-review.md`;
- продуктовый код — `.claude/rules/codestyle.md` §«Ловушки»;
- команды корпуса и свипы — `.claude/rules/measurement-commands.md`;
- условие маркера бэклога — `.claude/rules/backlog-section-form.md`
  §«Ловушки»;
- гейт и статус — `.claude/skills/update-roadmap-progress.md`;
- цикл сессий — `.claude/skills/session-chain.md` §«Ловушки и обходы».

## Проверки на момент снапшота

Дерево кода этой сессией не менялось (`tools/`, `.claude/` — вне него):
закрывающий прогон не нужен. Модуль `tests` зелен с закрытия захода 183 —
режим модулей, 151 тест, дефектов 0, 65 минут. Батарея обёртки реактора —
31 ось, все доказаны. Гейт инструментов корпуса — зелен вторым прогоном на
финальном состоянии; первый был красен двумя командами, обе правлены:
§-адрес снапшота без файла в абзаце и `retired-check.py` кодом 2 на корневом
`target/` (секция бэклога снята — `.claude/work/history/2026-09-26-e2e-run-cost.md`).

## Среда

Дом фактов и ловушек среды — `.claude/skills/environment-commands.md`;
стенда — `.claude/skills/local-stand.md`. Изменение среды — строка «Сборка
дерева»: режим классов обёртки и постоянный лог `target/reactor-test.log`.
