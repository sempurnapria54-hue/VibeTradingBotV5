# Snapshot v90

**Дата:** 2026-08-29.

## На какой вопрос отвечает этот файл

Где мы сейчас. **Итерации `DOCS_CHECK`/`GAPS_CLOSE` прекращены решением
держателя; корпус переписан по утверждённой концепции, дизайн-проход
риск-контура приземлён.** Сменяет v89.

## Состояние

Фаза 1 — `IN_PROGRESS`; шаги 1-6 `DONE`, шаг 7 — **переписан корпус, не
в `CODE`**, шаги 8-11 `HOLD`. Ветка `claude-audit`, `HEAD = d59827f`;
вся дельта — **staged, ждёт ручного ревью и коммита**.

**Режим — автономия до конца фазы 1.** Проектные развилки CC решает сам
и фиксирует в дайджесте; держателю — только продуктовый scope и
численные величины риск-аппетита.

## Что сменилось концептуально

Детектор стагнации остановил петлю «проверка → закрытие» на узле
покрытия. Держатель сменил концепцию: сначала ядро, из него —
переписывание корпуса.

**Три артефакта, утверждённые держателем:**

- `docs/concept.md` — ядро корпуса: четыре принципа (риск
  fail-once-then-halt; непредусмотренная ситуация не решается на ходу;
  наблюдаемость как требование конструкции; знание — один дом, чистые
  доки, исполнимая форма) и выведенная из них конструкция;
- `.claude/work/progress/corpus-rewrite-mapping.md` — раскладка по всем
  доках плюс отчёт исполнения;
- `.claude/work/progress/risk-contour-design.md` — цельный дизайн-проход
  узла покрытия и риск-контура.

## Что сделано

**Корпус переписан:** 234 файла и 1899 KB → 212 файлов и 833 KB (снято
56 %). Числа по каталогам и что переписано вручную против механической
чистки — отчёт исполнения в раскладке.

**Тип знания «решение» упразднён:** исход решения переехал в дом правила
или модели, довод-принцип — в концепцию, остальное удалено. Четыре файла
повышены до правил и процесса.

**Заведён исполнимый слой** `docs/spec/`: формула, предикат и закрытый
перечень живут одним экземпляром с набором примеров; доменный док
ссылается и форму не переписывает. Раннер — `SpecRunnerTest` и
`tools/spec-run.sh`. Сейчас три спецификации, 39 примеров, прогон
зелёный.

**Дизайн-проход приземлён** в правилах и моделях: покрытие защиты,
снятие защиты как риск-увеличивающее действие, обязательство покрытия,
порядок пакета по риск-классу, сделочный кэп нотинала.

Автономные развилки — `.claude/work/decision-digest.md`, итерация
2026-08-29 (Д93-Д100).

## Что висит

1. **Пользователь:** закоммитить дельту; обновить Project Knowledge —
   этот снапшот, `docs/concept.md`, `.claude/chat/structure-digest.md`
   (если менялся), дайджест решений.
2. **Два выноса держателю** — `risk-contour-design.md`, §«Что уходит
   держателю»:
   - **числа риск-аппетита** одним списком: `M` (сделочный кэп нотинала
     в долях базы риска) и, при априорном назначении, величины «про
     хвост». Дизайн от них не зависит: место есть, пустое место означает
     отказ;
   - **продуктовый scope:** должны ли эпизоды одной сделки вестись
     одинаково.
3. **Хвост переписывания** — `.claude/work/backlog.md` §«Хвост
   переписывания корпуса»: содержательный проход по семидесяти
   компонент-докам, составные вопросы в шапках, ещё семь предметов
   спецификации, машинный прогон эталонной стратегии, разделы
   исполнителей под новую конструкцию.
4. **Дайджест решений:** непрочитанные пакеты 2026-08-28 (Д45-Д72) и
   2026-08-29 (Д93-Д100), плюс более ранние.

## Чем теперь проверяется корпус

**Прогоном примеров, а не полнокорпусными линзами.** Правка формулы,
предиката или перечня доказывается прогоном `tools/spec-run.sh`; правка
дока — дифф-скоупом по затронутым домам. Машина `DOCS_CHECK`/`GAPS_CLOSE`
остановлена, детектор стагнации (`.claude/skills/stagnation-detection.md`)
сохранён как инструмент на будущее.

## Среда

Без изменений — см. `.claude/work/history/snapshots/snapshot-v87.md`.
