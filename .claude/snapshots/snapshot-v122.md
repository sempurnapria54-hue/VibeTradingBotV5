# Snapshot v122

**Дата:** 2026-09-04.

## На какой вопрос отвечает этот файл

Где мы сейчас. **ARCH-Q1…Q4 закрыты ответами держателя; шаг 1 фазы 2
исполнен — репозиторий стал монорепозиторием.** Сменяет v121.

## Состояние

Фаза 1 — `FOLDED`. **Фаза 2 — `IN_PROGRESS`**: шаг 1 в `CODE`, шаги 2-12
`HOLD`. Фазы 3-6 перенумерованы, 7-8 и прод-рубеж — `HOLD`. Ветка
`claude-audit`; дельта задачи — в working tree (staged), не закоммичена.

**Режим — автономия до прод-рубежа** (горизонт закрыт, ARCH-Q3). Решения —
Д250-Д587 в дайджесте (итерация 27 — новая). **Открытый вынос держателю
один:** ARCH-Q1 в остаточной части — какой именно провайдер облака и какая
CI-система; класс закрыт, гейтит только прод-рубеж.

## Что сделано в этой задаче

- **Четыре ответа держателя применены** — итоги, что каждый поменял, и
  пересмотр решения о новом репозитории:
  `.claude/work/history/2026-09-04-arch-questions-closed.md`.
- **Порядок фаз (ARCH-Q2):** Bybit (3) → сигнал и бэктест-гейт (4) →
  мультитенант и фронт (5) → портфель и аллокатор (6). Файлы фаз
  перенумерованы, входящие ссылки переякорены.
- **Шаг 1 фазы 2 — реструктуризация на месте.** Донор в `donor/`; скелет
  `services/` `libs/` `web/` `deploy/` с указателями; вторая ось
  размещения — `.claude/rules/knowledge-ownership-by-service.md`. План и
  снятые альтернативы —
  `.claude/decisions/monorepo-restructuring-in-place.md`; хроника и
  замеры — `.claude/work/progress/phase-2-step-1-chronicle.md`.
- **Проверки зелёные после реструктуризации** — девять команд корпуса
  код 0, `mvn -o -f donor/pom.xml test -DexcludedGroups=source-api-live`
  → 319 тестов, 0 падений (тот же счёт, что до неё).

## Что ратифицировано и не пере-решается

Всё из v104-v121, плюс ответы держателя этой задачи: класс облака
(управляемый Kubernetes у российского провайдера, провайдер — потом),
хостинг кода и CI решаются той же проработкой, порядок фаз 3-6, автономия
до прод-рубежа, состав ролей с границей «деньги» между `TRADER` и
`VIEWER`, монорепозиторий = текущий репозиторий, донор в `donor/`.

Из решений машины (Д577-Д587) ключевые: `docs/` не раскалывается по
сервисам (ось — карта, цена названа и заякорена); дом оси — своё правило;
якорь корня репозитория внутри `Spec`; область свипа `retired-check.py` —
префиксы деревьев кода; `README.md` кодового дерева — файл знания.

## Что висит

1. **`подтверждение`** — ARCH-Q1 в остатке: провайдер и CI-система.
   Проработка и её шесть критериев —
   `.claude/work/backlog.md` §«Проработка облака, хостинга кода и CI»;
   оживит — момент, когда роадмап упрётся в инфраструктурные шаги.
2. **До `DONE` шага 1** — зафиксированные исходы адверсариальных фокусов;
   у `performance` / `disaster` / `security` предмета в дельте, похоже,
   нет, и это надо назвать явно, а не умолчать
   (`phase-2-step-1-chronicle.md` §«Что осталось до `DONE`»).
3. **Два новых якоря в бэклоге:** §«Энфорсер оси владельца-сервиса»
   (оживит — первый шаг порта, читающий карту) и §«Статус-набор шага не
   покрывает шаг реструктуризации» (оживит — второй шаг без продуктовой
   дельты).
4. **Прежние хвосты** — S1 Vault, контракт броска диспетчера, ограничения
   контура доступа (оживит шаг 4 фазы 2), рост таблицы отказов, A12, хвост
   шага 8, долг адресных перекрёстий `docs/**`, провизорные числа
   калибровки, дубль эталона стратегии в корне (D-P6 / E-П3, владелец
   `knowledge-curator`) — свои якоря в бэклоге.
5. **Пользователю:** переиндексировать проект в IDEA — корень модуля
   переехал в `donor/`. И обновить Project Knowledge: этот снапшот,
   дайджест (итерация 27), `structure-digest.md`.

## Что дальше

**Закрыть шаг 1** зафиксированными исходами фокусов, затем **шаг 2 —
платформа**: Kubernetes, GitOps, операторы данных, окружения
пространствами имён. Провайдер-специфичного не пишется — кластер вне
облака, манифесты нейтральны.

## Среда

Изменений с v120 нет, кроме места сборки: `pom.xml` теперь
`donor/pom.xml`, и команды прогона идут из корня с `-f donor/pom.xml`.
Дом фактов — `.claude/tests/source-api/okx/code-preconditions.md`.
**Maven в `PATH` отсутствует** (рабочий — из поставки IDEA);
**`JAVA_HOME` среды указывает на JDK 11**, рабочая форма —
`JAVA_HOME=$HOME/.jdks/corretto-25.0.3`; лаунчер Python — `py -3`.
Принципал профиля `test` — в Vault (`secret/tradingbot/api-access-test`).
