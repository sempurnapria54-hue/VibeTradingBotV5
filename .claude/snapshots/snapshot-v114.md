# Snapshot v114

**Дата:** 2026-09-03.

## На какой вопрос отвечает этот файл

Где мы сейчас. **Кодовая фаза шага 8 закрыта: перечень детекторов
построен.** Сменяет v113.

## Состояние

Фаза 1 — `IN_PROGRESS`; шаги 1-7 `DONE`, **шаг 8 — `CODE` (перечень
закодирован)**, шаги 9-11 `HOLD`. Ветка `claude-audit`. **Дельта
закоммичена пятью коммитами** — по указанию держателя на эту серию задач.

**Режим — автономия до конца фазы 1**; решения — Д250-Д467 в дайджесте.

## Что происходит дальше

**Аппрув `CODE` шага 8** — три адверсариальных фокуса (`conventions` /
`performance` / `disaster`) по кодовой дельте шага, затем
`SYNC_DOCS_FROM_CODE` и пост-хок концепт-гейт, если на `CODE` въехал
концепт-инкремент.

## Чем закрыта эта итерация (2026-09-03)

Заходы `CODE_1`…`CODE_5`; решения Д462-Д467.

**Построено одиннадцать детекторов из двенадцати живых.** Кода нет
только у `A13` (переоценка инварианта ликвидации) — она объявлена
форвардом шага сопровождения в собственном разборе, а не пропущена.

| Заход | Что |
|---|---|
| `CODE_1` | каркас прохода: три счёт-широких среза, гейт полноты, `A10`, носители гистерезиса и счёта слепоты |
| `CODE_2` | маркер `clOrdId` у слоя команд + биржевые `A2`/`A3`/`A7` |
| `CODE_3` | учётные `A6`/`A8`/`A9` |
| `CODE_4` | инварианты сделки `A4`/`A11`/`A12` |
| `CODE_5` | свой код у `A11`, статус компонента, курация |

**Детекторы разложены по источнику операнда** (Д463): биржевые признаки,
сверка наших строк с биржей, инварианты живой сделки, полнота прохода —
у каждой группы свой гейт и свой гистерезис.

**Реакций шаг не завёл ни одной:** все зовут уже построенную лестницу
через общего исполнителя блокировки.

## Что ратифицировано и не пере-решается

Всё из v104-v113, плюс:

- **Маркер `clOrdId` — единственный дискриминатор «наше против чужого»**
  (Д462): признак «в БД строки нет» на этой тропе небезопасен, потому что
  заявка переживает рестарт посреди операции.
- **Формула потолков живёт в одном доме** (Д464): детектор зовёт
  `ceilingsBreachedWithoutAct`, а не пересчитывает неравенства.
- **У каждого основания свой код** (Д465): схлопывание разных расхождений
  в одну строку отнимает у разбора то, ради чего коды заведены.
- **Полная страница среза читается как неполный проход** (Д466).

## Чем проверяется корпус

Девять команд плюс `code-gate-check.py` — зелены. **Тесты: 270, 0
падений** (+18 к входу серии). Приложение профиля `test` поднимается с
полным графом детекторов; миграция V24 применена.

**Каждый заход предъявлен падающей пробой:** снятие маркера с биржевого
идентификатора роняет `ourOwnOrderIsNotForeign` (детектор снёс бы биржу
за нашу заявку); снятие операнда БД у `A9` роняет
`restingEntryOrderOfLiveDealIsNotOrphan` (запрет входов на каждом
нормальном отдыхающем входе); снятие гейта полноты графа роняет
`incompleteGraphSilencesEveryDetector`.

## Что висит

1. **`A13` — форвард шага сопровождения:** такт, гистерезис у широких
   стопов и выбор между ремоделом и выходом. Дом — `AnomalyJob.md`
   §«Переоценка инварианта ликвидации».
2. **`kind` объявлен в `AnomalyReport.md` и в коде отсутствует** —
   расхождение доки↔код старше шага 8; адресат — `SYNC_DOCS_FROM_CODE`.
3. **Пагинация счёт-широких срезов** — `.claude/work/backlog.md`
   §«Пагинация счёт-широких срезов детекции».
4. **Провизорные числа калибровки** (гистерезис, предел слепоты) — свой
   встречный якорь в бэклоге.
5. **Ручная поверхность ПОСТАНОВКИ safety-ступени не построена** — живут
   только операции снятия.
6. **Пользователю — обновить Project Knowledge:** этот снапшот, дайджест
   (Д250-Д467).

## Среда

Изменений с v108 нет. Дом фактов —
`.claude/tests/source-api/okx/code-preconditions.md`. **Maven в `PATH`
отсутствует**: рабочий бинарь — из связки IDEA
(`.../plugins/maven/lib/maven3/bin/mvn.cmd`) при `JAVA_HOME` на
`corretto-25.0.3`; лаунчер Python — `py -3`. Порт 8080 может быть занят
чужим процессом — прогон поднимался на `-Dserver.port=18081`.
