# Snapshot v33

**Дата:** 2026-06-06.

## На какой вопрос отвечает этот файл

Где мы сейчас и как сюда пришли (обкатка делегирования доведена до
материализации: правило срока внесено и уточнено, три прогона; шаг 3
«Индикаторы» переведён из `TOOLING` в `DOCS_CHECK_1`).

## Состояние

Фаза 1 — `IN_PROGRESS`; шаги 1-2 — `DONE`; **шаг 3 —
`DOCS_CHECK_1` (переведён этим заходом из `TOOLING`)**; шаги 4-11 —
`HOLD`. Кодовая база — без изменений. Работа среза: правило срока в
программе делегирования (две итерации), материализация
валидированного, прогон 3, gap-отчёт `DOCS_CHECK_1` шага 3.

## Что изменилось относительно v32

### Правило срока в программе делегирования

`.claude/processes/question-delegation.md`, новый раздел
«Взвешивание срока (now-vs-later)»: дедлайн фазы (все вопросы в
охвате фазы решаются до шага тестов); блокер текущего шага — сейчас;
по каждому не-блокеру — взвешивание «что изменится, если решить
сейчас / потом»; отбор состава — на владельце; при откладывании —
горизонт; взвешивание — часть пакета. Уточнение второй итерации:
**«устойчиво/выводимо сейчас» — само по себе не основание решать
сейчас** — только текущий выигрыш (живой блок/противоречие, выигрыш
конкретного ближайшего шага, скоропортящееся); чистая
классификация/размещение знания делается при создании файла
(R3-рутина), в прогоне не пре-решается. Калибровка — два кейса
(«факт есть — развилка позднего шага»; DEAL-Q3).

### Обкатка делегирования: прогоны 2-3 и материализация

- **Прогон 2** (по правилу срока): 5 взято / 1 петля шага / 11
  отложено; 7 вердиктов прогона 1 перевёрнуты в «отложено» —
  ответы зависят от исходов будущих шагов (шаги 5/7).
- **Валидация прогона 2 материализована:**
  - PROC-Q1 — принято, **закрыт**: рудимент `positionContext` снят
    из `CalculationContext.md`/`DealContext.md` (инвариант «одна
    `Deal` — максимум одна `Position`»), без decision;
  - CMD-Q1 — принято **с правкой**, закрыт decision'ом
    `.claude/decisions/executor-payload-file-granularity.md`:
    file-per-executor; **payload — раздел в доке своего executor'а**
    (прежнее «один `ServiceCommandPayload.md` с разделами»
    отменено); пометки CMD-Q1 сняты из 14 executor-файлов;
    подвопрос (базовый тип/дискриминатор, судьба файла) вынесен в
    **CMD-Q2** (горизонт — шаг 4);
  - ENUM-Q1 — **снят как архивный артефакт** (конфликт двух
    архивных доков; канон уже на `RISK_CONTROL`), без decision;
  - ORCH-Q1 — отложен: горизонт «конец фазы / оседание оси владения
    `Instrument.Status`» (узкий онбординг-кусок несёт seam шага 1);
  - DEAL-Q3 — отложен: шаг 4 (классификация при материализации).
- **Прогон 3** (по уточнённому правилу,
  `.claude/work/progress/delegation-rerun-2-open-questions.md`):
  **0 взято / 1 петля шага 3 (IND-Q1) / 14 отложено с горизонтами**
  (шаг 4 — DEAL-Q3, CMD-Q2; шаг 5 — RISK-Q1, RISK-Q2, INSTR-Q1,
  INSTR-Q2; шаг 7 — STRAT-Q4, OKX-Q1, OKX-Q3, DEAL-Q1, DEAL-Q2,
  OKX-Q2; конец фазы — ORCH-Q1; вне фазы — OKX-Q4). **Ожидает
  валидации состава/горизонтов.**
- **Леджер** (`.claude/work/delegation-ledger.md`): исходы 12
  прогонов проставлены; счёт гейта без изменений (классификация R3;
  трейдинг 0; концепт 0 — валидаций по существу у R1-категорий не
  состоялось).

Открытых вопросов: **15** (было 17: закрыты PROC-Q1 и CMD-Q1, снят
ENUM-Q1, добавлен CMD-Q2).

### DOCS_CHECK_1 шага 3 (этим же заходом)

Gap-отчёт — `.claude/work/progress/phase-1-step-3-docs-check-1.md`.
Сводка: **стадия 0 не чиста** — гейт охвата Г1 (входят ли
структура/фаза рынка в шаг 3); всего пробелов 6 (1
несогласованность, 3 структурных, 2 открытых вопроса), эскалаций 5.
Центральный структурный пробел — К2: материализация решения
`market-data-result-identity-keying` (схема идентичности,
канонизация params, новый UNIQUE, судьба поля `setting`) — кандидат
на делегирование `solution-designer` в `GAPS_CLOSE_1` (текущий шаг —
правило срока удовлетворено). Ядро шага (индикаторный кластер)
пройдено по стадиям 1-2; структура/фаза — условно, вердикты зависят
от Г1.

## Активные задачи

- `phase-1-step-3-docs-check-1.md` — gap-отчёт выпущен, **эскалации
  ждут разбора** (следующий чат, по одной итерации за раз).
- `delegation-run-open-questions.md`,
  `delegation-rerun-open-questions.md`,
  `delegation-rerun-2-open-questions.md` — прогоны обкатки; остаются
  в `progress/` до закрытия шага; прогон 3 ожидает валидации.
- `trading-council-materialization.md`,
  `trading-library-distillation.md` — без изменений.

## Текущий фронтир / следующее действие

Разбор эскалаций `DOCS_CHECK_1` шага 3 в чате (по одной за раз,
начиная с гейта охвата Г1) → `GAPS_CLOSE_1`. Параллельные хвосты:
валидация состава прогона 3 делегирования; CODE-хвост шага 2
(runtime-прогон Strategy API) — при поднятом PostgreSQL.

Коммит — за пользователем (всё staged, CC не коммитит).

## Что в работе / PK

- Ветка — `claude-audit`.
- **Project Knowledge:** последний снапшот теперь **`snapshot-v33`**
  (заменяет v32 в префлайте). `CLAUDE.md` / `structure.md` /
  `naming.md` / `place-knowledge.md` не менялись.
- Staged: материализация делегирования (канон: `open-questions.md`,
  decision, `CalculationContext`/`DealContext`/`risk-evaluation`/14
  executor-файлов/`ServiceCommandPayload`, `backlog.md`), правило
  срока (`question-delegation.md`), леджер, прогоны 2-3, gap-отчёт
  шага 3, роадмап, этот снапшот.
