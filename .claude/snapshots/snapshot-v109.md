# Snapshot v109

**Дата:** 2026-09-03.

## На какой вопрос отвечает этот файл

Где мы сейчас. **Строки исполнения расщеплены, число сделки считает
расчёт, конвейер движений дописан; из восьми живых позиций хвоста пять
закрылись — живых три.** Сменяет v108.

## Состояние

Фаза 1 — `IN_PROGRESS`; шаги 1-6 `DONE`, шаг 7 — **`CODE`**, шаги 8-11
`HOLD`. Ветка `claude-audit`, `HEAD = dc951ad` (`ROADMAP 1-7-4 CODE_10 —
свип и снапшот`); **дельта захода в рабочем дереве, staged, не
закоммичена** — ревью держателя в IDEA идёт по staged-дельте.

**Режим — автономия до конца фазы 1**; решения — Д250-Д421 в дайджесте.

## Карта заходов — живой контекст кодовой фазы

| Заход | Компоненты | Статус | Что держит вход |
|---|---|---|---|
| 1 | `DealStateMachine`, `DealTrancheStateMachine`, `DealActionState`, `DealContextService`, `DealOrchestratorJob` | **закодирован** | — |
| 2 | `StrategyActionOrchestrator`, `StrategyValidation`, `MarketDataExpirationChecker` | **закодирован** | — |
| 3 | `TranchePrecheckHandler`, `RiskBlockResolver`, `PriceCalculator`, `SafetyHoldCoordinator` | **в работе** | остаток Т2 (три названных ограничения) |
| 4 | `AttachedAlgoOrderStateResolver`, `CancelAlgoOrderExecutor`, `CreateAlgoOrderExecutor` | **в работе** | остаток Т2 (тропа переключения защиты) |
| 5 | `RefreshPositionExecutor`, `DealOpeningService`, `DealCashFlow`, `RefreshBillsExecutor` | **закодирован** | — |
| 6 | `SystemActionExecutor` | **закодирован** | — |
| 6 | `FinalizeDealExitExecutor` | **в работе** | остаток Т11 (мягкой биржевой ступени нет) |

**Реестр гейтов:** открытых позиций ноль; критерий выхода в `CODE`
проходит. **Реестр предусловий: открытых слотов грунта ноль.** Хвост
кода — `.claude/work/progress/phase-1-step-7-code-tail.md`: **три живых
позиции** (Т2, Т7, Т10, Т11 — Т10 условная, срабатывает при расширении
области преконтроля); закрытые перенесены в
`history/2026-09-03-step-7-code-tail-closures.md`.

## Что произошло с v108 (2026-09-03)

Ход и доводы —
`.claude/work/progress/phase-1-step-7-code-pass-execution-rows.md`;
решения Д407-Д421. Коротко:

- **Таблица строк исполнения расщеплена** (V22): стратегийные и
  системные исполнения хранятся раздельно, вид кодируется таблицей, а
  идентичность берётся из **одной** последовательности — анкер команды
  однозначен одним полем. Финализационный стек снят целиком.
- **`SystemActionExecutor` построен**: durable-строка системного
  действия с бюджетом попыток и ревизией живых исполнений; звено
  выводится из подтверждённого факта, снятая надобность строки не
  заводит. **Бюджет попыток заработал впервые** — бросок исчерпания в
  коде существовал и не производился ни одним носителем.
- **Число сделки считает расчёт** (Т13): `DealResultCalculator` по
  разбивке движений, недоступность вместо занижения, признаки отбора
  одной транзакцией с числом (V23), сверка P&L, аварийный терминал
  `MarkDealEmergencyClosedExecutor`. Механический ноль штатного терминала
  убран.
- **Конвейер движений дописан** (Т9): глубины уехали в контур биржи,
  дедуп журнального отчёта встал на стоящее состояние корзины — вместе с
  тропой её снятия, перерезолвом строк по текущему отображению.
- **Предусловие снятия жёсткой ступени** (`HoldClearanceGate`) меряет
  радиус тем же предикатом, что гейтит терминал сделки.
- **Четырёхзначный результат свежести снят из корпуса** (Т1) — сведением
  контракта к решению Д359, а не постройкой мёртвого кода.
- **Числа риск-аппетита `prod` заполнены значениями `test`** по решению
  держателя; носитель со стороны события — встречный якорь в бэклоге.

## Что ратифицировано и не пере-решается

Всё из v104-v108, плюс:

- **Идентичность строки исполнения — одна на оба вида** (Д407): анкер
  команды объявлен домом одним полем, и второе поле его переписывало бы.
- **Порядок «сперва звено, затем анкер»** (Д409): обратный оставлял бы
  живую строку без команды навсегда.
- **Тропа закрытия без входа ставит терминал звеном** (Д415): прямое
  ребро закрывало сделку без числа.
- **Недоступный итог — повторяемый отказ** (Д416): развязку ведёт бюджет
  попыток, а не тип ошибки.
- **Признаки отбора пишет общий носитель** (Д417), и durable-факт
  финализации охраняет их от перезаписи аварийным терминалом.
- **Корзина нераспознанных перерезолвится каждым проходом** (Д419) — это
  и тропа снятия состояния, и устранение молчаливого искажения области
  сверки.

## Чем проверяется корпус

Девять команд плюс мутационная проба; `code-gate-check.py` — ПРОЙДЕН.
**Тесты:** `mvn -o test -DexcludedGroups=source-api-live` → **228, 0
падений** (+24 в трёх новых классах). Приложение профиля `test`
поднимается со схемой **V23**; обе миграции проверены и на копии схемы
до прогона.

## Что висит

1. **Хвост кода — три живых позиции.** Т2 (три ограничения преконтроля:
   эмитент точечной отмены, величина покрытия на создании, фабрика
   `REPLACE`-ног), Т7 и Т11 адресованы шагу 8. Т10 условная — сработает
   при расширении области преконтроля.
2. **Мягкой биржевой ступени нет, и цена стала счётной:** расхождение
   сверки в боевом режиме допуска ступень **затребует**, а исполнитель
   блокировки на биржевом радиусе её не поднимет — разрыв раскроется тем
   ходом, который снимет разведочный режим.
3. **Боевые числа риск-аппетита не назначены** — в `prod` стои́т
   временная копия профиля `test`; встречный якорь заведён в бэклоге и
   проверяется гейтом закрытия фазы.
4. **Ветвь переоткрытия эпизода транша не построена** — эпизод всегда
   первый, инкремент `+1` писателя не имеет (пункт бэклога).
5. **61 указатель javadoc в несуществующие доки** — пункт бэклога.
6. **`.claude/projects/**` вне таблицы размещения** — диспозиция за
   держателем.
7. **Токен Vault корневой и бессрочный** — адресат шага 9.
8. **`excludedGroups` в `pom.xml`**, **пятая ось
   `preconditions-check.sh`**, **класс «уборка кейса на ветви отказа —
   руками»** — пункты бэклога, заходом не трогались.
9. **Припарковано** — 6 позиций реестра и 44 негейтящие находки `_34`.
10. **Пользователю — обновить Project Knowledge:** этот снапшот, дайджест
    (Д250-Д421).

## Среда

Изменений с v108 нет: контур поднимается вручную после ребута хоста,
Vault после рестарта контейнера запечатан (unseal у держателя), часы
хоста склонны к рассинхрону. Дом фактов —
`.claude/tests/source-api/okx/code-preconditions.md`. Токен
`.env.vault.test.local` действует; порт 8080 занят IDEA-прогоном —
контурная проба поднималась на 8099. **Maven в `PATH` отсутствует**:
рабочий бинарь — из связки IDEA
(`.../plugins/maven/lib/maven3/bin/mvn.cmd`) при `JAVA_HOME` на
`corretto-25.0.3`.
