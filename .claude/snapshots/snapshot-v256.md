# Snapshot v256

**Дата:** 2026-09-15.

## На какой вопрос отвечает этот файл

Где мы сейчас. **Машина шага переработана решением держателя; шаг 12
фазы 2 («Тесты») стои́т в `CONCEPT_REVIEW`.** Сменяет v255.

## Состояние

Фаза 1 — `FOLDED`. **Фаза 2 — `IN_PROGRESS`**: шаги 1-11 `DONE`, шаг 12 —
`CONCEPT_REVIEW`, шаг 13 `HOLD`. Прод-рубеж — `HOLD`.

**Машина шага с этого дня** —
`CONCEPT → CONCEPT_REVIEW → CODE → DOCS → REVIEW → DONE`
(`.claude/processes/roadmap-step-execution.md`,
`.claude/decisions/step-machine-rework.md`). Доковой петли до кода,
реестра контакта с кодом, популяций, мини-петель и их энфорсеров больше
нет; остались энфорсеры корпуса и свипы по роду правки
(`.claude/rules/edit-kind-obligations.md`,
`.claude/rules/measurement-commands.md`).

**Гейтящих выносов держателю нет.** Ближайший ход держателя — апрув в
`REVIEW` шага 12, когда шаг до него дойдёт.

**Дельта — в рабочем дереве, не закоммичена.** Ветка `claude-audit`.
Дерево кода тронуто тремя javadoc-указателями.

**Режим — автономия до прод-рубежа.** Решения — Д250-Д1552.

## Что сделано после v255

**Переработка машины шага** — итог и перечень снятого:
`.claude/work/history/2026-09-15-step-machine-rework.md`; снятые носители —
подпапка рядом (`decisions/`, `rules/`, `skills/`, `templates/`, `tools/`,
`work/`, `replaced/`).

- Процесс шага, скилл прогресса, ревью концепции, единица сессии, стоячий
  промпт, роли `reviewer` и `tester`, три тестовых скилла и шаблон кейсов
  переписаны; свипы и нормы измерения получили свои дома-правила; шаблон
  отчёта фокуса ревью заведён вместо gap-отчёта.
- Реестр снятых редакций: тринадцать записей сняты, пять перенацелены, две
  заведены. PROC-Q3 закрыт. Бэклог: маркеров под снятые статусы не было,
  тела четырёх секций переписаны, одна закрыта.

## Вход следующей сессии

**Следующая единица — `CONCEPT_REVIEW` шага 12**
(`.claude/skills/concept-review.md`): концепция тестирования — в хронике
шага (`.claude/work/progress/phase-2-step-12-chronicle.md` §Концепция);
отчёт — `work/progress/phase-2-step-12-concept-review.md`; вердикты
гейтинга открытых вопросов пересмотреть на шаг 12 (команда — в
`.claude/work/questions/open-questions.md`). Апрув переводит шаг в `CODE`;
`CODE` тестового шага — три под-шага по порядку
(`.claude/processes/roadmap-step-execution.md` §«Конец фазы»).

**Перечень `сейчас`** печатает `py tools/backlog-check.py` — шесть секций,
оживлённых открытием шага, и есть его вход.

**Ловушки** v206-v254 остаются в силе (кроме тех, что о снятых
инструментах). Подтверждённые этой единицей:

- **`py` вместо `python3`** — заглушка Windows Store возвращает 49.
- **Длинные тексты писать файлом**, точечные правки — python-скриптом с
  проверкой «вхождений ровно одно»; heredoc с кириллицей в `bash -c`
  рвётся.
- **Реестр снятых редакций держит популяции на файлах**: перенос или
  переписывание носителя роняет `retired-check.py` строками популяции —
  перенацеливать тем же ходом.
- **`anchor-check` печатает номер абзаца, а не строки**; сам адрес искать
  грепом по имени пассажа.

Обязательное перед завершением любой единицы:

1. **гейт инструментов корпуса** целиком — перечень в
   `.claude/skills/update-roadmap-progress.md` §«Гейт инструментов корпуса»,
   `backlog-check.py` с подменой статуса, если статус проставляется;
2. **реактор — `bash tools/reactor-test.sh`**, если тронуто дерево кода.

## Что висит

Всё, что живёт секциями бэклога с маркерами, печатает
`py tools/backlog-check.py` — в снапшоте не дублируется. Вне бэклога висит:

1. **Стенд от дерева кода отстал:** образов двух сервисов не собиралось,
   базы `audit` и `statistics` не пересоздавались (секция бэклога).
2. **Ни одна пара производитель-потребитель не встречалась на живом брокере.**
3. **Пользователю:** обновить Project Knowledge (этот снапшот и
   `.claude/chat/chat-project-instructions.md` — правлен под машину);
   прочитать блок дайджеста Д1544-Д1552 — право вето постфактум.

## Проверки на момент снапшота

**Гейт инструментов корпуса прогнан** целиком, ненулевых кодов нет, кода 2
нет. Реактор не гонялся: код тронут только комментариями.

## Среда

Против v132 не изменилась; полное описание — там, перечень ловушек — в v206
(`.claude/work/history/snapshots/snapshot-v206.md` §Среда) и он остаётся в
силе.
