# Snapshot v133

**Дата:** 2026-09-05.

## На какой вопрос отвечает этот файл

Где мы сейчас. **Шаг 7 фазы 2 — `CODE`: поставлен компонент 2 из 19
(проекции чужих реестров и их синк), проверен живым стендом. Заведена
цепочка сессий без держателя.** Сменяет v132.

## Состояние

Фаза 1 — `FOLDED`. **Фаза 2 — `IN_PROGRESS`**: шаги 1-6 `DONE`, шаг 7 —
`CODE`, шаги 8-12 `HOLD`. Фазы 3-6, 7-8 и прод-рубеж — `HOLD`.

**Дельта в working tree, не в истории.** Ветка `claude-audit`; всё
описанное — staged, не закоммичено (коммит руками).

**Режим — автономия до прод-рубежа.** Решения — Д250-Д746 (итерации 34-36).
**Открытых выносов держателю нет.**

## Что закрыто этим заходом

1. **Цепочка неинтерактивных сессий** — роадмап движется сессиями подряд
   без держателя и останавливается ровно тогда, когда нужен он. Сценарий
   `tools/session-loop.sh`, стоячий промпт `tools/session-prompt.md`,
   инструкция держателю `.claude/skills/session-chain.md`, итог —
   `.claude/work/history/2026-09-05-session-chain.md`.
2. **Компонент 2 шага 7** — проекции реестра счетов и каталога
   инструментов плюс тик синка. Хроника —
   `.claude/work/progress/phase-2-step-7-chronicle.md` §«Компонент 2 —
   проекции чужих реестров и их синк».

## Где стои́т шаг 7

Docs-first пройден целиком (четыре прогона, гейт перехода исполнен).
**Кодовый заход: 2 компонента из 19 поставлены** — модуль со схемой `V1`
и проекции с синком. Реестр компонентов и их статус —
`.claude/work/code-gate-ledger.json`.

**Вход следующего захода — компонент 4** (порт агрегата сделки и транша:
модели, персистентность, маппинг). Компонент 3 размечен, а не пропущен:
`tenant_risk_appetites` заводит тик синка, а `account_instrument_states`
материализуется **первым читателем пары**, и читателей ещё нет — заведи
его раньше, и он был бы неиспользуемым кодом.

**Ратифицировано шагом 7 (не пере-решать):** радиус торговой строки —
пара «счёт × инструмент»; проекции чужих реестров наполняет синхронное
чтение; числа риск-аппетита — на строке тенанта; копия определения
стратегии плюс временный писатель до шага 8; факты исполнения —
**опросом**; отказ соседа по ярусу — `PEER_SERVICE_UNAVAILABLE`.

## Что дала постановка компонента 2 (живой контекст)

| Что | Наблюдение |
|---|---|
| проекция каталога | 473 инструмента; навес правил у 300 — ровно там, где владелец его уже собрал |
| проекция счетов | строка со снимком реестра плюс торговые стартовые (`safety_rung = ACTIVE`, счётчики нулём, база риска пуста) |
| поверхность `auth` | появилось чтение реестра целиком — контракт его давал, формы не было |
| общая библиотека | расколоты перечни `ExchangeAccount.Status` / `SafetyRung`; у `Instrument` донорские значения **не сняты** — донор их читает |
| дефект, найденный прогоном | производная форма репозиторного запроса выбирала строки вместо колонки; поймал живой тик, не сборка |

## Что дальше — вход в следующий заход

1. **Компонент 4 шага 7** — порт агрегата сделки и транша, дальше по
   реестру: контекст прохода, FSM, расчётный слой, риск-гейт, команды,
   исполнители, холды, аномалии, outbox, поверхность.
2. **Кодовые хвосты трёх прежних парковок**: ставка комиссии, предикат
   свежести шага, контракт броска диспетчера.
3. **Аппрув `CODE`** — фокусы `conventions`/`performance`/`disaster`/
   `security` с зафиксированным исходом; автор код не аппрувит.

## Что висит

1. **Новое этим заходом:** донорские значения в расколотых перечнях общей
   библиотеки (`.claude/work/backlog.md` §«Донорские значения в
   расколотых перечнях общей библиотеки»; оживит удаление донора).
2. **Парковки стенда:** срезы стакана, экспорт трейсов, том
   Elasticsearch, политики сжатия гипертаблиц.
3. **Парковки прежних шагов:** сброс кэша ключей по ротации; осиротевшие
   секреты; WS-слушатель фактов исполнения; контекстный тест сервиса
   (цена отсутствия подтверждена ещё раз — см. дефект выше); подпись
   образов; транспорт внутри кластера; темы Kafka; размеры томов;
   ликвидации; ориентир по расходам на хранение.
4. **Прежние хвосты:** S1 Vault-хардненинг, рост таблицы отказов, A12,
   хвост шага 8, долг адресных перекрёстий `docs/**`, дубль эталона
   стратегии в корне.
5. **Пользователю:** переиндексировать проект в IDEA; обновить Project
   Knowledge (этот снапшот, дайджест итераций 35-36). На стенде заведены
   **тестовые** строки тенанта и счёта (`tn-stand-0001`, `ea-stand-0001`)
   — они нужны были, чтобы проверить синк реестра; удалять их или нет,
   решает держатель.

## Проверки на момент снапшота

Десять команд корпуса — код 0 (`spec-run`, `spec-mutation-check`,
`spec-scope-check`, `spec-pointer-check`, `anchor-check`, `retired-check`,
`population-derive-check`, `preconditions-check`, `deploy-layout-check`,
`code-gate-check`). Реактор — `mvn -o test -DexcludedGroups=source-api-live`,
`BUILD SUCCESS`: `auth` 10, `connector-okx` 27, `market-data` 38,
`trading-core` 6, донор 324.

**Живая проверка вместо прогонной:** тик синка проекций отработал на
стенде без ошибок, проекции наполнены живыми соседями.

## Среда

Против v132 не изменилась. Стенд поднят и работает; выкладка теперь
убирает прежние образы сервиса из узла (`tools/stand/deploy-services.sh`),
свободного места на системном диске — около 24 ГиБ.
