# Snapshot v66

**Дата:** 2026-07-13.

## На какой вопрос отвечает этот файл

Где мы сейчас и как сюда пришли. **Тема — методологическая сессия:
поправки CI по разбору находок (диета PK: состав → режим чтения),
проход по базе знания классами дефектов, закрытие шести
вынесенных находок, чистка закрытого из backlog/phase-1 + правило
переноса.** Сменяет v65.

## Состояние

Фаза 1 — `IN_PROGRESS`; шаги 1-6 `DONE`, **шаг 7 «Сделки и P&L» —
`DOCS_CHECK_3`** (концепция закрыта: `GAPS_CLOSE_1` 2026-07-03 —
`result-profit-source.md`; `GAPS_CLOSE_2` 2026-07-04 —
`pnl-finalization-mechanics.md`; гейт `CODE` — после чистого
`DOCS_CHECK_3`), шаги 8-11 `HOLD`. Ветка `claude-audit`,
`HEAD = d35a4a9 (ROADMAP 1-6-5)`.

**В staging, не закоммичено:** большой методологический пакет этой
и предыдущих сессий — правки CI/дайджеста (`.claude/chat/`),
решения (`context-cost-diet.md`), правила (`snapshot-format.md`,
`classification-report.md`, `tech-radar.md`, `codestyle.md`,
`structure.md`, новое `closed-work-transfer.md`), скиллы/процессы/
агенты (дефект-чистка), `backlog.md`/`phase-1.md` (чистка
закрытого), chronicle шага 6 (запись границы 6↔7), history-файл
чистки, этот снапшот. CC не коммитит — ревью/коммит в IDEA за
пользователем.

## Как сюда пришли (после v65)

Шаг 7 прошёл `DOCS_CHECK_1..3` / `GAPS_CLOSE_1..2` (отчёты —
`.claude/work/progress/phase-1-step-7-*.md`; форвард-материал —
`backlog.md` §Шаг 7). Параллельно — методологический трек по итогам
аудита контекстной стоимости (`context-cost-diet.md`):

1. **CI-поправки (находка 6 / п.5 пакета).** Диета PK по составу
   снята: пользователь синхронизирует PK массово, полные
   `structure`/`naming`/`place-knowledge` остаются в PK. Экономия —
   режимом чтения чата: префлайт «критическое на месте»
   (`CLAUDE.md`, `structure-digest.md`, последний снапшот), дефолт
   чтения — дайджест + снапшот, полные файлы точечно; нехватка
   дайджеста лечится дополнением дайджеста. Зафиксировано в CI и в
   `context-cost-diet.md` п.5 (пересмотр 2026-07-13).
2. **Проход по базе знания классами дефектов** (протухшие факты,
   мёртвые временные оговорки, хроника в нормативном тексте,
   мета-обоснования): 17 файлов поправлено механически; детали — в
   отчёте сессии, итог в самих файлах.
3. **Закрытие шести вынесенных находок:** обкатка классификации
   объявлена завершённой (скиллы стабильны с конца мая; снят статус
   «в обкатке» ×4, `classification-report.md` свёрнут до конечного
   результата); `tech-radar.md` разделён (живые ограничения — в
   записях, хроника — по указателям); `integration-okx.md` не
   тронут (И-1/И-2/И-3 — живой провенанс); backlog вычищен от
   закрытого; `phase-1.md` ужат (примечания шага 6 → chronicle);
   мёртвые оговорки «форма снимется с первого применения» сняты в
   6 применявшихся скиллах (жива только у `find-code-examples` —
   не применялся).
4. **Правило переноса закрытого** —
   `.claude/rules/closed-work-transfer.md`; вычищенные итоги — в
   `history/2026-07-13-backlog-phase-1-closed-cleanup.md`.

## Loose ends

- Staged-пакет — ревью/коммит в IDEA.
- Ручные синхронизации после коммита — см. «После коммита».
- `tradingbot.iml`, `vault.hcl` — untracked, не трогать.

## Что дальше

1. Шаг 7: разбор `DOCS_CHECK_3` → при чистом — гейт `CODE`
   (исполнительный хвост — `backlog.md` §Шаг 7: носители стадий
   1-2, N11-верификация агрегации positions-history до CODE).
2. Открытые вопросы шага 7 — `open-questions.md` (OKX-Q3
   funding-путь выбран в G2-механике; остатки — по списку).

## Среда

Тулчейн/инфра — без изменений (см. v62/v64): `mvn` 3.9.11 из
wrapper-dist, `JAVA_HOME=~/.jdks/corretto-25`; boot —
`SPRING_PROFILES_ACTIVE=test`, джобы `*_ENABLED=false`,
`VAULT_TOKEN`/`VAULT_URI` из `.env.vault.test.local`; docker
postgres:16 (5441 test), vault (8200).

## После коммита

1. **Custom Instructions:** текст из
   `.claude/chat/chat-project-instructions.md` скопировать в
   Settings → Custom Instructions (источник правды — поле в
   Settings; правился префлайт + режим чтения PK).
2. **PK:** заменить копии изменённых файлов — `structure-digest.md`
   (правилась шапка), снапшот (v66 заменяет v65); при массовой
   синхронизации PK также подтянутся `structure.md` (правка строки
   `.claude/chat/`) и `CLAUDE.md` (если менялся ранее).
