# Snapshot v115

**Дата:** 2026-09-04.

## На какой вопрос отвечает этот файл

Где мы сейчас. **Шаг 8 закрыт: контур наблюдает сам себя.** Сменяет v114.

## Состояние

Фаза 1 — `IN_PROGRESS`; шаги 1-8 `DONE`, шаги 9-11 `HOLD`. Ветка
`claude-audit`. **Дельта закоммичена** — по указанию держателя на эту
серию задач.

**Режим — автономия до конца фазы 1**; решения — Д250-Д487 в дайджесте.

## Что происходит дальше

**Шаг 9 «Безопасность»** — auth-инфраструктура (Spring Security,
`@PreAuthorize`, `SecurityFilterChain`) плюс остаточный хардненинг
секретов Vault. Содержание прорабатывается docs-first на самом шаге; там
же реактивируется фокус `security-review`. Форвард-материал —
`.claude/work/backlog.md` §S1 / §S2.

## Чем закрыт шаг 8 (2026-09-04)

Итог, хроника и разбор — `.claude/work/history/`
`2026-09-04-phase-1-step-8-anomaly-job.md` и одноимённая подпапка.

Одиннадцать детекторов из двенадцати живых; `A13` — объявленный форвард
шага сопровождения. Закрытие прошло тремя заходами: аппрув `CODE` (три
адверсариальных фокуса, 41 находка), `SYNC_DOCS_FROM_CODE` (29
расхождений), пост-хок концепт-гейт (`_9`/`_10`, 13 находок).

**Все три самые дорогие находки — ложное срабатывание жёсткой ступени**,
то есть снос всей биржи по рынку за собственный штатный ход системы:
петля самоэскалации через обход по одному статусу; `A7` против нашей
закрывающей заявки, у которой клиентского идентификатора нет в контракте;
`A4` без гейта живого обязательства.

## Что ратифицировано и не пере-решается

Всё из v104-v114, плюс:

- **Шаг детекции заводит наблюдателей, а не реакции** — ступень и радиус
  выводятся тремя осями, дом реакции у каждой строки уже существует.
- **Природа факта отчёта — свойство тропы, а не колонка** (Д478): у
  величины нет ни одного читателя, она известна писателю на call-site.
- **Дедуп состояния энфорсится двумя формами** (Д479) — стоящей строкой и
  признаком самого объекта; вторая строже.
- **Окно наблюдения одно на подтверждение и дедуп** (Д483): два окна
  разной ширины запирают признак после пропущенного тика.
- **Отчёт строится тогда и только тогда, когда есть тропа, чью омиссию он
  фиксирует** (Д485).
- **Радиус реакции и адрес наблюдения — разные оси** (Д486).

## Чем проверяется корпус

Девять команд плюс `code-gate-check.py` — зелены. **Тесты: 297, 0
падений.** Приложение профиля `test` поднимается с полным графом;
миграции `V24`/`V25` применены, индекс ключа дедупа на месте.

**Каждая правка закрытия предъявлена падающей пробой** — прежняя форма
возвращалась во временную копию, проба падала, форма возвращалась. Так же
доказаны обе оси команды, добавленной в конвенцию вердиктов гейтинга.

## Что висит

1. **Девять припаркованных позиций хвоста шага 8** — дом, что оживит и
   причина у каждой: `.claude/work/backlog.md` §«Хвост шага 8 (safety /
   AnomalyJob) — возврат по появлению носителя». Самые весомые: носитель
   «последнее наблюдение» (строка одна на окно, а не на всё держание
   состояния) и движение базы риска на терминале сделки — тропы нет ни у
   одного писателя.
2. **`CONV-Q1` — держателю.** Пять случаев, не покрытых `codestyle`;
   маршрут задан ролью ревьюера (конвенции пополняются через чат).
   Поведения кода ни один не меняет.
3. **Две посылки о контракте источника помечены несверенными** —
   обязательность параметра семьи у pending algo и лимит его частоты;
   сверка — предмет контура.
4. **`A13`** — форвард шага сопровождения.
5. **Провизорные числа калибровки** (гистерезис, окно, предел слепоты) —
   свой встречный якорь в бэклоге.
6. **Пользователю — обновить Project Knowledge:** этот снапшот, дайджест
   (Д250-Д487).

## Среда

Изменений с v108 нет. Дом фактов —
`.claude/tests/source-api/okx/code-preconditions.md`. **Maven в `PATH`
отсутствует**: рабочий бинарь — из связки IDEA; лаунчер Python — `py -3`.
Порт 8080 занят чужим процессом — прогон поднимался на `SERVER_PORT=18081`.
