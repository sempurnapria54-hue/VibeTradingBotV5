# Snapshot v70

**Дата:** 2026-07-18.

## На какой вопрос отвечает этот файл

Где мы сейчас. **Тема — шаг 7 «Сделки и P&L»: `GAPS_CLOSE_5` закрыл
свалидированную политику, машина пайплайна перестроена (правки A/B +
S1-S7), `DOCS_CHECK_6` прогнан — НЕ чист (21 находка, 4 узла) →
`GAPS_CLOSE_6`.** Тест правки B пройден: линзы переоткрыли
withhold-развилки сами. Сменяет v69.

## Состояние

Фаза 1 — `IN_PROGRESS`; шаги 1-6 `DONE`, **шаг 7 — `DOCS_CHECK_6`**
(НЕ чист; исход → `GAPS_CLOSE_6`), шаги 8-11 `HOLD`. Ветка
`claude-audit`, `HEAD = da02f24 (ROADMAP 1-7-3 GAPS_CLOSE_5)` — пакеты
`GAPS_CLOSE_5`, правок пайплайна и ревью ролей закоммичены
пользователем.

**В staging, не закоммичено:** пакет `DOCS_CHECK_6` (отчёт, хроника,
статус в `phase-1.md`, дерево) + этот снапшот (v69 →
`history/snapshots/`).

## Как сюда пришли (после v69)

1. **Сверка пакетом по `DOCS_CHECK_5`** проведена пользователем (узел 1 +
   H6/H13 + H5/H11/H15/H17); разошедшееся ушло в withhold.
2. **Правки пайплайна A/B:** закрытие находки — по её списку цитат
   (`roadmap-step-execution.md` §3); ось структурной свободы при
   генерации вариантов.
3. **`GAPS_CLOSE_5`** — материализована только свалидированная политика
   (мягкая реакция, блок-сет по направлению риска, радиус per-режим,
   ярлыки уровня сняты, claim H15 ограничен, перечень + таблица
   scope × reaction в `error-handling-policy`); withhold не тронут,
   в open-questions сознательно не заведён. Инспекция `src/` по H5:
   в коде `Instrument.Status` несёт **оба** — онбординговый `HOLD` и
   safety-`TRADE_BLOCKED`. Отчёт (обход по цитатам) —
   `phase-1-step-7-gaps-close-5.md`.
4. **Ревью ролей reviewer ↔ solution-designer** (по запросу
   пользователя) → правки S1-S7: общий формат gap-отчёта вынесен в
   **`.claude/templates/docs/gap-report.md`** (дом — выбор пользователя;
   обязательное поле «Цитаты»); новый скилл
   **`.claude/skills/design-fork.md`** (проработка развилки владельцем,
   зеркало правки B, эвристики переехали из роли); чеклист
   `conventions-review` дополнен; маршрут эскалации в `reviewer.md` —
   через владельцев; верификация CC кодифицирована
   (`concept-review` Шаг 3) + код-клаузы проверяются по `src/`;
   принцип самодостаточности скиллов уточнён (`structure.md`).
   Запись ревью — `notes/2026-07-18-ревью-ролей-…md`.
5. **`DOCS_CHECK_6`** — три независимые линзы (subagent'ы, прошлые
   отчёты запрещены) + верификация CC (V1-V7). **НЕ чист: 21 находка,
   гейтят 4, зависимых 3.** Отчёт —
   `.claude/work/progress/phase-1-step-7-docs-check-6.md`; хроника —
   `phase-1-step-7-chronicle.md`.

## Живой контекст: узлы `DOCS_CHECK_6` (→ `GAPS_CLOSE_6`)

1. **H1 — стейл-носители реакции** (все три линзы):
   `pnl-finalization-mechanics.md:211-222` и **CODE-пункт**
   `backlog.md:308-310` несут снятую реакцию (kill-switch + ручное
   снятие) против 6 носителей канона. Провенанс — остаток закрытия H2
   `GAPS_CLOSE_5` (носители вне списка цитат; свип не покрыл рабочие
   файлы). Выводимо, curator.
2. **H2+H3+H4 — снятие/enforcement мягкого холда (центр тяжести).**
   Ось снятия не задана; инвентари статусов стейл против кода; ярлыки
   L3/L4 на `AnomalyReport.scope`. **Развилка: холд как state vs
   вычисляемый предикат свежести** — «предикат» = крен всех трёх линз
   независимо (снимает снятие, радиус «аккаунт», зависимый H3).
   Policy-хвост: порог восстановления доверия после длинного отказа.
   Владельцы: solution-designer + trading-specialist → пользователь.
3. **H5 — cross-ccy guard мёртв** (счётом, две линзы): `ccy=USDT` в
   query отсекает нарушение до guard'а; сверка слепа. Крен — убрать
   `ccy` из query/линковки; структурная альтернатива — скан-компонент.
4. **H6 — lifecycle `AnomalyReport`** не вмещает журнальные аномалии
   шага 7 без kill-switch; управляющие устарели. Крен — журнальная
   тропа `CREATED → COMPLETED`; альтернатива — развести журнал/инцидент.

Не гейтят — 14 (детали в отчёте): радиус-принцип не прогнан по
L3-«серии неудач» (H7); две «пустые» клетки матрицы искажений непусты
(H8); свежесть ключа резолва не меряется (H9); fee-эхо под-случай →
расширить §AG3.5 (H10, зависимая); `triggerPx` — три версии → офдок,
integrator (H19); остальное — курация (H11-H18, H20-H21).

**Тест правки B — пройден:** гейтящие withhold-развилки переоткрыты
линзами самостоятельно со структурными вариантами (H5-имя — по коду;
H9-снятие; H17-cross-ccy; H6-радиус; H11-ключ); H1-тропа сузился по
построению. **Не переоткрыты** только не-гейтящие H13/H16/H19 прежнего
прогона — учесть при сверке.

**Кандидат в правку пайплайна** (не материализован, решение
пользователя): политикоменяющее закрытие завершается свип-грепом по
терминам **старой** политики по всем живым носителям вкл. рабочие
файлы (кейс H1).

**Ступень владельцев** — `Предложение` (счёт гейта 0, обнулён на
`DOCS_CHECK_4`); на `DOCS_CHECK_6` превышений нет (флаги — `предложил`
/ `эскалировал`).

## Loose ends

- Staged-пакет (`DOCS_CHECK_6` + снапшот) — ревью/коммит в IDEA.
- `GAPS_CLOSE_6` не начат — сначала дайджест/слепой проход по отчёту
  (сторона чата) и решения по узлам 2-4.
- Кандидат-свип старой политики — не ратифицирован.

## Что дальше (порядок)

1. Дайджест `DOCS_CHECK_6` → чат: слепой проход / решения пользователя
   по узлам (тяжёлый — узел 2: state vs предикат + policy-порог
   доверия).
2. **`GAPS_CLOSE_6`** под правкой A (+ свип-кандидат, если
   ратифицирован); H1 и курация (H11-H21) — выводимые, узлы 2-4 — по
   решениям.
3. **`DOCS_CHECK_7`** — подтверждающий.
4. После чистого прогона — **рантайм-фикстура §AG1.5** одним прогоном
   (N11 + H2-гранулярность + RQ-1…4; H10 расширяет вопросник §AG3.5),
   затем **`CODE`** (исполнительный хвост — `backlog.md` §Шаг 7;
   там же учесть H15: `REFRESH_FILLS`-swap — целевая дельта CODE).

## Среда

Тулчейн/инфра — без изменений (см. v62/v64/v67).

## После коммита

1. **PK:** заменить снапшот (v70 сменяет v69).
2. **Custom Instructions:** в сессии правился
   `.claude/chat/chat-project-instructions.md` (указатель правила
   «Эскалация узла» → `templates/docs/gap-report.md`) — если текст ещё
   не перенесён в Settings → Custom Instructions, перенести.
