# Snapshot v43

**Дата:** 2026-06-10.

## На какой вопрос отвечает этот файл

Где мы сейчас и как сюда пришли. **Тема — ревизия D закрыта:** пересмотр
market-data-модели strategy-layer выбран (вариант D), доведён в доках и коде,
гейты пройдены, шаг 3 фазы 1 переведён в `DONE`. Тема завершена — новый чат
стартует с PK-префлайта, без подъёма этой ветки.

## Состояние

Фаза 1 — `IN_PROGRESS`; **шаги 1-3 — `DONE`**, шаги 4-11 — `HOLD`. Ветка —
`claude-audit`. Всё в working tree, **не коммичено** (CC не коммитит;
коммит и обновление PK — за пользователем).

Ролляп фазы 1 — `IN_PROGRESS` (шаги 1-3 `DONE`, 4-11 `HOLD`); `roadmap.md` —
без изменений.

## Путь к точке (от v42)

1. **v42:** костяная развилка **R1 vs D** вынесена пользователю (упрощение
   market-data-модели). Реализация — после выбора.
2. **Выбор:** пользователь выбрал **D** («перестать шарить результаты»).
3. **Docs-first:** рационал D зафиксирован в доках (полный docs-first):
   5 решений ревизии — owner-ключевание, настройки в собственные строки,
   запрет полиморфных ключей, stateless-фаза, убрать `level` у правила фазы.
   Связность доков проверена (link-integrity, FK-нейминг) — чисто.
4. **Реализация в коде** (двумя стадиями, обе компилируются — JDK 25 +
   IntelliJ Maven):
   - **Stage A** (решения 4-5): фаза stateless (не персистится, вычисляется
     на лету), `MarketPhaseClassifier` → **`MarketPhaseResolver`** (рейн
     согласован с пользователем; `MarketPhaseClassification` снят — `resolve()`
     отдаёт `MarketPhase.Type`), убран `level`, удалён `MarketPhaseJob` +
     persistence-слой фазы; миграция **`V4`**.
   - **Stage B** (решения 1-3): настройки → собственные strategy-scope-
     таблицы (`UNIQUE(strategy_id, key)`, объявлены раз, адресуются по `key`);
     результаты ключуются настройкой-владельцем (реестры/`config_id`/шаринг
     убраны); миграция **`V5`** (~50 файлов: домен, entity, мапперы,
     jobs/services/8 калькуляторов, валидатор, api, пример JSON).
5. **Гигиена:** not-Auditable исключение для таблиц настроек задокументировано
   (`Strategy.md` §инварианты).
6. **DOCS_CHECK пост-D (шаги 1-3), оба фокуса — чисто:**
   - `concept-review`: doc↔doc целостность, link-integrity, нет стале-claims;
   - `trading-review` (акцент stateless-фаза): жёсткий торговый гейт пройден —
     анти-whipsaw операнд-уровневый корпусно-грунтован (KAMA-адаптив, медленный
     кроссовер); STRUCT-Q2 устранён как латентная торговая некорректность (ТВ-4).
   Отчёт — `.claude/work/progress/phase-1-docs-check-post-revision-d.md`.
7. **GAPS_CLOSE находок:** Г1 (стале-нота `config_id` реконсилирована),
   Ф1 → открытый вопрос **PHASE-Q2** (размещение `MarketPhase`), PHASE-Q1
   дописан торговым грунтом, ТВ-4 — строка в keying-решении.
8. **Шаг 3 → `DONE`** (пост-хок концепт-гейт §6a + торговый гейт пройдены).

## Что сделано (ревизия D — закрыта)

- **Решения (доки):** `market-data-result-identity-keying` (переписан под
  owner-ключевание), `market-phase-stateless` (новый), `strategy-tree-persistence`
  (§Ревизия — настройки в собственные строки), `persistence-representation`
  (§Запрет полиморфных ключей + исключение настроек), плюс синхронизированы
  `market-phase-conditional-classification`, `derived-market-data-code-increments`,
  `efficiency-ratio-as-catalog-indicator`, модели/компоненты/правила/процессы.
- **Код:** компилируется; миграции `V4`/`V5`; персистентных стратегий ещё нет
  (данные не переносились — только схема).
- **Доки ↔ код согласованы** по именам (`MarketPhaseResolver`) и схеме
  (owner-FK, strategy-scope-настройки).

## Открытый хвост (всё non-gating)

- **PHASE-Q1** — липкость/гистерезис фазы при stateless-резолве; владелец
  `trading-review`; S0 (анти-whipsaw операнд-уровневый, риск-аппетит принят).
- **PHASE-Q2** — размещение `MarketPhase` как вычисляемого значения (RVO vs
  доменный computed value; конфликт критериев из-за доменного enum `Type`,
  возможна код-правка извлечения enum); владелец `knowledge-curator`/
  `solution-designer`; горизонт — RVO-кластер шага 4+.
- **STRUCT-Q1** — калибровка числовых порогов структуры; фаза 2.
- **IND-Q1** — крипто-надёжность спот-объёма; фаза 4.

## Что висит дальше (по порядку)

1. **Коммит** staged-хвоста (ревизия D + предыдущие staged-правки) — за
   пользователем; CC не коммитит.
2. **Рантайм-проверка в IDEA** (Flyway `V1`-`V5` на dev-БД, тик джоб) — за
   пользователем; на нагрузочную проверку join-fetch дерева со strategy-scope-
   настройками (cartesian по Set-коллекциям — функционально ок, перф-наблюдение).
3. **Шаг 4** фазы 1 (Команды и lifecycle, `ServiceCommand`) — следующий шаг
   роадмапа; стартует штатным процессом (`TOOLING` → `DOCS_CHECK_1` …).

## Режим работы

**Содержательное закрытие** (не отладка пайплайна). Ревизия D — отдельный
трек поверх шага 3, теперь закрыт.

## Синхрон / PK / staged

- **Project Knowledge:** последний снапшот теперь **`snapshot-v43`**
  (заменяет v42 в префлайте — обновить PK после коммита). v42 историчен.
- **Staged:** все правки ревизии D (доки + код + миграции + отчёты + роадмап
  + open-questions + снапшот) застейджены, **не коммичены**.
