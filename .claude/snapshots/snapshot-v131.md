# Snapshot v131

**Дата:** 2026-09-05.

## На какой вопрос отвечает этот файл

Где мы сейчас. **Шаг 6 фазы 2 — `CODE`: сервис `market-data` заведён и
портирован, аппрув не получен.** Сменяет v130.

## Состояние

Фаза 1 — `FOLDED`. **Фаза 2 — `IN_PROGRESS`**: шаги 1-5 `DONE`, шаг 6 —
`CODE` (не закрыт), шаги 7-12 `HOLD`. Фазы 3-6, 7-8 и прод-рубеж — `HOLD`.

**Дельта в working tree, не в истории.** Ветка `claude-audit`; всё
описанное — staged, не закоммичено (коммит руками).

**Режим — автономия до прод-рубежа.** Решения — Д250-Д668 (итерация 29).
**Открытый вынос держателю один:** ARCH-Q1 в остатке — провайдер облака и
CI-система; гейтит прод-рубеж.

## Что закрыто

| Шаг | Что поставлено |
|---|---|
| 1 | монорепозиторий: `donor/`, скелет, вторая ось размещения |
| 2 | платформа: `deploy/base`, три оверлея, `deploy-layout-check.py`, спека контура окружения |
| 3 | `libs/domain-model` (82 класса), реактор, `EventEnvelope`, `InternalIdFactory`, доменный `Order.Side` |
| 4 | `services/auth`: схема `V1`, допуск контура, форма пути ключей, контур доступа на bearer |
| 5 | `services/connector-okx`: 36 операций, 36 эндпоинтов; запись ключей в `auth`; общие носители адреса, состава секрета и причины статуса |

Хроники и отчёты — `.claude/work/history/2026-09-05-phase-2-step-{1..5}-*`.

## Где стои́т шаг 6

**Docs-first пройден целиком** — пять прогонов (6 → 4 → 1 → 0 находок,
плюс контакт с кодом), четыре закрытия. **Кодовый заход выполнен.**
Артефакты — `.claude/work/progress/phase-2-step-6-*`; хроника несёт
состав поставленного и остаток.

**Ратифицировано шагом 6 (не пере-решать):**

- **глубина хранения — из класса ряда**; невосполнимое не чистится в
  `prod`, восполнимое — по глубине потребителя, производные — за свечами;
- **сбор идёт по требованию потребителя**, выраженному синхронной
  командой; полный листинг для советника — конфигурация, не требование;
- **производные ключуются идентичностью вычисления** (Д651), а
  **свежесть — толерантность читателя** (Д652): срок приезжает операндом
  чтения, строка результата его не несёт;
- **ликвидации ждут WS**, стакан и тикер собираются шагом 6;
- **тикер-срез — три агрегатных чтения**, потому что тикер площадки
  марк-цену и индекс не отдаёт;
- **клаузы фазы — операнд вызова** (Д654): market-data стратегий не
  читает; **горизонт бэкфилла — величина группы** (Д655);
  **идентичность заказывается глобально, инструменты приносит сбор**
  (Д656).

## Карта узлов сервиса (что где лежит)

| Узел | Носитель |
|---|---|
| схема и гипертаблицы | `services/market-data/src/main/resources/db/migration/V1__market_data_baseline.sql` |
| каталог и навес правил | `domain/service/InstrumentCatalogService`, `domain/jobs/InstrumentSyncJob` |
| свечной цикл | `domain/service/CandleLoader`, `domain/jobs/CandleJob` |
| реестры идентичностей | `persistence/service/ComputationConfigDataService`, `mapping/ComputationParamsJsonConverter` |
| производные | `domain/jobs/{IndicatorJob,MarketStructureJob}`, `domain/service/{indicator,structure}` |
| фаза | `domain/service/MarketPhaseService`, `domain/service/phase/MarketPhaseResolver` |
| срезы | `domain/service/SnapshotCollector`, `domain/jobs/SnapshotCollectionJob` |
| поверхность | `api/controller/{MarketDataDemandController,InstrumentController,MarketFeatureController,JobController}` |
| выход к площадке | `integration/ExchangeReadClient` (публичные чтения, сервисный токен) |
| интерпретатор условий | `libs/strategy-engine` (новый общий артефакт, Д653) |

## Что дальше — вход в следующий заход

1. **Аппрув `CODE`:** фокусы `conventions` / `performance` / `disaster` /
   `security` с зафиксированным исходом. Автор код не аппрувит.
2. **`SYNC_DOCS_FROM_CODE`:** фокус `divergence`. Дельта известна заранее
   и велика — горизонт бэкфилла на группе, каталог из листинга, площадка
   кодом, ставка комиссии вне владения `market-data`, контракт свечей
   коннектора на доменном таймфрейме, статусы инструмента без
   онбординговой тропы.
3. **Пост-хок концепт-гейт** (`.claude/processes/roadmap-step-execution.md`
   §«6a. Пост-хок концепт-гейт для CODE-инкрементов») — применим:
   Д653, Д654, Д662 — концепт-инкременты, въехавшие на `CODE`.

## Что висит

1. **`подтверждение`** — ARCH-Q1 в остатке: провайдер облака и CI-система.
2. **Парковки шага 6** — в `.claude/work/backlog.md`: §«Ставка комиссии —
   реестр переезжает к владельцу счёта»; §«Предикат свежести шага — к
   владельцу контекста оценки»; §«Политики сжатия и хранения гипертаблиц
   — по пину образа TimescaleDB»; §«Сбор ликвидаций — по добытому
   контракту WS-канала».
3. **Парковки прежних шагов**, там же: §«Сброс кэша ключей по событию
   ротации»; §«Осиротевшие секреты счетов»; §«Манифесты развёртывания
   сервисов»; §«WS-слушатель фактов исполнения»; §«Контекстный тест
   сервиса»; §«Размеры томов данных» (пере-диспозиционирована этим
   заходом); §«Подпись образов и политика допуска»; §«Транспорт внутри
   кластера»; §«Темы Kafka по производителям».
4. **Не проверено прогоном:** кластера и базы в среде нет; ни один сервис
   к живой площадке не ходил; миграция `V1` не применялась ни разу —
   Timescale она требует, и первая проверка этого будет при развёртывании.
5. **Прежние хвосты** — S1 Vault, контракт броска диспетчера, рост
   таблицы отказов, A12, хвост шага 8, долг адресных перекрёстий
   `docs/**`, дубль эталона стратегии в корне.
6. **Пользователю:** переиндексировать проект в IDEA (реактор:
   `libs/domain-model`, `libs/strategy-engine`, `services/auth`,
   `services/connector-okx`, `services/market-data`, `donor`). Обновить
   Project Knowledge: этот снапшот, дайджест (итерация 29),
   `structure-digest.md`.

## Проверки на момент снапшота

Семь команд корпуса — код 0 (новых неразрешимых адресов 0; дефектов
раскладки 0; критерий выхода в `CODE` пройден).
`mvn -o test -DexcludedGroups=source-api-live` → `BUILD SUCCESS`:
`auth` 10, `connector-okx` 27, `market-data` 25, `tradingbot` 324.

## Среда

Без изменений против v130: сборка реакторная из корня, Maven — из
поставки IDEA, `JAVA_HOME=$HOME/.jdks/corretto-25.0.3`, лаунчер Python —
`py -3`. Кластера Kubernetes и базы для прогона нет; сеть доступна.
