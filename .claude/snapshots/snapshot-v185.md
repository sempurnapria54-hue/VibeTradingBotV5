# Snapshot v185

**Дата:** 2026-09-08.

## На какой вопрос отвечает этот файл

Где мы сейчас. **Шаг 10 фазы 2 (аудит и статистика): `GAPS_CLOSE_5` открыт,
его первая единица — узел 1 — ОСТАНОВЛЕНА сигналом С5. Правки в корпус не
приземлены; реакция выбрана CC — концепт-проход по названной посылке, и он
берётся следующей единицей.** Сменяет v184.

## Состояние

Фаза 1 — `FOLDED`. **Фаза 2 — `IN_PROGRESS`**: шаги 1-9 `DONE`,
шаг 10 — **`GAPS_CLOSE_5`**, шаги 11-12 `HOLD`.

**Гейтящих выносов держателю нет.** С5 держателю по умолчанию не выносится
(`.claude/rules/stopped-node-disposition.md`); дефицита `подтверждение` узел не
несёт.

**Дельта staged, не закоммичена.** Ветка `claude-audit`. **Дерево кода не
тронуто**, как в v162-v184; **`docs/**` этой единицей тоже не тронут** —
остановленный узел в корпус не приземляется.

**Режим — автономия до прод-рубежа.** Решения — Д250-Д1177; эта единица
завела Д1171-Д1177.

## Что сделано после v184

**Единица сессии — узел 1 `GAPS_CLOSE_5`** (содержимое события против
потребителя: B1-B4). Отчёт —
`.claude/work/progress/phase-2-step-10-gaps-close-5-node-1.md`; текст правок —
`…-node-1-draft.md` **редакции третьей** (двенадцать правок); заходы —
`…-critique.md`, `…-verification.md`, `…-verification-2.md`. Голова закрытия и
раскладка 23 находок по пяти узлам — `phase-2-step-10-gaps-close-5.md`.

- **Узел остановлен по потолку.** Ряд блокирующих корпусную правку по заходам
  — **6 → 6 → 4**; третьей доработки правило не даёт
  (`.claude/decisions/mini-loop-verification-ceiling.md`).
- **Конструкция узла выдержала все три захода**, и это записано всеми тремя
  отчётами: правило, уровень «носитель», класс `КОД`, обе оси популяции,
  привязка позиций дельты `CODE` к находкам — зелены. Все блокирующие находки
  всех трёх заходов — в **тексте** правок.
- **Диагноз назвал посылку:** правка полна, когда разобраны её **предмет** и
  её **потребители**. Обе оси популяции меряют ровно это и по построению не
  видят **окрестность точки вставки**.
- **Правило узла найдено в корпусе, а не изобретено закрытием** —
  `docs/rules/audit-not-runtime-source.md` §«Что аудит фиксирует»; тот же дом
  на трёх своих пунктах правило не исполняет (правка 6 черновика).
- **Третья письменная форма обхода потребителей** («величина **едет**
  событием») дала десять носителей, которых не видели две первые, — включая
  дом самого правила и компонент-док исполнителя.

## Три вещи, которые дальше нельзя пере-решать

**1. Следующая единица — концепт-проход по окрестности точки вставки, а не
узел 2.** Узлы 2-5 ждут прохода: каждый будет приземлять текст в существующие
пассажи, то есть зависит от той же посылки. Предложение прохода — раздел «Предложение — цельный
проход по окрестности точки вставки» эскалации узла 1; он вводит окрестность
**третьей осью популяции корпусного захода** в
`.claude/skills/closure-population.md`.

**2. Проход под заморозку усиления измерения НЕ подпадает, и довод записан**
(Д1176). Заморожены энфорсеры, оси **команд** и новые дома правил измерения;
проход добавляет обязанность в **существующий** дом, исполняемую чтением, как
и обе нынешние оси. Линейка инструментов корпуса не двигается, прежние
закрытия под новую ось не пересматриваются.

**3. Правки узла 1 не потеряны и не откатываются — они лежат готовыми.**
Черновик редакции третьей несёт дословный текст двенадцати правок с местами
вставки; приземляет их **отдельная единица после прохода**, тем же порядком,
каким закрывался узел 1 `GAPS_CLOSE_4`. Известные дефекты этого текста названы
поимённо: Ю1-Ю4 (блокирующие) и Ю5-Ю11 в
`…-node-1-verification-2.md`.

## Вход следующей сессии

**Единица — концепт-проход по посылке диагноза.** Что он вводит, чего не
строит и чего стои́т — раздел «Предложение — цельный проход по окрестности
точки вставки» отчёта узла 1. После прохода: единица
приземления узла 1 (правки 1-10 черновика плюс снятие Ю1-Ю11), затем узлы 2-5
штатной посерийностью.

Раскладка узлов 2-5 — `phase-2-step-10-gaps-close-5.md` §«Раскладка находок
по узлам»: узел 2 (полный пересчёт: A1, A5, A8, C1), узел 3 (выборки
поверхности чтения: A2, A3, C2), узел 4 (строка состояния приёма: A4, A7, A9,
B7, B8, C3), узел 5 (прочие: A6, B5, B6, B9, B10, C4). **B6 закрывает правка 9
узла 1**; узел 5 её предъявляет, а не закрывает заново (Д1174).

## Что висит

1. **Дельта `CODE` шага 10** — счёт стои́т порождённым блоком в хронике.
2. **Компонентов шага 10 в `.claude/work/code-gate-ledger.json` нет** — их
   вносит простановка `CODE`. Позиции B1-B4 класса `КОД` попадут в реестр
   ходом, который приземляет узел 1.
3. **Позиции парковки узлов 5-6 `GAPS_CLOSE_3`** и парковка узла 1
   (исполнимость формы ценового результата на строках агрегатов).
4. **Образ сервиса без продвинутого тега резолвится подвижным `latest`**.
5. **Форма дедупа «спросить и вставить»** — шесть точек трёх сервисов;
   поглощённый отчёт ручной операции.
6. **Три долга инструментов:** проза реестра
   (`tools/retired-prose-debt.txt`), адресные перекрёстья `docs/**`,
   долг носителей команд (`tools/command-carrier-debt.txt`).
7. **Новый долг, объявленный этой единицей:** §-адрес javadoc в
   несуществующий пассаж существующего файла — два экземпляра в
   `libs/domain-model/.../event/`, класс шире (прогон по `libs`/`services` дал
   58 неразрешимых из 647). Текст парковки — правка 10 черновика; **в бэклог
   он приземляется вместе с узлом**.
8. **Цена сессий под правилом единицы не замерена.**
9. **События `auth` — производителя и темы нет**; экспорт метрик из семи
   прочих сервисов; таблицы `access_denials` нет ни у одного сервиса.
10. **Единый error-DTO у четырёх построенных сервисов.**
11. **Потерянное событие при неразбираемом содержимом** — DLT нет.
12. **Названные ограничения шага 7**, ревалидация активных определений при
    смене чисел риск-аппетита, `HOLD_RELEASED` не пишется, база риска на
    терминале не двигается.
13. **Внешний вызов внутри транзакции БД — двенадцать точек** у ядра.
14. **Донорские остатки в общей библиотеке** — снимаются удалением донора.
15. **Отчёты критики и верификации узлов 5-6 `GAPS_CLOSE_3` отдельными
    файлами не сохранены** — закрыто в части нормы, в части прошлого не
    закрывается ничем.
16. **Три находки седьмого прогона гейта `GAPS_CLOSE_4` (Т-1, Т-2, Т-3)** — о
    тексте о работе; уезжают с отчётом в `history/` при закрытии шага.
17. **Прежние хвосты:** S1 Vault-хардненинг, рост таблицы отказов,
    api-формы периметра без дока.
18. **Пользователю:** переиндексировать проект в IDEA; обновить Project
    Knowledge (этот снапшот). **Стенд от дерева кода не отстал** — код не
    тронут.

## Проверки на момент снапшота

**Гейт инструментов корпуса прогнан на этом состоянии** — весь перечень.
Числа прогонов здесь не пересказываются: их печатает сам прогон
(`.claude/rules/self-description-form.md`).

**Реактор не прогонялся: дерево кода не тронуто.** Последний прогон — на
этом же состоянии кода (замер v163).

**Живого прогона нет ни у одного компонента шагов 7-10.**

## Среда

Против v132 не изменилась; полное описание — там. Ловушки среды из v156-v184
остаются в силе, включая: полный гейт инструментов в 120 с не укладывается —
прогонять фоновой командой; рабочий бинарь — `py` (`python3` и `python` из
PATH — заглушка, её код 49 читается как «не измерялось»); в regex-шаблонах
для `tools/retired-check.py` пользоваться `\s+`; импорт модуля из `tools/`
через `importlib` роняет `__pycache__` в корень репозитория, и
`tools/retired-check.py` отказывает кодом 2 на нём; `cd` внутри составной
команды Bash сохраняется между вызовами; форма `py - <<'PYEOF'` рвётся на
длинном markdown с таблицами — большой раздел писать инструментом записи;
вывод `py` через пайп идёт в cp1251 и рвётся на кириллице (`export
PYTHONIOENCODING=utf-8`); `bash`-heredoc рвётся на многокилобайтном русском
тексте; перенос строки в шаблоне `grep -P` пишется escape-последовательностью;
имя в командном блоке связывается **внутри** блока; в §-адресе имя файла
пишется рядом с адресом; `\b` с кириллицей заменяется на `(?![а-яё])`;
извлечение шаблона из файла якорится различающим началом; в снапшоте снятое
**называется**, а не цитируется; раздел новой единицы называется с её
маркером; литерал `'\\'` внутри heredoc-скрипта приходит одиночным слешем и
рвёт строку; слепая линза, идущая грепом, обязана исключать
`.claude/work/progress/**` **в самой команде**.

**Ловушка `/tmp` в этой среде** (новая, из этой сессии): каталога `/tmp` у
Python нет — `io.open('/tmp/…','w')` падает `FileNotFoundError`, хотя `bash`
тот же путь принимает. Временный файл писать через
`os.path.join(tempfile.gettempdir(), …)`.

**Ловушка однострочного грепа по русскому термину** (новая): вхождение,
разорванное **переносом строки** между словами термина, построчный `grep` не
видит и молчит — на этой единице так потерялось вхождение «Названное\\nограничение»,
и перечень названных ограничений дома вышел неполным. Форма замера — терпимая
к переносу: `grep -oUzP '(?s)…'`.
