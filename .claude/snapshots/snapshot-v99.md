# Snapshot v99

**Дата:** 2026-09-01.

## На какой вопрос отвечает этот файл

Где мы сейчас. **Шаг 7 вышел в `CODE`: доковые циклы остановлены решением
держателя, критерий выхода стал покомпонентным, реестр `_34` переехал в
фазу кода входным гейтом кодовых заходов.** Сменяет v98.

## Состояние

Фаза 1 — `IN_PROGRESS`; шаги 1-6 `DONE`, шаг 7 — **`CODE`** (кодирование
не начато: все 19 компонентов реестра в статусе «не начат»), шаги 8-11
`HOLD`. Ветка `claude-audit`, `HEAD = 21b5c72` — **дельта закоммичена**;
вне коммита только `strategy-examples/` (untracked, парковка D-P6/E-П3).

**Режим — автономия до конца фазы 1**; решения этой сессии — Д244-Д249
в дайджесте.

## Что произошло

- **`DOCS_CHECK_34`** — семь независимых линз, 84 находки, 40 гейтящих
  (14 узлов, 42 позиции). Сработал машинный С2 (53 % → 55 % находок-следствий
  два прогона подряд), закрытие остановлено целиком, эскалация вынесена.
  Отчёт — `.claude/work/progress/phase-1-step-7-docs-check-34.md`.
- **Решение держателя** — выход в код сейчас, реестр `_34` как входной
  гейт покомпонентно (`.claude/decisions/code-contact-as-gate.md`);
  `GAPS_CLOSE_34` не проводится, эскалация закрыта **сменой рамки**, а не
  снятием посылки самосертификации.
- **Переход в `CODE`** — отчёт
  `.claude/work/progress/phase-1-step-7-code-transition.md` (что
  остановлено, куда приземлилась поправка, что переехало, готовность).

## Что теперь держит механизм

- **Гейт `CODE` покомпонентный:** `КОД` закрывается до или в момент
  кодирования **своего** компонента (решением, с правкой доков тем же
  ходом); `РИСК` — прежде любого компонента **торговой тропы**; открытый
  слот грунта гейтит свой компонент. Дом —
  `.claude/processes/roadmap-step-execution.md` §Гейты.
- **Реестр** `.claude/work/code-gate-ledger.json` — сквозной носитель
  фазы кода: 42 позиции, 19 компонентов, пять заходов; прогоняется при
  переходе **и перед каждым кодовым заходом**.
- **`code-gate-check.py`** — 24 оси (было 15); **`retired-check.py`** —
  17 записей, 77 строк популяций.
- **Заморозка усиления измерения снята** — своим собственным условием
  («до входа в `CODE`»), не отдельным решением.

## Чем проверяется корпус — десять команд

Все десять зелены голым запуском с объявленной среды (числа печатают
прогоны; ориентир: 554 примера, 381 величина, 6 популяций / 64 члена,
229 указателей, 550 адресов при долге 56, 17 записей реестра, 42 позиции
гейта). Код возврата 2 = «не измерялось».

## Карта заходов — живой контекст кодовой фазы

| Заход | Компоненты | Торг. тропа | Гейты на входе |
|---|---|---|---|
| 1 | `DealStateMachine`, `DealTrancheStateMachine`, `DealActionState`, `DealContextService`, `DealOrchestratorJob` | нет | A5, B3, B5; возврат парковки E3 |
| 2 | `StrategyActionOrchestrator`, `MarketDataExpirationChecker`, `StrategyValidation` | нет | B1, B2, B6, B8, B10, F4; возвраты B4, B9, E2 |
| 3 | `TranchePrecheckHandler`, `RiskBlockResolver`, `PriceCalculator`, `SafetyHoldCoordinator` | **да** | D1-D5, B7 + **все четыре `РИСК` до старта** |
| 4 | `AttachedAlgoOrderStateResolver`, `CancelAlgoOrderExecutor`, `CreateAlgoOrderExecutor` | **да** | C1, C6 (`РИСК`), C2-C5 |
| 5 | `RefreshPositionExecutor`, `DealOpeningService`, `RefreshBillsExecutor`, `DealCashFlow` | **да** | A1, A3, F2, F3; возвраты A2, A4а, A4б; **4 слота грунта** |

## Что ратифицировано и не пере-решается

Всё из v98, плюс:

- **Гейт `CODE` — покомпонентный, а не пороговый** (решение держателя);
  снятая редакция «`КОД`/`РИСК` в ноль до перевода статуса» — в реестре
  снятых с популяцией носителей.
- **Гейт грунта читается в той же рамке** (решение CC Д244; вето
  постфактум возвращает буквальное прочтение).
- **Составная находка даёт две позиции реестра только при двух разных
  дефектах**; два признака одного дефекта сводятся старшинством классов.
- **Контракт «сводка отчёта ↔ реестр гейта» живёт в доме формата
  отчёта** (`.claude/templates/docs/gap-report.md`), не в регулярке
  инструмента.
- **У эскалации детектора стагнации есть третий исход — смена рамки**;
  выносится наравне с двумя прежними
  (`.claude/processes/roadmap-step-execution.md` §4).

## Что висит

1. **Заход 1 — четыре гейта до первой строки кода:** A5 (правый операнд
   сверки экспозиции — сделка без входа не закрывается ни при одном
   прочтении), B3 (форма строки исполнения действия уровня сделки), B5
   (исчерпание бюджета стратегийной строки: один носитель против трёх),
   возврат парковки E3. Каждое — решение с правкой доков тем же ходом.
2. **Четыре позиции `РИСК`** (C1, C6 — покрытие встроенной защиты; F2,
   F3 — справочник движений счёта) обязаны закрыться **до захода 3**;
   гейт падает на попытке начать торговый компонент раньше.
3. **Слоты грунта — четыре, все на заходе 5:** п. 2 × `AG6.1`, п. 2 ×
   `AG6.2`, п. 7 × `AG1.7`, п. 16 × `AG6.1`. Владелец — `tester`; два из
   четырёх закрываются перепрогоном уже написанного кейса контура.
4. **Припарковано:** 14 позиций реестра (`.claude/work/backlog.md`
   §«Гейты фазы кода шага 7 — припаркованные позиции реестра»; семь
   возвращаются перед конкретным заходом) и 44 негейтящие находки (там же,
   §«Негейтящие находки `DOCS_CHECK_34` — по владельцам»).
5. **Держателю — три числа конфигурации при запуске** (риск на сделку,
   множитель потолка, предел серии убытков).
6. **Вето постфактум по Д244** — покомпонентное чтение гейта грунта;
   отмена возвращает шаг в пред-`CODE` до закрытия четырёх слотов.
7. **Адресный долг:** `.claude/**` — 56 под храповиком; `docs/**` — 62.
8. **Курационный заход** после первого кодового захода: G1/G3/G4/G6,
   E1/E9, гигиена `_34`; при закрытии шага — перенос закрытой петли
   `_26`…`_34` из `progress/` в `history/` (сейчас она держит живые
   входы: реестр называет отчёт `_34`, гейт его читает).
9. **Пользователю — обновить Project Knowledge:** этот снапшот, дайджест
   (Д221-Д249 прочитан пакетом), решение `code-contact-as-gate.md`.

## Среда

Без изменений с v98: Windows-хост (Git Bash, `py -3`), Docker-контур,
Vault не-dev; **дом фактов среды один** —
`.claude/tests/source-api/okx/code-preconditions.md` §«Среда контура —
проверено прогоном, не выведено». Vault-токен `.env.vault.test.local`
протух (перевыпуск — шаг 9); `mvnw` в репо нет.
