# Snapshot v7

**Дата:** 2026-05-26.

## На какой вопрос отвечает этот файл

Где мы сейчас и как сюда пришли (срез на дату).

## Состояние

С момента `snapshot-v6.md` (2026-05-25) проведена вторая обкатка
скилла классификации — прогон шести торговых моделей из архива и
разбор его результатов. По итогам закрыт блок открытых вопросов и
расширена структура типов.

## Что изменилось относительно v6

**Решения (новые):**
- `.claude/decisions/model-granularity.md` — что считать
  самостоятельной моделью, а что разделом внутри родителя. Закрыл
  NQ-A.
- `.claude/decisions/rule-source-of-truth.md` — первоисточник
  правила = его владелец (сущность / сквозной слой / маппинг /
  процесс / компонент). Закрыл Q1, Q2, Q3 (один decision на три).
- `.claude/decisions/client-layer-docs.md` — новый каталог
  `docs/client/<Биржа>/` под exchange-specific знание. Закрыл NQ-B.

**Структура (`structure.md`):**
- Добавлены строки `docs/client/<ExchangeName>/models/` («какие поля
  у модели API биржи?», PascalCase) и
  `docs/client/<ExchangeName>/rules/` («какое правило биржи?»,
  kebab-case).

**Скилл (`classify-type.md`):**
- В перечень продуктовых типов добавлены «модель API биржи» и
  «правило биржи».
- В признаки различения добавлены: «модель vs раздел внутри»
  (гранулярность), «первоисточник правила» (пять владельцев),
  «биржевое знание vs доменное» — со ссылками на новые decisions.

**Открытые вопросы:**
- Q1, Q2, Q3 удалены (закрыты `rule-source-of-truth.md`).
- NQ-C, NQ-D, NQ-E обсуждены и закрыты без отдельных decisions:
  NQ-C — частный случай гранулярности/lifecycle; NQ-D — активацией
  типа `docs/components/`; NQ-E — продуктовые вопросы Deal §15
  переносятся при будущей миграции Deal.
- Открытых общих вопросов нет.

**Тип `docs/components/`:**
- Введён ещё первой обкаткой, но не использовался. Признан рабочим
  домом для исполнителей (resolver / executor / checker / factory /
  job); вводится в активный оборот при миграции.

**Рабочее:**
- `.claude/work/backlog.md` — задача «Миграция архивных торговых
  сущностей в `docs/`».
- Вторая обкатка переведена в
  `.claude/work/history/2026-05-26-вторая-обкатка-классификации-торговые-модели.md`.
- Сырой материал прогона —
  `.claude/notes/2026-05-26-обкатка-классификации-торговые-модели.md`.

## Текущая структура

См. `.claude/rules/structure.md` (добавлены строки
`docs/client/<ExchangeName>/`).

**Продуктовые каталоги:**
- `docs/*` — по-прежнему заготовки с `.gitkeep`; реальная миграция —
  предстоящая задача из backlog.

## Активные задачи

Нет. Вторая обкатка завершена и переведена в `history/`.

## Открытые общие вопросы

Нет. Q1/Q2/Q3 закрыты `rule-source-of-truth.md`; Q4 закрыт в v6
`chat-vs-cc-knowledge-split.md`.

## Что в работе

- Предстоит миграция архивных торговых сущностей в `docs/` (в
  backlog, запуск в новом чате).
- Каркас классификации после второй обкатки: шаги 1-3 рабочие;
  гранулярность и первоисточник правил теперь покрыты decisions;
  скиллы `classify-area` и `classify-theme` — по-прежнему каркасы без
  содержания.
