# Snapshot v95

**Дата:** 2026-08-30.

## На какой вопрос отвечает этот файл

Где мы сейчас. **Грунт добран до одного открытого слота, все узлы
`GAPS_CLOSE_29` закрыты посерийно, `DOCS_CHECK_30` прогнан — но неполным
составом, и это записано ограничением.** Сменяет v94.

## Состояние

Фаза 1 — `IN_PROGRESS`; шаги 1-6 `DONE`, шаг 7 — `DOCS_CHECK_30`, шаги
8-11 `HOLD`. Ветка `claude-audit`, `HEAD = 5306196`; вся дельта сессии — в
рабочем дереве **staged, не закоммичена**.

**Режим — автономия до конца фазы 1**; решения этой сессии — Д177-Д191 в
дайджесте.

## Что произошло

- **Добор грунта** по двум решениям держателя: п. 17 закрыт наблюдением
  под поднятым фикстурным потолком, п. 10 выведен из-под гейта и закрыт
  заменителями (`.claude/decisions/unorderable-fact-substitutes.md`).
- **Узлы `_29` — все закрыты** посерийным проходом; отчёт —
  `.claude/work/progress/phase-1-step-7-gaps-close-29.md`.
- **`DOCS_CHECK_30`** — одна гейтящая находка, закрыта в том же заходе;
  отчёт — `.claude/work/progress/phase-1-step-7-docs-check-30.md`.

## Чем проверяется корпус — одиннадцать команд

| Команда | Что меряет | Состояние |
|---|---|---|
| `bash tools/spec-run.sh` | примеры сходятся | 22 спеки, 448 примеров |
| `bash tools/spec-mutation-check.sh` | величина что-то проверяет | 360 величин, недоказательных **0** |
| `bash tools/spec-mutation-probe.sh` | оси замера | 7 осей |
| `py tools/spec-scope-check.py docs/spec` | области, дом, копия формы | A:0 B:0 C:0 |
| `bash tools/spec-scope-probe.sh` | оси детектора | 6 осей |
| `py tools/spec-pointer-check.py` | **указатель ведёт в дом величины** | 106, чужих 0 — **новый** |
| `bash tools/spec-pointer-probe.sh` | оси нового детектора | 2 оси + контроль |
| `bash tools/preconditions-check.sh` | реестр == план == фактика | СОШЛОСЬ (ось 4 — новая) |
| `py tools/anchor-check.py` | разрешимость §-адресов `.claude/**` | 276 адресов, **37 неразрешимых** — в бэклоге |
| `bash tools/anchor-probe.sh` | оси детектора адресов | 4 оси + 3 контроля |
| `mvn -o test-compile` | дерево собирается | собирается |

## Грунт — одиннадцать слотов из двенадцати

Реестр и команды — `.claude/tests/source-api/okx/code-preconditions.md`.

**Открыт один слот — п. 7** (горизонт `fundingFee`): нужно удержать
позицию через границу funding-интервала (00:00 / 08:00 / 16:00 UTC) и
перепрогнать `ag1_7_numericFieldSemantics`. Ветвь сверки в код-тесте уже
есть.

**П. 10 из гейтящей таблицы снят** — не закрыт, а выведен из-под гейта:
эпизод ADL инициирует биржа. Три заменителя названы поимённо, третий —
пункт живого наблюдения на запуске.

**Несущий факт сессии: защита переживает терминал родителя.** При непустом
наливе встроенная защита материализуется самостоятельной живой заявкой на
налитый объём; исход не зависит от инициатора терминала, `algoId` живой
записи не равен `attachAlgoId` родителя, обратной ссылки нет. Отсюда
`CANCEL_BY_PARENT` разведён по наливу родителя.

## Что ратифицировано и не пере-решается

- **База риска следует за балансом в обе стороны, автоматически** (v94);
  делитель всех четырёх потолков — снимок базы, а при его отсутствии
  живая база счёта.
- **Нотинал укладывается в потолок с запасом** `0.01` (v94).
- **Точечных ручек в фазе 1 нет** (v94).
- **Дом закрытой формы риска один** — `docs/spec/risk-at-stop.json`.
- **Адресуемая единица эпизода — пара** `(externalId, externalCreatedAt)`:
  `posId` переиспользуется, наблюдено на восьми эпизодах подряд.
- **FSM транша прогоняют два обработчика из трёх** (активная сделка и
  координированный выход; ошибочное состояние — нет).
- **Имена DTO источника — суффиксом** `{Сущность}{Биржа}Response`.

## Что висит

1. **Держателю — три числа конфигурации при запуске:** максимальный риск
   на сделку, максимальный множитель катастрофического потолка, предел
   серии подряд убыточных сделок. До задания — штатный отказ.
2. **`DOCS_CHECK_30` прогнан неполным составом** — семь независимых линз
   не запускались. Строка `_30` в детектор стагнации **не подаётся**;
   следующий полный прогон сравнивается с `_29`. **Гейт `CODE` не снят**
   ни этим прогоном, ни грунтом.
3. **`CODE`-дельта прирастает** позициями этого захода: колонка
   `deals.entry_market_phase`, тройной ключ эпизода, энфорсер запрета
   входа в окне сворачивания, энфорсмент жёсткой ступени по обоим
   радиусам, разведённая идемпотентность отчёта, операнд налива родителя
   у судьбы встроенной защиты, `DEAL_GRAPH_INCOMPLETE` у преконтроля,
   два реджекта положительности долей, снятие `MANUAL_*` вместе с
   javadoc трёх payload'ов.
4. **Адресный долг:** `docs/**` — 62 (было 194), почти весь внутри двух
   файлов-гигантов; `.claude/**` — 37 неразрешимых. Оба измеряются
   командами и заведены в бэклог.
5. **Гигиена корпуса и распил гигантов** — курационный заход после
   закрытия шага 7.
6. **Пользователю — обновить Project Knowledge:** этот снапшот,
   `docs/concept.md` (менялся — класс «имя величины спеки»),
   `.claude/rules/structure.md`, `.claude/rules/codestyle.md`, дайджест
   решений (непрочитанные пакеты, включая Д177-Д191).

Корпус: 237 файлов знания в `docs/` (215 доков и 22 спецификации).

## Среда

Без изменений против v94: Linux, Docker-контур поднят, Vault распечатан,
JDK 25 — `~/.jdks/corretto-25`, Maven — из кэша wrapper'а. Точные команды
прогона — `.claude/tests/source-api/okx/code-preconditions.md`.
