# Snapshot v243

**Дата:** 2026-09-12.

## На какой вопрос отвечает этот файл

Где мы сейчас. **Слой сообщения отделён от интеграции, общие артефакты сведены
в один дом `services/common/`, каталог `libs/` снят. Шаг 11 не открыт; вход в
него по-прежнему разрешён заранее.** Сменяет v242.

## Состояние

Фаза 1 — `FOLDED`. **Фаза 2 — `IN_PROGRESS`**: шаги 1-10 `DONE`, шаги 11-12
`HOLD`. Прод-рубеж — `HOLD`.

**Гейтящих выносов держателю нет.** Единственный вынос — `BFF-Q1` с вердиктом
«не гейтит, адресат шага 12».

**Дельта staged, не закоммичена.** Ветка `claude-audit`. **Дерево кода тронуто
сильно** — реактор прогонялся и зелен.

**Режим — автономия до прод-рубежа.** Решения — Д250-Д1480.

## Что сделано после v242

**Единица — запрос держателя целиком:** разделить слой интеграции, ввести слой
сообщения, свести общие артефакты. Отчёт захода —
`.claude/work/progress/message-layer-and-common-artifacts.md`; разбор
альтернатив — `.claude/decisions/message-layer-split.md`.

- **`integration` разделён** на `internal/{event,api}` и `external/api` во всех
  шести сервисах, у которых этот пакет был. Дом правила —
  `.claude/rules/codestyle.md` §«Слой сообщения: внутренняя шина».
- **Формы сообщений получили суффикс `Message`, свой пакет и мапперы.**
  Фабрики `*Content.of(доменная модель)` сняты; писатели факта уехали из
  `domain.event` в слой шины и получили доменную поверхность
  (`CoreEventWriter`, `StrategyEventWriter`).
- **Каталог `libs/` снят.** Общие артефакты живут в `services/common/`; модели
  сгруппированы и разведены по слою — `common/model/{domain,message}`.
- **Измерение приведено к новой раскладке:** ось 1a снята вместе с каталогом,
  ось 1b опознаёт артефакт по `pom.xml` (а не по глубине), популяция
  `enum-domain-pointer-check` выводится теперь и из порождённых носителей.
- **Три отчёта закрытых единиц перенесены в `history/`**, область энфорсеров
  занял отчёт этой единицы.

## Четыре вещи, которые дальше нельзя пере-решать

**1. Раскладка границы — решение держателя.** `integration` делится по
**собеседнику** (`internal` — сосед по монорепозиторию, `external` — система
вне его), шина живёт слоем `internal/event`. Вызов соседа остаётся интеграцией
и в третий слой не выносится (Д1463).

**2. Дом общих артефактов один и лежит в `services/common/`.** Модели
группируются каталогом и разводятся по слою; сам `model` артефактом не
является. Артефакт опознаётся по `pom.xml`, а не по глубине (Д1476-Д1477).

**3. Домен формы сообщения не держит — ни в сигнатурах, ни транзитом.**
Признак механический и мерится грепом по двум осям: импорт пакета форм в
`**/domain/**` и слово `Message` в его теле; обе обязаны быть пусты. Прогона у
этого признака нет — он держится дисциплиной и этой записью.

**4. Открытие шага 11 — своя единица, а не флип статуса** (перенесено из v242 и
остаётся в силе). Простановка статуса **сама** оживляет секции бэклога с
условием `шаг:2-11:открыт`, и каждая обязана получить исход тем же ходом.
Перечень печатает `py tools/backlog-check.py --статус 2-11=DOCS_CHECK_1`.
Разрешение на вход выдано заранее: `HOLD` у шага 11 означает «ни одного
под-шага не сделано», а не «ждём держателя».

## Вход следующей сессии

**Первая единица — открытие шага 11 («Тесты»):** `TOOLING`, затем
`DOCS_CHECK_1`, и **тем же ходом** — исход сработавших секций бэклога.

**Её попутная обязанность:** перенести отчёт этой единицы из
`.claude/work/progress/` в папку истории. Раньше нельзя — пустая область
роняет три энфорсера кодом 2; у задачи своя секция бэклога.

**Ловушки** v206-v242 остаются в силе целиком. Подтверждены и добавлены этой
единицей:

- **`mvn test-compile` без сноса классов даёт ЛОЖНЫЙ зелёный** — maven считает
  модуль собранным по свежести `target/`. Единственная честная сборка —
  `bash tools/reactor-test.sh`; он же ловит вакуум;
- **перечень путей сноса в `reactor-test.sh` пишется руками** — новый уровень
  модулей (`services/common/model/<слой>`) надо добавить туда же, иначе
  прогон вакуумный;
- **`insertion-neighborhood-check` читает текст рабочего дерева, а дельту
  берёт `метка..HEAD`** — несохранённые вставки сдвигают номера строк и
  **маскируют** находки. Проверять на чистом дереве: `git worktree add`;
- **`git worktree add` в длинный путь падает** «Filename too long» — заводить
  в коротком (`/c/wtchk`);
- **`tools/doc-pointer-check.py` печатает стрелку и падает на cp1251** уже
  после вердикта: запускать с `PYTHONIOENCODING=utf-8`, иначе находка не
  видна;
- **греп в python-скрипте: `\\b` внутри не-raw строки — это backspace**, а не
  граница слова; маркеры бэклога правятся raw-строкой;
- **heredoc с `\\n` внутри python-строки** рвётся переносом — писать такие
  куски отдельным файлом-фрагментом.

Обязательное перед завершением любой единицы:

1. **гейт инструментов корпуса** целиком — перечень в
   `.claude/skills/update-roadmap-progress.md` §«Гейт инструментов корпуса»,
   вместе с `backlog-check.py` (с подменой статуса, если статус проставляется);
2. **сборка — `bash tools/reactor-test.sh`**, если тронуто дерево кода;
3. **`py tools/code-gate-check.py`** — перед каждым кодовым заходом.

## Что висит

Всё, что живёт секциями бэклога с маркерами, печатает
`py tools/backlog-check.py` — в снапшоте не дублируется. Вне бэклога висит:

1. **Продуктовая половина B2 у держателя** — `BFF-Q1`, оживитель — шаг 12.
2. **Стенд от дерева кода отстал:** образы четырёх сервисов не собирались,
   роли и базы не заводились, тем и подписки нет.
3. **Ни одна пара производитель-потребитель не встречалась на живом брокере** —
   форма провода проверена сквозным тестом у журнала и чтением у прочих.
4. **Спека и её реализация не связаны ничем механическим** — `SpecRunnerTest`
   гоняет спеки против их собственных примеров; тесты сервисов переписывают
   примеры руками. Наблюдение, задачи пока нет: держатель о нём знает.
5. **Пользователю:** обновить Project Knowledge (этот снапшот) и
   **переиндексировать проект в IDEA** — три Maven-модуля переехали
   (`libs/*` → `services/common/`), один заведён заново
   (`common/model/message`), и один сменил `artifactId`
   (`common-model` → `message-model`).

## Проверки на момент снапшота

**Гейт инструментов корпуса прогнан** на состоянии после всех правок — весь
перечень его дома, ненулевых кодов нет, кода 2 нет; вдобавок
`bash tools/spec-run.sh`, `bash tools/spec-mutation-check.sh` и
`bash tools/preconditions-check.sh`. **Реактор зелен**
(`bash tools/reactor-test.sh`), и число проб за всю единицу не изменилось —
охрана не потеряна. Числа прогонов здесь не пересказываются: их печатает сам
прогон (`.claude/rules/self-description-form.md`).

## Среда

Против v132 не изменилась; полное описание — там, перечень ловушек — в v206
(`.claude/work/history/snapshots/snapshot-v206.md` §Среда) и он остаётся в силе.
