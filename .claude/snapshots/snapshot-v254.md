# Snapshot v254

**Дата:** 2026-09-12.

## На какой вопрос отвечает этот файл

Где мы сейчас. **Шаг 11 фазы 2 исполнен прямым порядком держателя: раздел
`audit-statistics` на `audit` и `statistics` построен, статус —
`SYNC_DOCS_FROM_CODE`.** Сменяет v253.

## Состояние

Фаза 1 — `FOLDED`. **Фаза 2 — `IN_PROGRESS`**: шаги 1-10 `DONE`, шаг 11 —
`SYNC_DOCS_FROM_CODE`, шаги 12-13 `HOLD`. Прод-рубеж — `HOLD`.

**Гейтящих выносов держателю нет.**

**Дельта — staged, не закоммичена.** Ветка `claude-audit`. Дерево кода
тронуто: два модуля вместо одного.

**Режим — автономия до прод-рубежа.** Решения — Д250-Д1536.

## Что сделано после v253

**Шаг идёт прямым порядком держателя, а не штатной машиной**
(`.claude/decisions/step-11-holder-direct-order.md`): дизайн-проход,
`DOCS_CHECK_N` и `GAPS_CLOSE_N` сняты; их место заняло концептуальное
описание каждого сервиса. Отчёт исполнения —
`.claude/work/progress/phase-2-step-11-holder-order-pass.md`.

- **П. 0 — разбор дерева.** Вторая оборванная сессия была доработкой 1 узла 4:
  она закрыла в корпусе все семь находок критики и не написала о себе ничего.
  Разбор дописал два раздела отчёта узла, привёл таблицу заходов к факту и
  закрыл четыре красных гейта. Мини-петля узла не доводится — петля снята.
- **П. 1 — девять концептуальных описаний**, подкаталог
  `docs/architecture/services/`: по файлу на единицу развёртывания, пять осей
  в каждом (что делает и зачем, что держит истиной, что потребляет и
  публикует, какие вызовы делает и принимает, чего не делает намеренно).
- **П. 2 — раздел в коде и деплое.** `services/audit` (переименование с
  вынутой статистической половиной) и `services/statistics` (новый модуль):
  свои факты двух зёрен гипертаблицами, свой слушатель с отбором по признаку
  несомого класса, свой тик состояния приёма, пересчёт по своим фактам, своя
  база, свой образ, свой манифест с тремя сетевыми политиками. Чужих
  подключений не осталось ни одного.
- **П. 3 — документация по факту.** Имя `audit-statistics` сведено по живому
  корпусу; спека полноты переименована в `docs/spec/durable-reception.json`
  (величины `consequenceSeriesStart`, `receptionLowerBound`); спека
  `owner-role-grants.json` снята; инвентарь объявляет девять процессов.
- **Три секции бэклога закрыты** — обе дельты кода шага и оси формы
  `statistics`; итог — `.claude/work/history/2026-09-12-phase-2-step-11-split.md`.

## Вход следующей сессии

**Следующая единица — закрытие шага:** зафиксированные исходы адверсариальных
фокусов. `conventions`, `performance`, `disaster`, `security` — на дельте
`CODE`; `divergence` — на `SYNC_DOCS_FROM_CODE`. Ни один этой единицей не
прогонялся, и без них статус `DONE` не проставляется
(`.claude/skills/update-roadmap-progress.md`).

**После закрытия шага 11 открываются «Тесты» (шаг 12)** — подтверждения
держателя ждать не нужно, разрешение выдано 2026-09-12. На границе шага
держатель обещал отдельным промптом запустить парковку концепции
тестирования.

**Ловушки** v206-v252 остаются в силе целиком. Добавленные этой единицей:

- **`py` вместо `python3`** — заглушка Windows Store возвращает 49.
- **Heredoc с кириллицей в теле `bash -c` иногда рвётся** на разборе
  оболочки: длинные тексты надёжнее писать файлом и применять скриптом.
- **`retired-check` и `spec-pointer-check` видят снапшот** — цитата снятой
  редакции в действующем снапшоте роняет прогон; ротация снапшота их гасит.
- **Уникальность гипертаблицы обязана включать колонку разбиения** — иначе
  Timescale отвергает `create_hypertable`.

Обязательное перед завершением любой единицы:

1. **гейт инструментов корпуса** целиком — перечень в
   `.claude/skills/update-roadmap-progress.md` §«Гейт инструментов корпуса»,
   вместе с `backlog-check.py` (с подменой статуса, если статус проставляется);
2. **сборка — `bash tools/reactor-test.sh`**, если тронуто дерево кода;
3. **`py tools/code-gate-check.py`** — перед каждым кодовым заходом.

**Среда:** `python3` в этой оболочке разрешается в заглушку Windows Store и
возвращает код 49 — инструменты корпуса зовутся через `py`.

## Что висит

Всё, что живёт секциями бэклога с маркерами, печатает
`py tools/backlog-check.py` — в снапшоте не дублируется. Вне бэклога висит:

1. **Фокусы закрытия шага 11 не прогонялись** — ни один из пяти.
2. **Стенд от дерева кода отстал:** образов двух новых сервисов не
   собиралось, база `statistics` с гипертаблицами не заводилась, вторая
   durable-группа на живом брокере не поднималась.
3. **Ни одна пара производитель-потребитель не встречалась на живом брокере.**
4. **Спека и её реализация не связаны ничем механическим** — наблюдение,
   задачи пока нет: держатель о нём знает.
5. **Пользователю:** обновить Project Knowledge (этот снапшот); прочитать
   блок дайджеста Д1522-Д1536 — право вето постфактум.

## Проверки на момент снапшота

**Гейт инструментов корпуса прогнан** целиком, ненулевых кодов нет, кода 2
нет. **Реактор зелен**: компиляция всего дерева и весь unit-набор; числа
печатает сам прогон (`.claude/rules/self-description-form.md`).

## Среда

Против v132 не изменилась; полное описание — там, перечень ловушек — в v206
(`.claude/work/history/snapshots/snapshot-v206.md` §Среда) и он остаётся в
силе.
