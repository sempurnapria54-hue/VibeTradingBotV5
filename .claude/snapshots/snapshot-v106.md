# Snapshot v106

**Дата:** 2026-09-02.

## На какой вопрос отвечает этот файл

Где мы сейчас. **Среда ожила: срок ожидания исполнен оживлением, слот
AG6.2 и замер A4а закрыты, funding-слот добирается.** Сменяет v105.

## Состояние

Фаза 1 — `IN_PROGRESS`; шаги 1-6 `DONE`, шаг 7 — **`CODE`**, шаги 8-11
`HOLD`. Ветка `claude-audit`, `HEAD = 534b3be`; **дельта в рабочем
дереве, staged, не закоммичена**. Вне коммита — `strategy-examples/`
(untracked, парковка D-P6/E-П3).

**Режим — автономия до конца фазы 1**; решения — Д250-Д380 в дайджесте.

## Карта заходов — живой контекст кодовой фазы

| Заход | Компоненты | Статус | Что держит вход |
|---|---|---|---|
| 1 | `DealStateMachine`, `DealTrancheStateMachine`, `DealActionState`, `DealContextService`, `DealOrchestratorJob` | **закодирован** | — |
| 2 | `StrategyActionOrchestrator`, `MarketDataExpirationChecker`, `StrategyValidation` | **в работе** | остаток Т1 (агрегатная поверхность шага) |
| 3 | `TranchePrecheckHandler`, `RiskBlockResolver`, `PriceCalculator`, `SafetyHoldCoordinator` | **в работе** | остаток Т2 (три названных ограничения) |
| 4 | `AttachedAlgoOrderStateResolver`, `CancelAlgoOrderExecutor`, `CreateAlgoOrderExecutor` | **в работе** | Т5 (постановка встроенной защиты) |
| 5 | `RefreshPositionExecutor`, `DealOpeningService` | **в работе** | хвост Т6, Т7, Т8 |
| 5 | `DealCashFlow` | **закодирован** | — |
| 5 | `RefreshBillsExecutor` | **в работе** | остаток Т9 (эмиттер — с агрегатной поверхностью; конфиг-глубина; дедуп отчёта) |

**Реестр гейтов:** открытых позиций ноль; A4а закрыта замером
(2026-09-02); критерий выхода в `CODE` проходит. Хвост кода —
`.claude/work/progress/phase-1-step-7-code-tail.md` (Т1-Т11).

## Что произошло с v105

**Среда ожила (2026-09-02)** — итог и указатели:
`.claude/work/history/2026-09-02-env-revival-ground-batch.md`; снятая
запись об отказе — дом среды; исход решения о сроке —
`.claude/decisions/env-wait-deadline.md` §«Исход решения». Коротко:
очередь расчёта разобралась сама (4 заявки исполнились, 2 отменились;
ответа по тикету 38426982 не было), проба из кода подтвердила терминал,
позиции-хвосты закрыты руками, запрет живых заявок и оси срока сняты.
Первый прогон фикстуры: 8 кейсов зелёные — закрыт слот п. 2 × `AG6.2`
(177 кандидатов в `observations/AG6_2.md`) и замер A4а (равенство меток,
0 мс, посылка подтверждена). Вечером — Т5 (постановка встроенной защиты,
наблюдение `slTriggerPxType` добыто), слой `DealCashFlow` (V16),
операнды окна линковки (V17) и расчётной валюты (V18), fetch-поверхность
bills; по решению держателя закодирован `RefreshBillsExecutor`, не
дожидаясь funding-кейса. Прогон funding-кейса принёс факт `posBalChg`
(V19). **Тесты: 173, 0 падений.** Решения Д375-Д392.

## Что ратифицировано и не пере-решается

Всё из v104/v105, плюс:

- **Терминальный переход доказывается фактом состояния, не принятием
  запроса** — ровно так среда и была сломана: приём шёл, терминала не
  было.
- **Равенство меток источника читается подтверждением посылки «не
  раньше»** — предикат окна включающий (`>=`), Д380.
- **Побочный факт уборки слот не закрывает** — слот закрывается прогоном
  своего кейса (наблюдением), даже когда уборка предъявила предмет
  слота.

## Чем проверяется корпус

Девять команд плюс мутационная проба; `code-gate-check.py` — ПРОЙДЕН
(после снятия осей срока). **Тесты:** `mvn -o test
-DexcludedGroups=source-api-live` → **173, 0 падений** (прогон после
Т5 + захода по движениям счёта). Приложение профиля `test` поднимается
со схемой **V19**; live-контур исполняется.

## Что висит

1. **Funding-слот п. 7 × `AG1.7` — ЗАКРЫТ перепрогоном 2026-09-03**
   (граница 00:00 UTC, `BUILD SUCCESS`): `fundingFee` в записи закрытия
   доступен сразу, оба знака предъявлены, сверка
   Σ(`balChg`+`posBalChg`) сходится точно. Открытых слотов грунта — ноль.
   Попутный факт источника (isolated: расчёт в `posBalChg` при
   `balChg=0`) записан реестром и инвентарём; `posBalChg` — сырым фактом
   до колонки (V19).
2. **Хвост кода Т1-Т11** — ближайшие: Т5 (постановка встроенной защиты;
   после неё — наблюдение `slTriggerPxType`), заход по числам риска
   (Т8, Т10).
3. **Боевые числа риск-аппетита не назначены** — числа держателя;
   `prod` до них не поднимается.
4. **61 указатель javadoc в несуществующие доки** — пункт бэклога.
5. **`.claude/projects/**` вне таблицы размещения** — диспозиция за
   держателем.
6. **Токен Vault корневой и бессрочный** — адресат шага 9.
7. **`excludedGroups` в `pom.xml`** — пункт бэклога.
8. **Пятая ось `preconditions-check.sh`** объявлена и не реализована.
9. **Класс «уборка кейса на ветви отказа — руками»** (`Verify.end` ниже
   упавшего ожидания) — не чинился, предъявлен прогоном 2026-09-02.
10. **Припарковано** — 6 позиций реестра и 44 негейтящие находки `_34`.
11. **Пользователю — обновить Project Knowledge:** этот снапшот, дайджест
    (Д250-Д380).

## Среда

Контур: Docker Desktop поднимается вручную после ребута хоста; Vault
после рестарта контейнера **запечатан** — unseal у держателя; **часы
хоста склонны к рассинхрону** (w32time стоял Manual/Stopped, уход ~3 ч
ломал подпись — ресинк `w32tm /resync` с правами администратора).
Остальное — дом фактов среды
`.claude/tests/source-api/okx/code-preconditions.md` (торговая часть
демо-стыка **работает** с 2026-09-02; запись об отказе — снята). Токен
`.env.vault.test.local` действует.
