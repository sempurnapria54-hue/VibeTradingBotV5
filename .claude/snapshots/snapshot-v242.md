# Snapshot v242

**Дата:** 2026-09-11.

## На какой вопрос отвечает этот файл

Где мы сейчас. **Шаг 10 фазы 2 закрыт `DONE`; межшаговое окно 2-10 → 2-11
отработано — шесть задач-энфорсеров закрыты. Вход в шаг 11 разрешён заранее,
подтверждения держателя ждать не нужно.** Сменяет v241.

## Состояние

Фаза 1 — `FOLDED`. **Фаза 2 — `IN_PROGRESS`**: шаги 1-10 `DONE`, шаги 11-12
`HOLD`. Прод-рубеж — `HOLD`.

**Гейтящих выносов держателю нет.** Единственный вынос — `BFF-Q1` с вердиктом
«не гейтит, адресат шага 12».

**Дельта staged, не закоммичена.** Ветка `claude-audit`. **Дерево кода эта
единица не трогала вовсе** — правились инструменты и корпус; реактор поэтому не
прогонялся, и это не пропуск (`git status --porcelain services/ libs/ donor/`
пуст).

**Режим — автономия до прод-рубежа.** Решения — Д250-Д1457.

## Что сделано после v241

**Единица — межшаговое окно 2-10 → 2-11 целиком.** Отчёт —
`.claude/work/progress/phase-2-interstep-enforcers.md`; итог —
`.claude/work/history/2026-09-11-interstep-enforcers.md`.

- **Шесть секций-энфорсеров закрыты.** Четыре получили свою команду
  (`surface-path-check.py`, `ownership-map-check.py`,
  `insertion-neighborhood-check.py`, `enum-domain-pointer-check.py`), две —
  расширение области и нормализации у `tools/retired-check.py` (подоси 20b/20c
  и ось 26).
- **Гейт инструментов корпуса вырос с четырнадцати команд до восемнадцати.**
- **Прогоны нашли на живом корпусе три вещи:** два двойных именованных владения
  в карте владельцев (сняты), один носитель снятой редакции, невидимый из-за
  javadoc-переноса (механизм теперь его видит), пять §-адресов, повисших после
  снятия секций (переведены).
- **Починен §-адрес в снимке v241** — заход до выдачи задачи, в перечень дельты
  вошёл и объявлен избытком.

## Три вещи, которые дальше нельзя пере-решать

**1. Открытие шага 11 — своя единица, а не флип статуса.** Простановка статуса
**сама** оживляет секции бэклога с условием `шаг:2-11:открыт`, и каждая обязана
получить исход тем же ходом. Их **семь**; перечень печатает
`py tools/backlog-check.py --статус 2-11=DOCS_CHECK_1`. Окно их не брало —
его границы объявлены шестью секциями (примечание в `phase-2.md`).

**2. Разрешение на вход в шаг 11 уже выдано.** Статус `HOLD` у шага 11 означает
«ни одного под-шага не сделано», а не «ждём держателя». Спрашивать заново не
нужно.

**3. Остатки новых энфорсеров названы в шапке каждого и не являются долгом.**
Верность владельца и прозаический кластер у карты; пробы О1/О3/О4 у окрестности;
указатель у носителей помимо записи формы. Это границы измерения, объявленные
вместе с ним, — не задачи.

## Вход следующей сессии

**Первая единица — открытие шага 11 («Тесты»):** `TOOLING`, затем
`DOCS_CHECK_1`, и **тем же ходом** — исход семи сработавших секций бэклога.
Попутная обязанность, у которой своя секция, — перенести отчёт перехода шага 10
и отчёт этого окна в папки истории.

**Ловушки** v206-v241 остаются в силе целиком. Подтверждены и добавлены этой
единицей:

- **`python` в этой среде — заглушка магазина Windows**: `python3 …` корпуса
  здесь исполняется `py …`;
- **новый инструмент обязан переключить вывод на UTF-8** (`sys.stdout.reconfigure`):
  консоль здесь cp1251, и прогон падает на печати находки, а не на её
  отсутствии;
- **вложенный heredoc в Bash рвёт длинный markdown с блоками кода** — писать
  такие файлы отдельным инструментом записи;
- **`grep -P` в этой среде отказывает на не-UTF-8 локали**: перед ним ставится
  `LC_ALL=C.UTF-8`, иначе «supports only unibyte and UTF-8 locales»;
- **снятие секции бэклога роняет `anchor-check.py` по §-адресам на неё**, а не
  только по клеймам: свип по прозе их не находит — находит прогон;
- **пустая `.claude/work/progress/` роняет два энфорсера кодом 2**;
- **греп термина литеральными пробелами даёт ЛОЖНЫЙ НОЛЬ** — перенос строки
  рвёт фразу.

Обязательное перед завершением любой единицы:

1. **гейт инструментов корпуса** целиком — восемнадцать команд, вместе с
   `backlog-check.py` (с подменой статуса, если статус проставляется);
2. **сборка — `bash tools/reactor-test.sh`**, если тронуто дерево кода;
3. **`py tools/code-gate-check.py`** — перед каждым кодовым заходом.

## Что висит

Всё, что живёт секциями бэклога с маркерами, печатает
`py tools/backlog-check.py` — в снапшоте не дублируется. Вне бэклога висит:

1. **Продуктовая половина B2 у держателя** — `BFF-Q1`, оживитель — шаг 12.
2. **Стенд от дерева кода отстал:** образы четырёх сервисов не собирались,
   роли и базы не заводились, тем и подписки нет.
3. **Ни одна пара производитель-потребитель не встречалась на живом брокере** —
   форма провода проверена сквозным тестом у журнала и чтением у прочих.
4. **Спека и её реализация не связаны ничем механическим** — `SpecRunnerTest`
   гоняет спеки против их собственных примеров и объявляет себя «проверкой
   корпуса»; тесты сервисов переписывают примеры руками. Наблюдение сессии,
   задачи пока нет: держатель о нём знает.
5. **Пользователю:** обновить Project Knowledge (этот снапшот). Новых
   Maven-модулей единица не заводила — переиндексация IDEA не нужна.

## Проверки на момент снапшота

**Гейт инструментов корпуса прогнан** на состоянии после всех правок — весь
перечень из восемнадцати команд, ненулевых кодов нет, кода 2 нет; вдобавок
`bash tools/spec-run.sh`, `bash tools/spec-mutation-check.sh` и
`bash tools/preconditions-check.sh`. **Реактор не прогонялся** — дерево кода не
тронуто. Числа прогонов здесь не пересказываются — их печатает сам прогон
(`.claude/rules/self-description-form.md`).

## Среда

Против v132 не изменилась; полное описание — там, перечень ловушек — в v206
(`.claude/work/history/snapshots/snapshot-v206.md` §Среда) и он остаётся в силе.
