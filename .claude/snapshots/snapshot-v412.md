# Снапшот v412

**Дата:** 2026-09-23.

## На какой вопрос отвечает этот файл

Где мы сейчас.

## Состояние

Сменяет v411. Фаза 1 — `FOLDED`. **Фаза 2 — `IN_PROGRESS`**: шаги 1-11
`DONE`, **шаг 12 — `CODE·3/3`**, шаг 13 `HOLD`. Прод-рубеж — `HOLD`.

**Машина шага** — `CONCEPT → CONCEPT_REVIEW → CODE → DOCS → REVIEW → DONE`
(`.claude/processes/roadmap-step-execution.md`). `CODE` тестового шага идёт
тремя под-шагами: тест-кейсы → адверсариальное ревью кейсов → код тестов.
**Под-шаги 1 и 2 закрыты; идёт под-шаг 3 — код тестов.** Уровень 2 написан
целиком; на уровне 1 ящик есть у всех восьми сервисов. Ящики `strategies`,
`audit` и `statistics` написаны целиком; у `trading-core` не пройден хвост с
чужими блокерами; у `bff` написаны группы `B1`-`B3` из одиннадцати.

**Дельта — в рабочем дереве, не закоммичена.** Ветка `claude-audit`. Дерево
кода с последнего зелёного реактора не менялось.

**Режим — автономия до прод-рубежа.** Решения — Д250-Д2032
(`.claude/work/decision-digest.md`).

## Что сделано после v411

Три прохода по пайплайну, дерево кода не тронуто.

- **Правила разделены на ядро и правила со scope.** Ядро — пять правил,
  остальные входят в контекст по чтению файла по маске; безусловная нагрузка
  сессии и субагента упала вдвое. Решение —
  `.claude/decisions/rules-tiering.md`, замер —
  `.claude/work/history/2026-09-23-rules-tiering.md`.
- **Ярус правила объявляется явно, файл без яруса роняет гейт** —
  `tools/rule-tier-check.py`; отчёт —
  `.claude/work/history/2026-09-23-rule-tier-gate.md`.
- **Снапшот отвечает сам, без хвоста.** Постоянное содержимое замыкания
  отсылок прежних снапшотов переехало в дома по своему вопросу; отсылка к
  прежнему снапшоту как к носителю роняет гейт — `tools/snapshot-tail-check.py`.
  Правило — `.claude/rules/snapshot-format.md` §«Снапшот отвечает сам»;
  отчёт — `.claude/work/history/2026-09-23-snapshot-without-tail.md`.

В гейте инструментов корпуса теперь две новые команды.

## Вход следующей сессии

**Единица — ящик `bff` целиком; позиция — группа `B4`** (окно
переигрывания и явный разрыв), дальше `B5`-`B11` в той же сессии, пока не
упёрлась в контекст или время. Группа единицей не является
(`.claude/rules/session-work-unit.md` §«Внутри единицы сессия продолжает»,
§«Порядок закрытия сессии»).

- **Форма кода, обязанности захода и контрольный прогон** —
  `.claude/skills/test-code.md` §«Форма захода под-шага 3», §«Контрольный
  прогон на нейтрализованной сборке»; **ловушки кода тестов** — там же
  §«Ловушки и обходы».
- **Темп шага** — `.claude/decisions/test-step-pace-measures.md`.
- **Обвязка ящика `bff` построена целиком:** `openedStreamOf` (барьерная
  запись на подписку), зонд назначения партиций в `@BeforeEach`, свой
  HTTP-клиент у подписки, `issuedTicketWith`, `subscribeBearing`.
- **Хвост ящика `trading-core`** — `.claude/tests/cases/trading-core.md`
  §«Кейсы, не прогоняемые сегодня».
- **Колонка «Факт» у групп `B1`-`B3` ящика `strategies`** —
  секция `.claude/work/backlog.md` о пустой колонке
  «Факт» кейсов `strategies` у реализованных клеток групп `B1`-`B3`.
- **Хвост апрувленных кейсов `jsonb-overlay-roundtrip`** — там же
  §«Негейтящие находки второго круга кейсов `jsonb-overlay-roundtrip` не
  исполнены»; оживитель — закрытие шага.
- Сквозных прогонов нет ни у одного сервиса.

## Что держателю решать

1. **Источник цены `MARK_PRICE` / `INDEX_PRICE`** —
   `.claude/work/backlog.md` §«Источник цены `MARK_PRICE` / `INDEX_PRICE`
   не отвергается создáнием».
2. **Читатель `exitOutcome`** — там же §«Журнальные отчёты исходов
   округления выхода — читателя `exitOutcome` в ядре нет».
3. **Ключи demo-окружения OKX и распечатанный Vault** — без них не
   прогоняется ни один кейс мишени `-D` и весь `smoke-live`.

Прочие ожидания держателя печатает `py tools/backlog-check.py` строкой
`держатель:`.

## Что висит

Висящее живёт секциями `.claude/work/backlog.md` и
`.claude/work/prod-checks.md` и печатается `py tools/backlog-check.py`; в
снапшоте не дублируется. Этим проходом заведены пять секций: хвост
`jsonb-overlay-roundtrip`, перечень `Order.Type` в доке и три продуктовых
факта, добытых кодом тестов; закрыта секция защиты от рецидива капитализации
`beanspec` — конвенция встала в `.claude/rules/codestyle.md` §«Ловушки».

**Пользователю:** обновить Project Knowledge (этот снапшот); прочитать блоки
дайджеста Д1564-Д2032 — право вето постфактум.

## Ловушки

Ловушки живут в домах по своему вопросу; снапшот их не держит:

- среда, оболочка, правка корпуса скриптом —
  `.claude/skills/environment-commands.md` §«Ловушки и обходы»;
- код тестов — `.claude/skills/test-code.md` §«Ловушки и обходы»;
- письмо кейсов и их ревью — `.claude/skills/test-design.md`,
  `.claude/skills/test-review.md`;
- продуктовый код — `.claude/rules/codestyle.md` §«Ловушки»;
- команды корпуса и свипы — `.claude/rules/measurement-commands.md`;
- гейт и статус — `.claude/skills/update-roadmap-progress.md`;
- цикл сессий — `.claude/skills/session-chain.md` §«Ловушки и обходы».

## Проверки на момент снапшота

Гейт инструментов корпуса — целиком, одним прогоном на финальном состоянии
(`.claude/skills/update-roadmap-progress.md` §«Гейт инструментов корпуса»);
исход — отчёт прохода. Реактор этим проходом не прогонялся: дерево кода не
тронуто, числа последнего зелёного прогона —
`.claude/work/progress/phase-2-step-12-chronicle.md` §«Проверки».

## Среда

Дом фактов и ловушек среды — `.claude/skills/environment-commands.md`;
стенда — `.claude/skills/local-stand.md`. Изменений среды нет.
