# Snapshot v28

**Дата:** 2026-06-05.

## На какой вопрос отвечает этот файл

Где мы сейчас и как сюда пришли (срез после материализации
торгового совета, наполнения и дистилляции торговой библиотеки и
постановки специалиста поверх дистиллята; шаг 2 готов к `CODE`
после торговой валидации советом).

## Состояние

Фаза 1 — `IN_PROGRESS`; шаг 1 — `DONE`; **шаг 2 (Стратегия) —
`GAPS_CLOSE_7`** (закрыт, готов к `CODE` после торговой валидации);
шаги 3-11 — `HOLD`. Из фронтира v27 выполнен пункт 1
(спроектировать и завести торговый совет); впереди пункт 2
(валидация торговой модели шага 2 советом) → затем `CODE`.

## Что изменилось относительно v27

### Торговый совет материализован (коммит `ee426f0`)

По решению чата 2026-06-04 (4 артефакта + decision):

- **Агент** `.claude/agents/trading-specialist.md` — первый член
  «торгового совета» (зонтичное имя): валидирует торговое,
  предлагает варианты, отвечает на торговые вопросы; front-load
  по ходу проектирования. Аналитик — будущая фаза, не заведён.
- **Фокус** `.claude/skills/trading-review.md` — адверсариальный
  проход по торговой корректности; зарегистрирован в реестре
  `.claude/agents/reviewer.md` (активен, под-шаг `DOCS_CHECK_N`).
- **Торговая категория в петле** —
  `.claude/processes/roadmap-step-execution.md`: та же машина
  `DOCS_CHECK_N`/`GAPS_CLOSE_N`; приоритет по тяжести, внутри
  содержательного торговое ведёт (рябь — концептная проверка
  заново); содержательно-блокирующее гейтит `CODE` («модель не
  выражает нужное торговое правило» — жёсткий гейт); два касания
  (специалист конструктивом, критик в петле); forward-находки —
  cross-cutting паркинг.
- **Decision** `.claude/decisions/trading-council.md` — решение с
  пятью отвергнутыми альтернативами; «сбалансированность»
  специалиста намеренно не определена.

### Библиотека наполнена и дистиллирована (коммит `ee426f0`)

- Корпус: 6 книг (Vince, Tharp, Carver ST, Carver AFTS, Kaufman,
  Harris) — выложены пользователем, перемещены в
  `.claude/library/trading/raw/` (гитигнорятся, локальные).
- **Дистиллят** `.claude/library/trading/distilled/` — 5 файлов:
  `corpus-map.md` (издания, офсеты PDF↔печатная страница, главы по
  пиллерам) + 4 пиллера (`risk-and-sizing`, `system-design`,
  `strategy-patterns`, `microstructure`). Тезисы с указателями
  книга/глава/страница; манипуляции — кросс-каттинг внутри
  микроструктуры/риска; расхождения источников — «⚠»; пробелы
  корпуса (крипто-специфика) — «∅ в корпусе нет», не достроены.
- `structure.md`: строки `raw/` (книги) и `distilled/` (дистиллят);
  `.gitignore` — гитигнорится только `raw/`. Прогресс —
  `trading-library-distillation.md` (завершено).

### Специалист поверх дистиллята (staged, не закоммичено)

Проход 2 хэндоффа (детали — «Проход 2» в
`trading-council-materialization.md`):

- **Агент:** компетенция + рыночные манипуляции; рабочее знание —
  дистиллят по пиллерам (грузить только релевантный); PDF —
  источник правды, нырок — исключение по критериям (точная
  формула/порог, условия/исключения, предпосылки сетапа, конфликт
  «⚠», молчание дистиллята); на «∅» не выдумывает — добор через
  чат; стойка — указатель книга/глава/страница.
- **Фокус:** грунтовка тем же дистиллятом (одно знание — один
  дом); примеры манипуляционной наивности (стоп в очевидном месте,
  вход под спуфинг); формат находки — ссылка-указатель.
- **Decision:** дом знания уточнён — `raw/` источник правды +
  `distilled/` рабочее знание.
- Петля и реестр — дельты нет (покрыты проходом 1).

## Активные задачи

- Итерационные progress-файлы шага 2 — в `progress/` до `DONE` шага.
- `trading-council-materialization.md` — отчёт классификации,
  проходы 1-2 завершены.
- `trading-library-distillation.md` — завершено, осталось ревью.

## Текущий фронтир / следующее действие

**Торговая валидация модели шага 2 советом** (пункт 2 фронтира
v27): специалист проходит по торговой модели, задаёт вопросы —
закрываем; адверсариальный торговый фокус — в петле
(`DOCS_CHECK_8`, если будут правки). Только после этого — `CODE`
шага 2.

Коммит — за пользователем (правки прохода 2 staged, CC не коммитит).

## Открытые общие вопросы

`open-questions.md`: **15** (без изменений с v27).

## Что в работе / PK

- Шаг 2 — ждёт торговой валидации → `CODE`.
- **Project Knowledge:** последний снапшот теперь **`snapshot-v28`**
  (заменяет v27 в префлайте). В PK также обновить **`CLAUDE.md`**
  (упоминание библиотеки) и **`.claude/rules/structure.md`**
  (строки `raw/` и `distilled/`) — менялись, закоммичены в
  `ee426f0`. `naming.md` / `place-knowledge.md` не менялись.
- Staged сейчас: `trading-specialist.md`, `trading-review.md`,
  `trading-council.md`, `trading-council-materialization.md`
  (проход 2), этот снапшот.
