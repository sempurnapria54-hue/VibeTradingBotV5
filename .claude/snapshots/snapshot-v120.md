# Snapshot v120

**Дата:** 2026-09-04.

## На какой вопрос отвечает этот файл

Где мы сейчас. **Шаг 9 фазы 1 закрыт: девять шагов из одиннадцати `DONE`.**
Сменяет v119.

## Состояние

Фаза 1 — `IN_PROGRESS`; шаги **1-9 `DONE`**, шаги 10 (тесты) и 11 (фронт) —
`HOLD`. Ветка `claude-audit`, дерево чисто. **Дельта закоммичена** — по
указанию держателя на эту серию задач.

**Режим — автономия до конца фазы 1**; решения — Д250-Д548 в дайджесте.
**Открытых выносов держателю нет**, `.claude/work/progress/` пуст.

## Чем закрыт шаг 9

Итог, хроника и все восемь отчётов прогонов —
`.claude/work/history/2026-09-04-phase-1-step-9-security/`.

**Построен периметр доступа**, которого у корпуса не было вовсе: на
открытии «аутентифик» давало одно вхождение, «авториз» — ноль. Дом
входящего доступа, носитель отвергнутого вызова, перечень кодов auth-отказа
источника (добыт прогоном), дом изъятия из ретраибельного, ступень 2 у
отказа кредов и носитель дискриминатора актора.

**Ряд гейтящих находок: 5, 5, 7, 4, 2, 2, 0, 0.** Перелом — на смене рамки
после первой эскалации.

## Что ратифицировано и не пере-решается

Всё из v104-v119, плюс (Д543-Д548):

- **Второй носитель инвентаря не заводится:** поимённый перечень
  контроллеров снят — закрытие выражается дефолтом контура.
- **Коды `401`/`403` — записанные факты**, но общий перечень кодов
  поверхности остаётся открытым вопросом.
- **Актор строки отказа и её предмет разведены**, и довод — тропа, где
  значения расходятся сегодня, а не выдуманная будущая популяция.
- **Порядок обработчиков оркестратора несущий** — от узкого класса к
  широкому.
- **Чужая машинерия не правится внутри закрытия своего шага** — находка
  паркуется с обеими названными развязками.
- **Указатели продуктового корпуса ведут в носитель, а не в шаг роадмапа.**

## Чему шаг научил корпус

- **Периметр оказался двумя предметами** — входящий и исходящий доступ; их
  сожительство в одном доме и было механизмом, который мерил сигнал С2.
- **Доля при малом знаменателе схождения не мерит** — измеритель поправлен
  объявленной нижней границей (четыре гейтящих находки).
- **Охват правки, не снимающей редакции, не измеряется ничем** — диагноз
  второй эскалации, остаётся верным и незакрытым.
- **Применённая миграция неизменяема, включая комментарии.**
- **Сверка код↔доки обязана идти в обе стороны:** две находки из
  четырнадцати — устаревшие обещания в javadoc, которых доковая сверка не
  видит вовсе.

## Чем проверяется корпус

Девять команд плюс `code-gate-check.py` — все код 0. **Тесты: 319, 0
падений.**

## Что висит

1. **S1 — хардненинг Vault** (политики/approle, ротация, unseal): кодом не
   решается ни один пункт — операции над самим хранилищем, нужен
   root-доступ держателя. Дом — `.claude/work/backlog.md`, S1.
2. **Контракт броска диспетчера команд разошёлся с кодом** — предмет
   машинерии шага 7, обе развязки названы; дом — `backlog.md`.
3. **Два ограничения контура доступа** (CSRF при Basic, проверка хэша на
   каждом вызове) — оживит фронт либо второй субъект.
4. **Рост таблицы отказов не ограничен; поле адреса не заведено** — решается
   вместе с реакцией на серию отказов.
5. **Пересмотр посылки о единственном принципале** — встречный якорь в
   бэклоге.
6. **A12** (слой опровержения в продуктовом корпусе), **хвост шага 8**
   (девять позиций), **долг адресных перекрёстий `docs/**`**, провизорные
   числа калибровки — свои якоря.
7. **Закрытие фазы 1** — не только последний шаг `DONE`: состав пунктов
   держит `.claude/skills/update-roadmap-progress.md` §«Гейт закрытия фазы».
8. **Пользователю — обновить Project Knowledge:** этот снапшот, дайджест
   (Д250-Д548).

## Что дальше

**Шаг 10 — тесты**, затем шаг 11 — фронт. Оба идут по той же машине
docs-first; фронт снимет два названных ограничения контура доступа, потому
что вводит браузерную тропу к поверхности.

## Среда

Изменений с v119 нет. Дом фактов —
`.claude/tests/source-api/okx/code-preconditions.md`. **Maven в `PATH`
отсутствует**; **`JAVA_HOME` среды указывает на JDK 11**, рабочая форма —
`JAVA_HOME=$HOME/.jdks/corretto-25.0.3`; лаунчер Python — `py -3`. Принципал
профиля `test` — в Vault (`secret/tradingbot/api-access-test`).
