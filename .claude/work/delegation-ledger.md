# Леджер гейта делегирования

## На какой вопрос отвечает этот файл

Какой счёт прогонов гейта делегирования у каждой категории владельцев
(данные по доверию в разрезе агентов;
`.claude/processes/question-delegation.md`).

## Текущие ступени

| Категория | Владелец | Ступень | Счёт чистых подряд |
|---|---|---|---|
| Классификация | `knowledge-curator` | Автономия | — (гейт пройден) |
| Трейдинг | `trading-specialist` | Предложение | 0 |
| Концепт | `solution-designer` | Предложение | **0** (обнулён 2026-07-15) |
| Интеграция | `integrator` | Предложение | 0 |
| Тестирование | `tester` | Предложение | 1 |

Гейт: N=3 чистых валидации подряд → предложение повышения,
подтверждает пользователь. Чистая = принято без существенной правки.

Строка **Интеграция** добавлена 2026-06-12 — синхрон с картой
владельцев (`.claude/processes/question-delegation.md`); категория
заведена 2026-06-11, в леджере отсутствовала (рассинхрон закрыт).

## Прогоны

| Дата | Вопрос | Категория | Владелец | Исход валидации |
|---|---|---|---|---|
| 2026-06-06 | PROC-Q1 | классификация | knowledge-curator | принято чисто, материализовано 2026-06-06 (рудимент снят, вопрос закрыт). Вне счёта гейта — категория на Автономии |
| 2026-06-06 | DEAL-Q3 | классификация | knowledge-curator | на валидации отложено (шаг 4: классификация делается при материализации — уточнение правила срока). Вне счёта гейта |
| 2026-06-06 | CMD-Q1 | классификация | knowledge-curator | принято с правкой (payload — раздел у своего executor'а, общий файл отменён); закрыт decision'ом, подвопрос → CMD-Q2. Вне счёта гейта |
| 2026-06-06 | ENUM-Q1 | трейдинг + классификация | trading-specialist, knowledge-curator | снят как архивный артефакт (2026-06-06) — не исход делегирования, в счёт не идёт |
| 2026-06-06 | RISK-Q1 | трейдинг | trading-specialist | снят с прогона перепрогоном по правилу срока: отложен (шаг 5), валидации не было |
| 2026-06-06 | STRAT-Q4 | трейдинг | trading-specialist | снят перепрогоном по правилу срока: отложен (шаг 7), валидации не было |
| 2026-06-06 | OKX-Q1 | трейдинг | trading-specialist | снят перепрогоном по правилу срока: отложен (шаг 7), валидации не было |
| 2026-06-06 | OKX-Q3 | трейдинг | trading-specialist | снят перепрогоном по правилу срока: отложен (шаг 7), валидации не было |
| 2026-06-06 | INSTR-Q2 | трейдинг | trading-specialist | снят перепрогоном по правилу срока: отложен (шаг 5), валидации не было |
| 2026-06-06 | DEAL-Q1 | концепт | solution-designer | снят перепрогоном по правилу срока: отложен (шаг 7), валидации не было |
| 2026-06-06 | INSTR-Q1 | концепт | solution-designer | снят перепрогоном по правилу срока: отложен (шаг 5), валидации не было |
| 2026-06-06 | ORCH-Q1 | концепт | solution-designer | на валидации отложено (конец фазы / оседание оси владения `Instrument.Status`); валидации по существу не было — счёт без изменения |
| 2026-06-12 | Пилотный план тестов API OKX | тестирование | tester | принято чисто (аппрув без существенной правки) — **счёт гейта 1/3** |
| 2026-07-15 | `DOCS_CHECK_4` шага 7: слепой проход по 5 узлам (класс холда; контур FUTURES; ось резолва; знак ставки; измеритель свежести) | концепт | solution-designer (+ CC-сборка эскалаций) | **4 совпадения, 1 существенное расхождение ⇒ счёт обнулён (был 0, остаётся 0).** Совпали: контур SWAP-only, ось резолва по сырому `instType`, знак ставки снимается при маппинге, счётчик обновлений как измеритель. **Разошлось — узел холда:** эскалация подала «L4 целиком vs новый слабый класс», пользователь выбрал **scope = инструмент**. Диагноз — **дефект сборки эскалации, не незнание**: вариант «scope=инструмент» лежал в отчёте находкой линзы C (H15), но в §Варианты не попал — варианты собраны только из линзы A. Правка процесса — §«Сборка эскалации из узла — по всем линзам узла» (легла в `concept-review`; 2026-07-18 секция переехала в `.claude/templates/docs/gap-report.md`) |

| 2026-08-01 | `DOCS_CHECK_6` шага 7: узел снятия/enforcement мягкого холда (H2/H3/H4) | концепт | solution-designer (линзы `concept-review` ×2 + `trading-review`) | **Существенное расхождение ⇒ счёт остаётся 0.** Крен всех трёх линз независимо — «холд не state, а вычисляемый предикат гейта входа» (авто-снятие); пользователь выбрал **ручное снятие**, scope фазы 1, довод — human-in-the-loop (пайплайн в отладке, человек идёт разбирать причину сбоя). **Дефектом сборки эскалации не является:** вариант «ручное снятие» в §Варианты отчёта присутствовал (вариант 2) — сборка отработала. Расхождение — **пользовательский вход фазового веса**: довод «пайплайн в отладке» из доков не выводится, владелец его знать не мог. Агента по этой находке **не дообучаем**. Дообучение сделано по **H9** (свежесть у ключа резолва): линза свернула живую развилку формулой «покрыто доводом, чинится записью» — правка легла в `.claude/skills/concept-review.md` §«Покрыто доводом, чинится записью» |

**Обнуление 2026-07-15 (концепт).** По гейту (`question-delegation.md`
§«Гейт повышения») существенная правка прерывает прогон и обнуляет счёт
чистых подряд. Счёт категории и так стоял на 0, поэтому фактического
отката ступени нет — запись фиксирует **причину**: расхождение вызвано не
качеством ответа владельца, а **сборкой эскалации** (вариант, выбранный
пользователем, до него не доехал). Это делает прогон непригодным для
счёта в обе стороны: 4 совпадения тоже не засчитываются, потому что
слепой проход шёл против неполного ключа. Диагноз и правка процесса —
`.claude/work/progress/phase-1-step-7-gaps-close-4.md` §«Правки пайплайна».

Прогон 3 (2026-06-06, по уточнённому правилу срока): состав пуст —
0 взято, 1 петля текущего шага, 14 отложено с горизонтами; на
валидацию идут только состав и горизонты. Отчёт —
`.claude/work/history/2026-06-06-delegation-validation/delegation-rerun-2-open-questions.md`.

Разборы решений (трейл) —
`.claude/work/history/2026-06-06-delegation-validation/delegation-run-open-questions.md` (прогон 1)
и `.claude/work/history/2026-06-06-delegation-validation/delegation-rerun-open-questions.md`
(прогон 2, правило срока).

| 2026-08-01 | `DOCS_CHECK_7` шага 7: слепой проход по 10 узлам (закрытие — `GAPS_CLOSE_7`) | концепт | solution-designer (линзы `concept-review` ×2 + `trading-review`) | **Пять существенных расхождений из шести решённых узлов ⇒ счёт остаётся 0.** Пользователь снял пять посылок отчёта: (1) у снапшота положения закрытия **есть** durable-дом — `Position`; (2) многоэндпоинтная refresh-команда каноном **допускается**, это не «команда внутри команды»; (3) счётчик попыток **есть** — механизм `StrategyAction`; (4) метку свежести навеса держат **audit-поля строки-владельца**, не JSON; (5) «вход только из `ACTIVE`» относится **только** к мягкой блокировке. Совпало: формула комиссии (H10) — закрытая форма выводима, крен линзы принят. **Дефектом сборки эскалации не является** — варианты собраны по всем линзам узлов. Диагноз общий и структурный: линзы объявляли **отсутствующим то, что уже существует** в соседнем правиле или в коде, и поднимали находку до гейтящей, не установив покрытие. Кандидат в правку пайплайна («прежде чем поднять до гейтящей — установить, не покрыт ли вопрос действующим инвариантом/механизмом») **вынесен на решение пользователя, не материализован** — как и обратный свип, предложенный самим `DOCS_CHECK_7`. Отдельно зафиксирован **дефект отчётности**: флаг действия CC не проставлен на 13 находках из 28 (H16-H28), дефицит не указан на H21 |

| 2026-08-02 | Развилка границы «команда ↔ действие»: **валидация 4** (1 позиция — окно консолидации входа) | концепт | solution-designer | **0 совпадений из 1 ⇒ счёт остаётся 0, ступень `Предложение`.** Крен был «(ii) принять повтор», принято **«(i) закрыть окно»**: перевод `ENTRY_FINALIZED` — одной транзакцией с завершением действия. Существенное уточнение пользователя к квалификации: это **не исключение, а второй экземпляр действующего паттерна** — N7 уже связывает запись `Deal.resultProfit` транзакцией с завершением звена; статусные рёбра уже пишутся командами (`MARK_*`). Диагноз: **непримененная эвристика 4 в обратном направлении** — вариант (i) отброшен доводом «размывает границу handler ↔ действие», то есть подан как новое исключение из слоения, тогда как действующий прецедент того же исключения (N7) держался в §4 той же проработки; при его учёте довод исчезает. Правка — `design-fork.md` эвристика 4: отбрасывая вариант как «исключение из правила», искать действующий прецедент такого же исключения; найден и структурно совпадает — довод-отбрасыватель снимается. **Развилка решена целиком в своём охвате** (4 валидации; счёт по позициям: В1 — существенная правка посылки, В2 — 1/4, В3 — 0/3, В4 — 0/1) |

| 2026-08-02 | Развилка границы «команда ↔ действие»: **валидация 3** (3 позиции) | концепт | solution-designer | **0 совпадений из 3 ⇒ счёт остаётся 0, ступень `Предложение`.** **(поз. 1)** `KILL_SWITCH_ACTION` — крен «строка на каждую сделку каскада (реализация принятого A)», принято **не материализовать действием** (вариант (d) той же проработки): В2.3 отменено, системных действий четыре, развязки двойного учёта / политики per action type / терминального контракта / переписывания В1.4 закрылись вместе со строкой. Диагноз: проработка **сама вскрыла** оба факта, подрывающих основание В2.3 (звеньев-команд нет; наблюдаемость закрыта `AnomalyReport`), **сама** назвала (d) ближайшей альтернативой — и всё равно поставила креном (a) формулой «как реализация принятого решения»; ратифицированное решение сработало ограничением набора после того, как его основание было подорвано. Правка — `design-fork.md` эвристика 3 (распространена на решения пользователя; формула «как реализация принятого» названа антипаттерном). **(поз. 2)** факт консолидации входа — крен «статус строки действия», принят **`Deal.status = ENTRY_FINALIZED`**. Диагноз: вариант (c) был отброшен верным принципом («действие не опирается на признак, появляющийся позже»), но **ущерба** нарушение не влекло — при атомарной команде и безопасном повторе цена оказалась лишним проходом петли; правка — `design-fork.md` шаг 3 «Крен»: принцип проверяется на ущерб, нет ущерба — вариант остаётся в наборе как цена-суждение. **(поз. 3)** `StrategyActionType` — крен «не трогать», принято **переименовать**; **не дефект агента**: вопрос вынесен решением, цена (persisted-значения + внешний контракт) названа честно, расхождение — от непроверенного входа (наличие потребителей API). Вход проверен на этой валидации: потребителей в репозитории нет ⇒ план совместимости не нужен |

| 2026-08-02 | Развилка границы «команда ↔ действие»: **валидация 2** (4 позиции проработки) | концепт | solution-designer | **Совпало 1 из 4 ⇒ счёт остаётся 0, ступень `Предложение`.** Совпало: вывод стадии из подтверждённых фактов (поз. 3) — принят с довесками (флага «рестарт с нуля» нет, курсора нет, транзакционная клауза, определение подтверждённого факта по каждому действию). Расхождения: **(поз. 1)** объём строк — крен был «строка-слот», принято «копить» (`COMPLETED` жёстко терминален, политика очистки отложена к фазе 3); диагноз — **цена варианта подана как единая величина «здесь и сейчас»**, откладываемая за границу фазы операционная нагрузка сработала отбрасывателем; правка — `.claude/skills/design-fork.md` эвристика 6. **(поз. 2, самое содержательное)** именование — крен `SETTLE_DEAL_*` стоял на принципе «имя действия ≠ имя команды», принцип **снят**: уровни разводит систематический суффикс (`_COMMAND`/`_ACTION`). Диагноз — ограничение выведено из одного принятого имени и дальше не подвергалось сомнению: все три предложенных варианта отличались **основой**, ось «систематический маркер уровня» в пространство вариантов не попала; правка — `.claude/rules/naming.md` §«Разведение уровней абстракции» (способ разведения — самостоятельная развилка **до** подбора имён). **(поз. 4)** дефект ключа шага 4 — крен «вести самостоятельным пунктом backlog», принято «чинить этой же правкой»; **не дефект агента**: страховка на случай отсрочки пересмотра была разумной при его информации, в диагностику не идёт. Сверх позиций пользователь ввёл **`KILL_SWITCH_ACTION`** пятым системным действием — решение поверх В1.4 и поверх довода об отказе анкера cleanup-командам; породило пакет развязок (радиус vs deal-scoped носитель, тройной счёт, терминальный контракт, ключ политики лимита) → на валидацию 3 |

| 2026-08-02 | Развилка границы «команда ↔ действие» (узлы 1/3/4/6 `DOCS_CHECK_8`): проработка направления, принятого пользователем | концепт | solution-designer | **Существенная правка ⇒ счёт остаётся 0.** Принято: прочтение «последовательность как действие» (крен владельца), топология V2 (крен), исход узла 4 вариантом (а), имя `REFRESH_DEAL_CONTEXT`, реакция на исчерпание — действующее правило (довод пользователя — этап отладки, из доков не выводится). Отклонён крен по счётчику ((α) обнуление при продвижении стадии → принят (β) сквозной бюджет). **Существенное — снятая посылка:** проработка исходила из того, что действие стратегии исполняется в сделке однократно, и приписывала проблему тождества экземпляра только системным действиям; пользователь снял (грид — N исполнений одного узла, **параллельных**), §3.3 переработан целиком. **Дефектом сборки не является:** посылка выводилась из носителей (`UNIQUE(deal_id, strategy_action_id)`, единственный резолв `findByDealIdAndStrategyActionId`), которые её прямо кодируют. Побочный результат прогона: те же носители **дефектны уже сегодня** (второй ноге грида некуда лечь, счётчик общий) — дефект шага 4, живущий независимо от развилки. Владельца по этой позиции **не дообучаем** (вход пользовательский); переносимое — эвристика «ключ, работающий на однократности, проверять на повторяемости и параллельности», кандидат в `design-fork.md` |

| 2026-08-01 | `DOCS_CHECK_8` шага 7: подтверждающий прогон после `GAPS_CLOSE_7` (три независимые линзы) | концепт + трейдинг | solution-designer / trading-specialist (линзы `concept-review` ×2 + `trading-review`) | **Не прогон делегирования — прогон ревью; счёт категорий не двигается.** Записан ради двух измерений. **(1) Цена самопроверки измерена.** Предшествующая одноли́нзовая попытка того же под-шага, выполненная CC (автором `GAPS_CLOSE_7`), из пяти гейтящих узлов независимых линз нашла три, **два пропустила целиком** (неисполнимый retry-анкер добывающих команд `F-A3`; мёртвый анкер журнальной аномалии `F-B5`) и один нашла в одиночку (уничтожение анкера реактивного холда). Линза B к тому же **отвергла** одну её находку доводом из `persistence/README.md`. Вывод для процесса: самопроверка автора закрытия даёт ~60% гейтящих узлов и производит ложные — гейт `CODE` на ней стоять не может, что и записано в `roadmap-step-execution.md` §Гейты. **(2) Класс дефекта закрытия измерен.** 7 из 11 гейтящих находок — один класс: носитель/механизм назначен, а операнд / канал / анкер под ним отсутствует или уничтожается сопутствующей правкой; два рецидива **дословные** (H4 повторяет H4 `DOCS_CHECK_7`, H14 — H5 `GAPS_CLOSE_4`). Дефектом сборки эскалации это не является — варианты собраны по всем линзам узлов; дефицит на стороне **закрытия**, не ревью. Кандидат в правку пайплайна вынесен на решение пользователя, не материализован. Дисклозура: линзе C утекла одна строка из `progress/*` через поиск по репозиторию — усилила `F-C2`, не породила |
