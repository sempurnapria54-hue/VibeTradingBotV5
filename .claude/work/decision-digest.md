# Дайджест решений CC

## На какой вопрос отвечает этот файл

Какие проектные решения CC принял автономно в текущей итерации
(на чтение пользователю пакетом, с правом вето постфактум).

## Как читать

Режим автономии до конца фазы 1: все проектные развилки CC решает
сам и фиксирует здесь — решение одной строкой, альтернативы одной
строкой, почему так. Пользователю выносится только дефицит
`подтверждение` (цели, приоритеты, продуктовый scope и намерение,
численный риск-аппетит, сроки). Вето по любому пункту возвращается
CC задачей. После чтения пользователем разобранные пункты
переезжают в `history/` (пакетом, с датой).

---

## Итерация 2026-09-01 (8) — кодовый заход 5, продолжение: замер среды и разбор гейта

Решения CC, принятые автономно; альтернативы и довод — по строке каждое.
Вето постфактум. Компонентов заход не добавил: оба оставшихся гейтятся
грунтом, и грунт неисполним по среде — разбор ниже и в `ENV-Q1`.

| # | Решение | Альтернативы | Почему так |
|---|---|---|---|
| Д315 | **Диагноз отказа контура переписан: не «совпадение заявок не идёт», а «заявка не достигает терминала ни одной тропой»** | оставить прежнюю формулировку, добавив к ней наблюдений | прежняя мерила **у́же** предмета и потому предлагала ложные обходы (сменить тип заявки, инструмент, тропу отмены). Различающая ось — покоящаяся лимитная заявка вне рынка: совпадения не требует, отмену поддерживает однозначно. Замер: отмена принята (`code=0`), состояние не изменилось, повторная → `51410` «already in canceling status **or pending settlement**» — то есть встал **расчёт**, а совпадение лишь один из его переходов |
| Д316 | **Довод «стакан глубокий, публичные сделки идут» снят как неразличающий** | оставить как контекст | demo зеркалит рыночные данные с боевого, а совпадение симулирует отдельно: живая лента о состоянии симулятора не высказывается. Довод создавал ложную уверенность, что дело в нашей стороне |
| Д317 | **`51403` на батчевой отмене `market`/`ioc` снят из признаков поломки** | оставить в перечне осей | это **штатный** отказ параметра: эти типы батчевой отмены не принимают. Значащий ответ даёт лимитная заявка, и он — `51410`. Прежний перечень смешивал штатный отказ с дефектом |
| Д318 | **Гейт грунта соблюдён (повторно, уже своей пробой): компоненты не кодировались** | применить схему заменителей `unorderable-fact-substitutes.md` к обоим слотам | её §Область **прямо** выводит из-под себя предусловие, событие которого заказуемо нами, и называет пересечение funding-интервала поимённо — то есть ровно п. 7. Отказ среды делает событие незаказуемым **фактически**, оставаясь заказуемым по существу; ADL незаказуем **структурно и бессрочно**, и на этой разнице стои́т всё решение |
| Д319 | **Срок ожидания среды вынесен держателю (`ENV-Q1`), численного порога CC не предлагает** | назначить срок автономно; ждать бессрочно молча | основание решения о заменителях («гейт мерит везение») наступает **по истечении срока**, а срок — численный порог, и пороги за держателем. Молчаливое бессрочное ожидание — та же бессрочность, только незаписанная |
| Д320 | **Класс дефицита `slTriggerPxType` исправлен `подтверждение` → `грунт`** (поправка держателя) | оставить как было | разница операционная: `подтверждение` в режиме автономии **выносится** держателю, а поле требует не санкции, а наблюдения, и слот под него заведён. Блокеров у наблюдения два, оба названы: Т5 (защита не уходит на биржу — эха нет по построению) и отказ среды |
| Д321 | **«210 ошибок контекста» разобраны как свойство КОМАНДЫ, а не среды** | чинить как средовой дефицит; занести в долг шага 9 | в `pom.xml` нет конфигурации surefire, поэтому голый `mvn -o test` тянет 211 живых кейсов, чей контекст требует `VAULT_TOKEN`. Обе половины проверены прогоном: `-DexcludedGroups=source-api-live` → 85/0; живой кейс с сорснутым токеном → 2/0. К шагу 9 отношения не имеет |
| Д322 | **Правка `excludedGroups` в `pom.xml` НЕ сделана этим заходом — заведена пунктом** | сделать сразу (одна строка) | меняет умолчание сборки для всех потребителей (IDEA, будущий CI), а состав захода — доменные компоненты. Информация передана полностью: и дефект, и точный состав правки, и обе проверяющие команды |
| Д323 | **Кейс `AG1.7` прогнан ВНЕ окна намеренно; в окно при нынешней среде не запускается** | прогнать в окно (19:10-19:54 UTC); не прогонять вовсе | вне окна кейс упирается в `assumeTrue` **до** первой заявки и счёта не трогает — и при этом пишет исход персистентно, чем предъявляет ветвь пропуска (файла наблюдений у кейса не существовало вовсе, клейм реестра держался на чтении кода). В окно он прошёл бы assumption и пошёл открывать позицию, то есть гарантированно оставил бы ещё один неснимаемый хвост, не имея шанса закрыть слот |
| Д324 | **Драйв-буквенная форма `MAVEN_HOME` исправлена на POSIX** — `.claude/tests/source-api/okx/code-preconditions.md` §«Воспроизводимые команды» | оставить, добавив оговорку | команда как записана **не исполнялась**: в Git Bash разделитель `PATH` — двоеточие, и «C:» отрезается отдельным элементом (`mvn: command not found`). Дефект не был замечен потому, что для `-v` и прямого вызова `"$MAVEN_HOME/bin/mvn"` форма годится |
| Д325 | **Стоячие клеймы среды сведены к дому и поправлены по факту**: «токен протух» (в двух носителях), «ответная половина п. 16 открыта», «слот закрывается запуском в окно» | править по мере встречи | все три противоречили **собственным** соседним строкам своих файлов: реестр объявлял п. 16 закрытым и тут же открытым, шапка команд — токен протухшим при живом токене ниже. Класс — не устаревание, а несведённость правки с носителем тем же ходом |

---

## Итерация 2026-09-01 (7) — кодовый заход 5 шага 7: движения счёта и P&L

Решения CC, принятые автономно; альтернативы и довод — по строке каждое.
Вето постфактум.

| # | Решение | Альтернативы | Почему так |
|---|---|---|---|
| Д298 | **Поле сделки переименовано под свою роль**: `billsWindowEnd` → `coverageProvenThrough` (колонка `coverage_proven_through`), «порог доказанного покрытия» вместо «верхней границы окна линковки» | поправить только прозу, имя оставить; снять поле как беспотребительское | механизм дефекта — само ИМЯ: реализация по нему поставила бы поле в предикат линковки, а у живой сделки оно пусто, и не прилинковалось бы **ни одно** движение. Снять поле нельзя — потребитель есть и назван (`duty.coverageProven` обязанности сверки). Переименование стои́т ноль: в коде поля не было |
| Д299 | **Операнд обязанности сверки переименован вместе с полем** — `duty.windowClosed` → `duty.coverageProven`; 24 вхождения в спеке | оставить имя операнда прежним | имя операнда несло ту же ложную редакцию («окно закрыто»), и расхождение имени с полем возвращало бы её каждым чтением |
| Д300 | **Клейм однородности покрытий сверки стал УСЛОВНЫМ**: два условия названы поимённо, названа цена нарушения второго | оставить безусловным до замера; снять клейм целиком | замер контура сегодня неисполним (средовой блокер), а безусловный клейм с неподтверждённой посылкой — ровно находка. Снятие клейма убрало бы и верную его половину: первое условие держится конструкцией, не наблюдением |
| Д301 | **Дом адресуемой единицы эпизода ссылается на дом окна, а не пересказывает его** | переписать в `Position.md` резолв границы целиком | стыковая истина живёт один раз (`carrier-levels`): копия в доке деталей и породила противоречие дома самому себе через восемь строк |
| Д302 | **Дом единицы эпизода добавлен в популяцию записи снятой редакции со своим шаблоном пришедшей** | расширить общий шаблон записи | носитель выражал снятое ДРУГИМИ словами («операнда границы нет вовсе»), и свип по форме его не видел; общий шаблон пришлось бы растянуть до ложных срабатываний |
| Д303 | **Ось 3 проверки предусловий читает клейм ПОСТРОЧНО (по слоту), а не по кейсу**; строка, объявившая код-тест и не назвавшая его разбираемой формой, — дефект | оставить слияние строк одного кейса | слияние делало ложный клейм строки неизмеримым: он проходил зелёным на соседней строке того же кейса |
| Д304 | **Отображение движений OKX в категории построено по справочнику**: типы `2`, `8`, `9` — строкой ТИПА, тип `5` — парами | размечать всё парами; размечать всё типами | у типа принудительного закрытия две природы в одном номере (закрывающие исполнения и заёмная механика), и типовая строка затянула бы вторую в сверку; поголовная разметка парами лишила бы неизвестный подтип экономического типа громкого сигнала |
| Д305 | **Список исключений сверки несёт только пары внутри экономических типов**; прочие 28 верхних типов отображения не имеют вовсе | перечислить исключениями все 28 типов | тип без отображения садится в принимающую корзину и из области уходит сам; вычитать списком то, что в область не попало, — второй носитель того же факта |
| Д306 | **У OKX нет пар, отображаемых в `TRADE_FEE`/`REBATE`**: комиссия свопа приходит полем внутри торгового движения, гранулярность слитная | завести отображение «на вырост» | отображение без движения-носителя — угадывание; значения перечня при этом остаются, они принадлежат модели, а не источнику |
| Д307 | **Инструмент фикстуры кейса `AG1.7` заменён на `AVAX-USDT-SWAP`**; торгуемость пары аккаунтом проверена пробой, НЕ создающей заявки | оставить прежний инструмент; проверять торгуемость постановкой заявки | прежний отвечает `51155` («local compliance restrictions»), то есть кейс не собрал бы фикстуру и в идеальном окне; проба постановкой в нынешней среде оставляла бы неснимаемый хвост |
| Д308 | **Замер порядка меток источника (A4а) встроен в существующий кейс `AG1.5`**, а не заведён отдельным гейтящим кейсом плана | завести новый кейс плана | A4а — позиция реестра гейтов, а не слот предусловия: новый гейтящий кейс потребовал бы правки множества пар «пункт × кейс» в плане и реестре и завёл бы гейт там, где его нет |
| Д309 | **`Deal.position` → `Deal.positions`**, живой эпизод резолвится предикатом модели | оставить одну позицию и завести отдельную таблицу истории эпизодов | вторая таблица — второй носитель одной сущности; предикат на коллекции выражает инвариант «живой не более одного» там же, где он объявлен |
| Д310 | **Ключ `uk_position_deal` заменён частичным тройным** `(deal_id, external_id, external_created_at)`; миграция `V13` вводит целевую форму одним ходом | вводить колонки nullable с последующим ужесточением | до конца фазы 1 таблицы пусты, бэкфилла нет (`pre-launch-schema-changes`); прежний ключ отвергал бы легитимный второй эпизод под переиспользованным идентификатором |
| Д311 | **`strategy_detail_id` стал nullable, а биекция «пусто ⟺ `RECOVERY`» закреплена CHECK-ограничением** | оставить `NOT NULL` и завести деталь-заглушку; заменить ограничение соглашением | заглушка приписала бы восстановленной сделке чужой выбор входа; соглашение не держит — на биекции стои́т ветвление читателей |
| Д312 | **Восстановительная тропа создания сделки закодирована, хотя вызывающего у неё пока нет** (детектор аномалии — шаг 8) | отложить тропу до шага 8 | без неё у значения `RECOVERY` нет писателя вовсе, и на этом рушится вся конструкция суррогатной границы: признак «позиция наблюдалась», допущение пустой детали и ветвь материализации эпизода становятся недостижимыми |
| Д313 | **Нога 2 не завершает звено при недобытой записи закрытия** — возвращает успех исполнения, но статус действия не проставляет | считать пустой ответ основанием терминала | статус позиции уже определён ногой 1; недобытый факт повторяется бюджетом действия, а терминал по молчанию источника — благоприятное умолчание |
| Д314 | **Гейт грунта соблюдён как есть: `RefreshBillsExecutor` и `DealCashFlow` не кодировались** | перепривязать слоты (объявить, что они гейтят конфигурацию, а не компонент) и кодировать | перепривязка сделана бы тем, кого гейт ограничивает; проба на копии реестра подтверждает, что при начатых компонентах критерий не проходит, и причина называется поимённо |

---

## Итерация 2026-09-01 (6) — кодовый заход 4 шага 7: встроенная защита

Решения CC, принятые автономно; альтернативы и довод — по строке каждое.
Вето постфактум.

| # | Решение | Альтернативы | Почему так |
|---|---|---|---|
| Д291 | **Три тропы потери покрытия разведены ЗНАЧЕНИЕМ**: `PROTECTION_PLACEMENT_FAILED` / `PROTECTION_LOST` / `PROTECTION_TRIGGER_FAILED` | оставить одно значение и различать тропу по персистентному `failCode`; оставить как было | `failCode` разделяет только «есть код» и «нет кода» — отказ постановки и `order_failed` несут его оба, то есть тропа 1 от тропы 3 им не отличима. Реакция у трёх одна, но она отвечает на «что делать», а причина закрытия — на «что произошло»; ход тот же, которым разведены `SIZE_MIN_LOT_EXCEEDS_RISK_BUDGET` и `RISK_PER_ACTION_EXCEEDED` |
| Д292 | **`failCode` персистится, `failReason` — нет** (лог) | персистить обе; не персистить ни одной | машинный операнд разбора один; вторая колонка удваивала бы его строкой свободной формы. Не персистить ни одной означало бы оставить операнд решения в снапшоте прохода — ровно предмет находки |
| Д293 | **Сверка объявленной базы `MARK` идёт только по НЕПУСТОМУ эху**; расхождение — нарушение биржевого инварианта на РОДИТЕЛЬСКОЙ заявке | читать пустое эхо нарушением; не сверять вовсе | пустое эхо — недобытый факт: нарушением оно уводило бы штатную сделку в аварийный контур по молчанию источника. Не сверять — оставить объявленную обязательность базы без единого энфорсера на стороне факта. Заполненность эха фактически не наблюдалась: дефицит `подтверждение` вынесен слотом |
| Д294 | **Снятие встроенной защиты получило СВОЮ команду** `CANCEL_ATTACHED_PROTECTION`, а не адресата в `CANCEL_ALGO_ORDER` | обобщить существующую команду адресатом (`TargetEntityType`); завести словарь намерений, из которого каждая цель резолвит свою причину | словари причин у двух целей **не пересекаются**, поэтому одна команда причину не типизирует; словарь намерений требует матрицы «намерение × адресат» с недостижимыми клетками, своего дома и энфорсера — след **больше**, чем у второй команды. Бремя бо́льшего следа снято именно так, а не проигнорировано. Биржевой эндпоинт при этом общий и не удваивается |
| Д295 | **Порядок ног входного `REPLACE` по филлу больше НЕ ветвится**: cancel-old → подтверждение → place-new при любом наливе | оставить две строки и переписать только довод третьей; оставить как было | довод третьей строки был её единственной опорой и опровергнут наблюдением: при непустом наливе защита уходит в самостоятельную живую заявку ровно на налитый объём, то есть налитый объём покрыт СВОЕЙ защитой на всём переходе. Опережающая постановка новой ноги при этом ничего не покрывает, а окно двойного входного риска открывает. Строка без опоры — дефект носителя, а не консервативный остаток |
| Д296 | **Резолвер встроенной защиты переехал из per-биржевого пакета в домен**, per-биржевая реализация снята | оставить интерфейс + OKX-реализацию как у трёх соседних резолверов | соседи маппят СЫРОЙ статус источника — у них биржевой словарь и есть предмет; у встроенной защиты статуса нет вовсе, решение стои́т на нормализованных фактах, и OKX-реализация не несла ни одного биржевого факта. Доменная истина в интеграционном слое — нарушение раскладки слоёв (`codestyle` §Слои) |
| Д297 | **Исход резолвера — свой тип** `AttachedProtectionResolution` с явным флагом «исход не определён» | переиспользовать общий `StatusResolveResult` и кодировать неопределённость пустым статусом | обязанность поднять сигнал должна быть видна на call-site, а не выводиться из пустого поля; у трёх соседних резолверов третьего состояния нет вовсе, и общий тип его не выражает |

---

## Итерация 2026-09-01 (5) — кодовый заход 3 шага 7: риск и уровни

Решения CC, принятые автономно; альтернативы и довод — по строке каждое.
Вето постфактум.

| # | Решение | Альтернативы | Почему так |
|---|---|---|---|
| Д267 | **Бессрочность отказа — свойство КОДА**, объявленное рядом со значением (поле перечня, конструктор не даёт умолчать) | держать перечень бессрочных в карте реакции; вычислять признак операндом | второй носитель разошёлся бы с первым первой же правкой (`carrier-levels`); конструктор заставляет НОВЫЙ код объявить признак, то есть правило не стареет молча |
| Д268 | **Свёртка вердикта: род — стадия, причина — старший код, бессрочность — конъюнкция** | строгая свёртка (любой код вне карв-аута ⇒ `ERROR`); мягкая (любой в карв-ауте ⇒ не `ERROR`) | обе однородные ошибочны в названную сторону: строгая рвёт сделку без живого риска, мягкая маскирует рассогласование учёта. Стадия от состава перечня не зависит вовсе |
| Д269 | **Временный отказ до живого риска даёт `SKIP_ACTION`, а не терминал** | оставить безусловный терминал по стадии | уровень сетки, отвергнутый занятым бюджетом, при терминале теряется навсегда: бюджет освободится выходом соседа, а транша уже нет. Значение `SKIP_ACTION` в перечне было и не имело ни одного производителя |
| Д270 | **`SIZE_MIN_LOT_EXCEEDS_RISK_BUDGET` — отдельное значение**, а не операнд «размер поднят полом» | вернуть код в карв-аут целиком; ввести операнд в условии карв-аута | различение «лот неделим» / «расчёт разошёлся» видно **в данных**, а не вычисляется (П3); возврат целиком стёр бы сигнал о настоящем расхождении |
| Д271 | **Якорь — один на все уровни, перечень потребителей закрыт таблицей** | распространить якорь прозой; развести якоря защитных и целевых уровней | подразумевание и дало расхождение; разные якоря у величин, которые сравниваются между собой на одном проходе, делают полосу невыразимой |
| Д272 | **`levelSource` резолвится по объявленному блоку**, поле в дереве стратегии не заводится; неоднозначность отвергается созданием | поле в дереве (колонка + api + домен + миграция); приоритет блоков вместо реджекта | значение уже выводимо из блока (`design-simplicity`); приоритет отвечал бы за автора там, где он сам себе противоречит |
| Д273 | **Область стоящей ступени — инструмент, биржевой радиус исключён явно** | включить биржевой радиус; оставить два имени с разной областью | под мягкой биржевой ступенью живые сделки сопровождаются полностью, и заморозка повтора отменяла бы то, что ступень обязана сохранять |
| Д274 | **`Instrument.ENTRY_BLOCKED` заведён в перечне статусов** вместе с предикатом стоящей ступени | оставить чтение по одному `TRADE_BLOCKED` | мягкая ступень — половина области операнда, и без значения она была невыразима; лестница объявляет обе ступени давно, отставал перечень |
| Д275 | **Слой опровержения, внесённый мной в продуктовые доки, снят тем же заходом** | оставить (детектор поймал только одну цитату из четырёх) | `docs/concept.md` запрещает слой опровержения в корпусе; провенанс живёт в решениях и дайджесте. Три пассажа переписаны механизмом дефекта, без цитаты снятой редакции |

## Итерация 2026-09-01 (4) — РИСК-заход шага 7: C1, C6, F2, F3

Решения CC, принятые автономно; альтернативы и довод — по строке каждое.
Вето постфактум.

| # | Решение | Альтернативы | Почему так |
|---|---|---|---|
| Д262 | **`positionLive` оставлен в спеке как выразительное средство**, хотя предикат его больше не читает | удалить операнд вместе с заменой | без него состояние «живую позицию держит СОСЕД» невыразимо примером, и проба отката возвращалась зелёной — то есть доказательство держалось бы на состоянии, которого пример не предъявляет. Первая проба это и показала |
| Д263 | **Пустой разбор истории не ставит терминала вовсе** и поднимает сигнал; отдельная причина `PROTECTION_TRACE_NOT_FOUND` НЕ заводится | завести отдельную причину (вариант 3 находки); оставить с расширенным ограничением | новая причина снимает ложное имя, но не благоприятность: «не нашли» по-прежнему читалось бы как «снята». Предмет находки — исход, а не его имя |
| Д264 | **Артефакт второго шага назван одним домом на оба слота** — `docs/integrations/okx/rules/cash-flow-categories.md` (таблица отображения + секция типов вне экономики) | по конфигу на слот; оставить «конфиг исключений» без имени | без имени «зафиксировано» непроверяемо; два дома для одного справочника разошлись бы первой же правкой (`carrier-levels`) |
| Д265 | **Пятая ось `preconditions-check.sh` объявлена в процедуре гейта, но не реализована этим заходом** | реализовать сейчас; не упоминать | заход РИСК-овый, и его предмет — четыре позиции; ось названа с адресатом, чтобы обещание не потерялось. Названо как ограничение, а не как сделанное |
| Д266 | **Зависшую заявку демо-контура снял вручную**, факт записан в дом предусловий как свойство контура | оставить как есть; молча удалить упоминание | уборка кейса стои́т ниже упавшего ожидания и не отработала; хвост на счёте оставлять нельзя, а сам факт («рыночная заявка на демо может не исполниться и не сняться») — грунт, который обязан знать всякий кейс с фикстурой по рынку |

## Итерация 2026-09-01 (3) — кодовый заход 2 шага 7: оркестрация шагов стратегии

Решения CC, принятые автономно; альтернативы и довод — по строке каждое.
Вето постфактум.

| # | Решение | Альтернативы | Почему так |
|---|---|---|---|
| Д257 | **Охрана пустоты в фильтре `allocationOutOfRange` ОСТАВЛЕНА**, хотя крен находки предписывал снять её вместе с `coalesce` | снять `notNull` буквально по крену | строгое чтение там давало бы отказ вычисления на состоянии, у которого уже есть свой реджект (`allocationNotDeclared`) — ровно класс A5, только этажом ниже. Предмет проверки диапазона — диапазон, а не наличие; отклонение от буквы крена названо здесь |
| Д258 | **Оси популяций объявляются ТАБЛИЦЕЙ ДОМА**, и команда вывода читает её | оставить вывод из формулы спеки; захардкодить оси в скрипте | популяция, чей `derive.from` — сама проверяемая спека, выводит перечень из проверяемого текста (B9); таблица в доме делает дом источником, а расхождение формулы с осями — видимым сверкой перечней |
| Д259 | **`branchRiskBearing` / `branchCovered` — разведение операндов по уровню объекта шага**, а не отдельная величина для агрегатной поверхности | отдельная агрегатная величина ветви; умолчание «незащищённая» у шага сделки | вторая величина — второй носитель одной истины (`carrier-levels`); умолчание в любую сторону не бесплатно (`GRACEFUL_CLOSE` на покрытой сделке закрывает то, что под контролем) |
| Д260 | **Обязательности переехали из аннотаций в валидатор** там, где они условны: пустой пакет (по типу шага), доля аллокации (по `orderType`), дистанция стопа (по способу расчёта) | оставить аннотации и подстроить эталон под них | аннотация выражает БЕЗУСЛОВНУЮ обязательность, а все три условны — и в двух случаях аннотация запрещала то, что корпус объявляет законным (форма выхода «только условие»; `BREAKEVEN` без доли) |
| Д261 | **Операнд стоящей ступени взят ближайшим существующим** (блокировка торговли по инструменту) с названным ограничением в коде | подставить `false` до захода лестниц; завести носитель ступени сейчас | `false` выключал бы гейт повтора целиком — петля «`FAILED` → новая строка» ожила бы; свой носитель — предмет захода лестниц и холдов. Признак шире нужного, но ошибка ЗАПРЕЩАЮЩАЯ: лишний повтор к бирже не уходит |

## Итерация 2026-09-01 (2) — модельный заход шага 7: разделение сделки и транша

Решения CC, принятые автономно; альтернативы и довод — по строке каждое.
Вето постфактум.

| # | Решение | Альтернативы | Почему так |
|---|---|---|---|
| Д250 | **Шаги стратегии ключуются статусом ТРАНША** (`stepsByStatus`), колонка `deal_status` → `tranche_status` | оставить ключ сделочным и резолвить уровень через тип шага; ключ-объединение «статус транша ∪ маркер уровня сделки» | эталонный артефакт репозитория УЖЕ объявляет ключи `PRECHECK`/`ENTRY_FINALIZED`/`MANAGING` — модель отставала от него, а не наоборот; агрегатную поверхность несёт тип шага (`EXIT`/`FAIL_SAFE`), для неё второй ключ не нужен |
| Д251 | **Заявка принадлежит траншу** (`deal_tranche_id`), `deal_id` остаётся агрегатным представлением; раскладка заявок по траншам — **в памяти**, одним запросом на сделку | запрос заявок на каждый транш; перенести `deal_id` с заявки вовсе | teardown уровня сделки и сверка экспозиции читают заявки сделки целиком — снятие `deal_id` заставило бы их собирать объединение по траншам; запрос на транш давал бы N+1 на сетке, а число траншей задаёт стратегия |
| Д252 | **Транш не пишет статус сделки — он его просит** (`escalateDealToError`); отказ СТРАТЕГИЙНОЙ строки в `ERROR` не ведёт | оставить траншу право звать `markError`; общий тип перехода с полями обоих уровней | право писать статус сделки у транша означало бы, что уровень разделён только на бумаге; общий тип позволял бы вернуть статус транша там, где применяется переход сделки. Исполнение закрытого B5 |
| Д253 | **Идемпотентность — ДВА частичных ключа**: потраншевый и уровня сделки, второй с условием `deal_tranche_id is null` | один индекс по трём колонкам | `NULL` в уникальном индексе Postgres сам с собой не конфликтует — индекс по трём колонкам пропустил бы два живых исполнения одного агрегатного действия. Форма названа закрытием B3 и здесь исполнена |
| Д254 | **`positionReopenAllowed` заведён в модели стратегии**; пустое читается как «не допускает» | подставить `false` в вызове охраны; подставить `true` | эталон настройку объявляет, модель её не несла — операнд охраны переоткрытия брался бы из воздуха. Пустое = запрет: разрешение риска объявляется явно, умолчание в разрешающую сторону запрещено (П1) |
| Д255 | **`RetryBudgetExhaustedException` заведён как контракт ЛОВЛИ**; бросающая сторона — `ServiceCommandExecutor`, заход исполнительного слоя | ловить по исходу команды вместо броска; отложить развязку целиком до захода исполнителя | предмет закрытого B5 — именно ловля и её развязка по классу броска; исход команды класса броска не несёт. Ограничение названо: пока тип не бросается, ветвь не достижима |
| Д256 | **Полнота графа прохода — названное ограничение**: сейчас читается как «контекст собран», доводится вместе с добычей положений закрытия | считать граф полным безусловно; завести признак сейчас | безусловная полнота — разрешающая ошибка на гейте терминала; собственный признак полноты требует добычи, которой заход движений счёта ещё не сделал |

---

## Итерация 2026-09-01 (1) — закрытие `GAPS_CLOSE_33`: адресуемая единица эпизода и признаки отбора

Решения CC, принятые автономно; альтернативы и довод — по строке каждое.
Вето постфактум.

| # | Решение | Альтернативы | Почему так |
|---|---|---|---|
| Д276 | Адресуемая единица эпизода — пара `(externalId, externalCreatedAt)`; ключ пересобран тройкой | новое поле-дискриминатор; снять ключ вовсе, оставив `uk_position_deal_live` | второй операнд уже существует (аудит) и **проверен наблюдением** на обеих сторонах; снятие ключа оставило бы легитимный дубль без охраны |
| Д277 | Признаки отбора и счётчик серии стоя́т на **непустоте записанного числа** / `resultProfitFinalized`, а не на вычислимости итога на проходе | завести отдельный durable-признак «четвёрка записана» | признаки пишет финализация **той же транзакцией**, что и число, поэтому второй операнд выражал бы то же самое |
| Д278 | FSM транша прогоняют два обработчика из трёх; ответ записан в доке слоёв исполнения | записать в компонент-доке машины сделки | стыковая истина, дом — не док деталей (`carrier-levels`) |
| Д279 | Энфорсер запрета входа в окне сворачивания стои́т на **цели** перехода (`ENTRY_SUBMITTED`), а не на паре рёбер | гейт по каждой паре | в цель приходят два ребра; гейт по паре пришлось бы дублировать, и он разошёлся бы |
| Д280 | Идемпотентность отчёта отделена от анкера реакции; дедуп `STATE` спрашивает **стоящее состояние объекта** | оставить общий анкер; ключ по коду для реакции тоже | журнальная тропа завершает отчёт сразу, поэтому дедуп по статусу отчёта не срабатывал **никогда** |
| Д281 | Делитель потолков — снимок базы, а при его отсутствии живая база (`base` стала величиной) | писать снимок до преконтроля; объявить первое действие исключением | снимок пишет `CreateOrderExecutor` после преконтроля; перенос писателя менял бы момент заморозки |
| Д282 | Триггер «нарушение риск-политики при живой защите» гоняет **те же** неравенства при нулевом акте | завести собственные величины детектора | собственные величины были бы копией формы под другим именем — ось C детектора |
| Д283 | Фаза рынка кладётся в контекст прохода; темпоральный операнд `TREND_CHANGED` — `Deal.entryMarketPhase` (write-once) | персистировать историю фазы; снять `TREND_CHANGED` и переписать эталон | истории у фазы нет по построению; «смена тренда» как условие выхода и означает уход от фазы входа |
| Д284 | Уровень на всю позицию **отказывает вычислением** при голом транше | клэмп; брать худший из известных | подстановка чужого уровня — та же разрешающая ошибка этажом выше; цена отказа названа (пост-действенность снимает дедлок) |
| Д285 | Полнота покрытия не выводит защитную заявку из-под преконтроля | оставить оговорку | полнота — свойство размера, потолок считается по уровню |
| Д286 | `UNKNOWN_EXTERNAL_STATUS` снят из `Position.CloseReason` | завести производителя | отказ резолва у позиции недостижим по построению; снятие — требование дома резолва |
| Д287 | Конвенция имён DTO источника сведена к **суффиксной** | принять префиксную, поправив `codestyle` | суффикс сохраняет систематичность всей таблицы слоёв; префикс дублирует пакет |
| Д288 | Долг адресов `docs/**` сведён механически (194 → 62) в этом же заходе, а не отдельной задачей | оставить курационному заходу | правка безопасна (путь остаётся), а мои собственные 70 адресов иначе ушли бы в долг |
| Д289 | Заведены два новых энфорсера: разрешимость §-адресов и «указатель ведёт в дом величины» | завести задачи в бэклог | оба класса воспроизводятся при каждом переносе/переименовании; без энфорсера их находит только следующий прогон |
| Д290 | `DOCS_CHECK_30` помечен несравнимым и в детектор стагнации не подаётся | подать число как обычное | прогон мерил другую конструкцию (без семи линз); подстановка дала бы ложное падение стоимости по C3 |


## Итерация 2026-08-26: материализация пакета решений держателя

### Д1. Маршрутизация четырёх концептуальных правил

- **Решение:** п.1 (иерархия исполнения) → `docs/rules/execution-hierarchy.md`
  (новый); п.2 (неожиданное поведение биржи) → принцип в
  `docs/rules/error-handling-policy.md` (уровень 4), механика состояний —
  `docs/rules/exchange-hold.md`; п.3 (простота и переиспользование) →
  `.claude/rules/design-simplicity.md` (новый); п.4 (живой риск без
  защиты) → инвариант в `docs/rules/live-risk-protection.md`
  (обобщён), реакция — ступень 2 лестницы в `exchange-hold.md`.
- **Альтернативы:** один общий файл «четыре правила держателя»
  (отвергнуто: типы знания разные — иерархия и инвариант это
  `docs/rules/`, дефолт проектирования — правило пайплайна); отдельные
  новые файлы для п.2/п.4 (отвергнуто: у обоих уже есть дома-правила,
  новый файл дублировал бы — против п.3 и решения 3).
- **Почему:** тип знания определяется вопросом (`structure.md`);
  усиление существующих домов вместо новых носителей — сам п.3.

### Д2. Лестница серьёзности exchange-scope: `HOLD` ↔ `TRADE_BLOCKED`

- **Решение:** двухступенчатая лестница биржевых safety-состояний,
  зеркалящая пару инструмента: `Exchange.HOLD` — заморозка (блок
  `SUBMIT_*`, safety/read разрешены, живые сделки доживают под текущим
  стопом, **без** kill-switch; триггер — неожиданное поведение биржи,
  п.2) и `Exchange.TRADE_BLOCKED` — полная реакция (то же + kill-switch
  flatten всей биржи + каскад активных сделок в `ERROR`; триггер —
  живой риск без защиты, п.4). Дом — `docs/rules/exchange-hold.md`;
  решение с альтернативами — `docs/rules/exchange-hold.md`.
- **Альтернативы:** сохранить безусловный flatten на controlled
  exception (отвергнуто: ступени п.2/п.4 схлопываются в одну, kill
  платит рыночную цену за ещё защищённый риск); kill только
  незащищённых сделок на ступени 2 (отвергнуто: инвариант защиты
  нарушен — доверие к собственному учёту «защищено» подорвано);
  новые имена ступеней (отвергнуто: `HOLD` и `TRADE_BLOCKED` уже в
  обороте, переиспользование снимает запаркованный разнобой имён).
- **Почему:** kill-switch адресует незащищённый живой риск — на
  ступени 1 адресата нет (стопы живы на бирже), реакция без адресата
  платит цену за отсутствующий риск (принцип уже в
  `error-handling-policy.md`); консервативный принцип «радиус
  неизвестен → тормозим широко» сохранён в **scope** (вся биржа) на
  обеих ступенях.

### Д3. Выходы и границы ступеней лестницы

- **Решение:** снятие обеих ступеней — вручную; `TRADE_BLOCKED`
  снимается только в `HOLD` (после подтверждения «незащищённого
  живого риска нет»), `HOLD` — в `ACTIVE` (после «причина понята и
  устранена»); эскалация `HOLD → TRADE_BLOCKED` разрешена и реакцию
  не пропускает; вход в `HOLD` — из `ACTIVE`, в `TRADE_BLOCKED` — из
  любого статуса.
- **Альтернативы:** снятие `TRADE_BLOCKED` сразу в `ACTIVE`
  (отвергнуто: после flatten нужно окно наблюдения/разбора — спуск по
  ступеням даёт его штатно); авто-снятие по восстановлению признака
  (отвергнуто ранее держателем, `GAPS_CLOSE_6`, — не пересматривалось).
- **Почему:** зеркалит ратифицированные множества входа инструмента
  (`instrument-hold.md`); два разных условия снятия
  (риск устранён / причина понята) — два разных шага.

### Д4. Cleanup/safety-команды — третье исключение иерархии, не дефект

- **Решение:** свип по правилу «иерархия исполнения» показал тропы
  эмиссии команд вне `StrategyAction` (cleanup handler'ов:
  `ExitPendingHandler`, `ErrorHandler`, реакция `AnomalyJob`) — они
  признаны легитимным исключением и вписаны в
  `docs/rules/execution-hierarchy.md`, а не
  «починены» под иерархию; заодно `ServiceCommandExecutor.md` приведён
  к классовому принципу многоэндпоинтности `REFRESH_*` (вместо
  поимённого перечисления двух команд).
- **Альтернативы:** переделать cleanup-тропы под действия (отвергнуто:
  ратифицировано `fsm-execution-layering.md` — cleanup сознательно вне
  действий, без анкера; иерархия держателя — про создание торгового
  намерения, не про снятие риска).
- **Почему:** правило п.1 говорит «торговля идёт по стратегии»;
  риск-снижающие дочистки — той же природы, что kill-switch
  (уже названное исключение).

### Д5. Что осталось в §3 процесса после снятия механики ступеней

- **Решение:** в переписанном §3 `roadmap-step-execution.md` сохранены
  дисциплины против переоткрытий (закрытие по списку цитат находки,
  терминологический свип при смене политики, структурный свип при
  вводе поля, клеймы полноты, сцепленность хвостов, разведочный режим
  guard'а) + добавлены автономный разбор и правка через дом политики;
  сняты — сверки «ожидаемый = ответивший», флаги действия CC и
  «расширение», слепые проходы, пакеты; из отчёта свипа убран полный
  вывод грепа (осталась команда + пофайловая таблица исходов).
- **Альтернативы:** снять и свипы целиком (отвергнуто: недоведённые
  свипы — источник №2 потока вопросов по анализу; при «доме политики»
  свип дешевеет сам); оставить полные выводы грепов (отвергнуто
  решением 5 держателя — форма кормила рост отчётов).
- **Почему:** механика ступеней снята решением держателя; дисциплина
  доведения правок — ортогональна ей и остаётся.

### Д6. Выражение лестницы в компонент-моделях холда

- **Решение:** `HoldSignal.ReactionClass` получает третье значение
  `FREEZE` (заморозка) между `SOFT` и `FULL`; существующая фабрика
  `exchange(code, exchangeId)` перенацелена на ступень 1
  (`EXCHANGE + FREEZE → Exchange.HOLD`), для ступени 2 вводится
  `exchangeTradeBlock(code, exchangeId)` (`EXCHANGE + FULL`);
  `SafetyHoldCoordinator` остаётся держателем только FULL-реакции —
  FREEZE ставит `HoldService` напрямую; отчёт ступени 1 идёт
  `AnomalyReport.severity = NON_CRITICAL` (ратифицированная семантика
  «severity отвечает только на „гоняется ли kill-switch“» сохранена).
- **Альтернативы:** новая фабрика для ступени 1 при сохранении
  `exchange()` за FULL (отвергнуто: все зарегистрированные зовущие
  `exchange()` — controlled-тропы, которые и переезжают на ступень 1;
  перенацеливание фабрики меньше трогает реестр зовущих); новое
  значение severity для «холд без kill» (отвергнуто: ломает
  ратифицированную семантику severity ради уже выразимого).
- **Почему:** минимальный след (п.3): существующие имена и семантики
  переиспользованы, новых сущностей — одно значение енума и одна
  фабрика.

### Д7. Границы переписывания Custom Instructions чата

- **Решение:** сверх списка держателя «оставить» сохранён один
  сжатый пункт «Отладка пайплайна» (сначала дефицит, потом ответ;
  правка агента/процесса вместо ручной адъюдикации) — 4 строки вместо
  прежнего развёрнутого; узловые эскалации, дайджесты
  `DOCS_CHECK_N`/`GAPS_CLOSE_N` с аудитом, сверки пакетом, слепые
  проходы, гейт/леджер — убраны полностью; итог 32,6 → 20 KB.
- **Альтернативы:** убрать отладку пайплавна целиком (отвергнуто:
  пункт не из валидационной механики, на него ссылается
  `.claude/processes/pipeline-shakedown.md`, и он действует при любом
  режиме).
- **Почему:** список «убрать» перечислял валидационную механику;
  цикл отладки — ортогональная методология.

### Д8. Распил `pnl-finalization-mechanics.md`: дом сверки — новое правило

- **Решение:** действующая политика сверки bills ↔ net и асимметрия
  троп отказа добычи получили дом `docs/rules/pnl-reconciliation.md`
  (~12 KB); решение сжато до указателей (157 → 24 KB) с сохранением
  входящих §-якорей (§Следствия, §5a, §«Предусловия `CODE`»); полная
  редакция — в
  `.claude/work/history/2026-08-26-policy-home-split/`.
- **Альтернативы:** разнести сверку по Deal.md + компонентам без
  общего правила (отвергнуто: политика сквозная — Deal, финализатор,
  AnomalyReport, ни один не владеет целиком; ровно этот кластер давал
  переоткрытия — epsilon, 7 заходов); отдельные decision-файлы на
  каждое из шести решений (отвергнуто: пакет один, дома политик уже
  разъехались — шесть указателей в одном файле дешевле шести файлов).
- **Почему:** `policy-home` + критерий владельца
  (`rule-source-of-truth`): сквозное — в `docs/rules/`.

### Д9. Потолок отчёта прогона — 40 KB

- **Решение:** ориентир объёма отчёта `DOCS_CHECK_N` — до 40 KB
  (никогда не сотни); полные выводы грепов в отчёт не вклеиваются
  (команда — да, вывод — нет); зафиксировано в
  `.claude/templates/docs/gap-report.md` §«Потолок объёма».
- **Альтернативы:** 20 KB (отвергнуто: три линзы + верификация CC на
  реальном шаге в 20 KB не помещались и потолок продавливался бы
  сразу); без числа, «десятки KB» прозой (отвергнуто: непроверяемо).
- **Почему:** мандат решения 5 («десятки KB, не сотни», число — выбор
  CC); 40 KB — ×6 меньше пиковых 232-255 KB и вмещает 20-30 находок
  новой формы.

### Д10. Диета рабочих файлов — исполнение и пограничные случаи

- **Решение:** `backlog.md` 130 → 92 KB (закрытое — в
  `history/2026-08-26-workfiles-diet.md`, policy-пересказы — до задач
  с указателями, добавлена CODE-дельта лестницы и пункт пересмотра
  автономии в гейт закрытия фазы 1); `open-questions.md` 62 → 27 KB —
  **ни один вопрос не признан закрытым** (все 16 несут живой остаток),
  каждый сжат до «постановка + блокер + владелец + указатели»;
  закрытые под-части вопросов сведены к строкам-указателям; заголовок
  IND-Q1 «(владелец — шаг 3)» → «(якорь — фаза 4)» (шаг 3 закрыт,
  пометка противоречила телу).
- **Альтернативы:** закрывать вопросы с «де-факто действующим»
  вариантом (например OKX-Q2) — отвергнуто: в тексте это отложение,
  не решение; при неочевидности вопрос оставлялся живым.
- **Почему:** правило диеты (`closed-work-transfer.md`) велит убирать
  решённое, а не спорное; хроника заходов и снятая аргументация
  (H26 в PNL-Q1, разбор H8 в RISK-Q3) — в git/архиве, живой остаток —
  в файле.

### Д11. Ступень 2 поверх L3, не пятый уровень

- **Решение:** градация остаётся четырёхуровневой; уровень 4 внутри
  двухступенчатый (дом лестницы — `exchange-hold.md`); реестр реакций
  дополнен формой «заморозка» между «запретом новых входов» и «холдом
  с kill-switch»; бесстоповая live-позиция постфактум переезжает с L3
  (инструмент) на ступень 2 уровня 4 (вся биржа) — по п.4 держателя.
- **Альтернативы:** ввести уровень 5 (отвергнуто: перенумерация L1-L4
  разошлась бы по десяткам носителей ради того же содержания).
- **Почему:** минимальный след (п.3); «4 уровня» — устойчивая ссылка
  по всей базе.

### Д12. Похудение `Deal.md`: остаток больше ориентира — намеренно

- **Решение:** `Deal.md` 136 → 89,5 KB; снята история (номера находок,
  снятые редакции, кейсы — архив в
  `history/2026-08-26-policy-home-split/Deal-full.md`), сверка
  bills ↔ net сведена к Deal-части + указателям на
  `docs/rules/pnl-reconciliation.md`, реакция на нарушение контракта
  записи приведена к ступени 1; все 17 входящих §-якорей сохранены.
  Остаток выше ориентира 35-45 KB оставлен: это собственность `Deal`
  по критерию владельца (таблицы структуры/енумов/персистентности,
  действующие формулы R и предикаты) — дальнейшее сжатие резало бы
  действующую политику.
- **Альтернативы:** дожать до ориентира выносом формул R в отдельное
  правило (отвергнуто: формула поля — собственность сущности,
  `rule-source-of-truth`; вынос создал бы новый носитель против п.3);
  свести §«Обнаружение рассогласования пары носителей» к указателю
  (отвергнуто: пара `Type ↔ positionReducingOnly` — инвариант ног
  `Deal`, и `pnl-reconciliation.md` сам ссылается сюда как на дом).
- **Почему:** ориентир объёма — следствие снятия истории, не бюджет
  (та же логика, что у snapshot-format).

---

## Итерация 2026-08-27: закрытие `DOCS_CHECK_18` (`GAPS_CLOSE_18`)

### Д13. Носитель эпизодов многоэпизодной сделки — строка `Position`

- **Решение:** инвариант «≤1 `Position` на `Deal`» заменён на «≤1
  **живая**»; закрытый эпизод остаётся строкой той же таблицы,
  адресуемой своим `posId`. `Deal.position` → `Deal.positions` +
  предикат `livePosition()`; `resultProfit` — Σ по строкам, пересчётом
  (не накоплением). Схема: `−uk_position_deal`, `+uk_position_deal_live`
  (частичный по `ACTIVE`), `+uk_position_deal_external`
  (`deal_id, external_id`), `+ix_position_deal`.
- **Альтернативы:** отдельная сущность эпизода (отвергнута: ни одного
  атрибута сверх `Position`, идентичность общая — `posId`);
  поле-аккумулятор на `Deal` (отвергнуто: ломает идемпотентность
  `REFRESH_*` — повторное чтение задваивало бы); транзитное чтение
  positions-history финализатором (отвергнуто: переоткрывает
  ратифицированный durable-дом факта закрытия).
- **Почему:** идентичность строки уже совпадает с идентичностью
  эпизода у источника; ослабление кардинальности дешевле новой
  сущности по шкале следа (`design-simplicity`), и держатель прямо
  просил «без лишних сущностей».

### Д14. Вопрос порядка «защита до ордера» разрешён как «вместе, не до»

- **Решение:** отдельного порядка не вводится. Защита risk-creating
  входа — встроенная, уходит **той же заявкой** (`attachAlgoOrds`), то
  есть окна «позиция без защиты» нет по построению; второй эпизод
  рождается защищённым, потому что его нога проходит тот же
  преконтроль.
- **Альтернативы:** ставить standalone-защиту до ордера (**неисполнимо**:
  все семь `AlgoOrder.ConditionType` — protective/closing, размер
  считается от несуществующей позиции — заявка была бы reduce-only без
  предмета); гейт «ждать подтверждения защиты перед филлом» (отвергнут:
  ничего не добавляет к attached-механизму и вводит ожидание).
- **Почему:** гипотеза держателя проверена по интеграции OKX; «вместе»
  строго сильнее «до», и механизм уже ратифицирован (Р8
  `GAPS_CLOSE_16`).

### Д15. Ступень 1 биржевой лестницы остаётся без автоматических триггеров — ⚠️ ОТМЕНЁН Д22/Д23

> **Не действует.** Решение отменено следующей итерацией: держатель
> назначил ступени 1 **автоматический** триггер (`MISMATCHED` при
> калиброванном допуске), и вместе с ним вернулись `ReactionClass.FREEZE`
> и фабрика `HoldSignal.exchange(...)`. Действующая редакция — **Д22**
> (актор реакции) и §«Итерация 2026-08-27 (2): закрытие `DOCS_CHECK_19` (`GAPS_CLOSE_19`)» ниже; дом —
> `docs/rules/pnl-reconciliation.md` §«Реакция на расхождение»,
> `docs/rules/exchange-hold.md`. Пункт оставлен непомеченным до N4
> `DOCS_CHECK_20`; обе итерации на момент пометки не прочитаны
> пользователем, то есть пакет содержал два взаимоисключающих решения
> без указателя, какое действует.

- **Решение:** после переноса «неожиданного поведения биржи» на ступень
  2 у ступени 1 остаются только ручная постановка и спуск со ступени 2;
  так и записано. Класс реакции `FREEZE` и фабрика
  `HoldSignal.exchange()` **не вводятся** (производителей нет);
  `Exchange.HOLD` сохраняется — он нужен как ручной гейт входов и как
  посадочная ступень при снятии `TRADE_BLOCKED`.
- **Альтернативы:** упразднить ступень 1 целиком (отвергнуто:
  ратифицированный спуск `TRADE_BLOCKED → HOLD → ACTIVE` проверяет два
  условия по одному — без промежуточной ступени окно наблюдения
  исчезает); придумать ступени 1 новый автоматический триггер
  (отвергнуто: спекулятивное правило, держатель прямо запретил
  выдумывать).
- **Почему:** «если штатных триггеров нет — так и зафиксируй» —
  указание держателя; пустая клетка названа, а не замаскирована.

### Д16. Блок-сет ступени 2 — все команды `Trade`, read открыт

- **Решение:** «ничего не шлём» материализовано как блок **всех** команд
  permission `Trade` после teardown; единственное исключение — сам
  teardown kill-switch'а. Read/refresh остаётся открыт.
- **Альтернативы:** закрыть и read (отвергнуто: нарушает инвариант
  «сделка всегда доходит до терминала» — каскад увёл сделки в `ERROR`, и
  терминализация требует фактов; человек остался бы без материала
  разбора); оставить `CANCEL_*`/`CLOSE_POSITION_COMMAND` открытыми, как
  было (отвергнуто: прямо противоречит «ничего не шлём»).
- **Почему:** «сворачиваемся» — про торговое намерение, не про
  наблюдение.

### Д17. Актор маршрутизации «неполное число ⇒ ошибочная тропа» — `ExitPendingHandler`

- **Решение:** предикат исполняет выходная проверка handler'а; живая
  строка `FINALIZE_DEAL_EXIT_ACTION` закрывается `SKIPPED`, холд не
  поднимается. Сам предикат переписан перечнем значений
  (`breakdownIncomplete ∈ {INCOMPLETE_BY_WINDOW, NOT_ASSESSED}` либо
  `rateStatus ∈ {RATE_UNAVAILABLE, SETTLE_CURRENCY_UNAVAILABLE}`).
- **Альтернативы:** актор — `MarkDealClosedExecutor` (отвергнут: пятое
  ребро и звено с двумя статусными исходами — конструкции в каноне
  нет); актор — само действие, уступающее `FINALIZE_DEAL_ERROR_ACTION`
  (отвергнут: `next(type, ctx)` возвращает звено **своего** действия,
  «уступить» ему нечем — потребовалась бы новая конструкция).
- **Почему:** развилка «`CLOSED` vs `ERROR`» на этой тропе уже
  принадлежит handler'у; цена — задержка в один проход при снятом живом
  риске.

### Д18. Второму и третьему числам риска — свой предикат отбора

- **Решение:** `incurredRiskAmount` / знаменатель `currentRiskAmount`
  отбирают ноги по `type ∈ входных И accumulated_fill_size > 0`, без
  конъюнкта по статусу; знаменатель `R` свой предикат сохраняет.
  Вычитаемое `protectionRelievedRiskAmount` получило закрытую форму
  (комиссионный член входит, размер поногово, знак не клэмпится).
- **Альтернативы:** один предикат на все числа (отвергнут: исключает
  частично исполнившуюся и снятую ногу — ровно ту, ради которой второе
  число заведено; третье число при этом ломается арифметически);
  чисто ценовое вычитаемое (отвергнуто: ложный «снятый риск» порядка
  двойной комиссии на каждой сделке).
- **Почему:** клауза дока уже обещала это поведение, а предикат его
  отменял; счётный случай назван на обеих осях.

### Д19. Потолок отчёта против мандатных полей — решён в пользу полей

- **Решение:** ориентир 40 KB относится к отчёту **штатной структуры**
  и бюджетом не является; мандатные поля находки ему не уступают,
  перечни вариантов остаются в отчёте, превышение **называется**, а не
  устраняется урезанием. Записано в дом формы
  (`.claude/templates/docs/gap-report.md` §«Потолок объёма»).
- **Альтернативы:** вынести перечни вариантов отдельным носителем
  (отвергнуто: «перечень самонесущий», а закрытие идёт по цитатам той же
  находки — расщепление вернуло бы исходный класс дефекта); жёстко
  резать до 40 KB (отвергнуто: теряются входы закрытия).
- **Почему:** та же ошибка чтения, что уже снята у снапшота
  (`snapshot-format.md`); правило переиспользовано, а не изобретено.

---

## Итерация 2026-08-27 (2): закрытие `DOCS_CHECK_19` (`GAPS_CLOSE_19`)

### Д20. Осуществимость калибровки допуска — реальных сделок не требует

- **Решение:** калибровка исполнима на demo (прокси `/raw` + фикстура
  `.claude/tests/source-api/okx/plan.md` §AG1.5 + `GET /account/bills`); три калибруемые величины
  **безразмерны или абсолютны-округлительные** (`k` — множитель
  ожидаемой комиссии, `0.5%` — доля оборота, `0.01` — шум-флор),
  поэтому различие demo и прода по **уровню** ставок и фондирования
  перенос не обесценивает. Проверить надо **структуру** потока
  (гранулярность trade-bill, состав типов, знаки) — разовой ад-хок
  сверкой на проде. **Численный риск-аппетит держателю не выносится.**
- **Альтернативы:** калибровать малыми реальными сделками (отвергнуто:
  величины безразмерны, структура проверяется чтением); отложить
  калибровку до накопления живой статистики без интеграторского прогона
  (отвергнуто: держатель назвал прогон основным путём, статистика —
  страховка).
- **Почему:** держатель просил проверить осуществимость и вынести
  риск-аппетит, **если** переносимости нет; переносимость есть по той
  оси, которая для калибровки существенна.

### Д21. Разведочный режим — флаг контура, ручной выход, `N` провизорно

- **Решение:** носитель — per-exchange настройка
  (`@ConfigurationProperties`, дом того же класса, что список исключений
  сверки); выход **ручной**, сервисной операцией по образцу снятия
  холда; порог наблюдений `N = 20` закрытых сделок с обязанной сверкой,
  **величина провизорна** и объявлена таковой; счётчика не заводится —
  величина выразима запросом по `deals`. Срок привязан к **выходу в
  боевую торговлю**, встречный якорь — `backlog.md` §«Закрытие фазы 1 —
  снятие разведочного режима допуска сверки» + гейт в
  `update-roadmap-progress.md`.
- **Альтернативы:** автоматический выход по критерию сходимости
  (отвергнуто: заводит две новые провизорные величины вместо одной, а
  решение «наблюдений достаточно» — суждение); колонка-счётчик на
  `deals` (отвергнуто: величина выразима запросом по уже
  персистируемым признакам); выводить `N` из свойств потока
  (отвергнуто: вывода нет, выдавать провизорное за выведенное нельзя).
- **Почему:** переиспользован ратифицированный механизм встречного
  якоря — условие снятия, записанное только внутри правила, читает тот,
  кому оно и так не нужно.

### Д22. Актор реакции на `MISMATCHED` — исполнитель терминального ребра

- **Решение:** ступень 1 поднимают `MarkDealClosedExecutor` /
  `MarkDealEmergencyClosedExecutor` той же транзакцией, что применяют
  терминал. Финализатор в реестр детекторов **не возвращается**.
- **Альтернативы:** финализатор (отвергнут: он выведен из реестра **по
  построению**, чтобы радиусная реакция не поднималась поверх сделки,
  чей переход не применён; возврат заменил бы конструкцию оговоркой);
  выходная проверка handler'а (отвергнута: на аварийной тропе handler'а
  после терминала нет — потребовалось бы два разных актора).
- **Почему:** терминальное звено применяет переход само, поэтому
  конструкция сохраняется без исключений.

### Д23. Узел конвейера движений закрыт одним ходом — `billsFetchedThrough`

- **Решение:** новая колонка `deals.bills_fetched_through` (монотонная
  вперёд) — durable-факт «движения добыты до момента T». Закрывает
  сразу: предикат завершения bills-звена (`billsFetchedThrough ≥
  billsWindowEnd`), операнд «движения добывались» для обязанности сверки
  и `breakdownIncomplete`, горизонт догона курса. Линковка при этом
  переведена на **подвижную** границу (при сохранении, пока `Deal`
  нетерминален).
- **Альтернативы:** «`∃` прилинкованная строка» как операнд (отвергнуто:
  не различает «добыли, движений нет» и «не добывали», и опирается на
  непроверенную посылку о непустоте набора); фиксированный порядок
  звеньев с bills последним (отвергнуто: вводит курсор там, где
  стадия выводится из фактов, и оставляет аварийную тропу без
  закрывающего звена); отдельное системное действие под добычу
  (отвергнуто: ось «enum дублирует durable-дискриминатор» отвергнута
  дважды).
- **Почему:** одна колонка закрывает три гейтящих находки; Р7
  («добыча не ждёт границы») не переоткрывается — меняется предикат
  **завершения**, а не эмиссии.

### Д24. Пересчёт четырёх чисел риска — единым правилом, а не по полям

- **Решение:** кто меняет любой операнд — пересчитывает всю четвёрку
  целиком; писателей семь (добавлены `RefreshAlgoOrderExecutor` и
  `CancelAlgoOrderExecutor`). Клейм «ветка вырожденного знаменателя
  недостижима» снят: она **обрабатываема**.
- **Альтернативы:** сохранить разделение места истины и привести к нему
  три компонент-дока (отвергнуто: остаётся незаписанное окно
  рассогласования между наблюдением филла и наблюдением позиции); не
  персистить третье и четвёртое числа (переоткрывает принятое).
- **Почему:** только единое правило делает значения функцией
  **состояния**, а не порядка событий, — что доки сами и объявляют.

### Д25. Принцип писателя записан классом — новое правило

- **Решение:** `docs/rules/writer-named-for-every-value.md`; оговорка в
  `absent-value-semantics.md` §«Чего правило не решает» сведена к
  ссылке. Тем же классом закрыты имена ключей двух новых таблиц.
- **Альтернативы:** оставить требование границей чужого правила
  (отвергнуто: в этом виде оно чинилось поштучно пять прогонов подряд и
  дало два новых пробела одним прогоном).
- **Почему:** сработал детектор «механика не определена ⇒ проверь на
  незаписанный принцип»; принцип действовал, но был записан случаем.

### Д26. Свип процесса расширен на правки кардинальности и предикатов

- **Решение:** `.claude/processes/roadmap-step-execution.md` §3 получил
  второй перечень носителей — сборщики контекста, исполнители-читатели
  коллекции, предикаты-соседи поля, сборка schema-дельты, клаузы «этого
  не бывает». Признак применимости другой: имя не вводится, поэтому
  текстовый и структурный свипы проходят чисто.
- **Альтернативы:** расширить существующий перечень (отвергнуто: у него
  другой признак применимости — «закрытие называет новое имя»);
  ограничиться кейсом в отчёте (отвергнуто: класс дал восемь из десяти
  гейтящих находок прогона).
- **Почему:** дефект был процессным, а не предметным.

## Итерация 2026-08-28: закрытие `DOCS_CHECK_20` (`GAPS_CLOSE_20`)

### Д27. `Deal.billsWindowEnd` — монотонное вперёд, не write-once

- **Решение:** граница двигается с каждым закрывшимся эпизодом (guard
  `where bills_window_end is null or bills_window_end < :uTime`);
  предикат завершения `REFRESH_POSITION_COMMAND` получил квантор «у
  каждой строки `Position` сделки». Второго носителя (граница эпизода)
  не заводится.
- **Альтернативы:** оставить write-once (отвергнуто: предикат
  завершения bills-звена истинен **до** закрытия последнего эпизода, и
  число уходит в чистый `CLOSED` заниженным, а не в ветку «недоступно»);
  отдельная колонка `bills_window_end_last_episode` (отвергнуто: больший
  след без названного различения — четыре потребителя читают границу на
  финализации, когда двигаться уже нечему).
- **Почему:** формула «uTime последней записи, окно накрывает все
  эпизоды» уже стояла в исполнителе; write-once и она написаны **одним**
  закрытием и противоречили друг другу.

### Д28. Дискриминатор броска радиусной реакции — дизъюнкция

- **Решение:** бросок не поднимается, когда `Deal.status = ERROR`
  **или** «живого риска нет» (коллекция `positions` **непуста**, все
  строки `CLOSED`, положение закрытия добыто у каждой). Явная непустота
  — против вакуумной истинности.
- **Альтернативы:** оставить C5-предикат один (отвергнуто: на аварийной
  ветви (b) он ложен и даёт полный холд там, где H3/H4 ратифицировали
  терминал без холда); вернуть `Deal.status = ERROR` один (отвергнуто:
  не покрывает довод C5 — штатный отказ добычи разбивки при закрытой
  позиции платит остановкой контура).
- **Почему:** оба довода радиуса ратифицированы и не покрывают друг
  друга; цена дизъюнкции — один операнд, новых мест чтения ноль.
- **Побочно:** `POST_MORTEM_HARVEST_EXHAUSTED` **снят** — производителя
  не осталось, а реестр не держит значений без производителя.

### Д29. Ребро `* → ERROR` — карв-аут по природе тропы

- **Решение:** тропы **решения** (handler решил) — ребро пишет звено
  `MARK_DEAL_ERROR_COMMAND`; тропы **перехвата** (enforcement холда, оба
  `catch` оркестратора) — прямая запись петлёй. Затребователь первого
  исполнения — handler тропы решения, второго — `ErrorHandler` на обеих
  тропах.
- **Альтернативы:** «всегда звено» (отвергнуто: требует эмиссии
  действия из обработчика исключения — новая точка отказа там, где нужен
  гарантированный перевод); «всегда `nextStatus`» (отвергнуто:
  `MARK_DEAL_ERROR_COMMAND` теряет единственное ребро, аварийная тропа —
  анкер попыток).
- **Почему:** оба механизма действовали фактически, граница между ними
  не была записана — четыре носителя описывали ребро пассивным залогом.

### Д30. Гейт `positionReopenAllowed` — наблюдатель `ManagingHandler`

- **Решение:** входная классификация `ACTIVE && externalSize == 0`
  ветвится по параметру: `true` — штатное состояние, `false` — гейт
  снимает живые входные ноги cleanup-командами.
- **Альтернативы:** наблюдатель `RefreshPositionExecutor` (отвергнуто:
  исполнитель команды не читает параметры стратегии по построению);
  отдельный тип шага стратегии (отвергнуто: необъявленный шаг молча не
  работает — ровно то, чего избегает обязательность параметра).
- **Почему:** прежняя редакция относила `externalSize == 0` к
  cleanup/retry/anomaly **безусловно**, то есть считала дефектом и
  легитимное переоткрытие.

### Д31. Операнд трейлинга — `condition.trailing.externalPrice`

- **Решение:** имя операнда исправлено; приземление `condition.*`
  утверждено — пополевой мердж `updateFromSnapshot` с
  `nullValuePropertyMappingStrategy = IGNORE`.
- **Альтернативы:** плоская колонка
  `algo_orders.external_trailing_stop_price` (отвергнуто: больший след,
  дублирует уже живущее значение); оставить `Order.plannedStopPrice`
  (отвергнуто: возвращает снятый дефект — при трейлинге attached снята,
  `plannedStopPrice` неподвижен).
- **Почему:** клейм дока о коде был **ложен** (`AlgoOrder.externalPrice`
  = `actualPx`, верифицировано по `AlgoOrderMapper.java`), а реализация
  по месту истины давала бы постоянно пустую колонку на трейлинговом
  классе.

### Д32. Судьба `ix_deal_cash_flow_unlinked` и used-статуса `instId`

- **Решение:** оба переобоснованы через **действующую** тропу:
  `instId` — ось предиката линковки (ответ `/account/bills`
  аккаунт-широкий), индекс — форвард-слот шага 8.
- **Альтернативы:** снять индекс и вывести `instId` в unused
  (отвергнуто: ломает действующий предикат); снять только индекс
  (отвергнуто: заведение сейчас бесплатно, ретроспективное — миграция на
  непустой таблице).
- **Почему:** единственная названная популяция отложенной линковки
  («строки предыдущей редакции контура») исключена правилом «таблицы
  пусты».

### Д33. Форма комиссионного члена `riskAtCurrentStop`

- **Решение:** член считается по `stopCurrent_i` (не по плановому
  стопу); ставка выводится **обращением закрытой формы сайзинга** из
  шести persisted-чисел — нового поля не заводится.
- **Альтернативы:** заморозить член на плановом стопе (отвергнуто:
  ломает клейм «при недвинутом стопе поле равно нулю точно»);
  персистить ставку восьмым числом (отвергнуто: выводима,
  `design-simplicity`).
- **Почему:** прочтений было два, и клейм точности верен только при
  одном; выбор записан явно.

### Д34. Способ разведения имён уровней лимита риска — ⚠️ ЧАСТИЧНО ОТМЕНЁН Д43

> **Отменена вторая половина решения** (N2 `DOCS_CHECK_21`): имя
> `riskPerDealPercent` («потолок как процент базы») снято **тем же
> закрытием**, хвостом `RISK-Q3-A` — вместе со сменой формы второго
> лимита на множитель. Действующие имена — Д43
> (`cumulativeRiskPerDealMultiplier` /
> `simultaneousRiskPerDealPercent`, последнее разведено по владельцу
> Д47). **Способ** разведения (систематический маркер) в силе — отменено
> только конкретное имя.

- **Решение:** систематический маркер уровня — `riskPerActionPercent`
  (поактный) и `riskPerDealPercent` (потолок сделки); существующее
  `riskPerTradePercent` переименовано, код реджекта — тоже.
- **Альтернативы:** уникальные основы (`riskPerTradePercent` +
  `dealRiskCapPercent`) — ноль правок сейчас, но имя `perTrade`
  продолжает утверждать уровень сделки, будучи поактным, то есть
  сохраняет ровно ту ложную запись, которую нашла C6.
- **Почему:** развилка пройдена **до** подбора имён с обеими осями в
  переборе (`.claude/rules/naming.md` §«Разведение уровней
  абстракции»); цена — механическая правка на пустых таблицах, и она
  тем дешевле, чем раньше сделана.

### Д35. Носитель накопленного риска сделки — существующее поле — ⚠️ ОТМЕНЁН Д42/Д46

> **Отменено** (N2 `DOCS_CHECK_21`): форма правой части снята Д42
> (множитель вместо процента базы), код `RISK_PER_DEAL_EXCEEDED` больше
> не существует (действующие — `RISK_PER_DEAL_CUMULATIVE_EXCEEDED` /
> `RISK_PER_DEAL_SIMULTANEOUS_EXCEEDED`), а операнд слева перестал быть
> голым `plannedRiskAmount` — Д46. В силе остаётся только принцип
> «третьего носителя не заводится».

- **Решение:** потолок сделки проверяется по `Deal.plannedRiskAmount`
  (уже Σ по живым и исполнившимся ногам входа) против
  `riskPerDealPercent × Deal.plannedRiskEquityBase`; отдельного
  «расходуемого бюджета» не заводится. Второй эпизод отдельного
  преконтроля не требует — нога, его открывающая, прошла преконтроль при
  постановке и с тех пор входит в Σ.
- **Альтернативы:** расходуемый ресурс (вариант (в) `RISK-Q3`) —
  отвергнут: второй носитель той же величины с инкрементальным
  накоплением, зависящий от порядка событий.
- **Почему:** `design-simplicity` — универсализировать существующее.
- **`BLOCKED` по `RISK_PER_DEAL_EXCEEDED` в `ERROR` не уводит:**
  исчерпанный бюджет — ожидаемый исход стратегии, не рассогласование
  состояния; карв-аут по образцу `PRECHECK + BLOCKED ⇒ CLOSED`.

### Д36. Пола `minSz` у reduce-only размера нет

- **Решение:** три исхода округления вниз (штатный частичный; выход
  целиком с журнальным `PARTIAL_EXIT_ROUNDED_TO_FULL`; неисполнение с
  `PARTIAL_EXIT_BELOW_MIN_SIZE`).
- **Уточнено `GAPS_CLOSE_27` (E4): исходов четыре, не три** — полных
  выхода два, и они разведены значением (`FULL_BY_FRACTION` против
  `FULL`). Действующий перечень и довод — ниже, §«Итерация 2026-08-29:
  закрытие узла Н7 `GAPS_CLOSE_27`».
- **Альтернативы:** поднимать до `minSz` (отвергнуто: молчаливая
  подмена решения автора стратегии в благоприятную сторону);
  блокировать всегда (отвергнуто: на позиции с остатком меньше `minSz`
  частичный выход невыразим в принципе).
- **Почему:** обе minimal-проверки смотрят на сторону и неувеличение
  позиции; «закрыли больше объявленного» ни одна не выражает.

### Д37. Ценовая база триггера защиты — принцип записан, поле заведено

- **Решение:** база триггера у **всякой** защитной заявки — объявленное
  стратегией `StopLossSettings.triggerPriceType`; заведены
  `AttachedAlgoOrder.triggerPriceType`, колонка
  `attached_algo_orders.trigger_price_type` и поле payload'а; биржевой
  default не подставляется. `RiskValidator` знает базу стопа при
  проверке запаса до ликвидации.
- **Альтернативы:** зафиксировать `MARK` константой (отвергнуто: у
  стратегии уже есть обязательное поле — константа его обесценила бы);
  оставить как есть (отвергнуто: инвариант «ликвидация за стопом»
  читается выполненным при разных базах — отказ тихий и направленный).
- **Почему:** сработал детектор «механика не определена ⇒ незаписанный
  принцип» (четыре однотипных артефакта).

### Д38. Гейт исполнения свипа — в скилле, не третьим перечнем в §3

- **Решение:** `.claude/skills/update-roadmap-progress.md` получил
  §«Гейт выхода из `GAPS_CLOSE_N`»: политикоменяющее закрытие не
  переводится в следующий статус, пока отчёт не несёт раздела свипов с
  командами и пофайловой таблицей.
- **Альтернативы:** третий перечень носителей в §3 (отвергнуто прямо:
  пропущенные носители покрывал **первый** перечень — дефект был в
  непрогоне, а не в неполноте списка).
- **Почему:** та же конструкция встречного якоря, что у гейта закрытия
  фазы: правило, записанное только в процессе, читает тот, кому оно и
  так нужно.

## Итерация 2026-08-28 (2): хвост `GAPS_CLOSE_20` — `RISK-Q3-A` и П7-B

### Д39. Одновременный лимит — производное четвёрки, третьего носителя нет

- **Решение:** живой риск в моменте =
  `max(0, Deal.plannedRiskAmount − Deal.protectionRelievedRiskAmount)`.
  Величина раскладывается ровно на две половины, названные держателем
  (невыполненные входные ноги = `plannedRiskAmount − incurredRiskAmount`;
  открытая позиция при действующей защите = `riskAtCurrentStop` =
  `incurredRiskAmount − protectionRelievedRiskAmount`), и
  `incurredRiskAmount` в сумме сокращается. Ни колонки, ни счётчика
  остатка не заводится — остаток вычисляется в точке проверки.
- **Альтернативы:** отдельное persisted-число «живой риск» (отвергнуто:
  четвёртый носитель той же величины, а четвёрка уже пересчитывается
  целиком семью писателями); инкрементальный счётчик остатка
  (отвергнуто тем же доводом, что «расходуемый бюджет» на кумулятивном
  уровне — зависимость от порядка событий).
- **Почему:** `design-simplicity` — универсализировать существующее;
  величина по построению не зависит от порядка событий.
- **Две клаузы записаны явно:** пустое `protectionRelievedRiskAmount`
  даёт вычитаемое ноль (**консервативное** прочтение, не благоприятное
  умолчание); знак клэмпится в нуле **в лимите**, а не в поле — иначе
  запертая прибыль покупала бы дополнительный риск.

### Д40. Одновременный лимит — глобальный конфиг, база текущая

- **Решение:** `simultaneousRiskPerDealPercent = 1%`,
  `@ConfigurationProperties`, колонки нет; база — **текущий**
  `externalAvailableEquity`, в отличие от кумулятивного потолка, чья
  база зафиксирована `Deal.plannedRiskEquityBase`.
- **Альтернативы:** поле детали стратегии (отвергнуто решением
  держателя: стратегия обязана умещаться, не переопределять);
  фиксированная база (отвергнуто: лимит отвечает на вопрос «сколько под
  ударом **сейчас**», и база обязана быть сегодняшней).
- **Названное ограничение:** радиус конфига глобальный, не per-exchange;
  при второй бирже величина захочет разреза, как уже случилось со
  списком исключений сверки.

### Д41. Валидация одновременного лимита — статика на create + динамика в преконтроле

- **Решение:** на create стратегии (400) — `n_risk_creating(step) ×
  riskPerActionPercent ≤ simultaneousRiskPerDealPercent` для **каждого**
  шага (пакет действий шага исполняется целиком); в сопровождении —
  третье неравенство `RiskValidator` с кодом
  `RISK_PER_DEAL_SIMULTANEOUS_EXCEEDED`.
- **Альтернативы:** только динамика (отвергнуто: стратегия с заведомо
  непроходимым веером ног принималась бы и молча исполнялась частично);
  только статика (отвергнуто: повторные срабатывания
  `GRID_MANAGEMENT` статически не объявлены и объявляться не будут).
- **Почему проверка попадает на create вопреки «торгово-суждённое — на
  activate»:** она не суждённая, а арифметическая — сравнивает
  объявленный веер с числом, известным до запуска.

### Д42. Кумулятивный потолок — множитель, без валидации

- **Решение:** `StrategyDetail.cumulativeRiskPerDealMultiplier` (`K`);
  форма — `K × riskPerActionPercent × Deal.plannedRiskEquityBase`.
  Прежняя редакция «процент базы» (`riskPerDealPercent`) снята.
  **Валидации нет ни снизу, ни сверху** (решение держателя): при
  `K < 1` первая же нога не проходит, и это выбор автора стратегии.
  Предложенный CC инвариант `K ≥ 1` снят.
- **Почему следствие названо, а не запрещено:** отказ на `PRECHECK`
  громкий и немедленный (сделка закрывается с `RISK_CONTROL`, live risk
  не взят), а второй уровень контроля над явно объявленным числом
  дублировал бы первый.

### Д43. Имена двух сделочных лимитов разведены осью, не основой

- **Решение:** `cumulativeRiskPerDealMultiplier` и
  `simultaneousRiskPerDealPercent` — общая основа `RiskPerDeal`,
  префикс-маркер оси (`cumulative` / `simultaneous`), суффикс единицы по
  действующей конвенции числовых полей. Коды реджекта симметричны:
  `RISK_PER_DEAL_CUMULATIVE_EXCEEDED` /
  `RISK_PER_DEAL_SIMULTANEOUS_EXCEEDED`.
- **Альтернативы:** уникальные основы (`dealRiskCapPercent` +
  `simultaneousRiskLimitPercent`) — отвергнуто тем же доводом, которым
  выбран способ разведения на Д34: уровень должен читаться из любого
  имени. Развилка **повторно не проходится** — выбран уже принятый
  способ.

### Д44. `Deal.externalCreatedAt` — биржевой якорь нижней границы; писатель через scanner

- **Решение (приземление ответа держателя по П7-B, вариант (г)):**
  суррогат нижней границы окна bills — `Deal.externalCreatedAt`,
  биржевой момент создания сделки. Читает **`EntryScannerJob`** (`GET
  /public/time`) и передаёт в `DealOpeningService`, который пишет поле
  той же транзакцией, что создаёт строку.
- **Альтернативы:** запрос из `DealOpeningService` (отвергнуто: его
  контракт прямо говорит «на биржу не ходит», и он получает уже
  выбранные данные); `externalCreatedAt` первого наблюдённого объекта
  сделки (отвергнуто: зависит от того, что объект вообще появился, —
  ровно на подтропе «позиция вокруг чужого риска» его может не быть).
- **Отказ `public/time` ⇒ сделка не создаётся** — реджект на скане, до
  взятия риска; подстановка системных часов запрещена классом,
  подстановка любого другого числа — `absent-value-semantics.md`.
- **Схемной правки нет:** `external_created_at` уже есть у строки как
  audit-колонка; заводится **писатель**, не поле.
- **`PNL-Q2` закрыт этим же ходом:** провенанс границы различим без
  нового признака — `billsWindowBegin` непуст ⇒ добыта, пуст ⇒
  подставлена. Клауза-противоречие в `SystemActionExecutor.md` снята;
  у правила доменов исключений не осталось.

## Итерация 2026-08-28 (3): закрытие `DOCS_CHECK_21` (`GAPS_CLOSE_21`)

### Д45. База риска — свободный остаток расчётной валюты, не account-level equity

- **Решение (C6):** база сайзинга и всех трёх лимитов —
  `Balance.externalAvailableBalance` строки `ccy = settleCurrency`;
  величина названа **базой риска**, account-level
  `externalAvailableEquity` риск-контуром **не читается**.
- **Альтернативы:** записать единицу и состав `availEq` как есть
  (отвергнуто: база остаётся мультивалютным агрегатом с плавающей
  переоценкой, а её номинация не объявлена ни одним носителем); явный
  множитель курса (отвергнуто: третий источник курса, невоспроизводимо
  постфактум).
- **Почему:** риск и допуск сверки живут в settle-ccy — база обязана
  быть в той же единице; TR5 не переоткрывается, а доводится (отвергали
  переоценку в базе, агрегат несёт её же). Побочно снимает
  источниковый дефицит: непроверенная посылка о номинации перестала
  быть несущей.

### Д46. Операнды сделочных лимитов вычисляются в точке проверки, четвёрка — наблюдения

- **Решение (приземление C1/C2/C3):** `liveRiskNow` и `dealRiskTaken`
  считает `RiskValidator` по уже загруженному runtime graph
  (`DealContext.deal`); четыре персистируемых числа риска остаются
  отчётными, операндами лимитов не служат; `plannedRiskAmount` —
  по-прежнему знаменатель `R`.
- **Альтернативы:** пятое persisted-поле («взятое снятыми ногами»)
  — отвергнуто (`design-simplicity`: у величины нет отчётного
  потребителя, а граф в точке проверки уже собран); оставить лимиты на
  четвёрке — отвергнуто ответами держателя.
- **Почему:** требование «отчёт выразим без join'а» относится к выборке
  `R`, не к лимиту; так закрывается и «носителя остатка не заводится»
  (`RISK-Q3-A` не переоткрывается — носитель по-прежнему не заводится).

### Д47. Имена двух потолков одновременного лимита разведены осью владельца

- **Решение (C9):** `globalSimultaneousRiskPerDealPercent` (конфиг) и
  `strategySimultaneousRiskPerDealPercent` (поле детали); безмаркерное
  `simultaneousRiskPerDealPercent` снято.
- **Альтернативы:** уникальные основы (`systemRiskCeilingPercent` +
  `simultaneousRiskPerDealPercent`) — отвергнуто: два потолка одной
  величины перестают читаться как семья, инвариант «один ≤ другого»
  не виден из имён.
- **Почему:** тот же способ разведения, что выбран на прошлой развилке
  (`.claude/rules/naming.md` §«Разведение уровней абстракции»), —
  повторно развилка не проходится; префикс владельца ставится первым,
  чтобы имя читалось «владелец → ось → уровень → единица».

### Д48. `N_overlap` — механика create-анализа пересечений

- **Решение (C9, механика поручена CC):** статическая проверка
  сравнивает с максимумом стратегии **максимальную мощность множества
  risk-creating действий, способных быть живыми одновременно**: пакет
  одного шага целиком + действия соседних шагов, чьи ноги поздний шаг
  не снимает явным действием; повторные срабатывания одного шага
  статически не суммируются.
- **Альтернативы:** прежнее «пакет каждого шага по отдельности»
  (отвергнуто: молча полагает, что ноги разных шагов не пересекаются, —
  а шаги сопровождения наращивают, не снимая); полный символьный анализ
  достижимости дерева (отвергнуто: развилка условий рынком не
  разрешима, оценка сверху и есть ответ).
- **Почему:** проверка обязана ловить конфигурацию, не помещающуюся в
  лимит **никогда**; оценка сверху для этого достаточна и вычислима из
  структуры дерева.

### Д49. `stopCurrentLive` — наименее благоприятный уровень живых защит

- **Решение (цена C1, названа по требованию держателя):** живая позиция
  меряется до **одного** стопа — минимального для LONG / максимального
  для SHORT среди живых защит эпизода.
- **Альтернативы:** новейший уровень (отвергнуто: занижает живой риск —
  направление, запрещённое `absent-value-semantics`); поногово
  (отвергнуто: возвращает пожизненные накопители, ради ухода от которых
  формула и менялась).
- **Почему:** в переходном окне двойной защиты части позиции стоят под
  разными уровнями; завышение риска допустимо и самоустраняется,
  занижение — нет. Величина завышения ограничена разностью уровней ×
  размер и записана.

### Д50. Живой эпизод без защиты — консервативное слагаемое, без нового кода реджекта

- **Решение:** слагаемое берётся как риск живого эпизода при **плановых**
  стопах его ног (`orders.position_id` — колонка шага); нового
  `RiskCheckCode` не заводится.
- **Альтернативы:** новый код `LIVE_RISK_WITHOUT_PROTECTION`
  (отвергнуто: у состояния уже есть своя реакция — ступень 2 лестницы,
  третий enforcement дублировал бы её); читать пустоту как ноль
  (запрещено `absent-value-semantics`).
- **Почему:** прочтение то же, что было у пустого
  `protectionRelievedRiskAmount` («защиты нет ⇒ ничего не снято»), —
  регрессии нет, а инвариант enforced своим механизмом.

### Д51. `§«…»` адресует заголовок **или** лид-жирный пассаж

- **Решение (A9 + B9):** конвенция записана в
  `.claude/rules/structure.md` §Принципы; дефект — адрес, которому не
  соответствует ни заголовок, ни лид-жирный пассаж; сокращать имя
  пассажа нельзя.
- **Альтернативы:** переписать ~100 ссылок под заголовки (отвергнуто:
  правит следствие, а не причину, и ломает line-wrap); нарезать длинные
  доки заголовками под каждый адрес (отвергнуто: уровень заголовков
  растёт ради адресации).
- **Почему:** прогон, считавший разрешимость только по заголовкам, дал
  93 «битых» адреса, из которых реально битых ~20; незаписанная
  конвенция делает такой прогон генератором ложных находок. Битые
  починены поимённо.

### Д52. Вторая точка входа `SystemActionExecutor.reviseLiveExecutions`

- **Решение (A3 + A14):** ревизия живых SYSTEM-исполнений на проходе
  оркестратора и на терминальном ребре; переход
  `RETRY_PENDING → SKIPPED` допущен.
- **Альтернативы:** писателем назначить звено соседнего действия
  (отвергнуто самим доком); не закрывать вовсе (частичный ключ держит
  слот занятым); право отдать handler'у (переоткрывает канон о правах
  handler'а).
- **Почему:** прежний единственный вход `next(type, …)` работает **по
  названному типу**, а на обоих сценариях `SKIPPED` нужный тип уже не
  называет никто. Правило дополнено требованием достижимости писателя
  (`docs/rules/writer-named-for-every-value.md`).

### Д53. Выходная проверка `ManagingHandler` — конъюнкт `positionReopenAllowed` + дизъюнкт «живых ног нет»

- **Решение (A4):** «живой позиции нет» уводит в `EXIT_PENDING`, только
  если переоткрытие запрещено **либо** живых входных ног не осталось.
- **Альтернативы:** оставить безусловно и записать ограничение
  (отвергнуто: ветка `positionReopenAllowed = true` становится условно
  мёртвой, ратифицированное решение о многоэпизодной сделке
  отгружается погашенным); дискриминатор по `posId` вместо размера
  (переоткрывает принятое).
- **Почему:** делает решение исполнимым **независимо** от того,
  оставляет источник закрытую позицию в выдаче или убирает; второй
  дизъюнкт закрывает бессрочное удержание слота
  `uk_deal_active_instrument`.

### Д54. Область `Σ|amount|` и взвешивание омиссионного члена филлами

- **Решение (A5 + C11):** относительный член считается по тем же
  строкам, что сверяются четырьмя парами (расчётная валюта, вне списка
  исключений); омиссионный — по предикату **взятого** риска с
  весом `accumulatedFillSize_i / plannedSizeContracts_i`.
- **Альтернативы:** `Σ|amount × appliedRate|` по строкам с
  резолвленным курсом (отвергнуто: допуск занижается там, где число уже
  неполно, и становится путь-зависимым от догона); якорь на `|Σ net|`
  (переоткрывает реш.5).
- **Почему:** обе стороны теста обязаны быть однородны по покрытию;
  комиссионный поток порождают филлы, а не объявленные ноги, — иначе
  частота остановок контура становится функцией манеры ведения заявки.

### Д55. Холд по расхождению сверки — после коммита терминала

- **Решение (A7):** `HoldService.hold(...)` зовётся **после** коммита
  транзакции, применившей терминал, на обеих тропах.
- **Альтернативы:** та же транзакция (отвергнуто: отказ реакции ронял
  бы финализацию — нарушение двух записанных инвариантов); холд до
  коммита (обратное окно: контур остановлен по расхождению, которого в
  базе может не оказаться).
- **Почему:** цена — короткое окно «терминал применён, ступень 1 ещё не
  стои́т» — названа и самоустраняется идемпотентностью холда.

### Д56. Схемные объекты шага названы, выровнены и приведены к форме правила

- **Решение (B1-B5, B11, B13):** три частичных ключа получили имена
  (`uk_deal_strategy_action_state_target` / `…_action`,
  `uk_deal_system_action_state_action`); `uk_position_deal_external`
  стал частичным; в сборку внесено `−target`; `status` близнеца
  выровнен вверх до `varchar(64)`; существующий FK переименован вместе
  с таблицей; `deal_system_action_states` расписана поколоночно.
- **Альтернативы:** `UNIQUE NULLS NOT DISTINCT` вместо частичного
  (отвергнуто: требует фиксации минимальной версии PostgreSQL, которой
  в доках нет, и расходится с формой сестринских ключей); оставить
  `varchar(32)` и записать асимметрию (отвергнуто: цена выравнивания
  нулевая на пустых таблицах, а доводом симметрии тот же файл уже
  пользовался).
- **Почему:** сборка объявляет, что безымянных объектов в ней не
  остаётся; клейм проверяется **по объектам**, а не по строкам.

### Д57. §Персистентность `AttachedAlgoOrder` заведена, nullable получил довод

- **Решение (B3):** место истины схемы `attached_algo_orders` — в
  `docs/models/domain/core/Order.md`; вводимая `trigger_price_type`
  остаётся nullable, довод записан: строка материализуется и из
  биржевого эха, которое типа триггера не несёт.
- **Альтернативы:** `not null` сразу (отвергнуто: отказ вставки на
  подтверждающей тропе, где значение не наблюдается); оставить схему в
  сборке (правило прямо запрещает).
- **Почему:** обязательность поля держится на стороне постановки, а не
  на колонке; пробел у `algo_orders` — в backlog, шагом не
  затрагивается.

### Д58. Свип получил второй предмет — конструкцию; хвост закрытия свипается отдельно

- **Решение (процессная находка отчёта):** §3 процесса дополнена
  «свип идёт по конструкции, а не только по снимаемым терминам» и
  «хвост закрытия — такое же закрытие»; проход по клеймам полноты
  дополнен «счёт пересобирается из перечня».
- **Альтернативы:** четвёртый перечень носителей (отвергнуто явно —
  третий уже был отвергнут, а дефект не в полноте списка); усиление
  гейта (отвергнуто: гейт исполнялся, мимо прошёл предмет).
- **Почему:** двенадцать гейтящих находок прогона — следствия
  предыдущего закрытия, у которого свипы были прогнаны и записаны;
  значит менять надо предмет свипа, а не список носителей.

### Д59. Кейс `MG9.5` заведён под грунт базиса `last` ↔ `mark`

- **Решение (N1):** в план тестов добавлен содержательный кейс,
  ссылки на несуществующие §MG-базис и §C-фикстура приведены к
  именам кейсов плана.
- **Альтернативы:** снять назначение грунта (отвергнуто: величина
  запаса действительно наблюдение, а не вывод).
- **Почему:** грунт был назначен на носитель, которого нет, — сбор
  неисполним, находка образует петлю (§4 процесса).

### Д60. Гейт свипа получил якорь со стороны события

- **Решение (N3):** формулировка «скилл применяется по сигналу из чата»
  снята; скилл прогоняется как часть каждого под-шага (обязанность уже
  записана в процессе). Пункт пересмотра worst-case добавлен третьим
  носителем гейта закрытия фазы — с условием по данным, а не по фазе.
- **Альтернативы:** завести отдельный якорь в `backlog.md` по образцу
  гейта фазы (отвергнуто: обязанность уже существует в процессе,
  второй носитель той же политики — против `policy-home`).
- **Почему:** гейт, заведённый против незамечаемого пропуска, сам
  зависел от того, вспомнят ли его позвать.


## Итерация 2026-08-28 (4): закрытие `DOCS_CHECK_22` (`GAPS_CLOSE_22`)

Четыре развилки разрешил держатель (C5, C2, C4, B3 — приземление в
`.claude/work/history/2026-08-29-step-7-docs-check-loop/phase-1-step-7-gaps-close-22.md`); ниже — решения,
принятые CC автономно в ролях владельцев.

### Д61. Омиссионный член приведён к дому у обоих адресатов

- **Решение (C1):** формула в `FinalizeDealExitExecutor.md` получила
  множитель `accumulatedFillSize/plannedSizeContracts` и предикат
  **взятого** риска; ратификация конъюнкта состояния снята. В `Deal.md`
  и `Order.md` пересказ тождества помечен «это тождество риска, а не
  член допуска».
- **Альтернативы:** переписать дом под адресатов (отвергнуто — правка
  C11 `DOCS_CHECK_21` ратифицирована держателем); оставить два
  прочтения и назвать расхождение (отвергнуто — дом сам указывает на
  эти два дока как на дома формул).
- **Почему:** `policy-home` требует, чтобы указатели дома вели на
  действующую редакцию; писатель `CODE` пошёл бы по указателю и взял
  снятую.

### Д62. Изъятие маржи под сделку — названное ограничение, не контроль

- **Решение (C3, вариант (a)):** записана клауза «база движется под
  сделкой не только от просадки, но и от изъятия маржи», со счётным
  примером и с оговоркой, чего статическая проверка `N_overlap` **не**
  утверждает.
- **Альтернативы:** считать правую часть от базы **до** изъятия
  (отложено — новое чтение поля позиции в горячем пути, вопрос о марже
  при многоэпизодности); маржинальное неравенство на create (отложено —
  операнд `leverage` на момент create к инструменту не привязан).
- **Почему:** направление отказа **консервативное** (позиций меньше), а
  `absent-value-semantics` запрещает смещение только в разрешающую
  сторону; строить контроль под допустимое смещение — больший след без
  счётного довода.

### Д63. Носитель отображения категорий — существующий конфиг, не второй

- **Решение (B1, вариант (а)):** отображение `type`/`subType` →
  `CashFlowCategory` живёт в том же per-exchange
  `@ConfigurationProperties`, что список исключений; заведено
  предусловие `CODE` п. 16.
- **Альтернативы:** константы адаптера (правка требует релиза, прямо
  противоречит записанному «не хардкод»); рантайм-справочник с
  persisted-носителем (новая сущность, синк, свежесть, ветка отказа — и
  внутри себя всё равно требует отображения); сузить енум до
  `FUNDING`/`OTHER` (убивает три пары сверки из четырёх).
- **Почему:** обе таблицы — про значения источника, обе меняются по
  наблюдению и обе обязаны править́ся без релиза; второй носитель того
  же класса запрещает `design-simplicity`.

### Д64. Эпизоды разводит предикат пары, а не строка приоритета

- **Решение (B2, вариант (а)):** предикат пары
  `protectionRelievedRiskAmount` получил четвёртый конъюнкт
  `position_id = livePosition().id`; `incurredRiskAmount` остаётся
  пожизненным.
- **Альтернативы:** вычитать слагаемые закрытых эпизодов из
  уменьшаемого (вводит вторую, эпизодную редакцию того же числа);
  читать строку 4 буквально (обесточивает наблюдение на всей
  многоэпизодной популяции); вынести поле на строку `positions`
  (колонка на второй сущности).
- **Почему:** только этот вариант сохраняет и клейм точного нуля при
  недвинутом стопе, и наблюдаемость на многоэпизодной сделке, не
  переоткрывая семантику второго числа.

### Д65. Строка 4 приоритета осталась сделочной

- **Решение (B2, побочное):** клейм «строка 4 срабатывает на всей
  сделке сразу» **сохранён** — ложным был не он, а приписанный ей
  поногово-нулевой вклад ног закрытого эпизода.
- **Альтернативы:** объявить строку 4 поноговой (отвергнуто — тогда
  «поле пусто целиком» перестаёт быть её следствием, и ветка «защиты
  нет вовсе» теряет носителя).
- **Почему:** из двух конфликтующих утверждений правым объявлено то,
  которое согласовано с собственным следствием строки.

### Д66. Комиссионный член живого риска остаётся round-trip

- **Решение (C6, вариант (2)):** член не сужается до ноги выхода;
  завышение названо, направление консервативное, порядок ≈0.05R.
- **Альтернативы:** сузить до `rate × stopCurrentLive × …` (отвергнуто —
  разводит две формы одной величины: та же round-trip-форма стои́т в
  закрытом сайзинге).
- **Почему:** цена расщепления выше цены неточной формулировки, и
  формулировка чинится словом.

### Д67. Вырожденный знаменатель объявлен общим на четыре формулы

- **Решение (C7):** оговорка дома «слагаемое ноги с
  `plannedSizeContracts = 0` — ноль, не деление» распространена на все
  четыре формулы указателем, а не копией.
- **Альтернативы:** повторить оговорку у каждой (отвергнуто —
  `policy-home`: пересказ вне дома).
- **Почему:** асимметрия «у одной разобрано, у трёх нет» читалась как
  неразобранная ветка и воспроизвела бы находку.

### Д68. cross-ccy funding в числителе `R` — названное ограничение

- **Решение (C8):** записано, что операнд де-микширования покрывает
  только settle-ccy; расширение — на шаге ожидаемости.
- **Альтернативы:** расширить операнд сейчас (отвергнуто — шаг 7
  R-мультипликатор не считает вовсе).
- **Почему:** ограничение достижимо (cross-ccy — нарушение инварианта,
  не невозможность), поэтому обязано быть названо, а не подразумеваться.

### Д69. Закоммиченная миграция неизменна; её шапка — архив

- **Решение (N4, вариант (б)):** заведено правило в
  `persistence-representation.md`; устаревшая ссылка в шапке миграции не
  чинится, как не чинятся ссылки в `history/`.
- **Альтернативы:** разрешить переписывание до конца фазы 1 (отвергнуто
  — довод `pre-launch-schema-changes` про **строки**, а контрольная
  сумма ломается в чужих окружениях независимо от данных); не
  записывать и чинить по случаю (отвергнуто — класс воспроизводится у
  всех шапок `V1`…`V10`).
- **Почему:** конструкция «архив описывает прошлое, его ссылки не
  чинятся» в проекте уже действует — переиспользована, а не заведена
  заново.

### Д70. План хранит наблюдение, конфиг — действующее значение

- **Решение (N5, вариант (б)):** `AG6.1` перепрогоняется с записью
  **перечня** `type`/`subType`; рабочее отображение живёт в конфиге.
- **Альтернативы:** записать перечень в контракт-док источника
  (отвергнуто — прямо противоречит «вместо хардкода списка в доке», и
  перечень стареет вместе с биржей); оставить мощность (отвергнуто —
  грунт формально «добыт», содержания нет).
- **Почему:** то же разделение, что уже принято для списка исключений:
  датированный факт прогона и действующее значение — разные роли.

### Д71. Имя пассажа очищено от номера находки

- **Решение (N6):** лид-жирный пассаж переименован в «Асимметрия
  числителя и знаменателя — их две», пять адресов §H25 переписаны.
- **Альтернативы:** адресовать полным именем с номером (отвергнуто —
  имя неудобно и вшивает провенанс прогона в адрес, предмет `PROC-Q3`).
- **Почему:** адрес обязан быть выразим по имени пассажа; номер находки
  именем быть не может.

### Д72. Счёты пересобраны вместе с перечнями

- **Решение (N1, N2 и свип):** «восемь гейтящих предусловий» → «десять»
  вместе с перечнем п-номеров и атрибуцией кейсов; заголовок `AG1.9` —
  «пп. 1 и 15»; провенанс `MG9.5` приведён к телу кейса.
- **Альтернативы:** править счёт отдельно от перечня (отвергнуто —
  ровно тот дефект, который правило «перечень, а не счёт» и адресует).
- **Почему:** ложный клейм полноты есть инструкция «дальше не искать».

## Итерация 2026-08-28 (5): закрытие `DOCS_CHECK_23` (`GAPS_CLOSE_23`)

### Д73. Покрытие защиты — обеспечиваемый инвариант, а не допущение формул

Решение держателя (C1). `PARTIAL_STOP_LOSS` в роли основной защиты
допустим; совокупное покрытие живых защит обязано составлять 100 %
живой экспозиции. **Альтернативы:** (2) запретить частичную защиту
create-реджектом — режет скейл-аут лестницей, нужный грид-стратегиям
фазы 1; (3) записать ограничение — оставляет **разрешающее** смещение,
запрещённое `absent-value-semantics`. **Почему так:** инвариант делает
посылку формул истинной по обеспечению, поэтому множитель `externalSize`
в них правки не требует — минимальный след при полном снятии дефекта.

### Д74. Перебор C1 был неполон — выбран вариант, которого в нём не было

Держатель назвал «(3)», описав содержание, ему противоположное.
Исполнено по описанию. **Почему записано:** пропуск очевидной развязки
(«разрешить дробность, потребовать полноту») — дефект перебора, а не
ответа; следующий прогон не должен искать обоснование выбранного среди
четырёх ложных.

### Д75. Операнды покрытия разные по природе носителя

`AttachedAlgoOrder` размера не несёт (закрывает свою ногу целиком) ⇒
покрытие = `Order.accumulated_fill_size`; standalone ⇒ `AlgoOrder.size`.
**Альтернатива:** завести размер на attached — новая колонка ради
величины, уже выразимой. Найдено структурным свипом: первая редакция
предиката была для attached невычислима.

### Д76. Преконтроль требует монотонности покрытия, а не полноты

Полнота — только на гейтах троп, после исполнения пакета шага.
**Почему:** лестница ставится ступень за ступенью, и требование полноты
в преконтроле сделало бы её непостроимой (после первой ступени из
четырёх покрытие законно 25 %). Найдено свипом по конструкции.

### Д77. Код `PROTECTION_COVERAGE_REDUCED` — в карв-ауте, не авария

Реджект защитного действия, уменьшающего покрытие, — сработавший
контроль, а не рассогласование: старая защита остаётся живой
(place-new → cancel-old). **Альтернатива** (в `ERROR`) рвала бы живой
риск там, где он под контролем. Внесён в дом карв-аута — иначе
воспроизводился бы дефект C2.

### Д78. «Риск акта» определён по классу действия; состав неравенств при этом не сужается

Крен (1), подтверждён держателем (C3). На ослаблении защиты «риск акта»
= 0, `liveRiskNow` пост-действенный. **Альтернативы:** «риск акта = 0»
при пред-действенном `liveRiskNow` пропускало бы любое отодвигание стопа;
«риск акта = риск позиции при новом стопе» задваивало бы слагаемое и
блокировало штатный ремодел. **Названная цена:** две редакции одного
операнда.

**Счёт в прежнем заголовке («на weakening — три неравенства») снят
`GAPS_CLOSE_28`** (Г2 / П15): классом действия сужаются **значения
операндов**, а не состав проверок — действующая редакция в доме
`docs/rules/risk-validator-scope.md`, число здесь не воспроизводится.

### Д79. `1 %` — провизорная условность со смыслом «верхняя граница»

Решение держателя (C4). Фактический риск задаёт стратегия; глобальное
число — только потолок вложенности. **Почему записано:** величина
связывает сверху всю риск-конфигурацию, а немаркированное число
читается как выведенное. Пометка доведена до всех трёх утверждений
числа в файле.

### Д80. Триггер `NOT_ASSESSED` один, перечень пересобран по существу

Дизъюнкт «нижняя граница не резолвится» **недостижим** (суррогат непуст
у каждой строки `deals`: `public/time` отказал ⇒ сделка не создаётся);
пустой `externalCreatedAt` — нарушенный инвариант с уже назначенной
реакцией. **Альтернатива** (свести носители копированием) перенесла бы
мёртвую ветку в код.

### Д81. Область `Σ|amount|` — объединение областей четырёх пар

Конъюнкт «категория экономическая (не `OTHER`)» внесён явно; шестая
строка заведена в реестре областей. **Альтернатива** («все строки
settle-ccy») односторонне ослабляет допуск ровно там, где тип движения
не распознан, — то есть на всей популяции до наполнения отображения
категорий.

### Д82. Критерий исключения численной конвенции — по происхождению типа

Прежний («модель допускает непарсящееся значение») опровергнут у своего
единственного носителя: граница реджектит непарсящуюся ставку и строку
не пишет. Новый критерий — «доменный тип строковый по решению о сыром
хранении». **Альтернатива** (перевести ставки в `numeric`) переоткрывает
решение об оси группы.

### Д83. Сравнение значения группы `TradeFeeRate` — численное

Ставки — `compareTo == 0`, `level` — строкой. **Почему:** persisted-форма
не канонична (round-trip сериализации), и посимвольное сравнение делало
бы «историю» и `refreshCount` функцией нотации источника при **тихом**
отказе.

### Д84. Знаменатель `currentRiskAmount` получил ось эпизода

Четвёртый конъюнкт `position_id = livePosition().id` — тот же операнд,
что у пары риск-чисел. **Почему:** числитель эпизодный, знаменатель был
пожизненным; расхождение покрытий занижало долю в разрешающую сторону.
Риск-взвешивание остаётся приближением — это другая ось.

### Д85. Номер шага цикла — не адрес

Ссылаться на шаг оркестратора следует именем: вставка шага сдвигает
нумерацию у всех цитирующих, а грепом по «шаг N» их не найти. Записано
после **второго** случая.

### Д86. Клеймы полноты правятся вместе с перечнями

Шесть сумм реестра областей, четыре реджекта create, четыре кода
карв-аута — каждый счёт пересобран из собственного перечня и сверен
колонкой, а не по памяти правки.

## Итерация 2026-08-28 (6) — `GAPS_CLOSE_24`

Пять пунктов `подтверждение` разрешил держатель (остаток лестницы,
четвёртая точка в `MANAGING`, трейлинг как покрытие, два отклонённых
продуктовых сужения, довод отклонения TR1). Ниже — автономное.

### Д87. Операнд покрытия attached — `min(size, accumulatedFillSize)`

Не голый `size` и не голый `accumulated_fill_size`. **Почему:** каждая
величина по отдельности завышает покрытие на своей тропе — `size` при
частичном филле ноги, `accumulatedFillSize` при защите, поставленной на
часть ноги (её размер объявляет вызывающий через
`AttachedProtectionPayload.size`). Обе тропы разрешающие; `min` закрывает
обе, не вводя ни поля, ни запроса. **Отвергнуто:** инвариант
`attached.size = размер ноги` с точкой проверки — единственный вариант,
добавляющий механизм там, где хватает второго операнда.

### Д88. Посылка «`AttachedAlgoOrder` поля размера не несёт» была ложной

Поле есть в доменной структуре, в таблице персистентности, в миграции
`V6`, в снапшоте и в коде — и дом инварианта ссылался ровно на тот файл,
который его перечисляет. Ложной была и вторая привязка («доля
`closeFractionPercents` живёт на `AlgoOrder`»). **Следствие для метода:**
отрицательные посылки закрытия проверяются по пяти носителям, а не по
структурным носителям вводимого.

### Д89. Покрытие standalone — `size − coalesce(externalSize, 0)`

Частично сработавшая защита (`PARTIALLY_COMPLETED`) покрывает остаток, а
не весь заявленный размер. **Почему:** считать её покрывающей целиком —
завышение; выбрасывать целиком — занижение, дающее **ложную ступень 2** с
flatten на живой торговле, что дороже одного операнда.

### Д90. Введён `AlgoOrder.isActiveLike()` = {PENDING, ACTIVE, PARTIALLY_COMPLETED}

Дом предиката называл `isActiveLike` у `AlgoOrder`, а метода не было;
имевшийся `isLive()` шире на `CREATED`. **Почему набор именно такой:**
`CREATED` — заявка не отправлена, засчитывать её покрытием значит принять
ACK-семантику, отвергнутую проектом; `PARTIALLY_COMPLETED` — защита жива.
Набор **не совпадает** с одноимённым методом `AttachedAlgoOrder` (два
статуса) — у attached состояния «частично сработала» нет.

### Д91. Множество защит — закрытый перечень `conditionType`

`STOP_LOSS`, `PARTIAL_STOP_LOSS`, `OCO_FULL`, `TRAILING_PERCENTS`,
`TRAILING_VALUE`; тейки исключены. **Почему перечень, а не новое поле
роли:** различение уже выразимо существующим `conditionType`
(`design-simplicity`). **Почему в доме предиката, а не выводом из соседних
наборов:** соседние наборы (пять значений для резолва уровня, три для
`closeReason = TRIGGERED`) служат другим вопросам и на create-половину, где
операнд — действие с осью `actionType`, не переносятся.

### Д92. Предикат преконтроля — покрытие после завершения ремодела ≥ экспозиции

Синтез двух вариантов перебора, ни один из которых по отдельности не
годился: операнд «после завершения `REPLACE`» берётся из первого (резолв
замещаемой по `replacesInternalId`), база сравнения — из второго
(`externalSize`, не прошлое покрытие). **Почему не монотонность:** на
place-ноге она тождественно истинна (старая защита ещё жива), а при
прочтении «после ремодела» блокирует штатный класс «скейл-аут + пересчёт
защиты на меньший, но полный размер». **Почему перебор пришлось
синтезировать:** отчёт выписал варианты порознь, и цена первого («ложные
тихие реджекты») снимается ровно вторым — тот же дефект полноты перебора,
что зафиксирован Д74.

### Д93. `CANCEL_ALGO_ORDER_COMMAND` в scope валидатора не вводится

Снятие ступени лестницы без замены преконтроль не ловит и ловить не
будет. **Почему:** той же командой идут teardown выхода и kill-switch, и
валидируемость означала бы, что их можно отклонить блок-сетом холда.
Тропу ловит четвёртая точка (выходная проверка `MANAGING`). Дом предиката
прежде называл её среди ловимых преконтролем — ложно.

### Д94. Писатель `orders.position_id` — `RefreshPositionExecutor`

Не `RefreshOrderExecutor` «при первом филле»: на открывающем входе
локальной строки `Position` в этот момент ещё нет, а write-once оставлял
бы колонку пустой навсегда у ровно той ноги, ради которой она заводится —
то есть воспроизводился довод, которым был отвергнут
`CreateOrderExecutor`. **Названная цена:** нога, филлующаяся в двух
эпизодах, отходит первому наблюдению — уже принятое приближение.
**Отвергнуто:** двойной писатель (второй механизм там, где хватает
одного); вывод связи из окна времён (переоткрывает ратифицированное).

### Д95. `stopCurrent_i` и `stopCurrentLive` разведены именами

Правило «наименее благоприятный уровень на всю позицию» относится к
сделочному `stopCurrentLive`; поноговый `stopCurrent_i` берёт новейшую
живую защиту. **Почему разведение, а не выбор одного:** оба операнда
легитимны и живут каждый при своём числе; прежде оба селектора стояли в
одном пассаже и на живой лестнице давали разные значения. **Отвергнуто:**
распространить «наименее благоприятный» на `stopCurrent_i` — требует
адресации «защита этой ноги» у standalone, которой нет.

### Д96. `NOT_ASSESSED` у штатного финализатора недостижим

Производит значение только аварийный терминал: предикат эмиссии звена
штатного финализатора — терминальный **исход** добычи, а исчерпание
бюджета уводит сделку ошибочной тропой. Перечень производителей в реестре
кодов аномалий сужен до одного. **Читать** значение штатный финализатор
обязан — колонка одна на обе тропы.

### Д97. Перечень троп исчезновения защиты пересобран: три → пять

Добавились cancel-old ноги входа при `REPLACE` и снятие ступени лестницы
стратегией. **Почему это находка свипа, а не линзы:** клейм лежал в
`CancelAlgoOrderExecutor`, правки его текстом не касались — предмет
изменили правки в других файлах.

### Д98. Отсрочка уровней 2-3 и отклонение per-instrument guard'ов — разные оси

«Агрегатной экспозиции нет, торгуется один инструмент» остаётся верным
доводом **для уровней 2-3** и не переносится на per-instrument контроли:
их предмет — отношение моего размера к ёмкости рынка, и «инструмент один»
его не уменьшает. Развод записан в трёх носителях, включая
`trading-constraints.md`, который свип нашёл сам — линзами он назван не был.

---

## Итерация 2026-08-29 — правка пайплайна: детектор стагнации итераций

Задача держателя: завести в машину прогонов проверку на топтание на
месте. Форма носителя делегирована CC.

### Д99. Носитель детектора — скилл плюс гейт в простановке статуса

- **Решение:** процедура, пороги, состав эскалации — один новый файл
  `.claude/skills/stagnation-detection.md`; исполнение — гейтом в
  `.claude/skills/update-roadmap-progress.md` §«Гейт входа в
  `GAPS_CLOSE_N`»; в `roadmap-step-execution.md` §4 — указатель.
- **Альтернативы:** правило в `.claude/rules/` (отвергнуто: предмет —
  процедура «как проверить», а не формулировка политики; тип знания
  определяется вопросом); раздел в процессе (отвергнуто: процесс уже
  несёт восемь перечней свипов, девятый там не читается, а скилл
  самодостаточен по `structure.md`); отдельный агент-наблюдатель
  (отвергнуто: замер механический, роли не требует).
- **Почему:** гейты этого проекта уже живут в скилле, который
  **исполняется** при смене статуса — та же конструкция встречного
  якоря, что у гейта свипов и гейта закрытия фазы. Проверка, живущая
  только в своём файле, читается тем, кто её и так помнит.

### Д100. Пороги — по фактике шага 7, а не по умозрению

- **Решение:** С1 (рецидив узла) — два прогона подряд при закрытии
  между ними; С2 (бумеранг) — ≥ ½ гейтящих на двух прогонах подряд;
  С3 (стоимость) — окно шесть прогонов, среднее гейтящих второй тройки
  ≥ 70 % первой; С4 (перебор класса Д74) — два закрытия подряд.
- **Альтернативы:** три прогона у С1 (отвергнуто: третье закрытие узла
  и есть то, что детектор обязан предотвратить — на узле покрытия он
  сработал бы уже после факта); абсолютные пороги по числу находок
  (отвергнуто: ряд `_1`…`_25` колеблется 8-18 гейтящих без тренда,
  абсолютный порог не отличает шум от серии).
- **Почему:** пороги проверены на ряду — детектор срабатывает в
  известных точках топтания (`_4`, `_8`, `_16`, `_20`, `_22`, `_25`),
  включая узел покрытия защиты, латавшийся тремя закрытиями подряд.
  Калибровочная таблица — в скилле, величины — в реестре хроники.

### Д101. Реестр прогонов — секция хроники шага, не новый файл

- **Решение:** величины замера (находок / гейтящих / узлы / доля
  следствий закрытия / объём / дефект перебора) ведутся таблицей в
  `phase-<N>-step-<M>-chronicle.md`; ретроспектива `_1`…`_25` собрана
  по хронике и отчётам, ненайденное помечено `н/д`.
- **Альтернативы:** отдельный файл-леджер (отвергнуто: второй носитель
  того же предмета — против `design-simplicity`; хроника и так едет в
  `history/` вместе с шагом); считать сигналы чтением хроники каждый
  раз (отвергнуто: три тысячи строк прозы на каждый замер, и величины
  в ней записаны неоднородно).
- **Почему:** реестр — не пересказ: хроника несёт разбор, реестр несёт
  числа. `н/д` вместо восстановленной по памяти величины —
  сознательно: детектор считает по записанному.

### Д102. Срабатывание одноразово на цикл, со сбросом после выбора

- **Решение:** сигнал, поднявший эскалацию, снимается выбором
  держателя (любым); счётчики окон обнуляются, тот же сигнал не
  поднимается, пока не наберётся новое окно. Повторная эскалация по
  тому же сигналу обязана назвать, что изменилось после предыдущей.
- **Альтернативы:** ослабить порог С3 так, чтобы он срабатывал реже
  (отвергнуто: на ряду шага 7 падения на треть не показывает ни одно
  окно с `_9` — ослабление порога записало бы несхождение как норму).
- **Почему:** без сброса С3 звенел бы на каждом прогоне и перестал бы
  что-либо значить; со сбросом он остаётся честным и при этом
  используемым.

---

## Итерация 2026-08-29: переписывание корпуса по концепции

Развилки, пройденные автономно при исполнении задач 3 и 4. Держателю
выносится только то, что названо в §«Что уходит держателю» дизайн-прохода
(`.claude/work/progress/risk-contour-design.md`).

### Д93. Тип знания «решение» (`docs/decisions/`) упразднён

- **Решение:** каталог снят целиком (29 файлов, 330 KB). Исход решения
  переехал в дом соответствующего правила или модели; довод, ставший
  принципом, — в концепцию; остальное удалено, история в git. Четыре
  файла повышены до правил и процесса: `replace-not-amend.md`,
  `strategy-condition-contract.md`, `strategy-validation.md`,
  `docs/processes/fsm-execution-layering.md`.
- **Альтернатива:** оставить тип для решений с внешним адресатом.
- **Почему так:** концепция объявляет, что довод живёт в ней, а исход —
  в доме; у отдельного файла-решения после этого не остаётся
  собственного вопроса. Держать тип ради формы значило бы держать второй
  носитель уже размещённого знания.

### Д94. Форма исполнимой спецификации — JSON плюс раннер в тестах

- **Решение:** `docs/spec/*.json` — именованные величины (скалярное
  выражение либо агрегат по коллекции) плюс набор примеров
  «состояние → ожидаемое». Раннер — `SpecRunnerTest` и автономный
  `tools/spec-run.sh`; язык выражений минимальный (арифметика,
  сравнения, булева логика, доступ по точке, восемь функций).
- **Альтернативы:** YAML (новая зависимость); код домена как дом формулы
  (кода ещё нет, а спецификация нужна до него); внешний DSL (цена
  парсера выше выигрыша).
- **Почему так:** формула получает один экземпляр и проверяется
  прогоном, а не вычиткой; JSON не требует новых зависимостей, а
  выражения остаются читаемыми человеком.
- **Что уже поймано:** первый прогон нашёл рассогласование чисел в
  примерах чисел риска — ровно тот класс, который прежде находили линзы.

### Д95. `risk-creating-entry-protection.md` → `live-risk-protection.md`

- **Решение:** правило переименовано; предмет расширен с «вход» на
  «живой риск в любой момент».
- **Альтернатива:** сохранить имя ради входящих ссылок (58 штук).
- **Почему так:** правило после дизайн-прохода держит три обязательства,
  из которых только первое про вход; имя вводило в заблуждение. Ссылки
  переписаны одной правкой.

### Д96. §-адресация снята по всему корпусу

- **Решение:** внутрифайловые адреса (`§«…»`, §Слово) удалены везде;
  ссылка стала смысловой, на файл.
- **Альтернатива:** оставить адреса в «стабильных» доках.
- **Почему так:** требование концепции; плюс адреса ломались от любой
  правки текста и давали ложную работу. На переписанном корпусе имена
  секций сменились почти везде, так что альтернатива означала бы
  массовую починку адресов вместо их снятия.

### Д97. Правило места бизнес-логики переехало в конвенции кода

- **Решение:** `.claude/rules/codestyle.md` снят,
  содержание — в `.claude/rules/codestyle.md`.
- **Почему так:** это конвенция написания кода, а не правило продукта;
  концепция относит выбор конвенций к `.claude/`.

### Д98. Скаффолд-README слоёв моделей и манифест покрытия сняты

- **Решение:** три `README.md` слоёв (`api`, `persistence`,
  `externalSnapshot`) удалены — они отвечали на вопрос размещения, чей
  дом `.claude/rules/structure.md`. `.claude/processes/api-docs-completion.md`
  и `docs/models/mapping/Order.md` сняты: первый — реестр полноты
  работы интегратора (артефакт пайплайна), второй — описание того, что
  решено не вводить.
- **Почему так:** «один факт — один дом» и запрет на носитель
  отсутствующего факта.

### Д99. Порядок исполнения пакета шага — по риск-классу

- **Решение:** диспетчер выбирает следующим действие по риск-классу
  (устанавливающие защиту раньше снимающих), а не по порядку вставки.
- **Альтернативы:** ключ порядка на поле уровня грид-сетки (совмещение
  двух природ); реджект таких пакетов на create (ломает эталонную
  стратегию).
- **Почему так:** резолв риск-класса петля выполняет и так; поле уровня
  сохраняет свой смысл; эталонная стратегия остаётся валидной.

### Д100. Снятие защиты введено в scope преконтроля

- **Решение:** scope режется по предмету команды, а не по её имени:
  teardown выхода и kill-switch не валидируются, снятие защитной заявки
  при живой экспозиции — валидируется веткой risk-weakening.
- **Альтернатива:** оставить весь класс отмены вне scope.
- **Почему так:** снятие защиты риск не снимает, а увеличивает; прежняя
  классификация была неверна, и именно она делала тропу «снять и
  поставить» неловимой.

---

## Итерация 2026-08-29 (второй проход): хвосты переписывания

### Д101. Язык спецификации расширен тремя конструкциями

- **Решение:** добавлены (а) подгрузка артефакта репозитория в состояние
  примера, (б) обход вложенных коллекций с флэттенингом по спискам и
  словарям, (в) необязательный идентификатор — отсутствующее поле даёт
  пустоту вместо отказа. Плюс функция округления вниз по шагу.
- **Альтернатива:** держать примеры только синтетическими.
- **Почему так:** без подгрузки артефакта эталонная стратегия остаётся
  непроверяемой машинно, а именно её непрогон дал самый тяжёлый класс
  находок. Необязательный идентификатор нужен там, где предмет проверки —
  **наличие** поля.

### Д102. Эталонная стратегия приведена к действующей модели

- **Решение:** в `trend-following-ema.json` переименован поактный потолок,
  добавлены три обязательных поля торгуемой детали, типы действий
  приведены к словарю с суффиксом уровня.
- **Почему так:** машинный прогон нашёл шесть расхождений артефакта с
  моделью. Концепция требует править эталон тем же ходом, а не оставлять
  расхождение спеки и входа.
- **Значения:** потолки заданы так, чтобы выполнялась вложенность
  (поактный = максимум стратегии = глобальный = 1 %), множитель
  кумулятивного — 2, переоткрытие запрещено. Это параметры **примера**, а
  не системные величины риск-аппетита.

### Д103. Ложное «сошлось» на вложенных путях исправлено

- **Решение:** комбинированные суффиксы пути разбираются посегментно.
- **Почему записано:** первая редакция обхода молча возвращала пустую
  коллекцию, и проверки над ней проходили вырожденно. Класс дефекта — тот
  же, что правило запрещает в доменных доках: благоприятный исход при
  недобытом операнде. В спецификации он опаснее, потому что выглядит
  зелёным прогоном.

### Д104. Составные вопросы переформулированы, а не разделены на файлы

- **Решение:** 120 файлов получили один вопрос; перечни осей ответа («что
  делает, границы, енумы, лимиты») из формулировки вопроса убраны.
- **Альтернатива:** делить файлы по числу склеенных вопросов.
- **Почему так:** в подавляющем большинстве случаев склейка была не двумя
  предметами, а вопросом плюс оглавлением ответа. Деления потребовал бы
  лишь случай двух настоящих предметов в одном файле, и такого не
  нашлось.

### Д105. Форма компонент-дока зафиксирована

- **Решение:** компонент-док состоит из «что делает», контракта (где он
  есть), «что читает и пишет», специфики и границ; политику не
  пересказывает, а ссылается на дом.
- **Почему так:** пересказ политики в компонент-доках был главным
  источником расхождений — одна политика жила в пяти-десяти носителях.

### Д106. Матрицы переходов заявок переехали в исполнимую спецификацию

- **Решение:** матрицы `Order`, `AttachedAlgoOrder`, `AlgoOrder`, таблицы
  резолва внешнего статуса и предикат свежести живут в `docs/spec/`; доки
  жизненного цикла несут смысл состояний и ссылку, форму не повторяют.
- **Альтернатива:** оставить матрицы в доках, а в спеке держать только
  числовые формулы.
- **Почему так:** матрица — закрытый перечень, то есть ровно тот класс
  знания, который концепция требует держать в исполнимой форме. У `Order`
  матрицы не было вовсе — она была выведена и сразу оформлена как
  спецификация, а не как ещё одна текстовая таблица.

### Д107. Пошаговый прогон сделки утверждает отсутствие реакции, а не только числа

- **Решение:** прогон эталонной сделки — двенадцать состояний, и на
  каждом утверждается `invariantViolated = false`, а не только величина
  покрытия.
- **Почему так:** проверять числа недостаточно: конструкция обещает, что
  **штатный проход не даёт биржевой ступени 2 ни разу**. Это утверждение
  и должно проверяться машинно, иначе обещание держится на вычитке.
- **Что прогон закрепил:** шаг «трейлинг поставлен, но не активирован» —
  снятие безубытка откладывается, а не отказывает; неактивированный
  трейлинг покрытия не несёт.

### Д108. `includes` в раннере: величина переиспользуется, а не копируется

- **Решение:** спецификация подключает величины соседней по имени;
  одноимённая величина из двух источников — отказ прогона.
- **Альтернатива:** копировать формулу в каждую спецификацию, которой она
  нужна.
- **Почему так:** копия формулы — тот же дефект «двух домов», от которого
  избавлялся корпус. Отказ на дубле имени делает нарушение невозможным
  молча.

### Д109. §-адреса из рабочих файлов в переписанные доки сведены к указателю на файл

- **Решение:** 54 повисших §-адреса (секции переименованы переписыванием)
  заменены ссылкой на файл-дом без адреса секции.
- **Альтернатива:** перенацелить каждый адрес на новое имя секции.
- **Почему так:** доки теперь втрое меньше и несут по одному вопросу —
  файла достаточно, чтобы найти пассаж. Перенацеливание воспроизвело бы
  ту же хрупкость при следующей правке. Внутри `docs/` §-адресов нет
  вовсе — этого требует концепция.

---

## Итерация 2026-08-29: приземление катастрофического потолка сделки

Схема ратифицирована держателем; ниже — развилки, которые CC решил сам
при её приземлении. Итог — `.claude/work/history/2026-08-29-catastrophic-deal-ceiling.md`.

### Д110. «Максимальный риск на сделку» — это уже существующее число конфигурации

- **Решение:** первым сомножителем катастрофического потолка взят
  `globalSimultaneousRiskPerDealPercent`; третьего числа риск-аппетита
  конфигурация не получает — их ровно два, как и ратифицировано.
- **Альтернатива:** завести отдельное `maxRiskPerDealPercent` рядом.
- **Почему так:** существующее число отвечает ровно на этот вопрос —
  сколько риска система допускает на сделку. Отдельное дало бы две
  величины об одном, расходящиеся при первой же правке
  (`.claude/rules/design-simplicity.md`).

### Д111. Имена разведены по установленной паре «strategy… / global…»

- **Решение:** `StrategyDetail.strategyCatastrophicRiskPerDealMultiplier`
  и конфигурационный `globalCatastrophicRiskPerDealMultiplier`.
- **Альтернатива:** безмаркерное имя у поля стратегии
  (`catastrophicRiskMultiplier`) и `…Max` у конфигурационного.
- **Почему так:** пара уже есть в корпусе (`strategySimultaneous…` /
  `globalSimultaneous…`), и в ней «global» и означает системный максимум.
  Свой способ разведения на соседнем уровне читался бы как другой смысл
  (`.claude/rules/naming.md` §«Разведение уровней абстракции»).

### Д112. Реджект рантайма остался прежним кодом, create-реджект — новый

- **Решение:** нарушение кэпа по-прежнему `DEAL_NOTIONAL_EXCEEDED`;
  на создании — новый `STRATEGY_CATASTROPHIC_MULTIPLIER_ABOVE_GLOBAL`.
- **Альтернатива:** переименовать рантайм-код под новое имя потолка.
- **Почему так:** меряется по-прежнему номинал, и код называет
  нарушенное, а не способ вычисления правой части. Create-реджект
  отделён потому, что «стратегия просит больше конфигурации» и «сделка
  переросла потолок» — разные события с разными адресатами.

### Д113. Незаданное конфигурационное число отказывает адресно

- **Решение:** незаданный максимальный риск на сделку отвергает
  risk-creating действие; незаданный максимальный множитель отвергает
  **создание стратегии**.
- **Альтернатива:** оба гнать в рантайм-отказ действия.
- **Почему так:** второе число участвует только в create-сверке. Отказ в
  рантайме означал бы, что стратегия с непроверенным множителем уже
  заведена, и место ошибки конфигурации — не создание
  (`docs/rules/strategy-validation.md` §«Линия реза»).

### Д114. `DEAL_NOTIONAL_EXCEEDED` включён в карв-аут исчерпанного бюджета

- **Решение:** реджект катастрофического потолка не уводит сделку в
  `ERROR` — рядом с тремя сделочными кодами
  (`docs/processes/risk-evaluation.md`).
- **Альтернатива:** оставить перечень карв-аута прежним (три кода).
- **Почему так:** дом уже говорит «`BLOCKED` по потолкам — не авария», и
  довод глобального кода верен здесь дословно: потолок мог ужесточить
  держатель после приёма стратегии, а маршрут в `ERROR` снимал бы живой
  риск позиции, который под контролем, и загрязнял бы выборку `R`.

---

## Итерация 2026-08-29: дизайн-проход агрегатной сделки

Направление ратифицировано держателем; ниже — развилки конструкции,
решённые CC внутри направления. Проход —
`.claude/work/history/2026-08-29-tranche-landing/aggregate-deal-design.md`.

### Д115. Имя уровня — «транш», не «нога» и не «деталь»

- **Решение:** `StrategyTranche` (объявление) / `DealTranche`
  (материализация).
- **Альтернативы:** `DealLeg` («нога» — слово держателя); `DealDetail`
  (его рабочее имя).
- **Почему так:** «нога» в корпусе уже несёт два смысла — заявка и
  половина двуногой команды; третий стоил бы 214 вхождений в 52 файлах.
  «Деталь» на стороне стратегии означает правила фазы, и пара
  `Strategy…Detail` / `Deal…Detail` называла бы несоответствующие вещи.
  Развилка дешева до приземления и вынесена держателю на подтверждение.

### Д116. Ошибочное состояние — уровня агрегата; у транша его нет

- **Решение:** у транша семь статусов (сегодняшние минус `ERROR` и
  `EMERGENCY_CLOSED`); неустранимый отказ транша поднимается в `ERROR`
  агрегата и останавливает набор риска по всей сделке.
- **Альтернатива:** держать ошибочный статус на транше и продолжать
  торговать остальными.
- **Почему так:** транши делят одну нетто-позицию и одну арифметику
  экспозиции. Сломался учёт одного — радиус недоверия равен сделке; иначе
  это «пережить ошибку и продолжить», чего П1 не допускает.

### Д117. Эпизод остаётся биржевым фактом агрегата; экспозиция транша — производная

- **Решение:** строки `Position` висят на агрегате; экспозиция транша
  считается как филл его входных заявок минус закрытое его же
  инструментами; сумма по траншам сверяется с нетто-размером.
- **Альтернатива:** дать траншу собственные строки эпизодов.
- **Почему так:** в нетто-режиме биржевого эпизода на транш не
  существует — `posId` один на инструмент. Производная не зависит от
  порядка событий, а сверка с биржевым фактом даёт детектор
  неатрибутируемого риска, которого сегодня нет вовсе.

### Д118. Траншевый инвариант покрытия принят, хотя он строже агрегатного

- **Решение:** «каждый транш закрыт своим стопом» вместо «сумма покрытий
  не ниже нетто-экспозиции».
- **Альтернатива:** оставить агрегатную достаточность (экономически её
  хватает: на нетто-позиции любой стоп на весь размер покрывает всё).
- **Почему так:** ужесточение идёт в консервативную сторону (П1 §3) и
  покупает атрибуцию: без неё worst-case транша неизвестен, а экспозиция
  невыводима. Выразительность не теряется — «один стоп на сетку»
  выражается N стопами по одной цене.

### Д119. Транши материализуются эагерно; грид — шаблон с лимитным входом уровня

- **Решение:** все объявленные транши заводятся при создании сделки, каждый
  в своём `PRECHECK`; шаблон несёт `levelCount` и `levelStep`; вход уровня
  — лимитная заявка на цене уровня, а не условие на цену.
- **Альтернативы:** лениво заводить транш при срабатывании уровня;
  выражать уровень условием.
- **Почему так:** «уровень объявлен и ждёт» обязано быть видно в данных
  (П3), а лимитная заявка отдаёт триггер бирже и тем самым делает защиту
  уровня встроенной — инвариант покрытия обеспечивается уже стоящим
  энфорсером, а не новым.

### Д120. Агрегатная поверхность шагов остаётся, но сужена до выхода

- **Решение:** `StrategyDetail.stepsByStatus` сохраняется только для
  `EXIT` и `FAIL_SAFE`; всё остальное объявляется на транше.
- **Альтернатива:** убрать агрегатную поверхность целиком, размножив выход
  по всем траншам.
- **Почему так:** выход всей сделки — утверждение обо всех траншах сразу;
  размноженный по N объявлениям он был бы совпадением N деклараций, а не
  обеспеченным фактом (П1 §2).

---

## Итерация 2026-08-29: приземление траншей

Имя ратифицировано держателем (`Tranche`), приземление выполнено по §E
прохода. Ниже — развилки, вскрывшиеся при внесении. Итог —
`.claude/work/history/2026-08-29-tranche-landing.md`.

### Д121. `entryReason` остался на сделке, вниз уехал только `entryStepType`

- **Решение:** причина заведения сделки — поле агрегата; тип шага входа —
  поле транша.
- **Альтернатива:** перенести оба на транш, как планировал §C4 прохода.
- **Почему так:** у восстановленной сделки (живой риск, заведённый вне
  приложения) объявленных траншей нет, и её транш синтетический —
  `RECOVERY` тогда некуда записать. Причина заведения **сделки** и тип
  шага входа **транша** отвечают на разные вопросы.

### Д122. Обработчики разведены префиксом уровня, а не суффиксом

- **Решение:** `TranchePrecheckHandler` … `TrancheExitPendingHandler`
  против `DealActiveHandler` / `DealExitPendingHandler`; `ErrorHandler`
  остался без префикса — он один и только у сделки.
- **Альтернатива:** оставить прежние имена у траншевых обработчиков,
  различая уровни по контексту.
- **Почему так:** `EXIT_PENDING` существует на обоих уровнях, и
  безымянный `ExitPendingHandler` стал бы двусмысленным ровно там, где
  цена ошибки максимальна — на выходе.

### Д123. Аварийный терминал терминальность траншей не гейтит

- **Решение:** штатный терминал требует `allTranchesTerminal`, аварийный
  — только доказанного отсутствия живого риска.
- **Альтернатива:** требовать терминальности траншей на обеих тропах.
- **Почему так:** строка транша стала бы второй точкой отказа аварийного
  контура. Аварийная тропа отвечает за снятие риска, а не за аккуратность
  строк; аккуратность добирается разбором.

### Д124. Закрытый объём встроенной защиты читается её размером

- **Решение:** в экспозиции транша сработавшая встроенная защита
  вычитается своим размером при терминальном срабатывании; отдельного
  эхо-поля «сколько закрыто» не вводится.
- **Альтернатива:** завести поле эха закрытого объёма встроенной защиты.
- **Почему так:** траншевый инвариант требует, чтобы встроенная покрывала
  филл своей заявки целиком, поэтому её размер и есть закрытый объём.
  Ошибка подстановки (частичное исполнение защиты) ловится сверкой Σ с
  нетто-размером и уводит в консервативную сторону — останов, а не проход.

## Итерация 2026-08-29: закрытие `DOCS_CHECK_26` (`GAPS_CLOSE_26`)

Решения приняты автономно владельцами по карте; держательские половины
пришли отдельным заходом и здесь не дублируются (они в отчёте закрытия
`.claude/work/progress/phase-1-step-7-gaps-close-26.md` §«Решения
держателя»).

- **Д125. Форма `resultProfit` заведена отдельной спецификацией**
  (`docs/spec/deal-result.json`), а не разделом в `pnl-reconciliation`.
  Альтернатива — доложить величину в существующую спеку сверки.
  Почему так: у них разные вопросы («чему равен итог» против «сошлась ли
  сверка»), и склейка нарушила бы правило одного вопроса на файл.

- **Д126. `Deal.md` переписан на указатель, форму не держит.**
  Альтернатива — оставить формулу прозой рядом со спекой. Почему так:
  `.claude/rules/structure.md` называет домом формы `docs/spec/`, а две
  редакции одной формулы расходятся первыми.

- **Д127. Конвенция знака пар сверки — сырая, финансирование
  де-нормализуется в правом операнде.** Альтернатива — нормализовать обе
  стороны. Почему так: у комиссии и штрафа знак сырой с обеих сторон, и
  смешение конвенций дало бы расхождение на всей популяции; выведено из
  `PositionCloseResult.md`, не изобретено.

- **Д128. Писатель курса чужой валюты — `RefreshBillsExecutor`**, той же
  транзакцией, что и строка движения. Альтернатива — отдельный компонент
  пересчёта. Почему так: корпус уже неявно указывал на этого исполнителя
  (`CandleGroup.md` называет его добытчиком свечи котировки), и новый
  компонент был бы бо́льшим следом без счётной разницы.

- **Д129. Резолв уровня защиты заведён в `protection-coverage.json`**, а
  не в новой спеке и не переадресован. Почему так: обе спеки уже
  ссылались туда — правка делает их указатели истинными вместо того,
  чтобы менять указатели в двух местах.

- **Д130. `Deal.md` и `risk-policy.md` не противоречили друг другу** —
  они определяли **две разные** величины (уровень по заявке против
  уровня на всю позицию). Обе сохранены и разведены именами
  (`trancheStopCurrent` / `stopCurrentLive`, `legSignedStopDistance` /
  `positionSignedStopDistance`). Альтернатива — объявить одну редакцию
  правой: отвергнута, она потеряла бы операнд одного из потребителей.

- **Д131. Ключ системных исполнений расширен траншем только для
  потраншевого типа** (`FINALIZE_DEAL_ENTRY_ACTION`). Альтернатива —
  включить транш во все системные ключи. Почему так: у трёх остальных
  типов транша нет по существу, и пустая колонка в ключе вернула бы
  неоднозначность, ради снятия которой таблицы и разведены.

- **Д132. У `Order.positionId` победила редакция `Order.md`**
  (`RefreshPositionExecutor` в транзакции материализации эпизода).
  Альтернатива — рефреш заявки при первом филле. Почему так: у первой
  записан довод, которого у второй нет, — до материализации строки
  позиции локального эпизода не существует, и указывать не на что.

- **Д133. Противоречие по нижней границе окна снято различением, а не
  выбором стороны:** писатель колонки один, операнд чтения двухместный.
  Альтернатива — снять суррогат. Почему так: без него сделка,
  заведённая восстановлением, осталась бы без окна вовсе — то есть без
  разбивки движений.

- **Д134. Реестр предусловий `CODE` перенесён в контур тестов**
  (`.claude/tests/source-api/okx/code-preconditions.md`), а не
  восстановлен в `docs/rules/`. Почему так: он отвечает на вопрос
  проверочной деятельности, а не продуктовый; прежнее размещение было
  ошибкой, и переписывание снесло его именно как процессную арматуру.

- **Д135. Остановка торговли выражена существующим мягким холдом биржи**
  (`Exchange.HOLD`), нового механизма не заведено. Альтернативы —
  отдельный статус остановки; сворачивание `TRADE_BLOCKED`. Почему так:
  требуемое поведение совпадает с мягкой ступенью дословно, а
  сворачивание рвало бы живую позицию, чего серия убытков не требует.

- **Д136. Счётчик серии не двигают ни ноль, ни недоступный результат.**
  Альтернатива — считать их «не убытком» и обнулять. Почему так:
  обнуление — благоприятный исход, и отдавать его безубыточной либо
  неизмеримой сделке значит дать благоприятное умолчание (П1 §4).

- **Д137. База риска — хранимая величина счёта (`Exchange.riskBase`), её
  начальное значение есть наблюдение, а не число конфигурации.**
  Альтернатива — третье число риск-аппетита у держателя. Почему так:
  держатель назначает **процент** от базы, а не саму базу; свободный
  остаток при первом наблюдении — состояние счёта, назначать его нечем.

- **Д138. Реакция на расхождение Σ экспозиций — биржевая ступень 2 в обе
  стороны.** Альтернатива — инструментный радиус. Почему так:
  неатрибутированная экспозиция по построению не покрыта (покрытие
  считается потраншево), а живой риск без покрытия — записанный триггер
  ступени 2.

- **Д139. 27 битых ссылок из `src/` на снятый `docs/decisions/` не
  правились в этом закрытии**, а заведены в CODE-дельту. Почему так:
  правка 44 файлов Java — работа слоя `CODE`; переписывать javadoc
  вслепую значило бы посадить 27 новых указателей без проверки, что
  цель верна.

- **Д140. Снапшот перевыпущен (v93), а не пропатчен.** Альтернатива —
  поправить устаревшие строки v92. Почему так: v92 отстал по существу
  (число конфигурационных величин, счёт спецификаций, клейм о чистоте
  корпуса), и точечная правка сделала бы его внутренне несогласованным.

---

## Итерация 2026-08-29: закрытие узла Н7 `GAPS_CLOSE_27`

- **Д141. Полных выхода два, и они разведены значением перечня
  (`FULL_BY_FRACTION` против `FULL`).** `FULL_BY_FRACTION` — объявленная
  доля забрала экспозицию целиком, округления не было, журнальный отчёт не
  заводится; `FULL` — «частично» на этом размере невыразимо, заводится
  `PARTIAL_EXIT_ROUNDED_TO_FULL`. Альтернативы: (A) снять из
  `SizeCalculator.md` конъюнкт «< живой экспозиции» и оставить `PARTIAL` —
  отвергнуто, в перечне остался бы ложный ярлык на достижимой конфигурации
  (доля ровно 100 % валидацию проходит); (B) одно значение `FULL` на оба
  исхода — отвергнуто, читатель обязан перевыводить `exitSize >= minSize`,
  чтобы решить, заводить ли отчёт об округлении, и величина перестаёт быть
  достаточной для единственного решения, ради которого названа. Почему так:
  цена C измерена — +1 значение в перечне **RVO** (`CalculatedSize` не
  персистится): 0 колонок схемы, 0 значений в БД, 0 внешних контрактов.
  Уточняет Д36 в части счёта.

- **Д142. Дом закрытой формы сайзинга — `docs/spec/order-sizing.json`, и
  переиспользование идёт двумя тропами.** Зависимость по **значению** —
  операнд с указателем на дом-спеку (уже конвенция корпуса:
  `risk-limits.stopCurrentLive`, `order-sizing.ladder.trancheExposure`);
  зависимость по **форме**, вычисляемой на своих строках, — `includes` из
  единственного дома. Альтернативы: развести имена руками (отвергнуто
  счётно — это действующий сегодня механизм, под ним прожили необнаруженными
  5 групп дублей); отдельная спека-файл «замкнутая форма сайзинга»
  (отвергнуто: больший след без выигрыша, дом уже есть). Почему так:
  правило раннера «величина объявлена дважды» охраняет только рёбра
  `includes`, поэтому форма обязана иметь ровно один дом, а не согласованные
  копии.

- **Д143. Носитель исхода округления — `CalculatedSize.exitOutcome`
  (перечень `ExitOutcome`), читатель — per-type `StrategyActionExecutor`
  действия выхода.** Альтернатива — оставить исход вычислимым только в
  спеке. Почему так: до правки у четырёх исходов не было ни одного
  рантайм-носителя, и решение «заводить ли журнальный отчёт / не отправлять
  действие» не имело операнда.

## Итерация 2026-08-29: закрытие `DOCS_CHECK_27` (`GAPS_CLOSE_27`, узлы Н1-Н6, Н8, Н9)

Решения, принятые CC автономно при закрытии восьми узлов. Держательская
половина (пять ответов) — в отчёте закрытия §«Решения держателя», сюда не
дублируется. Решения узла Н7 — секцией выше.

- **Д144. Удостоверитель полноты не резолвится из коллекции, чью пустоту
  он удостоверяет.** Признак `hadEntry` разобран на два факта с разными
  durable-носителями: пустота ног — колонкой сделки (окно линковки, единственный
  писатель при первой отправленной заявке), пустота эпизодов — фактом наблюдения
  позиции. Альтернатива — подпереть единый удостоверитель. Почему так: единый
  операнд ошибался в **обе** стороны (вакуумная истина у ног, ложный отказ у
  восстановленной сделки), и круговая зависимость воспроизводилась бы на
  каждой ревизии. Запрет записан клаузой в доме.

- **Д145. Предикат полноты требует непустоты там, где пустота незаконна, и
  называет коллекции, чья пустота законна.** Альтернатива — оставить `all`
  как есть либо потребовать непустоты у всех пяти конъюнктов. Почему так:
  `all` над пустой коллекцией истинен, поэтому «ничего не загружено» проходило
  как «всё загружено», а числа выходили нулями в разрешающую сторону; но у
  части коллекций пустота законна по построению тропы.

- **Д146. Писатели энфорсеров первого принципа — оба терминальных
  исполнителя, счётчик в транзакции терминала, ступень и база после её
  коммита.** Альтернатива — отдельный компонент-энфорсер. Почему так: третьей
  терминальной тропы нет, оба операнда уже в контексте прохода, а прецедент
  «после коммита» уже ратифицирован реакцией на расхождение сверки.

- **Д147. Бессрочное ожидание курса уложено в существующий бюджет попыток
  добычи, без собственного срока и счётчика.** Альтернатива — свой таймер
  либо своя реакция. Почему так: живая строка без курса означает «факт не
  добыт», то есть звено не завершается — это уже выражено; при исчерпании
  срабатывает существующий триггер холда инструмента.

- **Д148. Ручное управление safety-остановкой — сервисная операция поверх
  существующей точки входа реакций, а не команда командного lifecycle и не
  второй механизм.** Режим вызова = существующий класс реакции; собственного
  перечня режимов нет. Альтернативы — команда (отвергнута держателем),
  отдельный компонент, расширение сигнала. Почему так: оба названных режима
  ложатся на существующие ступени, а собственный механизм дублировал бы
  состав реакции, идемпотентность и путь снятия.

- **Д149. Снятие холда идёт своей тропой и называет снимаемую ступень
  явно.** Альтернатива — снятие через ту же точку входа и «снять всё».
  Почему так: снятие на биржу не ходит, а его отказ по предусловию обязан
  быть виден сразу; неявная цель дала бы шаг по лестнице при двойном вызове
  и вернула бы торговлю.

- **Д150. Катастрофический потолок получил слагаемое проверяемого акта;
  нотинал живой ноги считается по неисполненной доле.** Альтернатива —
  оставить сравнение существующего нотинала. Почему так: без слагаемого акта
  первый вход вычислял `0 ≤ потолок` и пропускал любой размер, а полный
  плановый размер задваивал налитую долю и делал потолок строже объявленного.

- **Д151. Безубыток декларируется способом расчёта и берёт точную форму
  `якорь × (1 ± f)/(1 ∓ f)`, а не линейное приближение.** Альтернатива —
  приближение с записанной ошибкой. Почему так: остаток приближения
  систематичен и знако-асимметричен по стороне, а шаг называет себя
  безубытком; точная форма делает клейм «ноль» проверяемым величиной спеки.

- **Д152. Способ расчёта «безубыток» допустим только у действия,
  переносящего уровень.** Альтернатива — не ограничивать роль. Почему так:
  без ограничения он объявим как первичная защита, дистанция риска гаснет в
  ноль, сайзинг умножается, а защита срабатывает немедленно.

- **Д153. Якорь безубытка и пола — ветвь, а не подстановка по умолчанию.**
  Альтернатива — `coalesce` к плановой цене. Почему так: фактическая средняя
  у живого эпизода может быть ещё не наблюдена, и откат к плановой дал бы
  благоприятное умолчание, запрещённое П1 §4; теперь при отсутствии якоря
  величина отказывает.

- **Д154. Реестр гейтящих предусловий переведён на слоты «пункт × кейс», а
  проверка его сходимости с планом — на исполнимый скрипт.** Альтернатива —
  команда в тексте реестра. Почему так: пункт с двумя носителями факта
  закрывался одним наблюдением, а текстовая команда сравнивала множества
  кейсов и на состоянии, породившем дефект, давала пустой вывод.

## Итерация 2026-08-30: поправки механизмов закрытия (задача держателя)

Сами четыре поправки — решение держателя
(`.claude/decisions/closure-mechanism-amendments.md`); автономно решены
детали исполнения.

- **Д155. Ось «носители прогона» скрипта предусловий — сверка клейма
  колонки с фактикой, а не справка.** Альтернатива — вывести ось из числа
  объявленных (оставить печать фактики без влияния на код возврата).
  Почему так: стандарт «падающая проба по каждой оси» требует, чтобы ось
  умела падать; сверка клейм↔фактика ловит реальный класс дефекта
  (ложный клейм покрытия — «в коде», когда кейс без код-теста), а
  сокращение объявления оставило бы его без механической проверки.

- **Д156. Доказательство осей — батарея sed-проб во временных копиях, а
  не git-фикстуры.** Альтернативы — фикстуры пред-состояния в репозитории
  (~380 KB + правило обновления) или адрес `git show HEAD`. Почему так:
  относительный адрес уже протухал (F3 `DOCS_CHECK_28`), фикстуры —
  бо́льший след без выигрыша; пробы воспроизводимы из текущего состояния,
  а их протухание видно по правилу «зелёная проба переякоривается».
  Историческая редакция адресована неподвижно (`git show d132a37:…`).

## Итерация 2026-08-30: закрытие `GAPS_CLOSE_28`

Решения держателя (три) — во входе закрытия
`.claude/work/progress/phase-1-step-7-gaps-close-28/_input.md`. Ниже —
автономные решения CC, принятые узлами закрытия.

- **Д157. Правило сопоставления закрывающего исполнения с траншами —
  FIFO по возрасту транша.** Альтернативы: пропорционально экспозиции
  (порождает дроби округления и остаток на каждом транше);
  LIFO; durable-поле-накопитель (теряет производность). Почему так:
  порядок задан наблюдаемым фактом, гашение идёт целыми размерами,
  полное закрытие обнуляет каждый транш. Ключ порядка — Д163; статус
  самого порядка (несущий, а не конвенция) и цена LIFO — Д164.
  Различитель «чужое закрытие» **внутри окна не сохраняется** — сверка
  там слепа в обе стороны, и это названное ограничение правила; вне окна
  различитель работает без оговорок. Дом —
  `docs/models/domain/aggregate/DealTranche.md`.

- **Д158. Окно атрибуции закрывающего исполнения — durable-статус
  сделки, а не факт отправки команды.** Альтернатива — гейт «команда
  закрытия отправлена». Почему так: команда не хранится, на агрегатной
  тропе строки исполнения нет, и гейт не пережил бы рестарт; статусное
  окно durable и покрывает обе тропы, включая kill-switch, где команды
  нет вовсе.

- **Д159. Тропа восстановления — вторая тропа `DealOpeningService`, а не
  отдельный компонент.** Альтернатива — `DealRecoveryService`. Почему
  так: два писателя `Deal` дублировали бы атомарность создания,
  слот-гейт и присвоение идентичности при совпадении почти всех шагов.
  Следствия: `strategy_detail_id` становится nullable, `entry_reason` —
  `NOT NULL`, биекция закрепляется `CHECK`.

- **Д160. `EntryReason` сведён к двум значениям** (`STRATEGY`,
  `RECOVERY`). Альтернатива — сохранить `MANUAL` и `UNKNOWN`. Почему
  так: `MANUAL` дублировал бы `RECOVERY` (обе — живой риск без
  объявления), а `UNKNOWN` запрещён П1 как благоприятное умолчание;
  писателя ни у того, ни у другого не существовало.

- **Д161. Исходы округления выхода переветвлены с внешнего признака на
  остаток.** Альтернатива — оставить ветвление по «размер ≥ `minSz`».
  Почему так: прежняя форма оставляла остаток ниже минимального
  торгового размера — незащищаемую и незакрываемую дробь, — и одновременно
  давала полному выходу по объявленной доле ложный отчёт об округлении.

- **Д162. Дом `hasLiveEpisode` — единственный, потребители берут
  значение операндом.** Альтернатива — оставить два объявления.
  Почему так: обе прежние копии реализовывали один конъюнкт из двух, и
  расхождение копий не ловилось ничем.

- **Д163. Ключ порядка атрибуции траншей — `DealTranche.id`, а не время
  первого филла входа.** Мандат держателя на проектирование правила
  сопоставления называл ключом время первого филла; действующее правило
  ключуется идентификатором транша. Альтернатива — сохранить названный
  ключ. Почему так: время первого филла у транша, чей вход ещё не налился,
  пусто, то есть ключ не определён ровно там, где порядок и нужен, а два
  транша, налившиеся в одном тике, дают равные ключи и порядок между ними
  не задают; идентификатор монотонен по заведению и определён всегда.
  **Это подмена решения держателя решением CC** — вынесено сюда именно для
  вето: если ключ был выбран содержательно (возраст по рынку, а не по
  заведению), решение отменяется, и тогда правило обязано назвать, чем
  разрешается пустой и равный ключ.

- **Д164. Порядок атрибуции — несущий выбор, а не свободная конвенция.**
  Прежняя редакция объявляла различие FIFO и LIFO ненаблюдаемым и
  закрывала порядковую часть правила названным ограничением.
  Альтернатива — оставить как есть. Почему так: посылка ограничения
  («дочистка защит идёт после подтверждённого закрытия сделки») неверна —
  дочистка потраншевая и гейтится нулём экспозиции своего транша, поэтому
  достижимо состояние, где старший транш уже дочищен и закрыт, а младший
  несёт экспозицию под живой защитой; на нём порядки различаются, и
  перевёрнутый даёт ложную биржевую ступень на штатном выходе.

## Итерация 2026-08-30: правка пайплайна — смена способа доказательства

Решения держателя приземлены задачей правки пайплайна
(`.claude/decisions/proof-method-change.md`); ниже — автономные
проектные выборы CC внутри приземления.

- **Д165. Дома и имена носителей пакета.** Решение —
  `.claude/decisions/proof-method-change.md`; правило уровней
  носителей — новым файлом `.claude/rules/carrier-levels.md`, не
  секцией `policy-home.md`; классификатор —
  `.claude/skills/classify-gap-level.md`; дом мутационного стандарта —
  `roadmap-step-execution.md` §3 рядом со стандартом осей, строка
  `docs/spec/` в `structure.md` — указателем. Альтернативы: расширить
  `policy-home.md` (отвергнуто: вопрос файла стал бы составным —
  политики и стыковые решения); норму приёмки целиком в строку
  `docs/spec/` (отвергнуто: стандарты доказательства уже живут в §3
  процесса — прецедент осей; размещение — не стандарт).
- **Д166. Состав классификатора уровня пробела.** Пять уровней
  держателя развёрнуты таблицей «признак → род действия → минимальный
  объём»; порядок проверки сверху вниз (верхний уровень поглощает
  нижние); составная находка раскладывается по частям; действие
  нижнего уровня может переквалифицировать находку вверх.
  Альтернатива — плоский перечень признаков без порядка (отвергнуто:
  без порядка продуктовый пробел закрывается «доказательством» —
  достраиванием недоказуемого).

## Итерация 2026-08-30 (2): среда, грунт, `GAPS_CLOSE_29`

Автономные проектные выборы CC при сборе грунта и дизайн-проходе.
Продуктовые решения держателя (база риска, запас нотинала, точечные
ручки) здесь **не** дублируются — их дома названы в отчёте
`.claude/work/progress/phase-1-step-7-gaps-close-29.md`.

- **Д167. Демо-контур доказывается отрицанием, а не флагом.** Проверка
  «креды демо» сделана падающей пробой: тот же ключ без заголовка
  `x-simulated-trading` бьётся в боевой контур и обязан получить отказ
  (`50101`). Альтернатива — сверить `okx.simulated=1` в профиле
  (отвергнуто: это утверждение о **нашей** настройке, а не свойство
  ключа; при подмене секрета в Vault такая проверка молчит).
- **Д168. Общая фикстура сделки — один класс, а не кейс на файл.**
  `Ag1DealFixtureLiveTest` строит цепочку «эпизод с частичным закрытием +
  второй эпизод в том же окне» один раз и обслуживает семь кейсов
  (AG1.5–1.9, AG3.4, AG6.2). Альтернатива — по кейсу в своём классе
  (отвергнуто: план прямо требует «фикстура общая, прогон один», а
  межклассовое разделение состояния в JUnit выражается только статикой).
- **Д169. `observeContent(...)` — отдельный инструмент, не расширение
  `observe(...)`.** Второй зовут ~200 кейсов, часть из них тянет ордера
  и позиции; печать состава у всех раздула бы лог прогона. Решение
  предписано планом (`.claude/tests/source-api/okx/plan.md`) §AG6.1 и исполнено дословно.
- **Д170. Слот, чью фикстуру собрать не удалось, остаётся `PENDING`.**
  Исход «событие не наступило» гейт не закрывает — прямое указание
  держателя 2026-08-30; прежняя редакция (`OBSERVED_ABSENT` закрывает,
  решение П18 `GAPS_CLOSE_17`) снята в обоих носителях. Следствие
  названо: `CODE` шага 7 может ждать эпизода ADL, которого биржа не
  обязана произвести.
- **Д171. Исключений из мутационного стандарта нет: охранный инвариант
  несёт предъявленный контрпример.** Ярлык-исключение удостоверял бы
  наличие указателя, а не свойство величины, и «фальсифицирующего
  состояния не существует» механически не доказуемо ни одной пробой.
  Вместо ярлыка — пример с ключом `unreachable`, называющий, чем
  состояние исключено, и роняющий инвариант на нём. Инвариант, у
  которого контрпримера не существует, — переобъявление соседней
  формулы: он снимается, клейм переезжает в её ноту. Альтернативы и
  цена — `.claude/decisions/acceptance-by-measurement.md`.
- **Д172. Набор констант замера дополнен литералами корпуса.** Без него
  число, ожидаемое во всех примерах одним номиналом, не ловилось бы
  фиксированным набором `{true,false,0,1,null}` вовсе — а оно и есть
  недоказательное. Цена — перечень вырос с 50 до 59 позиций, все
  доведены.
- **Д173. Дом закрытой формы риска — новая спека `risk-at-stop`, а не
  один из четырёх прежних.** Форма нужна четырём предметам с **разными
  якорями**; поселить её в любом из них означало бы, что три
  оставшихся подключают чужой предмет. Альтернатива — связать имена
  величинами-псевдонимами в потребителях (отвергнуто: псевдоним
  объявляет второй дом имени и даёт детектору областей видимости
  ложные находки класса B; проверено прогоном).
- **Д174. Исключение оси C объявляется в спеке, а не зашивается в
  скрипт.** `independentFrom` + `independentReason` на величине.
  Альтернатива — список пар-исключений внутри детектора (отвергнуто:
  довод исключения уехал бы из носителя, к которому относится, и
  устаревал бы молча).
- **Д175. Неполный граф даёт `UNDETERMINED`, а не отказ вычисления.**
  У торгового исхода закрытия уже есть значение «решить нельзя», и оно
  не-durable-опасно; заводить второй способ сказать то же самое
  (отказ) — лишний след. У `deal-lifecycle` охрана поставлена
  конъюнктом в `riskProvenAbsent`: там «доказано отсутствие» и есть
  предмет, о котором неполный перечень лгать не должен.
- **Д176. Ветвь `BREAKEVEN` в `PriceCalculator` — контролируемая ошибка
  расчёта, а не реализация уровня.** Дерево не компилировалось с
  `GAPS_CLOSE_27`; минимальная починка возвращает сборку, не выдавая
  за реализацию того, чего нет (валидаторы роли уровня в коде тоже
  отсутствуют, и половинчатая реализация считала бы безубыток и для
  первичной защиты — того, что правило прямо запрещает). Альтернатива —
  снять значение енума до `CODE`-дельты (отвергнуто: backlog-план
  шага написан от того, что значение уже заведено).


## Итерация 2026-08-30: `GAPS_CLOSE_30` — дизайн-проход концепции приёмки

Развилки, решённые CC автономно внутри выбора держателя
(`.claude/decisions/acceptance-by-measurement.md`). Итог закрытия —
`.claude/work/progress/phase-1-step-7-gaps-close-30.md`.

- **Д177. Класс «величина-теорема» снят целиком, а не усилен пробой.**
  Усиленная косвенная проба всё равно принимает недотестированную
  величину: «фальсифицирующего состояния не существует» механически не
  доказуемо. Альтернатива — требовать предъявленного состояния при
  сохранённом ярлыке (отвергнуто: механизм, выглядящий доказательством и
  им не являющийся, и есть снимаемая посылка). Инвариант без контрпримера
  оказывается переобъявлением соседней формулы и снимается по
  `carrier-levels`.
- **Д178. Батареи осей переехали внутрь своих команд, отдельные
  `*-probe.sh` сняты.** Альтернатива — оставить пробы отдельными и
  добавить гейт их прогона (отвергнуто: гейт — ещё один носитель, который
  можно не исполнить; батарея, которую нельзя не запустить, дешевле).
- **Д179. Привязка указателя на величину — синтаксическая, не оконная.**
  Оконная («ближайшая ссылка в 160 символах») дала 19 находок, **все
  ложные**. Действующая — по смежности, с рангом формы при конфликте.
- **Д180. Адрес обратными кавычками не окружается; в кавычках — разговор
  о форме.** Различение механическое и снимает иллюстрации правил из
  области детектора без per-файловых исключений. Цена — нормализация 11
  вхождений в шести файлах.
- **Д181. Накопленный долг §-адресов объявлен построчно храповиком
  (`tools/anchor-debt.txt`), а не парковкой в backlog.** Альтернативы:
  оставить детектор красным (отвергнуто: постоянно красная проверка учит
  её игнорировать) и сузить детектор до прежней формы (отвергнуто:
  возвращает измеренную долю предмета к трети). Храповик роняет прогон на
  новом дефекте и на устаревшей строке долга.
- **Д182. Датированная заметка-запись объявляет исключение маркером в
  первой строке.** Её §-адреса ведут в раскладку того момента; чинить их
  нечем, переписывать под нынешние имена — сделать запись ложной.
  Исключение даёт объявление, а не расположение файла; число исключённых
  печатает прогон.
- **Д183. Разрешённые места энфорсера снятых редакций — только носители
  пайплайна.** Файл `docs/**` в них не попадает никогда: `docs/concept.md`
  §«Опровержение правит на месте» запрещает слой опровержения в корпусе.
  Следствие: пересказ «прежняя редакция держала базу невозрастающей» снят
  из четырёх продуктовых носителей, довод переехал в решение.
- **Д184. Исход `SEARCH_MORE` закрыт пятым источником цикла добычи, а не
  материализацией доменной сущности.** Крен линзы был на материализацию
  доводом «закрывает и вторую находку тем же ходом»; вторая закрыта своим
  креном, довод отпал, и по дефолту минимального следа выбран меньший.
- **Д185. Операнд уровня защиты после акта — контракт операнда, а не
  вторая величина.** Величина `stopPriceAfterAct` с собственной формой
  риска была бы копией закрытой формы на другом операнде — ровно тот
  дефект, который ловит ось C.2. Резолв назван на call-site преконтроля.
- **Д186. Список шагов одного статуса — упорядоченный first-match.**
  Конвенция та же, что у правил фазы; вторая в модели не заводится.
  Альтернативы: применять все подходящие подряд (отвергнуто: две команды
  на биржу по одной позиции) и отбирать сильнейший порог (отвергнуто:
  вводит ось сравнения шагов, которой в модели нет).
- **Д187. Поглощённый ручной вызов заводит строку журнала, если ступень
  подняла автоматика.** Спека приведена на сторону дома
  (`error-handling-policy.md`): ключ отчёта — свой, и у ручного основания
  поверх автоматической ступени строки по нему нет.
- **Д188. Причину закрытия сделки пишет затребователь терминального
  ребра, до звена расчёта.** Альтернатива — писать её терминальным
  исполнителем (отвергнуто: финализация выхода причину **читает**, и её
  клейм «читает» перестал бы быть верным). Наследование транша сведено к
  таблице отображения события сворачивания, без нового значения перечня.
- **Д189. Два недостижимых значения перечней сняты, а не оставлены
  «на вырост».** `CONDITION_EXPIRED` у заявки (истечение условия —
  терминал транша) и `UNKNOWN_EXTERNAL_STATUS` у встроенной защиты
  (собственного внешнего статуса у неё нет — тот же довод, которым
  значение снято у `Position`).
- **Д190. Слоты грунта, закрытые фактом из лога, возвращены в
  `PENDING`.** Альтернатива — оставить закрытыми, дописав перечень в
  реестр руками (отвергнуто: наблюдением это не станет, а реестр начал бы
  нести число, которого прогон не производил). Заведён
  `persistObservation`: кейс пишет перечень в репозиторий.

---

## Итерация 2026-08-31 — `GAPS_CLOSE_31`, дизайн-проход полноты закрытия

Решения CC, принятые автономно; альтернативы и довод — по строке каждое.
Вето постфактум. Выбор держателя по эскалации С1 (сменить концепцию
полноты закрытия) — `.claude/decisions/closure-completeness-by-population.md`,
он здесь не дублируется.

| # | Решение | Альтернативы | Почему так |
|---|---|---|---|
| Д192 | Популяция объявляется **в спеке** ключом `populations` с осями `keys`, фильтром `where` и контрпримером у недостижимого члена | держать перечень прозой в отчёте закрытия; строить граф-артефакт связей | перечень, который никто не прогоняет, зелен всегда; прозаический вариант и есть снимаемая посылка |
| Д193 | Равенство «перечень членов == перечень объявленных рёбер» машиной **не** держится, и клейма об этом нет | объявить равенство и не проверять; строить перечень рёбер из популяции | клейм полноты без энфорсера запрещает писателю искать дальше — хуже отсутствия клейма; вторая альтернатива требует операции, которой в языке спек нет |
| Д194 | Писатель `closeReason` назван **по ребру**, а не по событию; на рёбрах в `ERROR` причина не пишется | писать причину терминальным звеном; развести на два поля | у тропы перехвата обработчика нет вовсе — назначать писателя некому; `ERROR` не терминал |
| Д195 | Причина сделки на маршрутах, где её дают транши, — **старшая** по объявленному порядку, а не причина последнего закрывшегося | по времени закрытия транша с тай-брейком по `id` | операнда «время закрытия транша» не существует: у `Auditable` только время последнего изменения, и в одной транзакции метки равны |
| Д196 | `Deal.ShutdownReason` сохраняет холдовые значения, но получает **второго писателя** — шаг энфорсмента жёсткой ступени на ребре в `ERROR` | снять холдовые значения как недостижимые | у сделки, поднятой в `ERROR` каскадом, второго отчёта аномалии не заводится, и без поля durable-ответа «почему» не остаётся (П3) |
| Д197 | Судьба встроенной защиты выводится из пары «терминален родитель» + «есть налив», **независимо от присутствия** элемента в снапшоте | оставить гейт по присутствию; завести второй предикат рядом | присутствие живости не доказывает (наблюдение контура), а второй предикат — второй носитель одной истины |
| Д198 | Исход ненайденности вынесен во **вторую ступень** (`searchExhaustedOutcome`) | оставить единой формулой | единая формула объявляла покрытие потерянным, **не поискав**, у самой частой популяции |
| Д199 | Шаг стратегии применяется на эпизоде транша **однократно**; признак производный, из строк исполнения | предикат пересечения порога; полоса в условии; отбор «самого сильного порога» | предикат пересечения требует носителя, зависящего от порядка событий; полоса перекладывает инвариант на автора; отбор переоткрывает ратифицированный first-match |
| Д200 | Ступень лестницы у триггера «исчерпание бюджета» выводится **осью** (покрытие после отказа), а не назначается формой | назвать вторую ось «тип отказа» и дать ей дом | вторая ось ломает клейм обеих лестниц об одной оси и требует таблицы отображения, которой нет |
| Д201 | На входной тропе первым отказывает **сайзинг**, и его отказ по стороне уровня — отказ шага, не авария | перенести проверку стороны уровня до сайзинга | перенос заводит новую точку проверки ради единого кода реджекта; развязка исходов дешевле |
| Д202 | Область `primaryStopOnLossSide` сужена до действий, **объявивших способ расчёта**; изнанку закрывает инвариант покрытия | переписать эталон на `REPLACE`; ввести третье значение роли | переписывание эталона упирается в смену типа заявки; третье значение — больший след при том же исходе |
| Д203 | Поглощённый ручной вызов заводит строку по **паре** операндов (вызов ручной × ступень поднял не он) | разводить по соотношению ступеней | асимметрии между «слабее» и «равен» правило не знает; автоматический повтор строку уже завёл своей тропой |
| Д204 | Сводные счёта слотов сняты как **носитель**, а не пересобраны | пересобрать числа; завести пятую ось энфорсера | правка «пересобрать» уже применялась и класс её пережила; снятие носителя дешевле энфорсера |
| Д205 | Ветвь пропуска кейса контура пишет исход **персистентно** | поправить только клаузу реестра | иначе «не гонялся» и «гонялся, окно не подошло» не различаются в данных — ровно тот дефект, что стоил двух слотов |
| Д206 | Дискриминатор пары реакций на устаревание — «есть живой риск И он не покрыт»; неразрешимый уровень читается **непокрытым** | дискриминатор `trancheCovered` в одиночку; третье поле реакции | одиночный предикат делает «нечего защищать» неотличимым от «покрыто»; третье поле — больший след и правка эталона в восьми местах |
| Д207 | Область правила «кейсы обезличенно» — **роль файла** (обязательный вход линзы), а не его тип | оставить область на скилл-файлах | механизм правила — независимость линзы, и он не различает скилл от процесса; 24 номера снято из процесса и двух скиллов |

---
