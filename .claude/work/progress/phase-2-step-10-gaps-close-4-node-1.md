# `GAPS_CLOSE_4`, узел 1 — провод: чем едет конверт, что едет содержимым, кто читает

## На какой вопрос отвечает этот файл

Как закрыты находки A1, A6, B4, B5 прогона `DOCS_CHECK_4`.

## Правило узла

**У всего, что едет проводом между сервисами, дом формы называет три вещи —
ЧТО едет, ЧЕМ едет и КТО читает, — и ни одно из трёх не держится клеймом,
который построенный код опровергает.**

Четыре находки — четыре способа нарушить это правило одним домом
(`docs/architecture/contracts.md`): «чем едет» не сказано вовсе (A1), «кто
читает» сказано ложно (A6), «что едет» недосказано у одного класса (B4) и
пересказано ложным клеймом невыводимости у другого (B5).

## Уровень пробела у каждой находки

| Находка | Уровень | Род закрывающего действия |
|---|---|---|
| A1 | **концепция-стык** — контракт между производителем и потребителем; дом не вправе быть стороной стыка | правка дома уровня истины + симметричный обход всех сторон стыка |
| A6 | **носитель** — истина верна в двух домах, копия в третьем разошлась | свести копию к дому, истину не переформулировать |
| B4 позиция 1 | **концепция-стык** — состав содержимого против механизма заполнения колонок радиуса | правка дома состава + обход производителей и владельца журнала |
| B4 позиция 2 | **носитель** — ложный пример и ложный довод в одном доке | точечная правка двух пассажей |
| B5 | **носитель** — дом величины прямо объявляет «своего поля нет», а копия требует поле | свести копию к дому |

**Доказательства новой спекой узел не заводит, и это решение** (Д1091):
ни одна находка не стои́т на уровне «доказательство» — предикат ни у кого не
переживает нейтрализацию, пример ни у кого не парный. Отвергнутая
альтернатива — спека «какие идентичности радиуса класс кладёт верхним
уровнем» с популяцией по классам события: она была бы **вторым** носителем
состава содержимого (`.claude/rules/carrier-levels.md`), а охрана состава
уже объявлена и лежит там, где форма существует, — тест состава у писателя
каждой формы (`docs/architecture/contracts.md` §«Названное ограничение
сменило сторону: потребители построены, а производители — не у всех
классов», позиция 19 дельты `CODE`).

## Популяция (построена ДО первой правки)

Осей четыре: три носительские и одна предметная, плюс собственный артефакт
закрытия.

### О1. «Дом называет, ЧЕМ едет каждое поле конверта» (A1)

Ось — **носители**, где транспортная упаковка конверта объявлена либо
предполагается.

```
LC_ALL=C.UTF-8 grep -rlUzP '(?si)(заголовк[а-яё]+ сообщени|ключ записи|record\.key|RecordHeader|HEADER_EVENT|EventHeaders)' \
  docs/ .claude/rules/ .claude/processes/ .claude/work/backlog.md services/*/src libs/*/src deploy/
LC_ALL=C.UTF-8 grep -rlUzP '(?si)(конверт[а-яё]* событи|EventEnvelope|outbox_events)' \
  docs/ .claude/rules/ .claude/processes/ .claude/work/backlog.md libs/*/src services/*/src
```

**Шаблон даёт три ложных срабатывания, и они названы, а не отброшены
молча:** `ExchangeAccountDataService.java` («ключ записи» о ключе строки
биржевого счёта), `docs/rules/api-access-policy.md` и
`tools/preconditions-check.sh` («заголовками» о HTTP-контексте вызова и о
заголовках тест-плана). Членами популяции они не являются.

| Член | Исход |
|---|---|
| `docs/architecture/contracts.md` §«Конверт события» | **правлен**: новый пассаж «Как конверт лежит на проводе — поимённо» плюс пассаж о единственном носителе имён |
| `docs/models/domain/other/AuditRecord.md` §«Единственная ветвь, на которой строки не будет, — неполный вход» | **правлен**: перечень пяти обязательных значений получил указатель, **откуда** каждое берётся; прежде потребитель, написанный по нему, искал заголовок тенанта |
| `libs/…/domain/event/EventEnvelope.java` | правило выполняется: класс транспортные величины **упоминает** (ключ партиции в javadoc двух полей), формы транспорта не объявляет и к дому отсылает. Отсутствие `eventType` в классе — **живая позиция 1 дельты `CODE`**, заведённая `GAPS_CLOSE_1` (A8) |
| `services/trading-core/…/integration/EventPublisher.java` | правило выполняется: приватные константы, имена равны именам полей, ключ записи — тенант; упоминание со ссылкой на дом уже стои́т |
| `services/strategies/…/integration/EventPublisher.java` | правило выполняется: то же, класс тождествен предыдущему по модулю пакета |
| `services/bff/…/util/Constants.java` §`EventHeaders` | правило выполняется: три имени из шести, все равны именам полей — периметру остальные не нужны |
| `services/bff/…/integration/StreamEventConsumer.java` | правило выполняется и **служит источником кодирования**: тенант берётся из `record.key()`, момент — `OffsetDateTime.parse`. Объявляемая форма списана с построенного читателя, а не назначена ему |
| `services/trading-core/…/integration/StrategyDefinitionConsumer.java` | правило выполняется: **четвёртая** копия имён (два из шести), обе равны именам полей. Что тенанта он не берёт ни из ключа, ни из заголовка — **не предмет узла**: у копии определения тенант приезжает внутри снимка |
| `services/{trading-core,strategies}/…/persistence/model/OutboxEntity.java` | правило выполняется: колонки равны полям конверта; `version` и `occurred_at` объявлены `nullable = false`, поэтому `String.valueOf(...)` на них литерала `"null"` не даёт — **сверено, дефекта нет** |
| `services/{trading-core,strategies}/…/db/migration/V1__*.sql`, `V6__outbox_table_plural_name.sql` | правило выполняется: колонки `outbox_events` равны полям конверта |
| `docs/components/OutboxRelayJob.md` | правило выполняется: форма реле, транспортной упаковки не объявляет — упоминание со ссылкой на дом |
| `docs/components/AuditEventListener.md` | правило выполняется: форма слушателя, транспорта не объявляет, указывает на владельца предмета |
| `docs/architecture/data-ownership.md` | правило выполняется: ключ партиции назван тенантом, второй записи формы нет |
| `docs/architecture/{services,platform,tenant-and-exchange}.md`, `.claude/rules/tech-radar.md` | правило выполняется: упоминают конверт указателем, транспорта не объявляют |
| `.claude/work/backlog.md` §«Конверт события не разбирается читателем — та же форма, что была у ack» | **правлен**: числовой клейм «берёт три величины из заголовков» снят: поля конверта из заголовков, тенант из ключа записи. Оживитель позиции переназначен ещё `GAPS_CLOSE_1` (A16) и правкой не тронут: новый пассаж дома объявляет форму, а разбора конверта не заводит |

### О2. «Дом называет, КТО читает событие» (A6)

Ось — **носители**, перечисляющие потребителей события.

```
LC_ALL=C.UTF-8 grep -rn 'потребител[а-яё]* событ' docs/ .claude/rules/ .claude/processes/ .claude/work/backlog.md
LC_ALL=C.UTF-8 grep -rn 'аудит, статистика' docs/ .claude/
```

| Член | Исход |
|---|---|
| `docs/architecture/contracts.md` строка таблицы событий ядра | **правлен**: «статистика» снята |
| `docs/architecture/contracts.md` §«Содержимое несёт идентичности и то, чего потребитель не дочитает сам» | **правлен**: «статистика» снята, добавлен указатель на дом |
| `docs/architecture/services.md` строка `audit-statistics` | правило выполняется — **это дом**: «потребитель событий один — аудит; статистика не потребляет, а пересчитывает проекцию» |
| `docs/architecture/data-ownership.md` §«Outbox и доставка» и строки раскладки | правило выполняется: «одно следствие», писатель inbox — потребитель события; статистики среди потребителей нет |
| `docs/rules/statistics-aggregates.md` §«Пересчёт — проекция, а не накопитель» | правило выполняется: прямо отрицает потребление событий статистикой |
| `docs/models/domain/other/AuditRecord.md` (два вхождения) | правило выполняется: речь о писателе строки, не о перечне потребителей |
| `docs/dictionary/audit.md` | правило выполняется: назван **сервис** `audit-statistics`, и он действительно потребитель; модуль статистики потребителем не назван |
| `docs/rules/error-handling-policy.md` | правило выполняется: «сам потребитель событий» — о писателе флага остановки |
| `docs/spec/audit-journal.json` нота `recordedAt` | правило выполняется: писатель строки, не перечень потребителей |
| `docs/models/domain/aggregate/Strategy.md` | правило выполняется: потребитель событий владельца — ядро; о статистике не говорит |
| `docs/components/{DealOpeningService,StrategyDemandJob}.md` | правило выполняется: журнал либо ядро как потребитель конкретного класса |
| `.claude/work/backlog.md` (два вхождения) | правило выполняется: о числе деплоев, не о перечне потребителей события |
| код: `StrategyDefinitionConsumer`, `RegistryProjectionJob`, `StrategyDemandService`, `StrategyDataService`, `V8__inbox_events.sql` | правило выполняется: потребитель — ядро, статистика в них не упомянута |
| `deploy/**` и `services/*/src/main/resources/**` | правило выполняется: consumer-group статистики нет ни в одном манифесте и ни в одной конфигурации |

### О3. «У каждого класса события состав содержимого сходится с его предметом» (B4, B5)

Ось — **предметная**: класс события. Перечень выводится из таблицы
производителей дома, а не из классов, встреченных в коде.

```
bash tools/derive/event-class-manual-path.sh
```

Двадцать один член. **Форма исхода одинакова у всех: «несёт ли класс
верхним уровнем каждую идентичность радиуса, какая у его предмета есть» и
«нет ли в его объявленном составе производного поля».**

| Класс | Исход |
|---|---|
| `OrderDecided` | правило выполняется: сделка, счёт, инструмент — верхним уровнем; стратегия предметом не адресуется. Производного в составе нет |
| `DealOpened` | правило выполняется: сделка, счёт, инструмент — верхним уровнем; идентичность определения добавляется позициями 3 и 14 дельты `CODE` — тем же уровнем |
| `DealShutdownInitiated` | **недостижим**: класс объявлен таблицей, значения перечня нет — содержимого не существует. Достижимым становится позицией 12 дельты `CODE` |
| `DealClosed` | **правлен этим закрытием**: из объявленного состава снят производный член и введён признак полноты графа третьим операндом формы; идентичности радиуса — верхним уровнем, стратегия добавляется позицией 2 |
| `HoldRaised` | правило выполняется: счёт и инструмент верхним уровнем; сделки и стратегии у предмета нет. **Примером однозначной пустоты не служит** — колонка инструмента у него пуста по экземпляру (счётный радиус холда), и на роль иллюстрации однозначности класс не годится |
| `HoldReleased` | **недостижим**: писатель объявлен, значения перечня нет — содержимого не существует |
| `AnomalyReported` | правило выполняется: счёт и инструмент верхним уровнем; идентичность отчёта — деталь строки, не радиус |
| `StrategyActivated` | **правлен этим закрытием**: все три идентичности радиуса едут рядом со снимком верхним уровнем, а не одна; прежде две из трёх лежали внутри снимка и колонки молча оставались пустыми |
| `StrategyDeactivated` | правило выполняется: три идентичности верхним уровнем — `StrategyLifecycleContent`; **и он же новый пример однозначной пустоты** (колонка сделки пуста по предмету, счёт и инструмент `not null` в схеме владельца) |
| `StrategyDeleted` | правило выполняется: то же содержимое |
| `BacktestCompleted` | **недостижим**: предмета не существует — бэктест приезжает фазой 4 |
| `SignalDetected` | **недостижим**: предмета не существует — фаза 4 вместе с производителем |
| `SignalOutcomeRecorded` | **недостижим**: предмета не существует — фаза 4 |
| `ExecutionObserved` | **недостижим**: единственный механизм публикации — WS-слушатель коннектора, он ждёт добытого контракта каналов |
| `MembershipChanged` | **недостижим**: у `auth` нет ни outbox, ни реле, ни темы |
| `ExchangeAccountRegistered` | **недостижим**: то же — производителя у `auth` нет |
| `ExchangeKeysRotated` | **недостижим**: то же |
| `TenantSuspended` | **недостижим**: то же |
| `InstrumentListed` | **недостижим**: у `market-data` outbox не заведён — производителя нет |
| `InstrumentRulesChanged` | **недостижим**: то же |
| `FeaturesUpdated` | **недостижим**: то же |

**Причины недостижимости взяты у уже объявленной популяции того же
перечня** (`docs/spec/event-actor-presence.json`, ось «класс события») и
здесь не изобретены: два правила стоя́т на одной оси, и разойтись их
причинам недостижимости было бы дефектом. Сверка сделана чтением —
машиной она не проверяется, и это названное ограничение перечня.

### О4. «Клейм невыводимости стои́т ровно на невыводимом» (B5)

Ось — **носители**, где объявлен состав `DealClosed` либо клейм о
производном.

```
LC_ALL=C.UTF-8 grep -rlUzP '(?si)(не выводит из прочих|ничего производного|до финансирования|ценов[а-яё]+ результат)' \
  docs/ .claude/rules/ .claude/processes/ .claude/work/backlog.md .claude/work/code-gate-ledger.json services/*/src libs/*/src
```

| Член | Исход |
|---|---|
| `docs/architecture/contracts.md` §«Отсюда состав `DealClosed` — он несёт операнды отчёта, а не только исход» | **правлен**: производный член снят из перечня, введён признак полноты графа третьим операндом формы, клейм невыводимости стал истинным, назван складывающий читатель |
| `docs/architecture/contracts.md` §«Штраф принудительного закрытия — операнд, а не деталь» | **правлен**: у «обоих публикуемых чисел» назван референт — числа строки агрегата; после снятия члена перечня референт в перечне не разрешался |
| `docs/rules/loss-streak-halt.md` §«Операнд собирается на терминале из уже добытых чисел» | правило выполняется — **это дом**: «собственного поля под ценовой результат не заводит: величина производна и хранится в своих слагаемых» |
| `docs/rules/loss-streak-halt.md` §«Доступность у ценового результата та же, что у итога», §«Второе слагаемое охранено полнотой графа» | правило выполняется — **это дом ветви пустоты и дом охраны полноты графа**; правка узла обе клаузы читает, а не переписывает |
| `docs/spec/loss-streak-halt.json` величина `priceResult` | правило выполняется: форма производной величины объявлена ровно в одном доме, включая конъюнкт полноты графа |
| `docs/spec/statistics-aggregates.json` операнд `priceResult` | **правлен**: нота называет писателя операнда (складывает пересчёт **по форме дома** из трёх приезжающих), причину, по которой форма здесь не переписывается, и отказ объявлять операнд второй величиной |
| `docs/spec/statistics-aggregates.json` величина `fundingDecompositionHolds` | правило выполняется: контрпример `unreachable` объявляет тождество трёх чисел построением, то есть корпус и до правки знал третье число выводимым. **Охраной ветви полноты графа не служит** — при нулевом финансировании тождество сохраняется, и это названо |
| `docs/rules/statistics-aggregates.md` §«Выигрышность меряется результатом до финансирования» | правило выполняется: указатель на операнд спеки, формы не переписывает |
| `docs/components/MarkDealClosedExecutor.md` | правило выполняется: говорит о **счётчике серии**, собирающем операнд в процессе; о поле события не заявляет |
| `docs/components/MarkDealEmergencyClosedExecutor.md` §«Признаки отбора» | правило выполняется и **служит источником достижимости ветви**: «он приходит на усечённом» графе штатно — на этом стои́т клауза о пустоте слагаемого |
| `services/trading-core/…/domain/safety/LossStreakCounter.java` | правило выполняется: складывает операнд в процессе — построенный подтверждает форму дома |
| `services/trading-core/src/test/java/…/DealTerminalEdgeTest.java` | правило выполняется: комментарий теста несёт клейм «ребро на таком графе не ставится вовсе» — он о ребре **счётчика серии**, не о терминале сделки, и конструкция узла на него **не опирается**: ветвь названа значением слагаемого, а не объявлена недостижимой |
| `docs/models/domain/core/Position.md`, `docs/models/integrations/okx/PositionsHistoryOkxResponse.md` | **не потребители**: речь о финансировании эпизода как о величине биржи, состава события не касаются |
| `.claude/work/backlog.md` §«Энфорсер П1» | правило выполняется: прямо пишет «отдельного поля под ценовой результат не заводится» |
| `.claude/work/code-gate-ledger.json` компонент `LossStreakCounter` | правило выполняется: «ценовой результат» назван предметом компонента, полем события — нет |
| `.claude/work/progress/phase-2-step-10-chronicle.md` дельта `CODE`, позиции 2 и 13 | **правлены**: производный член снят, признак полноты графа введён, референт «обоих чисел» уточнён |

### О5. Собственный артефакт закрытия

| Артефакт | Исход |
|---|---|
| пассаж «Как конверт лежит на проводе — поимённо» | перечитан целиком после правки; число обязательных значений в нём **не объявляется** — их дом у владельца журнала, и `traceContext` в перечень не входит |
| правка послабления о верхнем уровне | внесена **на месте**, вторым пассажем не заведена: `docs/concept.md` §«Опровержение правит на месте» |
| две записи реестра снятых редакций | прогнаны `py tools/retired-check.py` — 90 записей, 292 строки популяции, 0 дефектов; шаблоны написаны **прозой**, не именем поля (ловушка Д1086) |
| правки дельты `CODE` | счёт живых позиций **пересобран** из перечня и правлен строкой: 41 заведённая минус снятая пятая = 40 |

## Решения узла

| # | Решение |
|---|---|
| Д1087 | транспортная форма конверта объявляется в доме формы одним пассажем; второго носителя имён заголовков не заводится |
| Д1088 | «статистика» снята из перечня потребителей событий ядра — потребитель один, аудит |
| Д1089 | послабление о верхнем уровне читается квантором «каждая»; у `StrategyActivated` идентичностей радиуса три |
| Д1090 | `priceResult` объявленным полем `DealClosed` не становится, а форму дома читатель применяет **дословно**: событие несёт **признак полноты графа сделки** третьим операндом |
| Д1091 | новой спеки узел не заводит — ни одна находка не на уровне «доказательство» |
| Д1092 | названная цена варианта (а) находки B5 (объявить `priceResult` величиной спеки агрегатов) **отклонена с доводом**: имя уже объявлено величиной в своём доме, а копия формулы под другим именем — дефект конвенции `docs/spec/`; маршрут `includes` назван и отвергнут ценой |
| Д1093 | пассаж о контексте трассировки переименован, его «отвергнуто» исправлено: отвергался заголовок как **единственный** носитель поля, а не как транспорт |

Полные формулировки с альтернативами и ценой —
`.claude/work/decision-digest.md`.

## Правки, приземлённые в корпус

Счёт пересобран по перечню носителей, а не написан рядом с ним: **правок
в продуктовом корпусе — 14** (девять в `contracts.md`, четыре в
`AuditRecord.md`, одна нота спеки), правленых носителей корпуса — три.
Сверх корпуса тронуты четыре носителя пайплайна: бэклог (клейм плюс новая
секция), хроника (пять позиций дельты), реестр снятых редакций (две
записи), дайджест (блок из семи решений). Правки вносились скриптами,
сверявшими единственность каждого блока «Было».

| Носитель | Что |
|---|---|
| `docs/architecture/contracts.md` | восемь правок: пассаж «Как конверт лежит на проводе — поимённо» и пассаж о единственном носителе имён; строка таблицы событий; абзац о потребителе; состав `DealClosed` (снят производный член, введён признак полноты графа, назван складывающий читатель); референт «обоих публикуемых чисел»; послабление о верхнем уровне — квантором «каждая», на месте; пассаж о контексте трассировки — имя и «отвергнуто» |
| `docs/models/domain/other/AuditRecord.md` | четыре правки: пример однозначной пустоты, требование к производителю (три идентичности), довод про `AnomalyReport`, указатель «откуда берётся каждое обязательное значение» |
| `docs/spec/statistics-aggregates.json` | нота операнда `priceResult` |
| `.claude/work/backlog.md` | числовой клейм «три величины из заголовков» снят: поля конверта из заголовков, тенант из ключа записи. **Числа в клейме не осталось намеренно** — оно стареет при каждом новом поле конверта, а предмет позиции в нём не нуждается |
| `.claude/work/progress/phase-2-step-10-chronicle.md` | пять правок дельты `CODE`: позиции 2, 13, 16а, новая 35, строка счёта живых позиций |
| `tools/retired-check.py` | две записи снятых редакций с популяциями |

## Свипы

### Предмет 1 — снятые термины

| Снятая редакция | Команда | Исход |
|---|---|---|
| «аудит, статистика и `bff`» как перечень потребителей | `LC_ALL=C.UTF-8 grep -rn 'аудит, статистика' docs/ .claude/` | два вхождения, оба в `contracts.md`, оба правлены; запись реестра заведена и прогнана |
| «результат до финансирования наравне с итогом» в составе события | `LC_ALL=C.UTF-8 grep -rlUzP '(?si)до финансирования наравне' docs/ .claude/ services/ libs/` | одно вхождение, правлено; запись реестра заведена и прогнана. Законные вхождения «до финансирования» у дома агрегатов шаблоном не ловятся — требуется полная фраза |
| «у события жизненного цикла определения биржевого счёта нет» / «биржи не имеет вовсе» | `LC_ALL=C.UTF-8 grep -rlUzP '(?si)(биржи не имеет вовсе\|биржевого счёта нет)' docs/ .claude/ libs/ services/` | два вхождения в `AuditRecord.md`, оба правлены. **Записи реестра не заводится, и это названо:** снятое — ошибка факта о собственной модели, а не редакция политики; пересказать факт о поле нечем, он проверяется самой моделью |

### Предмет 2 — введённая конструкция

Введены три конструкции, и каждая проверена независимо от текста правки:

1. **«имя заголовка берётся из имени поля»** — сверено по всем четырём
   построенным копиям имён (два публикатора, два потребителя):
   расхождений ноль, то есть конструкция описывает построенное, а не
   предписывает ему.
2. **«тенант заголовком не дублируется»** — сверено по единственному
   построенному потребителю, читающему тенанта (`StreamEventConsumer`):
   берёт `record.key()`; публикаторы заголовка тенанта не ставят.
3. **«событие несёт признак полноты графа сделки»** — сверено с формой
   дома (`docs/spec/loss-streak-halt.json`, величина `priceResult`:
   конъюнкт полноты стои́т в ней и правкой не тронут), с нотами обоих
   слагаемых (ни у итога, ни у финансирования своей ветви доступности не
   появляется — их дом остаётся верным дословно) и с достижимостью ветви
   (`docs/components/MarkDealEmergencyClosedExecutor.md` §«Признаки
   отбора»: «он приходит на усечённом»). Проверка поймала **первую**
   редакцию этой конструкции: пустота слагаемого вместо признака заводила
   слагаемому ветвь доступности, которую его дом отрицает дословно, — то
   есть закрытие правило бы копию, расходясь с домом. Редакция снята и
   заменена операндом формы.

### Свип структурных носителей

Правка **вводит компоненты содержимого** (две идентичности у
`StrategyActivated`) и **снимает один** (`priceResult` у `DealClosed`),
поэтому пройден фиксированный перечень:

| Носитель перечня | Исход |
|---|---|
| §Структура доменной модели | не применим: содержимое события — не доменная модель, состав объявляется домом контракта |
| §Персистентность | **не тронут намеренно**: колонки радиуса журнала уже объявлены, состав их не меняется — меняется то, откуда они заполняются |
| schema-дельта шага | не тронута: позиции 6, 7, 9а состава колонок не меняют |
| состав `*ExternalSnapshot`, used/unused инвентаря источника | не применим: предмет не биржевой |
| mapping-таблицы | не применим: содержимое события маппером не проходит |
| инвентарь читателей у владельца данных | **пройден**: читателей содержимого два — журнал (кладёт как доставлено) и джоба пересчёта (читает поимённо); обоим правка добавляет два компонента и убирает один |
| контракт вызова у создателя сущности | **пройден**: создатель — писатель события; оба терминальных исполнителя и код перехода определения в `ACTIVE` названы позициями дельты `CODE` (2, 16а) |

## Состояние узла: ОСТАНОВЛЕН по С5

Правки лежат в рабочем дереве и **в корпус не приземлены**. Всё ниже
описывает работу, доведённую до остановки, а не вошедшую в корпус.

**Выбор сделан 2026-09-08 (`PROC-Q8` = (а)):** приземление идёт **после**
цельного прохода по механической проверке самоописания правки
(`.claude/decisions/edit-self-description-pass.md`); третьей доработки нет.

## Исход мини-петли

- **раундов критики — 1**, **верификаций — 2** (потолок исчерпан);
- **раунд 1 — 14 находок, 7 блокирующих** правки, идущие в корпус (К1-К7);
  все семь закрыты **до** приземления, шесть негейтящих (К8-К14) приняты
  тем же ходом, ни одна не отложена;
- **верификация 1 — 8 находок, 5 блокирующих** (V1-V5); все пять закрыты
  доработкой, три негейтящие (V6-V8) приняты тем же ходом;
- **верификация 2 — 8 находок, 1 блокирующая (W1)**: комментарий записи
  реестра снятых редакций объявляет пришедшей ту редакцию, которую
  верификация 1 отвергла;
- **потолок исчерпан ⇒ сработал сигнал С5**
  (`.claude/decisions/mini-loop-verification-ceiling.md`): третьей
  доработки нет, **узел остановлен и в корпус не приземлён**. Эскалация с
  диагнозом посылки — `phase-2-step-10-gaps-close-4.md` §«Эскалация:
  концептуальный дефицит (сигнал С5, узел 1)»;
- **объявленные остатки — четыре**, все названы поимённо в §2 эскалации
  (W1, W2, W3, W8).

Отчёты заходов — `phase-2-step-10-gaps-close-4-node-1-critique.md`,
`phase-2-step-10-gaps-close-4-node-1-verification.md`,
`phase-2-step-10-gaps-close-4-node-1-verification-2.md`.

### Чему научил узел: блокирующее приходило не от конструкции, а от ЧИСЛА, ЧУЖОГО ДОМА и САМООПИСАНИЯ

Из тринадцати блокирующих трёх заходов (7 + 5 + 1) ни одна не опровергла
**конструкцию** узла — все четыре находки закрыты тем родом действия,
который им назначен уровнем пробела. Опровергались три других класса, и
третий стал предметом эскалации С5:

- **число и равенство множеств, написанные правкой** (К1, К2, К5, V5,
  V6): столкнутые «пятёрки» разных множеств, объявленное равенство
  перечней шестого и пятого размера, несдвинутый счёт позиций дельты, три
  ложных числа в рамке, счёт величин из заголовков;
- **клауза, заведённая в обход чужого дома** (К3, К4, V1, V2, V3):
  снятый конъюнкт формулы, незаплаченная цена варианта, ветвь
  доступности, приписанная операнду вопреки его дому, и пассаж, чьё
  «отвергнуто» опровергается построенным кодом;
- **самоописание правки, написанное пересказом предыдущего состояния**
  (V5, W1, W5, W6, W8): исход захода, объявленный до его прогона; строки
  популяции, описывающие исход снятой редакцией; и — блокирующее —
  запись реестра, называющая пришедшей отвергнутую редакцию. Диагноз
  этого класса и вынос по нему — `phase-2-step-10-gaps-close-4.md`
  §«Эскалация: концептуальный дефицит (сигнал С5, узел 1)».

Отсюда две конвенции для следующих узлов. **Первая:** всякое число и
всякое равенство множеств, написанное правкой, пересчитывается по своему
перечню **до** того, как правка уходит критику. **Вторая:** клауза,
меняющая **область определения** чужого операнда (когда он пуст, когда
доступен), приземляется только вместе с правкой его дома — либо не
приземляется вовсе; проверяется это открытием ноты операнда, а не чтением
формулы, в которую он входит.

## Дефекты рационала (не держат приземления)

1. Сверка причин недостижимости О3 с популяцией спеки актора сделана
   чтением; машиной она не проверяется (названо в самой О3).
2. Ложные срабатывания шаблона О1 названы поимённо, но шаблон не сужен —
   повторный прогон даст те же три лишних имени.

## Припаркованная позиция

**Тождество «операнд `priceResult` есть сумма двух приезжающих под
конъюнктом полноты» не исполняется ни одним примером спеки агрегатов.**

- **Дом** — `.claude/work/backlog.md` §«Исполнимость формы ценового
  результата на строках агрегатов».
- **Что оживит** — ход, приводящий операнды спеки агрегатов к операндам
  дома (единственный, при котором `includes` перестаёт стоить перебора
  всех примеров); ближайший такой ход — `CODE` шага 10, где форму
  исполняет джоба пересчёта.
- **Причина** — маршрут `includes` требует свести операнды спеки к
  операндам дома по всем её примерам, и цена перевешивает (Д1092).
- **Остаток не введён этим узлом:** до правки операнд подавался готовым
  числом ровно так же, и тождество не исполнялось ни тогда, ни сейчас.

## Связи

- Закрытие целиком — `phase-2-step-10-gaps-close-4.md`.
- Находки — `phase-2-step-10-docs-check-4-lens-a.md` (A1, A6),
  `-lens-b.md` (B4, B5).
- Популяция — `.claude/skills/closure-population.md`.
- Уровни пробела — `.claude/skills/classify-gap-level.md`.
