# Узел 1 `GAPS_CLOSE_5` — текст правок до приземления

## На какой вопрос отвечает этот файл

Какой текст узел 1 закрытия `GAPS_CLOSE_5` предлагает внести в корпус.

Файл — **предмет критики и верификации**, а не носитель корпуса: правки
лежат здесь до заходов мини-петли и приземляются после них
(`.claude/processes/roadmap-step-execution.md` §«Закрытие гейтящего узла
проходит мини-петлю критики до приземления в корпус»). Рамка узла, правило,
уровень, популяция и исходы заходов — в
`phase-2-step-10-gaps-close-5-node-1.md`.

**Редакция четвёртая — приземлённая.** Текст ниже равен тому, что
приземление внесло в корпус: одиннадцать находок второй верификации (Ю1-Ю11)
сняты правкой этого файла, и добавлена правка 3в, которой требует Ю2. Что
именно каждая находка изменила — таблицей в
`phase-2-step-10-gaps-close-5-node-1.md` §«Диспозиция находок второй
верификации». Прежние редакции здесь не сохраняются: файл — рабочий, и его
предмет — приземлённый текст, а не история его редакций.

## Правка 1 — `docs/architecture/contracts.md` §События, названное ограничение состава

**Место.** Сразу после пассажа «Решение о заявке несёт идентичность
транша» и перед «У классов с ручной тропой содержимое несёт актора» — то
есть после последнего требования состава и перед образцом формы, которую
правка распространяет.

**Текст:**

> **Часть требований состава построенные записи не несут — это названное
> ограничение, а не умолчание.** В отличие от ограничения абзацем выше,
> это — **о составе события**, а не о модели ядра. Не несут — поимённо:
> терминальное событие сделки не везёт ни операндов отчёта, ни признаков
> отбора — из них едут только итог и его расчётная валюта, а идентичность
> определения среди невезомых; событие создания сделки не везёт ни фазы
> входа, ни идентичности определения; решение о заявке не везёт
> идентичности транша.
> **Дом** — эта секция, **оживитель** — под-шаг `CODE` шага 10 фазы 2
> (состав содержимого), **причина** — та же, что у поля актора ниже:
> состав приезжает одним ходом с версией формы, и версия поднимается один
> раз на весь состав. Ни одно требование при этом не сужается: сузить его
> значило бы отдать читателю неверный исход вместо пустого.
>
> **Цена названа на откладывающем, и она НЕ «строк не будет».** Сделочное
> зерно агрегатов до оживителя **собирается — и собирается ложным**:
> определение стои́т ключом зерна, а пустота ключа объявлена значением
> «сделка стратегии не имеет» (`docs/rules/statistics-aggregates.md`);
> прочие ключи зерна событие несёт — счёт и расчётную валюту содержимым,
> тенанта и момент конвертом, — поэтому строка выходит правдоподобной, а
> считается в ней **один счётчик закрытых сделок**: денежные суммы
> отбираются предикатом, чьих операндов — признака принятия риска и
> результата до финансирования — событие не везёт. Тем же
> механизмом колонка радиуса определения получает у сделок, заведённых по
> объявлению, **не пробел, а ложное значение**
> (`docs/models/domain/other/AuditRecord.md`). Заявки при нескольких
> уровнях входа к траншам неатрибутируемы. Все эти величины
> **невосстановимы задним числом** — тем же доводом, каким они и объявлены
> операндами: строки за период уже накоплены без них, и дочитать их
> разрешённой строкой некому.

## Правка 1б — `docs/architecture/contracts.md`, какая половина пары на каком классе

**Место.** Клауза «**Требование стои́т на ОБОИХ классах сделки**, не только
на терминальном» пассажа «Событие создания сделки несёт контекст входа — фазу
рынка и идентичность определения» — **переписывается на месте**, а не
дополняется хвостом (Ю6): хвост оставил бы рядом первую редакцию клаузы, то
есть завёл бы слой опровержения (`docs/concept.md` §«Опровержение правит на
месте»).

**Почему правка нужна.** Клауза «**Требование стои́т на ОБОИХ классах
сделки**» половин пары не различает, и оба прочтения правдоподобны. Узкое
прочтение установлено **соседним перечнем**: состав терминального события
называет идентичность определения и фазы входа не называет. Развилка
поэтому не выносится, а предъявляется: выбор между прочтениями делает не
эта правка, а перечень, который стои́т в доме выше неё.

**Текст (клауза целиком, взамен нынешней):**

> **Требование стои́т на обоих классах сделки, и половины пары на них
> разные.** Идентичность определения несут оба класса — без неё отчёт по
> определению не собирается с момента входа; фаза входа остаётся атрибутом
> входа и едет с событием создания. Перечень состава терминального события
> выше называет определение и фазы не называет; второй записи фазы на
> терминале не заводится, и потребителя, который читал бы её там, среди
> объявленных нет (`docs/rules/audit-not-runtime-source.md`).

## Правка 2 — `docs/architecture/contracts.md` §«Конверт события»

**Место.** Сразу после пассажа «Дом формы» — то есть буквально рядом с
клеймом «поля ниже — его полный состав», к которому ограничение и относится.
Место «после таблицы» отвергнуто (Ю4): оно рвало бы антецедент «Абзац выше
называет род упаковки» у соседнего пассажа о проводе — тот разрешался бы во
врезку, рода упаковки не называющую.

**Текст:**

> **Построенный класс конверта дискриминатора не несёт — это названное
> ограничение, а не умолчание.** Класс держит остальные поля таблицы, и
> его набор — **не тот**, который потребитель журнала объявляет
> обязательным: у класса недостаёт ровно класса события, а стои́т вместо
> него контекст трассировки, обязательным не объявленный и законно пустой
> (`docs/models/domain/other/AuditRecord.md` — дом перечня обязательных).
> Оба писателя outbox собирают конверт без класса события и ставят его на
> строку outbox отдельным вызовом; контекст трассировки остаётся у них
> пустым — законно, и это объявлено ниже. **Дом** — эта секция,
> **оживитель** — под-шаг `CODE` шага 10
> фазы 2 (состав конверта), **причина** — та же, что у состава
> содержимого.
>
> **Что из этого следует до оживителя.** Потребитель, принимающий класс за
> исполнимую запись формы, дискриминатора в нём не найдёт: javadoc класса
> называет его такой записью, и javadoc правится ходом оживителя. На
> проводе разрыва нет — как объявлено ниже, класс события едет колонкой
> строки outbox и заголовком, как прочие поля конверта, кроме тенанта: он
> ключ записи, —
> поэтому «полный состав» выше остаётся нормой **формы**, а расходится с
> ней носитель формы.

## Правка 3 — `docs/models/domain/other/AuditRecord.md`, указатель у колонки радиуса

**Место.** Хвостом пассажа «Отсюда требование к производителю, а не
исключение у читателя» — там, где разобран и закрыт тот же механизм у
активации определения.

**Текст:**

> **Тем же требованием сегодня расходятся и классы сделки:** идентичности
> определения не несёт компонентом верхнего уровня ни событие создания
> сделки, ни терминальное, и колонка получает у обоих ложное значение по
> тому же механизму. Названное ограничение живёт в доме состава
> содержимого (`docs/architecture/contracts.md`), и второй его записи
> здесь не заводится.

**Второй абзац редакции третьей снят (Ю1).** Он адресовал носитель именем
захода («правкой 3б выше по этому же файлу») — в корпусе такого носителя нет,
а сам оборот есть разговор о редакционном решении, а не знание. Остаток
пустоты после оживителя разобран там, где живёт сам инвариант, и указателя
на это не требуется.

## Правка 3б — `docs/models/domain/other/AuditRecord.md`, однозначность пустоты правится НА МЕСТЕ

**Место.** Пассаж «Значение колонки пусто, когда содержимое идентичности не
несёт» — сам инвариант, а не хвост соседнего пассажа.

**Почему правка нужна.** Пустота колонки радиуса у восстановленной сделки
законна и после оживителя: класс определение производит, а у этой сделки его
не было. Инвариант же сформулирован без этой ветви — «класс события её не
производит по построению», — и после оживителя стал бы ложным без единого
хода по файлу. Дописать ветвь хвостом соседнего пассажа значило бы завести
**слой опровержения** — второй носитель того же факта с отрицанием, — что
запрещено `docs/concept.md` §«Опровержение правит на месте». Поэтому пассаж
переписывается на месте.

**Текст (пассаж целиком, взамен нынешнего):**

> **Значение колонки пусто, когда содержимое идентичности не несёт**, и
> пустота однозначна: **либо класс события идентичности не производит, либо
> предмет строки её не имеет** (`docs/rules/absent-value-semantics.md`).
> Первое — события жизненного цикла определения, у которых нет **сделки**:
> счёт и инструмент определение называет всегда, а сделки у него не бывает
> по предмету. Второе — сделка, заведённая восстановительной тропой: класс
> определение производит, а у этой сделки его не было
> (`docs/models/domain/aggregate/Deal.md`), и пустота означает «сделка
> заведена вокруг живого риска». Обе ветви — **значение**, и различает их
> предмет строки, а не наличная форма содержимого: класс, у которого
> идентичность есть **у предмета**, а компонента верхнего уровня нет, даёт
> не пробел, а ложное значение.

## Правка 3в — `docs/models/domain/other/AuditRecord.md`, однобранчевая равносильность десятью строками выше

**Место.** Оговорка применимости в пассаже «Колонками выходят ровно те
идентичности…» — «колонка пуста **ровно тогда**, когда класс события
идентичности не производит».

**Почему правка нужна** (Ю2). Правка 3б вводит вторую ветвь пустоты, а эта
равносильность однобранчева и после приземления стала бы ложной — причём
расхождение вносит сама правка, в том же файле. Двух записей одного
инварианта не бывает (`.claude/rules/policy-home.md`), поэтому оговорка
сводится к указателю на инвариант, а состав ветвей живёт у него одного.

**Текст (хвост оговорки, взамен нынешнего):**

> и оговорка применимости сохранена; чем пустота колонки однозначна и какие
> у неё ветви — ниже, у самого инварианта.

## Правка 4 — `docs/rules/statistics-aggregates.md`, указатель у дома состава чисел

**Место.** **После** таблицы величин, замыкающей пассаж «Операнд агрегата
— поле СОДЕРЖИМОГО события, а не поле чужой базы», и **выше** клейма
«Признаки отбора едут в терминальном событии»: иначе читатель встречает
клейм раньше ограничения. Довод редакции третьей («вставка перед таблицей
разорвала бы указатель „перечень несчитаемого — ниже“») неверен и снят
(Ю8): перечень живёт в §«Чего агрегаты не считают» четырьмя сотнями строк
ниже, и абзац между указателем и таблицей его адресности не двигает.

**Текст:**

> **Сегодня построенное содержимое терминального события операндов этого
> зерна не везёт, кроме итога и его валюты, и зерно от этого не пропадает,
> а собирается ложным:** определение — ключ зерна, его пустота объявлена
> значением, и строка выходит под ключом «сделка стратегии не имеет».
> Названное ограничение живёт в доме состава содержимого
> (`docs/architecture/contracts.md`) — там же его оживитель и причина;
> правило выше этим не отменяется и исключения не заводится. Второе зерно
> — происшествий — на построенном содержимом собирается: его операнды
> едут.

## Правка 5 — хроника шага, §«Дельта `CODE`, накопленная шагом»

**Место.** Позиции 1, 3 и 14 таблицы дельты.

**Почему правки дословны** (Ю11). Редакция третья описывала форму строк
словами, тогда как прочие правки давали цитату; дословный текст обеих
строк — ниже.

**Позиция 1 (взамен нынешней строки):**

> `| 1 | `eventType` в классе `EventEnvelope` — поле есть в заголовках и
> колонкой строки outbox, но не в форме; **тем же ходом правится javadoc
> класса**, называющий его исполнимой записью объявленной формы |
> `GAPS_CLOSE_1`, A8; javadoc — `GAPS_CLOSE_5`, узел 1 (B4) |`

**Позиция 14 (взамен нынешней строки — дописано происхождение сведённой):**

> `| 14 | `entryMarketPhase` **и** идентичность определения в
> `DealOpenedContent` | часть 2, Р9-5, Р9-5б; **сюда сведена позиция 3** —
> `GAPS_CLOSE_1`, A4 |`

**Строка снятия (добавляется внизу таблицы, следом за строкой снятой
позиции 5):**

> `| — | **снята** позиция 3 (`strategyInternalId` в `DealOpenedContent`):
> её предмет целиком содержится в позиции 14, которая заводит в том же
> классе и фазу входа, и идентичность определения; перенумерации нет — она
> сломала бы ссылки на позиции | `GAPS_CLOSE_5`, узел 1 (B2) |`

**Форма снятия — та же, что у уже снятой позиции 5:** строка позиции 3
остаётся на месте, а снятие объявляется отдельной строкой с индексом `—`.
Дубль входил в порождённый блок счёта, то есть число членов описывало
перечень с задвоенной позицией.

Блок счёта после правки **пересчитывается прогоном**
(`py tools/self-count-check.py --emit 'rows:…'`), рукой не пишется.

## Правка 6 — `docs/rules/audit-not-runtime-source.md`, разрыв у пунктов, которые на состав опираются

**Место.** Хвостом пассажа «У каждого пункта назван носитель» §«Что аудит
фиксирует» — до перечня пунктов, которые на носитель опираются.

**Почему правка нужна.** Файл **объявляет само правило узла** («пункт
держится либо классом события с полем содержимого, либо названным
ограничением с домом, оживителем и причиной») и на трёх пунктах его не
исполняет: носителем названы четыре признака отбора, контекст входа и
идентичность транша — ровно то, чего построенные записи не везут.

**Текст:**

> **Носители, на которые опираются пункты ниже, объявлены и построенными
> записями сегодня не везутся** — признаки отбора, контекст входа и
> идентичность транша. Названное ограничение с домом, оживителем и
> причиной живёт в доме состава содержимого
> (`docs/architecture/contracts.md`), и второй его записи здесь не
> заводится; пункты от этого не слабеют — у каждого назван носитель, а
> разрыв между носителем и построенным назван там, где живёт сам носитель.

## Правка 7 — `docs/components/DealOpeningService.md`, короткое упоминание у исполнителя

**Место.** Хвостом пассажа «Отдельной записи в аудит здесь нет, и заводить
её не нужно: контекст входа уезжает СОБЫТИЕМ создания сделки».

**Текст:**

> **Построенный исполнитель пары сегодня не кладёт**, и это названное
> ограничение дома состава содержимого
> (`docs/architecture/contracts.md`), а не свойство этого дока: там же его
> оживитель и причина.

## Правка 8 — `docs/architecture/contracts.md`, ложное число в пассаже об акторе

**Место.** Пассаж «Актор полем конверта не становится, и это решение, а не
пропуск».

**Было** (дословно): «Состав выше — полный, и шестое поле версионировало бы
все классы разом ради величины, у которой на большинстве из них ровно одно
значение».

**Станет:** «Состав выше — полный, и ещё одно поле версионировало бы все
классы разом ради величины, у которой на большинстве из них ровно одно
значение». Выделения правка не вводит: его в пассаже не было.

**Почему.** В таблице состава шесть строк, актор был бы седьмым: число
неверно. Форма тоже: число без механического вывода не пишется
(`.claude/rules/self-description-form.md`), а вывода у него нет.

## Правка 9 — `docs/architecture/contracts.md`, форма ограничения у требования о пустом значении

**Место.** Пассаж «Сегодня требование нарушено, и это состояние кода, а не
спор о правиле».

**Почему правка нужна.** Пассаж есть названное ограничение того же класса,
что и правка 1, но несёт из трёх обязательных величин одну — ход; ни дома,
ни причины у него нет. Сверх того он несёт **ручной счёт** («пять
call-site'ов в трёх точках»), у которого нет механического вывода и
который с построенным не сходится.

**Текст (пассаж целиком, взамен нынешнего):**

> **Сегодня требование нарушено, и это названное ограничение, а не спор о
> правиле.** Точки публикации ядра оборачивают величины в
> `String.valueOf(...)`, и среди обёрнутых есть объявленные законно
> пустыми — примеры, которыми правило само себя иллюстрирует.
> `String.valueOf` пустого даёт литерал `"null"`, и цена счётна у
> потребителя: предикат «значения нет» становится тождественно ложным, а
> недоступное входит в суммы под видом значения
> (`docs/rules/statistics-aggregates.md`). **Дом** — эта секция,
> **оживитель** — позиция 4 дельты `CODE` шага 10 фазы 2 (та, что правит
> обёртку пустого значения), **причина** — правка идёт одним ходом с
> составом содержимого: те же исполнители ядра правятся обоими ходами.
> Счёта точек здесь нет: он стареет с каждым ходом по коду, а перечень их живёт
> у оживителя.

## Правка 10 — `.claude/work/backlog.md`, парковка адресов пассажей в javadoc

**Место.** Хвостом секции «Указатели javadoc в несуществующие доки».

**Текст:**

> **Соседний класс того же корня: адрес пассажа в javadoc, ведущий в
> НЕСУЩЕСТВУЮЩИЙ пассаж существующего файла.** `doc-pointer-check.py` мерит
> существование **файла**, `anchor-check.py` — адреса `.claude/**`; адрес
> пассажа, написанный в javadoc, не мерит ни один. Встречено при обходе
> потребителей узла 1 `GAPS_CLOSE_5` шага 10 фазы 2, в
> `libs/domain-model/.../domain/event/`: `StrategyEventType` адресует в
> `docs/architecture/contracts.md` пассаж «Формы событий и запросов» —
> слова стоя́т началом обычного абзаца, ни заголовком, ни лид-жирным
> пассажем они не являются; `OrderDecidedContent` адресует туда же пассаж
> «Содержимое несёт идентичности», а пассаж носит имя «Содержимое несёт
> идентичности и то, чего потребитель не дочитает сам» — сокращение, не
> подпадающее ни под скобочный хвост, ни под тире-разделитель. Знак
> параграфа здесь намеренно не воспроизведён: этот файл в области
> `anchor-check.py`, и цитата адреса завела бы новый неразрешимый.
> **Экземпляры названы как встреченные обходом узла, а не как объём
> класса:** команды, мерящей его, нет — её и заводит оживитель; разовая
> проба резолвером, воспроизводящим разбор `anchor-check.py`, дала на
> `libs/**`, `services/**`, `web/**` порядок в полсотни неразрешимых
> адресов из шести с половиной сотен, и это замер момента, а не поле
> пункта.
> **Что оживит** — снятие заморозки усиления измерения, то есть вход шага 10
> фазы 2 в `CODE` (`.claude/rules/docs-loop-limits.md` §«Объём измерения до
> кода — ремонт, не усиление»): ось у существующей команды до него не
> заводится, а без оси класс не мерится ничем. **Владелец** —
> `knowledge-curator`.

## Чего правки НЕ делают

- **Не сужают ни одного требования состава.** Все требования §События
  остаются; правка 1б не сужает пару, а называет, какая её половина на
  каком классе стои́т, — и называет по перечню, который в доме уже есть.
- **Не заводят нового дома.** Дом требований — §События, дом формы
  конверта — §«Конверт события»; правки 3, 4, 6 и 7 — указатели деталей на
  дом (`.claude/rules/policy-home.md`), и ни один не пересказывает
  политику. Правки 1б, 3б, 3в, 8 и 9 — правки **на месте**, в самих домах:
  они не добавляют второй редакции рядом с первой (`docs/concept.md`
  §«Опровержение правит на месте»).
- **Не вносят в `docs/**` адресов пассажей.** Ссылки смысловые, на дом
  (`docs/concept.md` §Ссылки); долг адресных перекрёстий правки не растят.
- **Не вводят счётного клейма в корпус** и **снимают уже стоящие**
  (правки 8 и 9). Счёта снимаемых здесь нет: он писался бы рукой и
  механического вывода не имеет (`.claude/rules/self-description-form.md`).
- **Не трогают кода.** Направление синхронизации до `CODE` — «ведёт док»;
  расходится исполнитель, и приводит его ход дельты.
