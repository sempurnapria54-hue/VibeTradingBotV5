# Линза G — концепция: остаток корпуса + структурная гигиена

Фокус — `concept-review` (`.claude/skills/concept-review.md`), роль `reviewer`.
Фаза 1, шаг 7 «Сделки и P&L», статус `DOCS_CHECK_26`, **до `CODE`** → ведёт док;
клейм дока о фактическом устройстве кода верифицируется грепом по `src/`.

## Охват

**Часть 1 (остаток корпуса).** Прочитаны полностью: `docs/components/` —
`CandleJob.md`, `IndicatorJob.md`, `IndicatorService.md`, `MarketStructureJob.md`,
`MarketStructureService.md`, `MarketStructureResolver.md`, `MarketPhaseService.md`,
`MarketPhaseResolver.md`, `MarketPriceDataService.md`, `MarketDataExpirationChecker.md`,
`AnomalyJob.md`, `RefreshBillsExecutor.md`, `FinalizeDealExitExecutor.md` (два
последних — по следу находки G1); `docs/models/domain/other/` — `Candle.md`,
`CandleGroup.md`, `IndicatorValue.md`, `MarketPhase.md`, `MarketStructure.md`,
`AnomalyReport.md`, `Auditable.md`, `InstrumentExternalRules.md`, `DealCashFlow.md`
(по следу G1); `docs/lifecycles/CandleGroup.md`, `docs/lifecycles/AnomalyReport.md`;
`docs/processes/candle-loading.md`, `docs/processes/market-data-calculation.md`;
`docs/dictionary/leg-term.md`; `docs/models/api/OkxRawApiRequest.md`;
`docs/rules/market-data-retention.md`, `docs/rules/market-data-freshness.md`;
`docs/models/mapping/TimeFrame.md`, `docs/models/mapping/DealCashFlow.md` (по следу G1).

**Слои-скаффолды.** `docs/models/api/` — один файл (`OkxRawApiRequest.md`).
`docs/models/persistence/` **на диске отсутствует**; `docs/models/externalSnapshot/`
**на диске отсутствует** — оба объявлены в таблице `.claude/rules/structure.md`.
Входящих ссылок на них из корпуса нет (проверено полным списком путевых ссылок),
поэтому битых адресов это не даёт; расхождение — на стороне `structure.md`.

**Часть 2 (гигиена).** Механически пройден весь корпус: 235 файлов, из них
213 `.md` + 15 `.json` (`docs/spec/`) + 7 `.gitkeep`. Проверены: раздел
«На какой вопрос отвечает этот файл» во всех 213 доках; все путевые ссылки
(185 уникальных целей); §-адреса; markdown-ссылки и якоря; процессная арматура
по восьми регулярным классам; соответствие размещения таблице `structure.md`;
полное сличение `.claude/knowledge-tree.md` с фактикой `docs/`.

**Вне охвата.** Сквозные кластеры сделки / риска / защиты / холда / стратегии /
интеграций — у других линз; здесь они затрагиваются только там, где след находки
моего охвата в них уходит (G1, G2), и это помечено.

**Стадия остановки.** Прошёл все стадии (стадия 2). Гейта стадии 0 нет:
механика рыночных данных описана, обход не останавливался.

**Об объёме.** Файл ~76 KB в байтах / ~40 тыс. знаков (кириллица — 2 байта на
знак), то есть по знакам — на ориентире `gap-report.md` (до 40 KB), по байтам —
вдвое выше. Пересказ снят: выводы грепов не вклеены (только заключение и точки
`файл:строки`), повторных форм одной таблицы нет. Превышение по байтам
составляют две вещи, которые правило защищает от урезания: мандатный набор полей
на каждой из 12 находок части 1 (включая перечни вариантов с ценой в строке —
`gap-report.md` §«Цена варианта»: перечень самонесущий, выносить его нельзя) и
разовый раздел части 2 — сплошная механическая гигиена всего корпуса, которая
в штатную структуру отчёта не входит и в ориентир не считается.

---

## Часть 1 — концепт остатка корпуса

### Находки — блокирует выход в CODE

#### G1. Пять полей строки движения объявлены, а писателя, добытчика и «лестницы деградации» у них в корпусе нет

- **Вопрос.** `DealCashFlow` несёт `appliedRate`, `rateStatus` и три координаты
  свечи курса. Ни один док не называет **достижимого** писателя этих полей, не
  описывает добычу свечи котировки, не определяет «деградацию» разрешения и не
  описывает «догон курса». П3 требует писателя у каждого значения, и писатель
  обязан быть достижим; здесь назван актор, у которого нет носителя.
- **Цитаты (носители).**
  - `docs/models/domain/other/DealCashFlow.md:27-31` — поля; `:30` — «таймфрейм.
    Фиксирует и **разрешение** — секундное либо огрублённое **деградацией**».
  - `docs/models/domain/other/DealCashFlow.md:63-72` — перечень `RateStatus`
    (`NOT_REQUIRED` / `APPLIED` / `RATE_UNAVAILABLE`) и «На состоянии курса
    ключуются область суммирования чужой валюты и **множество строк, ждущих
    догона курса**».
  - `docs/models/mapping/DealCashFlow.md:82` — таблица «Что проставляет
    вызывающий»: «`appliedRate`, `rateStatus`, три координаты свечи | **пересчёт
    чужой валюты**». Актора с таким именем/ролью в `docs/components/` нет
    (проверено листингом всех 69 компонент-доков).
  - `docs/components/RefreshBillsExecutor.md` — единственный компонент,
    объявленный писателем строк движения («маппит их в строки разбивки и
    персистит с дедупом»). Слов «курс», «свеч», «валют» в файле **нет**
    (греп пуст).
  - `docs/models/domain/other/CandleGroup.md:62-66` — «**единственный легальный
    потребитель** [`ONE_SECOND`] — координата 2 ссылки на свечу курса на строке
    `DealCashFlow`. Она свечу не хранит и группы не заводит: свеча котировки
    добывается у источника и локально не персистится
    (`docs/components/RefreshBillsExecutor.md`)» — **целевой док этой клаузы не
    содержит** (проверено чтением файла целиком).
  - `docs/rules/trading-constraints.md:94-98` — «оно персистится, линкуется и
    входит в результат эквивалентом по курсу на момент операции. Курс и исход его
    получения — поля движения».
  - `docs/components/FinalizeDealExitExecutor.md:31-32` — «Плюс слагаемое чужой
    валюты — движения, оплаченные вне расчётной валюты, **по применённому
    курсу**».
  - `grep -rn "деград" docs/` → 3 попадания: `concept.md:155` (про тихую
    деградацию, другой предмет), `CandleGroup.md:72`, `DealCashFlow.md:30` — оба
    последних **ссылаются** на лестницу, ни один её не определяет.
  - `grep -rn "догон" docs/` → 1 попадание (`DealCashFlow.md:72`): механизм
    назван, дома нет.
  - `grep -rn "rateStatus\|RATE_UNAVAILABLE\|APPLIED" docs/spec/*.json` →
    **пусто**. Закрытый перечень, на котором ключуется область суммирования,
    живёт абзацем — против `docs/concept.md` §«Исполнимая форма» («Перечень —
    данные спецификации, а не абзац»).
- **Целевой док.** Писатель + момент добычи свечи курса — § в
  `docs/components/RefreshBillsExecutor.md` либо новый компонент-док; лестница
  деградации разрешения — `docs/rules/pnl-reconciliation.md` либо отдельное
  правило; перечень `RateStatus` и предикат области суммирования —
  `docs/spec/pnl-reconciliation.json` (или новая спецификация).
- **Ожидаемый владелец.** `solution-designer` (конструкция звена и лестницы);
  доля грунта «есть ли секундный бар на нужных парах» — `integrator`.
- **Трассировка.** Вход — мой охват: `CandleGroup.md` §«Енум `TimeFrame`»,
  где легальность `ONE_SECOND` держится ровно на одном потребителе. Открывал:
  `DealCashFlow.md` (модель), `mapping/DealCashFlow.md` (кто проставляет),
  `RefreshBillsExecutor.md` (объявленный писатель), `FinalizeDealExitExecutor.md`
  (потребитель слагаемого), `trading-constraints.md`, `pnl-reconciliation.md`
  (целиком), `docs/spec/pnl-reconciliation.json`, `docs/spec/cash-flow-linkage.json`.
  Грепы: «деград», «догон», «курс», «чужой валют», «rateStatus».
- **Достроенный контекст.** Посылка «добычу свечи курса делает тот же проход,
  что пишет строку» — **из носителя не выведена**: она правдоподобна по
  «Линковка выполняется тем же проходом, который сохранил строку»
  (`RefreshBillsExecutor.md`), но про курс там ничего не сказано.
- **Варианты.**

  | вариант | что делает | названная цена |
  |---|---|---|
  | 1. Достаточность существующего: писатель — `RefreshBillsExecutor`, курс добывается тем же проходом | § в существующем доке; `rateStatus` резолвится синхронно, `RATE_UNAVAILABLE` — терминальное состояние строки | след минимальный (правка одного дока + спеки). Цена: проход добычи получает вторую ответственность за состояние (свеча котировки) — трение с `docs/rules/ack-not-runtime-truth.md` §атомарность команды; контекстная связность — один call-site |
  | 2. Отдельное звено «пересчёт чужой валюты» (компонент + своя строка исполнения) | добыча курса — своя команда со своими повторами; «догон курса» получает носитель и триггер | след: сущность/компонент + строка исполнения. Цена: +1 компонент, +1 тропа идемпотентности; выигрыш — «догон» становится выразим, а не остаётся словом. Контекстная связность выше: два места обязаны знать порядок |
  | 3. Крен: вариант 1 **плюс** лестница деградации отдельным правилом | писатель минимальный, а огрубление разрешения (секунда → минута → …) описывается один раз для всех потребителей | след: § правила + ветка спеки. Счётная разница против «не описывать»: без лестницы `appliedRateCandleTimeframe` не отличает «секунда» от «огрублено», то есть П3-разбор («откуда операнд взят») невосстановим — ошибка наблюдаемости, а не удобства |
  | 4. Сузить продуктовый scope: чужая валюта запрещена жёстко, поля не заводятся | снимает весь узел | цена — **продуктовое решение владельца продукта** (дефицит `подтверждение`); противоречит `trading-constraints.md:94` «число не завышается молча» |

  **Крен — вариант 3.** Довод по одной оси (наблюдаемость): без лестницы поле
  разрешения не различает добытое и огрублённое, и П3-вопрос «каково было
  значение операнда и откуда взят» не имеет ответа в данных. Вариант 2 не
  отвергнут: он единственный даёт носитель «догону курса»; если догон остаётся
  в конструкции — вариант 2 обязателен, и тогда 3 становится его частью, а не
  альтернативой.
- **Ярлык исхода.** `варианты-с-креном`.
- **Ярлык дефицита.** `работа` (конструкция звена и лестницы) + `грунт`
  (доступность секундного бара — `integrator`). Половина «запрещать ли чужую
  валюту вовсе» — `подтверждение`, но она не едет за компанию: без её ответа
  варианты 1-3 остаются в силе.
- **Пересечение линз.** Узел лежит на границе моего охвата (рыночные данные,
  `TimeFrame`) и охвата P&L-линзы (`DealCashFlow`, сверка). Внесён сюда, потому
  что вход — `CandleGroup.md`; если P&L-линза подняла тот же узел, находки
  сводятся в одну развилку (общий ход правки).

#### G2. Клейм «открытое предусловие `CODE` п. 5» указывает дом, который его не содержит; перечня предусловий в корпусе нет

- **Вопрос.** Два дока обусловливают поведение маппера открытым предусловием и
  адресуют его `docs/rules/pnl-reconciliation.md`. Целевой док никакого перечня
  предусловий, никакого «п. 5» и вообще ничего про секундное разрешение не
  содержит. Битая ссылка **скрывает отсутствующую политику**: проверить, снято
  предусловие или нет, по корпусу нельзя, а от этого зависит, компилируется ли
  ветка маппера и не подменяется ли секунда минутой молча.
- **Цитаты (носители).**
  - `docs/models/domain/other/CandleGroup.md:68-72` — «**Доступность секундного
    разрешения у источника — открытое предусловие** (`integrator`, предусловие
    `CODE` п. 5, `docs/rules/pnl-reconciliation.md`). Решением оно **не
    закрыто**…».
  - `docs/models/mapping/TimeFrame.md:33-40` — «какой бар секундного разрешения
    (и есть ли он вовсе на нужных парах котировки) — открытое предусловие `CODE`
    п. 5 (`docs/rules/pnl-reconciliation.md`). До ответа строка в
    `util.Constants.Okx` не заводится, а ветка маппера при вызове с `ONE_SECOND`
    бросает».
  - `docs/rules/pnl-reconciliation.md` — прочитан целиком; грепы «секунд»,
    «разрешен», «предусловие», «п. 5», «курс» — **пусто**. Ближайший по смыслу
    §«Разведочный режим допуска» говорит про калибровку допуска, не про
    разрешение свечи.
  - Второй экземпляр того же класса: `docs/models/mapping/DealCashFlow.md:72-73`
    — «Отображение типов источника в категории — **предусловие запуска**: пока
    оно не наполнено, вся популяция попадает в принимающую корзину» —
    предусловие без носителя и без владельца.
- **Целевой док.** Либо § в `docs/rules/pnl-reconciliation.md` (перечень
  предусловий запуска: предмет, владелец, признак снятия), либо адрес чинится на
  реальный носитель, если он существует вне `docs/`; во втором случае —
  смысловая ссылка, а не «п. 5» (`docs/concept.md` §«Ссылки»: адресные
  перекрёстья не используются).
- **Ожидаемый владелец.** `integrator` — первым (факт источника и владение
  предусловием); `knowledge-curator` — починка адреса после ответа.
- **Трассировка.** Вход — `CandleGroup.md` §«Енум `TimeFrame`». Открывал:
  `mapping/TimeFrame.md`, `rules/pnl-reconciliation.md` (целиком),
  `docs/spec/pnl-reconciliation.json`, `mapping/DealCashFlow.md`.
- **Варианты.**

  | вариант | что делает | названная цена |
  |---|---|---|
  | 1. Достаточность существующего: адрес чинится на смысловую ссылку, содержание предусловия дописывается в целевой док | ссылка перестаёт быть битой, статус предусловия становится читаемым | правка клаузы в двух доках + § в одном. Цена ≈ 0 по следу; складывается из трёх правок, проверяется грепом «предусловие» |
  | 2. Завести один носитель «предусловия запуска» (перечень: предмет, владелец, признак снятия) | закрывает и `TimeFrame`, и «отображение типов в категории» одним ходом | § правила либо новый док. Цена: +1 носитель; выигрыш — второй экземпляр класса уже существует, то есть это не единичный случай |
  | 3. Снять предусловие фактом источника (`integrator` сверяет и отвечает) | предусловия не остаётся вовсе | цена — работа контура тестов источника; не отменяет 1-2 для второго экземпляра |

  **Крен — вариант 2 плюс 3.** Сработал признак «однотипные per-случайные ноты
  у нескольких артефактов сразу» (`concept-review` §Детекторы свипа, «механика не
  определена — сперва проверить на незаписанный принцип»): два разных дока
  независимо изобрели формулу «предусловие запуска / `CODE` п. N» без общего
  носителя.
- **Ярлык исхода.** `варианты-с-креном`.
- **Ярлык дефицита.** `грунт` (факт источника — `integrator`).

### Находки — прочее

Полный набор полей на каждой; поля «варианты» и «дефицит» пустыми не оставлены.

#### G3. `docs/components/MarketPhaseService.md` — раздел `## Назначение` пуст

- **Вопрос.** Заголовок без тела; предмет компонента не назван, есть только
  контракт метода и поведение при устаревании. Единственный пустой раздел в
  корпусе (сплошной скриптовый проход по всем 213 докам).
- **Цитаты.** `docs/components/MarketPhaseService.md:7-9`.
- **Целевой док.** Он же.
- **Ожидаемый владелец.** `knowledge-curator`.
- **Трассировка.** Обход компонент рыночных данных; подтверждено проходом
  «заголовок `## `, следом сразу другой `## ` без непустых строк между ними».
- **Варианты.** Развилки нет — дописать назначение либо снять пустой заголовок.
- **Ярлык исхода.** `выводимо`. **Дефицит.** Нет — закрыто прогоном.

#### G4. Открытый вопрос владельца оркестрации и «запаркованное» живут в доменном процессном доке

- **Вопрос.** `candle-loading.md` несёт §«Владелец оркестрации — открытый
  вопрос», §«Глубина под прогрев… (**запарковано**)» и описание провизорного
  состояния кода. `docs/concept.md` §«Доменный док» относит «в каком статусе
  находится соседний вопрос» к тому, чего доменный док **не содержит**; П4
  запрещает процессную арматуру.
- **Цитаты.** `docs/processes/candle-loading.md:70-76` (запарковано);
  `:77-90` (открытый вопрос + «В коде шага 1 `CandleJob` несёт **провизорную**
  координацию готовности (`refreshInstrumentReadiness`…)»); `:105` (§Связи:
  «Владелец оркестрации не назначен — вопрос открыт»).
- **Верификация клейма о коде.** Клейм **истинен**:
  `src/main/java/com/example/tradingbot/domain/jobs/CandleJob.java:61,86` —
  `refreshInstrumentReadiness()` существует и вызывается из тика. Дефект в
  **жанре** записи, не в факте.
- **Целевой док.** Вопрос — в `.claude/work/questions/open-questions.md`; в
  `candle-loading.md` остаётся описание того, что есть, без статуса.
- **Ожидаемый владелец.** `knowledge-curator`.
- **Трассировка.** Чтение процесса целиком; греп «открытый вопрос | запарковано |
  провизорн» по `docs/`; греп `refreshInstrumentReadiness` по `src/`.
- **Варианты.** Развилки нет (перенос статуса в рабочие файлы механический).
- **Ярлык исхода.** `выводимо`. **Дефицит.** Нет — закрыто прогоном.

#### G5. `market-data-calculation.md` держит роадмап-статусы соседних шагов

- **Вопрос.** Процессный док рассуждает о том, что материализуется на шаге 5,
  что «не материализуется на шаге 3», и что «семантика activate (422) отложена
  до зрелости поздних шагов (4/7)». Это статус соседнего вопроса, а не знание
  процесса.
- **Цитаты.** `docs/processes/market-data-calculation.md:14-16`, `:32-38`,
  `:96-108`.
- **Целевой док.** Он же (вычистить); статусы — в `.claude/work/roadmap/`.
- **Ожидаемый владелец.** `knowledge-curator`.
- **Трассировка.** Чтение процесса; греп роадмап-шагов по корпусу (часть 2,
  класс A).
- **Варианты.** Развилки нет.
- **Ярлык исхода.** `выводимо`. **Дефицит.** Нет — закрыто прогоном.

#### G6. `MarketStructure.md` — четыре класса дефекта в одном доке

- **Вопрос.** Док одновременно несёт: (а) две ссылки на самого себя, (б)
  оборванный внутренний адрес, (в) слой опровержения с битой атрибуцией, (г)
  номера находок прошлых прогонов и роадмап-пометы.
- **Цитаты.**
  - (а) `:108-109` — «извлекая их из готового `IndicatorValue` (fork-A —
    `docs/models/domain/other/MarketStructure.md`)»; `:188-191` — «(см.
    `docs/models/domain/other/MarketStructure.md` случай идентичности)». Обе
    ссылки указывают на файл, в котором стоят; ни заголовка, ни лид-жирного
    пассажа «случай идентичности» в нём нет (проверено чтением файла целиком).
  - (б) `:164-166` — «точка отсчёта свежести — `windowEndAt`, не `confirmedAt`
    (см. хранения)» — адрес оборван (по смыслу — §«Правила хранения»).
  - (в) `:16-18` — «реестр `market_structure_configs` **убран ревизией трек D**
    (см. `docs/rules/market-data-freshness.md`)». Целевой док (прочитан целиком)
    реестра не упоминает; сам оборот — слой опровержения, запрещённый
    `docs/concept.md` §«Опровержение правит на месте».
  - (г) `:108` fork-A; `:112` D3; `:119-121` «хвост пользователя» / «провизорны»;
    `:123`, `:155` `CODE`; `:88` «зафиксирована на `CODE` шага 3».
- **Целевой док.** Он же.
- **Ожидаемый владелец.** `knowledge-curator`.
- **Трассировка.** Чтение дока целиком; скрипт self-reference по всему корпусу;
  чтение `market-data-freshness.md` целиком для проверки атрибуции.
- **Варианты.** Развилки нет по (а), (б), (г). По (в) крена нет: действующая
  формулировка «Результат ключуется настройкой-владельцем» в доме **есть**
  (`docs/rules/market-data-freshness.md` §Правило), так что после снятия слоя
  опровержения указатель останется корректным.
- **Ярлык исхода.** `выводимо`. **Дефицит.** Нет — закрыто прогоном.

#### G7. `IndicatorValue.md` и `MarketPhase.md` — тот же класс

- **Вопрос.** Те же классы, распределённые по двум докам, плюс открытый торговый
  вопрос без дома.
- **Цитаты.**
  - `docs/models/domain/other/IndicatorValue.md:14` — «реестр `indicator_configs`
    **убран ревизией трек D** (см. `docs/rules/market-data-freshness.md`)»
    (атрибуция битая — как в G6(в)); `:47` — «(введён **fork A** — …)»; `:90` —
    «**Адресный компонент в условии (D1).**».
  - `docs/models/domain/other/MarketPhase.md:16` — self-reference «(…ни одна →
    `UNKNOWN`; см. `docs/models/domain/other/MarketPhase.md`)»; `:30` — «Точный
    состав полей runtime-значения — деталь реализации (`CODE`)»; `:57-58` —
    «(`docs/rules/market-data-retention.md` пересмотра)» — фраза оборвана; `:62`
    — «отдельной подтверждаемости / гистерезиса фазы нет — это **открытый
    торговый вопрос**».
- **Верификация клейма о коде.** `MarketPhase` действительно не персистится:
  `src/main/java/com/example/tradingbot/domain/model/trade/market_phase/MarketPhase.java`
  — POJO без JPA-аннотаций, два поля (`instrumentId`, `type`). Клейм дока истинен.
  Побочно: javadoc того же класса ссылается на
  `docs/decisions/market-phase-stateless.md`, которого не существует (часть 2,
  входящие из `src/`).
- **Целевой док.** Оба; «открытый торговый вопрос» — в
  `.claude/work/questions/open-questions.md`.
- **Ожидаемый владелец.** `knowledge-curator` (чистка); торговая половина
  (гистерезис фазы) — `trading-specialist`.
- **Трассировка.** Чтение обоих доков; self-reference-скрипт; грепы «открыт»,
  «`CODE`», «ревизией»; чтение `MarketPhase.java`.
- **Варианты.** По чистке развилки нет. Гистерезис фазы — самостоятельный
  торговый вопрос; здесь фиксируется только то, что у него нет дома. Вклад —
  постановка.
- **Ярлык исхода.** `выводимо` (чистка) / `неразрешимо-владельцем` (гистерезис).
  **Дефицит.** По гистерезису — `подтверждение` (торговое поведение и
  риск-аппетит).

#### G8. Оборванные позиции ссылок: из парного адреса удалён один элемент

- **Вопрос.** В трёх местах остался синтаксический след удалённой ссылки —
  скобка с одинокой запятой; в одном — предлог без референта. Читатель не может
  установить, что удалено и было ли оно несущим.
- **Цитаты.**
  - `docs/models/domain/other/CandleGroup.md:56` — «(, `docs/rules/market-data-retention.md`)».
  - `docs/models/api/OkxRawApiRequest.md:22` — «(`.claude/decisions/source-api-target-rebase.md`,)».
  - `docs/models/integrations/okx/TickerOkxResponse.md:23` — «(`.claude/decisions/source-api-target-rebase.md`,)».
  - `docs/lifecycles/CandleGroup.md:59` — «Детали, ранее отложенные **на**,
    зафиксированы здесь.» — предлог без референта плюс слой опровержения
    («ранее отложенные»).
- **Целевой док.** Каждый из четырёх.
- **Ожидаемый владелец.** `knowledge-curator`.
- **Трассировка.** Греп `( *,` / `, *)` по всем `.md` в `docs/` + чтение мест.
- **Варианты.** Развилки нет.
- **Ярлык исхода.** `выводимо`. **Дефицит.** Нет — закрыто прогоном.

#### G9. `AnomalyJob` — инвариант «ликвидация за стопом» несёт реакцию без предиката запуска

- **Вопрос.** Док утверждает, что нарушение инварианта ведёт к перестановке
  защиты либо контролируемому выходу, и тут же снимает с себя такт проверки и
  гистерезис, пометив их «предметом шага сопровождения». `docs/concept.md` П1-2
  («Везде, где корпус говорит "инвариант", обязан существовать энфорсер»)
  требует механизма, а не намерения; сама помета — роадмап-статус в доменном
  доке.
- **Цитаты.** `docs/components/AnomalyJob.md:44-54` (весь §«Переоценка инварианта
  ликвидации»), особенно `:48-50` («у позиции, которую просто ведут, инвариант
  не переоценивался бы никогда — при том что цена ликвидации **едет**») и `:54`
  («Такт проверки, гистерезис… — предмет шага сопровождения»).
- **Целевой док.** `docs/components/AnomalyJob.md` (предикат запуска) либо
  `docs/rules/live-risk-protection.md`; статус — в роадмап, не в док.
- **Ожидаемый владелец.** `trading-specialist` (такт и гистерезис — торговая
  развилка); `knowledge-curator` — снятие роадмап-пометы.
- **Трассировка.** Чтение `AnomalyJob.md`, `risk-validator-scope.md`,
  `live-risk-protection.md`; греп «предмет шага».
- **Варианты.** Развилка есть, но она **не моего шага**: сопровождение позиции —
  не предмет шага 7. Выписываю без крена, чтобы пропуск был виден; вклад —
  постановка.
- **Ярлык исхода.** `неразрешимо-владельцем` (в горизонте шага 7).
  **Дефицит.** `подтверждение` (такт и гистерезис — величины риск-аппетита).
- **Гейт.** Не гейтит шаг 7 — зависимая: гейтит шаг сопровождения.

#### G10. Довод «Почему …» живёт в доменных доках — системно, 13 пассажей

- **Вопрос.** `docs/concept.md` §«Доменный док» перечисляет «почему выбрано так,
  а не иначе; какие были альтернативы» среди того, чего доменный док **не
  содержит**; §«Место концепции в корпусе» задаёт проверку вычитанием. По
  корпусу таких пассажей 13 в 11 файлах (заголовки `## Почему…` и лид-жирные
  `**Почему…**` вне `concept.md`), плюс родственная лексика gap-отчёта
  «Названная цена» — 7 вхождений в 6 файлах.
- **Цитаты (по моему охвату — развёрнуто, остальные точками).**
  - `docs/dictionary/leg-term.md:26-30` — «**Почему разведено именно так.** …цену
    разведения платит новый уровень, а не 200 существующих упоминаний» — довод
    плюс счёт упоминаний, то есть арматура процесса переименования.
  - `docs/models/domain/other/AnomalyReport.md:64-70` — «**Почему природа, а не
    код, служит дискриминатором.**»; `:72-77` — «**Торговое следствие —
    счётность неизвестных исходов.**».
  - `docs/models/domain/other/Auditable.md:44-48` — «Названная цена: у наследника
    без события источника два биржевых поля останутся пустыми навсегда. Это
    дешевле, чем исключение из бинарного правила.»
  - Остальные точки: `components/PriceCalculator.md:56`,
    `components/SizeCalculator.md:87`, `components/DealActiveHandler.md`,
    `components/MarkDealClosedExecutor.md:56`,
    `models/domain/core/Instrument.md:74`,
    `models/domain/other/TradeFeeRate.md:26`,
    `models/integrations/okx/AccountBillOkxResponse.md:35`,
    `processes/risk-evaluation.md:65`, `rules/deal-without-operations.md:30`,
    `rules/exit-teardown-order.md:14,28,34`, `rules/replace-not-amend.md:15,38`,
    `rules/pnl-reconciliation.md:52,77`, `rules/trading-constraints.md`.
- **Целевой док.** Адрес зависит от исхода развилки (см. варианты).
- **Ожидаемый владелец.** `solution-designer` — вопрос конструкции корпуса
  (граница «док / концепция»), а не размещения отдельного файла.
- **Трассировка.** Греп заголовков и лид-жирных «Почему» / «Названная цена»;
  чтение трёх пассажей моего охвата целиком и применение к ним проверки
  вычитанием.
- **Варианты.**

  | вариант | что делает | названная цена |
  |---|---|---|
  | 1. Достаточность существующего: пассажи проходят проверку вычитанием и остаются | если без довода знание становится неоднозначным, довод — часть знания (`concept.md` §«Место концепции в корпусе») | цена ≈ 0 правками. Цена скрытая: критерий применяется пассаж за пассажем — 13 индивидуальных суждений, механически не проверяемых |
  | 2. Вынести доводы в `docs/concept.md` | доводы в одном доме | правка 11 файлов + рост концепции. Цена: часть доводов узкопредметна (например, «почему природа, а не код, дискриминатор идемпотентности») и в ядро принципов не поднимается — концепция превратится в реестр решений, что она сама запрещает |
  | 3. Крен: применить проверку вычитанием разово и пакетом, зафиксировав исход по каждому пассажу | остаётся то, без чего знание неоднозначно; уходит то, что объясняло разовый выбор | работа на 13 позиций (+7 «Названная цена»). Счётная разница против варианта 1: без разового прохода критерий не применяется никогда, и класс воспроизводится каждой ревизией |

  **Крен — вариант 3.** Продуктовой оси здесь нет вовсе, спор только о жанре —
  значит разовый проход закрывает класс, а не отдельные находки.
- **Ярлык исхода.** `варианты-с-креном`. **Дефицит.** `работа`.

#### G11. Книжный провенанс в доменном доке

- **Вопрос.** `MarketStructure.md` §«Семантика классификации» шесть раз несёт
  конструкцию «*Источник говорит* (…) [Kaufman гл. N]». Это провенанс знания, а
  не само знание; `docs/concept.md` П4 запрещает ссылочную арматуру, а дом
  торгового первоисточника — `.claude/library/trading/`
  (`.claude/rules/structure.md`).
- **Цитаты.** `docs/models/domain/other/MarketStructure.md:130,139,146,151,160,167`
  (шесть вхождений «Источник говорит»).
- **Целевой док.** Развилка: чистка `MarketStructure.md` либо явное исключение
  для книжного провенанса в `docs/concept.md` §«Ссылки».
- **Ожидаемый владелец.** `trading-specialist` первым (ценность провенанса для
  торговой сверки), затем `knowledge-curator`.
- **Трассировка.** Чтение §«Семантика классификации» целиком; сверка со строкой
  `.claude/library/trading/distilled/` в `.claude/rules/structure.md`.
- **Варианты.**

  | вариант | что делает | названная цена |
  |---|---|---|
  | 1. Снять провенанс, оставить правила | док становится чистым знанием | цена: теряется трассируемость семантики к корпусу; при этом `distilled/` требует указателя книга+глава+страница — то есть указатель нужен, но с другой стороны |
  | 2. Оставить как есть, записать исключение в концепции | легализует книжный провенанс в доменных доках | цена: исключение в П4 открывает форму для любых «источник говорит», включая процессные |
  | 3. Крен: тезисы уезжают в `.claude/library/trading/distilled/`, док ссылается смысловой ссылкой на дистиллят | провенанс в своём доме, док чист | правка одного дока + наполнение дистиллята; цена контекстной связности — читатель `MarketStructure.md` обязан знать про второй носитель |

  **Крен — вариант 3**, с оговоркой: он опирается на посылку, что дистиллят по
  Kaufman заведён. **Посылка не проверена** — содержимое `.claude/library/` вне
  моего охвата.
- **Ярлык исхода.** `варианты-с-креном`. **Дефицит.** `работа`.

#### G12. `OkxRawApiRequest.md` — DTO проверочного контура в продуктовом слое api

- **Вопрос.** Док описывает конверт generic raw-passthrough, чей единственный
  назначенный потребитель — контур тестов API источника («контур проверяет
  контракт биржи, не наш слой»). `.claude/rules/structure.md` относит знание
  проверочной деятельности к `.claude/tests/{testType}/{source}/`, а
  `docs/models/api/` — к «Request/response DTO нашего API».
- **Цитаты.** `docs/models/api/OkxRawApiRequest.md:5-7` (вопрос файла), `:20-24`
  (назначение и ссылка на `.claude/decisions/source-api-target-rebase.md`),
  `:26-32` (§Связи: `OkxProxyController`, `OkxRestClient.dispatch`,
  `.claude/rules/structure.md`).
- **Верификация клейма о коде.** Клейм истинен:
  `src/main/java/com/example/tradingbot/api/controller/OkxProxyController.java` и
  `src/main/java/com/example/tradingbot/api/model/request/OkxRawApiRequest.java`
  существуют; DTO лежит в `api/model/request` — формально это **наш** api.
- **Целевой док.** Развилка размещения (остаться / переехать).
- **Ожидаемый владелец.** `knowledge-curator`.
- **Трассировка.** Чтение дока; сверка с таблицей `structure.md`; греп по `src/`.
- **Варианты.**

  | вариант | что делает | названная цена |
  |---|---|---|
  | 1. Крен: остаться в `docs/models/api/` | классификация по тому, где лежит код (наш api-слой) | цена ≈ 0; складывается из нуля правок. Цена скрытая: тип каталога чуть размывается («наш api» включает служебный эндпоинт контура) |
  | 2. Переехать в `.claude/tests/source-api/okx/` | классификация по потребителю (проверочная деятельность) | правка + перенос + обновление входящих. Цена: DTO перестаёт быть виден со стороны api-слоя, где он физически лежит |

  **Крен — вариант 1** (признак «где лежит код», а не «кто потребитель»).
- **Ярлык исхода.** `варианты-с-креном`. **Дефицит.** Нет — закрыто прогоном.

### Подтверждено-ОК

С названным уровнем сверки (`gap-report.md`: «"подтверждено-ОК" называет уровень
сверки»).

| предмет | уровень сверки | результат |
|---|---|---|
| Цепочка «свечи → индикаторы → структура → фаза» | сверка **состава звеньев и направления зависимости** между `processes/market-data-calculation.md` §«Цепочка зависимостей данных», `components/IndicatorJob.md`, `components/MarketStructureJob.md`, `components/MarketPhaseService.md`, `components/MarketPhaseResolver.md` | согласованы: фаза считается на чтение, не персистируется, наследует свежесть входов — одинаково во всех пяти носителях |
| Свежесть: формула и точка отсчёта | сверка **операндов формулы** (`referencePoint`, `expiredAt`, предикат) между `rules/market-data-freshness.md`, `components/MarketDataExpirationChecker.md`, `models/domain/other/IndicatorValue.md`, `models/domain/other/MarketStructure.md`, `docs/spec/market-data-freshness.json` | совпадают: точка отсчёта — `candleTimestamp` / `windowEndAt`, `confirmedAt` — гейт, не отсчёт; свежесть на чтение, колонкой не хранится |
| Owner-ключевание результатов | сверка **клаузы в доме против четырёх указателей** — `rules/market-data-freshness.md` прочитан целиком | клауза «Результат ключуется настройкой-владельцем: у строки ровно один владелец» **в доме есть**; указатели из `IndicatorValue.md`, `MarketStructure.md`, `MarketDataExpirationChecker.md`, `market-data-calculation.md` корректны. Термин «owner-ключевание» в доме дословно не встречается — это словоупотребление, не расхождение политики |
| Правило закрытых свечей / отсутствие look-ahead | сверка **предиката области** (что попадает в БД) между `components/CandleJob.md`, `models/domain/other/Candle.md` §«Правило закрытых свечей», `models/mapping/Candle.md`, `lifecycles/CandleGroup.md` | согласованы: `confirm=1` фильтруется на производящей стороне, признак на домене не хранится |
| Density-инвариант группы свечей | сверка **формулы и границ её применения** между `models/domain/other/CandleGroup.md` §«Целостность по count» и `lifecycles/CandleGroup.md` §«Проверка целостности» | совпадают, включая оговорку «плотность меряется на фактических границах, дотягивание до планового горизонта — забота `BACKFILL`» |
| `AnomalyReport`: две оси (критичность и природа факта) | сверка **состава перечней и предиката идемпотентности** между `models/domain/other/AnomalyReport.md` и `lifecycles/AnomalyReport.md` | согласованы построчно: `Severity` — про kill-switch, `Kind` — про «один отчёт на что»; матрица переходов и гейт терминала критичной тропы не расходятся |
| Retention результатов | сверка **предиката области** (`IndicatorValue`, `MarketStructure` — да; `MarketPhase` — нет) между `rules/market-data-retention.md` и обоими модельными доками | совпадает; якорь пересмотра назван |
| `Auditable`: бинарность набора колонок и два домена времени | сверка **состава колонок и писателя по каждой** против `rules/time-utc.md` | согласовано; правило «либо все шесть, либо ни одной» без исключений в корпусе |
| `InstrumentExternalRules`: ставка не на навесе | сверка **границ носителя** против `models/domain/other/TradeFeeRate.md` и `models/domain/core/Instrument.md` | согласовано: на навесе только `externalFeeGroupId` (ключ), ставка — своя модель, валюты — на инструменте |
| Клеймы доков о коде в моём охвате | греп по `src/`: `refreshInstrumentReadiness`, `JobController` + `POST /candle-loading/trigger`, `CandleJobFacade`, `OkxProxyController`, `MarketPhase` (не entity), `strategyIndicatorSettingId` | **все проверенные клеймы истинны**; дефектов «док утверждает факт о коде, которого нет» в моём охвате нет |

---

## Часть 2 — структурная гигиена всего корпуса

Гигиена по умолчанию **не гейтит** `CODE`. Исключение — G2 (битая ссылка скрывает
отсутствующую политику), поднято в часть 1.

### Составные вопросы файлов

Раздел «На какой вопрос отвечает этот файл» присутствует во **всех 213** доках
(пропусков 0). Разбор формулировок:

| файл | формулировка | разбор | владелец | исход |
|---|---|---|---|---|
| `docs/integrations/okx/contracts/max-size.md:5-7` | «Каков контракт операций оценки максимального размера ордера (`max-size`) **и** доступного баланса/эквити под сделку (`max-avail-size`)» | два предмета склеены через «и»: два вопроса о двух операциях, а не оси одного ответа | `knowledge-curator` | **дефект** — переформулировать до одного («контракт операций пре-оценки размера») либо разделить файл |
| `docs/lifecycles/AlgoOrder.md:5-12` | вопрос + три абзаца указателей на дома спецификаций | правило требует **одной фразы**; указатели — материал §Связи | `knowledge-curator` | **дефект формы** — вынести указатели |
| `docs/lifecycles/Deal.md:5-9` | то же | то же | `knowledge-curator` | то же |
| `docs/lifecycles/Order.md:5-12` | то же | то же | `knowledge-curator` | то же |
| `docs/rules/external-status-resolution.md:5-11` | то же | то же | `knowledge-curator` | то же |
| `docs/rules/market-data-freshness.md:5-10` | то же | то же | `knowledge-curator` | то же |
| `docs/processes/candle-loading.md:5` | «процесс добычи **и** поддержания целостности свечной истории» | одна оркестрация, один владелец, один цикл статусов; «и» соединяет фазы одного процесса | — | **ОК**, не дефект |
| `contracts/account-position-risk.md`, `contracts/position-tiers.md`, `contracts/service-urls.md` | «и» внутри перечисления состава ответа | оси ответа, не вопросы | — | **ОК** |
| `docs/rules/writer-named-for-every-value.md:5` | «писателя для каждого значения **и** перехода» | область действия одного правила | — | **ОК** |
| остальные 203 дока | — | один вопрос | — | **ОК** |

**Итого дефектов: 6** (1 составной вопрос + 5 разделов-вопросов с посторонним
содержимым).

**Побочно, вне `docs/`:** таблица `.claude/rules/structure.md` задаёт для
`docs/integrations/{name}/contracts/` вопрос «Каков контракт **и какие лимиты** у
этой операции…» — сама формулировка составная, то есть правило нарушено в
эталоне. Владелец — `knowledge-curator`; исход — переформулировать эталон, иначе
каждый новый contract-док наследует склейку.

### Битые ссылки и §-адреса

Метод: собраны **все** вхождения путей вида `docs/…`, `.claude/…`, `src/…`,
`tools/…` с расширениями `.md/.json/.java/.sh/.sql/.yml` из всех файлов `docs/`
(185 уникальных целей), каждая проверена на существование. Отдельно проверены
§-адреса, markdown-ссылки и якоря `#`. Каждый заявленный битый адрес проверен
**чтением целевого файла**, не грепом заголовков.

| ссылка | источник | цель | исход |
|---|---|---|---|
| 185 уникальных путевых ссылок | весь `docs/` | все существуют | **ОК — битых нет** |
| `§«Имя»`-адреса | весь `docs/` | `grep '§' docs/` → **0 вхождений** | **ОК**; кандидатов нет вовсе, поэтому ложных находок на лид-жирных пассажах не выдано. Согласуется с `docs/concept.md` §«Ссылки»: «Внутрифайловые адресные перекрёстья не используются» — корпус переписан под это правило |
| markdown-ссылки `[текст](путь)` | весь `docs/` | 0 вхождений | **ОК** |
| якоря `путь.md#anchor` | весь `docs/` | 0 вхождений | **ОК** |
| `docs/models/domain/other/MarketStructure.md` | `models/domain/other/MarketStructure.md:109` | сам файл; пассажа «fork-A» в нём нет | **битая** — self-reference (G6) |
| `docs/models/domain/other/MarketStructure.md` | `models/domain/other/MarketStructure.md:190` | сам файл; пассажа «случай идентичности» нет | **битая** — self-reference (G6) |
| `docs/models/domain/other/MarketPhase.md` | `models/domain/other/MarketPhase.md:16` | сам файл | **битая** — self-reference (G7) |
| `docs/rules/command-lifecycle.md` | `rules/command-lifecycle.md:74` (§Связи: «Граница "команда — действие" — …») | сам файл: дом границы указан на себя | **битая** — self-reference |
| «см. хранения» | `models/domain/other/MarketStructure.md:165-166` | адрес оборван | **битая** (G6) |
| «(, `docs/rules/market-data-retention.md`)» | `models/domain/other/CandleGroup.md:56` | первый элемент пары удалён | **битая** (G8) |
| «(`.claude/decisions/source-api-target-rebase.md`,)» | `models/api/OkxRawApiRequest.md:22` | второй элемент удалён | **битая** (G8) |
| «(`.claude/decisions/source-api-target-rebase.md`,)» | `models/integrations/okx/TickerOkxResponse.md:23` | то же | **битая** (G8) |
| «ранее отложенные **на**,» | `lifecycles/CandleGroup.md:59` | референт отсутствует | **битая** (G8) |
| `docs/rules/pnl-reconciliation.md` («предусловие `CODE` п. 5») | `models/domain/other/CandleGroup.md:69`, `models/mapping/TimeFrame.md:36` | файл существует, **обещанной клаузы не содержит** (прочитан целиком) | **битая атрибуция** → поднято в G2 как гейтящее |
| `docs/components/RefreshBillsExecutor.md` («свеча котировки добывается у источника») | `models/domain/other/CandleGroup.md:65-66` | файл существует, клаузы не содержит (греп «курс \| свеч \| валют» пуст) | **битая атрибуция** → G1 |
| `docs/rules/market-data-freshness.md` («реестр … убран ревизией трек D») | `models/domain/other/IndicatorValue.md:14`, `models/domain/other/MarketStructure.md:16-18` | файл существует, реестров не упоминает | **битая атрибуция** → G6 / G7 |
| 27 путей `docs/…` из javadoc `src/` | `src/` (144 уникальные ссылки, из них 27 битых) | целей нет: весь `docs/decisions/` (14 файлов), 7 переименованных компонент-доков (`PrecheckHandler` → `TranchePrecheckHandler` и т. п.), `DealFinalizationState` (модель + lifecycle), `mapping/TradeFill.md`, `rules/risk-creating-entry-protection.md`, `components/ClientService.md`, `components/RefreshFillsExecutor.md` | **битые входящие**; не гигиена `docs/`, но обратная навигация код→док сломана переписыванием корпуса. Владелец `code-writer`; исход — синхронизировать javadoc-указатели на под-шаге `CODE` |

**Итого внутри `docs/`:** битых путевых ссылок **0** (из 185); битых §-адресов
**0**; markdown-ссылок и якорей **0**; self-reference **4** (в 3 файлах);
оборванных адресов **4**; битых атрибуций **4**. Сумма — **12**.
Снаружи: **27 битых входящих из `src/`**.

### Процессная арматура в docs/

`docs/concept.md` П4: «Никакой процессной и ссылочной арматуры. Ни ссылок на
проверочные прогоны и итерации…». `concept.md` из подсчёта исключён — он про
арматуру говорит, а не несёт её. Слова-омонимы («прогоняет FSM», «переоткрытие
эпизода», «снятие риска») отсеяны вручную.

| класс | вхождений | файлов | точки (образцы) | владелец | исход |
|---|---|---|---|---|---|
| **A. Номера шагов и фаз роадмапа** | 19 | 15 | `models/mapping/Instrument.md:15,17,31,34,68,72,82`; `lifecycles/Instrument.md:6,12,55,79`; `contracts/trade-fee.md:19,121`; `contracts/fills-archive.md:31,33,39`; `contracts/order-precheck.md:20,32`; `contracts/position-tiers.md:27`; `contracts/position.md:51`; `contracts/price-limit.md:24`; `contracts/server-time.md:19`; `models/mapping/TimeFrame.md:30`; `models/integrations/okx/TickerOkxResponse.md:16,37`; `models/integrations/okx/TradeFeeOkxResponse.md:50`; `models/integrations/okx/InstrumentOkxResponse.md:45,84`; `models/domain/other/MarketStructure.md:88`; `lifecycles/Strategy.md:53`; `components/models/CalculatedStrategyAction.md:46`; `components/EntryScannerJob.md`; `processes/candle-loading.md:88`; `processes/market-data-calculation.md:14,35,101,106`; `rules/instrument-hold.md`; `rules/trading-constraints.md` | `knowledge-curator` | вычистить: «вводится шагом 7» → описание поля; «материализуется на шаге 5» → в роадмап |
| **B. Под-шаг `CODE` / «предусловие `CODE`»** | 15 | 11 | `lifecycles/CandleGroup.md:60`; `lifecycles/Strategy.md:53`; `models/domain/other/CandleGroup.md:69`; `models/domain/other/MarketPhase.md:30`; `models/domain/other/MarketStructure.md:123,155`; `models/mapping/TimeFrame.md:36`; `models/mapping/InstrumentExternalRules.md:109`; `models/integrations/okx/InstrumentOkxResponse.md:41,57,60`; `models/integrations/okx/AccountBillOkxResponse.md:14`; `models/integrations/okx/PositionsHistoryOkxResponse.md:69`; `contracts/position.md:97` | `knowledge-curator` | вычистить; носитель предусловий — см. G2 |
| **C. Номера находок и прогонов** | 15 | 6 | `contracts/algo-order.md:23-24,41,53,98,100,120` (И-1 / И-2 / И-3, «подтверждён живым прогоном»); `contracts/position-tiers.md:18` («## Уточнение пути (находка прогона 3)»); `models/mapping/AlgoOrder.md:84,141,152,154,180`; `models/mapping/Order.md:88`; `models/domain/other/IndicatorValue.md:47,90` (fork A, D1); `models/domain/other/MarketStructure.md:108,112` (fork-A, D3). Рядом: `contracts/order-precheck.md:34` (**В-9**), `contracts/position.md:51` (**В-3**), `contracts/trade-fee.md:19` (**В-7**), `contracts/mark-price.md:39`, `contracts/open-interest.md:36` («расхождение зафиксировано прогоном») | `knowledge-curator` + `integrator` (contracts-доки OKX) | вычистить; факт остаётся, номер находки уходит |
| **D. Открытые вопросы, парковки, «провизорное», «хвост пользователя», «предмет шага»** | 18 | 9 | `processes/candle-loading.md:70-76,77-90,105`; `components/AnomalyJob.md:54`; `lifecycles/Instrument.md:31,52`; `models/domain/other/CandleGroup.md:68,72`; `models/domain/other/MarketStructure.md:119,121`; `models/mapping/TimeFrame.md:36`; `models/integrations/okx/PositionsHistoryOkxResponse.md:69,71`; `contracts/instrument.md`; `rules/risk-policy.md:200,232` | `knowledge-curator`; торговые половины — `trading-specialist` | вопросы → `open-questions.md`, в доке остаётся знание. **Оговорка:** `rules/risk-policy.md:200` («провизорное число выглядит как контроль, но им не является») — цитата принципа из `concept.md`, не статус вопроса → **ОК**, не дефект |
| **E. Слой опровержения** (нарушено «правит на месте») | 8 | 7 | `models/domain/other/IndicatorValue.md:14`; `models/domain/other/MarketStructure.md:16-18`; `models/mapping/Instrument.md:68` («Формулировка "тем же синком" **снята**»); `models/mapping/InstrumentExternalRules.md:61` («Промежуточная редакция маппила их сюда; строки сняты»); `models/mapping/TradeFeeRate.md:88` («рудимент редакции…, снятой вместе с»); `models/integrations/okx/AccountBillOkxResponse.md:18` («Пока не снято, перечень…»), `:35` («**Ранее** `fee` отбрасывался с доводом…»); `lifecycles/DealTranche.md:71` («**Прежде** переоткрытый эпизод оставался в…»); `lifecycles/CandleGroup.md:59` («Детали, **ранее** отложенные на,») | `knowledge-curator` | переписать на месте: действующая формулировка без отсылки к прежней |
| **F. Лексика gap-отчёта в доменных доках** | 7 | 6 | `components/DealActiveHandler.md`; `components/MarkDealClosedExecutor.md:56`; `models/domain/other/Auditable.md:46`; `rules/pnl-reconciliation.md:52,77`; `rules/replace-not-amend.md:38`; `rules/trading-constraints.md` — все «Названная цена…» | `knowledge-curator` | пограничный класс: «названная цена» иногда несёт знание (что именно теряется), иногда — форму отчёта; разбирается вместе с G10 |
| **G. Довод «Почему …» в доменном доке** | 13 | 11 | см. G10 | `solution-designer` | развилка, см. G10 |
| **H. Книжный провенанс** | 6 | 1 | `models/domain/other/MarketStructure.md:130,139,146,151,160,167` | `trading-specialist` | развилка, см. G11 |

**Итого арматуры: 101 вхождение в 41 файле** (19+15+15+18+8+7+13+6 = 101; файлы
между классами пересекаются). Механически закрываемых курацией (A+B+C+E) —
19+15+15+8 = **57**. Требующих разбора по существу (D+F+G+H) — 18+7+13+6 = **44**.

### Размещение вне типа

Сплошная сверка каждого каталога `docs/` с таблицей `.claude/rules/structure.md`.

| наблюдение | разбор | владелец | исход |
|---|---|---|---|
| `docs/models/persistence/` **отсутствует на диске**, объявлен в `structure.md` | слой-скаффолд, «наполняется по мере появления»; входящих ссылок нет | `knowledge-curator` | **не дефект `docs/`**; при желании — `.gitkeep`, чтобы каталог соответствовал таблице |
| `docs/models/externalSnapshot/` **отсутствует на диске**, объявлен в `structure.md` | то же; типы `*ExternalSnapshot` упоминаются в 6+ доках (`CandleExternalSnapshot`, `MarketPriceDataExternalSnapshot`, …), но `structure.md` сам оговаривает «файл — только при самостоятельном содержании» | `knowledge-curator` | **не дефект** |
| `docs/models/api/` — единственный файл описывает DTO проверочного контура | см. G12 | `knowledge-curator` | развилка, крен — оставить |
| `docs/dictionary/` — одна статья (`leg-term.md`) при `.gitkeep` | тип наполняется по потребности | — | **ОК** |
| `docs/models/domain/other/Auditable.md` — базовый класс, не хранимая модель | `structure.md` для `other` явно называет «аудита и иных не-классов — по теме» | — | **ОК** |
| `docs/spec/` — 15 `.json` без раздела «На какой вопрос отвечает» | `structure.md` задаёт вопрос типу каталога; JSON — исполнимый артефакт, не markdown-док знания | — | **ОК** |
| остальные 12 каталогов (`components/`, `components/models/`, `lifecycles/`, `processes/`, `rules/`, `models/domain/{core,aggregate,other}/`, `models/mapping/`, `models/integrations/okx/`, `integrations/okx/{contracts,rules}/`) | каждый док проверен на соответствие вопросу каталога | — | **ОК — размещений вне типа не найдено** |

### Расхождения knowledge-tree

Метод: из секции `docs/` файла `.claude/knowledge-tree.md` извлечены все листья
`*.md` / `*.json` и сопоставлены с фактикой `find docs -type f`.

| проверка | результат |
|---|---|
| листьев в дереве (секция `docs/`) | **228** |
| файлов на диске (`.md` + `.json`, без `.gitkeep`) | **228** |
| в дереве, отсутствуют на диске | **0** |
| на диске, отсутствуют в дереве | **0** |
| совпадение множеств базовых имён (208 уникальных) | **полное** |
| совпадение кратностей повторяющихся имён (`AlgoOrder.md` ×3, `Instrument.md` ×3, `Order.md` ×3, `Position.md` ×3, `Strategy.md` ×3, `Deal.md` ×2, `DealTranche.md` ×2, `Candle.md` ×2, `CandleGroup.md` ×2, `AnomalyReport.md` ×2, `DealActionState.md` ×2, `DealCashFlow.md` ×2, `InstrumentExternalRules.md` ×2, `MarketPriceData.md` ×2, `TradeFeeRate.md` ×2) | **полное** |

**Расхождений нет.** `.gitkeep` в дереве не индексируются по его собственному
правилу — соответствует.

---

## Сводка

Счётные строки пересчитаны по телу отчёта после его закрытия; каждое число
сходится с собственным перечнем в той же строке.

**Часть 1 — концепт.**

- **Гейтящих находок — 2:** G1 (писателя, добытчика и лестницы деградации курса
  чужой валюты в корпусе нет; `RateStatus` вне спецификации), G2 (клейм
  «предусловие `CODE` п. 5» адресует док, который его не содержит; носителя
  предусловий нет).
- **Прочих находок — 10:** G3, G4, G5, G6, G7, G8, G9, G10, G11, G12.
- **Всего находок части 1 — 12.**
- **По ярлыку исхода:** `выводимо` — 5 (G3, G4, G5, G6, G8);
  `варианты-с-креном` — 5 (G1, G2, G10, G11, G12); составной
  `выводимо` + `неразрешимо-владельцем` — 1 (G7);
  `неразрешимо-владельцем` — 1 (G9). Перечислено: 5 + 5 + 1 + 1 = **12**.
- **По ярлыку дефицита:** `работа` — 3 (G1, G10, G11); `грунт` — 2 (G1, G2);
  `подтверждение` — 2 (G7 торговая половина, G9); «нет — закрыто прогоном» —
  6 (G3, G4, G5, G6, G8, G12). Перечислено: 3 + 2 + 2 + 6 = **13** — больше
  числа находок, потому что G1 несёт два дефицита.
- **По ожидаемым владельцам:** `knowledge-curator` — 7 (G3, G4, G5, G6, G7,
  G8, G12); `solution-designer` — 2 (G1, G10); `trading-specialist` — 3 (G7
  торговая половина, G9, G11); `integrator` — 2 (G1 грунт, G2).
- **Подтверждено-ОК — 10 позиций**, каждая с названным уровнем сверки.
- **Вынос пользователю (дефицит `подтверждение`) — 3 пункта:** нужен ли
  гистерезис фазы (G7); такт переоценки инварианта ликвидации и гистерезис (G9);
  допустима ли чужая валюта вовсе (продуктовая половина G1). Остальное
  закрывается автономно.

**Часть 2 — гигиена (не гейтит `CODE`).**

- **Составные вопросы файлов — 6 дефектов** из 213 доков (1 составной вопрос +
  5 разделов-вопросов с посторонним содержимым); раздел присутствует во всех
  213, пропусков **0**. Побочно — составная формулировка в самой таблице
  `.claude/rules/structure.md` (строка contracts).
- **Битые ссылки и §-адреса — 12 внутри `docs/`:** путевых битых **0** из 185
  целей; §-адресов **0** (грep пуст, ложных находок не выдано);
  markdown-ссылок и якорей **0**; self-reference **4**; оборванных адресов
  **4**; битых атрибуций **4** (0+0+0+4+4+4 = 12). Плюс **27 битых входящих из
  `src/`** — вне `docs/`, владелец `code-writer`.
- **Процессная арматура — 101 вхождение в 41 файле**, восемь классов:
  A 19, B 15, C 15, D 18, E 8, F 7, G 13, H 6 (19+15+15+18+8+7+13+6 = 101).
  Механически закрываемых курацией (A+B+C+E) — **57**; требующих разбора по
  существу (D+F+G+H) — **44**.
- **Размещение вне типа — 0 дефектов** (три наблюдения по скаффолдам не дефекты;
  одно вынесено находкой G12 как развилка).
- **Расхождения knowledge-tree — 0** (228 листьев против 228 файлов, множества
  имён и кратности совпадают полностью).
- **Всего позиций гигиены — 119** (6 + 12 + 101).

**Рекомендация.** `GAPS_CLOSE_26` нужен: до `CODE` шага 7 обязаны закрыться
**G1** и **G2** — без назначенного писателя курса и без носителя предусловия
пять полей строки движения и ветка маппера `ONE_SECOND` не реализуемы, а
П3-вопрос «откуда операнд взят» по данным не восстанавливается. Гигиена части 2
(119 позиций) закрывается курационным свипом параллельно и выход в `CODE` не
блокирует.
