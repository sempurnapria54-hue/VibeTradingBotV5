# Передача кейсов `jsonb-overlay-roundtrip` на ревью

## На какой вопрос отвечает этот файл

Что построено заходом письма кейсов `jsonb-overlay-roundtrip` и как автор
сверил их с носителями.

## Что построено

**Документ кейсов `.claude/tests/cases/jsonb-overlay-roundtrip.md`** —
последний, шестой из предметов уровня 2, введённых правкой перечня
2026-09-17, и четырнадцатый предмет уровня целиком
(`.claude/decisions/test-contour-design-pass.md` §«Перечень предметов и
порядок работы»). Предмет — пять форм навеса JSONB, живущих семью классами в
трёх деревьях; двенадцать групп, состав меток — сам документ.

**Популяция выведена двумя входами.** Первый — закрытый реестр кандидатов
уровня 2 (`py tools/pure-logic-candidates.py`), печатающий восемь классов с
именем-маркером, из которых один к навесу отношения не имеет и назван вне
предмета. Второй — писатели навеса, названные домом хранения: перебор по
сериализующему звену и по колонкам схемы дал **класс, которого реестр не
печатает по построению** — писатель снимка отчёта аномалии, у которого
конвертера нет вовсе. Он не взят в предмет по критерию «отказ приходит к
читателю» (отказ поглощён, читателя в коде нет) и стои́т пробелом `G5`.

**Новая ось формы, которую предмет вводит** — «выход кейса есть тождество
пары, а оператора тождества у предмета нет»: ни один класс общей доменной
библиотеки не переопределяет сравнение, поэтому ожидание «перечитанное равно
записанному» не выразимо ни одним умолчанием, а выбор способа сравнения сам и
есть предмет — рекурсивное сравнение по полям видит потерю, которую сравнение
строк JSON не видит по построению.

## Правка соседнего документа на месте

Соседний документ уровня 2 (`.claude/tests/cases/domain-model-predicates.md`)
передал сюда пробел `G3` по посылке «имена дискриминатора действия читает
конвертер навеса JSONB у трёх сервисов». Посылка проверена по коду и **не
держится**: дерево действий хранится реляционно — базовая таблица плюс
таблицы по видам, — и ни один из семи конвертеров навеса действие не
сериализует. Строка `G3` правлена **на месте** (`docs/concept.md`
§«Опровержение правит на месте»): адресат пробела переставлен на читателя
провода, а чем посылка опровергнута — названо ссылкой.

```bash
export LC_ALL=C.UTF-8
grep -rn 'StrategyAction\b' services/trading-core/src/main/java/com/example/tradingcore/mapping/ services/strategies/src/main/java/com/example/strategies/mapping/
ls services/strategies/src/main/java/com/example/strategies/persistence/model/ | grep -i action
```

## Находки владельцам

Девять; восемь получили секции бэклога тем же заходом, девятая (`F7`) закрыта
правкой соседнего документа и секции не получает. Перечень и указатели —
§«Находки владельцам» самого документа.

## Самопроверка по критерию апрува

Дельта захода — **все кейсы документа**: это первая его редакция
(`.claude/skills/test-design.md` §«Самопроверка автора до передачи на ревью»).

| Кейс | Носитель открыт | Что говорит | Сходится | Отсутствующие выходы | Достижимость |
|---|---|---|---|---|---|
| U1.1 | `docs/models/domain/core/AlgoOrder.md` §«Условие срабатывания»; `RuntimeJsonConverter` конструктор | триггерная ветка несёт уровни стопа и тейка; маппер — копия с политикой непустых полей | да | трейлинг пуст у объекта и отсутствует ключом в строке | да |
| U1.2 | там же | наблюдаемый уровень появляется при наблюдении, после активации | да | триггер пуст; наблюдаемого уровня нет ни в строке, ни у объекта | да |
| U1.3 | там же | после активации уровень часть факта | да | триггер пуст | да |
| U1.4 | `RuntimeJsonConverter#conditionToJson`, охрана пустоты | пусто на входе — пусто на выходе | да | ни строки, ни исключения | да |
| U1.5 | `RuntimeJsonConverter#jsonToCondition`, охрана пустоты | то же обратной стороной | да | то же | да |
| U1.6 | тот же метод, перехват отказа разбора | отказ обёрнут своим сообщением с сохранённой причиной | да | значения не подставляется, лога нет | да |
| U1.7 | `ObjectMapper` автоконфигурации Boot, умолчание отказа на неизвестном свойстве | неизвестное свойство отбрасывается | да, **прогоном не проверено** (умолчание библиотеки) | отказа нет | да |
| U1.8 | `.claude/rules/codestyle.md` §«Слои моделей и enum'ы»; перехват отказа | енум хранится именем; разбор имени вне перечня — отказ | да | ветви умолчания у перечня нет | да |
| U1.9 | `docs/models/domain/core/AlgoOrder.md` §«Условие срабатывания» | «ровно один механизм — trigger XOR trailing» — требование к модели | да | конвертер охраны не ставит | **недостижимо**: инвариант держит владелец; названо в документе |
| U2.1 | javadoc формы последней ошибки; `.claude/rules/codestyle.md` §«Слои моделей и enum'ы» | запись собирается по канонической форме; енум — именем | **носителя в `docs/**` нет → `F1`** | ключей сверх трёх компонентов нет | да |
| U2.2 | `RuntimeJsonConverter` конструктор | политика непустых полей | да | ключа пустого компонента в строке нет | да |
| U2.3 | javadoc формы | канонический конструктор записи ничего сверх не требует | **носителя в `docs/**` нет → `F1`** | охраны у формы нет | да |
| U2.4 | javadoc формы | отказ пришёл бы на тропе повтора | **носителя в `docs/**` нет → `F1`** | лога нет, значения не подставляется | да |
| U2.5 | умолчание отказа на неизвестном свойстве | отсутствие компонента записи отказом не является | да, **прогоном не проверено** | отказа нет | да |
| U2.6 | `RuntimeJsonConverter`, охраны пустоты обеих сторон | пусто — пусто | да | то же | да |
| U3.1 | `docs/models/domain/core/AlgoOrder.md` §Персистентность | перечень внешних идентификаторов — навес, своей таблицы нет | да | ничего сверх перечня | да |
| U3.2 | `RuntimeJsonConverter#stringListToJson` | пустой перечень пустотой не является | да | пустотой не становится | да |
| U3.3 | охраны пустоты обеих сторон | пусто — пусто | да | пустой перечень и пустота различимы | да |
| U3.4 | `RuntimeJsonConverter#jsonToStringList`, вывод параметра типа | элементы объявленного типа | да | — | да |
| U3.5 | умолчание приведения числа к строке | приведение разрешено умолчанием | да, **прогоном не проверено** | охраны типа элемента нет | да |
| U3.6 | перехват отказа разбора | несовпадение формы входа — отказ | да | — | да |
| U4.1 | `docs/rules/persistence-representation.md` §«Полиморфный JSONB» | дискриминатор живёт на владельце, в payload не дублируется | да | ключа тега в строке нет ни под одним именем | да |
| U4.2 | `StrategyJsonConverter#writeJson` | сериализация по рантайм-типу аргумента | да | — | да |
| U4.3 | выбор класса по значению перечня, без ветви умолчания | восемь типов, девятый уронил бы компиляцию | да | — | да |
| U4.4 | тот же выбор, разбор имени в значение перечня | неизвестное имя — отказ разбора имени | да | обёртки здесь нет: класс чужой | да |
| U4.5 | `StrategyJsonConverter#indicatorParamsFromEntity`, тройная охрана | три пустых входа дают пустоту | да | исключения нет ни на одном | да |
| U4.6 | `docs/rules/persistence-representation.md` §«Полиморфный JSONB» | об охране соответствия колонки и тела дом молчит | **пробел дока → `F5`**; кейс помечен | ни отказа, ни лога | **недостижимо**: писатель ставит тело и колонку одним ходом |
| U4.7 | умолчание отказа на неизвестном свойстве | тег в теле отбрасывается | да, **прогоном не проверено** | — | да |
| U5.1 | `StrategyJsonConverter` конструктор | политика непустых полей | да | ключа пустого поля нет | да |
| U5.2 | там же | пустой объект и пустота различимы | да | пустотой объект не становится | да |
| U5.3 | контраст конструкторов шести и седьмого конвертеров | политика включения меняет строку, не объект | да | round-trip этой оси не мерит — сказано в клетке | да |
| U5.4 | колонка срока свежести в миграциях обоих сервисов; перечень форм конвертера | длительности среди форм конвертера нет | **расхождение с javadoc → `F6`** | ожидания нет и оно не выдумывается | **не прогоняется**: входа не существует; условие возврата названо |
| U6.1 | `docs/rules/strategy-condition-contract.md`; `docs/models/domain/aggregate/Strategy.md` | правила проверяются по локальному порядку внутри условия | да | ничего сверх дерева | да |
| U6.2 | `RuntimeJsonConverter`-аналог вывода параметра типа у формы условия | пустой перечень остаётся перечнем | да | пустотой не становится | да |
| U6.3 | вывод параметра типа у формы клауз фазы | восстанавливаются элементы объявленного типа | да | безымянных карт не возникает | да |
| U6.4 | `StrategyJsonConverter`, четыре пары методов | у каждой формы свой класс на чтении | да | формы не путаются между собой | да |
| U6.5 | перехват отказа разбора | значение перечня вне словаря — отказ | да | — | да |
| U6.6 | `.claude/tests/cases/domain-model-predicates.md`; умолчание отказа на неизвестном свойстве | предикаты пустого перечня — предмет соседа | да | отказа нет | да |
| U6.7 | `.claude/rules/codestyle.md` §«Слои моделей и enum'ы» | енум едет именем | да | — | да |
| U7.1 | `docs/models/domain/other/InstrumentExternalRules.md` §«Ставка комиссии»; примесь копии ядра | изъятие обеспечивает конвертер навеса; копия ядра изымает только идентификатор владельца | **расхождение → `F2`** | ставка в строке ЕСТЬ — названо выходом | да |
| U7.2 | там же; примесь копии `market-data` | копия изымает только ставку | да | идентификатор владельца в строке есть | да |
| U7.3 | обе примеси | изъятия наблюдаемы только на заполненных полях | да | строки совпадают дословно | да |
| U7.4 | `docs/models/domain/other/TradeFeeRate.md`; хранилищный слой навеса ядра | ставка — атрибут комиссионного уровня счёта, копия разошлась бы со сменой тира | **расхождение → `F2`** | — | **недостижимо**: писатель проекции берёт объект из синка; названо |
| U7.5 | `InstrumentExternalRules#toDecimal` | нечисловое читается как отсутствие спецификации | да | навес мусор переносит, гасит модель | да |
| U7.6 | охраны пустоты обеих копий | пусто — пусто | да | — | да |
| U7.7 | умолчание отказа на неизвестном свойстве | лишнее отбрасывается | да, **прогоном не проверено** | — | да |
| U7.8 | `docs/models/domain/other/InstrumentExternalRules.md` §Структура | ветвь ненормализованного статуса — исход резолва у источника | да | разбором она не выбирается | да |
| U8.1 | `docs/models/domain/other/MarketOrderBook.md` §Персистентность | уровни живут навесом, FK на них ниоткуда не ведёт | да | ничего сверх перечня | да |
| U8.2 | `OrderBookLevelJsonConverter#levelsToJson`; миграция `market-data`, стороны среза `not null` | пусто на записи даёт пустой массив | да | пустота в обязательную колонку не легла бы | да |
| U8.3 | `OrderBookLevelJsonConverter#jsonToLevels` | пусто на чтении даёт пустой список | да | пустотой не становится | да |
| U8.4 | `OrderBookLevelJsonConverter` конструктор | общий бин берётся как есть | **расхождение с формой шести соседей → `F4`** | ключ с пустым значением в строке ЕСТЬ | **недостижимо** обойти: площадка отдаёт число заявок не всегда; названо |
| U8.5 | там же | форма колонки зависит от настроек чужого бина | **расхождение → `F4`** | ключа нет — контраст назван | да |
| U8.6 | перехват отказа разбора | своё сообщение, сохранённая причина | да | — | да |
| U8.7 | там же | несовпадение формы входа | да | — | да |
| U9.1 | `docs/models/domain/other/IndicatorValue.md` §«Ключевание — идентичностью вычисления» | одна идентичность — одна строка реестра | да | — | да |
| U9.2 | там же | разные параметры — разные строки | да | — | да |
| U9.3 | `ComputationParamsJsonConverter#buildCanonicalMapper` | непустые поля у обеих форм | да | пустого ключа нет ни в одной | да |
| U9.4 | javadoc конвертера | хранимая и каноническая отвечают на разные вопросы | да | совпадение форм не требуется | да |
| U9.5 | `docs/rules/persistence-representation.md` §«Полиморфный JSONB»; `#toIndicatorParams` | разбор по типу владельца, а не по тегу в теле | да | тег в теле не читается | да |
| U9.6 | `ComputationParamsJsonConverter#convert` | свой класс отказа с именем целевого класса | да | обёртки в `IllegalStateException` здесь нет | да |
| U9.7 | умолчание отказа на неизвестном свойстве | лишнее поле отбрасывается | да, **прогоном не проверено** | охраны состава у конвертера нет | да |
| U9.8 | охраны обоих публичных входов; приватное чтение без охраны | охраны стоя́т у входов | да | исключения нет ни на одном | да |
| U10.1 | `.claude/rules/carrier-levels.md` | у одной формы один носитель поведения | да | — | да |
| U10.2 | выбор класса по значению перечня у обеих копий | разведение у копий одинаково | да | — | да |
| U10.3 | обе примеси конвертера правил | копии несут разные правила | **расхождение → `F2`** | ожидание у пары раздельное — названо | да |
| U10.4 | тела обеих копий конвертера стратегии | различие ровно в одном слове javadoc | да | **прогоном не мерится** — пробел `G4` | да |
| U11.1 | умолчание отказа на неизвестном свойстве | снятое поле отбрасывается | да, **прогоном не проверено** | читатель не падает | да |
| U11.2 | там же | добавленное поле остаётся пустым | да, **прогоном не проверено** | отказа нет | да |
| U11.3 | перечень сериализационных аннотаций общей доменной библиотеки | форма навеса не запинена ничем | **пробел охраны → `F3`** | отказа нет — потеря молчит | **недостижимо**: переименования не было; названо |
| U11.4 | охрана пустоты конвертера; поведение библиотеки на литерале пустоты | охрана до литерала не доходит | да, **прогоном не проверено** | — | **недостижимо из базы**: колонка такого не хранит; названо |
| U11.5 | перехват отказа разбора; поведение библиотеки на пустом содержимом | пустая строка и пустая колонка — разные состояния | да, **прогоном не проверено** | — | **недостижимо из базы**; названо |
| U11.6 | перехват отказа разбора | несовпадение типа поля — отказ | да | единственная редакция, чей отказ читатель видит | да |
| U12.1 | §Предмет документа; команда чтения часов | ввода-вывода у предмета нет | да | ни базы, ни темы, ни вызова, ни часов | да |
| U12.2 | перехват отказа у семи против поглощения у писателя снимка аномалии | конвертер отказ поднимает | да | лога нет, значения не подставляется | да |
| U12.3 | javadoc конвертера навеса runtime-строк | пустое поле навеса неотличимо от ненаблюдённого | да | исключение одно и названо | да |
| U12.4 | кейсы `U1.9`, `U4.6`, `U9.7` | охраны инвариантов у навеса нет | да | — | да |

## Чем сверка ограничена, и это названо

- **Утверждения об умолчаниях библиотеки сериализации** (отказ на неизвестном
  свойстве, приведение числа к строке, чтение литерала пустоты, поведение на
  пустом содержимом) выведены **чтением** конфигурации автоконфигурации Boot
  и документации формата, а не прогоном; в таблице они помечены. Их проверка —
  предмет под-шага 3: первый же прогон кейсов `U1.7`, `U3.5`, `U11.4`, `U11.5`
  их подтвердит либо опровергнет, и опровержение будет находкой, а не правкой
  ожидания.
- **Тождество через базу не проверялось ничем** и в предмет не входит: ось
  названа пробелом `G2` и адресована ящику сервиса.
- **Подбор метода конвертера маппером** наблюдается сборкой, а не аргументом;
  пробел `G6`.

## Следующая единица

**Первый круг ревью документа** — фокус `test-review`
(`.claude/skills/test-review.md`), прогоняет не автор. С апрувом этого
документа **уровень 2 закрывается целиком — четырнадцать предметов из
четырнадцати**, и под-шаг 2 `CODE` шага заканчивается: открывается под-шаг 3
(код тестов).
