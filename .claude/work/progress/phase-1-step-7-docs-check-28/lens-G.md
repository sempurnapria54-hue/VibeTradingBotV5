# Линза G — остаток корпуса и структурная гигиена (`DOCS_CHECK_28`)

## На какой вопрос отвечает этот файл

Какие пробелы нашла линза G прогона `DOCS_CHECK_28` шага 7 фазы 1.

## Охват

**Часть 1 — остаток корпуса.** `docs/dictionary/**` (1 файл),
`docs/models/api/**` (1 файл), `docs/models/persistence/**` (каталога
нет — см. N7), плюс сквозной проход по всем `docs/**` (191 файл) на
предмет противоречий с `docs/concept.md`.

**Часть 2 — структурная гигиена репозитория.** `docs/**` и `.claude/**`
(кроме запрещённых путей): ссылочная целостность (2969 файловых ссылок,
595 `§«…»`-адресов), `.claude/knowledge-tree.md` против фактики,
«вопрос файла ровно один» (318 файлов знания), размещение по таблице
`.claude/rules/structure.md`, дом политики, процессная арматура в `docs/`.

**Вне охвата:** `src/`, содержание торговых развилок (линзы торгового
фокуса), содержание `.claude/work/history/**`, `docs-check-26/27`,
`gaps-close-26/27`, `phase-1-step-7-chronicle.md`, снапшоты,
`decision-digest.md` — независимость прогона.

**Статус шага на входе — `DOCS_CHECK_28`, до `CODE`: ведёт док.**
Расхождений «доки ↔ код» линза не искала и в отчёт не выносила.

## Стадия остановки

**Стадия 2 — прошла все стадии.** Гейтящих технических вопросов и
неописанных процессов в охвате линзы не обнаружено; обход дошёл до
компонентов и моделей и завершился штатно. Гейтящих находок — **0**
(правило «гейт не ставится на невыясненном»: ни одна находка линзы не
блокирует механику шага — все они о **форме** корпуса и его реестрах,
а не о недостающей спецификации того, что шаг исполняет).

---

## Находки — блокирует выход в CODE

**Нет.** 0 находок.

---

## Находки — прочее

### N1. Корпус `docs/**` систематически адресует внутренние пассажи, что `docs/concept.md` прямо запрещает — и запрещает сам себе

**Вопрос.** `docs/concept.md` в двух местах запрещает адресную ссылочную
арматуру внутри корпуса; в `docs/**` таких адресов 204, включая один в
самой концепции. При этом `.claude/rules/structure.md` §Принципы
легитимирует ту же форму и задаёт правило её разрешения. Два
управляющих носителя дают противоположные правила, и **область
действия каждого не названа** — из чего не выводится, дефект ли
конкретный `§`-адрес в доке.

**Цитаты (носители).**
- `docs/concept.md:195-198` — «**Никакой процессной и ссылочной
  арматуры.** Ни ссылок на проверочные прогоны и итерации, ни строчных
  якорей, ни адресных перекрёстий на внутренние пассажи.»
- `docs/concept.md:506-509` (§Ссылки) — «Ссылка **смысловая, на дом**…
  Не адрес пассажа, не строка, не якорь. Внутрифайловые адресные
  перекрёстья не используются: они ломаются от любой правки текста…»
- `docs/concept.md:320` — «…а база расти сама не должна
  (§«Сжатие в просадке»).» — **самоприменимость**: концепция
  использует ровно тот внутрифайловый адрес, который сама запретила
  двумя разделами выше.
- `.claude/rules/structure.md:107-125` §Принципы — «**`§«…»` адресует
  именованный пассаж…** Внутрифайловая ссылка на свой же пассаж
  (`§«…»` без имени файла) — законна.»
- Носители в корпусе — 204 адреса в 46 файлах; плотнейшие:
  `docs/rules/manual-halt.md` (45), `docs/rules/risk-policy.md` (14),
  `docs/models/mapping/DealCashFlow.md` (12),
  `docs/rules/pnl-reconciliation.md` (11), `docs/lifecycles/Deal.md` (10),
  `docs/rules/loss-streak-halt.md` (9),
  `docs/models/domain/aggregate/Deal.md` (8).

**Целевой док.** Правка `docs/concept.md` §Ссылки **либо**
`.claude/rules/structure.md` §Принципы — тот из двух, чья область
окажется уже; плюс сноска об области в проигравшем. Массовый свип
204 адресов — следствие, не сама правка.

**Ожидаемый владелец.** `knowledge-curator` (реконсиляция носителей
правила формы корпуса). Правка `concept.md` — через
`solution-designer`, если победит сужение концепции.

**Трассировка.** Открыты: `docs/concept.md` целиком;
`.claude/rules/structure.md` §Принципы (из контекста CLAUDE.md);
грепом собран счёт `§` по `docs/**`; вручную открыт `docs/concept.md:316-322`
и подтверждено, что адрес указывает на собственную секцию
«Сжатие в просадке» (существует, `docs/concept.md:392`). Проверено
встречное прочтение: `docs/concept.md` Часть V относит методологию
к `.claude/`, значит `§`-адреса легальны в `.claude/**` и нелегальны в
`docs/**` — но **ни один из двух носителей этого разграничения не
записывает**, поэтому вывод остаётся реконструкцией читателя.

**Варианты.**

| Вариант | Что делает | Названная цена |
|---|---|---|
| **A. Существующий носитель достаточен** — прочитать §Принципы `structure.md` как правило для `.claude/**`, `concept.md` — для `docs/**` | новой структуры нет | цена: разграничение нигде не записано ⇒ каждый прогон переоткрывает вопрос заново (этот отчёт — экземпляр). Контекстная связность: читателю обоих файлов надо знать Часть V концепции, чтобы вывести область |
| **B. Приписать область в обоих носителях** (строка-указатель) | `structure.md` §Принципы получает «правило действует в `.claude/**`; форма ссылок корпуса — `docs/concept.md` §Ссылки»; `concept.md` §Ссылки — встречное «в `.claude/**` действует иное» | след: правка клаузы в двух доках. Цена: 204 адреса в `docs/**` остаются дефектами, свип отложен и невидим |
| **C. B + свести 204 адреса к смысловым ссылкам на дом** | корпус приводится к §Ссылки | след: правка ~46 файлов. Цена: часть адресов несёт различающую нагрузку (в `manual-halt.md` 45 адресов на один файл — это внутренняя навигация по 48 KB), и её замена смысловой ссылкой требует **распила гигантов**, то есть отдельного проектного хода |
| **D. Снять запрет из `concept.md` §Ссылки** и признать адресацию нормой корпуса | один носитель формы вместо двух | след: правка клаузы концепции. Цена: **переоткрывает ратифицированный принцип П4** — довод концепции («адреса ломаются от любой правки и производят ложную работу») этим вариантом не опровергается, а отменяется; и он **эмпирически верен** — см. §Гигиена, где 116 из 595 адресов потребовали ручной сверки, а 3 оказались сокращёнными или прозвищными |

**Крен — B, с C как отдельной задачей.** Довод счётный, не оценочный:
B закрывает **ложную работу прогонов** (этот отчёт вынужден был
вручную открыть 116 целей, чтобы отделить дефект от конвенции)
минимальным следом; C трогает 46 файлов и упирается в распил
`manual-halt.md` / `risk-policy.md`, то есть в другую развилку. D
отвергнут: он снимает принцип, а не расхождение носителей.

**Ярлык исхода.** `варианты-с-креном`.

**Ярлык дефицита.** `подтверждение` — **только на выборе между B и D**:
D переоткрывает форму, ратифицированную концепцией, и заменять её
нечем, кроме решения держателя о том, чем корпус платит за навигацию.
Выводимая половина (что расхождение носителей есть дефект и область
обязана быть записана) закрывается CC.

---

### N2. Процессно-историческая арматура живёт внутри продуктового корпуса

**Вопрос.** `docs/concept.md` перечисляет, чего доменный док не
содержит: номера проверок, кто ратифицировал, что было записано раньше
и почему снято. Корпус несёт всё перечисленное — номерами прогонов,
ссылками в `.claude/work/**`, датированными атрибуциями «решение
держателя» и слоем опровержения.

**Цитаты (носители).**

*Клаузы, которые нарушены:*
- `docs/concept.md:464-467` — «**Не содержит:** почему выбрано так, а
  не иначе; какие были альтернативы; что было записано раньше и почему
  снято; какая проверка это нашла; кто ратифицировал; сколько раз
  переоткрывали…»
- `docs/concept.md:512-520` (§«Опровержение правит на месте») — «В
  тексте нет ни «прежняя редакция снята», ни «клейм ложен», ни
  «уточнено», ни отсылок к тому, чем знание было раньше.»

*Кластер «номер прогона / проверка»:*
- `docs/models/domain/core/AlgoOrder.md:104-106` — «*(Решение
  восстановлено `GAPS_CLOSE_26`: оно было потеряно при переписывании
  корпуса, а не отменено…)*»
- `docs/rules/risk-policy.md:111-115` — «*(Решение держателя
  2026-08-29 … альтернатива «третье число конфигурации» отклонена …
  Закрытие `GAPS_CLOSE_26`.)*»
- `docs/rules/risk-policy.md:224-227` — «*(Решение держателя
  2026-08-29 … Отклонены — третье число риск-аппетита … Закрытие
  `GAPS_CLOSE_26`.)*»
- `docs/integrations/okx/contracts/position-tiers.md:18` — заголовок
  «## Уточнение пути (находка прогона 3)»

*Кластер «ссылка в `.claude/work/**` из корпуса»:*
- `docs/rules/loss-streak-halt.md:195-196` — «(см.
  `.claude/work/progress/phase-1-step-7-gaps-close-27/node-H2.md`
  §«Раунд 1 мини-петли»)»
- `docs/lifecycles/Strategy.md:17` —
  `.claude/work/history/2026-05-27-миграция-торговых-сущностей/tasks-strategy.md`
- `docs/rules/manual-halt.md:46`, `docs/rules/manual-halt.md:273`,
  `docs/lifecycles/Deal.md:131`,
  `docs/models/domain/core/AlgoOrder.md:96`,
  `docs/components/SafetyHoldCoordinator.md:79` — ссылки на
  `.claude/work/backlog.md`

*Кластер «кто ратифицировал»:*
- `docs/models/domain/core/Instrument.md:91` — «**Правило (решение
  держателя, 2026-08-29).**»
- `docs/rules/exchange-hold.md:57-58` — «…холд не снимает (решение
  держателя 2026-08-29).»
- `docs/rules/loss-streak-halt.md:120` — «(решение держателя …)»
- `docs/rules/manual-halt.md:23` — «ратифицированная граница»

*Кластер «слой опровержения»:*
- `docs/rules/deal-without-operations.md:53-54` — «…частичным его
  объявляла **прежняя редакция, и она снята**.»
- `docs/rules/loss-streak-halt.md:191-194` — «…те самые состояния, на
  которых **снятая редакция** (`holdStandingAfter = haltTriggered`)
  даёт снятие; откатом проверено, что они расходятся»
- `docs/rules/risk-policy.md:408-410` — «**Прежде декларатора не
  было**, и стратегии выражали «безубыток» долей от входа…»
- `docs/models/mapping/AlgoOrder.md:155` — «следствие **снято решением**
  REPLACE-only»

**Целевой док.** Каждый цитируемый док — вычитанием арматуры; содержимое,
которое арматура несла (провенанс, отвергнутые альтернативы, хроника),
переезжает в `.claude/decisions/` либо в `.claude/work/history/`, откуда
корпус на него **не ссылается**.

**Ожидаемый владелец.** `knowledge-curator`.

**Трассировка.** Грепы по `docs/**`: `\.claude/`, `DOCS_CHECK|GAPS_CLOSE|
решение держателя|ратифиц|прогон N|итерац`, `прежн. редакц|клейм ложен|
уточнено|снят`. Каждое попадание открыто с контекстом ±2 строки и
отделено от омонимов: «переоткрытие» как **доменное ребро транша**
(`docs/lifecycles/DealTranche.md`, `docs/rules/exit-teardown-order.md`,
`docs/components/TrancheManagingHandler.md`) — не арматура и в перечень
не включено; `docs/rules/risk-policy.md:553` («величины про хвост —
решение держателя») — не атрибуция ратификации, а содержательное
утверждение, дублирующее `docs/concept.md:405`, и уходит в N4.

**Варианты.**

| Вариант | Что делает | Названная цена |
|---|---|---|
| **A. Существующий носитель достаточен** — «арматура помечена курсивом-скобками, читатель её отличит» | правки нет | цена названа самой концепцией: слой опровержения «расходится, устаревает и порождает третий слой» (`docs/concept.md:516-519`). Счётный случай уже в корпусе: `loss-streak-halt.md:191` **воспроизводит снятую формулу** `holdStandingAfter = haltTriggered` в тексте правила — читатель, реализующий это место, видит две формулы и обязан угадать действующую |
| **B. Вычесть арматуру, содержание — в `decisions/`/`history/`** | корпус приводится к `concept.md:464-467` | след: правка ~16 пассажей в 12 файлах. Цена: провенанс перестаёт быть виден из дока — восстанавливается через git и `decisions/`; это ровно та цена, которую концепция называет принятой (`docs/concept.md:520`) |
| **C. B + встречный якорь** — там, где арматура несла **живое обязательство** (`backlog.md` §«Снятие ограничения `MARK`-only», §«CODE-дельта шага»), ссылка заменяется формулировкой ограничения **без адреса рабочего файла** | снимает ссылку в `work/`, сохраняя обязательство | след: B + переформулировка 5 пассажей. Цена: обязательство перестаёт быть кликабельным из корпуса; якорь остаётся со стороны `backlog.md` |

**Крен — C.** Счётная разница с B: пять пассажей ссылаются на
`backlog.md` не ради истории, а ради **живого форварда** (снятие
ограничения, CODE-дельта); голое вычитание по B потеряло бы
обязательство, а не только арматуру.

**Ярлык исхода.** `варианты-с-креном`.

**Ярлык дефицита.** Дефицита нет — выводится из
`docs/concept.md:464-467` и §«Опровержение правит на месте»; закрывает CC.

---

### N3. Формула переписана прозой рядом с живой спекой; один предикат исполнимой формы не имеет вовсе

**Вопрос.** Концепция объявляет спецификацию **домом формулы** и
запрещает доку переписывать формулу словами. В корпусе четыре кластера,
где формула живёт и в спеке, и прозой (в одном — в четырёх прозаических
носителях сразу), и один предикат целостности, у которого спеки нет.

**Цитаты (носители).**

*Клауза:*
- `docs/concept.md:484-489` — «**Дом формулы — спецификация, а не
  проза.** Доменный док называет величину, её роль и её писателя и
  **ссылается** на спецификацию. Он не переписывает формулу словами.
  Это ровно тот шов, на котором корпус расходился…»
- `docs/concept.md:192-193` (П4) — «**Формулы, предикаты и закрытые
  перечни — в исполнимой форме**, одним экземпляром, с прогоном против
  примеров.»

*Кластер 1 — безубыток (формула + её вывод + отвергнутое приближение
в двух носителях):*
- `docs/rules/risk-policy.md:412-425` — блок ```text``` с
  `безубыток(LONG) = якорь × (1 + ставка) / (1 − ставка)` и
  `безубыток(SHORT) = …`, плюс вывод из условия нулевого P&L, плюс
  разбор линейного приближения с остатком `±2 × ставка² × якорь`
- `docs/spec/stop-distance.json` → `values[].name = "breakevenLevel"`,
  `expr = "if(direction == 'LONG', entryAnchor * (1 + feeRate) / (1 - feeRate), entryAnchor * (1 - feeRate) / (1 + feeRate))"`
  — **та же формула**, и её `note` **повторяет тот же вывод и то же
  отвергнутое приближение**
- третий носитель того же: `docs/components/PriceCalculator.md`
  §«Безубыток считается с комиссией» (адресуется из
  `docs/rules/risk-policy.md:405`)

*Кластер 2 — катастрофический потолок:*
- `docs/rules/risk-policy.md:136-140` — блок ```text``` с
  `катастрофический потолок = globalSimultaneousRiskPerDealPercent / 100 × база × strategyCatastrophicRiskPerDealMultiplier`
- `docs/spec/risk-limits.json` (`home` = `docs/rules/risk-policy.md`)

*Кластер 3 — свежесть, четыре прозаических носителя одной формулы:*
- `docs/rules/market-data-freshness.md:22` —
  `expiredAt = точка отсчёта + срок свежести настройки-владельца`
- `docs/components/MarketDataExpirationChecker.md:34` —
  `expiredAt = referencePoint + ownerSetting.expirationDuration`
- `docs/models/domain/other/IndicatorValue.md:103` —
  `expiredAt = candleTimestamp + ownerSetting.expirationDuration`
- `docs/models/domain/other/MarketStructure.md:192` —
  `expiredAt = windowEndAt + …`
- дом формы — `docs/spec/market-data-freshness.json`
  (`home` = `docs/rules/market-data-freshness.md`)

*Кластер 4 — линейный убыток:*
- `docs/rules/trading-constraints.md:22-24` — «весь сайзинг и весь
  расчёт `R` построены на линейной формуле `убыток = |вход − стоп| ×
  контракты × ctVal`»; исполнимые дома — `docs/spec/order-sizing.json`,
  `docs/spec/deal-risk-numbers.json`

*Предикат без исполнимой формы:*
- `docs/models/domain/other/CandleGroup.md:84` —
  `count == (actualLast − actualFirst) / step + 1`; в `docs/spec/`
  спеки свечной целостности нет (перечень из 19 файлов проверен —
  свечей в нём нет)

**Целевой док.** `docs/rules/risk-policy.md` (два блока → ссылка),
`docs/rules/trading-constraints.md`,
`docs/components/MarketDataExpirationChecker.md`,
`docs/models/domain/other/IndicatorValue.md`,
`docs/models/domain/other/MarketStructure.md` — прозаические экземпляры
снимаются в пользу ссылки на дом; `docs/models/domain/other/CandleGroup.md`
— вопрос о заведении `docs/spec/candle-group-integrity.json`.

**Ожидаемый владелец.** `solution-designer` (что переносится в спеку и
нужна ли новая) при участии `knowledge-curator` (свод носителей).

**Трассировка.** Собраны все арифметические выражения вне ```-блоков в
`docs/rules`, `docs/models`, `docs/components` (грепом по `=` с
операторами), отфильтрованы присваивания-константы (`code == 0`,
`updatable = false`, `Type = REDUCE_ONLY`). Каждый оставшийся кластер
сверен со спекой: `docs/spec/*.json` прочитаны программно на `home` и
на присутствие ключа. **Расхождений между прозой и спекой не найдено**
— формулы совпадают дословно; находка о дублировании, не о конфликте.
Проверена достижимость обратного: если бы расхождение было, находка
была бы гейтящей (писатель кода реализовал бы не ту формулу).

**Варианты.**

| Вариант | Что делает | Названная цена |
|---|---|---|
| **A. Существующего носителя достаточно** — спека уже дом, проза «иллюстрирует» | правки нет | цена названа концепцией: «одна формула в нескольких носителях приобретает несколько редакций, и расхождение обнаруживается не чтением, а инцидентом» (`docs/concept.md:487-489`). Счётный случай уже виден в кластере 3: четыре носителя дают **три разных первых операнда** (`точка отсчёта` / `referencePoint` / `candleTimestamp` / `windowEndAt`) — совпадают они или нет, из прозы не выводится |
| **B. Свести прозу к ссылке на дом** во всех четырёх кластерах | корпус приводится к §Исполнимая форма | след: правка клаузы в 7 доках. Цена: `risk-policy.md` теряет самодостаточность чтения — читатель обязан открыть JSON; контекстная связность растёт на один файл |
| **C. B + завести спеку свечной целостности** | закрывает и предикат без формы | след: B + новый файл `docs/spec/`. Цена: свечная целостность — не предмет шага 7; заведение спеки под неё вне текущего шага. Отложенная цена закрытия потом: предикат уже живёт в доке единственным экземпляром прозой, и каждая правка окна загрузки будет править его вручную |
| **D. B, но `note` спеки тоже вычистить** от вывода и отвергнутого приближения | убирает второй экземпляр довода | след: B + правка `docs/spec/stop-distance.json`. Цена: **переоткрывает форму спеки** — `note` как место для довода в `docs/spec/` нигде не запрещён, и концепция про поля спеки не высказывается |

**Крен — B, C выносится отдельной задачей, D — под вопросом.** Довод:
B закрывает наблюдаемое дублирование минимальным следом; C трогает
предмет вне шага; D требует решения о том, что вообще лежит в `note`
спеки, — этого решения в корпусе нет.

**Ярлык исхода.** `варианты-с-креном`.

**Ярлык дефицита.** Дефицита нет по B и C — выводится из
`docs/concept.md:484-489`, закрывает CC. По D — `работа`: нужна
формулировка того, что несут поля спеки, и её место.

---

### N4. Дом политики асимметрии областей объявлен в mapping-доке — вне критерия дома

**Вопрос.** `docs/models/mapping/DealCashFlow.md` объявляет себя домом
продуктовой политики, ссылаясь на `.claude/rules/policy-home.md`. Но
критерий дома в этом же правиле называет двух возможных владельцев —
`docs/rules/` либо док **сущности**-владельца; mapping-док не тот и не
другой, а док **перехода между слоями**
(`.claude/rules/structure.md:57`: «Как сущность переходит между слоями»).

**Цитаты (носители).**
- `docs/models/mapping/DealCashFlow.md:171-176` — «### Асимметрия
  областей: из суммы исключено, из блокировки — нет … **Это дом
  политики асимметрии** (`.claude/rules/policy-home.md`): прочие
  носители несут строку-указатель сюда и довода не повторяют.»
- `.claude/rules/policy-home.md` §Правило — «**Дом** — файл слоя правил
  по владельцу политики: продуктовая политика — `docs/rules/` (или док
  сущности-владельца, если политика принадлежит одной сущности…)»
- Указатели, признающие этот дом: `docs/rules/pnl-reconciliation.md:54`,
  `docs/models/domain/other/DealCashFlow.md:83`,
  `docs/models/mapping/DealCashFlow.md:126`
- Кандидаты по критерию: `docs/rules/pnl-reconciliation.md` (правило) и
  `docs/models/domain/other/DealCashFlow.md` (док сущности-владельца)
- Дом формы того же различения — `docs/spec/deal-result.json`
  (`inCrossCurrencyScope` против `inCrossCurrencyBlockingScope`), и
  mapping-док это признаёт: «дом формы и дом политики здесь разные, и
  это штатно» (`docs/models/mapping/DealCashFlow.md:175-176`)

**Целевой док.** `docs/models/mapping/DealCashFlow.md` (перенос
секции) → `docs/rules/pnl-reconciliation.md` либо
`docs/models/domain/other/DealCashFlow.md`; в mapping-доке остаётся
строка-указатель.

**Ожидаемый владелец.** `knowledge-curator`.

**Трассировка.** Грепом собраны все носители строки «Асимметрия
областей» в `docs/**` (4 попадания) и все самообъявления «дом политики»
(2: этот и `docs/components/RefreshBillsExecutor.md:126`, который
**ссылается**, а не объявляет себя домом — это корректная форма).
Открыт `docs/models/mapping/DealCashFlow.md:160-185`, открыто правило
`.claude/rules/policy-home.md` (из контекста CLAUDE.md), сверены оба
названных им типа дома с типом mapping-дока по таблице
`.claude/rules/structure.md`.

**Варианты.**

| Вариант | Что делает | Названная цена |
|---|---|---|
| **A. Существующий носитель достаточен** — политика фактически про **маппинг** движения в домен, значит mapping-док и есть владелец | правки нет | цена: критерий `policy-home.md` перестаёт разрешать вопрос механически — «слой правил либо док сущности» больше не исчерпывает варианты, и каждый следующий дом требует спора. Счётный случай: политика уже читается **тремя** носителями за пределами маппинга (`pnl-reconciliation.md`, домен-док, спека) — то есть потребляется вне слоя, который её держит |
| **B. Перенести в `docs/rules/pnl-reconciliation.md`** | дом = правило, как первый вариант критерия | след: перенос секции + 3 указателя. Цена: `pnl-reconciliation.md` (19.7 KB) растёт; политика асимметрии — не только про сверку P&L, но и про блокировку итога |
| **C. Перенести в `docs/models/domain/other/DealCashFlow.md`** | дом = док сущности-владельца, второй вариант критерия | след: перенос секции + 3 указателя. Цена: домен-док получает политику, тогда как саму асимметрию **производит** маппинг (какая строка попала в область) |
| **D. Расширить критерий `policy-home.md`** третьим типом дома — «док перехода, когда политику производит переход» | легализует текущее размещение | след: § правила в `.claude/rules/policy-home.md`. Цена: критерий из двухзначного становится трёхзначным, то есть требует различения «производит» vs «владеет» — которого сейчас нет |

**Крен — B.** Шкала следа (`gap-report.md` §«Выбор варианта») ставит
«правка клаузы + указатели» ниже, чем «§ правила»; счётная разница с
A: политика потребляется в трёх носителях вне маппинга, то есть
достаточность на **поверхности потребления** не выполняется. Между B и
C выбор B: политика различает **две области** (слагаемое и блокировка),
и вторая — про публикацию итога, то есть предмет правила сверки, а не
сущности движения.

**Ярлык исхода.** `варианты-с-креном`.

**Ярлык дефицита.** Дефицита нет — выводится из
`.claude/rules/policy-home.md`; закрывает CC.

---

### N5. `docs/dictionary/leg-term.md` несёт довод выбора и цену отвергнутой альтернативы

**Вопрос.** Единственная словарная статья корпуса содержит раздел
«Почему разведено именно так» с обоснованием и подсчётом цены
альтернативы — то, что концепция из корпусного дока исключает и
относит к себе либо к `decisions/`.

**Цитаты (носители).**
- `docs/dictionary/leg-term.md:26-29` — «**Почему разведено именно
  так.** Слово «нога» в корпусе уже несёт два значения; третье сделало
  бы предложения вида «нога ноги» неразличимыми. Транш — слово без
  прежних значений, поэтому **цену разведения платит новый уровень, а
  не 200 существующих упоминаний**.»
- `docs/concept.md:464-467` — «**Не содержит:** почему выбрано так, а
  не иначе; какие были альтернативы…»
- `docs/concept.md:12-21` — «Правило разделения одно: **док отвечает
  «что», концепция отвечает «почему»**. Проверка — вычитанием…»
- Встречный носитель того же выбора:
  `.claude/rules/naming.md` §«Разведение уровней абстракции» — держит
  **способ** разведения как правило пайплайна

**Целевой док.** `docs/dictionary/leg-term.md` — вычесть раздел;
довод (если он ещё нужен) — в `.claude/decisions/`, где живёт выбор
способа разведения.

**Ожидаемый владелец.** `knowledge-curator`.

**Трассировка.** Открыт `docs/dictionary/leg-term.md` целиком (единственный
файл каталога, 1980 байт), применена **проверка вычитанием** из
`docs/concept.md:14-17`: убран раздел «Почему разведено именно так» —
знание («нога = заявка; половина двуногой команды; не уровень сделки —
это транш») осталось полным и однозначным. Значит раздел был
аргументацией, а не частью знания. Раздел «Чем нога не является»
проверен той же проверкой и **оставлен**: без него «нога» и «транш»
становятся неразличимы, то есть это знание, а не довод.

**Варианты.** Развилки нет — проверка вычитанием разрешила однозначно.

**Ярлык исхода.** `выводимо`.

**Ярлык дефицита.** Дефицита нет — закрыто прогоном.

---

### N6. `docs/models/api/OkxRawApiRequest.md`: оборванная ссылка, арматура решения и §Связи, ведущий не туда

**Вопрос.** Единственный файл `docs/models/api/` несёт три дефекта
разного класса, из которых один — **битая по существу ссылка**: §Связи
адресует «конвенции api-слоя» в `.claude/rules/structure.md`, где
конвенций api-слоя нет.

**Цитаты (носители).**
- `docs/models/api/OkxRawApiRequest.md:22` — «…контур проверяет
  контракт биржи, не наш слой
  (`.claude/decisions/source-api-target-rebase.md`**,)**.» — оборванная
  ссылка: висящая запятая перед закрывающей скобкой
- `docs/models/api/OkxRawApiRequest.md:31` — «Конвенции api-слоя —
  `.claude/rules/structure.md`.»
- `.claude/rules/structure.md:54` — вся строка про `docs/models/api/`:
  «Что это за модель API нашего сервиса? … Request/response DTO нашего
  API. Слой-скаффолд.» — **конвенций api-слоя нет**
- Фактический дом конвенций — `.claude/rules/codestyle.md`
  §«Контроллеры / API» (Swagger-аннотации, `internalId` наружу,
  `@ParameterObject`, порядок путей)
- `docs/models/api/OkxRawApiRequest.md:26-28` — «Решение о ре-базе
  контура на сырьё — `.claude/decisions/source-api-target-rebase.md`»
  — арматура «почему выбрано так» в корпусном доке
- **Тот же обрыв ссылки повторён**:
  `docs/models/integrations/okx/TickerOkxResponse.md:20-23` — «decision
  о ре-базе контура на сырьё
  (`.claude/decisions/source-api-target-rebase.md`**,)**: держим только
  заведённые поля…»

**Целевой док.** `docs/models/api/OkxRawApiRequest.md` и
`docs/models/integrations/okx/TickerOkxResponse.md`.

**Ожидаемый владелец.** `integrator` — оба дока описывают поверхность
источника и его контур (находки по нативным моделям идут
интеграционному владельцу первым, `gap-report.md` §«Набор полей
находки»); реадресация §Связи — `knowledge-curator`.

**Трассировка.** Открыт `docs/models/api/OkxRawApiRequest.md` целиком
(единственный файл каталога). Клейм «конвенции api-слоя —
`structure.md`» **верифицирован открытием цели**: прочитана строка 54
таблицы размещения — обещанной клаузы нет; затем найден фактический
носитель грепом по `.claude/rules/codestyle.md`. Обрыв `` `,) ``
собран грепом по `docs/**` и `.claude/**` — 2 попадания в живом
корпусе, оба цитированы.

**Варианты.** Развилки нет по обрыву ссылки и по реадресации §Связи
(дефект записи, чинится курационно, от продуктового выбора не
зависит). По арматуре решения — та же развилка, что N2, и **закрывается
тем же ходом**; отдельно не выносится.

**Ярлык исхода.** `выводимо`.

**Ярлык дефицита.** Дефицита нет — закрыто прогоном.

---

### N7. `docs/models/persistence/` и `docs/models/externalSnapshot/` объявлены живыми типами размещения, но носителей нет

**Вопрос.** Два типа знания зарегистрированы в таблице размещения и в
процедуре классификации как действующие дома. Каталогов на диске нет,
в `.claude/knowledge-tree.md` их нет. Классификатор, дошедший до этих
строк, направляет фрагмент в несуществующее место.

**Цитаты (носители).**
- `.claude/rules/structure.md:49` — строка `docs/models/externalSnapshot/`
  — «Единственное, что выходит за `ClientService`/adapter… Файл — только
  при самостоятельном содержании.»
- `.claude/rules/structure.md:53` — строка `docs/models/persistence/` —
  «Слой-скаффолд; наполняется по мере появления persistence-проекций.»
- `.claude/skills/classify-type.md:67,73,121,122` — оба каталога в
  перечне целей классификации
- `.claude/decisions/model-layer-ontology.md:57,79` — те же каталоги
- `.claude/work/progress/corpus-rewrite-mapping.md:512,554` — те же
- Фактика: `ls docs/models/` → `api  domain  integrations  mapping`
- `.claude/knowledge-tree.md` — ни `persistence/`, ни `externalSnapshot/`
  в дереве нет
- Фактический дом persistence-знания сегодня:
  `docs/rules/persistence-representation.md` + секции §Персистентность
  в доменных доках (например
  `docs/models/domain/aggregate/Strategy.md:274`)

**Целевой док.** `.claude/rules/structure.md` (две строки таблицы) +
`.claude/skills/classify-type.md`; `.claude/knowledge-tree.md` — по
исходу.

**Ожидаемый владелец.** `knowledge-curator`.

**Трассировка.** Автоматический свод несуществующих целей по всем
`.md`-ссылкам репозитория дал оба каталога; проверено `ls docs/models/`;
проверено отсутствие в `knowledge-tree.md` грепом; найден фактический
носитель persistence-знания (`docs/rules/persistence-representation.md`
существует; §Персистентность найдена в доменном доке). Проверено
встречное: `docs/models/api/` в той же таблице помечен так же
(«Слой-скаффолд»), но **существует и наполнен** — значит «скаффолд» в
таблице не означает «каталога может не быть».

**Варианты.**

| Вариант | Что делает | Названная цена |
|---|---|---|
| **A. Существующий носитель достаточен** — «скаффолд заведётся, когда понадобится» | правки нет | цена: `classify-type` — исполняемая процедура, и она направляет фрагмент в путь, которого нет; ошибка тихая (файл создастся, каталог появится, дерево разойдётся). Счётный случай: `.gitkeep` есть в `docs/models/`, `docs/rules/`, `docs/lifecycles/`, `docs/dictionary/` — то есть **проект уже держит пустые каталоги явно**, и отсутствие этих двух не читается как «пока не нужен» |
| **B. Материализовать каталоги** `.gitkeep`-ом и внести в дерево | приводит фактику к таблице | след: 2 файла `.gitkeep` + 2 узла дерева. Цена: заводится пустой тип, который может не понадобиться до конца фазы 1 |
| **C. Снять обе строки из таблицы и `classify-type`**, назвав действующий дом (§Персистентность доменного дока; `*ExternalSnapshot` — состав внутри mapping-дока) | приводит таблицу к фактике | след: правка 2 строк таблицы + 4 мест `classify-type`. Цена: если persistence-проекции появятся в `CODE`, тип придётся вводить заново — и **это уже случалось**, `model-layer-ontology.md` вводил их решением |
| **D. B для `persistence/`, C для `externalSnapshot/`** | разводит по фактическому спросу | след: смешанный. Цена: требует установить, есть ли у `*ExternalSnapshot` самостоятельное содержание — а таблица сама ставит это условием («Файл — только при самостоятельном содержании»), то есть ответ не выводится из таблицы |

**Крен — C.** Дефолт минимального следа: сегодня оба типа пусты, а
знание, ради которого они заводились, живёт в других домах и
**потребляется оттуда**. Бремя обоснования — на B/D, и счётного случая
(«вот фрагмент, которому в существующих домах нет места») прогон не
нашёл. Оговорка: C **переоткрывает** `model-layer-ontology.md` —
помечено явно.

**Ярлык исхода.** `варианты-с-креном`.

**Ярлык дефицита.** Дефицита нет — выводится из фактики и
`.claude/rules/curation.md` §8; закрывает CC.

---

### N8. Битый `§`-адрес в обязательном входе линз: `question-delegation.md §«Калибровка»`

**Вопрос.** Два скилла машины ссылаются на секцию «Калибровка» в
`.claude/processes/question-delegation.md`. Такой секции нет — ни
заголовком, ни лид-жирным пассажом. Один из ссылающихся —
`concept-review.md`, **обязательный вход каждой линзы** прогона: линза,
проверяющая формулу «X верно по существу, чинится Y», отправляется за
критерием в несуществующее место.

**Цитаты (носители).**
- `.claude/skills/concept-review.md:182-184` — «…сам акт «признать один
  носитель правым» есть выбор среди альтернатив
  (`.claude/processes/question-delegation.md` §Калибровка).»
- `.claude/skills/design-fork.md:118-120` — «Формула «X верно по
  существу, чинится Y» — крен, не вывод
  (`question-delegation.md` §Калибровка).»
- `.claude/processes/question-delegation.md` — полный перечень
  заголовков: §Суть, §Карта владельцев, §Что доходит до пользователя,
  §«Критерий один: выводится ли ответ из концепции», §«Составная
  развилка расщепляется по этому же критерию», §«Что критерий НЕ
  разрешает», §Дайджест решений, §«Источниковый дефицит — эскалация
  всегда», §«Взвешивание срока (now-vs-later)», §«Что выключено до
  конца фазы 1», §Связи. Слова «Калибровк» в файле **нет вовсе**
- Второй адрес того же класса:
  `.claude/skills/design-fork.md:82` — «(`.claude/templates/docs/gap-report.md`
  §одноимённый)»: адрес **прозвищем**, а не именем пассажа; в
  `gap-report.md` слова «одноимённ» нет, целевой пассаж называется
  «**Ось структурной свободы обязательна.**»

**Целевой док.** `.claude/skills/concept-review.md`,
`.claude/skills/design-fork.md` — переадресация на
§«Критерий один: выводится ли ответ из концепции» и на
§«Ось структурной свободы обязательна» соответственно.

**Ожидаемый владелец.** `knowledge-curator`.

**Трассировка.** Автоматический разбор 595 `§`-адресов дал 116
подозрений; **каждое подозрение по `docs/**` (22) открыто вручную —
битых нет** (все оказались ключами спек `docs/spec/*.json`,
подтверждёнными грепом, либо ссылками `П1 §4` на нумерованные
следствия принципа). По `.claude/**` вручную открыто 20 подозрений;
эти два подтверждены грепом по целевым файлам («Калибровк» — 0
попаданий; «одноимённ» — 0 попаданий), остальные оказались лид-жирными
пассажами, разорванными переносом строки, либо целями, названными
прозвищем и разрешимыми (`codestyle §«Неиспользуемый код»`).

**Варианты.** Развилки нет — целевые пассажи существуют под другими
именами, адрес чинится дословной записью
(`.claude/rules/structure.md` §Принципы: «Адрес пишется дословно по
имени пассажа»).

**Ярлык исхода.** `выводимо`.

**Ярлык дефицита.** Дефицита нет — закрыто прогоном.

---

## Гигиена

Счёты и точки; полный набор полей находки к позициям этого раздела не
применяется.

### Ссылочная целостность — счёт

| Измерение | Проверено | Автофлаг | Открыто вручную | Подтверждено битых |
|---|---|---|---|---|
| Файловые ссылки (`.md`/`.json`/`.sh`/`.java` в бэктиках), 321 файл | 2969 | 142 | 47 | **36** (см. таблицу ниже) + 24 в `.claude/notes/**` (архивный класс) |
| `§«…»`-адреса, `docs/**` | 204 | 22 | **22 (все)** | **0** |
| `§«…»`-адреса, `.claude/**` (без `notes/`) | 391 | 94 | 20 | **2** (N8) + 3 сокращённых/неточных |
| **Итого `§`-адресов** | **595** | 116 | 42 | **2 битых + 3 сокращённых** |

**Не проверено (запрещённая область).** 8 ссылок из
`.claude/work/backlog.md` в `.claude/work/history/**` — цель вне
охвата линзы; помечены как непроверенные, а не как разрешённые.
Остаток 95 автофлагов по `.claude/**` (`notes/`, `tests/`, `chat/`,
`backlog.md`) вручную не вскрывался — по выборке 47 доля ложных
срабатываний ~60 % (относительные сокращения вида `contracts/order.md`,
`rules/adapter-constants.md`, разрешимые от каталога дока).

### Битые ссылки на несуществующие каталоги — живые файлы

| Цель (не существует) | Носители |
|---|---|
| `docs/decisions/` | `.claude/agents/solution-designer.md:57`, `.claude/skills/classify-type.md:86`, `.claude/work/backlog.md:887`, `.claude/work/progress/corpus-rewrite-mapping.md:112,126,130,163,228,376,616,652` — **11** |
| `docs/models/core/` | `.claude/decisions/model-layer-ontology.md:12`, `.claude/decisions/models-core-vs-other.md:26,52,57` — **4** |
| `docs/models/other/` | `.claude/decisions/model-layer-ontology.md:13`, `.claude/decisions/models-core-vs-other.md:29,52,58` — **4** |
| `docs/client/` | `.claude/decisions/model-layer-ontology.md:126,128,141,184` — **4** |
| `docs/models/persistence/` | `.claude/rules/structure.md:53`, `.claude/skills/classify-type.md:73,122`, `.claude/decisions/model-layer-ontology.md:79`, `corpus-rewrite-mapping.md:554` — **5** (см. N7) |
| `docs/models/externalSnapshot/` | `.claude/rules/structure.md:49`, `.claude/skills/classify-type.md:67,121`, `.claude/decisions/model-layer-ontology.md:57`, `corpus-rewrite-mapping.md:512` — **5** (см. N7) |
| `docs/models/mapping/rules/`, `docs/rules/mapping/` | `.claude/decisions/model-layer-ontology.md:157` — **2** |
| `.claude/work/blind/` | `.claude/processes/question-delegation.md:146` — **1** (контекст — описание снятого механизма; чинится пометкой, не восстановлением каталога) |

**Ложные срабатывания, проверенные открытием цели** (в перечень не
входят): `docs/endpointFunctionList.md`
(`.claude/skills/integration-okx.md:121` — явно «**в его
репозитории**», внешний SDK); `docs/roadmap/`
(`.claude/decisions/product-roadmap-type.md:88` — **отвергнутая
альтернатива**); `docs/integrations/okx/ws/`, `rules/okx-ws-channels.md`
(`open-questions.md:71-72` — **предлагаемые** варианты нерешённого
вопроса); `Java/Controller.md` (`.claude/rules/naming.md:17` — цель
`.claude/templates/code/Java/Controller.md` существует);
`snapshots/snapshot-vN.md` (шаблон имени, не файл).

### Сокращённые / неточные `§`-адреса

| Носитель | Адрес | Фактическое имя пассажа |
|---|---|---|
| `.claude/rules/codestyle.md:365` | `§Контроллеры/API` | `## Контроллеры / API` (пробелы) |
| `.claude/rules/tech-radar.md:45` | `backlog.md §Инфра-долг I2` | склейка двух уровней: `## Инфра-долг (Boot 4 миграция / рантайм-робастность)` + `### I2. Миграция кода на Jackson 3` |
| `.claude/rules/tech-radar.md:46` | `.claude/work/backlog.md §Унификация инфраструктуры джоб` | лид-жирный `**Унификация инфраструктуры джоб — горизонт фаза 3 (код-ревью заход 2, …**` — адрес обрезан |

### `.claude/knowledge-tree.md` против фактики

| Позиция | Точка |
|---|---|
| **`.claude/work/questions/` отсутствует в дереве целиком** — вместе с `open-questions.md` (фиксированный файл, зарегистрирован `.claude/rules/structure.md:78`) и подкаталогом `tasks/`. Файл имеет входящие ссылки из `concept-review.md`, `structure.md`, `classify-type.md` | `.claude/knowledge-tree.md:148-166` (узел `work/`); грепом `questions` по дереву — 0 попаданий |
| **`tools/` отсутствует в дереве целиком** — 5 артефактов: `spec-run.sh`, `boot-test.sh`, `preconditions-check.sh`, `postman/TradingBot.postman_collection.json`, `postman/TradingBot.local.postman_environment.json`. Дерево заканчивается `src/` и `.claude-archive/` | `.claude/knowledge-tree.md:422-424` |
| Прочих расхождений нет: **0** узлов дерева указывают на несуществующие файлы; 345 файловых узлов сверены с 389 фактическими (дельта — 44, из них 42 в запрещённой области `progress/…docs-check-26/27`, `gaps-close-27` и `.claude/projects/`, 2 — `open-questions.md` и `tools/`) | скрипт-сверка |

### Размещение и незарегистрированные артефакты

| Позиция | Точка |
|---|---|
| **`tools/` не зарегистрирован в таблице размещения.** `.claude/rules/structure.md` строки для `tools/` не имеет, хотя сама на него ссылается («Прогон — `tools/spec-run.sh`» в строке `docs/spec/`). Курация §5: «Файл/папка вне типов таблицы — классифицировать» | `.claude/rules/structure.md:69`; `ls tools/` |
| **`.claude/work/2026-08-26-step-7-question-flow-analysis.md`** — файл в корне `work/`, тип которого таблица не предусматривает (`work/` держит `backlog.md`, `decision-digest.md` и подкаталоги). Имеет **живые** входящие ссылки из `.claude/rules/policy-home.md` и `.claude/rules/design-simplicity.md`, то есть в `history/` не переносим без правки правил | `.claude/work/2026-08-26-step-7-question-flow-analysis.md`; `.claude/rules/structure.md:75-84` |
| **`.claude/projects/`** — в дереве есть (узел с плейсхолдером `<путь-проекта>`, помечен «артефакт харнесса»), в таблице `structure.md` строки нет | `.claude/knowledge-tree.md:87` |
| `docs/models/api/OkxRawApiRequest.md` описывает DTO **тест-контура** источника (`POST /api/proxy/okx/raw`), тогда как знание контура тестов `.claude/decisions/test-knowledge-type.md` относит в `.claude/tests/`. Граница «api-модель нашего сервиса» vs «артефакт контура» в таблице не разведена | `docs/models/api/OkxRawApiRequest.md:1-10`; `.claude/rules/structure.md:54` |

### «Вопрос файла ровно один»

Проверено 318 файлов знания; раздел «На какой вопрос отвечает этот
файл» присутствует у **всех** (0 пропусков). Склейка нескольких
вопросов — **5 подтверждённых** (из 41 кандидата с союзом «и»;
остальные — перечисления внутри одного вопроса либо двоеточная
расшифровка, что правилом не запрещено):

| Файл | Формулировка | Сколько вопросов |
|---|---|---|
| `.claude/decisions/model-layer-ontology.md:5` | «Как организованы… **куда уходит** не-модельное биржевое знание, **как живёт** маппинг между слоями» | 3 |
| `.claude/decisions/source-api-target-rebase.md:5` | «Почему контур… бьёт в сырьё…, **и какими вариантами** решены сопутствующие развилки» | 2 |
| `.claude/notes/2026-05-29-ростер-тулинга-роадмап.md:5` | «Какой состав тулинга нужен…, **в каком порядке** артефакты материализуются **и как устроен** каждый» | 3 |
| `.claude/notes/2026-06-05-аудит-делегируемости-вопросов.md:5` | «Кто… может владеть каждым вопросом… — **и какие вопросы** можно снять с пользователя» | 2 |
| `.claude/notes/2026-07-06-аудит-контекстной-стоимости.md:5` | «Что показал аудит… **и какие предложения** по оптимизации из него следуют» | 2 |

Пограничные, оставлены без находки (одна операция, объекты
перечислением): `.claude/skills/update-roadmap-progress.md`
(«обновить статус шага **и** пересчитать статус фазы»),
`docs/processes/candle-loading.md` («процесс добычи **и** поддержания
целостности»), `docs/integrations/okx/contracts/max-size.md` (один док
на `max-size` **и** `max-avail-size`; имя файла — по первому).

### Курация — прочее

| Позиция | Точка |
|---|---|
| `.claude/notes/` — 5 заметок несут ~24 ссылки в каталоги и файлы **исчезнувшего** корпуса (`docs/models/Position.md`, `docs/client/`, `docs/conventions/`, `docs/rules/exchange-hold-policy.md` и т. п.). По `.claude/rules/curation.md` §4 — кандидаты в `history/`; **три из них имеют живые входящие ссылки** (провенанс правил), поэтому переносятся не все | `.claude/notes/2026-05-26-*.md:60-575`, `2026-06-05-*.md:74,99`, `2026-07-02-*.md:54`, `2026-08-23-*.md:77,199,201` |
| `.claude/tests/source-api/okx/plan.md` — 339 KB; `.claude/work/backlog.md` — 173 KB; `docs/rules/risk-policy.md` — 50.7 KB; `docs/rules/manual-halt.md` — 48.2 KB (45 внутренних `§`-адресов на файл — см. N1, вариант C) | `find docs .claude -name '*.md' -printf '%s %p'` |

---

## Сводка

| Раздел | Число |
|---|---|
| Находки — блокирует выход в CODE | **0** |
| Находки — прочее | **8** (N1–N8) |
| Позиции гигиены | **24** |

**Пересчёт по телу.** Гейтящих — 0 (раздел пуст). Прочих — 8:
N1 (`§`-адресация vs концепция), N2 (процессная арматура в корпусе),
N3 (формула прозой при живой спеке + предикат без спеки),
N4 (дом политики в mapping-доке), N5 (довод в словарной статье),
N6 (`OkxRawApiRequest`: обрыв ссылки, §Связи не туда),
N7 (`persistence/` и `externalSnapshot/` без носителей),
N8 (битый `§`-адрес в обязательном входе линз).

Гигиена — 24 конкретные точки дефекта (таблица счёта ссылочной
целостности — измерение, не позиция): битые каталоги — 8 (по целям);
сокращённые/неточные `§`-адреса — 3; расхождения дерева — 2
(`work/questions/`, `tools/`); размещение и незарегистрированные
артефакты — 4; склейки вопроса файла — 5; курация — 2.
8 + 3 + 2 + 4 + 5 + 2 = 24.

**По ярлыкам исхода.** `варианты-с-креном` — **5** (N1, N2, N3, N4, N7);
`выводимо` — **3** (N5, N6, N8). 5 + 3 = 8.

**По ярлыкам дефицита.** `подтверждение` — **1** (N1, только выбор
между вариантом B и вариантом D: D переоткрывает форму, ратифицированную
концепцией). `работа` — **1** (N3, вариант D: что несут поля `note` в
`docs/spec/`). Остальные **6** — дефицита нет, закрывает CC. 1 + 1 + 6 = 8.

**По владельцам.** `knowledge-curator` — 6 (N1, N2, N4, N5, N7, N8);
`solution-designer` — 1 (N3); `integrator` — 1 (N6).

**Вынос пользователю.** Одна позиция — N1, развилка B-vs-D: сохранять
ли запрет адресной ссылочной арматуры в `docs/concept.md` §Ссылки при
том, что корпус несёт 204 адреса и часть из них — навигация по
40-50 KB доку. Остальное закрывается автономно.

**Рекомендация.** `GAPS_CLOSE_28` нужен, но **выход в `CODE` линза G не
блокирует**: все восемь находок — о форме корпуса и его реестрах, ни
одна не оставляет шаг без спецификации того, что он исполняет.
Единственная находка, чей исход стоит закрыть до массового свипа, —
N1: пока область действия `docs/concept.md` §Ссылки и
`.claude/rules/structure.md` §Принципы не разведена, каждый следующий
прогон переоткрывает 204 адреса заново.
