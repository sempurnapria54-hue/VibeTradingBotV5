# `DOCS_CHECK_27`, линза F — предусловия выхода в `CODE` и рабочие файлы

## На какой вопрос отвечает этот файл

Какие пробелы линза F нашла в предусловиях выхода шага 7 в `CODE` и в
рабочих файлах.

## Охват

**Проверено (полностью):**

- `.claude/work/backlog.md` (1805 строк), `.claude/work/questions/open-questions.md`
  (350 строк), `.claude/work/questions/tasks/` (**пуст** — только `.gitkeep`);
- `.claude/work/roadmap/roadmap.md`, `.claude/work/roadmap/phase-1.md`;
- `.claude/tests/source-api/okx/code-preconditions.md` (реестр целиком);
- `.claude/processes/source-api-testing.md`, `.claude/skills/update-roadmap-progress.md`,
  `.claude/processes/roadmap-step-execution.md` §Гейты / §3 / §4.

**Проверено точечно (греп + чтение секций):** `.claude/tests/source-api/okx/plan.md`
(2908 строк) — шапка (1-135), §Структурный изоморфизм (137-150), §Исходы
содержательного кейса (203-230), §Инвариант полноты (231-262), все десять
гейтящих кейсов и их соседи, хвост (2845-2908);
`.claude/tests/source-api/okx/collection.postman_collection.json` (греп по
идентификаторам кейсов); `src/test/java/com/example/tradingbot/integration/sourceapi/okx/`
(62 файла — греп по идентификаторам кейсов, чтение `Ag6BillSubtypesLiveTest`,
`OkxSourceApiLiveTestBase.observe`); `docs/spec/` (прогон `tools/spec-run.sh`);
`docs/rules/pnl-reconciliation.md` (заголовки + греп якорей);
`.claude/processes/okx-coverage-manifest.md`, `.claude/processes/api-docs-completion.md`,
`.claude/knowledge-tree.md`, `.claude/decisions/gating-node-closure-depth.md`.

**Вне охвата:** `docs/` кроме адресов, на которые ссылаются мои файлы
(линзы A-E); `src/` кроме контура `sourceapi/okx` (не предмет);
запрещённые прогоном носители — `.claude/work/history/**`,
`progress/phase-1-step-7-docs-check-26*`, `progress/phase-1-step-7-gaps-close-26.md`,
`progress/phase-1-step-7-chronicle.md`, `.claude/snapshots/**`,
`.claude/work/decision-digest.md` — **не открывались**; ни одна находка на
них не опирается.

**Дисклозура независимости.** Проверяемые файлы плотно размечены маркерами
`H<N> DOCS_CHECK_<M>` / `GAPS_CLOSE_<M>` (PROC-Q3 `open-questions.md`); линза
видела, какие места уже правились. Ни одна находка ниже не выведена из
маркера — все стоят на прочитанных носителях и на прогонах (`spec-run.sh`,
грепы по `src/` и по коллекции).

## Стадия остановки

**Стадия 2 — обход пройден целиком, ранней остановки не было.**

Стадия 0 чиста в части `open-questions.md`: **гейтящих механику шага 7
открытых вопросов нет** (разбор — §«Сверка с `open-questions.md`» ниже).
Гейт грунта (реестр предусловий) обход **не останавливает** по §4
`roadmap-step-execution.md`: находки с единственным дефицитом `грунт`
собираются, не дожидаясь чистого прогона, — поэтому проверка продолжена по
всем стадиям охвата.

---

# Находки — блокирует выход в `CODE`

## F1. Реестр предусловий и план расходятся именем кейса (п. 1) и статусом (п. 2)

- **Вопрос.** Реестр — дом ответа «что обязано быть добыто до `CODE`», а его
  колонки «Кейс» и «Статус» у двух пунктов не совпадают с планом: п. 1
  называет только `AG1.5`, тогда как план и `backlog.md` называют носителем
  п. 1 **и** `AG1.9`; п. 2 несёт один статус «⚠️ перепрогон» на пару
  `AG6.1` + `AG6.2`, тогда как `AG6.2` **никогда не гонялся** (⏳ `PENDING`).
  Правило гейта — «предусловие закрывается прогоном **своего** кейса»;
  исполнитель, читающий реестр, закроет п. 1 одним `AG1.5`, а п. 2 сочтёт
  «уже наблюдалось, нужен лишь перепрогон» — тогда как половина п. 2 вообще
  не имеет наблюдения.
- **Цитаты (носители).**
  - `.claude/tests/source-api/okx/code-preconditions.md:43` — «| 1 | Агрегирует
    ли источник partial-закрытия … | `AG1.5` | ⏳ не собрано |»
  - `.claude/tests/source-api/okx/code-preconditions.md:44` — «| 2 | Справочник
    типов движений … | `AG6.1`, `AG6.2` | ⚠️ перепрогон (наблюдение сохранено
    мощностью, не перечнем) |»
  - `.claude/tests/source-api/okx/plan.md:1677` — «### AG1.9 … **ГЕЙТИТ `CODE`**
    (предусловия **пп. 1 и 15**)»
  - `.claude/tests/source-api/okx/plan.md:84` — «`AG1.9` — пп. 1 и 15»
  - `.claude/tests/source-api/okx/plan.md:1974` — «### AG6.2 … ⏳ **PENDING** …
    (предусловие п. 2)»
  - `.claude/work/backlog.md:626-629` — «`AG1.5` (п. 1), `AG1.9` (пп. 1 и 15 —
    вторая половина кейса несёт самостоятельное предусловие)»
- **Целевой док.** `.claude/tests/source-api/okx/code-preconditions.md`
  (правка двух ячеек таблицы: п. 1 — добавить `AG1.9`; п. 2 — развести статус
  по кейсам: `AG6.1` ⚠️ перепрогон / `AG6.2` ⏳ не собрано).
- **Ожидаемый владелец.** `integrator` (владелец знания о контракте источника
  и о фактах, которые от него нужны); исполнение — `tester` при прогоне.
- **Трассировка.** Открыл реестр целиком → выписал тройки «пункт → кейс →
  статус» → грепнул каждый идентификатор в `plan.md` (`grep -n "\bAG1\.5\b" …`
  и т. д.) → сверил заголовки кейсов, где статус и гейт-разметка живут
  дословно → сверил третьим носителем (`backlog.md` §«Гейт `CODE` упирается
  в»). Расхождение воспроизводится на всех трёх.
- **Варианты.**

  | Вариант | Что делает | Названная цена |
  |---|---|---|
  | (а) Достаточно существующего носителя: правится реестр | Две ячейки приводятся к плану; план и backlog не трогаются | Правка клаузы в одном доке (низ шкалы следа). Контекстная связность: 0 новых мест — реестр уже дом. **Проверяемость:** после правки колонка «Кейс» реестра = множеству гейт-пометок в заголовках плана, сверяется одним грепом |
  | (б) Правится план (п. 1 снимается с `AG1.9`) | `AG1.9` объявляется носителем только п. 15 | Цена — потеря факта: заголовок `AG1.9` называет «окно с несколькими записями = эпизоды сделки», и это **и есть** вторая половина вопроса п. 1 («агрегирует ли в одну запись на `posId`»); снятие делает п. 1 закрываемым наблюдением, которое его не покрывает |
  | (в) Развести п. 1 на два пункта реестра | «одна запись на `posId`» и «одна запись на эпизод» становятся разными предусловиями | Больший след (значение перечня + перенумерация); счётной разницы нет — оба кейса всё равно гоняются на одной фикстуре §AG1.5 |
- **Крен — (а).** Минимальный след, и он же единственный, где ни один
  наблюдаемый факт не теряется. (б) отбрасывается счётным доводом: п. 1
  закрывался бы наблюдением, не покрывающим его вторую половину.
- **Ярлык исхода.** `варианты-с-креном`.
- **Ярлык дефицита.** Дефицита нет — закрыто прогоном (правка выводима из
  сопоставления носителей).

## F2. У девяти из десяти гейтящих кейсов нет исполнимого носителя

- **Вопрос.** Предусловие закрывается **прогоном** кейса. Прогонять нечего:
  из десяти гейтящих кейсов код-тест есть только у `AG6.1`, и в
  Postman-коллекции — тоже только `AG6.1`. Реестр при этом объявляет «Кейс на
  каждый пункт заведён» — верно про **описание** кейса и неверно про
  **исполнимость**; между «кейс описан» и «кейс можно прогнать» разницы
  реестр не проводит, и она вся.
- **Цитаты (носители).**
  - `.claude/tests/source-api/okx/code-preconditions.md:55-57` — «**Итого
    одиннадцать; собрано ноль.** Кейс на каждый пункт заведён»
  - `.claude/tests/source-api/okx/code-preconditions.md:27-29` — «Предусловие
    закрывается **прогоном своего кейса** в контуре …»
  - `src/test/java/com/example/tradingbot/integration/sourceapi/okx/` — 62
    файла; греп `AG1.5|AG1.6|AG1.7|AG1.8|AG1.9|AG3.4|AG6.2|MG7.5|M17.5` даёт
    **ноль** попаданий (единственное попадание — `AG6.1` в
    `Ag6BillSubtypesLiveTest.java:21`)
  - `.claude/tests/source-api/okx/collection.postman_collection.json` — греп
    тех же девяти идентификаторов даёт **ноль** попаданий (`AG6.1` — 6)
  - `.claude/tests/source-api/okx/plan.md:73-75` — «`M17.5` … **Код-теста
    нет**; в прогон 2026-06-20 не входил» — оговорка сделана **только** для
    `M17.5`, хотя верна для девяти
- **Целевой док.** (1) `.claude/tests/source-api/okx/code-preconditions.md` —
  колонка «носитель прогона» либо явная строка о его отсутствии; (2) сами
  носители — код-тесты и запросы коллекции (работа `tester`).
- **Ожидаемый владелец.** `tester` (автор плана, коллекции и код-тестов —
  `.claude/processes/source-api-testing.md` §Участники).
- **Трассировка.** Из реестра взял десять гейтящих идентификаторов → грепнул
  каждый по каталогу код-тестов и по коллекции → нашёл единственное попадание
  → прочитал `Ag6BillSubtypesLiveTest` целиком, чтобы отличить «тест есть» от
  «тест покрывает» (см. F3) → проверил, называет ли план отсутствие
  носителей: называет для одного кейса из девяти.
- **Варианты.**

  | Вариант | Что делает | Названная цена |
  |---|---|---|
  | (а) Написать девять код-тестов + запросы коллекции | Гейт становится исполнимым | Реальная работа `tester`; **но** блокирована процессом до аппрува пары план+коллекция (F4) и средовым дефицитом (P8). Контекстная связность низкая: восемь из девяти висят на одной фикстуре §AG1.5 (цепочка open→partial-close→close), то есть это один заход, а не девять |
  | (б) Записать отсутствие носителей в реестр колонкой | Гейт остаётся неисполнимым, но перестаёт читаться как «осталось нажать кнопку» | Правка одного дока; цена — «записать ограничение как известное» не двигает `CODE` ни на шаг, и отложенная цена та же самая |
  | (в) Переоценить гейтящесть части предусловий | Часть уходит в «записанное ограничение», как уже сделано с допуском сверки и `M15.7` | Переоткрывает решения держателя по каждому снимаемому пункту; счётного основания нет — ни один пункт не стал выводимым из концепции |
- **Крен — (а) + (б) одним ходом:** носители пишутся, а до их появления реестр
  несёт колонку «носитель прогона» (иначе строка «Кейс … заведён» продолжает
  читаться как готовность). Порядок задан F4: сперва аппрув пары, затем
  код-тесты — процесс это мандатит явно.
- **Ярлык исхода.** `варианты-с-креном`.
- **Ярлык дефицита.** `работа` (написание носителей) — не `подтверждение`:
  ни численной величины, ни продуктового scope здесь нет.

## F3. Код-тест единственного «покрытого» гейтящего кейса не способен произвести требуемое наблюдение

- **Вопрос.** Предусловия пп. 2 и 16 требуют **перечень** пар
  `type`/`subType`; план прямо называет прошлый дефект — «в отчёт кейса ушла
  только **мощность** множества». Но исполнимый носитель перепрогона —
  `Ag6BillSubtypesLiveTest.ag6_1_directDictionary` — пишет через
  `observe(...)`, который логирует **`data.size`** и три кода, и ничего из
  содержимого `data`. Перепрогон в текущем виде воспроизведёт ровно тот же
  дефект: вернёт `data.size=32` и снова не сохранит перечень. Требование
  «сохранить **все** пары с описанием» носителя не имеет.
- **Цитаты (носители).**
  - `.claude/tests/source-api/okx/plan.md:1945` — «### AG6.1 … ⚠️ ПЕРЕПРОГОН —
    **ГЕЙТИТ `CODE`** (предусловия пп. 2 и 16: наблюдение сохранено мощностью,
    не перечнем)»
  - `.claude/tests/source-api/okx/plan.md:1965-1967` — «**Что делать при
    перепрогоне:** сохранить **все** пары `type`/`subType` с описанием
    источника — в исход кейса (датированное наблюдение)»
  - `src/test/java/…/okx/Ag6BillSubtypesLiveTest.java:22-29` — ассерты
    `assertOk` / `isArray` / `dataEmpty` + `observe("AG6.1", r)`; содержимого
    `data` тест не читает
  - `src/test/java/…/okx/OkxSourceApiLiveTestBase.java:372-376` — `observe(...)`
    логирует `data.size=…`, `data0.sCode`, `data0.sMsg`; элементов массива не
    выводит
  - `.claude/work/backlog.md:1436-1442` — «Требуется **перепрогон с записью
    перечня**, и он обслуживает оба предусловия»
- **Целевой док.** `.claude/tests/source-api/okx/plan.md` §AG6.1 (кейс обязан
  назвать, **чем** перечень сохраняется) + код-тест `Ag6BillSubtypesLiveTest`
  (работа `tester`).
- **Ожидаемый владелец.** `tester`.
- **Трассировка.** Прочитал §AG6.1 → увидел требование «сохранить все пары» →
  пошёл проверять, чем оно исполняется: открыл единственный код-тест `AG6.1`
  целиком → он ничего из `data` не читает → открыл `observe(...)` в базовом
  классе → подтвердил: инструмент фиксации по построению пишет мощность.
  Отрицательная посылка проверена чтением обоих носителей, не памятью.
- **Варианты.**

  | Вариант | Что делает | Названная цена |
  |---|---|---|
  | (а) Достаточно существующего носителя: расширить `observe(...)` | Базовый хелпер печатает элементы `data` | Одна правка, но бьёт по **всем** кейсам контура: логи 200 тестов распухают; часть `data` несёт ордера/позиции. Контекстная связность плохая — каждый вызывающий обязан знать, что теперь печатается всё |
  | (б) Кейс-специфичный дамп в `ag6_1_directDictionary` | Тест сам выписывает пары в лог/файл наблюдения | След — один тест-метод; связность нулевая. Цена: у контура появляется первый кейс с собственной фиксацией — прецедент, который придётся повторить для `AG6.2`, `AG1.7`, `AG3.6` (они тоже требуют **состав**, не форму) |
  | (в) Ввести в базу отдельный `observeContent(caseId, r)` | Явный второй инструмент: «наблюдение формы» и «наблюдение состава» | Больший след (метод в базе + конвенция, когда какой); **счётная разница названа**: носителей состава уже четыре, и (б) для них — четыре копии одного кода |
- **Крен — (в).** Больший след обоснован счётно: четыре содержательных кейса
  шага 7 требуют состава, (б) даёт четыре копии, (а) меняет поведение всех
  ~200 кейсов ради четырёх.
- **Ярлык исхода.** `варианты-с-креном`.
- **Ярлык дефицита.** `работа`.

## F4. Клейм полноты «коллекция 1:1 с планом» ложен; семнадцать кейсов шага 7 не проходили `REVIEW`/`APPROVE`

- **Вопрос.** План объявляет коллекцию 1:1 с собой и называет её
  «ревью/аппрув-артефактом». Фактически **ни один** из семнадцати
  содержательных кейсов шага 7 в коллекции не представлен. Отсюда два
  следствия, и второе тяжелее: (1) клейм полноты ложен и запрещает писателю
  искать дальше; (2) процесс контура даёт аппрув **на пару** план + коллекция
  и говорит «код-тесты и прогон **до аппрува не запускаются**» — значит сбор
  грунта по десяти гейтящим кейсам процедурно заблокирован, а единственный
  зафиксированный аппрув (2026-06-19) относится к составу плана **до**
  появления этих кейсов.
- **Цитаты (носители).**
  - `.claude/tests/source-api/okx/plan.md:145-147` — «Коллекция
    (`collection.postman_collection.json`) — **1:1 с этим планом**. …
    коллекция — ревью/аппрув-артефакт»
  - `.claude/tests/source-api/okx/plan.md:18-20` — «**DESIGN аппрувнут**
    (2026-06-19): план + коллекция прошли два независимых адверсариальных
    ревью»
  - `.claude/tests/source-api/okx/plan.md:66-69` — «**Счёт прогона 2026-06-20
    относится к составу плана на ту дату.** … кейсы, заведённые после него, в
    них не входят» — оговорка про **счёт**, но не про **аппрув**
  - `.claude/processes/source-api-testing.md:69-72` — «APPROVE. … Аппрув — на
    пару план + коллекция. **Код-тесты и прогон до аппрува не запускаются.**»
  - `.claude/processes/source-api-testing.md:64-67` — «REVIEW плана и
    коллекции … **соответствие план ↔ коллекция**»
  - `collection.postman_collection.json` — греп семнадцати идентификаторов:
    попадания только у `AG6.1` (кейс от 2026-06-19)
- **Целевой док.** `.claude/tests/source-api/okx/plan.md` §Статус и
  §Структурный изоморфизм (клейм 1:1 сводится к фактике; называется, какие
  кейсы вне коллекции и почему) + прохождение этапов 2-3 процесса на дельте
  плана.
- **Ожидаемый владелец.** `tester` (автор пары); `reviewer` фокус
  `test-review` — этап 2.
- **Трассировка.** Читая §Изоморфизм, заметил клейм полноты → по правилу
  «отдельный проход по носителям с клеймом полноты» проверил его грепом по
  коллекции, а не чтением → ноль попаданий → вернулся в процесс, чтобы понять
  последствие: аппрув даётся на пару, прогон до аппрува запрещён → сверил
  дату аппрува с датами добавления кейсов (`M17.5` — 2026-08-29).
- **Варианты.**

  | Вариант | Что делает | Названная цена |
  |---|---|---|
  | (а) Достроить коллекцию до 1:1 и провести этапы 2-3 на дельте | Клейм становится истинным, аппрув покрывает гейтящие кейсы | Работа: 17 кейсов, из них цепочки (`M17.5` — три цепочки, 31 строка). Единственный путь, при котором прогон грунта не нарушает собственный процесс |
  | (б) Снять клейм 1:1 и объявить коллекцию срезом на 2026-06-19 | Ложная запись чинится, коллекция замораживается | Правка клаузы; цена названа процессом: аппрув-артефактом становится нечего ревьюить, а этап 3 требует пару — то есть гейтящие кейсы выводятся из-под аппрува насовсем |
  | (в) Переструктурировать: аппрув-артефактом объявить план, коллекцию — опциональной | Убирает пару из процесса | **Переоткрывает принятое** (`source-api-testing.md` §APPROVE). Выписан, не выбран: цена — исчезает единственный артефакт, на котором ревьюер проверяет «соответствие план ↔ коллекция», а он ловит именно расхождения формы запроса |
- **Крен — (а), с (б) как немедленной частью:** ложная запись чинится сразу и
  независимо от того, когда будет достроена коллекция (правило «дефект записи
  и продуктовая ось разводятся»).
- **Ярлык исхода.** `варианты-с-креном`.
- **Ярлык дефицита.** `работа`.

## F5. Правило закрытия предусловия исходом `OBSERVED_ABSENT` адресует снесённый дом реестра

- **Вопрос.** `OBSERVED_ABSENT` — единственный исход, закрывающий гейт без
  наблюдения события, и он **обязывает** записать посылку-достройку «в
  реестре предусловий». Адрес реестра в этом правиле —
  `docs/rules/pnl-reconciliation.md`, то есть **прежний** дом, снесённый
  переписыванием корпуса и восстановленный в
  `.claude/tests/source-api/okx/code-preconditions.md`. В целевом файле нет ни
  слова «реестр», ни «предусловие», ни `OBSERVED_ABSENT`. Исполнитель,
  закрывающий предусловие этим исходом, не найдёт, куда писать обязательную
  строку, — и «молча закрыть гейт нельзя» превращается в «закрыть молча».
  Реестр со своей стороны на `OBSERVED_ABSENT` ссылается — механика разорвана
  ровно посередине.
- **Цитаты (носители).**
  - `.claude/tests/source-api/okx/plan.md:214-218` — «**Правило.** Исход
    `OBSERVED_ABSENT` **закрывает** гейтящее предусловие `CODE` … в реестре
    предусловий (`docs/rules/pnl-reconciliation.md`) у соответствующего пункта
    появляется строка «посылка проверкой **не подтверждена** …»
  - `.claude/tests/source-api/okx/code-preconditions.md:29-31` — «исход
    `OBSERVED_ABSENT` закрывает предусловие наравне с наблюдением — тогда у
    пункта появляется строка «посылка не подтверждена, ветка недостижима»»
  - `.claude/tests/source-api/okx/code-preconditions.md:9-15` — «Прежний дом
    реестра — `docs/rules/pnl-reconciliation.md`, и это было ошибкой
    размещения … снесло реестр, **не перенеся** его»
  - `docs/rules/pnl-reconciliation.md` — 13 заголовков + греп
    `реестр|предусловие|OBSERVED_ABSENT` → **ноль** попаданий
  - `.claude/tests/source-api/okx/plan.md:1668-1673` — §AG1.8 планирует
    закрытие именно этим исходом («прогнан вместе с §AG1.5, ADL за время …»)
- **Целевой док.** `.claude/tests/source-api/okx/plan.md:217` — адрес меняется
  на `.claude/tests/source-api/okx/code-preconditions.md`; заодно реестр
  получает место под строку-посылку (сейчас в таблице такой колонки нет —
  строке «посылка не подтверждена» физически некуда лечь).
- **Ожидаемый владелец.** `integrator` (владелец реестра фактов источника);
  `knowledge-curator` — по части адреса-указателя.
- **Трассировка.** Читал §«Исходы содержательного кейса — их три» ради
  понимания, чем закрывается `AG1.8` (условно наблюдаемый ADL) → увидел адрес
  реестра → **не перенёс цитату, не открыв цель**: открыл
  `docs/rules/pnl-reconciliation.md`, снял заголовки, грепнул три термина →
  ноль → сверил с самим реестром, который описывает этот же переезд как
  дефект размещения. Свип переезда реестра до плана не доехал.
- **Варианты.**

  | Вариант | Что делает | Названная цена |
  |---|---|---|
  | (а) Достаточно существующего носителя: правится адрес в плане | Одна ссылка ведёт в действующий дом | Правка клаузы в одном доке. **Не полна сама по себе:** в таблице реестра нет колонки под строку-посылку, и правило продолжит быть неисполнимым |
  | (б) (а) + колонка/сноска реестра под непроверенную посылку | Строка «посылка не подтверждена, ветка недостижима» получает место | След — колонка в одной таблице; связность нулевая. Проверяемость: после закрытия `AG1.8` исходом `OBSERVED_ABSENT` строка обязана появиться, её отсутствие видно глазом |
  | (в) Снять `OBSERVED_ABSENT` как закрывающий исход | Гейт закрывается только наблюдением | **Переоткрывает решение держателя** (П18 валидации `GAPS_CLOSE_17`, вариант A, цена названа и принята). Выписан, не выбран: цена — `CODE` ждёт бессрочно события, которое биржа не обязана произвести |
- **Крен — (б).** Минимальный след, при котором правило становится
  исполнимым; (а) в одиночку чинит адрес и оставляет механику без места.
- **Ярлык исхода.** `варианты-с-креном`.
- **Ярлык дефицита.** Дефицита нет — закрыто прогоном.

## F6. У гейта грунта нет встречного якоря в исполняемом скилле

- **Вопрос.** Реестр объявляет собственное правило гейта («шаг 7 не входит в
  `CODE`, пока хотя бы одно гейтящее предусловие не закрыто»), но
  **исполняемая** проверка при смене статуса этого условия не знает:
  `update-roadmap-progress.md` §«Гейт-предусловие перехода» проверяет для
  `CODE` **только** чистый `DOCS_CHECK`; `roadmap-step-execution.md` §Гейты
  говорит то же. Реестр не упомянут ни в одном носителе механики статусов —
  входящие ссылки на него есть только из плана, backlog, дерева знания и
  дайджеста. Это тот самый класс дефекта, против которого проект уже трижды
  строил встречный якорь (закрытие фазы, свипы, глубина закрытия): условие,
  записанное только внутри правила, читает тот, кому оно и так нужно.
- **Цитаты (носители).**
  - `.claude/tests/source-api/okx/code-preconditions.md:27-28` — «**Шаг 7 не
    входит в `CODE`, пока хотя бы одно гейтящее предусловие не закрыто.**»
  - `.claude/skills/update-roadmap-progress.md:87-88` — «**`CODE`** —
    соответствующий `DOCS_CHECK` прошёл **чисто** (отчёт в истории шага)» — и
    больше ничего
  - `.claude/processes/roadmap-step-execution.md:418-423` — «**Гейт `CODE` —
    чистый `DOCS_CHECK`.** … Чистый проход `DOCS_CHECK` — обязательное условие
    гейта концепции»
  - Греп `code-preconditions` по живым носителям: `knowledge-tree.md:144`,
    `plan.md:73,919`, `backlog.md:612,625,643`, `decision-digest.md:1876` —
    **ни одного** попадания в `.claude/skills/` и `.claude/processes/`
  - `.claude/skills/update-roadmap-progress.md:16-23` — прецедент:
    «Формулировка «по сигналу из чата» снята … гейт … заведён **против**
    незамечаемого пропуска и сам страдал тем же»
- **Целевой док.** `.claude/skills/update-roadmap-progress.md`
  §«Гейт-предусловие перехода (`CODE` / `DONE`)» — второй пункт под `CODE`;
  дом формулировки — `.claude/processes/roadmap-step-execution.md` §Гейты.
- **Ожидаемый владелец.** `knowledge-curator` (дом и указатели механики
  пайплайна); формулировка гейта — `solution-designer`.
- **Трассировка.** Проверял, чем механически обеспечен «собранный грунт» как
  часть гейта `CODE` → открыл скилл простановки статуса целиком → нашёл только
  условие чистоты → перепроверил дом гейта в процессе → то же → грепнул имя
  реестра по всем живым носителям, чтобы исключить якорь где-то ещё →
  входящих ссылок из механики статусов нет. Самоприменимость правила: проект
  требует встречного якоря от правил с условием снятия — и не применил
  требование к собственному гейту грунта.
- **Варианты.**

  | Вариант | Что делает | Названная цена |
  |---|---|---|
  | (а) Достаточно существующего носителя: гейт есть, просто читается вместе с реестром | Ничего не меняется | **Опровергается конструкцией:** реестр открывает тот, кто уже решил его исполнить; статус же проставляет скилл, который реестра не знает. Ровно тот довод, которым проект снял «по сигналу из чата» |
  | (б) Пункт в §«Гейт-предусловие перехода» под `CODE`: «все гейтящие предусловия реестра шага закрыты (прогон кейса либо `OBSERVED_ABSENT`)» | Проверка исполняется в момент смены статуса | Правка § правила (середина шкалы следа); связность: скилл обязан знать путь к реестру шага — один адрес. Проверяемость: гейт механический, читается таблица |
  | (в) Обобщить: «у шага есть реестр предусловий → пройти его» с указателем в `phase-N.md` | Работает для будущих шагов, не только для 7 | Больший след (конвенция + строка в роадмапе); **счётная разница не названа** — второго шага с реестром сегодня нет |
- **Крен — (б).** Минимальный след, закрывающий именно тот разрыв, который
  наблюдается; (в) — та же правка плюс спекулятивная конвенция без второго
  случая (`design-simplicity`: спекулятивные правила не вносим).
- **Ярлык исхода.** `варианты-с-креном`.
- **Ярлык дефицита.** Дефицита нет — закрыто прогоном.

## F7. Backlog предписывает собирать `AG3.4` «после чистого `DOCS_CHECK`» — снятая редакция порядка

- **Вопрос.** Рабочий файл, адресованный исполнителю следующего под-шага,
  ставит гейтящий кейс `AG3.4` (предусловие п. 9) в «рантайм-хвост … после
  чистого `DOCS_CHECK` — порядок последовательный». Это **снятая** редакция:
  §4 процесса и сам план говорят, что находка с единственным дефицитом
  `грунт` уходит на сбор **сразу**, иначе образуется петля; §AG3.4 плана
  несёт это дословно. В том же перечне рядом стоят `AG3.5`, `AG12.5`,
  `AG12.4`, `M1.7` — негейтящие, для которых старый порядок верен; поэтому
  ошибка не видна как ошибка: она выглядит однородным перечнем.
- **Цитаты (носители).**
  - `.claude/work/backlog.md:1472-1477` — «**Рантайм-хвост на той же фикстуре
    §AG1.5** (один прогон, **после чистого `DOCS_CHECK`** — порядок
    последовательный): **H2** … **RQ-4** `ccy` fee-bills = USDT (**§AG3.4**)»
  - `.claude/tests/source-api/okx/plan.md:1808` — §AG3.4 «**Статус:** ⏳
    **PENDING — до `CODE` шага 7** … **чистого прогона концепции не ждёт —
    единственный блокер `грунт`**»
  - `.claude/processes/roadmap-step-execution.md:278-292` — «**Сбор внешнего
    грунта не ждёт чистого прогона.** … Такие находки выносятся на сбор грунта
    **сразу** … Признак петли: находка несёт `грунт` **без** сопутствующего
    `работа` или `подтверждение`»
  - `.claude/tests/source-api/okx/code-preconditions.md:33-37` — то же правило
    в реестре
  - `.claude/work/backlog.md:1417-1419` — §«Грунт `integrator`» шапкой
    утверждает **обратное** и верное: «Собирается, **не дожидаясь чистого
    прогона**» — файл несёт обе редакции одновременно
- **Целевой док.** `.claude/work/backlog.md` §«Рантайм-верификация и форвард»
  — `AG3.4` выводится из «после чистого `DOCS_CHECK`»-перечня (либо перечень
  переформулируется: «негейтящий хвост той же фикстуры»).
- **Ожидаемый владелец.** `knowledge-curator` (диета рабочих файлов,
  `closed-work-transfer.md` §«Диета рабочих файлов (решение держателя,
  2026-08-26)»); содержание порядка — дом процесса, пересказывать в backlog
  нечего.
- **Трассировка.** Грепнул «после чистого / чистого прогона» по своим файлам →
  четыре попадания: два в реестре (действующая редакция), одно в шапке §Грунт
  (действующая), одно в §Рантайм-верификация → сверил список кейсов этого
  пункта с реестром гейтящих → `AG3.4` в нём есть → открыл §AG3.4 плана,
  чтобы проверить, не изменился ли статус кейса: план несёт действующую
  редакцию дословно.
- **Варианты — развилки нет.** Дефект записи: одна из двух редакций в одном
  файле опровергнута домом политики и вторым носителем в том же файле. Правка
  курационная, продуктового выбора не содержит.
- **Ярлык исхода.** `выводимо`.
- **Ярлык дефицита.** Дефицита нет — закрыто прогоном.

## F8. Backlog называет калибратор допуска действующим «предусловием `CODE` п. 8»

- **Вопрос.** В §«Рантайм-верификация и форвард» записано, что `k`
  «вдобавок **стоит предусловием `CODE` п. 8**». Пункта 8 в реестре нет, а
  калибратор допуска **перестал быть гейтом** решением держателя
  (`GAPS_CLOSE_19`) — это записано и в реестре, и в том же backlog двумя
  секциями выше. Рабочий файл несёт снятую редакцию политики и мёртвый номер
  одновременно; исполнитель, читающий §Рантайм-верификация, получит
  двенадцатое гейтящее предусловие, которого не существует. Здесь же — ссылка
  на «`PNL-Q1` п. 3», тогда как третий пункт `PNL-Q1` закрыт и в вопросе
  осталось два.
- **Цитаты (носители).**
  - `.claude/work/backlog.md:1485-1489` — «величины (`0.01`, `0.5%`, `k`)
    провизорны … `k` вдобавок **стоит предусловием `CODE` п. 8** и вторым
    основанием **`PNL-Q1` п. 3**»
  - `.claude/work/backlog.md:634-639` — «**Калибратор допуска гейтом быть
    перестал** (решение держателя, `GAPS_CLOSE_19`): в `CODE` выходим с
    некалиброванным допуском»
  - `.claude/tests/source-api/okx/code-preconditions.md:66-70` — «**Калибратор
    допуска сверки** — гейтом быть перестал решением держателя
    (`GAPS_CLOSE_19`)»; таблица реестра пункта 8 не содержит (пп. 1, 2, 5, 6,
    7, 9, 10, 14, 15, 16, 17)
  - `.claude/work/questions/open-questions.md:142-146` — «PNL-Q1 … **Два
    места**, где система **не может измерить** собственное искажение …
    (Третий пункт — реакция на `MISMATCHED` — **закрыт** `GAPS_CLOSE_19`)»
- **Целевой док.** `.claude/work/backlog.md` §«Рантайм-верификация и форвард»,
  пункт «Калибровка epsilon сверки bills↔net (N10)» — снять «предусловием
  `CODE` п. 8» и «`PNL-Q1` п. 3», оставив указатель на §«Закрытие фазы 1 —
  снятие разведочного режима допуска сверки».
- **Ожидаемый владелец.** `knowledge-curator`.
- **Трассировка.** Грепнул `предусловие(м) `CODE` п\.` по своим файлам →
  четыре попадания → сверил каждый номер с таблицей реестра: п. 10 и п. 15 (в
  плане) существуют, п. 8 — нет → нашёл в двух носителях явное решение, что
  калибратор гейтом не является → проверил вторую половину ссылки, открыв
  `PNL-Q1`: пунктов два, третий закрыт, о чём вопрос пишет прямо.
- **Варианты — развилки нет.** Оба клейма опровергаются живыми носителями
  (реестр, решение держателя, сам `PNL-Q1`). Дефект записи; продуктового
  выбора нет.
- **Ярлык исхода.** `выводимо`.
- **Ярлык дефицита.** Дефицита нет — закрыто прогоном.

## F9. Backlog приписывает гейтящее предусловие п. 7 пункту, который сам объявляет «гейта нет»

- **Вопрос.** §«Грунт `integrator`» п. 7 говорит о **знаках** трёх операндов,
  помечает «**гейта нет**» — и завершается строкой «Предусловие `CODE` п. 7».
  Но п. 7 реестра — это **горизонт доступности `fundingFee`**, и он гейтящий;
  план разводит это дословно: «предусловия п. 7 — горизонт `fundingFee`;
  п. 14 — форма пустого значения; **знаки (2) гейта не образуют**». Зеркальная
  половина дефекта: сам горизонт `fundingFee` живёт в §Грунт п. 4 **без**
  пометки гейта и без имени кейса, а п. 14 (форма пустого значения) в §Грунт
  не представлен вовсе. Исполнитель, идущий по backlog, прочтёт «п. 7 —
  знаки, гейта нет» и снимет с плана гейтящее предусловие.
- **Цитаты (носители).**
  - `.claude/work/backlog.md:1450-1454` — «7. **Знаки трёх операндов записи и
    bill'а штрафа** (кейс `AG1.7`) — **гейта нет** … Предусловие `CODE` п. 7.»
  - `.claude/work/backlog.md:1428-1430` — «4. **Семантика `fundingFee` записи
    positions-history**: накоплен за жизнь `posId` или только за последнее
    закрытие.» — без гейт-пометки и без кейса
  - `.claude/tests/source-api/okx/plan.md:1587` — «### AG1.7 … **ГЕЙТИТ
    `CODE`** (предусловия п. 7 — горизонт `fundingFee`; п. 14 — форма пустого
    значения; **знаки (2) гейта не образуют**)»
  - `.claude/tests/source-api/okx/code-preconditions.md:47,50` — «| 7 |
    Горизонт доступности `fundingFee` в записи закрытия | `AG1.7` | ⏳ не
    собрано |»; «| 14 | Форма пустого значения числовых полей записи … |
    `AG1.7` | ⏳ не собрано |»
- **Целевой док.** `.claude/work/backlog.md` §«Грунт `integrator` для шага 7»:
  п. 7 — снять ложную привязку к номеру реестра; п. 4 — пометить гейтом и
  назвать кейс `AG1.7`; добавить п. 14 либо явно отослать к реестру.
- **Ожидаемый владелец.** `integrator` (соответствие «факт источника ↔
  предусловие»); `knowledge-curator` — по форме указателя.
- **Трассировка.** Читал §Грунт целиком, сверяя каждый пункт с таблицей
  реестра → на п. 7 обнаружил конфликт «гейта нет» ↔ «п. 7 гейтящий» → открыл
  заголовок `AG1.7` в плане, где разведение сделано дословно → вернулся в
  §Грунт искать, где живёт горизонт `fundingFee`: нашёл п. 4 без пометки →
  искал п. 14: в §Грунт отсутствует.
- **Варианты — развилки нет.** Расхождение снимается планом дословно («знаки
  (2) гейта не образуют» + «предусловия п. 7 … п. 14»). Дефект записи в
  рабочем файле; продуктового выбора нет.
- **Ярлык исхода.** `выводимо`.
- **Ярлык дефицита.** Дефицита нет — закрыто прогоном.

## F10. Манифест покрытия — источник набора запросов — адресуется по несуществующему пути и не зарегистрирован

- **Вопрос.** Манифест покрытия объявлен **источником набора запросов** плана
  и домом колонки покрытия; §«Инвариант полноты» плана («60 эндпоинтов = весь
  in-perimeter») проверяется против него, а расширение плана новыми кейсами по
  триггеру «новый эндпоинт» обязано двигать его колонку. Шесть живых носителей
  адресуют его как `docs/integrations/{name}/coverage-manifest.md` — такого
  файла **нет**. Фактически манифест лежит в
  `.claude/processes/okx-coverage-manifest.md`, при этом (а) отвечает не на
  вопрос своего каталога («Как устроен этот методологический процесс?», а он —
  «Какова полнота покрытия поверхности»), (б) **отсутствует в
  `knowledge-tree.md`**. План вдобавок отсылает за «строками
  `есть-док`/`создан`/`обновлён`» к `api-docs-completion.md`, где таких строк
  нет: это процесс, описывающий, как манифест строить.
- **Цитаты (носители).**
  - `.claude/processes/source-api-testing.md:46,107,150` — «источник набора
    запросов — манифест покрытия (`docs/integrations/{name}/coverage-manifest.md`)»;
    «Манифест покрытия (…) несёт **колонку статуса покрытия контуром**»
  - `.claude/skills/test-design.md:31,173,187` — тот же путь трижды
  - `.claude/processes/api-docs-completion.md:57-58` — «Манифест — отдельный
    док в папке источника (`docs/integrations/{name}/coverage-manifest.md`)»
  - `ls docs/integrations/okx/` → только `contracts/` и `rules/`; файла нет
  - `.claude/processes/okx-coverage-manifest.md:1-7` — «# OKX: манифест
    покрытия поверхности API … Какова полнота покрытия поверхности OKX REST
    API» (35 меток покрытия в файле)
  - `.claude/knowledge-tree.md:88-95` — листинг `processes/`: семь файлов,
    `okx-coverage-manifest.md` **не перечислен**
  - `.claude/tests/source-api/okx/plan.md:114` — «Источник набора —
    `.claude/processes/api-docs-completion.md` (строки
    `есть-док`/`создан`/`обновлён`)»
  - `.claude/tests/source-api/okx/plan.md:256-262` — «**Колонка покрытия
    манифеста** … подлежит **переразметке** … отдельным проходом владельца
    колонки»
- **Целевой док.** (1) адрес — во всех шести носителях приводится к фактике
  либо файл переносится в дом по `structure.md`; (2)
  `.claude/knowledge-tree.md` — регистрация; (3) `plan.md:114` — указатель на
  манифест, а не на процесс.
- **Ожидаемый владелец.** `knowledge-curator` (размещение и дерево);
  `integrator` — владелец содержания манифеста.
- **Трассировка.** Проверял, чем обеспечен §«Инвариант полноты» плана (клейм
  «60 = весь in-perimeter») → пошёл к манифесту по адресу из процесса →
  `ls docs/integrations/okx/` показал два каталога → `find . -iname
  "*manifest*.md"` нашёл файл в `.claude/processes/` → прочитал его шапку
  (вопрос файла — про полноту, не про процесс) → грепнул дерево знания: не
  зарегистрирован → проверил альтернативный адрес из плана
  (`api-docs-completion.md`): греп `есть-док|🟡|🟢|🔴` — одно попадание, в
  определении легенды; таблицы нет.
- **Варианты.**

  | Вариант | Что делает | Названная цена |
  |---|---|---|
  | (а) Достаточно существующего носителя: правятся шесть адресов на фактический путь | Ссылки разрешаются | Шесть правок-клауз; **но** оставляет файл в каталоге, чей вопрос он не несёт, и вне дерева — следующая курация найдёт его снова как незарегистрированный артефакт |
  | (б) Перенести манифест в `docs/integrations/okx/coverage-manifest.md` | Шесть адресов становятся верными без правки | Ноль правок в носителях; цена — противоречит переписыванию корпуса, вычистившему процессную арматуру из `docs/`, и **воспроизводит** ровно ту ошибку размещения, которую реестр предусловий описывает про себя (`code-preconditions.md:9-15`) |
  | (в) Признать манифест артефактом проверочной деятельности и дать ему дом рядом с реестром (`.claude/tests/source-api/okx/`) + правка шести адресов + запись в дерево | Тип и вопрос сходятся; манифест лежит рядом с планом, который его потребляет | Больший след (перенос + шесть правок + строка дерева). **Счётная разница названа:** единственный вариант, при котором вопрос файла совпадает с вопросом каталога, и он повторяет уже принятое решение для реестра — тот же класс артефакта, тот же дом |
- **Крен — (в).** Прецедент назван и ратифицирован: реестр предусловий
  переехал в `.claude/tests/{testType}/{source}/` ровно по этому доводу
  (`code-preconditions.md` §«Почему реестр здесь, а не в `docs/`»); манифест —
  тот же жанр («что покрыто проверкой»), и (б) переоткрывает это решение.
- **Ярлык исхода.** `варианты-с-креном`.
- **Ярлык дефицита.** Дефицита нет — закрыто прогоном.

---

# Находки — прочее

## P1. Backlog держит секцию, ложность которой он сам же констатирует

- **Вопрос.** Заголовок утверждает «2 блокера, **НЕ закрыты**», а §«Гигиена,
  заведённая прогоном `_26`» того же файла констатирует, что оба блокера
  закрыты в коде и пути устарели, с предписанием «секцию переписать или
  снять». Предписание не исполнено; ложный клейм остался в рабочем файле.
- **Цитаты (носители).** `.claude/work/backlog.md:1612` (заголовок), `:1616`
  («**Правки по ревью не вносились**»), `:1637-1641` («**Ложный клейм** … оба
  блокера **закрыты в коде** … Секцию переписать или снять»).
- **Целевой док.** `.claude/work/backlog.md` §«Агентское ревью всей кодовой
  базы (2026-07-02) — 2 блокера, НЕ закрыты».
- **Ожидаемый владелец.** `knowledge-curator`.
- **Трассировка.** Читал §Гигиена `_26` → она называет собственный файл
  носителем ложного клейма → открыл названную секцию → клейм на месте, в
  заголовке.
- **Варианты — развилки нет** (дефект записи, констатированный в том же файле).
- **Ярлык исхода.** `выводимо`. **Ярлык дефицита.** Нет — закрыто прогоном.

## P2. Ссылки вида `pnl-reconciliation.md реш.N` / `§epsilon` не разрешаются

- **Вопрос.** Четыре ссылки backlog и две ссылки плана адресуют
  `docs/rules/pnl-reconciliation.md` по якорям «реш.4/5/6» и «§epsilon». В
  целевом файле нет ни «реш.», ни «epsilon»/«ε»: тринадцать секций, допуск
  живёт под §«Допуск». По `structure.md` адрес разрешается в заголовок либо
  лид-жирный пассаж — здесь ни того, ни другого.
- **Цитаты (носители).** `.claude/work/backlog.md:542, 1245, 1352, 1489`;
  `.claude/tests/source-api/okx/plan.md:1525, 1806`;
  `docs/rules/pnl-reconciliation.md` — греп `реш\.[0-9]|epsilon|ε` → ноль.
  **Вне охвата, тот же кластер (передаю линзам A-E):**
  `docs/integrations/okx/contracts/instrument.md:35`,
  `docs/integrations/okx/contracts/position.md:98`,
  `docs/integrations/okx/contracts/trade-fee.md:31`,
  `docs/models/integrations/okx/InstrumentOkxResponse.md:73`,
  `docs/models/mapping/TradeFeeRate.md:179,188`.
- **Целевой док.** Мои шесть — `.claude/work/backlog.md` и
  `.claude/tests/source-api/okx/plan.md` (адреса приводятся к действующим
  именам секций).
- **Ожидаемый владелец.** `knowledge-curator`.
- **Трассировка.** Грепнул `реш\.[0-9]` по своим файлам → открыл цель, снял
  заголовки, грепнул якоря → ноль → расширил греп на `docs/`, чтобы понять
  размер кластера (шесть носителей вне охвата).
- **Варианты — развилки нет** (битые адреса; правка механическая).
- **Ярлык исхода.** `выводимо`. **Ярлык дефицита.** Нет — закрыто прогоном.

## P3. Backlog предписывает `StrategyDetail.positionReopenAllowed` — снятого владельца поля

- **Вопрос.** CODE-дельта «Многоэпизодная сделка» называет носителем
  `StrategyDetail`. Приземление траншей перевесило поле на `StrategyTranche`,
  и схемная дельта **того же файла** это фиксирует
  (`−strategy_details.position_reopen_allowed`, `+strategy_tranches…`). Весь
  корпус знает только `StrategyTranche.positionReopenAllowed`. Исполнитель,
  читающий дельту сверху вниз, получит противоречивое предписание.
- **Цитаты (носители).** `.claude/work/backlog.md:753`
  («**`StrategyDetail.positionReopenAllowed`** + гейт в `MANAGING`»); `:91` и
  `:93` (схема: поле на `strategy_tranches`, снято с `strategy_details`);
  `docs/models/domain/aggregate/DealTranche.md:97`,
  `docs/rules/exit-teardown-order.md:47`,
  `docs/spec/deal-tranche-lifecycle.json:10`,
  `docs/components/TrancheManagingHandler.md:23`,
  `docs/models/domain/aggregate/Strategy.md:138` — везде `StrategyTranche`.
- **Целевой док.** `.claude/work/backlog.md:753`.
- **Ожидаемый владелец.** `knowledge-curator`.
- **Трассировка.** Грепнул `StrategyDetail\.` по рабочим файлам → одно
  попадание → грепнул `positionReopenAllowed|position_reopen_allowed` по
  `docs/` (семь носителей, все — `StrategyTranche`) и по backlog (схема того
  же файла подтверждает перенос).
- **Варианты — развилки нет** (снятая редакция владельца поля).
- **Ярлык исхода.** `выводимо`. **Ярлык дефицита.** Нет — закрыто прогоном.

## P4. Счётный клейм «14 предметов, 157 примеров» опровергнут прогоном

- **Вопрос.** §«Переписывание корпуса — что осталось» утверждает «исполнимая
  спецификация — **14 предметов, 157 примеров**, прогон зелёный». Прогон
  штатного раннера даёт **17 спецификаций, 210 примеров**. Клейм — часть
  довода «работы по корпусу нет», то есть счёт здесь несёт нагрузку, а не
  украшает. Правило «перечень, а не счёт»: перечень (файлы `docs/spec/`)
  вырос, счёт не пересобран.
- **Цитаты (носители).** `.claude/work/backlog.md:36-38`; `ls docs/spec/*.json`
  → 17 файлов; `bash tools/spec-run.sh` → «Спецификаций: 17, примеров: 210 /
  ВСЕ ПРИМЕРЫ СОШЛИСЬ».
- **Целевой док.** `.claude/work/backlog.md` §«Переписывание корпуса — что
  осталось».
- **Ожидаемый владелец.** `knowledge-curator`.
- **Трассировка.** Проверял разрешимость ссылок `docs/spec/*.json` из backlog
  (все разрешились) → заметил рядом счётный клейм → пересобрал из перечня
  (`ls`) → расхождение → прогнал `tools/spec-run.sh`, чтобы получить число из
  исполнимого источника, а не подсчётом строк.
- **Варианты — развилки нет.** Числа получены прогоном штатного носителя.
- **Ярлык исхода.** `выводимо`. **Ярлык дефицита.** Нет — закрыто прогоном.

## P5. В backlog нет CODE-дельты `GAPS_CLOSE_25`

- **Вопрос.** Регистр CODE-дельт в §«Шаг 7» несёт блоки `_10`, `_16/_17`,
  `_18/_19`, `_20`, `_21`, `_22`, `_23+_24`, `_26`. Блока `_25` нет, при том
  что прогон `_25` существовал и дал 14 гейтящих находок. Либо закрытие `_25`
  дельты не породило (тогда это стоит записать явно), либо дельта не
  зарегистрирована и уедет в `CODE` невидимой.
- **Цитаты (носители).** `.claude/work/backlog.md` — греп
  `GAPS_CLOSE_[0-9]+`: строки 687, 743, 777, 846, 894, 920, 1077, 1329 (блоки
  дельт); `_25` не встречается ни разу.
  `.claude/decisions/gating-node-closure-depth.md:25-26` — «8 из 9 гейтящих
  `_24` и **9 из 14 гейтящих `_25`** — слои того же узла; остановлен
  детектором только на входе в `GAPS_CLOSE_25`».
- **Целевой док.** `.claude/work/backlog.md` §«CODE-дельта шага» — блок `_25`
  либо явная строка «`_25`: дельты `CODE` нет — закрытие доковое / узел
  остановлен эскалацией».
- **Ожидаемый владелец.** `solution-designer` (владелец закрытия узла);
  `knowledge-curator` — по регистрации.
- **Трассировка.** Собрал перечень блоков дельт грепом → заметил пропуск между
  `_24` и `_26` → проверил, существовал ли прогон `_25`, по **разрешённому**
  носителю (решение в `.claude/decisions/`, не история) → существовал, с 14
  гейтящими находками. **Посылка помечена непроверенной:** из запрета на
  чтение отчётов `_25`/`_26` я не могу установить, породило ли закрытие `_25`
  дельту; находка — о **пропуске в регистре**, не о потерянной дельте.
- **Варианты — развилки нет** на уровне записи: пропуск либо заполняется
  блоком, либо объясняется строкой. Что именно — знает владелец закрытия.
- **Ярлык исхода.** `выводимо`. **Ярлык дефицита.** Нет — вопрос адресуем
  владельцу без внешнего знания.

## P6. Клейм плана «содержательных кейсов шага 7 семнадцать» смешивает категории

- **Вопрос.** Шапка считает семнадцать «содержательных кейсов шага 7»: 16 с
  заголовком «Содержательный (шаг 7, …)» плюс `AG6.1`, чей заголовок —
  «**Прямой** — справочник типов bills», без пометки шага. Счёт верен только
  при склейке двух категорий; при следующей ревизии перечня расхождение
  воспроизведётся.
- **Цитаты (носители).** `.claude/tests/source-api/okx/plan.md:77-81`;
  `plan.md:1945` (заголовок `AG6.1`); греп `^### .*Содержательный \(шаг 7`
  → **16** попаданий.
- **Целевой док.** `.claude/tests/source-api/okx/plan.md` §Статус.
- **Ожидаемый владелец.** `tester`.
- **Трассировка.** Пересобрал счёт из перечня грепом по заголовкам → 16 →
  сверил семнадцатый элемент списка (`AG6.1`) с его заголовком.
- **Варианты — развилки нет** (формулировка: «шестнадцать содержательных +
  один прямой, требующий перепрогона»).
- **Ярлык исхода.** `выводимо`. **Ярлык дефицита.** Нет — закрыто прогоном.

## P7. Арифметика «десять кейсов несут одиннадцать предусловий» не сходится из названных оснований

- **Вопрос.** Шапка объясняет переход 10 → 11 тремя двойными кейсами (`AG1.7`
  — пп. 7 и 14; `AG1.9` — пп. 1 и 15; `AG6.1` — пп. 2 и 16). Из этих
  оснований получается **13** слотов, а не 11: до одиннадцати их сводят два
  **неназванных** перекрытия — `AG1.5` и `AG1.9` оба несут п. 1, `AG6.1` и
  `AG6.2` оба несут п. 2. Итог верен, объяснение — нет; и именно неназванное
  перекрытие по п. 1 дало F1.
- **Цитаты (носители).** `.claude/tests/source-api/okx/plan.md:81-84`;
  `.claude/tests/source-api/okx/code-preconditions.md:41-53` (таблица, 11
  строк); `plan.md:1523,1677,1945,1974` (гейт-пометки четырёх пересекающихся
  кейсов).
- **Целевой док.** `.claude/tests/source-api/okx/plan.md` §Статус.
- **Ожидаемый владелец.** `tester`.
- **Трассировка.** Пересобрал отображение «кейс → предусловия» из десяти
  заголовков → получил мультимножество из 13 → свернул в множество из 11 →
  сверил с таблицей реестра.
- **Варианты — развилки нет** (счёт правится вместе с перечнем: перекрытия
  называются).
- **Ярлык исхода.** `выводимо`. **Ярлык дефицита.** Нет — закрыто прогоном.

## P8. Средовой дефицит автономного прогона жив

- **Вопрос.** Сбор грунта требует прогона на demo; backlog фиксирует, что
  автономно он невозможен (нет `mvnw`/собранного jar, токен Vault только в
  IDEA-конфиге). Проверено: в репозитории нет ни `mvnw`, ни `.mvn/`, ни
  `target/*.jar`. То есть даже при готовых код-тестах (F2) прогон упирается в
  среду — второй независимый блокер грунта, живущий отдельно от реестра, и
  реестр о нём не упоминает.
- **Цитаты (носители).** `.claude/work/backlog.md:1778-1805`; `ls` корня —
  `pom.xml` есть, `mvnw`/`.mvn` отсутствуют; `target/*.jar` нет;
  `.claude/tests/source-api/okx/plan.md:23-25` («RUN прогнан (2026-06-20 …
  `mvn -o test -Dgroups=source-api-live`)»).
- **Целевой док.** `.claude/tests/source-api/okx/code-preconditions.md` —
  указатель на §«Средовой дефицит автономного RUN тестов» backlog, чтобы «что
  мешает» читалось там же, где гейт; сама секция backlog правки не требует.
- **Ожидаемый владелец.** `tester` (инвариант роли записан там же); снятие
  самого дефицита — вне ролей ревью.
- **Трассировка.** Читая реестр, спросил «что мешает прогону» → нашёл секцию
  backlog → проверил её посылки командой `ls` по трём носителям (wrapper,
  `.mvn`, jar) — все три отсутствуют, посылка не устарела.
- **Варианты — развилки нет** (констатация живого пункта; правка — добавить
  указатель из реестра).
- **Ярлык исхода.** `выводимо`. **Ярлык дефицита.** `работа` (снятие
  средового дефицита — не задача этого прогона).

## P9. Нумерация реестра несёт необъяснённые пропуски

- **Вопрос.** Реестр идёт 1, 2, 5, 6, 7, 9, 10, 14, 15, 16, 17. Пропуски 3, 4,
  8, 11, 12, 13; §«Что в реестр НЕ входит» разбирает только 13 (и калибратор
  допуска — не называя, что это п. 8). Пропуски в нумерации правилами
  разрешены, но здесь номер 8 **живёт** в backlog как действующее предусловие
  (F8) — то есть молчание реестра о снятых номерах даёт мёртвой ссылке
  выглядеть живой.
- **Цитаты (носители).** `.claude/tests/source-api/okx/code-preconditions.md:41-53`
  (таблица), `:60-72` (§«Что в реестр НЕ входит» — 13, калибратор, `M15.7`);
  `.claude/work/backlog.md:1487` («предусловием `CODE` п. 8»);
  `.claude/rules/closed-work-transfer.md` §«Ссылочная целостность»
  («Нумерация оставшихся пунктов не сдвигается — пропуски в нумерации
  допустимы»).
- **Целевой док.** `.claude/tests/source-api/okx/code-preconditions.md`
  §«Что в реестр НЕ входит» — назвать номера снятых пунктов (в первую очередь
  8 у калибратора).
- **Ожидаемый владелец.** `integrator`.
- **Трассировка.** Выписал номера строк таблицы → сопоставил с §«Что в реестр
  НЕ входит» → 13 объяснён, 3/4/8/11/12 — нет → грепнул номера по рабочим
  файлам: п. 8 нашёлся живым в backlog.
- **Варианты — развилки нет** (правка — назвать номер у уже описанного
  снятого пункта).
- **Ярлык исхода.** `выводимо`. **Ярлык дефицита.** Нет — закрыто прогоном.

## P10. §Структурный изоморфизм плана называет код-тесты «будущими»

- **Вопрос.** §Изоморфизм трижды пишет «(будущий) класс код-тестов»,
  «(будущий) тест-метод», «(будущий) шаг-ассерт», тогда как §Статус того же
  файла утверждает «CODE-тесты написаны» и называет каталог. Секция не
  обновлена после этапа 4 процесса; в сочетании с F2 (девять кейсов без
  носителей) читатель не может по плану установить, какие тест-методы
  существуют, а какие «будущие».
- **Цитаты (носители).** `.claude/tests/source-api/okx/plan.md:139-143`;
  `plan.md:20-21` («CODE-тесты написаны
  (`src/test/java/…/integration/sourceapi/okx/`)»).
- **Целевой док.** `.claude/tests/source-api/okx/plan.md` §Структурный
  изоморфизм.
- **Ожидаемый владелец.** `tester`.
- **Трассировка.** Читал §Изоморфизм ради правила соответствия план ↔
  коллекция → заметил «(будущий)» → сверил с §Статус того же файла.
- **Варианты — развилки нет** (снятая редакция; правка — снять «(будущий)»,
  оговорив непокрытые кейсы по F2).
- **Ярлык исхода.** `выводимо`. **Ярлык дефицита.** Нет — закрыто прогоном.

## P11. Реестр не различает «кейс описан» и «кейс исполним» — статусная колонка одномерна

- **Вопрос.** Колонка «Статус» реестра принимает значения «⏳ не собрано» и
  «⚠️ перепрогон» — обе про **наблюдение**. Состояние носителя (описан / в
  коллекции / есть код-тест) колонкой не выражено, и потому F2 не виден из
  реестра вовсе: одиннадцать пунктов «не собрано» читаются как «осталось
  прогнать», а фактически девять из десяти кейсов прогнать нечем. Находка
  постановочная: объясняет, почему F2 не была замечена реестром, и предлагает
  ось, а не чинит F2.
- **Цитаты (носители).** `.claude/tests/source-api/okx/code-preconditions.md:41-53`
  (колонки: №, что установить, кейс, статус); `:55` («Кейс на каждый пункт
  заведён»); греп по `src/test/.../okx/` и по коллекции (см. F2).
- **Целевой док.** `.claude/tests/source-api/okx/code-preconditions.md`
  (таблица реестра).
- **Ожидаемый владелец.** `integrator` (форма реестра) + `tester` (значения).
- **Трассировка.** Собрав таблицу «предусловие → кейс → код-тест», заметил,
  что вторая и третья колонки в реестре склеены в одну; проверил, не несёт ли
  различение план: не несёт («⏳ PENDING» стоит и у кейса с код-тестом, и без).
- **Варианты.**

  | Вариант | Что делает | Названная цена |
  |---|---|---|
  | (а) Достаточно существующего носителя: состояние носителя читается из плана | Ничего не меняется | **Опровергается фактикой:** план тоже не различает — «⏳ PENDING» стоит и у кейсов с код-тестом, и без; отсутствие носителя названо ровно один раз из девяти (`M17.5`) |
  | (б) Колонка «носитель прогона» в реестре (`—` / коллекция / код-тест) | Разрыв «описан ≠ исполним» становится видимым в доме гейта | Колонка в одной таблице; связность нулевая; проверяемость — греп идентификатора по двум каталогам |
  | (в) Статус кейса в плане расширить на состояние носителя | Ось живёт там, где кейс | Правка конвенции статусов плана (17 заголовков + шаблон `test-plan.md`); больший след при той же разрешающей способности |
- **Крен — (б).** Меньший след при равной разрешающей способности; и дом
  гейта — реестр, а не план.
- **Ярлык исхода.** `варианты-с-креном`. **Ярлык дефицита.** Нет — закрыто
  прогоном.

---

# Сверка с `open-questions.md`

Проход по всем 15 открытым вопросам. **Гейтящих механику шага 7 — ноль**,
поэтому стадия 0 обход не остановила.

| Вопрос | Отношение к шагу 7 | Классификация |
|---|---|---|
| ORCH-Q1 (владелец онбординга инструмента) | Горизонт «конец фазы 1»; механику `Deal`/P&L не задаёт | не гейтит |
| CMD-Q4 (перечисление неизвестных live-сущностей) | Остаток явно отнесён к шагу 8 (`AnomalyJob`) | не гейтит |
| OKX-Q2 (`TradeFillsArchive`) | Runtime фазы 1 fills не читает; пофилловый аудит вынесен за фазу 1 | не гейтит |
| OKX-Q4 (WS-каналы) | Якорь пересмотра — архитектурный рубеж | не гейтит |
| STRAT-Q4 (percent-anchor) | Контракт условий стратегии, не итог сделки | не гейтит |
| IND-Q1 (надёжность объёма) | Якорь — фаза 4, записан и в `roadmap.md` | не гейтит |
| STRUCT-Q1 (пороги структуры рынка) | Владелец — фаза 2 (бэктест-гейт) | не гейтит |
| PHASE-Q1 (гистерезис фазы) | Горизонт — торговая проработка фазы | не гейтит |
| **PNL-Q1** (измеримость искажений) | Ближе всех к шагу 7: `triggerPx` ликвидации/ADL и неизмеримость цены холда. Половина про `Deal.closeOutcome` **закрыта**; остаток — измеримость искажения, а не спецификация числа | **не гейтит** (тип «неотвеченный вопрос»); ср. F8 — backlog ссылается на его несуществующий п. 3 |
| ORCH-Q2 (порядок teardown у kill-switch) | Расхождение `exit-teardown-order.md` ↔ `KillSwitchExecutor.md`; горизонт «конец фазы 1 перед тестами»; артефакт шага 6 | **зависимый** — гейтил бы, если исход (а) переформирует порядок teardown, который шаг 7 наблюдает при аварийном терминале |
| FEE-Q1 (слот ставки комиссии) | Файл сам пишет «Не гейтит: читателей ставки двое, финализатор не читает» | не гейтит |
| ANOM-Q6 (популяция частичного индекса) | Помечен зависимым, горизонт — фаза 3 | не гейтит |
| HOLD-Q2 (снятие жёсткой блокировки) | Онбординг инструмента/биржи; решается вместе с ORCH-Q1 | не гейтит |
| PHASE-Q2 (размещение `MarketPhase`) | Файл сам пишет «Не гейтит» | не гейтит |
| PROC-Q3, PROC-Q2 (пайплайн) | Методология прогонов и скиллов | не гейтит (PROC-Q3 — предмет дисклозуры в §Охват) |

`.claude/work/questions/tasks/` — **пуст**; локальных вопросов задачи нет.

---

# Роадмап — соответствие фактике

- **Шаг 7 = `DOCS_CHECK_27`** (`phase-1.md:39`) соответствует: прогон 27 идёт,
  его каталог `progress/phase-1-step-7-docs-check-27/` создан.
- **Ролляп фазы 1.** Шаги: 1-6 `DONE`, 7 `DOCS_CHECK_27`, 8-11 `HOLD` ⇒ по
  правилу «иначе → `IN_PROGRESS`» фаза = `IN_PROGRESS`; `roadmap.md:24` несёт
  `IN_PROGRESS`. **Сходится.**
- **Прочие фазы** — все `HOLD`, детальных файлов нет; `roadmap.md:53-55` это
  фиксирует. Архитектурный рубеж — `HOLD` вручную, ролляп не считается.
- **Гейт закрытия фазы 1** (`update-roadmap-progress.md` §«Гейт закрытия
  фазы») называет три носителя в backlog; все три на месте: §«Закрытие фазы 1
  — обязательный пересмотр допущения "таблицы пусты"» (`backlog.md:234`),
  §«Закрытие фазы 1 — снятие разведочного режима допуска сверки» (`:277`),
  §«Пересмотр worst-case открывающего входа — по данным первого периода живой
  торговли» (`:309`). **Расхождений нет.**
- Примечание `phase-1.md:60-65` про закрытие фазы согласовано со скиллом.

**Находок по роадмапу нет.**

---

# Блокирующие открытые вопросы

Нет. Ни один пункт `open-questions.md` не гейтит механику шага 7 (таблица
выше). Гейт `CODE` держится не открытым вопросом, а **несобранным грунтом**
(F1-F5) и **неисполнимостью самого гейта** (F6).

# Вынос пользователю

**Пусто.** Ни одна находка не несёт дефицит `подтверждение`: ни одна не
упирается в численную величину риск-аппетита и ни одна не меняет продуктовый
scope. F4(в), F5(в) и F10(б) переоткрывают принятые решения — они выписаны с
пометкой и **не** выбраны креном, поэтому до держателя не едут
(`gap-report.md` §«Отмена решения держателя ярлыка сама по себе не даёт»).

---

# Сводка

**Гейтящих находок — 10** (F1-F10). **Прочих — 11** (P1-P11). Всего 21.
Числа пересобраны по телу отчёта после его закрытия: разделов находок два, в
первом заголовки F1…F10, во втором P1…P11.

**По ярлыку исхода:** `варианты-с-креном` — 8 (F1, F2, F3, F4, F5, F6, F10,
P11); `выводимо` — 13 (F7, F8, F9, P1, P2, P3, P4, P5, P6, P7, P8, P9, P10).
`принято-в-работу` — 0; `неразрешимо-владельцем` — 0. Сумма 8 + 13 = 21.

**По ярлыку дефицита:** `работа` — 4 (F2, F3, F4, P8); «дефицита нет» — 17.
`грунт` — 0 (грунт здесь не дефицит находки, а её предмет);
`подтверждение` — 0. Сумма 4 + 17 = 21.

**По ожидаемым владельцам** (находка может иметь двух — сумма больше 21):
`knowledge-curator` — 9 (F5, F6, F7, F8, F9, F10, P1, P2, P3, P4);
`tester` — 7 (F2, F3, F4, P6, P7, P8, P10, P11);
`integrator` — 5 (F1, F5, F9, P9, P11);
`solution-designer` — 2 (F6, P5).

**Вердикт.** Прогон **не чист**. Шаг 7 в `CODE` не выходит: гейт грунта
неисполним (F2-F5), сам гейт не проверяется механикой статусов (F6), а реестр
и рабочие файлы расходятся между собой в именах кейсов, статусах и номерах
предусловий (F1, F7, F8, F9). Требуется `GAPS_CLOSE_27`, и по правилу §4
процесса работа по F2-F5 (единственный дефицит — `работа` вокруг `грунт`)
начинается **не дожидаясь** чистоты остальных линз.

## Таблица «предусловие → кейс → код-тест → статус»

| № реестра | Что установить (кратко) | Кейс (реестр) | Кейс (план, гейт-пометка) | В коллекции | Код-тест | Статус реестра | Статус плана |
|---|---|---|---|---|---|---|---|
| 1 | Агрегация partial-закрытий в одну запись на `posId` | `AG1.5` | `AG1.5` (п. 1) **+ `AG1.9` (пп. 1 и 15)** | нет | нет | ⏳ не собрано | ⏳ PENDING |
| 2 | Полный перечень `type`/`subType` | `AG6.1`, `AG6.2` | `AG6.1` (пп. 2 и 16), `AG6.2` (п. 2) | `AG6.1` — да; `AG6.2` — нет | `AG6.1` — есть, **перечня не пишет (F3)**; `AG6.2` — нет | ⚠️ перепрогон (**один на пару — F1**) | `AG6.1` ⚠️ ПЕРЕПРОГОН / `AG6.2` ⏳ PENDING |
| 5 | Секундный бар котировки (курс cross-ccy) | `MG7.5` | `MG7.5` (п. 5) | нет | нет | ⏳ не собрано | ⏳ PENDING |
| 6 | Оси адресации записи без `posId` | `AG1.6` | `AG1.6` (п. 6) | нет | нет | ⏳ не собрано | ⏳ PENDING |
| 7 | Горизонт доступности `fundingFee` | `AG1.7` | `AG1.7` (п. 7) | нет | нет | ⏳ не собрано | ⏳ PENDING (**backlog зовёт п. 7 «знаками, гейта нет» — F9**) |
| 9 | Комиссии в расчётной валюте | `AG3.4` | `AG3.4` (п. 9, «вторая проверка») | нет | нет | ⏳ не собрано | ⏳ PENDING (**backlog ставит после чистого прогона — F7**) |
| 10 | След автоделевериджа | `AG1.8` | `AG1.8` (п. 10) | нет | нет | ⏳ не собрано | ⏳ PENDING (закрытие ожидается `OBSERVED_ABSENT` — **F5**) |
| 14 | Форма пустого числового значения | `AG1.7` | `AG1.7` (п. 14) | нет | нет | ⏳ не собрано | ⏳ PENDING |
| 15 | Отдельная запись на каждый `posId` в окне | `AG1.9` | `AG1.9` (пп. 1 и 15) | нет | нет | ⏳ не собрано | ⏳ PENDING |
| 16 | Отображение `type`/`subType` → категория движения | `AG6.1` | `AG6.1` (пп. 2 и 16) | да | есть, **перечня не пишет (F3)** | ⚠️ перепрогон | ⚠️ ПЕРЕПРОГОН |
| 17 | Судьба встроенной защиты при отмене родителя | `M17.5` | `M17.5` (п. 17) | нет | нет | ⏳ не собрано | ⏳ PENDING (план сам пишет «код-теста нет») |

**Итого:** одиннадцать предусловий, десять кейсов (перекрытия — п. 1
`AG1.5`+`AG1.9`, п. 2 `AG6.1`+`AG6.2`; `AG1.7` на пп. 7 и 14, `AG1.9` на
пп. 1 и 15, `AG6.1` на пп. 2 и 16); **собрано ноль**; исполнимый носитель — у
одного кейса из десяти, и тот не производит требуемого наблюдения.

## Что мешает прогону грунта — сводно

1. **Носителей нет** — девять из десяти гейтящих кейсов не имеют ни
   код-теста, ни запроса коллекции (F2).
2. **Единственный носитель не покрывает предмет** — `observe()` пишет
   мощность, а нужен перечень (F3).
3. **Процедурный блок** — этапы `REVIEW` / `APPROVE` пары план + коллекция на
   дельту плана не пройдены, а прогон до аппрува процессом запрещён (F4).
4. **Средовой блок** — нет `mvnw` / собранного jar и токена Vault в окружении
   shell (P8).
5. **Механический блок отсутствует там, где нужен** — гейт `CODE` по грунту
   при смене статуса не проверяется вовсе (F6).

## Связи

- Формат находки — `.claude/templates/docs/gap-report.md`.
- Процедура обхода — `.claude/skills/concept-review.md`.
- Гейты и §3 / §4 — `.claude/processes/roadmap-step-execution.md`.
- Реестр предусловий — `.claude/tests/source-api/okx/code-preconditions.md`.
- Процесс контура — `.claude/processes/source-api-testing.md`.
