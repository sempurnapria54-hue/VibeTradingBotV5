# GAPS_CLOSE_17 — закрытие находок `DOCS_CHECK_17`

## На какой вопрос отвечает этот файл

Как закрыты находки `DOCS_CHECK_17` шага 7 фазы 1.

## Контекст

- **Под-шаг:** `GAPS_CLOSE_17`, закрытие 30 находок отчёта
  `.claude/work/progress/phase-1-step-7-docs-check-17.md`.
- **Ступень категорий — `Предложение`:** молча не финализируется ничего.
  Отсюда три класса исходов, и они разведены явно:
  - **11 находок закрыты прогоном** — реконсиляция уже ратифицированного
    (флаг `разместил в рамках принятого`: решение держателя определяет исход
    однозначно, CC выбирает только носитель и формулировку);
  - **8 находок внесены предложением владельца** (флаг `предложил`) — крен
    не переоткрывает ратифицированного, но выбор среди альтернатив сделан
    ⇒ **ждут валидации** и идут в счёт гейта делегирования;
  - **11 находок не внесены** — их крен переоткрывает решение держателя либо
    упирается в продуктовый scope; вынесены держателю, составные разведены
    на половины.
- **Запечатанный ключ слепого прохода** —
  `.claude/work/blind/phase-1-step-7-gaps-close-17.md`, записан **одним
  ходом до первого вопроса держателю**, охватывает все 18 развилок. Отчёт на
  него **ссылается и сверки не проводит**: сверка — третий вход чата.
- **Свипы — четыре**, все с воспроизводимым артефактом (команда + полный
  вывод) и пофайловой таблицей (§Свипы).

## Сводка исходов

| Класс | Сколько | Находки |
|---|---|---|
| Закрыто прогоном (реконсиляция ратифицированного) | **11** | H1, H2, H3, H4, H22, H23, H24, H25, H26, H29, H30 |
| Внесено предложением владельца — ждёт валидации | **8** | H5, H6, H9, H10, H12, H13, H14, H16 |
| Вынесено держателю, в доки не внесено | **11** | H7, H8, H11, H15, H17, H18, H19, H20, H21, H27, H28 |

Сумма — **30**, сходится с телом отчёта прогона.

---

## Часть 1. Закрыто прогоном — по списку цитат каждой находки

Каждая цитата из отчёта прогона отмечена: **правлено** (что именно) либо
**не требует правки** (почему).

### H1 — операнд `stop_i` назван снятой конструкцией

| Цитата-носитель | Исход |
|---|---|
| `pnl-finalization-mechanics.md:453-461` §epsilon «Где живут операнды» | **правлено**: перечень операндов переписан на пять persisted-полей ноги (`plannedStopPrice`, `plannedContractValue` вместо резолва через `attachedAlgoOrders` и навес); добавлен подпункт «Прежняя редакция снята» с указанием, **почему** (встроенная защита доборной ноги снимается, durable-операндом быть перестала) |
| `pnl-finalization-mechanics.md:951-955` §Следствия (реестр дельты `CODE`) | **правлено**: пункт переписан — «операнды епсилона — persisted-числа ноги, предиката селекции нет»; явно сказано, что коллекция `attachedAlgoOrders` финализатору не требуется и читателем навеса он не является |
| `FinalizeDealExitExecutor.md:18-20` §Назначение | **правлено**: «+ уровень стопа её attached-защиты» → `plannedStopPrice`; перечень стал пятью операндами тождества |
| `FinalizeDealExitExecutor.md:237-238` §epsilon | **правлено**: «плюс уровень её attached SL» → «включая уровень стопа `plannedStopPrice`» с пометкой, что attached-защита операндом быть перестала |
| `FinalizeDealExitExecutor.md:273-306` (действующая редакция) | **не требует правки** — это и есть носитель актуального операнда, к нему приводились остальные |
| `pnl:490-497`, `InstrumentExternalRulesDataService.md:64` | **не требует правки** — оба уже несут действующую редакцию (финализатор выведен из читателей навеса; читателей два) |

**Кто ответил:** `knowledge-curator`. **Ожидался:** `knowledge-curator`.
**Сверка ролей — совпало.** **Флаг действия CC:** `разместил в рамках
принятого`. **Расширение:** нет — правка проводит принятое (Р3 / H5
`GAPS_CLOSE_16`), новых правил не назначает.

### H2 — шестая колонка не приземлена в место истины схемы `Order`

| Цитата-носитель | Исход |
|---|---|
| `Order.md:31-54` таблица §Структура | **правлено**: добавлена строка `plannedStopPrice` |
| `Order.md:331-343` §Персистентность, перечень `ALTER` | **правлено**: `planned_stop_price` добавлен, счёт исправлен («все четыре» → «все пять» `numeric(36,18)`), добавлен провенанс Р3 |
| `Order.md:344` инвариант «шесть или ни одного» | **не требует правки** — клейм верен, к нему приводился перечень |
| `Order.md:145`, `:228` (§«шестое число») | **не требует правки** — носитель актуальной редакции |
| `Order.md:318-320` («место истины схемы сущности») | **не требует правки** — правило верно, оно и было основанием гейта |
| `pnl:1021-1025` (сборка-указатель) | **не требует правки** — состав там был полон |

**Кто ответил:** `knowledge-curator`. **Ожидался:** `knowledge-curator`.
**Совпало.** **Флаг:** `разместил в рамках принятого`. **Расширение:** нет.

### H3 — payload перечисляет пять полей, называя шестью

| Цитата-носитель | Исход |
|---|---|
| `CreateOrderExecutor.md:150-157` §CreateOrderCommandPayload | **правлено**: `plannedStopPrice` добавлен, перечень приведён к шести, добавлена клауза «перечень обязан совпадать со счётом» |
| `CreateOrderExecutor.md:90-102` §«Куда пишутся шесть чисел» | **не требует правки** — счёт и состав там верны |
| `pnl:896-902` §Следствия («четыре поля планового риска») | **правлено**: «четыре» → «шесть», перечень дополнен `plannedContractValue` и `plannedStopPrice`, добавлена клауза о совпадении счёта |
| `pnl:963-965` | **не требует правки** — уже говорит «шесть» |
| `ServiceCommandPayload.md` §«Где описаны подтипы» | **не требует правки** — определяет дом описания, он и правился |

**Кто ответил:** `knowledge-curator`. **Ожидался:** `knowledge-curator`.
**Совпало.** **Флаг:** `разместил в рамках принятого`. **Расширение:** нет.

### H4 — реестр дельты заказывает снятую конструкцию «нулевые попытки»

| Цитата-носитель | Исход |
|---|---|
| `pnl:913-916` §Следствия | **правлено**: пункт переписан на действующую редакцию — дискриминатор `Deal.status` управляет **броском**, не учётом; отказ канала не бросает, дефект содержимого бросает как на штатной; прежняя редакция названа снятой |
| `command-action-boundary.md` §8 | **правлено**: «жёсткий отказ чтения = "недоступно"» снято, асимметрия переписана **по природе отказа** |
| `command-action-boundary.md` §9 | **правлено**: «записанное отклонение от единой мягкой формы» снято со ссылкой на H8 `DOCS_CHECK_16` |
| `pnl:711-717`, `SystemActionExecutor.md:180-183`, `ServiceCommandExecutor.md` §«Ветка `ERROR`», `command-lifecycle.md:94-96`, `MarkDealEmergencyClosedExecutor.md:58-63` | **не требуют правки** — все пять несут действующую редакцию, к ним приводились остальные |
| `instrument-hold.md` §«Форма реакции», `HoldService.md` §«Различителя нет» | **не требуют правки по H4** — они верны; их граница уточнена отдельно (H13) |

**Кто ответил:** `knowledge-curator`. **Ожидался:** `knowledge-curator`.
**Совпало.** **Флаг:** `разместил в рамках принятого`. **Расширение:** нет.

### H22 — due-for-retry как ось выборки + битая кросс-ссылка

| Цитата-носитель | Исход |
|---|---|
| `DealOrchestratorJob.md:143-148` §«Критерии выборки» | **правлено**: буллет переписан — due-for-retry объявлен **предикатом продвижения над уже загруженными строками**, а не критерием отбора; названо, почему множества совпадают (сделка со строкой `RETRY_PENDING` не терминальна) |
| `DealActionState.md:337-346` | **не требует правки** — его формулировка верна; битой была **атрибуция**, и целевая секция теперь содержит обещанную клаузу |

**Кто ответил:** `knowledge-curator`. **Ожидался:** `knowledge-curator`.
**Совпало.** **Флаг:** `разместил в рамках принятого`. **Расширение:** нет.

### H23 — эмитент `MARK_DEAL_EMERGENCY_CLOSED_COMMAND`

| Цитата-носитель | Исход |
|---|---|
| `MarkDealEmergencyClosedExecutor.md:224` | **правлено**: «Команду эмитит `ErrorHandler`» → «действие инициирует `ErrorHandler`; команду эмитит звено `FINALIZE_DEAL_ERROR_ACTION`», со ссылкой на канон и на симметричный executor |
| `SystemActionExecutor.md:260-262`, `MarkDealClosedExecutor.md:45-47`, `fsm-execution-layering.md` §«Handler — 3 метода» | **не требуют правки** — носители канона, к ним приводился четвёртый |

**Кто ответил:** `knowledge-curator`. **Ожидался:** `knowledge-curator`.
**Совпало.** **Флаг:** `разместил в рамках принятого`. **Расширение:** нет.

### H24 — процесс-док без системного слоя и с несмягчённым инвариантом

| Цитата-носитель | Исход |
|---|---|
| `deal-management.md` §«Поток во времени» | **правлено**: поток разведён на **ветвь стратегии** и **системную ветвь** (`SystemActionExecutor` + три системных действия), с клаузой «статусные рёбра пишет звено, а не handler» |
| `deal-management.md` §«Статусная механика и recovery» | **правлено**: добавлено смягчение по валюте на тропах без входа + прямая ссылка на §«Смягчение…»; названа цена безусловного прочтения (недостижимый терминал на самом частом сценарии сканера) |
| `lifecycles/Deal.md` §«Смягчение по валюте», `fsm-execution-layering.md` §Слои | **не требуют правки** — носители актуальной редакции |

**Кто ответил:** `knowledge-curator`. **Ожидался:** `knowledge-curator`.
**Совпало.** **Флаг:** `разместил в рамках принятого`. **Расширение:** нет.

### H25 — две клаузы `Deal.md` о коде ложны

| Цитата-носитель | Исход |
|---|---|
| `Deal.md:26` (пакет `…model.core.deal.Deal`) | **правлено** → `…model.aggregate.deal.Deal` (факт кода, V8) |
| `Deal.md:565-566` («`accumulatedFillSize` персистится (`V4`)») | **правлено** → `V6`, `orders.accumulated_fill_size` (факт миграций, V8) |

**Кто ответил:** `knowledge-curator`. **Ожидался:** `knowledge-curator`.
**Совпало.** **Флаг:** `разместил в рамках принятого`. **Расширение:** нет.
**Единственный случай правки дока по коду на до-`CODE` стадии** — клауза
утверждала факт о коде, факт ложен (оговорка правила стадии).

### H26 — ревизии H9/Р7 не доехали до двух доков

| Цитата-носитель | Исход |
|---|---|
| `Position.md:85-86` | **правлено**: довод переписан — нижнюю границу пишет `SubmitOrderExecutor` всегда при постановке, к моменту запроса она стоит; операнд всё равно берётся с ноги, чтобы тропа не зависела от порядка двух собственных записей; прежняя редакция названа выражавшей снятую топологию |
| `RefreshBillsExecutor.md:11-15` §Назначение | **правлено**: «по окну сделки `begin`/`end`» → «`[Deal.billsWindowBegin, now]`, верхняя граница запроса подвижна» |
| `RefreshBillsExecutor.md:70-74`, `Deal.md:996-999`, `PositionCloseResult.md:352-359` | **не требуют правки** — носители актуальной редакции |

**Кто ответил:** `knowledge-curator` (обе правки оказались формулировочными;
концептуальной половины, требовавшей `solution-designer`, не возникло —
альтернативный операнд адресации уже разобран и закрыт в
`PositionCloseResult.md`). **Ожидался:** `solution-designer` (половина) +
`knowledge-curator`. **Сверка ролей — расхождение в сторону упрощения**,
зафиксировано: концепт-владелец не потребовался.
**Флаг:** `разместил в рамках принятого`. **Расширение:** нет.

### H29 — разметка `ГЕЙТИТ` в плане расходится с реестром

| Цитата-носитель | Исход |
|---|---|
| `plan.md` заголовки `AG1.5`, `AG1.7`, `AG3.4` | **правлено**: проставлен маркер `ГЕЙТИТ CODE` с указанием пункта реестра (п. 1, п. 7, п. 9); у `AG1.7` явно оговорено, что гейт — только горизонт `fundingFee`, знаки (2) гейта не образуют |
| `plan.md` заголовки `AG1.6`, `AG6.2`, `MG7.5` | **не требуют правки** — маркер уже стоял |
| `pnl:1183-1184` («перечень сквозной») | **не требует правки** — правило верно, план приводился к нему |
| кейс п. 10 | **правки не получил** — кейса не существует, это H27 (вынесена держателю) |

**Кто ответил:** `knowledge-curator`. **Ожидался:** `knowledge-curator` +
`tester`. **Совпало** (роль `tester` не потребовалась: правка чисто
разметочная). **Флаг:** `разместил в рамках принятого`. **Расширение:** нет.

### H30 — отменённое условие «после чистого `DOCS_CHECK_4`»

| Цитата-носитель | Исход |
|---|---|
| `plan.md:1521` (`AG3.4` §Статус) | **правлено**: условие снято, заменено на «чистого прогона концепции не ждёт — единственный блокер `грунт`» со ссылкой на `roadmap-step-execution.md` §4 |
| `plan.md:1558` (`AG3.5` §Статус) | **правлено** тем же образом |
| `plan.md:1869` (`AG12.5` §Статус) | **правлено** тем же образом |
| `plan.md:149` («ассерт чистого снапшота») | **не требует правки** — нерелевантное попадание грепа |

**Кто ответил:** `knowledge-curator`. **Ожидался:** `knowledge-curator`.
**Совпало.** **Флаг:** `разместил в рамках принятого`. **Расширение:** нет.

---

## Часть 2. Внесено предложением владельца — ждёт валидации

Каждая позиция несёт флаг `предложил`, потому что решение держателя исход
**однозначно не определяет**: назван выбор среди альтернатив, не отметаемых
принципом. Перечни вариантов и крены — в ключе (П1-П8).

| # | Находка | Что внесено | Носитель правки | Кто ответил | Ожидался | Сверка | Расширение |
|---|---|---|---|---|---|---|---|
| П1 | **H5** | `CreateOrderExecutor` пересчитывает **две суммы**; добавлен подраздел «Почему третье поле не пересчитывает» со счётным доводом | `Deal.md` §«Взятый риск» (таблица триггеров + новый подраздел) | `solution-designer` | `solution-designer` | совпало | нет — правка вводит механику **внутри** находки |
| П2 | **H6** | страховочный конъюнкт распространён на все три формулы; разведены «слагаемых нет ⇒ пусто» и «слагаемые есть, филлов нет ⇒ ноль»; названа достижимость (nullable в `V6`, `ACTIVE` при `externalSize = 0`) | `Deal.md` §«Взятый риск» (таблица величин + новый буллет) | `solution-designer` | `solution-designer` | совпало | нет |
| П3 | **H9** | три строки реестра кодов получили аварийного производителя; `SETTLE_CURRENCY_UNAVAILABLE` — всех трёх писателей валюты | `AnomalyReport.md` §«Реестр кодов шага 7» | `solution-designer` | `solution-designer` | совпало | нет |
| П4 | **H10** | операнд признака полноты приведён к H20 (`t_добычи − billsWindowBegin`); строка «неподвижная граница» потеряла «контроль полноты»; выровнен аварийный терминал | `Deal.md` §«Верхних границ две», `MarkDealEmergencyClosedExecutor.md:189` | `knowledge-curator` | `knowledge-curator` / `solution-designer` | совпало | нет |
| П5 | **H12** | строка `ERROR` состава цикла = «сущности, живые в runtime graph, плюс позиция»; названы счётный случай и цена (добыча дороже при общем бюджете) | `SystemActionExecutor.md` §«Состав конкретного цикла» | `solution-designer` | `solution-designer` | совпало | нет |
| П6 | **H13** | граница правила оговорена в обоих носителях: «независимо от `Deal.status`» — про класс реакции, не про условие броска | `instrument-hold.md` §«Форма реакции», `HoldService.md` §«Различителя нет» | `solution-designer` | `solution-designer` (+ держатель, если чтение буквальное) | совпало по проектной половине; держательская **вынесена** (§Часть 3) | нет — обе ратификации сохранены |
| П7 | **H14** | ветка «`billsWindowBegin` пуст ⇒ звено не эмитится»; явно сказано, что вторым операндом предиката это не становится; суррогат границы запрещён | `SystemActionExecutor.md` §«предикат эмиссии bills-звена» | `solution-designer` | `solution-designer` | совпало | нет |
| П8 | **H16** | конъюнкт «за вычетом исключённых типов» дописан в три строки реестра областей; «конъюнкт четырёх из пяти» → «всех пяти» | `DealCashFlow.md` §«Область суммирования», `pnl:1379` | `knowledge-curator` | `knowledge-curator` | совпало | нет |

**Все восемь позиций идут в счёт гейта делегирования** как одна валидация
(единица счёта — вынос пакета, не позиция).

---

## Часть 3. Вынесено держателю — в доки не внесено

Основание невнесения у каждой позиции — механическое: в переборе есть вариант
с пометкой «переоткрывает принятое», **и этот же вариант — крен**, либо цена
варианта есть **продуктовое решение владельца продукта**.

| # | Находка | Что выносится держателю | Проектная половина (кому и в каком состоянии) | Ярлык дефицита | Флаг действия CC |
|---|---|---|---|---|---|
| П9 | **H8** | «`NOT_APPLICABLE` пишется или снимается» — крен (A′) отменяет собственный ратифицированный клейм `Deal.md` о том, что пустота на этих тропах есть **значение**, а не пропуск (H3 `GAPS_CLOSE_13`) | приземление выбранного во все пять носителей — `solution-designer`, **проработано**, ждёт исхода | `подтверждение` | `эскалировал` |
| П10 | **H7** | что означает `currentRiskAmount`; вариант «не персистить» переоткрывает `GAPS_CLOSE_16` | форма второго носителя («снятый защитой риск») — `trading-specialist` + `solution-designer`, **проработана** | `подтверждение` | `эскалировал` |
| П11 | **H11** | нужен ли признак полноты **числа**; варианты 4 и 5 переоткрывают ратифицированное | форма признака (третье значение против отдельного енума) — `solution-designer`, **проработана** | `подтверждение` | `эскалировал` |
| П12 | **H15** | форма пустого значения несобытийных полей — рантайм-факт источника | дом конвенции и починка двух битых кросс-ссылок — `integrator` (первым) + `knowledge-curator`; **от прогона не зависит**, но внесение отложено до выбора варианта | `грунт` + `работа` | `эскалировал` |
| П13 | **H17** | чем масштабировать epsilon; вариант 4 переоткрывает H9 `GAPS_CLOSE_13`, корпус объявляет фандинг перпетуалов пробелом | форма масштабирования и признак срока — `trading-specialist`, **проработана** | `грунт` + `подтверждение` | `эскалировал` |
| П14 | **H18** | чем удерживается worst-case; вариант 5 переоткрывает ратифицированный бенчмарк | измеритель запаса до ликвидации — `trading-specialist` + `integrator`, **проработан**; оценимость `liqPx` до входа — открытый хвост `integrator` | `подтверждение` + `работа` | `эскалировал` |
| П15 | **H19** | торгуются ли инверсные контракты; цена реджекта — продуктовое решение | место реджекта — `solution-designer`, **проработано**; форма инверсной формулы — **источниковый дефицит**, не достраивается | `грунт` + `подтверждение` | `эскалировал` |
| П16 | **H21** | входит ли `CLOSE_ACTION` в `CODE` шага 7 (scope) | устройство исполнителя и дочистка — `solution-designer`, **не проработаны**: компонент-док заводится после решения о scope. Реестр дельты позицию **уже несёт** с пометкой «scope открыт» | `подтверждение` + `работа` | `эскалировал` |
| П17 | **H27** | чем закрывается гейт п. 10 без кейса; вариант «снять гейт» переоткрывает статус | кейс `AG1.8` в план — `tester` + `integrator`, **не заведён** до решения | `работа` + `подтверждение` | `эскалировал` |
| П18 | **H28** | закрывает ли гейт исход «прогнан, не наблюдалось» | форма фиксации исхода — `tester`, **проработана** | `подтверждение` | `эскалировал` |
| — | **H20** | вклад в `RISK-Q3` (третья ось — форма добора; цена всех трёх потолков-кандидатов) | не требуется: вклад, не дефект. **Внесён в `open-questions.md` §`RISK-Q3`** | `подтверждение` | `эскалировал` |

---

## Сверка «ожидаемый владелец = ответивший» — пофайндингово

Ключ сверки по каждой находке — её собственный **ярлык дефицита** из отчёта
прогона: `подтверждение` ⇒ ожидался **держатель**; `работа` / `грунт` ⇒
ожидался штатный владелец.

| Находка | Ярлык дефицита | Ожидался | Фактически ответил | Совпало |
|---|---|---|---|---|
| H1, H2, H3, H4, H22, H23, H24, H25, H29, H30 | дефицита нет | штатный владелец | `knowledge-curator` | **да** (10) |
| H26 | дефицита нет | `solution-designer` + `knowledge-curator` | `knowledge-curator` | **частично** — концепт-владелец не потребовался |
| H5, H6, H12, H14 | `работа` | `solution-designer` | `solution-designer` | **да** (4) |
| H9 | `подтверждение` | держатель | `solution-designer` (внесено предложением) | **нет** — см. разбор ниже |
| H10, H16 | `подтверждение` | держатель | `knowledge-curator` (внесено предложением) | **нет** — см. разбор ниже |
| H13 | `подтверждение` | держатель (половина) | `solution-designer` — проектная половина; держательская **вынесена** | **да** по разведению |
| H7, H8, H11, H15, H17, H18, H19, H20, H21, H27, H28 | `подтверждение` (+`грунт`/`работа`) | держатель | **вынесено держателю, не отвечено** | **да** (11) |

**Разбор трёх расхождений (H9, H10, H16) — ярлык завышен прогоном, не
мис-роутинг.** У всех трёх ярлык `подтверждение` был поставлен по формуле
«в переборе есть переоткрывающий вариант», но **крен на нём не стоит**:

- **H9** — крен (A) дописывает производителей в реестр; переоткрывающий (D)
  отвергнут по следу. Правило требует `подтверждение` только когда
  переоткрывающий вариант **и есть крен**;
- **H10** — крен (A) проводит уже ратифицированный H20-операнд;
  переоткрывает его вариант (B), который отвергнут;
- **H16** — крен (A) дописывает конъюнкт; переоткрывающий (C) отвергнут.

Это **зеркало** кейса, на котором правило записано (там ярлык был **занижен**
при переоткрывающем крене). Здесь он **завышен** при непереоткрывающем — и
даёт ложное расхождение в сверке ровно так же. Формулировка правила
(«признак механический: перебор содержит вариант с пометкой "переоткрывает",
**и этот же вариант — крен**») расхождение уже исключает; прогон применил
только первую половину признака. **Позиция для следующего прогона:** проверять
обе половины признака, а не наличие переоткрывающего варианта в переборе.

**Мис-роутинга нет.** Проверено пересчётом по таблице выше, а не прозой:
совпало 25 позиций из 30, частично 1 (H26), расхождений по существу 0,
формальных расхождений от завышенного ярлыка — 3.

---

## Свипы

### Свип 1 — операнд `stop_i` и предикат селекции

**Команда:** `grep -rn "предикат селекции\|селекции attached\|attachedAlgoOrders" docs/ .claude/work/backlog.md .claude/work/questions/open-questions.md .claude/tests/`
**Попаданий: 20.**

| Файл (§секция) | Исход |
|---|---|
| `pnl-finalization-mechanics.md:458` §epsilon | **правлено** (H1) |
| `pnl-finalization-mechanics.md:953,955` §Следствия | **правлено** (H1) |
| `pnl-finalization-mechanics.md:521` §«Ветка "операнд пуст"» | **не требует** — описывает **снятие** производителя, редакция актуальна |
| `FinalizeDealExitExecutor.md:279,306` | **не требует** — носители актуальной редакции («почему не из `attachedAlgoOrders`») |
| `InstrumentExternalRulesDataService.md:90` | **не требует** — фиксирует снятие |
| `Deal.md:472` | **не требует** — исторический разбор снятой ветки |
| `AnomalyReport.md:355` | **не требует** — прямо говорит «производителя у кода не осталось» |
| `Order.md:197` | **не требует** — «прежняя редакция резолвила через `attachedAlgoOrders`» |
| `AttachedAlgoOrderStateResolver.md:23`, `lifecycles/Order.md:72,88,119`, `AlgoOrder.md:99`, `Order.md:54,404`, `mapping/Order.md:45,66,101,129`, `risk-creating-entry-protection.md:88` | **не требуют** — про коллекцию как таковую (снапшот, маппинг, embedded-поле), не про операнд епсилона |
| `backlog.md:583,596` | **не требуют** — оба уже несут действующую редакцию |

**Итог:** живых попаданий снятой конструкции не осталось (правлено 3 из 20).

### Свип 2 — «нулевые попытки» / «записанное отклонение»

**Команда:** `grep -rn "нулевые попытки\|нулевых попыток\|попыток ноль\|счётчик не раст\|записанное отклонение\|записанного отклонения" docs/ .claude/work/backlog.md .claude/work/questions/open-questions.md .claude/tests/`
**Попаданий: 11.**

| Файл (§секция) | Исход |
|---|---|
| `pnl-finalization-mechanics.md:914` §Следствия | **правлено** (H4) |
| `HoldService.md:147,216`, `MarkDealEmergencyClosedExecutor.md:59`, `SystemActionExecutor.md:169,181`, `lifecycles/Deal.md:208`, `AnomalyReport.md:382`, `command-lifecycle.md:94`, `instrument-hold.md:170,230` | **не требуют** — все описывают конструкцию как **снятую**; редакции актуальны |

**Дополнительное попадание вне грепа** (найдено чтением §8/§9, термин там
иной): `command-action-boundary.md:157-159` и `:160-173` — **правлено** (H4).
Отмечается явно: греп по терминам снимаемой политики этот носитель **не
поймал**, потому что он выражал ту же политику **другими словами** («жёсткий
отказ чтения = "недоступно"»). Это ограничение терминологического свипа как
метода.

### Свип 3 — шестое число планового риска

**Команда:** `grep -rn "plannedStopPrice\|planned_stop_price" docs/ .claude/work/backlog.md .claude/tests/` (18 попаданий) и
`grep -rn "шесть чисел\|шесть колонок\|четыре поля планового\|все шесть\|шести чисел" docs/ .claude/work/backlog.md` (21 попадание).

| Файл (§секция) | Исход |
|---|---|
| `Order.md` §Структура, §Персистентность | **правлено** (H2) |
| `CreateOrderExecutor.md` §CreateOrderCommandPayload | **правлено** (H3) |
| `pnl-finalization-mechanics.md:896` §Следствия | **правлено** (H3) |
| `pnl:965,1023,1026`, `Order.md:145,188,196,226,228`, `mapping/Order.md:55`, `risk-creating-entry-protection.md:81`, `FinalizeDealExitExecutor.md:275,277,294,331`, `CreateOrderExecutor.md:95` | **не требуют** — носители актуальной редакции |
| `Auditable.md:62`, `DealActionState.md:58,63,300`, `DealCashFlow.md:152`, `TradeFeeRate.md:286` | **не требуют** — «шесть» про audit-колонки, другая ось |
| `backlog.md:501,504,533,579,580` | **не требуют** — уже несут шесть |

### Свип 4 — границы окна и `CLOSE_ACTION`/`REDUCE_ONLY`

**Команды:** `grep -rn "обе границы окна\|окну сделки .begin\|begin./.end\|обе границы" docs/` (10 попаданий);
`grep -rn "CLOSE_ACTION\|REDUCE_ONLY\|StrategyPositionAction" docs/ .claude/work/backlog.md` (30 попаданий).

| Файл (§секция) | Исход |
|---|---|
| `Position.md:85` | **правлено** (H26) |
| `RefreshBillsExecutor.md:15` | **правлено** (H26) |
| `Strategy.md:646` (перечень видовых таблиц) | **правлено** (H21-хвост): добавлена `strategy_position_actions` |
| `pnl` §Следствия | **правлено** (H21-хвост): заведены два пункта — `Order.Type.REDUCE_ONLY` и форма явного выхода, с явной пометкой «scope открыт» |
| `account-bills.md:65-66`, `fills.md:54-55`, `order.md:58,94`, `mapping/Order.md:214` | **не требуют** — `begin`/`end` как параметры API источника, другая ось |
| `pnl:1222`, `fsm-execution-layering.md:138,142,148`, `Strategy.md:472,473,478,499,502,506,512`, `Order.md:74,79,90,93,212,265,280`, `Deal.md:450,456,485`, `ClosePositionExecutor.md:19`, `CreateOrderExecutor.md:117,124`, `CancelOrderExecutor.md:25`, `RefreshOrderExecutor.md:43`, `RefreshPositionExecutor.md:106`, `CalculatedSize.md:27`, `RiskCheckResult.md:44,96,101` | **не требуют** — носители актуальной редакции |

---

## Структурный свип — что изменилось в составе файлов

| Действие | Файл |
|---|---|
| создан | `.claude/work/blind/phase-1-step-7-gaps-close-17.md` (запечатанный ключ) |
| создан | `.claude/work/progress/phase-1-step-7-gaps-close-17.md` (этот отчёт) |
| правлен | 14 доков `docs/**` + `.claude/tests/source-api/okx/plan.md` |

Дерево-индекс обновлено тем же ходом (`.claude/knowledge-tree.md`).

## Что остаётся открытым после `GAPS_CLOSE_17`

1. **18 позиций ждут валидации держателя** — 8 внесённых предложением
   (П1-П8) и 10 вынесенных (П9-П18), плюс вклад H20 в `RISK-Q3`. Ключ
   слепого прохода записан.
2. **Пакет валидации `GAPS_CLOSE_16` (25 позиций) по-прежнему не пройден.**
   Счёт гейта делегирования — `0/3`.
3. **Гейт `CODE` закрыт** по двум независимым основаниям: прогон не чист
   (17 гейтящих находок, часть внесена предложением и валидации не прошла) и
   семь рантайм-предусловий открыты, у одного носителя проверки не
   существует (H27).
4. **`DOCS_CHECK_18` не запускается**, пока не пройдены валидации: иначе
   прогон пойдёт против доков, половина которых — непровалидированные
   предложения.

## Связи

- Отчёт прогона — `.claude/work/progress/phase-1-step-7-docs-check-17.md`.
- Запечатанный ключ — `.claude/work/blind/phase-1-step-7-gaps-close-17.md`.
- Хроника шага — `.claude/work/progress/phase-1-step-7-chronicle.md`.
