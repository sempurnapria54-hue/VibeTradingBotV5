# Хроника шага 7 фазы 1 — после смены концепции

## На какой вопрос отвечает этот файл

Что происходило на шаге 7 фазы 1 после смены концепции.

## Назначение

Хроника **текущего** отрезка шага 7 — того, что идёт после остановки
петли `DOCS_CHECK_1`…`DOCS_CHECK_25` и переписывания корпуса по
`docs/concept.md`. Хроника прежнего отрезка (двадцать пять прогонов,
двадцать четыре закрытия) закрыта и лежит в
`.claude/work/history/2026-08-29-step-7-docs-check-loop/` —
источником истины о системе она не является.

## Реестр прогонов

Вход детектора стагнации (`.claude/skills/stagnation-detection.md`).
Строка заполняется при выпуске отчёта прогона.

**Ретроспектива `_1`…`_25`** — в архивной хронике
(`.claude/work/history/2026-08-29-step-7-docs-check-loop/phase-1-step-7-chronicle.md`
§«Реестр прогонов»). Здесь — прогоны после смены концепции; нумерация
сквозная.

| Прогон | Находок | Гейтящих | Отчёт, KB | Гейтящие узлы / несущий класс | Следствия предыдущего закрытия | Дефект перебора в предыдущем закрытии |
|---|---|---|---|---|---|---|
| `_27` | 109 (+9 гигиены) | 43 (8 узлов) | 30 + 602 приложениями | Объём загрузки контекста; **энфорсеры П1 без писателя**; ожидание курса чужой валюты; **заголовочное число**; терминальные контракты; **риск-контур и катастрофический потолок**; спеки-дубли; **гейт грунта** | **4 узла из 8 — рецидивы узлов `_26`** (У7→Н2, У1→Н4, У2/У6→Н6, У5→Н8); ещё 2 частично | нет — переборы `GAPS_CLOSE_26` называли альтернативы явно |
| `_28` | 129 (+24 гигиены) | 44 | 34 + 1.1 MB приложениями | Читатели признака полноты; счётчик серии против фандинга; полярность в новом доме признака входа; терминальные тропы и матрица; таблица потолков (акт добавлен одной строке из трёх); дубль `legFeeRate`; скрипт предусловий не меряет объявленного | **7 узлов `_27` из 9 вернулись гейтящими**; но атрибутируемых правкам `_27` — ≈ 10-12 из 44 (25 %) | нет |
| `_27` | 109 (+9 гигиены) | 43 (8 узлов) | 30 + 602 приложениями | Объём загрузки; энфорсеры П1; ожидание курса; заголовочное число; терминальные контракты; риск-контур; спеки; гейт грунта | 4 узла из 8 — рецидивы узлов `_26` | нет |
| `_26` | 90 | 35 (7 узлов) | 37 + 644 приложениями | Заголовочное число без формы/операндов/писателя (5 линз); траншевый операнд риск-контура; транш в ключах и предусловиях; нижняя граница окна; реестр предусловий `CODE` (4 линзы); обеспеченность потолка на шорте; энфорсер П1 | 2 узла из 7 — непроведённые следствия переписывания корпуса | **да** — у переписывания раздела свипов нет вовсе |

**Как читать строку `_26`.** Величины с `_1`…`_25` **не сопоставимы**:
прогоны считают разные корпуса, и `_26` первым сводит находки к узлам.
«Отчёт, KB» — сборка (37 KB) плюс отчёты семи линз с мандатными полями
(644 KB): ориентир §«Потолок объёма» относится к сборке, приложения
считаются отдельно.

## Хроника

- **Смена концепции (2026-08-29).** Детектор стагнации сработал на
  входе в `GAPS_CLOSE_25` по трём сигналам из четырёх; держатель выбрал
  «сменить концепцию». Написано ядро `docs/concept.md`, корпус переписан
  по нему (234 файла / 1899 KB → 228 / 719 KB), приземлены два
  ратифицированных направления — катастрофический потолок сделки и
  агрегатная сделка с траншами. Итоги —
  `.claude/work/history/2026-08-29-catastrophic-deal-ceiling.md`,
  `.claude/work/history/2026-08-29-tranche-landing.md`.

- **Статус приведён к фактике (2026-08-29).** В роадмапе стояла метка
  `GAPS_CLOSE_25`, тогда как штатного закрытия `_25` не было: детектор
  остановил узел, держатель сменил концепцию, и дальше шло переписывание
  корпуса. Метка заменена на `DOCS_CHECK_26` — статус прогона, которым
  проверяется результат смены. Ролляп фазы не сдвинулся (`IN_PROGRESS`).

- **`DOCS_CHECK_26` — гейт выхода в `CODE` (2026-08-29).** Отчёт —
  `.claude/work/progress/phase-1-step-7-docs-check-26.md`, отчёты линз и
  слой верификации — в одноимённой подпапке. Прогон полнокорпусный: семь
  независимых линз (концепция ×4, торговая корректность, доки↔спецификации,
  предусловия `CODE`) плюс средства нового поколения — прогон
  спецификаций **зелёный** (15 предметов, 178 примеров), сверка доков со
  спецификациями и проверка предусловий `CODE` по рабочим файлам.

  **Гейт не пройден по трём независимым основаниям:** прогон не чист (35
  гейтящих находок в семи узлах); ни одно из гейтящих предусловий `CODE`
  не собрано, а реестр, который их перечислял, уничтожен переписыванием
  (восемь кейсов тест-плана `PENDING` с меткой «ГЕЙТИТ `CODE`»);
  переписывание корпуса было политикоменяющим закрытием **без раздела
  свипов**, и его непроведённые следствия составляют два узла из семи.

  **Что подтвердилось в новой концепции:** перечни статусов, рёбер и
  обработчиков сделки и транша сошлись во всех носителях включая спеки;
  траншевость инварианта покрытия и формула экспозиции сошлись
  слагаемое-в-слагаемое; арифметика `risk-limits`, `deal-risk-numbers`,
  `pnl-reconciliation` пересчитана вручную и сходится; `knowledge-tree`,
  размещение и отсутствие §-адресов внутри `docs/` — чисто. Дефект не в
  новой концепции, а в **незавершённости переноса** на неё.

  **Названное ограничение средства:** зелёный прогон спецификаций не
  доказывает несходства имён — правило «одноимённая из двух источников —
  отказ» работает внутри графа `includes`, а `includes` объявлен в 4
  файлах из 15; кросс-файловый дубль `signedStopDistance` с разными
  операндами прогон пропустил.

  Статус на выпуске отчёта — `DOCS_CHECK_26`; `GAPS_CLOSE_26` не
  проставлялся до исполнения гейта свипов по переписыванию.

- **`GAPS_CLOSE_26` — закрытие (2026-08-29).** Отчёт —
  `.claude/work/progress/phase-1-step-7-gaps-close-26.md`.

  **Сперва исполнен гейт свипов по переписыванию корпуса** — он
  блокировал закрытие. Свипы прогнаны обоими предметами (снятые термины и
  введённая конструкция) плюс структурный, по клеймам полноты и по
  производным носителям; команды и пофайловые исходы — в отчёте. Свипы
  нашли то, чего не находил ни один прогон по терминам: 27 битых входящих
  из `src/` на снятый `docs/decisions/`, ложный клейм «база риска —
  свободный остаток» в двух файлах и устаревшую форму операнда потолков в
  рабочем файле.

  **Закрыты пять автономных узлов** (заголовочное число обрело форму,
  операнды и производителя; риск-контур доведён до траншей; транш доехал
  до ключей и писателей; окно линковки разведено на писателя и операнд;
  реестр предусловий восстановлен в контуре тестов) **и пять решений
  держателя** (шорт равнозначен лонгу с названной ценой; энфорсер П1
  серией убытков; невозрастающая база везде; блокировка итога
  неполученным курсом; восстановление `MARK`-only).

  **Прогон примеров:** 17 спецификаций, 210 примеров, зелёный (было
  15 / 178). Заведены `deal-result.json` и `loss-streak-halt.json`.

  **Граница делегирования уточнена держателем** и внесена в
  `.claude/processes/question-delegation.md`: критерий — выводимость из
  концепции; механический признак «отменяет решение держателя ⇒
  `подтверждение`» снят из шаблона gap-отчёта как слишком широкий.

  **Снапшот v93 выпущен**, v92 — в `history/snapshots/`: он отстал по
  существу (число конфигурационных величин, счёт спецификаций, клейм о
  чистоте корпуса), а не в одной строке.

  Статус шага — `DOCS_CHECK_27`.

- **`DOCS_CHECK_28` — прогон не чист, поднята эскалация (2026-08-30).**
  Отчёт — `.claude/work/progress/phase-1-step-7-docs-check-28.md`; семь
  линз и слой верификации — в одноимённой подпапке. **44 гейтящих
  находки**, 61 прочая, 24 позиции гигиены.

  **Условие держателя от `_27` выполнено:** семь узлов из девяти
  вернулись гейтящими при том, что механизмы глубины и мини-петли
  **работали** (68 замечаний критиков на `_27`, 33 гейтящих, более
  двадцати откатов). Формально окно С1 после сброса не набрано — вопрос
  вынесен по условию держателя, а не по арифметике детектора.

  **Классов возврата четыре, и лечатся они по-разному:** дефект в
  собственном новом артефакте закрытия (правило противоречит себе через
  70 строк; таблица потолков исправлена в одной строке из трёх);
  следующий слой того же узла (механизм заведён, тропа чтения не доведена
  до потребителей); **не взятое в охват** — 66 прочих и 9 позиций гигиены
  `_27` не закрывались, обещанная волна курации не проведена, три
  хенд-оффа отложены «отдельным заходом», который не состоялся; и
  **новое, добытое лучшими средствами**.

  **Концепции касается только четвёртый класс, и он не рецидив:** на
  эталоне **сайзинг вырождается в аллокацию** — поактный потолок риска
  при штатной волатильности не связывает никогда, пара «риск 1 % при
  стопе 0.6 %» невыразима; **штатный выход эталона не уменьшает
  экспозицию транша**, то есть сделка и транш сходятся не на всех тропах.

  **Две ошибки оркестратора исправлены в источнике:** завышенный
  счётный клейм в слое верификации и приведённый как доказательство
  зелёный вывод скрипта предусловий — скрипт **не меряет двух из трёх**
  объявленных осей (воспроизведено пробой).

  Статус **остаётся `DOCS_CHECK_28`**: закрывать два неэскалированных
  узла значило бы предрешить ответ держателя.

- **Кейс п. 17 заведён в план (2026-08-29).** Последнее гейтящее
  предусловие `CODE`, не имевшее носителя, получило кейс **`M17.5`**
  (`.claude/tests/source-api/okx/plan.md`): судьба встроенной защиты при
  отмене родителя, три цепочки — ненали́тый родитель (детерминированная
  база), **частично налитый родитель** (несущая, с конструкцией
  частичного налива через глубину книги и правилом вырождения попытки),
  `ioc`-дублёр (терминал при непустом наливе без нашей отмены). 31 строка
  запросов. Реестр предусловий и `backlog.md` переведены на имя кейса;
  клейм «пункт 17 не имеет кейса» снят. Провенанс — A8 `DOCS_CHECK_24`.

- **Сбор грунта заблокирован средой (2026-08-29).** Прогон контура
  `source-api` невозможен в текущей среде — проверено фактически, не
  выведено: `mvn` отсутствует (и wrapper'а нет), доступная Java — 11 при
  требуемой 25, Docker CLI отсутствует (⇒ `docker-compose.yml` поднять
  нечем), Vault `127.0.0.1:8200` не отвечает (в нём demo-креды OKX,
  `secret/tradingbot/okx-test`), Postgres-test `5441` не слушает. Сеть ни
  при чём: публичный OKX отвечает `HTTP 200`. Собрано **ноль из
  одиннадцати** предусловий; кейсы и реестр к прогону готовы, не хватает
  только среды. Что нужно поднять — `.claude/tests/source-api/okx/code-preconditions.md`
  §«Почему грунт не собран».

- **`DOCS_CHECK_27` — прогон не чист, детектор сработал (2026-08-29).**
  Отчёт — `.claude/work/progress/phase-1-step-7-docs-check-27.md`, отчёты
  семи линз и слой верификации — в одноимённой подпапке. **43 гейтящих
  находки в восьми узлах**, 66 прочих, 9 позиций гигиены.

  **Верификация подтвердила все несущие клеймы и не сняла ни одной
  находки**; одна усилена (предикат полноты контекста не запрещает
  расчёт, а разрешает его на пустом множестве — агрегат `all` над пустой
  коллекцией истинен), две исправлены в источнике по ходу прогона (счёт
  кейсов в §Статус плана, написанный в этой же сессии).

  **Что подтвердилось:** одиннадцать из одиннадцати правок
  `GAPS_CLOSE_26` доехали до носителей; прогон спецификаций зелёный
  (17 / 210, воспроизведён); ссылочная целостность `docs/` чиста (0 битых
  файловых ссылок, 0 неразрешимых §-адресов).

  **Детектор стагнации: сработал С1 на четырёх узлах** — У1, У2/У6, У5,
  У7 прогона `_26` закрыты `GAPS_CLOSE_26` и вернулись гейтящими. С2 не
  срабатывает (нет двух измеримых прогонов подряд), С3 не считается
  (окно после сброса не набрано), С4 не срабатывает. Эскалация выпущена;
  **диагноз — не концепция, а глубина закрытия**: все четыре узла
  вернулись на следующей глубине (носитель есть — механики нет).
  Предложение — не смена концепции, а исполнение критерия глубины и
  мини-петли, введённых в тот же день **после** `GAPS_CLOSE_26`.

  Статус шага — `GAPS_CLOSE_27`; четыре узла в закрытие не входят до
  выбора держателя.

- **`GAPS_CLOSE_27` — закрытие (2026-08-29).** Отчёт —
  `.claude/work/progress/phase-1-step-7-gaps-close-27.md`; отчёты девяти
  узлов и семи проходов критики — в одноимённой подпапке (16 файлов).

  **Первое закрытие по механизмам глубины и мини-петли.** Держатель по
  эскалации С1 выбрал **продолжать итерации** — четыре рецидивных узла
  закрыты здесь же новыми механизмами; смена концепции отложена до
  проверки: вернёт ли `_28` те же узлы при работавших механизмах.

  **Приземлены пять решений держателя:** недопуск инструмента без
  расчётной валюты (отказ на входе, у существующих сделок — аварийная
  тропа); ручной килл-свич как сервисная операция поверх существующего
  механизма, без новых сущностей, плюс описанная **поверхность снятия**
  (она стала единственным выходом после решения о ручном снятии); пол
  дистанции стопа и **декларируемый безубыток** точной формы; запрет
  авто-снятия холда — снят целиком, выражен падающим предикатом.

  **Закрыто девять узлов**, из них два остались **не чистыми** с
  названными основаниями (у матрицы переходов нет исполнимого носителя;
  правки третьего раунда узла ручки не верифицированы, семь находок с
  адресатами). Прогон примеров — **19 спецификаций, 315 примеров,
  зелёный** (на входе 17 / 210).

  **Мини-петля отработала не формально:** 68 замечаний критиков, 33
  гейтящих; ни одно закрытие не было чистым с первого раза. Поймано, в
  частности: инвариант-константа, ничего не проверявшая; «воспроизводимая
  команда», молчавшая на состоянии, породившем находку; формула
  безубытка, дававшая не ноль; потолок, сломанный на двух состояниях из
  трёх; регрессия с бесконечным сворачиванием на автоматической стороне;
  правка, убравшая свидетеля дефекта из примеров.

  **Три уточнения механизмов** внесены по итогам первого применения
  (`roadmap-step-execution.md` §3): пример засчитывается, только если
  падает на прежней форме; пример стои́т на достижимом состоянии; команда,
  объявленная проверкой, прогоняется на двух состояниях.

  **Грунт не сдвинулся** — 0 из 13 слотов, блокер средовой.

  Статус шага — `DOCS_CHECK_28`.

- **Правка пайплайна: глубина закрытия гейтящих узлов (2026-08-29).**
  Задача держателя. Закрытие гейтящего узла обязано доводиться до
  исполнимой глубины (формулировка → носитель существует → механика →
  spec-пример для счётного клейма) и до приземления в корпус проходить
  мини-петлю критики концепт-линзой (максимум два раунда); исполнение
  обоих механизмов — гейтом выхода из `GAPS_CLOSE_N`. Дом —
  `.claude/processes/roadmap-step-execution.md` §3; гейт —
  `.claude/skills/update-roadmap-progress.md` §«Гейт выхода из
  `GAPS_CLOSE_N`»; решение и отвергнутая альтернатива
  (оркестратор-агент по узлу) —
  `.claude/decisions/gating-node-closure-depth.md`; прогон правил на
  послойном рецидиве узла покрытия защиты (`_23`→`_25`) —
  `.claude/work/history/2026-08-29-gaps-close-depth-mechanisms.md`.
  Действует со следующего закрытия (`GAPS_CLOSE_27`).
