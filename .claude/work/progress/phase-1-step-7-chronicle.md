# Хроника шага 7 «Сделки и P&L» (Фаза 1)

## На какой вопрос отвечает этот файл

Какова хроника под-шагов шага 7 Фазы 1.

## Назначение

Хроника текущего шага ведётся здесь (не в `phase-1.md` — там только
таблица статусов); при `DONE` шага файл переезжает в
`history/` вместе с остальными артефактами шага
(`.claude/rules/closed-work-transfer.md`). Отчёты под-шагов —
соседние `phase-1-step-7-*.md` в `progress/`.

## Хроника

- **Шаг 7 → `DOCS_CHECK_1` (2026-07-03):** стартован шаг «Сделки и P&L»
  (`TOOLING` без новых артефактов — фокусы `concept`/`trading` активны). Scope
  (граница 6↔7): расчёт числа `resultProfit` на терминале (вкл. PnL
  `EMERGENCY_CLOSED`) + агрегация фактов в `Deal`; заменяет placeholder-ZERO
  шага 6. Форвард-долг на шаг 7: комиссии в риск-расчёте (§6a шага 5),
  `positions-history` realizedPnl-разложение (В-3), funding SWAP (В-6/OKX-Q3 —
  выбор пути), `trade-fee` (В-7), граница audit/история (шаг 8) vs PnL-число.
  Прогон — три независимых ревьюер-субагента (concept ×2 + trading); CC
  верифицировал ключевые атрибуции грепом. **Не чисто — 6 пробелов, все сходятся
  к центральному блокеру G1** (стадия 0): источник данных `resultProfit` не выбран,
  три дока противоречат (fills/`TradeFill` `Deal.md` ↔ bills/`DealCashFlow`
  `account-bills.md` ↔ positions-history/`realizedPnl` `position.md`); OKX-Q1/Q3
  открыты. Торговый инвариант (TR-1/TR-2/TR-3, блокеры) задаёт направление: число =
  **net** realized P&L (комиссии+funding+liqPenalty) на любом терминале →
  fills-only исключён (fills не несут funding/liqPenalty). G2 (агрегирующая модель
  name-level), G3 (компонент-расчёта не назначен), G4 (fills не агрегирует
  algo-exit) — на выбранном пути G1; G5 (число `EMERGENCY_CLOSED`), G6 (комиссии в
  сайзинге — policy + нюанс скоупа) — отдельные хвосты. Обход остановлен на стадии
  0. **Исход → `GAPS_CLOSE_1`** (после решений пользователя по G1-пути и G6-policy).
  Отчёт — `.claude/work/progress/phase-1-step-7-docs-check-1.md`.
- **Шаг 7 → `GAPS_CLOSE_1` (2026-07-03):** пробелы `DOCS_CHECK_1` закрыты
  согласованными с пользователем решениями (стадия 0 расчищена). **G1** — источник
  числа `resultProfit` выбран (новый `docs/decisions/result-profit-source.md`):
  заголовочное число = **net realized P&L готовым из positions-history**
  (`realizedPnl = pnl+fee+fundingFee+liqPenalty`), категорийная разбивка — из bills
  (`DealCashFlow`), сумма bills сверяется с net; **fills-only отвергнут**
  (`OkxFillResponse` без `fundingFee`/`liqPenalty`). Примирены три расходящихся дока
  (`Deal.md` §Итоговый PnL, `account-bills.md`, `position.md` §История). **OKX-Q1
  закрыт** (persisted `TradeFill` не вводится; инспекция native: positions-history
  несёт `closeAvgPx`/`openAvgPx` → fills для avg-цены не нужны; `REFRESH_FILLS` —
  кандидат на снятие, диспозиция stage-1). **OKX-Q3 закрыт** (funding — через
  bills/positions-history, не `funding-rate-history`; В-3/В-6 разрешены). **G5** —
  число на `EMERGENCY_CLOSED` = фактический realized net вкл. `liqPenalty` (остаток
  DEAL-Q2 закрыт). **G3** — расчёт назначен `FinalizeDealExitExecutor` (число +
  разбивка + сверка), запись на терминале — `MarkDealClosedExecutor`
  (placeholder-ZERO снят). **G6** — прогнозная комиссия включена в риск-сайзинг
  (ставка `trade-fee`, В-7 активирован; `per-trade-risk-policy`/`RiskValidator`/
  `SizeCalculator`). **G4** — resolved-by-path (число не из fills). **G2** — целевые
  носители (positions-history-снапшот + `DealCashFlow`, native
  `OkxPositionsHistoryResponse`) зафиксированы; **структурная спека — стадии 1-2, на
  `DOCS_CHECK_2`**. Реконсилировано 20+ доков + open-questions/backlog/manifest;
  дельта staged. Отчёт — `.claude/work/progress/phase-1-step-7-gaps-close-1.md`.
  **Исход → перезапуск `DOCS_CHECK_2`** (стадии 1-2: процессы/модели/mapping/native
  под выбранный путь).
- **Шаг 7 → `DOCS_CHECK_2` (2026-07-03):** descend на стадии 1-2 под выбранный путь
  (positions-history + bills). Три независимых ревьюер-субагента (concept ×2 —
  механика/стадия 1 и модели/стадия 2 — + trading); CC верифицировал несущие
  атрибуции грепом/`ls` (нет refresh-команд под positions-history/bills; нет
  `MARK_DEAL_EMERGENCY_CLOSED`; `MarkDealErrorExecutor` пишет только `ERROR`;
  `ErrorHandler` без `FINALIZE_DEAL_EXIT`; нет `OkxPositionsHistoryResponse`/
  positions-history-снапшота/`DealCashFlow`; нет fee-поля в `CalculationContext`).
  **Не чисто — 13 находок (11 гейтят).** Кластеры: (A) три носителя пути только
  name-level — native `OkxPositionsHistoryResponse` (N1), positions-history-снапшот
  +имя доменной сущности (N2), модель/mapping/персистенция/линковка `DealCashFlow`
  (N3-N5); (B) механика стадии 1 — **добыча фактов positions-history/bills не
  назначена** (N6, центр тяжести; ни одна `REFRESH_*` их не производит), носитель
  staged-числа между `FINALIZE_EXIT` и `MARK_CLOSED` (N7, ломается об идемпотентность),
  владелец+провенанс аварийного числа (N8); (C) поток ставки `trade-fee` в отрезанный
  от биржи сайзинг (N9); (D) реакция на расхождение сверки bills↔net + epsilon +
  cross-ccy (N10); (E) торговые блокеры — **N8** (контракт `EMERGENCY_CLOSED`
  неисполним для compute-failure-провенанса → усечение левого хвоста R, жёсткий гейт)
  и **N11** (непроверенный инвариант агрегации partial-close на `posId` → рантайм-
  верификация); (F) не гейтят — `REFRESH_FILLS`-диспозиция (N12, ripple по 6
  handler'ам) и funding-как-holding-cost без форвард-дома (N13, форвард к экспектанси/
  фаза 2). Владельцы: `solution-designer` (N2/N3/N5/N6/N7/N8/N9/N10/N12) + `integrator`
  (N1/N4/N11-рантайм) + `trading-specialist` (N8/N11/N13); хвост пользователя тонкий
  (N10 epsilon, N13 scope). Ролляп фазы без изменений (`IN_PROGRESS`: 1-6 `DONE`, 7
  в `DOCS_CHECK_2`, 8-11 `HOLD`). Отчёт — `.claude/work/progress/phase-1-step-7-docs-check-2.md`.
  **Исход → `GAPS_CLOSE_2`.**
- **Шаг 7 → `GAPS_CLOSE_2` (2026-07-04):** descend-закрытие стадий 1-2 —
  13 находок закрыты, механика материализована. **Якорь** — новый
  `docs/decisions/pnl-finalization-mechanics.md`. **N6+N12:** добыча P&L-фактов —
  новые refresh-команды **`REFRESH_POSITIONS_HISTORY`** (positions-history-снапшот)
  + **`REFRESH_BILLS`** (`DealCashFlow`), заменяют снятый `REFRESH_FILLS` (его
  order-fill-метрики покрыты `REFRESH_ORDER`); каскад снятия по ~18 докам
  (агент). **N1-N5 (носители):** созданы native `OkxPositionsHistoryResponse`,
  снапшот `PositionCloseResultExternalSnapshot` (`mapping/PositionCloseResult.md`),
  модель+mapping+таблица `DealCashFlow` (`deal_cash_flows`, FK `deal_id`,
  `UNIQUE(external_bill_id)`; линковка по окну+`instId`+`ccy`, bills не несут
  dealId). **N7:** носитель staged-числа = **поле `Deal`** — `FINALIZE_DEAL_EXIT`
  пишет `resultProfit` на `Deal` в одной транзакции с `COMPLETED` (рестарт-safe),
  `MARK_DEAL_CLOSED` ассертит+терминализует (не пишет число). **N8:** аварийный
  терминал получил владельца — новая команда/executor **`MARK_DEAL_EMERGENCY_CLOSED`**
  (`DealFinalizationType.MARK_EMERGENCY_CLOSED`, симметрично `MARK_CLOSED`);
  провенанс-контракт **исполним** — best-effort: (a) ликвидация → фактический net;
  (b) отказ расчёта → `resultProfit = null` c маркером «неисчислимо» (**не ноль**),
  число не зануляется, левый хвост R не усекается. **N9:** ставка `trade-fee` —
  дом на `InstrumentExternalRules` (навес), калькуляторы читают через
  `CalculationContext.instrumentExternalRules` (без нового поля/fetch). **N10:**
  сверка bills↔net → `AnomalyReport`, **не блокирует** финализацию (число =
  positions-history net); epsilon провизорный (хвост пользователя); cross-ccy guard.
  **N11:** инвариант агрегации positions-history выписан + **рантайм-верификация**
  (test-план §AG1.5, ⏳ PENDING, гейтит CODE). **N13:** разделяющий довод
  комиссия-в-R / funding-в-post-cost-expectancy зафиксирован, форвард-дом — фаза 2.
  Enum `ServiceCommandType` 16→18. Реконсилировано ~35 доков (4 параллельных
  агента на непересекающихся наборах + ядро). Ролляп фазы без изменений
  (`IN_PROGRESS`: 1-6 `DONE`, 7 в `GAPS_CLOSE_2`, 8-11 `HOLD`). Отчёт —
  `.claude/work/progress/phase-1-step-7-gaps-close-2.md`. **Исход → `DOCS_CHECK_3`.**
- **Шаг 7 → `DOCS_CHECK_3` (2026-07-04):** подтверждающий прогон после
  материализации стадий 1-2. Три независимых ревьюер-субагента (concept ×2 —
  механика + модели/mapping — + trading); CC верифицировал несущие атрибуции
  грепом/`ls`. **Ядро механики N6/N7/N8/N12 — проведено полно и согласованно**
  (enum=18, N7 tx-связка «FINALIZE пишет число на `Deal` / MARK_CLOSED ассертит»
  везде, N8 терминал `MARK_DEAL_EMERGENCY_CLOSED` с владельцем, PositionCloseResult-
  и DealCashFlow-пути field-level согласованы). **Не чисто — 8 находок (3 гейтят):**
  **H1 (докогейт)** — N9 fee-wiring доспецифицирован наполовину: модель
  `InstrumentExternalRules` получила fee-поля, но **нет** native `OkxTradeFeeResponse`,
  **нет** маппинга ставки, `mapping/InstrumentExternalRules` **отбрасывает `groupId`**
  (а резолв feeGroup SWAP на нём завязан), `InstrumentExternalRulesSyncJob` не описан
  дочитывать `trade-fee` → `takerFeeRate()` останется null; **H2** — гранулярность
  bills: маппинг выбрасывает native `fee`/`pnl`, если OKX эмитит комбинированный
  trade-bill (`balChg=pnl+fee`) → `TRADE_FEE` недосчитан (гейтит **разбивку**, не
  заголовочное число; рантайм-верификация + вернуть `fee` в used); **H3/N11** —
  инвариант агрегации positions-history (рантайм-гейт, уже трекается §AG1.5). Не
  гейтят: гигиена ×5 (H4 — `lifecycles/DealFinalizationState` без `MARK_EMERGENCY_CLOSED`,
  `risk-validator-scope` без него в списке, мёртвые ссылки на удалённый
  `RefreshFillsExecutor`, неполные cross-ref), H5 (`Instant` vs `OffsetDateTime` в
  снапшоте), H6 (форвард: null-drop смещает ожидаемость — «пометки достаточно» торгово
  неверно, → фаза 2), H7 (переякорить epsilon на Σ|amount|), H8 (инвариант «комиссии в
  USDT, не OKB»). **Торговый синтез:** три механизма (N8 null-drop, N11 недосчёт,
  опущенный гэп-проскок) смещают левый хвост оптимистично согласованно — форвард-фокус
  фазы ожидаемости. Владельцы `GAPS_CLOSE_3`: `integrator` (H1/H2 native/mapping/sync) +
  `knowledge-curator` (гигиена H4/H5); форвард H6-H8. Ролляп фазы без изменений
  (`IN_PROGRESS`: 1-6 `DONE`, 7 в `DOCS_CHECK_3`, 8-11 `HOLD`). Отчёт —
  `.claude/work/progress/phase-1-step-7-docs-check-3.md`. **Исход → `GAPS_CLOSE_3`**
  (узкий) + **рантайм-верификация N11/H2 до `CODE`**.
- **Шаг 7 → `GAPS_CLOSE_3` (2026-07-14):** узкое закрытие находок `DOCS_CHECK_3`.
  Решения по H2/H5/H6/H7/H8 приняты пользователем до исполнения — под-шаг их
  **фиксирует, не переоткрывает**. **Закрыто 6 из 8:** **H2-докочасть** — native
  `fee` возвращён в used, `TRADE_FEE`/`REBATE` резолвятся по нему (не по знаку
  `balChg`), `balChg` остаётся под сумму-сверку; **безопасный суперсет** (корректен
  при любой гранулярности → не ждёт §AG1.5). Носитель — компонента `externalFee` на
  строке, **не вторая строка** (выведено из `UNIQUE(external_bill_id)`: две строки
  задвоили бы Σ`amount` и не имели бы второго `billId`); следствие — realized-слагаемое
  = `amount` − `externalFee`, native `pnl` остаётся unused. **H5** — `Instant` →
  `OffsetDateTime` в `PositionCloseResult` + (сверх находки) в native
  `OkxPositionsHistoryResponse`, иначе рассинхрон воспроизводился бы с другой стороны.
  **H6** — «пометки достаточно» ревизовано: null outcome-коррелирован, drop
  воспроизводит смещение омиссией; принятое направление — **добор числа** до истечения
  окна positions-history (~3 мес), материализация → форвард фазы 2; решение «не ноль»
  верно, не тронуто. **H7** — epsilon переякорен с `|net|` на Σ`|amount|` (охраняемая
  ошибка — композиция разбивки, масштабируется оборотом); провизорность величины
  сохранена. **H8** — инвариант «комиссии только в settle-ccy» **принят**; дом —
  `docs/rules/trading-constraints.md` §Валюта комиссии (реестр инвариантов контура), не
  OKX-док: формулировка **нейтральна к источнику**, довод — переносимость (свойство
  схемы «net в одной валюте», не особенность OKX); guard сохранён как детектор
  нарушения; «например комиссия в OKB» выровнено в 4 доках. **H4** — гигиена ×5
  (F-A1..A5) закрыта; сверх находки — refresh-список в `risk-validator-scope` тоже был
  стар (добавлены `REFRESH_POSITIONS_HISTORY`/`REFRESH_BILLS`).
  **H1 (N9 fee-wiring) — ЗАКРЫТ, гейт `CODE` снят.** Проработан `integrator`'ом
  (ступень Предложение, прогон против офдока 2026-07-14) → провалидирован в чате →
  зафиксирован. **Гейт закрылся без новой фактуры от биржи:** `groupId` уже приходит
  `/public/instruments` («instType and groupId should be used together to determine a
  trading fee group») — дыры в фактуре не было, был **отброшенный ключ** (отброс
  сделан до changelog OKX 2025-11-21, который ввёл `groupId`/`feeGroup` и задепрекейтил
  флэт `maker`/`taker` для SWAP). Ось резолва — **пара (`instType`,`groupId`)**, перечень
  групп не хардкодится («actual return values shall prevail»). **Хранение (уточнение
  пользователя — ревизует место, не seam):** фактура подтвердила, что ставка
  **group×tier-level**, а не инструмент-level ⇒ новая модель **`TradeFeeRate`** —
  **одна строка на группу**, таблица с историей, **не копия на каждом инструменте**
  (копия — заготовка под расхождение при смене тира); на навесе только ключ
  `externalFeeGroupId`; **seam не двинулся** (калькуляторы читают там же, через
  `CalculationContext.instrumentExternalRules`, аксессоры гидрируются). Запись:
  значение изменилось → новая строка, совпало → `modifiedAt` ⇒ бесплатно история
  скачков тира + **прямой измеритель свежести**; `level`/`ts` пишутся датчиком (такт
  пересчёта тира офдок не специфицирует — пусть ответят собственные данные).
  Такт — **(A′) чтение раз-на-тик** (1 вызов `trade-fee(instType=SWAP)` до цикла;
  один писатель навеса); отвергнуты (B) две джобы (гонка RMW по навесу) и (C) чтение
  на входе. **Холд биржи по несвежести** > 24 ч (конфиг): при часовом такте это
  24 неудачи подряд = поломка интеграции; сделки доживают. **Снятая посылка:** тезис
  «съехавшая ставка вылезет аномалией на сверке» **не подтвердился** — сверка сравнивает
  bills ↔ positions-history, оба факта биржи, прогноз в ней не участвует (верифицировано
  грепом) ⇒ аспект 2 `CODE` не гейтил. **Аспект 3 (найден интегратором, в задании его не
  было):** null-политика — IGNORE-null + **реджект `FEE_RATE_UNAVAILABLE`**; fallback
  отвергнут (подставленное число выглядит фактом; заниженная ставка → позиция больше
  положенной — та же болезнь, что H6/H2). **Инвариант organic-base-rates** записан как
  условие: вход в MM-программу требует пересмотра оси запроса. **Часть 4** — `elpMaker`
  проверен → **unused**, но не по предполагавшейся причине (лежит **внутри `feeGroup[]`**,
  а не в плоской шестёрке; не потребляем, т.к. берём только `taker`/`maker`, а это ставка
  ELP/RPI-программы) ⇒ защитную механику не строим; прод-переименование → `rpiMaker`
  **2026-07-28, внутри горизонта шага 7** — отмечено. **Общее правило** записано решением
  `docs/decisions/source-model-change-absorption.md` (изменение источника поглощается
  native-слоем; резолв **по заполненности, не по дате** — заполненность самокорректируется
  при сдвиге сроков). **Вне scope:** рантайм-трек — N11 (§AG1.5) + H2-рантайм (§AG3.5) +
  RQ-1…RQ-4 (M1.7/AG3.4/AG12.4/AG12.5) заведены PENDING-кейсами на **одной** фикстуре
  §AG1.5, **не гонялись**. Исполнение: ядро — CC, периферия — 3 параллельных субагента.
  Создано 4 файла, правлено ~30. Ролляп фазы без изменений (`IN_PROGRESS`: 1-6 `DONE`,
  7 в `GAPS_CLOSE_3`, 8-11 `HOLD`). Отчёт —
  `.claude/work/progress/phase-1-step-7-gaps-close-3.md`.
  **Исход: доковых гейтов не осталось → `DOCS_CHECK_4`** (H1 задел
  mapping/sync-job/риск-слой и ввёл новую модель — концепт-прогон обязателен);
  рантайм-фикстура — после чистого `DOCS_CHECK_4`.

- **Шаг 7 → `DOCS_CHECK_4` (2026-07-15):** подтверждающий прогон после
  `GAPS_CLOSE_3` — три независимые линзы (concept-A механика, concept-B модели
  field-level, C торговая) + верификация несущих атрибуций CC грепом (V1-V11).
  **НЕ чист: 18 находок, гейтят 9.** Кучкуются в **4 узла**. (1) **Гидрация
  (H1)** — прошлый H1 закрыт не до конца: дом ставки переехал, а поверхность
  чтения доспецифицирована **только для `SizeCalculator`**; несущий довод
  `GAPS_CLOSE_3` «seam не двинулся» для `RiskValidator` **ложен** (его сигнатура
  — `DealContext`, читает через `findByInstrumentId`, `CalculationContext` у него
  нет); владелец гидрации не назван, `InstrumentExternalRulesDataService` о
  `TradeFeeRate` не знает вовсе. (2) **Холд по несвежести (H3/H4/H5/H6)** —
  новый триггер подключён к блок-сету, спроектированному под другой триггер:
  `SUBMIT_ALGO_ORDER` — это **main protection и reduce-only partial exit**, т. е.
  «сделки доживают» (`exchange-hold.md:59`) противоречит `:38-40` **в том же
  файле**; класс холда не определён (реакция против L4 = kill-switch +
  `AnomalyReport` + ручное снятие; тропа `HoldSignal`→`SafetyHoldCoordinator`
  синк-джобе недоступна; §Снятие в `exchange-hold` отсутствует, а
  `instrument-hold:47-48` на неё ссылается — битая кросс-ссылка); `modifiedAt`
  как измеритель свежести **не следует** из JPA auditing (no-op не dirty; touch
  нигде не задан) ⇒ ложный холд после 24 ч **здоровой** работы; `Exchange.HOLD`
  вне енума (унаследовано, backlog п.9). (3) **Ось резолва (H7/H8)** — пара
  (instType, groupId) единообразно **заявлена** в 6 доках, но **не применена**:
  индекс на сырой колонке против запроса по доменному enum (+ реальный довод
  `UNKNOWN`-коллизии); матч по голому `groupId`; контур **SWAP/FUTURES** против
  чтения `instType=SWAP`. (4) **Число vs категория (H2/H9)** — знак ставки
  («минус = комиссия», `trade-fee.md:111`) **не сведён** с `+ commissions`
  (`abs` нет нигде на пути) ⇒ буквальная композиция даёт **позицию больше
  положенной** — ровно тот отказ, ради которого отвергли fallback; резолв
  категории при комбинированной bill-записи без приоритета ⇒ `REALIZED_PNL` не
  назначается никогда. **Подтверждено-ОК:** перенос дома ставки чист (остатков
  нет), `TradeFeeRate`↔mapping↔native согласованы field-level, H5-типы выровнены,
  H7-epsilon единообразен, арифметика H2-суперсета верна при обеих
  гранулярностях, H4-гигиена закрыта фактически. **Гипотезы не подтвердились:**
  «одна строка на группу vs история» — противоречия нет (`UNIQUE` НЕТ задано
  явно); «реджект = 4-й outcome-коррелированный механизм» — нет (но 4-й нашёлся
  по другому маршруту — H16 cross-ccy). **Торговый синтез:** к прежнему
  оптимистичному крену по оси **исходов** добавился новый **пессимистичный** по
  оси **возможностей** (цена консерватизма всюду оценена в ноль) — крены на
  разных осях, друг друга не гасят. **Эскалации — 4** (тяжёлая: класс холда —
  L4 vs новый слабый класс «гейт входа»; FUTURES в контуре; ось резолва;
  граница «доки vs code-writer» для H2/H5). Ролляп фазы без изменений
  (`IN_PROGRESS`). Отчёт —
  `.claude/work/progress/phase-1-step-7-docs-check-4.md`.
  **Исход: `DOCS_CHECK_4` НЕ чист → `GAPS_CLOSE_4`;** гейт `CODE` не снят,
  рантайм-фикстура §AG1.5 — по-прежнему после чистого прогона.

- **Шаг 7 → `GAPS_CLOSE_4` (2026-07-15):** закрытие 18 находок `DOCS_CHECK_4`.
  Решения по всем 4 узлам приняты пользователем до исполнения — под-шаг их
  **фиксирует, не переоткрывает**. **Узел 1 (H1) — гидрация:** владелец назван
  — **хранилищный слой** (`InstrumentExternalRulesDataService`, новый
  §«Гидрация ставки комиссии»); обе тропы чтения навеса гидрированы, читатели о
  `TradeFeeRate` не знают. Снят ложный claim `GAPS_CLOSE_3` «seam не двинулся»:
  разведено **поверхность чтения** (одна, не двигалась) vs **тропы чтения**
  (две — через фабрику и прямая у `RiskValidator`); отсюда и владелец —
  единственная общая для них точка. **Узел 2 (H3/H4/H6) — холд:** scope =
  **инструмент** (не биржа); дом — `instrument-hold.md` §«Несвежесть ставки
  комиссии», из `exchange-hold.md` триггер убран. Введён **новый
  концепт-критерий** — `error-handling-policy.md` §«Радиус ущерба задаёт
  scope»: **уровень задаёт реакцию, радиус ущерба — scope** (несвежесть по
  классу — L4, но биржа исправна ⇒ радиус = инструменты группы). Реакция
  **не смягчена** (kill-switch + `AnomalyReport` + ручное снятие); слабого
  класса холда не заведено. H3 разрешён **по построению**: холд инструмента не
  оставляет живых сделок (kill-switch их снимает) ⇒ противоречие «блокируем
  управление позицией, но сделки доживают» исчезло. H4-битая кросс-ссылка
  починена на стороне цели (§Снятие заведён; вопрос `exchange-hold.md`
  переформулирован — был **уже содержания**). H6 **испарился вместе с
  посылкой** (писателя `Exchange.HOLD` шаг 7 не вводит); разнобой имён
  (`TRADE_BLOCKED`) запаркован явной ссылкой — `Exchange.md` §Енумы +
  `backlog.md` п.9. **Узел 3 (H7/H8):** ключ `trade_fee_rates` — **сырая** пара
  (`externalInstrumentType`, `externalFeeGroupId`), индекс теперь совпадает с
  ключом; довод `UNKNOWN`-коллизии записан (доки его не делали). Контур фазы 1
  — **SWAP-only**, FUTURES вынесен (горизонт — шаг с отдельными биржами);
  доменный enum и `delivery`-поля остаются (модель шире контура). Матч в синке —
  по паре, не по голому `groupId`. **Узел 4 (H2/H9):** знак ставки снимается
  **при маппинге** `× −1` (не `abs` — `abs` потерял бы ребейт), одним местом;
  ниже маппинга ставка = **издержка**, `+ commissions` верно в обе стороны.
  Записана **асимметрия** с `DealCashFlow.externalFee` (там знак сырой — факт,
  участвует в арифметике `amount − externalFee = pnl`), чтобы следующий проход
  не «выровнял». H9: категория — по **типу операции**; приоритет не нужен (у
  комбинированной записи один `type` → `REALIZED_PNL`, комиссия — компонентой);
  ветка нуля задана (`0` ≠ `null`); **область суммирования** — новый раздел,
  три суммы / три области, `FUNDING` исключён из разбивки, но **не** из
  сверки. **H5/H11:** заведено поле **`refreshCount`** — инкремент на каждом
  успешном чтении ⇒ строка dirty ⇒ `modifiedAt` двигается; довод «счётчик
  отказов не нужен» **перепроверен и держится**, но опора сменилась (был
  незаписанный побочный эффект — потому и ложен; стал `refreshCount`).
  «Значение группы» = taker/maker/**`level`** (тир — часть значения, иначе
  датчик не ловит смену тира без смены ставки); `externalTs` — метка ответа,
  строку не рождает. **H10 возвращена как сомнение** (не закрыта молча);
  крен: механизм = **отсутствие записи** (append-only), не IGNORE-null —
  рудимент редакции «ставка на навесе». **H12** — довод переформулирован (вывод
  не изменился): «стоп 1-2%» не свойство системы, и рисковали **дрейфом**, а не
  **уровнем** ставки. **H18** — OKX-Q2 разобран: вопрос **жив** (он про
  off-band-архив, не про executor; живые входящие ссылки), stale-опора
  переписана; сверх находки — дрейф `knowledge-tree.md` (2 строки). **Форвард**
  (H13-H17) — в `backlog.md`; **форвард-фокус переформулирован двухосевым**:
  ось **исходов** (оптимистичный крен: H6 + N11 + TR2 + **H16**) × ось
  **возможностей** (пессимистичный: H13 + H15); оси не гасят друг друга ⇒
  бэктест↔live двусторонне несопоставимы. **Порядок «чистый `DOCS_CHECK` →
  рантайм» проверен — держится:** острота снялась решением по H3/H4 (свежесть
  меряется **по группе** ⇒ «чей возраст при N группах» не возникает); RQ-1
  проверяет **посылку** дизайна, а не добирает спеку — остаётся до `CODE`.
  **Правки пайплайна (5):** ярлык исхода на каждой находке + пустая ячейка
  дайджеста = названный дефект отчёта (`concept-review` §Выход,
  `chat-project-instructions`); владелец на каждой гейтящей, торговые →
  `trading-specialist` (`trading-review`); **сборка вариантов эскалации из всех
  линз узла** (`concept-review`, новый §) — главная правка; **гейт не ставится
  на невыясненном** (новый §; кейс — H6); на `Предложении` молча не
  финализируется ничего + калибровка «X верно, чинится Y — это крен, а не
  вывод» (`question-delegation`). **Счёт гейта делегирования: 4 совпадения,
  1 существенное расхождение ⇒ обнулён** (леджер, строка 2026-07-15). Диагноз
  расхождения — **дефект сборки эскалации, не незнание**: вариант
  «scope=инструмент» лежал в отчёте находкой линзы C (H15), но §Варианты собраны
  только из линзы A ⇒ до пользователя не доехал. Прогон непригоден для счёта в
  **обе** стороны (слепой проход шёл против неполного ключа). Исполнение: ядро —
  CC, периферия — 3 субагента. Две поправки исполнителей приняты (в т. ч. отказ
  внести ошибку из задания CC — про `FUNDING` рядом с суммой-сверкой).
  Создан 1 файл, правлено ~25. Ролляп фазы без изменений (`IN_PROGRESS`: 1-6
  `DONE`, 7 в `GAPS_CLOSE_4`, 8-11 `HOLD`). Отчёт —
  `.claude/work/progress/phase-1-step-7-gaps-close-4.md`.
  **Исход: доковых гейтов не осталось → `DOCS_CHECK_5`** (закрытие задело 4
  узла, ввело концепт-критерий + поле `refreshCount`, сменило ось ключа
  (миграционный порог), переписало резолв категории и перенесло триггер холда
  между правилами — концепт-прогон обязателен); гейт `CODE` не снят,
  рантайм-фикстура §AG1.5 — после чистого прогона.

- **Шаг 7 → `DOCS_CHECK_5` (2026-07-15):** подтверждающий прогон после
  `GAPS_CLOSE_4` — три независимые линзы (concept-A механика/холды, concept-B
  модели field-level, C торговая) + верификация несущих атрибуций CC (V1-V10).
  **НЕ чист: 22 находки, гейтят 10, зависимых 2.** Кучкуются в **4 узла**.
  **Ключевой итог (линза C, подтверждён A независимо):** `GAPS_CLOSE_4` развёл
  оси «уровень → реакция» и «радиус ущерба → scope» — верный ход — но применил
  радиус **только к scope холда**; у **реакции** тоже есть радиус (kill-switch
  адресует живые сделки). Радиус проверили у одного элемента и не проверили у
  другого, хотя критерий был на руках. (1) **Узел холда (H1-H10, самый
  тяжёлый, торговый по природе)** — **H1**: тропа синк-джобы к холду не
  существует (весь аппарат анкерён на триггерной сделке: `HoldSignal` едет на
  `DealTransition`, `react(signal, dealContext)`, `fireInstrument(dealContext)`
  бьёт по графу **одной** сделки) — это **прямой невзятый остаток H4**
  `DOCS_CHECK_4` (тот имел 3 части: класс / **тропа** / §Снятие; закрыты 1-я и
  3-я, тропа из перечня выпала); **H2**: kill-switch на живой сделке действует
  **за радиусом ущерба**, который сам критерий и устанавливает — ставка
  потребляется только в сайзинге **нового** входа, `R` живой сделки задан
  стопом на бирже, `resultProfit` считается из фактов ⇒ реакция снимает то,
  чего нет, платя полспреда + правый хвост R; **H3**: §«Что блокирует»
  противоречит себе (`:88-90` risk-reducing разрешены ↔ `:93-95` partial exit
  срублен), «H3 разрешён по построению» — **разрешение ампутацией** (порядок
  обоснования инвертирован; держится только при безотказном kill-switch, а он
  bounded 3 попытками); **H4**: в **доминирующем** режиме отказа (один вызов
  `trade-fee` на все группы; severity выведена из «права/эндпоинт/подпись» —
  аккаунт-радиус) scope=инструмент вырождается в **весь контур** ⇒ принятый
  вариант строго хуже отвергнутого (та же поверхность + kill-switch по каждой
  живой + снятие ×N); штатный `enabled=false` ⇒ kill-switch по портфелю;
  **H5**: писатель пишет `TRADE_BLOCKED`, а енум/правило — `HOLD` (разнобой H6
  не испарился, а **переехал** на инструмент; шаг 7 **вводит писателя**);
  **H6 (находка CC, ни одной линзой не поднята)**: `HoldScope` вéлит scope к
  уровню (`INSTRUMENT` ≡ L3, `EXCHANGE` ≡ L4, «уровни соответствуют
  error-градации») — ровно то тождество, которое новый критерий отрицает ⇒ у
  L4-класса с instrument-scope нет корректного представления в аппарате.
  (2) **Ось ключа (H11-H13)** — H7 закрыт на оси `instType` и **воспроизведён
  на оси `exchangeId`**: ключ заявлен **парой** в 6 доках, индекс и резолв —
  **тройка**, а навес `exchangeId` физически не несёт ⇒ claim «индекс теперь
  совпадает с ключом» ложен. (3) **Категорийная ось (H14-H16)** — H9 приведён
  в модели и mapping, **native остался** на снятой знаковой оси; claim
  «суперсет корректен при любой гранулярности» **слишком силён** для суммы
  комиссии (счётом: под-случай B2 роняет комиссию в 0). (4) **Cross-ccy
  (H17, независимая находка)** — единственный документированный детектор
  инварианта **пуст по построению**: `ccy` фильтруется дважды выше по потоку
  (query `ccy=USDT` + предикат линковки). **Подтверждено-ОК:** гидрация
  (узел 1 `GAPS_CLOSE_4`) закрыта чисто, `refreshCount` закрывает H5/H11 **по
  механизму** (проверено, не по заявлению), знак `× −1` одним местом (7
  носителей согласованы, арифметика счётом), IGNORE-null вычищен, OKX-Q2
  починен, SWAP-only согласован. **Торговый синтез:** `GAPS_CLOSE_4` болезнь
  «цена консерватизма = 0» частично вылечил и одновременно **воспроизвёл в
  максимальной форме** — kill-switch подан как *снятие* риска, т. е. цена
  записана **со знаком выгоды**. Двухосевой форвард-фокус **держится как
  декомпозиция** (третьей оси нет), но знак — свойство **механизма**, не оси:
  решение этого прогона заполнило пустовавшую клетку «исходы × пессимистично»
  (H22), H21 добавил шестой механизм. **Эскалации — 6** (тяжёлая — узел холда,
  собрана из **всех** линз узла: реакция (C) / блок-сет (C) / тропа (A) /
  различение режимов (C); правка пайплайна прошлого прогона применена).
  **Кандидат в правку пайплайна:** закрытие находки не проходит по её
  **собственному списку цитат** — класс дефекта третий прогон подряд («seam не
  двинулся» → «H6 испарился»/«H12 закрыт»/«индекс совпадает»); диагноз
  структурный (§Закрытые отчитывается по узлу, находка называет носители
  поимённо). Ролляп фазы без изменений (`IN_PROGRESS`: 1-6 `DONE`, 7 в
  `DOCS_CHECK_5`, 8-11 `HOLD`). Отчёт —
  `.claude/work/progress/phase-1-step-7-docs-check-5.md`.
  **Исход: `DOCS_CHECK_5` НЕ чист → `GAPS_CLOSE_5`;** гейт `CODE` не снят,
  рантайм-фикстура §AG1.5 — по-прежнему после чистого прогона (H15 её питает).

- **Шаг 7 → `GAPS_CLOSE_5` (2026-07-18):** закрытие под сверку пакетом
  (узел 1 + H6/H13 прошлой сессии, H5/H11/H15/H17 — текущей); материализована
  **только свалидированная политика**, структурные развилки — **withhold**
  (тестовый материал `DOCS_CHECK_6`). До под-шага — **две правки пайплайна**:
  **(A)** закрытие находки — по её собственному списку цитат из отчёта, с
  отметкой каждой (правлено / не требует правки — почему); отдельный явный
  шаг, перечень из отчёта, не заново (`roadmap-step-execution.md` §3; класс
  дефекта «обоснование верно для части носителей» — третий прогон подряд);
  **(B)** ось структурной свободы при генерации вариантов: «переструктурировать»
  (новый компонент / сущность / радиус / модель угроз) выносится наравне с
  «починить внутри» (`concept-review.md`, новый §; сверка дала 4 расхождения
  одного класса — пользователь выбирал переструктурирование, линза чинила
  внутри онтологии). **Закрыто по спискам цитат (правка A применена):**
  **H2** — реакция на несвежесть **мягкая**: kill-switch снят, запрет новых
  входов по инструментам протухшей группы + `AnomalyReport`, живые сделки
  доживают под своим стопом; критерий расширен — **радиус ущерба ограничивает
  и реакцию** (`error-handling-policy.md`). **H3** — блок-сет режет **создание
  нового риска**, сопровождение открытой сделки проходит; граница — общая с
  `risk-validator-scope` (новый § там). **H4** — радиус **по режиму отказа**
  как политика: «вызов не прошёл» → аккаунт; «группа отсутствует в ответе» →
  группа; «синк выключен» — не источник холда. **H6-ярлыки** — ярлыки уровня
  сняты с `HoldScope`/`KillSwitchService`/API kill-switch (scope = радиус;
  уровень — ось error-политики); радиус `INSTRUMENT_GROUP` **не введён**
  (withhold). В `error-handling-policy` — **полный перечень scope и реакций +
  сводная таблица scope × reaction**. **H15** — claim
  гранулярность-независимости ограничен (держится для Σ`amount` и
  realized-слагаемого, не для Σ`externalFee`; под-случай B2 описан, факт —
  §AG1.5). Выводимые: **H14** (native переведён на ось `type`/`subType`),
  **H12** (пара в 3 клаузах scope), **H18** (реш.4 выровнен по уточнённому
  доводу), **H20** (`HoldSignal.md` перебакечен в п.9), **H10-ссылка**
  (gatekeeper → `instrument-hold.md`), **H21** (рамка «безопасная сторона»
  снята — недосайзинг), **H22** (форвард-фокус — **две оси × две стороны**,
  механизмы по клеткам; клетка «исходы × пессимистично» опустела снятием
  kill-switch). **Withhold (осознанно открыто, в open-questions не заведено —
  чтобы не подсказывать стадии 0):** H1-тропа (+расхождение «обнаружение
  отдельным компонентом»), H5-имя, H6-радиус, H7/H8 (зависимые от H1), H9,
  H11/H13 (вкл. ложный claim «индекс совпадает с ключом»), H16, H17 (cross-ccy
  целиком; свалидировано только направление «сторож оживить»), H19.
  **Инспекция `src/` (H5):** `Instrument.Status` в коде несёт **оба** статуса —
  `HOLD` (онбординговый «придержан») и `TRADE_BLOCKED` (safety; javadoc:
  «Отдельный смысл от онбординг-`HOLD`») ⇒ **расходятся доки** (инвентарь
  `Instrument.md` неполон), дефект в доковом инвентаре, не в аппарате; вход
  для финализации H5 следующим прогоном (сам H5 не финализирован). Правлено
  16 файлов доков/рабочих + 2 файла пайплайна; попутно: дрейф
  `knowledge-tree.md` (docs-check-5 не был внесён), мусорный хвост в
  `docs-check-5.md` вычищен. Ролляп фазы без изменений (`IN_PROGRESS`: 1-6
  `DONE`, 7 в `GAPS_CLOSE_5`, 8-11 `HOLD`). Отчёт (обход по спискам цитат) —
  `.claude/work/progress/phase-1-step-7-gaps-close-5.md`.
  **Исход → `DOCS_CHECK_6`** (проверка: поднимает ли линза с правкой B
  структурные развилки из withhold сама); гейт `CODE` не снят.

- **Шаг 7 → `DOCS_CHECK_6` (2026-07-18):** подтверждающий прогон после
  `GAPS_CLOSE_5` — три независимые линзы (concept-A механика/холды, concept-B
  модели field-level, C торговая; прошлые отчёты линзам запрещены) +
  верификация несущих атрибуций CC (V1-V7). **Первый прогон обновлённой
  машины** (формат `templates/docs/gap-report.md`: обязательные цитаты; ось
  структурной свободы; код-клаузы по `src/`). **НЕ чист: 21 находка, гейтят
  4, зависимых 3.** Узлы: (1) **стейл-носители реакции** (H1, все три линзы
  независимо) — `pnl-finalization-mechanics.md:211-222` и **CODE-пункт**
  `backlog.md:308-310` предписывают снятую реакцию (kill-switch + ручное
  снятие) против 6 согласных носителей канона `GAPS_CLOSE_5`; провенанс —
  остаток закрытия H2 (носители вне списка цитат, свип не покрыл рабочие
  файлы). (2) **Снятие/enforcement мягкого холда** (H2+H3+H4, центр
  тяжести) — ось снятия не задана нигде (у kill-switch-класса есть, у
  мягкого нет; по Карверу риск-процесс без «условия отката» неполон);
  доковые инвентари статусов стейл против кода (`TRADE_BLOCKED`
  материализован в обоих енумах, `instrument-hold` указывает на
  онбординговый `HOLD`); ярлыки L3/L4 остались на `AnomalyReport.scope`.
  Настоящая конструкторская развилка — **state vs вычисляемый предикат**
  (крен всех трёх линз независимо — предикат). (3) **Cross-ccy guard мёртв**
  (H5, две линзы счётом): `ccy=USDT` в query отсекает нарушение до guard'а,
  вопреки трём декларациям «не отбрасывается молча» — единственный детект
  инварианта не срабатывает никогда, сверка тоже слепа. (4) **Lifecycle
  `AnomalyReport`** (H6) — не вмещает журнальные аномалии шага 7 без
  kill-switch (единственный путь к `COMPLETED` — через
  `KILL_SWITCH_EXECUTED`), управляющие устарели. Не гейтят: 14 (радиус-принцип
  не прогнан по L3 «серии неудач»; две «пустые» клетки матрицы искажений
  непусты; свежесть ключа резолва не меряется; посылка «fee ⊆ amount» не
  записана + fee-эхо под-случай; AG3.5-claim и ссылки AG1.5↔AG3.5;
  «REFRESH_FILLS снят» прошедшим временем против кода; triggerPx — три
  версии по трём носителям; гигиена). **Тест правки B — пройден:** все
  гейтящие withhold-развилки переоткрыты линзами самостоятельно, со
  структурными вариантами в наборах (H5-имя — по коду, S4 сработала;
  H9-снятие — «предикат вместо статуса» у всех трёх; H17-cross-ccy — вкл.
  вариант скан-компонента; H6-радиус; H11-ключ); H1-тропа сузился по
  построению (мягкая реакция сняла нужду в kill-switch-тропе). Не
  переоткрыты только не-гейтящие хвосты (H13/H16/H19 прежнего прогона).
  **Кандидат в правку пайплайна:** свип «сверх списка цитат» после
  политикоменяющего закрытия (по терминам старой политики, по всем живым
  носителям вкл. рабочие файлы). Ярлыки: выводимо — 13, варианты-с-креном —
  6, принято-в-работу — 1, неразрешимо-владельцем — 1 (triggerPx → офдок).
  Владельцы: curator 11 / solution-designer 7 / trading-specialist 4 /
  integrator 2. Ролляп фазы без изменений (`IN_PROGRESS`: 1-6 `DONE`, 7 в
  `DOCS_CHECK_6`, 8-11 `HOLD`). Отчёт —
  `.claude/work/progress/phase-1-step-7-docs-check-6.md`.
  **Исход: `DOCS_CHECK_6` НЕ чист → `GAPS_CLOSE_6`;** гейт `CODE` не снят,
  рантайм-фикстура §AG1.5 — после чистого прогона (H10 расширяет AG3.5).

- **Шаг 7 → `GAPS_CLOSE_6` (2026-08-01):** закрытие 21 находки
  `DOCS_CHECK_6` по спискам цитат. Решения блиц-прохода чата
  материализованы, не пере-решались; хвосты владельцу проработаны с
  вариантами. **Узел 1 (H1):** оба стейл-носителя снятой реакции
  реконсилированы — хвост реш.4 `pnl-finalization-mechanics` и CODE-пункт
  `backlog`. **Узел 2 (H2/H3/H4):** снятие мягкого холда — **ручное**
  (решение пользователя, расходится с креном всех трёх линз на предикат;
  довод — human-in-the-loop, пайплайн в отладке), предикат свежести —
  **форвард на установившийся режим**, не отказ. **H3 решён по коду и дал
  третье имя статуса:** `Instrument.Status.TRADE_BLOCKED` в коде не «гейт
  входа» — `DealOrchestratorJob.enforceHold` уводит все активные сделки
  held-scope в `ERROR`, то есть переиспользование воскресило бы снятое
  kill-switch-поведение ⇒ введён `ENTRY_BLOCKED`; побочно найдено, что
  мягкий холд не должен быть анкером идемпотентности реактивного контура
  (иначе маскирует последующий kill-switch-триггер). Инвентари статусов
  `Instrument`/`Exchange` синхронизированы с кодом. **H4:** ярлыки L3/L4
  сняты, `HoldScope` расширен `INSTRUMENT_GROUP` (работа поля — снимать
  трёхзначную неоднозначность `instrumentId = null`). **H5:** guard оживлён
  — `ccy` убран из query и из критериев матчинга, ветка на записи «персист
  + линковка + `AnomalyReport`»; **итоговый P&L — в USDT (вариант 1b)**:
  не-USDT движения входят по курсу на момент закрытия; механизм курса —
  открытый `CCY-Q1` с проработанными вариантами. Побочно: H16 выведен из
  клетки «исходы × оптимистично» матрицы искажений. **H6:** одна сущность,
  поведение по `severity`; lifecycle получил журнальную тропу без
  kill-switch, управляющие дополнены `SafetyHoldCoordinator` и
  производителями шага 7; severity перестала задавать блокировку торговли.
  **H7:** серия неудач разведена на вход-сайд (мягкая реакция) и
  управление-сайд (kill-switch адресен). **H9:** свежесть меряется у обеих
  половин резолва — значения ставки и **ключа группы** (свой измеритель на
  навесе). **H13:** посылка про `StrategyAction` по коду не подтвердилась
  (`FINALIZE_DEAL_EXIT` — `ServiceCommandType`; `StrategyActionType` —
  другая ось), но существо решения материализовано: `REFRESH_POSITIONS_HISTORY`
  — вложенный шаг потребляющего действия, снапшот in-memory,
  идемпотентность на уровне действия; введён критерий «факт без
  durable-дома не пересекает границу прохода». **H19:** сверка офдока не
  удалась (страница усечена; дефицит `грунт`), расхождение обезврежено
  контрактно — `triggerPx` всегда опционален, единственный носитель
  формулировки — контракт-док. Остальные (H8, H10-H12, H14-H18, H20, H21) —
  курация по цитатам. **Правки пайплайна материализованы:** свип по
  терминам снимаемой политики после политикоменяющего закрытия
  (`roadmap-step-execution` §3) и распознавание паттерна «покрыто доводом,
  чинится записью» как повода эскалировать (`concept-review`). Свип
  применён: сверх списков цитат поймал 8 носителей прошедшего времени
  `REFRESH_FILLS`, 2 носителя снятой severity-политики, 1 неполный перечень
  реакций. Счёт гейта делегирования (Концепт) — **0** (прогон не чист,
  расхождение — пользовательский вход фазового веса). Правлено 34 файла;
  заведён `CCY-Q1`. Отчёт —
  `.claude/work/progress/phase-1-step-7-gaps-close-6.md`.
  **Исход: гейт `CODE` не снят; следующий ход — `DOCS_CHECK_7`.**

- **Шаг 7 → `DOCS_CHECK_7` (2026-08-01):** подтверждающий прогон после
  `GAPS_CLOSE_6`; три независимых линзы (concept A/B + trading C), запрет на
  чтение прошлых отчётов/снапшотов/notes/history, верификация код-клауз по
  `src/`. **28 находок, гейтят 11, зависимых 3.** Политика, ратифицированная
  на `GAPS_CLOSE_6`, подтверждена всеми линзами; **не доведена
  материализация** — четыре гейтящих узла из десяти суть прямой остаток
  самого `GAPS_CLOSE_6`, и все одного класса: новый контракт не сверен с
  потребителями. (1) **H13-остаток** (H1+H2+H3, центр тяжести): операнд
  окна `end` = `externalUpdatedAt` снапшота недостижим в момент, когда
  `REFRESH_BILLS` линкует — снапшот in-memory внутри другого действия
  (вскрыто **обеими** концепт-линзами счётом); два handler-дока по-прежнему
  эмитят `REFRESH_POSITIONS_HISTORY` отдельной командой; вложенная команда
  не введена в канон (`command-lifecycle` «одна команда за проход»), канала
  возврата снапшота в `ServiceCommandExecutionResult` нет, а заявленный
  прецедент «как REPLACE композирует ноги» отвергается самим
  `replace-not-amend`. (2) **Операнд cross-ccy** (H4): ветка сравнивает с
  `Deal.resultProfitCurrency`, который пишет финализация — на момент записи
  bills он `null`, guard снова мёртв на новом месте. (3) **Чистый `CLOSED`
  без числа** (H5, две линзы независимо): PRECHECK-реджекты уходят в `CLOSED`
  помимо `MARK_DEAL_CLOSED`, ассертируемый инвариант ложен; маркер `null`
  перегружен («неисчислимо» с добором vs «сделки не было»). (4) **Вход в
  safety-статусы** (H13): «только из `ACTIVE`» и эскалация
  `ENTRY_BLOCKED → TRADE_BLOCKED` — в соседних клаузах одного абзаца, а
  `blockTrade` в коде охраняет `WHERE status='ACTIVE'`. Новые узлы, до
  которых прошлые прогоны не доходили: **идентичность записи P&L** (H6+H7 —
  `posId` переиспользуется ~30 дней, фикстура §AG1.5 второго эпизода не
  содержит; фолбэк по `instId` без правила выбора); **что есть `R`** (H8+H9
  — лимит риска проверяется по-действию при `GRID_ENTRY` в контуре;
  знаменатель R не персистится); **формула комиссии** (H10 —
  самореферентность + база нотинала выхода, направленно-асимметричный крен
  для шортов); **носители на JSONB-навесе** (H11+H12 — nullability/бэкфилл
  `confirmedAt`, слот гидрированной ставки уедет в JSONB); **тропа отказа
  добычи** (H15); **измеримость искажений** (H16-H18 — ADL как второй
  механизм клетки «исходы × пессимистично»; цена холда структурно
  неизмерима; реакция на N11-недосчёт обратна собственному критерию радиуса).
  **Верификация CC V1-V12**; V11 **изменила ярлык**: посылка F-C3
  (`GRID_ENTRY` в контуре) установлена по докам и коду ⇒ находка переведена
  из зависимой в гейтящую. **Кандидат в правку пайплайна:** обратный свип —
  по **потребителям вводимого** контракта (кто читает поле, кто эмитит
  команду, доступен ли операнд в момент исполнения предиката); принятый свип
  идёт от старых слов к носителям, этот — от нового контракта к
  потребителям. Ярлыки: `варианты-с-креном` — 16, `выводимо-Предложение` —
  12; дефицит: работа 22, подтверждение 3, грунт 2. Владельцы:
  solution-designer 10 / trading-specialist 8 / knowledge-curator 8 /
  integrator 2. Ролляп фазы без изменений (`IN_PROGRESS`: 1-6 `DONE`, 7 в
  `DOCS_CHECK_7`, 8-11 `HOLD`). Отчёт —
  `.claude/work/progress/phase-1-step-7-docs-check-7.md`.
  **Исход: `DOCS_CHECK_7` НЕ чист → `GAPS_CLOSE_7`;** гейт `CODE` не снят.

- **2026-08-01 — `GAPS_CLOSE_7` (закрытие 28 находок `DOCS_CHECK_7`).**
  Прогон начался со **снятия пяти посылок** отчёта: добытое положение
  закрытия кладётся **на `Position`** (durable), чтение positions-history —
  **ещё один эндпоинт внутри `REFRESH_POSITION`** (канон это допускает,
  новый тип команды не вводится), счётчик попыток **есть** на уровне
  `StrategyAction`, метку свежести навеса держат **audit-поля строки-владельца**,
  «вход только из `ACTIVE`» относится **только к мягкой блокировке**.
  Каждая посылка снята в носителе, а не обойдена. **Узел 1** (H1-H3): добыча
  — вторая нога evidence-cycle `REFRESH_POSITION`, результат полями на
  `Position`; `REFRESH_POSITIONS_HISTORY` снята из целевого состава (18→17),
  док компонента удалён; операнд `end` окна линковки взят с `Position`, а
  **несущим доводом записан инвариант «одна активная сделка на инструмент»**
  плюс условие «линковка — пока сделка удерживает слот». **Узел 2** (H4):
  сравнение с **расчётной валютой инструмента**, пересчёт **сразу на записи**
  по курсу отдельного вызова биржи, курс фиксируется полем `appliedRate`;
  расхождение «курс на момент закрытия» снято, **CCY-Q1 закрыт**. **Узел 5б**
  (H9): `Deal.plannedRiskAmount`/`plannedRiskCurrency`, пишет
  `CreateOrderExecutor` входного действия, write-once. **Узел 6** (H10):
  закрытая форма `contracts ≤ budget / (ctVal × (|entry−stop| + rate ×
  (entryPx + stopPx)))` — фиксированной точки нет, обе ноги по своим ценам,
  асимметрия для шортов снята. **Узел 7а** (H11+H26): измеритель свежести
  переехал на `Instrument.externalModifiedAt`, `refreshCount`/`confirmedAt`
  навеса сняты, наследование `Auditable` у навеса снято; альтернатива
  «отдельная таблица» отвергнута с доводом. **Узел 8** (H13+H14): множества
  входа зафиксированы (`ENTRY_BLOCKED` — из `ACTIVE`;
  `TRADE_BLOCKED`/`CLOSED`/`ERROR` — из любого статуса), три носителя
  реконсилированы, расхождение с `blockTrade` зафиксировано CODE-пунктом.
  **Узел 9** (H15): нового якоря нет, записана **асимметрия троп** (штатная —
  ретрай по счётчику; аварийный терминал — жёсткий отказ = «недоступно» +
  терминал). Выводимые H19-H25/H27/H28 закрыты своими скиллами (анкер
  идемпотентности `AnomalyReport` тройкой, enforcement холда в
  `DealOrchestratorJob`, `RISK_POLICY` vs `EXCHANGE_HOLD`, сужение снапшота,
  реестр кодов `NON_CRITICAL`, вопросник §AG1.5 + дефолт якоря `begin`,
  единое имя времени источника, четыре следа редакций, семантика `ccy`).
  **Оставлено открытым:** `PNL-Q1` (узел 10 целиком), `RISK-Q3` (H8),
  `FEE-Q1` (H12), `HOLD-Q2`. **Вынесено предложениями:** узел 3
  (`ERROR` + `ENTRY_CONDITION_EXPIRED` — три довеска), узел 4 (адресация
  парой; H7 закрывается следствием), носитель расчётной валюты (крен —
  колонка на `Instrument`). Отмечен **дефект отчётности `DOCS_CHECK_7`**:
  флаг действия CC не проставлен на 13 находках из 28 (H16-H28), дефицит не
  указан на H21. Правка пайплайна («прежде чем поднять до гейтящей —
  проверить, не покрыт ли вопрос действующим инвариантом/механизмом»)
  вынесена на решение пользователя, не материализована. Затронут 51 файл
  (42 дока правлено + 1 удалён, 7 рабочих, 1 новый). Ролляп фазы без изменений
  (`IN_PROGRESS`: 1-6 `DONE`, 7 в `GAPS_CLOSE_7`, 8-11 `HOLD`). Отчёт —
  `.claude/work/progress/phase-1-step-7-gaps-close-7.md`.
  **Исход: 24 находки закрыты, 4 открыты, 3 в предложениях; гейт `CODE` не
  снят → следующий ход `DOCS_CHECK_8`.**


- **2026-08-01 — `DOCS_CHECK_8` (подтверждающий прогон после `GAPS_CLOSE_7`).**
  Прогон — **три независимых ревьюер-субагента** (concept линза-A: механика /
  оркестрация / командный слой / правила; concept линза-B: модели / mapping /
  native / персистентность поле-уровнево + миграции; линза-C: торговая
  корректность, грунт — дистиллят корпуса). Линзам запрещены отчёты прошлых
  итераций, снапшоты, заметки, history. *Предшествующая одноли́нзовая
  самопроверка CC замещена этим прогоном и из хроники снята; её измеренный
  результат сохранён в отчёте: из пяти гейтящих узлов линз самопроверка
  нашла три, два пропустила целиком (неисполнимый retry-анкер добывающих
  команд; мёртвый анкер журнальной аномалии), один нашла в одиночку
  (уничтожение анкера реактивного холда).* **27 находок; гейтят 11,
  зависимых 3.** Из них **5 — собственные находки CC**, не поднятые ни одной
  линзой. **Ключевой итог: политика подтверждена всеми линзами — ни одна не
  нашла неверного решения; сломана исполнимость.** Семь гейтящих — **один
  класс**: носитель/механизм назначен, а операнд / канал / анкер под ним
  отсутствует или уничтожается — тот самый класс, который вскрыл
  `DOCS_CHECK_7` и который `GAPS_CLOSE_7` закрывал, воспроизведённый его же
  закрытием семикратно; два рецидива **дословные** (H4 повторяет H4
  `DOCS_CHECK_7`, H14 — H5 `GAPS_CLOSE_4`). Узлы: (1) **операнд `end`** —
  `Position.externalModifiedAt` пишут обе ноги `REFRESH_POSITION`, предикат
  фолбэка невыразим (три источника независимо); (2) **знаменатель `R`**
  (центр тяжести, 5 находок) — фиксируется плановая геометрия вместо
  принятого риска [Tharp гл.6 с.144-146]; у писателя нет канала получения
  величины; `plannedRiskCurrency` ссылается на поле, пустое в момент записи;
  предикат членства R-выборки не определён и «`plannedRiskAmount != null`»
  **неверен**; write-once — скрытый выбор правила агрегации;
  (3) **retry-анкер добывающих команд неисполним структурно** —
  `DealActionState.strategy_action_id` `not null` + `UNIQUE(deal_id,
  strategy_action_id)`, а системные `REFRESH_*` эмитит handler вне
  strategy-action (верификация V6/V7 подняла ярлык); (4) **исход
  `FINALIZE_EXIT` при пустых фактах** — два взаимоисключающих предписания, у
  ретрая нет пути к числу, здоровая сделка уезжает в `EMERGENCY_CLOSED`;
  (5) **cross-ccy** — операнд без дома, эндпоинт котировки не назван,
  политика отказа не задана, слагаемое числа зависит от best-effort-добычи;
  (6) **анкеры идемпотентности** — снятие охраны с `blockTrade` уничтожает
  анкер реактивного холда (kill-switch гонялся бы каждый проход), а анкер
  журнальной аномалии мёртв на своей тропе (`CREATED → COMPLETED` сразу);
  (7) **измеритель свежести ключа группы** — null-ветка не определена,
  писателя метки в коде нет вовсе ⇒ либо весь контур в `ENTRY_BLOCKED` на
  первом тике, либо измеритель нем; (8) **epsilon-якорь** (торговая,
  не гейтит) — Σ`|amount|` доминирована исходом сделки, то есть численно ≈
  отвергнутому `|net|`, и допуск растёт вместе с исходом ⇒ полнота разбивки
  **outcome-коррелирована**. Верификация CC V1-V8 (шесть клауз доков о коде
  и схеме проверены чтением `src/`). Линза B **отвергла** одну находку
  предыдущей самопроверки (схема `Position` — паттерн шага 4, не регрессия).
  Дисклозура: линзе C утекла одна строка из `progress/*` (усилила `F-C2`, не
  породила). **Кандидат в правку пайплайна** (встречный к предложенному
  `GAPS_CLOSE_7`): закрытие, вводящее носитель, обязано назвать его операнд,
  канал и анкер — и проверить, что каждый существует в момент использования и
  не уничтожается сопутствующей правкой; 7 из 11 гейтящих — ровно этот
  дефицит. Ролляп фазы без изменений (`IN_PROGRESS`: 1-6 `DONE`, 7 в
  `DOCS_CHECK_8`, 8-11 `HOLD`). Отчёт —
  `.claude/work/progress/phase-1-step-7-docs-check-8.md`.
  **Исход: `DOCS_CHECK_8` НЕ чист → `GAPS_CLOSE_8`;** гейт `CODE` не снят.
