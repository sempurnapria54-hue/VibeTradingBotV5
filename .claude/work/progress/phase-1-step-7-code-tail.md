# Хвост кодовой фазы шага 7 — что в компонентах не дописано

## На какой вопрос отвечает этот файл

Что осталось дописать в компонентах, переведённых в «в работе».

## Зачем файл заведён

Статус «в работе» в реестре гейтов
(`.claude/work/code-gate-ledger.json`) говорит, что компонент **начат**,
и молчит о том, **чего в нём нет**. Три захода подряд оставляли хвост, а
носителя у хвоста не было: он жил в примечании снапшота одной строкой и
в памяти захода. Этот файл — носитель; позиция закрывается вместе с
доводкой компонента и уходит по
`.claude/rules/closed-work-transfer.md`.

**Хвост — не дефект и не долг корпуса.** Каждая позиция ниже —
**названное** ограничение: доки закрыты, решение принято, код отстаёт
на сознательно проведённой границе захода. Гейт `CODE` этого класса не
меряет — он меряет закрытие находок, а не полноту кодирования.

## Позиции

### Т1. Ветвь свежести и реакции на устаревание (заход 2 → закрыт 2026-09-02)

**Закрыто заходом по реакциям на устаревание** (отчёт —
`.claude/work/progress/phase-1-step-7-code-pass-expiration.md`): ветвь
разведена (`StrategyMarketDataExpiredSetting.resolve` / `isUnprotected` —
величины `protectionBranch` и `expiredSettingResolved`), её операнды
заведены на моделях (`DealTranche.isCovered` / `stopUnresolved`,
`Deal.allTranchesCovered` / `stopUnresolved`), свежесть данных шага
считается (`MarketDataExpirationChecker.stepDataFresh`), а обе аварийные
реакции получили затребователя: `GRACEFUL_CLOSE` — ребро
`ACTIVE → EXIT_PENDING` с причиной `MARKET_DATA_EXPIRED`, `KILL_SWITCH` —
сигнал жёсткой ступени инструмента (`DealActiveHandler`).

**Остаток — два названных ограничения:**

- **агрегатная поверхность шага не обходится ничем.** Разведение
  `stepIsDealLevel` выражено выбором пары операндов на call-site, и обе
  пары заведены; но шагов уровня сделки (`EXIT` / `FAIL_SAFE` агрегатной
  поверхности) не обходит ни один проход — признака «шаг уровня сделки» в
  модели нет, и потребителя у агрегатной пары поэтому нет тоже. Приезжает
  вместе с самой поверхностью;
- **`MarketDataExpirationResult` не построен.** Четыре статуса
  (`NOT_EXPIRED` / `PARTIALLY_EXPIRED` / `EXPIRED` / `MISSING`) и перечень
  причин различают то, чего реакция не читает: отсутствие и устаревание
  ведут к одному исходу. Вместе с ним не построены и объявленные
  `checkForEntry` / `checkForStep`
  (`docs/components/MarketDataExpirationChecker.md` §Контракт) — у
  четырёхзначного результата нет потребителя, а сочинять его ради
  соответствия сигнатуре значило бы завести мёртвый код.

### Т2. Преконтроль: остаток после захода по преконтролю (заход 3 → закрыт 2026-09-01 кроме остатка)

**Закрыто заходом по преконтролю** (отчёт —
`.claude/work/progress/phase-1-step-7-code-pass-precontrol.md`): тропа
резолвера, ветка ослабления на создании защиты, ветка ослабления на
снятии отдельной защиты (`CancelAlgoOrderActionExecutor` +
`RiskValidator.validateProtectionRemoval` + покрытие на модели транша),
карв-аут исчерпанного бюджета и отдельная тропа `DEAL_GRAPH_INCOMPLETE`.

**Прежняя посылка позиции была неверна:** резолвер к тропе приходил —
через `CreateOrderActionExecutor`, а не через обработчик напрямую;
позиция описывала состояние захода 3 и с тех пор не сверялась.

**Остаток — три названных ограничения:**

- **снятие ВСТРОЕННОЙ защиты преконтроля не проходит**
  (`docs/components/CancelAttachedProtectionExecutor.md` §«Снятие
  проходит преконтроль»): эмитента у него нет вовсе — тропа
  переключения защиты точечной отмены не шлёт
  (`TrancheProtectionSwitchedHandler`, forward-refinement). Гейт без
  вызывающего был бы мёртвым кодом;
- **предикат покрытия на СОЗДАНИИ защиты не считается**: исполнимой
  формы у него нет — спека держит `coverageAfterRemoval`, величины
  «покрытие после создания» в ней не объявлено, а сочинять её значило бы
  завести дом мимо стандарта;
- **резолв цели снятия по корню цепочки замещений** (величина
  `cancelTargetCandidates`) — цель резолвится через строку исполнения
  целевого действия; цепочек в рантайме нет, пока фабрика `REPLACE`-ног
  их не порождает.

**Когда:** первые два — ходом по переключению защиты (тропа снятия
встроенной) и заходом по числам риска соответственно; третий — фабрикой
`REPLACE`-ног.

### Т3. Уровни: остаток после захода по уровням (заход 3 → закрыт 2026-09-01 кроме безубытка)

**Закрыто заходом по уровням** (отчёт —
`.claude/work/progress/phase-1-step-7-code-pass-levels.md`): якорь уровня
ветвью по наличию живого эпизода с отказом вычисления при ненаблюдённой
средней (D4) и резолв `levelSource` по объявленному блоку настроек плюс
роль объявления и сужение области охраны стороны уровня (D5).

**Закрыто заходом по ставке комиссии** (2026-09-01): операнд ставки
получил поле, писателя и источник, `BREAKEVEN` считается точной формой от
якоря и ставки, а сторона округления защитного уровня стала выводиться из
положения уровня относительно якоря (прочь от него), а не из стороны
сделки — иначе перенос в безубыток округлялся бы В СТОРОНУ убытка.

**Остатка у позиции нет.**

### Т4. Цикл добычи материализованной защиты (заход 4 → закрыт 2026-09-02)

**Закрыто ходом по циклу добычи** (отчёт —
`.claude/work/progress/phase-1-step-7-code-pass-expiration.md`):
`IntegrationService` получил поверхность обеих ног
(`getPendingMaterializedProtections`, `getMaterializedProtectionHistory`
по ноге разбора), `AlgoOrderOkxResponse` — недостающие поля `sz` и
`failReason`, `OrderMapper` — вторую форму снапшота встроенной защиты, а
`RefreshOrderExecutor` обходит цикл: нога живых, вторая ступень, три ноги
разбора с обрывом на первой нашедшей. Гейт запуска не продублирован —
его отдаёт сам резолвер (`runsSearchCycle`), чтобы добытчик не заводил
свою копию.

**Приехало тем же ходом:** нога истории **отдельной** заявки (цикл 1
`AlgoOrder`) шла без обязательного операнда — эндпоинт истории требует
`algoId` либо `state`, иначе отвечает `code=50015`. Вызов не мог
исполниться ни разу; теперь операнд закрывается идентификатором записи,
а при пустом — терминальными состояниями двумя вызовами, как объявлено
`docs/models/mapping/AlgoOrder.md`.

**Остатка у позиции нет:** сигнал на пустом разборе приехал ходом по
мягкой ступени (ниже) — исход «не определён» поднимает
`INSTRUMENT_PROTECTION_FATE_UNKNOWN` мягкой ступенью инструмента.

### Т11. Мягкая ступень: построена, но потребителей у неё меньше объявленного (ход 2026-09-02, НОВАЯ)

**Есть:** ступень как ось сигнала (`HoldRung`, `HoldSignal.rung`), общий
исполнитель блокировки (`HoldService`: мягкую ведёт сам, жёсткую отдаёт
координатору целиком), писатель и снятие мягкого статуса
(`blockEntry` / `unblockEntry` плюс операция API), журнальная тропа
отчёта (`AnomalyReportService.journal`, `NON_CRITICAL`) и эскалация
мягкой в жёсткую (`blockTrade` из обоих исходных статусов).

**Приехало тем же ходом:** гард `blockTrade` пропускал только `ACTIVE`,
то есть инструмент с мягкой ступенью не принял бы жёсткую **ни разу**.
Дефект был недостижим ровно потому, что писателя у мягкой ступени не
существовало; ввод писателя делает его достижимым, поэтому он закрыт тем
же ходом.

**Нет трёх вещей:**

- **мягкой БИРЖЕВОЙ ступени** — отдельного статуса у неё в модели биржи
  нет, а объявленные триггеры (сверка результата, серия убытков, ручной
  вызов) не закодированы: ветвь без вызывающего была бы мёртвым кодом;
- **блок-сета преконтроля `INSTRUMENT_SAFETY_HOLD`** — мягкая ступень
  сегодня гасит только выборку entry-скана (она берёт `ACTIVE`), а
  объявленное `docs/rules/instrument-hold.md` §Enforcement блокирование
  **ослабления защиты** на живых сделках не работает. Код приезжает
  заходом по числам риска вместе с остальными семью (Т10);
- **предусловия «живого риска не осталось» у снятия ЖЁСТКОЙ ступени** —
  `unblockTrade` его не проверяет, хотя `docs/rules/manual-halt.md`
  §«Снятие: что во что переходит и при каком предусловии» его объявляет.
  Дефект предсуществующий, к мягкой ступени отношения не имеет; у самой
  мягкой предусловие («жёсткая не стои́т») выполнено гардом статуса.

### Т5. Постановка встроенной защиты на биржу (закрыта 2026-09-02)

**Закрыто заходом по постановке защиты:** `PlaceOrderOkxRequest` несёт
`attachAlgoOrds` (элемент — `AttachAlgoOrdOkxRequest`: клиентский id,
триггер, ценовая база всегда, market-флаг исполнения; `sz` не
отправляется — источник ведёт защиту на налитый объём), маппинг —
элементным методом `OrderMapper.domainToPlaceRequest(AttachedAlgoOrder)`;
`SubmitOrderExecutor` пишет защите факт «родитель отправлен»
(CREATED → PENDING) на обеих тропах успешного сабмита. Целевое
расхождение из `docs/models/mapping/Order.md` снято той же правкой.
Наблюдение `slTriggerPxType`, которое позиция гейтила, добыто замером
тем же днём: эхо обеих форм непустое
(`.claude/tests/source-api/okx/code-preconditions.md`
§«Что осталось открытым»).

### Т6. `DealOpeningService` — материализация траншей по объявлениям детали (заход 5, НОВАЯ)

**Есть:** обе тропы создания сделки; на каждой материализуется **один**
транш — входная в `PRECHECK`, восстановительная в `MANAGING`, обе с
`episodeSeq = 1`.

**Нет:** материализации **по объявлениям детали** — «по одному на
объявление, по `levelCount` на шаблон»
(`docs/components/DealOpeningService.md`). Причина названа: объявлений
транша в модели стратегии не существует вовсе — нет ни `StrategyTranche`,
ни поля `strategyTrancheId` у `DealTranche`, ни уровня. Пока их нет,
материализовать по объявлениям нечего, а заводить их «на вырост» —
предмет захода по модели стратегии, не этого.

**Чем это НЕ является:** разрешающей ошибкой. Один транш — законная
конфигурация детали с единственным объявлением; сетка при нынешней модели
просто не выражается.

### Т7. Восстановительная тропа заведена без вызывающего (заход 5, НОВАЯ)

`DealOpeningService.recoverDeal` написан и покрыт тестом, но в продуктовом
коде его никто не зовёт: зовёт его детектор аномалии
(`docs/components/AnomalyJob.md`), а он — шаг 8
(`.claude/work/backlog.md` §«Шаг 8 (safety / AnomalyJob)»).

**Почему тропа всё же закодирована** — довод Д312 дайджеста: без неё у
значения `RECOVERY` нет писателя, и вместе с ним недостижимы признак
«позиция наблюдалась», допущение пустой детали и ветвь материализации
эпизода у сделки без входной заявки. Позиция закрывается вместе с
детектором аномалии.

### Т8. `RefreshPositionExecutor` — ось эпизода и пересчёт чисел риска (заход 5, НОВАЯ)

**Есть:** обе ноги обхода, различение эпизодов парой, материализация
строки закрытого эпизода, наполнение положением закрытия, движение порога
доказанного покрытия монотонным `UPDATE`.

**Нет** двух работ, названных тем же доком:

- **ось эпизода на ногах** — проставление идентификатора эпизода ногам с
  непустым филлом и пустой осью, write-once. Операнда у неё нет:
  `Order` поля эпизода не несёт;
- **пересчёт четвёрки чисел риска сделки** — гейтится признаком полноты
  графа, который контекст прохода готовым не отдаёт (величины
  `graphComplete` на `DealContext` нет), а сами четыре числа на `Deal` не
  заведены.

**Когда:** заходом по числам риска — вместе с их домом и признаком
полноты контекста; порознь ни одна из двух работ операнда не имеет.

### Т10. `RiskValidator` — состав проверок у́же объявленного (заход по преконтролю, НОВАЯ)

**Есть:** торговые ограничения инструмента, сторона уровней, guard
ликвидации, риск на сделку по `riskPerTradePercent`, инвариант
risk-creating входа без стопа, предикат покрытия на снятии.

**Приехали заходом по читателю чисел риска** (2026-09-01):
`LOSS_LIMIT_NOT_CONFIGURED`, `RISK_APPETITE_NOT_CONFIGURED` (новый код —
второе незаданное число обязано быть различимо в данных) и
`FEE_RATE_UNAVAILABLE` на действии, ставящем либо переносящем уровень.
Плюс два неравенства **создания** стратегии
(`STRATEGY_SIMULTANEOUS_RISK_ABOVE_GLOBAL`,
`STRATEGY_CATASTROPHIC_MULTIPLIER_ABOVE_GLOBAL`) и обязательность
четвёрки риск-чисел у торгуемой детали.

**Нет** восьми кодов, объявленных `docs/components/RiskValidator.md`:
`DEAL_GRAPH_INCOMPLETE` как **fail-fast по признаку полноты графа**
(сам код заведён и используется узкой ветвью покрытия, признака
`graphComplete` на контексте нет), `INSTRUMENT_SETTLE_CURRENCY_MISSING`,
`RISK_PER_ACTION_EXCEEDED`, три кода потолков сделки,
`STOP_DISTANCE_BELOW_FLOOR`, `RISK_CREATING_UNDER_COLLAPSE`,
`INSTRUMENT_SAFETY_HOLD`, `DEAL_NOTIONAL_EXCEEDED`. Их операнды —
четвёрка чисел сделки, признак полноты графа и снимок базы риска;
приезжают заходом по числам риска (Т8).

**Следствие для карв-аута:** перечень
`docs/processes/risk-evaluation.md` §«Карв-аут исчерпанного бюджета
сделки» воплощён теми пятью членами, чьи коды заведены; остальные
приезжают вместе со своими кодами.

**Три числа риск-аппетита получили читателя** (2026-09-01): порог серии
убытков и максимальный риск на сделку — fail-fast преконтроля, оба
множителя — неравенства создания стратегии. «Пустое место — отказ»
держится теперь кодом, а в боевом профиле пустое число роняет **подъём**
приложения (`RiskAppetiteStartupCheck`).

**Когда:** заходом по числам риска (Т8) — у большинства из них операнд
тот же: четвёрка чисел сделки и признак полноты графа.

### Т9. Компоненты движений счёта — закодированы (заход 5, 2026-09-02)

**Закодированы оба:** `DealCashFlow` (модель, персистентность V16/V19,
маппинг всех слоёв, датасервис) и `RefreshBillsExecutor` (конвейер
7d→архив с пагинацией внутри границы, граница разбора, дедуп по ключу
идемпотентности, резолв категории по контуру биржи с журнальным
`UNCLASSIFIED_CASH_FLOW`, предикат линковки по спеке, лестница курса
0-4 с координатами свечи, догон курса, предикат остановки
`rateBlocking`). Привязка «слот п. 7 × `AG1.7` → компонент» снята
решением держателя 2026-09-02 (см. реестр гейтов); сам слот закрывается
перепрогоном правленого кейса.

**Остаток — три названных ограничения:**

- **эмиттера `REFRESH_BILLS` нет** — звено добычи движений эмитится
  агрегатной поверхностью (остаток Т1), как и у `REFRESH_FILLS`;
- **глубина конвейера не в конфигурации** — её потребитель (признак
  структурной неполноты `Deal.breakdownIncomplete`) не закодирован и
  приезжает с финализацией выхода; сейчас конвейер решает только
  «звать ли архив» по контрактной глубине свежего эндпоинта (7 дней);
- **дедуп журнального отчёта** — по «отчёт с ключом уже существует»,
  без различения «состояние снялось и вернулось»: операнда снятия
  (опустевшей корзины) пока не наблюдает никто.

## Связи

- Реестр гейтов и статусы компонентов — `.claude/work/code-gate-ledger.json`.
- Перенос закрытого — `.claude/rules/closed-work-transfer.md`.
- Отчёт захода 4 — `.claude/work/progress/phase-1-step-7-code-pass-4.md`.
