# Черновик правок узла 2 `GAPS_CLOSE_5`

## На какой вопрос отвечает этот файл

Каким текстом закрывается узел 2 прогона `DOCS_CHECK_5` шага 10 фазы 2.

Редакция — **четвёртая**, по пробам окрестности приземляющего захода;
третья была по первой верификации
(`phase-2-step-10-gaps-close-5-node-2-verification.md`), вторая — по
раунду критики (`…-node-2-critique.md`). Носитель черновой: в корпус текст
уезжает приземляющим заходом, отчёт узла —
`phase-2-step-10-gaps-close-5-node-2.md`.

**Форма ссылок — продуктовая** (`docs/concept.md` §Ссылки): в `docs/**`
адресных перекрёстий `§«…»` не используется, ссылка смысловая, на дом;
законны только имя принципа (`П1`) и имя величины спеки. Три §-адреса,
попадающие в переписываемые пассажи, снимаются тем же ходом — это
погашение названного долга корпуса, а не новая правка.

**Что редакция вторая изменила против первой:** отбор прохода переведён с
нижней границы полноты на самый ранний уцелевший момент приёма (К2);
ветвь пустого операнда заменена названной недостижимостью (К3); внесены
вывод о консервативности осей времени (К4) и клауза «охраняет от
ухудшения, а не восстанавливает» (К5); названо место величины окна (К1);
поправлены три текстовых дефекта (К8-К10). Правки 6, 7, 9, 10 приведены к
новому операнду тем же ходом.

## Правка 1 — момент писателя (A1)

**Цель.** `docs/rules/statistics-aggregates.md`, буллет «Писатель»
раздела о пересчёте.

**Было.**

```
- **Писатель** — джоба пересчёта модуля статистики; **момент** — её тик
  плюс ручной триггер (`.claude/rules/codestyle.md` §Джобы).
```

**Стало.**

```
- **Писатель** — джоба пересчёта модуля статистики; **момент** — её тик,
  и другого момента у неё нет. Внерасписанного запуска через фасад у джоб
  этого сервиса не бывает: поверхность модуля объявлена только читающей, и
  ручной триггер завёл бы входящую точку записи, которой инвентарь сервису
  не даёт (`docs/architecture/services.md`); довод формы держит дом
  конвенций джоб (`.claude/rules/codestyle.md`), где он назван своим у
  сервиса и старшим общего. Тик достижим расписанием, и другого момента
  писателю не нужно (`docs/rules/writer-named-for-every-value.md`).
```

## Правка 2 — форма пересчёта одна (A1)

**Цель.** Тот же раздел, буллеты об окне тика и о полном пересчёте.

**Было.**

```
- **Что пересчитывается за тик — скользящее окно последних суток**,
  число которых назначает владелец. Сутки берутся по моменту
  **происшествия** события: в них и лежит сделка, к которой оно
  относится.
- **Полный пересчёт** — та же джоба по всему журналу. **Писатель тот же,
  момент — ручной триггер держателя** (`.claude/rules/codestyle.md`
  §Джобы: у каждой джобы есть внерасписанный запуск через фасад);
  расписания у него нет намеренно — он нужен после опоздания, о котором
  знает человек, а не периодически. Законен он всегда: проекция от
  порядка не зависит и повтора не боится.
```

**Стало.**

```
- **Что пересчитывается за тик — скользящее окно последних суток**,
  число которых назначает владелец. Сутки берутся по моменту
  **происшествия** события: в них и лежит сделка, к которой оно
  относится. **Число суток окна — СВОЯ величина конфигурации сервиса**, а
  не константа кода и не копия пределов выдачи: те ограничивают
  запрошенное читателем и консервативны в сторону **меньшего** окна, эта
  назначает объём собственной работы прохода и консервативна в сторону
  **большего** — довод стои́т ниже, у названной цены окна.
- **Форма пересчёта одна, и «полный» есть значение операнда окна, а не
  вторая форма.** Окно, назначенное шире прожитого журналом времени,
  накрывает его целиком — это и есть полный пересчёт; отдельной джобы,
  отдельного момента и отдельного писателя у него нет. Догнать событие
  старше окна значит **расширить окно**: операнд уже объявлен, писателя
  уже имеет, и второй величины не заводится
  (`.claude/rules/design-simplicity.md`). Цена названа: реакция идёт
  сменой конфигурации сервиса и её применением, а не нажатием, и потому
  медленнее на время развёртывания. Законен пересчёт всегда — проекция от
  порядка не зависит и повтора не боится.
```

## Правка 3 — объём прохода и область журнала (A5, C1)

**Цель.** Тот же раздел, два новых буллета сразу после правки 2.

**Стало (вставка).**

```
- **Проход идёт посуточными порциями: запрос и upsert на каждые сутки
  окна, а не один запрос на всё окно.** Иначе окно, расширенное до всего
  журнала, дало бы диапазонный скан по таблице, которая в `prod` не
  чистится и растёт без предела
  (`docs/models/domain/other/AuditRecord.md`), — ровно ту безлимитную
  выборку, которую запрещает дом конвенций кода
  (`.claude/rules/codestyle.md`). Тот же принцип уже исполняет соседняя
  джоба модуля, удаляющая старое ограниченными порциями
  (`docs/components/JournalCleanupJob.md`). **Ограничен порционностью
  объём СКАНА, а не объём результата:** второй ограничен произведением
  операндов ключа зерна и порционности не требует. **По существу
  порционность ничего не стоит:** проекция от порядка не зависит и
  повтора не боится, поэтому обрыв на середине окна оставляет часть суток
  пересчитанными, часть — с прежними числами, и каждая половина верна на
  свой момент сборки.
- **Проход не пишет суток, начавшихся раньше самой ранней уцелевшей
  строки журнала.** Операнд — `journalMinRecordedAt` в
  `docs/spec/audit-journal.json`, величина владельца журнала: её предмет
  — что в журнале **лежит**, а не что группа обещает, и двигает её вперёд
  чистка непроизводственного окружения. Сутки, часть строк которых
  удалена, дали бы группу меньшего состава, а upsert по ключу зерна
  заменил бы верные числа частичными — молча, потому что момент сборки
  строки остался бы свежим.
  - **Оси времени у сравнения разные, и расхождение консервативно.**
    Сутки зерна отсчитываются по моменту происшествия, операнд считается
    по моменту приёма. У всякой удалённой строки момент приёма был меньше
    самого раннего уцелевшего, а произойти событие раньше, чем было
    принято, не могло; значит сутки, начавшиеся не раньше операнда, ни
    одной строки потерять не могли.
  - **Отбор охраняет от ухудшения, а не восстанавливает.** Строка
    отобранных суток остаётся такой, какой её собрал последний прогон по
    тому журналу, что был, и полнее уже не станет; если её не собирал ни
    один прогон — строки нет вовсе. Читателю остаётся клейм нижней
    границы полноты, который выдача отдаёт вместе с числами.
  - **Пустой операнд ветви не требует.** Величины нет, только когда
    журнал пуст, а пустому журналу нечего группировать: строки пишет
    слушатель приёма, и без принятого события их не появляется. Отбор на
    этом состоянии не исполняется, потому что не исполняется и сам
    пересчёт.
  - **Отметкой прогресса чтения отбор не является.** Запрет такой
    отметки стои́т ниже и адресует операнд, по которому выбиралось бы
    пересчитываемое **по тому, что проход уже сделал**. Здесь операнд —
    свойство **журнала**, а не пройденного пути: прогон его не двигает, и
    строка, зафиксированная позже своего момента, из-за него не выпадает.
    Теряет она свои сутки только там, где журнал уже усечён, то есть где
    восполнять нечего.
  - **Вторым операндом отбор не берёт момент наблюдения тем.** Тема,
    присоединённая к подписке позже, нижнюю границу полноты двигает, но
    строк журнала не убавляет: читается она с самой ранней доступной
    позиции, то есть строки прошлых суток после неё только прибавляются.
    Отбор по границе полноты был бы строже механизма — он запрещал бы
    улучшение строки, а не её ухудшение.
```

## Правка 4 — выход из названной цены окна (A1)

**Цель.** Тот же раздел, первое предложение пассажа о цене окна.

**Было.**

```
**Названная цена окна.** Событие о сделке старше окна, приехавшее с
опозданием, свои сутки не подвинет до полного пересчёта. Условие
```

**Стало.**

```
**Названная цена окна.** Событие о сделке старше окна, приехавшее с
опозданием, свои сутки не подвинет, пока окно не будет расширено до них.
Условие
```

## Правка 5 — третья оговорка момента сборки (A1, C1)

**Цель.** Тот же раздел, буллеты оговорок под пассажем о моменте сборки.

**Было.**

```
- строка **внутри** окна получает свежий момент, **не включив** событие,
  приехавшее с опозданием больше окна: до полного пересчёта её число
  беднее своего момента;
- строка **вне** окна не пересчитывается вовсе, и её момент застывает:
  это означает «пересчитывать нечего», а не «устарело»;
```

**Стало.**

```
- строка **внутри** окна получает свежий момент, **не включив** событие,
  приехавшее с опозданием больше окна: пока окно не расширено до его
  суток, её число беднее своего момента;
- строка **вне** окна не пересчитывается вовсе, и её момент застывает:
  это означает «пересчитывать нечего», а не «устарело»;
- строка суток, **которых проход не пишет**, не пересчитывается и внутри
  окна, и её момент застывает по причине, названной у отбора прохода
  выше;
```

## Правка 6 — область клейма о расхождении (C1)

**Цель.** Тот же раздел, пассаж о невозможности тихого расхождения.

**Было.**

```
**Расхождение агрегата с журналом невозможно тихо:** агрегат выводим из
журнала повторным прогоном, и разница двух прогонов есть дефект
пересчёта, а не потерянное событие.
```

**Стало.**

```
**Расхождение агрегата с журналом невозможно тихо:** агрегат выводим из
журнала повторным прогоном, и разница двух прогонов есть дефект
пересчёта, а не потерянное событие. Клейм высказан о сутках, которые
проход пишет: сутки, начавшиеся раньше самой ранней уцелевшей строки
журнала, он не пишет вовсе, и двух прогонов там не бывает.
```

## Правка 7 — клейм восполнимости у дома (C1)

**Цель.** `docs/rules/statistics-aggregates.md`, последний абзац раздела
о персистентности.

**Было.**

```
Строки обеих таблиц **восполнимы полностью** — выводимы из журнала
повторным пересчётом, поэтому глубины хранения у них нет
(`docs/models/domain/other/AuditRecord.md`).
```

**Стало.**

```
Строки обеих таблиц **восполнимы из журнала повторным пересчётом на всём,
что журнал несёт**, поэтому глубины хранения у них нет
(`docs/models/domain/other/AuditRecord.md`). Безусловной восполнимость не
бывает: непроизводственное окружение журнал чистит, и сутки, начавшиеся
раньше самой ранней уцелевшей строки, пересчёту не подлежат — отбор
прохода и его вывод объявлены выше, разделом о пересчёте.
```

## Правка 8 — вычислитель производных (A8)

**Цель.** `docs/rules/statistics-aggregates.md`, раздел «Что хранится»,
новый пассаж сразу после пассажа о слагаемых.

**Стало (вставка).**

```
**Производные вычисляет ПОТРЕБИТЕЛЬ выдачи, а не эта поверхность, и это
названное ограничение.** Выборка агрегатов отдаёт хранимые слагаемые;
доля выигрышных, профит-фактор, ожидаемость, средний R и предикаты их
показуемости (`docs/spec/statistics-aggregates.json`, величины
`winRateReportable` и `rReportable`) исполняются стороной, которая числа
**показывает**. **Дом** — этот пассаж; **что оживит** — построение фронта
(шаг 12 фазы 2, `.claude/work/roadmap/phase-2.md`); **причина** —
производное есть форма показа, а состав экранных операций чтения отложен
с домом и оживителем (`.claude/work/backlog.md`), и завести часть формы
экрана раньше её потребителя значило бы обойти собственную парковку.
Периметр вычислителем при этом не становится: он проксирует форму
владельца и собственных контрактов «на экран» не заводит
(`docs/architecture/contracts.md`). До оживителя предикаты показуемости
не исполняет ни один построенный компонент, и делить на ноль некому —
показывающей стороны нет ни одной; молчания об этом здесь не остаётся.
```

## Правка 9 — указатель у владельца журнала (C1)

**Цель.** `docs/models/domain/other/AuditRecord.md`, раздел «Глубина
хранения», последний пассаж.

**Было.**

```
**Агрегаты глубины не имеют по другой причине:** они выводимы из журнала
повторным пересчётом, то есть восполнимы полностью
(`docs/rules/statistics-aggregates.md`).
```

**Стало.**

```
**Агрегаты глубины не имеют по другой причине:** они выводимы из журнала
повторным пересчётом — на всём, что журнал несёт. Безусловной
восполнимость не бывает: чистка удаляет строки, и сутки, начавшиеся
раньше самой ранней уцелевшей, пересчёту больше не подлежат. Отбор
прохода и его вывод живут у дома пересчёта
(`docs/rules/statistics-aggregates.md`) и здесь не пересказываются.
```

## Правка 10 — указатель у актора чистки (C1)

**Цель.** `docs/components/JournalCleanupJob.md`, раздел «Границы»,
второй буллет.

**Было.**

```
- Он не трогает агрегатов: они выводимы из журнала повторным пересчётом и
  глубины не имеют (`docs/rules/statistics-aggregates.md`).
```

**Стало.**

```
- Он не трогает агрегатов **прямо**, и глубины они не имеют: строка
  выводима из журнала повторным пересчётом
  (`docs/rules/statistics-aggregates.md`). **Косвенно он область
  пересчёта сужает:** удалённые им строки двигают вперёд самую раннюю
  уцелевшую, а суток, начавшихся раньше неё, пересчёт больше не пишет —
  иначе upsert заменил бы верные числа частичными. Отбор объявлен у дома
  пересчёта, там же его вывод.
```

## Чем правки стоя́т друг к другу

- **Правки 1-2 закрывают A1 одним решением:** момент сведён к тику,
  вторая форма снята, догон выражен операндом окна, у операнда назван
  не только писатель, но и место. Ни один из четырёх носителей клейма
  инвентаря не трогается — дом агрегатов приведён к ним, а не они к нему.
- **Правка 3 закрывает A5 и C1 двумя буллетами одного предмета** — объём
  прохода и его область. Они стоя́т рядом намеренно: оба отвечают на
  вопрос «сколько журнала проход читает и на какой его части пишет».
- **Правки 4-6 — потребители правок 1-3 внутри того же раздела:** цена
  окна, оговорки момента сборки, клейм о расхождении. Каждая была верна
  при двух формах пересчёта и безусловной восполнимости; ни одна не
  переписывает решения, все три к нему приведены.
- **Правки 7, 9, 10 — обход трёх носителей клейма восполнимости.** Дом
  несёт отбор и его вывод, две детали — указатель.
- **Правка 8 стои́т особняком:** она закрывает A8, у которого предмет
  другой — величина, которую проход не пишет.
- **Правки 11-13 — не находки прогона, а обязанности правки 3.** Первая
  чинит соседа, чей счётный клейм вставка делает ложным; третья — соседа,
  чей предмет группировки вставка сдвигает; обе поймала проба О3
  окрестности. Вторая доводит решение до дельты `CODE`, без которой оно до
  кода не доедет.
- **Уточнение правки 2 о «своей» величине окна — тоже проба О3:** тот же
  файл уже объявляет величиной конфигурации сервиса предел окна выдачи, и
  с противоположным направлением консервативности; без слова «своя» два
  предела читались бы одним.

## Правка 11 — счёт запросов прохода (окрестность правки 3)

**Цель.** `docs/rules/statistics-aggregates.md`, конец буллета «Как джоба
достаёт входы, при том что журнал лежит в ДРУГОЙ базе».

**Было.**

```
  Запроса, соединяющего две базы, при этом нет — запрет
  того же дома не нарушается: там запрещён **кросс-базовый запрос**, а
  здесь два независимых запроса к двум базам в одном проходе.
```

**Стало.**

```
  Запроса, соединяющего две базы, при этом нет — запрет
  того же дома не нарушается: там запрещён **кросс-базовый запрос**, а
  здесь запросы к каждой базе идут отдельно, в одном проходе.
```

Счёт запросов снят: он был верен при одном запросе на окно и стал ложным
от посуточных порций и запроса операнда отбора (правка 3). Довод, ради
которого пассаж написан, сохранён дословно.

## Правка 12 — позиция дельты `CODE` (A1, A5, C1)

**Цель.** `.claude/work/progress/phase-2-step-10-chronicle.md`, таблица
§«Дельта `CODE`, накопленная шагом», новая позиция после 37.

**Стало (вставка строкой таблицы).**

```
| 38 | **конструкция прохода пересчёта: окно из конфигурации, посуточные порции, отбор по уцелевшему журналу.** Число суток окна — величина конфигурации сервиса; «полный пересчёт» есть её значение, а не второй исполнитель — ручного фасада у джоб этого сервиса нет. Проход идёт **порциями по суткам** (запрос и upsert на порцию) и **не пишет суток, начавшихся раньше самой ранней уцелевшей строки журнала**. Довод, вывод о консервативности осей и разведение с запретом отметки прогресса — `docs/rules/statistics-aggregates.md`, здесь не пересказываются. **Охрана позиции 22 читается на порцию**: `bucket_date` входит в ключ зерна, и одним запросом ключи фикстуры, лежащей в разных сутках, не собираются — фикстура кладётся в одни сутки либо счёт складывается по порциям. Своя охрана — тесты: после чистки, вынесшей часть суток за уцелевший минимум, строка этих суток прогоном не изменена; сутки внутри уцелевшего журнала пересчитаны; обрыв между порциями оставляет пересчитанными ровно пройденные сутки | `GAPS_CLOSE_5`, узел 2 (A1, A5, C1) |
```

**Почему новой позицией, а не правкой 21.** Позиция 21 отвечает на вопрос
«откуда джоба берёт входы и чем держится „только на чтение“» — её предмет
тропа и грант. Конструкция прохода — другой предмет, и сведение их в одну
позицию сделало бы её ответом на два вопроса
(`.claude/rules/structure.md` §Принципы). Ссылка на 22 стои́т внутри 38,
потому что двигает **чтение** её охраны, а не саму охрану.

## Правка 13 — предмет группировки при порциях (окрестность правки 3)

**Цель.** `docs/rules/statistics-aggregates.md`, первое предложение буллета
«Агрегирование выполняется В БАЗЕ ЖУРНАЛА, а не в памяти джобы».

**Было.**

```
  Запрос к `audit` группирует строки окна **по ключу зерна собираемой
  строки** и отдаёт **по строке на ключ**;
```

**Стало.**

```
  Запрос к `audit` группирует строки порции **по ключу зерна собираемой
  строки** и отдаёт **по строке на ключ**;
```

Предмет группировки — порция, а не окно: посуточные порции правки 3
делают запрос на сутки, и «строки окна» после неё описывают не тот
запрос. Довод об объёме результата и всё остальное в буллете остаётся
дословно — он верен при любом размере порции и становится только тесней.
