# Закрытие шага 8 фазы 1

## На какой вопрос отвечает этот файл

Чем закрыт шаг 8 фазы 1.

## Назначение

Отчёт закрывающего захода: аппрув `CODE` (фокусы `conventions` /
`performance` / `disaster`), `SYNC_DOCS_FROM_CODE` (фокус `divergence`) и
пост-хок концепт-гейт. Гейты —
`.claude/processes/roadmap-step-execution.md` §Гейты; простановка
статусов — `.claude/skills/update-roadmap-progress.md`.

**Состояние на входе.** `HEAD = 46e63fa` (`ROADMAP 1-8 CODE_5`), 270
тестов без падений, девять проверок корпуса зелены, `code-gate-check.py`
— ПРОЙДЕН (42 позиции реестра, открытых ноль), реестр предусловий —
открытых слотов ноль.

## 1. Аппрув `CODE` — три адверсариальных фокуса

Предмет — код шага 8: `git diff 72bb462..HEAD -- src/`. Фокусы прогнаны
по своим скиллам независимыми проходами; стойка адверсариальная — искали
дефект, не подтверждали корректность. Автор кода свой код не аппрувил.

**Находок 55** (`conventions` 23, `performance` 13, `disaster` 19), из них
после сведения дублей — **41 различная позиция**. Блокирующих по оценке
фокусов — 12.

### 1.1. Что закрыто правкой кода

Каждая правка предъявлена **падающей пробой**: прежняя форма возвращалась
во временную копию, проба падала, форма возвращалась обратно.

| # | Находка (фокусы) | Механизм | Чем закрыта |
|---|---|---|---|
| **K1** | обход берёт только `ACTIVE`-инструменты (`D1`, `D7`, `P13`) | `A9` уводит инструмент в `ENTRY_BLOCKED` → следующим тиком он выпадает из контура → `A2` видит его живые сущности «вне контура» → жёсткая ступень + flatten всей биржи. Плюс `A6` недостижим: его популяция — как раз заблокированный инструмент | контур читается целиком (`findContourWithin`), статус выборку не сужает. Проба: `blockedInstrumentStaysUnderObservation` + `standingInstrumentRungWithLiveEntitiesIsReported` — на прежней форме 2 падения |
| **K2** | жёсткая биржевая ступень встаёт только из `ACTIVE` (`D2`) | биржа под мягкой ступенью (её ставит и слепота `A10`) — жёсткая находка проглатывается молча: kill-switch не гоняется, отчёта нет. Дом лестницы говорит «из любого» | `updateStatusUnlessAlready`; тест `ExchangeHardRungEscalationTest` (2 теста) |
| **K3** | окно подтверждения без верхней границы (`D3`) | ручной триггер через 5 с после планового тика «подтверждает» транзиторное расхождение → жёсткая ступень | `confirmationMinAge` (30 с) как верхняя граница; проба `confirmationWindowHasUpperBound` |
| **K4** | журнальная строка не дедуплицирована (`P1`, `D4`, `D16`) | `A6`/`A8` пишут строку **каждым тиком** бессрочно — 1440 строк в сутки на залипшую сущность, и каждая тянет 2 HTTP к бирже; объявленный им гистерезис не читался никогда | гейт стоящей строки перенесён **перед** журнальной веткой. Проба `journalOnlyFindingDoesNotDuplicateStandingRow` — на прежней форме падает |
| **K5** | нулевая позиция считается живой (`D6`) | четыре следствия сразу: `A2` сносит биржу за закрытую позицию, `A6` рапортует при пустом счёте, `A9` молчит в своей популяции, восстановление берёт не ту строку | нормализация один раз, в сборке среза. Пробы `zeroSizedPositionIsNotLive`, `nullSizedPositionIsNotLive` |
| **K6** | потолок страницы меряется у склейки (`D5`, `D14`, `P`-хвост) | срез algo — три семьи по 100; 120 строк суммарно объявляют проход неполным при полностью добытом срезе → три тика → мягкая ступень со снятием вручную. У позиций потолок не запрашивался вовсе, но мерился тем же числом | мера уехала на сторону источника, к выданной странице. Проба `concatenatedFamiliesDoNotMarkPassIncomplete` |
| **K7** | отметка прохода до детекции (`D9`) | упавшая детекция при добытом срезе сбрасывает счёт слепоты — «не смотрели» становится неотличимо от «ничего не нашли» | операнд — исход наблюдения, отметка после обхода. Проба `failedDetectionIsNotCountedAsObservedPass` + контроль |
| **K8** | обрыв обхода биржевых признаков на первом отказе (`D10`) | остальные чужие сущности тика не увидены при внешне чистом проходе | `try` внутрь цикла — симметрично двум соседним детекторам |
| **K9** | `A7` с гистерезисом в один тик (`D12`) | наша закрывающая заявка уходит `close-position`, у которого `clOrdId` **нет в контракте**: висит в срезе без маркера → жёсткая ступень за наш штатный выход | гистерезис два тика. Ось не менялась — опровергнута посылка «наш ход признака не производит» |
| **K10** | писатель счётчиков биржи не назван | `NOT NULL`-колонки, api-запрос их не несёт → JPA шлёт явный `null` поверх дефолта, вставка падает. Класс пришёл с шага 7 (`consecutive_loss_count`), шаг 8 добавил второй экземпляр | `ExchangeService.create` ставит обе нулём; тест `createNamesWriterForMandatoryCounters` |
| **K11** | предикат дедупа без индекса, `count(*)>0` вместо `exists` (`P2`) | скан всей таблицы журнала на каждую находку каждого тика; связка «журнал растёт → проверка дороже» кормит сама себя | миграция `V25` + форма `exists` |
| **K12** | `findAll()` по биржам (`C6`, `P5`, `D15`-часть) | единственный безлимитный `findAll` в `src/main`, введён этой дельтой; имя обещало фильтр, которого нет | окно + честное имя `findContourWithin` |
| **K13** | порядок гардов и дубль запроса (`P3`, `P4`, `P8`, `C22`) | запрос к БД впереди дешёвой проверки поля; тот же признак резолвится дважды за тик; выборка инструментов внутри цикла по биржам | признак резолвится один раз на инструмент и передаётся вниз; выборки вынесены из цикла |
| **K14** | гигиена конвенций (`C1`-`C4`, `C5`, `C7`-`C10`, `C13`-`C15`, `C17`, `C18`, `P12`) | осиротевший javadoc в 4 точках (объявление вклинилось между комментарием и его декларацией — документация у прежнего метода пропала); ручные проверки коллекций; `Collectors.toSet()` с именем класса; поле без javadoc; магическое `32`; мёртвые возвраты; порядок импортов; избыточный `@InjectMocks`; двойной вызов за одним значением | все сведены к правилу |

### 1.2. Что закрыто разбором, без правки

| # | Находка | Исход |
|---|---|---|
| **C11** | `Exchange.blindPassCount` в доменной модели никто не читает | **не дефект**: поле — операнд round-trip'а domain ↔ persistence. Снятие его сделало бы полноблочный `save` записывающим `null` в `NOT NULL`-колонку — то есть ровно тот дефект, который закрывает `K10` |
| **C16** | явный `Integer.valueOf` в `assertEquals` | **не дефект по существу**: разводит перегрузку `(Object, Object)`; правило запрещает его как «избыточный», и здесь обоснование не держится. Ушёл вопросом к держателю (`CONV-Q1` п.3) |
| **C19** | `CONSTANT.equals(x)` против `Objects.equals` | буквальное чтение делает дефектом 129 мест корпуса против 112 «правильных». Правка дома конвенций — не автономная; `CONV-Q1` п.1 |
| **C20** | доменный слой читал `Constants.Okx` | **снято попутно** правкой `K6`: константа уехала на сторону источника. Само право осталось вопросом (`CONV-Q1` п.4) |
| **C21** | ручной маппинг в `AlgoOrderMapper` | **вне дельты**: метод рукописный из-за дерева `condition`, дельта лишь добавила в него поле |
| **C23** | калибровочные числа без ключей в yaml | **не дефект**: `codestyle` §Джобы требует конфига для `enabled` и CRON, и они есть. Вынос прочих knob'ов — вопрос владельца, не правило |
| **D18** | `markPass` — `@Modifying` плюс чтение проекции в одной транзакции | **корректно**: скалярная JPQL-проекция читает БД после авто-флаша. Хрупкость названа в javadoc репозитория |
| **D19** | заявка без маркера, пережившая деплой | **припаркована**: это предусловие деплоя, а не правка кода |

### 1.3. Что припарковано

Восемь позиций, чей блокер — **отсутствие носителя**, а не работа.
Дом — `.claude/work/backlog.md` §«Хвост шага 8 (safety / AnomalyJob) —
возврат по появлению носителя»; у каждой названы дом, что оживит и
причина (`.claude/rules/parking-address.md`).

| Позиция | Что оживит |
|---|---|
| строка наблюдения одна на **окно**, а не на всё стоящее состояние | носитель «последнее наблюдение» либо пересмотр окна первым периодом прогонов |
| слепок внешнего состояния стои́т двух чтений биржи (`P1`-хвост) | ход, переносящий срез прохода в слепок (меняет контракт находки) |
| заявка без маркера, пережившая деплой (`D19`) | первый запуск новой версии на счёте — предусловие деплоя |
| `terminalLocally` читает сущность по одной (`P7`) | достаточное число живых заявок, чтобы поштучное чтение мерилось |
| контролируемое исключение на тропе прохода без ловца (`D8`) | ввод резолвера статуса в тропу любого из трёх срезов |
| отчёт с блокировкой не несёт предмет в данных (`D13`) | ход, вводящий предмет как журнальное поле вне ключа |
| мультибиржевая корректность прохода (`D15`) | вторая строка `exchanges` (горизонт — фаза 3) |
| пул планировщика в один поток (`D17`) | ход, вводящий `spring.task.scheduling.pool.size` |

Отдельно — **`P6`** (`DealContext` пересобирается по каждой активной
сделке, удваивая тяжелейшую периодическую нагрузку): фокус пометил её
«требует замера», деградации при объёмах фазы 1 не утверждает. Остаётся
названным ограничением в том же разделе бэклога.

### 1.4. Что ушло держателю

**`CONV-Q1`** (`.claude/work/questions/open-questions.md`) — пять
случаев, не покрытых `codestyle`. Маршрут задан ролью
(`.claude/agents/reviewer.md` §«Что входит»): у фокуса `conventions`
непокрытый случай идёт в чат напрямую, потому что конвенции пополняются
через чат. Вердикт гейтинга — **не гейтит**: ни один не меняет поведения
кода.

**Самопроверка выноса** (`.claude/processes/question-delegation.md`
§«Вынос проходит самопроверку, и она записывается»): владелец закрыл всё,
что выводится, — четыре из пяти позиций получили разбор с ценой и
вариантами, пятая (`C20`) снята попутной правкой. Не выводится остаток
одного класса: **правка дома конвенций**. Он не проектная развилка, и
режим автономии на него не распространён — `codestyle` §Наполнение
называет чат единственным способом пополнения.

### 1.5. Исход аппрува

**Находок 41, закрыто 33 (14 правкой кода, 8 разбором, 11 гигиеной в
составе `K14`), припарковано 8, вынесено держателю 5** (последние —
подмножество закрытых разбором, счётом не задваиваются).

Прогон после закрытия: `mvn -o test -DexcludedGroups=source-api-live` →
**293 теста, 0 падений** (+23 к входу). Компиляция с
`-Dmaven.compiler.showDeprecation=true` — предупреждений по нашим классам
ноль (фокус `conventions` этого измерить не смог: `mvn` не на `PATH`).
Девять команд корпуса зелены; `anchor-check` поймал **собственный
артефакт закрытия** — новый §-адрес с сокращённым по двоеточию хвостом,
адрес приведён к дословному имени.
