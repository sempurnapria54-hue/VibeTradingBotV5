# DOCS_CHECK_2 шага 10 фазы 2 — линза A (концепция)

## На какой вопрос отвечает этот файл

Какие концептные пробелы найдены линзой A в прогоне `DOCS_CHECK_2` шага 10
фазы 2.

## Охват

**Предмет.** Шаг 10 фазы 2 — «Аудит и статистика: один деплой, потребители
событий, журнал по тенанту, агрегаты, read-only API»
(`.claude/work/roadmap/phase-2.md:40`, примечания 84-104). Статус на входе —
`DOCS_CHECK_2`, то есть **до `CODE`**: ведёт док, «в коде нет» даёт дельту
`CODE`. Оговорка стадии применена: клейм дока о **фактическом** устройстве
кода при расхождении — дефект дока.

**Проверено.** `docs/models/domain/other/`: `AuditRecord.md`, `Auditable.md`,
`AccessDenial.md`; `docs/rules/`: `statistics-aggregates.md`,
`audit-not-runtime-source.md`, точечно `api-access-policy.md`,
`market-data-retention.md`, `loss-streak-halt.md`,
`writer-named-for-every-value.md`; `docs/spec/`:
`statistics-aggregates.json`, `audit-journal.json`,
`event-actor-presence.json`; `docs/architecture/` целиком;
`docs/dictionary/audit.md`; точечно `docs/models/domain/aggregate/Deal.md`;
`tools/derive/journal-continuity-branches.sh`,
`event-class-manual-path.sh`, `statistics-deal-classes.sh`;
`.claude/work/backlog.md`, `.claude/work/decision-digest.md`,
`.claude/work/code-gate-ledger.json`, `.claude/work/questions/open-questions.md`.

**Верификация клеймов о коде** (граница `concept-review` соблюдена — код читан
только как носитель-цель атрибуции, ревью корректности не производилось):
`libs/domain-model/.../domain/event/**`, писатели событий в
`services/trading-core/**`, `services/strategies/**`, `services/bff/**`,
`services/*/src/main/resources/application.yaml`, `deploy/base/services/`.

**Вне охвата (помечено, не проверялось).** Фаза 4 (сигнал, аллокатор,
бэктест), фаза 5 (второй субъект), шаг 11 (тесты), шаг 12 (фронт).

**Инструменты корпуса прогнаны — все зелёные**, то есть находки лежат там, где
детекторов нет: `bash tools/spec-run.sh` (27 спек, 645 примеров),
`bash tools/spec-mutation-check.sh` (427 величин, недоказательных 0),
`population-derive-check.py`, `spec-scope-check.py`, `spec-pointer-check.py`,
`anchor-check.py`, `doc-pointer-check.py`, `retired-check.py`,
`deploy-layout-check.py`. Указатели на файлы во всех доках охвата разрешаются
(проверено циклом по `` `путь` ``-вхождениям).

## Стадия остановки

**Прошёл все стадии (стадия 2).** Стадия 0 не чиста (A2, A12 — граница шага),
но обход не останавливался: механика описана достаточно, а гейтящие находки
стадии 0 адресуют скоуп, а не неизвестную механику.

---

# Находки: блокирует выход в CODE

## A1. Класс `DealShutdownInitiated` не лежит ни на одной стороне закрытого перечня, и счёт «девять из двадцати одного» ложен

**Вопрос.** Шаг обязан «назвать поимённо, каких классов журнал не несёт и чем
каждый закрывается» (`phase-2.md:98-104`); дом перечня — `AuditRecord.md`
§«Каких классов журнал сегодня не несёт», объявленный закрытым («Построены
**девять**, и журнал несёт ровно их»). Ровно один класс таблицы §События не
попал ни в построенные, ни в непокрытые. Построено **восемь**, непокрытых —
**тринадцать**.

```
bash tools/derive/event-class-manual-path.sh | sort > /tmp/all.txt
LC_ALL=C.UTF-8 grep -hoP '^\s{4}\K[A-Z][A-Z_]+(?=,?$)' \
  libs/domain-model/src/main/java/com/example/tradingbot/domain/event/CoreEventType.java \
  libs/domain-model/src/main/java/com/example/tradingbot/domain/event/StrategyEventType.java \
  | LC_ALL=C.UTF-8 awk -F_ '{s="";for(i=1;i<=NF;i++){s=s toupper(substr($i,1,1)) tolower(substr($i,2))};print s}' \
  | sort > /tmp/built.txt
LC_ALL=C.UTF-8 sed -n '/^## Каких классов журнал сегодня не несёт/,/^\*\*Перечень/p' \
  docs/models/domain/other/AuditRecord.md | grep '^| `' \
  | LC_ALL=C.UTF-8 awk -F'|' '{print $2}' \
  | LC_ALL=C.UTF-8 grep -oP '`\K[A-Z][A-Za-z]+(?=`)' | sort > /tmp/notcarried.txt
cat /tmp/built.txt /tmp/notcarried.txt | sort -u > /tmp/covered.txt
comm -23 /tmp/all.txt /tmp/covered.txt
```

Вывод — одна строка: `DealShutdownInitiated`. Счёт сторон: 21 / 8 / 12.

**Следствие не косметическое.** `audit-not-runtime-source.md:38-44` объявляет
носителем клейма «`shutdownReason` виден в таймлайне» именно этот класс. Класса
нет — значит, у пункта, для которого тот же док требует «либо класс события с
полем содержимого, либо названное ограничение с домом, оживителем и причиной»
(`:21-24`), нет ни того, ни другого; соседние пункты того же раздела
ограничение называют, этот — нет. `HoldReleased` стои́т в **точно том же**
состоянии (писатель объявлен, значения в перечне нет) и назван непокрытым и
припаркован (`.claude/work/backlog.md` §«Класс события `HoldReleased` и его
ручная тропа»). Асимметрия — пропуск, не решение.

**Цитаты (носители).**

| Носитель | Что стои́т |
|---|---|
| `docs/models/domain/other/AuditRecord.md:104-119` | «двадцать один… Построены **девять**» + таблица 12 имён |
| `docs/architecture/contracts.md:53` | строка §События с `DealShutdownInitiated` |
| `docs/architecture/contracts.md:64` | writer-таблица: два писателя, два момента |
| `docs/architecture/contracts.md:194-196` | «из двадцати одного… построен у **девяти**… Двенадцать прочих» |
| `docs/architecture/reserved-extension-points.md:93-95` | третья копия счёта: «девять из двадцати одного» |
| `.claude/work/roadmap/phase-2.md:89-91` | четвёртая, расходящаяся: «двадцать классов… построены **восемь**» |
| `docs/rules/audit-not-runtime-source.md:21-24, 38-44` | клейм о таймлайне на несуществующем классе |
| `docs/spec/event-actor-presence.json:20, 44-48, 177-187` | нота «двенадцать недостижимы»; член без `unreachable`; пример с `producerBuilt: true` |
| `libs/domain-model/.../CoreEventType.java` | пять значений; javadoc обещает «повторяет таблицу привязки» |

**Целевой док.** `AuditRecord.md` (13-я строка + счёт); `contracts.md:194-196`;
`reserved-extension-points.md:93-95`; `phase-2.md:89-91`;
`audit-not-runtime-source.md` (ограничение вместо клейма);
`event-actor-presence.json` (контрпример); позиция в `backlog.md`.

**Ожидаемый владелец.** `knowledge-curator` (счёт и перечни) +
`solution-designer` (заводить класс или парковать по образцу `HoldReleased`).

**Трассировка.** `AuditRecord.md` целиком → счёт 21/9 не сошёлся с `phase-2.md`
20/8 → обе стороны выведены механически (команда выше) → разность = один класс
→ открыты `contracts.md` (обе таблицы), `CoreEventType.java`,
`event-actor-presence.json`, `audit-not-runtime-source.md`,
`reserved-extension-points.md`, две секции `backlog.md`.

**Варианты.**

| Вариант | Что делает | Названная цена (правки / контекстная связность) |
|---|---|---|
| 1. Существующий носитель достаточен: класс назван таблицей, писатели есть | Ничего не менять | Не покрывает: обе стороны перечня класса не содержат, счёт ложен в трёх носителях, клейм таймлайна необеспечен. 0 правок / связность 0 |
| 2. 13-я строка непокрытых + счёт «8 из 21» в четырёх носителях + парковка по образцу `HoldReleased` | Перечень = фактике, ограничение получает дом/оживитель/причину | 4 дока + `unreachable` и правка примера спеки + секция бэклога / правящий обязан знать все четыре копии счёта |
| 3. Завести класс сейчас: значение, содержимое, два писателя ядра | Снимает и перечень, и клейм таймлайна | Правка чужого сервиса и общего артефакта — тот же довод несводимости, что у производителя `auth`; счётной ошибки исхода у варианта 2 не предъявлено / связность высокая |
| 4. Снять счёт из носителей-деталей: `contracts.md` и `reserved-extension-points.md` ссылаются на дом | Одна копия счёта вместо четырёх | 2 дока; цена — абзац о биллинге теряет самонесущесть / читатель идёт в дом |
| 5. Крен: 2 + 4 | Перечень и ограничение чинятся в доме, копии счёта — в ссылку | Сумма 2 и 4; 3 отвергнут по минимальному следу |

**Крен** — 5. Ось структурной свободы выписана: вариант 3 переоткрывает границу
шага «чужих производителей не строит» и помечен этим.

**Ярлык исхода.** `варианты-с-креном`.
**Ярлык дефицита.** Нет — закрыто прогоном.
**Гейтит.** Да. Класс — `НОСИТЕЛЬ`. Компонент — `audit-statistics: журнал,
потребитель событий`.

---

## A2. Содержимое построенных классов ядра не несёт ни одного операнда сделочного агрегата, а спека объявлена над операндом, которого у потребителя нет

**Вопрос.** `statistics-aggregates.md` объявляет зерно «тенант × счёт ×
**определение стратегии** × сутки × валюта» и двенадцать слагаемых по
операндам `tookRisk`, `priceResult`, `grossResult`, `fee`, `funding`,
`plannedRisk`. Агрегат — проекция журнала, чужой базы не читает: «строки на
чтение базы ядра у аудита нет» (`contracts.md:133`). Ни один операнд не едет в
построенном содержимом.

```
LC_ALL=C.UTF-8 grep -c "strategyInternalId\|tookRisk\|riskBearing\|priceResult\|plannedRisk\|fee\|funding\|closeOutcome\|reconciliationStatus\|breakdownIncomplete\|riskBenchmarkAvailability" \
  libs/domain-model/src/main/java/com/example/tradingbot/domain/event/DealClosedContent.java
LC_ALL=C.UTF-8 grep -c "strategyInternalId\|marketPhase\|phase" \
  libs/domain-model/src/main/java/com/example/tradingbot/domain/event/DealOpenedContent.java
LC_ALL=C.UTF-8 grep -c "tranche" \
  libs/domain-model/src/main/java/com/example/tradingbot/domain/event/OrderDecidedContent.java
```

Все три — `0`. Фактический состав: `DealClosedContent(dealInternalId,
exchangeAccountInternalId, instrumentInternalId, status, closeReason, result,
resultCurrency)`; `DealOpenedContent(…, entryReason, direction)`;
`OrderDecidedContent(…, orderType, direction, plannedSizeContracts,
plannedEntryPrice)`.

**Второй слой — в самой спеке.** Операнды
`docs/spec/statistics-aggregates.json:6-16` определены через доменные поля
ядра (`Deal.resultProfit`, `Deal.plannedRiskAmount`,
`Instrument.externalSettlementCurrency`), а коллекция `deals` — «закрытые
сделки суток». Тропы к ним у вычислителя нет: строки §«Синхронные вызовы» от
`audit-statistics` наружу не существует (`contracts.md:21-31`), а
`data-ownership.md:12` запрещает кросс-базовые запросы. Рядом при этом объявлен
счётный критерий: бремя на стороне, предлагающей дочитывание, — она обязана
назвать строку (`contracts.md:112-115`).

**Требование объявлено, носителя нет, хода не назначено.**
`contracts.md:116-135` требует от `DealClosed` девять величин, Д1016
(`decision-digest.md:3791`) фиксирует решение; позиции, называющей ход, нет:
`LC_ALL=C.UTF-8 grep -n "DealClosedContent\|DealOpenedContent" .claude/work/backlog.md`
— ноль. Критерий глубины требует носителя **существующего**, а не обещанного.

**Цитаты (носители).** `docs/rules/statistics-aggregates.md:20-42`, `:51-62`;
`docs/spec/statistics-aggregates.json:6-16`;
`docs/architecture/contracts.md:112-115`, `:116-124`, `:125-135`, `:136-145`,
`:153-159`, `:21-31`; `docs/models/domain/aggregate/Deal.md:47-50, 57` (четыре
признака отбора существуют durable-полями); `libs/domain-model/.../DealClosedContent.java`,
`DealOpenedContent.java`, `OrderDecidedContent.java`;
`services/trading-core/.../MarkDealClosedExecutor.java:198-205`,
`MarkDealEmergencyClosedExecutor.java:169-176`,
`DealOpeningService.java:168-172`, `CreateOrderExecutor.java:107-114`;
`.claude/work/backlog.md` (позиции нет).

**Целевой док.** `docs/architecture/contracts.md` (ход назван либо требование
переведено в названное ограничение); `docs/spec/statistics-aggregates.json`
(операнды через поля **содержимого**, а не через доменные поля ядра); новая
позиция `.claude/work/backlog.md` с адресатом и условием возврата.

**Ожидаемый владелец.** `solution-designer` (состав содержимого и граница шага)
+ `code-writer` (дельта `CODE`).

**Трассировка.** `statistics-aggregates.md` → спека → операнды указывают на
`Deal`/`Instrument` → проверена тропа чтения (§«Синхронные вызовы»,
`data-ownership.md:12`) → нет → проверено, едут ли операнды содержимым (три
record'а, четыре call-site'а) → нет → проверено, назван ли ход (`backlog.md`,
`code-gate-ledger.json`) → нет.

**Варианты.**

| Вариант | Что делает | Названная цена (правки / контекстная связность) |
|---|---|---|
| 1. Существующего носителя достаточно: считать по `status`, `closeReason`, `result`, `resultCurrency` | Ничего не менять | **Ошибка исхода счётна**: зерно теряет ключ «определение стратегии»; `riskBearingDeals`, `plannedRiskSum`, `feeSum`, `fundingSum`, `priceResultSum` операнда не имеют; `winningDeals` считался бы по итогу — прямо запрещено домом (`statistics-aggregates.md:71-83`). 0 правок / связность 0 |
| 2. Расширить содержимое: `DealClosed` +9 полей, `DealOpened` +2, `OrderDecided` +1 | Даёт операнды на проводе до накопления строк | 3 record'а + 4 call-site'а + версия формы; правка чужого сервиса внутри шага 10 / связность низкая — писатели держат величины в контексте |
| 3. Строка §«Синхронные вызовы» `audit-statistics → trading-core` и дочитывание | Содержимое не трогает | Дом сам называет асимметрию не в пользу: непоехавшая величина **невосстановима задним числом**; плюс пара сетевой политики и второй читатель того, что меняется каждой транзакцией ядра / связность высокая |
| 4. Сузить агрегат до того, что журнал несёт | Правило = носителю | **Цена — продуктовое решение владельца продукта**: режет функциональность шага; потери необратимы |
| 5. Переструктурировать: агрегат считает владелец сделки, аудит остаётся журналом | Снимает и границу баз, и дефицит операндов | Переоткрывает ратифицированное («агрегаты — проекция журнала», инвентарь `services.md`); второй писатель чисел отчёта / связность высокая. Выписан по оси структурной свободы, не выбран |
| 6. Крен: 2 + переопределение операндов спеки + названный ход (позиция бэклога, если шаг его не несёт сам) | Носитель существует, спека на достижимых операндах, граница шага названа | Сумма 2 и правки спеки; счётная разница против 1 предъявлена |

**Крен** — 6.
**Ярлык исхода.** `варианты-с-креном`.
**Ярлык дефицита.** Нет — продуктовой половины у крена нет (вариант 4 не
выбран).
**Гейтит.** Да. Класс — `КОД` (величина без операнда). Компонент —
`audit-statistics: джоба пересчёта агрегатов`, `libs/domain-model: содержимое
классов ядра`.

---

## A3. Актор не едет содержимым ни одного построенного класса, при том что три носителя и спека утверждают, что едет у трёх

**Вопрос.** Корпус трижды утверждает как факт о сегодняшнем дне, что у трёх
построенных классов жизненного цикла определения актор едет полем содержимого.

```
LC_ALL=C.UTF-8 grep -ric "actor\|createdBy\|principal" \
  libs/domain-model/src/main/java/com/example/tradingbot/domain/event/
```

— сумма по всем файлам `0`. `StrategyActivatedContent` — один компонент
(`definition`), `StrategyLifecycleContent` — три идентичности.

Величина **невосстановима задним числом** по критерию того же дома
(`contracts.md:112-115`): журнал накопит строки без актора, и «кто вернул
определение в работу» за период не восстановится ничем — при том что самое
дорогое ручное действие корпус называет прямо (`Auditable.md:39-44`).

**Цитаты (носители).** `docs/architecture/contracts.md:161-169`;
`docs/models/domain/other/Auditable.md:96-110`;
`docs/architecture/tenant-and-exchange.md:179-183`;
`docs/spec/event-actor-presence.json:222-253` (три примера
`producerBuilt: true` / `actorTravelsInContent: true`);
`libs/domain-model/.../StrategyActivatedContent.java`,
`StrategyLifecycleContent.java`;
`services/strategies/.../StrategyStatusWriter.java:65-67`;
`.claude/work/backlog.md` §«Класс события `HoldReleased` и его ручная тропа»
(поле актора запланировано только у **непостроенного** класса).

**Целевой док.** `libs/domain-model` (дельта `CODE`) либо — при отсрочке —
названное ограничение с домом/оживителем/причиной в `AuditRecord.md` плюс
правка трёх клеймов и примеров спеки.

**Ожидаемый владелец.** `solution-designer` (состав содержимого) +
`code-writer` (поле и писатель).

**Трассировка.** `Auditable.md` целиком → §«Где актор живёт в событии — в
содержимом, а не в конверте» → греп по пакету событий → ноль → открыты
`event-actor-presence.json`, `tenant-and-exchange.md`, `contracts.md`, писатель
`strategies`.

**Общий ход с A2 зафиксирован явно:** обе закрываются одним классом изменения —
расширением содержимого построенного класса чужого сервиса; развилка о самом
ходе выписана в A2. Здесь вторая её сторона: владелец правки — `strategies`, и
различие несущее для оценки «сводима ли дельта шага к его предмету».

**Варианты.**

| Вариант | Что делает | Названная цена |
|---|---|---|
| 1. Существующий носитель: `AuditRecord` пишет один актор — потребителя событий (`:88-89`) | Ничего не менять | Не покрывает: актор строки одинаков у всех строк; инициатор активации не выражен ничем / 0 правок |
| 2. Поле актора в двух record'ах `strategies`, значение из предъявленного токена | Клейм трёх носителей становится верным | Общий артефакт + писатель чужого сервиса + версия формы / связность низкая: токен у писателя есть |
| 3. Перевести три клейма в названное ограничение | Снимает ложную запись без кода | **Отложенная цена названа**: период до хода остаётся без актора навсегда — класс, для которого дом кладёт бремя на откладывающего / 4 дока + спека |
| 4. Полем конверта | Один носитель на все классы | Отвергнуто домом дважды (`Auditable.md:96-100`, `contracts.md:161-169`): расширяет полный состав и версионирует все события |
| 5. Крен: 2, запасной — 3 с полным набором полей парковки | Либо величина есть, либо её отсутствие названо | — |

**Крен** — 2.
**Ярлык исхода.** `варианты-с-креном`.
**Ярлык дефицита.** Нет.
**Гейтит.** Да. Класс — `КОД`. Компонент — `libs/domain-model: содержимое
классов `strategies``.

---

## A4. Пустое значение уезжает в содержимом строкой `"null"` — ровно у двух примеров, которыми дом иллюстрирует запрет

**Вопрос.** `contracts.md:171-178` запрещает сериализовать отсутствующую
величину строкой и называет два примера: «недоступный результат, цена рыночной
заявки». Оба писателя делают именно это:

```
LC_ALL=C.UTF-8 grep -rn "String.valueOf(deal.getResultProfit())\|String.valueOf(deal.getCloseReason())\|String.valueOf(order.getPrice())" \
  services/trading-core/src/main/java --include=*.java
```

— пять попаданий: `CreateOrderExecutor.java:113`,
`MarkDealClosedExecutor.java:202-203`,
`MarkDealEmergencyClosedExecutor.java:173-174`. `String.valueOf(null)` даёт
строку `"null"`.

**Следствие для предмета шага счётное.** Операнды спеки `priceResult` /
`grossResult` объявлены «Пусто — результат недоступен»
(`statistics-aggregates.json:7-8`), а `resultUnavailableDeals` заведена затем,
чтобы недоступность не выпадала молча (`statistics-aggregates.md:64-69`). При
строке `"null"` предикат `isNull(priceResult)` ложен всегда,
`countsAsUnavailable` тождественно ложно, счётчик тождественно ноль, а
недоступные сделки входят в суммы под видом значения — умолчание в
благоприятную сторону (П1 следствие 4) с ошибкой в разрешающую сторону.

**Клейм об энфорсере при этом дан:** «форма каждого **построенного** класса
проверяется прогоном» (`contracts.md:190-193`). Прогона, проверяющего эту
форму, в корпусе не названо.

**Цитаты (носители).** `docs/architecture/contracts.md:171-178`, `:190-193`;
`docs/spec/statistics-aggregates.json:7-8`;
`docs/rules/statistics-aggregates.md:64-69`;
`docs/rules/absent-value-semantics.md` (дом двух природ пустоты); три файла
писателей по строкам выше.

**Целевой док.** Дельта `CODE` у писателей ядра + позиция `backlog.md`; клейм
`contracts.md:190-193` приводится к фактике.

**Ожидаемый владелец.** `code-writer` (форма писателя) + `knowledge-curator`
(клейм о прогоне).

**Трассировка.** `contracts.md` §«Пустое значение едет пустым, а не текстом» →
проверены все четыре писателя событий ядра → пять `String.valueOf` на
nullable-операндах → сверено со спекой агрегатов → следствие счётно.

**Варианты.**

| Вариант | Что делает | Названная цена |
|---|---|---|
| 1. Существующего носителя достаточно: правило записано и адресовано писателю | Ничего не менять | Не покрывает: правило без энфорсера — «заявленный потолок» (П1 следствие 2); дефект уже в коде / 0 правок |
| 2. Снять `String.valueOf` у nullable-операндов, типизировать компоненты | Пустота едет пустотой | 5 call-site'ов + 3 record'а; версия формы не меняется при строковом типе / связность низкая |
| 3. Строка остаётся, недоступность — отдельным булевым полем | Не трогает типы | Второй носитель одной истины; `"null"` остаётся легальной строкой домена |
| 4. Энфорсер: проба сериализации содержимого в тесте писателя | Делает клейм «проверяется прогоном» истинным | Тест на сервис; **обязателен при 2 и при 3**, иначе класс возвращается / связность 0 |
| 5. Крен: 2 + 4 | Дефект снят и охраняется | Сумма |

**Крен** — 5.
**Ярлык исхода.** `варианты-с-креном`.
**Ярлык дефицита.** Нет.
**Гейтит.** Да. Класс — `КОД` (состояние, ведущее в неверный исход). Компонент
— `libs/domain-model: содержимое классов ядра`, `audit-statistics: джоба
пересчёта агрегатов`.

---

## A5. «Остановка приёма» объявлена исходом, а механизма не имеет; перечень ветвей разрыва закрыт и третьей ветви не содержит

**Вопрос.** `AuditRecord.md:203-212` объявляет единственную ветвь без строки —
неполный конверт — и её исход: «обработка останавливается, и отставание группы
потребителя становится наблюдаемым метрикой и алертом». Механизм остановки не
назван нигде: что останавливается (слушатель, группа, процесс), что делается со
смещением, чем остановка отличается от исключения, поглощённого обработчиком
ошибок.

Различение несущее, а не реализационное: построенный сосед на той же
технологии при том же входе **пропускает** сообщение и продолжает
(`services/trading-core/.../StrategyDefinitionConsumer.java:63-67` — «Strategy
fact without envelope headers is skipped»), то есть умолчание конструкции даёт
ровно тот молчаливый пропуск, который дом объявляет запрещённым
(`AuditRecord.md:196-202`).

**Отсюда третья ветвь разрыва, которой перечень не содержит.** Ветви объявлены
исчерпывающе — «Ветвей разрыва ровно две, обе достижимы и независимы»
(`:245-253`), — и `tools/derive/journal-continuity-branches.sh:45-50`
отказывает, если их не ровно две. Ветвь «смещение продвинулось, строки нет»
(поглощение отказа после исчерпания попыток; фиксация смещения без записи) не
наблюдаема ни одним из двух объявленных операндов: лаг возвращается к нулю,
возраст последнего принятого падает, `journalLowerBound` не двигается,
`continuityClaimable` остаётся истинным. Дыра бесшумна — класс, против которого
раздел и заведён.

Дисциплина фиксации смещения в `docs/**` не описана нигде:
`LC_ALL=C.UTF-8 grep -rn "смещени" docs/` — четыре попадания, ни одно не о
порядке «записать строку → зафиксировать смещение».

**Цитаты (носители).** `docs/models/domain/other/AuditRecord.md:189-212`,
`:245-253`; `docs/spec/audit-journal.json:8-9, 22-26`;
`tools/derive/journal-continuity-branches.sh:45-50`;
`docs/architecture/data-ownership.md:141-149` (отметка «в той же транзакции,
что и следствие» — про базу, не про смещение);
`services/trading-core/.../StrategyDefinitionConsumer.java:63-67`.

**Целевой док.** `AuditRecord.md` (механизм остановки + третья ветвь либо
предъявленная её недостижимость); `docs/spec/audit-journal.json` (популяция);
`tools/derive/journal-continuity-branches.sh`; возможно `data-ownership.md`
(порядок фиксации смещения как сквозное свойство потребителей).

**Ожидаемый владелец.** `solution-designer` (механизм и перечень) +
`code-writer` (форма остановки).

**Трассировка.** `AuditRecord.md` §«Когда строка не пишется» → «обработка
останавливается» без подлежащего-механизма → проверено поведение построенного
потребителя → пропускает → проверено, покрыт ли исход «пропустили и продвинули
смещение» перечнем ветвей → нет → проверена дисциплина смещения в корпусе → нет.

**Варианты.**

| Вариант | Что делает | Названная цена |
|---|---|---|
| 1. Существующего носителя достаточно: «останавливается» + алерт | Ничего не менять | Не покрывает: умолчание даёт пропуск, а пропуск невидим обоим операндам / 0 правок |
| 2. Назвать механизм остановки (обработчик без пропуска, смещение не двигается), оставить две ветви | Минимальный след; третья ветвь недостижима **по построению** и предъявляется контрпримером | Пассаж + `unreachable`-пример / связность 0. Требует, чтобы недостижимость была обеспечена, а не заявлена |
| 3. Третья ветвь `RECORD_SKIPPED` с собственным операндом (счётчик пропущенных) | Наблюдаемость не зависит от того, удержится ли механизм | Счётчик + правка дома, спеки и команды вывода (она жёстко требует двух ветвей) / связность средняя |
| 4. Тема мёртвых писем | Классический исход для неразбираемого | Отвергнуто домом с доводом (`AuditRecord.md:213-219`); переоткрывать нечем |
| 5. Дисциплина смещения сквозным правилом (дом — `data-ownership.md`) | Закрывает класс у обеих построенных групп и будущих | Больший след; **счётная разница предъявлена**: групп уже две, и обе о дисциплине молчат / связность падает |
| 6. Крен: 2 + 5 | Механизм назван, недостижимость третьей ветви обеспечена правилом | Сумма |

**Крен** — 6.
**Ярлык исхода.** `варианты-с-креном`.
**Ярлык дефицита.** Нет.
**Гейтит.** Да. Класс — `КОД` (ветвь без писателя; исход, ведущий к молчаливой
потере). Компонент — `audit-statistics: потребитель событий`.

---

## A6. Пересчёт проекции пересекает границу двух баз, и ни механизма, ни разрешения на это в корпусе нет

**Вопрос.** Агрегаты — «персистентная проекция журнала», пересчитываемая джобой
модуля статистики «из журнала целиком»
(`statistics-aggregates.md:9-12, 157-163`). Журнал в базе `audit`, агрегаты в
`statistics` (`data-ownership.md:37-38`, `statistics-aggregates.md:255-256`).
Тот же файл объявляет: «Кросс-базовых запросов нет» (`data-ownership.md:12`).

Как джоба читает чужую базу, не сказано ни одним носителем:
`LC_ALL=C.UTF-8 grep -rn "двух баз\|обеих баз\|datasource" docs/` даёт только
констатацию, что баз две. Варианты различаются следствиями для П4 и для объёма
выборки, а выборка заведомо большая (окно суток по всем тенантам,
`AuditRecord.md:281-286`), то есть попадает под правило «только окном либо
пагинацией».

**Заготовленный шов не срабатывает.** `data-ownership.md:26-29` объявляет
разведение баз «заготовленным швом разъезда на два процесса»,
`services.md:80-88` называет условие разъезда. После разъезда чтение журнала
статистикой становится сетевым вызовом, для которого строки §«Синхронные
вызовы» нет, — а «пара, которой здесь нет, в кластере запрещена»
(`contracts.md:44-46`). Клейм о шве дан, носителя у него нет.

**Цитаты (носители).** `docs/architecture/data-ownership.md:12`, `:17-29`,
`:31-38`; `docs/rules/statistics-aggregates.md:9-12`, `:155-173`, `:253-261`;
`docs/architecture/services.md:80-88`; `docs/architecture/contracts.md:21-31`,
`:44-46`; `.claude/rules/codestyle.md` (правило выборок окном).

**Целевой док.** `data-ownership.md` (граница «кросс-базовых запросов нет» для
потребителя с двумя базами); `statistics-aggregates.md` (как джоба читает
журнал и каким окном); `contracts.md` (строка §«Синхронные вызовы», если шов
обязан быть проходимым).

**Ожидаемый владелец.** `solution-designer`.

**Трассировка.** `statistics-aggregates.md` §«Пересчёт — проекция, а не
накопитель» → писатель и вход названы → раскладка баз → журнал в другой базе →
запрет кросс-базовых запросов → таблица синхронных вызовов → строки нет → клейм
о шве разъезда.

**Варианты.**

| Вариант | Что делает | Названная цена |
|---|---|---|
| 1. Существующего носителя достаточно: один процесс, два источника, агрегация в памяти | Ничего не менять | 0 правок; **цена названа**: агрегация суток по всем тенантам в памяти, и правило больших выборок этого не покрывает; шов разъезда остаётся непроходимым |
| 2. Назвать механизм (второй источник, только чтение, окно суток, курсор) в доме агрегатов | Делает исполнимым уже решённое | Пассаж + уточнение границы в `data-ownership.md` / связность низкая |
| 3. Свести обе таблицы в одну базу | Снимает границу | Переоткрывает «по базе на владельца данных» и снимает объявленный шов / связность 0, но потеря объявленного свойства |
| 4. Строка §«Синхронные вызовы» `audit-statistics → audit-statistics` заранее | Делает шов проходимым до разъезда | Строка = пара сетевой политики; вызова сегодня нет — строка опережает предмет, что корпус запрещает у тем и классов |
| 5. Переструктурировать: статистика — второй потребитель того же потока | Убирает границу баз | Отвергнуто домом («у события ровно одно следствие» — `services.md:76-79`, `data-ownership.md:150-158`); выписан с пометкой «переоткрывает принятое» |
| 6. Крен: 2 + названное ограничение о шве (условие возврата — момент разъезда, дом — бэклог) | Механизм назван, несрабатывание шва перестаёт быть скрытым | Сумма 2 + позиция бэклога |

**Крен** — 6.
**Ярлык исхода.** `варианты-с-креном`.
**Ярлык дефицита.** Нет.
**Гейтит.** Да. Класс — `КОД` (тропа чтения без механизма и без разрешения).
Компонент — `audit-statistics: джоба пересчёта агрегатов`.

---

## A7. Группа журнала объявлена читающей одну тему, а несёт классы двух производителей; перечень durable-групп неполон

**Вопрос.** Журнал несёт три класса жизненного цикла определения
(`AuditRecord.md:104-119` — их нет в таблице непокрытых); публикуются они в
тему `strategies.facts`
(`services/trading-core/src/main/resources/application.yaml:81-83`,
`services/bff/src/main/resources/application.yaml:53`). При этом:

- `AuditRecord.md:222-231` говорит о **теме** в единственном числе: «Группа
  журнала читает тему с самой ранней доступной позиции»;
- `data-ownership.md:199-203` объявляет закрытый перечень, по которому
  **обязано** быть написано правило алерта: «Правило пишется по КАЖДОЙ
  durable-группе… Групп сегодня две — ядро на теме `strategies` и журнал
  аудита **на теме ядра**».

Подписки журнала на `strategies.facts` не называет ни один носитель. Следствие:
отставание журнала на теме определений правилом алерта не покрыто, а обещание
алерта, данное у темы активаций (`docs/lifecycles/Strategy.md`), на ней не
исполняется. Дополнительно не определено, чем считается `unconsumedRecords`
при двух темах: при сумме молчание одной темы маскирует остановку на другой —
тем самым механизмом, против которого второй конъюнкт и введён
(`audit-journal.json:34`).

**Цитаты (носители).** `docs/models/domain/other/AuditRecord.md:104-119`,
`:222-231`; `docs/architecture/data-ownership.md:182-193`, `:199-203`;
`docs/spec/audit-journal.json:12, 33-36`;
`docs/architecture/contracts.md:476-480`; конфигурации тем построенных
сервисов.

**Целевой док.** `data-ownership.md` (перечень durable-групп и предмет операнда
при нескольких темах); `AuditRecord.md` (сколько тем читает группа и как это
согласуется с позицией `earliest`).

**Ожидаемый владелец.** `solution-designer`.

**Трассировка.** `AuditRecord.md` §«Позиция чтения и с какого момента журнал
полон» → «читает тему» → сверено с перечнем несомых классов (два производителя)
→ `data-ownership.md` §«Outbox и доставка» → перечень называет журналу не ту
тему → проверены имена тем в конфигурациях.

**Варианты.**

| Вариант | Что делает | Названная цена |
|---|---|---|
| 1. Существующего носителя достаточно: читать «группу» как группу, а не пару | Ничего не менять | Не покрывает: перечень называет тему у каждой группы, и у журнала названа не та; правило остаётся невыполненным на второй теме / 0 правок |
| 2. Перечень парами «группа × тема» (три пары); операнд алерта — на тему | Правило покрывает всю популяцию | Пассаж + уточнение операнда спеки / связность низкая |
| 3. Две группы у журнала (по теме на группу) | Разводит лаг и позицию по производителям | Второй набор конфигурации и алертов; **счётной разницы против 2 не предъявлено** |
| 4. Одна тема на всё | Снимает множественность | Отвергнуто домом: тема одна на производителя и род (`contracts.md:268-279`) |
| 5. Крен: 2 | Минимальный след, популяция полна | — |

**Крен** — 2.
**Ярлык исхода.** `варианты-с-креном`.
**Ярлык дефицита.** Нет.
**Гейтит.** Да. Класс — `КОД` (популяция охраны объявлена у́же фактической).
Компонент — `audit-statistics: потребитель событий`.

---

## A8. Второе зерно статистики структуры не имеет: из четырёх счётчиков назван один, колонок и типов нет ни у одной таблицы

**Вопрос.** Шаг пишет миграции обеих таблиц статистики, то есть по
функциональному порогу доки обязаны задать колонки, типы, nullability и
ограничения. Заданы только ключи зерна и словесное «двенадцать хранимых
слагаемых» / «счётчики происшествий суток»
(`statistics-aggregates.md:255-272`).

- У **сделочного** зерна двенадцать величин названы (`:51-62`) и объявлены в
  спеке, но типов, точности и nullability нет; журнальная строка их имеет
  полностью (`AuditRecord.md:150-166`) — эталон в том же охвате.
- У **зерна происшествий** словами названы четыре счётчика («заведённых сделок,
  решений о заявке, поднятых ступеней, отчётов о происшествиях» — `:113-118`),
  а в спеке объявлена **одна** величина `raisedHolds` с нотой «форма, общая для
  всех счётчиков этого семейства». Имён у трёх остальных нет нигде.

Семейство — закрытый перечень (отбор строк журнала по классу события), а
концепция требует для закрытого перечня исполнимой формы одним экземпляром.
Популяции по оси «класс события → счётчик» в спеке нет: единственная популяция
— восемь классов закрытой сделки.

**Цитаты (носители).** `docs/rules/statistics-aggregates.md:113-131`,
`:253-276`; `docs/spec/statistics-aggregates.json` (величина `raisedHolds`;
популяция «классы закрытой сделки»);
`docs/models/domain/other/AuditRecord.md:150-166`; `docs/concept.md`
§«Исполнимая форма».

**Целевой док.** `statistics-aggregates.md` §Персистентность (колонки обеих
таблиц с типами и nullability + четыре имени счётчиков);
`docs/spec/statistics-aggregates.json` (три недостающие величины и, при
варианте 3, популяция «счётчик × класс события»).

**Ожидаемый владелец.** `solution-designer` (состав) + `code-writer` (типы по
`docs/rules/persistence-representation.md`).

**Трассировка.** `statistics-aggregates.md` §Персистентность → две строки без
колонок → сверено со спекой: у зерна происшествий одна величина → сверен счёт
«двенадцать» у сделочного зерна (сходится) → сверено с `AuditRecord.md`
§Персистентность как эталоном полноты.

**Варианты.**

| Вариант | Что делает | Названная цена |
|---|---|---|
| 1. Существующего носителя достаточно: «форма общая для семейства» | Ничего не менять | Не покрывает миграцию: имён колонок нет, код их изобретёт; закрытый перечень проверяется вниманием / 0 правок |
| 2. Назвать три величины в спеке и колонки обеих таблиц с типами | Миграция пишется по доку | 1 док + 1 спека по образцу существующей величины / связность 0 |
| 3. Плюс популяция «класс события → есть ли счётчик», выводимая из перечней производителей | Новый класс обязан получить исход «счётчик есть / нет — почему» | Команда вывода + популяция; **счётная разница**: сегодня появление класса не обязывает никого решить, считается ли он |
| 4. Одна таблица с колонкой-дискриминатором зерна | Убирает вторую таблицу | Смешивает два зерна; пустое определение получает два смысла — прямо запрещено домом (`:119-128`) |
| 5. Крен: 2 + 3 | Структура задана и охраняется прогоном | Сумма |

**Крен** — 5.
**Ярлык исхода.** `варианты-с-креном`.
**Ярлык дефицита.** Нет.
**Гейтит.** Да. Класс — `КОД` (структура, попадающая в миграцию, не задана).
Компонент — `audit-statistics: схема статистики, миграция V1`.

---

## A9. Предикат непрерывности, который «читатель обязан увидеть», не имеет ни писателя операндов, ни тропы к читателю

**Вопрос.** `docs/spec/audit-journal.json:24` объявляет `continuityClaimable`
величиной, которую «читатель обязан увидеть рядом с числами». Поверхность
чтения отдаёт **только** нижнюю границу (`AuditRecord.md:281-296`); предиката в
перечне отдаваемого нет.

У обоих его операндов нет писателя:
`LC_ALL=C.UTF-8 grep -n "писател\|пишет" docs/models/domain/other/AuditRecord.md`
даёт семь попаданий, ни одно не о `receptionHalted` и
`lagExceededRetention`. Пара операндов **алерта** писателя имеет
(`data-ownership.md:182-185` — «Обе производит сам потребитель»), пара операндов
**предиката** — нет. Это прямое нарушение
`docs/rules/writer-named-for-every-value.md` и П3.

**Второй слой.** Оба операнда алерта производит сам потребитель. Когда
потребитель не работает как процесс (перезапуск, вытеснение, отказ старта), ни
одна величина не производится, и алерт молчит — молчание неотличимо от
здоровья. Корпус этот класс называет и применяет к тику живости
(`audit-journal.json:34` ссылается на него прямо), но к собственному
измерителю не применяет: условия алерта на **отсутствие** ряда нигде нет, а
`platform.md:183-192` перечисляет лишь «лаг потребителей».

**Цитаты (носители).** `docs/spec/audit-journal.json:8-12`, `:22-26`;
`docs/models/domain/other/AuditRecord.md:245-260`, `:281-296`;
`docs/architecture/data-ownership.md:182-193`;
`docs/architecture/platform.md:183-192`;
`docs/rules/writer-named-for-every-value.md`.

**Целевой док.** `AuditRecord.md` (писатель и момент каждого операнда; состав
отдаваемого выборкой); `audit-journal.json` (нота приводится к тому, что дом
обещает); `data-ownership.md` (условие алерта на отсутствующую метрику).

**Ожидаемый владелец.** `solution-designer`.

**Трассировка.** `audit-journal.json` целиком → нота `continuityClaimable`
обещает предикат читателю → §«Как журнал читается» → отдаётся только нижняя
граница → греп по писателям операндов → нет → проверено, чем закрывается отказ
самого потребителя → ничем.

**Варианты.**

| Вариант | Что делает | Названная цена |
|---|---|---|
| 1. Существующего носителя достаточно: непрерывность наблюдается алертом | Ничего не менять | Не покрывает клейм спеки; остаётся дыра «потребитель мёртв — операндов нет» / 0 правок |
| 2. Снять обещание из ноты (величина — операнд алерта, не поверхности чтения) и назвать писателя обоих операндов | Спека = тому, что дом обещает | 1 нота + пассаж с писателями / связность 0 |
| 3. Отдавать предикат вместе с нижней границей | Читатель получает обещанное | Оба операнда придётся сделать durable, иначе поверхность вернёт значение из памяти прохода / связность высокая |
| 4. Плюс условие алерта на **отсутствие** ряда метрики потребителя | Закрывает «мёртвый потребитель молчит» | Правило алерта в `platform.md`; **счётная разница**: сегодня отказ процесса не поднимает ничего / связность низкая |
| 5. Переструктурировать: непрерывность выводить из самого журнала | Не требует ни операндов, ни писателей | Невыводимо: пропущенная строка следов не оставляет — выписан по оси структурной свободы и снят по существу |
| 6. Крен: 2 + 4 | Клейм верен, дыра измерителя закрыта | Сумма |

**Крен** — 6; вариант 3 назван и не выбран: два новых durable-носителя ради
величины, у которой уже есть тропа наблюдения.

**Ярлык исхода.** `варианты-с-креном`.
**Ярлык дефицита.** Нет.
**Гейтит.** Да. Класс — `КОД` (величина без писателя; клейм спеки без тропы).
Компонент — `audit-statistics: потребитель событий`, `audit-statistics:
поверхность чтения`.

---

## A10. Колонка радиуса `strategy_internal_id` у `StrategyActivated` объявленным механизмом не заполняется

**Вопрос.** Дом объявляет механизм: «разбирать содержимое для этого не нужно:
`content` ложится как доставлено, а колонки радиусов заполняются best-effort —
чего в содержимом нет, остаётся пустым» (`AuditRecord.md:191-195`). Тут же —
исключение: «У `StrategyActivated` идентичность определения лежит внутри
снимка (`StrategyActivatedContent.definition`)… колонка заполняется из него»
(`:76-80`).

Механизм и исключение несовместимы: внутри снимка поле зовётся `internalId`, не
`strategyInternalId`.

```
LC_ALL=C.UTF-8 grep -n "private String internalId" \
  libs/domain-model/src/main/java/com/example/tradingbot/domain/model/aggregate/strategy/Strategy.java
```

— строка 39. Поиск по имени колонки даст пусто; чтобы колонку заполнить,
читатель обязан знать форму класса — то, что дом объявил ненужным. Заодно
ослабляется соседний клейм устойчивости («класс, которого читатель не знает…
строке не мешают»). Исход при нынешней записи: колонка пуста, а пустота у неё
объявлена значением со смыслом «класс события идентичности не производит»
(`:70-74`) — в данных окажется ложное значение, а не пробел.

**Цитаты (носители).** `docs/models/domain/other/AuditRecord.md:64-80`,
`:189-195`; `libs/domain-model/.../Strategy.java:39`;
`libs/domain-model/.../StrategyActivatedContent.java`;
`docs/rules/absent-value-semantics.md`.

**Целевой док.** `AuditRecord.md` (правило извлечения колонок радиусов: по
каким ключам и как выражено исключение).

**Ожидаемый владелец.** `solution-designer` (правило) + `code-writer` (форма).

**Трассировка.** §«Почему четыре колонки идентичности, а не все» → исключение
для `StrategyActivated` → сверено с §«Когда строка не пишется» → противоречие →
проверено имя поля внутри снимка в общем артефакте.

**Варианты.**

| Вариант | Что делает | Названная цена |
|---|---|---|
| 1. Существующего носителя достаточно: «заполняется из него» | Ничего не менять | Механизма нет, а объявленный даёт пусто; в данных ложное значение / 0 правок |
| 2. Назвать правило извлечения явно: перечень путей на класс | Механизм исполним | Пассаж + таблица «класс → путь»; **растёт связность**: новый класс обязан попасть в таблицу |
| 3. Извлекать по имени поля на любой глубине | Один механизм без таблицы | Для этой колонки не сработает — имени в снимке нет; глубинный поиск подберёт чужие совпадения |
| 4. Просить производителя положить `strategyInternalId` рядом со снимком | Механизм однороден для всех классов | Правка чужого сервиса — тот же ход, что в A2/A3 / связность у читателя падает до нуля |
| 5. Крен: 4, а до его исполнения — 2 | Однородный механизм; пока хода нет, исключение записано исполнимо | Сумма |

**Крен** — 5 (общий ход с A2/A3 зафиксирован).
**Ярлык исхода.** `варианты-с-креном`.
**Ярлык дефицита.** Нет.
**Гейтит.** Да. Класс — `КОД` (колонка получает значение, означающее не то, что
произошло). Компонент — `audit-statistics: потребитель событий`.

---

# Находки: прочее

## A11. Клейм «потребителей теперь три, включая `audit-statistics`» ложен — сервиса не существует

**Вопрос.** `contracts.md:190-193`: «теперь их три — ядро на трёх классах
определения, `bff` и `audit-statistics`, — и форма каждого **построенного**
класса проверяется прогоном». Единицы нет ни в `services/` (семь имён), ни в
`deploy/base/services/` (семь манифестов); прогона у неё быть не может.
Природа расхождения иная, чем у A1 (там неполный перечень, здесь клейм о
несуществующем), но чинится одним ходом с ним.

**Цитаты (носители).** `docs/architecture/contracts.md:190-193`;
`ls services/`; `ls deploy/base/services/`;
`python.exe tools/deploy-layout-check.py` (зелёный: детектор мерит только
обратное направление — единица в каталоге вне инвентаря).

**Целевой док.** `docs/architecture/contracts.md:190-193`.
**Ожидаемый владелец.** `knowledge-curator`.
**Трассировка.** Верификация атрибуции при разборе A1: клейм о построенности
проверен листингом каталогов и манифестов.

**Варианты.**

| Вариант | Что делает | Названная цена |
|---|---|---|
| 1. Оставить | — | Ложная запись живёт и после шага читается как факт / 0 правок |
| 2. «Потребителей два; третий приезжает шагом 10» | Запись верна на сегодня | Одна фраза / связность 0 |
| 3. Снять число, оставив условие («форма проверяется прогоном там, где потребитель построен») | Число не стареет | Одна фраза; **счётная разница**: число уже устарело один раз / связность 0 |

**Крен** — 3.
**Ярлык исхода.** `варианты-с-креном`.
**Ярлык дефицита.** Нет.
**Гейтит.** Нет.

---

## A12. Состав точек чтения не специфицирован, парковка без дома, а объявленный дом api-форм периметра предметом пуст

**Вопрос.** Формулировка шага включает «read-only API» (`phase-2.md:40`), а
владелец предмета отказывается его специфицировать: «Состав точек здесь не
специфицируется, и это решение… угадывать контракт до его потребителя —
фронта, шаг 12 — значит выбирать форму раньше предмета»
(`AuditRecord.md:322-327`). Причина и оживитель названы, **дома нет**: позиции
в `backlog.md` или примечания роадмапа не заведено; по
`.claude/rules/parking-address.md` отсутствие любой из трёх величин делает
позицию потерей.

Смежно: `contracts.md:485-492` называет домом форм периметра
`docs/models/api/`, а каталог содержит один файл — `OkxRawApiRequest.md`,
который карта владельцев прямо объявляет **не** предметом периметра. Api-формы
потока, построенные шагом 9, дома в корпусе не имеют — обязательность, живущая
только в коде.

**Цитаты (носители).** `.claude/work/roadmap/phase-2.md:40`;
`docs/models/domain/other/AuditRecord.md:281-296`, `:322-327`;
`docs/architecture/contracts.md:25`, `:485-492`; `ls docs/models/api/`;
`.claude/rules/knowledge-ownership-by-service.md` (строка `docs/models/api/`);
`.claude/rules/parking-address.md`.

**Целевой док.** `AuditRecord.md` (дом позиции) + новая позиция `backlog.md`;
отдельно `docs/models/api/` как хвост шага 9.

**Ожидаемый владелец.** `knowledge-curator` (дом парковки, разметка владельца)
+ `solution-designer` (что именно шаг 10 обязан отдать).

**Трассировка.** §«Как журнал читается» → ограничения отбора заданы, состав
точек нет → проверено, есть ли дом у отложенного (бэклог, роадмап) → нет →
проверено, где живут api-формы периметра → каталог пуст по предмету.

**Варианты.**

| Вариант | Что делает | Названная цена |
|---|---|---|
| 1. Существующего носителя достаточно: решение записано, причина и оживитель названы | Ничего не менять | Позиция без дома не возвращается: её видит только открывший `AuditRecord.md` / 0 правок |
| 2. Позиция бэклога с адресатом (шаг 12) и условием возврата | Парковка полна | Секция + строка-указатель / связность 0 |
| 3. Специфицировать состав точек сейчас | Шаг отдаёт объявленное формулировкой | Выбор формы раньше потребителя — довод дома против; но **цена отказа тоже названа**: строка §«Синхронные вызовы» и пара сетевой политики существуют без цели |
| 4. Зафиксировать сужение скоупа в роадмапе («контур и ограничения отбора; состав точек — шаг 12») | Формулировка перестаёт обещать больше, чем шаг несёт | Примечание к строке 10 / связность 0 |
| 5. Крен: 2 + 4 | Позиция имеет дом, формулировка — фактику | Сумма |

**Крен** — 5.
**Ярлык исхода.** `варианты-с-креном`.
**Ярлык дефицита.** Нет.
**Гейтит.** **Зависимая:** гейтит, если развилка разрешится вариантом 3 (шаг
обязан отдать состав точек в этом шаге). При 2/4/5 — не гейтит.

---

## A13. Пересказ оживителя у `AccessDenial` у́же своего дома: пятый владелец базы, заводимый этим шагом, в нём не назван

**Вопрос.** `AccessDenial.md:157-161`: «оживит её ближайший кодовый заход **по
каждому из четырёх владельцев базы**». Дом позиции —
`.claude/work/backlog.md` §«Таблица отказов доступа у сервисов со своей базой»
— несёт пятую сторону явно: «**Что шаг 10 несёт сам:** `audit-statistics`
заводит таблицу в базе `audit`». Читатель дока заключит, что ретрофит его
сервиса не касается, — при том что §Персистентность того же дока
`audit-statistics` называет. Класс — копия истины в доке деталей, разошедшаяся
с домом.

**Цитаты (носители).** `docs/models/domain/other/AccessDenial.md:149-161`;
`.claude/work/backlog.md` §«Таблица отказов доступа у сервисов со своей базой»;
`docs/rules/api-access-policy.md` §«След отказа пишет тот, у кого есть база».

**Целевой док.** `docs/models/domain/other/AccessDenial.md:157-161`.
**Ожидаемый владелец.** `knowledge-curator`.
**Трассировка.** `AccessDenial.md` §Персистентность → адрес таблицы у
`audit-statistics` назван → оживитель говорит о четырёх → дом в бэклоге
называет пятого и возлагает его на шаг 10.

**Варианты.**

| Вариант | Что делает | Названная цена |
|---|---|---|
| 1. Оставить | — | Шаг может не завести таблицу, и никто не заметит / 0 правок |
| 2. Сослаться на дом без пересказа числа | Число живёт в одном месте | Одна фраза / читатель идёт в бэклог |
| 3. Дописать пятого в пересказ | Пересказ верен сегодня | Копия остаётся и разойдётся снова / 1 правка |

**Крен** — 2.
**Ярлык исхода.** `варианты-с-креном`.
**Ярлык дефицита.** Нет.
**Гейтит.** Нет.

---

# Блокирующие открытые вопросы

Проход по `.claude/work/questions/open-questions.md` сделан целиком.
Блокирующих шаг 10 нет.

- **RISK-Q2** (`:356-394`) уже помечен **зависимым** для шага 10 и сегодня не
  гейтит: агрегат показывает оба числа, а выигрышность привязана к операнду
  энфорсера серии. Проверено независимо: `loss-streak-halt.md:35-48` ↔
  `statistics-aggregates.md:71-97` ↔ величина `countsAsWinning` согласованы по
  операнду (ценовой результат) и по трактовке нуля.
- Прочие (`ARCH-Q1`, `ORCH-Q1/Q2`, `OKX-Q2/Q4`, `STRAT-Q4`, `IND-Q1`,
  `STRUCT-Q1`, `PHASE-Q1/Q2`, `PNL-Q1`, `FEE-Q1`, `ANOM-Q6`, `CTX-Q1`,
  `RISK-Q1`, `PROC-Q2/Q3`) специфицировать шаг не мешают и сюда не дублируются.

# Вынос пользователю

**Пусто.** Ни одна находка не упирается в численную величину риск-аппетита или
в продуктовый scope: единственный вариант, режущий функциональность (A2,
вариант 4), в крен не взят и выписан с пометкой «цена — продуктовое решение
владельца продукта». Ярлык `подтверждение` не поставлен ни разу.

# Сводка

**Находок — 13.** Гейтящих — **10** (A1-A10), прочих — **3** (A11-A13), из них
A12 зависимая: гейтит при одном исходе собственной развилки.

| Узел | Находки |
|---|---|
| Перечень классов и счёт построенного | A1 |
| Состав содержимого построенных классов (операнды, актор, пустота) | A2, A3, A4 |
| Приём событий: остановка, ветви разрыва, темы, извлечение радиусов | A5, A7, A10 |
| Граница двух баз и пересчёт проекции | A6 |
| Структура таблиц статистики | A8 |
| Наблюдаемость непрерывности: писатели операндов и тропа к читателю | A9 |

**Общие ходы зафиксированы явно.** A2 + A3 + A10 (вариант 4) закрываются одним
классом изменения — расширением содержимого построенных классов чужих
сервисов; развилка о самом ходе выписана в A2. A1 + A11 чинятся правкой одного
пассажа `docs/architecture/contracts.md:190-196`.

**Рекомендация.** `GAPS_CLOSE_2` нужен; `DOCS_CHECK_3` после него — да: шесть
из десяти гейтящих находок правят носители, которые сами являются входом
следующего прогона (перечни, спеки, популяции).

**Превышение ориентира объёма названо.** Отчёт больше ориентира ~40 KB
(`.claude/templates/docs/gap-report.md` §«Потолок объёма»). Пересказ снят:
полные выводы грепов не вклеены (в отчёте команды и точки `файл:строки`),
повторных перечней носителей нет, вторых форм одной таблицы нет; первая
редакция была сокращена на треть именно этим. Остаток превышения составляют
**мандатные поля**: тринадцать находок, у каждой список цитат таблицей,
перебор вариантов с ценой в собственной строке по двум измерениям и полный
набор ярлыков — то, что правило прямо ставит выше ориентира.

## Сводка счётом

| Линза | Фокус | Позиций реестра |
|---|---|---|
| A | концепция | 10 |

Позиций 10 при 13 находках: класс получают только гейтящие
(`.claude/skills/classify-code-blocking.md` §«Когда применять»); составных
находок, требующих расщепления на несколько записей, среди них нет — у каждой
гейтящей ровно один класс. Третья колонка — число позиций реестра.

**Классы гейтящих позиций:** `КОД` — 9 (A2-A10); `НОСИТЕЛЬ` — 1 (A1).
