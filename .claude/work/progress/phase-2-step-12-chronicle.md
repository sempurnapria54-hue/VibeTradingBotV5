# Хроника шага 12 фазы 2 — тесты

## На какой вопрос отвечает этот файл

Что сделано на шаге 12 фазы 2.

## Статус

**`CODE·2/3` (с 2026-09-16).** Счёт в статусе — текущий под-шаг `CODE` из
общего их числа; форма и её дом —
`.claude/processes/roadmap-step-execution.md` §«Машина шага». Шаг — первый под новой машиной
`CONCEPT → CONCEPT_REVIEW → CODE → DOCS → REVIEW → DONE`
(`.claude/decisions/step-machine-rework.md`). Концепция дана держателем;
ревью её достаточности проведено и дало **апрув** —
`.claude/work/progress/phase-2-step-12-concept-review.md`. Вердикты гейтинга
всех открытых вопросов переставлены на шаг 12: гейтящих нет, один
(`RISK-Q2`) — зависимый.

**Материал держателя по концепции тестирования (2026-09-12) записан
2026-09-16** в §«Концепция» — разбор по уровням, границы и отсылка «как» к
дизайн-проходу шага.

**`CODE` тестового шага — три под-шага, всегда в этом порядке**
(`.claude/processes/roadmap-step-execution.md` §«Конец фазы»): тест-кейсы →
адверсариальное ревью кейсов → код тестов. Под-шаг 3 не начинается, пока
под-шаг 2 не дал апрув по кейсам.

**Апрув идёт по предметам, и критерий у него один — валидность кейса**
(решение держателя 2026-09-17,
`.claude/decisions/case-review-approval-criterion.md`; дом формулировки —
`.claude/skills/test-review.md`). Апрув получили: `auth`
(`.claude/work/progress/phase-2-step-12-approve-test-auth.md`),
`connector-okx`
(`.claude/work/progress/phase-2-step-12-approve-test-connector-okx.md`),
`market-data`
(`.claude/work/progress/phase-2-step-12-approve-test-market-data.md`),
`trading-core`
(`.claude/work/progress/phase-2-step-12-approve-test-trading-core.md`),
`strategies`
(`.claude/work/progress/phase-2-step-12-approve-test-strategies.md`),
`audit`
(`.claude/work/progress/phase-2-step-12-approve-test-audit.md`),
`statistics`
(`.claude/work/progress/phase-2-step-12-approve-test-statistics.md`),
`bff`
(`.claude/work/progress/phase-2-step-12-approve-test-bff.md`).
Перечень предметов и их порядок — у дизайн-прохода
(`.claude/decisions/test-contour-design-pass.md`), здесь не пересказывается.

**Уровень 1 перечня закрыт целиком — восемь предметов из восьми апрувлены**
(заход 35). **Развилка «расширять ли перечень уровня 2» закрыта заходом 36**:
признак получил вторую ступень, перечень расширен с восьми предметов до
четырнадцати; действующая формулировка живёт в доме
(`.claude/decisions/test-contour-design-pass.md` §«Признак предмета уровня 2 —
две ступени, и вторая обязательна») и здесь не пересказывается.

**Следующие единицы под-шага 2 — ревью тринадцати уже написанных документов**:
восемь уровня 2 и пять уровня 3, по документу на единицу, тем же критерием
валидности и той же формой захода. **После них** пишутся шесть новых
документов уровня 2 — под-шаг 1 по ним открыт заново — и каждый проходит своё
ревью; порядок шестёрки назван в доме перечня.

**Под-шаг 1 `CODE` закрыт: документы кейсов всех трёх уровней написаны, и последний предмет перечня — ревизия существующего набора — тоже** (`.claude/work/progress/phase-2-step-12-existing-tests-revision.md`). Шаг входит в **под-шаг 2** — адверсариальное ревью кейсов (`.claude/skills/test-review.md`). Первым заходом под-шага был **дизайн-проход контура
тестов**, закрытый 2026-09-16: «как» названо решением
`.claude/decisions/test-contour-design-pass.md` (форма ящика, субстрат,
стабы, время, ассерт по базе, дом сквозного набора, включение в реактор,
метки кейсов) и там же — **перечень предметов и порядок их прохождения**.
Здесь он не пересказывается (`.claude/rules/policy-home.md`).

**Первый документ кейсов написан** — чёрный ящик `auth`,
`.claude/tests/cases/auth.md` (форма — `.claude/skills/test-design.md`).
Предмет был выбран первым потому, что он малый и задаёт форму документа
дешевле остальных; каждый следующий предмет перечня добавляет ровно одну
новую ось формы. Состав кейсов здесь не пересказывается — дом у него сам
документ; развилки его письма — дайджест Д1595-Д1599.

**Второй документ кейсов написан** — чёрный ящик `connector-okx`,
`.claude/tests/cases/connector-okx.md`. Новая ось формы, ради которой предмет
стоя́л вторым, введена и проверена: **две мишени** с разной достижимостью
(`-S` — стаб с записанными ответами, `-D` — живая demo-площадка), секреты и
подпись. Состав кейсов здесь не пересказывается — дом у него сам документ;
развилки его письма — дайджест Д1600-Д1605.

**Третий документ кейсов написан** — чёрный ящик `market-data`,
`.claude/tests/cases/market-data.md`. Новая ось формы, ради которой предмет
стоя́л третьим, введена и проверена: **тик как вход** — пять проходов по
расписанию, у каждого свой ручной фасад, возраст данных ставится в данных, а
часы процесса не двигаются. Состав кейсов здесь не пересказывается — дом у него
сам документ; развилки его письма — дайджест Д1606-Д1611.

**Четвёртый документ кейсов написан** — чёрный ящик `trading-core`,
`.claude/tests/cases/trading-core.md`. Новая ось формы, ради которой предмет
стоя́л четвёртым, введена и проверена: **объём** — он режется **единицей
работы** (тик, вызов поверхности, принятое сообщение), а рёбра FSM,
неравенства риск-гейта, лестница ступеней и арифметика сайзинга отданы
уровню 2. Состав кейсов здесь не пересказывается — дом у него сам документ;
развилки его письма — дайджест Д1612-Д1617.

**Три находки припарковано секциями бэклога:** тенант на читающих тропах ядра
операндом не принимается; обещанный `409` на отказе блокировки у асинхронного
фасада недостижим по построению; половина синка проекций по счетам отказ одной
строки не терпит.

**Пятый документ кейсов написан** — чёрный ящик `strategies`,
`.claude/tests/cases/strategies.md`. Новая ось формы, ради которой предмет
стоя́л пятым, введена и проверена: **событие как выход** — сервис не потребляет
ничего, выходом кейса чаще всего служит строка outbox, а не ответ, и предметом
ассерта становится **состав содержимого**, потому что недоложенное поле снимка
наблюдается у чужого сервиса. Состав кейсов здесь не пересказывается — дом у
него сам документ; развилки его письма — дайджест Д1618-Д1622.

**Четыре находки припарковано секциями бэклога:** доля запаса нотинала названа
числом тенанта в доме валидации; гонка активаций отвечает `500` вместо
объявленного `409`; перечень кодов у мутирующих точек у́же производимого; чистка
опубликованных строк outbox писателя не имеет.

**Шестой документ кейсов написан** — чёрный ящик `audit`,
`.claude/tests/cases/audit.md`. Новая ось формы, ради которой предмет стоя́л
шестым, введена и проверена: **событие как вход** — вход кейса есть запись
брокера, собранная поимённо; у приёма два следствия, ложащихся разными
транзакциями; отсутствие выхода измеряется **смещением**, а не только базой;
полнота приёма — предмет ассерта, а не подробность. Состав кейсов здесь не
пересказывается — дом у него сам документ; развилки его письма — дайджест
Д1623-Д1629.

**Четыре находки припарковано секциями бэклога:** обязательность входа мерится
ненулевой ссылкой, а не непустым значением; остановка приёма объявлена
потемной, а исполняется группово; гасящего писателя момента разрыва в боевом
окружении нет, а нижняя граница там двигается вторым операндом; третья ветвь
пустоты колонки радиуса дома не имеет.

**Седьмой документ кейсов написан** — чёрный ящик `statistics`,
`.claude/tests/cases/statistics.md`. Новая ось формы, ради которой предмет
стоя́л седьмым, введена и проверена: **пересчёт проекции** — у события два
следствия, разнесённые во времени разными исполнителями, поэтому кейс приёма
и кейс пересчёта разведены; отсутствие выхода получило третью форму («строка
не пересчитана»), и различает её момент сборки; повторный прогон стал
предметом ассерта; арифметика зерна осталась на уровне 1 целиком, потому что
считает её база, а не Java. Состав кейсов здесь не пересказывается — дом у
него сам документ; развилки его письма — дайджест Д1630-Д1637.

**Четыре находки припарковано секциями бэклога:** поглощение повтора у обеих
вставок факта целится в `event_id` один, а уникальное ограничение
гипертаблицы составное — приём не кладёт ни одного факта; свои величины
полноты названы журнальными в описании поверхности и в javadoc, а вместе с
ними жив пересказ снятых разделом конструкций; пустой плановый риск выпадает
из знаменателя R и не виден ни одному счётчику; обязательность входа мерится
ненулевой ссылкой, а не непустым значением.

**Восьмой документ кейсов написан** — чёрный ящик `bff`,
`.claude/tests/cases/bff.md`; им закрыт **уровень 1** целиком. Новая ось
формы, ради которой предмет стоя́л восьмым, введена и проверена: **поток в
браузер** — билет вместо заголовка, окно переигрывания, пульс живости и
отсутствие своей базы. Состав кейсов здесь не пересказывается — дом у него
сам документ; развилки его письма — дайджест Д1638-Д1645.

**Первый документ кейсов уровня 2 написан** — расчётный слой движка
стратегий, `.claude/tests/cases/strategy-engine-calculation.md`. Им же
введена **форма уровня 2** (группа с базовой сборкой, кейс — строка таблицы
из пяти колонок); дом формы — сам документ, развилки — дайджест
Д1646-Д1652.

**Второй документ кейсов уровня 2 написан** — слой условий движка
стратегий, `.claude/tests/cases/strategy-engine-condition.md`. Новая ось
формы, ради которой предмет стоя́л вторым, введена и проверена: **выход
булев, и ложь у него одна на все причины** — посчитанная ложь, недоступный
операнд и неисполняемый элемент грамматики дают одно значение, поэтому
единицей доказательства стала **пара строк**, различающихся ровно одним
операндом, а третью причину отличает единственный наблюдаемый след — запись
лога. Состав кейсов здесь не пересказывается — дом у него сам документ;
развилки его письма — дайджест Д1653-Д1659.

**Девять находок припарковано секциями бэклога**, и первая из них —
класс, которого прежние восемь документов не давали: порог обоих правил
хода объявляется плоским полем правила, а интерпретатор читает его только
из константного операнда, то есть оба порога ложны всегда и эталонное
определение репозитория задето. Остальные: источник цены у ценового
операнда не читается; кроссовер с ценовым операндом ложен всегда; предикат
фазы игнорирует оператор, который создание требует; тип «свеча закрыта»
константно истинен; шесть операторов и шесть типов источника объявлены и не
исполняются; пустое условие истинно и даёт безусловную фазу; интерпретатор
бросает там, где объявлена консервативная ложь; гейт покрытия операндов
ядра не мерит предыдущую половину.

**Третий документ кейсов уровня 2 написан** — слой статусных машин
торгового ядра, `.claude/tests/cases/trading-core-fsm.md`. Новая ось формы,
ради которой предмет стоял третьим, введена и проверена: **выход прохода
состоит из двух независимых половин** — команд и статусного ребра, —
производят их разные уровни, и гейт матрицы снимает ровно одну, не трогая
вторую; отличить «обработчик ребра не предложил» от «гейт ребро отверг» по
самому переходу нельзя, и различает их запись лога. Здесь же **впервые на
уровне 2 появились моки**, и признак у них механический: подменяется
коллаборатор, у которого есть ввод-вывод. Состав кейсов не пересказывается —
дом у него сам документ; развилки письма — дайджест Д1660-Д1665.

**Пять находок припарковано секциями бэклога.** Две — расхождение двух
носителей: контракт аварийного терминала объявлен спекой двумя конъюнктами, а
гейт мерит один (направление разрешающее, охрана живёт у писателя числа); дом
жизненного цикла транша объявляет запасное значение причины закрытия
ненаписуемым, а код пишет его фолбэком, и в причину сделки оно не резолвится.
Третья — дом обработчика активной сделки называет его исполнителем двух
входных проверок, которых он не делает. Четвёртая — неиспользуемый
внедрённый коллаборатор. Пятая — **у инструмента корпуса**: маркеры
ввода-вывода у команды кандидатов уровня 2 ищут с границей слова слева, а
конвенция имён проекта ставит перед ними имя сущности, поэтому два самых
частых маркера перечня почти ничего не видят.

**Четвёртый документ кейсов уровня 2 написан** — слой преконтроля риска и
карта его реакций, `.claude/tests/cases/trading-core-risk.md`. Новая ось
формы, ради которой предмет стоя́л четвёртым, введена и проверена: **выход —
упорядоченный перечень отказов, свёртываемый в односоставный вердикт**,
поэтому строка кейса называет **и состав перечня, и позицию члена**: решение
одно и то же на любой причине, а причину реакции карта берёт первым членом —
перестановка проверок молча меняет причину закрытия транша. Здесь же
разведены **два этажа** предмета: кейсы карты строят перечень прямо, а не
прогоняют валидатор ради него. Состав кейсов не пересказывается — дом у него
сам документ; развилки письма — дайджест Д1666-Д1672.

**Признак уровня 2 у предмета выполнен не буквально, и это объявлено, а не
умолчано:** валидатор читает три операнда своей тропой, то есть ввод-вывод у
него есть; моков поэтому четыре, а не ноль. Предмет остался на уровне 2
потому, что вся его работа — неравенства и предикаты над аргументами, и
ровно на этом стои́т вторая точка входа, переиспользуемая детектором
(дайджест Д1668).

**Пять находок припарковано секциями бэклога**, и первые две — класс,
которого прежние документы не давали: **клетка карты реакций, которую не
может заполнить ни один вход**. Восемь значений перечня кодов риск-проверки
не производит ни один класс ядра, и на двух из них стоя́т ветвь запроса
добычи и член карв-аута; предупредительный исход недостижим по той же
причине — единственная фабрика результата ставит блокирующий статус, а дом
карты и узел-гейт держат этот род действующим. Третья — дом состава проверок
не называет код незаданного числа риск-аппетита ни одной строкой обеих своих
таблиц, хотя одна из пяти точек его производства **fail-fast**, то есть
меняет форму выхода. Четвёртая — расхождение двух носителей: дом объявляет
набор проверок ветки ослабления защиты тем же самым, а точка входа снятия
мерит два предиката и ни одного потолка (направление разрешающее). Пятая —
пустой признак полноты графа преконтроль читает как предъявленный, а писатель
четвёрки чисел риска в том же пакете — противоположно.

**Пятый документ кейсов уровня 2 написан** — слой защитных ступеней
торгового ядра, `.claude/tests/cases/trading-core-safety.md`. Новая ось
формы, ради которой предмет стоя́л пятым, введена и проверена: **выхода как
значения у предмета нет вовсе** — точки входа отдают `void` либо признак
«переход применился», и наблюдаемый результат есть **протокол вызовов**:
кто позван, с каким аргументом, в каком порядке и кто **не** позван ни
разу. Отсюда у строки три несущие величины: состав вызовов, отрицательное
ожидание и порядок. Поглощение сигнала целиком состоит из неслучившегося, а
три шага полной реакции из пяти наблюдаемы только порядком. Состав кейсов не
пересказывается — дом у него сам документ; развилки письма — дайджест
Д1673-Д1678.

**Счётчик серии убытков взят в предмет сверх трёх названных перечнем осей,
и это объявлено:** он единственный счётный триггер мягкой счётной ступени,
и лестница без него осталась бы с непроверенной областью определения.

**Пять находок припарковано секциями бэклога**, и первые четыре — новые
секции. Статус «снятие риска выполнено и подтверждено» координатор ставит до
того, как посмотрел на исход, — то есть и на неподтверждённом снятии, а
незакрытый отчёт объявлен домом требуемым следом неподтверждённой реакции.
Доминирование биржевых ступеней над инструментными реакциями объявлено домом
лестницы и не энфорсится ни одним писателем: счётная ступень не входит
операндом ни в ребро подъёма, ни в сервис блокировки, ни в координатор.
Карв-аут возврата инструмента в онбординговый статус живёт в трёх носителях,
включая операнд исполнимой спеки, и неисполним по построению раздела полей
между двумя сервисами. Отказ ребра подъёма на тропе эскалации уходит не
вызывающему, а в перехват инструментной реакции, и счётная ступень не
поднимается больше никогда. Пятая — расширение уже стоявшей секции: ту же
пустоту признака полноты графа читают ложностью ещё два носителя ядра, и
условие её закрытия мерило один файл.

**Шестой документ кейсов уровня 2 написан** — слой перевода форм
источника OKX, `.claude/tests/cases/okx-mapping.md`. Новая ось формы, ради
которой предмет стоя́л шестым, введена и проверена: **вход бывает текстом
провода**, и разбирает его не кейс, а сериализатор. Отсюда мишень
называется суффиксом метки, хотя площадка у предмета одна: у групп `-J`
форма источника приезжает разобранной из тела ответа тем же
сериализатором, что на проводе, а у прочих собирается кодом кейса. Довод
предъявлен построенным дефектом — поле, чей второй символ заглавный,
дважды биндилось в пустоту, и кейс без суффикса его бы не увидел. Состав
кейсов не пересказывается — дом у него сам документ; развилки письма —
дайджест Д1679-Д1684.

**Признак уровня 2 у предмета выполнен механически, и это редкость для
предмета такого объёма:** ни один класс четырёх пакетов не ходит в базу,
к соседу, в брокер и к часам — греп маркеров ввода-вывода даёт одно
попадание, и то в тексте комментария.

**Пять находок припарковано секциями бэклога, все пять заведены заново.**
Размер встроенной защиты, приехавшей в теле родителя, в снапшот не
переносится — имена полей различаются, явного соответствия нет, а
политика непокрытых целей молчалива; та же величина у второй формы того
же снапшота переносится. Отказ разбора ставки комиссии и времени данных
источника проглочен маппером, хотя дом объявляет непарсящуюся ставку
контролируемой ошибкой границы и прямо называет молчаливую пустоту
недопустимой. Снапшот рыночного среза — единственный из четырнадцати
переходов, где перевод идёт встроенным преобразованием, и пустая строка
источника роняет разбор целого среза листинга вместо того, чтобы дать
пустоту. Четыре таблицы маппинга объявляют, что биржевое имя инструмента
в домен не идёт, — а домен его несёт, дом каждой модели объявляет его
атрибутом границы и называет писателем коннектор. Ветка секундного бара
маппера таймфрейма молча отдаёт строку, которую дом объявил незаведённой,
а саму ветку — бросающей.

**Седьмой документ кейсов уровня 2 написан** — расчётный слой рыночных
данных, `.claude/tests/cases/market-data-indicators.md`; его ось формы и
находки — строка захода 16 таблицы ниже, состав кейсов — сам документ.

**Восьмой и последний документ кейсов уровня 2 написан** — предикаты общих
доменных моделей, `.claude/tests/cases/domain-model-predicates.md`. Новая
ось формы, ради которой предмет стоя́л восьмым, введена и проверена: **выход
бывает мутацией получателя**, а не возвращённым значением, и строка такого
кейса называет состояние модели после вызова целиком — включая поля, которых
вызов не тронул. Три следствия одной оси: write-once читается **вторым**
вызовом, отказ называется **ребром матрицы** плюс непотревоженным статусом, а
монотонность и накопление — серией вызовов. Состав кейсов не пересказывается
— дом у него сам документ; развилки письма — дайджест Д1691-Д1697.

**Признак уровня 2 выполнен механически и без единой оговорки** — греп
маркеров ввода-вывода по всему дереву артефакта даёт одно попадание, и то в
тексте комментария. Моков поэтому нет ни одного, и это объявлено признаком
предмета: коллабораторов у доменной модели нет, а подменённый предикат
проверял бы исполнителя против модели, которой в проде не существует.

**Семь находок припарковано, шесть секций заведено заново; седьмая
пользуется уже стоявшей.** Живой эпизод сделки резолвится дизъюнкцией, тогда
как дом называет живым конъюнкцию двух фактов, и предикат читают полтора
десятка точек ядра. Матрица рёбер обычной заявки не исполняется **ни одним**
носителем кода, хотя у двух её соседей по той же спеке охрана стои́т на самой
модели. Множитель «нового закрытого бара» дома в корпусе не имеет. Справочник
компонентов индикатора указан домом у файла, который его не несёт. Перевод
типа сработавшей защиты в причину выхода транша объявлен только javadoc.
Формула середины спреда живёт у дока перевода, а не у владельца величины.
Седьмая — два ступенчатых предиката инструмента читают донорские значения
перечня.

**Уровень 2 закончен целиком, и уровень 3 за ним — тоже.** Пять его предметов
записаны заходами 18-22; состав каждого живёт в своём документе, здесь не
пересказывается (`.claude/rules/policy-home.md`). Последний из них,
`smoke-live`, — единственный, чья мишень развёрнута чужим ходом: прогон стои́т
снаружи окружения и наблюдает его тем же, чем пользователь.

**Ревизия существующего набора записана, и ею под-шаг 1 закрыт.** Каждый
класс тестовых деревьев `services/**` получил механический признак и исход
из закрытого словаря; население таблицы сверяется с деревом командой, а не
счётом (`.claude/work/progress/phase-2-step-12-existing-tests-revision.md`).
Ни один тест не снят и не правлен: снятие принадлежит последнему под-шагу
`CODE`. Названы две вещи, которых у набора не было: **цена сноса класса
`структура`** — предметы, которых ящик не воспроизводит ни при какой
расстановке (границы транзакций, раздел колонок между писателями,
присутствие аннотации на тропе, объявленный классом состав конверта), — и
**находка о классах общих артефактов**, проверяемых из деревьев сервисов.
Развилки письма — дайджест Д1736-Д1742.

**Уровень 2 начат: апрувлены два его предмета** —
`.claude/work/progress/phase-2-step-12-approve-test-strategy-engine-calculation.md`
и
`.claude/work/progress/phase-2-step-12-approve-test-strategy-engine-condition.md`.
Критерий тот же, а предмет бездомности едет вглубь: у ящика пинилось
HTTP-число, у расчётного слоя — **значение** внутреннего контракта, у
интерпретатора условий — **сам предикат**, и последнюю бездомность корпус
объявляет сам. Дом у всех трёх законно бывает кодом, если названо звено.
Состав находок здесь не пересказывается (`.claude/rules/policy-home.md`) —
дом у него отчёт, строки заходов 37 и 38 таблицы §Заходы.

**Под-шаг 2 — адверсариальное ревью кейсов — начат**
(`.claude/skills/test-review.md`). Его вход — двадцать один документ
`.claude/tests/cases/` плюс перечень предметов; находку о `common/platform`
читает оно же (`.claude/work/backlog.md` §«Классы общих артефактов
проверяются из тестовых деревьев сервисов»). **Под-шаг 3 не начинается**,
пока ревью не дало апрув по кейсам.

**Единица под-шага 2 — один предмет, и порядок у него тот же, что у
под-шага 1.** Отчёт заводится свой на предмет
(`phase-2-step-12-review-test-<предмет>.md`), потому что петля «браковка →
правка автором → перепроверка» идёт по предмету, а не по всему перечню:
общий отчёт на двадцать один документ пришлось бы переписывать после
каждой доработки, и его самоописание отстало бы на редакцию
(`.claude/rules/self-description-form.md`).

**Первый предмет ревью — `auth` — забракован.** Апрув не дан; девять
находок с полным набором полей —
`.claude/work/progress/phase-2-step-12-review-test-auth.md`. Состав здесь
не пересказывается (`.claude/rules/policy-home.md`); что ревью **сошлось**
и перепроверке не подлежит, названо в отчёте §«Что проверено и сошлось».
Правку кейсов делает автор, а не ревьюер, — и она отдельная единица
работы.

**Правка по девяти находкам исполнена** —
`.claude/work/progress/phase-2-step-12-fix-test-auth.md`: каждая находка
получила исход, четыре развилки с креном финализированы, четыре находки
уехали владельцам парковками. **Апрув этим не даётся:** следующий ход по
предмету — перепроверка `reviewer`, и до неё `auth` остаётся забракованным.
Состав правок здесь не пересказывается — дом у него отчёт правки и сам
документ кейсов.

**Перепроверка прошла и апрува не дала** —
`.claude/work/progress/phase-2-step-12-recheck-test-auth.md`. Восемь находок
первого круга из девяти закрыты по существу; форма документа, двадцать шесть
указателей, двенадцать секций парковок и условие чужой парковки проверены и
сошлись. Две находки второго круга, из них одна гейтящая; состав здесь не
пересказывается — дом у него отчёт (`.claude/rules/policy-home.md`).

**Ось, которой у первого круга не было: правка, закрывающая находку,
воспроизводит класс, снятый ею же.** Кейс, заведённый по непокрытой оси
конфигурации, несёт отрицательное ожидание, чей наблюдатель не назван ни
одним решением контура, — ровно то, что заход снял двумя кейсами раньше; а
адрес умолчания в нём взят из описания среды держателя, не из клиента, и
пропущена схема, на которой весь исход и держится. **Следующий ход по
предмету — правка автором по двум находкам второго круга**, затем третий
круг перепроверки.

**Класс находки воспроизвёлся у двух чужих предметов**, и это единственное,
что заход оставил за пределами своего предмета: половина ожидания «сегодня —
след отказа виден в логе» стои́т без носителя ещё у `market-data` и
`trading-core`. Правки их документов заход не делал (чужая единица), а
условие возврата мерит оба разом — `.claude/work/backlog.md`
§«Сегодняшняя половина кейсов следа отказа доступа не выведена ни одним
домом».

**Правка по двум находкам второго круга исполнена** —
`.claude/work/progress/phase-2-step-12-fix-test-auth-2.md`: крен гейтящей
находки разведён на две половины (ненаблюдаемое ожидание снято, риск закрыт
осью контура — всякий ящик с хранилищем пришпиливает адрес целиком),
провенанс снят из двух секций, слепота детектора припаркована. Состав здесь
не пересказывается — дом у него отчёт правки.

**Третий круг перепроверки прошёл и апрува не дал** —
`.claude/work/progress/phase-2-step-12-recheck-test-auth-2.md`. Обе находки
второго круга закрыты по существу, посылка о клиенте хранилища выведена
третий раз независимо и держится, литерал мёртвого адреса проверен по ядру
клиента, достижимость предусловия B6.4 доказана отсутствием фабрики
`ConnectionDetails`. Три находки третьего круга, из них две гейтящие; состав
— дом у отчёта (`.claude/rules/policy-home.md`).

**Ось, которой у второго круга не было: правка, закрывшая находку по
существу, дала ей дефектную форму, а соседняя клетка того же захода
воспроизвела тот же класс.** Единственный оставшийся ассерт кейса назван
адресом с идентичностью, которой у прогона нет по построению кейса; а
предусловие соседнего кейса, куда правка внесла восстановление контейнера,
называет следствие о содержимом и молчит о следствии об адресе — то есть ту
же «неназванную половину адреса», из-за которой находка второго круга и
возникла. **Следующий ход по предмету — правка автором по трём находкам
третьего круга**, затем четвёртый круг перепроверки.

**Правка по трём находкам третьего круга исполнена** —
`.claude/work/progress/phase-2-step-12-fix-test-auth-3.md`: ассерт B6.4 переведён
на вычислимый префикс, B2.17 получил свой контейнер и свой контекст, радиус
решения 5 расширен до сервиса, работающего с хранилищем. Правило субстрата
разрушающего кейса заведено осью контура; тот же класс у `connector-okx`
припаркован. Состав здесь не пересказывается — дом у него отчёт правки.

**Четвёртый круг перепроверки прошёл и апрува не дал** —
`.claude/work/progress/phase-2-step-12-recheck-test-auth-3.md`. Все три находки
третьего круга закрыты по существу; достижимость B2.17 выведена из конфигурации
сервиса, входящий §-адрес переведён, форма документа и §-адреса зелены. Две
находки четвёртого круга, обе гейтящие; состав — дом у отчёта
(`.claude/rules/policy-home.md`).

**Ось, которой у третьего круга не было: правка, снявшая зависимость ассерта от
недоступного операнда, завела зависимость от соседа по ОБЩЕМУ субстрату.**
Абсолютное отрицание («под префиксом счетов нет ни одной записи») стои́т над
контейнером, который делят все кейсы модуля, а начальное его содержимое
предусловие не называет — при том что соседний кейс того же документа его
называет. Вторая: популяция признака новой оси собрана шаблоном термина, и члены
класса, выраженные не словом «Vault», исхода не получили. **Следующий ход по
предмету — правка автором по двум находкам четвёртого круга**, затем пятый круг
перепроверки.

**Правка по двум находкам четвёртого круга исполнена** —
`.claude/work/progress/phase-2-step-12-fix-test-auth-4.md`: начальное состояние
общего контейнера внесено в предусловия B6.4 и B2.10, признак оси субстрата
расширен на диспозицию «восстанавливает» и обязан называть **механизм**, а не
исход, у каждой половины признака назван свой довод. Популяция признака
выведена заново **по субстрату**, а не по слову: сверх трёх членов, названных
ревью, проход тремя формами поиска дал ещё пять — в `statistics`, `strategies`,
`bff` и вторым кейсом у `connector-okx`, — и все припаркованы поимённо. Состав
здесь не пересказывается — дом у него отчёт правки.

**Пятый круг перепроверки прошёл и апрува не дал** —
`.claude/work/progress/phase-2-step-12-recheck-test-auth-4.md`. Обе находки
четвёртого круга закрыты **по существу**, популяция признака оси перепроверена
встречной формой поиска и новых членов не дала. Три находки пятого круга, все
гейтящие, плюс два мелких; состав — дом у отчёта
(`.claude/rules/policy-home.md`).

**Ось, которой у четвёртого круга не было: правка закрывает находку по адресу,
названному отчётом, а не по классу, который отчёт объявил.** Обе находки
четвёртого круга оказались классами: `X-1` живёт в одиннадцати кейсах
документа, а не в двух, потому что популяция была выведена по **предусловиям**,
тогда как класс задан **ассертом**; `X-2` провёл обязанность «называть
механизм» по чужим документам и не провёл её по второму субстрату собственного
— три кейса держат ожидание о брокере, снятое их же соседом B7.1 как
ненаблюдаемое. Третья находка — своя: новое предусловие B6.4 опирается на имя
окружения, которого кейс не пинит, а умолчание у свойства пусто; писатель ключей
проверяет имя **до** обращения к клиенту хранилища, и кейс в нынешней редакции
зелен при исправном `VAULT_URI`, то есть тавтологичен. **Следующий ход по
предмету — правка автором по трём находкам пятого круга**, затем шестой круг
перепроверки.

**Шестой круг перепроверки прошёл и апрува не дал** —
`.claude/work/progress/phase-2-step-12-recheck-test-auth-5.md`. Все три находки
пятого круга закрыты **по существу**: B6.4 пинит имя окружения, и посылка о
порядке в писателе ключей проверена по коду; отрицание содержимого хранилища
разностное — встречная форма поиска новых членов не дала; брокер остался у
своего дома B7.1. Две находки шестого круга, обе гейтящие, плюс два мелких;
состав — дом у отчёта (`.claude/rules/policy-home.md`).

**Ось, повторившаяся третий круг подряд: редакция поднята в дом родом, а
исполнена по одному члену рода.** Правка сняла обе находки правильно — и обе
подняла в дом контура правилом с широким радиусом: «отрицание содержимого
**общего субстрата**» и «ожидание о **субстрате, которого у ящика нет**».
Субстратов у контура три, а исполнены редакции по одному каждая: по хранилищу
и по брокеру. Общая **база** несёт снятую форму двадцатью клетками и
одиннадцатью предусловиями пиньнутой пустоты; **стабы соседей** — четырьмя
клетками, снятыми B7.2 как ненаблюдаемые, причём две из них правка трогала
этим же заходом, убирая из них половину о брокере. Клейм «первый предмет
приведён к форме целиком» при этом уже написан в трёх носителях. **Следующий
ход по предмету — правка автором по двум находкам шестого круга**, затем
седьмой круг перепроверки.

**Правка по двум находкам шестого круга сделана, и механизм закрыт у дома, а
не у адресов** — `.claude/work/progress/phase-2-step-12-fix-test-auth-6.md`.
Взят крен (а) `Y-1`: все клетки о содержимом общей базы переведены в разностную
форму либо в радиус своих строк кейса, пиньнутая пустота осталась одна — у
B3.2, который берёт свой контейнер, потому что его предмет и есть состояние
всей таблицы. `Y-2` закрыт снятием четырёх клеток со ссылкой на B7.2 и правкой
пассажа §«Чем достаются выходы», называвшего стабы соседей механизмом
отрицания. Сверх названного: в дом контура внесён пассаж о **радиусе рода**
(перебор идёт по субстратам ящика, а не по адресам отчёта), клейм «целиком»
снят в двух живых носителях, а команда вывода кандидатов в парковке чужих
документов расширена словами базы и разбором предусловий. **Следующий ход по
предмету — седьмой круг перепроверки.**

**Седьмой круг перепроверки прошёл и апрува не дал** —
`.claude/work/progress/phase-2-step-12-recheck-test-auth-6.md`. Обе находки
шестого круга закрыты **по существу**, и разбор автора воспроизведён дословно
(52 клетки, 28 разностных, 4 абсолютных попадания — все не члены, одно
предусловие пиньнутой пустоты). **Впервые за петлю правка не воспроизвела
внутри своего предмета класс, которым была забракована.** Три находки седьмого
круга, все гейтящие, плюс три мелких; состав — дом у отчёта
(`.claude/rules/policy-home.md`).

**Ось круга: класс ушёл из предмета в ДОМ, а второй его вид — в полноту
покрытия.** `X-1` — пассаж, поднявший обязанность перебирать род по субстратам
ящика, **закрыл перечень субстратов тремя**, тогда как его собственная
иллюстрация в том же абзаце считает субстратом четвёртый род — стаб; у
одиннадцати чужих документов он даёт 44 клетки снятой формы, которых не
печатает ни команда парковки, ни перебор по названному перечню. `X-2` — правка
ввела **второе основание** брать свой контейнер («предмет кейса есть состояние
всего контейнера») и оставила его в документе, указав на дом, где признак один
и он о разрушении субстрата; адресат парковки, читающий дом буквально, снимет
предмет трёх чужих кейсов. `X-3` — первая находка петли не этого класса:
негатив «обязательный вход не предъявлен» покрыт двумя полями из семи и одним
claim'ом из трёх, при том что документ сам объявляет принцип поимённой метки на
поле. **Следующий ход по предмету — правка автором по трём находкам седьмого
круга**, затем восьмой круг перепроверки.

**Нумерация в снапшоте была сдвинута и выправлена этим заходом:** круг седьмой,
а перепроверка — шестая (заходы 3, 5, 7, 9, 11, 13 суть перепроверки 1-6).

**Правка по трём находкам седьмого круга сделана, и две из трёх — правки
дома** — `.claude/work/progress/phase-2-step-12-fix-test-auth-7.md`. Перечень
субстратов открыт до четырёх (стаб внесён четвёртым членом с делением «сценарий
ответа — вход, счётчик обращений — накопленное состояние»); второе основание
брать свой контейнер заведено в доме, и у пиньнутой пустоты объявлены два
исхода; принцип «метка на единицу» поднят в дом-скилл признаком, реестр
обязательных входов выведен из кода, и по нему заведены семь недостающих кейсов
(B1.12, B1.13, B2.18-B2.22). **Следующий ход по предмету — восьмой круг
перепроверки.**

**Восьмой круг перепроверки прошёл и апрува не дал** —
`.claude/work/progress/phase-2-step-12-recheck-test-auth-7.md`. Все три находки
седьмого круга закрыты **по существу**: реестр воспроизводится дословно (10
единиц, 7 кейсов заведено), разбор клеток — тоже (59 клеток, 35 разностных, 4
абсолютных не-члена, одно законное предусловие пустоты), дома приведены и
иллюстрация со счётом больше не расходится. **Второй круг подряд правка не
воспроизвела внутри своего предмета класс, которым была забракована.** Три
находки восьмого круга, все гейтящие, плюс два мелких; состав — дом у отчёта
(`.claude/rules/policy-home.md`).

**Ось круга: забраковано то, что правка написала сама, и то, чего петля не
трогала ни одним кругом.** `Z-1` — правка закрыла мелкое замечание по адресу
(B2.6) и написала пять новых кейсов той же категории с ровно той конвенцией
предусловий, которую замечание объявило дефектной: категория несёт теперь две
конвенции в одном документе, и класс шире дельты (семь кейсов против одного
приведённого). `Z-2` и `Z-3` — один род: клетка рассказывает о коде то, чего в
коде нет. У `Z-2` разведение B1.12 с B1.3 оперто на «другую ветвь предиката», а
обе клетки берёт один и тот же `if`. У `Z-3` кейс B1.9 не мерит объявленного
предмета вовсе: подпись проверяется **до** издателя, и токен, подписанный чужим
ключом, отвергается механизмом B1.7 — снятие валидатора издателя оставит кейс
зелёным. Новое в круге то, что обе клетки рода живут **вне дельты**: одну
написала сама правка, вторая стои́т в документе с первого захода и ни одним
кругом не читалась против исходников контейнера. **Следующий ход по предмету —
правка автором по трём находкам восьмого круга**, затем девятый круг
перепроверки.

**Правка по трём находкам восьмого круга сделана** —
`.claude/work/progress/phase-2-step-12-fix-test-auth-8.md`: все три крена (а),
семь предусловий категории переведены на форму «своих нет», довод B1.12
переписан на пустоту операнда, B1.9 пиньнул ключ подписи стаба; обе
команды-доказательства перенесены в сам документ, вторая половина признака
субстрата внесена в дом контура.

**Девятый круг перепроверки прошёл и апрува не дал** —
`.claude/work/progress/phase-2-step-12-recheck-test-auth-8.md`. Все три находки
восьмого круга закрыты **по существу**: команда популяции печатает ноль кейсов
с названной зависимостью, ветви `requireBrowserToken` и названный регресс
проверены по коду, перечень валидаторов контура — по исходникам
`JwtValidators`. **Третий круг подряд правка не воспроизвела внутри своего
предмета класс, которым была забракована**, и впервые обе команды постоянного
носителя прогоняются дословно из самого носителя. Четыре находки девятого
круга, все гейтящие, плюс три мелких; состав — дом у отчёта
(`.claude/rules/policy-home.md`).

**Ось круга: класс закрыт у одного носителя и воспроизведён у соседнего тем же
заходом.** Три находки из четырёх — этого рода. `W-1`: признак предусловий
поднят в дом верно, а в документе рядом написан его пересказ (при объявлении
«здесь не пересказывается»), чей антецедент накрывает четыре кейса этого же
документа — у B1.3, B1.4, B1.12 и B1.13 отказ производит тот же механизм, а
предусловия законны, причём у B1.4 предусловие и есть предмет кейса; род тот
же, что у мелкого прошлого круга — признак не отсекает не-члена сам. `W-2`:
перебор класса «кейс о контуре токена» дал исход по трём непокрытым осям
(`typ`, отпечаток сертификата, `aud`), и исход этот живёт только в отчёте
захода — то самое, за что правка чинила мелкое 1; довод по `typ` вдобавок
опровергается посылкой, внесённой тем же заходом в B1.9 как довод
достижимости. `W-3`: свип правки объявил клетки B2.21 и B2.22 верными,
прочитав в каждой половину, которую напечатал шаблон, — вторая половина
(«тропа и тело у них разные») противоречит строке «Ожидаемые выходы» тех же
кейсов. `W-4` — своего рода: команда, сделанная носителем клейма о форме
документа, базового гейта не несёт и на неразобранном предмете печатает ноль
клеток с кодом `0`, тогда как соседняя команда того же раздела гейт несёт.
**Следующий ход по предмету — правка автором по четырём находкам девятого
круга**, затем десятый круг перепроверки.

**Правка по четырём находкам девятого круга сделана** —
`.claude/work/progress/phase-2-step-12-fix-test-auth-9.md`. Крены: `W-1` (а) —
следствие буллета предусловий стало перечнем без критерия; `W-2` **(б)** —
заведён кейс B1.14 на ось `typ`, а исход двух оставшихся осей (`aud`,
`x5t#S256`) внесён в документ по **механическому** признаку «валидатор в
перечне есть и его предикат на нашей тропе достижим», выведенному по
исходникам линии Spring Security 7.0.0; прежний довод («нарушение
конструируется самому себе») снят как неразличающий. `W-3` (а) — клауза о теле
снята в обеих клетках, разведение осталось тропой. `W-4` (а) — у команды 1
базовый гейт: ноль кейсов либо ноль клеток ⇒ код 2; проба падающая, обе команды
прогнаны на двух состояниях. Три мелких закрыты, и у мелкого 1 свип нашёл
**третье** вхождение сверх двух названных. Команда 2 переведена на свой
предмет: сервис приходит аргументом, реестра чужих предметов постоянный
носитель `auth` больше не держит.

**Заход завёл находку F-9 и её парковку:** контур принимает заголовок `typ`
только со значением `JWT` либо без него, а какое значение ставит токен
выбранного провайдера идентичности, не называет ни один дом; расхождение
закрывает браузерную тропу целиком. Дом парковки — `.claude/work/backlog.md`
§«Значение заголовка `typ` у выбранного провайдера идентичности ни одним домом
не названо». **Следующий ход по предмету — десятый круг перепроверки**
(перепроверка девятая, заход 19).

**Десятый круг перепроверки прогнан — браковка десятая** (перепроверка девятая,
заход 19): `.claude/work/progress/phase-2-step-12-recheck-test-auth-9.md`. Три
находки девятого круга из четырёх и все три мелких закрыты чисто, и **впервые за
петлю правка не воспроизвела класс, которым была забракована, ни у одного
носителя**: перечень предусловий верен по обеим осям (восемь кейсов формы «своих
нет», сверх них ни одного), клауза о теле снята без нового противоречия, перечень
валидаторов контура и тропа его подключения сверены по исходникам
`spring-security-oauth2-jose-7.0.0` и автоконфигурации Boot 4 **заново**, оба
базовых гейта выдержали мутацию ревьюера — у команды 1 **вторую**, которой
правка не предъявляла.

**Забракован один пассаж — новый признак отбора осей контура токена.** Единицей
он взял **ось**, а наблюдаемой единицей тропы является **валидатор**, и отсюда
признак ложен в обе стороны: исключение `aud` стои́т на свойстве «валидатора в
перечне нет», которое есть и у подписи, — а у подписи кейс есть и обязан быть;
и он же требует кейса на `nbf` (`JwtTimestampValidator` валидирует две оси, а
предикат «`nbf` в будущем» на нашей тропе достижим), которого в документе нет и
которого дом негатива единицей не признаёт. Клейм «оси перебраны, и исход у
каждой свой» этим ложен. Рекомендованный крен — переписать признак с единицей
«валидатор»: тогда популяция закрыта перечнем `createDefaultWithValidators`,
`aud` выпадает из неё по построению, а единицу `nbf` держит B1.8. Плюс три
мелких: исключение `aud` держится отсутствующим свойством `audiences` без
наблюдателя; посылка находки F-9 смешивает заголовок `typ` с claim'ом
содержимого; `закрыто-когда` секции F-9 берёт подстроку `typ`, а не предмет.

**Своя находка захода, к предмету не относящаяся, припаркована:** каталог
`target/` в корне — предписанное место мутационных проб — уводит
`tools/retired-check.py` в код 2; дом — `.claude/work/backlog.md` §«Каталог проб
`target/` в корне уводит `retired-check.py` в код 2».

**Одиннадцатый круг открыт правкой — десятая правка прогнана** (заход 20):
`.claude/work/progress/phase-2-step-12-fix-test-auth-10.md`. Гейтящая находка
закрыта рекомендованным креном (а): признак переписан с единицей **исполнитель
отказа на тропе разбора токена**, популяция названа кодом (декодер контура плюс
перечень, который он получает от `getValidators`), и признак **применён к
каждому названному имени таблицей** — пяти исполнителям, трём осям и отпечатку;
расхождений нет ни одного, тогда как прежний давал два. Популяция выведена по
исходникам заново, а не принята из отчёта перепроверки. Три мелких закрыты в
тех же двух носителях: в буллет внесено, чем снимается исключение `aud`
(свойство `audiences` либо бин валидатора — свойство конфигурации, не
библиотеки); посылка F-9 сведена к «значение не названо», предположение о линии
провайдера убрано и в документе, и в парковке; `закрыто-когда` секции F-9
переведено с подстроки на предмет — `греп:"typ[^a-zA-Z]"`, проба на трёх
состояниях падающая.

**Своя находка захода припаркована:** клейм буллета о негативе по единице
(«каждая его единица получила исход, читается прогоном команды») шире того, что
команда 2 печатает, — она выводит реестр единиц и сопоставления с метками
кейсов не делает; род тот же, что у `W-4` девятого круга. Дом —
`.claude/work/backlog.md` §«Клейм о втором прогоне кейсов `auth` шире того, что
команда печатает»; условие доказано пробой на двух состояниях.

**Одиннадцатый круг закрыт перепроверкой — браковка одиннадцатая** (перепроверка
**десятая**, заход 21):
`.claude/work/progress/phase-2-step-12-recheck-test-auth-10.md`. Новый признак
применён ревьюером к каждому названному имени самостоятельно и в своих границах
сошёлся — расхождений нет ни одного; популяция выведена по исходникам заново
(`JwtValidators`, три валидатора перечня, автоконфигурация Boot) и совпала;
обе команды документа прогнаны дословно и числа правки воспроизвели; свип снятых
редакций в живых носителях пуст.

**Апрув не дан — три гейтящие находки и три мелких.** Класс, шедший кругами,
воспроизведён на уровень выше: **признак по-прежнему не выводит популяцию, а
называет её.** Единицей объявлен «исполнитель отказа на тропе разбора токена», а
популяцией — «декодер плюс перечень валидаторов»; в зазоре лежит
`DefaultBearerTokenResolver`, отказывающий на той же тропе раньше декодера и не
получивший исхода. Вторая — тот же род на соседней клетке: мелкое девятого круга
(«исключение держится без наблюдателя») закрыто у `aud` и не проведено по
`X509CertificateThumbprintValidator`, чья причина к тому же опирается на посылку
о производителе токена — предмет, которого не называет ни один дом. Третья —
клейм о команде 2, найденный самой правкой и **припаркованный**: парковка для
этого класса не законна (блокер не отсутствие носителя), а документ остался с
утверждением, которому противоречит его же §«Команды разбора».

**Счёт перепроверок выправлен:** отчётов перепроверки девять было до этого
захода, поэтому заход 21 — **десятая** перепроверка; одиннадцатым идёт **круг**.
Прежняя строка называла её одиннадцатой, смешав два счёта.

**Следующий ход по предмету — четырнадцатая правка автором** (заход 28, круг
пятнадцатый), затем четырнадцатая перепроверка. Дальнейшие круги ведёт таблица
§Заходы; прозой здесь они не пересказываются.

Два вопроса, чьё закрытие
кейс не вправе подменить, названы в отчёте ревью концепции
(`.claude/work/progress/phase-2-step-12-concept-review.md`
§«Два вопроса, которые кейс может закрепить»).

## Концепция

**Носитель концепции шага — этот раздел.** Материал держателя дан
2026-09-12, записан 2026-09-16 дословно по смыслу. Трёхуровневая формула
(чёрный ящик, юниты по критерию чистой логики, сквозные) и порядок трёх
под-шагов `CODE` здесь не пересказываются — их дом
`.claude/processes/roadmap-step-execution.md` §«Конец фазы»; происхождение
машины — `.claude/decisions/step-machine-rework.md`.

**Что доказывает шаг.** Систему, не модули: чёрный ящик каждого сервиса
плюс сквозные тесты через API. Цель — рефакторить сервисы без опаски;
позже наборы встраиваются в пайплайн GitLab.

### 1. Чёрный ящик сервиса (Testcontainers)

Каждый сервис отдельно.

- **Входы:** вызовы API, потреблённые события, ответы внешних систем
  (стабы), конфигурация и **время** — джоба по расписанию есть вход,
  которым управляет тест.
- **Выходы:** ответы API, опубликованные события, исходящие вызовы, **база
  сервиса**. **Отсутствие выхода — тоже выход:** «риск-гейт отказал»
  утверждается как «ни ордера, ни события», а не остаётся непроверенным.

**Материал держателя к дизайн-проходу:** при ассертах на базу развести, где
схема — контракт (журнал аудита, факты статистики), а где рабочая
внутренность, — чтобы тесты не закрепляли хранение против цели
рефакторинга.

### 2. Юнит-тесты — только чистая логика

Юниты пишутся только на чистую логику с комбинаторным входом:
риск-математика, размер позиции, переходы FSM, парсеры, календарь и время.
Через стабильные интерфейсы, без моков внутренностей. Проводка,
репозитории, оркестрация — только чёрный ящик. Существующий набор реактора
ревьюется этим критерием: тесты, закрепляющие структуру, — под снос, когда
их поведение покрыл ящик.

### 3. Интеграционные — только против биржи, только коннектор

Термин держателя: «интеграционные» здесь — против биржи; контейнеры базы и
Kafka — субстрат чёрного ящика, не интеграционные. У коннектора два слоя:

- чёрный ящик против **стаба** с записанными ответами реальной площадки —
  детерминированно, в каждом пайплайне;
- чёрный ящик против **живой площадки** — demo-окружение источника,
  тестовый профиль, тестовый аккаунт биржи — по расписанию и по запросу, не
  в каждом пайплайне: это детектор дрейфа API площадки. Записи для стаба
  берутся из этих прогонов.

**Обе формы — чёрный ящик самого `connector-okx`** (уточнение держателя
2026-09-16, §«Пересмотр границы»): отдельной тест-обращённой поверхности у
сервиса не появляется.

### 4. Сквозные — два набора

- **Ядро** против стаба биржи — в каждом пайплайне, широкие сценарии.
- **Дым** против тестового аккаунта биржи — по расписанию и перед выходом
  на живого; узкий: выставил → исполнилось/отменилось → видно в журнале и
  агрегатах.

Площадка — локальный стенд сейчас; тот же набор переезжает на кластер как
есть.

### Границы концепции

- **Предмет — построенные сервисы фазы 2 в их итоговом составе** (после
  раздела `audit-statistics` шагом 11).
- **«Как» — предмет дизайн-прохода шага, не концепции:** стабы, управление
  временем, обвязка сквозных, состав сценариев; за ними — перечень
  предметов по уровням, среда прогона, место документов кейсов, включение в
  реактор.
- **Работа с источником — внутри трёх уровней, а не рядом с ними**
  (§«Пересмотр границы»).
- **Роль `tester` и скиллы `test-*` сегодня привязаны к контуру API
  источника.** Новый предмет им даёт тот же дизайн-проход; агенты и скиллы
  под него появляются от потребности единиц шага, не заранее.
- **Нагрузочное тестирование и генератор (эмулятор) сбоев — не предмет
  шага:** держатель отнёс их к концу фазы 6. Парковка со своим триггером —
  `.claude/work/backlog.md` §«Нагрузочные прогоны и генератор сбоев — конец
  фазы 6».

### Пересмотр границы (решение держателя, 2026-09-16)

**Оговорка «контур API источника — свой процесс» снята.** Три уровня
концепции покрывают **всё построенное фазой 2, включая интеграцию с
источником**: работа с OKX проверяется как чёрный ящик `connector-okx` на
demo-окружении — как у любого другого сервиса. **Отдельный контур со своей
тест-обращённой поверхностью (`/raw`-passthrough) в сервисах не строится.**

Пересмотр адресует границу, данную 2026-09-15 вместе с машиной шага; три
уровня, порядок трёх под-шагов `CODE` и предмет — построенные сервисы фазы 2
в итоговом составе — не меняются.

**Судьба знания контура фазы 1** (`.claude/work/history/2026-09-16-donor-and-contour-removal/source-api-okx/`, его
процесс, скиллы и решения) этой правкой не решается и в ней не тронута:
держатель решает её после изложения понимания концепции.

**Ответы держателя на названные пункты (2026-09-16).**

- **`/raw` в карте владельцев:** верен пересмотр границы; строка карты
  исправлена под решение.
- **Контур фазы 1 снят** вместе с донором; переиспользуемое взято
  материалом для кейсов `connector-okx`
  (`.claude/decisions/source-api-contour-retired.md`).
- **«Интеграционные» — частный случай уровня 1:** чёрный ящик коннектора
  против двух мишеней (стаб с записанными ответами, demo). Так и
  раскладываются кейсы.
- **Существующие юниты** — предмет ревью критерием чистой логики; тест,
  закрепляющий структуру, снимается **последним** под-шагом `CODE` и не
  раньше, чем ящик покрыл его поведение. Ни один существующий тест до
  этого не удаляется. (Этим закрыт остаток записи 2026-09-15 о границе
  «юнит-тесты, идущие вместе с кодом, — не предмет шага».)
- **Demo-прогоны:** ключи и распечатанный Vault держатель предоставит к
  под-шагу 3; в пайплайн они не входят.

## Вход шага — секции бэклога `сейчас`

Открытие шага (2026-09-15, закрытием шага 11) оживило шесть секций
бэклога условием `шаг:2-12:открыт`; каждая получила исход «взята в
работу». Две из них закрыты 2026-09-16 снятием контура тестов API
источника и его гейта (`.claude/decisions/source-api-contour-retired.md`).
Действующий перечень печатает `py tools/backlog-check.py`. Предметы
оставшихся, без счёта:

| Секция | Владелец | Предмет |
|---|---|---|
| «Ветвь «отчёт есть, ступень не поднята» наблюдателя не имеет» | `trading-core` | откат ребра оставляет отчёт без поднятой ступени |
| «Контролируемое исключение на тропе прохода детекции ловца не имеет» | `solution-designer` | посылка «ступень поднимется сама» на тропе джобы не держится |
| «I5. Round-trip тесты OKX-DTO у коннектора не портированы» | `connector-okx` | корректность read-снапшотов с таймстампами держится только аннотациями |
| «Контекстный тест сервиса — по появлению базы в прогоне» | `tester` | класс дефектов, который не ловит ни компиляция, ни модульный тест |

Секции с оживителем `шаг:2-12:CODE` (позиции класса `ИЗМЕРЕНИЕ`,
популяции спек) сработают входом шага в `CODE`.

## Заходы

| Дата | Статус | Заход | Исход |
|---|---|---|---|
| 2026-09-15 | `HOLD` → `DOCS_CHECK_1` | открытие шага закрытием шага 11 (прежняя машина) | `записан` — итог в `.claude/work/history/2026-09-12-phase-2-step-11-split.md` |
| 2026-09-15 | `DOCS_CHECK_1` → `CONCEPT_REVIEW` | переработка машины шага решением держателя; концепция тестирования записана | `записан` — `.claude/work/history/2026-09-15-step-machine-rework.md` |
| 2026-09-16 | `CONCEPT_REVIEW` (без перехода) | материал держателя по концепции тестирования размещён в носителе; часть «конец фазы 6» отделена парковкой | `записан` — §«Концепция», `.claude/work/backlog.md` §«Нагрузочные прогоны и генератор сбоев — конец фазы 6» |
| 2026-09-16 | `CONCEPT_REVIEW` (без перехода) | держатель снял оговорку «контур API источника — свой процесс»; изложение понимания концепции — держателю, ход дальше за ним | `записан` — §«Пересмотр границы»; инвентарь донора — `.claude/work/history/2026-09-16-donor-inventory.md` |
| 2026-09-16 | `CONCEPT_REVIEW` (без перехода) | ответы держателя по пяти названным пунктам; контур фазы 1 снят, донор удалён | `записан` — `.claude/decisions/source-api-contour-retired.md`, итог — `.claude/work/history/2026-09-16-donor-and-contour-removal.md` |
| 2026-09-16 | `CONCEPT_REVIEW` → `CODE` | ревью достаточности концепции: две проверки фокуса, вердикты гейтинга открытых вопросов переставлены на шаг 12 | `записан` — `.claude/work/progress/phase-2-step-12-concept-review.md`, вердикт «апрув» |
| 2026-09-16 | `CODE` (под-шаг 1, заход 1) | дизайн-проход контура тестов: развилки «как» с альтернативами, перечень предметов по трём уровням и порядок их прохождения, среда — прогоном | `записан` — `.claude/decisions/test-contour-design-pass.md`, дайджест Д1581-Д1594 |
| 2026-09-16 | `CODE` (под-шаг 1, заход 2) | первый документ кейсов — чёрный ящик `auth`: поверхность, контур доступа, конфигурация как вход, отрицательные ожидания; четыре находки припаркованы | `записан` — `.claude/tests/cases/auth.md`, дайджест Д1595-Д1599 |
| 2026-09-16 | `CODE` (под-шаг 1, заход 3) | второй документ кейсов — чёрный ящик `connector-okx`: ключи и подпись, команды и семантика подтверждения, добыча факта, публичные чтения, классы отказа границы, отсутствие выходов, мишень `-D`; четыре находки припаркованы | `записан` — `.claude/tests/cases/connector-okx.md`, дайджест Д1600-Д1605 |
| 2026-09-16 | `CODE` (под-шаг 1, заход 4) | третий документ кейсов — чёрный ящик `market-data`: требования потребителя, пять тиков как вход, цикл единицы сбора, производные, невосполнимые срезы, свежесть как операнд читателя, отсутствие выходов; пять находок припарковано | `записан` — `.claude/tests/cases/market-data.md`, дайджест Д1606-Д1611 |
| 2026-09-16 | `CODE` (под-шаг 1, заход 5) | четвёртый документ кейсов — чёрный ящик `trading-core`: отбор входа, порядок шагов прохода и перехваты, жизненный цикл команды и классы отказа, риск-гейт, ступени защиты и их идемпотентность, ручная поверхность, проактивная детекция, приём фактов определений, outbox и реле, проекции, поверхность чтения, контур доступа, конфигурация и схема, отсутствие выходов; три находки припарковано | `записан` — `.claude/tests/cases/trading-core.md`, дайджест Д1612-Д1617 |
| 2026-09-16 | `CODE` (под-шаг 1, заход 6) | пятый документ кейсов — чёрный ящик `strategies`: создание и его отказы, перечень и чтение, матрица переходов, предусловия готовности активации, содержимое и конверт события, реле outbox как тик-вход, контур доступа, конфигурация и схема, отсутствие выходов; четыре находки припарковано | `записан` — `.claude/tests/cases/strategies.md`, дайджест Д1618-Д1622 |
| 2026-09-16 | `CODE` (под-шаг 1, заход 7) | шестой документ кейсов — чёрный ящик `audit`: приём события в строку, неполный вход как единственная ветвь отказа, тик состояния приёма и его ряды, три исхода сравнения смещений, обе величины полноты, чистка по глубине и ось окружения, журнальная выборка чтения, контур доступа, конфигурация и схема, отсутствие выходов; четыре находки припарковано | `записан` — `.claude/tests/cases/audit.md`, дайджест Д1623-Д1629 |
| 2026-09-16 | `CODE` (под-шаг 1, заход 8) | седьмой документ кейсов — чёрный ящик `statistics`: приём в факт своего зерна и три его ветви, неполный вход, тик состояния приёма и его ряды, три исхода сравнения смещений, нижняя граница одним операндом, предикат непрерывности, пересчёт окном и посуточными порциями, арифметика сделочного зерна, счётчики происшествий, агрегатная выборка чтения, контур доступа, конфигурация и схема, отсутствие выходов; четыре находки припарковано | `записан` — `.claude/tests/cases/statistics.md`, дайджест Д1630-Д1637 |
| 2026-09-16 | `CODE` (под-шаг 1, заход 9) | восьмой и последний документ кейсов уровня 1 — чёрный ящик `bff`: вывод контекста из членств и кэш, билет как значение, открытие подписки и радиус тенанта, окно переигрывания и явный разрыв, пульс как измеритель, приём события в запись потока, форма на проводе к браузеру, проксирование владельцу, контур доступа и единый формат отказа, конфигурация как вход, границы периметра; шесть находок припарковано | `записан` — `.claude/tests/cases/bff.md`, дайджест Д1638-Д1645 |
| 2026-09-16 | `CODE` (под-шаг 1, заход 10) | первый документ кейсов **уровня 2** — юнит по критерию чистой логики, расчётный слой движка стратегий: форма уровня выбрана здесь (группа с базовой сборкой, кейс — строка таблицы), база цены и её источники, якорь уровня и его ветвь, четыре способа расчёта стопа, точный безубыток, тейк и трейлинг, две стороны округления, закрытая форма размера входа, четыре исхода выхода, ступень защитной лестницы, действия без цены и размера, порядок и обёртка оркестратора, отсутствие выходов слоя; шесть находок припарковано | `записан` — `.claude/tests/cases/strategy-engine-calculation.md`, дайджест Д1646-Д1652 |
| 2026-09-16 | `CODE` (под-шаг 1, заход 11) | второй документ кейсов **уровня 2** — слой условий движка стратегий: новая ось формы (булев выход, пара строк как единица доказательства, лог как третий различитель), конъюнкция и её вырождения, операторы сравнения и их область, индикаторный операнд и его компонент, ценовой операнд, кроссовер на двух половинах времени, объёмный фильтр, структура рынка и пробой, два предиката фазы, факты сделки и защиты, пороги хода, whitelist контекста пустотой операндов, типы без исполнения, отсутствие выходов; девять находок припарковано | `записан` — `.claude/tests/cases/strategy-engine-condition.md`, дайджест Д1653-Д1659 |
| 2026-09-16 | `CODE` (под-шаг 1, заход 12) | третий документ кейсов **уровня 2** — слой статусных машин торгового ядра: новая ось формы (выход прохода — две независимые половины, и гейт снимает ровно одну; лог как единственный различитель между молчанием обработчика и отказом матрицы), две матрицы рёбер, доказанное отсутствие живого риска, оба контракта терминала сделки и их асимметрия, разрешение полного закрытия нетто-экспозиции, три обработчика сделки, переоткрытие эпизода и его номер, запрет нового риска под сворачиванием, контракт терминала транша, каскад и старшинство просьб, шесть обработчиков транша, рабочий блок и диспозиция плана, отбор шага с тремя охранами, гейт свежести и её эскалация, отсутствие выходов; **моки появились впервые на уровне 2** — по механическому признаку «у коллаборатора есть ввод-вывод»; пять находок припарковано | `записан` — `.claude/tests/cases/trading-core-fsm.md`, дайджест Д1660-Д1665 |
| 2026-09-16 | `CODE` (под-шаг 1, заход 13) | четвёртый документ кейсов **уровня 2** — слой преконтроля риска и карта его реакций: новая ось формы (выход — упорядоченный перечень, свёрнутый в односоставный вердикт; строка называет и состав, и позицию члена), два этажа предмета и разведение их кейсов, входные гейты и fail-fast, торговые ограничения инструмента, ставка комиссии и якорь себестоимости, четыре уровневые проверки, база потолков, риск и нотинал акта по классу действия, пять неравенств, окно сворачивания и блок-сет ступени, вторая точка входа при нулевом акте, снятие защиты, свёртка вердикта, три строки карты реакций и признак живого риска, бессрочность кода, разрешающее множество гейта, четвёрка чисел риска и её запись, отсутствие выходов; пять находок припарковано | `записан` — `.claude/tests/cases/trading-core-risk.md`, дайджест Д1666-Д1672 |
| 2026-09-16 | `CODE` (под-шаг 1, заход 14) | пятый документ кейсов **уровня 2** — слой защитных ступеней торгового ядра: новая ось формы (выхода-значения нет вовсе, наблюдаемый результат есть протокол вызовов — состав, отрицательные ожидания, порядок), лестница ступеней и её триггеры, каскад радиуса, поглощение сигнала, гейт терминала отчёта и эскалация, консервативная агрегация каскадного снятия риска, ключ дедупа отчёта, гистерезис, предел слепоты, ручная пара и её отказы, счётчик серии убытков; пять находок припарковано | `записан` — `.claude/tests/cases/trading-core-safety.md`, дайджест Д1673-Д1678 |
| 2026-09-16 | `CODE` (под-шаг 1, заход 15) | шестой документ кейсов **уровня 2** — слой перевода форм источника OKX: новая ось формы (вход бывает текстом провода, мишень суффиксом метки), конверт ответа и манглинг имён свойств, позиционные формы свечи и книги, пять форм разбора сырой строки, знаковые конвенции и словари площадки, четырнадцать переходов «источник → снапшот», два перехода «снапшот → домен» с разными стратегиями пустоты, пять форм запросов, подтверждение приёма с двумя кодами, три резолвера, таймфрейм, отсутствие выходов; пять находок припарковано | `записан` — `.claude/tests/cases/okx-mapping.md`, дайджест Д1679-Д1684 |
| 2026-09-16 | `CODE` (под-шаг 1, заход 16) | седьмой документ кейсов **уровня 2** — расчётный слой рыночных данных: новая ось формы (вход и выход — ряды, и строка называет начало ряда, его длину и член; ожидание бывает свойством, а не числом), общий контракт вычислителя и зона прогрева, числовые помощники, восемь вычислителей по своим осям, окно и свинг-пивоты структуры, классификация типа, состав уровней, событие пробоя, вырожденные входы и консервативный исход, порядок клауз фазы, отсутствие выходов; **у арифметики шести вычислителей дома в корпусе нет**, и её кейсы помечены непрогоняемыми; семь находок припарковано | `записан` — `.claude/tests/cases/market-data-indicators.md`, дайджест Д1685-Д1690 |
| 2026-09-16 | `CODE` (под-шаг 1, заход 17) | восьмой и последний документ кейсов **уровня 2** — предикаты общих доменных моделей: новая ось формы (выход бывает мутацией получателя, и строка называет состояние после вызова целиком, включая непотревоженные поля; write-once читается вторым вызовом, отказ — ребром матрицы, монотонность — серией), экспозиция и покрытие транша, действующий уровень, живой риск и перечни, инициатор выхода, агрегатные предикаты сделки, эпизоды и суммы издержек, старшинство причин, живость заявок и защит, рёбра статуса, эпизод-позиция, ступени радиусов, дерево стратегии, грамматика условия, рыночные модели, арифметика и формы идентичности, отсутствие выходов; моков нет ни одного по признаку предмета; семь находок припарковано | `записан` — `.claude/tests/cases/domain-model-predicates.md`, дайджест Д1691-Д1697 |
| 2026-09-16 | `CODE` (под-шаг 1, заход 18) | первый документ кейсов **уровня 3** — сквозная тропа «активация определения → сделка»: новая ось формы (единица предмета — тропа, выходы перечисляются по сторонам, предмет кейса — стык, порядок — такой же ассерт, как состав), пять сторон и четыре стаба, создание с валидацией у ядра, активация и копия, вход и сделка, заявка через коннектор до стаба площадки, факт в зеркале, журнал и полнота приёма, факты и счётчики агрегата, порядок и отсутствие следов; развилка обвязки закрыта решением 12 дизайн-прохода; три находки припаркованы | `записан` — `.claude/tests/cases/e2e-strategy-to-deal.md`, `.claude/decisions/test-contour-design-pass.md` §«12. Сторона сквозной тропы поднимается СВОИМ ПРОЦЕССОМ, а не вторым контекстом в той же JVM», дайджест Д1698-Д1704 |
| 2026-09-16 | `CODE` (под-шаг 1, заход 19) | второй документ кейсов **уровня 3** — сквозная тропа «выход, сверка движений, терминал сделки»: новая ось формы (пролог живёт в чужом документе и называется его кейсом поимённо; следы пролога не переспрашиваются; разрыв между тропами — первый собственный отрезок; концов у тропы два, и кейс называет свой), экспозиция в зеркале и четыре триггера выхода, каскад в транши и порядок снятия у площадки, добыча фактов закрытия через коннектор, сверка в двух режимах допуска, оба терминала и содержимое терминального события, журнал и статистика тропы, порядок и отсутствие следов; три находки припарковано | `записан` — `.claude/tests/cases/e2e-exit-and-close.md`, дайджест Д1705-Д1712 |
| 2026-09-16 | `CODE` (под-шаг 1, заход 20) | третий документ кейсов **уровня 3** — сквозная тропа «жёсткая ступень → снятие живого риска → отчёт и событие»: новая ось формы (вход тропы есть расхождение, которое система обязана заметить сама; число тиков — часть входа; два входа в один стык разведены кодом и разрезом счётчика; незавершённость — объявленный исход с положительным ассертом), детекция и гистерезис, гейт полноты среза и предел слепоты, порядок снятия риска у площадки и подтверждение фактами, каскад радиуса и причина по стоящей ступени, снимки отчёта и дедуп, эскалация радиуса и исчерпанный предел, ручная пара с доведением и снятием, счётчики происшествий и их разрезы, порядок и отсутствие следов; три находки припарковано | `записан` — `.claude/tests/cases/e2e-safety-teardown.md`, дайджест Д1713-Д1719 |
| 2026-09-16 | `CODE` (под-шаг 1, заход 21) | четвёртый документ кейсов **уровня 3** — сквозная тропа «токен → контекст тенанта → чтение и поток живых данных»: новая ось формы (тропа продуктового состояния не меняет вовсе, и отрицательное ожидание становится главным её клеймом; второй конец тропы — браузер, которого играет сам прогон, и у кейса потока появилось поле «чем кончилась подписка»; идентичность проверяется каждой стороной заново; радиус тенанта проверяется дважды и второй раз не наследует первого), владелец членств впервые сторона, а не стаб; вывод тенанта и заведение при нуле членств, кэш и его срок, чтение у пяти владельцев и команда пользователя, бюджет повторов, билет и подписка, идентичность и момент записи, две темы одним проводом, переигрывание и явный разрыв, оба радиуса, согласие показанного и прочитанного, отказы сторон и брокера, порядок и неизменность состояния; три находки припарковано | `записан` — `.claude/tests/cases/e2e-perimeter-read.md`, дайджест Д1720-Д1727 |
| 2026-09-16 | `CODE` (под-шаг 1, заход 22) | пятый и последний документ кейсов **уровня 3** — дым `smoke-live` против развёрнутого окружения и demo-счёта площадки: новая ось формы (мишень развёрнута чужим ходом, а прогон снаружи неё — сторон он не поднимает, базы ему недоступны, предусловие он читает, а не ставит; исход живой площадки есть множество, а не значение; след необратим, и teardown живёт внутри кейса; расхождение с апидоком — находка апидоку), вход именем хоста окружения, счёт демо-контура и подпись, рыночные данные и их контур, заявка выставленная и снятая, исполненная и закрытая, журнал и агрегаты, возврат счёта и то, что дым оставляет навсегда, дрейф площадки и отсутствие лишнего; четыре находки припарковано | `записан` — `.claude/tests/cases/smoke-live.md`, дайджест Д1728-Д1735 |
| 2026-09-16 | `CODE` (под-шаг 1, заход 23) | последний предмет перечня, форма не кейсовая — **ревизия существующего набора тестов**: каждый класс тестовых деревьев `services/**` получил механический признак, мерённый грепом, и исход из закрытого словаря шести значений; правило вывода исхода названо вместе с очерёдностью, отклонения от него — поимённо; население таблицы сверено с деревом командой, а не счётом; названа цена сноса класса `структура` — предметы, которых ящик не воспроизводит ни при какой расстановке; ни один тест не снят и не правлен; одна находка припаркована | `записан` — `.claude/work/progress/phase-2-step-12-existing-tests-revision.md`, `.claude/work/backlog.md` §«Классы общих артефактов проверяются из тестовых деревьев сервисов», дайджест Д1736-Д1742 |
| 2026-09-16 | `CODE` (под-шаг 2, заход 1) | первый предмет адверсариального ревью кейсов — чёрный ящик `auth`: форма, указатели, выбор уровня и порождающие категории проверены и сошлись; **апрув не дан** — девять находок, из них к автору кейсов шесть, владельцам предмета три. Ось, которой у под-шага 1 не было: перечень носителей находки, собранный по шаблону термина, пропускает носитель, выражающий то же другими словами, — снятый клейм об акторе живёт вторым носителем в javadoc, и условие закрытия его парковки этого носителя не берёт. Прочие оси: ожидание без носителя, ожидание, не выводимое из названного дома, ожидание вида «не упало», отрицательное ожидание, которое нечем наблюдать, непокрытая ось конфигурации, отсутствующая сторона границы, две мишени под одной меткой, сокращённый перечень истин предмета | `записан` — `.claude/work/progress/phase-2-step-12-review-test-auth.md` §«Находки» |
| 2026-09-17 | `CODE` (под-шаг 2, заход 2) | правка кейсов `auth` автором по девяти находкам ревью: каждая получила исход, четыре крена ревью финализированы, шесть новых кейсов (две расщеплённые метки, ось конфигурации, вторая сторона границы выборки, недостижимость терминальных рёбер), четыре находки уехали владельцам парковками, класс находки R-2 воспроизведён у двух чужих предметов и припаркован своим условием | `записан` — `.claude/work/progress/phase-2-step-12-fix-test-auth.md` §«Исход каждой находки» |
| 2026-09-17 | `CODE` (под-шаг 2, заход 3) | **перепроверка** исправленных кейсов `auth` — второй круг петли ревью: восемь находок первого круга из девяти закрыты по существу, форма, двадцать шесть указателей, двенадцать секций парковок и условие чужой парковки проверены и сошлись; **апрув снова не дан** — две находки. Ось, которой у первого круга не было: **правка, закрывающая находку, воспроизводит класс, снятый ею же** — кейс, заведённый по непокрытой оси конфигурации, несёт отрицательное ожидание, чей наблюдатель не назван ничем, а адрес умолчания в нём взят из описания среды держателя, а не из клиента (схема пропущена, и на ней держится весь исход). Вторая: провенанс в двух заведённых секциях бэклога, которого детектор не видит, потому что мерит одну письменную форму слова из двух | `записан` — `.claude/work/progress/phase-2-step-12-recheck-test-auth.md` §«Находки» |
| 2026-09-17 | `CODE` (под-шаг 2, заход 4) | правка кейсов `auth` по двум находкам второго круга плюс три мелких замечания: крен гейтящей находки разведён на две половины — ожидание сведено к наблюдаемому (ассерт по хранилищу **прогона** с названной различительной силой), а риск «прогон дотянулся до живого хранилища машины» закрыт **осью контура**: всякий ящик с Vault пришпиливает адрес целиком — `scheme`/`host`/`port`, а не один `uri`; посылка находки перепроверена по исходникам клиента, а не принята на слово; провенанс снят из двух секций, слепота детектора к второй письменной форме слова припаркована условием-прокси; предмет вернулся ревьюеру на третий круг | `записан` — `.claude/work/progress/phase-2-step-12-fix-test-auth-2.md` §«Исход каждой находки», дайджест Д1749-Д1753 |
| 2026-09-17 | `CODE` (под-шаг 2, заход 5) | **вторая перепроверка** кейсов `auth` — третий круг петли ревью: обе находки второго круга закрыты по существу, посылка о клиенте хранилища выведена независимо третий раз, литерал мёртвого адреса проверен по ядру клиента, достижимость предусловия доказана отсутствием фабрики `ConnectionDetails`; **апрув снова не дан** — три находки, из них две гейтящие. Ось, которой у второго круга не было: правка, закрывшая находку по существу, дала ей форму с невычислимым операндом, а соседняя клетка того же захода воспроизвела класс «названа половина адреса» | `записан` — `.claude/work/progress/phase-2-step-12-recheck-test-auth-2.md` §Находки, вердикт «браковка» |
| 2026-09-17 | `CODE` (под-шаг 2, заход 6) | правка кейсов `auth` по трём находкам третьего круга: ассерт B6.4 переведён на вычислимый префикс, B2.17 получил свой контейнер и свой контекст, радиус решения 5 расширен до сервиса, работающего с хранилищем; правило субстрата разрушающего кейса заведено осью контура, тот же класс у коннектора припаркован | `записан` — `.claude/work/progress/phase-2-step-12-fix-test-auth-3.md` §«Исход каждой находки», дайджест Д1754-Д1758 |
| 2026-09-17 | `CODE` (под-шаг 2, заход 7) | **третья перепроверка** кейсов `auth` — четвёртый круг петли ревью: все три находки третьего круга закрыты по существу (ассерт переведён на вычислимый префикс, разрушающий кейс выведен с общего субстрата, радиус решения 5 приведён к предмету, входящий §-адрес переведён), достижимость B2.17 выведена из конфигурации сервиса (`kv.enabled: false`, `authentication: TOKEN`), форма документа и §-адреса зелены; **апрув снова не дан** — две находки, обе гейтящие. Ось, которой у третьего круга не было: правка, снявшая зависимость ассерта от недоступного операнда, завела зависимость от **соседа по общему субстрату** — абсолютное отрицание над контейнером, чьё начальное содержимое предусловие не пинит, тогда как соседний кейс того же документа его пинит; вторая — популяция нового признака собрана шаблоном термина, и члены класса, выраженные не словом «Vault» («база недоступна», «контейнер брокера остановлен»), исхода не получили | `записан` — `.claude/work/progress/phase-2-step-12-recheck-test-auth-3.md` §Находки, вердикт «браковка» |
| 2026-09-17 | `CODE` (под-шаг 2, заход 8) | правка кейсов `auth` по двум находкам четвёртого круга плюс три мелких: начальное состояние общего контейнера внесено в предусловия B6.4 и B2.10 (крен (а) ревью — форма своя же, B2.1); признак оси субстрата расширен до «объявляет общий субстрат недоступным либо восстанавливает его», и кейс обязан называть механизм, а не исход, который видит сервис; у двух половин признака разведены доводы. **Популяция признака выведена по субстрату, а не по слову** — три формы поиска против одного шаблона термина, и сверх трёх членов отчёта ревью найдено ещё пять (`statistics` B3.12 и B11.11, `strategies` B7.4/B7.5 и B8.8, `bff` B5.3, `connector-okx` B9.3-S); каждый получил исход, карв-ауты названы поимённо (права, бин слушателя, процесс стороны, непоставленный контейнер). Предмет вернулся ревьюеру на пятый круг | `записан` — `.claude/work/progress/phase-2-step-12-fix-test-auth-4.md` §«Исход каждой находки», дайджест Д1759-Д1761 |
| 2026-09-17 | `CODE` (под-шаг 2, заход 9) | **четвёртая перепроверка** кейсов `auth` — пятый круг петли ревью: обе находки четвёртого круга закрыты по существу, популяция признака оси перепроверена встречной формой поиска и новых членов не дала, парковки зелены у `backlog-check`. **Апрув не дан** — три гейтящие находки и два мелких. Новая ось круга: правка закрывает находку по адресу, названному отчётом, а не по классу, который отчёт объявил, — `X-1` живёт в одиннадцати кейсах документа вместо двух (популяция выводилась по предусловиям, а класс задан ассертом), `X-2` не проведён по второму субстрату собственного документа. Третья находка своя: B6.4 не пинит имя окружения, умолчание у свойства пусто, писатель проверяет имя до обращения к клиенту хранилища — кейс тавтологичен | `записан` — `.claude/work/progress/phase-2-step-12-recheck-test-auth-4.md` §Находки |
| 2026-09-17 | `CODE` (под-шаг 2, заход 10) | правка кейсов `auth` по трём находкам пятого круга плюс двум мелким: B6.4 пинит имя окружения (`dev`) и перестал быть тавтологичным, отрицание содержимого хранилища переписано в **разностную** форму одной редакцией на весь документ (четырнадцать клеток, абсолютных ноль), клетки «в брокер ничего» сняты — дом отрицания B7.1; правило формы поднято в ось контура, класс в чужих документах кейсов припаркован | `записан` — `.claude/work/progress/phase-2-step-12-fix-test-auth-5.md` §«Исход каждой находки», дайджест Д1762-Д1767 |
| 2026-09-17 | `CODE` (под-шаг 2, заход 11) | **пятая перепроверка** кейсов `auth` — шестой круг петли ревью: все три находки пятого круга закрыты по существу (B6.4 пинит имя окружения, и посылка о порядке в писателе проверена по коду; отрицание содержимого хранилища разностное — встречная форма поиска новых членов не дала; брокер остался у своего дома B7.1), оба мелких исполнены. **Апрув не дан** — браковка шестая: обе новые находки того же механизма, что и пятый круг, на осях, которых он не называл. `Y-1` — правило разностной формы поднято в дом с радиусом «общий субстрат», а исполнено по одному субстрату из трёх: у общей **базы** 20 абсолютных клеток и 11 предусловий пиньнутой пустоты, при том что клейм «приведён к форме целиком» уже написан в трёх носителях. `Y-2` — редакция «ожидание о субстрате, которого у ящика нет, снимается» проведена по брокеру и не проведена по **стабам соседей**: четыре клетки держат форму, снятую B7.2, и одна из них расходится с ним же | `записан` — `.claude/work/progress/phase-2-step-12-recheck-test-auth-5.md` §Находки |
| 2026-09-17 | `CODE` (под-шаг 2, заход 12) | правка кейсов `auth` по двум находкам шестого круга плюс двум мелким: взят крен (а) `Y-1` — **все** клетки о содержимом общей базы переведены в разностную форму либо в радиус своих строк кейса, пиньнутая пустота снята у десяти предусловий, одиннадцатое (B3.2) переведено на свой контейнер, потому что предмет кейса и есть состояние всей таблицы; `Y-2` — четыре клетки о стабах соседей сняты со ссылкой на B7.2, пассаж §«Чем достаются выходы» приведён. Сверх названного: в дом контура внесён пассаж о **радиусе рода** (перебор по субстратам ящика, а не по адресам отчёта), клейм «приведён к форме целиком» снят в двух живых носителях и заменён указателем на команду, а команда вывода кандидатов в парковке чужих документов расширена словами базы и разбором предусловий — после расширения она печатает кандидатов в двенадцати документах. Предмет вернулся ревьюеру на седьмой круг | `записан` — `.claude/work/progress/phase-2-step-12-fix-test-auth-6.md` §«Исход каждой находки», дайджест Д1768-Д1774 |
| 2026-09-17 | `CODE` (под-шаг 2, заход 13) | **шестая перепроверка** кейсов `auth` — седьмой круг петли ревью: обе находки шестого круга закрыты по существу, разбор автора воспроизведён дословно (52 клетки, 28 разностных, 4 абсолютных попадания — все не члены, одно предусловие пиньнутой пустоты), посылки клеток о коде проверены по исходникам сервиса, форма документа цела. **Впервые за петлю правка не воспроизвела внутри своего предмета класс, которым была забракована.** **Апрув не дан** — три гейтящие находки и три мелких. Оси, которых у шестого круга не было: класс ушёл в **дом** — пассаж, поднявший обязанность перебирать род по субстратам ящика, закрыл перечень тремя, а его же иллюстрация считает четвёртый род (стаб), и у одиннадцати чужих документов он даёт 44 клетки снятой формы; правка ввела **второе основание** брать свой контейнер и оставила его в предмете, указав на дом, где признак один и он о разрушении субстрата; третья находка не этого класса — негатив «обязательный вход не предъявлен» покрыт двумя полями из семи и одним claim'ом из трёх при объявленном самим документом принципе метки на поле | `записан` — `.claude/work/progress/phase-2-step-12-recheck-test-auth-6.md` §Находки, вердикт «браковка» |
| 2026-09-17 | `CODE` (под-шаг 2, заход 14) | **седьмая правка** кейсов `auth` по трём гейтящим находкам седьмого круга и трём мелким. **Две правки — дома, не предмета:** перечень субстратов контура открыт до четырёх (стаб чужой поверхности внесён четвёртым членом с делением «сценарий ответа — вход кейса, счётчик обращений — накопленное состояние»); заведено **второе основание** брать свой контейнер — предмет кейса есть состояние субстрата целиком, — и у пиньнутой пустоты объявлены два исхода вместо одного. **Третья — полнота негатива:** принцип «метка на единицу» поднят в дом-скилл ПРИЗНАКОМ (единица — то, чья обязательность снимаема независимо и чьё снятие наблюдаемо снаружи), реестр выводится из кода, исход «не покрыта с доводом» объявлен законным; по реестру `auth` из десяти единиц заведены семь недостающих кейсов (B1.12, B1.13, B2.18-B2.22). Обе парковки перевооружены, заведены две новые; запись реестра снятых редакций — «перечень субстратов контура закрыт тремя» | `записан` — `.claude/work/progress/phase-2-step-12-fix-test-auth-7.md`, дайджест Д1775-Д1779 |
| 2026-09-17 | `CODE` (под-шаг 2, заход 15) | **седьмая перепроверка** кейсов `auth` — восьмой круг петли ревью: все три находки седьмого круга закрыты по существу (реестр обязательных входов воспроизведён дословно — 10 единиц, 7 кейсов заведено; разбор клеток — 59 клеток, 35 разностных, 4 абсолютных не-члена, одно законное предусловие пустоты; дома приведены, иллюстрация со счётом не расходится), посылки новых кейсов проверены по исходникам своим и контейнерным. **Апрув не дан** — три гейтящие находки и два мелких. Ось круга: забраковано то, что правка написала сама, и то, чего петля не трогала ни одним кругом. `Z-1` — пять новых кейсов написаны с той конвенцией предусловий, которую та же правка сняла у B2.6; категория несёт две конвенции, класс шире дельты (семь кейсов). `Z-2` и `Z-3` — один род: клетка утверждает о коде то, чего в коде нет — «другая ветвь предиката» у B1.12 при одном общем `if` и B1.9, не мерящий обязательности `iss` вовсе, потому что подпись проверяется до издателя | `записан` — `.claude/work/progress/phase-2-step-12-recheck-test-auth-7.md` §Находки, вердикт «браковка» |
| 2026-09-17 | `CODE` (под-шаг 2, заход 16) | **восьмая правка** кейсов `auth` по трём гейтящим находкам восьмого круга и двум мелким. Все три крена — (а). `Z-1`: семь предусловий категории «обязательный вход не предъявлен» переведены на форму «своих нет» — отказ производит `@Valid` до тела метода, и состояния базы он не видит; конвенция в документе стала одна (прогон команды ревью печатает ноль кейсов с названной зависимостью), а признак «предусловия называют только то, что меняет исход» поднят в дом-скилл вместе с областью обратного довода. `Z-2`: довод B1.12 переписан с несуществующей «второй ветви предиката» на **пустоту операнда**, и назван регресс, который ловит только он (`claim.equals(...)` вместо `Objects.equals` даёт `500` вместо `403`). `Z-3`: B1.9 пинит ключ подписи стаба, `iss` остался единственной расходящейся осью — прежняя редакция мерила механизм B1.7, потому что подпись проверяется до валидаторов; радиус проведён по всему классу «кейс о контуре токена» (B1.7, B1.8 правлены, три непокрытые оси названы с доводом). Мелкие: обе команды-доказательства перенесены из отчётов в сам документ (§«Команды разбора») — клейм постоянного носителя больше не опирается на файл, который уедет в `history/`; признак субстрата получил вторую половину, отсекающую сторону тропы механически. Предмет вернулся ревьюеру на девятый круг | `записан` — `.claude/work/progress/phase-2-step-12-fix-test-auth-8.md` §«Исход каждой находки», дайджест Д1780-Д1784 |
| 2026-09-17 | `CODE` (под-шаг 2, заход 17) | **восьмая перепроверка** кейсов `auth`: дельта правки по трём гейтящим находкам восьмого круга и двум мелким. Все три закрыты **по существу** — команда популяции печатает ноль кейсов с названной зависимостью, ветви `requireBrowserToken` и названный регресс проверены по коду, перечень валидаторов контура — по исходникам `JwtValidators` (тип, момент, отпечаток сертификата, издатель; подпись раньше них). Обе команды §«Команды разбора» прогнаны дословно из документа и совпали с прозой отчёта правки до члена. **Браковка девятая:** четыре гейтящие находки, три из них одного рода — класс закрыт у одного носителя и воспроизведён у соседнего тем же заходом. `W-1` пересказ признака предусловий в документе ложен на четырёх кейсах этого же документа; `W-2` перебор класса «кейс о контуре токена» остался в отчёте захода, а довод по `typ` опровергается доводом достижимости B1.9; `W-3` вторая половина клеток B2.21 и B2.22 противоречит их же строке «Ожидаемые выходы», и свип объявил клетки верными, прочитав в каждой половину; `W-4` команда-носитель клейма базового гейта не имеет и на неразобранном предмете печатает ноль с кодом `0`. Плюс три мелких. Предмет вернулся автору на десятый круг | `записан` — `.claude/work/progress/phase-2-step-12-recheck-test-auth-8.md` §Находки, §Вердикт |

| 2026-09-17 | `CODE` (под-шаг 2, заход 18) | **девятая правка** кейсов `auth` по четырём гейтящим находкам девятого круга и трём мелким; крены — (а), (б), (а), (а). Заведён кейс B1.14 на ось `typ`, признак отбора осей контура токена внесён в документ, клауза о теле снята, у команды 1 появился базовый гейт, команда 2 переведена на свой предмет; заведена находка F-9 и её парковка. *(Строка восстановлена заходом 19 по отчёту правки — своим заходом она записана не была.)* | `записан` — `.claude/work/progress/phase-2-step-12-fix-test-auth-9.md` §«Исход каждой находки» |
| 2026-09-17 | `CODE` (под-шаг 2, заход 19) | **девятая перепроверка** кейсов `auth` — десятый круг петли: три находки девятого круга из четырёх и все три мелких закрыты по существу; перечень валидаторов контура и тропа его подключения сверены заново по исходникам Spring Security 7.0.0 и автоконфигурации Boot 4, оба базовых гейта выдержали мутацию ревьюера (у команды 1 — вторую, правкой не предъявленную), перечень предусловий проверен по обеим осям. **Впервые за петлю правка не воспроизвела класс браковки ни у одного носителя.** **Браковка десятая** — одна гейтящая находка и три мелких: признак отбора осей контура токена взял единицей **ось**, а наблюдаемой единицей тропы является **валидатор**, и потому ложен в обе стороны — исключение `aud` держится свойством, которое есть и у подписи (у неё кейс есть), а сам признак требует кейса на `nbf`, которого нет и которого дом негатива единицей не признаёт; клейм «оси перебраны» ложен третьей осью. Своя находка захода (каталог проб `target/` уводит `retired-check.py` в код 2) припаркована | `записан` — `.claude/work/progress/phase-2-step-12-recheck-test-auth-9.md` §Находки, §Вердикт |
| 2026-09-17 | `CODE` (под-шаг 2, заход 20) | **десятая правка** кейсов `auth` — одиннадцатый круг петли: гейтящая находка закрыта креном (а) — признак переписан с единицей **исполнитель отказа на тропе разбора токена**, популяция названа кодом (декодер контура плюс перечень от `getValidators`) и выведена по исходникам заново, а признак **применён к каждому названному имени таблицей** (пять исполнителей, три оси, отпечаток) — расхождений нет ни одного против двух у прежнего. Три мелких закрыты в тех же двух носителях: названо, чем снимается исключение `aud` (свойство `audiences` либо бин валидатора — свойство конфигурации, не библиотеки); посылка F-9 сведена к «значение не названо», предположение о линии провайдера убрано и в документе, и в парковке; `закрыто-когда` секции F-9 переведено с подстроки на предмет, проба на трёх состояниях падающая. Своя находка захода припаркована: клейм буллета о негативе по единице шире того, что печатает команда 2 | `записан` — `.claude/work/progress/phase-2-step-12-fix-test-auth-10.md` §«Исход каждой находки», §«Применение нового признака к каждому названному члену и не-члену» |
| 2026-09-17 | `CODE` (под-шаг 2, заход 21) | **десятая перепроверка** кейсов `auth` — одиннадцатый круг петли: новый признак применён ревьюером к каждому названному имени самостоятельно и в своих границах сошёлся, популяция выведена по исходникам заново и совпала, обе команды документа прогнаны дословно и воспроизвели числа правки, свип снятых редакций в живых носителях пуст, гейт корпуса зелен. **Апрув не дан** — три гейтящие находки и три мелких. Класс воспроизведён уровнем выше: признак называет популяцию вместо того, чтобы её выводить, и в зазоре между объявленной единицей («исполнитель отказа на тропе разбора токена») и названной популяцией («декодер плюс перечень валидаторов») лежит `DefaultBearerTokenResolver` без исхода; мелкое девятого круга закрыто у `aud` и не проведено по `X509CertificateThumbprintValidator`, чья причина опирается на посылку о производителе токена; клейм о команде 2 найден самой правкой и припаркован, хотя блокером не является отсутствие носителя. Мелкие: регистронезависимость сверки `typ`, односторонняя граница у `закрыто-когда` F-9, разошедшийся счёт перепроверок | `записан` — `.claude/work/progress/phase-2-step-12-recheck-test-auth-10.md` §Находки, §Вердикт |
| 2026-09-17 | `CODE` (под-шаг 2, заход 22) | **одиннадцатая правка** кейсов `auth` — двенадцатый круг петли: три гейтящие находки и три мелких закрыты. Популяция признака **переведена из названной в выводимую** — звенья тропы разбора делятся на производящие отказ и передающие чужой, клетку получает каждое; `DefaultBearerTokenResolver` получил исход «без кейса» с доводом по коду (исход совпадает с декодерным на уровне контракта, а единый error-DTO различия не вернёт). Причина у `X509CertificateThumbprintValidator` переписана: предикат достижим, но только на токене, которого наш производитель не выпускает, — и у посылки о производителе заведён наблюдатель (секция бэклога, находка F-10). Клейм о команде 2 сведён к печатаемому, секция бэклога **перевооружена** на расширение команды. Заведена **команда 3** — точки конфигурации тропы разбора, прогнана на трёх состояниях (действительное 0, базовый гейт код 2, мутация ловится). Правка нашла и закрыла своё: регресс-клейм B1.14 был ложен наполовину — `createDefaultWithValidators` умолчаний не роняет. **Следующий ход по предмету — одиннадцатая перепроверка** (роль `reviewer`, заход 23) | `записан` — `.claude/work/progress/phase-2-step-12-fix-test-auth-11.md` |
| 2026-09-17 | `CODE` (под-шаг 2, заход 23) | **одиннадцатая перепроверка** кейсов `auth` — двенадцатый круг петли: класс, которым забракованы одиннадцать кругов подряд, **снят на уровне признака** — признак исполним чужой рукой, и перепроверка вывела тропу самостоятельно от фильтра контура, получив ту же форму; все три поправки правки сверены по коду и верны. **Апрув не дан** — три гейтящие находки и четыре мелких. Признак **применён не до конца**: два звена с клеткой «передаёт» отказ производят сами — фильтр на DPoP-привязанном токене (`cnf.jkt`, посылка та же, что у `x5t#S256`, а наблюдатель `F-10` её не покрывает) и провайдер на `JwtException`, не являющемся `BadJwtException`, чей исход перебрасывается мимо точки входа и даёт отказ контейнера без тела, притом достижим **субстратом прогона** (JWKS-стаб). Команда 3, заведённая наблюдателем расширения популяции, не печатает четырёх точек — двух DSL (проверено мутацией ревьюера) и двух свойств ветви декодера, из которых `public-key-location` удаляет `JwtIssuerValidator`, то есть исполнителя кейса B1.9. Вводный абзац §«Команды разбора» отстал на редакцию: «Два клейма… Обе воспроизводимы» при трёх командах и снятом втором клейме | `записан` — `.claude/work/progress/phase-2-step-12-recheck-test-auth-11.md` |
| 2026-09-17 | `CODE` (под-шаг 2, заход 24) | **двенадцатая правка** кейсов `auth` — тринадцатый круг петли: три гейтящие находки и четыре мелких закрыты. Признак **доведён до конца по каждому звену**: клетки приведены к тому, что звенья делают, — фильтр и провайдер получили двойную клетку «передаёт и производит», конвертер переведён в «передаёт», а в тропу добавлены два звена, которых в ней не было: `SupplierJwtDecoder` (производит свой `JwtDecoderInitializationException`) и `AuthenticationEntryPointFailureHandler` (развилка исхода, своего отказа не производит). Ветвь DPoP получила исход «без кейса» с посылкой о производителе и **своим** наблюдателем — секцией бэклога; ветвь `AuthenticationServiceException` получила **кейс B1.15**, разведённый с B1.7 по коду `nimbus-jose-jwt-10.4` (наследники `JOSEException` → `JwtException`, `BadJOSEException` → `BadJwtException`), — эта линия распакована впервые за петлю. Команда 3 расширена до трёх классов точек (подмена звена, пополнение перечня, выбор ветви декодера) и на мутации ловит все четыре точки, которых не брала; прогнана на трёх состояниях. Вводный абзац §«Команды разбора» приведён к трём командам без чисел о себе. **Свип нашёл и закрыл своё:** клейм «сегодня он печатает ноль» в §«Чем достаются выходы» отстал от расширенной команды. **Следующий ход по предмету — двенадцатая перепроверка** (роль `reviewer`, заход 25) | `записан` — `.claude/work/progress/phase-2-step-12-fix-test-auth-12.md` |
| 2026-09-17 | `CODE` (под-шаг 2, заход 25) | **двенадцатая перепроверка** кейсов `auth` — тринадцатый круг петли: дельта правки прочитана по исходникам заново, и две линии распакованы впервые за петлю (`spring-security-core-7.0.0`, `spring-boot-security-4.0.0`). Сошлось существенное: иерархия исключений `nimbus-jose-jwt-10.4`, на которой стои́т разведение `B1.15` с `B1.7`; достижимость `B1.15` неизвестным `kid` (вторичный заход `JWKSetBasedJWKSource` плюс отсутствие терпимости к отказу у сборки Spring); переброс `AuthenticationServiceException` обработчиком отказа и порядок фильтров; приоритет трёх свойств ветви декодера; три команды документа прогнаны дословно и совпали до члена. **Апрув не дан** — три гейтящие находки и четыре мелких. `W-1`: признак «клетку получает каждое звено» снова не доведён до конца — `ProviderManager` стои́т между фильтром и провайдером, производит свой `ProviderNotFoundException` и может перерешить исход через родителя, а в тропе не назван ни разу, притом его подмену объявляет наблюдателем команда 3 того же документа. `W-2`: клетка подтверждения `B1.15` опровергает сама себя — цитирует дом, фиксирующий код числом (`500`), и следующей фразой отрицает существование такого дома; аналогия с `B1.13` не держится, потому что у соседа отказ другого класса. `W-3`: расширенная команда 3 не печатает трёх точек своего объявленного радиуса — проверено **своей** мутацией (бин и точка DSL `AuthenticationProvider`, бин `JwkSetUriJwtDecoderBuilderCustomizer`: внесено три, напечатано ноль), и §«Чего она не видит» их не называет. **Следующий ход по предмету — тринадцатая правка автором** (роль `tester`, заход 26) | `записан` — `.claude/work/progress/phase-2-step-12-recheck-test-auth-12.md` §Находки, §Вердикт |
| 2026-09-17 | `CODE` (под-шаг 2, заход 26) | **тринадцатая правка** кейсов `auth` — четырнадцатый круг петли: три гейтящие находки и четыре мелких закрыты. В тропу вставлены **два звена между резолвером заголовка и провайдером** — резолвер менеджера (лямбда, передаёт) и `ProviderManager` (двойная клетка: копит чужой отказ и бросает его после обхода перечня, а производит свой `ProviderNotFoundException`); названы ход к родителю, которым исход может быть перерешён, и **доказательство** того, что родитель пуст (условие `MissingAlternativeUserDetailsManagerOrUserPropertiesConfigured` требует отсутствия `OpaqueTokenIntrospector`, а стартер ресурс-сервера его приносит). Развилка `W-2` закрыта **числом, а не парковкой**: оба рода пассажа дома дают `500`, фраза «код ответа ни одним домом не зафиксирован» снята как ложная, аналогия с `B1.13` заменена разведением по механизму. Команда 3 получила **шесть** новых классов, и перечень её классов объявлен **выводимым из тропы**, а не вспомненным; остаток назван родом (точка, не называющая типа звена), а не объёмом; все шесть доказаны — пять мутацией, шестой обеими сторонами. Мелкое 4 оказалось крупнее замечания: цепочка Boot зарегистрирована на **все** типы диспетчеризации, `ERROR`-диспетч повторно входит в тот же фильтр, и до сборщика тела тропа `B1.15` не доходит ни при каком его виде — заведены находка `F-11` и секция бэклога. **Свип нашёл и закрыл своё:** снятая фраза стоя́ла ещё у двух кейсов одним текстом и сужена у обоих без правки их ожиданий. **Следующий ход по предмету — тринадцатая перепроверка** (роль `reviewer`, заход 27) | `записан` — `.claude/work/progress/phase-2-step-12-fix-test-auth-13.md` |
| 2026-09-17 | `CODE` (под-шаг 2, заход 27) | **тринадцатая перепроверка** кейсов `auth` — четырнадцатый круг петли: браковка четырнадцатая, три гейтящие находки и три мелких. Класс новый и тяжелее прежних — две находки прошлого круга закрыты **фактами, которых в коде нет**: родитель `ProviderManager` на нашей сборке `null` (`AuthenticationConfiguration#getAuthenticationManager` возвращает `null` тремя звеньями подряд), а `ERROR`-диспетч в `BearerTokenAuthenticationFilter` **не входит** (`OncePerRequestFilter#skipDispatch` плюс `shouldNotFilterErrorDispatch()` умолчанием `true`) — то есть число `500`, пиньнутое в ожидание `B1.15`, `F-11` и секция бэклога стоя́т на опровергнутых посылках. Третья находка прежнего класса: клейм «исчезновение наблюдаемо счётом класса, упавшим в ноль» — нулей команда 3 не печатает. Сошлись обе половины двойной клетки `ProviderManager`, иерархия исключений, шесть новых классов команды 3 и все три команды документа дословно | `записан` — `.claude/work/progress/phase-2-step-12-recheck-test-auth-13.md`, вердикт «браковка» |
| 2026-09-17 | `CODE` (под-шаг 2, заход 28) | **решение держателя: критерий апрува документа кейсов и граница петли** — валидность кейса как единственный гейт апрува, пробел покрытия / пробел дока / форма круга не дают, один документ — не больше трёх кругов, третий без апрува — эскалация с остатком; дом правила — `.claude/skills/test-review.md`, дефицит самопроверки автора закрыт таблицей сверки в отчёте передачи (`.claude/skills/test-design.md`). **Первое применение — `auth`, апрув дан:** из 61 кейса невалидным по критерию был один — B1.15 (число `500` пиньнуто на опровергнутой посылке), переведён в форму класса исхода с числом как предметом прогона; четыре кейса валидны как помеченные пробелом дока (B2.6-половина, B2.15, B4.2, B5.7 — находки F-4, F-5, F-6 и парковка справочника); прочие 56 валидны без правки. Вне кейсов тем же заходом: тропа `ProviderManager` приведена к исходникам (родитель `null`, три факта, наблюдатель — три класса команды 3), F-11 и секция бэклога переписаны под проверенный механизм `ERROR`-диспетча и секция переименована, команда 3 печатает нули и класс `UserDetailsService`, заведён раздел «Пробелы покрытия» с G1-G4 (два — под-шаг 3, два — бэклог). Все три команды документа прогнаны дословно. **Следующий ход — второй предмет перечня под-шага 2** (`connector-okx`) по новому критерию | `записан` — `.claude/work/progress/phase-2-step-12-approve-test-auth.md`, `.claude/decisions/case-review-approval-criterion.md`, дайджест Д1785-Д1789, вердикт «апрув» |
| 2026-09-17 | `CODE` (под-шаг 2, заход 29) | **апрув кейсов `connector-okx` по критерию валидности** — второй предмет перечня, первый круг документа. Гейтящая находка одна и системная: двадцать два кейса пинили HTTP-число (`422`/`502`/`503`), которого **не фиксирует ни один дом** — `docs/components/IntegrationService.md` §«Классы отказа на границе — дом здесь» и `docs/rules/error-handling-policy.md` §«Внешняя поверхность» объявляют коды провизорными, javadoc `GlobalExceptionHandler` повторяет то же, и такой кейс краснел бы на исправной системе ровно ходом выравнивания. Ожидания переведены на **класс отказа** (поле `code` закрытого перечня дома), число отдано прогону; заведена секция §«Число ответа и класс отказа — разные ожидания». Числа, написанные не нами (`200`, `401`, `405`, `404`), оставлены с домами. Сверх того: перебор перечней дома дал два пробела покрытия (постановка algo и постановка со встроенной защитой — G1, G2, под-шаг 3) и две находки владельцам (F-5 — формы prehash подписи в корпусе нет; F-6 — четыре живых дока называют снятый класс донора `OkxIntegrationService`), обе с секциями бэклога. **Все 90 кейсов валидны, апрув дан**; под-шаг 3 по `connector-okx` открыт. | `записан` — `.claude/work/progress/phase-2-step-12-approve-test-connector-okx.md` |
| 2026-09-17 | `CODE` (под-шаг 2, заход 30) | **апрув кейсов `market-data` по критерию валидности** — третий предмет перечня, первый круг документа. Гейтящих находок три. Первая — тот же системный класс, что у двух предыдущих предметов: десять кейсов пинили HTTP-число, которое пишет **наш собственный** `GlobalExceptionHandler`, при доме, объявляющем перечень кодов вопросом без ответа (`docs/rules/error-handling-policy.md` §«Внешняя поверхность»); ожидания переведены на класс отказа, число отдано прогону, признак «кто число написал» записан секцией §«Число ответа и класс отказа — разные ожидания». Отличие от `connector-okx` названо: у `market-data` перечня классов **дома нет вовсе** — находка F-6. Вторая — B2.6 обещал обход правил на пустом окне, тогда как тик только возвращает курсор и выходит. Третья — B9.2 ждал пограничного класса там, где перевода не происходит: несобираемый адрес соседа отказывает до вызова (F-8). Сверх того: перебор рёбер цикла единицы сбора дал три расхождения дома с построенным (`BACKFILL → SYNC` без писателя, `BACKFILL → CHECK` без дома, `DELETED` без писателя — F-7), три пробела покрытия (G1–G3, под-шаг 3) и F-9 (javadoc `JobController` адресует несуществующий класс охраны). Четыре новые секции бэклога. **Все 114 кейсов валидны, апрув дан**; под-шаг 3 по `market-data` открыт, следующий предмет под-шага 2 — `trading-core`. | `записан` — `.claude/work/progress/phase-2-step-12-approve-test-market-data.md` |
| 2026-09-17 | `CODE` (под-шаг 2, заход 31) | **апрув кейсов `trading-core` по критерию валидности** — четвёртый предмет перечня, первый круг документа. Гейтящих находок три: восемь кейсов пинили `400` нашего же обработчика при доме, называющем перечень кодов вопросом без ответа (тот же системный класс, что у трёх предыдущих предметов); B3.5 приписывал СДЕЛКЕ исход, который дом приписывает ДЕЙСТВИЮ; B12.7 стоял на недостижимом предусловии — `auth` не зовёт ни одна читающая точка поверхности. Плюс две новые находки владельцам (F-4 — отказ соседа по ярусу уводит сделку в `ERROR` вопреки дому; F-5 — перечня классов отказа поверхности нет ни в одном доме), три пробела покрытия G1–G3 и две новые секции бэклога. Все 166 кейсов валидны, апрув дан | `записан` — `.claude/work/progress/phase-2-step-12-approve-test-trading-core.md` |
| 2026-09-17 | `CODE` (под-шаг 2, заход 32) | **апрув кейсов `strategies` по критерию валидности** — пятый предмет перечня, первый круг документа. Гейтящая находка одна, и это тот же системный класс, что у четырёх предыдущих предметов: шестнадцать кейсов пинили число ОТКАЗА, которое пишет наш собственный код — `ResponseStatusException(HttpStatus.…)` на месте броска либо ветвь обработчика, — при доме, объявляющем перечень кодов вопросом без ответа. У этого предмета класс отказа один на все броски (`STRATEGY_REQUEST_REJECTED`), поэтому ожидание переведено на ПАРУ «класс плюс реджект-код текста», а не на класс; и названа асимметрия, которой у соседей не было: **`400` на создании фиксирует заголовок дома валидации**, а у активации числа нет вовсе. Две новые находки владельцам: F-5 — числовые ключи базы владельца едут в снимке определения и у читателя ложатся ключами ЕГО строки (сверено на обоих концах провода); F-6 — у ненайденности в контексте тенанта нет ни дома, ни реджект-кода, и правило живёт только в javadoc. Раздела «Пробелы покрытия» у документа не было — заведён: G1 (17 реджект-кодов валидатора из 27 без прямого кейса, и документа уровня 2 у валидатора нет вовсе), G2 (реджект источника цены объявлен домом и в коде отсутствует — ждёт держателя), G3 (`403` недостижим по посылке одного субъекта). Две новые секции бэклога, две правки формы без круга. **Все 83 кейса валидны, апрув дан**; под-шаг 3 по `strategies` открыт, следующий предмет под-шага 2 — `audit`. | `записан` — `.claude/work/progress/phase-2-step-12-approve-test-strategies.md` |
| 2026-09-17 | `CODE` (под-шаг 2, заход 33) | **апрув кейсов `audit` по критерию валидности** — шестой предмет перечня, первый круг документа. Гейтящих находок две. Первая — шестое подряд воспроизведение системного класса: четыре кейса журнальной выборки пинили `400`, который пишет ОДНА ветвь нашего обработчика (`onQueryRejected`) на все поводы; реджект-кодов у предмета нет вовсе, поэтому ожидание переведено на пару «класс `QUERY_NOT_ACCEPTED` плюс повод, названный домом выборки», а число ушло в «Факт». Вторая — своя у предмета: B4.6 стоял на предусловии «ожидания по партиции нет», которого на тропе назначения не бывает — слушатель ребалансировки сажает ожидание на каждой из трёх ветвей, а взято оно было из javadoc трекера смещений. Три новые находки владельцам: F-5 — четвёртая ветвь сравнения смещений (брокер не отдал наименьшего доступного) дома не имеет и ожидания не сажает, то есть слепит второй наблюдатель разрыва без durable-следа; F-6 — мёртвая копия перечня кодов ручной операции у `audit` (читателя в сервисе нет ни одного, а пин слова провода обязан стоять у писателя формы); F-7 — javadoc выборки объявляет три отвержения вопроса, а их четыре. Раздела «Пробелы покрытия» у документа не было — заведён: G1 (`INTERNAL_FAILURE` без прямого кейса), G2 (край свежести «возраст ровно допустимый» при объявленном несущим нестрогом сравнении), G3 (предмета уровня 2 у `audit` не назначено ни одного при 50 кандидатах команды — тот же класс, что G1 у `strategies`), G4 (скалярное содержимое принимается наравне с документом, и ветви у этого нет ни в доме, ни в кейсах). Три новые секции бэклога, две правки формы без круга. **Все 127 кейсов валидны, апрув дан**; под-шаг 3 по `audit` открыт, следующий предмет под-шага 2 — `statistics`. | `записан` — `.claude/work/progress/phase-2-step-12-approve-test-audit.md` |
| 2026-09-17 | `CODE` (под-шаг 2, заход 34) | **апрув кейсов `statistics` по критерию валидности** — седьмой предмет перечня, первый круг документа. Гейтящих находок три. Первая — **седьмое подряд** воспроизведение системного класса: шесть кейсов агрегатной выборки пинили `400`, который пишет одна ветвь нашего обработчика (`onQueryRejected`) на все шесть поводов; реджект-кодов нет, поэтому ожидание переведено на пару «класс `QUERY_NOT_ACCEPTED` плюс повод», а число ушло в «Факт». Отличие от соседа названо: дом ограничений отбора называет **четыре** повода из шести, и у двух кейсов носителем законно стал предикат формы вопроса (критерий допускает код при молчании дока). Вторая — B4.6 стоял на предусловии «ожидания по партиции нет», которого на тропе назначения не бывает; тот же дефект и тот же javadoc, что найдены у соседа заходом 33, — второй носитель одной формы. Третья, нового рода: B7.13 обещал запрос группировки на каждые сутки окна у каждого зерна, а охрана начала ряда его гасит — на описанном предусловии красный прогон не различал бы «проход стал диапазонным» и «охрана сработала»; предусловие сужено до состояния, где предмет не подменяется. Три находки владельцам: F-5 и F-7 припаркованы **расширением существующей секции** вторым носителем (дом формы приёма сквозной, экземпляров у него столько, сколько durable-потребителей); F-6 — новая секция: порядок границ окна не назван домом **ни одной** читающей поверхности, включая журнальную, где заход 33 этого не заметил. Раздела «Пробелы покрытия» у документа не было — заведён: G1 (`INTERNAL_FAILURE` без прямого кейса и без упоминания), G2 (край свежести «возраст ровно допустимый»), G3 (четвёртая ветвь сравнения смещений — ждёт дома по F-5), G4 (предмета уровня 2 у `statistics` не назначено ни одного при **63** кандидатах команды — **третий предмет подряд** тем же классом), G5 (узел содержимого не того типа). Одна новая секция бэклога, одна расширена; правок формы не потребовалось. **Все 168 кейсов валидны, апрув дан**; под-шаг 3 по `statistics` открыт, следующий предмет под-шага 2 — `bff`, последний по перечню уровня 1. | `записан` — `.claude/work/progress/phase-2-step-12-approve-test-statistics.md` |
| 2026-09-17 | `CODE` (под-шаг 2, заход 35) | **апрув кейсов `bff` по критерию валидности** — восьмой и ПОСЛЕДНИЙ предмет уровня 1, первый круг документа. Гейтящих находок десять — больше, чем у любого предшественника, и причина названа: у периметра классов отказа четыре, а не один, и ошибки кейсов расслоились по ним. N-1 — **восьмое подряд** воспроизведение системного класса, и замер у него здесь сильнее: греп по всему телу `docs/architecture/contracts.md` — дома всей конструкции периметра — не даёт **ни одного** HTTP-числа, то есть молчание дома измерено, а не предположено; восемь кейсов переведены на класс (и повод, где он есть). У охраны роутера повода нет вовсе (бросок без пояснения), и различителем стало **отсутствие исходящего вызова**. N-2 — B9.2 ждал `404` на съёме метрик БЕЗ предъявления, а умолчание цепочки закрыто: путь отвечает контуром, и кейс проверял обратное соседнему B9.1; вход разведён надвое (без токена и под токеном). N-3 — B9.6 называл отказами контейнера три входа из четырёх, которые перехватывает роутер `/api/v1/{owner}/**` (он принимает ЛЮБОЙ глагол), а четвёртый — «тело, не разбирающееся контейнером» — недостижим: разбирающей тело точки у периметра нет ни одной. N-4 — классов девять (6+3), а форм восемь; «восемь классов, каждый своей формой» противоречило соседнему B7.3. N-5…N-10 — «вчетверо» против втрое (кейс расходился со своим же долгом в бэклоге), «два значения в билете» против трёх, «идентичности нет ни в теле» против пустого компонента, дом-носитель, говорящий обратное (ноль членств дом называет заведением, а не отказом), два значения из пяти, не доходящих до охраны имени, и пустой перечень тем, который `split` превращает в одну тему с пустым именем. Две новые находки владельцам: F-7 — дом перечисляет состав билета у́же построенного и при этом сам требует снятого поля; F-8 — механизм из N-10. Шесть находок предыдущего захода (F-1…F-6) перечитаны против кода и подтверждены все. Раздела «Пробелы покрытия» не было — заведён (G1…G7); существенное — G3: предмета уровня 2 у `bff` не назначено ни одного при **28** кандидатах, **четвёртый предмет подряд** тем же классом, и это закрывает уровень 1. Две новые секции бэклога. **Все 108 кейсов валидны, апрув дан**; под-шаг 3 по `bff` открыт. **Уровень 1 перечня закрыт целиком — восемь предметов из восьми.** | `записан` — `.claude/work/progress/phase-2-step-12-approve-test-bff.md` |
| 2026-09-17 | `CODE` (под-шаг 2, заход 36) | **развилка перечня предметов уровня 2 закрыта расширением** — единица не документ кейсов, а правка решения (`.claude/decisions/test-contour-design-pass.md` §«Признак предмета уровня 2 — две ступени, и вторая обязательна»). Признак получил вторую ступень — «перебор, которого ящик не берёт», читаемую по документу ящика; перечень расширен с восьми предметов до четырнадцати. Вход развилки починен той же единицей: у команды кандидатов снята граница слова слева у типовых маркеров (827 кандидатов было ложным счётом, стало 720) и заведён второй столбец «точка ветвления» — обе группы форм объявлены в шапке и доказаны батареей из 17 осей, исполняемой тем же прогоном; каждая форма ветвления имеет свою падающую пробу. Замер: 959 классов, 720 кандидатов, 167 с ветвлением, из них 83 не накрыты ни одним предметом прежнего перечня — шесть новых берут 44, один уже брал `trading-core-risk`, 38 остаются ящику четырьмя классами с доводом. Пробелы G у `strategies`, `audit`, `statistics` и `bff` получили исход в своих документах; секция бэклога о слепоте команды закрыта, секция об общих артефактах перевооружена на под-шаг 3 | `записан` — `.claude/work/progress/phase-2-step-12-level-2-list-pass.md` |
| 2026-09-17 | `CODE` (под-шаг 2, заход 37) | **апрув кейсов `strategy-engine-calculation` по критерию валидности** — первый предмет **уровня 2**, первый круг документа. Гейтящих находок семь, 62 строки из 152. Первая — девятое подряд воспроизведение системного класса, и предмет у него другой: пинится не HTTP-число, а **значение внутреннего контракта** (код отказа — 41 строка, сторона округления цены — 12, значение перечня результата — 4), и дом у такого значения законно бывает кодом; дефект в том, что клетка называла пассаж, устанавливающий **основание** отказа, и молчала о звене, где живёт само значение. Закрыто секцией документа «Дом пиньнутого значения…» с признаком, который объявлен не ею, а самим `CalculationErrorCodes`. Остальные шесть: носитель, говорящий противоположное (§Округление против market-like тропы); утверждение о цене, подтверждённое разделом о размере; «полусумма лучших цен» у дока, где середины спреда нет ни словом; «доля сто процентов по имени типа» у дока, который её не устанавливает; вход, не производящий ожидаемого отказа (базовая сборка считает первую ступень, а отказ — только у последней); достижимость, помеченная по одному рубежу из двух — второй рубеж есть **отбор исполнителя**, и калькулятор внедрён лишь у двух исполнителей ядра, оба требующих `CREATE_ACTION`. Арифметика **всех** пиньнутых чисел пересчитана вручную — расхождений ноль. Пять находок владельцам заведены заново (F-7…F-11), шесть прежних подтверждены чтением, шесть пробелов покрытия записаны разделом документа | `записан` — `.claude/work/progress/phase-2-step-12-approve-test-strategy-engine-calculation.md` §Вердикт |
| 2026-09-18 | `CODE` (под-шаг 2, заход 38) | **апрув второго документа кейсов уровня 2** — интерпретатор условий движка стратегий. Системный класс сменил предмет: у расчётного слоя бездомным было пиньнутое ЗНАЧЕНИЕ, здесь бездомен САМ ПРЕДИКАТ, и объявляет это корпус — `docs/rules/strategy-condition-contract.md` называет восемь типов правил из семнадцати «без дома». Тридцать клеток ссылались на эти доки как на носителей ожидания, включая **выдуманную цитату** («шаг применим, когда истинны все» — фразы в корпусе нет). Закрыто секцией §«Дом предиката…» с одиннадцатью классами и названным звеном. Второй класс — достижимость: перечень «охран второго рубежа» был собран по памяти, и вывод его **от валидатора** дал тринадцать неотмеченных охранённых входов и одну отметку на входе, который создание пропускает (документ противоречил собственной парковке F-6). Ещё: базовая сборка группы порогов отвергалась созданием; три строки не называли выхода вовсе; одна ждала истины на входе, дающем ложь; утверждение об отказе создания по неподдержанному источнику цены ложно на обоих носителях. Все девять припаркованных находок под-шага 1 подтверждены чтением; F-8 перевооружена с двух троп на четыре (пустой оператор роняет и пересечение, и равенство структуры — при том что сравнение ту же охрану несёт), кейсы U5.12 и U7.14 заведены. Семь пробелов покрытия записаны новым разделом документа | `записан` — `.claude/work/progress/phase-2-step-12-approve-test-strategy-engine-condition.md` §Вердикт |
| 2026-09-18 | `CODE` (под-шаг 2, заход 39) | **апрув третьего документа кейсов уровня 2** — слой статусных машин сделки и транша. Системный класс сменил предмет **третий раз подряд**: у расчётного слоя бездомным было пиньнутое ЗНАЧЕНИЕ, у интерпретатора — САМ ПРЕДИКАТ, здесь — **правило, связывающее две половины выхода**. Документ вводил ось блок-цитатой «отвергнутое ребро команд прохода не отменяет», подписанной компонент-доком машины сделки; в этом доке фразы нет ни одним словом — правило живёт в javadoc двух классов, то есть стыковое решение, которое читают девять обработчиков, дома в корпусе не имеет (F-7). Гейтящих находок семь, одиннадцать строк плюс цитата. Крупнейшие три: «переход пустой» у трёх строк координированного выхода при том, что проход доходит до затребования финализации — и СОСЕДНИЕ строки той же группы это называют; истина, объявленная на входе, где поштучный конъюнкт живого риска ложен (экспозиции `3` и `−3`); вход, которого не производит ни один писатель (сворачивающаяся сделка с аварийной причиной — писателей четыре, проверено грепом). Ещё: отсутствие записи объявлено там, где машина её пишет на любом отклонении; одно имя `cleanTerminalContract` у спеки и у гейта называет разные конъюнкции (F-8); базовая сборка не пинила провенанс числа, и второй дизъюнкт аварийного контракта не был предъявлен ни одной строкой; счёт существующего набора написан рукой и неверен (13 против «четырнадцати»). **F-4 перевооружена**: обработчиков с неиспользуемым коллаборатором диспозиции два, а не один, и парковка опиралась на неверный счёт зовущих (три из шести, а не пять). **F-6 заведена** — затребованная каскадом ступень теряется на двух исходах прохода активной сделки из пяти, направление разрешающее. Кейсов стало 314 против 311; заведён раздел «Пробелы покрытия» (G1-G6) | `записан` — `.claude/work/progress/phase-2-step-12-approve-test-trading-core-fsm.md` §Вердикт |
| 2026-09-18 | `CODE` (под-шаг 2, заход 40) | **апрув кейсов `trading-core-risk`** — четвёртый предмет уровня 2. Системный класс сменил НАПРАВЛЕНИЕ: у трёх соседей носитель молчал, здесь он говорит — и говорит противоположное коду. Дом якоря себестоимости называет запрещённую конструкцию поимённо («ветвь, а не coalesce»), а преконтроль делает именно coalesce; предикат живого эпизода объявлен двумя обязательными конъюнктами, а резолв берёт строку по третьему — «живой риск ЛИБО статус активен». Обе клетки документа описывали КОД и подписывали его ДОМОМ, то есть закрепляли расхождение зелёным кейсом (R-6). Ещё: вход, которого не бывает, — предикат модели над статусом пустым не бывает, а пустой счёт роняет проход раньше проверки базы; сборка группы признака живого риска не пинила членство кода в карв-ауте, хотя бессрочность и карв-аут пересекаются на четырёх кодах из двадцати одного и четырнадцати; два входа не определяли выхода; эффект разрешающей реакции с предупреждением приписан §Назначение дока, который его не называет, — живёт он в javadoc (R-7); «живая входная нога» отбирается двумя разными предикатами в одном пакете, и у второй точки входа завышение даёт ложную находку детектора (R-8). **R-2 перевооружена** — предмет у неё расхождение ТРЁХ домов, а не «дом объявил, код не исполнил»: дом кодов недостижимость предупреждения объявляет прямо. **Перечень красных кейсов сведён к действительным**: два из четырёх были зелены. Заведён раздел «Пробелы покрытия» (G1-G4) | `записан` — `.claude/work/progress/phase-2-step-12-approve-test-trading-core-risk.md` §Вердикт |
| 2026-09-18 | `CODE` (под-шаг 2, заход 41) | **апрув кейсов `trading-core-safety`** — пятый предмет уровня 2. Системный класс сменил УРОВЕНЬ: расходятся не дом с кодом, а **дом с домом**, и обе стороны стоя́т в одном и том же документе кейсов. Координатор объявляет, что доведение недоделанного второй строки отчёта не заводит, и объясняет это ключом дедупа; U9.10 того же документа верно утверждает, что у критичной тропы — той самой, которая эту строку и заводит, — дедупа нет вовсе. То же у гейта полноты прохода: §«Гейт полноты среза» безоговорочно объявляет, что детектор журнальную счётную фабрику не зовёт, а `A10` того же дока требует строку, которую записать больше нечем (S-7, прогоном не ловится — кортежи фабрик неразличимы). Находки валидности: отрицание шире дома и кода («иного вызова между шагами нет» при двух записях статуса и чтении популяции каскада); «каскад ничего не зовёт» там, где выборка и резолв причины зовутся и отдают пусто; выход доведения назван без открытого отчёта; ожидание подписано §Фабрики, а живёт в §Структура; перехват по счёту выдан за дом, хотя ни один пассаж его не называет — заведён раздел звеньев кода; отказ ребра не называл остающуюся строку журнала, которую дом объявляет достижимым пределом наблюдаемости. Клейм о собственном способе наблюдения («лог — ровно там, где он весь эффект») оказался у́же восьми своих кейсов. Два кандидата в пробелы проверены и СНЯТЫ: форма запуска накрыта чёрным ящиком, рёбра из `ERROR` энфорсера не требуют по объявлению дома | `записан` — `.claude/work/progress/phase-2-step-12-approve-test-trading-core-safety.md` §«Находки и их классы» |
| 2026-09-18 | `CODE` (под-шаг 2, заход 42) | **апрув кейсов `okx-mapping`** — шестой предмет уровня 2. Системный класс сменил НОСИТЕЛЬ: расходятся не два дома и не дом с кодом, а **объявление маппера и его порождённая реализация** — ожидания были списаны с таблицы `@Mapping`, а исполняет их файл, который печатает процессор. Два класса невалидности читаются только там: охрана пустого входа у перехода с двумя источниками **конъюнктивна**, а выражения стоя́т вне неё (семь строк, три из них — `NullPointerException` вместо объявленной пустоты), и радиус конвенции «пустая строка — пустота» есть формы РАЗБОРА, тогда как строковое поле переносится как есть (десять строк; документ различие знал и в этих клетках не применил). Прочие гейтящие: вход, которого форма источника не несёт; утверждение о составе снапшота, которого у него нет; отрицание шире кода и шире своего же документа; кейс, объявивший контрактом дефект, при двух соседях на том же пассаже дома, помеченных «не прогоняется»; цитата, приписанная не тому файлу, и дом группы, процитированный шире радиуса (буквальное чтение делало бы все кейсы группы красными). **Самая дорогая находка — владельцу и из порождённого файла:** тип заявки переносится в доменный перечень через `Enum.valueOf`, и материализация роняет `IllegalArgumentException` на каждом реальном ответе площадки; ни один прогон этого не видит — единственный тест тропы подменяет все мапперы (`M-8`, плюс новый красный кейс `U21.18`). Заведены `M-6`-`M-8` секциями бэклога и раздел «Пробелы покрытия» (`G1`-`G2`), которого у документа не было; звенья кода без дома собраны в свою секцию (`Z1`-`Z3`); счёты `snapshot` и форм манглинга правлены. Кейсов стало 423 (было 422). Отчёт — `.claude/work/progress/phase-2-step-12-approve-test-okx-mapping.md` |
| 2026-09-18 | `CODE` (под-шаг 2, заход 43) | **апрув кейсов `market-data-indicators`** — седьмой предмет уровня 2, первый круг документа. Системный класс воспроизвёлся пятнадцатый раз и сменил НОСИТЕЛЬ ещё раз: у этого предмета расходятся **документ и дом, который он сам цитирует**. Раздел «Четвёртая половина оси» объявлял, что выведение прогрева живёт только в javadoc, — а `docs/rules/strategy-condition-contract.md` §«Прогрев выводится, override возможен» задаёт его классом («оконные — период, рекурсивные — кратно периоду, составные — от старшего периода»), и на этот же пассаж документ ссылается тремя строками выше: два кейса стоя́ли непрогоняемыми на ложном основании (`N-4`), третий приписывал число `§Прогрев`, где его нет (`N-3`). Второй класс — **зелёный кейс объявляет контрактом собственный припаркованный дефект**: `U13.5` и `U13.4` списывали ожидание с реализации там, где сам документ парковал это расхождение находками `M-4` и `M-3` для соседних носителей (`N-2`, `N-8`). Третий — **вход не достигает состояния, которое клетка описывает**: экстремум стохастика берётся по окну, а не по бару (`N-5`, `N-6`). Плюс выход, названный не весь (пятое поле полос на нулевой ширине, `N-7`), отрицание шире своего же довода (лог пишет настоящий коллаборатор резолвера фазы, `N-10`), вход, которого нет в форме модели (у доменной свечи признака закрытости не существует, `N-9`), и клетка, снявшая ожидание шире, чем молчит дом (`N-11`). Две новые находки владельцам: `M-8` — непроставленный объём читается нулём, то есть благоприятным умолчанием, которое дом запрещает, а его закрытый перечень исключений этой арифметики не несёт; `M-9` — подтверждённый пробой ищется только в хвосте окна, и удержание, завершившееся раньше последнего бара, событием не становится вовсе (это не `M-4`: там событие есть с неверным моментом, здесь события нет). Секция `M-4` перевооружена третьим носителем. Заведены разделы «Пробелы покрытия» (`G1`-`G5`) и «Звенья кода, названные один раз» (`Z1`-`Z2`), которых у документа не было; заведён кейс `U9.9`. Кейсов стало 152 (было 151), красных — семь (было пять). Отчёт — `.claude/work/progress/phase-2-step-12-approve-test-market-data-indicators.md` |

## Что шаг застаёт

- **Стенд от дерева кода отстал**, и обязанность пересоздать обе базы перед
  подъёмом названа своей секцией бэклога.
- **Ни одна пара производитель-потребитель не встречалась на живом брокере** —
  форма провода держится пробами у сторон.
- **Реактор зелен** на всём дереве; числа печатает сам прогон.
- **Тесты в модулях есть** (`src/test` у всех сервисов, кроме общих
  артефактов без логики) — это юниты, идущие с кодом; чёрного ящика и
  сквозных прогонов нет ни у одного сервиса.

## Связи

- Детальный роадмап фазы — `.claude/work/roadmap/phase-2.md`.
- Машина шага — `.claude/processes/roadmap-step-execution.md`.
- Скилл ведения прогресса — `.claude/skills/update-roadmap-progress.md`.
- Роль и скиллы тестового шага — `.claude/agents/tester.md`,
  `.claude/skills/test-design.md`, `.claude/skills/test-review.md`,
  `.claude/skills/test-code.md`.
