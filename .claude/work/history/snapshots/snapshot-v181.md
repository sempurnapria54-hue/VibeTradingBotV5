# Snapshot v181

**Дата:** 2026-09-08.

## На какой вопрос отвечает этот файл

Где мы сейчас. **Шаг 10 фазы 2 (аудит и статистика): шестой прогон гейта выхода
прогнан, исход красный. Все три находки пятого прогона закрыты; открыты три
новые — две у девятнадцатой единицы, одна у четырнадцатой. Статус шага не
двигался.** Сменяет v180.

## Состояние

Фаза 1 — `FOLDED`. **Фаза 2 — `IN_PROGRESS`**: шаги 1-9 `DONE`,
шаг 10 — **`GAPS_CLOSE_4`** (все пять узлов приземлены; прогонов гейта шесть,
находки первых пяти закрыты), шаги 11-12 `HOLD`.

**Гейтящих выносов держателю нет.**

**Дельта в working tree, не в истории.** Ветка `claude-audit`; всё staged, не
закоммичено. Дерево кода не тронуто, как в v162-v180.

**Режим — автономия до прод-рубежа.** Решения — Д250-Д1160; эта единица новых
решений не заводила: прогон гейта корпус читает, а не меняет.

## Что сделано после v180

**Единица сессии — шестой прогон гейта выхода из `GAPS_CLOSE_4`.** Отчёт —
`.claude/work/progress/phase-2-step-10-gaps-close-4.md` §«Гейт выхода из
`GAPS_CLOSE_4` — шестой прогон 2026-09-08, исход КРАСНЫЙ».

- **Все три находки пятого прогона (П1-П3) закрыты и проверены исполнением.**
  Форма 1 свипа восемнадцатой единицы исполняется и даёт выдачу; четыре команды
  свипов разобраны таблицами целиком; три пробы не-членства дают код 1. Хвост-3
  зелен по всем предметам гейта.
- **Ш2 (блокирующая)** — блок пробы оси Р3 девятнадцатой единицы читает
  переменную, которую связывает только проза рядом. Отказ **невидим**: пустой
  шаблон совпадает с каждой строкой, блок отвечает кодом 0 и числом строк
  файла. Третья форма класса «объявленное исполнение подтверждено не тем, что
  написано»; ось 7 энфорсера её не ловит по построению.
- **Ш1 (несущая)** — таблица оси Р4 девятнадцатой единицы не равна выдаче своей
  команды: собственный отчёт закрытия стои́т в выдаче без исхода, а живой
  снапшот назван адресом, который эта же сессия сделала ложным ротацией.
  Оговорка «таблицы разбирают классы, а не имена», внесённая той же единицей в
  таблицы свипов, к таблице популяции применена не была.
- **Ш3 (несущая)** — прозаический счёт носителей выдачи в свипе хвоста-1
  опровергнут прогоном той же команды. Существо исхода («род `радиус` не
  сыгран») перепроверено чтением каждого носителя и остаётся; ложен ровно счёт.

**Правки корпуса единица не делала.** Записан отчёт прогона, строка хроники и
этот снапшот.

## Три вещи, которые дальше нельзя пере-решать

**1. Статус шага не проставлен и остаётся `GAPS_CLOSE_4`.** Гейт красен, пока
его прогон не дал чистого исхода; `DOCS_CHECK_5` открывается только после него.
**Седьмой прогон берёт сессия, закрытия Ш1-Ш3 не писавшая.**

**2. Клетки таблиц исхода прогонов не правятся.** Клетка есть исход
датированного захода; признак — `.claude/rules/self-description-form.md`
§«Датированный замер клеймом не является». Клейм **прозой рядом** о состоянии
открытого предмета — наоборот, правится на месте (Д1160), и находка Ш3 —
именно этот случай.

**3. Команды популяций задним числом не переписываются** (Д1150, Д1154). У Ш2
это **не действует и сказано явно**: команда, читающая несвязанное имя, никогда
не исполнялась как написана — прежнего перечня у неё нет.

## Вход следующей сессии

**Единица — закрытие Ш1-Ш3 шестого прогона гейта.** Диспозиция и разбор —
`.claude/work/progress/phase-2-step-10-gaps-close-4.md` §«Красное — находки
шестого прогона».

- **Ш2** закрывается связыванием имени **внутри блока** (первой строкой), а не
  прозой рядом; таблица Р3 после этого перечитывается против выдачи связанного
  шаблона.
- **Ш1** закрывается сведением строки о снапшоте к **классу** и внесением
  строки о самом отчёте закрытия. Перечня имён снапшотов в таблице не остаётся
  ни в какой редакции: он устаревает ротацией, то есть ходом самой сессии.
- **Ш3** закрывается снятием счёта и заменой его указателем на команду.
- **Развилка, которую закрывающая единица обязана назвать явно:** заводить ли
  `tools/command-carrier-check.py` ось 9 — «блок опирается на имя, которого сам
  не связывает». Доводы обеих сторон записаны в
  `.claude/work/progress/phase-2-step-10-gaps-close-4.md` §«Что дальше по итогам
  шестого прогона».
- **Урок шести прогонов, годный дальше:** правка, объявленная **классовой**,
  проходится по **всем** таблицам захода перечислением, а не по тем, где
  находка её назвала. Девятнадцатая единица внесла оговорку о классах в таблицы
  свипов и не внесла в таблицу популяции, хотя писала обе одним заходом.

## Что висит

1. **Дельта `CODE` шага 10 — счёт стои́т порождённым блоком в хронике.**
2. **Компонентов шага 10 в `.claude/work/code-gate-ledger.json` нет** — их
   вносит простановка `CODE`.
3. **Позиции парковки узлов 5-6 `GAPS_CLOSE_3`** и парковка узла 1
   (исполнимость формы ценового результата на строках агрегатов).
4. **Образ сервиса без продвинутого тега резолвится подвижным `latest`**
   (`.claude/work/backlog.md`).
5. **Форма дедупа «спросить и вставить»** — шесть точек трёх сервисов;
   поглощённый отчёт ручной операции. Обе — секции бэклога.
6. **Три долга инструментов:** проза реестра
   (`tools/retired-prose-debt.txt`), адресные перекрёстья `docs/**`,
   клетки-носители команд (`tools/command-carrier-debt.txt`).
7. **Цена сессий под правилом единицы не замерена.**
8. **События `auth` — производителя и темы нет**; экспорт метрик из семи
   прочих сервисов; таблицы `access_denials` нет ни у одного сервиса.
9. **Единый error-DTO у четырёх построенных сервисов.**
10. **Потерянное событие при неразбираемом содержимом** — DLT нет.
11. **Названные ограничения шага 7**, ревалидация активных определений при
    смене чисел риск-аппетита, `HOLD_RELEASED` не пишется, база риска на
    терминале не двигается.
12. **Внешний вызов внутри транзакции БД — двенадцать точек** у ядра.
13. **Донорские остатки в общей библиотеке** — снимаются удалением донора.
14. **Отчёты критики и верификации узлов 5-6 `GAPS_CLOSE_3` отдельными
    файлами не сохранены** — закрыто в части нормы, в части прошлого не
    закрывается ничем.
15. **Живые отчёты единиц маркера `(корпусный)` не несут** — кроме отчёта
    прохода, приземления узла 2 и хвостов: ретро-разметки нет по правилу
    курации.
16. **Прежние хвосты:** S1 Vault-хардненинг, рост таблицы отказов,
    api-формы периметра без дока.
17. **Пользователю:** переиндексировать проект в IDEA; обновить Project
    Knowledge (этот снапшот). **Стенд от дерева кода не отстал** — код не
    тронут.

## Проверки на момент снапшота

**Гейт инструментов корпуса прогнан на этом состоянии** — весь перечень плюс
`code-gate-check.py`. Числа прогонов здесь не пересказываются — их печатает сам
прогон (`.claude/rules/self-description-form.md`).

**Реактор не прогонялся: дерево кода не тронуто.** Последний прогон — на этом
же состоянии кода (замер v163).

**Живого прогона нет ни у одного компонента шагов 7-10.**

## Среда

Против v132 не изменилась; полное описание — там. Ловушки среды из v156-v180
остаются в силе, включая: полный гейт инструментов в 120 с не укладывается —
прогонять фоновой командой; рабочий бинарь — `py` (`python3` и `python` из
PATH — заглушка, её код 49 читается как «не измерялось»); в regex-шаблонах для
`tools/retired-check.py` пользоваться `\s+`; импорт модуля из `tools/` через
`importlib` роняет `__pycache__` в корень репозитория, и `tools/retired-check.py`
отказывает кодом 2 на нём; `cd` внутри составной команды Bash сохраняется между
вызовами; форма `py - <<'PYEOF'` рвётся на длинном markdown с таблицами —
большой раздел писать инструментом записи во временный файл внутри репозитория
и вклеивать коротким скриптом (временный файл удаляется тем же ходом).

**Ловушка переноса строки в шаблоне `grep -P`** (из v179, в силе и мерится
машиной — ось 7 `tools/command-carrier-check.py`): длинный `-P`-шаблон
переносится **между** аргументами, внутри шаблона перенос пишется
escape-последовательностью. Оборотная сторона: команда, **ищущая** непарную
кавычку, не вправе нести непарную кавычку сама.

**Ловушка несвязанного имени в командном блоке** (новая, из находки Ш2):
блок, читающий переменную, которую связывает только проза рядом, исполняется,
возвращает 0 и отвечает **не на свой вопрос** — у `grep` пустой шаблон
совпадает с каждой строкой. Имя связывается **внутри блока**; машиной эта форма
сегодня не мерится.

**Ловушка снапшота против реестра снятых редакций** (из v178, в силе): в
снапшоте снятое **называется**, а не цитируется, и рядом стои́т действующая
редакция с указателем на её дом.

**Ловушка одноимённых заголовков** (из v178, в силе): раздел новой единицы
называется с её маркером; §-адрес обязан разрешаться однозначно.
