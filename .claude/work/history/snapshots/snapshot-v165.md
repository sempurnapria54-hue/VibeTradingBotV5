# Snapshot v165

**Дата:** 2026-09-08.

## На какой вопрос отвечает этот файл

Где мы сейчас. **Шаг 10 фазы 2 (аудит и статистика): `GAPS_CLOSE_4`,
пятая единица — узел 2 (A7) — ЗАКРЫТА. Следующая единица — узел 3.**
Сменяет v164.

## Состояние

Фаза 1 — `FOLDED`. **Фаза 2 — `IN_PROGRESS`**: шаги 1-9 `DONE`,
шаг 10 — **`GAPS_CLOSE_4`**, шаги 11-12 `HOLD`.

**Гейтящих выносов нет, и гейтящих находок в прогоне тоже не осталось**:
A7 была последней из шестнадцати. Узлы 3-5 несут ноль гейтящих.

**Дельта в working tree, не в истории.** Ветка `claude-audit`; всё staged,
не закоммичено. Сверх v164 тронуты: `.claude/work/roadmap/phase-2.md`
(примечание к шагу 10 — граница шага), `docs/models/domain/other/AuditRecord.md`,
`docs/rules/statistics-aggregates.md`, `docs/architecture/contracts.md`,
`docs/rules/api-access-policy.md`, `.claude/work/backlog.md` (секция
переименована по предмету), `.claude/rules/knowledge-ownership-by-service.md`
(указатель), плюс отчёты узла и рамки, хроника, дайджест (Д1111-Д1114),
снапшот. **Дерево кода не тронуто** — как в v162-v164.

**Режим — автономия до прод-рубежа.** Решения — Д250-Д1114.

## Что сделано после v164

**Единица сессии — узел 2 `GAPS_CLOSE_4`** (A7: что `CODE` обязан построить
на поверхности чтения). Узел **приземлён**; отчёт —
`.claude/work/progress/phase-2-step-10-gaps-close-4-node-2.md`
§«Приземление». Заходов — раунд критики и одна верификация; потолок
(`PROC-Q7`) не тронут.

**1. Половина A7, названная линзой дефицитом `подтверждение`, держателю НЕ
вынесена** (Д1111). Вариант «перенести read-only API из шага 10 в шаг 12»
жил посылкой «выдача = HTTP-точка», которую линза сама объявила
достроенной. Со снятой достройкой переносить нечего; запись самопроверки
не-выноса — в отчёте узла.

**2. Что шаг строит — названо** (Д1111): по **выборке на предмет** (строки
журнала, строки агрегатов) с ограничениями отбора, объявленными у владельца
каждого предмета; отложен **состав ЭКРАННЫХ операций** и их api-формы.
Прежняя редакция откладывала «состав точек» целиком — и под то же имя
попадала выборка, чей состав корпусом уже задан.

**3. У агрегатной выборки заведён свой дом ограничений** (Д1112,
`docs/rules/statistics-aggregates.md`): радиус, окно по суткам зерна,
курсор; обязательство границы с предикатом взято ссылкой у владельца
журнала. Асимметрию нашла верификация — до неё правки молча распространяли
журнальные ограничения на второй предмет.

**4. Предел ширины окна получил место, направление и писателя** (Д1113):
свойство конфигурации сервиса, консервативность в сторону меньшего окна,
дефолт кладёт `CODE`; запрос без окна и шире предела **отвергается**, а не
сужается молча. Прежняя парковка числа стояла на посылке «поверхности до
фронта не существует», которую узел снимает.

**5. Пятый домен назван** (Д1114): `audit-statistics` внесён в перечень
принимающих тенанта (`docs/architecture/contracts.md` §«Контекст тенанта в
вызове»); сверх перечня объявлено требование к будущим доменам — чей радиус
обязателен, тот называет здесь, чем тенант принимается.

## Три вещи, которые дальше нельзя пере-решать

**1. Узел 2 закрыт целиком.** Д1111-Д1114 вошли в силу с приземлением;
возврат к «шаг строит ноль точек» — регресс.

**2. Дом ограничений — у владельца ПРЕДМЕТА, и предметов два.** Журнал —
`AuditRecord.md`, агрегаты — `statistics-aggregates.md`; общее обязательство
(граница + предикат) — один дом у журнала, остальные ссылаются.

**3. Признак деления самоописания не переоткрывается** (Д1100): число с
механическим выводом живёт порождённым блоком, число без вывода не пишется.

## Вход следующей сессии

**Единица — узел 3 `GAPS_CLOSE_4`: пороги, границы и моменты замера**
(A9, A10, B12; гейтящих нет). Дальше — узлы 4 и 5; раскладка —
`.claude/work/progress/phase-2-step-10-gaps-close-4.md` §«Раскладка находок
по узлам».

Рамка узла та же: правило одной фразой → популяция до первой правки →
проект правок → мини-петля критики (потолок — две верификации) →
приземление → обход потребителей и свипы. **Урок узла 2, стоящий дороже
прочих:** уровень «концепция-стык» требует симметричного обхода **всех**
сторон стыка — их оказалось пять вместо трёх, и три блокирующие находки
верификации были одной и той же правкой, не доведённой до соседнего дома.

## Что висит

1. **Дельта `CODE` шага 10 — счёт стои́т порождённым блоком в хронике**;
   позиция 36 (выборки чтения) заведена этой сессией.
2. **Компонентов шага 10 в `.claude/work/code-gate-ledger.json` нет** — их
   вносит простановка `CODE`.
3. **Позиции парковки узлов 5-6 `GAPS_CLOSE_3`** и парковка узла 1
   (исполнимость формы ценового результата на строках агрегатов —
   `.claude/work/backlog.md`).
4. **Долг прозы реестра** (`tools/retired-prose-debt.txt`).
5. **Цена сессий под правилом единицы не замерена** — замер по журналу
   первого прогона.
6. **События `auth` — производителя и темы нет**; экспорт метрик из семи
   прочих сервисов; таблицы `access_denials` нет ни у одного сервиса.
7. **Единый error-DTO у четырёх построенных сервисов.**
8. **Потерянное событие при неразбираемом содержимом** — DLT нет.
9. **Названные ограничения шага 7**, ревалидация активных определений при
   смене чисел риск-аппетита, `HOLD_RELEASED` не пишется, база риска на
   терминале не двигается.
10. **Внешний вызов внутри транзакции БД — двенадцать точек** у ядра.
11. **Донорские остатки в общей библиотеке** — снимаются удалением донора.
12. **Отчёты критики и верификации узлов 5-6 `GAPS_CLOSE_3` отдельными
    файлами не сохранены** (C3 прогона `DOCS_CHECK_4`, узел 5
    `GAPS_CLOSE_4`): генератор `passes:` на таких узлах числа заходов
    вывести не может.
13. **Прежние хвосты:** S1 Vault-хардненинг, рост таблицы отказов,
    api-формы периметра без дока.
14. **Пользователю:** переиндексировать проект в IDEA; обновить Project
    Knowledge (этот снапшот). **Стенд от дерева кода не отстал** — код не
    тронут.

## Проверки на момент снапшота

**Одиннадцать команд корпуса прогнаны на этом состоянии.** Рабочий бинарь —
`py`; `python3` из PATH — заглушка, её код 49 читается как «не измерялось».
Гейт прогонялся **фоновой** командой: в 120 с он не укладывается. Числа
прогонов здесь не пересказываются — их печатает сам прогон
(`.claude/rules/self-description-form.md`).

**Реактор не прогонялся: дерево кода не тронуто.** Последний прогон — на
этом же состоянии кода (замер v163).

**Живого прогона нет ни у одного компонента шагов 7-10.**

## Среда

Против v132 не изменилась; полное описание — там. Ловушки среды из
v156-v164 остаются в силе, включая рвущийся длинный heredoc (правки —
коротким python-скриптом, подаваемым heredoc'ом в `py -`) и **полный гейт
инструментов, не укладывающийся в 120 с** — прогонять фоновой командой.
Коды гейта снимаются обёрткой, вызывающей команду без конвейера: `echo
"rc=$?"` после конвейера с `tail` возвращает код `tail`.
