# Snapshot v218

**Дата:** 2026-09-10.

## На какой вопрос отвечает этот файл

Где мы сейчас. **Шаг 10 фазы 2 (аудит и статистика) в `CODE`: шестой прогон
фокусов аппрув не выдал — гейтят Ф7 и Ф8.** Сменяет v217.

## Состояние

Фаза 1 — `FOLDED`. **Фаза 2 — `IN_PROGRESS`**: шаги 1-9 `DONE`, шаг 10 — **`CODE`**,
шаги 11-12 `HOLD`.

**Гейтящих выносов держателю нет.** Эта единица вопроса не выносила;
`.claude/work/questions/open-questions.md` ею не пополнялся.

**Дельта staged, не закоммичена.** Ветка `claude-audit`. **Дерево кода этой
единицей НЕ тронуто** — прогон фокусов корпус читает, а не меняет.

**Режим — автономия до прод-рубежа.** Решения — Д250-Д1361 (эта единица
решений не принимала: фокусы не чинят).

## Что сделано после v217

**Единица сессии — шестой прогон фокусов.** Отчёт —
`.claude/work/progress/phase-2-step-10-code-focuses-6.md`; статус — в
`phase-2-step-10-chronicle.md` §Статус.

- **Дельта K17 перепроверена по предмету.** Полнострочной записи
  существующей строки сделки в дереве ядра не осталось: писатель строки
  целиком зовётся дважды, обе точки — **заведение**, а `create` на модели с
  идентичностью бросает. Позиции прошлых прогонов перепроверены
  собственным чтением дерева — построчно в отчёте; закрытой не
  подтвердилась одна, Ф6.
- **Ф7 (гейтит).** Клейм «у каждого писателя **статуса** свой guard, и
  **перечень закрыт**», которым K17 заменил прежний клейм о писателях
  **ребра**, выведен не из дерева: `set d.status` даёт **шесть** запросов,
  таблица дома — **четыре** строки. Отсутствуют оба каскадных радиуса,
  хотя каскад назван писателем статуса **абзацем выше в той же секции**.
- **Ф8 (гейтит по корпусу).** Вывод той же популяции показывает: у сделки,
  уведённой каскадом, `shutdown_reason` не пишется **никогда** — оба его
  писателя гардированы статусами, которые каскад уже сменил. Второе
  следствие — `DealShutdownInitiated` у этой популяции не публикуется
  вовсе. Два корпусных носителя утверждают обратное. **Код — шага 7 фазы 2,
  вне дельты шага 10**; ложен корпусный клейм.
- **Прочие три фокуса расхождений не дали.** Порядок «число и признаки →
  ребро» разобран и признан безвредным; обращений к базе дельта K17 не
  добавила, а убрала (`merge` = `select` + `update` всех колонок → один
  точечный `update`).

## Три вещи, которые дальше нельзя пере-решать

**1. Перечень исполнителей не переоткрывается** (v195). Новый исполнитель
вносится **строкой** реестра, а не поводом пересобрать перечень.

**2. Реестр компонентов сквозной и новым шагом не пересобирается** (Д1184).
Находки в нём — прогона `DOCS_CHECK_34`; гейт срабатывает **на компоненте**,
когда тот начинается.

**3. Существующая строка сделки правится ТОЛЬКО точечным гардированным
запросом** (Д1358-Д1361). Запись строки целиком заводит новую строку и на
существующей отказывает. Ф7 предмет этого решения **не** оспаривает — она о
**клейме полноты** перечня писателей, а не о самой разводке.

## Вход следующей сессии

**Единица — заход K18: закрытие Ф7 и Ф8** по маршруту владельца дома
конвенций (`.claude/skills/conventions-review.md` §«Форма замечания»).
Варианты по обеим находкам предъявлены отчётом прогона без навязанного
крена; у Ф8 вариант (а) держит кодовую половину **вне** дельты шага 10 и
закрывает корпусную — парковкой по
`.claude/rules/parking-address.md` §«Признак верного дома».

**Аппрув выдаёт прогон, а не закрытие**
(`.claude/processes/roadmap-step-execution.md` §«Аппрув `CODE` —
зафиксированный исход фокусов, не устное согласие»), и вести его обязана
сессия, K18 не писавшая.

Порядок дальше: K18 → **седьмой** прогон фокусов → `SYNC_DOCS_FROM_CODE`
(фокус `divergence`) → пост-хок концепт-гейт
(`.claude/processes/roadmap-step-execution.md` §6a). Каждый — своей единицей.

Обязательное перед завершением любой такой единицы:

1. **гейт инструментов корпуса** целиком;
2. **сборка — `bash tools/reactor-test.sh`**, если тронуто дерево кода;
   точечный прогон одного модуля — `mvn -o -am -pl services/<модуль> test` из
   корня. **`-am` обязателен**: общий артефакт в локальном репозитории не
   установлен. `mvn` на `PATH` нет — рабочая форма вызова стои́т в §Прогоны
   отчёта `.claude/work/progress/phase-2-step-10-code-pass-k13.md`;
3. **`py tools/code-gate-check.py`** — перед каждым кодовым заходом.

## Что висит

1. **Текст JPQL новых запросов не мерится ничем** — `@SpringBootTest` у
   модуля нет намеренно; пробы мерят слой `DealDataService` и вызывающих.
   Оживитель общий с п. 2 — первый живой прогон ядра на базе.
2. **Разведение пар джоб ядра ПРОЧИТАНО, а не измерено** — живого прогона
   ядра на базе нет ни одного.
3. **Слепота считается состоявшимися проходами** — тропа входа возраста
   наблюдения не спрашивает; припарковано с оживителем.
4. **Класс `HoldReleased` не построен** — ручное снятие ступени фиксируется
   строкой отчёта, а не классом снятия (`.claude/work/backlog.md`).
5. **Эпизод транша событием не различается** — счётчика эпизода нет и у самой
   заявки; ограничение модели ядра, названо у дома состава.
6. **Строки журнала, накопленные до хода, не чинятся:** операнды агрегата
   невосстановимы задним числом.
7. **Таблицы и писателя строки отказа у четырёх соседей нет** (`auth`,
   `trading-core`, `strategies`, `market-data`) — общий долг
   (`.claude/work/backlog.md`).
8. **Перехватчик `Exception` съедает отказ по правам у `bff` и `strategies`**
   — тот же класс, что закрытая S1; припаркован.
9. **Предикат актора написан трижды** — со-расположение названо у всех трёх,
   единый носитель припаркован.
10. **Глубины у `access_denials` нет ни у одного сервиса** — величина
    выводится из той же наблюдённой частоты, что и порог реакции на серию
    отказов.
11. **Пул планировщика у `market-data` — один поток**, и дом ему ничего не
    предписывает: измерителя среди пяти его джоб нет.
12. **Остаток пары «чистка × пересчёт»** у `audit-statistics`: удаление,
    зафиксированное между чтением операнда и группировкой тех же суток, даёт
    частичную строку; цена принята и названа у дома отбора.
13. **Экспорт метрик у семи сервисов из восьми не построен**; экспорт
    **трейсов** не построен ни у кого.
14. **Подъём контекста не проверен ни у `audit-statistics`, ни у `strategies`,
    ни у `bff`, ни у ядра в части `AsyncConfigurer`:** `@SpringBootTest` у
    модулей нет намеренно.
15. **Транзакционная граница пары «ребро + факт» прогоном не мерится** —
    держит объявление метода, проверяет каркас.
16. **Ни план запроса, ни порядок взятия строчных замков у модуля приёма не
    мерятся ничем** — живого прогона у него нет ни одного; обе позиции
    припаркованы одной секцией с общим оживителем.
17. **Отбор ресурсов наблюдателем по метке `release` живым кластером не
    проверен**; оживитель — подъём стенда на дереве кода.
18. **Дубль запроса `earliestRecordedAt` у двух подключений остался** (Д1297).
19. **Позиции парковки узлов 5-6 `GAPS_CLOSE_3`** и парковка узла 1
    (исполнимость формы ценового результата на строках агрегатов).
20. **Кандидат энфорсера окрестности** — бэклог.
21. **Стенд от дерева кода отстал:** образы `audit-statistics`, `strategies`,
    `trading-core` и `bff` с этими правками не собирались, роли и базы на
    стенде не заводились, тем и подписки на стенде нет. Плюс прежнее — образ
    сервиса без продвинутого тега резолвится подвижным `latest`.
22. **Форма дедупа «спросить и вставить»** — шесть точек трёх сервисов.
23. **Три долга инструментов:** проза реестра
    (`tools/retired-prose-debt.txt`), адресные перекрёстья `docs/**`, долг
    носителей команд (`tools/command-carrier-debt.txt`).
24. **§-адрес javadoc в несуществующий пассаж** — класс припаркован; энфорсера
    у класса по-прежнему нет.
25. **Цена сессий под правилом единицы не замерена.**
26. **События `auth` — производителя и темы нет.**
27. **Единый error-DTO у четырёх построенных сервисов** (`auth`,
    `connector-okx`, `market-data`, `trading-core`).
28. **Потерянное событие при неразбираемом содержимом** — DLT нет.
29. **Названные ограничения шага 7**, ревалидация активных определений при
    смене чисел риск-аппетита, `HOLD_RELEASED` не пишется, база риска на
    терминале не двигается.
30. **Внешний вызов внутри транзакции БД — двенадцать точек** у ядра.
31. **Донорские остатки в общей библиотеке** — снимаются удалением донора.
32. **Отчёты критики и верификации узлов 5-6 `GAPS_CLOSE_3` отдельными файлами
    не сохранены.**
33. **Т-1…Т-3 седьмого прогона гейта `GAPS_CLOSE_4`; Ц-2…Ц-4 первого и Д-1…Д-4
    второго прогона гейта `GAPS_CLOSE_5`** — все о тексте о работе; уезжают с
    отчётами в `history/` при закрытии шага.
34. **Прежние хвосты:** S1 Vault-хардненинг, api-формы периметра без дока.
35. **Пользователю:** обновить Project Knowledge (этот снапшот). Новых
    Maven-модулей эта единица не заводила — переиндексация IDEA не нужна.

## Проверки на момент снапшота

**Гейт инструментов корпуса прогнан** на состоянии после всех правок, включая
этот файл, — весь перечень, ненулевых кодов нет, кода 2 нет. Числа прогонов
здесь не пересказываются: их печатает сам прогон
(`.claude/rules/self-description-form.md`).

**Реактор прогнан** (`bash tools/reactor-test.sh`) — зелёный. Дерево кода эта
единица не меняла: прогон предъявлен как исходное состояние, к которому
относятся находки.

## Среда

Против v132 не изменилась; полное описание — там, перечень ловушек — в v206
(`.claude/work/history/snapshots/snapshot-v206.md` §Среда) и он остаётся в
силе целиком. Ловушки v207-v217 тоже в силе, включая: `python3` в этой среде
разрешается в заглушку Windows Store и падает кодом 49 — рабочая форма `py`.
