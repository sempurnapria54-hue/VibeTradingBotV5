# Snapshot v113

**Дата:** 2026-09-03.

## На какой вопрос отвечает этот файл

Где мы сейчас. **Т11 закрыт постройкой, шаг 8 переведён в `CODE` по
гейту.** Сменяет v112.

## Состояние

Фаза 1 — `IN_PROGRESS`; шаги 1-7 `DONE`, **шаг 8 — `CODE`**, шаги 9-11
`HOLD`. Ветка `claude-audit`. **Дельта закоммичена тремя коммитами** —
вопреки базовому правилу, по прямому указанию держателя на эту задачу;
ревью в IDEA идёт по истории коммитов.

**Режим — автономия до конца фазы 1**; решения — Д250-Д461 в дайджесте.

## Что происходит дальше

**Кодирование шага 8** по перечню `docs/components/AnomalyJob.md`
§«Что ищет»: двенадцать детекторов из тринадцати плюс обязательство
`clOrdId` у слоя команд. Гейт **покомпонентный** — `code-gate-check.py`
прогоняется перед каждым кодовым заходом, а не только на переходе.

## Чем закрыта эта итерация (2026-09-03)

- **Т11 — мягкая биржевая ступень построена.** Итог —
  `.claude/work/history/2026-09-03-soft-exchange-rung.md`; решения
  Д457-Д461. Разрыв был **кодовым, не концептуальным**: доки описывали
  ступень целиком, а `src/` её не имел.
- **Шаг 8 в `CODE`.** Хроника и исход гейта —
  `.claude/work/progress/phase-1-step-8-chronicle.md`.

## Что ратифицировано и не пере-решается

Всё из v104-v112, плюс:

- **Мягкая ступень исполняется одной тропой на обоих радиусах** (Д457):
  радиус выбирает исполнителя статуса, составы разводят лестницы.
- **Снятие биржевого сворачивания двухходовое** (Д458): первый ход ведёт
  в мягкую ступень под энфорсером живого риска, второй — в рабочее
  состояние. Спека объявляла целью `HOLD` ещё до постройки; код с ней
  расходился.
- **Гейт живого риска на мягкую ступень не распространён** (Д459): она
  принятый риск не снимала, и гейт сделал бы её неснимаемой в её же
  штатном состоянии.
- **`exchangeSoft` разведена с `exchangeJournal`** (Д460): до постройки
  разница была безобидной, после — вызов журнальной фабрики завёл бы
  запрет входов по всей бирже там, где реакции нет.

## Чем проверяется корпус

Девять команд — зелены; сборка предъявлена: **248 тестов, 0 падений**
(+7 к входу задачи). `code-gate-check.py` — **ПРОЙДЕН**, 42 позиции
сводки против 42 записей реестра, открытых ноль; реестр под шаг 8 не
пересобирался (последний прогон чист) и получил только строки
**компонентов**. `preconditions-check.sh` — открытых слотов ноль.

**Каждая правка Т11 предъявлена падающей пробой:** откат цели первого
хода снятия даёт `expected: <HOLD> but was: <ACTIVE>`; дублирование гейта
живого риска на второй ход роняет свой тест; возврат мягкой ступени к
инструментной ветви роняет свой; возврат снятой формулировки в носитель
роняет обе оси записи реестра снятых редакций.

## Что висит

1. **`CODE`-дельта шага 8** — двенадцать детекторов, обязательство
   `clOrdId` у слоя команд, провизорные числа калибровки со своим
   встречным якорем.
2. **Ручная поверхность ПОСТАНОВКИ safety-ступени не построена** —
   живут только операции снятия (обе ступени биржи, обе инструмента).
   Дом задачи — `.claude/work/backlog.md` §«Лестница биржевых
   safety-состояний — CODE-дельта (2026-08-26)».
3. **Пользователю — обновить Project Knowledge:** этот снапшот, дайджест
   (Д250-Д461).

Остальное — v110 §«Что висит», пункты 1-10, за вычетом закрытого Т11.

## Среда

Изменений с v108 нет. Дом фактов —
`.claude/tests/source-api/okx/code-preconditions.md`. **Maven в `PATH`
отсутствует**: рабочий бинарь — из связки IDEA
(`.../plugins/maven/lib/maven3/bin/mvn.cmd`) при `JAVA_HOME` на
`corretto-25.0.3`; лаунчер Python — `py -3` (`python3` в Git Bash —
заглушка WindowsApps).
