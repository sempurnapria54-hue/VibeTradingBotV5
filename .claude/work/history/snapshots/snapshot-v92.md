# Snapshot v92

**Дата:** 2026-08-29.

## На какой вопрос отвечает этот файл

Где мы сейчас. **Корпус переписан по концепции; два ратифицированных
направления — катастрофический потолок сделки и агрегатная сделка с
траншами — приземлены. Наружу осталось задание двух чисел конфигурации
при запуске.** Сменяет v91.

## Состояние

Фаза 1 — `IN_PROGRESS`; шаги 1-6 `DONE`, шаг 7 — **корпус написан и
дважды переприземлён, в `CODE` не входил**, шаги 8-11 `HOLD`. Ветка
`claude-audit`, `HEAD = 0ddb3db`, дерево чистое: вся дельта
закоммичена.

**Режим — автономия до конца фазы 1.** Проектные развилки CC решает сам
и фиксирует в дайджесте; держателю — только продуктовый scope и
численные величины риск-аппетита.

## Артефакты, на которых стоит корпус

- `docs/concept.md` — ядро: четыре принципа (риск fail-once-then-halt;
  непредусмотренная ситуация не решается на ходу; наблюдаемость как
  требование конструкции; знание — один дом, чистые доки, исполнимая
  форма) и выведенная из них конструкция. **Утверждён держателем.**
- `.claude/work/progress/corpus-rewrite-mapping.md` — раскладка по всем
  докам плюс отчёты исполнения. **Утверждена держателем.**
- `.claude/work/progress/risk-contour-design.md` — дизайн-проход узла
  покрытия и риск-контура (Р1-Р11); оба его выноса закрыты.
- `.claude/work/history/2026-08-29-tranche-landing/aggregate-deal-design.md`
  — дизайн-проход агрегатной сделки с исполнимым аннексом (17 примеров,
  зелёный); приземлён.

## Что приземлено после v91

**Катастрофический потолок сделки** — итог
`history/2026-08-29-catastrophic-deal-ceiling.md`, дом
`docs/rules/risk-policy.md`. Потолок = максимальный риск на сделку ×
множитель `StrategyDetail`, ограниченный вторым числом конфигурации; кэп
номинала равен потолку (худший ход принят 100 %), отдельная константа
упразднена; величины «про хвост» априорно не назначаются. Лимитов стало
четыре — `docs/concept.md` §«Четыре вопроса — четыре лимита».

**Агрегатная сделка и транши** — итог
`history/2026-08-29-tranche-landing.md`. `Deal` стал агрегатом (пять
статусов, слот, эпизоды, окно линковки, результат, числа риска),
`DealTranche` — самостоятельной сделкой внутри сделки (семь статусов плюс
ребро переоткрытия). Инвариант покрытия стал траншевым; экспозиция
транша — производная его заявок, а её сумма сверяется с нетто-размером
эпизода (новый детектор неатрибутируемого риска). Стратегия объявляет
транши шаблоном «транш × уровни»; имя уровня ратифицировано — `Tranche`.

**Две цены приняты держателем и записаны:** потраншевый `R` из фактов
нетто-режима не выводится (R — метрика сделки); внешнее частичное
сокращение позиции становится громким — останов вместо молчаливого
поглощения.

## Что висит

1. **Держателю — задать два числа конфигурации при запуске:**
   максимальный риск на сделку и максимальный множитель катастрофического
   потолка. До задания — штатный отказ: первое отвергает risk-creating
   действие, второе — создание стратегии. Это ожидаемое состояние, а не
   дефект (`.claude/work/backlog.md` §«Переписывание корпуса — что
   осталось»).
2. **Пользователю — обновить Project Knowledge:** этот снапшот,
   `docs/concept.md` (менялся), дайджест решений. `structure-digest.md`
   не менялся.
3. **Работ по корпусу нет; вся оставшаяся работа шага 7 — `CODE`.**
   Три CODE-дельты в `.claude/work/backlog.md`: §«Агрегатная сделка и
   транши — CODE-дельта» (схема траншей, статусные енумы, две машины
   состояний, сверка Σ, валидация create), §«CODE-дельта шага» (весь
   накопленный хвост шага, включая катастрофический потолок) и
   §«Cross-cutting миграции».
4. **Статус шага 7 в роадмапе — устаревшая метка.**
   `.claude/work/roadmap/phase-1.md` держит `GAPS_CLOSE_25`, тогда как
   машина `DOCS_CHECK`/`GAPS_CLOSE` остановлена ещё до v91, а корпус с
   тех пор писался и приземлялся. Метку следует привести к фактике при
   следующем ведении роадмапа.
5. **Дайджест решений:** непрочитанные пакеты 2026-08-28 (Д45-Д72) и
   2026-08-29 (Д93-Д124), плюс более ранние.

## Чем проверяется корпус

**Прогоном примеров, а не полнокорпусными линзами.** Правка формулы,
предиката или перечня доказывается прогоном `tools/spec-run.sh`; правка
дока — дифф-скоупом по затронутым домам.

- **Исполнимый слой `docs/spec/` — 15 предметов, 178 примеров, зелёный.**
  Формула, предикат и закрытый перечень живут одним экземпляром; величина
  переиспользуется через `includes`, одноимённая из двух источников —
  отказ прогона.
- **Эталонная стратегия проверяется машинно дважды:** деревом
  (`strategy-reference.json` читает
  `src/main/resources/strategy-examples/trend-following-ema.json`) и
  пошаговым проходом транша (`strategy-walkthrough.json`).
- **Каталог прогона переключается** — `SPEC_DIR=<каталог> bash
  tools/spec-run.sh`; так гоняется и аннекс дизайн-прохода из `history/`.
- Машина `DOCS_CHECK`/`GAPS_CLOSE` остановлена; детектор стагнации
  (`.claude/skills/stagnation-detection.md`) сохранён как инструмент на
  будущее.

Корпус: 228 файлов знания в `docs/` (213 доков и 15 спецификаций).
Процессной арматуры в нём нет: §-адресов внутри `docs/` — ноль.

## Среда

Без изменений — см. `.claude/work/history/snapshots/snapshot-v87.md`.
