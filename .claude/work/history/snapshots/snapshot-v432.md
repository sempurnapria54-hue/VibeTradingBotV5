# Снапшот v432

**Дата:** 2026-09-24.

## На какой вопрос отвечает этот файл

Где мы сейчас.

## Состояние

Сменяет v431. Фаза 1 — `FOLDED`. **Фаза 2 — `IN_PROGRESS`**: шаги 1-11
`DONE`, **шаг 12 — `REVIEW`**, шаг 13 `HOLD`. Прод-рубеж — `HOLD`.

**Машина шага** — `CONCEPT → CONCEPT_REVIEW → CODE → DOCS → REVIEW → DONE`
(`.claude/processes/roadmap-step-execution.md`).

**Дельта — в рабочем дереве, не закоммичена.** Ветка `claude-audit`.

**Режим — автономия до прод-рубежа.** Решения — Д250-Д2100
(`.claude/work/decision-digest.md`).

## Что сделано после v431

- **Заход 153 — кодовый заход кластера kill-switch и ручной остановки**
  (хроника шага, заход 153; Д2101-Д2103): инструментная половина снятия риска
  читает сделки пары сама — ручная полная остановка на паре снимает риск;
  исполнитель закрывает позиции радиуса вне графа сделок и подтверждает радиус
  срезом позиций. Три секции K1 закрыты —
  `.claude/work/history/2026-09-24-step-12-sections-closed-by-code.md`.

## Вход следующей сессии

**Единица — кодовый заход закрытия находок шага 12, следующий по тяжести:**
прочие секции работы шага — перечень печатает первая команда записи захода 153
в хронике. У ядра по тяжести первой стоит
`.claude/work/backlog.md` §«Позицию инструмента вне контура снятие риска закрыть не может»
(правка контракта закрытия у коннектора). У коннектора остаются
§«Структурная валидация записи закрытия построена на одну проверку из трёх»
(клетка `B5.4`) и единый error-DTO (клетки `B8.2`, `B8.6`). Заходы тестера:
клетки ящика ядра за `F-13` — §«Клетки ящика `trading-core` за чужими
находками ждут их закрытия», клетки тропы активации — §«Клетки тропы
активации за копией определения ждут закрытия находки `F6`», и два
прерывистых отказа реактора — §«Ящик `statistics`: клетка B11.5 прерывисто не
находит ряд непотреблённых записей», §«Юнит `bff`: клетка U3.8 кэша членства
прерывисто оставляет просроченную запись». После кодовых заходов — фокус
`test` (продолжение захода 147) и прочие фокусы плана захода 142.

## Что держателю решать

1. **Апрув `REVIEW` шага 12** — по условию держателя: правки кода по находкам
   сделаны, все тесты шага написаны и зелены.

Прочие ожидания держателя печатает `py tools/backlog-check.py` строкой
`держатель:`.

## Что висит

Висящее живёт секциями `.claude/work/backlog.md` и
`.claude/work/prod-checks.md` и печатается `py tools/backlog-check.py`; в
снапшоте не дублируется.

**Пользователю:** обновить Project Knowledge (этот снапшот).

## Ловушки

Ловушки живут в домах по своему вопросу; снапшот их не держит. В разделе
ловушек дома — оглавление: споткнулся — найди строку по словам области и
симптома, тело читай по номеру в `.claude/traps/<дом>-traps.md`.

- среда, оболочка, правка корпуса скриптом, площадка OKX —
  `.claude/skills/environment-commands.md` §«Ловушки и обходы»;
- код тестов — `.claude/skills/test-code.md` §«Ловушки и обходы»;
- письмо кейсов и их ревью — `.claude/skills/test-design.md`,
  `.claude/skills/test-review.md`;
- продуктовый код — `.claude/rules/codestyle.md` §«Ловушки»;
- команды корпуса и свипы — `.claude/rules/measurement-commands.md`;
- условие маркера бэклога — `.claude/rules/backlog-section-form.md`
  §«Ловушки»;
- гейт и статус — `.claude/skills/update-roadmap-progress.md`;
- цикл сессий — `.claude/skills/session-chain.md` §«Ловушки и обходы».

## Проверки на момент снапшота

Реактор захода 153 зелен третьим прогоном: 1715 файлов, 4871 тест, дефектов 0;
два прежних прогона покраснели прерывистыми клетками `statistics` и `bff`
(секции выше). Гейт инструментов корпуса — одним прогоном на финальном
состоянии захода 153; исход — отчёт захода.

## Среда

Дом фактов и ловушек среды — `.claude/skills/environment-commands.md`;
стенда — `.claude/skills/local-stand.md`. Изменений среды за заход нет.
