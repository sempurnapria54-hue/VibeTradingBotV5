# Snapshot v137

**Дата:** 2026-09-06.

## На какой вопрос отвечает этот файл

Где мы сейчас. **Шаг 7 фазы 2 — `CODE`: компонент добычи состояния закрыт
целиком, парковка резолва внешнего статуса снята решением.** Сменяет v136.

## Состояние

Фаза 1 — `FOLDED`. **Фаза 2 — `IN_PROGRESS`**: шаги 1-6 `DONE`, шаг 7 —
`CODE`, шаги 8-12 `HOLD`. Фазы 3-6, 7-8 и прод-рубеж — `HOLD`.

**Дельта в working tree, не в истории.** Ветка `claude-audit`; всё
описанное — staged, не закоммичено (коммит руками).

**Режим — автономия до прод-рубежа.** Решения — Д250-Д796 (итерации
34-43). **Открытых выносов держателю нет.**

## Что закрыто этим заходом

1. **Парковка резолва внешнего статуса** — решением с правкой всех
   разошедшихся носителей. Дом —
   `docs/rules/external-status-resolution.md` §«Где резолвится — сторона
   выбирается по словарю источника».
2. **Компонент «исполнители площадки: добыча состояния» — целиком.**
   Заявка (два цикла), условная заявка, эпизод позиции, движения средств.
   Хроника — `.claude/work/progress/phase-2-step-7-chronicle.md`, разделы
   «…— три из четырёх» и «…— закрыт».

## Где стои́т шаг 7

Docs-first пройден целиком. **Кодовый заход: 8 компонентов из 21
поставлено.** Реестр и статус каждого — `.claude/work/code-gate-ledger.json`.

**Вход следующего захода — расчётный слой `libs/strategy-engine`.**
Разведка сделана 2026-09-05 и не устарела: размер строится **от спеки**
(`docs/spec/order-sizing.json`, 19 примеров — готовые тест-кейсы), а не
портируется; донорский слой отстал от доковой редакции. Открытая развилка
формы операндов `CalculationContextFactory` решается вместе с первым
вызывающим. Риск-гейт заблокирован этим слоем.

**Далее по реестру:** `DealContextService` и сборка контекста прохода,
FSM сделки и транша с обработчиками, риск-гейт, `SystemActionExecutor` и
звенья финализации, терминальные исполнители и сверка P&L, холды,
детекция аномалий, сканер входа, outbox, поверхность ядра.

**Ратифицировано шагом 7 (не пере-решать):** радиус торговой строки —
пара «счёт × инструмент»; проекции чужих реестров наполняет синхронное
чтение; числа риск-аппетита — на строке тенанта; копия определения
стратегии плюс временный писатель до шага 8; факты исполнения —
**опросом**; отказ соседа по ярусу — `PEER_SERVICE_UNAVAILABLE`; ноги
агрегата собираются **обходом траншей**, донорское `Deal.orders` ядром не
читается; **резолв сырого статуса — у коннектора, назначение исхода — у
ядра** (Д785).

## Что дала постановка этих компонентов (живой контекст)

| Что | Наблюдение |
|---|---|
| закрытый эпизод | уезжал к ядру **без идентичности**: шлюз собирал его тропой обновления, а она пару «идентификатор, время создания» не переносит по построению |
| тело добытой заявки | гасило встроенные защиты явным `ignore` — резолвер их состояния остался бы без операнда первой ступени |
| мапперы коннектора | четыре метода `updateFrom*` не звал никто: остаток донорской формы, где сущность держало ядро |
| охраны переходов | штатный тик наблюдения живой сущности падал бы на `ACTIVE -> ACTIVE`: подтверждение статуса переходом не является |
| откат транзакции | отметка исхода терялась бы при броске контролируемого исключения — ступень поднялась бы по факту, которого в базе нет |
| цикл добычи заявки | док называл четыре ноги, четвёртой — «архив истории»; операции с таким предметом в контракте коннектора нет вовсе |

## Что дальше — вход в следующий заход

1. **Расчётный слой `libs/strategy-engine`** (см. выше) и снятая им
   блокировка риск-гейта.
2. **Кодовые хвосты двух прежних парковок**: ставка комиссии, предикат
   свежести шага.
3. **Аппрув `CODE`** — фокусы `conventions`/`performance`/`disaster`/
   `security` с зафиксированным исходом; автор код не аппрувит.
4. **В `SYNC_DOCS_FROM_CODE` накоплено четыре расхождения:**
   `docs/models/domain/aggregate/DealTranche.md` §Персистентность не
   называет трёх колонок налива;
   `docs/models/domain/other/DealActionState.md` §Инварианты называет ключ
   живого потраншевого исполнения тройкой вместо четвёрки;
   `docs/components/ServiceCommandExecutor.md` знает два исхода исполнения,
   а их три; `docs/components/RefreshBalanceExecutor.md` называет радиус
   отчёта биржей, а база живёт на **счёте**.

## Что висит

1. **Прежнее того же класса:** конверт события `EventEnvelope` без
   читателя; донорские поля агрегата и донорские значения в расколотых
   перечнях общей библиотеки — оживит удаление донора.
2. **Парковки стенда:** срезы стакана, экспорт трейсов, том
   Elasticsearch, политики сжатия гипертаблиц.
3. **Парковки прежних шагов:** сброс кэша ключей по ротации; осиротевшие
   секреты; WS-слушатель фактов исполнения; контекстный тест сервиса;
   подпись образов; транспорт внутри кластера; темы Kafka; размеры томов;
   ликвидации; ориентир по расходам на хранение.
4. **Прежние хвосты:** S1 Vault-хардненинг, рост таблицы отказов, A12,
   хвост шага 8, долг адресных перекрёстий `docs/**`, дубль эталона
   стратегии в корне.
5. **Пользователю:** переиндексировать проект в IDEA; обновить Project
   Knowledge (этот снапшот, дайджест итераций 35-43). На стенде остаются
   **тестовые** строки тенанта и счёта (`tn-stand-0001`, `ea-stand-0001`)
   — удалять их или нет, решает держатель.

## Проверки на момент снапшота

Десять команд корпуса — код 0 (`spec-run`, `spec-mutation-check`,
`spec-scope-check`, `spec-pointer-check`, `anchor-check`, `retired-check`,
`population-derive-check`, `preconditions-check`, `deploy-layout-check`,
`code-gate-check`). Реактор — `mvn -o test -DexcludedGroups=source-api-live`,
`BUILD SUCCESS`: `auth` 10, `connector-okx` 27, `market-data` 38,
`trading-core` **124**, донор 324.

**Живого прогона у компонента нет и быть пока не может:** команды шлёт
FSM, которой ещё нет. Персистентность разбивки движений сверится со
схемой на старте (`ddl-auto: validate`) при ближайшей выкладке; миграция
`V4` при ней же и накатится.

## Среда

Против v132 не изменилась. Стенд поднят и работает. Сборка идёт назначенным
JDK и Maven (`JAVA_HOME=C:/Users/RomanKrd/.jdks/corretto-25.0.3`, maven3 из
поставки IDEA) — системный `JAVA_HOME` указывает на JDK 11 и проект им не
собирается. Python корпусных прогонов —
`C:/Users/RomanKrd/AppData/Local/Programs/Python/Python311/python.exe`
(`python3` в PATH — заглушка магазина приложений).
