# Snapshot v214

**Дата:** 2026-09-10.

## На какой вопрос отвечает этот файл

Где мы сейчас. **Шаг 10 фазы 2 (аудит и статистика) в `CODE`: четвёртый прогон
фокусов аппрува не выдал — гейтит Ф5.** Сменяет v213.

## Состояние

Фаза 1 — `FOLDED`. **Фаза 2 — `IN_PROGRESS`**: шаги 1-9 `DONE`, шаг 10 — **`CODE`**,
шаги 11-12 `HOLD`.

**Гейтящих выносов держателю нет.** Эта единица вопроса не выносила;
`.claude/work/questions/open-questions.md` ею не пополнялся.

**Дельта staged, не закоммичена.** Ветка `claude-audit`. **Дерево кода этой
единицей НЕ тронуто:** прогон фокусов корпус и код читает, а не правит.

**Режим — автономия до прод-рубежа.** Решения — Д250-Д1352.

## Что сделано после v213

**Единица сессии — четвёртый прогон фокусов** по дельте K15. Отчёт —
`.claude/work/progress/phase-2-step-10-code-focuses-4.md`; статус — в
`phase-2-step-10-chronicle.md` §Статус. Решений эта единица не принимала:
прогон находит и формулирует, не чинит и не решает.

- **Дельта K15 перепроверена по предмету, а не по отчёту.** Ф1-Ф4
  подтверждены закрытыми: у периметра объявлен
  `spring.task.scheduling.pool.size` и заведена своя `SchedulerCapacityTest`;
  дом цены одновременности пары стои́т у владельца строки, обе стороны стыка
  несут указатель, клейм «общей колонки нет» сверен по запросам репозитория;
  чеклист фокуса пополнен; пирамида аннотаций переставлена. Все шестнадцать
  позиций прошлых прогонов (D1, D2, S1, S2, P1, P2, K1-K5, R1, R2, Ф1-Ф4)
  подтверждены собственным чтением дерева и собственными свипами.
- **Ф5 гейтит: клейм корпуса, которым дом о пуле объявлен неприменимым к
  ядру, ложен на `AnomalyJob`.** Довод парковки — «задержка каждой джобы
  наблюдаема через её собственный выход» — у аномалий не держится: чистый
  проход пишет только `blindPassCount = 0` bulk-`update`'ом, момента прохода
  в строке нет, а несостоявшийся проход счётчика не двигает. Голодающий тик
  читается как «наблюдение идёт, аномалий нет», предел при этом переведён в
  время дословно («три минуты слепоты») и выведен из такта, а такт делится с
  шестью соседями на одном потоке умолчания.
- **Компонент — на торговой тропе:** ступень слепоты есть единственный
  запрет набирать риск вслепую.
- **Признак измерителя в доме читается двояко** — узко (экспортируемый ряд
  наблюдателя окружения; у ядра экспорта метрик нет вовсе) и по существу
  механизма (молчание неотличимо от здоровья наблюдаемого). Клейм выбрал
  чтение молча. Вариантов закрытия три, крена прогон не ставит: исполнить дом
  у ядра (с обходом пар семи джоб), исправить клейм с сохранением парковки,
  либо сузить признак в доме.
- **Прочие три фокуса — `performance`, `disaster`, `security` — расхождений
  не дали.**

## Три вещи, которые дальше нельзя пере-решать

**1. Перечень исполнителей не переоткрывается** (v195). Новый исполнитель
вносится **строкой** реестра, а не поводом пересобрать перечень.

**2. Реестр компонентов сквозной и новым шагом не пересобирается** (Д1184).
Находки в нём — прогона `DOCS_CHECK_34`; гейт срабатывает **на компоненте**,
когда тот начинается.

**3. Имена ключей содержимого зафиксированы читателем** (Д1263) и повторены
дословно; сверка — `CoreEventFormTest`. Пере-называть их нельзя: расхождение
не ловится ничем — величина выйдет пустой, а строка агрегата правдоподобной.

## Вход следующей сессии

**Единица — кодовый заход K16: закрытие Ф5.** Вход — раздел находки Ф5 отчёта
`.claude/work/progress/phase-2-step-10-code-focuses-4.md`: там предъявлены
доказательства, названы цена и три варианта. Выбор — владельца дома конвенций (`code-writer`),
решение уходит строкой в `.claude/work/decision-digest.md`.

**Аппрув выдаёт прогон, а не закрытие**
(`.claude/processes/roadmap-step-execution.md` §«Аппрув `CODE` —
зафиксированный исход фокусов, не устное согласие»). После K16 — **пятый**
прогон фокусов, и ведёт его сессия, K16 не писавшая.

Порядок дальше: K16 → пятый прогон фокусов → `SYNC_DOCS_FROM_CODE`
(фокус `divergence`) → пост-хок концепт-гейт
(`.claude/processes/roadmap-step-execution.md` §6a). Каждый — своей единицей.

Обязательное перед завершением любой такой единицы:

1. **гейт инструментов корпуса** целиком;
2. **сборка — `bash tools/reactor-test.sh`**, если тронуто дерево кода;
   точечный прогон одного модуля — `mvn -o -am -pl services/<модуль> test` из
   корня. **`-am` обязателен**: общий артефакт в локальном репозитории не
   установлен. `mvn` на `PATH` нет — рабочая форма вызова стои́т в §Прогоны
   отчёта `.claude/work/progress/phase-2-step-10-code-pass-k13.md`;
3. **`py tools/code-gate-check.py`** — перед каждым кодовым заходом.

## Что висит

1. **Ф5 не закрыта** — см. §«Вход следующей сессии».
2. **Класс `HoldReleased` не построен** — ручное снятие ступени фиксируется
   строкой отчёта, а не классом снятия (`.claude/work/backlog.md`).
3. **Эпизод транша событием не различается** — счётчика эпизода нет и у самой
   заявки; ограничение модели ядра, названо у дома состава.
4. **Строки журнала, накопленные до хода, не чинятся:** операнды агрегата
   невосстановимы задним числом.
5. **Таблицы и писателя строки отказа у четырёх соседей нет** (`auth`,
   `trading-core`, `strategies`, `market-data`) — общий долг
   (`.claude/work/backlog.md`).
6. **Перехватчик `Exception` съедает отказ по правам у `bff` и `strategies`**
   — тот же класс, что закрытая S1; припаркован
   (`.claude/work/backlog.md`).
7. **Предикат актора написан трижды** — со-расположение названо у всех трёх,
   единый носитель припаркован (`.claude/work/backlog.md`).
8. **Глубины у `access_denials` нет ни у одного сервиса** — величина выводится
   из той же наблюдённой частоты, что и порог реакции на серию отказов.
9. **Пул планировщика у соседей — один поток** (`trading-core`,
   `market-data`). У `market-data` измерителя среди джоб нет; **у ядра это
   ровно предмет Ф5** — прежний вердикт «измерителя нет» прогоном опровергнут
   на `AnomalyJob`. Ход, поднимающий пул в ядре, обязан пройти пары его джоб.
10. **Остаток пары «чистка × пересчёт»** у `audit-statistics`: удаление,
    зафиксированное между чтением операнда и группировкой тех же суток, даёт
    частичную строку. Достижимо только при окне пересчёта не у́же глубины
    хранения; цена принята и названа у дома отбора.
11. **Экспорт метрик у семи сервисов из восьми не построен**; экспорт
    **трейсов** не построен ни у кого.
12. **Подъём контекста не проверен ни у `audit-statistics`, ни у `strategies`,
    ни у `bff`, ни у ядра в части `AsyncConfigurer`:** `@SpringBootTest` у
    модулей нет намеренно.
13. **Транзакционная граница пары «ребро + факт» прогоном не мерится** —
    держит объявление метода, проверяет каркас.
14. **Ни план запроса, ни порядок взятия строчных замков у модуля приёма не
    мерятся ничем** — живого прогона у него нет ни одного. Обе позиции
    припаркованы одной секцией с общим оживителем
    (`.claude/work/backlog.md` §«Порядок замков и планы запросов у модуля
    приёма — не мерены ничем»); цена порядка названа у дома строки
    (`docs/models/domain/other/AuditRecord.md`).
15. **Отбор ресурсов наблюдателем по метке `release` живым кластером не
    проверен**; оживитель — подъём стенда на дереве кода.
16. **Дубль запроса `earliestRecordedAt` у двух подключений остался** (Д1297).
17. **Позиции парковки узлов 5-6 `GAPS_CLOSE_3`** и парковка узла 1
    (исполнимость формы ценового результата на строках агрегатов).
18. **Кандидат энфорсера окрестности** — бэклог.
19. **Стенд от дерева кода отстал:** образы `audit-statistics`, `strategies`,
    `trading-core` и `bff` с этими правками не собирались, роли и базы на
    стенде не заводились, тем и подписки на стенде нет. Плюс прежнее — образ
    сервиса без продвинутого тега резолвится подвижным `latest`.
20. **Форма дедупа «спросить и вставить»** — шесть точек трёх сервисов.
21. **Три долга инструментов:** проза реестра
    (`tools/retired-prose-debt.txt`), адресные перекрёстья `docs/**`, долг
    носителей команд (`tools/command-carrier-debt.txt`).
22. **§-адрес javadoc в несуществующий пассаж** — класс припаркован в
    `.claude/work/backlog.md`; энфорсера у класса по-прежнему нет.
23. **Цена сессий под правилом единицы не замерена.**
24. **События `auth` — производителя и темы нет.**
25. **Единый error-DTO у четырёх построенных сервисов** (`auth`,
    `connector-okx`, `market-data`, `trading-core`).
26. **Потерянное событие при неразбираемом содержимом** — DLT нет.
27. **Названные ограничения шага 7**, ревалидация активных определений при
    смене чисел риск-аппетита, `HOLD_RELEASED` не пишется, база риска на
    терминале не двигается.
28. **Внешний вызов внутри транзакции БД — двенадцать точек** у ядра.
29. **Донорские остатки в общей библиотеке** — снимаются удалением донора.
30. **Отчёты критики и верификации узлов 5-6 `GAPS_CLOSE_3` отдельными файлами
    не сохранены.**
31. **Т-1…Т-3 седьмого прогона гейта `GAPS_CLOSE_4`; Ц-2…Ц-4 первого и Д-1…Д-4
    второго прогона гейта `GAPS_CLOSE_5`** — все о тексте о работе; уезжают с
    отчётами в `history/` при закрытии шага.
32. **Прежние хвосты:** S1 Vault-хардненинг, api-формы периметра без дока.
33. **Пользователю:** переиндексировать проект в IDEA; обновить Project
    Knowledge (этот снапшот).

## Проверки на момент снапшота

**Гейт инструментов корпуса прогнан** на состоянии после всех правок, включая
этот файл, — весь перечень, ненулевых кодов нет, кода 2 нет. Числа прогонов
здесь не пересказываются: их печатает сам прогон
(`.claude/rules/self-description-form.md`).

**Реактор прогнан** (`bash tools/reactor-test.sh`) — дерево кода этой единицей
не менялось, прогон предъявлен как подтверждение зелёного состояния дельты
K15, а не как исход собственной правки.

## Среда

Против v132 не изменилась; полное описание — там, перечень ловушек — в v206
(`.claude/work/history/snapshots/snapshot-v206.md` §Среда) и он остаётся в
силе целиком. Ловушки v207-v213 тоже в силе.

**Ловушка этой сессии — одна:** `grep` с альтернацией без `-E` (`"a|b"`) в
BRE ищет литеральную черту и молча возвращает пустую выдачу — то есть ровно
тот исход, который свип обязан доказывать. Форма для отчёта — `grep -rnE`
либо экранированная черта; в блоке отчёта пишется исполнимая.
