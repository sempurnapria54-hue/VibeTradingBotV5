# Snapshot v153

**Дата:** 2026-09-07.

## На какой вопрос отвечает этот файл

Где мы сейчас. **Шаг 10 фазы 2 (аудит и статистика): `DOCS_CHECK_3`
прогнан и выпущен — 28 находок, 10 гейтящих, эскалации нет; `GAPS_CLOSE_3`
открыт поузловой рамкой, узел 1 из шести ведётся пятой редакцией и в
корпус ещё не приземлён.** Сменяет v152.

## Состояние

Фаза 1 — `FOLDED`. **Фаза 2 — `IN_PROGRESS`**: шаги 1-9 `DONE`,
шаг 10 — `GAPS_CLOSE_3` (узел 1 в работе), шаги 11-12 `HOLD`.

**Дельта в working tree, не в истории.** Ветка `claude-audit`; всё
staged, не закоммичено. **Корпус `docs/**` этой сессией не тронут** —
приземления ещё не было.

**Режим — автономия до прод-рубежа.** Решения — Д250-Д1042.
**Открытых выносов держателю нет.**

## Что сделано этой сессией

| Что | Исход | Носитель |
|---|---|---|
| `DOCS_CHECK_3` | **выпущен**: 28 находок, 10 гейтящих; три линзы, A и B слепые | `.claude/work/progress/phase-2-step-10-docs-check-3.md` + три части |
| детектор стагнации | ни один сигнал не сработал; разбор записан, эскалации нет | там же, §«Детектор стагнации» |
| `GAPS_CLOSE_3` | открыт; раскладка 28 находок по шести узлам | `phase-2-step-10-gaps-close-3.md` |
| узел 1 (ключ группировки пересчёта) | 2 раунда критики + 2 верификации (13, 10, 8, 6 дефектов); редакция 5; **не приземлён** | `phase-2-step-10-gaps-close-3-node-1.md` + 4 отчёта критики |
| гейт инструментов | десять команд корпуса — код 0 (прогнаны в начале и в конце) | раздел о проверках ниже |

## Три вещи, которые стои́т помнить из этого захода

**Гейтящий остаток впервые упал больше чем на треть — 28 → 21 → 10.**
Поузловая рамка (`.claude/decisions/per-node-closure-frame.md`) сработала
по своему предмету: в корпус попадает только сошедшееся, и остаток убывает
монотонно. **Чего она не сняла — происхождение остатка:** девять гейтящих
находок из десяти по-прежнему лежат на конструкциях, введённых закрытиями
этого же шага. Изменилась величина, не механизм.

**Доля следствий закрытия в одиночку не читается, и это записано в самом
детекторе.** 9 из 10 — выше порога С2, но абсолютный ряд падает вдвое;
`.claude/skills/stagnation-detection.md` §«Минимальный знаменатель» именно
этот случай и называет своим кейсом («доля показывала 100 %, остаток убывал
втрое»). Эскалации нет; условие пересмотра названо — неубывающий остаток на
`DOCS_CHECK_4` при доле выше ½.

**Несущий класс прогона новый: клейм носителя, ложный по его собственному
дому** — девять находок из двадцати восьми. Закрытый список без двух своих
членов; «ровно два объявленных исключения», которых правило объявляет
другими; имена величин, которых нет; «пара политики уже есть» против
манифеста; «два класса» вместо четырёх; «больше одной» при одной. Знание
почти везде верно — не сверен **клейм о его полноте или счёте**.

## Вход следующей сессии

**Узел 1 `GAPS_CLOSE_3` — довести до чистоты и приземлить.** Две
верификации пройдены; редакция 5 написана и ждёт **третьей** верификации —
её надо назначить первым ходом (предмет: редакция 5, оба предмета критики,
все опубликованные команды дословно). Чистый исход → приземление пяти правок
(три в `docs/rules/statistics-aggregates.md`, одна в хронике — позиция 22
дельты `CODE`, одна в `tools/retired-check.py`), затем строка дайджеста и
переход к узлу 2.

**Порядок узлов посерийный** (§«Раскладка находок по узлам» в
`phase-2-step-10-gaps-close-3.md`): 1 — ключ группировки; 2 — полнота
журнала и квантор её предиката (A1, A5, B3); 3 — состав счётчиков агрегата
(B1, B2, B5, B6, C5); 4 — радиус чтения и его энфорсер (A3, A7); 5 —
клеймы и копии носителей (A4, A6, A8, A12, B7, C1, C2, C8, C9 плюс две
позиции, переданные узлом 1); 6 — приёмка и разметка шага (A9, A10, A11,
C3, C4, C6, C7).

**Что узел 1 стои́т дорого и почему это ожидаемо.** Четыре захода критики
поймали 13 + 10 + 8 + 6 дефектов, и подавляющая часть — **в тексте
правки**, не в конструкции. Класс воспроизводится дословно: доработка
вносит дефект того же класса на месте снятого (Н1 → V1 → W1; V5 → W2;
V6 → W4; V7 → W3).

**Отсюда конвенция, введённая редакцией 5 и обязательная для узлов 2-6:**
правка публикуется парой **«Было / Стало»** с дословным воспроизведением
правимого пассажа целиком, а не описанием диапазона строк. Диапазон
границу описывает, пара её **предъявляет**, и расхождение видно без
прогона. Сверх того: §-адресов в `docs/**` не вводить вовсе; популяцию
оси строить командой, выводящей **весь** предмет, а не подмножество.

## Что висит

1. **Дельта `CODE` шага 10 — 26 живых позиций** (хроника, одна таблица);
   узел 1 добавляет двадцать седьмую (тест джобы пересчёта) — по
   приземлению.
2. **События `auth` — производителя и темы нет**; **экспорт метрик из семи
   прочих сервисов**; **таблицы `access_denials` нет ни у одного сервиса**.
3. **Единый error-DTO у четырёх построенных сервисов.**
4. **Потерянное событие при неразбираемом содержимом** — DLT нет.
5. **Названные ограничения шага 7**, **ревалидация активных определений при
   смене чисел риск-аппетита**, **`HOLD_RELEASED` не пишется**, **база риска
   на терминале не двигается**.
6. **Внешний вызов внутри транзакции БД — двенадцать точек** у ядра.
7. **Донорские остатки в общей библиотеке** — снимаются удалением донора.
8. **Новое от `DOCS_CHECK_3`, вне узлов закрытия:** долг адресных
   перекрёстий `docs/**` вырос за шаг 422 → 610 (C3), слой опровержения
   64 → 75 (C4), замеры обеих парковок в бэклоге устарели вдвое.
9. **Прежние хвосты**: S1 Vault-хардненинг, рост таблицы отказов, состав
   точек чтения `audit-statistics` (шаг 12), api-формы периметра без дока.
10. **Пользователю:** переиндексировать проект в IDEA; обновить Project
    Knowledge (этот снапшот). **Стенд от дерева кода не отстал** — сессия
    кода не писала.

## Проверки на момент снапшота

**Десять команд корпуса — все код 0**, прогнаны в начале сессии (до первой
правки) и в конце. Рабочий бинарь python назван в §Среда; `python3` из
PATH — заглушка, её код 49 читается как «не измерялось».

Спеки — **27** спецификаций, **656** примеров (40 контрпримеров), **11**
популяций, **117** членов (33 недостижимых); мутационный замер — **453**
величины, 0 переживших. Реестр снятых редакций — **66** записей, **233**
строки популяции, 0 дефектов. Указатели величин — 259, ведут не в дом 0.
Доковые указатели — 1780, битых 0. Адреса `.claude/**` — 768, новых
неразрешимых 0 (долг 54, не вырос). Раскладка развёртывания — 0
расхождений. Предусловия — сошлись.

**Реактор не прогонялся:** дерево кода сессия не трогала.

**Живого прогона нет ни у одного компонента шагов 7-10.**

## Среда

Против v132 не изменилась; полное описание — там. Сборка идёт назначенным
JDK и Maven (`JAVA_HOME=C:/Users/RomanKrd/.jdks/corretto-25.0.3`, maven3 из
поставки IDEA). **Python корпусных прогонов — `py -3` либо
`C:/Users/RomanKrd/AppData/Local/Programs/Python/Python311/python.exe`**;
`python3` в Git Bash — заглушка магазина приложений, печатает `Python` и
выходит кодом 49 (дом факта —
`.claude/tests/source-api/okx/code-preconditions.md`, строка «Лаунчер
Python»). Офлайновая сборка зелёная; `mvn clean` офлайн не работает —
пересборка через `tools/reactor-test.sh`.
