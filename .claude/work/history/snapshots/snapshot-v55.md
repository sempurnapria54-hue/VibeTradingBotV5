# Snapshot v55

**Дата:** 2026-06-20.

## На какой вопрос отвечает этот файл

Где мы сейчас и как сюда пришли. **Тема — контур тестов API источника
(`source-api`) — закрыта полностью: остаток после v54 доведён, система
приведена в консистентное состояние, задача формально закрыта**
(прогресс/tasks → `history/`). Заход — **финальное закрытие задачи**
(не continuation). Снапшот обычного состава; новый чат стартует с
PK-префлайта. Дополняет v54 (v54 пометил тему закрытой, но нёс остаток
follow-on; здесь остаток закрыт).

## Состояние

Фаза 1 роадмапа — `IN_PROGRESS`; шаги 1-4 `DONE`, 5-11 `HOLD` (без
изменений против v54). Ветка `claude-audit`. **Задача контура source-api
— ЗАКРЫТА**: артефакты перенесены в
`.claude/work/history/2026-06-20-source-api-contour/` (+ summary
`2026-06-20-source-api-contour.md`); `progress/` по контуру пуст.
Остаточных фейлов нет. Работа сессии — staged, не коммичено (CC не
коммитит); затронутые классы гоняются `mvn -o test` зелёными.

## Путь к точке (от v54) — остаток закрыт

v54 нёс остаток follow-on (C3 apidoc-sync, TG4.1, `/raw`-only скиллы,
housekeeping). Всё закрыто:

**1. C3 apidoc-sync — И-2.** `cancel-advance-algos` **жив на demo вопреки
офдоку** (delisted 2025-04-24) — зафиксировано в `algo-order.md`:
рантайм-подтверждение (`b.code=0/sCode=0`; фейк-id → 51293) с провенансом
`рантайм` + пометка в шапке «Внешний источник правды»
(`external-source-sync`). Оговорка И-2 «ждёт demo-кредов» снята.

**2. TG4.1 — разобран и закрыт (тест-тюнинг, не C3).** Изначальный фейл:
amend-batch заACKан, но `getOrder` не отразил `newSz/newPx` за 25s poll.
Причина — известная асинхронность amend на demo (>25s), **уже** учтённая
у одиночного amend (TG3.1: poll 60s), но batch-кейс остался на дефолтных
25s. Тест выровнен на 60s → **TG4 3/3 green** (`validate-tg4-02.log`).
План (строки TG4.get1/get2/verify + §Статус) обновлён.

**3. Скиллы контура → `/raw`-only.** `test-design`, `test-collection`,
`test-review` выровнены: снят субсет-модель — «манифест ∩ клиент»,
`client-coverage-gap`, «вариант-gap = что строит клиент»; покрытие — весь
in-perimeter через `/raw` (тело строится руками), ассерты — на сырых полях
JSON (`OkxApiResponse<JsonNode>`). Термин «A2-passthrough» сохранён как
текущий (это и есть generic `/raw`). `test-code` был выровнен ранее (v53).

**4. Housekeeping.** 3 progress-файла контура + tasks-файл развилок
архивированы в `history/2026-06-20-source-api-contour/`; написан
summary. Развилки A/B/C ре-базы были закрыты (A2/B1/C3) — открытых
форвард-заметок нет, перенос в backlog не потребовался.

## Состояние интеграции (итог)

Контур стабилен, остаточных фейлов нет; все RUN-находки маршрутизированы
(И-2 → апидок; TG4.1 → тест-тюнинг; AG5 → prod-only + тесты). Манифест:
все in-perimeter `🟢 в коде`, success deep-архива AG5 — prod-only.
План: 312 RUN-строк заполнены, каждая ключуется к своей лог-записи.

## Среда

Без изменений против v54. CC компилирует/гоняет тесты (`mvn -o test`,
corretto-25); demo-среда (Vault test + demo + Postgres-test + сеть OKX)
доступна (использовалась для RUN/перевалидаций). prod — вне контура.

## Следующий шаг (задача закрыта)

Тема source-api исчерпана. Новый чат — PK-префлайт, затем **следующая
тема по приоритету пользователя**: следующий шаг роадмапа (Фаза 1 шаг 5+)
или иная задача. Источниковых хвостов по контуру не осталось.

## Принципы

Без изменений против v54 (sweep+halt едино для сущностей и настроек;
контур проверяет контракт биржи, не наш код; `/raw`-only dispatch; цель —
документировать реальное поведение OKX; durable-план заполняется по
step-меткам — каждая строка ключуется к своей `observe`-записи).

## Отложено / на будущее

- **prod-проверка success deep-архива AG5** — ад-хок пользователем вне
  контура (success-контракт на demo неверифицируем).
- **Вырождение ветвления cancel-algos↔advance** (отменяет ли `cancel-algos`
  и advance-семью) — не проверялось; корректность ветвления (а) не
  блокирует, отдельный вопрос при надобности.
- Прежний C3-хвост: ticker `markPx`/`idxPx` офдок-сверка (не из этого
  контура).

## Открытые вопросы

Без изменений против v54 (CMD-Q5/Q6, PHASE-Q1/Q2, STRUCT-Q1, IND-Q1,
RISK-Q1/Q2, INSTR-Q1/Q2, ORCH-Q1, DEAL-Q1/Q2/Q3, OKX-Q1/Q2/Q3/Q4,
STRAT-Q4, CMD-Q2/Q4). §Нерешённое контура — закрыты.

## Гейты делегирования

- **`tester`** — задача контура закрыта (DESIGN+CODE+RUN+разбор находок).
- **`integrator`** — C3 apidoc-sync по И-2 выполнен; дальнейших C3 по
  контуру нет.
- Прочие роли — без изменений (`solution-designer` 1/3).

## Режим работы

**Задача source-api закрыта.** Новый чат — следующая тема по приоритету
пользователя. Не отладка пайплайна.

## Синхрон / PK / staged

- **Project Knowledge:** последний снапшот теперь **`snapshot-v55`**
  (заменяет v54 в префлайте — **обновить PK после коммита**).
- **Закоммичено пользователем** (коммиты «Тесты апи 5/6/7», до этой
  сессии-хвоста): RUN-заполнение, A2, AG5, §I3, мис-филл I-cred.
- **Staged, не закоммичено (дельта закрытия остатка):** `algo-order.md`
  (И-2 C3), `Tg4AmendBatchOrdersLiveTest.java` (poll 60s), `plan.md`
  (TG4.1 + AG8.1 + §Статус), скиллы `test-design`/`test-collection`/
  `test-review` (`/raw`-only), `Ag8SetLeverageLiveTest.java` (observe),
  архив `history/2026-06-20-source-api-contour/` + summary, этот снапшот.
- **Untracked (не наши / транзиентные):** run-логи
  `.claude/work/run-logs/*.log`, `fill-plan.py`, `tradingbot.iml`,
  `vault.hcl`.
- Напоминание `external-source-sync`: рантайм-расхождение И-2 в
  `algo-order.md` помечено провенансом `рантайм`; офдок остаётся
  источником правды.
