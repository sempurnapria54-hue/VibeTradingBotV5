# Снапшот v385

**Дата:** 2026-09-20.

## На какой вопрос отвечает этот файл

Где мы сейчас.

## Состояние

Сменяет v384. Фаза 1 — `FOLDED`. **Фаза 2 — `IN_PROGRESS`**: шаги 1-11
`DONE`, **шаг 12 — `CODE·3/3`**, шаг 13 `HOLD`. Прод-рубеж — `HOLD`.

**Машина шага** — `CONCEPT → CONCEPT_REVIEW → CODE → DOCS → REVIEW → DONE`
(`.claude/processes/roadmap-step-execution.md`). `CODE` тестового шага идёт
тремя фиксированными под-шагами: тест-кейсы → адверсариальное ревью кейсов →
код тестов. **Под-шаги 1 и 2 закрыты целиком**; **идёт под-шаг 3 — код
тестов.** **Уровень 2 написан целиком (14 из 14); на уровне 1 ящик есть у
ШЕСТИ сервисов из восьми** — у `strategies` написан целиком, у
`trading-core` не пройден хвост с чужими блокерами, у `audit` написаны **три
группы из одиннадцати**.

**Дельта — в рабочем дереве, не закоммичена.** Ветка `claude-audit`.
**Дерево кода эта сессия не меняла вовсе** — только `src/test` сервиса
`audit`; реактор прогнан.

**Режим — автономия до прод-рубежа.** Решения — Д250-Д1923.

## Что сделано после v384

Одна единица: **заход 104 — группа `B3` ящика `audit`** (тик состояния
приёма: состав пар, живость, ряды). Подробности — хроника шага
`.claude/work/progress/phase-2-step-12-chronicle.md` §«Тридцать шестой заход
под-шага 3 (104) написал группу `B3` ящика `audit`»; здесь только то, что
меняет картину.

- **Написаны 13 клеток из 14**; `B3.14` класса не получает вовсе — её
  ожидание мерит **существующая проба предмета** `SchedulerCapacityTest`
  (Д1828). Прогонов в пакете ящика стало **43** против 31; зелены 42, красна
  та же одна — `B2.10`, помеченная `debt`.
- **Деление на классы у этой группы ДРУГОЕ, чем у соседней: класс равен
  КОНФИГУРАЦИИ КОНТЕКСТА.** Семь клеток штатных осей — одним классом, шесть
  сдвигающих ось — своими. Довод `B2` («класс равен клетке») здесь не
  действует: клетки `B3` приёма не ломают.
- **Назначение партиций отнимается СНЯТИЕМ ТЕМ у брокера** — остановка
  контейнера была бы касанием второго бина, а решение 6 называет
  единственной точкой касания тик.
- **Брокер субстрата тем больше не заводит сам**, и это условие четырёх
  клеток разом: тема, объявленная подпиской и отсутствующая у брокера, —
  вход `B3.5` и `B3.9`; снятая тема, которая не возвращается, — вход `B3.6`
  и `B3.8`.
- **Половина `B3.9`, стоявшая в перечне непрогоняемых, стала прогоняемой** —
  ровно тем поводом, который перечень и называл; строка снята.
- **`B3.8` написана с названной заменой оси** (ряд порога вместо ряда
  возраста), остаток объявлен пробелом покрытия `G6`: состав рядов возраста
  внутри живого контекста неподвижен по построению.
- **Контрольный прогон взял четыре оси; три сошлись точно, четвёртая уронила
  на одну клетку больше предсказанного** — и это промах ПРЕДСКАЗАНИЯ, а не
  оси: `B3.3` заимствует у соседнего предмета часы (пауза выражена ростом
  ряда возраста). Направление ошибки безопасное.
- **Находок владельцам заход не добыл ни одной.**

## Вход следующей сессии

**Следующая единица — группа `B4` ящика `audit`** (обнаружение разрыва: три
исхода сравнения смещений; 10 клеток).

Что единица застаёт:

- **обвязка ящика `audit` полна**, и к ней добавлены: раздельные перечни
  «заводимых» и «объявленных» тем у субстрата, брокер без автозаведения,
  снятие тем у провода (`Wire#deleteTopics`), снятие таблицы у наблюдателя
  строк (`Rows#withoutTable`), чтение ряда по имени величины и счёт рядов
  приёма целиком, вызов произвольным методом (`AuditBox#call`);
- **своё препятствие у `B4` названо заранее**: её предусловия говорят о
  СМЕЩЕНИЯХ — «непрочитанное удалено брокером», «смещение записи больше
  ожидаемого», — и ставятся они у брокера, а не в базе; повод для первого
  тот же, что у `B2.15`, — срок хранения темы;
- **форма кода уровня 1** задана заходами 83-104, **уровня 2** — заходами
  69-82; всё, что перечисляли v325-v384 §«Вход следующей сессии», остаётся
  в силе дословно (`.claude/work/history/snapshots/snapshot-v325.md` …
  `…/snapshot-v384.md`);
- **контрольный прогон на нейтрализованной сборке — часть единицы** (Д1830);
  предсказание называет клетки поимённо до прогона, мутация ставится и
  снимается **восстановлением файла из индекса**, а не правкой назад;
- **ревизия существующего набора проб предмета** — свой предмет перечня
  (Д1828); у этой группы она дала исход — `B3.14` записана зелёной чужой
  пробой, второго носителя не заведено;
- **хвост ящика `trading-core`** — клетки, ждущие живой экспозиции (`F-13`),
  терминала сделки с числом и трёх своих названных причин; их оживители
  сегодня не сработали (`.claude/tests/cases/trading-core.md` §«Кейсы, не
  прогоняемые сегодня»);
- **колонка «Факт» у двадцати семи клеток групп `B1`-`B3` ящика
  `strategies` пуста** — своя секция бэклога;
- **хвост апрувленных документов** — негейтящие находки `Ф-3`-`Ф-5` по
  `jsonb-overlay-roundtrip`.

## Что держателю решать

Против v384 не изменилось:

1. **Источник цены `MARK_PRICE` / `INDEX_PRICE`** — реджект создания либо
   снятие ограничения (кейс `U22.8` предмета
   `strategy-definition-validation`, красный по построению).
2. **Читатель `exitOutcome`.**
3. **Ключи demo-окружения OKX и распечатанный Vault** — без них не
   прогоняется ни один кейс мишени `-D` и весь `smoke-live`.

Плюс прежние ожидания (Vault-хардненинг, миграция на Jackson 3, область
тестового токена) — печатает `py tools/backlog-check.py` строкой `держатель:`.

## Что висит

Всё, что живёт секциями бэклога, печатает `py tools/backlog-check.py`; в
снапшоте не дублируется. Вне бэклога список не изменился: пункты 1-14
§«Что висит» снапшота v302 остаются в силе дословно
(`.claude/work/history/snapshots/snapshot-v302.md`), **кроме п. 6** (закрыт
заходом 68). Существенное: стенд отстал от дерева кода; вход снаружи в
окружение не построен; **пары производитель-потребитель из ДВУХ сервисов** на
живом брокере по-прежнему не встречалось.

Три больших свипа с измеренным объёмом остаются открытыми: слой опровержения
в `docs/**`, свод адресных перекрёстий, долг §-адресов
(`tools/anchor-debt.txt`) и процедура синка в доках. Каждый — своя единица.

**Пользователю:** обновить Project Knowledge (этот снапшот); прочитать блоки
дайджеста Д1564-Д1923 — право вето постфактум.

## Ловушки

**Ловушки** v206-v384 остаются в силе. Подтверждено и добавлено этой сессией:

- **Автозаведение тем у брокера отнимает вход у клетки.** Пока оно включено,
  тема, объявленная подпиской и у брокера отсутствующая, появляется первым же
  спросом метаданных потребителя, а снятая тема возвращается обратно — то
  есть оживляет приём ровно там, где клетка его хоронит.
- **Снятие темы роняет назначение на время ребалансировки, и такт этой
  минуты уносит ряды ВСЕ.** Ожидание, написанное только на исчезновении ряда
  ушедшей темы, сходится на такте НЕЖИВОГО приёма — то есть по причине
  соседней клетки. Условием стои́т пара: ряда ушедшей нет, ряд оставшейся
  есть.
- **Пауза, выраженная ростом чужого ряда, привязывает клетку к чужому
  предмету.** Она честнее сна потока (платит ровно столько, сколько нужно),
  но мутация того предмета красит клетку, чей предмет цел. Направление
  ошибки безопасное, и цена называется при письме, а не после прогона.
- **Состав рядов возраста внутри живого контекста неподвижен по
  построению:** тик приводит строки к объявленной подписке тем же тактом,
  которым считает ряды. Клетка о сужении состава на этом ряде мерила бы
  недостижимое.
- Прочие ловушки — снапшоты v283, v285-v384 §Ловушки; они не переписываются.

Обязательное перед завершением любой единицы:

1. **гейт инструментов корпуса** целиком — перечень в
   `.claude/skills/update-roadmap-progress.md` §«Гейт инструментов корпуса»
   (шестнадцать команд), `backlog-check.py` с подменой статуса, если статус
   проставляется;
2. **реактор — `bash tools/reactor-test.sh`**, если тронуто дерево кода.

## Проверки на момент снапшота

**Гейт инструментов корпуса прогнан** целиком; все шестнадцать команд вернули
0. **Реактор прогнан** — 1558 файлов, 4431 тест, дефектов 0 (против
1552/4418 у v384: прибавка — шесть классов группы `B3` и её тринадцать
прогонов). Пакет ящика прогнан отдельно — 43 теста, все зелены (помеченная
`debt` клетка в умолчание не входит):

```bash
mvn -o -am -pl services/audit test -Dtest='com.example.audit.box.*Test' -Dsurefire.failIfNoSpecifiedTests=false
```

## Среда

Против v132 не изменилась, кроме состава локального репозитория по тестовым
библиотекам и **образов субстрата ящика** (строки «Артефакты тестового
контура» и «Образы субстрата ящика»), а также тестовых областей одиннадцати
модулей (`test.excluded.groups`: счёт печатает
`grep -rl 'test.excluded.groups' --include=pom.xml .`). Дом фактов среды — команды сборки, пути JDK
и Maven, лаунчер Python, Vault, demo-ключ и прайминг —
`.claude/skills/environment-commands.md`.
