# Снапшот v347

**Дата:** 2026-09-19.

## На какой вопрос отвечает этот файл

Где мы сейчас.

## Состояние

Сменяет v346. Фаза 1 — `FOLDED`. **Фаза 2 — `IN_PROGRESS`**: шаги 1-11
`DONE`, **шаг 12 — `CODE·2/3`**, шаг 13 `HOLD`. Прод-рубеж — `HOLD`.

**Машина шага** — `CONCEPT → CONCEPT_REVIEW → CODE → DOCS → REVIEW → DONE`
(`.claude/processes/roadmap-step-execution.md`). `CODE` тестового шага идёт
тремя фиксированными под-шагами: тест-кейсы → адверсариальное ревью кейсов →
код тестов.

**Петли ревью кейсов нет — есть критерий и потолок в три круга** (решение
держателя 2026-09-17, `.claude/decisions/case-review-approval-criterion.md`;
дом формулировки — `.claude/skills/test-review.md` §«Критерий апрува документа
кейсов»). Апрув блокирует **только невалидность кейса**. **Круг — ревью либо
перепроверка с записанным вердиктом; правка автора между кругами кругом не
является.**

**Уровни 1 и 3 закрыты целиком** — восемь из восьми и пять из пяти.
**Уровень 2: тринадцать документов апрувлено, четырнадцатый прошёл первый
круг, получил браковку и ПРАВКУ автора; ждёт перепроверки (круг второй из
трёх).** Ненаписанных предметов уровня 2 больше нет.

**Дельта — в рабочем дереве, не закоммичена.** Ветка `claude-audit`.
**Дерево кода эта сессия не меняла** — оно читалось и прогонялось пробами вне
репозитория; реактор не прогонялся и не был нужен.

**Режим — автономия до прод-рубежа.** Решения — Д250-Д1799.

## Что сделано после v346

Одна единица: **правка автором по находкам первого круга
`jsonb-overlay-roundtrip`** (заход 66). Отчёт —
`.claude/work/progress/phase-2-step-12-hand-test-jsonb-overlay-roundtrip.md`
§«Правка по находкам первого круга — передача на перепроверку». Здесь —
только то, что меняет картину.

- **Все три гейтящие закрыты, четыре негейтящие исполнены той же единицей.**
  Кейсов стало 81 вместо 76; находок владельцам — 12 вместо 9 (`F10`, `F11`,
  `F12`, у каждой своя секция бэклога).
- **Базовая сборка документа сменилась на маппер сборки бина** — решение
  принято один раз и распространено на двенадцать групп; свежий маппер в
  кейсе теперь незаконен.
- **Заход шёл двумя сессиями, и это названо в отчёте.** Первая внесла правку
  в документ и **не записала исхода** — ни отчёта, ни строки хроники;
  приземляемая точка была пропущена. Вторая дописала отчёт, хронику и снапшот.
- **Запись отчёта нашла собственную ложность измеренного клейма.** «Различий
  у свежего маппера с бином ровно два, набор модулей у обоих пуст» мерило
  `Jackson2ObjectMapperBuilder` **в одиночку**; бин собирает
  `Jackson2AutoConfiguration`, который вешает на сборщик
  `StandardJackson2ObjectMapperBuilderCustomizer` с картой `FEATURE_DEFAULTS`,
  а модули сборщик берёт служебной загрузкой. Против бина расхождений
  **четыре плюс три модуля**; исправленная форма пина даёт **ноль**
  расхождений и совпадающие множества модулей. Клейм правлен **на месте**
  (`.claude/rules/self-description-form.md`).

## Вход следующей сессии

**Следующая единица — перепроверка документа `jsonb-overlay-roundtrip`,
круг второй из трёх** (`.claude/skills/test-review.md`, фокус `test-review`,
прогоняет не автор). С апрувом этого документа **уровень 2 закрывается
целиком — четырнадцать предметов из четырнадцати**, и под-шаг 2 `CODE`
заканчивается: открывается под-шаг 3 (код тестов).

**Что перепроверке читать первым** (названо отчётом правки, не выводить
заново): `.claude/tests/cases/jsonb-overlay-roundtrip.md` §«Маппер сборки
бина — что это и почему не свежий» — сборка, на
которой стои́т весь документ, менялась дважды за один заход; клетки `U7.1`-
`U7.3`, `U10.3` — состав ключей строки выводится теперь **командой**, а не
чтением полей.

**Что форма захода уже включает** (не выводить заново) — всё, что перечисляли
v325-v346 §«Вход следующей сессии», остаётся в силе дословно
(`.claude/work/history/snapshots/snapshot-v325.md` …
`…/snapshot-v346.md`). **Заход 66 добавляет одну ось:**

- **Бин автоконфигурации — не то же, что маппер её сборщика.** Между ними
  стои́т кастомайзер Boot: два признака и набор модулей приезжают **от него**.
  Утверждение о конфигурации бина, измеренное против сборщика в одиночку,
  ложно — и выглядит измеренным. Сверяется прогоном на четырёх мапперах
  сразу: свежий, сборщик, сборщик плюс `FEATURE_DEFAULTS`, пин.

## Что держателю решать

Против v346 не изменилось:

1. **Источник цены `MARK_PRICE` / `INDEX_PRICE`** — реджект создания либо
   снятие ограничения.
2. **Читатель `exitOutcome`.**
3. **Ключи demo-окружения OKX и распечатанный Vault** — обещаны к под-шагу 3
   `CODE`; без них не прогоняется ни один кейс мишени `-D` и весь
   `smoke-live`.

Плюс прежние ожидания (Vault-хардненинг, миграция на Jackson 3, область
тестового токена) — печатает `py tools/backlog-check.py` строкой `держатель:`.

## Что висит

Всё, что живёт секциями бэклога, печатает `py tools/backlog-check.py`; в
снапшоте не дублируется. Вне бэклога список не изменился: пункты 1-14
§«Что висит» снапшота v302 остаются в силе дословно
(`.claude/work/history/snapshots/snapshot-v302.md`). Существенное: стенд
отстал от дерева кода; ни одна пара производитель-потребитель не встречалась
на живом брокере; вход снаружи в окружение не построен; обязанности заведения
каталога `tests/` принадлежат под-шагу 3; архивов Testcontainers и WireMock в
локальном репозитории нет.

**Три негейтящие находки формы у `trading-core-calc`** (`Ф-5`, `Ф-6`, `Ф-7`) и
**семь у `platform-shared-logic`** (`Ф-8`-`Ф-14`) по-прежнему ждут ближайшего
захода по своим документам — их дом отчёт перепроверки, секций бэклога они не
получают.

Три больших свипа с измеренным объёмом остаются открытыми: слой опровержения
в `docs/**`, свод адресных перекрёстий, долг §-адресов
(`tools/anchor-debt.txt`) и процедура синка в доках. Каждый — своя единица.

**Пользователю:** обновить Project Knowledge (этот снапшот); прочитать блоки
дайджеста Д1564-Д1799 — право вето постфактум.

## Ловушки

**Ловушки** v206-v346 остаются в силе. Подтверждено и добавлено этой сессией:

- **Heredoc в этой оболочке работает** — в том числе с закавыченным
  разделителем (`py - <<'EOF'`), вопреки записи v346; ломается не он, а
  длинная однострочная кавычка. Для правок по месту рабочая форма —
  короткий скрипт `py` с `assert` на число попаданий: подмена, не нашедшая
  цели, падает, а не молчит.
- **`python` и `python3` — заглушки Windows Store** (подтверждено ещё раз:
  скрипт через `python` вышел кодом 49, ничего не выполнив). Только `py`.
- **Java-инструменты Windows путей вида `/c/Users/…` не понимают** — `$(pwd -W)`
  и `C:/Users/…`, разделитель `;` (v346).
- Прочие ловушки — снапшоты v283, v285-v346 §Ловушки; они не переписываются.

Обязательное перед завершением любой единицы:

1. **гейт инструментов корпуса** целиком — перечень в
   `.claude/skills/update-roadmap-progress.md` §«Гейт инструментов корпуса»,
   `backlog-check.py` с подменой статуса, если статус проставляется;
2. **реактор — `bash tools/reactor-test.sh`**, если тронуто дерево кода.

## Проверки на момент снапшота

**Гейт инструментов корпуса прогнан** целиком; исход в конверте сессии.
Подмены статуса не требовалось: статус шага не менялся. **Реактор не
прогонялся и не требовался** — дерево кода эта сессия не меняла.

## Среда

Против v132 не изменилась. Дом фактов среды — команды сборки, пути JDK и
Maven, лаунчер Python, Vault и demo-ключ —
`.claude/skills/environment-commands.md`. Сборка: JDK `corretto-25.0.3`,
maven из дистрибутива IDEA (`REACTOR_MVN`), в `PATH` его нет. Состав
локального репозитория по тестовым библиотекам — §«Что висит» снапшота v302,
п. 6.
