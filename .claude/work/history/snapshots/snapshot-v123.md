# Snapshot v123

**Дата:** 2026-09-05.

## На какой вопрос отвечает этот файл

Где мы сейчас. **Шаг 1 фазы 2 закрыт в `DONE`: фокусы прогнаны, находка
закрыта, доки синхронизированы.** Сменяет v122.

## Состояние

Фаза 1 — `FOLDED`. **Фаза 2 — `IN_PROGRESS`**: шаг 1 `DONE`, шаги 2-12
`HOLD`. Фазы 3-6, 7-8 и прод-рубеж — `HOLD`. Ветка `claude-audit`; дельта
задачи — в working tree, не закоммичена.

**Режим — автономия до прод-рубежа** (горизонт закрыт, ARCH-Q3). Решения —
Д250-Д590 в дайджесте (итерация 28 — новая). **Открытый вынос держателю
один:** ARCH-Q1 в остаточной части — какой именно провайдер облака и какая
CI-система; класс закрыт, гейтит только прод-рубеж.

## Что сделано в этой задаче

**Шаг 1 фазы 2 доведён до `DONE`.** Итог, отчёты фокусов, таблицы проверок
и разбор расхождений —
`.claude/work/history/2026-09-05-phase-2-step-1-monorepo.md`.

- **`conventions` — находка К1, закрыта на уровне класса.** Пять
  `README.md` деревьев монорепозитория, объявленных файлами знания
  решением этого же шага (Д582), остались вне области свипа
  `tools/retired-check.py`; `web/` и `deploy/` не покрывались вовсе.
  Инструмент получил **ось 20** — сверку области с фактическим корнем
  репозитория (Д588); обе стороны фальсифицирующей пробы записаны.
- **`performance` / `disaster` / `security`** — предмет назван у каждого,
  находок нет; отказ якоря корня предъявлен прогоном (код 2 «не
  измерялось», не код 1).
- **`SYNC_DOCS_FROM_CODE`** — 14 расхождений: родовая ссылка `src/**`
  после переезда дерева кода не разрешается ни во что (Д590), пересказ
  области свипа в решении сведён к указателю (Д589).
- **Проверки зелёные:** девять команд корпуса код 0 (носителей 1057 против
  1052 до правки — ровно пять `README.md`),
  `mvn -o -f donor/pom.xml test -DexcludedGroups=source-api-live` →
  **319 тестов, 0 падений**.

## Что ратифицировано и не пере-решается

Всё из v104-v122. Ключевое от держателя: класс облака (управляемый
Kubernetes у российского провайдера, провайдер — потом), хостинг кода и CI
решаются той же проработкой, порядок фаз 3-6, автономия до прод-рубежа,
состав ролей с границей «деньги» между `TRADER` и `VIEWER`,
монорепозиторий = текущий репозиторий, донор в `donor/`.

Из решений машины (Д577-Д590) ключевые: `docs/` не раскалывается по
сервисам (ось — карта, цена названа и заякорена); дом оси — своё правило;
якорь корня репозитория внутри `Spec`; `README.md` кодового дерева — файл
знания; область свипа `retired-check.py` сверяется с фактическим корнем
репозитория.

## Что висит

1. **`подтверждение`** — ARCH-Q1 в остатке: провайдер и CI-система.
   Проработка и её шесть критериев —
   `.claude/work/backlog.md` §«Проработка облака, хостинга кода и CI»;
   оживит — момент, когда роадмап упрётся в инфраструктурные шаги.
   **Шага 2 это не гейтит:** он строит провайдер-нейтральную форму.
2. **Форма закрытия фокуса без находок** — практика «предмет назван +
   вердикт назван» сложилась на одном примере; поднимать ли её в клаузу
   гейта `DONE`, покажет второй случай
   (`.claude/work/backlog.md` §«Статус-набор шага не покрывает шаг
   реструктуризации»; оживит — шаг 2).
3. **Два якоря шага 1:** §«Энфорсер оси владельца-сервиса» (оживит —
   первый шаг порта, читающий карту) в `.claude/work/backlog.md`.
4. **Прежние хвосты** — S1 Vault, контракт броска диспетчера, ограничения
   контура доступа (оживит шаг 4 фазы 2), рост таблицы отказов, A12, хвост
   шага 8, долг адресных перекрёстий `docs/**`, провизорные числа
   калибровки, дубль эталона стратегии в корне (D-P6 / E-П3, владелец
   `knowledge-curator`) — свои якоря в бэклоге.
5. **Пользователю:** переиндексировать проект в IDEA — корень модуля
   переехал в `donor/`. И обновить Project Knowledge: этот снапшот,
   дайджест (итерация 28), `structure-digest.md`.

## Что дальше

**Шаг 2 — платформа:** Kubernetes, GitOps (раннеры в кластере + Argo CD),
операторы данных (Postgres+Timescale, Kafka, Vault, Elastic,
Prometheus/Grafana, Jaeger), окружения `dev`/`stage`/`prod` пространствами
имён. Провайдер-специфичного не пишется — кластер вне облака, манифесты
нейтральны; конкретного `.yml` одного хостинга CI тоже нет, только форма.

## Среда

Изменений с v122 нет. Место сборки — `donor/pom.xml`, команды прогона идут
из корня с `-f donor/pom.xml`. Дом фактов —
`.claude/tests/source-api/okx/code-preconditions.md`.
**Maven в `PATH` отсутствует** (рабочий — из поставки IDEA);
**`JAVA_HOME` среды указывает на JDK 11**, рабочая форма —
`JAVA_HOME=$HOME/.jdks/corretto-25.0.3`; лаунчер Python — `py -3`.
Принципал профиля `test` — в Vault (`secret/tradingbot/api-access-test`).
