# Snapshot v282

**Дата:** 2026-09-16.

## На какой вопрос отвечает этот файл

Где мы сейчас.

## Состояние

Сменяет v281. Фаза 1 — `FOLDED`. **Фаза 2 — `IN_PROGRESS`**: шаги 1-11
`DONE`, **шаг 12 — `CODE`**, шаг 13 `HOLD`. Прод-рубеж — `HOLD`.

**Машина шага** — `CONCEPT → CONCEPT_REVIEW → CODE → DOCS → REVIEW → DONE`
(`.claude/processes/roadmap-step-execution.md`). `CODE` тестового шага идёт
тремя фиксированными под-шагами: тест-кейсы → адверсариальное ревью кейсов →
код тестов. **Под-шаг 1 закрыт целиком** — 21 документ кейсов плюс ревизия
существующего набора; шаг входит в **под-шаг 2**.

**Дельта — в рабочем дереве, не закоммичена.** Ветка `claude-audit`.
**Дерево кода эта сессия не трогала** — реактор не прогонялся и не был
нужен (`git status --porcelain -- services/ web/ deploy/` пуст).

**Режим — автономия до прод-рубежа.** Решения — Д250-Д1742.

## Что сделано после v281

Одна единица: **двадцать третий заход под-шага 1 `CODE` — ревизия
существующего набора тестов**, последний предмет перечня и единственный
некейсовой формы (`.claude/work/progress/phase-2-step-12-existing-tests-revision.md`).

1. **Каждый класс тестовых деревьев `services/**` получил строку:**
   механический признак, мерённый грепом (закрытый словарь из семи букв), и
   исход из закрытого словаря шести значений — `логика`, `в-уровень-2`,
   `в-ящик`, `структура`, `корпус`, `оснастка`. Правило вывода исхода
   названо вместе с очерёдностью; отклонения от него — поимённо.
2. **Ни один тест не снят и не правлен.** Снятие принадлежит под-шагу 3 и
   наступает не раньше, чем поведение покрыл ящик (ответ держателя).
3. **Названа цена сноса класса `структура`** — предметы, которых ящик не
   воспроизводит ни при какой расстановке: границы транзакций приёма,
   раздел колонок между писателями, присутствие аннотации на тропе,
   объявленный классом состав конверта. Развилку решает заход снятия.
4. **Одна находка припаркована:** у общего артефакта `common/platform`
   предмета уровня 2 нет, а классы его проверяются из тестовых деревьев
   четырёх сервисов; у `common/model/domain` своего тестового дерева нет
   вовсе. Секция бэклога «Классы общих артефактов проверяются из тестовых
   деревьев сервисов».
5. **Развилки письма — дайджест Д1736-Д1742.**

## Вход следующей сессии

**Под-шаг 2 — адверсариальное ревью кейсов** (`.claude/skills/test-review.md`).
Вход: двадцать один документ `.claude/tests/cases/`, перечень предметов
(`.claude/decisions/test-contour-design-pass.md`) и ревизия существующего
набора. Находку о `common/platform` читает это же ревью: решать, нужен ли
артефакту предмет уровня 2, — его работа, а не под-шага 1.

- **Под-шаг 3 не начинается** до апрува кейсов на под-шаге 2.
- **Дом сквозного набора — каталог `tests/` верхнего уровня**, и обязанности
  его заведения принадлежат под-шагу 3.

## Что держателю решать

Против v281 не изменилось:

1. **Источник цены `MARK_PRICE` / `INDEX_PRICE`** — реджект создания либо
   снятие ограничения; плюс соседний вопрос того же предмета: источник цены
   у ценового операнда условия не читается никакой.
2. **Читатель `exitOutcome`.**
3. **Ключи demo-окружения OKX и распечатанный Vault** — обещаны к
   под-шагу 3 `CODE`; без них **ни один кейс мишени `-D`** чёрного ящика
   коннектора не прогоняется, как и весь `smoke-live`.

Плюс прежние ожидания (Vault-хардненинг, общий ход миграции на Jackson 3,
область тестового токена) — печатает `py tools/backlog-check.py` строкой
`держатель:`.

## Что висит

Всё, что живёт секциями бэклога, печатает `py tools/backlog-check.py`; в
снапшоте не дублируется. Вне бэклога:

1. **Стенд от дерева кода отстал**, базы `audit` и `statistics` не
   пересоздавались (секция бэклога).
2. **Ни одна пара производитель-потребитель не встречалась на живом
   брокере.** У `trading-core` и `strategies` контекст ящика к тому же
   сегодня не поднимается — автоконфигурации брокера в дереве нет, и **все
   пять написанных троп уровня 3 красны за публикацией**.
3. **Вход снаружи в окружение не построен ни одним звеном** — две секции
   бэклога; до них дым не прогоняется даже с ключами.
4. **Обязанности заведения каталога `tests/`** принадлежат под-шагу 3:
   строка `structure.md`, `README.md`, шаблон в `ROOTS` у
   `retired-check.py`, путь сноса классов в `reactor-test.sh`, **аргументы
   реактора `verify`** и модуль в корневом `pom.xml`.
5. **Тестового дерева у `services/common/model` и `services/common/platform`
   нет вовсе** — теперь это секция бэклога, а не только заметка снапшота;
   перенос классов принадлежит под-шагу 3.
6. **WireMock под JDK 25 прогоном не проверен**; **jar'ов Testcontainers и
   WireMock в локальном репозитории нет** — прайминг под-шага 3.
7. **Записей площадки для мишени `-S` нет ни одной** — кейсы стаба написаны
   от контракта, и первый прогон `-D` обязан их снять.
8. **Сериализатор групп `-J` под-шаг 3 обязан взять тот же, что на
   проводе.** В дереве лежат обе линии, конвертеры выбирают третью.
9. **У расчётного слоя рыночных данных тестов нет ни одного**, и класс
   непрогоняемых кейсов там держится отсутствием дома формул — не средой.
10. **Пользователю:** обновить Project Knowledge (этот снапшот); прочитать
    блоки дайджеста Д1564-Д1742 — право вето постфактум.

Три больших свипа с измеренным объёмом остаются открытыми: слой
опровержения в `docs/**`, свод адресных перекрёстий `§«`, долг §-адресов
(`tools/anchor-debt.txt`) и процедура синка в доках. Каждый — своя единица.

## Ловушки

**Ловушки** v206-v281 остаются в силе. Подтверждённые этой сессией и новые:

- **`verify(` в тексте теста не означает Mockito.** Доменный метод
  `verify(билет)` и `MockRestServiceServer.verify()` попадают в любой
  наивный шаблон; признак, ошибающийся в разрешающую сторону, приписывает
  классу чужой исход, и проверить его нечем.
- **Признак теста-наследника читается у его контракта, а не у файла.**
  Класс без единого `@Test` может быть полноценной пробой: форма живёт в
  общем артефакте (`SchedulerCapacityContract`).
- **Heredoc `<<'PY'` в этой среде съедает одиночный обратный слеш** внутри
  строкового литерала Python — скрипт падает синтаксической ошибкой.
  Длинные скрипты пишутся `Write`-ом в `.claude/work/progress/` и удаляются
  тем же ходом.
- **§-адрес с вложенными кавычками `«…»` внутри имени пассажа битый по
  построению.**
- **Замена длинного имени по корпусу не берёт вхождение, разорванное
  переносом строки.**
- **§-адрес разрешается к файлу, названному в ТОМ ЖЕ абзаце.**
- **§-адрес, закрытый обратной кавычкой вместо закрывающей ёлочки, ни одним
  прогоном не ловится.**
- **§-адрес к величине спеки формой `§имяВеличины` не разрешается.**
  Рабочая форма — «`docs/spec/<файл>.json`, величина `<имя>`».
- **Условие `закрыто-когда` мерится построчно.**
- **Два сервиса на одном classpath не живут.**
- **`python` в этой среде исполняет заглушку молча** — зовётся `py`.
- **Счётный клейм в прозе — дефект формы**, даже когда число верно
  (`.claude/rules/self-description-form.md`).
- **§-адрес на секцию бэклога, которой ещё нет, роняет прогон.** Находка и её
  парковка пишутся **одним** заходом, и парковка — первой.
- **Составление длинного документа кусками:** `Write` во временный файл,
  `cat … >> цель`, `rm`.
- **Таблица, у которой в строках больше клеток, чем в шапке, ни одним
  прогоном корпуса не ловится.**
- **`py` вместо `python3`**; вывод инструментов рвётся на кириллице без
  `PYTHONIOENCODING=utf-8`; `grep -P` кириллицу в `\w`, `\b`, `\d` не берёт.

Обязательное перед завершением любой единицы:

1. **гейт инструментов корпуса** целиком — перечень в
   `.claude/skills/update-roadmap-progress.md` §«Гейт инструментов корпуса»,
   `backlog-check.py` с подменой статуса, если статус проставляется;
2. **реактор — `bash tools/reactor-test.sh`**, если тронуто дерево кода.

## Проверки на момент снапшота

**Гейт инструментов корпуса прогнан** целиком: ненулевых кодов нет, кода 2
нет. Подмены статуса не требовалось — статус шага не менялся. **Реактор не
прогонялся и не требовался** — дерева кода сессия не касалась. Что инвентарь
ревизии накрывает тестовые деревья целиком, предъявлено командой сверки
мультимножеств —
`.claude/work/progress/phase-2-step-12-existing-tests-revision.md`
§«Равенство перечней предъявляет прогон, а не проза»: выдача пуста.

## Среда

Против v132 не изменилась. Дом фактов среды — команды сборки, пути JDK и
Maven, лаунчер Python, Vault и demo-ключ —
`.claude/skills/environment-commands.md`. Сборка: JDK `corretto-25.0.3`,
maven из дистрибутива IDEA (`REACTOR_MVN`), в `PATH` его нет. Состав
локального репозитория по тестовым библиотекам — п. 6 §«Что висит».
