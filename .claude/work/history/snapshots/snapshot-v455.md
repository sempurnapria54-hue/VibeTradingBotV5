# Снапшот v455

**Дата:** 2026-09-25.

## На какой вопрос отвечает этот файл

Где мы сейчас.

## Состояние

Сменяет v454. Фаза 1 — `FOLDED`. **Фаза 2 — `IN_PROGRESS`**: шаги 1-11
`DONE`, **шаг 12 — `REVIEW`**, шаг 13 `HOLD`. Прод-рубеж — `HOLD`.

**Машина шага** — `CONCEPT → CONCEPT_REVIEW → CODE → DOCS → REVIEW → DONE`
(`.claude/processes/roadmap-step-execution.md`).

**Дельта — в рабочем дереве, не закоммичена** (поверх `142e0f88`). Ветка
`claude-audit`.

**Режим — автономия до прод-рубежа.** Решения — Д250-Д2176
(`.claude/work/decision-digest.md`).

## Что сделано после v454

- **Заход 178 — тестер, группа `E2` тропы выхода** (хроника шага, заход 178;
  Д2172-Д2176). `ExitCascadePathTest`: пять клеток зелены, `E2.3` красна `debt`
  по находке `F7`; `E2.2` поправлена на месте. У стаба площадки — сценарии,
  которые переводит принятая команда. Новые находки `code-writer`: `F7`
  (поверхность сделки отдаёт экспозицию транша без приписанного объёма
  закрытия) и `F8` (сосед того же прохода проверке одновременного риска не
  виден) — свои секции бэклога. Ловушки — TC-200, TC-201. Разведка группы `E3`
  по коду записана в хронике.

## Вход следующей сессии

Единица — секция тестера `.claude/work/backlog.md` §«Клетки тропы активации за
копией определения ждут закрытия находки `F6`»; продолжить с группы `E3`
документа `e2e-exit-and-close` по разведке записи захода 178 (пролог —
`ExitTrail#walkToScaledIn` до терминальных траншей при пустой истории площадки,
затем запись закрытия), затем `E4`-`E7` и документ `e2e-safety-teardown`
целиком. Секции находок `F7`, `F8` тропы выхода и прочие секции `сейчас` шага —
кодовые заходы `code-writer`.

## Что держателю решать

1. **Апрув `REVIEW` шага 12** — по условию держателя: правки кода по находкам
   сделаны, все тесты шага написаны и зелены. Открытыми остаются клетки
   сквозных троп (секция выше), клетки таблицы §«Кейсы, не прогоняемые
   сегодня» документа кейсов `trading-core` и секции бэклога с маркером
   `сейчас`.
2. **Плечо пар стенда** — вход по паре без назначенного плеча отвергается;
   к первому прогону на стенде плечо назначается точкой
   `PUT …/pair-settings/…` (число — риск-аппетит держателя).

Прочие ожидания держателя печатает `py tools/backlog-check.py` строкой
`держатель:`.

## Что висит

Висящее живёт секциями `.claude/work/backlog.md` и
`.claude/work/prod-checks.md` и печатается `py tools/backlog-check.py`; в
снапшоте не дублируется.

**Пользователю:** обновить Project Knowledge (этот снапшот).

## Ловушки

Ловушки живут в домах по своему вопросу; снапшот их не держит. В разделе
ловушек дома — оглавление: споткнулся — найди строку по словам области и
симптома, тело читай по номеру в `.claude/traps/<дом>-traps.md`.

- среда, оболочка, правка корпуса скриптом, площадка OKX —
  `.claude/skills/environment-commands.md` §«Ловушки и обходы»;
- код тестов — `.claude/skills/test-code.md` §«Ловушки и обходы»;
- письмо кейсов и их ревью — `.claude/skills/test-design.md`,
  `.claude/skills/test-review.md`;
- продуктовый код — `.claude/rules/codestyle.md` §«Ловушки»;
- команды корпуса и свипы — `.claude/rules/measurement-commands.md`;
- условие маркера бэклога — `.claude/rules/backlog-section-form.md`
  §«Ловушки»;
- гейт и статус — `.claude/skills/update-roadmap-progress.md`;
- цикл сессий — `.claude/skills/session-chain.md` §«Ловушки и обходы».

## Проверки на момент снапшота

Сборка модуля `tests` с зависимостями захода 178 (`mvn -o -am -pl tests
verify`): 12 модулей, 4821 тест, дефектов 0, 47 минут. Гейт инструментов
корпуса — одним прогоном на финальном состоянии захода 178; исход — запись
захода.

## Среда

Дом фактов и ловушек среды — `.claude/skills/environment-commands.md`;
стенда — `.claude/skills/local-stand.md`. Изменений среды нет.
