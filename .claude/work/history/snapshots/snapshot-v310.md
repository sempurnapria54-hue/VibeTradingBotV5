# Snapshot v310

**Дата:** 2026-09-17.

## На какой вопрос отвечает этот файл

Где мы сейчас.

## Состояние

Сменяет v309. Фаза 1 — `FOLDED`. **Фаза 2 — `IN_PROGRESS`**: шаги 1-11
`DONE`, **шаг 12 — `CODE·2/3`**, шаг 13 `HOLD`. Прод-рубеж — `HOLD`.

**Машина шага** — `CONCEPT → CONCEPT_REVIEW → CODE → DOCS → REVIEW → DONE`
(`.claude/processes/roadmap-step-execution.md`). `CODE` тестового шага идёт
тремя фиксированными под-шагами: тест-кейсы → адверсариальное ревью кейсов →
код тестов. Под-шаг 1 закрыт целиком; **идёт под-шаг 2**.

**Петли ревью кейсов больше нет — есть критерий и потолок в три круга**
(решение держателя 2026-09-17,
`.claude/decisions/case-review-approval-criterion.md`; дом формулировки —
`.claude/skills/test-review.md` §«Критерий апрува документа кейсов»). Апрув
блокирует **только невалидность кейса**; пробел покрытия, пробел дока и форма
круга не дают.

**Апрув получили два предмета из перечня** (перечень и порядок —
`.claude/decisions/test-contour-design-pass.md`): `auth` (заход 28),
`connector-okx` (заход 29). Следующий предмет под-шага 2 — третий по перечню,
по тому же критерию.

**Дельта — в рабочем дереве, не закоммичена.** Ветка `claude-audit`.
**Дерево кода эта сессия не меняла** (только читала исходники сервиса) —
реактор не прогонялся и не был нужен.

**Режим — автономия до прод-рубежа.** Решения — Д250-Д1784.

## Что сделано после v309

Одна единица: **апрув кейсов `connector-okx` по критерию валидности** (заход
29) — `.claude/work/progress/phase-2-step-12-approve-test-connector-okx.md`.
Состав здесь не пересказывается (`.claude/rules/policy-home.md`). Коротко:

- **Гейтящая находка одна, и она системная:** двадцать два кейса пинили
  HTTP-число (`422`/`502`/`503`), которого **не фиксирует ни один дом** —
  `docs/components/IntegrationService.md` §«Классы отказа на границе — дом
  здесь» и `docs/rules/error-handling-policy.md` §«Внешняя поверхность»
  объявляют коды провизорными, javadoc `GlobalExceptionHandler` повторяет то
  же. Такой кейс краснел бы на **исправной** системе ровно ходом
  выравнивания.
- **Ожидания переведены на класс отказа** (поле `code` закрытого перечня
  дома), число отдано прогону; заведена секция `.claude/tests/cases/connector-okx.md`
  §«Число ответа и класс отказа — разные ожидания». Числа, написанные **не нами** (`200`, `401`, `405`,
  `404`), оставлены с домами — та же форма, что у одобренного `auth`.
- **Перебор перечней дома поимённо** дал два пробела покрытия: постановка
  условной заявки (`POST …/algo-orders`) и постановка заявки со встроенной
  защитой — G1, G2, исход под-шаг 3.
- **Две находки владельцам с секциями бэклога:** F-5 — формы prehash подписи
  приватного запроса в корпусе нет (единственный носитель — код); F-6 —
  четыре живых дока называют исполнителем снятый класс донора
  `OkxIntegrationService`.
- Все 90 кейсов валидны, **апрув дан**; под-шаг 3 по `connector-okx` открыт.

## Вход следующей сессии

**Следующая единица — применение того же критерия к третьему предмету
перечня** (`.claude/decisions/test-contour-design-pass.md` §«Перечень
предметов и порядок работы»). Форма захода отработана дважды и читается с
двух отчётов: `phase-2-step-12-approve-test-auth.md` и
`phase-2-step-12-approve-test-connector-okx.md`.

**Что форма захода уже включает** (не выводить заново):

- метка своей дельты отбивается **до первой правки** (`git write-tree`);
- механические оси мерятся разбором документа: четыре поля у каждого кейса,
  существование путей-носителей, разрешимость §-адресов;
- каждый перечень дома (операции, классы отказа, рёбра) **перебирается
  поимённо**, и непокрытая единица уходит меткой `G` в раздел
  документа кейсов, заведённый образцом в `.claude/tests/cases/connector-okx.md`
  §«Пробелы покрытия»;
- ожидание, у которого дом молчит, берётся из кода с названным звеном и
  пометкой «прогоном не проверено», а пробел уходит находкой `F` с секцией
  бэклога;
- отчёт несёт таблицу самопроверки автора по критерию
  (`.claude/skills/test-design.md`).

**Класс, найденный дважды и стоящий проверки у каждого следующего предмета:**
пиньнутая величина, которой названный носитель не фиксирует. У `auth` это
было число `500` на опровергнутой тропе, у `connector-okx` — двадцать два
числа при доме, объявляющем коды провизорными.

**Исходники распаковываются** из локального репозитория Maven (`-sources.jar`
лежат рядом с jar'ами). Каталог распаковки заводится **внутри репозитория** и
удаляется до гейта — см. §Ловушки.

## Что держателю решать

Против v309 не изменилось:

1. **Источник цены `MARK_PRICE` / `INDEX_PRICE`** — реджект создания либо
   снятие ограничения; плюс соседний вопрос того же предмета: источник цены
   у ценового операнда условия не читается никакой.
2. **Читатель `exitOutcome`.**
3. **Ключи demo-окружения OKX и распечатанный Vault** — обещаны к под-шагу 3
   `CODE`; без них **ни один кейс мишени `-D`** чёрного ящика коннектора не
   прогоняется, как и весь `smoke-live`.

Плюс прежние ожидания (Vault-хардненинг, общий ход миграции на Jackson 3,
область тестового токена) — печатает `py tools/backlog-check.py` строкой
`держатель:`.

## Что висит

Всё, что живёт секциями бэклога, печатает `py tools/backlog-check.py`; в
снапшоте не дублируется. Вне бэклога — против v309 список не изменился, и
пункты 1-14 §«Что висит» снапшота v302 остаются в силе дословно
(`.claude/work/history/snapshots/snapshot-v302.md`). Существенное из них:
стенд отстал от дерева кода; ни одна пара производитель-потребитель не
встречалась на живом брокере; вход снаружи в окружение не построен ни одним
звеном; обязанности заведения каталога `tests/` принадлежат под-шагу 3;
архивов Testcontainers и WireMock в локальном репозитории нет.

**Предметов выбранного провайдера идентичности три, и все три — секции
бэклога с владельцем `solution-designer`**, ждущие живого провайдера:
значение заголовка `typ`, привязка токена к сертификату клиента
(`cnf.x5t#S256`) и привязка к ключу предъявителя по DPoP (`cnf.jkt`); дом
ответа у всех трёх один — `docs/architecture/contracts.md` §«Контекст тенанта
в вызове».

Три больших свипа с измеренным объёмом остаются открытыми: слой опровержения
в `docs/**`, свод адресных перекрёстий, долг §-адресов
(`tools/anchor-debt.txt`) и процедура синка в доках. Каждый — своя единица.

**Пользователю:** обновить Project Knowledge (этот снапшот); прочитать блоки
дайджеста Д1564-Д1784 — право вето постфактум.

## Ловушки

**Ловушки** v206-v309 остаются в силе. Подтверждено этой сессией:

- **Пустой каталог `target/` в корне уводит `retired-check.py` в код 2**
  («каталоги верхнего уровня вне объявленной области»). Заводится любой
  разовой пробой или распаковкой; удаляется до гейта. Секция `.claude/work/backlog.md`
  §«Каталог проб `target/` в корне уводит `retired-check.py` в код 2».
- **`grep`-шаблон, берущий §-адрес по первому слову, даёт ложную битую
  ссылку** на адресах с обратными кавычками внутри имени
  (`§«Domain Order → OKX request»`). Проверять такие находки глазами по
  целевому файлу, а не принимать по выдаче.
- Прочие ловушки — снапшоты v283, v285-v309 §Ловушки; они не переписываются.

Обязательное перед завершением любой единицы:

1. **гейт инструментов корпуса** целиком — перечень в
   `.claude/skills/update-roadmap-progress.md` §«Гейт инструментов корпуса»,
   `backlog-check.py` с подменой статуса, если статус проставляется;
2. **реактор — `bash tools/reactor-test.sh`**, если тронуто дерево кода.

## Проверки на момент снапшота

**Гейт инструментов корпуса прогнан** целиком — все пятнадцать команд с кодом
0; исход в §«Гейт инструментов корпуса» отчёта единицы и в конверте сессии.
Подмены статуса не требовалось: статус шага не менялся. **Реактор не
прогонялся и не требовался** — дерева кода дельта не касается
(`git diff --name-only <метка> -- services/ web/` пуст).

## Среда

Против v132 не изменилась. Дом фактов среды — команды сборки, пути JDK и
Maven, лаунчер Python, Vault и demo-ключ —
`.claude/skills/environment-commands.md`. Сборка: JDK `corretto-25.0.3`,
maven из дистрибутива IDEA (`REACTOR_MVN`), в `PATH` его нет. Состав
локального репозитория по тестовым библиотекам — §«Что висит» снапшота v302,
п. 6.
