# Snapshot v255

**Дата:** 2026-09-15.

## На какой вопрос отвечает этот файл

Где мы сейчас. **Шаг 11 фазы 2 закрыт (`DONE`), шаг 12 («Тесты») открыт.**
Сменяет v254.

## Состояние

Фаза 1 — `FOLDED`. **Фаза 2 — `IN_PROGRESS`**: шаги 1-11 `DONE`, шаг 12 —
`DOCS_CHECK_1`, шаг 13 `HOLD`. Прод-рубеж — `HOLD`.

**Гейтящих выносов держателю нет.**

**Дельта — в рабочем дереве, не закоммичена.** Ветка `claude-audit`. Дерево
кода тронуто: два модуля вместо одного плюс три общих артефакта.

**Режим — автономия до прод-рубежа.** Решения — Д250-Д1543.

## Что сделано после v254

**Закрытие шага 11: пять адверсариальных фокусов, `DONE`, открытие шага 12.**
Итог и все отчёты — `.claude/work/history/2026-09-12-phase-2-step-11-split.md`
и подпапка рядом с ним.

- **Семнадцать находок, гейтящих нет.** Двенадцать закрыты по предмету, пять
  припаркованы. Общий механизм большинства: раздел переносил построенное и
  вместе с кодом перенёс его **самоописание** — идентификаторы переименовала
  машина, а проза осталась описывать снятую конструкцию.
- **Класс, который стои́т помнить:** переадресация указателей при переносе
  дома шла **по владельцу файла**, а не по дому пассажа. Неразрешимых
  §-адресов в деревьях кода было 17 на входе шага и 59 перед закрытием;
  после реконсиляции — 12, и ни одного у `audit` и `statistics`.
- **Четыре новые секции бэклога**: пересоздание баз перед подъёмом стенда,
  близнецы модуля приёма, чтение начала ряда на каждые сутки окна, уровень
  `journal` в ключе подключения журнала.
- **Шесть секций взяты в работу** открытием шага 12 — они и есть его
  действующий перечень; печатает `py tools/backlog-check.py`.

## Вход следующей сессии

**Следующая единица — работа шага 12 («Тесты»), статус `DOCS_CHECK_1`.**
Держатель объявил, что на границе шага отдельным промптом запустит **парковку
концепции тестирования**; прямой порядок, если он последует, замещает
последовательность под-шагов на названном шаге, как это было со шагом 11.
**До этого промпта штатный `DOCS_CHECK_1` не начинать.**

**Первое, что обязана прочитать сессия шага 12** — перечень `сейчас` из
выдачи `py tools/backlog-check.py`: шесть секций, оживлённых открытием шага,
и есть его вход.

**Ловушки** v206-v254 остаются в силе целиком. Подтверждённые этой единицей:

- **`py` вместо `python3`** — заглушка Windows Store возвращает 49.
- **Heredoc с кириллицей в теле `bash -c` рвётся** на разборе оболочки:
  длинные тексты надёжнее писать файлом и применять скриптом; для точечных
  правок надёжнее всего короткий python-скрипт с проверкой «вхождений ровно
  одно».
- **`retired-check` и `spec-pointer-check` видят снапшот** — цитата снятой
  редакции в действующем снапшоте роняет прогон.
- **`insertion-neighborhood-check` привязывает дельту к метке отчёта**:
  корпусный заход без отчёта в `work/progress/` отдаёт свою дельту
  предшественнику, и тот краснеет на носителе, которого не трогал. Переезд
  отчётов шага в `history/` это снимает.
- **Вывод в консоли ломает кириллицу** — прогоны с русским текстом читать
  через `PYTHONIOENCODING=utf-8` и файл, а не напрямую.

Обязательное перед завершением любой единицы:

1. **гейт инструментов корпуса** целиком — перечень в
   `.claude/skills/update-roadmap-progress.md` §«Гейт инструментов корпуса»,
   вместе с `backlog-check.py` (с подменой статуса, если статус проставляется);
2. **сборка — `bash tools/reactor-test.sh`**, если тронуто дерево кода;
3. **`py tools/code-gate-check.py`** — перед каждым кодовым заходом.

**Среда:** `python3` в этой оболочке разрешается в заглушку Windows Store и
возвращает код 49 — инструменты корпуса зовутся через `py`.

## Что висит

Всё, что живёт секциями бэклога с маркерами, печатает
`py tools/backlog-check.py` — в снапшоте не дублируется. Вне бэклога висит:

1. **Стенд от дерева кода отстал:** образов двух сервисов не собиралось, база
   `statistics` с гипертаблицами не заводилась, вторая durable-группа на
   живом брокере не поднималась. **Обязанность пересоздать обе базы перед
   подъёмом** названа своей секцией бэклога — оба `V1` переписаны на месте
   при прежних именах баз.
2. **Ни одна пара производитель-потребитель не встречалась на живом брокере.**
3. **Спека и её реализация не связаны ничем механическим** — наблюдение,
   задачи пока нет: держатель о нём знает.
4. **Пользователю:** обновить Project Knowledge (этот снапшот); прочитать
   блоки дайджеста Д1522-Д1536 и Д1537-Д1543 — право вето постфактум.

## Проверки на момент снапшота

**Гейт инструментов корпуса прогнан** целиком, ненулевых кодов нет, кода 2
нет. **Реактор зелен**: полная компиляция всех деревьев и весь unit-набор;
числа печатает сам прогон (`.claude/rules/self-description-form.md`).

## Среда

Против v132 не изменилась; полное описание — там, перечень ловушек — в v206
(`.claude/work/history/snapshots/snapshot-v206.md` §Среда) и он остаётся в
силе.
