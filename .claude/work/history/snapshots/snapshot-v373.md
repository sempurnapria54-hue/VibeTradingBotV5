# Снапшот v373

**Дата:** 2026-09-20.

## На какой вопрос отвечает этот файл

Где мы сейчас.

## Состояние

Сменяет v372. Фаза 1 — `FOLDED`. **Фаза 2 — `IN_PROGRESS`**: шаги 1-11
`DONE`, **шаг 12 — `CODE·3/3`**, шаг 13 `HOLD`. Прод-рубеж — `HOLD`.

**Машина шага** — `CONCEPT → CONCEPT_REVIEW → CODE → DOCS → REVIEW → DONE`
(`.claude/processes/roadmap-step-execution.md`). `CODE` тестового шага идёт
тремя фиксированными под-шагами: тест-кейсы → адверсариальное ревью кейсов →
код тестов. **Под-шаги 1 и 2 закрыты целиком**; **идёт под-шаг 3 — код
тестов.** **Уровень 2 написан целиком (14 из 14); уровня 1 написано три ящика
из восьми, ЧЕТВЁРТЫЙ идёт** — `auth`, `connector-okx`, `market-data` закрыты,
`trading-core` в работе.

**Дельта — в рабочем дереве, не закоммичена.** Ветка `claude-audit`.
**Дерево кода эта сессия меняла** — реактор прогнан, зелен.

**Режим — автономия до прод-рубежа.** Решения — Д250-Д1897.

## Что сделано после v372

Одна единица, **группу не доводящая**: **заход 92 — начало кода группы `B3`
ящика `trading-core`** (команды площадке). Подробности — хроника шага
`.claude/work/progress/phase-2-step-12-chronicle.md` §Статус, «Двадцать
четвёртый заход под-шага 3 (92)»; здесь только то, что меняет картину.

- **Написаны семь клеток `B3`** — `B3.1`-`B3.4`, `B3.7`-`B3.9`
  (`DealCommandBoxTest`). Пять зелены в штатном наборе, две красны ПО
  ПОСТРОЕНИЮ с меткой `debt` (`B3.3`, `B3.9`). Итого у ящика 102 клетки.
- **Построена тропа до КОМАНДЫ ПЛОЩАДКЕ — целиком тропами ящика,** прямой
  записи в предусловиях группы нет ни одной. До захода расчёт действия падал
  `NO_REFERENCE_PRICE`, и ни одна команда не была достижима.
- **Тропа потребовала четырёх новых входов:** цены в связке фич; навес
  инструмента **именами контракта** (сокращённые имена разбор молча
  отбрасывал — навес материализовался без размерных спецификаций); ставка
  комиссии своим тиком; определение со встроенной защитой, долей аллокации и
  четырьмя потолками риска на детали.
- **Новая ось формы: ребро транша едет ПРОХОДОМ ПОЗЖЕ своей команды** —
  выходная проверка читает ногу, а нога отправляется после того, как переход
  отдан.
- **Контрольный прогон взял пять осей**, все предсказаны поимённо до прогона;
  четыре сошлись точно, **пятая не упала вовсе — и этим вскрыла тавтологичный
  наблюдатель `B3.3`**: факт восстановления отправки приносила добыча,
  зовущая тот же путь поиска. Наблюдатель перевооружён метками звеньев.
- **Находок владельцам четыре** (`F1`-`F4`), все припаркованы секциями
  бэклога: снимок предела повторов без писателя; повтор возвращает строку в
  планирование и пересчитывает уже созданную ногу (против клейма дома);
  пустое намерение reduce-only делает ногу входа невидимой своему траншу;
  ветвь восстановления отправки недостижима.

## Вход следующей сессии

**Следующая единица — продолжение той же:** код остатка группы `B3` и далее
`B4`. Что она застаёт:

- **тропа до команды площадке построена** (`DealCommandBoxTest#openCommandDeal`):
  нога входа создаётся, отправляется, добывается; лестница добычи, классы
  отказа границы и отвергнутые креды пройдены клетками;
- **восемь клеток `B3` не написаны, и делит их предусловие.** `B3.6` — форма
  предусловия не найдена: у никогда не отправленной ноги системная цепочка
  добычи требует её фактов каждым проходом, а лестница на такой ноге
  кончается контролируемым исключением. `B3.5` — исход «звено не завершено»
  на входной тропе не возникает вовсе: его производит
  `RefreshPositionExecutor`, то есть предусловие требует живого эпизода и его
  закрытия. `B3.10`-`B3.15` — все требуют живой позиции и выходной тропы;
- **живой позиции у ящика ещё нет** — её строит группа `B5`, и от неё зависят
  и остаток `B3`, и три ненаписанные клетки `B2` (`B2.11`, `B2.15`, `B2.18`);
- **субстрат и обвязка целиком** — `TradingCoreSubstrate`, `TradingCoreBox`,
  `SharedTradingCoreBox`, `Rows` (плюс `refuse`/`allow`), `PeerStub`,
  `IdentityStub`, `AppLog`, `Feed` (плюс `featuresWithPrice`, `ack`, `order`,
  `absent`, `tradeFeeRate`), `Bodies`, `Wire`, `Definitions` (плюс
  `withEntryCommandOnPhase`), `BrokerGate`, `SubstrateImagePinTest`;
- **класс со своим контекстом обязан называть и свою группу потребителя, и
  свою тему** — требование механическое, дом — шапка `TradingCoreSubstrate`;
- **контрольный прогон на нейтрализованной сборке — часть единицы** (Д1830);
  предсказание называет клетки поимённо до прогона, и у захода 92 одна ось из
  пяти не упала вовсе — нормальный исход метода, дающий находку;
- **ревизия существующего набора проб предмета** — свой предмет перечня
  (Д1828); заходом 92 не исполнена: клетки группы `B3` покрывают тропу,
  которой у модульных проб предмета нет;
- **форма кода уровня 1** задана заходами 83-92, **уровня 2** — заходами
  69-82; всё, что перечисляли v325-v372 §«Вход следующей сессии», остаётся в
  силе дословно (`.claude/work/history/snapshots/snapshot-v325.md` …
  `…/snapshot-v372.md`);
- **хвост апрувленных документов** — негейтящие находки `Ф-3`-`Ф-5` по
  `jsonb-overlay-roundtrip`.

## Что держателю решать

Против v372 не изменилось:

1. **Источник цены `MARK_PRICE` / `INDEX_PRICE`** — реджект создания либо
   снятие ограничения (кейс `U22.8` предмета
   `strategy-definition-validation`, красный по построению).
2. **Читатель `exitOutcome`.**
3. **Ключи demo-окружения OKX и распечатанный Vault** — без них не
   прогоняется ни один кейс мишени `-D` и весь `smoke-live`.

Плюс прежние ожидания (Vault-хардненинг, миграция на Jackson 3, область
тестового токена) — печатает `py tools/backlog-check.py` строкой `держатель:`.

## Что висит

Всё, что живёт секциями бэклога, печатает `py tools/backlog-check.py`; в
снапшоте не дублируется. Вне бэклога список не изменился: пункты 1-14
§«Что висит» снапшота v302 остаются в силе дословно
(`.claude/work/history/snapshots/snapshot-v302.md`), **кроме п. 6** (закрыт
заходом 68). Существенное: стенд отстал от дерева кода; вход снаружи в
окружение не построен; **пары производитель-потребитель из ДВУХ сервисов** на
живом брокере по-прежнему не встречалось.

Три больших свипа с измеренным объёмом остаются открытыми: слой опровержения
в `docs/**`, свод адресных перекрёстий, долг §-адресов
(`tools/anchor-debt.txt`) и процедура синка в доках. Каждый — своя единица.

**Пользователю:** обновить Project Knowledge (этот снапшот); прочитать блоки
дайджеста Д1564-Д1897 — право вето постфактум.

## Ловушки

**Ловушки** v206-v372 остаются в силе. Подтверждено и добавлено этой сессией:

- **Заготовка ответа соседа с сокращёнными именами полей разбирается МОЛЧА в
  пустоту.** Навес инструмента приезжает ядру доменной моделью целиком, и
  ключ, которого модель не несёт, разбор отбрасывает: заготовка выглядит
  наполненной, а навес материализуется без размерных спецификаций, и отказ
  приходит на первом же сайзинге — то есть на входе, который клетка считала
  поставленным.
- **Один путь чужой поверхности бывает у ДВУХ звеньев, и по попаданию стаба
  они неразличимы.** Поиск ноги по клиентскому идентификатору зовут и
  повторная отправка, и первая нога лестницы добычи; клетка, наблюдающая
  «восстановление состоялось» по попаданию, зелена и на нейтрализованной
  сборке. Различают звенья МЕТКИ, которые каждое ставит порознь.
- **Ребро транша едет проходом ПОЗЖЕ своей команды.** Выходная проверка
  обработчика читает ногу, а нога отправляется командой после того, как
  переход отдан; клетка о статусе транша подаёт на один тик больше, чем
  называет её вход.
- **Повтор отказавшего звена возобновляет ПАКЕТ, а не звено:** строка
  возвращается в планирование, и следующий проход исполняет создание, а не
  отправку. Клетка о бюджете повторов поэтому считает проходы вдвое, а не
  по числу попыток (находка `F2`).
- Прочие ловушки — снапшоты v283, v285-v372 §Ловушки; они не переписываются.

Обязательное перед завершением любой единицы:

1. **гейт инструментов корпуса** целиком — перечень в
   `.claude/skills/update-roadmap-progress.md` §«Гейт инструментов корпуса»
   (шестнадцать команд), `backlog-check.py` с подменой статуса, если статус
   проставляется;
2. **реактор — `bash tools/reactor-test.sh`**, если тронуто дерево кода.

## Проверки на момент снапшота

**Гейт инструментов корпуса прогнан** целиком; все шестнадцать команд вернули
0. **Реактор прогнан** — 1492 файла, 4272 теста, дефектов 0 (против 1491 и
4267 у v372). Пакет ящика прогнан отдельно — 90 тестов, все зелены:

```bash
mvn -o -am -pl services/trading-core test -Dtest='com.example.tradingcore.box.*Test' -Dsurefire.failIfNoSpecifiedTests=false
```

Группа `B3` прогоняется одним классом; две её клетки с меткой `debt` в
штатный набор не входят:

```bash
mvn -o -am -pl services/trading-core test -Dtest='DealCommandBoxTest' -Dsurefire.failIfNoSpecifiedTests=false
```

## Среда

Против v132 не изменилась, кроме состава локального репозитория по тестовым
библиотекам и **образов субстрата ящика** (строки «Артефакты тестового
контура» и «Образы субстрата ящика»), а также тестовых областей одиннадцати
модулей (`test.excluded.groups`). Дом фактов среды — команды сборки, пути JDK
и Maven, лаунчер Python, Vault, demo-ключ и прайминг —
`.claude/skills/environment-commands.md`.
