# Snapshot v16

**Дата:** 2026-05-29.

## На какой вопрос отвечает этот файл

Где мы сейчас и как сюда пришли (срез после запуска Фазы 1 в
исполнение и материализации тулинга роадмапа под шаг 1).

## Состояние

Фаза 1 роадмапа запущена в исполнение (`IN_PROGRESS`); шаг 1
(поток рыночных данных) — в `TOOLING`. Исполнение идёт по
`.claude/processes/roadmap-step-execution.md`. Под-шаг `TOOLING`
шага 1 пройден: материализован тулинг роадмапа — скилл
`concept-review`, роль `reviewer` (активен только концептный
фокус), ростер тулинга. Следующее действие — `DOCS_CHECK_1`
(первый прогон сквозной проверки концепции под код шага 1),
исполняется в новом чате.

## Что изменилось относительно v15

### Роадмап — запуск исполнения

- **Фаза 1:** `HOLD` → `IN_PROGRESS` (ролляп: шаг 1 ушёл из
  `HOLD`). Остальные фазы и архитектурный рубеж — `HOLD`.
- **Шаг 1 Фазы 1** (поток рыночных данных): `HOLD` → `TOOLING`.
  Шаги 2-10 — `HOLD`.
- Начато исполнение шага 1 по
  `.claude/processes/roadmap-step-execution.md`; под-шаг
  `TOOLING` пройден.

### Новый тулинг роадмапа (2026-05-29)

- **`concept-review`** — скилл, `.claude/skills/concept-review.md`.
  Сквозная проверка концепции доков под потребность кода шага:
  функциональный порог по полям, три типа пробелов
  (несогласованности / name-level без структуры / неотвеченные
  вопросы), сверка с `open-questions.md`, gap-отчёт
  (progress-файл), эскалация неоднозначного в чат. Размещение
  найденного — не здесь, а штатным потоком на `GAPS_CLOSE_N`.
- **`reviewer`** — роль-агент, `.claude/agents/reviewer.md`.
  Адверсариальный критик с переключаемыми фокусами. Активен
  только концептный фокус (`concept-review`); фокусы security /
  conventions — на под-шаге `CODE`, test — на шаге 9. Роль =
  стойка + границы + реестр фокусов; процедуру держит скилл
  фокуса.
- **Ростер тулинга** —
  `.claude/notes/2026-05-29-ростер-тулинга-роадмап.md`. Состав
  тулинга роадмапа целиком, статусы материализации, принятый
  дизайн. Подход (а): состав зафиксирован сейчас, файлы
  артефактов создаются на тех под-шагах, где нужны (исключение —
  `concept-review` и `reviewer`, нужны сразу под шаг 1).

### Backlog

- Добавлен маркер «приложить референс-доки для `code-writer`
  перед `CODE`» (example-based подход к написанию кода;
  референс-доки растут итеративно).

### Decisions / правила — без supersede

Запуск исполнения и новый тулинг не пересекаются с действующими
decisions; ничего не помечается `SUPERSEDED`.

### Open-questions

Без изменений. Все 12 открытых продуктовых вопросов не закрыты.
Сверка «какие вопросы блокируют шаг 1» — часть `DOCS_CHECK_1`,
ещё не выполнена.

## Активные задачи

Активная исполнительная задача — шаг 1 Фазы 1 (поток рыночных
данных), под-шаг `TOOLING` пройден. Прочих активных задач нет.

## Текущий фронтир / следующее действие

- Шаг 1 в `TOOLING`. Следующее — **`DOCS_CHECK_1`**: первый
  прогон (обкатка) сквозной проверки концепции под код шага 1,
  ролью `reviewer` с фокусом `concept-review`. Выход — gap-отчёт
  (progress-файл) → разбор пробелов на `GAPS_CLOSE_1` в чате.
- Тулинг в обкатке: формулировки `concept-review` и роли
  `reviewer` уточняются по итогам первых прогонов.
- `DOCS_CHECK_1` запускается в новом чате (в этот заход не
  выполнялся).

## Открытые общие вопросы

`open-questions.md`: DEAL-Q1, DEAL-Q2, PROC-Q1, RISK-Q1, TIME-Q1,
ENUM-Q1, CMD-Q1, OKX-Q1, OKX-Q2, OKX-Q3, OKX-Q4, DEAL-Q3 — все 12
(без изменений с v15).

## Что в работе

- Шаг 1 Фазы 1, под-шаг `TOOLING` пройден; следующее —
  `DOCS_CHECK_1` в новом чате. Project Knowledge требует
  обновления: последний снапшот теперь `snapshot-v16` (заменяет
  v15 в префлайте); добавлены новые файлы (`concept-review`,
  `reviewer`, ростер тулинга), затронут `backlog.md`.
