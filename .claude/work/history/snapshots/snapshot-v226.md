# Snapshot v226

**Дата:** 2026-09-10.

## На какой вопрос отвечает этот файл

Где мы сейчас. **Шаг 10 фазы 2 (аудит и статистика): пост-хок концепт-гейт
`DOCS_CHECK_6` ПРОЙДЕН, шаг в `GAPS_CLOSE_6`.** Сменяет v225.

## Состояние

Фаза 1 — `FOLDED`. **Фаза 2 — `IN_PROGRESS`**: шаги 1-9 `DONE`, шаг 10 —
**`GAPS_CLOSE_6`**, шаги 11-12 `HOLD`.

**Гейтящих выносов держателю нет.** Единственный вынос прогона — продуктовая
половина B2 — **не гейтит** (адресат шага 12); заводится вопросом в
`GAPS_CLOSE_6`, а не этой единицей.

**Дельта staged, не закоммичена.** Ветка `claude-audit`. **Дерево кода эта
единица не меняла:** правки легли в `.claude/work/**` и роадмап.

**Режим — автономия до прод-рубежа.** Решения — Д250-Д1378.

## Что сделано после v225

**Единица сессии — прогон `DOCS_CHECK_6`**, пост-хок концепт-гейт
`.claude/processes/roadmap-step-execution.md` §6a. Отчёт —
`.claude/work/progress/phase-2-step-10-docs-check-6.md` плюс три файла линз;
статус — в `phase-2-step-10-chronicle.md` §Статус.

- **Предмет — четыре концепт-инкремента**, въехавших через `CODE`: ребро
  подъёма ступени как единственный писатель факта (Д1366-Д1367), актор в
  содержимом `DealShutdownInitiated` (Д1365), домен указателем на дом
  перечня и слово провода (Д1370-Д1371), ребро причины остановки с двумя
  затребователями (Д1333, Д1362).
- **Двадцать одна находка** — A 13, B 4, C 4. **Гейтит одна.**
- **B1 (класс `КОД`):** первый ход энфорсмента резолвит `shutdownReason` по
  радиусу **сигнала**, а дом клетки и шаг оркестратора — по радиусу
  **стоящей** ступени. Два затребователя одного ребра пишут в одну
  durable-колонку разное на достижимой тропе.
- **Несущий класс прочих:** писатель факта сдвинулся на ребро, а носители,
  которые о нём говорят, — нет; одиннадцать находок закрываются правкой
  одного узла.
- **Четверное независимое совпадение линз** по популяции события подъёма
  ступени плюс три парных (A1↔C2, A12↔C1, B4↔C3).
- **Предъявлено:** шесть верификаций CC (V1-V6), все подтверждены
  собственным чтением целей; гейт инструментов корпуса целиком — 0; реактор
  не прогонялся, потому что дерево кода не тронуто.

## Три вещи, которые дальше нельзя пере-решать

**1. Перечень исполнителей не переоткрывается** (v195). Новый исполнитель
вносится **строкой** реестра, а не поводом пересобрать перечень.

**2. Реестр компонентов сквозной и новым шагом не пересобирается** (Д1184).
Находки `DOCS_CHECK_6` в реестр **не идут**: они закрываются своим
`GAPS_CLOSE_6`, а гейт срабатывает на компоненте, когда тот начинается.

**3. Гейтящесть находки фокуса определяет КЛАСС, а не суждение прогона**
(Д1375). `КОД` и `РИСК` закрываются до статуса; `НОСИТЕЛЬ`, `ИЗМЕРЕНИЕ`,
`ГРУНТ`, `ПРОЦЕСС` — **диспозиционируются**.

## Вход следующей сессии

**Единица — один узел `GAPS_CLOSE_6`** (`.claude/rules/session-work-unit.md`).
Вход узла — отчёт `phase-2-step-10-docs-check-6.md`: индекс находок,
дедупликация, класс гейтящей, выносы.

- **Узлов ожидается два, и раскладка названа отчётом:** несущий узел —
  «писатель сдвинулся, носители не пошли» (A1-A5, A7, A8, A11, A12, B3, B4,
  C1-C4 группируются вокруг него; одиннадцать закрываются одной правкой);
  **B1 идёт отдельным узлом**, потому что её предмет — построенное, а не
  носитель, и её закрытие меняет поведение кода.
- **Каждый узел проходит мини-петлю критики** до приземления в корпус,
  потолок верификаций — две
  (`.claude/decisions/mini-loop-verification-ceiling.md`).
- **Все двадцать одна находка обязана получить исход** — закрыта либо
  припаркована с домом, оживителем и причиной
  (`.claude/rules/parking-address.md`).
- **Вынос B2 заводится вопросом** в `.claude/work/questions/open-questions.md`
  с вердиктом «не гейтит — адресат шага 12».
- **После чистого `GAPS_CLOSE_6` — перепрогон концепт-проверки**; только
  чистый пост-хок гейт пускает шаг в `DONE`
  (`.claude/processes/roadmap-step-execution.md` §6a).
- **Границы петли в силе:** предмет гейта — корпус, объём измерения —
  ремонт, потолок прогонов гейта выхода — два
  (`.claude/rules/docs-loop-limits.md`).

Обязательное перед завершением любой единицы:

1. **гейт инструментов корпуса** целиком;
2. **сборка — `bash tools/reactor-test.sh`**, если тронуто дерево кода;
   точечный прогон — `mvn -o -am -pl services/<модуль> test` из корня.
   **`-am` обязателен**; `mvn` на `PATH` нет — рабочая форма вызова стои́т в
   §Прогоны отчёта `phase-2-step-10-code-focuses-9.md`;
3. **`py tools/code-gate-check.py`** — перед каждым кодовым заходом.

## Что висит

1. **Двадцать одна находка `DOCS_CHECK_6`** — вход `GAPS_CLOSE_6`, см. выше.
2. **Api-формы периметра без дока** — восемь форм потока в коде, ноль в
   `docs/models/api/`; оживитель сужен до захода по собственной поверхности
   периметра (Д1378). B2 того же прогона добавляет к ней вопрос состава.
3. **Связка идентичностей радиуса по ИМЕНИ не мерится** у шести классов
   событий из семи — Ф16, припаркована; тем же ходом правится клейм
   класс-javadoc пробы формы.
4. **Форма пина мягкой ступени разошлась с критерием своего блока** — Ф17;
   A11 того же прогона добавляет, что сам критерий дома не имеет.
5. **Энфорсер снятых редакций слеп к javadoc-переносу** — Ф15.
6. **Второй порядок цены отката ребра подъёма не назван** — Ф14.
7. **Ф11 открыта** — имя и javadoc константы гарда `CASCADE_SOURCE_STATUSES`.
8. **Транзакционная граница обоих рёбер прогоном не мерится** — держит
   объявление метода. Оживитель — первый живой прогон ядра на базе.
9. **Текст JPQL и нативных запросов не мерится ничем** — `@SpringBootTest` у
   модулей нет намеренно. Оживитель общий с п. 8.
10. **Разведение пар джоб ядра ПРОЧИТАНО, а не измерено.**
11. **Обращения каскада ступени выросли** — замера нет, тропа исключительная.
12. **Слепота считается состоявшимися проходами** — припарковано.
13. **Класс `HoldReleased` не построен.**
14. **Эпизод транша событием не различается.**
15. **Строки журнала, накопленные до хода, не чинятся.**
16. **Таблицы и писателя строки отказа у четырёх соседей нет** (`auth`,
    `trading-core`, `strategies`, `market-data`).
17. **Перехватчик `Exception` съедает отказ по правам у `bff` и `strategies`.**
18. **Предикат актора написан трижды** — единый носитель припаркован.
19. **Глубины у `access_denials` нет ни у одного сервиса.**
20. **Пул планировщика у `market-data` — один поток**; измерителя среди его
    джоб нет.
21. **Остаток пары «чистка × пересчёт»** у `audit-statistics`.
22. **Экспорт метрик у семи сервисов из восьми не построен**; экспорт
    **трейсов** не построен ни у кого.
23. **Подъём контекста не проверен** ни у `audit-statistics`, ни у
    `strategies`, ни у `bff`, ни у ядра в части `AsyncConfigurer`.
24. **Ни план запроса, ни порядок взятия строчных замков у модуля приёма не
    мерятся ничем.**
25. **Отбор ресурсов наблюдателем по метке `release` живым кластером не
    проверен.**
26. **Дубль запроса `earliestRecordedAt` у двух подключений остался** (Д1297).
27. **Позиции парковки узлов 5-6 `GAPS_CLOSE_3`** и парковка узла 1.
28. **Кандидат энфорсера окрестности** — бэклог.
29. **Стенд от дерева кода отстал:** образы `audit-statistics`, `strategies`,
    `trading-core` и `bff` с этими правками не собирались, роли и базы на
    стенде не заводились, тем и подписки на стенде нет.
30. **Форма дедупа «спросить и вставить»** — шесть точек трёх сервисов.
31. **Три долга инструментов:** проза реестра
    (`tools/retired-prose-debt.txt`), адресные перекрёстья `docs/**`, долг
    носителей команд (`tools/command-carrier-debt.txt`).
32. **§-адрес javadoc в несуществующий пассаж** — класс припаркован.
33. **Цена сессий под правилом единицы не замерена.**
34. **События `auth` — производителя и темы нет.**
35. **Единый error-DTO у четырёх построенных сервисов.**
36. **Потерянное событие при неразбираемом содержимом** — DLT нет.
37. **Названные ограничения шага 7**, ревалидация активных определений при
    смене чисел риск-аппетита, `HOLD_RELEASED` не пишется, база риска на
    терминале не двигается.
38. **Внешний вызов внутри транзакции БД — двенадцать точек** у ядра.
39. **Донорские остатки в общей библиотеке** — снимаются удалением донора.
40. **Отчёты критики и верификации узлов 5-6 `GAPS_CLOSE_3` отдельными
    файлами не сохранены.**
41. **Ряду прогонов фокусов не объявлено ни сигнала, ни порога** — детектор
    стагнации входом его не имеет; замер записан в хронику (Д1377).
42. **Т-1…Т-3 седьмого прогона гейта `GAPS_CLOSE_4`; Ц-2…Ц-4 первого и
    Д-1…Д-4 второго прогона гейта `GAPS_CLOSE_5`** — все о тексте о работе;
    уезжают с отчётами в `history/` при закрытии шага.
43. **Прежние хвосты:** S1 Vault-хардненинг.
44. **Пользователю:** обновить Project Knowledge (этот снапшот). Новых
    Maven-модулей эта единица не заводила — переиндексация IDEA не нужна.

## Проверки на момент снапшота

**Гейт инструментов корпуса прогнан** на состоянии после всех правок,
включая этот файл, — весь перечень, ненулевых кодов нет, кода 2 нет. Числа
прогонов здесь не пересказываются: их печатает сам прогон
(`.claude/rules/self-description-form.md`).

**Реактор этой единицей не прогонялся, и это названо:** дерево кода она не
трогала — правило зелёной сборки адресует под-шаг, изменивший `services/`,
`libs/` либо `donor/`.

## Среда

Против v132 не изменилась; полное описание — там, перечень ловушек — в v206
(`.claude/work/history/snapshots/snapshot-v206.md` §Среда) и он остаётся в
силе целиком. Ловушки v207-v225 тоже в силе, включая: `python3` в этой среде
разрешается в заглушку Windows Store и падает кодом 49 — рабочая форма `py`;
heredoc питона внутри `bash -c` уходит в ту же заглушку — рабочая форма
`py - <<'EOF'`; `grep -P` отказывает на не-UTF-8 локали, поэтому всякий свип
предваряется `export LC_ALL=C.UTF-8`. Полный прогон гейта инструментов
корпуса одной командой **не укладывается в 120 с** — он запускается фоном
либо разбивается на части. **Ловушка этой сессии:** bash-heredoc с длинным
русским телом в этой среде рвётся на разборе кавычек — файлы отчётов пишутся
инструментом записи, а не `cat <<EOF`.
