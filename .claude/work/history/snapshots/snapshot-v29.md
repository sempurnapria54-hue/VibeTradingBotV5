# Snapshot v29

**Дата:** 2026-06-05.

## На какой вопрос отвечает этот файл

Где мы сейчас и как сюда пришли (срез после `DOCS_CHECK_8` —
первого прогона торгового фокуса — и `GAPS_CLOSE_8`; торговая
валидация модели шага 2 выполнена, шаг 2 готов к `CODE`).

## Состояние

Фаза 1 — `IN_PROGRESS`; шаг 1 — `DONE`; **шаг 2 (Стратегия) —
`GAPS_CLOSE_8`** (закрыт; петля чистая, шаг готов к `CODE`);
шаги 3-11 — `HOLD`. Фронтир v28 (торговая валидация модели шага 2)
— **выполнен**: адверсариальное касание прошло итерацией
`DOCS_CHECK_8`/`GAPS_CLOSE_8`; конструктивное касание специалиста —
при авторинге на `CODE`.

## Что изменилось относительно v28

### `DOCS_CHECK_8` — первый прогон торгового фокуса

Отчёт — `phase-1-step-2-docs-check-8.md`. Тулинг прохода: роль
`reviewer` инлайн; фокус `concept-review` в верификационном режиме
(диф `docs/` с чистой `DOCS_CHECK_7` пуст) + **первый прогон**
фокуса `trading-review` (грунт — все 4 пиллера дистиллята +
`corpus-map`, нырки в PDF не понадобились). `trading-specialist`
намеренно не задействован (конструктив — вне петли критики).

Результат: концептная часть чистая (верификация правок
`GAPS_CLOSE_7` свипами — 0 хвостов); торговая часть — 2
содержательные находки (ТР1 worst-case guard; ТР2 sanity-диапазоны
— эскалация), 3 заметки (ТР3 наивный авторинг; ТР4 volume/wash
trading; ТР5 торговля до бэктест-гейта — снята Фазой 2 роадмапа),
положительная сверка модели с корпусом по 7 пунктам.
Содержательно-блокирующего («модель не выражает торговое правило»)
— не найдено.

### `GAPS_CLOSE_8` — все находки закрыты

Отчёт — `phase-1-step-2-gaps-close-8.md`. По решениям чата
2026-06-05:

- **ТР2 (вариант A):** create-валидация ограничивает числовые
  торговые поля только **структурно** (невозможные значения → 400);
  торгово-суждённые пороги — семантика **activate (422)**, отложена
  до шагов 4/7. Дополнен
  `docs/decisions/strategy-materialization-and-validation.md` + эхо
  в `Strategy.md` §Линия реза. Формулировка уточнена оговоркой
  «там, где поле — доля» (иначе противоречие с `ATR_PERCENT`
  `150 = 1.5 ATR`).
- **ТР1 → RISK-Q2** (открытый вопрос, владелец — шаг 5):
  страховочный кэп поверх вычисленного плеча; не пересмотр решения
  о динамическом плече.
- **ТР4 → IND-Q1** (открытый вопрос, владелец — шаг 3): надёжность
  биржевого объёма (wash trading) для volume-условий.
- **ТР3 → СТ-1:** tasks-файл
  `.claude/work/questions/tasks/phase-1-step-2-strategy.md` —
  чек-лист торгово-наивного авторинга для специалиста на `CODE`
  (CONTRARIAN без guard'ов; trailing с входа).
- **Гигиена:** пропущенный `TOOLING` торгового совета зафиксирован
  пометкой в `phase-1.md` §Примечания (таблица держит только
  текущий статус).

## Активные задачи

- Итерационные progress-файлы шага 2 — в `progress/` до `DONE` шага
  (добавились `docs-check-8`, `gaps-close-8`).
- `trading-council-materialization.md`,
  `trading-library-distillation.md` — без изменений с v28
  (завершены, остаётся ревью).

## Текущий фронтир / следующее действие

**`CODE` шага 2**: Strategy API (`POST`/`GET`/`PUT`) +
create-валидатор (структурно-ссылочный + структурный sanity
числовых полей) + «одна реализация» стратегии. При авторинге
реализации — конструктивное касание `trading-specialist` (чек-лист
СТ-1). Ревью-итерации `CODE` — фокусы `conventions` /
`performance` / `disaster` (`security` деактивирован до шага 9).

Коммит — за пользователем (всё staged, CC не коммитит).

## Открытые общие вопросы

`open-questions.md`: **17** (+2 с v28: RISK-Q2 — шаг 5, IND-Q1 —
шаг 3; гейтящих шаг 2 — 0).

## Что в работе / PK

- Шаг 2 — к `CODE`.
- **Project Knowledge:** последний снапшот теперь **`snapshot-v29`**
  (заменяет v28 в префлайте). `CLAUDE.md` / `structure.md` /
  `naming.md` / `place-knowledge.md` в этом заходе не менялись —
  в PK обновить только снапшот.
- Staged сейчас: `docs-check-8`, `gaps-close-8`,
  `open-questions.md` (+RISK-Q2, +IND-Q1, шапка),
  `tasks/phase-1-step-2-strategy.md` (новый),
  `strategy-materialization-and-validation.md`, `Strategy.md`,
  `phase-1.md` (статус + пометка `TOOLING`), этот снапшот.
