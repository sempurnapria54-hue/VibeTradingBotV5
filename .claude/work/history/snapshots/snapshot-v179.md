# Snapshot v179

**Дата:** 2026-09-08.

## На какой вопрос отвечает этот файл

Где мы сейчас. **Шаг 10 фазы 2 (аудит и статистика): пятый прогон гейта выхода
прогнан, исход красный — три находки, все у восемнадцатой единицы. Статус шага
не двигался; закрытие П1-П3 берёт следующая сессия.** Сменяет v178.

## Состояние

Фаза 1 — `FOLDED`. **Фаза 2 — `IN_PROGRESS`**: шаги 1-9 `DONE`,
шаг 10 — **`GAPS_CLOSE_4`** (все пять узлов приземлены; пять прогонов гейта
прогнаны, находки первых четырёх закрыты), шаги 11-12 `HOLD`.

**Гейтящих выносов держателю нет.**

**Дельта в working tree, не в истории.** Ветка `claude-audit`; всё staged, не
закоммичено. Дерево кода не тронуто, как в v162-v178.

**Режим — автономия до прод-рубежа.** Решения — Д250-Д1156; эта единица новых
решений не заводила: прогон гейта — заход **читающий**.

## Что сделано после v178

**Единица сессии — пятый прогон гейта выхода** (читающий заход, корпуса не
касается). Отчёт — `.claude/work/progress/phase-2-step-10-gaps-close-4.md`
§«Гейт выхода из `GAPS_CLOSE_4` — пятый прогон 2026-09-08, исход КРАСНЫЙ».

**Обе находки четвёртого прогона (Ч1, Ч2) закрыты — и это проверено
исполнением**, а не чтением: все одиннадцать осей двух гейтящих единиц
прогнаны заново, живого носителя выдачи без исхода не осталось ни у одной,
каждая помеченная не членом строка подтверждена прямой пробой (код 1). Правка
позиции 23 дельты `CODE` сверена с `docs/components/ReceptionStateJob.md`.

**Три находки — все у хвоста-3** (восемнадцатая единица, гейтом меряется
впервые), и все три суть классы, которые эта же единица чинила у других
единиц:

- **П1** (блокирующая) — первая из двух объявленных форм свипа не исполняется
  вовсе: внутри класса символов стои́т настоящий перенос строки, `grep -P`
  отвечает `only supports a single pattern` и кодом 2. Отсюда неверна строка
  чек-листа «потребители обойдены двумя формами», а клейм о числе носителей у
  каждой формы ничем не подтверждён и написан рукой в прозе.
- **П2** (несущая) — таблицы свипов не разбирают живых носителей своей же
  выдачи (артефакты собственной сессии) и ведут на прежний адрес снапшота,
  переехавшего в `history/` тем же заходом.
- **П3** (несущая) — популяция называет трёх не-членов: проба команды на
  каждом даёт код 1.

**Способ прогона расширен.** Сверх сверки «выдача против таблицы» исполнены
**все объявленные командные блоки** четырёх живых отчётов с записью кода
возврата: код 2 вернула команда находки П1 и никакая другая. Класс проверен
поиском по всему корпусу: носитель с таким переносом — этот и никакой другой.
`py tools/command-carrier-check.py` этого не мерит — он мерит **носитель**
команды, а не её **исполнимость**.

## Три вещи, которые дальше нельзя пере-решать

**1. Статус шага не проставлен и остаётся `GAPS_CLOSE_4`.** Гейт красен, пока
его прогон не дал чистого исхода; `DOCS_CHECK_5` открывается только после него.

**2. Клетки таблиц исхода прогонов не правятся.** Клетка есть исход
датированного захода; признак записан в
`.claude/rules/self-description-form.md` §«Датированный замер клеймом не
является».

**3. Команды популяций задним числом не переписываются** (Д1150, Д1154). У
находки П1 это **не действует и сказано явно**: у команды, которая никогда не
исполнялась, прежнего перечня нет — сохранять нечего.

## Вход следующей сессии

**Единица — закрытие П1-П3.** Берёт её сессия, пятого прогона не писавшая (эта
его писала).

- Что чем закрывается — §«Что дальше» пятого прогона в отчёте закрытия.
- **Состав выдачи свипов меняется с каждым заходом**: артефакты сессии (хроника,
  снапшот) попадают в выдачу собственных команд. Закрытие обязано разобрать
  **класс** таких носителей, как это сделала преамбула §«Популяции (построены ДО
  правки)» дизайн-прохода, а не перечисленные имена.
- **Урок пяти прогонов, годный дальше:** единица, закрывающая класс у чужих
  носителей, своих под тот же класс не подставляет — и именно поэтому
  подставляет. Свой отчёт проходится тем же прогоном, что и чужой, а командные
  блоки собственного раздела исполняются **до** того, как о них написано.
- Чист гейт → статус `DOCS_CHECK_5` и открытие пятого прогона `DOCS_CHECK`.

## Что висит

1. **Дельта `CODE` шага 10 — счёт стои́т порождённым блоком в хронике.**
2. **Компонентов шага 10 в `.claude/work/code-gate-ledger.json` нет** — их
   вносит простановка `CODE`.
3. **Позиции парковки узлов 5-6 `GAPS_CLOSE_3`** и парковка узла 1
   (исполнимость формы ценового результата на строках агрегатов).
4. **Образ сервиса без продвинутого тега резолвится подвижным `latest`**
   (`.claude/work/backlog.md`).
5. **Форма дедупа «спросить и вставить»** — шесть точек трёх сервисов;
   поглощённый отчёт ручной операции. Обе — секции бэклога.
6. **Три долга инструментов:** проза реестра
   (`tools/retired-prose-debt.txt`), адресные перекрёстья `docs/**`,
   клетки-носители команд (`tools/command-carrier-debt.txt`).
7. **Цена сессий под правилом единицы не замерена.**
8. **События `auth` — производителя и темы нет**; экспорт метрик из семи
   прочих сервисов; таблицы `access_denials` нет ни у одного сервиса.
9. **Единый error-DTO у четырёх построенных сервисов.**
10. **Потерянное событие при неразбираемом содержимом** — DLT нет.
11. **Названные ограничения шага 7**, ревалидация активных определений при
    смене чисел риск-аппетита, `HOLD_RELEASED` не пишется, база риска на
    терминале не двигается.
12. **Внешний вызов внутри транзакции БД — двенадцать точек** у ядра.
13. **Донорские остатки в общей библиотеке** — снимаются удалением донора.
14. **Отчёты критики и верификации узлов 5-6 `GAPS_CLOSE_3` отдельными
    файлами не сохранены** — закрыто в части нормы, в части прошлого не
    закрывается ничем.
15. **Живые отчёты единиц маркера `(корпусный)` не несут** — кроме отчёта
    прохода, приземления узла 2 и трёх хвостов: ретро-разметки нет по правилу
    курации.
16. **Прежние хвосты:** S1 Vault-хардненинг, рост таблицы отказов,
    api-формы периметра без дока.
17. **Пользователю:** переиндексировать проект в IDEA; обновить Project
    Knowledge (этот снапшот). **Стенд от дерева кода не отстал** — код не
    тронут.

## Проверки на момент снапшота

**Гейт инструментов корпуса прогнан на этом состоянии** — весь перечень плюс
`code-gate-check.py`, каждая команда вернула 0. Числа прогонов здесь не
пересказываются — их печатает сам прогон
(`.claude/rules/self-description-form.md`).

**Реактор не прогонялся: дерево кода не тронуто.** Последний прогон — на этом
же состоянии кода (замер v163).

**Живого прогона нет ни у одного компонента шагов 7-10.**

## Среда

Против v132 не изменилась; полное описание — там. Ловушки среды из v156-v178
остаются в силе, включая: полный гейт инструментов в 120 с не укладывается —
прогонять фоновой командой; рабочий бинарь — `py` (`python3` и `python` из
PATH — заглушка, её код 49 читается как «не измерялось»); в regex-шаблонах для
`tools/retired-check.py` пользоваться `\s+`; `py` путей `/tmp/**` не видит;
импорт модуля из `tools/` через `importlib` роняет `__pycache__` в корень
репозитория, и `tools/retired-check.py` отказывает кодом 2 на нём; `cd` внутри
составной команды Bash сохраняется между вызовами; форма `py - <<'PYEOF'`
рвётся на длинном markdown с таблицами — большой раздел писать инструментом
записи во временный файл внутри репозитория и вклеивать коротким скриптом.

**Новое этой сессии — ловушка переноса строки в шаблоне `grep -P`.** Шаблон,
разорванный переносом **внутри** аргумента (например, внутри класса символов),
читается `grep` как **два** шаблона, а `-P` больше одного не принимает: команда
отвечает `only supports a single pattern` и кодом 2. В отчёте такая команда
выглядит исполнимой, а при прогоне не измеряет ничего. Правило письма: длинный
`-P`-шаблон переносится **между** аргументами (обратным слэшем на конце
строки), а внутри шаблона перенос пишется escape-последовательностью.

**Ловушка снапшота против реестра снятых редакций** (из v178, в силе): в
снапшоте снятое **называется**, а не цитируется, и рядом стои́т действующая
редакция с указателем на её дом.

**Ловушка одноимённых заголовков** (из v178, в силе): раздел новой единицы
называется с её маркером. Буква находок пятого прогона — **П** — совпадает с
буквой осей популяции восемнадцатой единицы; разделы называются полными
именами, и §-адрес разрешается однозначно.
