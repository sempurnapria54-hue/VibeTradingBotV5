# Snapshot v247

**Дата:** 2026-09-12.

## На какой вопрос отвечает этот файл

Где мы сейчас. **Шаг 11 фазы 2 в `GAPS_CLOSE_1`; узел 1 (A4) приземлён,
осталось двадцать шесть гейтящих находок и девять прочих.** Сменяет v246.

## Состояние

Фаза 1 — `FOLDED`. **Фаза 2 — `IN_PROGRESS`**: шаги 1-10 `DONE`, шаг 11 —
`GAPS_CLOSE_1`, шаги 12-13 `HOLD`. Прод-рубеж — `HOLD`.

**Гейтящих выносов держателю нет.** Проверено прогоном `DOCS_CHECK_1` по каждой
из двадцати семи гейтящих находок всеми тремя линзами; единственный вынос
прежних шагов — `BFF-Q1` с вердиктом «не гейтит, адресат шага 13».

**Дельта единицы — staged, не закоммичена.** Ветка `claude-audit`. Дерево кода
не тронуто: единица правила только `docs/`, `tools/` и рабочие файлы.

**Режим — автономия до прод-рубежа.** Решения — Д250-Д1489.

## Что сделано после v246

**Единица — узел 1 `GAPS_CLOSE_1` (находка A4).** Отчёт —
`.claude/work/progress/phase-2-step-11-gaps-close-1-node-1.md`; заходы
мини-петли — `…-node-1-critique.md`, `…-node-1-verification.md`.

- **Статус шага** `DOCS_CHECK_1` → `GAPS_CLOSE_1`; гейт входа исполнен,
  сработавших оживителей бэклога ноль.
- **Снята противоречащая редакция** в `docs/architecture/data-ownership.md`
  §Раскладка: она объявляла, что разъезд лишь превращает чтение журнала
  статистикой в вызов по сети, то есть отвечала **противоположно** дому
  (`docs/architecture/services.md` §«Модули, которые нарочно не стали
  сервисами»). Дословно снятое здесь не цитируется — цитата в живом носителе
  сама роняет `tools/retired-check.py`; она в отчёте узла. Дом остался
  единственным.
- **Охрана заведена** — запись реестра `tools/retired-check.py` с популяцией из
  обоих архитектурных доков.
- **Мини-петля:** раунд критики 1 (одна блокирующая корпусная находка, девять
  текстовых), верификация 1 (ноль блокирующих, пять текстовых). Все
  диспозиционированы, узел приземлён.

## Четыре вещи, которые дальше нельзя пере-решать

**1. Раскладка границы — решение держателя.** `integration` делится по
**собеседнику** (`internal` — сосед по монорепозиторию, `external` — система
вне его), шина живёт слоем `internal/event`. Вызов соседа остаётся интеграцией
и в третий слой не выносится (Д1463).

**2. Дом общих артефактов один и лежит в `services/common/`.** Модели
группируются каталогом и разводятся по слою; сам `model` артефактом не
является. Артефакт опознаётся по `pom.xml`, а не по глубине (Д1476-Д1477).

**3. Домен формы сообщения не держит — ни в сигнатурах, ни транзитом.**
Признак механический и мерится грепом по двум осям: импорт пакета форм в
`**/domain/**` и слово `Message` в его теле; обе обязаны быть пусты. Прогона у
этого признака нет — он держится дисциплиной и этой записью.

**4. Статистика журнал аудита источником НЕ читает, и дом этой истины один —
`docs/architecture/services.md`.** Сетевой пары под такое чтение не заводится,
и строки под него нет в `docs/architecture/contracts.md`
§«Синхронные вызовы». Вариант «развести, но оставить
статистику читателем журнала» отвергнут держателем поимённо
(`.claude/decisions/audit-statistics-split.md`). Узел 1 снял вторую редакцию;
возврат ловит `tools/retired-check.py`.

## Вход следующей сессии

**Следующая единица — узел 2 `GAPS_CLOSE_1`: находки A1-A3** (что потребляет
`statistics` — классы, темы, группа, позиция чтения; форма хранилища фактов и
чем оно не второй журнал; дедуп и отметка обработанного у второго
потребителя). Это вход дизайн-прохода шага, чья секция бэклога уже
`оживит=сейчас` (`.claude/work/backlog.md` §«Ориентиры к дизайн-проходу по
`statistics`»).

Далее по голове прогона: **A5** (грант, спека `owner-role-grants.json`, её
команда вывода) и **C6** — оба стали закрываемы после A4; **A7** идёт **одним
ходом** с переименованием, потому что три машинные оси сцеплены и между
правками корпус красен при верных правках.

**Ловушки** v206-v246 остаются в силе целиком. Добавленные узлом 1 —
`.claude/work/progress/phase-2-step-11-chronicle.md` §«Ловушки, снятые узлом 1»
(разбор выдачи против её предъявления; свип, чью выдачу населяет собственная
правка; «проба пуста» при непустой выдаче; номера строк с объявленной
редакции; регистр в шаблоне анафор). В снапшоте они не пересказываются.

Обязательное перед завершением любой единицы:

1. **гейт инструментов корпуса** целиком — перечень в
   `.claude/skills/update-roadmap-progress.md` §«Гейт инструментов корпуса»,
   вместе с `backlog-check.py` (с подменой статуса, если статус проставляется);
2. **сборка — `bash tools/reactor-test.sh`**, если тронуто дерево кода;
3. **`py tools/code-gate-check.py`** — перед каждым кодовым заходом.

## Что висит

Всё, что живёт секциями бэклога с маркерами, печатает
`py tools/backlog-check.py` — в снапшоте не дублируется. Вне бэклога висит:

1. **Двадцать шесть гейтящих находок `DOCS_CHECK_1`** (A1-A3, A5-A15, B1-B10,
   C5, C6) и девять прочих — вход остальных узлов `GAPS_CLOSE_1`; части
   прогона в `progress/`.
2. **Продуктовая половина B2 у держателя** — `BFF-Q1`, оживитель — шаг 13.
3. **Стенд от дерева кода отстал:** образы четырёх сервисов не собирались,
   роли и базы не заводились, тем и подписки нет.
4. **Ни одна пара производитель-потребитель не встречалась на живом брокере** —
   форма провода проверена сквозным тестом у журнала и чтением у прочих.
5. **Спека и её реализация не связаны ничем механическим** — `SpecRunnerTest`
   гоняет спеки против их собственных примеров; тесты сервисов переписывают
   примеры руками. Наблюдение, задачи пока нет: держатель о нём знает.
6. **Пользователю:** обновить Project Knowledge (этот снапшот).

## Проверки на момент снапшота

**Гейт инструментов корпуса прогнан** на состоянии после всех правок — весь
перечень его дома, ненулевых кодов нет, кода 2 нет. Реактор этой единицей не
гонялся: дерева кода она не трогала. Числа прогонов здесь не пересказываются:
их печатает сам прогон (`.claude/rules/self-description-form.md`).

## Среда

Против v132 не изменилась; полное описание — там, перечень ловушек — в v206
(`.claude/work/history/snapshots/snapshot-v206.md` §Среда) и он остаётся в силе.
