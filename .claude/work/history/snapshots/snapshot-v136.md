# Snapshot v136

**Дата:** 2026-09-05.

## На какой вопрос отвечает этот файл

Где мы сейчас. **Шаг 7 фазы 2 — `CODE`: поставлены восемь исполнителей
постановки и снятия с писателем четвёрки чисел риска; добыча начата и
упёрлась в разошедшиеся носители резолва статуса.** Сменяет v135.

## Состояние

Фаза 1 — `FOLDED`. **Фаза 2 — `IN_PROGRESS`**: шаги 1-6 `DONE`, шаг 7 —
`CODE`, шаги 8-12 `HOLD`. Фазы 3-6, 7-8 и прод-рубеж — `HOLD`.

**Дельта в working tree, не в истории.** Ветка `claude-audit`; всё
описанное — staged, не закоммичено (коммит руками).

**Режим — автономия до прод-рубежа.** Решения — Д250-Д784 (итерации
34-42). **Открытых выносов держателю нет.**

## Что закрыто этим заходом

1. **Компонент «исполнители площадки: постановка и снятие»** —
   компонент реестра разделён надвое (Д773), первая половина закодирована
   целиком. Хроника —
   `.claude/work/progress/phase-2-step-7-chronicle.md` §«Компонент
   «исполнители площадки: постановка и снятие»».
2. **Писатель четвёрки чисел риска** — исполнимая форма
   `docs/spec/deal-risk-numbers.json` с записью и гейтом полноты графа.
3. **Компонент «исполнители площадки: добыча состояния» начат:** снимок
   средств, журнал происшествий целиком, первое наблюдение базы риска.
   Там же, §«Компонент «исполнители площадки: добыча состояния» — начат,
   не закрыт».
4. **Третий исход исполнения** — `notCompleted` («звено не завершено»):
   исполнимая форма трёх исходов `docs/spec/deal-context-load.json`.

## Где стои́т шаг 7

Docs-first пройден целиком. **Кодовый заход: 7 компонентов из 21
поставлено, один в работе.** Реестр и статус каждого —
`.claude/work/code-gate-ledger.json`.

**Что мешает следующему заходу — одна парковка, и она названа.**
`.claude/work/backlog.md` §«Где резолвится внешний статус сущности — два
дома разошлись»: доки коннектора, javadoc гейтвея и спека резолва
отвечают по-разному, а реализации резолверов у коннектора **не
вызываются никем** — сегодня доменный статус не резолвит ни одна сторона.
Без решения не пишутся три исполнителя добычи из четырёх.

**Вход следующего захода — два кандидата:**

- **закрыть парковку резолва решением** (правка обоих носителей тем же
  ходом) и доставить добычу: заявка, условная заявка, эпизод позиции,
  движения средств. Движениям сверх того нужна чтение свечи курса —
  операции `getIndexCandleAt` в клиенте ядра ещё нет;
- **расчётный слой `libs/strategy-engine`** — разведка сделана заходом
  2026-09-05 и не устарела: размер строится **от спеки**
  (`docs/spec/order-sizing.json`, 19 примеров — готовые тест-кейсы), а не
  портируется; открытая развилка формы операндов
  `CalculationContextFactory` решается вместе с первым вызывающим.

Риск-гейт по-прежнему заблокирован расчётным слоем.

**Ратифицировано шагом 7 (не пере-решать):** радиус торговой строки —
пара «счёт × инструмент»; проекции чужих реестров наполняет синхронное
чтение; числа риск-аппетита — на строке тенанта; копия определения
стратегии плюс временный писатель до шага 8; факты исполнения —
**опросом**; отказ соседа по ярусу — `PEER_SERVICE_UNAVAILABLE`; ноги
агрегата собираются **обходом траншей**, донорское `Deal.orders` ядром не
читается.

## Что дала постановка этих компонентов (живой контекст)

| Что | Наблюдение |
|---|---|
| два измерителя ноги | док называет `liquidationDistanceRatio` и `bookDepthAtPlacement` полями заявки, а ни модели, ни колонок не было — поймано письмом **писателя**, правится `V3` |
| радиус у средств и движений | поля звались `exchangeId` при доках и схеме, называющих **счёт**; тот же класс, второй раз за заход |
| донор мог сломаться молча | переименование поля роняет колонку донорского маппера **без слова** (политика `IGNORE`) — отображение объявлено явно |
| повтор создания | донорская форма генерировала новый клиентский идентификатор каждым проходом: повтор оставлял бы сироту, а на площадку она уехала бы вторым размером |
| третий исход | гейт полноты графа выразить было нечем: повторяемость выводилась из кода ошибки, и единственным повторяемым классом был отказ площадки |
| резолв статуса | резолверы у коннектора **есть и не зовутся**; три носителя о том, кто резолвит, читаются по-разному |

## Что дальше — вход в следующий заход

1. **Парковка резолва статуса** — закрыть решением с правкой носителей,
   затем доставить три исполнителя добычи.
2. **Кодовые хвосты двух прежних парковок**: ставка комиссии, предикат
   свежести шага.
3. **Аппрув `CODE`** — фокусы `conventions`/`performance`/`disaster`/
   `security` с зафиксированным исходом; автор код не аппрувит.
4. **В `SYNC_DOCS_FROM_CODE` накоплено четыре расхождения:**
   `docs/models/domain/aggregate/DealTranche.md` §Персистентность не
   называет трёх колонок налива;
   `docs/models/domain/other/DealActionState.md` §Инварианты называет ключ
   живого потраншевого исполнения тройкой вместо четвёрки;
   `docs/components/ServiceCommandExecutor.md` знает два исхода исполнения,
   а их три; `docs/components/RefreshBalanceExecutor.md` называет радиус
   отчёта биржей, а база живёт на **счёте**.

## Что висит

1. **Новое этим заходом:** развилка резолва внешнего статуса (см. выше).
2. **Прежнее того же класса:** конверт события `EventEnvelope` без
   читателя; донорские поля агрегата и донорские значения в расколотых
   перечнях общей библиотеки — оживит удаление донора.
3. **Парковки стенда:** срезы стакана, экспорт трейсов, том
   Elasticsearch, политики сжатия гипертаблиц.
4. **Парковки прежних шагов:** сброс кэша ключей по ротации; осиротевшие
   секреты; WS-слушатель фактов исполнения; контекстный тест сервиса;
   подпись образов; транспорт внутри кластера; темы Kafka; размеры томов;
   ликвидации; ориентир по расходам на хранение.
5. **Прежние хвосты:** S1 Vault-хардненинг, рост таблицы отказов, A12,
   хвост шага 8, долг адресных перекрёстий `docs/**`, дубль эталона
   стратегии в корне.
6. **Пользователю:** переиндексировать проект в IDEA; обновить Project
   Knowledge (этот снапшот, дайджест итераций 35-42). На стенде остаются
   **тестовые** строки тенанта и счёта (`tn-stand-0001`, `ea-stand-0001`)
   — удалять их или нет, решает держатель.

## Проверки на момент снапшота

Десять команд корпуса — код 0 (`spec-run`, `spec-mutation-check`,
`spec-scope-check`, `spec-pointer-check`, `anchor-check`, `retired-check`,
`population-derive-check`, `preconditions-check`, `deploy-layout-check`,
`code-gate-check`). Реактор — `mvn -o test -DexcludedGroups=source-api-live`,
`BUILD SUCCESS`: `auth` 10, `connector-okx` 27, `market-data` 38,
`trading-core` **92**, донор 324.

**Живого прогона у обоих компонентов нет и быть пока не может:** команды
шлёт FSM, которой ещё нет. Персистентность трёх новых сущностей и двух
новых колонок сверится со схемой на старте (`ddl-auto: validate`) при
ближайшей выкладке; миграция `V3` при ней же и накатится.

## Среда

Против v132 не изменилась. Стенд поднят и работает. Сборка идёт назначенным
JDK и Maven (`JAVA_HOME=C:/Users/RomanKrd/.jdks/corretto-25.0.3`, maven3 из
поставки IDEA) — системный `JAVA_HOME` указывает на JDK 11 и проект им не
собирается. Python корпусных прогонов —
`C:/Users/RomanKrd/AppData/Local/Programs/Python/Python311/python.exe`
(`python3` в PATH — заглушка магазина приложений).
