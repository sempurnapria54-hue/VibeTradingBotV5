# Snapshot v252

**Дата:** 2026-09-12.

## На какой вопрос отвечает этот файл

Где мы сейчас. **Шаг 11 фазы 2 в `GAPS_CLOSE_1`; узлы 1-3 приземлены, узел 4
(A5, A8, A9, B1, C5) правку положил — следующая единица — раунд критики узла
4.** Сменяет v251.

## Состояние

Фаза 1 — `FOLDED`. **Фаза 2 — `IN_PROGRESS`**: шаги 1-10 `DONE`, шаг 11 —
`GAPS_CLOSE_1`, шаги 12-13 `HOLD`. Прод-рубеж — `HOLD`.

**Гейтящих выносов держателю нет.**

**Дельта — staged, не закоммичена.** Ветка `claude-audit`. Дерево кода не
тронуто: единица правила `docs/**`, `.claude/**` и три файла `tools/**`.

**Режим — автономия до прод-рубежа.** Решения — Д250-Д1512.

## Что сделано после v251

**Единица — первая правка узла 4 со свипами**; отчёт —
`.claude/work/progress/phase-2-step-11-gaps-close-1-node-4.md`.

- **Правило узла:** свидетельство о полноте приёма производит тот, о чьей
  полноте оно свидетельствует. Строка состояния пары «группа × тема» живёт в
  базе самого durable-потребителя, обе величины считаются по ней, выдача чисел
  несёт свои величины, а не соседские.
- **Заведён сквозной дом формы** — `docs/rules/durable-consumer-reception.md`
  (владелец в карте — «сквозное»). Форма **вынесена** из дока журнала, а не
  скопирована: док стороны домом стыковой истины не бывает.
- **У статистики своё объявлено у владельца её следствия**
  (`docs/models/domain/other/StatisticsFact.md`): граница **одним** операндом,
  отсутствие гасящего писателя момента разрыва, свой тик, свои ряды с меткой
  сервиса и два правила алерта.
- **Чужое подключение к базе журнала снято вместе с грантом** — читателей у
  него не осталось. A5 закрыта по существу; снятие спеки грантов и
  переименование спеки полноты уехали дельтой кода: на них ссылается
  **применённая** миграция.
- **`insertion-neighborhood-check` починен по верности оси** — дельта захода
  ограничена меткой ближайшего последователя. Прежде отчёт предшественника
  краснел за правки последователей: коммитов внутри шага нет, и признак «заход
  идёт» верен у всех отчётов шага сразу.

## Вход следующей сессии

**Следующая единица — раунд критики узла 4** (независимый заход: автор
закрытия сам себя не критикует). Предмет — отчёт узла и его дельта против
метки `3aa50c5c8ed1ec0f8ce3fc5f9bff6d93526d6bc7`.

После него — верификация и приземление, затем очередной узел `GAPS_CLOSE_1` по
остатку гейтящих находок: A6, A7, A10, A14, A15, B4, B6-B10. Части прогона —
`.claude/work/progress/phase-2-step-11-docs-check-1*.md`.

- **B2 — продуктовая половина у держателя** (`BFF-Q1`, оживитель — шаг 13).
- **A7 идёт одним ходом** со сведением имени `audit-statistics` по корпусу: он
  же правит javadoc дерева кода, спеку грантов и имя спеки полноты.

**Ловушки** v206-v251 остаются в силе целиком; добавленные —
`phase-2-step-11-chronicle.md` §«Ловушки, оставленные узлом» (корпусный файл в
`allowed` реестра снятых редакций прогон отвергает структурно; спека полноты
несёт названный остаток в имени файла и двух величинах).

Обязательное перед завершением любой единицы:

1. **гейт инструментов корпуса** целиком — перечень в
   `.claude/skills/update-roadmap-progress.md` §«Гейт инструментов корпуса»,
   вместе с `backlog-check.py` (с подменой статуса, если статус проставляется);
2. **сборка — `bash tools/reactor-test.sh`**, если тронуто дерево кода;
3. **`py tools/code-gate-check.py`** — перед каждым кодовым заходом.

**Среда:** `python3` в этой оболочке разрешается в заглушку Windows Store и
возвращает код 49 — инструменты корпуса зовутся через `py`.

## Что висит

Всё, что живёт секциями бэклога с маркерами, печатает
`py tools/backlog-check.py` — в снапшоте не дублируется. Вне бэклога висит:

1. **Гейтящие находки `DOCS_CHECK_1`, не закрытые узлами 1-4** — вход
   остальных узлов `GAPS_CLOSE_1`.
2. **Мини-петля узла 4 не начата** — критика, верификация, приземление.
3. **Стенд от дерева кода отстал:** образы четырёх сервисов не собирались,
   роли и базы не заводились, тем и подписки нет.
4. **Ни одна пара производитель-потребитель не встречалась на живом брокере.**
5. **Спека и её реализация не связаны ничем механическим** — наблюдение,
   задачи пока нет: держатель о нём знает.
6. **Пользователю:** обновить Project Knowledge (этот снапшот); прочитать
   блоки дайджеста Д1500-Д1512 — право вето постфактум.

## Проверки на момент снапшота

**Гейт инструментов корпуса прогнан** на состоянии после всех правок — весь
перечень его дома, ненулевых кодов нет, кода 2 нет. Реактор этой единицей не
гонялся: дерева кода она не трогала. Числа прогонов здесь не пересказываются:
их печатает сам прогон (`.claude/rules/self-description-form.md`).

## Среда

Против v132 не изменилась; полное описание — там, перечень ловушек — в v206
(`.claude/work/history/snapshots/snapshot-v206.md` §Среда) и он остаётся в
силе.
