# Snapshot v224

**Дата:** 2026-09-10.

## На какой вопрос отвечает этот файл

Где мы сейчас. **Шаг 10 фазы 2 (аудит и статистика): аппрув `CODE` ВЫДАН
девятым прогоном фокусов; шаг в `SYNC_DOCS_FROM_CODE`.** Сменяет v223.

## Состояние

Фаза 1 — `FOLDED`. **Фаза 2 — `IN_PROGRESS`**: шаги 1-9 `DONE`, шаг 10 —
**`SYNC_DOCS_FROM_CODE`**, шаги 11-12 `HOLD`.

**Гейтящих выносов держателю нет.** Эта единица вопроса не выносила;
`.claude/work/questions/open-questions.md` ею не пополнялся.

**Дельта staged, не закоммичена.** Ветка `claude-audit`. **Дерево кода эта
единица не меняла:** четыре мутационные пробы восстановлены обратной заменой,
`git diff` по `services/` и `libs/` пуст.

**Режим — автономия до прод-рубежа.** Решения — Д250-Д1377.

## Что сделано после v223

**Единица сессии — девятый прогон фокусов**, и он **выдал аппрув `CODE`**.
Отчёт — `.claude/work/progress/phase-2-step-10-code-focuses-9.md`; статус — в
`phase-2-step-10-chronicle.md` §Статус; замер ряда прогонов фокусов — там же в
§«Реестр прогонов».

- **Ф12 и Ф13 закрыты по предмету, а не по отчёту о закрытии.** Мутация
  домена ступени красна; мутация класса отчёта красна; свип реестра снятых
  редакций по деревьям кода с нормализацией javadoc-переноса даёт ноль при
  контроле в 14 попаданий без allow-листа.
- **Пин СЛОВОМ доказан своей мутацией.** Переименование значения перечня
  (`HoldRung.HARD` → `HARD_STOP`) роняет **ровно** пробу пина словом:
  символьные пины уехали бы вместе с переименованием и промолчали.
- **Две новые находки, обе НЕ гейтят по классу** (`ИЗМЕРЕНИЕ` и `НОСИТЕЛЬ`,
  `.claude/skills/classify-code-blocking.md`), обе припаркованы:
  - **Ф16** — идентичности радиуса связаны у трёх приватных копий только
    совпадением ИМЕНИ, и связку не мерит ни одна проба у шести классов
    событий из семи. Предъявлено мутацией: переименование компонента формы
    оставило зелёными наборы проб ядра, периметра и журнала;
  - **Ф17** — константа `SOFT_RUNG_ON_THE_WIRE` стои́т под javadoc'ом,
    объявляющим критерий, которому она не отвечает.
- **Все позиции восьми прошлых прогонов перепроверены по предмету** —
  собственным чтением дерева и своими свипами; ни одна не переоткрылась.
- **Предъявлено:** 497 проб ядра зелены до мутаций и после восстановления,
  наборы периметра и журнала зелены, `bash tools/reactor-test.sh` — 0, гейт
  инструментов корпуса — 0.

## Три вещи, которые дальше нельзя пере-решать

**1. Перечень исполнителей не переоткрывается** (v195). Новый исполнитель
вносится **строкой** реестра, а не поводом пересобрать перечень.

**2. Реестр компонентов сквозной и новым шагом не пересобирается** (Д1184).
Находки в нём — прогона `DOCS_CHECK_34`; гейт срабатывает **на компоненте**,
когда тот начинается.

**3. Гейтящесть находки фокуса определяет КЛАСС, а не суждение прогона**
(Д1375). `КОД` и `РИСК` закрываются до статуса; `НОСИТЕЛЬ`, `ИЗМЕРЕНИЕ`,
`ГРУНТ`, `ПРОЦЕСС` — **диспозиционируются** (закрыты либо припаркованы с
домом, оживителем и причиной). Цена связки имён на проводе **принята и
названа** решением `docs/architecture/contracts.md` §«Носителя имён в общей
библиотеке не заводится, и это решение» — находка о ней мерит объём принятой
цены, а не вводит новый риск.

## Вход следующей сессии

**Единица — `SYNC_DOCS_FROM_CODE`**: фокус `divergence`
(`.claude/skills/divergence-review.md`) по всей дельте `CODE` шага 10 —
список add / change / remove, — затем реконсиляция
(`.claude/skills/reconcile-knowledge.md`). Направление **docs←code**: аппрув
снял вопрос «код или доки неверны». Проход одиночный, без `_N`.

- **Вход прогона** — дельта `CODE` шага целиком: `services/audit-statistics/**`,
  правки `services/trading-core/**`, `services/bff/**`,
  `libs/domain-model/**`, `libs/test-support/**`, `deploy/**` — плюс отчёты
  заходов K1-K20 в `.claude/work/progress/`.
- **Дальше:** пост-хок концепт-гейт (`.claude/processes/roadmap-step-execution.md`
  §6a) — он **применим**: на `CODE` вошли концепт-инкременты с решениями
  (ребро подъёма ступени, актор в содержимом, домен значения указателем).
  Ведётся как `DOCS_CHECK_N` по пост-sync докам. Каждый — своей единицей.

Обязательное перед завершением любой такой единицы:

1. **гейт инструментов корпуса** целиком;
2. **сборка — `bash tools/reactor-test.sh`**, если тронуто дерево кода;
   точечный прогон модулей — `mvn -o -am -pl services/<модуль> test` из корня.
   **`-am` обязателен**: общий артефакт в локальном репозитории не установлен.
   `mvn` на `PATH` нет — рабочая форма вызова стои́т в §Прогоны отчёта
   `.claude/work/progress/phase-2-step-10-code-focuses-9.md`;
3. **`py tools/code-gate-check.py`** — перед каждым кодовым заходом.

## Что висит

1. **Связка идентичностей радиуса по ИМЕНИ не мерится** у шести классов
   событий из семи — Ф16, припаркована второй осью секции о словах провода;
   тем же ходом правится клейм класс-javadoc пробы формы.
2. **Форма пина мягкой ступени разошлась с критерием своего блока** — Ф17,
   припаркована.
3. **Энфорсер снятых редакций слеп к javadoc-переносу** — Ф15, припаркована.
4. **Второй порядок цены отката ребра подъёма не назван** — Ф14, не гейтит.
5. **Ф11 открыта** — имя и javadoc константы гарда `CASCADE_SOURCE_STATUSES`.
6. **Транзакционная граница обоих рёбер прогоном не мерится** — держит
   объявление метода. Оживитель — первый живой прогон ядра на базе.
7. **Текст JPQL и нативных запросов не мерится ничем** — `@SpringBootTest` у
   модулей нет намеренно. Оживитель общий с п. 6.
8. **Разведение пар джоб ядра ПРОЧИТАНО, а не измерено.**
9. **Обращения каскада ступени выросли** — замера нет, тропа исключительная.
10. **Слепота считается состоявшимися проходами** — припарковано.
11. **Класс `HoldReleased` не построен.**
12. **Эпизод транша событием не различается.**
13. **Строки журнала, накопленные до хода, не чинятся.**
14. **Таблицы и писателя строки отказа у четырёх соседей нет** (`auth`,
    `trading-core`, `strategies`, `market-data`).
15. **Перехватчик `Exception` съедает отказ по правам у `bff` и `strategies`.**
16. **Предикат актора написан трижды** — единый носитель припаркован.
17. **Глубины у `access_denials` нет ни у одного сервиса.**
18. **Пул планировщика у `market-data` — один поток**; измерителя среди его
    джоб нет.
19. **Остаток пары «чистка × пересчёт»** у `audit-statistics`.
20. **Экспорт метрик у семи сервисов из восьми не построен**; экспорт
    **трейсов** не построен ни у кого.
21. **Подъём контекста не проверен** ни у `audit-statistics`, ни у
    `strategies`, ни у `bff`, ни у ядра в части `AsyncConfigurer`.
22. **Ни план запроса, ни порядок взятия строчных замков у модуля приёма не
    мерятся ничем.**
23. **Отбор ресурсов наблюдателем по метке `release` живым кластером не
    проверен.**
24. **Дубль запроса `earliestRecordedAt` у двух подключений остался** (Д1297).
25. **Позиции парковки узлов 5-6 `GAPS_CLOSE_3`** и парковка узла 1.
26. **Кандидат энфорсера окрестности** — бэклог.
27. **Стенд от дерева кода отстал:** образы `audit-statistics`, `strategies`,
    `trading-core` и `bff` с этими правками не собирались, роли и базы на
    стенде не заводились, тем и подписки на стенде нет.
28. **Форма дедупа «спросить и вставить»** — шесть точек трёх сервисов.
29. **Три долга инструментов:** проза реестра
    (`tools/retired-prose-debt.txt`), адресные перекрёстья `docs/**`, долг
    носителей команд (`tools/command-carrier-debt.txt`).
30. **§-адрес javadoc в несуществующий пассаж** — класс припаркован.
31. **Цена сессий под правилом единицы не замерена.**
32. **События `auth` — производителя и темы нет.**
33. **Единый error-DTO у четырёх построенных сервисов.**
34. **Потерянное событие при неразбираемом содержимом** — DLT нет.
35. **Названные ограничения шага 7**, ревалидация активных определений при
    смене чисел риск-аппетита, `HOLD_RELEASED` не пишется, база риска на
    терминале не двигается.
36. **Внешний вызов внутри транзакции БД — двенадцать точек** у ядра.
37. **Донорские остатки в общей библиотеке** — снимаются удалением донора.
38. **Отчёты критики и верификации узлов 5-6 `GAPS_CLOSE_3` отдельными
    файлами не сохранены.**
39. **Ряду прогонов фокусов не объявлено ни сигнала, ни порога** — детектор
    стагнации входом его не имеет; замер записан в хронику (Д1377).
40. **Т-1…Т-3 седьмого прогона гейта `GAPS_CLOSE_4`; Ц-2…Ц-4 первого и
    Д-1…Д-4 второго прогона гейта `GAPS_CLOSE_5`** — все о тексте о работе;
    уезжают с отчётами в `history/` при закрытии шага.
41. **Прежние хвосты:** S1 Vault-хардненинг, api-формы периметра без дока.
42. **Пользователю:** обновить Project Knowledge (этот снапшот). Новых
    Maven-модулей эта единица не заводила — переиндексация IDEA не нужна.

## Проверки на момент снапшота

**Гейт инструментов корпуса прогнан** на состоянии после всех правок, включая
этот файл, — весь перечень, ненулевых кодов нет, кода 2 нет. Числа прогонов
здесь не пересказываются: их печатает сам прогон
(`.claude/rules/self-description-form.md`).

**Реактор прогнан этой единицей** — дерево кода она трогала мутациями и
восстановила: 0 дефектов. Набор проб модуля `trading-core` — 497 зелёных, до
мутаций и после восстановления; наборы `bff` и `audit-statistics` зелены.

## Среда

Против v132 не изменилась; полное описание — там, перечень ловушек — в v206
(`.claude/work/history/snapshots/snapshot-v206.md` §Среда) и он остаётся в
силе целиком. Ловушки v207-v223 тоже в силе, включая: `python3` в этой среде
разрешается в заглушку Windows Store и падает кодом 49 — рабочая форма `py`;
heredoc питона внутри `bash -c` уходит в ту же заглушку — рабочая форма
`py - <<'EOF'`. **Ловушки этой сессии:** обратный слэш внутри heredoc'а
съедается — регекс со `\s` внутри `py - <<'EOF'` пишется через `chr(92)` либо
файл правится инструментом записи; временный скрипт, импортирующий реестр
`tools/retired-check.py`, читает ключ `pattern` (не `retired`) — неверный ключ
даёт зелёную выдачу **на любом корпусе**, поэтому у всякого такого свипа
обязателен контроль с проигнорированным `allowed`.
