# Snapshot v229

**Дата:** 2026-09-11.

## На какой вопрос отвечает этот файл

Где мы сейчас. **Шаг 10 фазы 2 (аудит и статистика): `GAPS_CLOSE_6`, узел 1
приземлён; узлы 2-6 открываются посерийно.** Сменяет v228.

## Состояние

Фаза 1 — `FOLDED`. **Фаза 2 — `IN_PROGRESS`**: шаги 1-9 `DONE`, шаг 10 —
**`GAPS_CLOSE_6`**, шаги 11-12 `HOLD`.

**Гейтящих выносов держателю нет.** Единственный вынос прогона — продуктовая
половина B2 — не гейтит и заводится узлом 5.

**Дельта staged, не закоммичена.** Ветка `claude-audit`. **Дерево кода эта
единица меняла** (шесть носителей, из них четыре — javadoc ядра); реактор
перепрогнан целиком.

**Режим — автономия до прод-рубежа.** Решения — Д250-Д1389.

## Что сделано после v228

**Единица сессии — приземление узла 1 `GAPS_CLOSE_6`**, заход корпусный и у
узла последний. Раздел отчёта —
`.claude/work/progress/phase-2-step-10-gaps-close-6-node-1.md` §«Приземление:
свипы и обход потребителей»; статус — в `phase-2-step-10-chronicle.md` §Статус.

- **Метка отбита до первой правки** (`git write-tree` → `efb46805…`), и перечень
  точек выведен разностью по ней: шесть носителей, восемь точек вставки. Метка
  отбита **после** постановки дельты узла в индекс — тогда разность даёт свою
  дельту, а не предшественников.
- **Новая норма поймала то, ради чего введена:** свип носительской оси дал
  носителя безусловного клейма, которого не называет **ни одна** находка второй
  верификации (лид javadoc `cascadeToError`). Перечень «по находкам» его не
  содержал бы по построению.
- **Все семь находок второй верификации закрыты**, включая два неблокирующих
  корпусных (Ф4, Ф5). Ряд блокирующих корпусных по проверенным заходам: **8 → 2 → 3**;
  четвёртого члена нет — у приземления проверяющего нет по построению.
- **Конструкция не тронута, и это предъявлено:** заход не правил ни одной пробы,
  а набор ядра зелен без их правки. Предметная ось не играет — названный исход.
- **Решения — Д1387-Д1389** (реестра снятых редакций на «безусловное прочтение»
  не заводится; Ф4/Ф5 закрыты, а не припаркованы; Ф2 закрыта снятием ложного
  указателя, а не его починкой).
- **Предъявлено:** гейт инструментов корпуса целиком — тринадцать команд, 0;
  реактор — 1582 файла, 1256 тестов, 0 дефектов.

## Три вещи, которые дальше нельзя пере-решать

**1. Узел 1 ЗАКРЫТ и приземлён.** Его находки (B1, К1-К11, В1-В6, Ф1-Ф7) исхода
не ждут; переоткрывать конструкцию резолва причины нечем — она принята критиком,
обеими верификациями и предъявлена четырьмя мутациями.

**2. Узлы 2-6 `GAPS_CLOSE_6` — посерийно, по одному на единицу работы.** Вход —
раскладка 21 находки в `phase-2-step-10-gaps-close-6.md`. Каждый гейтящий узел
идёт со своей мини-петлёй; негейтящие перечня популяции не требуют.

**3. Гейтящесть находки определяет КЛАСС, а не суждение прогона** (Д1375), и
реестр компонентов сквозной — новым шагом не пересобирается (Д1184).

## Вход следующей сессии

**Единица — узел 2 `GAPS_CLOSE_6`** (либо, если раскладка ставит иначе,
следующий по её порядку). Перед первой правкой:

- **отбить отправную метку** `git write-tree` и записать её в отчёт; перечень
  точек и терминов свипа выводить разностью по ней, а не по перечню находок
  (`.claude/rules/edit-kind-obligations.md` §«Предмет обязанностей — дельта
  захода, а не находки, на которые он отвечает»);
- **построить популяцию до правки** — оси предметная, носительская,
  окрестностная (`.claude/skills/closure-population.md`);
- **проставить классификацию по родам** в разделе, который называет строка
  таблицы заходов (`py tools/edit-kind-check.py` мерит именно это).

**После узлов 2-6 — прогон гейта выхода** (потолок прогонов — два, ведёт сессия,
закрытий не писавшая), затем перепрогон концепт-проверки; только чистый пост-хок
гейт пускает шаг в `DONE`.

Обязательное перед завершением любой единицы:

1. **гейт инструментов корпуса** целиком;
2. **сборка — `bash tools/reactor-test.sh`**, если тронуто дерево кода;
   точечный прогон — `REACTOR_MVN_ARGS="-am -pl services/trading-core test
   -DexcludedGroups=source-api-live" bash tools/reactor-test.sh`;
3. **`py tools/code-gate-check.py`** — перед каждым кодовым заходом.

## Что висит

1. **Узлы 2-6 `GAPS_CLOSE_6` не открыты** — см. выше.
2. **Api-формы периметра без дока** — восемь форм потока в коде, ноль в
   `docs/models/api/`; оживитель сужен до захода по собственной поверхности
   периметра (Д1378).
3. **Связка идентичностей радиуса по ИМЕНИ не мерится** у шести классов
   событий из семи — Ф16, припаркована.
4. **Форма пина мягкой ступени разошлась с критерием своего блока** — Ф17;
   A11 добавляет, что сам критерий дома не имеет (узел 4).
5. **Энфорсер снятых редакций слеп к javadoc-переносу** — Ф15.
6. **Второй порядок цены отката ребра подъёма не назван** — Ф14.
7. **Ф11 открыта** — имя и javadoc константы гарда `CASCADE_SOURCE_STATUSES`.
8. **Транзакционная граница обоих рёбер прогоном не мерится**; **текст JPQL и
   нативных запросов не мерится ничем** — `@SpringBootTest` у модулей нет
   намеренно. Оживитель общий — первый живой прогон ядра на базе.
9. **Разведение пар джоб ядра ПРОЧИТАНО, а не измерено.**
10. **Обращения каскада ступени выросли** — замера нет, тропа исключительная.
11. **Слепота считается состоявшимися проходами** — припарковано.
12. **Класс `HoldReleased` не построен**; **эпизод транша событием не
    различается**; **строки журнала, накопленные до хода, не чинятся**.
13. **Таблицы и писателя строки отказа у четырёх соседей нет** (`auth`,
    `trading-core`, `strategies`, `market-data`); **перехватчик `Exception`
    съедает отказ по правам** у `bff` и `strategies`; **предикат актора
    написан трижды**; **глубины у `access_denials` нет ни у одного сервиса**.
14. **Пул планировщика у `market-data` — один поток**; измерителя среди его
    джоб нет. **Остаток пары «чистка × пересчёт»** у `audit-statistics`.
15. **Экспорт метрик у семи сервисов из восьми не построен**; экспорт
    **трейсов** не построен ни у кого.
16. **Подъём контекста не проверен** ни у `audit-statistics`, ни у
    `strategies`, ни у `bff`, ни у ядра в части `AsyncConfigurer`.
17. **Ни план запроса, ни порядок взятия строчных замков у модуля приёма не
    мерятся ничем.**
18. **Отбор ресурсов наблюдателем по метке `release` живым кластером не
    проверен.**
19. **Дубль запроса `earliestRecordedAt` у двух подключений остался** (Д1297).
20. **Позиции парковки узлов 5-6 `GAPS_CLOSE_3`** и парковка узла 1.
21. **Кандидат энфорсера окрестности** — бэклог; остаток сужен до исполнения
    проб по объявленной метке (Д1386).
22. **Стенд от дерева кода отстал:** образы `audit-statistics`, `strategies`,
    `trading-core` и `bff` с этими правками не собирались, роли и базы на
    стенде не заводились, тем и подписки на стенде нет.
23. **Форма дедупа «спросить и вставить»** — шесть точек трёх сервисов.
24. **Три долга инструментов:** проза реестра
    (`tools/retired-prose-debt.txt`), адресные перекрёстья `docs/**`, долг
    носителей команд (`tools/command-carrier-debt.txt`).
25. **§-адрес javadoc в несуществующий пассаж** — класс припаркован.
26. **Цена сессий под правилом единицы не замерена.**
27. **События `auth` — производителя и темы нет**; **единый error-DTO у
    четырёх построенных сервисов**; **DLT при неразбираемом содержимом нет**.
28. **Названные ограничения шага 7**, ревалидация активных определений при
    смене чисел риск-аппетита, `HOLD_RELEASED` не пишется, база риска на
    терминале не двигается.
29. **Внешний вызов внутри транзакции БД — двенадцать точек** у ядра.
30. **Донорские остатки в общей библиотеке** — снимаются удалением донора.
31. **Отчёты критики и верификации узлов 5-6 `GAPS_CLOSE_3` отдельными
    файлами не сохранены.**
32. **Ряду прогонов фокусов не объявлено ни сигнала, ни порога** (Д1377).
33. **Т-1…Т-3 седьмого прогона гейта `GAPS_CLOSE_4`; Ц-2…Ц-4 первого и
    Д-1…Д-4 второго прогона гейта `GAPS_CLOSE_5`; неточность клетки О4 у
    доработки 2 узла 1** — все о тексте о работе; уезжают с отчётами в
    `history/`.
34. **Прежние хвосты:** S1 Vault-хардненинг.
35. **Пользователю:** обновить Project Knowledge (этот снапшот). Новых
    Maven-модулей эта единица не заводила — переиндексация IDEA не нужна.

## Проверки на момент снапшота

**Гейт инструментов корпуса прогнан** на состоянии после всех правок, включая
этот файл, — весь перечень, ненулевых кодов нет, кода 2 нет. **Реактор прогнан
целиком** — дельта единицы трогала дерево кода. Числа прогонов здесь не
пересказываются — их печатает сам прогон
(`.claude/rules/self-description-form.md`).

## Среда

Против v132 не изменилась; полное описание — там, перечень ловушек — в v206
(`.claude/work/history/snapshots/snapshot-v206.md` §Среда) и он остаётся в
силе целиком, вместе с ловушками v207-v228: `python3` разрешается в заглушку
Windows Store (рабочая форма `py`), heredoc питона внутри `bash -c` уходит
туда же (рабочая форма `py - <<'EOF'`), `grep -P` требует
`export LC_ALL=C.UTF-8`, полный гейт инструментов одной командой не
укладывается в 120 с, bash-heredoc с длинным русским телом рвётся на разборе
кавычек (длинные разделы отчётов пишутся инструментом записи во временный файл
и дописываются `cat`), `git diff <дерево>` печатает в stderr предупреждения о
CRLF по каждому тронутому файлу — вывод фильтруется. **Ловушка этой сессии:**
замер, скопированный из соседней таблицы отчёта, устаревает молча — три числа
гейта (`spec-run`, `population-derive-check`, `spec-mutation-check`) разошлись с
выдачей и правились сверкой, а не памятью.
