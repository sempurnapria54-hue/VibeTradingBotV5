# Snapshot v168

**Дата:** 2026-09-08.

## На какой вопрос отвечает этот файл

Где мы сейчас. **Шаг 10 фазы 2 (аудит и статистика): узел 5 `GAPS_CLOSE_4` —
последний — ЗАКРЫТ. Все узлы закрытия закрыты; сам под-шаг ещё нет.
Следующая единица — гейт выхода из `GAPS_CLOSE_N` и простановка следующего
статуса.** Сменяет v167.

## Состояние

Фаза 1 — `FOLDED`. **Фаза 2 — `IN_PROGRESS`**: шаги 1-9 `DONE`,
шаг 10 — **`GAPS_CLOSE_4`** (статус не двигался намеренно: под-шаг закрывает
гейт выхода, а он — следующая единица), шаги 11-12 `HOLD`.

**Гейтящих выносов нет.** Все 33 находки прогона имеют исход; узел 5 нёс
ноль гейтящих.

**Дельта в working tree, не в истории.** Ветка `claude-audit`; всё staged,
не закоммичено. Сверх v167 тронуты: `tools/derive/event-class-manual-path.sh`,
`docs/models/domain/other/AuditRecord.md`, `docs/architecture/contracts.md`,
`docs/rules/manual-halt.md`, `.claude/processes/roadmap-step-execution.md`,
`.claude/skills/update-roadmap-progress.md`, `.claude/skills/local-stand.md`,
`.claude/decisions/per-node-closure-frame.md`, `.claude/work/backlog.md`,
плюс отчёт узла и трёх его заходов, рамка закрытия, хроника, дайджест
(Д1127-Д1131), снапшот. **Дерево кода не тронуто** — как в v162-v167.

**Режим — автономия до прод-рубежа.** Решения — Д250-Д1131.

## Что сделано после v167

**Единица сессии — узел 5 `GAPS_CLOSE_4`** (A11, B9, B11, C3). Узел
**приземлён**; отчёт —
`.claude/work/progress/phase-2-step-10-gaps-close-4-node-5.md`
§«Приземление». Заходов — раунд критики и **две** верификации; потолок
(`PROC-Q7`) исчерпан и **не превышен**: вторая верификация блокирующих
корпусных дефектов не нашла, сигнал С5 не сработал.

**Правило узла:** клейм не сильнее своего предъявителя — команда печатает
обещанное, первоисточник существует, построенный исполнитель держит
объявленную форму; расхождение либо закрывается, либо становится названным
долгом.

**1. Обе стороны разности классов событий печатает одна команда** (Д1127).
Ключ `--built` у `tools/derive/event-class-manual-path.sh` печатает вторую
сторону в той же нотации; нормализация одна на сверку и на печать. Клейм у
владельца журнала усилен до предъявляемого: не «обе стороны», а **саму
разность** читатель получает прогоном (`comm -23` над двумя вызовами).

**2. Отчёт каждого захода мини-петли объявлен сохраняемым носителем**
(Д1128). Дом — `.claude/processes/roadmap-step-execution.md`: независимость
критика проверяется **только** по первоисточнику, а генератор `passes:`
считает заходы ровно по именам этих файлов — полностью несохранённый заход
не даёт ложного числа, он **отказывает** замер (код 2). Указатели — у гейта
статуса и у поправки `PROC-Q5`, из которой прежде был сделан обратный вывод.

**3. Три клейма скилла стенда заменены предъявителями** (Д1129): счёт «все
три» разошёлся с командой, собирающей четыре сервиса; строка `dev` больше не
переписывает перечень сервисов; разведены «что оверлей применяет» и «что
стенд собирает».

**4. Два расхождения исполнителя стали названным долгом, а доки не тронуты**
(Д1130, Д1131). Проверка «существует ли уже» перед вставкой предъявлена
**классом** из шести точек трёх сервисов (у каждой ключ в базе уже стои́т,
верная форма построена рядом) — дом идемпотентности верен и правки не
требует. У ручной поверхности к долгу добавлена клауза у дома: клейм таблицы
«заводит сама тропа» назван **нормой**, а не свойством построенного.

**5. Ретро-часть C3 не закрывается ничем, и это названо в самой норме:**
несохранённого захода не восстановить, отчёт закрытого узла описывает
прошлое и не переписывается.

## Три вещи, которые дальше нельзя пере-решать

**1. Узел 5 закрыт целиком.** Д1127-Д1131 вошли в силу с приземлением;
возврат ко второй команде в чужой нотации либо к счёту сервисов в прозе —
регресс.

**2. Отчёт захода мини-петли — носитель, а не черновик.** Узел с ненулевым
исходом и без файлов заходов гейт статуса не проходит.

**3. Расхождение построенного исполнителя с верным домом чинится долгом, а
не правкой дома.** Правка дома под построенное узаконила бы форму, которую
дом заведён запрещать.

## Вход следующей сессии

**Единица — гейт выхода из `GAPS_CLOSE_4` и простановка следующего статуса**
(`.claude/skills/update-roadmap-progress.md` §«Гейт выхода из
`GAPS_CLOSE_N`»): шесть предметов по отчёту закрытия — предъявленная
популяция гейтящих узлов, раздел свипов в предписанной форме, чек-листы
глубины, исходы мини-петли, полная диспозиция находок, записи выноса
держателю. Гейтящие узлы этого закрытия закрыты **дизайн-проходом** и узлом
1; узлы 2-5 гейтящих не несли.

Вход — `.claude/work/progress/phase-2-step-10-gaps-close-4.md` (рамка,
раскладка, статус заходов), отчёты узлов 1-5 и двух проходов рядом.
После гейта — следующий статус шага по
`.claude/processes/roadmap-step-execution.md` §4.

## Что висит

1. **Дельта `CODE` шага 10 — счёт стои́т порождённым блоком в хронике.**
2. **Компонентов шага 10 в `.claude/work/code-gate-ledger.json` нет** — их
   вносит простановка `CODE`.
3. **Позиции парковки узлов 5-6 `GAPS_CLOSE_3`** и парковка узла 1
   (исполнимость формы ценового результата на строках агрегатов).
4. **Образ сервиса без продвинутого тега резолвится подвижным `latest`**
   (`.claude/work/backlog.md`).
5. **Новое узлом 5:** форма дедупа «спросить и вставить» — шесть точек трёх
   сервисов; поглощённый отчёт ручной операции. Обе — секции бэклога.
6. **Долг прозы реестра** (`tools/retired-prose-debt.txt`).
7. **Цена сессий под правилом единицы не замерена.**
8. **События `auth` — производителя и темы нет**; экспорт метрик из семи
   прочих сервисов; таблицы `access_denials` нет ни у одного сервиса.
9. **Единый error-DTO у четырёх построенных сервисов.**
10. **Потерянное событие при неразбираемом содержимом** — DLT нет.
11. **Названные ограничения шага 7**, ревалидация активных определений при
    смене чисел риск-аппетита, `HOLD_RELEASED` не пишется, база риска на
    терминале не двигается.
12. **Внешний вызов внутри транзакции БД — двенадцать точек** у ядра.
13. **Донорские остатки в общей библиотеке** — снимаются удалением донора.
14. **Отчёты критики и верификации узлов 5-6 `GAPS_CLOSE_3` отдельными
    файлами не сохранены** — закрыто в части нормы узлом 5, в части прошлого
    не закрывается ничем.
15. **Прежние хвосты:** S1 Vault-хардненинг, рост таблицы отказов,
    api-формы периметра без дока, долг адресных перекрёстий.
16. **Пользователю:** переиндексировать проект в IDEA; обновить Project
    Knowledge (этот снапшот). **Стенд от дерева кода не отстал** — код не
    тронут.

## Проверки на момент снапшота

**Двенадцать команд корпуса прогнаны на этом состоянии.** Рабочий бинарь —
`py`; `python3` из PATH — заглушка, её код 49 читается как «не измерялось».
Гейт прогонялся **фоновой** командой: в 120 с он не укладывается. Числа
прогонов здесь не пересказываются — их печатает сам прогон
(`.claude/rules/self-description-form.md`).

**Реактор не прогонялся: дерево кода не тронуто.** Последний прогон — на
этом же состоянии кода (замер v163).

**Живого прогона нет ни у одного компонента шагов 7-10.**

## Среда

Против v132 не изменилась; полное описание — там. Ловушки среды из
v156-v167 остаются в силе. **Новое этой сессии:** heredoc `<<'MDEOF'` с
markdown-телом, содержащим тройные обратные кавычки, в этой оболочке рвётся
на «unexpected EOF» — файлы такого рода писать инструментом записи, а не
heredoc'ом; heredoc с телом Python остаётся рабочим. Полный гейт инструментов
в 120 с не укладывается — прогонять фоновой командой.
