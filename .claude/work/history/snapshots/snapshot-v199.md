# Snapshot v199

**Дата:** 2026-09-10.

## На какой вопрос отвечает этот файл

Где мы сейчас. **Шаг 10 фазы 2 (аудит и статистика) в `CODE`: сделан четвёртый
кодовый заход — компонент K4 (тик состояния приёма). Из тринадцати компонентов
закрыто четыре.** Сменяет v198.

## Состояние

Фаза 1 — `FOLDED`. **Фаза 2 — `IN_PROGRESS`**: шаги 1-9 `DONE`, шаг 10 — **`CODE`**,
шаги 11-12 `HOLD`.

**Гейтящих выносов держателю нет.** Эта единица вопроса не выносила;
`.claude/work/questions/open-questions.md` ею не пополнялся.

**Дельта staged, не закоммичена.** Ветка `claude-audit`. **Дерево кода
тронуто:** модуль `services/audit-statistics` получил тик состояния приёма,
добытчик срока хранения темы у брокера и три хода такта в репозитории; правлены
три доковых носителя ветви «срок не добыт», реестр компонентов и дайджест.

**Режим — автономия до прод-рубежа.** Решения — Д250-Д1248; эта единица завела
девять: Д1240-Д1248.

## Что сделано после v198

**Единица сессии — один кодовый заход по компоненту K4.** Отчёт —
`.claude/work/progress/phase-2-step-10-code-pass-k4.md`; статус — в
`phase-2-step-10-chronicle.md` §Статус и в реестре компонентов.

- **Тик построен целиком:** выключатель и такт `fixedDelay` в конфигурации,
  ручного фасада и защиты от перекрытия нет (объявленный класс экземпции, оба
  довода в javadoc), расписание сервиса включено этим же заходом.
- **Операнды разведены:** живость — контейнеры запущены и держат назначение;
  состав пар — **объявленная подписка** контейнера, а не назначение.
- **Ходов такта три, одной транзакцией:** заведение с поглощением конфликта,
  признак подписки, снятие признака у ушедших; строка ушедшей **не удаляется**,
  величин приёма такт не трогает.
- **Срок хранения темы добывается у брокера** тем же обходом; порог — доля от
  него, величина конфигурации (дефолт положен этим заходом). **Порог живёт
  ровно один такт.**
- **Живой прогон SQL: восемь утверждений зелены, пять мутаций SQL красны**;
  девять мутационных проб кода красны; тестов модуля 63;
  `bash tools/reactor-test.sh` — 0.
- **Правка корпуса тем же ходом:** ветвь «срок не добыт» приведена к признаку
  **применимости** срока в трёх носителях — прежняя редакция давала вечно
  срабатывающий алерт на теме, унаследовавшей срок от умолчания брокера.

## Три вещи, которые дальше нельзя пере-решать

**1. Перечень исполнителей не переоткрывается** (v195). Новый исполнитель
вносится **строкой** реестра, а не поводом пересобрать перечень.

**2. Реестр компонентов сквозной и новым шагом не пересобирается** (Д1184).
Находки в нём — прогона `DOCS_CHECK_34`; гейт срабатывает **на компоненте**,
когда тот начинается.

**3. Порядок компонентов реестром не назначен, и первенство K10 из
зависимости не следует** (Д1215). K10 меняет канонические конструкторы семи
записей, которые зовут писатели трёх сервисов. Он берётся вместе с
потребителями либо после них.

## Вход следующей сессии

**Единица — следующий кодовый заход** по одному компоненту шага 10.
Естественный следующий — **K5** (чистка журнала): его предмет — единственный
писатель, гасящий момент разрыва, и он же второй потребитель оси
`journalRetentionProfile`, которая до сервиса уже доезжает. Альтернатива —
K7/K8 (выборки чтения), но они приводят второе отображение схемы на классы и
первого читателя строк состояния, то есть больший заход.

Обязательное перед заходом и после него:

1. **простановка статуса кодирования компонента** в
   `.claude/work/code-gate-ledger.json` (`в работе` → `закодирован`), и
   **`py tools/code-gate-check.py` заново** — он прогоняется перед **каждым**
   кодовым заходом, не только на переходе;
2. **сборка — `bash tools/reactor-test.sh`**; точечный прогон одного модуля —
   `mvn -o -pl services/<модуль> test` из корня. `mvn` на `PATH` нет —
   рабочая форма вызова в отчёте `phase-2-step-10-code-pass-k4.md`
   §«Тесты модуля и мутационные пробы кода»;
3. **гейт инструментов корпуса** целиком перед завершением сессии.

Аппрув `CODE` (фокусы `conventions` / `performance` / `disaster` / `security`),
`SYNC_DOCS_FROM_CODE` и пост-хок концепт-гейт — после того, как компоненты
закодированы, по `.claude/processes/roadmap-step-execution.md`.

## Что висит

1. **Девять компонентов шага 10 из тринадцати — `не начат`**, включая писателя
   строки `access_denials` (Д1238).
2. **Экспорт метрик и правила алертов (K9) — операнды уже есть.** Порог по паре
   лежит снимком в памяти (`PairLagAlertThresholds`), экспортёра нет; **второе
   правило** — на пропажу ряда — приезжает тем же компонентом.
3. **Тропа к брокеру у трёх построенных соседей не объявлена ни сетевым
   правилом, ни автоконфигурацией** (`.claude/work/backlog.md` §«Тропа к
   брокеру у построенных соседей не объявлена ни правилом, ни
   автоконфигурацией»). Находка захода K3.
4. **Позиции парковки узлов 5-6 `GAPS_CLOSE_3`** и парковка узла 1
   (исполнимость формы ценового результата на строках агрегатов).
5. **Кандидат энфорсера окрестности** — бэклог; оживитель сработал переходом в
   `CODE`, закрытие — работа своего захода.
6. **Пять припаркованных позиций реестра** с условием «курационный заход после
   первого кодового захода» — оживитель сработал заходом K1.
7. **Стенд от дерева кода отстал:** образ `audit-statistics` не собран, роли и
   базы на стенде не заводились, тем и подписки на стенде нет. Плюс прежнее —
   образ сервиса без продвинутого тега резолвится подвижным `latest`.
8. **Форма дедупа «спросить и вставить»** — шесть точек трёх сервисов.
9. **Три долга инструментов:** проза реестра
   (`tools/retired-prose-debt.txt`), адресные перекрёстья `docs/**`, долг
   носителей команд (`tools/command-carrier-debt.txt`).
10. **§-адрес javadoc в несуществующий пассаж** — класс припаркован в
    `.claude/work/backlog.md`; оживитель сработал переходом в `CODE`.
11. **Цена сессий под правилом единицы не замерена.**
12. **События `auth` — производителя и темы нет.**
13. **Единый error-DTO у четырёх построенных сервисов** (`auth`,
    `connector-okx`, `market-data`, `trading-core`).
14. **Потерянное событие при неразбираемом содержимом** — DLT нет.
15. **Названные ограничения шага 7**, ревалидация активных определений при
    смене чисел риск-аппетита, `HOLD_RELEASED` не пишется, база риска на
    терминале не двигается.
16. **Внешний вызов внутри транзакции БД — двенадцать точек** у ядра.
17. **Донорские остатки в общей библиотеке** — снимаются удалением донора.
18. **Отчёты критики и верификации узлов 5-6 `GAPS_CLOSE_3` отдельными файлами
    не сохранены.**
19. **Т-1…Т-3 седьмого прогона гейта `GAPS_CLOSE_4`; Ц-2…Ц-4 первого и Д-1…Д-4
    второго прогона гейта `GAPS_CLOSE_5`** — все о тексте о работе; уезжают с
    отчётами в `history/` при закрытии шага.
20. **Прежние хвосты:** S1 Vault-хардненинг, рост таблицы отказов, api-формы
    периметра без дока.
21. **Пользователю:** переиндексировать проект в IDEA (у модуля добавилась
    зависимость `commons-collections4`); обновить Project Knowledge (этот
    снапшот).

## Проверки на момент снапшота

**Гейт инструментов корпуса прогнан** на состоянии после всех правок, включая
этот файл, — весь перечень, ненулевых кодов нет, кода 2 нет. Пофайловая
таблица исходов — `.claude/work/progress/phase-2-step-10-code-pass-k4.md`
§«Исход гейта инструментов». Числа прогонов здесь не пересказываются: их
печатает сам прогон (`.claude/rules/self-description-form.md`).

**Реактор прогнан: дерево кода тронуто.** `bash tools/reactor-test.sh` — 0,
компиляция всех деревьев и весь unit-набор.

**Живой прогон SQL такта есть** — на одноразовом Postgres 16, не на стенде и
не на образе окружения; брокера в прогоне нет вовсе.

## Среда

Против v132 не изменилась; полное описание — там. Ловушки среды из v156-v198
остаются в силе, включая: полный гейт инструментов в 120 с не укладывается —
прогонять фоновой командой (`tools/spec-mutation-check.sh` один держит несколько
минут); рабочий бинарь — `py`; в regex-шаблонах для `tools/retired-check.py`
пользоваться `\s+` и делать их терпимыми к разметке; вывод `py` через пайп идёт в
cp1251 и рвётся на кириллице (`export PYTHONIOENCODING=utf-8`); перед всяким
`-P`-прогоном ставить `export LC_ALL=C.UTF-8`; `\w` у `grep -P` кириллицы не
берёт — классы писать явно (`[а-яё]+`); файл журнала фоновой команды писать в
`$TEMP`; длинный кириллический heredoc в Bash рвётся — правки корпуса писать
временным `.py`-файлом либо инструментом записи файла; многострочная правка
целится в текст **с его отступом**; точная подстановка проверяется счётом
вхождений до записи; ждать фоновый гейт холостым циклом нельзя; точечный прогон
модуля идёт **из корня** (`-pl`), а не через `-f`; `docker` под Git Bash требует
`export MSYS_NO_PATHCONV=1`, а источник копирования — **относительным** путём;
`psql -c` с несколькими операторами шлёт их одной транзакцией.

**Ловушки этой сессии.**

- **Мутация, чья замена — пустая строка, не откатывается автоподстановкой.**
  Обратная правка ищет `''` и находит его между каждыми двумя символами файла.
  Мутацию, снимающую строку, объявлять **вместе с соседней строкой** — тогда обе
  стороны непусты. Стоило двух ручных восстановлений исходника.
- **`Set.of(массив)` бросает на дубликате.** Объявленная подписка контейнера
  приходит массивом, и повтор темы в нём законен: собирать через
  `List.of(...)` в изменяемое множество.
- **Литерал времени в пробнике ломает проверку связанных параметров:**
  `timestamptz '…10:00:00+00'` содержит двоеточия, и поиск `:\w+` после
  подстановки находит «несвязанный параметр `:00`». Проверять параметры **до**
  подстановки, по исходному телу запроса.
- **`cd` внутри составной команды Bash переносит рабочий каталог сессии.**
  Возвращаться явным `cd` в корень либо не использовать `cd` вовсе — пути
  абсолютные.
