# Snapshot v223

**Дата:** 2026-09-10.

## На какой вопрос отвечает этот файл

Где мы сейчас. **Шаг 10 фазы 2 (аудит и статистика) в `CODE`: гейтящие находки
Ф12 и Ф13 восьмого прогона фокусов закрыты, аппрув не выдан — его выдаёт
девятый прогон.** Сменяет v222.

## Состояние

Фаза 1 — `FOLDED`. **Фаза 2 — `IN_PROGRESS`**: шаги 1-9 `DONE`, шаг 10 — **`CODE`**,
шаги 11-12 `HOLD`.

**Гейтящих выносов держателю нет.** Эта единица вопроса не выносила;
`.claude/work/questions/open-questions.md` ею не пополнялся.

**Дельта staged, не закоммичена.** Ветка `claude-audit`. **Дерево кода этой
единицей тронуто:** четыре файла кода, две пробы, спека, реестр снятых
редакций.

**Режим — автономия до прод-рубежа.** Решения — Д250-Д1374.

## Что сделано после v222

**Единица сессии — кодовый заход K20**, закрывающий Ф12 и Ф13. Отчёт —
`.claude/work/progress/phase-2-step-10-code-pass-k20.md`; статус — в
`phase-2-step-10-chronicle.md` §Статус; позиции — в реестре компонентов у
`холды: …`, `trading-core: писатели событий, …` и
`audit-statistics: агрегатная выборка чтения`.

- **Ф12 закрыта на обоих концах.** Домен объявлен **указателем** на дом
  перечня (`docs/components/models/HoldSignal.md` §«Енум `HoldRung`») у
  четырёх носителей — обеих форм содержимого, ноты операнда спеки и
  приватной копии имён у периметра; состав перечня не копируется.
  Развилку прогона решил корпус: общий носитель имён запрещён решением
  `docs/architecture/contracts.md`, а спека стала бы вторым домом.
- **Измерено СЛОВОМ провода, а не символом перечня.** Признак механический —
  есть ли у слова ветвящийся потребитель. Три пробы в `CoreEventFormTest`;
  мутации M1 (ступень из лестницы объекта) и M2 (класс отчёта чужим
  перечнем) красны.
- **Свип по конструкции дал ВТОРОГО члена класса — `AnomalyReported.severity`**,
  закрыт тем же ходом.
- **Ф13 закрыта у трёх носителей и НЕ ждёт починки энфорсера.** Два кодовых
  носителя внесены **строками популяции** своих записей (ось 4 мерит носитель
  независимо от слепоты оси 1), донорский `StrategyEntity` — в `allowed`.
  Мутации M3 и M4 красны.
- **Предъявлено:** 497 проб модуля зелено (было 495), `bash tools/reactor-test.sh`
  — 0, гейт инструментов корпуса — 0.

## Три вещи, которые дальше нельзя пере-решать

**1. Перечень исполнителей не переоткрывается** (v195). Новый исполнитель
вносится **строкой** реестра, а не поводом пересобрать перечень.

**2. Реестр компонентов сквозной и новым шагом не пересобирается** (Д1184).
Находки в нём — прогона `DOCS_CHECK_34`; гейт срабатывает **на компоненте**,
когда тот начинается.

**3. Величину и её событие пишет ОДИН код ОДНОЙ транзакцией** (Д1362,
Д1366), а **домен значения содержимого объявляется указателем на дом
перечня и мерится у писателя формы** (Д1370, Д1371). Общего носителя имён
полей не заводится — это решение `docs/architecture/contracts.md`, а не
пробел.

## Вход следующей сессии

**Единица — ДЕВЯТЫЙ прогон фокусов** (`conventions` / `performance` /
`disaster` / `security`) по дельте K20. Он и выдаёт аппрув
(`.claude/processes/roadmap-step-execution.md` §«Аппрув `CODE` —
зафиксированный исход фокусов, не устное согласие»).

- **Вход прогона** — §«Что построено» отчёта
  `phase-2-step-10-code-pass-k20.md` (код, тесты, корпус) плюс сводки
  находок **всех восьми** прошлых прогонов: каждая позиция перепроверяется
  **по предмету, а не по отчёту о ней**.
- **Открытые негейтящие входы:** Ф11 (имя и javadoc константы гарда
  `CASCADE_SOURCE_STATUSES`), Ф14 (второй порядок цены отката: отказ ребра
  уводит сделку в `ERROR` без причины), Ф15 (слепота энфорсера,
  припаркована).

Дальше: `SYNC_DOCS_FROM_CODE` (фокус `divergence`) → пост-хок концепт-гейт
(`.claude/processes/roadmap-step-execution.md` §6a). Каждый — своей единицей.

Обязательное перед завершением любой такой единицы:

1. **гейт инструментов корпуса** целиком;
2. **сборка — `bash tools/reactor-test.sh`**, если тронуто дерево кода;
   точечный прогон одного модуля — `mvn -o -am -pl services/<модуль> test` из
   корня. **`-am` обязателен**: общий артефакт в локальном репозитории не
   установлен. `mvn` на `PATH` нет — рабочая форма вызова стои́т в §Прогоны
   отчёта `.claude/work/progress/phase-2-step-10-code-pass-k20.md`;
3. **`py tools/code-gate-check.py`** — перед каждым кодовым заходом.

## Что висит

1. **Слова провода четырёх компонентов терминала не запинены** — припарковано
   (`.claude/work/backlog.md`); форма закрытия — сквозная проба у потребителя
   по образцу `WireFormContractTest`.
2. **Энфорсер снятых редакций слеп к javadoc-переносу** — Ф15, припаркована.
3. **Второй порядок цены отката ребра подъёма не назван** — Ф14, не гейтит.
4. **Ф11 открыта, аппрув не гейтит.**
5. **Транзакционная граница обоих рёбер прогоном не мерится** — держит
   объявление метода. Оживитель — первый живой прогон ядра на базе.
6. **Текст JPQL и нативных запросов не мерится ничем** — `@SpringBootTest` у
   модулей нет намеренно. Оживитель общий с п. 5.
7. **Разведение пар джоб ядра ПРОЧИТАНО, а не измерено.**
8. **Обращения каскада ступени выросли** — замера нет, тропа исключительная.
9. **Слепота считается состоявшимися проходами** — припарковано.
10. **Класс `HoldReleased` не построен.**
11. **Эпизод транша событием не различается.**
12. **Строки журнала, накопленные до хода, не чинятся.**
13. **Таблицы и писателя строки отказа у четырёх соседей нет** (`auth`,
    `trading-core`, `strategies`, `market-data`).
14. **Перехватчик `Exception` съедает отказ по правам у `bff` и `strategies`.**
15. **Предикат актора написан трижды** — единый носитель припаркован.
16. **Глубины у `access_denials` нет ни у одного сервиса.**
17. **Пул планировщика у `market-data` — один поток**; измерителя среди его
    джоб нет.
18. **Остаток пары «чистка × пересчёт»** у `audit-statistics`.
19. **Экспорт метрик у семи сервисов из восьми не построен**; экспорт
    **трейсов** не построен ни у кого.
20. **Подъём контекста не проверен** ни у `audit-statistics`, ни у
    `strategies`, ни у `bff`, ни у ядра в части `AsyncConfigurer`.
21. **Ни план запроса, ни порядок взятия строчных замков у модуля приёма не
    мерятся ничем.**
22. **Отбор ресурсов наблюдателем по метке `release` живым кластером не
    проверен.**
23. **Дубль запроса `earliestRecordedAt` у двух подключений остался** (Д1297).
24. **Позиции парковки узлов 5-6 `GAPS_CLOSE_3`** и парковка узла 1.
25. **Кандидат энфорсера окрестности** — бэклог.
26. **Стенд от дерева кода отстал:** образы `audit-statistics`, `strategies`,
    `trading-core` и `bff` с этими правками не собирались, роли и базы на
    стенде не заводились, тем и подписки на стенде нет.
27. **Форма дедупа «спросить и вставить»** — шесть точек трёх сервисов.
28. **Три долга инструментов:** проза реестра
    (`tools/retired-prose-debt.txt`), адресные перекрёстья `docs/**`, долг
    носителей команд (`tools/command-carrier-debt.txt`).
29. **§-адрес javadoc в несуществующий пассаж** — класс припаркован.
30. **Цена сессий под правилом единицы не замерена.**
31. **События `auth` — производителя и темы нет.**
32. **Единый error-DTO у четырёх построенных сервисов.**
33. **Потерянное событие при неразбираемом содержимом** — DLT нет.
34. **Названные ограничения шага 7**, ревалидация активных определений при
    смене чисел риск-аппетита, `HOLD_RELEASED` не пишется, база риска на
    терминале не двигается.
35. **Внешний вызов внутри транзакции БД — двенадцать точек** у ядра.
36. **Донорские остатки в общей библиотеке** — снимаются удалением донора.
37. **Отчёты критики и верификации узлов 5-6 `GAPS_CLOSE_3` отдельными
    файлами не сохранены.**
38. **Т-1…Т-3 седьмого прогона гейта `GAPS_CLOSE_4`; Ц-2…Ц-4 первого и
    Д-1…Д-4 второго прогона гейта `GAPS_CLOSE_5`** — все о тексте о работе;
    уезжают с отчётами в `history/` при закрытии шага.
39. **Прежние хвосты:** S1 Vault-хардненинг, api-формы периметра без дока.
40. **Пользователю:** обновить Project Knowledge (этот снапшот). Новых
    Maven-модулей эта единица не заводила — переиндексация IDEA не нужна.

## Проверки на момент снапшота

**Гейт инструментов корпуса прогнан** на состоянии после всех правок, включая
этот файл, — весь перечень, ненулевых кодов нет, кода 2 нет. Числа прогонов
здесь не пересказываются: их печатает сам прогон
(`.claude/rules/self-description-form.md`).

**Реактор прогнан этой единицей** — дерево кода она правила: 0 дефектов.
Набор проб модуля `trading-core` — 497 зелёных, до мутаций и после
восстановления.

## Среда

Против v132 не изменилась; полное описание — там, перечень ловушек — в v206
(`.claude/work/history/snapshots/snapshot-v206.md` §Среда) и он остаётся в
силе целиком. Ловушки v207-v222 тоже в силе, включая: `python3` в этой среде
разрешается в заглушку Windows Store и падает кодом 49 — рабочая форма `py`;
heredoc питона внутри `bash -c` уходит в ту же заглушку — рабочая форма
`py - <<'EOF'`. **Ловушка этой сессии:** `git checkout -- <файл>` откатывает
**всю** правку файла, а не только мутацию; при мутационной пробе внутри
собственной правки восстанавливать надо обратной заменой, а не из индекса.
