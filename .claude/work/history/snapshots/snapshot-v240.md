# Snapshot v240

**Дата:** 2026-09-11.

## На какой вопрос отвечает этот файл

Где мы сейчас. **Шаг 10 фазы 2 (аудит и статистика): гейт выхода из
`GAPS_CLOSE_6` пройден ЗЕЛЁНЫМ по корпусу; следующая единица — переход шага в
`DONE` целиком.** Сменяет v239.

## Состояние

Фаза 1 — `FOLDED`. **Фаза 2 — `IN_PROGRESS`**: шаги 1-9 `DONE`, шаг 10 —
**`GAPS_CLOSE_6`** (гейт пройден, статус ещё не записан), шаги 11-12 `HOLD`.

**Гейтящих выносов держателю нет.** Единственный вынос — `BFF-Q1` с вердиктом
«не гейтит, адресат шага 12».

**Дельта staged, не закоммичена.** Ветка `claude-audit`. **Дерево кода эта
единица не трогала** — правлен один корпусный носитель (`.claude/rules/edit-kind-obligations.md`)
и три отчёта; реактор поэтому не прогонялся.

**Режим — автономия до прод-рубежа.** Решения — Д250-Д1445.

## Что сделано после v239

**Единица — второй (и последний) прогон гейта выхода из `GAPS_CLOSE_6`**
(единица 13 §«Единицы закрытия»). Разбор —
`.claude/work/progress/phase-2-step-10-gaps-close-6.md` §«Второй прогон гейта
выхода из `GAPS_CLOSE_6`».

- **Исход — ЗЕЛЁНЫЙ по корпусу.** Шесть предметов гейта зелены у шести узлов и
  двух концепт-проходов. Ряд корпусных находок прогонов гейта — **2 → 0**;
  **С6 не сработал**, потолок исчерпан сходимостью, а не остановкой.
- **К1 и К2 перепроверены пробами на нынешней редакции**, а не по вердикту
  прогона 1: обе формы обхода потребителей узла 1 перепрогнаны и сведены с
  таблицей носительской оси **машинно** (38 членов выдачи, ноль без исхода);
  проба К2 на переписанной секции бэклога даёт по всем четырём расхождениям
  ноль.
- **`target/` выведен из области исправленной пробой:** форма обхода с
  `--include=*.java` берёт и порождённые исходники, а они не корпус
  (`git check-ignore` → `target/`). Прогон исполнил исправленную пробу, а не
  поверил отчёту.
- **Сверх шести предметов проверена обязанность рода `дом` на входящих
  указателях** — её не мерит ни `anchor-check.py` (мерит разрешимость), ни
  `doc-pointer-check.py` (мерит существование файла). У всех трёх домов,
  заведённых закрытием, входящие указатели есть.
- **Находка Г1 — ложный счёт пассажей-домов** в
  `.claude/rules/edit-kind-obligations.md` («родов семь, а пассажей-домов
  девять»): родов с двумя пассажами **три**, пассажей **десять**, и клейм ложен
  **с рождения** — введён `GAPS_CLOSE_4` того же шага тем же коммитом, что и
  строка, его опровергающая. Ни одного предмета гейта не нарушает, С6 не
  двигает; **правлена на месте**, счёт снят (порождённой формы под такой вывод
  в закрытом перечне генераторов нет).
- **Находка Г2 — ось 9 `tools/command-carrier-check.py` слепа к `read` с
  флагом:** имя, связанное `while IFS= read -r line` в том же блоке, объявляется
  несвязанным. Дефект ложно-положительный, предъявлен собственным прогоном (блок
  пробы Г1 получил два ДЕФЕКТа и переписан без цикла) и **припаркован** —
  `.claude/work/backlog.md` §«Ось 9 энфорсера команд слепа к `read` с флагом»:
  правка энфорсера требует своей падающей пробы и контроля.
- **Статус НЕ проставлен, и это порядок, а не пропуск** — см. §«Вход следующей
  сессии».

## Три вещи, которые дальше нельзя пере-решать

**1. Гейт выхода пройден; третьего прогона нет и не нужно.** Потолок — два
прогона на закрытие, и второй зелен
(`.claude/rules/docs-loop-limits.md` §«Потолок прогонов гейта выхода — два на
закрытие»). Переоткрывать `GAPS_CLOSE_6` не на чем: все шесть предметов
предъявлены пробами, разбор — в отчёте.

**2. Запись `DONE` идёт ПОСЛЕ погашения долга клеток-носителей, а не до.**
Область `tools/command-carrier-check.py` — `.claude/work/progress/**`; все 21
строка `tools/command-carrier-debt.txt` адресуют отчёты шага 10; перенос
артефактов в `history/` предписан тем же ходом, что `DONE`
(`.claude/rules/closed-work-transfer.md` §Roadmap). Перенос без погашения роняет
гейт инструментов на строках долга без соответствия.

**3. Восемь секций бэклога ждут статуса `DONE` и обязаны получить исход тем же
ходом** (`.claude/rules/backlog-section-form.md` §«Исход сработавшего условия»).
Четыре из них ждут `шаг:2-10:CODE` — условие читает **нынешний** статус, а не
историю, и вернётся записью `DONE`; заморозка усиления измерения, под которой
они припаркованы, снята входом шага в код и сегодня не действует, то есть задачи
**открыты**.

## Вход следующей сессии

**Единица — переход шага 10 фазы 2 в `DONE` целиком.** Порядок ходов внутри
неё назван прогоном гейта
(`phase-2-step-10-gaps-close-6.md` §«Предсостояние статуса: восемь оживителей
срабатывают на `DONE`»):

1. **погасить `tools/command-carrier-debt.txt`** — 21 строка, все в живых
   отчётах шага 10; задача и её дом — `.claude/work/backlog.md` §«Команды в
   клетках таблиц у живых отчётов»;
2. **дать исход восьми сработавшим секциям** — закрыта / перевооружена / взята
   в работу; перечень секций стои́т таблицей в том же разделе отчёта;
3. **записать статус** `DONE` в `.claude/work/roadmap/phase-2.md` и пересчитать
   ролляп фазы (`.claude/skills/update-roadmap-progress.md`); фаза остаётся
   `IN_PROGRESS` — шаги 11-12 `HOLD`, гейт закрытия фазы не применяется;
4. **перенести артефакты шага в `history/`** — хронику и отчёты
   (`.claude/rules/closed-work-transfer.md`), починив входящие ссылки из живых
   носителей тем же ходом;
5. свип курации и снапшот.

**Предусловия `DONE` проверены этим прогоном и держатся:** аппрув `CODE` выдан
девятым прогоном фокусов (все четыре — `conventions`, `performance`, `disaster`,
`security`), `divergence` на `SYNC_DOCS_FROM_CODE` — пять расхождений, все
реконсилированы, `py tools/code-gate-check.py` — 0.

**Ловушки** v206-v239 остаются в силе целиком. Подтверждены этой единицей:

- **`python` в этой среде — заглушка магазина Windows**: `python3 …` корпуса
  здесь исполняется `py …`;
- **правки отчётов писать Python'ом с явным `utf-8` и `newline='\n'`**;
  **вложенный heredoc в Bash рвёт длинный markdown с блоками кода** — фрагмент
  писать отдельным файлом и сплайсить коротким `py -c`;
- **временный каталог в корне роняет `tools/retired-check.py` кодом 2**
  («каталоги верхнего уровня вне объявленной области») — убирать до гейта;
- **ось 9 `command-carrier-check.py` штрафует `while IFS= read -r name`** —
  блок с циклом чтения писать без флага нельзя, а с флагом энфорсер краснеет;
  обход — обойтись без цикла (см. Г2);
- **греп указателя литеральными пробелами даёт ЛОЖНЫЙ НОЛЬ**: имя пассажа
  разорвано переносом строки, а в java — ещё и звёздочкой продолжения javadoc.
  Проба обхода обязана быть терпима к переносу (`-UzP` + `\s+`), иначе она
  показывает отсутствие того, что есть.

Обязательное перед завершением любой единицы:

1. **гейт инструментов корпуса** целиком — четырнадцать команд, вместе с
   `backlog-check.py`;
2. **сборка — `bash tools/reactor-test.sh`**, если тронуто дерево кода;
3. **`py tools/code-gate-check.py`** — перед каждым кодовым заходом.

## Что висит

Всё, что живёт секциями бэклога с маркерами, печатает
`py tools/backlog-check.py` — в снапшоте не дублируется. Вне бэклога висит:

1. **Переход шага 10 в `DONE` не сделан** — следующая единица.
2. **Продуктовая половина B2 у держателя** — `BFF-Q1`, оживитель — шаг 12.
3. **Стенд от дерева кода отстал:** образы четырёх сервисов не собирались,
   роли и базы не заводились, тем и подписки нет.
4. **Отчёты критики и верификации узлов 5-6 `GAPS_CLOSE_3` отдельными файлами
   не сохранены.**
5. **Текстовые находки узлов 3-6** — уезжают с отчётами в `history/`.
6. **Пользователю:** обновить Project Knowledge (этот снапшот). Новых
   Maven-модулей единица не заводила — переиндексация IDEA не нужна.

## Проверки на момент снапшота

**Гейт инструментов корпуса прогнан** на состоянии после всех правок — весь
перечень, ненулевых кодов нет, кода 2 нет. **`backlog-check.py` с подменой
`--статус 2-10=DONE` даёт код 1 намеренно** — это предсостояние следующей
единицы, а не дефект: без подмены прогон зелен. **Реактор не прогонялся:**
дерева кода единица не касалась. Числа прогонов здесь не пересказываются — их
печатает сам прогон (`.claude/rules/self-description-form.md`).

## Среда

Против v132 не изменилась; полное описание — там, перечень ловушек — в v206
(`.claude/work/history/snapshots/snapshot-v206.md` §Среда) и он остаётся в силе
целиком, вместе с ловушками v207-v239. Ловушки этой единицы — в §«Вход
следующей сессии» выше.
