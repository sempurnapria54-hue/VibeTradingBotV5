# Snapshot v124

**Дата:** 2026-09-05.

## На какой вопрос отвечает этот файл

Где мы сейчас. **Шаги 1 и 2 фазы 2 закрыты: монорепозиторий и платформа.**
Сменяет v123.

## Состояние

Фаза 1 — `FOLDED`. **Фаза 2 — `IN_PROGRESS`**: шаги 1-2 `DONE`, шаги 3-12
`HOLD`. Фазы 3-6, 7-8 и прод-рубеж — `HOLD`. Ветка `claude-audit`; дельта
в working tree, не закоммичена.

**Режим — автономия до прод-рубежа.** Решения — Д250-Д611 (итерация 28).
**Открытый вынос держателю один:** ARCH-Q1 в остатке — провайдер облака и
CI-система; гейтит только прод-рубеж, шаги 3-12 не задевает.

## Что сделано в этой задаче

**Шаг 1 (монорепозиторий) закрыт** —
`.claude/work/history/2026-09-05-phase-2-step-1-monorepo.md`. Фокус
`conventions` дал находку класса «перечень молча теряет дерево»; закрыта
не инстансом, а осью 20 `retired-check.py` — сверкой области свипа с
фактическим корнем репозитория.

**Шаг 2 (платформа) закрыт** —
`.claude/work/history/2026-09-05-phase-2-step-2-platform/`. Семь прогонов
концепт-проверки (ряд находок 10 → 5 → 2 → 1 → 1 → 0, плюс пост-хок концепт-гейт
`.claude/processes/roadmap-step-execution.md` §6a с двумя), пять закрытий, `CODE` с шестью находками фокусов.

**Что поставлено кодом:**

- `deploy/base/` — 13 манифестов: пространство имён, запрет по умолчанию,
  установка семи операторов Argo CD-приложениями, инстансы данных;
- `deploy/{dev,stage,prod}/` — оверлеи, несущие **только** оси различия и
  единственное место тега образа на сервис;
- `tools/deploy-layout-check.py` — энфорсер раскладки, 12 осей, батарея
  тем же прогоном (поймал дефект собственного парсера контрольной осью);
- `tools/deploy-set-image-tag.sh` + `deploy_set_image_tag.py` — продвижение
  неизменяемого тега, 4 оси доказаны поимённо;
- `docs/spec/environment-contour.json` + `tools/derive/environment-contour-matrix.sh`
  — исполнимая форма правила допуска контура площадки.

**Что поменялось в концепции.** Правило яруса приведено к своему доводу
(Д600): ярус запрещает **восходящее**, разрешение даёт поимённая строка
контракта. Формулировка «только вниз» жила в `docs/concept.md` и была
шире собственного основания, запрещая заодно три живых внутриярусных
вызова.

**Проверки зелёные:** десять команд корпуса код 0 (носителей 1084, 43
записи снятых редакций, 23 спеки / 580 примеров, 389 величин
доказательны), `mvn -o -f donor/pom.xml test -DexcludedGroups=source-api-live`
→ **320 тестов, 0 падений**.

## Что ратифицировано и не пере-решается

Всё из v104-v123. Плюс от шага 2: контур площадки — атрибут счёта, а
окружение объявляет допустимые контуры (Д592-Д593); число реплик —
свойство сервиса, у ядра единица (Д601); транспорт внутри кластера
открытый **названным** выбором с условием пересмотра перед первым `prod`
(Д604); тег неизменяем и продвигается (Д596, Д607).

## Что висит

1. **`подтверждение`** — ARCH-Q1 в остатке: провайдер и CI-система.
   `.claude/work/backlog.md` §«Проработка облака, хостинга кода и CI»;
   оживит — инфраструктурные шаги роадмапа.
2. **Три новых якоря шага 2** в `.claude/work/backlog.md`: §«Подпись
   образов и политика допуска — ввод на прод-рубеже», §«Транспорт внутри
   кластера — пересмотр перед первым `prod`» (оба со встречным якорем),
   §«Размеры томов данных — назначение по наблюдаемому потреблению»
   (оживит — шаг 6, `market-data`), §«Темы Kafka по производителям»
   (оживит — шаги 4-8), §«Контур биржевого счёта — персистентность и
   писатель» (оживит — шаг 4).
3. **Что не проверено прогоном на шаге 2:** кластера в среде нет, ни один
   манифест не применялся; версии чартов — заметные плейсхолдеры
   `PIN-ON-FIRST-DEPLOY`, закрепляются первым развёртыванием
   (`deploy/README.md` §«Что не проверено прогоном»).
4. **Прежние хвосты** — S1 Vault, контракт броска диспетчера, ограничения
   контура доступа (шаг 4), рост таблицы отказов, A12, хвост шага 8, долг
   адресных перекрёстий `docs/**`, провизорные числа калибровки, дубль
   эталона стратегии в корне.
5. **Пользователю:** переиндексировать проект в IDEA (корень модуля —
   `donor/`); обновить Project Knowledge: этот снапшот, дайджест
   (итерация 28), `structure-digest.md`.

## Что дальше

**Шаг 3 — общая доменная модель (`libs/domain-model`):** сущности,
перечни, события и их конверт; разведение площадки, тенанта и биржевого
счёта. Первый шаг порта: `DOCS_CHECK` над доками по оси владельца
(`.claude/rules/knowledge-ownership-by-service.md`, кластер
`domain-model` — формы моделей ядра и `Auditable`) против
`docs/architecture/`, затем `CODE` с донором как источником.

## Среда

Изменений с v122 нет. Сборка — `donor/pom.xml`, команды из корня с
`-f donor/pom.xml`. **Maven в `PATH` отсутствует** (рабочий — из поставки
IDEA); **`JAVA_HOME` среды указывает на JDK 11**, рабочая форма —
`JAVA_HOME=$HOME/.jdks/corretto-25.0.3`; лаунчер Python — `py -3`.
**Кластера Kubernetes нет** — манифесты шага 2 не применялись.
