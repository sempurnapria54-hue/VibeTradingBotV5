# Snapshot v32

**Дата:** 2026-06-06.

## На какой вопрос отвечает этот файл

Где мы сейчас и как сюда пришли (срез посреди `TOOLING` шага 3:
`solution-designer` создан, первый прогон делегирования выполнен —
12 R1-пакетов ожидают валидации пользователя).

## Состояние

Фаза 1 — `IN_PROGRESS`; шаги 1-2 — `DONE`; **шаг 3 — `TOOLING`
(идёт сейчас)**; шаги 4-11 — `HOLD`. Кодовая база — без изменений.
Работа этого среза — обкатка программы делегирования: прогон
открытых вопросов по владельцам, результаты по R1.

## Что изменилось относительно v31

### Роль `solution-designer` создана

`.claude/agents/solution-designer.md` (коммиты `ea98678`/`0445b5f`):
конструктивный проектировщик концепции, владелец концепт-категории
делегирования. Границы: проектирует держателей состояния/
персистентность, топологию и нейминг доменных моделей, механизмы
оркестрации; торговое — `trading-specialist` (на смешанной развилке
торговое первым, как ограничение), размещение знания — curator,
критика — `reviewer`, код — `code-writer`. Рабочая процедура
намеренно не зафиксирована — снимается с первой обкатки (этот
прогон). Концепт-категория разблокирована с R1.

### Первый прогон делегирования (обкатка)

Задача: отработать открытые вопросы `open-questions.md` по
`.claude/processes/question-delegation.md`, результаты по R1.

**Состав:** взято 12 — PROC-Q1, DEAL-Q3, CMD-Q1 (curator; категория
штатно R3, возврат по R1 разовый — по условию прогона, вне счёта
гейта); ENUM-Q1 (specialist + curator); RISK-Q1, STRAT-Q4, OKX-Q1,
OKX-Q3, INSTR-Q2 (specialist); DEAL-Q1, INSTR-Q1, ORCH-Q1
(designer). Отложено 5 — DEAL-Q2, OKX-Q2, OKX-Q4 (П —
пользовательские), RISK-Q2 (якорь шаг 5), IND-Q1 (якорь шаг 3 —
возьмёт штатная петля самого шага).

**Ключевые рекомендации пакетов** (полные разборы — трейл):
PROC-Q1 — `PositionContext` рудимент; DEAL-Q3 — `domain/other` +
свой lifecycle (прецедент `AnomalyReport`); CMD-Q1 — подтвердить
file-per-executor; ENUM-Q1 — подтвердить `RISK_CONTROL`; RISK-Q1 —
не RVO, «глобальная risk policy» с материализацией на шаге 5;
STRAT-Q4 — паттерн «у каждого `percents` есть якорь», для
PROFIT_/LOSS_ фикс-якорь «% цены от входа» (не ROE — несовместимо с
динамическим плечом); OKX-Q1 — `TradeFill` нужен, срок шаг 7;
OKX-Q3 — bills обязательны для PnL перпа (funding), шаг 7 одним
заходом с fills; INSTR-Q2 — set-leverage на каждую сделку,
`externalLeverage` = дубль `externalMaxLeverage` (схлопывается
INSTR-Q1), `HOLD` к плечу не привязывать; DEAL-Q1 — расширить
`DealActionState` на системные действия (nullable `strategyActionId`
+ `SystemActionType`; `RuntimeTarget` уже включает `DEAL`/`NONE`);
INSTR-Q1 — rules → persisted-проекция снапшота **JSONB на
`Instrument`** с ренеймом к «specification»-семантике, единая
граница `InstrumentExternalSnapshot`; ORCH-Q1 — лёгкий координатор
онбординга (канонизация seam `refreshInstrumentReadiness`), не
FSM-машина; якорь пересмотра — backlog п.9.

### Трейл и леджер стоят (первое делегирование)

- **Леджер гейта** — `.claude/work/delegation-ledger.md`: ступени
  (классификация R3, трейдинг R1, концепт R1), 12 прогонов в
  статусе «ожидает валидации». Место выбрано CC (`.claude/work/`
  рядом с backlog; слота в `structure.md` нет) — на подтверждение.
- **Трейл (разборы решений)** —
  `.claude/work/progress/delegation-run-open-questions.md`: состав +
  12 полных разборов (основание, варианты, рекомендация, реплики
  агентов, материализация при валидации).

Канон не тронут: `open-questions.md`, `decisions/`, файлы знания —
без правок до валидации.

## Активные задачи

- `delegation-run-open-questions.md` — **ожидает валидации R1**.
- `trading-council-materialization.md`,
  `trading-library-distillation.md` — без изменений.

## Текущий фронтир / следующее действие

**Валидация 12 R1-пакетов пользователем** (принять / поправить /
отклонить по каждому) → материализация принятых (decisions, правки
канона, закрытие вопросов) → обновление леджера (чисто/правка; счёт
к гейту R1→R2 у трейдинга и концепта). После — закрытие `TOOLING` и
`DOCS_CHECK_1` шага 3 «Индикаторы». Хвосты на входе шага 3: IND-Q1
(возьмёт петля шага), warmup-derive у реализаций индикаторов,
runtime-прогон Strategy API при поднятом PostgreSQL.

Коммит — за пользователем (всё staged, CC не коммитит).

## Открытые общие вопросы

`open-questions.md`: **17** (без изменений — прогон подготовил
закрытия, но до валидации вопросы не снимаются; после материализации
ожидается закрытие до 5-7).

## Что в работе / PK

- Ветка — `claude-audit`.
- **Project Knowledge:** последний снапшот теперь **`snapshot-v32`**
  (заменяет v31 в префлайте). `CLAUDE.md` / `structure.md` /
  `naming.md` / `place-knowledge.md` не менялись.
- Staged сейчас: `delegation-run-open-questions.md` (трейл),
  `delegation-ledger.md`, этот снапшот; плюс 5 файлов
  `.claude-archive/.../skills/` — только перенос строк (LF/CRLF),
  содержательных изменений нет.
