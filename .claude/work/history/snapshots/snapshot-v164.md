# Snapshot v164

**Дата:** 2026-09-08.

## На какой вопрос отвечает этот файл

Где мы сейчас. **Шаг 10 фазы 2 (аудит и статистика): `GAPS_CLOSE_4`,
четвёртая единица — приземление узла 1 — ЗАКРЫТА. Следующая единица —
узел 2 (A7).** Сменяет v163.

## Состояние

Фаза 1 — `FOLDED`. **Фаза 2 — `IN_PROGRESS`**: шаги 1-9 `DONE`,
шаг 10 — **`GAPS_CLOSE_4`**, шаги 11-12 `HOLD`.

**Гейтящих выносов нет** на текущий момент; у узла 2 своя половина дефицита
`подтверждение` появится при его открытии (ниже).

**Дельта в working tree, не в истории.** Ветка `claude-audit`; всё staged,
не закоммичено. Сверх v163 тронуты: `docs/architecture/contracts.md` (дом
периметрового исхода), `docs/models/domain/other/AuditRecord.md` (указатель
на этот дом), `docs/spec/statistics-aggregates.json` (две ноты операндов),
плюс отчёты узла и рамки, хроника, дайджест (Д1108-Д1110), снапшот.
**Дерево кода не тронуто** — как в v162-v163.

**Режим — автономия до прод-рубежа.** Решения — Д250-Д1110.

## Что сделано после v163

**Единица сессии — приземление узла 1 `GAPS_CLOSE_4`** (A1, A6, B4, B5 —
провод: чем едет конверт, что едет содержимым, кто читает). Узел
**приземлён**; отчёт —
`.claude/work/progress/phase-2-step-10-gaps-close-4-node-1.md` §«Состояние
узла: ПРИЗЕМЛЁН». Рамка закрытия —
`.claude/work/progress/phase-2-step-10-gaps-close-4.md` §«Четвёртая единица
закрытия — приземление узла 1».

**Приземление — не третий заход мини-петли.** Потолок верификаций
(`PROC-Q7`) не тронут, доработки узла не было: единица разобрала два
остатка остановки, оставшихся после прохода по самоописанию, и оба были
дефектами **предмета**.

**1. W2 — периметровый исход получил дом там, куда вёл указатель**
(Д1108). `docs/architecture/contracts.md` §«Живые данные в браузер» несёт
теперь: неполный конверт и неразбираемое содержимое периметр пропускает, а
раздачу не останавливает; обязательных значений три, а не шесть (и что
подставляется вместо момента); цена пропуска — запись у клиента
неотличима от «событий не было». Клейм был истинен по построенному коду и
до правки — ложен был только адрес.

**2. W3 — нота операнда `resultProfit` спеки агрегатов приведена к своей
области определения** (Д1109). Равенство множеств пустоты с ценовым
результатом снято: у ценового результата есть своя причина — ложный
признак полноты графа на момент терминала, — и состояние «итог непуст,
ценовой результат пуст» достижимо. У ценового результата своя причина
сведена в перечень причин пустоты.

**3. Записи реестра снятых редакций снятие не завело, и довод назван**
(Д1110): снятое — ошибка факта о собственной величине, живое вхождение
одно и правится тем же ходом, а соседнее похожее (`fundingCost` в
`loss-streak-halt.json`) — действующая и **верная** редакция о другой
величине.

## Три вещи, которые дальше нельзя пере-решать

**1. Узел 1 закрыт целиком — конструкция, решения и остатки.** Д1087-Д1093
вошли в силу вместе с приземлением; ветвь полноты графа едет **признаком** в
форме события, а не пустотой слагаемого (Д1090). Возврат к пустоте —
регресс.

**2. Признак деления самоописания не переоткрывается** (Д1100): число с
механическим выводом живёт порождённым блоком, число без вывода не
пишется. Отчёты закрытых узлов и `history/` под правило не попадают.

**3. Долг прозы реестра гасится не отдельным ходом** — он уходит вместе со
снятием записей при закрытии шага; прогон падает на **новой** записи с
прозой и на строке долга без записи. Дом — `.claude/work/backlog.md`
§«Проза пришедшей редакции в записях реестра снятых редакций».

## Вход следующей сессии

**Единица — узел 2 `GAPS_CLOSE_4`: поверхность чтения — что шаг строит**
(находка A7, гейтящая). Обе причины ожидания у узлов 2-5 отработали: проход
по самоописанию закрыт, дом узла 2 (`docs/architecture/contracts.md`)
приземлён.

**У A7 своя половина дефицита `подтверждение` — продуктовый scope
поверхности чтения**, и её придётся выносить отдельно: вопрос в
`.claude/work/questions/open-questions.md` с вердиктом гейтинга, узел
паркуется по `.claude/rules/parking-address.md`, всё независимое от ответа
доводится, и только тогда цепочка отдаёт `holder_decision`.

**Порядок дальше:** узел 2 → узлы 3-5 (раскладка находок —
`phase-2-step-10-gaps-close-4.md`). Рамка узла: правило одной фразой →
популяция до первой правки → проект правок → мини-петля критики (потолок —
две верификации) → приземление → обход потребителей и свипы.

## Что висит

1. **Дельта `CODE` шага 10 — счёт стои́т порождённым блоком в хронике**, а
   не прозой.
2. **Компонентов шага 10 в `.claude/work/code-gate-ledger.json` нет** — их
   вносит простановка `CODE`.
3. **Позиции парковки узлов 5-6 `GAPS_CLOSE_3`** и парковка узла 1
   (исполнимость формы ценового результата на строках агрегатов —
   `.claude/work/backlog.md`).
4. **Долг прозы реестра** (`tools/retired-prose-debt.txt`).
5. **Цена сессий под правилом единицы не замерена** — замер по журналу
   первого прогона.
6. **События `auth` — производителя и темы нет**; экспорт метрик из семи
   прочих сервисов; таблицы `access_denials` нет ни у одного сервиса.
7. **Единый error-DTO у четырёх построенных сервисов.**
8. **Потерянное событие при неразбираемом содержимом** — DLT нет.
9. **Названные ограничения шага 7**, ревалидация активных определений при
   смене чисел риск-аппетита, `HOLD_RELEASED` не пишется, база риска на
   терминале не двигается.
10. **Внешний вызов внутри транзакции БД — двенадцать точек** у ядра.
11. **Донорские остатки в общей библиотеке** — снимаются удалением донора.
12. **Отчёты критики и верификации узлов 5-6 `GAPS_CLOSE_3` отдельными
    файлами не сохранены** (C3 прогона `DOCS_CHECK_4`, узел 5
    `GAPS_CLOSE_4`): генератор `passes:` на таких узлах числа заходов
    вывести не может.
13. **Прежние хвосты:** S1 Vault-хардненинг, рост таблицы отказов, состав
    точек чтения `audit-statistics` (шаг 12), api-формы периметра без дока.
14. **Пользователю:** переиндексировать проект в IDEA; обновить Project
    Knowledge (этот снапшот). **Стенд от дерева кода не отстал** — код не
    тронут.

## Проверки на момент снапшота

**Одиннадцать команд корпуса прогнаны на этом состоянии — все код 0.**
Рабочий бинарь — `py`; `python3` из PATH — заглушка, её код 49 читается как
«не измерялось». Гейт прогонялся **фоновой** командой: в 120 с он не
укладывается. Числа прогонов не пересказываются здесь: их печатает сам
прогон (`.claude/rules/self-description-form.md`).

**Реактор не прогонялся: дерево кода не тронуто.** Последний прогон — на
этом же состоянии кода: 1424 файла, 957 тестов, 0 дефектов (замер v163).

Долг адресных перекрёстий `docs/**` — 609 по замеру v158; замер не
повторялся.

**Живого прогона нет ни у одного компонента шагов 7-10.**

## Среда

Против v132 не изменилась; полное описание — там. Ловушки среды из
v156-v163 остаются в силе, включая рвущийся длинный heredoc (правки —
коротким python-скриптом из файла в корне репозитория, скрипт удаляется
после прогона) и **полный гейт инструментов, не укладывающийся в 120 с** —
прогонять фоновой командой. **Замечено этой сессией:** `echo "rc=$?"`
после конвейера с `tail` возвращает код `tail`, а не инструмента — коды
гейта снимаются обёрткой, вызывающей команду без конвейера.
