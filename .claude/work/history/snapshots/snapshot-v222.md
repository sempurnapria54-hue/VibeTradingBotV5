# Snapshot v222

**Дата:** 2026-09-10.

## На какой вопрос отвечает этот файл

Где мы сейчас. **Шаг 10 фазы 2 (аудит и статистика) в `CODE`: восьмой прогон
фокусов аппрув не выдал — гейтят Ф12 и Ф13.** Сменяет v221.

## Состояние

Фаза 1 — `FOLDED`. **Фаза 2 — `IN_PROGRESS`**: шаги 1-9 `DONE`, шаг 10 — **`CODE`**,
шаги 11-12 `HOLD`.

**Гейтящих выносов держателю нет.** Эта единица вопроса не выносила;
`.claude/work/questions/open-questions.md` ею не пополнялся.

**Дельта staged, не закоммичена.** Ветка `claude-audit`. **Дерево кода этой
единицей НЕ ТРОНУТО:** мутационная проба Ф12 восстановлена из индекса, и
`git status` по `services/` и `libs/` показывает то же состояние, что до неё.

**Режим — автономия до прод-рубежа.** Решения — Д250-Д1369 (эта единица
решений не принимала: прогон находки формулирует и не чинит).

## Что сделано после v221

**Единица сессии — восьмой прогон фокусов** (`conventions` / `performance` /
`disaster` / `security`) по дельте K19. Отчёт —
`.claude/work/progress/phase-2-step-10-code-focuses-8.md`; статус — в
`phase-2-step-10-chronicle.md` §Статус; позиции — в реестре компонентов у
`холды: …`, `trading-core: писатели событий, …` и
`audit-statistics: агрегатная выборка чтения`.

- **Дельта K19 по собственному предмету подтвердилась.** Оси A и B выведены
  из дерева заново; атомарность ребра проверена не объявлением метода, а
  обоими соседями внутри транзакции и пустой выдачей `@Transactional` у всех
  шести файлов тропы подъёма. Позиции всех семи прошлых прогонов
  перепроверены собственным чтением; ни одна не переоткрылась.
- **Ф12 гейтит: домен значения `HoldRaised.rung` не объявлен и не измерен.**
  Писатель кладёт `HoldRung` (`SOFT` / `HARD`), потребитель
  `audit-statistics` читает его SQL-литералом `= 'HARD'`; ни форма, ни дом
  привязки, ни нота операнда спеки не говорят, к какому перечню значение
  принадлежит, — а словом «ступень» корпус называет и лестничные
  `TRADE_BLOCKED` / `ENTRY_BLOCKED` / `HOLD`. **Предъявлено мутацией:**
  подмена значения литералом чужого перечня оставила все 495 проб модуля
  зелёными. Соседнее поле того же содержимого (`scope`) домен пинит.
- **Ф13 гейтит: снятая ЗАХОДОМ K19 редакция жива в javadoc
  `ManualHaltService`.** Свип рода `снятие` её не поймал: энфорсер не
  снимает продолжения javadoc, и в java между словами остаётся звёздочка.
  Реестр, прогнанный с нормализацией комментария, дал ещё два кодовых
  носителя — `BalanceRefreshTest` (верная проба со снятым держателем
  доводом) и донорский `StrategyEntity` (пропуск allow-листа).
- **Ф14 и Ф15 не гейтят.** Ф14 — второй порядок принятой цены отката
  (`interceptToError` уводит сделку в `ERROR` без причины); Ф15 — слепота
  энфорсера, припаркована в `.claude/work/backlog.md`.
- **Предъявлено:** набор проб модуля прогнан дважды (до мутации и после
  восстановления), оба раза 495 зелёных; гейт инструментов корпуса — 0.

## Три вещи, которые дальше нельзя пере-решать

**1. Перечень исполнителей не переоткрывается** (v195). Новый исполнитель
вносится **строкой** реестра, а не поводом пересобрать перечень.

**2. Реестр компонентов сквозной и новым шагом не пересобирается** (Д1184).
Находки в нём — прогона `DOCS_CHECK_34`; гейт срабатывает **на компоненте**,
когда тот начинается.

**3. Величину и её событие пишет ОДИН код ОДНОЙ транзакцией** (Д1362,
Д1366) — у ребра сделки и у ребра подъёма ступени. Затребователи ребро
**зовут**, а не подменяют; второго писателя не заводится. **Развилку «как
чинить Ф9/Ф10» K19 закрыл выбором (б), и восьмой прогон её не
переоткрывал** — Ф14 уточняет названную цену, а не требует её пересмотра.

## Вход следующей сессии

**Единица — кодовый заход, закрывающий Ф12 и Ф13.** Вход — §Ф12 и §Ф13
отчёта `phase-2-step-10-code-focuses-8.md`. **Посылка у находок разная, и
одной правкой они не закрываются:** Ф12 — о домене величины, не объявленном
ни на одном конце и не измеренном ничем; Ф13 — о носителе, до которого не
дошёл свип рода `снятие`.

- **У Ф12 развилка, и её решает владелец:** объявить домен у формы плюс
  запинить пробой производителя; вынести перечень в общий артефакт и читать
  на обоих концах; объявить величину спекой и мерить мутационной пробой.
- **У Ф13 три носителя правятся по-разному:** у `ManualHaltService` довод
  снимается, у `BalanceRefreshTest` заменяется на действующий (указатель —
  `docs/rules/risk-policy.md` §«Носитель базы и её писатели»), донорский
  `StrategyEntity` вносится в `allowed` своей записи реестра.

Дальше: **девятый** прогон фокусов (он и выдаёт аппрув) →
`SYNC_DOCS_FROM_CODE` (фокус `divergence`) → пост-хок концепт-гейт
(`.claude/processes/roadmap-step-execution.md` §6a). Каждый — своей единицей.

Обязательное перед завершением любой такой единицы:

1. **гейт инструментов корпуса** целиком;
2. **сборка — `bash tools/reactor-test.sh`**, если тронуто дерево кода;
   точечный прогон одного модуля — `mvn -o -am -pl services/<модуль> test` из
   корня. **`-am` обязателен**: общий артефакт в локальном репозитории не
   установлен. `mvn` на `PATH` нет — рабочая форма вызова стои́т в §Прогоны
   отчёта `.claude/work/progress/phase-2-step-10-code-pass-k19.md`;
3. **`py tools/code-gate-check.py`** — перед каждым кодовым заходом.

## Что висит

1. **Домен `HoldRaised.rung` не объявлен и не измерен** — Ф12, гейтит.
2. **Снятые редакции в javadoc трёх кодовых носителей** — Ф13, гейтит.
3. **Энфорсер снятых редакций слеп к javadoc-переносу** — Ф15,
   припаркована (`.claude/work/backlog.md`).
4. **Второй порядок цены отката ребра подъёма не назван** — Ф14, не гейтит.
5. **Ф11 открыта, аппрув не гейтит** — имя и javadoc константы гарда
   `CASCADE_SOURCE_STATUSES`.
6. **Транзакционная граница обоих рёбер прогоном не мерится** — держит
   объявление метода. Оживитель — первый живой прогон ядра на базе.
7. **Текст JPQL новых запросов не мерится ничем** — `@SpringBootTest` у
   модуля нет намеренно. Оживитель общий с п. 6.
8. **Разведение пар джоб ядра ПРОЧИТАНО, а не измерено.**
9. **Обращения каскада ступени выросли** — замера нет, тропа исключительная.
10. **Слепота считается состоявшимися проходами** — припарковано.
11. **Класс `HoldReleased` не построен.**
12. **Эпизод транша событием не различается.**
13. **Строки журнала, накопленные до хода, не чинятся.**
14. **Таблицы и писателя строки отказа у четырёх соседей нет** (`auth`,
    `trading-core`, `strategies`, `market-data`).
15. **Перехватчик `Exception` съедает отказ по правам у `bff` и `strategies`.**
16. **Предикат актора написан трижды** — единый носитель припаркован.
17. **Глубины у `access_denials` нет ни у одного сервиса.**
18. **Пул планировщика у `market-data` — один поток**; измерителя среди его
    джоб нет.
19. **Остаток пары «чистка × пересчёт»** у `audit-statistics`.
20. **Экспорт метрик у семи сервисов из восьми не построен**; экспорт
    **трейсов** не построен ни у кого.
21. **Подъём контекста не проверен** ни у `audit-statistics`, ни у
    `strategies`, ни у `bff`, ни у ядра в части `AsyncConfigurer`.
22. **Ни план запроса, ни порядок взятия строчных замков у модуля приёма не
    мерятся ничем.**
23. **Отбор ресурсов наблюдателем по метке `release` живым кластером не
    проверен.**
24. **Дубль запроса `earliestRecordedAt` у двух подключений остался** (Д1297).
25. **Позиции парковки узлов 5-6 `GAPS_CLOSE_3`** и парковка узла 1.
26. **Кандидат энфорсера окрестности** — бэклог.
27. **Стенд от дерева кода отстал:** образы `audit-statistics`, `strategies`,
    `trading-core` и `bff` с этими правками не собирались, роли и базы на
    стенде не заводились, тем и подписки на стенде нет.
28. **Форма дедупа «спросить и вставить»** — шесть точек трёх сервисов.
29. **Три долга инструментов:** проза реестра
    (`tools/retired-prose-debt.txt`), адресные перекрёстья `docs/**`, долг
    носителей команд (`tools/command-carrier-debt.txt`).
30. **§-адрес javadoc в несуществующий пассаж** — класс припаркован.
31. **Цена сессий под правилом единицы не замерена.**
32. **События `auth` — производителя и темы нет.**
33. **Единый error-DTO у четырёх построенных сервисов.**
34. **Потерянное событие при неразбираемом содержимом** — DLT нет.
35. **Названные ограничения шага 7**, ревалидация активных определений при
    смене чисел риск-аппетита, `HOLD_RELEASED` не пишется, база риска на
    терминале не двигается.
36. **Внешний вызов внутри транзакции БД — двенадцать точек** у ядра.
37. **Донорские остатки в общей библиотеке** — снимаются удалением донора.
38. **Отчёты критики и верификации узлов 5-6 `GAPS_CLOSE_3` отдельными
    файлами не сохранены.**
39. **Т-1…Т-3 седьмого прогона гейта `GAPS_CLOSE_4`; Ц-2…Ц-4 первого и
    Д-1…Д-4 второго прогона гейта `GAPS_CLOSE_5`** — все о тексте о работе;
    уезжают с отчётами в `history/` при закрытии шага.
40. **Прежние хвосты:** S1 Vault-хардненинг, api-формы периметра без дока.
41. **Пользователю:** обновить Project Knowledge (этот снапшот). Новых
    Maven-модулей эта единица не заводила — переиндексация IDEA не нужна.

## Проверки на момент снапшота

**Гейт инструментов корпуса прогнан** на состоянии после всех правок, включая
этот файл, — весь перечень, ненулевых кодов нет, кода 2 нет. Числа прогонов
здесь не пересказываются: их печатает сам прогон
(`.claude/rules/self-description-form.md`).

**Реактор этой единицей не прогонялся и прогоняться не обязан:** дерево кода
она оставила в том состоянии, в каком застала. Набор проб модуля
`trading-core` прогнан ею дважды — до мутации Ф12 и после восстановления,
оба раза зелено.

## Среда

Против v132 не изменилась; полное описание — там, перечень ловушек — в v206
(`.claude/work/history/snapshots/snapshot-v206.md` §Среда) и он остаётся в
силе целиком. Ловушки v207-v221 тоже в силе, включая: `python3` в этой среде
разрешается в заглушку Windows Store и падает кодом 49 — рабочая форма `py`.
**Ловушка этой сессии:** heredoc питона внутри `bash -c` уходит в ту же
заглушку — скрипт пишется во временный файл и запускается через `py <файл>`
либо подаётся как `py - <<'EOF'`.
