# Snapshot v329

**Дата:** 2026-09-18.

## На какой вопрос отвечает этот файл

Где мы сейчас.

## Состояние

Сменяет v328. Фаза 1 — `FOLDED`. **Фаза 2 — `IN_PROGRESS`**: шаги 1-11
`DONE`, **шаг 12 — `CODE·2/3`**, шаг 13 `HOLD`. Прод-рубеж — `HOLD`.

**Машина шага** — `CONCEPT → CONCEPT_REVIEW → CODE → DOCS → REVIEW → DONE`
(`.claude/processes/roadmap-step-execution.md`). `CODE` тестового шага идёт
тремя фиксированными под-шагами: тест-кейсы → адверсариальное ревью кейсов →
код тестов. **Идёт под-шаг 2.**

**Петли ревью кейсов нет — есть критерий и потолок в три круга** (решение
держателя 2026-09-17, `.claude/decisions/case-review-approval-criterion.md`;
дом формулировки — `.claude/skills/test-review.md` §«Критерий апрува документа
кейсов»). Апрув блокирует **только невалидность кейса**.

**Уровень 1 закрыт целиком** — восемь из восьми (заходы 28-35). **Уровень 2:
апрувлены все восемь написанных документов** (заходы 37-44); **перечень
уровня 2 при этом содержит четырнадцать строк** — шесть нижних введены
правкой 2026-09-17, документов у них нет, и под-шаг 1 по ним открывается
заново. **Уровень 3: апрувлены четыре предмета из пяти** —
`e2e-strategy-to-deal` (45), `e2e-exit-and-close` (46),
`e2e-safety-teardown` (47) и `e2e-perimeter-read` (48).

**Дельта — в рабочем дереве, не закоммичена.** Ветка `claude-audit`.
**Дерево кода эта сессия не меняла** — оно читалось; реактор не прогонялся и
не был нужен.

**Режим — автономия до прод-рубежа.** Решения — Д250-Д1794.

## Что сделано после v328

Одна единица: **апрув кейсов `e2e-perimeter-read`** (заход 48) —
`.claude/work/progress/phase-2-step-12-approve-test-e2e-perimeter-read.md`.
Состав находок там; здесь — только то, что меняет картину для следующей
сессии.

- **Класс находок ушёл из клеток ожиданий в РАЗДЕЛ ПРЕДУСЛОВИЙ.** У трёх
  первых троп отказывал предмет: звена нет, операнд собран не тем. Здесь
  предмет исправен на всех отрезках, а неисполнима **оснастка** — и разбор
  ожиданий её не видит, потому что каждое ожидание поодиночке верно.
  Две крупнейшие находки из девяти — оттуда.
- **Порядок, исключающий сам себя.** Предусловия требовали одновременно
  «счёт тенанта `T1` зарегистрирован» и «тенанта `T1` заводит первый ход
  тропы», тогда как регистрация счёта на несуществующем тенанте отвергается
  кодом и FK. Введён порядок из трёх тактов: заведение тенанта → пролог под
  ним → остальные кейсы.
- **Адрес соседа у периметра один на всех.** `perimeter.owner-url-template`
  со значением `http://{owner}:8080` даёт общий порт и хост по имени
  единицы; набор, где сторона есть **процесс** на хосте прогона, им не
  адресуется — не собирается ни одна группа тропы, включая синхронные.
  Позиция — секция бэклога, владелец `code-writer`; она **гейтит под-шаг 3**
  по этой тропе.
- **Класс «молчащий сосед» воспроизвёлся ТРЕТЬИМ документом подряд:** все
  семь эмитентов факта у ядра строят контекст сделки, то есть читают
  раскладку у `market-data`. Отрицание о стороне целиком ложно; на уровне 3
  оно привязывается к **отрезку** тропы.
- **Две новые секции бэклога**, обе владельца `code-writer`: адресация
  владельцев у периметра и `403` владельца членств, доезжающий браузеру
  внутренним отказом. Кейсов 47, новых заход не завёл.

## Вход следующей сессии

**Следующая единица — последний документ кейсов уровня 3** (`smoke-live`;
порядок дома — `.claude/decisions/test-contour-design-pass.md` §«Перечень
предметов и порядок работы»). После него — шесть новых документов уровня 2 и
их ревью.

**Что форма захода уже включает** (не выводить заново) — всё, что перечисляли
v325-v328 §«Вход следующей сессии», остаётся в силе дословно
(`.claude/work/history/snapshots/snapshot-v325.md`, `…/snapshot-v326.md`,
`…/snapshot-v327.md`, `…/snapshot-v328.md`). **Заход 48 добавляет три оси, и
они несущие:**

- **предусловия проверяются на ИСПОЛНИМОСТЬ названным средством**, а не на
  полноту: по каждой строке пройти код, которым она ставится, и спросить, что
  он требует на входе. Раздел предусловий читается как проза и разбором
  ожиданий не покрывается вовсе;
- **у оснастки свой предмет, и мерится он кодом СЕРВИСА, а не кодом тропы**:
  форма адреса соседа, форма предъявления, форма тика — свойства сервиса, на
  которые кейс уровня 3 опирается молча. Замер — сверка каждой названной оси
  конфигурации с тем, сколько **значений** она допускает;
- **отрицание «сосед молчит» привязывается к ОТРЕЗКУ тропы, а не к стороне**:
  сторона, молчащая на чтении, говорит на ходе, порождающем факт.

**Классы находок, встреченные у восьми предметов уровня 2 и у четырёх
предметов уровня 3**, перечислены в v325-v328 §«Вход следующей сессии» и
здесь не дублируются.

## Что держателю решать

Против v328 не изменилось:

1. **Источник цены `MARK_PRICE` / `INDEX_PRICE`** — реджект создания либо
   снятие ограничения.
2. **Читатель `exitOutcome`.**
3. **Ключи demo-окружения OKX и распечатанный Vault** — обещаны к под-шагу 3
   `CODE`; без них не прогоняется ни один кейс мишени `-D` и весь
   `smoke-live`.

Плюс прежние ожидания (Vault-хардненинг, миграция на Jackson 3, область
тестового токена) — печатает `py tools/backlog-check.py` строкой `держатель:`.

## Что висит

Всё, что живёт секциями бэклога, печатает `py tools/backlog-check.py`; в
снапшоте не дублируется. Вне бэклога список не изменился: пункты 1-14
§«Что висит» снапшота v302 остаются в силе дословно
(`.claude/work/history/snapshots/snapshot-v302.md`). Существенное: стенд
отстал от дерева кода; ни одна пара производитель-потребитель не встречалась
на живом брокере; вход снаружи в окружение не построен; обязанности заведения
каталога `tests/` принадлежат под-шагу 3; архивов Testcontainers и WireMock в
локальном репозитории нет.

**Новое существенное:** **под-шаг 3 по тропе периметра гейтится правкой
кода** — периметр не умеет адресовать владельцев порознь, и без этой оси
сквозной набор по нему не собирается ни одной группой. Прежнее новое v328
(safety-контур разорван на ручном входе; отчёт счёт-широкой реакции не несёт
ни экспозиции, ни остатка) в силе.

Три больших свипа с измеренным объёмом остаются открытыми: слой опровержения
в `docs/**`, свод адресных перекрёстий, долг §-адресов
(`tools/anchor-debt.txt`) и процедура синка в доках. Каждый — своя единица.

**Пользователю:** обновить Project Knowledge (этот снапшот); прочитать блоки
дайджеста Д1564-Д1794 — право вето постфактум.

## Ловушки

**Ловушки** v206-v328 остаются в силе. Подтверждено и дополнено этой сессией:

- **Heredoc с одинарными кавычками внутри рвёт команду `Bash`**: длинные
  русские тексты пишутся инструментом `Write` (файл целиком) либо скриптом
  `py <файл>`. Правка длинного документа — скриптом с проверкой
  единственности и наличия каждой старой строки: молчаливая замена «не
  найдено» даёт зелёный прогон на непроведённой правке. **Подтверждено на
  промахе одного союза** («и момент этот» против «а момент этот») — скрипт
  отказал, файл остался нетронутым, потому что запись идёт последней
  строкой, а не по ходу замен.
- **Мягкий перенос (U+00AD) заезжает в текст незаметно** и ломает поиск по
  слову: после письма длинных абзацев — проверка `t.count('\\u00ad')`.
- **Выдача `Bash` больше ~30 KB уходит в файл вместо терминала**: длинные
  файлы читаются окнами по 100-150 строк (`sed -n 'a,bp'`). Хроника шага —
  больше 200 KB, и целиком она не читается ни разу.
- **Рабочие скрипты кладутся в `.claude/work/progress/` и удаляются тем же
  заходом**: корневой `target/` роняет `py tools/retired-check.py` кодом 2.
- Прочие ловушки — снапшоты v283, v285-v328 §Ловушки; они не переписываются.

Обязательное перед завершением любой единицы:

1. **гейт инструментов корпуса** целиком — перечень в
   `.claude/skills/update-roadmap-progress.md` §«Гейт инструментов корпуса»,
   `backlog-check.py` с подменой статуса, если статус проставляется;
2. **реактор — `bash tools/reactor-test.sh`**, если тронуто дерево кода.

## Проверки на момент снапшота

**Гейт инструментов корпуса прогнан** целиком — все пятнадцать команд
вернули 0. Подмены статуса не требовалось: статус шага не менялся. **Реактор
не прогонялся и не требовался** — дельта дерева кода не касается
(`git diff --name-only <метка> -- services/ web/` пуст).

## Среда

Против v132 не изменилась. Дом фактов среды — команды сборки, пути JDK и
Maven, лаунчер Python, Vault и demo-ключ —
`.claude/skills/environment-commands.md`. Сборка: JDK `corretto-25.0.3`,
maven из дистрибутива IDEA (`REACTOR_MVN`), в `PATH` его нет. Состав
локального репозитория по тестовым библиотекам — §«Что висит» снапшота v302,
п. 6.
