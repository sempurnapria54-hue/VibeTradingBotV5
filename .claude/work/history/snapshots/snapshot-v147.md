# Snapshot v147

**Дата:** 2026-09-06.

## На какой вопрос отвечает этот файл

Где мы сейчас. **Шаг 8 фазы 2 — `DONE`; шаг 9 (BFF) открыт и стои́т на
`GAPS_CLOSE_1`: первый прогон дал 9 находок, все закрыты, конструкция
периметра записана. Вход следующего захода — проверочный
`DOCS_CHECK_2`.** Сменяет v146.

## Состояние

Фаза 1 — `FOLDED`. **Фаза 2 — `IN_PROGRESS`**: шаги 1-8 `DONE`, шаг 9 —
`GAPS_CLOSE_1`, шаги 10-12 `HOLD`. Фазы 3-6, 7-8 и прод-рубеж — `HOLD`.

**Дельта в working tree, не в истории.** Ветка `claude-audit`; всё
описанное — staged, не закоммичено (коммит руками).

**Режим — автономия до прод-рубежа.** Решения — Д250-Д962 (итерации
34-58). **Открытых выносов держателю нет.**

## Что закрыто этой сессией

**Шаг 8 «Стратегии» доведён от `CODE` до `DONE`.** Артефакты —
`.claude/work/history/2026-09-06-phase-2-step-8-strategies/`.

| Под-шаг | Исход |
|---|---|
| остаток `CODE` | закрыты обе названные позиции: тест поверхности контура и фикстура эталона у владельца (корневой дубль эталона исчез — `D-P6`/`E-П3` сняты) |
| аппрув `CODE` | четыре фокуса, **8 находок**: 7 закрыто, 1 припаркована с домом и условием возврата |
| `SYNC_DOCS_FROM_CODE` | **9 расхождений** (5 add, 4 change, 0 remove), все реконсилированы |
| пост-хок концепт-гейт (`.claude/processes/roadmap-step-execution.md` §6a) | ряд **1 → 2 → 0**; закрыт разрыв на тропе снятия риска |
| гейт `DONE` | одиннадцать команд корпуса — 0; `bash tools/reactor-test.sh` — 1377 файлов, **915 тестов**, 0 дефектов |

## Три вещи, которые стои́т помнить из этого захода

**Зелёный реактор ничего не измерял.** `mvn -o test` отчитался
`BUILD SUCCESS` и 908 тестов, не скомпилировав **ни одного файла**: во
всех модулях стояло «Nothing to compile». Донор был собран против прежней
редакции общей библиотеки, а шаг сменил корень `Strategy` — настоящая
компиляция дала семь ошибок. Закрыто дважды: донорское поле возвращено в
модель как названное, а гейт `src/` переведён на
`bash tools/reactor-test.sh`, который отказывается объявлять зелёное при
«Nothing to compile». **Прежняя команда гейтом больше не является**
(`.claude/skills/update-roadmap-progress.md`).

**Клейм «единый error-DTO» держался на половине троп.** Отказ доступа,
неразбираемое тело и непредъявленный заголовок контекста отвечали **пустым
телом**. Видно это только прогоном-пробой: при чтении кода все три
выглядят исправными. У четырёх соседних сервисов тот же дефект — назван и
припаркован.

**Клейм четырёх доков о писателе не подтверждался кодом.** Удаление
определения обязано уводить живые сделки в координированный выход; в коде
такой тропы не было вовсе. До шага 8 статус копии не двигал никто, и
недостающий читатель ничего не стоил — шаг завёл писателя и сделал разрыв
достижимым. Закрыто **кодом**, а не вычёркиванием клейма: правило «после
аппрува ведёт код» адресует решения, **принятые** в коде.

## Шаг 9 открыт: что уже сделано и что дальше

`TOOLING` пройден без артефактов; `DOCS_CHECK_1` дал **9 находок (8
гейтящих)**, `GAPS_CLOSE_1` закрыл все девять, припарковав 0. Кодовых
правок ноль — весь прогон о доках. Хроника и отчёты —
`.claude/work/progress/phase-2-step-9-*`.

**Особенность шага, названная сразу: источника порта нет.** У донора
периметра не существует, и `bff` — первая единица фазы 2, которая
строится не портом, а по одной концепции. Отсюда и характер находок:
восемь из девяти — «структуры нет вовсе», а не расхождение.

**Конструкция периметра записана — семь выборов** (Д955-Д962,
`.claude/work/decision-digest.md`): проверку права держит домен, а не
периметр; периметр **проксирует**, экранных контрактов не заводит;
позиция чтения потока — идентичность события, не смещение темы; форма на
проводе к браузеру — api-модель периметра; у каждой реплики своя группа
потребителя, дедуплицирует клиент; кэш членств эфемерен со сроком 60 с;
след отказа на периметре — лог и метрика.

**Вход следующего захода — `DOCS_CHECK_2`**, проверочный прогон по
следствиям: восемь узлов правили пять носителей, три из них сквозные
(`contracts.md`, `api-access-policy.md`, `services.md`).

**Два узла шага названы заранее:** он первый машинный потребитель формата
отказа всех поверхностей и первый потребитель перечней — то есть оживляет
обе позиции `.claude/work/backlog.md`: §«Единый error-DTO у поверхностей
соседних сервисов» и §«Постраничный обход перечня определений».

## Что висит

1. **Единый error-DTO у четырёх построенных сервисов** — `auth`,
   `connector-okx`, `market-data`, `trading-core`: отказы контейнера и
   отказ доступа отвечают не тем форматом (`.claude/work/backlog.md`,
   одноимённая секция).
2. **Потерянное событие при неразбираемом содержимом** — DLT и
   наблюдаемости нет; оживит первый живой прогон реле.
3. **Постраничный обход перечня определений** — сегодня окно без обхода.
4. **Названные ограничения шага 7** — `.claude/work/backlog.md`
   §«Названные ограничения кодирования шага 7».
5. **Ревалидация активных определений при смене чисел риск-аппетита.**
6. **`HOLD_RELEASED` не пишется**; **база риска на терминале не
   двигается** — писатели объявлены, ходы приезжают с потребителями.
7. **Внешний вызов внутри транзакции БД — двенадцать точек** у ядра
   (§«Перф-форвард»). Шаги 7 и 8 тринадцатой не добавили.
8. **Место истины схемы у четырёх чужих таблиц** — `memberships` у
   `auth`, три у `market-data`.
9. **Донорские остатки в общей библиотеке** — поля агрегата сделки,
   значения расколотых перечней и **числовой ключ инструмента у
   `Strategy`** (заведён этой сессией); все три снимаются удалением
   донора.
10. **Парковки стенда:** срезы стакана, экспорт трейсов, том
    Elasticsearch, политики сжатия гипертаблиц.
11. **Парковки прежних шагов:** предикат свежести шага; сброс кэша ключей
    по ротации; осиротевшие секреты; WS-слушатель фактов исполнения;
    контекстный тест сервиса; подпись образов; транспорт внутри кластера;
    размеры томов; ликвидации; ориентир по расходам на хранение.
12. **Прежние хвосты:** S1 Vault-хардненинг, рост таблицы отказов, долг
    адресных перекрёстий `docs/**`.
13. **Пользователю:** переиндексировать проект в IDEA; обновить Project
    Knowledge (этот снапшот, дайджест итераций 35-58). **Стенд отстал от
    дерева кода:** восьмой сервис (`strategies`) со своей базой, ролью и
    темой, у ядра две новые миграции (`V7`, `V8`) и потребитель Kafka.
    Роль `strategies` в кластере Postgres требует своего секрета
    (`postgres-role-strategies`) — команда постановки в
    `.claude/skills/local-stand.md`.

## Проверки на момент снапшота

Одиннадцать команд корпуса (десять плюс `code-gate-check`) — код 0.
Реактор — `bash tools/reactor-test.sh`: **1377 файлов скомпилировано, 915
тестов, дефектов 0** (`strategy-engine` 48, `auth` 10, `connector-okx` 27,
`market-data` 41, **`strategies` 18**, `trading-core` 446, донор 325).
Компиляция с показом deprecation — 1185 файлов, 0 предупреждений.

Прогон спек — 24 спецификации, 594 примера; мутационный замер — 398
величин, переживают нейтрализацию 0.

**Живого прогона нет ни у одного компонента шагов 7 и 8.** Первый — тик
`DealOrchestratorJob`, `EntryScannerJob` и реле обоих сервисов на стенде
после ближайшего развёртывания образов.

## Среда

Против v132 не изменилась. Стенд поднят и работает. Сборка идёт назначенным
JDK и Maven (`JAVA_HOME=C:/Users/RomanKrd/.jdks/corretto-25.0.3`, maven3 из
поставки IDEA — **путь `C:\Program Files\JetBrains\IntelliJ IDEA 2026.1\
plugins\maven\lib\maven3\bin\mvn.cmd`**; системный `JAVA_HOME` указывает на
JDK 11 и проект им не собирается). Оба пути — умолчания
`tools/reactor-test.sh`, переопределяются `REACTOR_JDK` / `REACTOR_MVN`.
Python корпусных прогонов —
`C:/Users/RomanKrd/AppData/Local/Programs/Python/Python311/python.exe`
(`python3` в PATH — заглушка магазина приложений).

**Офлайновая сборка зелёная** — докачивать в этой сессии ничего не
потребовалось: `spring-security-test` уже лежал в локальном репозитории.
