# Snapshot v121

**Дата:** 2026-09-04.

## На какой вопрос отвечает этот файл

Где мы сейчас. **Роадмап пересобран под сервисную платформу; фаза 1
растворена; дизайн-проход архитектуры записан.** Сменяет v120.

## Состояние

Фаза 1 — `FOLDED` (шаги 1-9 `DONE`, 10-11 сняты). Фазы 2-8 и прод-рубеж —
`HOLD`. **Следующий ход — шаг 1 фазы 2: переезд в монорепозиторий.**
Ветка `claude-audit`; дельта этой задачи — в working tree (staged), не
закоммичена.

**Режим — автономия**; горизонт «до конца фазы 1» снят, предложен
«до прод-рубежа» (ARCH-Q3). Решения — Д250-Д576 в дайджесте.
**Открытых выносов держателю четыре:** ARCH-Q1 облако, ARCH-Q2 порядок
фаз 3-6, ARCH-Q3 горизонт автономии, ARCH-Q4 состав ролей тенанта
(`.claude/work/questions/open-questions.md`). Ни один не гейтит фазу 2.

## Что сделано в этой задаче

- **Целевая конструкция** — новый тип `docs/architecture/` (восемь
  носителей: сервисы, тенант и биржа, сигнал/стратегия/аллокатор,
  владение данными, контракты, сбор данных, зарезервированные места,
  платформа). Выводы принципов — `docs/concept.md` раздел «Сервисная
  форма».
- **Развилки с альтернативами** —
  `.claude/decisions/service-architecture-design-pass.md` (Р1-Р16),
  `roadmap-rebuild-for-service-platform.md`,
  `corpus-relocation-by-service.md` (карта кластеров → сервис, порядок
  переезда), `architecture-docs-type.md`.
- **Роадмап** — `roadmap.md` (фазы 1-8 + прод-рубеж), `phase-2.md` …
  `phase-6.md`, `prod-gate.md`; `phase-1.md` растворён.
- **Переякорено** «до конца фазы 1» → «до прод-рубежа»: pre-launch-правило,
  четыре встречных якоря бэклога, автономия, второй субъект (шаг 1
  фазы 5), Raw-JDBC, уровни «биржа/портфель» (фаза 4). Старые номера фаз
  в бэклоге — по таблице соответствия в его шапке.
- **Радар** — семь записей `assess` под платформу.
- **Парковки** — `backlog.md` §«Парковки дизайн-прохода сервисной
  архитектуры».

## Что ратифицировано и не пере-решается

Всё из v104-v120, плюс решения держателя этой задачи: монорепозиторий с
сервисами, рубеж до прода и его состав, тенант как единица владения,
Bybit как проверка модели, аллокатор в рубеже без назначенных весов,
сбор по невосполнимости, советник/детекторы/модель/биллинг — вне рубежа с
зарезервированными местами. Из решений владельца (Д549-Д576) —
ключевые: риск-гейт и аллокатор внутри ядра; коннектор без базы и без
outbox; `Exchange` → площадка + тенант + счёт; сигнал — запись с исходом
всегда; движок — общая библиотека; TimescaleDB; Keycloak + свой `auth`;
Argo CD; порт первым.

## Что висит

1. **Четыре `подтверждение`** — ARCH-Q1…Q4 (выше).
2. **Отклонённые ориентиры чата** — outbox коннектора и «аудит без
   входящего REST» (уточнено до «на запись»); держатель вправе вернуть.
3. **Прежние хвосты** — S1 Vault, контракт броска диспетчера,
   ограничения контура доступа (оживит шаг 4 фазы 2), рост таблицы
   отказов, A12, хвост шага 8, долг адресных перекрёстий `docs/**`,
   провизорные числа калибровки — свои якоря в бэклоге; горизонты со
   старыми номерами фаз — по соответствию.
4. **Правка `structure.md` под вторую ось** — намеренно отложена до шага
   переезда (`corpus-relocation-by-service.md`).
5. **Пользователю — обновить Project Knowledge:** этот снапшот, дайджест
   (Д250-Д576), `structure-digest.md`.

## Что дальше

**Шаг 1 фазы 2 — переезд**: скелет монорепозитория, `.claude/` целиком,
сквозное `docs/`, сервисное по карте, правка `structure.md`, заморозка
донора. Затем шаг 2 — платформа (кластер вне облака до ARCH-Q1).

## Среда

Изменений с v120 нет. Дом фактов —
`.claude/tests/source-api/okx/code-preconditions.md`. **Maven в `PATH`
отсутствует**; **`JAVA_HOME` среды указывает на JDK 11**, рабочая форма —
`JAVA_HOME=$HOME/.jdks/corretto-25.0.3`; лаунчер Python — `py -3`. Принципал
профиля `test` — в Vault (`secret/tradingbot/api-access-test`).
