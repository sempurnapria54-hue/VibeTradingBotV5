# Снапшот v408

**Дата:** 2026-09-22.

## На какой вопрос отвечает этот файл

Где мы сейчас.

## Состояние

Сменяет v407. Фаза 1 — `FOLDED`. **Фаза 2 — `IN_PROGRESS`**: шаги 1-11
`DONE`, **шаг 12 — `CODE·3/3`**, шаг 13 `HOLD`. Прод-рубеж — `HOLD`.

**Машина шага** — `CONCEPT → CONCEPT_REVIEW → CODE → DOCS → REVIEW → DONE`
(`.claude/processes/roadmap-step-execution.md`). `CODE` тестового шага идёт
тремя фиксированными под-шагами: тест-кейсы → адверсариальное ревью кейсов →
код тестов. **Под-шаги 1 и 2 закрыты целиком**; **идёт под-шаг 3 — код
тестов.** **Уровень 2 написан целиком (14 из 14); на уровне 1 ящик есть у
СЕМИ сервисов из восьми**, и **два из них написаны целиком** — `strategies`
(10 групп, 81 прогон) и `audit` (11 групп, 126 прогонов); у `trading-core` не
пройден хвост с чужими блокерами, у `statistics` написаны **двенадцать групп
из тринадцати** (159 прогонов плюс три клетки метки `debt`).

**Дельта — в рабочем дереве, не закоммичена.** Ветка `claude-audit`.
**Дерева кода эта сессия НЕ меняла** — всё написанное лежит в
`services/statistics/src/test`; мутации трёх контрольных осей сняты, реактор
прогнан.

**Режим — автономия до прод-рубежа.** Решения — Д250-Д1989.

## Что сделано после v407

Одна единица: **заход 127 — группа `B12` ящика `statistics`** (конфигурация и
схема как вход, `B12.1`-`B12.11`). Подробности — хроника шага
`.claude/work/progress/phase-2-step-12-chronicle.md` §«Пятьдесят девятый заход
под-шага 3 (127) написал группу `B12` ящика `statistics`»; здесь только то,
что меняет картину.

- **Классов у группы ДЕВЯТЬ на десять прогоняемых клеток**, и режет её ось
  контекста: вход этой группы есть само положение осей конфигурации.
- **Две клетки живут вне `@SpringBootTest`** (Д1985): их ожидание — НЕподъём
  контекста, и подъём процесса производит сам кейс.
- **Независимость смещений двух групп взята на штатном контексте** (Д1986):
  вторая сторона — второе имя группы на брокере, а не второй процесс.
- **«Срок хранения темы не живёт в конфигурации» предъявлено сменой срока у
  БРОКЕРА** (Д1987), а не отрицанием существования ключа.
- **Четыре из шести величин кейса `B12.7` отданы существующим носителям**
  (Д1988); контекст живых тактов закрывается вместе с классом (Д1989).
- **Класс-мап документа кейсов дополнен пропущенной группой `B11`** — заход
  126 его не трогал.
- **Контрольный прогон — три оси, пять прогонов; две предсказаны точно,
  третья уронила на клетку больше.** **Находок владельцам — ноль**, первый же
  прогон группы был зелёным.

## Вход следующей сессии

**Следующая единица — последняя группа ящика `statistics`**: `B13`
(отсутствие выходов, 9 клеток); на ней ящик закрывается целиком. Единица
под-шага 3 — ящик сервиса целиком; приземляемая точка внутри неё — граница
документа кейсов (решение держателя 2026-09-21,
`.claude/decisions/test-step-pace-measures.md`).

Что единица застаёт:

- **обвязка ящика `statistics` полна** — перечень входов и наблюдателей
  v399 §«Вход следующей сессии», v403-v407 остаётся в силе дословно, и к нему
  прибавились подъём процесса кейсом (`StatisticsSubstrate#launchArguments`),
  читатель темы чужой группой (`Wire#readAllAsGroup`), назначение срока
  хранения теме (`Wire#setRetention`), заготовка строки агрегата с пустыми
  ключами (`Aggregates#dealRowWithEmptyKeys`), накатанные миграции и внешние
  ключи у наблюдателя строк (`Rows#appliedMigrations`, `#foreignKeyNames`);
- **форма кода уровня 1** задана заходами 83-127, **уровня 2** — заходами
  69-82; всё, что перечисляли v325-v407 §«Вход следующей сессии», остаётся в
  силе дословно (`.claude/work/history/snapshots/snapshot-v325.md` …
  `…/snapshot-v407.md`);
- **контрольный прогон на нейтрализованной сборке — часть единицы** (Д1830),
  ограничен тремя осями по сборкам захода (Д1951); предсказание называет
  клетки поимённо до прогона и перебирает клетки соседних классов;
- **ревизия существующего набора проб предмета** — свой предмет перечня
  (Д1828);
- **ящика нет у одного сервиса из восьми** — `bff`; сквозных прогонов нет ни
  у одного;
- **хвост ящика `trading-core`** — клетки, ждущие живой экспозиции (`F-13`),
  терминала сделки с числом и трёх своих названных причин; их оживители
  сегодня не сработали;
- **колонка «Факт» у двадцати семи клеток групп `B1`-`B3` ящика
  `strategies` пуста** — своя секция бэклога;
- **хвост апрувленных документов** — негейтящие находки `Ф-3`-`Ф-5` по
  `jsonb-overlay-roundtrip`.

## Что держателю решать

Против v407 не изменилось:

1. **Источник цены `MARK_PRICE` / `INDEX_PRICE`** — реджект создания либо
   снятие ограничения (кейс `U22.8` предмета
   `strategy-definition-validation`, красный по построению).
2. **Читатель `exitOutcome`.**
3. **Ключи demo-окружения OKX и распечатанный Vault** — без них не
   прогоняется ни один кейс мишени `-D` и весь `smoke-live`.

Плюс прежние ожидания (Vault-хардненинг, миграция на Jackson 3, область
тестового токена) — печатает `py tools/backlog-check.py` строкой `держатель:`.

## Что висит

Всё, что живёт секциями бэклога, печатает `py tools/backlog-check.py`; в
снапшоте не дублируется. Вне бэклога список не изменился: пункты 1-14
§«Что висит» снапшота v302 остаются в силе дословно
(`.claude/work/history/snapshots/snapshot-v302.md`), **кроме п. 6** (закрыт
заходом 68). Существенное: стенд отстал от дерева кода — и **пересоздание его
баз обязательно вдвойне: прежней схемы `statistics` не существовало вовсе**;
вход снаружи в окружение не построен; **пары производитель-потребитель из ДВУХ
сервисов** на живом брокере по-прежнему не встречалось.

Три больших свипа с измеренным объёмом остаются открытыми: слой опровержения
в `docs/**`, свод адресных перекрёстий, долг §-адресов
(`tools/anchor-debt.txt`) и процедура синка в доках. Каждый — своя единица.

**Пользователю:** обновить Project Knowledge (этот снапшот); прочитать блоки
дайджеста Д1564-Д1989 — право вето постфактум.

## Ловушки

**Ловушки** v206-v407 остаются в силе. Подтверждено и добавлено этой сессией:

- **Ожидание «контекст не поднимается» требует отказаться от каркаса
  теста.** Поднятый контекст есть предусловие всякого кейса ящика, поэтому
  клетка о ненастроенной оси поднимает процесс сама — и пусто у неё ровно
  одно, прочие оси настроены: иначе отказ сошёлся бы по чужому поводу.
- **Отрицание «величины нет в конфигурации» положительной формой сильнее.**
  Ключ, которого нет, невидим ни одному ассерту; смена той же величины в
  чужом хозяйстве (у брокера) двигает наблюдаемое — и это предъявляет, что
  берётся она оттуда.
- **Ось нейтрализации, трогающая наблюдаемое клетки, роняет её независимо от
  предмета.** Позиция чтения группы есть операнд и клетки о порядке
  «следствие, затем смещение»: лишняя краснота предсказана не была, и
  различение «дефект клетки / широкий радиус оси» стои́т на двух прогонах —
  клетка в одиночку на действительном состоянии и она же в одиночку под
  мутацией.
- **Контекст с живой джобой, бьющейся каждую секунду, закрывают
  `@DirtiesContext`.** Кэш каркаса контекстов не закрывает, а проход,
  читающий таблицы, которые соседние клетки у базы отнимают, становится
  чужим шумом и чужой нагрузкой.
- Прочие ловушки — снапшоты v283, v285-v407 §Ловушки; они не переписываются.

**Решения держателя 2026-09-21 по темпу шага
(`.claude/decisions/test-step-pace-measures.md`) действуют**: единица
под-шага 3 — ящик сервиса целиком (уровень 1) либо все юниты одной библиотеки
или модуля (уровень 2), группа совпадает со сборкой `mvn`; контрольный прогон
— в пределах сборки группы; перепроверка после правки по браковке —
субагентом с чистым контекстом в сессии автора.

Обязательное перед завершением любой единицы:

1. **гейт инструментов корпуса** целиком, одним прогоном на финальном
   состоянии — перечень в
   `.claude/skills/update-roadmap-progress.md` §«Гейт инструментов корпуса»
   (шестнадцать команд), `backlog-check.py` с подменой статуса, если статус
   проставляется;
2. **реактор — `bash tools/reactor-test.sh`**, если тронуто дерево кода.

## Проверки на момент снапшота

Числа прогонов и их исход — §«Проверки» хроники шага; на момент снапшота
гейт инструментов корпуса вернул 0 по всем шестнадцати командам, реактор
зелен. Пакет ящика `statistics` — 159 тестов, все зелены. **Прогон с
включённой меткой `debt` эта сессия не проводила** — группа `B12` клеток
метки не заводит; три её клетки красны по построению и в умолчание прогона не
входят.

## Среда

Против v132 не изменилась, кроме состава локального репозитория по тестовым
библиотекам и **образов субстрата ящика** (строки «Артефакты тестового
контура» и «Образы субстрата ящика»), а также тестовых областей одиннадцати
модулей (`test.excluded.groups`: счёт печатает
`grep -rl 'test.excluded.groups' --include=pom.xml .`). Дом фактов среды —
команды сборки, пути JDK и Maven, лаунчер Python, Vault, demo-ключ и
прайминг — `.claude/skills/environment-commands.md`.
