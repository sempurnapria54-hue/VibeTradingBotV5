# Snapshot v219

**Дата:** 2026-09-10.

## На какой вопрос отвечает этот файл

Где мы сейчас. **Шаг 10 фазы 2 (аудит и статистика) в `CODE`: находки Ф7 и Ф8
шестого прогона фокусов закрыты, аппрув по-прежнему не выдан.** Сменяет v218.

## Состояние

Фаза 1 — `FOLDED`. **Фаза 2 — `IN_PROGRESS`**: шаги 1-9 `DONE`, шаг 10 — **`CODE`**,
шаги 11-12 `HOLD`.

**Гейтящих выносов держателю нет.** Эта единица вопроса не выносила;
`.claude/work/questions/open-questions.md` ею не пополнялся.

**Дельта staged, не закоммичена.** Ветка `claude-audit`. **Дерево кода этой
единицей тронуто** — правки в `trading-core` и `libs/domain-model`.

**Режим — автономия до прод-рубежа.** Решения — Д250-Д1365 (эта единица —
Д1362-Д1365).

## Что сделано после v218

**Единица сессии — кодовый заход K18: закрытие Ф7 и Ф8.** Отчёт —
`.claude/work/progress/phase-2-step-10-code-pass-k18.md`; статус — в
`phase-2-step-10-chronicle.md` §Статус.

- **Обе находки закрыты ОДНОЙ правкой: посылка у них одна.** Каскад жёсткой
  ступени был **вторым, обеднённым писателем того же ребра** — ставил статус
  радиусным `update`'ом, причины и факта остановки не писал, после чего гард
  настоящего ребра не открывался уже никогда. Каскад стал **затребователем**
  ребра энфорсмента и идёт **по сделке за раз**; оба каскадных запроса удалены.
- **Ф7 закрылась выводом, а не строкой в таблице.** `grep "set d.status"` по
  дереву ядра даёт **четыре** запроса при четырёх строках дома
  (`docs/models/domain/aggregate/Deal.md` §Персистентность), у всех четырёх
  гард несёт `d.id = :dealId`. Число писателей **уменьшилось**.
- **Ф8 закрыта кодом, а не приведением корпуса к дефекту.** Вариант (а)
  прогона записал бы в корпус утрату гарантии, чьи доводы тот же корпус
  приводит, при том что код нарушал **собственное** правило —
  `docs/rules/writer-named-for-every-value.md` §«Писатель обязан быть
  достижим». Теперь статус, причина по радиусу и факт остановки идут одной
  транзакцией на сделку.
- **Следствие, доведённое тем же заходом:** команда держателя доходит до
  писателя класса `DealShutdownInitiated` — `hasManualPath` стал истинным, и
  **актор поехал содержимым**.
- **Дом соответствия «радиус — причина» поднят в `HoldScope`** — читателей
  стало два.
- **Предъявлено:** пять мутационных проб красны, реактор 0 (1580 файлов,
  1246 тестов), гейт инструментов корпуса целиком 0, две записи в реестре
  снятых редакций.

## Три вещи, которые дальше нельзя пере-решать

**1. Перечень исполнителей не переоткрывается** (v195). Новый исполнитель
вносится **строкой** реестра, а не поводом пересобрать перечень.

**2. Реестр компонентов сквозной и новым шагом не пересобирается** (Д1184).
Находки в нём — прогона `DOCS_CHECK_34`; гейт срабатывает **на компоненте**,
когда тот начинается.

**3. Существующая строка сделки правится ТОЛЬКО точечным гардированным
запросом** (Д1358-Д1361), и **писатель у ребра один при двух затребователях**
(Д1362): каскад ступени своего запроса статуса не имеет и не заводит.

## Вход следующей сессии

**Единица — СЕДЬМОЙ прогон фокусов** (`conventions` / `performance` /
`disaster` / `security`) по дельте шага, включая правки K18. Он и выдаёт
аппрув (`.claude/processes/roadmap-step-execution.md` §«Аппрув `CODE` —
зафиксированный исход фокусов, не устное согласие»); вести его обязана
сессия, K18 не писавшая.

Что прогон обязан перепроверить в первую очередь — по отчёту K18
§«Что осталось названным»: рост обращений каскада (чтение популяции плюс
ребро на сделку с тремя проекциями каждое), разведение двух затребователей
одного ребра, актор на автоматической тропе.

Порядок дальше: седьмой прогон фокусов → `SYNC_DOCS_FROM_CODE`
(фокус `divergence`) → пост-хок концепт-гейт
(`.claude/processes/roadmap-step-execution.md` §6a). Каждый — своей единицей.

Обязательное перед завершением любой такой единицы:

1. **гейт инструментов корпуса** целиком;
2. **сборка — `bash tools/reactor-test.sh`**, если тронуто дерево кода;
   точечный прогон одного модуля — `mvn -o -am -pl services/<модуль> test` из
   корня. **`-am` обязателен**: общий артефакт в локальном репозитории не
   установлен. `mvn` на `PATH` нет — рабочая форма вызова стои́т в §Прогоны
   отчёта `.claude/work/progress/phase-2-step-10-code-pass-k13.md`;
3. **`py tools/code-gate-check.py`** — перед каждым кодовым заходом.

## Что висит

1. **Текст JPQL новых запросов не мерится ничем** — `@SpringBootTest` у
   модуля нет намеренно; пробы мерят слой `DealDataService` и вызывающих.
   Оживитель общий с п. 2 — первый живой прогон ядра на базе.
2. **Разведение пар джоб ядра ПРОЧИТАНО, а не измерено** — живого прогона
   ядра на базе нет ни одного.
3. **Обращения каскада ступени выросли** — чтение популяции плюс ребро на
   сделку, каждое с тремя точечными проекциями; замера нет, тропа
   исключительная, мощность ограничена построением.
4. **Слепота считается состоявшимися проходами** — тропа входа возраста
   наблюдения не спрашивает; припарковано с оживителем.
5. **Класс `HoldReleased` не построен** — ручное снятие ступени фиксируется
   строкой отчёта, а не классом снятия (`.claude/work/backlog.md`).
6. **Эпизод транша событием не различается** — счётчика эпизода нет и у самой
   заявки; ограничение модели ядра, названо у дома состава.
7. **Строки журнала, накопленные до хода, не чинятся:** операнды агрегата
   невосстановимы задним числом.
8. **Таблицы и писателя строки отказа у четырёх соседей нет** (`auth`,
   `trading-core`, `strategies`, `market-data`) — общий долг
   (`.claude/work/backlog.md`).
9. **Перехватчик `Exception` съедает отказ по правам у `bff` и `strategies`**
   — тот же класс, что закрытая S1; припаркован.
10. **Предикат актора написан трижды** — со-расположение названо у всех трёх,
    единый носитель припаркован.
11. **Глубины у `access_denials` нет ни у одного сервиса** — величина
    выводится из той же наблюдённой частоты, что и порог реакции на серию
    отказов.
12. **Пул планировщика у `market-data` — один поток**, и дом ему ничего не
    предписывает: измерителя среди пяти его джоб нет.
13. **Остаток пары «чистка × пересчёт»** у `audit-statistics`: удаление,
    зафиксированное между чтением операнда и группировкой тех же суток, даёт
    частичную строку; цена принята и названа у дома отбора.
14. **Экспорт метрик у семи сервисов из восьми не построен**; экспорт
    **трейсов** не построен ни у кого.
15. **Подъём контекста не проверен ни у `audit-statistics`, ни у `strategies`,
    ни у `bff`, ни у ядра в части `AsyncConfigurer`:** `@SpringBootTest` у
    модулей нет намеренно.
16. **Транзакционная граница пары «ребро + факт» прогоном не мерится** —
    держит объявление метода, проверяет каркас.
17. **Ни план запроса, ни порядок взятия строчных замков у модуля приёма не
    мерятся ничем** — живого прогона у него нет ни одного; обе позиции
    припаркованы одной секцией с общим оживителем.
18. **Отбор ресурсов наблюдателем по метке `release` живым кластером не
    проверен**; оживитель — подъём стенда на дереве кода.
19. **Дубль запроса `earliestRecordedAt` у двух подключений остался** (Д1297).
20. **Позиции парковки узлов 5-6 `GAPS_CLOSE_3`** и парковка узла 1
    (исполнимость формы ценового результата на строках агрегатов).
21. **Кандидат энфорсера окрестности** — бэклог.
22. **Стенд от дерева кода отстал:** образы `audit-statistics`, `strategies`,
    `trading-core` и `bff` с этими правками не собирались, роли и базы на
    стенде не заводились, тем и подписки на стенде нет. Плюс прежнее — образ
    сервиса без продвинутого тега резолвится подвижным `latest`.
23. **Форма дедупа «спросить и вставить»** — шесть точек трёх сервисов.
24. **Три долга инструментов:** проза реестра
    (`tools/retired-prose-debt.txt`), адресные перекрёстья `docs/**`, долг
    носителей команд (`tools/command-carrier-debt.txt`).
25. **§-адрес javadoc в несуществующий пассаж** — класс припаркован; энфорсера
    у класса по-прежнему нет.
26. **Цена сессий под правилом единицы не замерена.**
27. **События `auth` — производителя и темы нет.**
28. **Единый error-DTO у четырёх построенных сервисов** (`auth`,
    `connector-okx`, `market-data`, `trading-core`).
29. **Потерянное событие при неразбираемом содержимом** — DLT нет.
30. **Названные ограничения шага 7**, ревалидация активных определений при
    смене чисел риск-аппетита, `HOLD_RELEASED` не пишется, база риска на
    терминале не двигается.
31. **Внешний вызов внутри транзакции БД — двенадцать точек** у ядра.
32. **Донорские остатки в общей библиотеке** — снимаются удалением донора.
33. **Отчёты критики и верификации узлов 5-6 `GAPS_CLOSE_3` отдельными файлами
    не сохранены.**
34. **Т-1…Т-3 седьмого прогона гейта `GAPS_CLOSE_4`; Ц-2…Ц-4 первого и Д-1…Д-4
    второго прогона гейта `GAPS_CLOSE_5`** — все о тексте о работе; уезжают с
    отчётами в `history/` при закрытии шага.
35. **Прежние хвосты:** S1 Vault-хардненинг, api-формы периметра без дока.
36. **Пользователю:** обновить Project Knowledge (этот снапшот). Новых
    Maven-модулей эта единица не заводила — переиндексация IDEA не нужна.

## Проверки на момент снапшота

**Гейт инструментов корпуса прогнан** на состоянии после всех правок, включая
этот файл, — весь перечень, ненулевых кодов нет, кода 2 нет. Числа прогонов
здесь не пересказываются: их печатает сам прогон
(`.claude/rules/self-description-form.md`).

**Реактор прогнан** (`bash tools/reactor-test.sh`) — зелёный, на состоянии
после правок дерева кода.

## Среда

Против v132 не изменилась; полное описание — там, перечень ловушек — в v206
(`.claude/work/history/snapshots/snapshot-v206.md` §Среда) и он остаётся в
силе целиком. Ловушки v207-v218 тоже в силе, включая: `python3` в этой среде
разрешается в заглушку Windows Store и падает кодом 49 — рабочая форма `py`.
**Ловушка этой сессии:** `bash`-heredoc с большим русским телом в этой оболочке
разбирается ненадёжно — длинные файлы пишутся инструментом записи, а точечные
правки идут короткими `py`-скриптами.
