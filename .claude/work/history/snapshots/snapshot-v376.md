# Снапшот v376

**Дата:** 2026-09-20.

## На какой вопрос отвечает этот файл

Где мы сейчас.

## Состояние

Сменяет v375. Фаза 1 — `FOLDED`. **Фаза 2 — `IN_PROGRESS`**: шаги 1-11
`DONE`, **шаг 12 — `CODE·3/3`**, шаг 13 `HOLD`. Прод-рубеж — `HOLD`.

**Машина шага** — `CONCEPT → CONCEPT_REVIEW → CODE → DOCS → REVIEW → DONE`
(`.claude/processes/roadmap-step-execution.md`). `CODE` тестового шага идёт
тремя фиксированными под-шагами: тест-кейсы → адверсариальное ревью кейсов →
код тестов. **Под-шаги 1 и 2 закрыты целиком**; **идёт под-шаг 3 — код
тестов.** **Уровень 2 написан целиком (14 из 14); уровня 1 написано три ящика
из восьми, ЧЕТВЁРТЫЙ идёт** — `auth`, `connector-okx`, `market-data` закрыты,
`trading-core` в работе.

**Дельта — в рабочем дереве, не закоммичена.** Ветка `claude-audit`.
**Дерево кода эта сессия меняла** — реактор прогнан, зелен.

**Режим — автономия до прод-рубежа.** Решения — Д250-Д1897.

## Что сделано после v375

Одна единица: **заход 95 — группа `B6` ящика `trading-core`** (ручная
поверхность остановки) целиком, кроме одной клетки. Подробности — хроника
шага `.claude/work/progress/phase-2-step-12-chronicle.md` §«Двадцать седьмой
заход под-шага 3 (95) написал группу `B6` и нашёл обрыв снятия риска на
инструментном радиусе»; здесь только то, что меняет картину.

- **Написаны двенадцать клеток `B6`** — `B6.1`-`B6.9`, `B6.11`-`B6.13`
  (`ManualHaltBoxTest`); одиннадцать зелены, `B6.13` красна по построению
  меткой `debt`. Итого у ящика 124 клетки, пакет — 111 тестов.
- **Живой риск у группы ставится живой НОГОЙ, а не экспозицией.** Нога —
  один из пяти признаков `riskProvenAbsent`, а неподтверждённость снятия
  ставится молчанием стаба: цикл исчерпывает бюджет, нога остаётся живой.
  Отсюда предусловие «ступень стои́т, снятие риска не подтвердилось» тропой
  ящика ставится целиком — обход находки `F-13`, а не ослабление.
- **Находка `F-14`: ручная полная постановка ИНСТРУМЕНТНОГО радиуса снятия
  риска не производит.** Исполнитель берёт триггерную сделку из контекста, а
  поверхность её туда не кладёт ни при каком вызове; отказ глотает перехват,
  и снаружи ход выглядит исполненным. Припаркована секцией бэклога «Ручная
  полная постановка инструментного радиуса роняет снятие риска».
- **Предусловие кейса `B6.5` прогоном исправлено:** на счёте под стоящей
  жёсткой ступенью мягкая постановка не отказывает, а поглощается; отказ
  даёт только «не в рабочем состоянии И без стоящей ступени своего радиуса».
  Обе стороны дома проверены одной клеткой.
- **Контрольный прогон взял пять осей**, все предсказаны поимённо до
  прогона; **все пять сошлись точно.**

## Вход следующей сессии

**Следующая единица — код группы `B7`** (проактивная детекция): её вход —
тик, а предусловия ставятся срезом счёта у стаба коннектора, то есть от
живой экспозиции она не зависит. Что она застаёт:

- **живой экспозиции у ящика по-прежнему нет, и причина названа** — находка
  `F-13`; от неё зависят `B5.1`-`B5.4`, `B5.10`, `B5.15`, остаток `B3`
  (`B3.5`, `B3.10`-`B3.15`) и пять клеток `B4` (`B4.2`, `B4.6`, `B4.7`,
  `B4.10`, `B4.12`);
- **терминал сделки с числом тоже не достижим** — он выводится из закрытого
  эпизода позиции; отсюда `B5.7`, `B5.11`-`B5.13` и `B6.10` ждут той же
  правки;
- **`B5.9` ждёт группы `B7`**: признак инструментного радиуса производят
  учётные детекторы, чьё предусловие — предмет той группы;
- **субстрат и обвязка целиком** — `TradingCoreSubstrate`, `TradingCoreBox`
  (плюс `freeze`, `fullHalt`), `SharedTradingCoreBox`, `Rows` (плюс
  `refuse`/`allow`), `PeerStub`, `IdentityStub`, `AppLog`, `Feed` (плюс
  `livePosition` — чужой живой эпизод для среза детекции), `Bodies`, `Wire`,
  `Definitions`, `BrokerGate`, `SubstrateImagePinTest`;
- **контрольный прогон на нейтрализованной сборке — часть единицы** (Д1830);
  предсказание называет клетки поимённо до прогона;
- **ревизия существующего набора проб предмета** — свой предмет перечня
  (Д1828); заходом 95 исполнена и изменений не потребовала;
- **форма кода уровня 1** задана заходами 83-95, **уровня 2** — заходами
  69-82; всё, что перечисляли v325-v375 §«Вход следующей сессии», остаётся в
  силе дословно (`.claude/work/history/snapshots/snapshot-v325.md` …
  `…/snapshot-v375.md`);
- **хвост апрувленных документов** — негейтящие находки `Ф-3`-`Ф-5` по
  `jsonb-overlay-roundtrip`.

## Что держателю решать

Против v375 не изменилось:

1. **Источник цены `MARK_PRICE` / `INDEX_PRICE`** — реджект создания либо
   снятие ограничения (кейс `U22.8` предмета
   `strategy-definition-validation`, красный по построению).
2. **Читатель `exitOutcome`.**
3. **Ключи demo-окружения OKX и распечатанный Vault** — без них не
   прогоняется ни один кейс мишени `-D` и весь `smoke-live`.

Плюс прежние ожидания (Vault-хардненинг, миграция на Jackson 3, область
тестового токена) — печатает `py tools/backlog-check.py` строкой `держатель:`.

## Что висит

Всё, что живёт секциями бэклога, печатает `py tools/backlog-check.py`; в
снапшоте не дублируется. Вне бэклога список не изменился: пункты 1-14
§«Что висит» снапшота v302 остаются в силе дословно
(`.claude/work/history/snapshots/snapshot-v302.md`), **кроме п. 6** (закрыт
заходом 68). Существенное: стенд отстал от дерева кода; вход снаружи в
окружение не построен; **пары производитель-потребитель из ДВУХ сервисов** на
живом брокере по-прежнему не встречалось.

Три больших свипа с измеренным объёмом остаются открытыми: слой опровержения
в `docs/**`, свод адресных перекрёстий, долг §-адресов
(`tools/anchor-debt.txt`) и процедура синка в доках. Каждый — своя единица.

**Пользователю:** обновить Project Knowledge (этот снапшот); прочитать блоки
дайджеста Д1564-Д1897 — право вето постфактум.

## Ловушки

**Ловушки** v206-v375 остаются в силе. Подтверждено и добавлено этой сессией:

- **Кейс, чьё предусловие отказа не даёт, чинится ДОМОМ, а не ослаблением
  ожидания.** Дом разводил два множества — «без стоящей ступени» и «под
  стоящей ступенью», — и кейс назвал второе, ожидая исхода первого. Клетка
  прогнана на множестве, которое отказ даёт, а вторая строка дома проверена
  той же клеткой отдельным вызовом: правится предусловие, ожидание остаётся.
- **Отказ, съеденный перехватом реакции, снаружи неотличим от исполненного
  хода.** Ступень встала, ответ `202`, а снятия риска не было ни одного;
  видно его только журналом приложения. Клетка о ступени при этом верна —
  находку дал **просмотр журнала прогона**, а не красная клетка.
- **Живой риск ставится не только экспозицией.** Признаков у
  `riskProvenAbsent` пять, и живая НОГА — один из них; тропа, обрывающаяся
  до налива, предусловию «риск не погашен» не мешает.
- Прочие ловушки — снапшоты v283, v285-v375 §Ловушки; они не переписываются.

Обязательное перед завершением любой единицы:

1. **гейт инструментов корпуса** целиком — перечень в
   `.claude/skills/update-roadmap-progress.md` §«Гейт инструментов корпуса»
   (шестнадцать команд), `backlog-check.py` с подменой статуса, если статус
   проставляется;
2. **реактор — `bash tools/reactor-test.sh`**, если тронуто дерево кода.

## Проверки на момент снапшота

**Гейт инструментов корпуса прогнан** целиком; все шестнадцать команд вернули
0. **Реактор прогнан** — 1495 файлов, 4293 теста, дефектов 0
(против 1494 и 4282 у v375). Пакет ящика прогнан отдельно — 111 тестов, все
зелены:

```bash
mvn -o -am -pl services/trading-core test -Dtest='com.example.tradingcore.box.*Test' -Dsurefire.failIfNoSpecifiedTests=false
```

Группа `B6` прогоняется одним классом:

```bash
mvn -o -am -pl services/trading-core test -Dtest='ManualHaltBoxTest' -Dsurefire.failIfNoSpecifiedTests=false
```

## Среда

Против v132 не изменилась, кроме состава локального репозитория по тестовым
библиотекам и **образов субстрата ящика** (строки «Артефакты тестового
контура» и «Образы субстрата ящика»), а также тестовых областей одиннадцати
модулей (`test.excluded.groups`). Дом фактов среды — команды сборки, пути JDK
и Maven, лаунчер Python, Vault, demo-ключ и прайминг —
`.claude/skills/environment-commands.md`.
