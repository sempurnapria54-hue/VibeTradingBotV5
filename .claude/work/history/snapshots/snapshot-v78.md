# Snapshot v78

**Дата:** 2026-08-04.

## На какой вопрос отвечает этот файл

Где мы сейчас. **`DOCS_CHECK_11` прогнан и НЕ чист: 26 открытых находок,
8 гейтящих.** Решения `GAPS_CLOSE_10` прогоном не оспорены — не доведены до
структуры-носителя. Следующий под-шаг — `GAPS_CLOSE_11`. Сменяет v77.

## Состояние

Фаза 1 — `IN_PROGRESS`; шаги 1-6 `DONE`, **шаг 7 — `GAPS_CLOSE_11`**,
шаги 8-11 `HOLD`. Ветка `claude-audit`, `HEAD = 3189bef`.

**Всё закоммичено** (отчёт `DOCS_CHECK_11`, хроника, статус шага,
снапшот-ход, две курационные перестановки — §Курация); рабочее дерево чисто.
PK обновлён.

## Как сюда пришли (после v77)

`DOCS_CHECK_11` — подтверждающий прогон, три независимые линзы с
ограниченным каталогом (A — механика/командный слой; B — модели/mapping/
native/персистентность + миграции; C — торговая корректность на дистилляте).
**30 находок линз → 25 консолидированных + 1 собственная находка CC = 26.**
Верификация CC `V1-V16`: отведённых нет, шесть усилены. Отчёт —
`progress/phase-1-step-7-docs-check-11.md`.

## Ратифицировано — не пере-решать

**Весь корпус решений шага 7 держится** — `DOCS_CHECK_11` не нашёл ни одного
неверного выбора; ядро подтверждено всеми тремя линзами независимо (отчёт
§Подтверждено-ОК, 17 позиций, каждая верифицирована счётом/грепом/сверкой с
`src/` и миграциями). В силе без изменений:

- **Развилка «команда ↔ действие»** целиком — `docs/decisions/
  command-action-boundary.md` (см. v76).
- **Принцип обхода класса `REFRESH_*`** (`command-lifecycle.md` §Правило):
  предикат остановки — «факт добыт»; исчерпанный цикл — не отказ.
- **Состав цикла добычи — из `DealContext`**; контур фазы 1 — **один
  инструмент** с энфорсментом гейтом входа; **авторитет валюты результата** —
  расчётная валюта инструмента; **область Σ-сверки — список исключений по
  бирже**; **анкер `AnomalyReport` разведён по природе** (`kind`
  `STATE`/`EVENT`); **порог серии в шаге 7 = 1**; **`R` остаётся плановым**
  при персистируемых операндах; **`0` на тропах без входа — только после
  проверки факта операций**.

Всё перечисленное — предмет **материализации** на `GAPS_CLOSE_11`, а не
пересмотра.

## Живой контекст: карта 26 находок `DOCS_CHECK_11`

**Класс — один на весь прогон: «ратифицированное решение не доведено до
структуры-носителя».** 22 находки из 26 внесены самим `GAPS_CLOSE_10`; три
(H3, H9, H15) нарушают правила, записанные тем же прогоном. Переоткрытий — 0.

**Гейтят (8):**

- **H1/H2** — тропы «закрытие без входа»: нет писателя `resultProfit = 0` и
  источника `resultProfitCurrency`; предикат «факт операций на бирже» без
  операнда (гейтит и запись нуля, и рамку R-выборки). Разбирать вместе.
- **H3** — `plannedEntryPrice`/`plannedSizeContracts` вне §Структура,
  §Персистентность и schema-дельты.
- **H4** — create-тропа ноги 2: `cTime` в отброшенных, состав создаваемой
  `Position` не задан. Кренs линз **разошлись** (A — брать границу с
  `Order`; B — внести `cTime`).
- **H6** — settle-ccy у трёх потребителей атрибутирована `DealContext`,
  который навеса не несёт.
- **H7** — операнд омиссионного члена epsilon не назван у финализатора и сам
  может отсутствовать.
- **H10** — сверка отгружается с заведомо пустым списком исключений.
- **H26** (собственная находка CC) — переклассификация гейтинга открытых
  вопросов, §ниже.

**Гейтят мягко (10):** H8 (`min` разрушает пол `0.01`), H9 (`appliedRate =
null` трёхзначен; область 4-й суммы), H11 (отложенный `UPDATE` без операнда
инструмента), H14 (мягкий холд без сигнала/производителя/кода), H15 (анкер
`STATE` мёртв у обоих производителей шага 7), H17 (применитель приравнивания
без клаузы), H20 (`fundingFee` выведен при названном потребителе), H21
(`closeAvgPx` смешивает partial-выходы и не-стоповые закрытия), H22 (сверка
на аварийном терминале: довод покрывает одну ветвь из двух), H23 (сделки с
операциями и пустым `R`).

**Зависимая (1):** H5. **Не гейтят (7):** H12, H13, H16, H18, H19, H24, H25.

**Курационные, развилки не несут (4):** H16, H17, H18, H19 — приведение
носителей к действующей редакции. У **H18 список цитат расширен CC** с трёх
до пяти носителей (плюс один двусмысленный): закрытие идёт по расширенному.

**Владельцы:** `solution-designer` — 10, `trading-specialist` — 8,
`integrator` первым — 5, `knowledge-curator` — 3.

**Несут вперёд, не потерять при закрытии:** у H15 два отложенных на шаг 8
хвоста анкера **сцеплены** — починка NULL в ключе немедленно активирует
схлопывание разных `code`, поодиночке ни один анкер не чинит.

## Открытые вопросы

**Изменение против v77 (H26):** `DOCS_CHECK_10` числил все пять релевантных
вопросов не-гейтящими; после правок `GAPS_CLOSE_10` это неверно у двух.

- **`CCY-Q2`** — **гейтящий**: шаг 7 и есть `CODE`-писатель валютных полей,
  до которого вопрос предписано проработать; позиция «область модели» — ось
  варианта 3 в H6. Разбирать вместе с H6.
- **`FEE-Q1`** — **гейтит мягко**: шаг 7 добавляет третьего читателя ставки
  (epsilon у финализатора, H7), структурного запрета на сохранение
  прочитанного инстанса нет. Разбирать вместе с H7.
- **`RISK-Q3`** — сросся с H23 и с осью варианта 2 в H3; **`PNL-Q1`** — H21
  уточняет его второго потребителя; **`HOLD-Q2`** — шага 7 не касается.

## Хвосты работы (не находки)

- **`integrator`, грунт-первым** (единственный дефицит `грунт` ⇒ по правилу
  §4 процесса идут **не дожидаясь** чистого прогона): **H5** (оси запроса
  истории позиций и поведение при нескольких записях в окне), **H25**
  (эндпоинт и ось запроса котировки cross-ccy). Плюс дом конфига исключений
  сверки по бирже (H10, хвост `GAPS_CLOSE_10`).
- **Рантайм-фикстура §AG1.5** (+ §AG3.4/AG3.5), до `CODE`: инвариант
  агрегации positions-history (N11), гранулярность bills, `ccy` fee-bills и
  состав `realizedPnl` при cross-ccy-издержке, entry-fee против `cTime`.
- **Шаг 8:** проектирование `TradeGuardJob`; переоценка инварианта
  «ликвидация за стопом» в `AnomalyJob`; перекрытие анкеров по scope (теперь
  сцеплено с H15).
- **Шаг ожидаемости / фаза 2:** funding в знаменателе R; добор числа на
  `EMERGENCY_CLOSED`; калибровка запаса на проскок.

## Пайплайн

- **Ступень — `Предложение`.** Правки процесса `GAPS_CLOSE_10` (10 штук)
  сработали: правка «кейсы в скиллах — обезличенно» **сняла источник
  протечки через скилл-файлы** — ни одна линза о нём не сообщила.
- **Кандидаты в правку от `DOCS_CHECK_11`** (3, не ратифицированы):
  (1) правка, вводящая поле или тропу чтения, обязана заканчиваться свипом
  по **структурным** носителям (§Структура, §Персистентность, schema-дельта,
  состав снапшота, mapping-таблица, инвентарь читателей) — существующие
  свипы ищут **текст**, а здесь недостаёт структуры, которой в тексте нет по
  определению; (2) отложенный хвост проверяется на сцепленность с тем, что
  шаг поставляет; (3) разведочный режим guard'а обязан называть условие
  выхода.
- **Прежние кандидаты** (без изменений): обратный свип по потребителям
  контракта; проверка покрытия; «операнд, канал и анкер».
- **Протечка провенанса через сами доки остаётся и лечиться не должна** —
  доки обязаны нести провенанс клауз; линза A показала двоякий эффект
  (снижение независимости против повышения чувствительности к
  самоприменимости).

## Курация (свип при снапшоте)

- `snapshot-v77.md` → `work/history/snapshots/`.
- `notes/2026-07-18-ревью-ролей-...md` → `work/history/`: тема закрыта
  (файл сам помечен «правки S1-S7 внесены, не живой список»), входящих
  ссылок из живых файлов нет.
- `knowledge-tree.md` синхронизировано (новый отчёт, снапшот, снятая
  заметка, счёт архивных снапшотов).
- **Незарегистрированных артефактов нет.** Единственный кандидат —
  `.claude/projects/<путь>/memory/` (`MEMORY.md` + `build-command.md`,
  закоммичен в репо) — **уже зарегистрирован** в дереве как артефакт
  харнесса с явной пометкой «не файлы знания проекта».
- Битых ссылок от перестановок нет: на снятую заметку живые файлы не
  ссылались (проверено грепом до переноса).

## Loose ends

- **Custom Instructions**: правка `chat-project-instructions.md` (правило
  ресурса) — вставлена ли в Settings, не подтверждено; висит с позапрошлой
  сессии, до вставки не действует. Проверить в чате.
- Целевые, в коде их нет: суффиксы enum'ов, `SystemActionExecutor`,
  V2-топология `deal_action_states`, `ENTRY_BLOCKED`,
  `HoldScope.INSTRUMENT_GROUP`, `REFRESH_BILLS_COMMAND`,
  `MARK_DEAL_EMERGENCY_CLOSED_COMMAND`, вторая нога
  `REFRESH_POSITION_COMMAND`, поля `Position`/`Deal`, колонка
  `anomaly_reports.kind`, секция `service-command-retry`, `AnomalyJob`.

## Что дальше (порядок)

1. **`GAPS_CLOSE_11`** — закрытие 26 находок. Пользовательский хвост (по
   фильтру выноса — дефицит `подтверждение`): **H1** (частично), **H4**,
   **H10**, **H14**; остальные идут владельцам, к пользователю возвращаются
   результатом. Курационные H16-H19 — без прохода.
2. **Сбор грунта параллельно**: H5 и H25 к `integrator` — не ждут чистого
   прогона.
3. **`DOCS_CHECK_12`** — подтверждающий; чист → гейт `CODE` снят.
4. **Рантайм-фикстура §AG1.5/AG3.4/AG3.5** — до `CODE`.
5. **`CODE`** — хвост в `backlog.md` §Шаг 7.

## Среда

Тулчейн/инфра — без изменений (см. v62/v64/v67). Уточнение, установленное
этим прогоном: **PostgreSQL 16** (`docker-compose.yml`, оба контура).

## После коммита

Коммит и обновление PK — **выполнены** (`HEAD = 3189bef`). Остаётся
подтвердить перенос `.claude/chat/chat-project-instructions.md` в Custom
Instructions (§Loose ends).
