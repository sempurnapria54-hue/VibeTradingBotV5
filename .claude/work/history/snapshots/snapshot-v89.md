# Snapshot v89

**Дата:** 2026-08-28.

## На какой вопрос отвечает этот файл

Где мы сейчас. **Шаг 7 прошёл `DOCS_CHECK_22` и `GAPS_CLOSE_22`;
следующий под-шаг — `DOCS_CHECK_23`, и он же гейт выхода в `CODE`.**
Сменяет v88 (сессия чата исчерпала ресурс и переезжает).

## Состояние

Фаза 1 — `IN_PROGRESS`; шаги 1-6 `DONE`, шаг 7 — **`DOCS_CHECK_23`**,
шаги 8-11 `HOLD`. Ветка `claude-audit`, `HEAD = fa93373`
(`ROADMAP 1-7-3 GAPS_CLOSE_21`); вся дельта двух под-шагов —
**staged, ждёт ручного ревью и коммита**.

**Режим — автономия до конца фазы 1**
(`.claude/processes/question-delegation.md`): проектные развилки CC
решает сам и фиксирует в дайджесте; пользователю — только дефицит
`подтверждение`.

## Контекст продолжения

### Где именно шаг 7

Прогон `DOCS_CHECK_22` дал 23 находки (11 гейтящих), закрытие
`GAPS_CLOSE_22` закрыло все. **Чистого прогона у шага ещё не было** —
`DOCS_CHECK_23` обязан пройти чисто, чтобы шаг ушёл в `CODE`
(гейт — `.claude/processes/roadmap-step-execution.md` §Гейты).

- Прогон — `.claude/work/progress/phase-1-step-7-docs-check-22.md`.
- Закрытие — `phase-1-step-7-gaps-close-22.md` (там же раздел свипов и
  рекомендация, что прогон 23 обязан проверить первым).
- Хроника шага — `phase-1-step-7-chronicle.md`.

### Решения держателя этой полосы (не пере-решать)

**Пайплайн:**

- **Режим автономии** до конца фазы 1; в чат — только
  `подтверждение`, остальное в `decision-digest.md`.
- **Четыре концептуальных правила**: иерархия исполнения
  (`docs/rules/execution-hierarchy.md`); уровень 4 error-градации —
  неожиданное поведение биржи (`docs/rules/error-handling-policy.md`);
  минимальный след — норма (`.claude/rules/design-simplicity.md`);
  инвариант «живой риск без защиты»
  (`docs/rules/risk-creating-entry-protection.md`).
- **Дом политики** — один носитель, остальные ссылаются
  (`.claude/rules/policy-home.md`).

**Лестница биржевых safety-состояний:** `Exchange.HOLD` (ступень 1,
мягкая, без kill-switch — единственный автотриггер `MISMATCHED` при
калиброванном допуске) ↔ `Exchange.TRADE_BLOCKED` (ступень 2, flatten).
Дом — `docs/rules/exchange-hold.md`.

**Риск-модель (дом — `docs/decisions/per-trade-risk-policy.md`; обзор —
§«Карта риск-модели (как читать целиком)» в шапке того же файла):**

- **три лимита** внутри уровня «риск на сделку»: поактный
  (`riskPerActionPercent`), кумулятивный потолок сделки (`K ×` поактный
  на **сжимающейся** базе `min(снимок, текущая)`), одновременный;
- **одновременный максимум двухуровневый**: глобальный конфиг
  `globalSimultaneousRiskPerDealPercent` (`1%`) и вложенный в него
  `StrategyDetail.strategySimultaneousRiskPerDealPercent`;
- **`liveRiskNow`** считается от **живой экспозиции** (неисполненная
  доля живых ног + риск живого эпизода до действующего стопа, клэмп на
  живом слагаемом), **`dealRiskTaken`** = заявленное живыми и
  исполнившимися + взятое снятыми; оба — производные графа в точке
  проверки, persisted-носителя нет;
- **знаменатель `R`** (`Deal.plannedRiskAmount`) операндом лимитов не
  служит и остаётся **заявленным**;
- **`MARK`-only до измерения базиса**: create отвергает
  `triggerPriceType ∈ {LAST, INDEX}`
  (`STRATEGY_TRIGGER_PRICE_TYPE_NOT_MARK`), провизорных величин не
  вводится;
- **worst-case открывающего входа**: кэп не вводится; поактный нотинал
  связан аллокацией (плечо к базе ≈1×), остаток дыры — **сделочный**;
- **сжатие потолка — по реализованной просадке**; открытая просадка
  живой сделки потолок не двигает.

**Условия пересмотра заякорены на данные живой торговли, не на срок** —
`.claude/work/backlog.md`: §«Пересмотр worst-case открывающего входа»
(включая сделочный кэп нотинала, потолок `allocationPercents`, операнд
кумулятивного потолка на открытой просадке, филл отдыхающей ноги);
§«Снятие ограничения `MARK`-only — по измеренному базису»; §«Закрытие
фазы 1 — снятие разведочного режима допуска сверки».

### Что висит дальше

1. **Пользователь:** закоммитить дельту; обновить Project Knowledge —
   как минимум **этот снапшот**, `.claude/chat/structure-digest.md`
   (если менялся), `.claude/work/decision-digest.md`, изменённые дома
   риск-модели (`docs/decisions/per-trade-risk-policy.md`,
   `docs/rules/risk-creating-entry-protection.md`,
   `docs/rules/absent-value-semantics.md`,
   `docs/rules/persistence-representation.md`).
2. **`DOCS_CHECK_23`** — гейт `CODE`. Что проверять первым — §Рекомендация
   отчёта `GAPS_CLOSE_22`: введённые конструкции, внутрифайловые
   расхождения, пересобранные клеймы полноты.
3. **При чистом прогоне — выход в `CODE`.** Гейтящих предусловий
   `CODE` **десять** (реестр —
   `docs/decisions/pnl-finalization-mechanics.md` §«Предусловия `CODE`
   шага 7»), их грунт собирается **не дожидаясь** чистого прогона.
   Кейс `AG6.1` переведён в ⚠️ ПЕРЕПРОГОН: наблюдение 2026-06-20
   сохранило мощность множества вместо перечня.
4. **Дайджест решений:** непрочитанных пакетов — **четыре итерации**
   2026-08-28 (Д45-Д72) плюс более ранние Д1-Д44; вето по пункту
   возвращается CC задачей. Прочитанное переезжает в `history/`.

## Что произошло за сессию (после v88)

| Что | Носитель |
|---|---|
| Обзорная секция риск-модели «как читать целиком» — роль каждой величины, писатель, проверяющий, указатели в дома | `docs/decisions/per-trade-risk-policy.md` §«Карта риск-модели (как читать целиком)» |
| `DOCS_CHECK_22`: три независимые линзы + слой верификации V1-V17 + 6 собственных находок CC | `progress/phase-1-step-7-docs-check-22.md` |
| `GAPS_CLOSE_22`: 23 находки закрыты, 4 развилки разрешил держатель | `progress/phase-1-step-7-gaps-close-22.md`, дайджест Д61-Д72 |
| Новое правило: закоммиченная миграция неизменна, её шапка — архив | `docs/rules/persistence-representation.md` |
| Второе названное исключение пустоты (`externalFee` в разности) | `docs/rules/absent-value-semantics.md` |
| Новое предусловие `CODE` п. 16 — отображение `type`/`subType` → `CashFlowCategory` | `docs/decisions/pnl-finalization-mechanics.md` |

## Класс дефекта, за которым следить

Прогон `_22` выделил новый подкласс: **правка доходит до одного пассажа
файла и не доходит до другого пассажа того же файла** (греп по термину
находит первое вхождение). Свип закрытия по этому предмету нашёл
носитель, не названный ни одной линзой
(`MarkDealEmergencyClosedExecutor.md` — третья копия формулы полноты).
Внутрифайловый свип теперь — обязательная часть закрытия.

## Среда

Без изменений — см.
`.claude/work/history/snapshots/snapshot-v87.md`.
