# Snapshot v169

**Дата:** 2026-09-08.

## На какой вопрос отвечает этот файл

Где мы сейчас. **Шаг 10 фазы 2 (аудит и статистика): гейт выхода из
`GAPS_CLOSE_4` прогнан — исход КРАСНЫЙ. Узел 2 переоткрыт; статус шага не
двигался. Следующая единица — верификация 2 узла 2.** Сменяет v168.

## Состояние

Фаза 1 — `FOLDED`. **Фаза 2 — `IN_PROGRESS`**: шаги 1-9 `DONE`,
шаг 10 — **`GAPS_CLOSE_4`** (гейт выхода статус не пропустил), шаги 11-12
`HOLD`.

**Гейтящих выносов держателю нет.** Вопрос закрытия был один — `PROC-Q8`, —
и он отвечен.

**Дельта в working tree, не в истории.** Ветка `claude-audit`; всё staged,
не закоммичено. Сверх v168 тронуты: отчёт закрытия
`.claude/work/progress/phase-2-step-10-gaps-close-4.md` (раздел гейта, строка
статуса узла 2, пятая единица), отчёт узла 2, хроника шага, снапшот.
**Корпус не тронут** — гейт ничего в `docs/**` не правил. **Дерево кода не
тронуто** — как в v162-v168.

**Режим — автономия до прод-рубежа.** Решения — Д250-Д1131 (новых эта
сессия не заводила: гейт — проверка, а не развилка).

## Что сделано после v168

**Единица сессии — гейт выхода из `GAPS_CLOSE_4`** (шесть предметов
`.claude/skills/update-roadmap-progress.md` §«Гейт выхода из
`GAPS_CLOSE_N`»). Единица **закрыта**; исход — **красный**, статус не
проставлен. Отчёт гейта —
`.claude/work/progress/phase-2-step-10-gaps-close-4.md` §«Гейт выхода из
`GAPS_CLOSE_4` — прогнан 2026-09-08, исход КРАСНЫЙ».

**1. Гейтящих единиц три, а не две — и v168 это называла неверно.**
Популяция выведена из колонки «Гейтит» индекса находок прогона:
дизайн-проход, узел 1 и **узел 2** (A7 несёт «да»). v168 объявляла
гейтящими только проход и узел 1, то есть выводила популяцию из памяти, а
не из носителя.

**2. Красное — четыре находки, все у узла 2, несущая одна.** **Н1:**
доработка, закрывшая **шесть блокирующих** находок верификации 1 (V1-V6),
приземлена **без верификации** — включая заведение нового носителя
(`docs/rules/statistics-aggregates.md` §«Что это за числа и кто их читает»),
которого критик не читал ни разу. Довод узла («вторая верификация не
потребовалась») описывает состояние **после** доработки — то самое, которое
второй заход и должен прочитать. **Н2-Н4** правят отчёт узла, а не корпус:
команда популяции не дословна у осей A и B и отсутствует у C и D; раздела
свипов в предписанной форме нет при трёх снятых формулировках; чек-лист
глубины не проставлен поимённо.

**3. Класс Н1 назван самим `PROC-Q7` раньше, чем узел его воспроизвёл.**
Замер решения перечисляет узлы 4, 5 и 6 `GAPS_CLOSE_3` строкой «приземлены
без второй верификации» (`.claude/decisions/mini-loop-verification-ceiling.md`
§«Замер, на котором решение стои́т»). Узел 3 несёт тот же довод, но гейтящих
находок у него ноль и гейт его не мерит; узлы 4 и 5 вторую верификацию
прогнали. **Потолок не тронут:** у узла 2 верификаций одна из двух
разрешённых, и второй заход законен без снятия нормы.

**4. Зелёное записано тоже, включая неприменимое.** У дизайн-прохода
мини-петли нет **по построению**: `PROC-Q6` = (а) выбран именно потому, что
на его классе мини-петля не работает
(`.claude/decisions/reception-executor-design-pass.md`), и заходы у прохода
свои. Узел 1 зелен по всем шести предметам. Диспозиция — все находки с
исходом, сводка порождённым блоком. Вынос держателю один и записан с ответом.

**5. Правки узла 2 из корпуса не откатываются.** Они закрывают названные
находки; непроверенным остаётся не их наличие, а их верность.

## Три вещи, которые дальше нельзя пере-решать

**1. Узел 2 не закрыт.** Его строка в таблице статусов — «идёт», и
переприземляется он только по чистой верификации. Объявить его закрытым без
второго взгляда — вернуть ровно ту находку, ради которой гейт и красен.

**2. Гейтящая популяция выводится из носителя, а не из памяти.** Колонка
«Гейтит» индекса находок прогона — источник; счёт «узлы 2-5 гейтящих не
несли» был выведен иначе и оказался ложным.

**3. Верификацию узла 2 берёт сессия, не читавшая его рационала.** Предмет
верификации — носители, а не рассказ о них
(`.claude/processes/roadmap-step-execution.md`); сессия гейта прочла отчёт,
раунд критики и верификацию 1 целиком и независимого взгляда не имеет.

## Вход следующей сессии

**Единица — верификация 2 узла 2 `GAPS_CLOSE_4`.** Читать **по носителям**
правки, закрывшие V1-V6, и в первую очередь новый носитель
`docs/rules/statistics-aggregates.md` §«Что это за числа и кто их читает».
Вход — сами носители плюс перечень находок
`phase-2-step-10-gaps-close-4-node-2-verification.md` §Находки и §«Как
закрыты»; рационала узла и его раунда критики верификация не читает.

**Тем же заходом закрываются Н2, Н3 и Н4** — они правят отчёт узла: команда
популяции по четырём осям дословно; раздел свипов на три снятые формулировки
с командами и пофайловой таблицей (плюс запись реестра снятых редакций либо
названный отказ от неё); чек-лист глубины поимённо с обходом потребителей
**двумя формами**. Образец обеих форм — узел 1 того же закрытия.

**По чистой верификации** узел переприземляется, и **гейт выхода
прогоняется заново** — уже по всем шести предметам у трёх гейтящих единиц.
После него — следующий статус шага по
`.claude/processes/roadmap-step-execution.md` §4: прогон `DOCS_CHECK_4`
чистым не был, поэтому следующий статус — `DOCS_CHECK_5`.

## Что висит

1. **Дельта `CODE` шага 10 — счёт стои́т порождённым блоком в хронике.**
2. **Компонентов шага 10 в `.claude/work/code-gate-ledger.json` нет** — их
   вносит простановка `CODE`.
3. **Позиции парковки узлов 5-6 `GAPS_CLOSE_3`** и парковка узла 1
   (исполнимость формы ценового результата на строках агрегатов).
4. **Образ сервиса без продвинутого тега резолвится подвижным `latest`**
   (`.claude/work/backlog.md`).
5. **Форма дедупа «спросить и вставить»** — шесть точек трёх сервисов;
   поглощённый отчёт ручной операции. Обе — секции бэклога.
6. **Долг прозы реестра** (`tools/retired-prose-debt.txt`).
7. **Цена сессий под правилом единицы не замерена.**
8. **События `auth` — производителя и темы нет**; экспорт метрик из семи
   прочих сервисов; таблицы `access_denials` нет ни у одного сервиса.
9. **Единый error-DTO у четырёх построенных сервисов.**
10. **Потерянное событие при неразбираемом содержимом** — DLT нет.
11. **Названные ограничения шага 7**, ревалидация активных определений при
    смене чисел риск-аппетита, `HOLD_RELEASED` не пишется, база риска на
    терминале не двигается.
12. **Внешний вызов внутри транзакции БД — двенадцать точек** у ядра.
13. **Донорские остатки в общей библиотеке** — снимаются удалением донора.
14. **Отчёты критики и верификации узлов 5-6 `GAPS_CLOSE_3` отдельными
    файлами не сохранены** — закрыто в части нормы, в части прошлого не
    закрывается ничем.
15. **Прежние хвосты:** S1 Vault-хардненинг, рост таблицы отказов,
    api-формы периметра без дока, долг адресных перекрёстий.
16. **Пользователю:** переиндексировать проект в IDEA; обновить Project
    Knowledge (этот снапшот). **Стенд от дерева кода не отстал** — код не
    тронут.

## Проверки на момент снапшота

**Двенадцать команд корпуса прогнаны на этом состоянии** — одиннадцать
перечня гейта плюс `code-gate-check.py`, каждая код 0. Рабочий бинарь —
`py`; `python3` из PATH — заглушка, её код 49 читается как «не измерялось».
Гейт прогонялся **фоновой** командой: в 120 с он не укладывается. Числа
прогонов здесь не пересказываются — их печатает сам прогон
(`.claude/rules/self-description-form.md`).

**Реактор не прогонялся: дерево кода не тронуто.** Последний прогон — на
этом же состоянии кода (замер v163).

**Живого прогона нет ни у одного компонента шагов 7-10.**

## Среда

Против v132 не изменилась; полное описание — там. Ловушки среды из
v156-v168 остаются в силе, включая: полный гейт инструментов в 120 с не
укладывается — прогонять фоновой командой; markdown-тело в heredoc рвётся —
писать инструментом записи. **Новое этой сессии:** heredoc `<<'PYEOF'` с
телом Python работает и когда тело несёт кириллицу и обратные кавычки в
строковых литералах, — рвётся именно markdown-тело, а не heredoc как таковой.
