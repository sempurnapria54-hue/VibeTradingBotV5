# Snapshot v132

**Дата:** 2026-09-05.

## На какой вопрос отвечает этот файл

Где мы сейчас. **Шаг 7 фазы 2 — `CODE`: docs-first пройден, кодовый заход
начат (компонент 1 из 19). Платформа впервые поднята вживую — локальный
стенд.** Сменяет v131.

## Состояние

Фаза 1 — `FOLDED`. **Фаза 2 — `IN_PROGRESS`**: шаги 1-6 `DONE`, шаг 7 —
`CODE` (docs-first закрыт, гейт перехода исполнен), шаги 8-12 `HOLD`.
Фазы 3-6, 7-8 и прод-рубеж — `HOLD`.

**Дельта в working tree, не в истории.** Ветка `claude-audit`; всё
описанное — staged, не закоммичено (коммит руками).

**Режим — автономия до прод-рубежа.** Решения — Д250-Д731 (итерация 34).
**Открытых выносов держателю нет:** ARCH-Q1 **приостановлен** — размещение
`prod` решается по наблюдённому потреблению стенда перед рубежом.

## Что закрыто этим заходом

1. **Пять решений держателя размещены** (потолка хранения нет; стенд
   сейчас и `prod` по наблюдению; советник — автономный управляющий с
   активацией по гейту; продажа стратегий — намерение; аллокатор не
   вытесняет живое). Карта носителей —
   `.claude/work/history/2026-09-05-holder-decisions.md`.
2. **Локальный стенд поднят и работает** — платформа, три сервиса,
   миграции применены впервые, живой синк каталога дошёл до площадки (473
   инструмента). Хроника и находки —
   `.claude/work/history/2026-09-05-local-stand.md`; команды —
   `.claude/skills/local-stand.md`; сценарии — `tools/stand/`.

## Где стои́т шаг 7

**Docs-first пройден целиком** — четыре прогона (12 → 5 → 2 → 0 гейтящих),
четыре закрытия; гейт перехода в `CODE` исполнен. **Кодовый заход начат:**
поставлен компонент 1 из 19 — модуль `services/trading-core` и схема
`V1` (29 таблиц), применённая **вживую** на стенде; сервис развёрнут в
`dev` и поднялся. Артефакты — `.claude/work/progress/phase-2-step-7-*`.

**Ратифицировано шагом 7 (не пере-решать):** радиус торговой строки —
пара «счёт × инструмент»; проекции чужих реестров наполняет синхронное
чтение; числа риск-аппетита — на строке тенанта; копия определения
стратегии плюс временный писатель до шага 8; факты исполнения —
**опросом**; отказ соседа по ярусу — `PEER_SERVICE_UNAVAILABLE`.

## Что даёт стенд (живой контекст)

| Узел | Состояние |
|---|---|
| кластер | kind v0.33.0, узел Kubernetes v1.37.0, один узел |
| GitOps | Argo CD v3.5.2 держит кластерный слой `deploy/base/platform` |
| данные `dev` | Postgres 17.10 + TimescaleDB 2.29.2, Kafka (KRaft), Elasticsearch 9.5.3 + Kibana + сборщик, Jaeger 2.20, Keycloak 26.7.3 с реалмом, Vault (распечатан) |
| сервисы | `auth`, `connector-okx`, `market-data`, `trading-core` — 1/1, миграции `V1` применены у всех, у кого есть база |
| потребление | ~0.21 ядра и ~9.6 ГиБ памяти на всю платформу в покое |
| место | образы 16 ГиБ в узле; **на системном диске держателя осталось 8.2 ГиБ** |

**Что стенд НЕ даёт:** `stage`/`prod` не поднимались, ингресса нет,
экспорта трейсов из сервисов нет, срезы стакана не собираются (сбор
ограничен статусом инструмента — позиция бэклога), боевой контур
недопустим по оси окружения.

## Что дальше — вход в следующий заход

1. **Кодовый заход шага 7 продолжается** — порт торгового ядра из
   донора: проекции чужих реестров и их синк, агрегат сделки и транша,
   FSM, команды, риск-гейт, холды, аномалии, outbox, `REFRESH`-контур.
   Реестр компонентов и их статус кодирования —
   `.claude/work/code-gate-ledger.json` (19 компонентов шага, поставлен
   первый — модуль и схема).
2. **Кодовые хвосты трёх прежних парковок** идут тем же заходом: ставка
   комиссии, предикат свежести шага, контракт броска диспетчера.
3. **Аппрув `CODE`** — фокусы `conventions`/`performance`/`disaster`/
   `security` с зафиксированным исходом; автор код не аппрувит.

## Что висит

1. **Парковки стенда:** срезы стакана (сбор по `ACTIVE`), экспорт
   трейсов, том Elasticsearch (умолчание 1 ГиБ), политики сжатия
   гипертаблиц (условие сработало — образ закреплён).
2. **Парковки прежних шагов:** сброс кэша ключей по ротации; осиротевшие
   секреты; WS-слушатель фактов исполнения; контекстный тест сервиса
   (цена отсутствия измерена стендом — четыре дефекта); подпись образов;
   транспорт внутри кластера; темы Kafka; размеры томов; ликвидации;
   мягкий ориентир по расходам на хранение.
3. **Прежние хвосты:** S1 Vault-хардненинг, рост таблицы отказов, A12,
   хвост шага 8, долг адресных перекрёстий `docs/**`, дубль эталона
   стратегии в корне.
4. **Пользователю:** переиндексировать проект в IDEA; обновить Project
   Knowledge (этот снапшот, дайджест итерации 34, `structure-digest.md`).
   На системном диске мало места — при следующей постановке стенда либо
   перенести хранилище Docker на другой диск, либо сжать образ.

## Проверки на момент снапшота

Восемь команд корпуса — код 0 (`spec-run`, `spec-mutation-check`,
`spec-scope-check`, `spec-pointer-check`, `population-derive-check`,
`preconditions-check`, `deploy-layout-check`; `anchor-check` — новых
неразрешимых нет). `retired-check` и `code-gate-check` — по своим
условиям прогона.

**Живая проверка вместо прогонной:** доступность API OKX с адреса стенда
— `200` за 0.9 с; миграции `V1` применены на живой базе; каталог
инструментов наполнен площадкой.

## Среда

Против v130 добавилось: kind (`%LOCALAPPDATA%\kind\kind.exe`), кластер
`kind-vibetrading`, каталог стенда `%LOCALAPPDATA%\vibetrading-stand`
(файл распечатывания Vault — единственный невосстановимый). Сборка —
реакторная из корня; `JAVA_HOME` среды указывает на JDK 11, поэтому
команды стенда назначают JDK 25 сами.
