# Snapshot v167

**Дата:** 2026-09-08.

## На какой вопрос отвечает этот файл

Где мы сейчас. **Шаг 10 фазы 2 (аудит и статистика): `GAPS_CLOSE_4`,
седьмая единица — узел 4 (A12, B10) — ЗАКРЫТА. Следующая единица —
узел 5, последний.** Сменяет v166.

## Состояние

Фаза 1 — `FOLDED`. **Фаза 2 — `IN_PROGRESS`**: шаги 1-9 `DONE`,
шаг 10 — **`GAPS_CLOSE_4`**, шаги 11-12 `HOLD`.

**Гейтящих выносов нет, гейтящих находок в прогоне не осталось.** Узел 5
несёт ноль гейтящих.

**Дельта в working tree, не в истории.** Ветка `claude-audit`; всё staged,
не закоммичено. Сверх v166 тронуты: `docs/architecture/platform.md`,
`docs/models/domain/other/AuditRecord.md`, `tools/deploy-layout-check.py`,
`tools/self-count-check.py`, `deploy/README.md`, три
`deploy/*/kustomization.yaml`, три `deploy/*/env.yaml`,
`.claude/work/backlog.md`, плюс отчёты узла и его трёх заходов, рамка,
хроника, дайджест (Д1121-Д1126), снапшот. **Дерево кода не тронуто** — как в
v162-v166.

**Режим — автономия до прод-рубежа.** Решения — Д250-Д1126.

## Что сделано после v166

**Единица сессии — узел 4 `GAPS_CLOSE_4`** (оси окружений). Узел
**приземлён**; отчёт —
`.claude/work/progress/phase-2-step-10-gaps-close-4-node-4.md`
§«Приземление». Заходов — раунд критики и **две** верификации; потолок
(`PROC-Q7`) исчерпан и **не превышен**: вторая верификация блокирующих не
нашла, сигнал С5 не сработал.

**Правило узла:** закрытый перечень мерится только выводом из дома; копия
перечня, живущая у измерителя, делает измеряемым себя, а не дом.

**1. Перечень осей окружений выводится из дома** (Д1121-Д1122). Копия
`REQUIRED_AXES` снята; дом объявляет машиночитаемое имя каждой оси колонкой
таблицы, прочерк объявляет отсутствие ключа и обязан назвать носитель
значения (мерится). Дефект предъявлен пробой с **обеими сторонами отката**:
строка оси, удалённая из дома, прежде оставляла замер зелёным — теперь
роняет его по расхождению на окружение.

**2. Дом достроен до собственного клейма** (Д1123-Д1124): тремя строками,
которых он не имел, — имя окружения, тег образа сервиса, избыточность
платформенных данных. Разбор остальных различий трёх наборов манифестов даёт
**проекция**: величина, чьё значение равно значению объявленной оси, строкой
перечня не становится, сколько бы мест записи ни имела.

**3. Непроверяемая половина клейма названа непроверяемой** (Д1125): машина
мерит совпадение ключей с колонкой дома; «всё прочее одинаково» держит
правило оверлея. Клейм, чья непроверяемая половина выглядит проверенной,
опаснее клейма без проверки.

**4. Второй перечень-копия найден и снят тем же родом действия** (Д1126):
словарь состояний захода у `tools/self-count-check.py` жил кортежем и успел
стать **шире** своего дома — нёс два слова, которых правило не объявляет.
Теперь выводится из строки таблицы правила; заведены оси 4a/4b с пробами.

**5. B10 закрыта первой единицей этого же закрытия** — позицией 7
дизайн-прохода; узел это предъявил поимённой сверкой требований находки с
корпусом, раскладка исправлена. **Цена варианта, выбранного проходом, была
названа линзой не тем адресом** («замер упадёт до правки дома», тогда как
падал он до правки копии) — тот же механизм, что A12.

## Три вещи, которые дальше нельзя пере-решать

**1. Узел 4 закрыт целиком.** Д1121-Д1126 вошли в силу с приземлением;
возврат к копии перечня рядом с измерителем — регресс.

**2. Проекция оси новой осью не является, и признак — равенство значений.**
Перечень, растущий с каждым потребителем оси, перестаёт быть перечнем.

**3. Клейм, чья непроверяемая половина выглядит проверенной, объявляется
непроверяемым.** Это не ослабление клейма, а снятие ложного обещания.

## Вход следующей сессии

**Единица — узел 5 `GAPS_CLOSE_4`: нотации команд, дедуп ядра, отчёт ручной
тропы, отчёты узлов** (A11, B9, B11, C3; гейтящих нет). Это **последний**
узел закрытия; после него `GAPS_CLOSE_4` закрывается и шаг идёт дальше по
`.claude/processes/roadmap-step-execution.md`. Раскладка —
`.claude/work/progress/phase-2-step-10-gaps-close-4.md` §«Раскладка находок
по узлам».

Рамка узла та же: правило одной фразой → популяция до первой правки →
проект правок → мини-петля критики (потолок — две верификации) →
приземление → обход потребителей и свипы. **Урок узла 4, стоящий дороже
прочих:** популяция, объявленная одним шаблоном грепа, обязана быть
**переисполнена дословно** — обход, которого не воспроизводит собственная
команда, пропускает ровно тот носитель, ради которого узел заведён (К4 → К2).
Второй урок: перебор «чем предметы фактически различаются» делается по
**всем** носителям предмета, а не по одному из двух (V1 пришла тем же
механизмом, что K1, — на одну итерацию позже).

## Что висит

1. **Дельта `CODE` шага 10 — счёт стои́т порождённым блоком в хронике.**
2. **Компонентов шага 10 в `.claude/work/code-gate-ledger.json` нет** — их
   вносит простановка `CODE`.
3. **Позиции парковки узлов 5-6 `GAPS_CLOSE_3`** и парковка узла 1
   (исполнимость формы ценового результата на строках агрегатов).
4. **Образ сервиса без продвинутого тега резолвится подвижным `latest`** —
   новая парковка узла 4 (`.claude/work/backlog.md`).
5. **Долг прозы реестра** (`tools/retired-prose-debt.txt`).
6. **Цена сессий под правилом единицы не замерена.**
7. **События `auth` — производителя и темы нет**; экспорт метрик из семи
   прочих сервисов; таблицы `access_denials` нет ни у одного сервиса.
8. **Единый error-DTO у четырёх построенных сервисов.**
9. **Потерянное событие при неразбираемом содержимом** — DLT нет.
10. **Названные ограничения шага 7**, ревалидация активных определений при
    смене чисел риск-аппетита, `HOLD_RELEASED` не пишется, база риска на
    терминале не двигается.
11. **Внешний вызов внутри транзакции БД — двенадцать точек** у ядра.
12. **Донорские остатки в общей библиотеке** — снимаются удалением донора.
13. **Отчёты критики и верификации узлов 5-6 `GAPS_CLOSE_3` отдельными
    файлами не сохранены** (C3 прогона `DOCS_CHECK_4`, узел 5
    `GAPS_CLOSE_4`).
14. **Прежние хвосты:** S1 Vault-хардненинг, рост таблицы отказов,
    api-формы периметра без дока, долг адресных перекрёстий.
15. **Пользователю:** переиндексировать проект в IDEA; обновить Project
    Knowledge (этот снапшот). **Стенд от дерева кода не отстал** — код не
    тронут.

## Проверки на момент снапшота

**Двенадцать команд корпуса прогнаны на этом состоянии.** Рабочий бинарь —
`py`; `python3` из PATH — заглушка, её код 49 читается как «не измерялось».
Гейт прогонялся **фоновой** командой: в 120 с он не укладывается. Числа
прогонов здесь не пересказываются — их печатает сам прогон
(`.claude/rules/self-description-form.md`).

**Реактор не прогонялся: дерево кода не тронуто.** Последний прогон — на
этом же состоянии кода (замер v163).

**Живого прогона нет ни у одного компонента шагов 7-10.**

## Среда

Против v132 не изменилась; полное описание — там. Ловушки среды из
v156-v166 остаются в силе, включая **рвущийся heredoc**: в этой сессии
`<<'PYEOF'` дважды превратил `\n` внутри строкового литерала в реальный
перевод строки и сломал файл синтаксически — правки кода подавать короткими
скриптами и **проверять `ast.parse` сразу после записи**. Полный гейт
инструментов в 120 с не укладывается — прогонять фоновой командой; коды
снимаются обёрткой без конвейера (`echo "rc=$?"` после `tail` возвращает код
`tail`).
