# Snapshot v160

**Дата:** 2026-09-08.

## На какой вопрос отвечает этот файл

Где мы сейчас. **Шаг 10 фазы 2 (аудит и статистика): открыт `GAPS_CLOSE_4`,
его первая единица — дизайн-проход по форме исполнителя приёма событий —
закрыта; следующая единица — закрытие пяти гейтящих находок, из посылки не
выводимых, и всех негейтящих.** Сменяет v159.

## Состояние

Фаза 1 — `FOLDED`. **Фаза 2 — `IN_PROGRESS`**: шаги 1-9 `DONE`,
шаг 10 — **`GAPS_CLOSE_4`**, шаги 11-12 `HOLD`.

**Открытых выносов держателю нет.** Работа не гейтится ничем.

**Дельта в working tree, не в истории.** Ветка `claude-audit`; всё staged,
не закоммичено. Сверх v159 тронуты: `docs/**` (три новых компонент-дока и
шесть правленых носителей), `docs/spec/**` (две спеки, только ноты),
`deploy/*/env.yaml` (новая ось), `tools/` (детектор раскладки, реестр снятых
редакций), **один файл дерева кода** (javadoc `strategies`), пайплайн
(правило кодстайла, карта владельцев, дерево знания, дайджест, хроника,
роадмап).

**Режим — автономия до прод-рубежа.** Решения — Д250-Д1086.

## Что сделано после v159

**Дизайн-проход по форме исполнителя приёма событий закрыт** — отчёт
`.claude/work/progress/phase-2-step-10-design-pass.md` (конструкция,
приземление, регресс — три записанных захода). Закрыты **одиннадцать**
гейтящих находок `DOCS_CHECK_4`, выводимых из одной посылки (A2, A3, A4,
A5, A8, B1, B2, B3, B6, B7, B8), и ни одна — правкой своего носителя:
каждая разрешилась следствием формы.

**Несущее, что проход дал корпусу:** безымянный «потребитель» разведён на
**трёх исполнителей** с тремя компонент-доками — `AuditEventListener`,
`ReceptionStateJob`, `JournalCleanupJob`. Условие возврата Д1074 сработало
буквально, и строка карты владельцев переписана.

Восемь позиций и их доводы — в отчёте прохода; решения машины внутри
прохода — Д1079-Д1086 в `.claude/work/decision-digest.md`.

## Три вещи, которые дальше нельзя пере-решать

**1. Форма исполнителя приёма названа держателем и развёрнута.** Опрос — у
контейнера, не у прикладного кода; величины состава и живости пишет тик
`ReceptionStateJob` и **молчит, когда приём не жив**; отказ продвинуть
смещение обеспечен **обработчиком ошибок**, а умолчание даёт обратное.

**2. У строки состояния приёма два писателя и две новые колонки.**
`lag_gap_at` (момент вместо признака; гасит чистка) и
`last_accepted_occurred_at` (возраст до первого приёма берётся от
`observed_since`). Схемной дельты это не добавляет — таблица ещё не
мигрирована.

**3. Единица работы сессии — одна** (`.claude/rules/session-work-unit.md`),
верификаций у узла не больше двух
(`.claude/decisions/mini-loop-verification-ceiling.md`).

## Вход следующей сессии

**Единица — вторая половина `GAPS_CLOSE_4`:** пять гейтящих находок, из
посылки не выводимых (**A1** — транспортная форма конверта и `tenantId`
заголовком; **A6** — статистика как потребитель событий в одном доме и
не-потребитель в двух; **A7** — состав того, что `CODE` строит на
поверхности чтения; **B4** — радиусные колонки `STRATEGY_ACTIVATED` и
ложный пример однозначности пустоты; **B5** — клейм о независимости
операндов `DealClosed`), плюс негейтящие A9-A12, B9-B12 и остатки линзы C
(C3, C9).

Вход: `.claude/work/progress/phase-2-step-10-docs-check-4.md` и три части
линз. Узлы ведутся под потолком двух верификаций; каждая находка обязана
получить исход — закрыта либо припаркована с домом, оживителем и причиной
(`.claude/rules/parking-address.md`).

## Что стои́т повторить из прошлого захода

Правки применяются скриптом, который сначала сверяет, что каждый блок
«Было» находится в цели ровно один раз (и нормализует CRLF для дерева
кода); скрипты приземления удаляются до `git add`. **Новая ловушка:**
опознавательный шаблон снятой редакции, написанный **именем колонки**,
ловит одноимённый код в другом регистре — свип детектора регистра не
различает; шаблон пишется прозой (Д1086).

## Что висит

1. **Дельта `CODE` шага 10 — 39 живых позиций** (было 32; проход переписал
   9а и 23 и добавил 28-34).
2. **Компонентов шага 10 в `.claude/work/code-gate-ledger.json` нет** — их
   вносит простановка `CODE`, и это пункт гейта перехода.
3. **Позиции парковки узлов 5-6:** корневые build-файлы вне области
   `retired-check.py`; строка раскладки у́же корпусной фактики у владельцев
   со своей базой; справочник площадок обещан шагом 4, а схемы у `auth`
   нет; прирост долга перекрёстий шагом 10.
4. **Цена сессий под правилом единицы не замерена** — замер по журналу
   первого прогона.
5. **События `auth` — производителя и темы нет**; экспорт метрик из семи
   прочих сервисов; таблицы `access_denials` нет ни у одного сервиса.
6. **Единый error-DTO у четырёх построенных сервисов.**
7. **Потерянное событие при неразбираемом содержимом** — DLT нет.
8. **Названные ограничения шага 7**, ревалидация активных определений при
   смене чисел риск-аппетита, `HOLD_RELEASED` не пишется, база риска на
   терминале не двигается.
9. **Внешний вызов внутри транзакции БД — двенадцать точек** у ядра.
10. **Донорские остатки в общей библиотеке** — снимаются удалением донора.
11. **Отчёты критики и верификации узлов 5-6 `GAPS_CLOSE_3` отдельными
    файлами не сохранены** (C3 прогона); C9 закрыта.
12. **Прежние хвосты:** S1 Vault-хардненинг, рост таблицы отказов, состав
    точек чтения `audit-statistics` (шаг 12), api-формы периметра без дока.
13. **Пользователю:** переиндексировать проект в IDEA; обновить Project
    Knowledge (этот снапшот). **Стенд от дерева кода не отстал** — правка
    javadoc поведения не меняет.

## Проверки на момент снапшота

**Одиннадцать команд корпуса прогнаны на этом состоянии — все код 0**
(десять гейта плюс `code-gate-check.py`). Рабочий бинарь — `py`; `python3`
из PATH — заглушка, её код 49 читается как «не измерялось».

Спеки — **28** спецификаций, **682** примера (42 контрпримера), **16**
популяций; мутационный замер — **475** величин, 0 переживших. Области
видимости — 0/0/0/0. Указатели величин — **266**, ведут не в дом 0. Реестр
снятых редакций — **88** записей (четыре новых — все от прохода), **288**
строк популяции, 0 дефектов, 1974 носителя просмотрено. Адреса `.claude/**`
— **807**, новых неразрешимых 0 (долг 54, не растёт). Доковые указатели
тела кода — 1782, битых 0. Раскладка развёртывания — 8 единиц, 3 окружения,
0 расхождений (батарея прогнала новую ось). Предусловия — сошлись. Критерий
выхода в `CODE` — 42 против 42.

**Реактор прогнан** (тронут javadoc `strategies`): **1424 скомпилированных
файла, 957 пройденных тестов, 0 дефектов**.

Долг адресных перекрёстий `docs/**` — **609** по замеру v158; проход добавил
три носителя, замер не повторялся.

**Живого прогона нет ни у одного компонента шагов 7-10.**

## Среда

Против v132 не изменилась; полное описание — там. Ловушки среды из v156-v159
остаются в силе. Новая этой сессией: **длинный heredoc в оболочке рвётся**
(`unexpected EOF while looking for matching`) — большие файлы пишутся
инструментом записи, правки — коротким python-скриптом из файла либо
`py - <<'PYEOF'`.
