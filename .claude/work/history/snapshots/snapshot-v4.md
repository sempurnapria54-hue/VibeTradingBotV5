# Snapshot v4

**Дата:** 2026-05-25.

## На какой вопрос отвечает этот файл

Где мы сейчас и как сюда пришли (срез на дату).

## Состояние

С момента `snapshot-v3.md` (2026-05-23) прошёл разбор двух
открытых вопросов из обкатки на legacy-документе `Жизненный цикл
сделки` — Q1 и Q2. По итогам:

- **Q1** (куда класть инвариант конкретного объекта) — остаётся
  открытым. Из трёх примеров обкатки: B1 закрыт отдельным
  decision (оказался не про инвариант, а про отрицательные
  формулировки); B3 и B7/B10/B12/B13 отложены с зафиксированными
  нюансами — для B3 всплыл третий вариант размещения
  (компонент/процесс), для B7 — чистый случай для файла объекта.
  Решение копится до новых случаев на разных сторонах критерия.
- **Q2** (первоисточник дублированного правила) — отложен. На
  единственном конкретном примере (формула live risk) разбор
  упёрся в отсутствие базовых торговых сущностей в `docs/`. До
  их появления развилка не разбирается предметно.

Принят план: вторая обкатка скилла классификации — на торговых
сущностях из `.claude-archive/2026-05-21/docs/domain`. Запуск —
в новом чате.

Параллельно — обновлены правила поведения в чате
(`chat-project-instructions.md`): правило «один вопрос за раз»
теперь явно работает на любом уровне вложенности; добавлен
механизм «мета-наблюдения про поведение в чате — сразу в
правила» (Claude приостанавливает разбор и предлагает обновление
правила, когда пользователь сообщает наблюдение про устойчивое
поведение); добавлен пункт «промпт для Claude Code не дублирует
`CLAUDE.md`».

## Текущая структура

См. `.claude/rules/structure.md` (без изменений с момента v3).

Кратко — изменения относительно `snapshot-v3.md`:

**Правила:**
- `.claude/rules/chat-project-instructions.md` — три изменения:
  правило «один вопрос за раз» расширено на любую вложенность
  (рекурсивно); добавлен пункт про мета-наблюдения; добавлен
  пункт «промпт для CC не дублирует `CLAUDE.md`».

**Скиллы:**
- `recognize-knowledge.md` — добавлен раздел «Что НЕ является
  кандидатом» с анти-признаком про отрицательные формулировки
  про модели.

**Решения:**
- `.claude/decisions/negative-statements-not-fixated.md` —
  отрицательные формулировки про модели сами по себе не
  фиксируются.

**Открытые вопросы:**
- В Q1 добавлены: пометка о статусе (B1 закрыт отдельным
  решением); два уточнения от 2026-05-25 — про B3 (третий
  уровень — компонент/процесс) и про B7/B10/B12/B13 (чистый
  случай для файла объекта).
- В Q2 добавлен статус (отложен до накопления базовых торговых
  сущностей в `docs/`).

**Продуктовые каталоги:**
- Без изменений. `docs/components/` и остальные `docs/*` —
  заготовки с `.gitkeep`.

**Рабочее:**
- `.claude/work/backlog.md` — пустой.
- Остальные `work/*` каталоги — пустые.

## Активные задачи

Нет.

## Открытые общие вопросы

Q1 и Q2 (см. `.claude/work/questions/open-questions.md`).

- **Q1** — открыт. B1 закрыт отдельным decision. B3 и
  B7/B10/B12/B13 отложены с уточнениями.
- **Q2** — открыт, отложен до появления базовых торговых
  сущностей в `docs/`.

## Что в архиве

`.claude-archive/2026-05-21/`:
- `CLAUDE.md` — старый.
- `.claude/` целиком — старая инфраструктура.
- `docs/` целиком — старая продуктовая документация. Источник
  материала для запланированной второй обкатки —
  `.claude-archive/2026-05-21/docs/domain`.

Брать оттуда что-то — осознанное действие, не автоматическое.

## Что изменилось с предыдущего снапшота

- Разобран B1 из Q1: оформлен отдельный decision
  `negative-statements-not-fixated.md`, в скилл
  `recognize-knowledge.md` добавлен раздел «Что НЕ является
  кандидатом».
- Разобраны B3 и B7/B10/B12/B13 из Q1: оба отложены, в
  `open-questions.md` зафиксированы нюансы каждого (третий
  уровень для B3, чистый случай для B7).
- Разобран Q2: отложен, в `open-questions.md` зафиксирован
  статус и причина (нужны базовые торговые сущности в `docs/`).
- Обновлён `chat-project-instructions.md`: рекурсивность правила
  «один вопрос за раз», пункт про мета-наблюдения, пункт про
  неповторение `CLAUDE.md` в промптах для CC. Custom
  Instructions в Settings → синхронизированы вручную.

## Что в работе

Каркас классификации продолжает обкатываться. Скиллы
`classify-area` и `classify-theme` — по-прежнему каркасы без
содержания; ждут материала.

Запланирована вторая обкатка скилла классификации. Источник
материала — `.claude-archive/2026-05-21/docs/domain`. Порядок:
`Balance.md` → `Position.md` → `Order.md` → `AlgoOrder.md` →
`Deal.md` → `Strategy.md`. Метод — прогон архивных документов
через скиллы классификации; конкретный режим (по одному
документу или по нескольким сразу) решается на месте. Запуск —
в новом чате.

Q1 и Q2 копятся до новых случаев и базовых торговых сущностей
соответственно.
