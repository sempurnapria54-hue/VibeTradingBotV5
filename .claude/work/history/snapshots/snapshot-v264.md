# Snapshot v264

**Дата:** 2026-09-16.

## На какой вопрос отвечает этот файл

Где мы сейчас.

## Состояние

Сменяет v263. Фаза 1 — `FOLDED`. **Фаза 2 — `IN_PROGRESS`**: шаги 1-11
`DONE`, **шаг 12 — `CODE`**, шаг 13 `HOLD`. Прод-рубеж — `HOLD`.

**Машина шага** — `CONCEPT → CONCEPT_REVIEW → CODE → DOCS → REVIEW → DONE`
(`.claude/processes/roadmap-step-execution.md`). `CODE` тестового шага идёт
тремя фиксированными под-шагами: тест-кейсы → адверсариальное ревью кейсов →
код тестов. **Идёт под-шаг 1.**

**Дельта — в рабочем дереве, не закоммичена.** Ветка `claude-audit`.
**Дерево кода эта сессия не трогала** — реактор не прогонялся и не был
нужен (`git status --porcelain -- services/ web/` пуст).

**Режим — автономия до прод-рубежа.** Решения — Д250-Д1617.

## Что сделано после v263

Одна единица: **пятый заход под-шага 1 `CODE` — четвёртый документ кейсов,
чёрный ящик `trading-core`** (`.claude/tests/cases/trading-core.md`). Состав
здесь не пересказывается — дом у него сам документ.

1. **Новая ось формы введена и проверена — объём.** Он режется **единицей
   работы** (тик, вызов поверхности, принятое сообщение), а не единицей
   конструкции: рёбра FSM, неравенства риск-гейта, лестница ступеней и
   арифметика сайзинга отданы уровню 2, ящику осталась оркестрация и
   наблюдаемый исход.
2. **Группы кейсов** — отбор входа, порядок шагов прохода и перехваты,
   жизненный цикл команды и классы отказа, риск-гейт, ступени защиты и их
   идемпотентность, ручная поверхность остановки, проактивная детекция, приём
   фактов определений, outbox и реле, проекции и синки, поверхность чтения,
   контур доступа, конфигурация и схема, отсутствие выходов.
3. **Развилки письма — дайджест Д1612-Д1617.** Несущая: отказ разложен по
   **классам реакции**, и у трёх классов из пяти верный исход — **отсутствие**
   радиусной реакции.
4. **Три находки припарковано секциями бэклога** — тенант на читающих тропах
   ядра операндом не принимается; обещанный `409` на отказе блокировки у
   асинхронного фасада недостижим по построению; половина синка проекций по
   счетам отказ одной строки не терпит.

## Вход следующей сессии

**Следующий документ кейсов — чёрный ящик `strategies`**
(`.claude/tests/cases/strategies.md`; скилл `.claude/skills/test-design.md`,
роль `tester`). Новая ось формы у него — **событие как выход**: снимок
определения.

- Порядок предметов и «чем достаётся выход» —
  `.claude/decisions/test-contour-design-pass.md`.
- Форма документа проверена на четырёх предметах; правке не подвергалась.
- Концепция — `.claude/work/progress/phase-2-step-12-chronicle.md`
  §«Концепция»; два вопроса, чьё закрытие кейс не вправе подменить, —
  `.claude/work/progress/phase-2-step-12-concept-review.md`
  §«Два вопроса, которые кейс может закрепить».
- **Под-шаг 3 не начинается** до апрува кейсов на под-шаге 2.

## Что держателю решать

Против v263 не изменилось:

1. **Источник цены `MARK_PRICE` / `INDEX_PRICE`** — реджект создания либо
   снятие ограничения.
2. **Читатель `exitOutcome`.**
3. **Ключи demo-окружения OKX и распечатанный Vault** — обещаны к
   под-шагу 3 `CODE`; без них **ни один кейс мишени `-D`** чёрного ящика
   коннектора не прогоняется (в пайплайн они и не входят).

Плюс прежние ожидания (Vault-хардненинг, общий ход миграции на Jackson 3,
область тестового токена) — печатает `py tools/backlog-check.py` строкой
`держатель:`.

## Что висит

Всё, что живёт секциями бэклога, печатает `py tools/backlog-check.py`; в
снапшоте не дублируется. Вне бэклога:

1. **Стенд от дерева кода отстал**, базы `audit` и `statistics` не
   пересоздавались (секция бэклога).
2. **Ни одна пара производитель-потребитель не встречалась на живом
   брокере.** У `trading-core` к этому добавилось названное следствие:
   автоконфигурации брокера в дереве нет, и **контекст ящика ядра сегодня не
   поднимается** — группы кейсов приёма и публикации красны целиком, пока
   долг не закрыт (секция бэклога о тропе к брокеру).
3. **Обязанности заведения каталога `tests/`** принадлежат под-шагу 3 и
   названы в решении: строка `structure.md`, `README.md`, шаблон в `ROOTS`
   у `retired-check.py`, путь сноса классов в `reactor-test.sh`, модуль в
   корневом `pom.xml`. Первые два инструмента при пропуске отвечают кодом 2,
   а не молчат.
4. **WireMock под JDK 25 прогоном не проверен** — отказ на подъёме стаба
   закрывается своим стабом на Spring, а не понижением JDK.
5. **Jar'ов Testcontainers и WireMock в локальном репозитории нет** — прайминг
   под-шага 3 (Maven Central достижим, мерено v260).
6. **Записей площадки для мишени `-S` нет ни одной** — кейсы стаба написаны
   от контракта, и первый прогон `-D` обязан их снять
   (`.claude/tests/cases/connector-okx.md` §«Кейсы, не прогоняемые сегодня»).
7. **Пользователю:** обновить Project Knowledge (этот снапшот); прочитать
   блоки дайджеста Д1564-Д1617 — право вето постфактум.

Три больших свипа с измеренным объёмом остаются открытыми: слой
опровержения в `docs/**`, свод адресных перекрёстий `§«`, долг §-адресов
(`tools/anchor-debt.txt`) и процедура синка в доках. Каждый — своя единица;
угаданный адрес даёт класс «ведёт не к тому дому», который не мерит ни один
прогон.

## Ловушки

**Ловушки** v206-v263 остаются в силе. Подтверждённые этой сессией и новые:

- **`py` вместо `python3`**; вывод инструментов рвётся на кириллице без
  `PYTHONIOENCODING=utf-8`; `grep -P` кириллицу в `\w`, `\b`, `\d` не берёт.
- **`§` перед ключом конфигурации — битый адрес** (поймано этой сессией):
  `anchor-check.py` читает `§`spring.kafka`` как адрес пассажа и не разрешает
  его. Ключ конфигурации называется прозой — «`application.yaml`, ключ
  `spring.kafka`».
- **Вложенные кавычки-ёлочки внутри заголовка ломают §-адрес**:
  `anchor-check.py` читает адрес до **первой** `»`. Ссылка на такой заголовок
  пишется прозой.
- **Длинный носитель пишется по частям через временный файл**, а не
  heredoc'ом: bash рвётся на кириллице с кавычками, а `Write` перезаписывает
  файл целиком. Форма — `Write` в `tools/part-*.md`, затем `cat … >> цель` и
  `rm`.
- **Счётный клейм в прозе — дефект формы**, даже когда число верно и
  относится к предмету, а не к себе (`.claude/rules/self-description-form.md`):
  «у предмета семь проходов» рядом с таблицей этих проходов — то самое
  число рядом с перечнем, который оно считает.
- **`|` в маркере бэклога делит условия, а не альтернацию регекса**; предмет,
  не выразимый одним шаблоном, остаётся **без** `закрыто-когда`, и это
  объявляется в теле секции.
- **`закрыто-когда` проверяется на действующем дереве сразу**: шаблон,
  который уже находит попадание, даёт «ПРЕДМЕТ ПОСТРОЕН» и роняет прогон.
- **`py` не понимает bash-путей `/tmp/…`**: скрипт-временник кладётся в
  `tools/` и удаляется тем же ходом, либо путь переводится `cygpath -w`.
- **`py -c "…"` в bash съедает обратные кавычки как подстановку команды.**
- **`python -` с heredoc в этой среде открывает REPL и скрипта не
  исполняет** — `py - <<'PY'` работает.
- **`Write` создаёт файл с LF**, а рабочее дерево — CRLF: у документов кейсов
  принят LF (`core.autocrlf=input` нормализует).

Обязательное перед завершением любой единицы:

1. **гейт инструментов корпуса** целиком — перечень в
   `.claude/skills/update-roadmap-progress.md` §«Гейт инструментов корпуса»,
   `backlog-check.py` с подменой статуса, если статус проставляется;
2. **реактор — `bash tools/reactor-test.sh`**, если тронуто дерево кода.

## Проверки на момент снапшота

**Гейт инструментов корпуса прогнан** целиком, все пятнадцать команд:
ненулевых кодов нет, кода 2 нет. Подмены статуса не требовалось — статус
шага не менялся. **Реактор не прогонялся и не требовался** — дерева кода
сессия не касалась.

## Среда

Против v132 не изменилась. Дом фактов среды — команды сборки, пути JDK и
Maven, лаунчер Python, Vault и demo-ключ —
`.claude/skills/environment-commands.md`. Сборка: JDK `corretto-25.0.3`,
maven из дистрибутива IDEA (`REACTOR_MVN`), в `PATH` его нет. Состав
локального репозитория по тестовым библиотекам — п. 5 §«Что висит».
