# Snapshot v207

**Дата:** 2026-09-10.

## На какой вопрос отвечает этот файл

Где мы сейчас. **Шаг 10 фазы 2 (аудит и статистика) в `CODE`: сделан
двенадцатый кодовый заход — составы форм ядра, актор и факт остановки. Все
тринадцать компонентов закодированы; впереди аппрув фокусами,
`SYNC_DOCS_FROM_CODE` и пост-хок концепт-гейт.** Сменяет v206.

## Состояние

Фаза 1 — `FOLDED`. **Фаза 2 — `IN_PROGRESS`**: шаги 1-9 `DONE`, шаг 10 — **`CODE`**,
шаги 11-12 `HOLD`.

**Гейтящих выносов держателю нет.** Эта единица вопроса не выносила;
`.claude/work/questions/open-questions.md` ею не пополнялся.

**Дельта staged, не закоммичена.** Ветка `claude-audit`. **Дерево кода
тронуто:** общий артефакт получил класс `DealShutdownInitiated` и составы пяти
форм ядра; `trading-core` — поставщика актора, перенос контекста в тред фасада,
звено «ребро + факт», маршрут ручной постановки через общего исполнителя, три
проекции идентичности; `bff` — форму нового класса. Правлены
`docs/architecture/contracts.md`, `tenant-and-exchange.md`, `Auditable.md`,
`AuditRecord.md`, `statistics-aggregates.md`, `manual-halt.md`,
`components/HoldService.md`, спека присутствия актора, реестр компонентов,
хроника и дайджест.

**Режим — автономия до прод-рубежа.** Решения — Д250-Д1340; эта единица завела
одиннадцать: Д1330-Д1340.

## Что сделано после v206

**Единица сессии — один кодовый заход** по компоненту ядра вместе с остатком
компонента общих форм. Отчёт —
`.claude/work/progress/phase-2-step-10-code-pass-k12.md`; статус — в
`phase-2-step-10-chronicle.md` §Статус и в реестре компонентов.

- **Терминальное событие стало отчётным:** оно везёт операнды агрегата и все
  четыре признака отбора. До хода сделочное зерно **собиралось ложным** —
  строка выходила правдоподобной, а считался в ней один счётчик.
- **Содержимое собирает фабрика формы** (Д1330): у терминала писателей два, и
  два списка из восемнадцати компонентов разошлись бы молча.
- **Пустое едет пустым — формой, а не аккуратностью вызова** (Д1331): числа
  переведены в числовые типы, пяти обёрток `String.valueOf` больше нет.
- **Класс `DealShutdownInitiated` построен, и пишет его звено, а не джоба**
  (Д1333): оба ребра присвоения причины идут через `DealStatusEdgeService` —
  ребро и факт одной транзакцией.
- **Ручная постановка пошла через общего исполнителя обеими ветвями** (Д1337);
  мимо осталась одна — доведение недоделанного, и события она не производит.
- **Актор появился у классов ядра**, литерал класса контура стал `system`
  (Д1335), контекст переживает асинхронный фасад.
- **Предъявлено прогоном:** девятнадцать мутационных проб красны; три новых
  тестовых файла у писателей; реактор — 0 (1574 файла, 1218 тестов).

## Три вещи, которые дальше нельзя пере-решать

**1. Перечень исполнителей не переоткрывается** (v195). Новый исполнитель
вносится **строкой** реестра, а не поводом пересобрать перечень.

**2. Реестр компонентов сквозной и новым шагом не пересобирается** (Д1184).
Находки в нём — прогона `DOCS_CHECK_34`; гейт срабатывает **на компоненте**,
когда тот начинается.

**3. Имена ключей содержимого зафиксированы читателем** (Д1263) и повторены
дословно; сверка — `CoreEventFormTest`. Пере-называть их нельзя: расхождение
не ловится ничем — величина выйдет пустой, а строка агрегата правдоподобной.

## Вход следующей сессии

**Единица — аппрув `CODE` фокусами** (`conventions` / `performance` /
`disaster` / `security`) по
`.claude/processes/roadmap-step-execution.md`. Кодирование шага закончено: в
реестре компонентов у всех тринадцати строк шага статус `закодирован`.
Дальше по процессу — `SYNC_DOCS_FROM_CODE` и пост-хок концепт-гейт, каждый
своей единицей.

Обязательное перед завершением любой такой единицы:

1. **гейт инструментов корпуса** целиком;
2. **сборка — `bash tools/reactor-test.sh`**, если тронуто дерево кода;
   точечный прогон одного модуля — `mvn -o -am -pl services/<модуль> test` из
   корня. **`-am` обязателен**: общий артефакт в локальном репозитории не
   установлен. `mvn` на `PATH` нет — рабочая форма вызова в отчёте
   `phase-2-step-10-code-pass-k12.md`;
3. **`py tools/code-gate-check.py`** — перед каждым кодовым заходом, если он
   будет.

## Что висит

1. **Класс `HoldReleased` не построен** — ручное снятие ступени фиксируется
   строкой отчёта, а не классом снятия (`.claude/work/backlog.md`).
2. **Эпизод транша событием не различается** — счётчика эпизода нет и у самой
   заявки; ограничение модели ядра, названо у дома состава.
3. **Строки журнала, накопленные до хода, не чинятся:** операнды агрегата
   невосстановимы задним числом.
4. **Таблицы и писателя строки отказа у четырёх соседей нет** (`auth`,
   `trading-core`, `strategies`, `market-data`) — общий долг
   (`.claude/work/backlog.md`).
5. **Экспорт метрик у семи сервисов из восьми не построен**; экспорт
   **трейсов** не построен ни у кого.
6. **Подъём контекста не проверен ни у `audit-statistics`, ни у `strategies`,
   ни у ядра в части `AsyncConfigurer`:** `@SpringBootTest` у модулей нет
   намеренно.
7. **Транзакционная граница пары «ребро + факт» прогоном не мерится** — держит
   объявление метода, проверяет каркас.
8. **Отбор ресурсов наблюдателем по метке `release` живым кластером не
   проверен**; оживитель — подъём стенда на дереве кода.
9. **Дубль запроса `earliestRecordedAt` у двух подключений остался** (Д1297).
10. **Тропа к брокеру у трёх построенных соседей не объявлена ни сетевым
    правилом, ни автоконфигурацией** (`.claude/work/backlog.md`).
11. **Позиции парковки узлов 5-6 `GAPS_CLOSE_3`** и парковка узла 1
    (исполнимость формы ценового результата на строках агрегатов).
12. **Кандидат энфорсера окрестности** — бэклог.
13. **Стенд от дерева кода отстал:** образы `audit-statistics`, `strategies` и
    `trading-core` с этими правками не собирались, роли и базы на стенде не
    заводились, тем и подписки на стенде нет. Плюс прежнее — образ сервиса без
    продвинутого тега резолвится подвижным `latest`.
14. **Форма дедупа «спросить и вставить»** — шесть точек трёх сервисов.
15. **Три долга инструментов:** проза реестра
    (`tools/retired-prose-debt.txt`), адресные перекрёстья `docs/**`, долг
    носителей команд (`tools/command-carrier-debt.txt`).
16. **§-адрес javadoc в несуществующий пассаж** — класс припаркован в
    `.claude/work/backlog.md`; энфорсера у класса по-прежнему нет.
17. **Цена сессий под правилом единицы не замерена.**
18. **События `auth` — производителя и темы нет.**
19. **Единый error-DTO у четырёх построенных сервисов** (`auth`,
    `connector-okx`, `market-data`, `trading-core`).
20. **Потерянное событие при неразбираемом содержимом** — DLT нет.
21. **Названные ограничения шага 7**, ревалидация активных определений при
    смене чисел риск-аппетита, `HOLD_RELEASED` не пишется, база риска на
    терминале не двигается.
22. **Внешний вызов внутри транзакции БД — двенадцать точек** у ядра.
23. **Донорские остатки в общей библиотеке** — снимаются удалением донора.
24. **Отчёты критики и верификации узлов 5-6 `GAPS_CLOSE_3` отдельными файлами
    не сохранены.**
25. **Т-1…Т-3 седьмого прогона гейта `GAPS_CLOSE_4`; Ц-2…Ц-4 первого и Д-1…Д-4
    второго прогона гейта `GAPS_CLOSE_5`** — все о тексте о работе; уезжают с
    отчётами в `history/` при закрытии шага.
26. **Прежние хвосты:** S1 Vault-хардненинг, рост таблицы отказов, api-формы
    периметра без дока.
27. **Пользователю:** переиндексировать проект в IDEA; обновить Project
    Knowledge (этот снапшот).

## Проверки на момент снапшота

**Гейт инструментов корпуса прогнан** на состоянии после всех правок, включая
этот файл, — весь перечень, ненулевых кодов нет, кода 2 нет. Пофайловая
таблица исходов — `.claude/work/progress/phase-2-step-10-code-pass-k12.md`
§«Исход гейта инструментов». Числа прогонов здесь не пересказываются: их
печатает сам прогон (`.claude/rules/self-description-form.md`).

**Реактор прогнан: дерево кода тронуто.** `bash tools/reactor-test.sh` — 0,
компиляция всех деревьев и весь unit-набор.

**Мутационная батарея захода — девятнадцать проб, все красны.** Живой базы,
живого брокера и живого стенда при этом не было: состав строки outbox и тело
содержимого сверены прогоном у писателя, а не наблюдением на проводе.

## Среда

Против v132 не изменилась; полное описание — там, перечень ловушек — в v206
(`.claude/work/history/snapshots/snapshot-v206.md` §Среда) и он остаётся в
силе целиком.

**Ловушки этой сессии.**

- **Мутационная батарея идёт минуты, и во время её прогона главное дерево
  ЛЕЖИТ МУТИРОВАННЫМ.** Правки того же дерева в это время затираются
  восстановлением; править параллельно можно только файлы, которых батарея не
  трогает.
- **Проба, чья фикстура даёт то же значение, что и подмена, мутации не
  ловит.** Мутация «автор строки — литерал контура» на ходе без принципала
  неотличима от верной; и она же вскрыла второе: якорь подмены встречался в
  файле дважды, замена легла на непокрытую точку, и проба вышла зелёной не
  потому, что код верен.
