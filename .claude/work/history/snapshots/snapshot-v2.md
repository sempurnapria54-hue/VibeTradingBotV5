# Snapshot v2

**Дата:** 2026-05-23.

## На какой вопрос отвечает этот файл

Где мы сейчас и как сюда пришли (срез на дату).

## Состояние

С момента `snapshot-v1.md` (2026-05-21) пайплайн обогащён
методологическим каркасом фиксации знания. Введён процесс
классификации из пяти шагов с сквозными операциями (постановка
вопроса + фильтр), набор атомарных скиллов на каждый шаг, и
роль knowledge-curator как оркестратор.

Принципы из `structure.md`:
- «Один файл = одна сущность» смягчено до «Один файл = одна
  тема» — допустимы связные группы по теме (пример:
  `naming.md`).
- Введён принцип «Скиллы самодостаточны» — скиллы содержат
  внутри себя знание, нужное для работы; не ссылаются на
  справочники и друг на друга; зависимости направлены сверху
  вниз (оркестратор → инструменты).

## Текущая структура

См. `.claude/rules/structure.md` (актуально на момент
снапшота).

Кратко — реальное содержание:

**Правила:**
- `CLAUDE.md` — базовые правила проекта.
- `.claude/rules/structure.md` — размещение знания, принципы.
- `.claude/rules/naming.md` — именование файлов.
- `.claude/rules/chat-project-instructions.md` — поведение
  Claude в чате claude.ai (резервная копия Custom
  Instructions).

**Процессы:**
- `.claude/processes/knowledge-classification.md` — каркас
  фиксации знания (5 шагов + сквозные операции + эволюция
  через накопление).

**Скиллы:**
- `.claude/skills/recognize-knowledge.md` — шаг 1,
  распознавание кандидата на фиксацию.
- `.claude/skills/classify-area.md` — шаг 2, определение
  области (каркас в обкатке).
- `.claude/skills/classify-type.md` — шаг 3, определение типа
  через вопрос (бывший `formulate-question.md`).
- `.claude/skills/classify-theme.md` — шаг 4, определение
  темы (каркас в обкатке).
- `.claude/skills/place-knowledge.md` — шаг 5, размещение.

**Агенты:**
- `.claude/agents/knowledge-curator.md` — оркестратор процесса
  фиксации знания.

**Решения:**
- `.claude/decisions/knowledge-classification.md` — обоснование
  каркаса классификации и атомарных скиллов.

**Рабочее:**
- `.claude/work/backlog.md`,
  `.claude/work/questions/open-questions.md` — пустые
  заготовки.
- Остальные каталоги (`notes/`, `templates/`,
  `work/progress/`, `work/questions/tasks/`, `work/history/`,
  продуктовые `docs/*`) пустые, помечены `.gitkeep`.

## Активные задачи

Нет.

## Открытые общие вопросы

Нет.

## Что в архиве

`.claude-archive/2026-05-21/`:
- `CLAUDE.md` — старый.
- `.claude/` целиком — старая инфраструктура
  (working-with-claude, playbook, adr, skills, agents,
  strategy-summary, notes, и т. п.).
- `docs/` целиком — старая продуктовая документация.

Брать оттуда что-то — осознанное действие, не автоматическое.

## Что изменилось с предыдущего снапшота

- Спроектирован и зафиксирован каркас классификации знания
  (5 шагов + сквозные операции + эволюция).
- Создан процесс `knowledge-classification.md`.
- Создано первое decision (`knowledge-classification.md`),
  введено понятие decision как обоснования.
- Создана роль `knowledge-curator` как оркестратор.
- Скиллы перестроены под каркас: распознавание → область →
  тип → тема → размещение. Шаги 1, 3, 5 — содержательные
  скиллы; шаги 2, 4 — каркасы в обкатке.
- Введён каталог `.claude/processes/`.
- Смягчён принцип «один файл = одна сущность» до «одна
  тема».
- Введён принцип самодостаточности скиллов.
- В `chat-project-instructions.md` добавлено правило: промпт
  для Claude Code отдаётся файлом-артефактом.

## Что в работе

Каркас классификации сделан, но не обкатан. Скиллы
`classify-area` и `classify-theme` — каркасы без содержания,
будут наполняться по мере появления реальных случаев и
накопления открытых вопросов. Это сознательная установка из
decision `knowledge-classification.md`.
