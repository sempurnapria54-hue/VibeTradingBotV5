# Snapshot v215

**Дата:** 2026-09-10.

## На какой вопрос отвечает этот файл

Где мы сейчас. **Шаг 10 фазы 2 (аудит и статистика) в `CODE`: находка Ф5
закрыта заходом K16, аппрув выдаёт пятый прогон фокусов.** Сменяет v214.

## Состояние

Фаза 1 — `FOLDED`. **Фаза 2 — `IN_PROGRESS`**: шаги 1-9 `DONE`, шаг 10 — **`CODE`**,
шаги 11-12 `HOLD`.

**Гейтящих выносов держателю нет.** Эта единица вопроса не выносила;
`.claude/work/questions/open-questions.md` ею не пополнялся.

**Дельта staged, не закоммичена.** Ветка `claude-audit`. **Дерево кода этой
единицей тронуто:** заведён модуль `libs/test-support`, правлены конфигурация
и сущность `trading-core`, три пробы.

**Режим — автономия до прод-рубежа.** Решения — Д250-Д1357.

## Что сделано после v214

**Единица сессии — кодовый заход K16:** закрытие единственной гейтящей находки
четвёртого прогона фокусов. Отчёт —
`.claude/work/progress/phase-2-step-10-code-pass-k16.md`; статус — в
`phase-2-step-10-chronicle.md` §Статус.

- **Ф5 закрыта вариантом (а).** Признак измерителя в доме
  (`.claude/rules/codestyle.md` §Джобы) **уточнён парой механических
  половин**: выход тика есть измерение о ЧУЖОМ состоянии, и голодание тика
  это измерение подменяет. Прежняя редакция (приложение через
  экспортируемый ряд) снята — она делала применимость правила свойством
  инфраструктуры наблюдения.
- **Дом исполнен у ядра:** `spring.task.scheduling.pool.size` = 7, охрана —
  проба вместимости; плюс объявлен пул подключений (одновременных писателей
  стало восемь).
- **Пары семи джоб пройдены**, исход каждой из двадцати одной назван. Одна
  была **не разведена** — синк проекций против торговых колонок строки
  счёта; разведена `@DynamicUpdate` на `ExchangeAccountEntity`.
- **Форма пробы вынесена в новый общий артефакт `libs/test-support`** по
  сработавшему условию Д1350 (третий сервис-измеритель); дельта захода —
  минус 132 строки.
- **Остаток назван и припаркован:** предел слепоты считает только
  СОСТОЯВШИЕСЯ проходы, а тропа входа возраста наблюдения не спрашивает.

## Три вещи, которые дальше нельзя пере-решать

**1. Перечень исполнителей не переоткрывается** (v195). Новый исполнитель
вносится **строкой** реестра, а не поводом пересобрать перечень.

**2. Реестр компонентов сквозной и новым шагом не пересобирается** (Д1184).
Находки в нём — прогона `DOCS_CHECK_34`; гейт срабатывает **на компоненте**,
когда тот начинается.

**3. Имена ключей содержимого зафиксированы читателем** (Д1263) и повторены
дословно; сверка — `CoreEventFormTest`. Пере-называть их нельзя: расхождение
не ловится ничем — величина выйдет пустой, а строка агрегата правдоподобной.

## Вход следующей сессии

**Единица — пятый прогон фокусов** (`conventions` / `performance` /
`disaster` / `security`) по дельте K16, и ведёт его сессия, K16 не писавшая.
**Аппрув выдаёт прогон, а не закрытие**
(`.claude/processes/roadmap-step-execution.md` §«Аппрув `CODE` —
зафиксированный исход фокусов, не устное согласие»).

Что прогону стои́т перепроверить **по предмету, а не по отчёту**: пары джоб
ядра (двадцать одна клетка, из них механически мерится одна — A × P), число
7 против дерева, форма общего артефакта и то, что подклассы действительно
исполняются (у каждой пробы три теста, было два).

Порядок дальше: пятый прогон фокусов → `SYNC_DOCS_FROM_CODE`
(фокус `divergence`) → пост-хок концепт-гейт
(`.claude/processes/roadmap-step-execution.md` §6a). Каждый — своей единицей.

Обязательное перед завершением любой такой единицы:

1. **гейт инструментов корпуса** целиком;
2. **сборка — `bash tools/reactor-test.sh`**, если тронуто дерево кода;
   точечный прогон одного модуля — `mvn -o -am -pl services/<модуль> test` из
   корня. **`-am` обязателен**: общий артефакт в локальном репозитории не
   установлен. `mvn` на `PATH` нет — рабочая форма вызова стои́т в §Прогоны
   отчёта `.claude/work/progress/phase-2-step-10-code-pass-k13.md`;
3. **`py tools/code-gate-check.py`** — перед каждым кодовым заходом.

## Что висит

1. **Разведение пар джоб ядра ПРОЧИТАНО, а не измерено** — живого прогона
   ядра на базе нет ни одного; механически мерится одна клетка из двадцати
   одной.
2. **Слепота считается состоявшимися проходами** — тропа входа возраста
   наблюдения не спрашивает; припарковано с оживителем.
3. **Класс `HoldReleased` не построен** — ручное снятие ступени фиксируется
   строкой отчёта, а не классом снятия (`.claude/work/backlog.md`).
4. **Эпизод транша событием не различается** — счётчика эпизода нет и у самой
   заявки; ограничение модели ядра, названо у дома состава.
5. **Строки журнала, накопленные до хода, не чинятся:** операнды агрегата
   невосстановимы задним числом.
6. **Таблицы и писателя строки отказа у четырёх соседей нет** (`auth`,
   `trading-core`, `strategies`, `market-data`) — общий долг
   (`.claude/work/backlog.md`).
7. **Перехватчик `Exception` съедает отказ по правам у `bff` и `strategies`**
   — тот же класс, что закрытая S1; припаркован.
8. **Предикат актора написан трижды** — со-расположение названо у всех трёх,
   единый носитель припаркован.
9. **Глубины у `access_denials` нет ни у одного сервиса** — величина выводится
   из той же наблюдённой частоты, что и порог реакции на серию отказов.
10. **Пул планировщика у `market-data` — один поток**, и дом ему ничего не
    предписывает: измерителя среди пяти его джоб нет (перебор оси A популяции
    K16).
11. **Остаток пары «чистка × пересчёт»** у `audit-statistics`: удаление,
    зафиксированное между чтением операнда и группировкой тех же суток, даёт
    частичную строку; цена принята и названа у дома отбора.
12. **Экспорт метрик у семи сервисов из восьми не построен**; экспорт
    **трейсов** не построен ни у кого.
13. **Подъём контекста не проверен ни у `audit-statistics`, ни у `strategies`,
    ни у `bff`, ни у ядра в части `AsyncConfigurer`:** `@SpringBootTest` у
    модулей нет намеренно.
14. **Транзакционная граница пары «ребро + факт» прогоном не мерится** —
    держит объявление метода, проверяет каркас.
15. **Ни план запроса, ни порядок взятия строчных замков у модуля приёма не
    мерятся ничем** — живого прогона у него нет ни одного; обе позиции
    припаркованы одной секцией с общим оживителем.
16. **Отбор ресурсов наблюдателем по метке `release` живым кластером не
    проверен**; оживитель — подъём стенда на дереве кода.
17. **Дубль запроса `earliestRecordedAt` у двух подключений остался** (Д1297).
18. **Позиции парковки узлов 5-6 `GAPS_CLOSE_3`** и парковка узла 1
    (исполнимость формы ценового результата на строках агрегатов).
19. **Кандидат энфорсера окрестности** — бэклог.
20. **Стенд от дерева кода отстал:** образы `audit-statistics`, `strategies`,
    `trading-core` и `bff` с этими правками не собирались, роли и базы на
    стенде не заводились, тем и подписки на стенде нет. Плюс прежнее — образ
    сервиса без продвинутого тега резолвится подвижным `latest`.
21. **Форма дедупа «спросить и вставить»** — шесть точек трёх сервисов.
22. **Три долга инструментов:** проза реестра
    (`tools/retired-prose-debt.txt`), адресные перекрёстья `docs/**`, долг
    носителей команд (`tools/command-carrier-debt.txt`).
23. **§-адрес javadoc в несуществующий пассаж** — класс припаркован; энфорсера
    у класса по-прежнему нет.
24. **Цена сессий под правилом единицы не замерена.**
25. **События `auth` — производителя и темы нет.**
26. **Единый error-DTO у четырёх построенных сервисов** (`auth`,
    `connector-okx`, `market-data`, `trading-core`).
27. **Потерянное событие при неразбираемом содержимом** — DLT нет.
28. **Названные ограничения шага 7**, ревалидация активных определений при
    смене чисел риск-аппетита, `HOLD_RELEASED` не пишется, база риска на
    терминале не двигается.
29. **Внешний вызов внутри транзакции БД — двенадцать точек** у ядра.
30. **Донорские остатки в общей библиотеке** — снимаются удалением донора.
31. **Отчёты критики и верификации узлов 5-6 `GAPS_CLOSE_3` отдельными файлами
    не сохранены.**
32. **Т-1…Т-3 седьмого прогона гейта `GAPS_CLOSE_4`; Ц-2…Ц-4 первого и Д-1…Д-4
    второго прогона гейта `GAPS_CLOSE_5`** — все о тексте о работе; уезжают с
    отчётами в `history/` при закрытии шага.
33. **Прежние хвосты:** S1 Vault-хардненинг, api-формы периметра без дока.
34. **Пользователю:** переиндексировать проект в IDEA (заведён новый
    Maven-модуль `libs/test-support`); обновить Project Knowledge (этот
    снапшот).

## Проверки на момент снапшота

**Гейт инструментов корпуса прогнан** на состоянии после всех правок, включая
этот файл, — весь перечень, ненулевых кодов нет, кода 2 нет. Числа прогонов
здесь не пересказываются: их печатает сам прогон
(`.claude/rules/self-description-form.md`).

**Реактор прогнан** (`bash tools/reactor-test.sh`) — зелёный на дельте K16,
включая новый модуль реактора.

## Среда

Против v132 не изменилась; полное описание — там, перечень ловушек — в v206
(`.claude/work/history/snapshots/snapshot-v206.md` §Среда) и он остаётся в
силе целиком. Ловушки v207-v214 тоже в силе.

**Ловушка этой сессии — одна:** `python3` в этой среде разрешается в заглушку
Windows Store и падает кодом 49, ничего не исполнив. Рабочая форма — `py`; в
отчётах и шапках инструментов при этом стои́т `python3`, и это расхождение
носителя со средой, а не дефект команды.
