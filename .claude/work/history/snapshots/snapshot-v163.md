# Snapshot v163

**Дата:** 2026-09-08.

## На какой вопрос отвечает этот файл

Где мы сейчас. **Шаг 10 фазы 2 (аудит и статистика): `GAPS_CLOSE_4`, третья
единица — проход по механической проверке самоописания правки — ЗАКРЫТА.
Следующая единица — приземление узла 1.** Сменяет v162.

## Состояние

Фаза 1 — `FOLDED`. **Фаза 2 — `IN_PROGRESS`**: шаги 1-9 `DONE`,
шаг 10 — **`GAPS_CLOSE_4`**, шаги 11-12 `HOLD`.

**Гейтящих выносов нет.** Из открытых вопросов — только негейтящие
(`PROC-Q2`, `PROC-Q3`, продуктовые).

**Дельта в working tree, не в истории.** Ветка `claude-audit`; всё staged,
не закоммичено. Сверх v162 тронуты: два новых носителя пайплайна (правило
`.claude/rules/self-description-form.md`, энфорсер
`tools/self-count-check.py`), новый файл долга
`tools/retired-prose-debt.txt`, отчёт прохода в `progress/`, плюс правки в
`retired-check.py`, гейте статуса, правиле единицы работы, `structure.md`,
дереве знания, бэклоге, хронике, `gaps-close-4.md`, отчёте дизайн-прохода,
дайджесте (Д1100-Д1107). **Дерево кода не тронуто** — как и в v162.

**Режим — автономия до прод-рубежа.** Решения — Д250-Д1107.

## Что сделано после v162

**Единица сессии — цельный проход по самоописанию правки, выбранный
ответом держателя `PROC-Q8` = (а).** Отчёт —
`.claude/work/progress/phase-2-step-10-self-description-pass.md` (три захода:
конструкция, приземление, регресс — все записаны).

**1. Ось «измеритель против снятия» закрыта признаком деления, а не выбором
стороны** (Д1100): *число, у которого есть механический вывод, живёт
порождённым блоком; число без механического вывода не пишется.* Чистый
измеритель потребовал бы реестра клеймов — а он сам есть самоописание и
стареет тем же механизмом; чистое снятие убрало бы два **несущих** счёта
(диспозиция находок, число верификаций как операнд С5).

**2. Заведён дом правила** — `.claude/rules/self-description-form.md`: две
законные формы клейма, таблица «что чем закрывается», граница датированного
замера (снапшот и отчёт закрытого узла описывают прошлое и под правило не
попадают), названная цена.

**3. Заведён энфорсер** — `tools/self-count-check.py`: три генератора
(`rows:`, `sum:`, `passes:`), четыре оси, батарея из девяти проб той же
командой. В `tools/retired-check.py` введена **ось 8** (проза записи не
пересказывает пришедшую редакцию) с объявленным долгом
`tools/retired-prose-debt.txt` и осями батареи 24-25. Новая команда внесена
в **гейт инструментов корпуса**.

**4. Две находки узла 1 закрыты — W1 и W8.** Пересказ пришедшей редакции
снят с обеих записей узла (на одной он и соврал — это W1, из-за которой узел
остановлен сигналом С5); ложное «свыше сорока» в парковке бэклога заменено
указателем на команду. Обе — самоописание, а не предмет узла: **предмет узла
1 (провод) в корпус по-прежнему не приземлён.**

## Три вещи, которые дальше нельзя пере-решать

**1. Приземление узла 1 — следующая единица, и его конструкция верна.**
Решения узла Д1087-Д1093 в силе и входят вместе с приземлением; ветвь
полноты графа едет **признаком** в форме события, а не пустотой слагаемого
(Д1090) — возврат к пустоте есть регресс.

**2. Признак деления самоописания не переоткрывается.** Ни «мерить всё»
(реестр клеймов стареет тем же механизмом), ни «снять всё» (уходит проверка
диспозиции и операнд С5). Отчёты закрытых узлов и `history/` под правило не
попадают — они описывают прошлое.

**3. Долг прозы реестра — 57 записей — гасится не отдельным ходом.** Он
уходит вместе со снятием записей при закрытии шага; прогон падает на **новой**
записи с прозой и на строке долга без записи. Дом задачи —
`.claude/work/backlog.md` §«Проза пришедшей редакции в записях реестра снятых
редакций».

## Вход следующей сессии

**Единица — приземление узла 1 `GAPS_CLOSE_4`** (A1, A6, B4, B5; предмет —
провод: чем едет конверт, что едет содержимым, кто читает). Правки лежат в
рабочем дереве; в таблице неприземлённых дефектов осталось **две** строки —
W2 (ложный §-адрес в `docs/architecture/contracts.md`) и W3 (ложная нота
операнда в спеке агрегатов; величина названа в перечне дефектов узла, а
здесь не повторяется — указатель на неё вёл бы не в её дом), обе — дефекты
**предмета**. Перечень — `phase-2-step-10-gaps-close-4.md` §«Эскалация:
концептуальный дефицит (сигнал С5, узел 1)», п. 2.

**Порядок дальше:** приземление узла 1 → узлы 2-5 (раскладка находок —
`phase-2-step-10-gaps-close-4.md`). У A7 (узел 2) своя половина дефицита
`подтверждение` — продуктовый scope поверхности чтения; её придётся выносить
отдельно.

**Повторная эскалация С5 по узлу 1 обязана назвать, что изменил проход и
почему не снял** (`.claude/decisions/edit-self-description-pass.md`
§«Сброс детектора»).

## Что висит

1. **Узел 1 не приземлён; два названных дефекта предмета в его правках** —
   W2 и W3 (см. выше).
2. **Дельта `CODE` шага 10 — 40 живых позиций**; счёт теперь стои́т
   порождённым блоком в хронике, а не прозой.
3. **Компонентов шага 10 в `.claude/work/code-gate-ledger.json` нет** — их
   вносит простановка `CODE`.
4. **Позиции парковки узлов 5-6 `GAPS_CLOSE_3`** и парковка узла 1
   (исполнимость формы ценового результата на строках агрегатов —
   `.claude/work/backlog.md`).
5. **Долг прозы реестра — 57 записей** (`tools/retired-prose-debt.txt`).
6. **Цена сессий под правилом единицы не замерена** — замер по журналу
   первого прогона.
7. **События `auth` — производителя и темы нет**; экспорт метрик из семи
   прочих сервисов; таблицы `access_denials` нет ни у одного сервиса.
8. **Единый error-DTO у четырёх построенных сервисов.**
9. **Потерянное событие при неразбираемом содержимом** — DLT нет.
10. **Названные ограничения шага 7**, ревалидация активных определений при
    смене чисел риск-аппетита, `HOLD_RELEASED` не пишется, база риска на
    терминале не двигается.
11. **Внешний вызов внутри транзакции БД — двенадцать точек** у ядра.
12. **Донорские остатки в общей библиотеке** — снимаются удалением донора.
13. **Отчёты критики и верификации узлов 5-6 `GAPS_CLOSE_3` отдельными
    файлами не сохранены** (C3 прогона `DOCS_CHECK_4`, узел 5
    `GAPS_CLOSE_4`). Побочно замечено проходом: генератор `passes:` на таких
    узлах вывести число заходов не может — отсутствие отчётов стало видимым.
14. **Прежние хвосты:** S1 Vault-хардненинг, рост таблицы отказов, состав
    точек чтения `audit-statistics` (шаг 12), api-формы периметра без дока.
15. **Пользователю:** переиндексировать проект в IDEA; обновить Project
    Knowledge (этот снапшот). **Стенд от дерева кода не отстал** — код не
    тронут.

## Проверки на момент снапшота

**Одиннадцать команд корпуса прогнаны на этом состоянии — все код 0**
(десять прежних плюс новая `self-count-check.py`). Рабочий бинарь — `py`;
`python3` из PATH — заглушка, её код 49 читается как «не измерялось». Гейт
прогонялся **фоновой** командой: в 120 с он не укладывается.

Спеки — **28** спецификаций, **682** примера (42 контрпримера); мутационный
замер — **475** величин, 0 переживших. Области видимости — 0/0/0/0 (59 имён
пропущено по операндному контракту). Указатели величин — **267**, ведут не в
дом 0. Реестр снятых редакций — **90** записей, **292** строки популяции, 0
дефектов, 1978 носителей; проза пришедшей редакции — **57** записей, все в
долге, новых 0. Адреса `.claude/**` — **831**, новых неразрешимых 0, долг 54
и не растёт. Популяции — 16 осей, перечни сошлись. Предусловия — сошлись.
Раскладка развёртывания — 8 единиц, 3 окружения, 0 расхождений. Доковые
указатели тела кода — 1782 при 891 файле, битых 0. Самоописание — **456**
носителей, 3 порождённых блока, 10 итоговых строк (13 колонок пропущено), 17
ячеек состояния, 0 дефектов.

**Реактор не прогонялся: дерево кода не тронуто.** Последний прогон — на
этом же состоянии кода: 1424 файла, 957 тестов, 0 дефектов.

Долг адресных перекрёстий `docs/**` — **609** по замеру v158; замер не
повторялся.

**Живого прогона нет ни у одного компонента шагов 7-10.**

## Среда

Против v132 не изменилась; полное описание — там. Ловушки среды из
v156-v162 остаются в силе, включая рвущийся длинный heredoc (большие файлы
пишутся инструментом записи, правки — коротким python-скриптом из файла) и
**полный гейт инструментов, не укладывающийся в 120 с** — прогонять фоновой
командой. **Замечено этой сессией:** файл, записанный heredoc'ом в `/tmp`,
до следующего вызова Bash не доживает — вспомогательные скрипты пишутся в
корень репозитория и удаляются после прогона.
