# Snapshot v107

**Дата:** 2026-09-03.

## На какой вопрос отвечает этот файл

Где мы сейчас. **Грунт шага 7 добран целиком (открытых слотов ноль),
Т5 и компоненты движений счёта закодированы.** Сменяет v106.

## Состояние

Фаза 1 — `IN_PROGRESS`; шаги 1-6 `DONE`, шаг 7 — **`CODE`**, шаги 8-11
`HOLD`. Ветка `claude-audit`, `HEAD = 534b3be`; **дельта двух суток в
рабочем дереве, staged, не закоммичена** (ревью держателя в IDEA). Вне
коммита — `strategy-examples/` (untracked, парковка D-P6/E-П3).

**Режим — автономия до конца фазы 1**; решения — Д250-Д392 в дайджесте.

## Карта заходов — живой контекст кодовой фазы

| Заход | Компоненты | Статус | Что держит вход |
|---|---|---|---|
| 1 | `DealStateMachine`, `DealTrancheStateMachine`, `DealActionState`, `DealContextService`, `DealOrchestratorJob` | **закодирован** | — |
| 2 | `StrategyActionOrchestrator`, `MarketDataExpirationChecker`, `StrategyValidation` | **в работе** | остаток Т1 (агрегатная поверхность шага) |
| 3 | `TranchePrecheckHandler`, `RiskBlockResolver`, `PriceCalculator`, `SafetyHoldCoordinator` | **в работе** | остаток Т2 (три названных ограничения) |
| 4 | `AttachedAlgoOrderStateResolver`, `CancelAlgoOrderExecutor`, `CreateAlgoOrderExecutor` | **в работе** | остаток Т2 (тропа переключения защиты); Т5 закрыт |
| 5 | `RefreshPositionExecutor`, `DealOpeningService` | **в работе** | хвост Т6, Т7, Т8 |
| 5 | `DealCashFlow` | **закодирован** | — |
| 5 | `RefreshBillsExecutor` | **в работе** | остаток Т9 (эмиттер — с агрегатной поверхностью; конфиг-глубина; дедуп отчёта) |

**Реестр гейтов:** открытых позиций ноль; критерий выхода в `CODE`
проходит. **Реестр предусловий: открытых слотов грунта ноль** — п. 7
закрыт последним (2026-09-03). Хвост кода —
`.claude/work/progress/phase-1-step-7-code-tail.md` (Т1-Т11).

## Что произошло с v105/v106 (2026-09-02 → 03)

Итог и указатели —
`.claude/work/history/2026-09-02-env-revival-ground-batch.md`; решения
Д375-Д392; хроника — `progress/phase-1-step-7-chronicle.md`. Коротко:

- **Среда ожила, срок исполнен оживлением** (исход —
  `.claude/decisions/env-wait-deadline.md` §«Исход решения»; запись об
  отказе в доме среды — снята; ответа по тикету 38426982 не было).
- **Грунт добран весь:** AG6.2 (177 кандидатов), A4а (0 мс, посылка
  подтверждена), AG1.7 — двумя прогонами в funding-окнах (оба знака
  `fundingFee`, доступен сразу после закрытия; сверка
  Σ(`balChg`+`posBalChg`) точная). Свип закрытых позиций грунта
  `integrator` — в history-файле.
- **Новый факт источника:** на isolated-марже расчёт финансирования
  ложится в `posBalChg` при `balChg=0`; поле переведено в used и
  проведено сырым фактом до колонки.
- **Код:** Т5 (постановка `attachAlgoOrds`, эхо `slTriggerPxType`
  непустое в обеих формах), слой `DealCashFlow` (V16, V19), операнды
  окна линковки (V17) и расчётной валюты (V18), `RefreshBillsExecutor`
  целиком (конвейер, категория+журнал, линковка, лестница курса, догон,
  `rateBlocking`).

## Что ратифицировано и не пере-решается

Всё из v104/v105, плюс:

- **Терминальный переход доказывается фактом состояния, не принятием
  запроса** — ровно так среда и была сломана.
- **Равенство меток источника читается подтверждением посылки «не
  раньше»** — предикат окна включающий (Д380).
- **Побочный факт уборки слот не закрывает** — слот закрывается прогоном
  своего кейса; расхождение сверки кейса читается фактом источника
  только после предъявления детерминированного объяснения (Д387).
- **Сырой факт источника едет до колонки без интерпретации** — компоновку
  решает дом-потребитель (Д388, `posBalChg`).

## Чем проверяется корпус

Девять команд плюс мутационная проба; `code-gate-check.py` — ПРОЙДЕН.
**Тесты:** `mvn -o test -DexcludedGroups=source-api-live` → **173, 0
падений**. Приложение профиля `test` поднимается со схемой **V19**;
live-контур исполняется, счёт demo чист (0 позиций, 0 заявок).

## Что висит

1. **Хвост кода Т1-Т11** — ближайшие: заход по числам риска (Т8, Т10;
   **численные пороги — держателя**), агрегатная поверхность (остаток
   Т1 — она же даст эмиттеры `REFRESH_FILLS`/`REFRESH_BILLS`), модель
   объявлений транша (Т6), детектор аномалии (Т7).
2. **Боевые числа риск-аппетита не назначены** — числа держателя;
   `prod` до них не поднимается.
3. **61 указатель javadoc в несуществующие доки** — пункт бэклога.
4. **`.claude/projects/**` вне таблицы размещения** — диспозиция за
   держателем.
5. **Токен Vault корневой и бессрочный** — адресат шага 9.
6. **`excludedGroups` в `pom.xml`** — пункт бэклога.
7. **Пятая ось `preconditions-check.sh`** объявлена и не реализована.
8. **Класс «уборка кейса на ветви отказа — руками»** (`Verify.end` ниже
   упавшего ожидания) — не чинился; предъявлен прогоном 2026-09-02.
9. **Припарковано** — 6 позиций реестра и 44 негейтящие находки `_34`.
10. **Пользователю — обновить Project Knowledge:** этот снапшот, дайджест
    (Д250-Д392).

## Среда

Контур: Docker Desktop поднимается вручную после ребута хоста; Vault
после рестарта контейнера **запечатан** — unseal у держателя; **часы
хоста склонны к рассинхрону** (w32time стоял Manual/Stopped, уход ~3 ч
ломал подпись — ресинк `w32tm /resync` с правами администратора; при
разборе «молчаний» сначала сверять часы с сервером источника). Остальное
— дом фактов среды `.claude/tests/source-api/okx/code-preconditions.md`
(торговая часть демо-стыка работает с 2026-09-02). Токен
`.env.vault.test.local` действует.
