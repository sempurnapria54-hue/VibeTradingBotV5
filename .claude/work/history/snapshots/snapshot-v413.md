# Снапшот v413

**Дата:** 2026-09-23.

## На какой вопрос отвечает этот файл

Где мы сейчас.

## Состояние

Сменяет v412. Фаза 1 — `FOLDED`. **Фаза 2 — `IN_PROGRESS`**: шаги 1-11
`DONE`, **шаг 12 — `CODE·3/3`**, шаг 13 `HOLD`. Прод-рубеж — `HOLD`.

**Машина шага** — `CONCEPT → CONCEPT_REVIEW → CODE → DOCS → REVIEW → DONE`
(`.claude/processes/roadmap-step-execution.md`). `CODE` тестового шага идёт
тремя под-шагами: тест-кейсы → адверсариальное ревью кейсов → код тестов.
**Под-шаги 1 и 2 закрыты; идёт под-шаг 3 — код тестов.** Уровень 2 написан
целиком; **уровень 1 написан у всех восьми сервисов**, кроме хвоста ящика
`trading-core` с чужими блокерами. Сквозного набора (уровень 3) нет.

**Дельта — в рабочем дереве, не закоммичена.** Ветка `claude-audit`.

**Режим — автономия до прод-рубежа.** Решения — Д250-Д2056
(`.claude/work/decision-digest.md`).

## Что сделано после v412

- **Ящик `bff` закрыт целиком** — группы `B4`-`B11` одной сессией, заход 131
  (`.claude/work/progress/phase-2-step-12-chronicle.md` §«Шестьдесят третий
  заход под-шага 3 (131) дописал ящик `bff` — группы `B4`-`B11` — и закрыл
  его»). 93 прогона умолчания зелены, с меткой `debt` — 103, красны ровно
  десять клеток долга. Контрольный прогон — три оси, предсказания сошлись
  дословно.
- **Новая находка `F-14`** — запись в брошенную подписку роняет рассылку и
  повторяет доставку: `.claude/work/backlog.md` §«Запись в брошенную подписку
  роняет рассылку и повторяет доставку». Из-за неё клетки тика пульса
  поднимают свою реплику, а `B3.7`/`B3.8` стали `debt` с повторённым входом.
- **Секция `F-8` поправлена по наблюдённому:** при пустом перечне тем
  контекст периметра не поднимается вовсе.
- **`SchedulerCapacityContract`** (общий `test-support`) открыл два помощника
  счёта `public static` — ими пользуется ящик (Д2053).

## Вход следующей сессии

**Единица — сквозной набор уровня 3; позиция — первый документ тропы,
`.claude/tests/cases/e2e-strategy-to-deal.md`.** Единицы уровня 3 таблица
единиц сессии не называет; по совпадению единицы со сборкой `mvn` это модуль
`tests/` (его ещё нет), приземляемая точка внутри — документ тропы. Назвать
единицу — ход владельца `tester` в начале захода.

- **Форма кода уровня 3** — `.claude/skills/test-code.md` §«Уровень 3 —
  сквозной набор»; развилка обвязки — `.claude/decisions/test-contour-design-pass.md`
  §«12. Сторона сквозной тропы поднимается СВОИМ ПРОЦЕССОМ, а не вторым
  контекстом в той же JVM».
- **Хвост ящика `trading-core`** — `.claude/tests/cases/trading-core.md`
  §«Кейсы, не прогоняемые сегодня».
- **Колонка «Факт» у групп `B1`-`B3` ящика `strategies`** — секция
  `.claude/work/backlog.md` о пустой колонке «Факт» кейсов `strategies` у
  реализованных клеток групп `B1`-`B3`.
- **Хвост апрувленных кейсов `jsonb-overlay-roundtrip`** — там же
  §«Негейтящие находки второго круга кейсов `jsonb-overlay-roundtrip` не
  исполнены»; оживитель — закрытие шага.

## Что держателю решать

1. **Источник цены `MARK_PRICE` / `INDEX_PRICE`** —
   `.claude/work/backlog.md` §«Источник цены `MARK_PRICE` / `INDEX_PRICE`
   не отвергается создáнием».
2. **Читатель `exitOutcome`** — там же §«Журнальные отчёты исходов
   округления выхода — читателя `exitOutcome` в ядре нет».
3. **Ключи demo-окружения OKX и распечатанный Vault** — без них не
   прогоняется ни один кейс мишени `-D` и весь `smoke-live`.

Прочие ожидания держателя печатает `py tools/backlog-check.py` строкой
`держатель:`.

## Что висит

Висящее живёт секциями `.claude/work/backlog.md` и
`.claude/work/prod-checks.md` и печатается `py tools/backlog-check.py`; в
снапшоте не дублируется. Этим проходом заведена одна секция (`F-14`) и
поправлена одна (`F-8`).

**Пользователю:** обновить Project Knowledge (этот снапшот).

## Ловушки

Ловушки живут в домах по своему вопросу; снапшот их не держит. В разделе
ловушек дома — оглавление: споткнулся — найди строку по словам области и
симптома, тело читай по номеру в `.claude/traps/<дом>-traps.md`.

- среда, оболочка, правка корпуса скриптом —
  `.claude/skills/environment-commands.md` §«Ловушки и обходы»;
- код тестов — `.claude/skills/test-code.md` §«Ловушки и обходы»;
- письмо кейсов и их ревью — `.claude/skills/test-design.md`,
  `.claude/skills/test-review.md`;
- продуктовый код — `.claude/rules/codestyle.md` §«Ловушки»;
- команды корпуса и свипы — `.claude/rules/measurement-commands.md`;
- гейт и статус — `.claude/skills/update-roadmap-progress.md`;
- цикл сессий — `.claude/skills/session-chain.md` §«Ловушки и обходы».

## Проверки на момент снапшота

Реактор зелен на всём дереве — числа захода 131 в хронике шага
(`.claude/work/progress/phase-2-step-12-chronicle.md` §«Шестьдесят третий
заход под-шага 3 (131) дописал ящик `bff` — группы `B4`-`B11` — и закрыл
его»). Гейт инструментов корпуса — целиком, одним прогоном на финальном
состоянии (`.claude/skills/update-roadmap-progress.md` §«Гейт инструментов
корпуса»); исход — отчёт прохода.

## Среда

Дом фактов и ловушек среды — `.claude/skills/environment-commands.md`;
стенда — `.claude/skills/local-stand.md`. Изменений среды нет.
