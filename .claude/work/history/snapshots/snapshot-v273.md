# Snapshot v273

**Дата:** 2026-09-16.

## На какой вопрос отвечает этот файл

Где мы сейчас.

## Состояние

Сменяет v272. Фаза 1 — `FOLDED`. **Фаза 2 — `IN_PROGRESS`**: шаги 1-11
`DONE`, **шаг 12 — `CODE`**, шаг 13 `HOLD`. Прод-рубеж — `HOLD`.

**Машина шага** — `CONCEPT → CONCEPT_REVIEW → CODE → DOCS → REVIEW → DONE`
(`.claude/processes/roadmap-step-execution.md`). `CODE` тестового шага идёт
тремя фиксированными под-шагами: тест-кейсы → адверсариальное ревью кейсов →
код тестов. **Уровень 1 под-шага 1 закончен; идёт уровень 2 — пять предметов
из восьми написаны.**

**Дельта — в рабочем дереве, не закоммичена.** Ветка `claude-audit`.
**Дерево кода эта сессия не трогала** — реактор не прогонялся и не был
нужен (`git status --porcelain -- services/ web/ deploy/` пуст).

**Режим — автономия до прод-рубежа.** Решения — Д250-Д1678.

## Что сделано после v272

Одна единица: **четырнадцатый заход под-шага 1 `CODE` — пятый документ
кейсов уровня 2, слой защитных ступеней торгового ядра**
(`.claude/tests/cases/trading-core-safety.md`). Состав здесь не
пересказывается — дом у него сам документ.

1. **Новая ось формы: выхода-значения у предмета нет вовсе.** Точки входа
   отдают `void` либо признак «переход применился», и наблюдаемый результат
   есть **протокол вызовов**: кто позван, с каким аргументом, в каком
   порядке и кто **не** позван ни разу. Отсюда у строки три несущие величины
   вместо одной — состав, отрицательное ожидание и порядок.
2. **Отрицательное ожидание несущее, а не украшение.** Поглощение сигнала
   целиком состоит из неслучившегося: статус не переставлен, снятие риска не
   гоняется, каскада нет, факта нет — а строка журнала есть. Три шага полной
   реакции из пяти наблюдаемы только порядком.
3. **Счётчик серии убытков взят сверх трёх названных перечнем осей.** Он
   единственный счётный триггер мягкой счётной ступени; лестница без него
   осталась бы с непроверенной областью определения.
4. **Признак уровня 2 объявлен выполненным не буквально.** Ввод-вывод есть у
   большинства единиц предмета; на уровне он оставлен потому, что вся
   собственная работа — соответствия и предикаты над аргументами, а чтения
   только приносят операнды.
5. **Пять находок припарковано, четыре секции заведены заново.** Статус
   подтверждённого снятия риска ставится и на неподтверждённом;
   доминирование биржевых ступеней над инструментными реакциями энфорсера не
   имеет; карв-аут возврата инструмента в онбординговый статус неисполним по
   построению раздела полей и живёт в трёх носителях; отказ ребра подъёма
   внутри эскалации гасится перехватом, и счётная ступень не поднимается
   больше никогда. Пятая — расширение уже стоявшей секции пустого признака
   полноты графа на всё дерево ядра.
6. **Развилки письма — дайджест Д1673-Д1678.**

## Вход следующей сессии

Дальше по перечню (`.claude/decisions/test-contour-design-pass.md`
§«Перечень предметов и порядок работы») — **шестой предмет уровня 2**,
`okx-mapping` (мапперы, снапшоты, резолв статусов, round-trip DTO
источника), затем `market-data-indicators`, `domain-model-predicates`,
**уровень 3** (сквозные тропы) и **ревизия существующего набора**.

- **Форма уровня 2 не развилка** — она объявлена в
  `.claude/tests/cases/strategy-engine-calculation.md` §«Форма уровня 2
  выбрана здесь» и наследуется; документ предмета добавляет к ней **свою**
  ось, если она у предмета есть, и не переоткрывает общую.
- **Кандидатов печатает `py tools/pure-logic-candidates.py`, и его выдача
  шире объявленного предмета** — находка, припаркованная секцией
  `.claude/work/backlog.md` §«Маркеры ввода-вывода у команды кандидатов уровня 2 слепы к конвенции имён».
  Пока она не закрыта, признак уровня у выбранного предмета
  проверяется руками: греп `DataService\b|Repository\b` по его классам.
- **Ожидание берётся из дома, а не из живого теста предмета.** У слоя защиты
  это дало пять кейсов, красных сегодня, и один непрогоняемый.
- Концепция — `.claude/work/progress/phase-2-step-12-chronicle.md`
  §«Концепция»; два вопроса, чьё закрытие кейс не вправе подменить, —
  `.claude/work/progress/phase-2-step-12-concept-review.md`
  §«Два вопроса, которые кейс может закрепить».
- **Под-шаг 3 не начинается** до апрува кейсов на под-шаге 2.

## Что держателю решать

Против v272 не изменилось:

1. **Источник цены `MARK_PRICE` / `INDEX_PRICE`** — реджект создания либо
   снятие ограничения; плюс соседний вопрос того же предмета: источник цены
   у ценового операнда условия не читается никакой.
2. **Читатель `exitOutcome`.**
3. **Ключи demo-окружения OKX и распечатанный Vault** — обещаны к
   под-шагу 3 `CODE`; без них **ни один кейс мишени `-D`** чёрного ящика
   коннектора не прогоняется (в пайплайн они и не входят).

Плюс прежние ожидания (Vault-хардненинг, общий ход миграции на Jackson 3,
область тестового токена) — печатает `py tools/backlog-check.py` строкой
`держатель:`.

## Что висит

Всё, что живёт секциями бэклога, печатает `py tools/backlog-check.py`; в
снапшоте не дублируется. Вне бэклога:

1. **Стенд от дерева кода отстал**, базы `audit` и `statistics` не
   пересоздавались (секция бэклога).
2. **Ни одна пара производитель-потребитель не встречалась на живом
   брокере.** У `trading-core` и `strategies` контекст ящика к тому же
   сегодня не поднимается — автоконфигурации брокера в дереве нет, и группы
   кейсов приёма и публикации у обоих красны целиком. **У `audit`,
   `statistics` и `bff` эта ось закрыта явным клиентом**, и их кейсы приёма
   прогоняемы.
3. **Обязанности заведения каталога `tests/`** принадлежат под-шагу 3 и
   названы в решении: строка `structure.md`, `README.md`, шаблон в `ROOTS`
   у `retired-check.py`, путь сноса классов в `reactor-test.sh`, модуль в
   корневом `pom.xml`. Первые два инструмента при пропуске отвечают кодом 2,
   а не молчат.
4. **WireMock под JDK 25 прогоном не проверен** — отказ на подъёме стаба
   закрывается своим стабом на Spring, а не понижением JDK.
5. **Jar'ов Testcontainers и WireMock в локальном репозитории нет** — прайминг
   под-шага 3 (Maven Central достижим, мерено v260).
6. **Записей площадки для мишени `-S` нет ни одной** — кейсы стаба написаны
   от контракта, и первый прогон `-D` обязан их снять
   (`.claude/tests/cases/connector-okx.md` §«Кейсы, не прогоняемые сегодня»).
7. **Пользователю:** обновить Project Knowledge (этот снапшот); прочитать
   блоки дайджеста Д1564-Д1678 — право вето постфактум.

Три больших свипа с измеренным объёмом остаются открытыми: слой
опровержения в `docs/**`, свод адресных перекрёстий `§«`, долг §-адресов
(`tools/anchor-debt.txt`) и процедура синка в доках. Каждый — своя единица;
угаданный адрес даёт класс «ведёт не к тому дому», который не мерит ни один
прогон.

## Ловушки

**Ловушки** v206-v272 остаются в силе. Подтверждённые этой сессией и новые:

- **`|` в маркере бэклога делит условия, а не альтернацию регекса** — и это
  относится к `закрыто-когда` наравне с `оживит`. Шаблон
  `"isFalse\((a|b)\.getX"` прогон читает как два условия и падает синтаксисом;
  рабочая форма — класс символов (`[a-zA-Z]*[Cc]ontext`) вместо альтернации.
- **Слова «прежняя редакция» в теле секции бэклога — маркер провенанса** и
  роняют прогон, даже когда речь о редакции самого маркера, а не о номере
  прогона.
- **§-адрес пишется дословно по имени пассажа целиком.** Сокращение до
  первого смыслового куска (`§«Содержимое несёт идентичности»` вместо
  `§«Содержимое несёт идентичности и то, чего потребитель не дочитает сам»`)
  — битый адрес: законно опускается только скобочный либо тире-хвост.
- **`BooleanUtils.isFalse(null)` — ложь, а не истина**, и класс держится:
  той же формой признак полноты графа читают ещё два носителя ядра, а
  признак «только журнал» у находки детектора — третий.
- **Составление длинного документа кусками:** `Write` во временный файл,
  `cat … >> цель`, `rm`; heredoc `<<'MD'` на длинной кириллице отказывает —
  любой апостроф рвёт команду. Временные куски кладутся в **существующий**
  каталог (`.claude/work/progress/`) и удаляются тем же ходом.
- **Таблица, у которой в строках больше клеток, чем в шапке, ни одним
  прогоном не ловится.** Проверяется скриптом при сборке из кусков.
- **`py` вместо `python3`**; вывод инструментов рвётся на кириллице без
  `PYTHONIOENCODING=utf-8`; `grep -P` кириллицу в `\w`, `\b`, `\d` не берёт.
- **§-адрес на секцию бэклога, которой ещё нет, роняет прогон.** Находка и её
  парковка пишутся **одним** заходом.
- **Счётный клейм в прозе — дефект формы**, даже когда число верно
  (`.claude/rules/self-description-form.md`).
- **`закрыто-когда` и машинный `оживит` проверяются на действующем дереве
  сразу**: условие, уже истинное, роняет прогон тем же заходом.

Обязательное перед завершением любой единицы:

1. **гейт инструментов корпуса** целиком — перечень в
   `.claude/skills/update-roadmap-progress.md` §«Гейт инструментов корпуса»,
   `backlog-check.py` с подменой статуса, если статус проставляется;
2. **реактор — `bash tools/reactor-test.sh`**, если тронуто дерево кода.

## Проверки на момент снапшота

**Гейт инструментов корпуса прогнан** целиком, все пятнадцать команд:
ненулевых кодов нет, кода 2 нет. Два прогона отдали ненулевой код на первой
попытке и были зазеленены правкой: `backlog-check.py` — альтернация внутри
шаблона нового маркера и слово провенанса в теле расширенной секции;
`anchor-check.py` — два сокращённых §-адреса в новом документе (обе ловушки
записаны выше). Подмены статуса не требовалось — статус шага не менялся.
**Реактор не прогонялся и не требовался** — дерева кода сессия не касалась.

## Среда

Против v132 не изменилась. Дом фактов среды — команды сборки, пути JDK и
Maven, лаунчер Python, Vault и demo-ключ —
`.claude/skills/environment-commands.md`. Сборка: JDK `corretto-25.0.3`,
maven из дистрибутива IDEA (`REACTOR_MVN`), в `PATH` его нет. Состав
локального репозитория по тестовым библиотекам — п. 5 §«Что висит».
