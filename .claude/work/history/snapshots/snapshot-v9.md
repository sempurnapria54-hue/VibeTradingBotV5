# Snapshot v9

**Дата:** 2026-05-27.

## На какой вопрос отвечает этот файл

Где мы сейчас и как сюда пришли (срез на дату).

## Состояние

С момента `snapshot-v8.md` (2026-05-26) закрыты два оставшихся
точечных скрипа третьей обкатки — NQ-H и NQ-G. Третья обкатка
классификации закрыта полностью (NQ-F, NQ-H, NQ-G); задача
перенесена из `progress/` в `history/`.

## Что изменилось относительно v8

**Решения (новые):**
- `.claude/decisions/fsm-handler-as-component.md` — handler-per-status
  FSM-сущности живёт как отдельный компонент
  `docs/components/<Handler>.md`; lifecycle Deal — по разделу на
  статус со ссылкой на handler; конструкция handler'а (3 типа
  проверок) — раздел `DealStateMachine.md`. Закрыл NQ-H.
- `.claude/decisions/master-index-not-fixated.md` — master-index /
  навигационные доки поверх многих сущностей не сохраняются как
  файлы знания: содержание распределяется по владельцам через
  `rule-source-of-truth.md`, файл-агрегатор не воспроизводится.
  Граница с сквозным правилом: у правила есть собственная
  формулировка, у индекса нет. Закрыл NQ-G.

**Скиллы:**
- `classify-type.md` — добавлен раздел «FSM-handler vs раздел
  lifecycle» в признаки различения близких типов (по
  `fsm-handler-as-component.md`).
- `recognize-knowledge.md` — в «Что НЕ является кандидатом» добавлен
  анти-признак про файлы-агрегаторы (master-index, «основной
  источник истины по X», «обзор всех Y») со ссылкой на
  `master-index-not-fixated.md`.

**Открытые вопросы:**
- NQ-G и NQ-H закрыты; в `open-questions.md` открытых вопросов не
  осталось.

**История:**
- `.claude/work/history/2026-05-27-третья-обкатка-классификации-процессы.md`
  — summary третьей обкатки; прогресс-файл удалён.

## Текущая структура

См. `.claude/rules/structure.md`. Относительно v8 структура каталогов
не менялась (последние изменения — `core/`/`other/` и
`components/models/` зафиксированы в v8).

**Продуктовые каталоги:** по-прежнему заготовки с `.gitkeep`;
реальная миграция — предстоящая задача из backlog.

## Активные задачи

- Активных задач в `progress/` нет (третья обкатка закрыта).
- В backlog — миграция архивных торговых сущностей в `docs/` (запуск
  в новом чате), теперь с полным каркасом классификации.

## Открытые общие вопросы

- Открытых нет. Q1-Q4 и NQ-A…NQ-H закрыты; история закрытия — в
  соответствующих decisions.

## Что в работе

- Каркас классификации после третьей обкатки держится целиком:
  `docs/components/`, `model-granularity`, `rule-source-of-truth`,
  `client-layer-docs`, RVO + core/other, FSM-handler как компонент,
  master-index не фиксируется. Скиллы `classify-area` и
  `classify-theme` — по-прежнему каркасы без содержания.
- Следующий крупный шаг — миграция архивных сущностей в `docs/`.
