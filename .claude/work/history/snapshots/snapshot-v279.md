# Snapshot v279

**Дата:** 2026-09-16.

## На какой вопрос отвечает этот файл

Где мы сейчас.

## Состояние

Сменяет v278. Фаза 1 — `FOLDED`. **Фаза 2 — `IN_PROGRESS`**: шаги 1-11
`DONE`, **шаг 12 — `CODE`**, шаг 13 `HOLD`. Прод-рубеж — `HOLD`.

**Машина шага** — `CONCEPT → CONCEPT_REVIEW → CODE → DOCS → REVIEW → DONE`
(`.claude/processes/roadmap-step-execution.md`). `CODE` тестового шага идёт
тремя фиксированными под-шагами: тест-кейсы → адверсариальное ревью кейсов →
код тестов. **Уровни 1 и 2 под-шага 1 закончены целиком; уровня 3 написано
три документа из пяти.**

**Дельта — в рабочем дереве, не закоммичена.** Ветка `claude-audit`.
**Дерево кода эта сессия не трогала** — реактор не прогонялся и не был
нужен (`git status --porcelain -- services/ web/ deploy/` пуст).

**Режим — автономия до прод-рубежа.** Решения — Д250-Д1719.

## Что сделано после v278

Одна единица: **двадцатый заход под-шага 1 `CODE` — третий документ кейсов
уровня 3, сквозная тропа «жёсткая ступень → снятие живого риска → отчёт и
событие»** (`.claude/tests/cases/e2e-safety-teardown.md`). Состав здесь не
пересказывается — дом у него сам документ.

1. **Новая ось формы — ВХОД ТРОПЫ ЕСТЬ РАСХОЖДЕНИЕ, А НЕ ХОД.** Ход теста —
   расстановка ответа стаба, а не вызов стороны; число поданных тиков есть
   часть входа, потому что гистерезис держится стоящей строкой отчёта;
   входов в один стык два — автоматика и ручная поверхность, — и различают их
   два наблюдаемых операнда, а не состав реакции; незавершённость есть
   объявленный исход и проверяется положительным ассертом.
2. **Стороны — `trading-core`, `connector-okx`, `audit`, `statistics`.**
   Стабами поднимаются площадка OKX, `market-data`, `auth` и провайдер
   идентичности; `strategies` поднимается только ради пролога и стороной не
   является; периметр на этой тропе не стои́т.
3. **Пролог собран из ДВУХ чужих документов** — первой тропы целиком плюс
   кейса наблюдения позиции у второй.
4. **Три находки припарковано:** перечень слов провода выводится командой,
   слепой к слову, уезжающему в запрос параметром; поверхность торгового
   состояния отдаёт по паре только идентичности, а не ступень; дом перечня
   детекторов разорван прозой на две таблицы.
5. **Развилки письма — дайджест Д1713-Д1719.**

## Вход следующей сессии

Дальше по перечню (`.claude/decisions/test-contour-design-pass.md`
§«Перечень предметов и порядок работы») — **уровень 3, тропа
`e2e-perimeter-read`**; затем `smoke-live` и **ревизия существующего
набора**.

- **Форма уровня 3 введена** первым предметом и **дополнена** вторым и
  третьим; четвёртый документ берёт все три ссылкой, а не пересказом:
  `.claude/tests/cases/e2e-strategy-to-deal.md` §«Новая ось формы — СТОРОНА
  ТРОПЫ И ЕЁ СЛЕД», `.claude/tests/cases/e2e-exit-and-close.md` §«Новая ось
  формы — ПРОЛОГ ЧУЖОЙ ТРОПЫ», `.claude/tests/cases/e2e-safety-teardown.md`
  §«Новая ось формы — ВХОД ТРОПЫ ЕСТЬ РАСХОЖДЕНИЕ, А НЕ ХОД».
- **Периметр стороной не был ни на одной из трёх написанных троп** — классы
  подъёма ступени, остановки и терминала он получает своей тропой, и стык
  «событие → запись потока → форма в браузер» принадлежит ей целиком.
  Предмет `e2e-perimeter-read` — токен, контекст тенанта, билет, подписка,
  окно переигрывания и проксирование владельцу.
- **Дом сквозного набора — каталог `tests/` верхнего уровня**, и обязанности
  его заведения принадлежат под-шагу 3, не под-шагу 1 (решение 8 плюс строка
  о `verify`).
- **Под-шаг 3 не начинается** до апрува кейсов на под-шаге 2.

## Что держателю решать

Против v278 не изменилось:

1. **Источник цены `MARK_PRICE` / `INDEX_PRICE`** — реджект создания либо
   снятие ограничения; плюс соседний вопрос того же предмета: источник цены
   у ценового операнда условия не читается никакой.
2. **Читатель `exitOutcome`.**
3. **Ключи demo-окружения OKX и распечатанный Vault** — обещаны к
   под-шагу 3 `CODE`; без них **ни один кейс мишени `-D`** чёрного ящика
   коннектора не прогоняется (в пайплайн они и не входят).

Плюс прежние ожидания (Vault-хардненинг, общий ход миграции на Jackson 3,
область тестового токена) — печатает `py tools/backlog-check.py` строкой
`держатель:`.

## Что висит

Всё, что живёт секциями бэклога, печатает `py tools/backlog-check.py`; в
снапшоте не дублируется. Вне бэклога:

1. **Стенд от дерева кода отстал**, базы `audit` и `statistics` не
   пересоздавались (секция бэклога).
2. **Ни одна пара производитель-потребитель не встречалась на живом
   брокере.** У `trading-core` и `strategies` контекст ящика к тому же
   сегодня не поднимается — автоконфигурации брокера в дереве нет, и **все
   три написанные сквозные тропы красны за публикацией**.
3. **Обязанности заведения каталога `tests/`** принадлежат под-шагу 3:
   строка `structure.md`, `README.md`, шаблон в `ROOTS` у
   `retired-check.py`, путь сноса классов в `reactor-test.sh`, **аргументы
   реактора `verify`** и модуль в корневом `pom.xml`.
4. **Тестового дерева у `services/common/model/domain` нет вовсе**, а два
   класса его предмета живут в тестовом дереве `auth` — развилка переноса
   принадлежит под-шагу 3.
5. **WireMock под JDK 25 прогоном не проверен** — отказ на подъёме стаба
   закрывается своим стабом на Spring, а не понижением JDK.
6. **Jar'ов Testcontainers и WireMock в локальном репозитории нет** — прайминг
   под-шага 3 (Maven Central достижим, мерено v260).
7. **Записей площадки для мишени `-S` нет ни одной** — кейсы стаба написаны
   от контракта, и первый прогон `-D` обязан их снять. **Перечень операций
   стаба вырос ещё раз**: тропа аварийного сворачивания требует от него три
   счёт-широких среза (позиции, pending-заявки, pending algo трёх семей),
   отмену заявки и algo-заявки, снятие встроенной защиты, закрытие позиции и
   чтения подтверждения по ним.
8. **Сериализатор групп `-J` под-шаг 3 обязан взять тот же, что на
   проводе.** В дереве лежат обе линии, конвертеры выбирают третью.
9. **У расчётного слоя рыночных данных тестов нет ни одного**, и класс
   непрогоняемых кейсов там держится отсутствием дома формул — не средой.
10. **Пользователю:** обновить Project Knowledge (этот снапшот); прочитать
    блоки дайджеста Д1564-Д1719 — право вето постфактум.

Три больших свипа с измеренным объёмом остаются открытыми: слой
опровержения в `docs/**`, свод адресных перекрёстий `§«`, долг §-адресов
(`tools/anchor-debt.txt`) и процедура синка в доках. Каждый — своя единица.

## Ловушки

**Ловушки** v206-v278 остаются в силе. Подтверждённые этой сессией и новые:

- **§-адрес к величине спеки формой `§имяВеличины` не разрешается.**
  Рабочая форма — «`docs/spec/<файл>.json`, величина `<имя>`»; детектор
  адресов принимает её, а голое `§имя` объявляет новым дефектом.
- **§-адрес с вложенными кавычками `«…»` внутри имени пассажа битый по
  построению** — детектор обрывает адрес на внутренней кавычке. Заголовок
  с цитатой внутри адресуется своей группой, а не поимённо.
- **§-адрес пишется дословно**: пропущенное слово в середине имени
  («Поглощённый повтор **ступени** неполнотой не является») роняет прогон.
- **Условие `закрыто-когда` мерится построчно.** Фраза, перенос строки в
  которой пришёлся на её середину, грепом не берётся.
- **Два сервиса на одном classpath не живут** (конфигурация и локация
  миграций).
- **`python` в этой среде исполняет заглушку молча** — зовётся `py`; исход
  правки проверяется грепом, а не кодом возврата.
- **Счётный клейм в прозе — дефект формы**, даже когда число верно
  (`.claude/rules/self-description-form.md`).
- **§-адрес на секцию бэклога, которой ещё нет, роняет прогон.** Находка и её
  парковка пишутся **одним** заходом, и парковка — первой.
- **Составление длинного документа кусками:** `Write` во временный файл,
  `cat … >> цель`, `rm`; heredoc `<<'MD'` на длинной кириллице отказывает.
  Временные куски кладутся в `.claude/work/progress/` и удаляются тем же ходом.
- **Таблица, у которой в строках больше клеток, чем в шапке, ни одним
  прогоном корпуса не ловится.** Проверяется скриптом при сборке из кусков.
- **`py` вместо `python3`**; вывод инструментов рвётся на кириллице без
  `PYTHONIOENCODING=utf-8`; `grep -P` кириллицу в `\w`, `\b`, `\d` не берёт.

Обязательное перед завершением любой единицы:

1. **гейт инструментов корпуса** целиком — перечень в
   `.claude/skills/update-roadmap-progress.md` §«Гейт инструментов корпуса»,
   `backlog-check.py` с подменой статуса, если статус проставляется;
2. **реактор — `bash tools/reactor-test.sh`**, если тронуто дерево кода.

## Проверки на момент снапшота

**Гейт инструментов корпуса прогнан** целиком: ненулевых кодов нет, кода 2
нет. Подмены статуса не требовалось — статус шага не менялся. **Реактор не
прогонялся и не требовался** — дерева кода сессия не касалась. Целостность
таблиц собранного документа, уникальность меток кейсов и наличие четырёх
обязательных полей у каждого кейса проверены скриптом: расхождений нет.

## Среда

Против v132 не изменилась. Дом фактов среды — команды сборки, пути JDK и
Maven, лаунчер Python, Vault и demo-ключ —
`.claude/skills/environment-commands.md`. Сборка: JDK `corretto-25.0.3`,
maven из дистрибутива IDEA (`REACTOR_MVN`), в `PATH` его нет. Состав
локального репозитория по тестовым библиотекам — п. 6 §«Что висит».
