# Snapshot v306

**Дата:** 2026-09-17.

## На какой вопрос отвечает этот файл

Где мы сейчас.

## Состояние

Сменяет v305. Фаза 1 — `FOLDED`. **Фаза 2 — `IN_PROGRESS`**: шаги 1-11
`DONE`, **шаг 12 — `CODE·2/3`**, шаг 13 `HOLD`. Прод-рубеж — `HOLD`.

**Машина шага** — `CONCEPT → CONCEPT_REVIEW → CODE → DOCS → REVIEW → DONE`
(`.claude/processes/roadmap-step-execution.md`). `CODE` тестового шага идёт
тремя фиксированными под-шагами: тест-кейсы → адверсариальное ревью кейсов →
код тестов. Под-шаг 1 закрыт целиком; **идёт под-шаг 2 — первый предмет из
двадцати одного прошёл двадцать четыре захода петли (двенадцать браковок,
двенадцать правок); апрув по нему не дан**.

**Счёт петли: круг = заход, перепроверка = заход ревьюера.** Заходы чётные —
правки 1-12, нечётные с третьего — перепроверки 1-11. **Тринадцатый круг
открыт** двенадцатой правкой; следующий ход — **двенадцатая перепроверка**
(заход 25).

**Дельта — в рабочем дереве, не закоммичена.** Ветка `claude-audit`.
**Дерево кода эта сессия не трогала** — реактор не прогонялся и не был нужен.

**Режим — автономия до прод-рубежа.** Решения — Д250-Д1784.

## Что сделано после v305

Одна единица: **двенадцатая правка кейсов `auth`** (роль `tester`, заход 24,
круг тринадцатый) —
`.claude/work/progress/phase-2-step-12-fix-test-auth-12.md`. Состав здесь не
пересказывается (`.claude/rules/policy-home.md`). Коротко:

- **Признак доведён до конца по каждому звену.** Клетки приведены к тому, что
  звенья делают: фильтр и провайдер получили двойную клетку «передаёт и
  производит», конвертер переведён в «передаёт», а в тропу добавлены **два
  звена, которых в ней не было** — `SupplierJwtDecoder` и
  `AuthenticationEntryPointFailureHandler`.
- **Кейс B1.15 заведён** на ветвь `AuthenticationServiceException`: провайдер
  недостижим при разборе токена, исход уходит мимо точки входа и мимо обоих
  наших сборщиков ответа. Разведён с B1.7 **по коду** `nimbus-jose-jwt-10.4` —
  линия распакована впервые за петлю.
- **Ветвь DPoP** получила исход «без кейса» с посылкой о производителе токена и
  **своим** наблюдателем — секцией бэклога (третья половина одного предмета
  провайдера).
- **Команда 3 расширена** до трёх классов точек и на мутации ловит все четыре
  точки, которых не брала; прогнана на трёх состояниях.
- **Свип нашёл и закрыл своё:** клейм «сегодня он печатает ноль» в
  §«Чем достаются выходы» отстал от расширенной команды.

## Вход следующей сессии

**Следующая единица — двенадцатая перепроверка кейсов `auth`** (роль
`reviewer`, заход 25): предмет — дельта двенадцатой правки, метка
`bcf521156f2ad4ff70e529bb5c71c284eeb2e91d`. Метка своей дельты отбивается своя
(`git write-tree` до первой правки).

**Что перепроверке стои́т прочитать по коду самостоятельно, а не по отчёту:**

- **цепочку звеньев целиком** — семь звеньев плюс обработчик отказа; правка
  утверждает, что клетку получило каждое и что двойных клеток три;
- **разведение B1.15 с B1.7** — оно держится на том, что `BadJOSEException`
  наследует `Exception`, а `KeySourceException` — `JOSEException`
  (`nimbus-jose-jwt-10.4`); если это неверно, кейс мерит не своё;
- **достижимость B1.15 в общем контуре** — правка утверждает, что кэш набора
  ключей обходится неизвестным `kid`, и что своего контура кейсу не нужно;
- **перечень команды 3** — мутацией по своему выбору, не по той, что в отчёте;
  класс «выбор ветви декодера» заведён отдельным, и довод у него свой.

**Исходники распаковываются** из локального репозитория Maven: четыре линии
Spring Security, автоконфиг Boot и `nimbus-jose-jwt` (`-sources.jar` лежат
рядом с jar'ами).

Альтернатива прежняя по форме: взять ревью следующего предмета перечня
(`connector-okx`) — под-шаг 3 не начинается, пока апрув не дан по **всем**
предметам, поэтому очередь — вопрос стоимости контекста, а не корректности.

## Что держателю решать

Против v305 не изменилось:

1. **Источник цены `MARK_PRICE` / `INDEX_PRICE`** — реджект создания либо
   снятие ограничения; плюс соседний вопрос того же предмета: источник цены
   у ценового операнда условия не читается никакой.
2. **Читатель `exitOutcome`.**
3. **Ключи demo-окружения OKX и распечатанный Vault** — обещаны к под-шагу 3
   `CODE`; без них **ни один кейс мишени `-D`** чёрного ящика коннектора не
   прогоняется, как и весь `smoke-live`.

Плюс прежние ожидания (Vault-хардненинг, общий ход миграции на Jackson 3,
область тестового токена) — печатает `py tools/backlog-check.py` строкой
`держатель:`.

## Что висит

Всё, что живёт секциями бэклога, печатает `py tools/backlog-check.py`; в
снапшоте не дублируется. Вне бэклога — против v305 список не изменился, и
пункты 1-14 §«Что висит» снапшота v302 остаются в силе дословно
(`.claude/work/history/snapshots/snapshot-v302.md`). Существенное из них:
стенд отстал от дерева кода; ни одна пара производитель-потребитель не
встречалась на живом брокере; вход снаружи в окружение не построен ни одним
звеном; обязанности заведения каталога `tests/` принадлежат под-шагу 3;
архивов Testcontainers и WireMock в локальном репозитории нет.

**Предметов выбранного провайдера идентичности стало три, и все три —
секции бэклога с владельцем `solution-designer`**, ждущие живого провайдера:
значение заголовка `typ`, привязка токена к сертификату клиента (`cnf.x5t#S256`)
и привязка к ключу предъявителя по DPoP (`cnf.jkt`). Третья заведена этой
сессией; дом ответа у всех трёх один — `docs/architecture/contracts.md`
§«Контекст тенанта в вызове».

Три больших свипа с измеренным объёмом остаются открытыми: слой опровержения
в `docs/**`, свод адресных перекрёстий, долг §-адресов
(`tools/anchor-debt.txt`) и процедура синка в доках. Каждый — своя единица.

**Пользователю:** обновить Project Knowledge (этот снапшот); прочитать блоки
дайджеста Д1564-Д1784 — право вето постфактум.

## Ловушки

**Ловушки** v206-v305 остаются в силе. Подтверждено и добавлено этой сессией:

- **`py` пишет в windows-`/tmp`, `bash` читает POSIX-`/tmp`** — файл, записанный
  одним и прочитанный другим, «не существует». Обмен идёт через путь внутри
  репозитория, который удаляется тем же вызовом.
- **Мутационная копия дерева сервиса ставится под именем вне инвентаря и
  удаляется тем же вызовом Bash** — иначе `deploy-layout-check.py` красен на
  каталоге, которого в `docs/architecture/services.md` нет. Гейт прогоняется
  после удаления.
- **Свип по своему же отчёту даёт самопопадания** — команда свипа стои́т в нём
  блоком кода и находит себя. Форма разбора: клетка на попадание, и
  самопопадание называется, а не прячется.
- **Каталог `target/` в корне уводит `retired-check.py` в код 2** — убирается
  **до** гейта (`rm -rf target`); предмет припаркован секцией бэклога.
- Прочие ловушки — снапшоты v283, v285-v305 §Ловушки; они не переписываются.

Обязательное перед завершением любой единицы:

1. **гейт инструментов корпуса** целиком — перечень в
   `.claude/skills/update-roadmap-progress.md` §«Гейт инструментов корпуса»,
   `backlog-check.py` с подменой статуса, если статус проставляется;
2. **реактор — `bash tools/reactor-test.sh`**, если тронуто дерево кода.

## Проверки на момент снапшота

**Гейт инструментов корпуса прогнан** целиком: все пятнадцать команд вернули
0, кода 2 нет. Подмены статуса не требовалось — статус шага не менялся.
**Реактор не прогонялся и не требовался** — дерева кода сессия не касалась.
Команды 1-3 документа кейсов прогнаны дословно, извлечённые из него самого;
команда 1 прогнана и на состоянии **до** правки — прирост ровно одна клетка
разностной формы, прочие классы неизменны.

## Среда

Против v132 не изменилась. Дом фактов среды — команды сборки, пути JDK и
Maven, лаунчер Python, Vault и demo-ключ —
`.claude/skills/environment-commands.md`. Сборка: JDK `corretto-25.0.3`,
maven из дистрибутива IDEA (`REACTOR_MVN`), в `PATH` его нет. Состав
локального репозитория по тестовым библиотекам — §«Что висит» снапшота v302,
п. 6.
