# Snapshot v112

**Дата:** 2026-09-03.

## На какой вопрос отвечает этот файл

Где мы сейчас. **Шаг 8 открыт: перечень детекторов заведён в доме, петля
`DOCS_CHECK`/`GAPS_CLOSE` пройдена до чистого прогона.** Сменяет v111.

## Состояние

Фаза 1 — `IN_PROGRESS`; шаги 1-7 `DONE`, **шаг 8 — `DOCS_CHECK_4`
(чист)**, шаги 9-11 `HOLD`. Ветка `claude-audit`. Дельта — в рабочем
дереве staged, коммитит держатель.

**Режим — автономия до конца фазы 1**; решения — Д250-Д449 в дайджесте.

## Что происходит дальше

**Вход в `CODE` шага 8** — после двух гейтов, названных ниже. Перед ним
реестр `code-gate-ledger.json` пополняется позициями шага 8: он сквозной
носитель фазы кода и под прогон не пересобирается.

## Чем закрыта эта итерация (2026-09-03)

Хроника — `.claude/work/progress/phase-1-step-8-chronicle.md`; отчёты —
`phase-1-step-8-docs-check-1.md`, `phase-1-step-8-gaps-close-1.md`;
решения Д439-Д449.

**Несущий ход: шаг детекции заводит наблюдателей, а не реакции.** Ступень
и радиус каждого детектора **выводятся** тремя ратифицированными осями
(уровень «чему нельзя доверять» → радиус ущерба → судьба принятого
риска), поэтому дом реакции у каждой строки уже существует, а
параллельной лестницы не заводится. Классификация держателя
`CRITICAL`/`NON_CRITICAL` легла на существующую ось без остатка:
`CRITICAL` ⟺ kill-switch в составе реакции ⟺ `HoldRung.HARD`.

**Перечень — тринадцать строк, дом `docs/components/AnomalyJob.md`
§«Что ищет».** Девять кейсов держателя разошлись по ним; три позиции
прежнего перечня сняты как не-детекторы, одна (`A5`) — как
переобъявление соседней.

**Возражение держателя по CR-4 дало ещё три находки одного класса**
(`_7`): у позиции строка эпизода заводится **лениво**, поэтому «есть на
бирже, нет в БД» — штатное окно, а не аномалия. `A1` был безопасен
(операнд сделочный), но его безопасность нигде не выводилась; `A8` стоял
в направлении, где отсутствие на бирже есть нормальное закрытие; `A9`
срабатывал бы на нашей отдыхающей входной заявке. Все три закрыты.

**Серия прогонов сошлась: 8 → 3 → 1 → 0 → 1 → 0 → 3 → 0.** Находки `_2`/`_3` —
дефекты текста, внесённого предыдущим закрытием (дубль носителя, слабая
атрибуция, ложный клейм критичности); `_5` — следствие ответа держателя.
Рецидивов узлов нет, детектор стагнации не сработал ни одним сигналом.

**Ответ держателя по `SAFE-Q1` пришёл в этом же заходе:** счёт
принадлежит системе единолично. Посылка приземлена в концепцию, реакция —
третьим пунктом ступени 2; `A2` и `A7` выведены в `(EXCHANGE, HARD)`.
Хвост дал одну находку и она закрыта: признак `A7` перенесён с «в БД
строки нет» на **наш маркер `clOrdId`** — прежний сносил бы биржу за нашу
же заявку, пережившую рестарт посреди операции.

## Что ратифицировано и не пере-решается

Всё из v104-v111, плюс:

- **Предмет детектора неполноты — проход, а не вызов** (Д443): отказ
  вызова поднимает ступень 2 сам, через границу интеграции.
- **Предел слепоты — мягкая биржевая ступень** (Д445): слепота не
  нарушает посылку «защита стоит на бирже и исполняется независимо от
  нашей интеграции», значит принятый риск покрыт и снимать его нечем.
- **Носитель гистерезиса — стоящий отчёт** (Д444): нового счётчика не
  заводится, и он переживает рестарт.
- **У журнального отчёта ключ дедупа — четвёрка** (Д447): состояние
  держится на сущности-предмете, а не на объекте радиуса.

## Чем проверяется корпус

Все девять проверочных команд — зелены; сборка предъявлена (`src/` тронут
javadoc'ом `AnomalyJob.java`): **241 тест, 0 падений**. `anchor-check` —
615 адресов, новых неразрешимых 0; одна строка объявленного долга
**погашена** (§«Реестр прогонов» разрешился заведённой хроникой) и снята
из `tools/anchor-debt.txt`. `retired-check` — 0 дефектов при 30 записях;
заведена тридцатая запись — о снятом доводе, которым перечень детекторов
объявлялся незавершённым по существу, — с популяцией из трёх носителей.
Она **поймала живое вхождение в собственном артефакте закрытия** до того,
как прогон стал зелёным.

## Что висит

1. **Т11 (мягкая биржевая ступень) — гейт входа в `CODE` шага 8.** Предел
   слепоты `A10` поднимает `(EXCHANGE, SOFT)`, исполнителя у пары нет.
   Дом задачи — `.claude/work/backlog.md` §«Лестница биржевых
   safety-состояний — CODE-дельта (2026-08-26)».
2. **Обязательство `clOrdId` у слоя команд — часть `CODE`-дельты.** Чужая
   заявка опознаётся нашим маркером на стороне биржи, а не отсутствием
   строки в БД; до исполнения обязательства `A7` кодом не закрывается.
3. **Гистерезис и предел слепоты — провизорные числа калибровки**, якорь
   пересмотра назван; встречный якорь — `.claude/work/backlog.md`
   §«Гистерезис детекторов аномалий — калибровка по первому периоду
   прогонов».
4. **Второй блок входа держателя («синхронизация по наличию сущностей»)
   не передан** и не достраивался: основания в корпусе нет. Если он нужен
   шагу — отдельный вход держателя.
5. **Пользователю — обновить Project Knowledge:** этот снапшот, дайджест
   (Д250-Д449).

Остальное — v110 §«Что висит», пункты 1-10 (правкой шага 8 не тронуты).

## Среда

Изменений с v108 нет. Дом фактов —
`.claude/tests/source-api/okx/code-preconditions.md`. **Maven в `PATH`
отсутствует**: рабочий бинарь — из связки IDEA
(`.../plugins/maven/lib/maven3/bin/mvn.cmd`) при `JAVA_HOME` на
`corretto-25.0.3`; лаунчер Python — `py -3` (`python3` в Git Bash —
заглушка WindowsApps).
