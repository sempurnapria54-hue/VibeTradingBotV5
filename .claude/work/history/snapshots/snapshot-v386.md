# Снапшот v386

**Дата:** 2026-09-20.

## На какой вопрос отвечает этот файл

Где мы сейчас.

## Состояние

Сменяет v385. Фаза 1 — `FOLDED`. **Фаза 2 — `IN_PROGRESS`**: шаги 1-11
`DONE`, **шаг 12 — `CODE·3/3`**, шаг 13 `HOLD`. Прод-рубеж — `HOLD`.

**Машина шага** — `CONCEPT → CONCEPT_REVIEW → CODE → DOCS → REVIEW → DONE`
(`.claude/processes/roadmap-step-execution.md`). `CODE` тестового шага идёт
тремя фиксированными под-шагами: тест-кейсы → адверсариальное ревью кейсов →
код тестов. **Под-шаги 1 и 2 закрыты целиком**; **идёт под-шаг 3 — код
тестов.** **Уровень 2 написан целиком (14 из 14); на уровне 1 ящик есть у
ШЕСТИ сервисов из восьми** — у `strategies` написан целиком, у
`trading-core` не пройден хвост с чужими блокерами, у `audit` написаны
**четыре группы из одиннадцати**.

**Дельта — в рабочем дереве, не закоммичена.** Ветка `claude-audit`.
**Дерево кода эта сессия не меняла вовсе** — только `src/test` сервиса
`audit`; реактор прогнан.

**Режим — автономия до прод-рубежа.** Решения — Д250-Д1923.

## Что сделано после v385

Одна единица: **заход 105 — группа `B4` ящика `audit`** (обнаружение
разрыва: три исхода сравнения смещений). Подробности — хроника шага
`.claude/work/progress/phase-2-step-12-chronicle.md` §«Тридцать седьмой заход
под-шага 3 (105) написал группу `B4` ящика `audit`»; здесь только то, что
меняет картину.

- **Написаны все 10 клеток** пятью классами; прогонов в пакете ящика стало
  **53** против 43, и **все они зелены** (помеченная `debt` клетка `B2.10` в
  умолчание не входит).
- **Деление на классы у этой группы ТРЕТЬЕ по счёту: класс равен
  конфигурации контекста ВМЕСТЕ С СОСТОЯНИЕМ ГРУППЫ НА БРОКЕРЕ.**
  Зафиксированное смещение, его отсутствие и удалённые записи — состояние,
  общее всем контекстам одного имени группы.
- **Первый момент обнаружения разрыва ящику был недоступен, и заход построил
  ему вход:** назначение партиций подаётся **вступлением чужого участника в
  группу** (`Wire#reassignPartitions`) — на брокере, без касания второго
  бина.
- **Смещение доставленной записи поднимается управляющей записью
  транзакции**, а «непрочитанное удалено» — удалением записей ниже конца
  темы. Обе замены повода объявлены: срок хранения есть величина окружения,
  а удаление на ходу гонкой с живым потребителем не выражается.
- **Повторная доставка ставится отказом обработки** (снятие таблицы журнала
  на время клетки) — единственная тропа, на которой брокер отдаёт ту же
  запись с тем же смещением.
- **Контрольный прогон взял пять осей; четыре сошлись точно, третья уронила
  на одну клетку больше предсказанного** — промах предсказания, механизм у
  которого свой: ожидаемое смещение живёт в памяти контекста и переживает
  клетку. Направление ошибки безопасное.
- **Находок владельцам заход не добыл ни одной.**
- **Оживитель клетки `B2.15` сработал** (прямое удаление записей темы
  построено этой обвязкой), и ей записан исход: клетку пишет своя единица —
  она в группе `B2`. Вместе с исходом записан вопрос, который её прогон
  разрешит.

## Вход следующей сессии

**Следующая единица — группа `B5` ящика `audit`** (полнота: нижняя граница;
7 клеток).

Что единица застаёт:

- **обвязка ящика `audit` полна**, и к ней добавлены: назначение партиций
  вступлением чужого участника (`Wire#reassignPartitions`), производитель
  транзакцией (`#publishInTransaction`), удаление записей темы
  (`#deleteRecordsBefore`), наименьшее доступное смещение
  (`#earliestOffset`), ожидание числа строк журнала
  (`AuditBox#awaitRecordCount`);
- **три деления классов заданы и проверены** — по клетке (`B2`), по
  конфигурации контекста (`B3`), по состоянию группы на брокере (`B4`);
- **своё препятствие у `B5` названо заранее**: её клетки говорят о ЧИСТКЕ и о
  снятии темы с подписки — первая требует своего профиля хранения, вторая
  сужения подписки, то есть обе сдвигают оси конфигурации и живут своими
  контекстами;
- **форма кода уровня 1** задана заходами 83-105, **уровня 2** — заходами
  69-82; всё, что перечисляли v325-v385 §«Вход следующей сессии», остаётся
  в силе дословно (`.claude/work/history/snapshots/snapshot-v325.md` …
  `…/snapshot-v385.md`);
- **контрольный прогон на нейтрализованной сборке — часть единицы** (Д1830);
  предсказание называет клетки поимённо до прогона, мутация ставится и
  снимается **восстановлением файла из индекса**, а не правкой назад;
- **ревизия существующего набора проб предмета** — свой предмет перечня
  (Д1828);
- **хвост ящика `trading-core`** — клетки, ждущие живой экспозиции (`F-13`),
  терминала сделки с числом и трёх своих названных причин; их оживители
  сегодня не сработали (`.claude/tests/cases/trading-core.md` §«Кейсы, не
  прогоняемые сегодня»);
- **колонка «Факт» у двадцати семи клеток групп `B1`-`B3` ящика
  `strategies` пуста** — своя секция бэклога;
- **хвост апрувленных документов** — негейтящие находки `Ф-3`-`Ф-5` по
  `jsonb-overlay-roundtrip`.

## Что держателю решать

Против v385 не изменилось:

1. **Источник цены `MARK_PRICE` / `INDEX_PRICE`** — реджект создания либо
   снятие ограничения (кейс `U22.8` предмета
   `strategy-definition-validation`, красный по построению).
2. **Читатель `exitOutcome`.**
3. **Ключи demo-окружения OKX и распечатанный Vault** — без них не
   прогоняется ни один кейс мишени `-D` и весь `smoke-live`.

Плюс прежние ожидания (Vault-хардненинг, миграция на Jackson 3, область
тестового токена) — печатает `py tools/backlog-check.py` строкой `держатель:`.

## Что висит

Всё, что живёт секциями бэклога, печатает `py tools/backlog-check.py`; в
снапшоте не дублируется. Вне бэклога список не изменился: пункты 1-14
§«Что висит» снапшота v302 остаются в силе дословно
(`.claude/work/history/snapshots/snapshot-v302.md`), **кроме п. 6** (закрыт
заходом 68). Существенное: стенд отстал от дерева кода; вход снаружи в
окружение не построен; **пары производитель-потребитель из ДВУХ сервисов** на
живом брокере по-прежнему не встречалось.

Три больших свипа с измеренным объёмом остаются открытыми: слой опровержения
в `docs/**`, свод адресных перекрёстий, долг §-адресов
(`tools/anchor-debt.txt`) и процедура синка в доках. Каждый — своя единица.

**Пользователю:** обновить Project Knowledge (этот снапшот); прочитать блоки
дайджеста Д1564-Д1923 — право вето постфактум.

## Ловушки

**Ловушки** v206-v385 остаются в силе. Подтверждено и добавлено этой сессией:

- **Назначение партиций у поднятого контекста случается РАНЬШЕ, чем есть
  куда писать.** Строки пар заводит тик, а он молчит, пока приём не жив: к
  моменту первого назначения обновления слушателя цели не находят. Клетка о
  сравнении смещений без поданного заново назначения мерила бы пустоту.
- **Ждать у чужого участника группы НАЗНАЧЕНИЯ нельзя — ждут вступления.**
  Диапазонная стратегия делит партиции потемно, и теме с единственной
  партицией достаётся один участник из двух; ребалансировку вызывает само
  вступление, а жадность стратегии делает её полной у всех.
- **Исход «не пишется ничего» требует предъявить ВОЗВРАТ назначения, и
  предъявляется он соседней темой.** Своя, тронутая записью, двигала бы ровно
  те величины, о неподвижности которых клетка утверждает.
- **Соседняя пара без зафиксированного смещения уносит клетку о
  неподвижности границы.** На том же назначении её ветвь — третья, момент
  наблюдения уезжает вперёд и поднимает нижнюю границу; клетка краснеет по
  причине, которой не ставила.
- **Ожидаемое смещение живёт в памяти контекста и переживает клетку.** В
  действительном состоянии величина самовосстанавливается (ожидание
  сдвигается всегда), но мутация, замораживающая её, красит СОСЕДНЮЮ клетку
  того же класса наследством, а не её предметом.
- Прочие ловушки — снапшоты v283, v285-v385 §Ловушки; они не переписываются.

Обязательное перед завершением любой единицы:

1. **гейт инструментов корпуса** целиком — перечень в
   `.claude/skills/update-roadmap-progress.md` §«Гейт инструментов корпуса»
   (шестнадцать команд), `backlog-check.py` с подменой статуса, если статус
   проставляется;
2. **реактор — `bash tools/reactor-test.sh`**, если тронуто дерево кода.

## Проверки на момент снапшота

**Гейт инструментов корпуса прогнан** целиком; все шестнадцать команд вернули
0. **Реактор прогнан** — 1563 файла, 4441 тест, дефектов 0 (против
1558/4431 у v385: прибавка — пять классов группы `B4` и её десять прогонов).
Пакет ящика прогнан отдельно — 53 теста, все зелены (помеченная `debt` клетка
в умолчание не входит):

```bash
mvn -o -am -pl services/audit test -Dtest='com.example.audit.box.*Test' -Dsurefire.failIfNoSpecifiedTests=false
```

## Среда

Против v132 не изменилась, кроме состава локального репозитория по тестовым
библиотекам и **образов субстрата ящика** (строки «Артефакты тестового
контура» и «Образы субстрата ящика»), а также тестовых областей одиннадцати
модулей (`test.excluded.groups`: счёт печатает
`grep -rl 'test.excluded.groups' --include=pom.xml .`). Дом фактов среды —
команды сборки, пути JDK и Maven, лаунчер Python, Vault, demo-ключ и
прайминг — `.claude/skills/environment-commands.md`.
