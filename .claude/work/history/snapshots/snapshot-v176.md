# Snapshot v176

**Дата:** 2026-09-08.

## На какой вопрос отвечает этот файл

Где мы сейчас. **Шаг 10 фазы 2 (аудит и статистика): находки Т1-Т4 третьего
прогона гейта выхода из `GAPS_CLOSE_4` ЗАКРЫТЫ, гигиена диспозиционирована.
Статус шага не двигался — четвёртый прогон гейта берёт следующая сессия.**
Сменяет v175.

## Состояние

Фаза 1 — `FOLDED`. **Фаза 2 — `IN_PROGRESS`**: шаги 1-9 `DONE`,
шаг 10 — **`GAPS_CLOSE_4`** (все пять узлов приземлены; находки трёх прогонов
гейта закрыты, четвёртого прогона ещё не было), шаги 11-12 `HOLD`.

**Гейтящих выносов держателю нет.**

**Дельта в working tree, не в истории.** Ветка `claude-audit`; всё staged, не
закоммичено. Дерево кода не тронуто, как в v162-v175.

**Режим — автономия до прод-рубежа.** Решения — Д250-Д1151; эта единица завела
Д1150-Д1151.

## Что сделано после v175

**Единица сессии — закрытие Т1-Т4** (шестнадцатая единица закрытия). Единица
**закрыта**, заход **корпусный**. Отчёт —
`.claude/work/progress/phase-2-step-10-gaps-close-4.md` §«Шестнадцатая единица
закрытия — закрытие Т1-Т4 третьего прогона гейта»: популяция четырёх осей с
командами блоками кода, классификация по всему словарю родов, три свипа,
чек-лист глубины.

- **Т1** — прозаическая строка исхода мини-петли узла 1 заменена порождённым
  блоком `passes:`; тело напечатано генератором.
- **Т2** — перечень оси О1 узла 1 сведён к выдаче своих команд: два
  `OutboxWriter.java` получили исход, перечень ложных срабатываний сокращён до
  подтверждённого прогоном (одно), носитель вне выдачи помечен, производный
  клейм раздела дефектов рационала того же отчёта пересчитан. **Шаблон не сужен** (Д1150): сужение
  переписало бы задним числом команду, которой перечень собран до правки.
- **Т3** — у узла 1 появился чек-лист глубины пунктами. Обе формы обхода
  потребителей исполнены и разложены пофайлово; **множества не совпали** —
  только форма 1 даёт `AuditRecord.md`, только форма 2 —
  `error-handling-policy.md`. Пункт «счётный клейм» проставлен впервые и несёт
  диспозицию гигиены.
- **Т4** — в таблицу свипа хвоста внесены два живых носителя выдачи команды 2;
  адрес переехавшего `snapshot-v173.md` переписан на историю.
- **Гигиена** — обе рукописные клетки О5 названы **замером на момент
  приземления узла** и получили указатель на команду, печатающую нынешнее
  число (Д1151). Пересчёт до сегодняшних чисел отвергнут нормой курации.

**Ни одна правка конструкций узлов не касается**: потолки верификаций узлов 1
и 2 не тронуты, мини-петли не переоткрыты.

## Три вещи, которые дальше нельзя пере-решать

**1. Статус шага не проставлен и остаётся `GAPS_CLOSE_4`.** Гейт красен, пока
его прогон не дал чистого исхода; `DOCS_CHECK_5` открывается только после него.

**2. Команда популяции задним числом не переписывается.** Перечень сводится к
её выдаче, а не выдача к перечню (Д1150) — иначе непересобираемость меняет
направление, а не исчезает.

**3. Ретро-правки закрытого не будет.** Отчёты приземлённых узлов и `history/`
описывают прошлое; клейм в них либо получает **названный момент**, либо
остаётся как есть — пересчёт делает их ложными (Д1151).

## Вход следующей сессии

**Единица — четвёртый прогон гейта выхода из `GAPS_CLOSE_4`.** Берёт её сессия,
закрытия Т1-Т4 не писавшая (эта его писала).

- Гейт — `.claude/skills/update-roadmap-progress.md` §«Гейт выхода из
  `GAPS_CLOSE_N`»; предметов семь, единиц гейтящих три (дизайн-проход, узел 1,
  узел 2) плюс **хвост** — теперь их два: закрытие Г1-Г4 и закрытие Т1-Т4.
- **Урок трёх прогонов, годный дальше:** предмет, объявленный зелёным без
  исполнения, остаётся красным ровно столько прогонов, сколько его читают.
  Команды прогонять, а не читать — включая команды популяций и свипов
  **хвостов**.
- Чист гейт → статус `DOCS_CHECK_5` и открытие пятого прогона.

## Что висит

1. **Дельта `CODE` шага 10 — счёт стои́т порождённым блоком в хронике.**
2. **Компонентов шага 10 в `.claude/work/code-gate-ledger.json` нет** — их
   вносит простановка `CODE`.
3. **Позиции парковки узлов 5-6 `GAPS_CLOSE_3`** и парковка узла 1
   (исполнимость формы ценового результата на строках агрегатов).
4. **Образ сервиса без продвинутого тега резолвится подвижным `latest`**
   (`.claude/work/backlog.md`).
5. **Форма дедупа «спросить и вставить»** — шесть точек трёх сервисов;
   поглощённый отчёт ручной операции. Обе — секции бэклога.
6. **Три долга инструментов:** проза реестра
   (`tools/retired-prose-debt.txt`), адресные перекрёстья `docs/**`,
   клетки-носители команд (`tools/command-carrier-debt.txt`).
7. **Цена сессий под правилом единицы не замерена.**
8. **События `auth` — производителя и темы нет**; экспорт метрик из семи
   прочих сервисов; таблицы `access_denials` нет ни у одного сервиса.
9. **Единый error-DTO у четырёх построенных сервисов.**
10. **Потерянное событие при неразбираемом содержимом** — DLT нет.
11. **Названные ограничения шага 7**, ревалидация активных определений при
    смене чисел риск-аппетита, `HOLD_RELEASED` не пишется, база риска на
    терминале не двигается.
12. **Внешний вызов внутри транзакции БД — двенадцать точек** у ядра.
13. **Донорские остатки в общей библиотеке** — снимаются удалением донора.
14. **Отчёты критики и верификации узлов 5-6 `GAPS_CLOSE_3` отдельными
    файлами не сохранены** — закрыто в части нормы, в части прошлого не
    закрывается ничем.
15. **Живые отчёты единиц маркера `(корпусный)` не несут** — кроме отчёта
    прохода, приземления узла 2 и двух хвостов: ретро-разметки нет по правилу
    курации.
16. **Прежние хвосты:** S1 Vault-хардненинг, рост таблицы отказов,
    api-формы периметра без дока.
17. **Пользователю:** переиндексировать проект в IDEA; обновить Project
    Knowledge (этот снапшот). **Стенд от дерева кода не отстал** — код не
    тронут.

## Проверки на момент снапшота

**Гейт инструментов корпуса прогнан на этом состоянии** — весь перечень плюс
`code-gate-check.py`, каждая команда вернула 0. Числа прогонов здесь не
пересказываются — их печатает сам прогон
(`.claude/rules/self-description-form.md`).

**Реактор не прогонялся: дерево кода не тронуто.** Последний прогон — на этом
же состоянии кода (замер v163).

**Живого прогона нет ни у одного компонента шагов 7-10.**

## Среда

Против v132 не изменилась; полное описание — там. Ловушки среды из v156-v175
остаются в силе, включая: полный гейт инструментов в 120 с не укладывается —
прогонять фоновой командой; рабочий бинарь — `py` (`python3` и `python` из
PATH — заглушка, её код 49 читается как «не измерялось»); в regex-шаблонах для
`tools/retired-check.py` пользоваться `\s+`; markdown-тело с собственным
heredoc рвёт внешний — писать инструментом записи; `py` путей `/tmp/**` не
видит; импорт модуля из `tools/` через `importlib` роняет `__pycache__` в
корень репозитория, и `tools/retired-check.py` отказывает кодом 2 на нём.

**Новое этой сессии.** `cd` внутри составной команды Bash **сохраняется**
между вызовами: следующий относительный путь считается от нового каталога, а
не от корня репозитория — возвращаться явным `cd` к корню либо не менять
каталог вовсе.
