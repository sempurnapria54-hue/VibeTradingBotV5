# Snapshot v311

**Дата:** 2026-09-17.

## На какой вопрос отвечает этот файл

Где мы сейчас.

## Состояние

Сменяет v310. Фаза 1 — `FOLDED`. **Фаза 2 — `IN_PROGRESS`**: шаги 1-11
`DONE`, **шаг 12 — `CODE·2/3`**, шаг 13 `HOLD`. Прод-рубеж — `HOLD`.

**Машина шага** — `CONCEPT → CONCEPT_REVIEW → CODE → DOCS → REVIEW → DONE`
(`.claude/processes/roadmap-step-execution.md`). `CODE` тестового шага идёт
тремя фиксированными под-шагами: тест-кейсы → адверсариальное ревью кейсов →
код тестов. Под-шаг 1 закрыт целиком; **идёт под-шаг 2**.

**Петли ревью кейсов нет — есть критерий и потолок в три круга** (решение
держателя 2026-09-17, `.claude/decisions/case-review-approval-criterion.md`;
дом формулировки — `.claude/skills/test-review.md` §«Критерий апрува документа
кейсов»). Апрув блокирует **только невалидность кейса**; пробел покрытия,
пробел дока и форма круга не дают.

**Апрув получили три предмета из перечня** (перечень и порядок —
`.claude/decisions/test-contour-design-pass.md`): `auth` (заход 28),
`connector-okx` (заход 29), `market-data` (заход 30). Следующий предмет
под-шага 2 — **`trading-core`**, четвёртый по перечню, по тому же критерию.

**Дельта — в рабочем дереве, не закоммичена.** Ветка `claude-audit`.
**Дерево кода эта сессия не меняла** (только читала исходники сервиса) —
реактор не прогонялся и не был нужен.

**Режим — автономия до прод-рубежа.** Решения — Д250-Д1784.

## Что сделано после v310

Одна единица: **апрув кейсов `market-data` по критерию валидности** (заход 30)
— `.claude/work/progress/phase-2-step-12-approve-test-market-data.md`. Состав
здесь не пересказывается (`.claude/rules/policy-home.md`). Коротко:

- **Гейтящих находок три.** Первая — тот же системный класс, что у двух
  предыдущих предметов: десять кейсов пинили HTTP-число, которое пишет **наш
  собственный** обработчик, при доме, объявляющем перечень кодов вопросом без
  ответа. Вторая — B2.6 обещал обход правил на пустом окне, тогда как тик
  только возвращает курсор и выходит. Третья — B9.2 ждал пограничного класса
  там, где перевода не происходит: несобираемый адрес соседа отказывает **до**
  вызова, и наружу приходит класс негодного входа.
- **Признак записан механический:** число производит наш обработчик — ожидание
  берёт класс (`code`), число пишет прогон; число производит контейнер, точка
  входа контура либо контракт успеха (`200`, `204`, `202`, `401`, `405`) —
  остаётся числом.
- **Отличие от `connector-okx` названо:** там перечень классов отказа имеет дом
  и закрыт, у `market-data` **дома нет вовсе** — находка F-6.
- **Перебор рёбер цикла единицы сбора** дал три расхождения дома с построенным:
  `BACKFILL → SYNC` объявлено и не пишется никем, `BACKFILL → CHECK` строится и
  домом не названо, `DELETED` объявлен «ручным удалением» при отсутствии такой
  операции (F-7, класс тот же, что у F-3).
- Три пробела покрытия (G1–G3, исход — под-шаг 3), F-9 (javadoc `JobController`
  адресует несуществующий класс охраны), четыре новые секции бэклога.
- Все 114 кейсов валидны, **апрув дан**; под-шаг 3 по `market-data` открыт.

## Вход следующей сессии

**Следующая единица — применение того же критерия к `trading-core`**
(`.claude/decisions/test-contour-design-pass.md` §«Перечень предметов и порядок
работы»; новая ось предмета — объём: FSM, команды, риск, холды, outbox). Форма
захода отработана трижды и читается с трёх отчётов:
`phase-2-step-12-approve-test-auth.md`,
`phase-2-step-12-approve-test-connector-okx.md`,
`phase-2-step-12-approve-test-market-data.md`.

**Что форма захода уже включает** (не выводить заново):

- метка своей дельты отбивается **до первой правки** (`git write-tree`);
- механические оси мерятся разбором документа: четыре поля у каждого кейса,
  существование путей-носителей, разрешимость §-адресов (**и своя дельта
  проверяется тем же прогоном — одна битая ссылка в ней и нашлась**);
- каждый перечень дома — операции поверхности, тики, рёбра жизненного цикла,
  классы отказа — **перебирается поимённо**, и непокрытая единица уходит меткой
  `G` в раздел «Пробелы покрытия» документа кейсов;
- ожидание, у которого дом молчит, берётся из кода с названным звеном и
  пометкой «прогоном не проверено», а пробел уходит находкой `F` с секцией
  бэклога;
- отчёт несёт таблицу самопроверки автора по критерию
  (`.claude/skills/test-design.md`).

**Два класса, найденные у всех трёх предметов и стоящие проверки у каждого
следующего:**

1. **Пиньнутая величина, которой названный носитель не фиксирует** — у `auth`
   число `500` на опровергнутой тропе, у `connector-okx` двадцать два числа при
   доме с оговоркой о провизорности, у `market-data` десять чисел своего
   обработчика при доме, объявляющем перечень открытым вопросом.
2. **Элемент жизненного цикла без писателя** — `Instrument.CREATED` (F-3),
   ребро `BACKFILL → SYNC` и статус `DELETED` (F-7). Ловится перебором рёбер
   поимённо против грепа статусов по дереву сервиса.

**Исходники читаются прямо из `services/**`** — распаковка нужна только для
библиотек; каталог распаковки заводится **внутри репозитория** и удаляется до
гейта (§Ловушки).

## Что держателю решать

Против v310 не изменилось:

1. **Источник цены `MARK_PRICE` / `INDEX_PRICE`** — реджект создания либо
   снятие ограничения; плюс соседний вопрос того же предмета: источник цены
   у ценового операнда условия не читается никакой.
2. **Читатель `exitOutcome`.**
3. **Ключи demo-окружения OKX и распечатанный Vault** — обещаны к под-шагу 3
   `CODE`; без них **ни один кейс мишени `-D`** чёрного ящика коннектора не
   прогоняется, как и весь `smoke-live`.

Плюс прежние ожидания (Vault-хардненинг, общий ход миграции на Jackson 3,
область тестового токена) — печатает `py tools/backlog-check.py` строкой
`держатель:`.

## Что висит

Всё, что живёт секциями бэклога, печатает `py tools/backlog-check.py`; в
снапшоте не дублируется. Вне бэклога — против v310 список не изменился, и
пункты 1-14 §«Что висит» снапшота v302 остаются в силе дословно
(`.claude/work/history/snapshots/snapshot-v302.md`). Существенное из них:
стенд отстал от дерева кода; ни одна пара производитель-потребитель не
встречалась на живом брокере; вход снаружи в окружение не построен ни одним
звеном; обязанности заведения каталога `tests/` принадлежат под-шагу 3;
архивов Testcontainers и WireMock в локальном репозитории нет.

**Предметов выбранного провайдера идентичности три, и все три — секции
бэклога с владельцем `solution-designer`**, ждущие живого провайдера:
значение заголовка `typ`, привязка токена к сертификату клиента
(`cnf.x5t#S256`) и привязка к ключу предъявителя по DPoP (`cnf.jkt`); дом
ответа у всех трёх один — `docs/architecture/contracts.md` §«Контекст тенанта
в вызове».

Три больших свипа с измеренным объёмом остаются открытыми: слой опровержения
в `docs/**`, свод адресных перекрёстий, долг §-адресов
(`tools/anchor-debt.txt`) и процедура синка в доках. Каждый — своя единица.

**Пользователю:** обновить Project Knowledge (этот снапшот); прочитать блоки
дайджеста Д1564-Д1784 — право вето постфактум.

## Ловушки

**Ловушки** v206-v310 остаются в силе. Подтверждено этой сессией:

- **`grep -c "@Scheduled"` по дереву сервиса считает и упоминания в
  javadoc.** У `market-data` он печатает шесть при пяти построенных тиках —
  шестое попадание в комментарии класса свойств. Ассерт «число тиков» обязан
  считать аннотации на **методах**, а не строки грепа.
- **Heredoc с кириллицей и обратными кавычками через `bash -c` рвётся:**
  правки корпуса удобнее подавать скриптом, записанным файлом, и удалять его
  до гейта.
- **Пустой каталог `target/` в корне уводит `retired-check.py` в код 2** —
  секция `.claude/work/backlog.md` §«Каталог проб `target/` в корне уводит
  `retired-check.py` в код 2».
- Прочие ловушки — снапшоты v283, v285-v310 §Ловушки; они не переписываются.

Обязательное перед завершением любой единицы:

1. **гейт инструментов корпуса** целиком — перечень в
   `.claude/skills/update-roadmap-progress.md` §«Гейт инструментов корпуса»,
   `backlog-check.py` с подменой статуса, если статус проставляется;
2. **реактор — `bash tools/reactor-test.sh`**, если тронуто дерево кода.

## Проверки на момент снапшота

**Гейт инструментов корпуса прогнан** целиком — все пятнадцать команд с кодом
0; исход в конверте сессии и в хронике шага. Подмены статуса не требовалось:
статус шага не менялся. **Реактор не прогонялся и не требовался** — дерева
кода дельта не касается
(`git diff --name-only c05ff2c8085b0e9379499529af2bbd5feeabfaa7 -- services/ web/`
пуст).

## Среда

Против v132 не изменилась. Дом фактов среды — команды сборки, пути JDK и
Maven, лаунчер Python, Vault и demo-ключ —
`.claude/skills/environment-commands.md`. Сборка: JDK `corretto-25.0.3`,
maven из дистрибутива IDEA (`REACTOR_MVN`), в `PATH` его нет. Состав
локального репозитория по тестовым библиотекам — §«Что висит» снапшота v302,
п. 6.
