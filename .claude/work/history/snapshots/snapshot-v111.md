# Snapshot v111

**Дата:** 2026-09-03.

## На какой вопрос отвечает этот файл

Где мы сейчас. **Шаг 7 закрыт; ход его закрытия внесён в процесс как
штатная практика закрытия любого шага.** Сменяет v110.

## Состояние

Фаза 1 — `IN_PROGRESS`; шаги 1-7 **`DONE`**, шаги 8-11 `HOLD`. Ветка
`claude-audit`. Правка процесса лежит **в рабочем дереве staged** —
коммитит держатель (базовое правило вернулось в силу: указание
коммитить самому касалось только задачи закрытия шага 7).

**Режим — автономия до конца фазы 1**; решения — Д250-Д438 в дайджесте.

## Что происходит дальше

Следующий шаг — **8, `AnomalyJob`** (`HOLD`). Он **не открыт**: прошлый
заход открыл его, вынес держателю три вопроса и остановился; заход
отменён, его правок в дереве нет. Шаг 8 идёт отдельным прогоном **по уже
исправленному процессу**, вместе с входом держателя (перечень кейсов
аномалий CR-1…CR-6 / NC-1…NC-3) и припаркованным в него материалом: Т7,
Т11, общий детектор нарушения инварианта покрытия.

## Чем закрыта эта итерация (2026-09-03)

Отчёт — `.claude/work/history/2026-09-03-closure-practice-standard.md`;
решения Д433-Д438. Верным закрытие шага 7 сделал **промпт**, а не
процесс; правка переносит четыре его предписания в постоянные носители:

- **прогон идёт до гейта одним заходом**, и условий остановки три —
  третье «блокер не работа» (`roadmap-step-execution.md` §4);
- **адресат парковки — носитель, который оживит** — практика Д422
  поднята из дайджеста в правило `.claude/rules/parking-address.md`;
- **вынос держателю проходит самопроверку с записью**, и признак
  невыводимости механический (`question-delegation.md`);
- **вопрос держателю не останавливает прогон** — зависимый узел
  паркуется.

Плюс разведён класс числа: риск-аппетит (держателя, провизорного
значения не бывает) против калибровки наблюдения (владельца,
консервативный дефолт + якорь калибровки). Прежде оба правила
действовали и противоречили друг другу.

## Что ратифицировано и не пере-решается

Всё из v104-v110, плюс:

- **Вынос выводимого из концепции — дефект, а не осторожность** (Д433,
  Д434): признак проверяется на тексте вопроса, а не на добросовестности
  выносящего.
- **Не всякое число — риск-аппетит** (Д435): признак счётный — операнд
  сайзинга/потолка/остановки против операнда распознавания.
- **Шаг закрывается с парковками в отчёте** (Д437): гейт требует
  диспозиции каждой находки, а не закрытия каждой.

## Чем проверяется корпус

Все девять проверочных команд (в их числе мутационная проба
`spec-mutation-check.sh`) — зелены; `src/` этой итерацией не трогался,
гейт сборки не применяется. `anchor-check` — 607 адресов, новых
неразрешимых 0 (все §-адреса правки разрешились); `retired-check` — 0
дефектов на 986 живых носителях; `spec-mutation-check` — 388 величин,
переживают нейтрализацию 0; `code-gate-check.py` — ПРОЙДЕН, 42 позиции
против 42 записей реестра.

## Что висит

Всё из v110 без изменений (правка процесса ни одного пункта не
закрывала и не двигала), плюс:

1. **Три вопроса открытия шага 8 не отвечены** — и по новому процессу
   держателю не адресованы: два выводимы из концепции (владелец —
   `solution-designer`), третий — калибровочное число владельца. Разбор —
   в отчёте итерации; исполнение — прогоном шага 8.
2. **Пользователю — обновить Project Knowledge:** этот снапшот, дайджест
   (Д250-Д438).

Остальное — v110 §«Что висит», пункты 1-10.

## Среда

Изменений с v108 нет: контур поднимается вручную после ребута хоста,
Vault после рестарта контейнера запечатан (unseal у держателя), часы
хоста склонны к рассинхрону. Дом фактов —
`.claude/tests/source-api/okx/code-preconditions.md`. **Maven в `PATH`
отсутствует**: рабочий бинарь — из связки IDEA
(`.../plugins/maven/lib/maven3/bin/mvn.cmd`) при `JAVA_HOME` на
`corretto-25.0.3`; лаунчер Python — `py -3` (`python3` в Git Bash —
заглушка WindowsApps).
