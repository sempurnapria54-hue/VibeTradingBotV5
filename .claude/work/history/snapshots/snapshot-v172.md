# Snapshot v172

**Дата:** 2026-09-08.

## На какой вопрос отвечает этот файл

Где мы сейчас. **Шаг 10 фазы 2 (аудит и статистика): узел 2 `GAPS_CLOSE_4`
ПРИЗЕМЛЁН — все восемь объявленных остатков разобраны. Статус шага не
двигался. Следующая единица — гейт выхода из `GAPS_CLOSE_4`, прогоняемый
заново.** Сменяет v171.

## Состояние

Фаза 1 — `FOLDED`. **Фаза 2 — `IN_PROGRESS`**: шаги 1-9 `DONE`,
шаг 10 — **`GAPS_CLOSE_4`** (все пять узлов приземлены; гейт выхода красен по
прошлому прогону и ждёт нового), шаги 11-12 `HOLD`.

**Гейтящих выносов держателю нет.**

**Дельта в working tree, не в истории.** Ветка `claude-audit`; всё staged, не
закоммичено. Сверх v171 тронуты: `docs/rules/statistics-aggregates.md`,
`docs/architecture/contracts.md`, `docs/architecture/services.md`,
`.claude/rules/knowledge-ownership-by-service.md`, `.claude/knowledge-tree.md`,
`.claude/work/backlog.md`, `tools/retired-check.py` (четыре записи), отчёт
узла 2, отчёт закрытия, хроника шага, дайджест (Д1144-Д1146), снапшот.
**Дерево кода не тронуто** — как в v162-v171.

**Режим — автономия до прод-рубежа.** Решения — Д250-Д1146.

## Что сделано после v171

**Единица сессии — приземление узла 2 `GAPS_CLOSE_4`.** Единица **закрыта**.
Отчёт — `.claude/work/progress/phase-2-step-10-gaps-close-4-node-2.md`
§«Приземление после концепт-прохода».

**1. Корпус (W1-W4).** Пять живых носителей, ведших за ограничениями **обеих**
выборок к дому журнала, переведены на дом своего предмета; пересказ состава
снят у строки контрактов. У агрегатного окна объявлен **свой** предел — с
писателем, местом и направлением консервативности; журнальный меряется
моментом происшествия, агрегатный сутками зерна, и одним числом они не
выражаются. Назван ключ курсора агрегатной страницы и его порядок. Вопрос
файла-носителя агрегатов переформулирован до одного, покрывающего внесённый
предмет.

**2. Отчёт узла (Н2-Н4).** Команды популяции дописаны **по всем четырём осям
дословно**, с поимённо названными ложными срабатываниями. Заведён раздел
свипов в предписанной форме — оба предмета плюс структурный; четыре записи в
`tools/retired-check.py` и один отказ с названной причиной (снятое — ошибка
факта, а не редакция политики). Чек-лист глубины проставлен поимённо, обход
потребителей предъявлен двумя формами.

**3. Правило `edit-kind-obligations` окупилось на первом применении, и это
измеримо.** Род `дом` вывел обход двумя формами, и **форма 1** дала **пятого**
дефектного потребителя (`.claude/rules/knowledge-ownership-by-service.md`),
которого верификация 2 не видела: её свип шёл по прозаической тропе, а этот
носитель выражал предмет именем дома. Род `величина` вывел свип структурных
носителей, и тот поймал позицию 36 дельты `CODE`, называвшую **один** предел
на две выборки. Ни одна из двух находок не следует из перечня остатков — обе
следуют из рода.

**4. Свип предмета 2 поймал третье, чего не было ни в одной находке:** курсор
агрегатной страницы был объявлен **без порядка**, по которому идёт, — а курсор
без порядка неисполним.

## Три вещи, которые дальше нельзя пере-решать

**1. Узел 2 закрыт; его правки из корпуса не откатываются.** Потолок
верификаций не тронут приземлением: оно разбор объявленных остатков, а не
заход мини-петли.

**2. Гейт выхода принимает приземление, а не приземление себя.** Заход,
принимающий собственные правки, независимости не имеет; гейт берёт сессия, не
писавшая приземления.

**3. Ретро-разметки закрытых отчётов не будет.** Энфорсер `edit-kind-check`
мерит только размеченное; цена названа в доме правила.

## Вход следующей сессии

**Единица — гейт выхода из `GAPS_CLOSE_4`, прогоняемый заново** по всем шести
предметам (популяция, раздел свипов, чек-лист глубины, исход мини-петли,
полная диспозиция находок, записи выноса) у **трёх** гейтящих единиц:
дизайн-проход, узел 1, узел 2. Прошлый прогон гейта —
`.claude/work/progress/phase-2-step-10-gaps-close-4.md` §«Гейт выхода из
`GAPS_CLOSE_4` — прогнан 2026-09-08, исход КРАСНЫЙ»; четыре красные находки
(Н1-Н4) адресованы приземлением, принимает их гейт.

**Следующий статус шага после чистого гейта — `DOCS_CHECK_5`** (прогон
`DOCS_CHECK_4` чистым не был).

## Что висит

1. **Дельта `CODE` шага 10 — счёт стои́т порождённым блоком в хронике.**
2. **Компонентов шага 10 в `.claude/work/code-gate-ledger.json` нет** — их
   вносит простановка `CODE`.
3. **Позиции парковки узлов 5-6 `GAPS_CLOSE_3`** и парковка узла 1
   (исполнимость формы ценового результата на строках агрегатов).
4. **Образ сервиса без продвинутого тега резолвится подвижным `latest`**
   (`.claude/work/backlog.md`).
5. **Форма дедупа «спросить и вставить»** — шесть точек трёх сервисов;
   поглощённый отчёт ручной операции. Обе — секции бэклога.
6. **Долг прозы реестра** (`tools/retired-prose-debt.txt`) и **долг адресных
   перекрёстий `docs/**`**, приросший этим шагом (в том числе приземлением
   узла 2).
7. **Цена сессий под правилом единицы не замерена.**
8. **События `auth` — производителя и темы нет**; экспорт метрик из семи
   прочих сервисов; таблицы `access_denials` нет ни у одного сервиса.
9. **Единый error-DTO у четырёх построенных сервисов.**
10. **Потерянное событие при неразбираемом содержимом** — DLT нет.
11. **Названные ограничения шага 7**, ревалидация активных определений при
    смене чисел риск-аппетита, `HOLD_RELEASED` не пишется, база риска на
    терминале не двигается.
12. **Внешний вызов внутри транзакции БД — двенадцать точек** у ядра.
13. **Донорские остатки в общей библиотеке** — снимаются удалением донора.
14. **Отчёты критики и верификации узлов 5-6 `GAPS_CLOSE_3` отдельными
    файлами не сохранены** — закрыто в части нормы, в части прошлого не
    закрывается ничем.
15. **Живые отчёты единиц маркера `(корпусный)` не несут** — кроме отчёта
    прохода и приземления узла 2: ретро-разметки нет по правилу курации.
16. **Прежние хвосты:** S1 Vault-хардненинг, рост таблицы отказов,
    api-формы периметра без дока.
17. **Пользователю:** переиндексировать проект в IDEA; обновить Project
    Knowledge (этот снапшот). **Стенд от дерева кода не отстал** — код не
    тронут.

## Проверки на момент снапшота

**Гейт инструментов корпуса прогнан на этом состоянии** — весь перечень плюс
`code-gate-check.py`, каждая команда вернула 0; исход записан в
`.claude/work/progress/phase-2-step-10-gaps-close-4-node-2.md`
§«Приземление после концепт-прохода». Рабочий бинарь —
`py`; `python3` из PATH — заглушка, её код 49 читается как «не измерялось».
Гейт прогонялся **фоновыми** командами двумя половинами: в 120 с целиком не
укладывается. Числа прогонов здесь не пересказываются — их печатает сам
прогон (`.claude/rules/self-description-form.md`).

**Реактор не прогонялся: дерево кода не тронуто.** Последний прогон — на этом
же состоянии кода (замер v163).

**Живого прогона нет ни у одного компонента шагов 7-10.**

## Среда

Против v132 не изменилась; полное описание — там. Ловушки среды из v156-v171
остаются в силе, включая: полный гейт инструментов в 120 с не укладывается —
прогонять фоновой командой; markdown-тело в heredoc рвётся — писать
инструментом записи и запускать `py <файл>`; в regex-шаблонах для
`tools/retired-check.py` пользоваться `\s+` (он берёт перенос строки).
**Новое этой сессии:** `flat_text` реестра снятых редакций схлопывает пробелы
и переносы, но **обратные кавычки вокруг имени файла остаются** — шаблон
`arrived` вида `<файл>\s+§«…»` на `` `файл` §«…» `` не срабатывает; писать
`<файл>.{0,3}§«…»`.
