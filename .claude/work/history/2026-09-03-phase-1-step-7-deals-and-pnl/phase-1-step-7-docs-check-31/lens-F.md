# Линза F — `DOCS_CHECK_31`

## На какой вопрос отвечает этот файл

Какие пробелы нашла линза F прогона `DOCS_CHECK_31` (предусловия `CODE`,
рабочие файлы, контур).

## Предмет и охват

Предмет строки «F» мандата: **предусловия `CODE`, рабочие файлы, контур**.

Проверялись: `.claude/tests/source-api/okx/code-preconditions.md`,
`.../plan.md`, `.../coverage-manifest.md`, `.../observations/`,
`tools/preconditions-check.sh`, `.claude/work/backlog.md`,
`.claude/work/questions/open-questions.md`,
`.claude/processes/source-api-testing.md`,
`.claude/processes/roadmap-step-execution.md` §Гейты + §3,
`.claude/skills/update-roadmap-progress.md`, `.claude/rules/structure.md`,
`.claude/knowledge-tree.md`, живые код-тесты контура
(`src/test/.../sourceapi/okx/`), `tools/retired-check.py`,
`tools/spec-pointer-check.py`.

Вне охвата: продуктовые доки `docs/**` кроме точечной верификации
атрибуций (funding-интервал, `account-bills`, спека `pnl-reconciliation`),
спеки как предмет (линза E), инструменты как предмет (линза G — здесь
разобраны только `preconditions-check.sh` и `retired-check.py` в части,
касающейся гейта грунта).

**Прогнаны все восемь команд мандата:** `spec-run.sh` (rc=0, 22 спеки,
473 примера), `spec-mutation-check.sh` (rc=0, 356 величин, переживают
нейтрализацию 0), `spec-scope-check.py` (rc=0), `spec-pointer-check.py`
(rc=0, 201 указатель), `anchor-check.py` (rc=0), `retired-check.py`
(rc=0, 846 носителей, 4 записи реестра), `preconditions-check.sh` (rc=0),
`mvn -o test-compile` (`BUILD SUCCESS`).

**Ловушка среды, найденная по ходу (не находка корпуса).** В shell
агента `grep` перекрыт функцией, маршрутизирующей в `ugrep 7.8.4`. Его ERE
**молча теряет** конструкцию `(^|[^0-9A-Za-z.])X\.Y` — ту самую границу
идентификатора, которой пользуются `tools/preconditions-check.sh` и
проверочная команда манифеста. Проверка манифеста, прогнанная в таком
shell, дала 35 «непокрытых» кейсов вместо 6. В `bash -c` и в самих
скриптах `grep` — GNU `/usr/bin/grep`, поэтому дефекта у корпуса нет; все
числа ниже пересняты через `bash -c` / `/usr/bin/grep`.

## Стадия остановки обхода

**Стадия 0 не чиста.** Гейтящие находки F1, F2, F4 касаются самого гейта
грунта: реестр и рабочий файл называют одно открытое предусловие, тогда
как их собственные таблицы держат три, а канон гейта `CODE` разрешает
закрывать слот исходом, у которого закрывающая сила снята. Обход тем не
менее доведён до конца предмета (стадии 1-2 предмета F — план, манифест,
процесс контура, CODE-дельта), потому что стадия 0 здесь не «неизвестна
механика», а «механика записана противоречиво»: проверять зависимое было
осмысленно.

---

# Блокирует выход в `CODE`

## F1. Счёт открытых слотов реестра противоречит собственной таблице: открыто три, объявлен один

- **Вопрос.** Реестр гейтящих предусловий — дом правила гейта `CODE`.
  Его таблица держит **три** слота в статусе `PENDING` (п. 2 × `AG6.1`,
  п. 7 × `AG1.7`, п. 16 × `AG6.1`), а сводный клейм того же файла — и
  повторяющий его рабочий файл — утверждают, что открыт **один** (п. 7) и
  что пп. 2 и 16 **закрыты**. Клейм полноты в доме гейта — это инструкция
  «дальше не искать».
- **Грунт.** Не торговый; предмет — состояние сбора источникового грунта.
- **Цитаты (носители).**
  - `.claude/tests/source-api/okx/code-preconditions.md:90` — строка п. 2
    × `AG6.1`: «⏳ `PENDING` — прогон был, но перечень остался **в логе**».
  - `.../code-preconditions.md:98` — строка п. 16 × `AG6.1`: «⏳
    `PENDING` — по той же причине, что и п. 2».
  - `.../code-preconditions.md:101-108` — «закрыто одиннадцать слотов из
    двенадцати (2026-08-30), открыт один — п. 7… закрыты пп. 1, **2**, 5,
    6, 9, 14, 15, **16**, 17 — девять пунктов из десяти… Счёт
    пересобирается **из таблицы**, а не правится отдельно».
  - `.../code-preconditions.md:350-353` — «**закрыто одиннадцать слотов
    из двенадцати**… открытым остался **один** слот (п. 7)».
  - `.../code-preconditions.md:516` — заголовок «### Что осталось
    открытым — один слот».
  - `.../code-preconditions.md:432-437` — тот же файл, обратное:
    «**П. 2 и 16 — … слоты ОТКРЫТЫ** … слоты закрыты не были и возвращены
    в `PENDING`».
  - `.claude/work/backlog.md:560` — заголовок «Носители прогона гейтящих
    слотов шага 7 — **хвост из одного слота**».
  - `.claude/work/backlog.md:563` — «**закрыто одиннадцать слотов из
    двенадцати**».
  - `.claude/work/backlog.md:568` — «**Что осталось — один слот:**».
  - `.claude/tests/source-api/okx/plan.md:83-84` — «из них **восемь дали
    факт целиком**, два частично (`AG1.7`, `AG1.8`)»: `AG6.1` сосчитан
    среди восьми, хотя его слоты возвращены в `PENDING`; фактически семь.
- **Почему гейтит.** Правило гейта (`code-preconditions.md:27`) держит шаг
  вне `CODE`, пока открыт хотя бы один слот; механический гейт
  (`update-roadmap-progress.md:153-163`) читает **колонку статуса** и
  сработает верно. Но исполнитель и планировщик читают **сводку**, и она
  говорит, что осталось дособрать один факт про фандинг. Пп. 2 и 16 —
  это перечень пар `type`/`subType` и построенное по нему отображение в
  категории движения; на них стоят четвёртая пара сверки и резолв
  категории. Реестр сам называет последствие: «Клейм «177 пар справочника
  в экономику сделки не входят» **снят**… Состав списка исключений сверки
  строится по сохранённому перечню — **после перепрогона**»
  (`:441-447`). Исполнитель, поверивший сводке, напишет резолв категорий и
  список исключений по несуществующему операнду.
- **Уровень пробела.** **Носитель** (истина верна в таблице; расходятся
  её копии-счёты в четырёх местах дома и трёх местах рабочего файла).
  Инструмента, ловящего это, нет: `tools/preconditions-check.sh` меряет
  пары «пункт × кейс», разведённость слота, носители прогона и
  разобранность гейт-заголовков — **колонку статуса и сводный счёт он не
  читает вовсе** (прогнан, rc=0 при трёх `PENDING`).
- **Целевой док.** `.claude/tests/source-api/okx/code-preconditions.md`
  (§Реестр — сводная строка; §«Что добыто и что осталось»; заголовок
  §«Что осталось открытым»), `.claude/work/backlog.md` §«Носители прогона
  гейтящих слотов шага 7…» (включая заголовок),
  `.claude/tests/source-api/okx/plan.md` §Статус.
- **Ожидаемый владелец.** `tester` (состояние контура и реестра);
  `knowledge-curator` — если выбирается вариант с механической охраной.
- **Трассировка.** Открыл реестр целиком → пересчитал таблицу построчно
  (9 ✅ / 3 ⏳) → сравнил со сводной строкой → нашёл прямое
  противоречие внутри одного файла (`:101-108` против `:432-437`) →
  свипнул термины счёта по живым носителям
  (`grep -rn 'одиннадцать слотов\|один слот\|двенадцати\|девять пунктов'`
  вне `progress/`, `history/`, chronicle) → нашёл повтор в `backlog.md` и
  третью форму в `plan.md:83` → прогнал `preconditions-check.sh`
  (rc=0) и убедился, что счёт не входит в предмет ни одной оси.
- **Варианты.**

  | Вариант | Что делает | Названная цена |
  |---|---|---|
  | A. Пересобрать счёт из таблицы во всех семи местах, оставив прозу | Клейм становится верным сегодня | Правка семи строк; **связность высокая** — семь мест обязаны знать состояние таблицы; класс воспроизводится следующей же правкой статуса (он уже воспроизводился: сводка не пересобралась при возврате двух слотов) |
  | B. Снять сводный счёт как отдельный носитель: в реестре и в бэклоге — ссылка «состояние слотов — таблица §Реестр», числа не дублировать | Единственный носитель счёта — сама таблица; расходиться нечему | Правка шести-семи мест + потеря беглого «сколько осталось» без открытия таблицы; цена **обратима** |
  | C. Ввести пятую ось `preconditions-check.sh`: сводный счёт пересобирается из колонки статуса и сверяется с объявленным | Счёт становится измеряемым, а не переписываемым | Ось + падающая проба в батарее (которой у команды пока нет — F9); скрипт начинает читать колонку статуса, то есть расширяет предмет с «полнота таблицы» на «её содержимое» — это меняет разделение с §«Гейт грунта» скилла, где чтение статусов — отдельный шаг |
  | D. B + C: числа снять, а таблицу охранять | Дом один и он проверяем | Сумма цен B и C; наибольший след |

  **Крен — B, с C как отдельным пунктом.** Довод счётный, не оценочный:
  правка A уже применялась (сводка `:101-108` несёт формулу «Счёт
  пересобирается из таблицы, а не правится отдельно» — то есть правило
  записано и нарушено тем же абзацем), и класс её пережил. B — меньший
  след по шкале (`design-simplicity`: снятие носителя дешевле, чем ещё
  один энфорсер) и снимает семь мест, обязанных знать чужое состояние.
- **Ярлык исхода.** `варианты-с-креном`.
- **Ярлык дефицита.** Дефицита нет — закрыто прогоном (факт установлен
  чтением таблицы, не требует ни грунта, ни подтверждения держателя).

## F2. Канон гейта `CODE` разрешает закрывать слот снятым исходом; энфорсер снятых редакций эту форму не видит

- **Вопрос.** Решением держателя 2026-08-30 у исхода `OBSERVED_ABSENT`
  отнята закрывающая сила. Дом правила (реестр), механика исходов (план)
  и исполнимый гейт (скилл) приведены к новой редакции. **Канон гейтов
  процесса — нет:** он по-прежнему называет `OBSERVED_ABSENT` способом
  закрыть слот. При этом заведённый тем же закрытием энфорсер снятых
  редакций молчит: его опознавательный шаблон видит одну письменную форму
  из двух.
- **Грунт.** Не торговый.
- **Цитаты (носители).**
  - `.claude/processes/roadmap-step-execution.md:713-716` — «все его
    слоты «пункт × кейс» закрыты (прогоном кейса **либо исходом
    `OBSERVED_ABSENT` с записанным допущением**). Дом правила и таблица
    слотов — реестр шага…; здесь правило не пересказывается».
  - `.claude/tests/source-api/okx/code-preconditions.md:35-38` — дом:
    «**Исход «событие не наступило» слот не закрывает** … закрывающей
    силы у него нет».
  - `.claude/tests/source-api/okx/plan.md:245`, `:256`, `:258-262` —
    «третий гейт не закрывает»; «**гейт не закрыт** — слот остаётся
    `PENDING`»; «решение держателя 2026-08-30 … **снимает** закрывающую
    силу».
  - `.claude/skills/update-roadmap-progress.md:153-161` — «Исход …
    (`OBSERVED_ABSENT`) слот **не закрывает** ни при какой записанной
    строке допущения».
  - `tools/retired-check.py:80-87` — запись реестра «закрывающая сила
    исхода OBSERVED_ABSENT», шаблон
    `Закрытие\s+исходом\s+`?OBSERVED_ABSENT`? | OBSERVED_ABSENT[^.\n]{0,60}закрыва\w+\s+(?:слот|гейт)`,
    разрешённое место — только `update-roadmap-progress.md`.
  - `.claude/tests/source-api/okx/code-preconditions.md:572-574` §Связи —
    «Дом гейта `CODE` (**оба условия**) —
    `.claude/processes/roadmap-step-execution.md` §Гейты», то есть реестр
    сам отправляет читателя в носитель со снятой редакцией.
- **Почему гейтит.** Это канон, на который ссылается сам реестр и по
  которому проставляется статус `CODE`. Открытым по нему остаётся п. 7
  (`AG1.7`, горизонт `fundingFee`) — ровно тот класс «условно
  наблюдаемого» события, ради которого прежняя редакция и вводилась.
  Читатель канона вправе закрыть слот «прогнан, событие не наступило» и
  ввести шаг в `CODE` с непроверенной посылкой — то, что решение держателя
  запретило. Направление ошибки **разрешающее**.
- **Уровень пробела.** Составной: **носитель** (живая копия снятой
  редакции вне дома — `carrier-levels.md`, `policy-home.md`) +
  **доказательство** (энфорсер объявляет полноту формы предмета, а меряет
  один порядок слов).
  - **Встречная форма предъявлена и измерена.** На плоском тексте
    `roadmap-step-execution.md` объявленный шаблон реестра даёт **0**
    вхождений; форма «глагол закрытия **перед** термином»
    (`закрыт\w+[^.]{0,80}OBSERVED_ABSENT`) — **1**: «закрыты (прогоном
    кейса либо исходом `OBSERVED_ABSENT`». Файл в разрешённые места этой
    записи не входит, то есть при совпадении шаблона проверка обязана была
    упасть. Шапка детектора
    (`tools/retired-check.py:16-25`) объявляет формой 1 «дословный термин
    снятой редакции»; термин здесь стои́т дословно — не совпал **порядок
    слов**, а не «другие слова», под которые шапка выводит своё
    исключение.
- **Целевой док.** `.claude/processes/roadmap-step-execution.md` §Гейты,
  условие 2 (снять перечисление способов закрытия — оно и объявлено
  непересказываемым); `tools/retired-check.py` — расширить шаблон записи
  до обеих форм и доказать осью батареи.
- **Ожидаемый владелец.** `knowledge-curator` (носитель пайплайна и
  инструмент), с уведомлением `tester` (потребитель гейта).
- **Трассировка.** Свипнул `OBSERVED_ABSENT` по живым носителям (кроме
  `progress/`, `history/`) → четыре живых носителя, три несут новую
  редакцию, один — старую → проверил, стои́т ли этот файл в разрешённых
  местах записи (`tools/retired-check.py:86`: только скилл) → прогнал
  объявленный шаблон и встречную форму по плоскому тексту цели → 0 против
  1 → сверил с шапкой детектора, что за форму он клеймит.
- **Варианты.**

  | Вариант | Что делает | Названная цена |
  |---|---|---|
  | A. Правка только канона: условие 2 сводится к «все слоты закрыты; способ закрытия — дом реестра» | Снимает пересказ и снятую редакцию одним ходом; §Гейты перестаёт противоречить собственной клаузе «здесь правило не пересказывается» | Правка одной строки; ноль связности |
  | B. Только расширить шаблон энфорсера | Ловит рецидив механически | Правка + проба; **дефект в каноне остаётся до следующего прогона** — энфорсер укажет на него, но сам не починит |
  | C. A + B | Носитель чист и класс охраняется | Сумма; шаблон записи становится порядко-независимым (`OBSERVED_ABSENT` в окне ±80 симв. от `закрыва\|закрыт`) |
  | D. Ничего: считать, что «с записанным допущением» уже неисполнимо, раз строки допущения нигде нет | Экономит правку | Разрешающая формулировка живёт в каноне гейта; **цена невидима до момента, когда кто-то ею воспользуется** — а воспользуется он ровно на п. 7, где событие может не наступить месяцами |

  **Крен — C.** A обязателен (носитель), B без A оставляет дефект жить;
  цена B — одна регулярка и одна проба, и она названа: без неё запись
  реестра даёт клейм полноты, которого её шаблон не производит.
- **Ярлык исхода.** `варианты-с-креном`.
- **Ярлык дефицита.** Дефицита нет — закрыто прогоном.

## F3. План утверждает, что перечень справочника сохранён; в репозитории его нет, а домов у факта названо два

- **Вопрос.** Слоты пп. 2 и 16 возвращены в `PENDING` именно потому, что
  перечень `type`/`subType` ушёл в лог. Колонка «Факт + наблюдения (RUN)»
  того же кейса в плане утверждает обратное — «перечень сохранён
  построчно». Каталог `observations/` при этом пуст. Вдобавок план
  предписывает сохранять перечень **в исход кейса**, а реестр и
  `structure.md` — в `observations/<КЕЙС>.md`: у одного факта два дома.
- **Грунт.** Источниковый факт (перечень справочника) — дефицит `грунт`
  остаётся открытым, он и есть предмет слота.
- **Цитаты (носители).**
  - `.claude/tests/source-api/okx/plan.md:2232` (ячейка «Факт») — «RUN
    2026-08-30 ✓ — **перечень сохранён построчно**: … **32 верхних типа и
    179 пар**».
  - `.claude/tests/source-api/okx/plan.md:2228` (заголовок того же кейса)
    — «✅ RUN 2026-08-30 (перепрогон) — … (предусловия пп. 2 и 16:
    **наблюдение сохранено мощностью, не перечнем**)» — противоречит
    собственной ячейке.
  - `.claude/tests/source-api/okx/plan.md:2262-2266` — «**Что делать при
    перепрогоне:** сохранить **все** пары `type`/`subType` … — **в исход
    кейса** (датированное наблюдение)».
  - `.claude/tests/source-api/okx/plan.md:2251-2260` — «Требуемый носитель
    — **отдельный инструмент наблюдения состава** в базовом классе контура
    (`observeContent(caseId, response)`…). Написание инструмента — работа
    `tester` в `src/test`, этап 4 контура»: инструмент уже написан
    (`OkxSourceApiLiveTestBase.java:390`), а требуемый сегодня
    `persistObservation` (`:422`) здесь не назван вовсе.
  - `.claude/tests/source-api/okx/code-preconditions.md:432-437` — дом:
    «перечень ушёл **в лог** … слоты закрыты не были».
  - `.claude/tests/source-api/okx/observations/` — в git только
    `.gitkeep` (`git ls-files`), файла `AG6_1.md` нет.
  - `.claude/rules/structure.md:38` — «**наблюдения кейсов**
    (`observations/<КЕЙС>.md` … пишет прогон, руками не правятся)».
- **Почему гейтит.** План — вход перепрогона и вход ревью отчёта (этапы
  5-6 контура). Читатель плана видит «✓ перечень сохранён» и «32 типа /
  179 пар» и делает вывод, что операнд пп. 2 и 16 добыт; отсюда прямая
  дорога к резолву категорий движения и списку исключений сверки по
  несуществующим данным. Дополнительно два объявленных дома одного факта
  нарушают П4 («факт — один дом»): исполнитель перепрогона не знает, куда
  писать, и «руками не правится» одного дома конфликтует с «записать в
  исход кейса» другого.
- **Уровень пробела.** **Концепция-стык** (два узла — план контура и
  новый персистентный носитель — держат одну истину, и контракт между
  ними не назван) поверх **носителя** (ложная ячейка «Факт»).
- **Целевой док.** `.claude/tests/source-api/okx/plan.md` §AG6.1 (ячейка
  «Факт», клауза «Что делать при перепрогоне», клауза «Чем перечень
  сохраняется»); `.claude/processes/source-api-testing.md` этап 5 (см. F12
  — тот же стык с другой стороны).
- **Ожидаемый владелец.** `tester` (план и кейсы контура — его дом по
  карте `question-delegation.md:28`).
- **Трассировка.** Реестр назвал `observations/AG6_1.md` носителем →
  проверил каталог (`.gitkeep` только) → открыл кейс в плане и его код-тест
  (`Ag6BillSubtypesLiveTest`: `observeContent` + `persistObservation`,
  оба существуют) → сверил ячейку «Факт» с реестром → нашёл
  противоречие внутри плана (заголовок против ячейки) → нашёл третью
  редакцию требования («в исход кейса») и устаревшее предписание написать
  уже написанный инструмент.
- **Варианты.**

  | Вариант | Что делает | Названная цена |
  |---|---|---|
  | A. Ячейку «Факт» привести к состоянию (мощность наблюдена, перечень не сохранён), клаузы перенацелить на `observations/`, устаревшее предписание снять | План согласуется с реестром и с `structure.md`; дом факта один | Правка трёх пассажей одного кейса; связность нулевая |
  | B. Оставить план домом перечня, а `observations/` не заводить | Один дом, привычный | **Переоткрывает решение последнего закрытия** и возвращает класс «факт в непроверяемой прозе»: ячейку правит рука, а не прогон, и расхождение с источником не ловится ничем |
  | C. Двойная запись: перечень и в `observations/`, и в ячейке «Факт» | Беглое чтение из плана сохранено | Второй носитель одного факта — прямой дефект по П4; расхождение копий не ловится ничем; отвергается доводом `policy-home` |
  | D. A + строка в §Структурный изоморфизм: «исход-перечень живёт в `observations/`, ячейка «Факт» несёт указатель и мощность» | Контракт стыка записан, а не выведен | A + одна клауза; контракт становится читаемым для будущих кейсов-перечней (их четыре: `AG6.1`, `AG6.2`, `AG1.7`, `AG3.6` — назван самим планом) |

  **Крен — D.** Счётный довод: план сам называет **четыре** кейса, чей
  исход есть перечень; без записанного контракта каждый из них
  переоткрывает развилку «куда писать» заново — этот же механизм и дал
  нынешнее расхождение.
- **Ярлык исхода.** `варианты-с-креном`.
- **Ярлык дефицита.** `грунт` — сам факт (перечень) добывается
  перепрогоном; **запись** контракта дефицита не имеет.

## F4. Рабочий файл предписывает исполнителю ложное расписание фандинга и не знает о заведённом носителе слота

- **Вопрос.** Единственный (по реестру) остающийся гейтящий слот — п. 7.
  Инструкция исполнителю в бэклоге предписывает ждать «границу
  funding-интервала (00:00 / 08:00 / 16:00 UTC)» — вывод, который реестр
  и план прямо называют **ложным**, — и утверждает, что «нужен только
  прогон», не зная о заведённом под слот отдельном кейсе. Сам новый кейс
  при этом не назван ни в строке реестра, ни в блоке воспроизводимых
  команд.
- **Грунт.** Источниковый (горизонт `fundingFee`), дефицит `грунт`
  остаётся.
- **Цитаты (носители).**
  - `.claude/work/backlog.md:570-573` — «Требуется удержание позиции через
    границу funding-интервала (**00:00 / 08:00 / 16:00 UTC**); ветвь
    сверки в код-тесте уже есть, нужен только прогон в подходящее окно».
  - `.claude/tests/source-api/okx/code-preconditions.md:522-533` — «Клейм
    … **был ложен**… интервал у инструмента определяется **разницей
    `fundingTime` и `nextFundingTime`** … на demo из 142 живых USDT-свопов
    **89 считают фандинг каждые четыре часа**… держался открытым по
    выводу, верному только для восьмичасового `ETH-USDT-SWAP`».
  - `.claude/tests/source-api/okx/plan.md:1773-1780` — та же поправка.
  - `docs/integrations/okx/contracts/funding-rate.md:38` — доковый дом:
    «**Интервал определять разницей этих полей**: типично 8 ч, биржа может
    сжать до 6/4/2/1 ч».
  - `.claude/tests/source-api/okx/code-preconditions.md:94` — строка п. 7,
    колонка «Носитель прогона»: «код-тест
    `Ag1DealFixtureLiveTest`.ag1_7_numericFieldSemantics» — кейс, которому
    фикстуру собрать не удалось; заведённый `Ag1FundingHorizonLiveTest`
    здесь не назван.
  - `.claude/tests/source-api/okx/code-preconditions.md:394-400`
    §«Воспроизводимые команды» — четыре строки `mvn -Dtest=…`,
    `Ag1FundingHorizonLiveTest` среди них нет.
  - `src/test/.../okx/Ag1FundingHorizonLiveTest.java` — кейс существует
    (класс `@Order(61)`, метод «п. 7 — горизонт fundingFee…»), назван в
    корпусе ровно один раз — `code-preconditions.md:537`.
- **Почему гейтит.** Это единственная инструкция исполнителю по
  единственному формально открытому слоту гейта `CODE`, и она
  **неисполнима как написана**: ожидание восьмичасовой границы на
  четырёхчасовом инструменте промахивается мимо ближайших двух границ, а
  указание «нужен только прогон» уводит на кейс, который по этому слоту уже
  провалился. Правило `closed-work-transfer` §«Диета рабочих файлов»
  прямо называет этот класс: рабочий файл адресован исполнителю
  следующего под-шага и опаснее докового.
- **Уровень пробела.** **Носитель** (снятая редакция факта живёт в
  рабочем файле; свип поправки до него не доехал).
- **Целевой док.** `.claude/work/backlog.md` §«Носители прогона гейтящих
  слотов шага 7…» пункт 1; `.claude/tests/source-api/okx/code-preconditions.md`
  (строка п. 7 — колонка «Носитель прогона»; §«Воспроизводимые команды»).
- **Ожидаемый владелец.** `tester`.
- **Трассировка.** Прочитал §«Что осталось открытым» реестра (поправка про
  4 ч) → свипнул `00:00 / 08:00 / 16:00` по корпусу и рабочим файлам →
  единственное живое вхождение — `backlog.md:571` → сверил с доковым домом
  `funding-rate.md:38` (говорит верно) → свипнул
  `Ag1FundingHorizonLiveTest` по всем разрешённым каталогам → одно
  вхождение → сверил со строкой реестра и блоком команд.
- **Варианты.**

  | Вариант | Что делает | Названная цена |
  |---|---|---|
  | A. Правка пункта бэклога: расписание снять, назвать кейс и способ выбора инструмента | Инструкция становится исполнимой | Правка трёх строк |
  | B. A + строка реестра п. 7 называет `Ag1FundingHorizonLiveTest`, блок команд получает свой `mvn -Dtest=` | Носитель слота виден там, где его ищут — в таблице и в командах | + две правки; ось 3 `preconditions-check.sh` начнёт сверять клейм по правильному кейсу (сегодня она проходит на любом из трёх файлов, упоминающих `AG1.7`) |
  | C. B + пункт бэклога сводится к указателю «состояние и способ — реестр §«Что осталось открытым»» | Снимает третий носитель одной инструкции | + правка; беглое «что осталось» из бэклога исчезает — то же соображение, что в F1/B |
  | D. Ничего (считать, что реестр читают раньше бэклога) | Экономит правку | Ложное предписание живёт в файле, который по правилу диеты читают **вместо** дома; цена уже реализовалась однажды — слот держался открытым по этому же выводу |

  **Крен — C.** Тот же довод, что в F1: три носителя одной инструкции
  расходятся при первой же правке, и они уже разошлись.
- **Ярлык исхода.** `варианты-с-креном`.
- **Ярлык дефицита.** `грунт` (сам факт) + записи дефицита не имеют.

## F5. Реестр приписывает новому кейсу персистентную запись исхода, которой на ветви пропуска нет

- **Вопрос.** Реестр утверждает, что заведённый под п. 7 кейс «вне окна
  пропускается assumption'ом **и пишет исход в персистентный носитель**».
  По коду персистентная запись стои́т **после** всех проверок: на ветви
  пропуска (штатной и ожидаемой) не пишется ничего. Различие «кейс не
  гонялся» / «гонялся и не дал события», названное планом **несущим**,
  в данных не появляется.
- **Грунт.** Не торговый.
- **Цитаты (носители).**
  - `.claude/tests/source-api/okx/code-preconditions.md:536-540` — «Кейс
    заведён — `Ag1FundingHorizonLiveTest`: он сам читает интервал …, **вне
    окна пропускается assumption'ом и пишет исход в персистентный
    носитель** (`observations/AG1_7.md`)».
  - `src/test/.../Ag1FundingHorizonLiveTest.java:105` —
    `Assumptions.assumeTrue(lead >= MIN_LEAD… && lead <= MAX_LEAD…)`:
    тест прерывается.
  - `src/test/.../Ag1FundingHorizonLiveTest.java:180-195` — единственный
    вызов `persistObservation`, ниже всех ассертов; на пропуске
    недостижим.
  - `.claude/tests/source-api/okx/plan.md:282` — «**Различие с `PENDING`
    несущее:** `PENDING` — кейс **не гонялся**, `OBSERVED_ABSENT` —
    гонялся и не дал события».
  - `docs/concept.md` §П1 следствие 4 — «**«Не проверяли» и «проверили,
    всё в порядке» обязаны различаться в данных**»; §П3 — «носитель
    наблюдаемости — **персистентная запись, не лог**».
- **Почему гейтит.** Это клауза дока **о коде** в доме гейта, и она
  опровергается кодом; по правилу верификации атрибуции
  (`concept-review` §«Клауза дока о коде верифицируется по коду») такое
  расхождение — дефект дока при любой стадии шага. Практическое
  следствие: слот п. 7 закрывается перепрогоном, и оператору нужен
  различимый след «прогон был, окно не подошло». Сегодня этот след
  существует только в консоли — ровно тот дефект, из-за которого пп. 2 и
  16 вернулись в `PENDING`, применённый к артефакту, заведённому для его
  устранения (детектор свипа «самоприменимость правил»).
- **Уровень пробела.** **Доказательство** (клейм о поведении носителя не
  выдерживает проверки на ветви, ради которой носитель и заведён).
- **Целевой док.** `.claude/tests/source-api/okx/code-preconditions.md`
  §«Что осталось открытым» (привести клаузу к коду) **или**
  `src/test/.../Ag1FundingHorizonLiveTest.java` (писать исход и на ветви
  пропуска) — выбор ниже.
- **Ожидаемый владелец.** `tester` (кейс контура), при выборе правки кода
  — он же, этап 4 контура.
- **Трассировка.** Реестр назвал две вещи о коде (читает интервал сам;
  пишет исход персистентно) → открыл кейс → первая подтвердилась
  (`fundingRate()` + разница `fundingTime`/`nextFundingTime`), вторая — нет:
  `persistObservation` один и стои́т после `assertRestoredOrHalt`-цепочки,
  ниже `assumeTrue` → сверил с клаузой плана о различии исходов и с П1-4.
- **Варианты.**

  | Вариант | Что делает | Названная цена |
  |---|---|---|
  | A. Правка клаузы реестра: «вне окна пропускается assumption'ом; исход пишется в персистентный носитель **при попадании в окно**» | Снимает ложный клейм | Правка одной строки; **различие «не гонялся / гонялся и промахнулся» остаётся невыразимым** — то есть П1-4 не удовлетворён, просто перестал быть обещан |
  | B. Правка кода: перед `assumeTrue` писать в `observations/AG1_7.md` строку «прогон <дата>, окно не подошло: lead=<...>, интервал=<...> ч» | Исход пропуска становится персистентным фактом; статус слота получает проверяемое основание | ~5 строк в кейсе; **второй** вызов `persistObservation` в одном кейсе — файл перезаписывается, поэтому последняя запись всегда актуальна (семантика «руками не правится» сохраняется) |
  | C. B + тот же приём у остальных кейсов с `assumeTrue` в контуре | Класс закрывается, а не экземпляр | Обход всех кейсов с assumption'ами; работа шире периметра шага 7 |
  | D. Ничего: считать статус реестра достаточным следом | Экономит правку | Статус пишет рука, а не прогон; «прогон был» становится клеймом без носителя — то самое, из-за чего два слота вернулись в `PENDING` |

  **Крен — B.** Счётный довод: A оставляет ровно ту дыру, которая уже
  один раз стоила двух слотов; цена B названа и мала, а C выходит за
  периметр шага и уходит в бэклог как отдельный пункт.
- **Ярлык исхода.** `варианты-с-креном`.
- **Ярлык дефицита.** Дефицита нет — закрыто прогоном.

---

# Прочее

## F6. Результат проверочной команды манифеста протух: объявлено 16 кейсов и девять методов, команда даёт 6 и пять

- **Цитаты.** `.claude/tests/source-api/okx/coverage-manifest.md:100-103`
  («Девять `🟡` — ровно те девять методов плана, у которых есть
  **непокрытые** кейсы (16 кейсов; из них 9 гейтят `CODE`)»),
  `:118-131` (команда + «→ **16** кейсов; их `##`-методы — `M1`, `M15`,
  `M17`, `AG1`, `AG3`, `AG6`, `AG12`, `MG7`, `MG9`: **девять**, и ровно
  они несут `🟡`»).
- **Измерение.** Дословный прогон записанной команды (GNU grep,
  `bash -c`): **6** кейсов — `AG12.4`, `AG12.5`, `AG3.5`, `M15.7`,
  `M1.7`, `MG9.5`; методов **пять** — `AG12`, `AG3`, `M1`, `M15`, `MG9`.
  Арифметическая половина клейма при этом сходится (прогон второй команды
  манифеста: `🟢`=49, `🟡`=9, `⚪`=2, `—`=10).
- **Почему прочее.** Направление расхождения консервативное: манифест
  утверждает покрытия **меньше**, чем есть. Правило отката требует 🟢
  только по **прошедшим ревью** код-тестам, а ревью новых тестов
  (этап 4 контура) в носителях не зафиксировано — то есть метки могут
  быть законны. Дефект в другом: **число предъявлено как измеренное**
  названной командой, а команда его больше не производит.
- **Уровень.** Доказательство. **Целевой док** — тот же манифест
  (§Легенда, счётные абзацы). **Владелец** — `tester`.
- **Варианты.** (a) пересчитать оба числа прогоном и датировать —
  цена: одна правка, класс воспроизведётся при следующем приросте тестов;
  (b) числа снять, оставив команду и правило «прогони и посмотри» —
  цена: беглое чтение теряется, зато носителя-двойника не остаётся;
  (c) вынести обе команды манифеста в `tools/` как восьмую команду гейта —
  цена: новый инструмент + батарея осей, зато счёт становится
  охраняемым, как у прочих. **Крен — (b)**, по доводу
  `design-simplicity` (меньший след) и по механизму F1 (переписываемое
  число расходится, вычисляемое — нет).
  **Исход** — `варианты-с-креном`; **дефицит** — нет.

## F7. Примечания 🟡-строк манифеста несут факты, опровергнутые прогонами 2026-08-30

- **Цитаты.** `coverage-manifest.md`, строка `Positions history` —
  «пять непокрытых кейсов … все **гейтят `CODE`** (…, пп. 1, 6, 7, **10**,
  14, 15)»: п. 10 выведен из-под гейта решением держателя 2026-08-30
  (`code-preconditions.md:315-336`). Строка `Bill types` — «`AG6.2` не
  покрыт (**ни код-теста**, ни делегирования)» и «у `AG6.1` код-тест
  есть, но **ассертит мощность** справочника»: у `AG6.2` код-тест есть
  (`Ag1DealFixtureLiveTest.ag6_2_typesOutsideDealEconomics`,
  `preconditions-check.sh` → «AG6.2 код-тест=2»), а тест `AG6.1` с
  2026-08-30 пишет перечень (`Ag6BillSubtypesLiveTest:56`). Строка
  `Cancel order` — «`M17.5` … не покрыт (ни код-теста…)»: есть
  (`M17CancelOrderLiveTest`). Строка `Index candles` — то же про `MG7.5`
  (`Mg7IndexCandlesLiveTest`).
- **Почему прочее.** Метка 🟡 сама по себе может быть законна (см. F6),
  но её **обоснование** ложно, и одно из обоснований (п. 10 гейтит)
  переоткрывает снятое решение держателя.
- **Уровень.** Носитель. **Целевой док** — манифест, колонка
  «Примечание» четырёх строк. **Владелец** — `tester`.
  **Варианты.** (a) переписать четыре примечания по фактике — цена: четыре
  правки; (b) заменить прозу примечания на перечень непокрытых кейсов,
  который печатает записанная команда — цена: примечание теряет довод
  отката, зато перестаёт быть вторым носителем. **Крен — (a)**: довод
  отката несёт знание, которого у команды нет.
  **Исход** — `варианты-с-креном`; **дефицит** — нет.

## F8. Оговорка манифеста о «букве правила» опровергнута самим правилом

- **Цитаты.** `coverage-manifest.md:30-38` — «Комментарий строки
  `.claude/tests/{testType}/{source}/` в `structure.md` **пока перечисляет
  только «тест-планы и библиотеку кейсов»** … Расширение предложено
  дословно в
  `.claude/work/progress/phase-1-step-7-gaps-close-27/node-H8.md` … **до
  его внесения** размещение опирается на §Принципы».
  `.claude/rules/structure.md:38` — строка уже называет и реестр
  предусловий, и манифест, и `observations/`.
- **Почему прочее.** Клейм ложен, но не меняет поведения: размещение
  правильно при обеих редакциях. Дополнительно — указатель из постоянного
  носителя в progress-файл закрытого прогона (класс, названный самим
  бэклогом, см. F13).
- **Уровень.** Гигиена. **Целевой док** — манифест §«Оговорка о букве
  правила» (снять целиком). **Владелец** — `knowledge-curator`.
  **Варианты.** (a) снять пассаж — цена: ноль; (b) переписать в «дом
  размещения назван строкой `structure.md`» — цена: одна строка.
  **Крен — (a)**: пассаж целиком есть довод, а не знание.
  **Исход** — `выводимо`; **дефицит** — нет.

## F9. Батарея осей `preconditions-check.sh` живёт вне команды, а гейт статуса утверждает обратное про все семь

- **Цитаты.** `tools/preconditions-check.sh:44-49` — «**ЧЕГО У ЭТОЙ
  КОМАНДЫ ПОКА НЕТ: батарея её осей живёт ВНЕ команды** — разделом
  `code-preconditions.md` §«Сверка реестра с планом…»».
  `.claude/skills/update-roadmap-progress.md:121-122` — «**Батареи осей
  исполняются теми же командами — отдельных проб-скриптов нет.**»
  `.claude/processes/roadmap-step-execution.md` §«Измерительная команда
  отказывается, когда мерить нечего» — «батарея осей исполняется **той
  же командой** и до измерения; недоказанная ось ⇒ измерение не
  проводится». Проверено прогонами: шесть команд печатают строки
  «доказана: …» (`spec-mutation-check`, `spec-scope-check`,
  `spec-pointer-check`, `anchor-check`, `retired-check`), седьмая
  (`preconditions-check.sh`) — нет.
  Долг назван: `.claude/work/backlog.md:694-712`.
- **Почему прочее.** Долг объявлен и имеет носитель с владельцем и
  моментом; дефект — в **клейме гейта**, который объявляет его
  несуществующим. Оператор, прочитавший гейт, батарею предусловий не
  прогонит никогда, а команда при этом всегда «измеряет» (кода 2 по
  недоказанной оси у неё быть не может).
- **Уровень.** Носитель (ложный клейм полноты на гейте) поверх
  доказательства (батарея вне команды). **Целевой док** —
  `.claude/skills/update-roadmap-progress.md` §«Гейт инструментов
  корпуса». **Владелец** — `knowledge-curator`.
  **Варианты.** (a) сузить клейм скилла: «батареи исполняются теми же
  командами, кроме `preconditions-check.sh` — её батарея внешняя, долг
  назван» — цена: одна строка, клейм становится верным сегодня и стареет
  при закрытии долга; (b) исполнить долг (перенести батарею внутрь) —
  цена: работа бэклога, зато клейм скилла становится верным без оговорок;
  (c) снять клейм скилла вовсе — цена: оператор перестаёт знать, что
  батареи вообще исполняются. **Крен — (a) сейчас, (b) как момент
  бэклога.**
  **Исход** — `варианты-с-креном`; **дефицит** — нет.

## F10. Ось «носители прогона» засчитывает носителем любое текстовое упоминание кейса

- **Цитаты.** `tools/preconditions-check.sh:143-153` — фактика носителя
  меряется `grep -rlE` по каталогу код-тестов.
  `.claude/tests/source-api/okx/code-preconditions.md:257-268` — «Колонка
  «Статус наблюдения» … вопрос «**а есть ли чем добывать?**» она не
  выражает вовсе … Цена решения названа: … проверяемость — ось 3
  `tools/preconditions-check.sh`: **клейм колонки сверяется с фактикой**».
- **Проба (встречная форма).** Временный каталог с единственным файлом
  `class Nothing { // TODO: кейс AG6.1 когда-нибудь напишем }` и штатной
  коллекцией: `bash tools/preconditions-check.sh <plan> <reg> $t/tests/
  $t/coll.json` → «AG6.1 **код-тест=1**», дефекта по этому кейсу нет
  (прочие кейсы падают, как и должны). То есть положительная сторона оси
  подтверждается комментарием.
- **Почему прочее.** Ложное срабатывание требует именно комментария с
  идентификатором кейса и без теста — на действительном состоянии его
  нет. Но клейм колонки («есть чем добывать») ось не производит, а
  реестр объявляет, что производит.
- **Уровень.** Доказательство. **Целевой док** — шапка
  `tools/preconditions-check.sh` (ось 3) и
  `code-preconditions.md` §«Колонка «Носитель прогона» — почему она
  отдельная». **Владелец** — `knowledge-curator`.
  **Варианты.** (a) сузить клейм: «ось меряет упоминание идентификатора
  в каталоге код-тестов, не наличие исполняемого теста» — цена: две
  строки, ось перестаёт давать клейм, которого не производит;
  (b) усилить ось до «идентификатор стоит в `@DisplayName` либо в имени
  метода» — цена: правка + две пробы; конвенция кейса становится
  обязательной для всех ~200 тестов контура (сегодня она соблюдается, но
  нигде не записана); (c) сверять по имени теста из колонки, а не по
  идентификатору кейса — цена: колонка становится машиночитаемой
  (формат `класс.метод`), правка 12 строк реестра. **Крен — (a)+(c)**:
  (a) чинит клейм немедленно и бесплатно, (c) даёт оси предмет, который
  она в состоянии измерить, и попутно закрывает половину F4 (строка п. 7
  называет не тот тест).
  **Исход** — `варианты-с-креном`; **дефицит** — нет.

## F11. Шапка `preconditions-check.sh` объявляет три оси, скрипт реализует четыре; письменные формы предмета не объявлены

- **Цитаты.** `tools/preconditions-check.sh:4-28` — перечень «Оси
  проверки»: 1) слот-сверка, 2) разведённость, 3) носители прогона.
  `:83-98` — реализованная позаголовочная охрана («каждый гейт-заголовок
  обязан дать хотя бы одну пару»), названная **осью 4** в реестре
  (`code-preconditions.md:139-146`) и доказанная там двумя пробами.
  Стандарт: `roadmap-step-execution.md` §«Полнота письменных форм
  предмета — обязательное свойство детектора» — «Детектор **объявляет в
  шапке**, какие письменные формы своего предмета он видит». Прочие пять
  детекторов несут блок «ФОРМЫ ПРЕДМЕТА, КОТОРЫЕ ДЕТЕКТОР ВИДИТ»; у этого
  его нет — формы плановой стороны («п. N», «пп. N и M», «пп. N, M и K»)
  названы внутри описания оси 1, формы реестровой стороны и формы
  идентификатора кейса — частично.
- **Уровень.** Гигиена (шапка) поверх доказательства (клейм «каждая
  доказана падающей пробой» покрывает три оси из четырёх).
  **Целевой док** — шапка скрипта. **Владелец** — `knowledge-curator`.
  **Варианты.** (a) дописать ось 4 и блок форм предмета — цена: 10 строк
  комментария; (b) объединить с F9(b): при переносе батареи внутрь шапка
  переписывается по шаблону остальных шести — цена: та же работа
  бэклога. **Крен — (b)**, ход общий с F9.
  **Исход** — `варианты-с-креном`; **дефицит** — нет.

## F12. Процесс контура не знает о персистентном носителе наблюдений

- **Цитаты.** `.claude/processes/source-api-testing.md:86-89` (этап 5
  RUN) — «Выход — **бланк отчёта (таблицы плана + колонка «факт»)** с
  ожиданием vs факт + наблюдения». `.claude/rules/structure.md:38` и
  `code-preconditions.md:437` — исход-перечень живёт в
  `observations/<КЕЙС>.md`, пишет прогон. Слова `observations` в процессе
  нет ни разу.
- **Почему прочее.** Процесс — дом того, как контур производит
  наблюдения; введённый носитель в нём не назван, поэтому следующий заход
  по контуру воспроизведёт исходный дефект (факт остаётся в бланке
  отчёта / логе). Обратная сторона F3: тот же стык, названный со стороны
  процесса.
- **Уровень.** Концепция-стык. **Целевой док** —
  `.claude/processes/source-api-testing.md` этап 5. **Владелец** —
  `tester`.
  **Варианты.** (a) дописать в этап 5: «исход-перечень пишет сам прогон в
  `observations/<КЕЙС>.md`; бланк отчёта несёт мощность и указатель» —
  цена: две строки, стык записан один раз для всех источников;
  (b) оставить знание в `structure.md` и реестре — цена: процесс, который
  читают перед заходом, о носителе не говорит, и правило приходится
  находить в таблице размещения. **Крен — (a)**, общий ход с F3/D.
  **Исход** — `варианты-с-креном`; **дефицит** — нет.

## F13. Перечень указателей из постоянных носителей в `work/progress/` неполон

- **Цитаты.** `.claude/work/backlog.md:2717-2721` — «Примеры на сегодня —
  **две** ссылки в `.claude/decisions/closure-mechanism-amendments.md` и
  ссылки в `.claude/tests/source-api/okx/coverage-manifest.md`».
  Свип по постоянным носителям (`rules`, `processes`, `skills`,
  `decisions`, `tests`, `templates`, `agents`, `knowledge-tree`, `docs`,
  `tools`) даёт сверх названного: `.claude/decisions/proof-method-change.md:19,139`
  и `.claude/decisions/acceptance-by-measurement.md:183` — оба решения
  заведены последним закрытием, оба несут тот же класс.
- **Уровень.** Гигиена. **Целевой док** — `.claude/work/backlog.md`
  §«Указатели из постоянных носителей в `work/progress/`». **Владелец** —
  `knowledge-curator`.
  **Варианты.** (a) дополнить перечень — цена: одна строка, перечень
  снова протухнет при следующем решении; (b) заменить перечень на
  воспроизводимую команду свипа — цена: одна строка, число всегда
  актуально. **Крен — (b)** (тот же довод, что в F1/F6).
  **Исход** — `варианты-с-креном`; **дефицит** — нет.

## F14. Новый кейс п. 7 вне структурного изоморфизма плана

- **Цитаты.** `.claude/tests/source-api/okx/plan.md:159-165`
  §«Структурный изоморфизм» — «**эндпоинт** → раздел плана / папка
  коллекции / **класс код-тестов**; **кейс** → подраздел плана + таблица /
  запрос коллекции / **тест-метод**». `Ag1FundingHorizonLiveTest` —
  второй класс на тот же эндпоинт (`positions-history`/`AG1`), его
  единственный тест-метод носит идентификатор `AG1.7`, у которого уже есть
  тест-метод в `Ag1DealFixtureLiveTest`; раздела/подраздела плана под
  новый носитель нет. `.claude/work/backlog.md:583-587` называет смежный
  открытый долг: «шестнадцать содержательных кейсов шага 7 в
  `collection.postman_collection.json` по-прежнему не представлены, и
  изоморфизм «план ↔ коллекция» не восстановлен».
- **Уровень.** Гигиена. **Целевой док** — `plan.md` §AG1.7 (назвать
  второй носитель) либо §Структурный изоморфизм (разрешить второй класс
  на эндпоинт с названным условием). **Владелец** — `tester`.
  **Варианты.** (a) назвать носитель в §AG1.7 и в строке реестра (ход
  общий с F4/B) — цена: две строки; (b) расширить изоморфизм клаузой
  «фикстурный кейс с собственным окном допустим отдельным классом» —
  цена: клауза + повод для будущих расщеплений. **Крен — (a)**.
  **Исход** — `варианты-с-креном`; **дефицит** — нет.

## F15. Вопрос `plan.md` в дереве знания составной

- **Цитаты.** `.claude/knowledge-tree.md:151` — «plan.md — Как проверяем
  API OKX на уровне сырья (**план + факт прогонов**)?»; собственная шапка
  файла (`plan.md:5-8`) несёт один вопрос без этого хвоста.
  `.claude/rules/structure.md` §Принципы — «**Вопрос — ровно один**».
- **Уровень.** Гигиена. **Целевой док** — `.claude/knowledge-tree.md`.
  **Владелец** — `knowledge-curator`.
  **Варианты.** (a) привести строку дерева к вопросу файла — цена: ноль;
  (b) считать хвост уточнением охвата — цена: правило «ровно один вопрос»
  теряет применимость к дереву. **Крен — (a)**.
  **Исход** — `выводимо`; **дефицит** — нет.

---

# Блокирующие открытые вопросы

`.claude/work/questions/open-questions.md` — **18** вопросов, у **каждого**
вердикт гейтинга стои́т первым абзацем (прогнана записанная механическая
проверка, вывод пуст). Гейтящих для шага 7 нет: 15 «не гейтит», 3
«зависимый» (`PNL-Q1`, `ORCH-Q2`, `RISK-Q2` — гейтящими станут при
названных исходах). Ни один вердикт не расходится с моей классификацией.

**Наблюдение (не находка).** Все 18 вердиктов датированы прогонами
`DOCS_CHECK_28` / `GAPS_CLOSE_28`; прогоны `_29`, `_30` их не
переставляли. Конвенция (`open-questions.md:522-525`) требует замены
строки «прогоном **нового шага**», а шаг тот же — формально нарушения
нет. Записываю потому, что механизм, ради которого поле заведено
(«расхождение нынешней классификации с записанной — находка»), три
прогона подряд не проверялся: несовпадения обнаружить было не с чем,
кроме собственного вывода.

# Вынос пользователю

**Пусто.** Ни одна находка не несёт дефицит `подтверждение`: все
разрешаются из записанного (`docs/concept.md`, дома правил, фактика
кода) и закрываются владельцами по карте. Дефицит `грунт` (перечень
справочника пп. 2/16 и горизонт `fundingFee` п. 7) — не вопрос
держателю, а сбор факта контуром; он и держит гейт.

# Сводка

- **Находок всего — 15.** Гейтящих — **5** (F1-F5), прочих — **10**
  (F6-F15). Пересчитано по телу отчёта: гейтящий раздел содержит F1, F2,
  F3, F4, F5; прочий — F6, F7, F8, F9, F10, F11, F12, F13, F14, F15.
- **Узлы гейтящих находок:**
  - F1 — сводный счёт открытых слотов реестра (реестр ×4, `backlog` ×3,
    `plan` ×1);
  - F2 — канон гейта `CODE` несёт снятую редакцию `OBSERVED_ABSENT`,
    энфорсер её формы не видит;
  - F3 — план утверждает сохранённый перечень справочника, которого нет;
    у факта названо два дома;
  - F4 — бэклог предписывает ложное расписание фандинга и не знает о
    носителе слота; носитель не назван ни в строке реестра, ни в
    воспроизводимых командах;
  - F5 — реестр приписывает новому кейсу персистентную запись исхода,
    которой на ветви пропуска нет.
- **Ярлыки исхода:** `варианты-с-креном` — 13; `выводимо` — 2 (F8, F15);
  `принято-в-работу` — 0; `неразрешимо-владельцем` — 0.
- **Ярлыки дефицита:** `грунт` — 2 (F3, F4, в части самого факта);
  «дефицита нет» — 13; `подтверждение` — 0; `работа` — 0.
- **Уровни пробела:** продукт — 0; концепция-стык — 2 (F3, F12);
  носитель — 5 (F1, F2, F4, F7, F9); доказательство — 4 (F2 второй
  половиной, F5, F6, F10); гигиена — 5 (F8, F11, F13, F14, F15).
- **Владельцы:** `tester` — 8 (F1, F3, F4, F5, F6, F7, F12, F14);
  `knowledge-curator` — 7 (F1 частью, F2, F8, F9, F10, F11, F13, F15).
- **Гейтящих, являющихся следствиями конструкций последнего закрытия —
  5 из 5** (вход сигнала C2 детектора стагнации):
  - F1 — возврат слотов пп. 2 и 16 в `PENDING` (конструкция «персистентный
    носитель наблюдений») выполнен, сводный счёт не пересобран;
  - F2 — снятие закрывающей силы `OBSERVED_ABSENT` и заведение
    `tools/retired-check.py` — обход потребителей не дошёл до канона
    гейтов, а объявленная форма энфорсера у́же собственного предмета;
  - F3 — введённый `observations/<КЕЙС>.md` не дошёл до плана, который
    остался вторым домом факта;
  - F4 — поправка о четырёхчасовом интервале фандинга и заведённый под
    неё кейс не дошли до рабочего файла и до строки реестра;
  - F5 — тот же новый кейс: клейм о его поведении не проверен по коду.
  Из прочих следствиями последнего закрытия являются F6, F7, F8, F9,
  F11, F12, F13 (**7 из 10**); самостоятельны F10, F14, F15.
- **Стадия остановки.** Стадия 0 не чиста (F1, F2, F4); обход предмета
  доведён до конца.
- **Вердикт линзы.** **Шаг 7 к `CODE` не готов.** Помимо двух незакрытых
  дефицитов `грунт` (перечень справочника — пп. 2 и 16; горизонт
  `fundingFee` — п. 7), сам гейт грунта описан противоречиво: его дом
  называет открытым один слот из трёх, его канон разрешает закрыть слот
  снятым исходом, а его инструкция исполнителю предписывает недостижимое
  окно и не тот носитель. Нужен `GAPS_CLOSE_31` и повторный
  `DOCS_CHECK_32`.

## Об объёме отчёта

77 KB против ориентира ~40 KB (`.claude/templates/docs/gap-report.md`
§«Потолок объёма»). Пересказ снят: полных выводов грепов в теле нет,
перечни носителей не повторяются, таблицы не дублируются. Превышение
составляют **мандатные поля**: у пятнадцати находок — список цитат с
точками, перебор вариантов с ценой в строке и трассировка; на них же
приходится около двух третей текста. По правилу «поля потолку не
уступают» оставлено как есть.

# Приложение: прогнанные измерения (воспроизводимы)

```bash
bash tools/preconditions-check.sh                       # rc=0, 9 кейсов, «СОШЛОСЬ»
bash tools/spec-run.sh                                  # rc=0, 22 спеки / 473 примера
bash tools/spec-mutation-check.sh                       # rc=0, 356 величин, переживают 0
python3 tools/spec-scope-check.py docs/spec             # rc=0
python3 tools/spec-pointer-check.py                     # rc=0, 201 указатель
python3 tools/anchor-check.py                           # rc=0
python3 tools/retired-check.py                          # rc=0, 846 носителей
mvn -o test-compile                                     # BUILD SUCCESS

# F1: пересчёт таблицы реестра построчно
/usr/bin/grep -cE '^\| *[0-9]+ *\|' .claude/tests/source-api/okx/code-preconditions.md   # 12 слотов
/usr/bin/grep -c 'PENDING' <(sed -n '88,99p' .claude/tests/source-api/okx/code-preconditions.md)  # 3

# F2: объявленная форма энфорсера против встречной (плоский текст цели)
python3 - <<'PY'
import re, io
t = re.sub(r'\s*\n\s*', ' ', io.open('.claude/processes/roadmap-step-execution.md', encoding='utf-8').read())
print(len(re.findall(r'Закрытие\s+исходом\s+`?OBSERVED_ABSENT`?|OBSERVED_ABSENT[^.\n]{0,60}закрыва\w+\s+(?:слот|гейт)', t, re.I)))  # 0
print(re.findall(r'закрыт\w+[^.]{0,80}OBSERVED_ABSENT', t, re.I))                                                                   # 1
PY

# F6: дословный прогон проверочной команды манифеста (GNU grep!)
#   → 6 кейсов, 5 методов (манифест объявляет 16 и 9)

# F10: проба ложного носителя
t=$(mktemp -d); mkdir -p "$t/tests"
printf 'class Nothing {\n  // TODO: кейс AG6.1 когда-нибудь напишем\n}\n' > "$t/tests/Nothing.java"
cp .claude/tests/source-api/okx/collection.postman_collection.json "$t/coll.json"
bash tools/preconditions-check.sh .claude/tests/source-api/okx/plan.md \
     .claude/tests/source-api/okx/code-preconditions.md "$t/tests/" "$t/coll.json"
#   → «AG6.1 код-тест=1» на файле без единого теста
rm -rf "$t"
```
