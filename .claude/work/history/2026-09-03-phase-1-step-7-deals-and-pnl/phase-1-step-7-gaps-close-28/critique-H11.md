# Критика закрытия узла Н11 `GAPS_CLOSE_28`

## На какой вопрос отвечает этот файл

Что независимая концепт-линза нашла в закрытии узла Н11.

## Рамка

- **Предмет** — `.claude/work/progress/phase-1-step-7-gaps-close-28/node-H11.md`
  и его правки в корпусе; не полный корпус.
- **Стойка** — `reviewer` / `concept-review`; критерий глубины —
  `.claude/processes/roadmap-step-execution.md` §3, включая
  §«Оси проверочной команды доказываются поимённо» и «обход ВСЕХ
  потребителей».
- **Метод** — чтение, греп и **прогон**. Батарея проб выполнена дословно,
  счётные клеймы пересобраны из операндов, восстановление манифеста
  сверено с git. Правок в корпус не вносил; временные копии удалены.
- **Снимок состояния — 09:39.** Отчёт правился автором **в ходе** моего
  прохода (`node-H11.md`: 09:29 → 09:30 → 09:36 → 09:38;
  `code-preconditions.md` — 09:35). Часть расхождений «отчёт против
  корпуса» по F4 за это время автор закрыл сам; ниже — **только то, что
  остаётся на 09:39**, с пометкой там, где вопрос был снят по ходу.

---

## 1. Батарея проб — прогнана целиком, дословно

Каждая проба §«Сверка реестра с планом — исполнимой проверкой»
выполнена как записана (мутации во временных копиях). **Не упавших проб
нет.**

| # | Проба | Обещано | Фактически | Вывод |
|---|---|---|---|---|
| 1 | пп. 20, 21 в гейт-пометке плана | `rc=1` | **`rc=1`** | «< AG1.5 20», «< AG1.5 21» — совпало |
| 2 | строка слота `AG1.9`-п. 1 удалена | `rc=1` | **`rc=1`** | «< AG1.9 1» — совпало |
| 3 | два кейса в одной строке реестра | `rc=1` | **`rc=1`** | «пункт 1 — в одной строке 2 кейса (AG1.5, AG1.9…)» |
| 4 | `AG1.5` объявляет несуществующий код-тест | `rc=1` | **`rc=1`** | «реестр объявляет код-тест, грепом … не найден» |
| 5 | носители `AG6.1` скрыты за «нет» | `rc=1` | **`rc=1`** | два `ДЕФЕКТ`а — совпало |
| 6 | носители подменены чужим `AG6X1` | `rc=1` | **`rc=1`** | два `ДЕФЕКТ`а |
| 7 | колонка шапки `Кейс` → `Case` | `rc=3` | **`rc=3`** | «шапка … не разобрана» |
| — | **действительное состояние** | `rc=0` | **`rc=0`** | «СОШЛОСЬ» |

**Двухсостоянность седьмой пробы подтверждена независимо.** Прежняя
форма скрипта (`git show HEAD:tools/preconditions-check.sh`) на той же
фикстуре даёт `AG6.1 код-тест=1 коллекция=1` → «СОШЛОСЬ», `rc=0`; новая
даёт `0/0` → два `ДЕФЕКТ`а, `rc=1`. Ось идентификатора доказана, а не
объявлена.

Проверил и саму сборку шаблона (инструментированная копия скрипта):
регулярка на прогоне — `(^|[^0-9A-Za-z])AG1\.5([^0-9]|$)`, точка
действительно экранирована; `M17.5` не совпадает ни с `M1755`, ни с
`M17.55`.

---

## 2. Гейтящие замечания

### Г1. `plan.md:292-293` несёт снятый счёт: «Двенадцать строк … 19 кейсов»

**Гейтит.**

> `.claude/tests/source-api/okx/plan.md:292-293`
> «Двенадцать строк манифеста несут `🟡 в плане` — это методы, у которых
> есть кейсы **без код-теста** (19 кейсов; девять из них гейтят `CODE` —
> `code-preconditions.md`).»

Фактика (пересобрана мной из операндов): строк `🟡` — **9**, непокрытых
кейсов — **16**. Обе величины исправлены хвостом закрытия (§10 находка 1)
в манифесте и в доме — и **не доехали до `plan.md`**, хотя таблица §2-F5
и свип П1-4 помечают `plan.md` как «правлен». Mtime подтверждает:
`plan.md` — 09:02, `coverage-manifest.md` — 09:24; хвостовая правка счёта
файл не трогала.

Это ровно класс §3 «Правка, затронувшая перечень и не затронувшая его
текстом счёт, оставляет ложный клейм — то есть инструкцию "дальше не
искать"», и ровно тот дефект, который мини-петля уже поймала один раз в
этом же закрытии. Пункт 4 чек-листа глубины F5 («счётный клейм
пересобран») по этому носителю не выполнен.

Пересборка (воспроизводимо):

```bash
awk -F'|' '/^\| / && NF>=7 { v=$5; gsub(/^[ \t]+|[ \t]+$/,"",v)
    if (v!="Покрытие") c[v]++ } END { for (k in c) printf "%-16s %d\n", k, c[k] }' \
  .claude/tests/source-api/okx/coverage-manifest.md
# → 🟢 49, 🟡 9, ⚪ 2, — 10
```

### Г2. Две строки манифеста несут `🟢 в коде` и текст отката в 🟡 одновременно

**Гейтит.**

> `.claude/tests/source-api/okx/coverage-manifest.md:195`
> «| Cancel algo (ordinary) | … | **🟢 в коде** | офдок | … `cancelAlgos`
> Откат 🟢→🟡: кейс `M20.2` (прямой + вариант) код-теста не имеет |»
>
> `.claude/tests/source-api/okx/coverage-manifest.md:196`
> «| Cancel advance algo | … | **🟢 в коде** | офдок | … `cancelAdvanceAlgos`
> Откат 🟢→🟡: кейс `M21.2` (прямой + вариант) код-теста не имеет |»

Хвост закрытия снял три ложных отката (§10 находка 1), **вернув метку**,
но фразу отката в «Примечании» убрал только у одной строки из трёх:
`Close position` (`:177`) чист, `Cancel algo` и `Cancel advance algo` —
нет. Строка утверждает о себе два взаимоисключающих факта.

Проверка: строк со словом «Откат 🟢» — **11**, строк `🟡` — **9**;
разница ровно эти две.

Цена не косметическая: манифест — **чек-лист инварианта полноты**, и
следующий читатель, применяющий записанное в строке правило, откатит
метку и разрушит счёт 49/9/2, который закрытие только что свело.
Это тот же класс «правка не доехала до места, где метка ставится»,
который мини-петля поймала на `source-api-testing.md` §Этапы п. 4.

### Г3. `test-design.md` пересказывает правило отката в дореформенной редакции

**Гейтит.**

> `.claude/skills/test-design.md:66-69`
> «**Прирост кейсов на уже покрытой строке откатывает её метку.** Кейс,
> добавленный к методу, у которого метка стоит `🟢 в коде`, обязывает
> вернуть строку в `🟡 в плане` — **до того, как на новый кейс появится
> код-тест**.»

Дом после хвостовой правки говорит другое:

> `.claude/processes/source-api-testing.md` §«Колонка покрытия в манифесте»
> «**"Непокрытый" ≠ "нет код-теста со своим идентификатором".** Кейс
> покрыт, если выполнено **одно из двух**: (а) на него есть прошедший
> ревью код-тест, либо (б) он **делегирован цепочке** …»

По формулировке потребителя делегированный кейс держит строку в `🟡`
**навсегда**: своего код-теста у него не появится никогда. Это прямое
противоречие дому — и именно оно дало три ложных отката, которые мини-
петля уже исправляла. Mtime: `test-design.md` — 09:05, определение
«непокрытого» приехало в дом позже; свип хвоста потребителя не перечитал.

Пункт 2 чек-листа глубины F5 («потребители обойдены, каждый сверен с
**новой** редакцией») по этому носителю не выполнен: он сверен со старой.

Отдельно: по `.claude/rules/policy-home.md` этот пассаж пересказывает
политику вне дома и должен быть сведён к упоминанию со ссылкой — что
разом снимает и расхождение.

---

## 3. Не гейтящие замечания

### Н1. Идентификаторная граница асимметрична; клейм шире доказанного

**Не гейтит** (латентно). Скрипт строит шаблон
`(^|[^0-9A-Za-z])<кейс>([^0-9]|$)`: слева исключены буквы и цифры,
справа — **только цифры**. Проверено прогоном настоящей конструкции из
`tools/preconditions-check.sh:112`:

- `AG6.1` совпадает с `AG6.1b` (1 попадание);
- `M16.neg` совпадает с `M16.neg.dupClId` (1 попадание).

В плане такие имена **есть**: `### M16.neg` и `### M16.neg.dupClId`
(`plan.md:842,851`), `### M19.trailing` и `### M19.trailing-spread`
(`plan.md:1141,1163`). В реестре кейсов с буквенным хвостом сегодня нет,
поэтому дефект латентен — но именно на латентности узел настаивал,
закрывая F11 («дефект был латентным … и всё равно чинится»).
Клейм в `code-preconditions.md` («кейс ищется **как идентификатор**») и в
шапке скрипта шире того, что доказано: седьмая проба покрывает только
левую границу и экранирование точки.

### Н2. Записанная независимая команда реализует половину клаузы (б) дома

**Не гейтит.** Дом требует: делегирование засчитывается, если строка
`Покрыт …` есть **и** «названные звенья код-тестами покрыты». Команда в
`coverage-manifest.md` §Легенда проверяет только первое
(`grep -q '^Покрыт '`). Вторая половина механически и не выйдет:
план пишет `M19trailing.cancel`, тест — `M19.trailing`
(`M19PlaceAlgoOrderLiveTest.java:150,165`), греп по звену даёт ноль.
Сегодня фактика в порядке — все 11 делегаций разрешаются, я проверил, —
но проверка слабее определения, которое объявляет своим предметом. Это
тот же класс, что §10 находка 2 («записанная проверка счёт не
проверяет»), на один уровень глубже.

### Н3. Обход потребителей `preconditions-check.sh` неполон

**Не гейтит** (правок не требуют, но пункт 2 требует их **назвать**).
Живые потребители, отсутствующие в таблице П2-1:

- `.claude/decisions/closure-mechanism-amendments.md:61-64` — несёт клейм
  о скрипте («все три оси поднимают дефект до кода возврата, каждая
  доказана падающей пробой») и `:87` — указатель на батарею;
- `.claude/rules/structure.md:63` — строка `tools/`, называет
  `preconditions-check.sh` и стандарт доказательства осей.

Оба клейма после правки Н11 остаются верными — потому исход «не требует
правки», а не дефект содержания; дефект — в полноте перечня.

### Н4. Строчные адреса в таблицах отчёта протухли

**Не гейтит.** `backlog.md` правился другими узлами, и якоря отчёта
разъехались:

- П2-1: `.claude/work/backlog.md:664,668` — сейчас там PNL-цена отказа и
  разрешимость R-выборки; действительный пассаж про гейт грунта —
  ~`backlog.md:723-740`;
- §2-F4: `.claude/work/backlog.md:1847-1854` (`RQ-4`) — `RQ-4` сейчас на
  `:1894`.

### Н5. §4: «вердикт у каждого вопроса — 16 из 16» не пересобирается

**Не гейтит** (механика держится). На 09:39 в `open-questions.md`
**19** вопросов и **19** вердиктов: два добавлены другими узлами того же
закрытия с пометкой `GAPS_CLOSE_28` (`:334` EXIT-Q1, `:352` CTX-Q1),
плюс ещё один. Записанная в конвенции команда прогнана мной дословно:
вывод пуст; двухсостоянно — на копии без одной строки печатает
`БЕЗ ВЕРДИКТА: ORCH-Q1.`. То есть **проверка верна, а число в отчёте —
нет**: это счётный клейм на живом операнде, и он уже разошёлся.

### Н6. Остаточное противоречие в лид-жирном заголовке §2-F4

**Не гейтит.** `node-H11.md:173` — «2. **Носитель назван; у одного
адресата — не существует.**», а тело того же пункта (`:186`) и §7
(`:549`) говорят «ЗАВЕДЁН координатором … критерий **выполнен**».
Заголовок пассажа утверждает противоположное содержанию. (Расхождение
«отчёт против корпуса» по F4 автор снял в 09:38 в ходе моего прохода —
осталась только эта строка.)

### Н7. Х3 частично снят событиями

**Не гейтит.** Х3 утверждает, что `tools/` «не значится ни строкой в
`.claude/rules/structure.md`, ни узлом в `.claude/knowledge-tree.md`».
Первая половина неверна: `structure.md:63` каталог **регистрирует**;
там же записано, что инструменты в `knowledge-tree.md` не индексируются
**сознательно** — то есть вторая половина не дефект, а решение.

### Н8. Х1 и заведённая секция называют разный состав этапа 4

**Не гейтит.** Х1 (`node-H11.md`) — «код-тесты на **16** непокрытых
кейсов, из них 9 гейтящих»; `backlog.md:437` — «Написать код-тесты
(этап 4) на **девять** кейсов без носителя». Обе величины осмысленны
(9 — гейтящий подмножеством), но в одном хенд-оффе они не разведены.

### Н9. «Шаг 3» в §«Гейт грунта» адресует два разных перечня

**Не гейтит.** `update-roadmap-progress.md:118-125`: «Шаг стои́т **перед**
шагом 3 потому, что шаг 3 читает **колонку статуса**». В файле два
нумерованных перечня, и §Шаги п. 3 — это «Обновить ячейку **«Статус»**
строки шага». Имеется в виду локальный п. 3 (колонка «Статус
наблюдения» реестра), но столкновение номеров и слова «колонка статуса»
читается двояко.

---

## 4. Что проверено и найдено чистым

- **Батарея проб** — семь падений + зелёное действительное состояние
  (§1); седьмая проба воспроизведена на двух формах грепа.
- **F2, шаг 0 гейта.** `update-roadmap-progress.md:113-131` — гейт
  **прогоняет** `bash tools/preconditions-check.sh` шагом 2, до чтения
  колонки статуса, «ненулевой код возврата → статус **не** проставлять».
  Применимость («есть ли у шага реестр») стои́т первой — шаг без реестра
  больше не ловит `rc=3`. Встречность есть: дом-реестр
  (`code-preconditions.md:67-76`) называет скрипт инструментом гейта и
  указывает на §«Гейт грунта».
- **F5, счёт манифеста.** Обе записанные команды прогнаны **из файла**:
  `🟢 49 / 🟡 9 / ⚪ 2 / — 10`, in-perimeter 60; независимый перечень даёт
  **16** непокрытых кейсов, их методы — `M1, M15, M17, AG1, AG3, AG6,
  AG12, MG7, MG9` (**9**), и ровно эти девять строк несут `🟡`. Ни одной
  `🟢`-строки с непокрытыми кейсами не нашёл (кроме дефекта Г2 — там
  расходится **текст**, не метка).
- **Схождение двух перечней.** 16 непокрытых кейсов и 16 содержательных
  кейсов шага 7 — **одно и то же множество** (`comm -3` пуст); 9
  гейтящих кейсов реестра целиком внутри него. Все 16 отсутствуют в
  коллекции (греп идентификаторным шаблоном — по нулю попаданий).
- **Легенда манифеста** колонку покрытия объясняет
  (`coverage-manifest.md:89-97`) и сведена к указателю на дом, а не к
  копии; уровень клейма полноты назван (`:53-57`).
- **Восстановление потерянных таблиц.** Набор операций секций `Trade` и
  `Public Data` **побитово совпадает** с `ea20cbd^` (md5 списка операций
  идентичен); поимённая сверка путей манифест ↔ план — **58 = 58**,
  `comm -3` пуст в обе стороны.
- **Счётные клеймы реестра** пересобраны из таблицы: слотов **13**,
  различных пунктов **11** (1, 2, 5, 6, 7, 9, 10, 14, 15, 16, 17),
  различных кейсов **10** — сходится.
- **Счёт кейсов плана**: 214 нумерованных идентификаторов + 11 именованных
  вариантов = **225**; два заголовка действительно несут по два
  идентификатора (`plan.md:512,667`).
- **F13.** Поимённая проверка вердиктов прогнана двухсостоянно (см. Н5).
  Три вердикта сверены с телом вопроса по существу: `ORCH-Q1` («не
  гейтит» — предмет шага 1, горизонт конец фазы 1) ✔; `PNL-Q1`
  («зависимый» — предмет шага 7, но признанная неизмеримость, гейтящим
  станет при выборе варианта с новым носителем) ✔; `ORCH-Q2`
  («зависимый» — предмет шага 6, назван горизонт) ✔.
- **F15/F9.** `grep -c '^\*\*Встречный якорь\.\*\*' backlog.md` → **4**,
  на секциях `:241, :286, :320, :387`, включая четвёртый якорь
  §«Снятие ограничения `MARK`-only …», который прежний шаблон не ловил.
  Встречная проверка `.claude/rules/` безусловна и сходится. Прямые
  кавычки `"таблицы пусты"` в живых носителях — **ноль** (кроме отчётов
  самой мини-петли).
- **F4, ярлык.** Довод «`подтверждение` неприменимо» проверен по дому:
  `question-delegation.md:41-50` действительно сводит его к двум
  предметам (численные величины риск-аппетита, продуктовый scope).
  Отказ от расщепления, предложенного линзой, обоснован корректно.
- **Разрешимость §-адресов**, введённых закрытием в правленные файлы, —
  проверена, битых нет.
- **§Этапы п. 4** дома выровнен с правилом отката («все кейсы строки
  покрыты — код-тестом либо делегированием»).

---

## 5. Вердикт

**Узел не чист. Гейтящих замечаний — 3** (Г1, Г2, Г3), не гейтящих — 9.

Все три гейтящих — **один класс**: хвостовая правка счёта и определения
(«19/12» → «16/9», «непокрытый» = нет код-теста **и** нет делегирования)
доехала до дома и до манифеста, но не до трёх носителей, которые её
пересказывают: `plan.md`, две строки самого манифеста и
`test-design.md`. Мини-петля уже ловила этот класс дважды в этом же
закрытии (§10 находки 1 и 3) — третье повторение означает, что свип
хвоста прогонялся по **изменённым файлам**, а не по **потребителям
изменённой величины**, как требует пункт 2 критерия глубины.

Остальное — гигиена и полнота перечней; ни одно из не гейтящих
замечаний не отменяет закрытия F2, F5, F6, F11, F12, F13, F15, F9,
которые я проверил исполнимо и нашёл состоятельными.

Отдельно отмечу как сильную сторону: батарея проб воспроизводится
дословно и падает по каждой объявленной оси; счётные клеймы манифеста
и реестра пересобираются из операндов; восстановление потерянных таблиц
сверено с git и с планом поимённо. Это уровень доказательности, которого
критерий требует, — дефект не в нём, а в радиусе хвостового свипа.
