# Линза B — правила системы, риск-контур, P&L (фаза 1, шаг 7, `DOCS_CHECK_26`)

Направление синхронизации — **ведёт док** (до `CODE`). «В доке есть, в коде
нет» находкой не является; клеймы дока о фактическом устройстве кода
верифицированы грепом по `src/` (один клейм оказался ложным — B9).

## Охват

**Прочитано целиком (31/31 `docs/rules/`):** `absent-value-semantics`,
`ack-not-runtime-truth`, `audit-not-runtime-source`, `command-lifecycle`,
`condition-ruletype-granularity`, `controlled-exchange-exceptions`,
`deal-without-operations`, `error-handling-policy`, `exchange-hold`,
`execution-hierarchy`, `exit-teardown-order`, `external-status-resolution`,
`idempotency-via-unique`, `instrument-hold`, `live-risk-protection`,
`market-data-freshness`, `market-data-retention`, `no-partial-close`,
`persistence-representation`, `pnl-reconciliation`,
`raw-exchange-dto-boundary`, `replace-not-amend`, `risk-policy`,
`risk-validator-scope`, `runtime-error-classification`,
`strategy-condition-contract`, `strategy-validation`, `time-utc`,
`trading-configuration-ownership`, `trading-constraints`,
`writer-named-for-every-value`.

**Процесс:** `docs/processes/risk-evaluation.md`.

**Компоненты (охват линзы):** `RiskValidator`, `RiskBlockResolver`,
`SizeCalculator`, `PriceCalculator`, `CalculationContextFactory`,
`HoldService`, `SafetyHoldCoordinator`, `KillSwitchService`,
`KillSwitchExecutor`, `RetryPolicyService`.

**Компонентные модели:** `RiskCheckResult`, `RiskValidationResult`,
`RiskBlockAction`, `CalculatedPrice`, `CalculatedSize`,
`CalculationContext`, `CalculationError`, `HoldSignal`.

**Доменные модели:** `DealCashFlow`, `TradeFeeRate`.

**Открыто ради верификации кросс-ссылок и атрибуций** (перенос атрибуции в
отчёт без открытия цели запрещён): `docs/concept.md`,
`docs/models/domain/aggregate/Deal.md`, `.../Strategy.md`,
`docs/models/domain/other/Auditable.md`, `docs/models/domain/core/Exchange.md`,
`docs/models/domain/core/Instrument.md` (§Status),
`docs/models/mapping/DealCashFlow.md`,
`docs/components/FinalizeDealExitExecutor.md`,
`docs/components/MarkDealClosedExecutor.md`,
`docs/components/MarkDealEmergencyClosedExecutor.md`,
`docs/components/RefreshBillsExecutor.md`,
`docs/components/RefreshPositionExecutor.md`,
`docs/components/CreateOrderExecutor.md`,
`docs/components/CancelOrderExecutor.md`,
`docs/components/SubmitOrderExecutor.md`,
`docs/components/SystemActionExecutor.md`,
`docs/components/DealOrchestratorJob.md`,
`docs/components/InstrumentExternalRulesSyncJob.md`,
`docs/components/InstrumentExternalRulesDataService.md`, весь `docs/spec/`
(15 файлов — по заголовкам; `risk-limits.json`, `pnl-reconciliation.json`,
`deal-lifecycle.json` — телом),
`src/main/java/.../domain/command/calc/CalculatedSize.java`, `.../SizeMode.java`.

**Рабочие файлы:** `.claude/work/questions/open-questions.md` — целиком;
`.claude/work/backlog.md` — **прицельно** (140 KB; §«Шаг 7 — исполнительный
хвост», §«Закрытие фазы 1 — снятие разведочного режима», §«CODE cross-ccy»,
§«CODE стадий 1-2» + греп по предмету шага). Целиком не читался — уровень
сверки назван.

**Вне охвата линзы:** lifecycles, `docs/models/integrations/`,
`docs/integrations/okx/`, tranche-handlers, market-data компоненты — линза A.

## Стадия остановки

**Прошёл все стадии (стадия 2).** Гейтящего вопроса стадии 0 в смысле
«неизвестна механика» нет: кто добывает движения, кто линкует, кто считает
число, кто ставит терминал — описано и по последовательности
непротиворечиво. Гейт стои́т не на механике, а на **вычислимости,
наблюдаемости и разрешимости собственного гейта шага** — находки B1-B5.

---

# Находки — блокирует выход в CODE

## B1. У `resultProfit` — заголовочной величины шага — нет исполнимой спецификации; формула живёт прозой в пяти доках, а её арифметика — в рабочем файле

**Вопрос.** Шаг 7 обязан посчитать `resultProfit`. Концепция требует
машинно-проверяемой формы «одним экземпляром, с прогоном против примеров», а
доменному доку предписывает «называть величину, её роль и её писателя и
**ссылаться** на спецификацию, не переписывая формулу словами». Фактически:
спецификации у `resultProfit` нет; словесная редакция формулы живёт в пяти
доках; арифметика второго слагаемого не выражена в корпусе вовсе — она
существует только строкой CODE-инструкции в `backlog.md`.

**Цитаты (носители).**
- `docs/models/domain/aggregate/Deal.md:131-156` — блок
  `resultProfit = Σ net по эпизодам + cross-ccy-слагаемое`; предикат области
  cross-ccy прозой («курс применён **и** тип вне списка исключений биржи
  **и** категория экономическая»); §Связи (`Deal.md:415-423`) на
  спецификацию результата не ссылается.
- `docs/rules/trading-constraints.md:94-98` — вторая редакция того же
  предиката.
- `docs/components/FinalizeDealExitExecutor.md:23-32` — третья.
- `docs/components/MarkDealEmergencyClosedExecutor.md:16-24` — четвёртая.
- `docs/models/mapping/PositionCloseResult.md:89` — пятая.
- `docs/spec/` — 15 файлов; `resultProfit` встречается только в
  `deal-lifecycle.json:13,47` как **вход** предиката терминального ребра
  (`cleanTerminalContract`). Верифицировано грепом по всем `docs/spec/*.json`
  на `resultProfit|externalRealizedProfit`.
- `.claude/work/backlog.md:1176-1179` — «слагаемое Σ(`amount` ×
  `appliedRate`) по строкам `rateStatus = APPLIED` считает финализатор;
  недоступный курс — `appliedRate = null`, `rateStatus = RATE_UNAVAILABLE`,
  слагаемое не вносится, долг догона, курс не подставляется». Это **и есть**
  недостающая арифметика; она живёт в рабочем файле, а не в доме, вопреки
  `.claude/rules/closed-work-transfer.md` §«Диета рабочих файлов»
  («рабочий файл — не носитель политики»).
- Контраст: у соседних величин шага спецификация есть —
  `docs/spec/deal-risk-numbers.json`, `docs/spec/pnl-reconciliation.json`,
  `docs/spec/cash-flow-linkage.json`, `docs/spec/order-sizing.json`.

**Целевой док.** Новый `docs/spec/deal-result-profit.json` (`episodeNet`,
`crossCcyTerm`, `resultProfit`, `resultAvailable` + примеры, включая ветки
«net недоступен» и «строка чужой валюты без курса»); свод прозы в `Deal.md`
к «величина, роль, писатель + ссылка»; в `trading-constraints.md`,
`FinalizeDealExitExecutor.md`, `MarkDealEmergencyClosedExecutor.md`,
`PositionCloseResult.md` — упоминание со ссылкой вместо повтора; строка
`backlog.md` сводится к «что сделать + дом».

**Ожидаемый владелец.** `solution-designer` (форма и место спецификации);
арифметика перевода по курсу — `trading-specialist`.

**Трассировка.** Открыл `Deal.md` §«Итоговый результат сделки» → искал дом
формулы; §Связи отправляет в `deal-risk-numbers.json` (числа риска) и
`pnl-reconciliation.md` (сверка), но не в спецификацию самого числа →
перечислил `docs/spec/` и прогрепал по имени величины → нашёл только
вхождение-операнд → прогрепал корпус на «cross-ccy | чужая валюта |
применённому курсу» (пять прозаических носителей, ни одной формулы: не
названы ни направление умножения, ни знак, ни обращение со строками
`RATE_UNAVAILABLE`) → прогрепал `backlog.md`, где арифметика и нашлась.

**Варианты.**

| вариант | что делает | названная цена |
|---|---|---|
| **A. Завести `docs/spec/deal-result-profit.json`, прозу свести к ссылке, строку backlog — к указателю** (крен) | величина получает одну машинно-проверяемую редакцию и примеры; арифметика переезжает из рабочего файла в дом | правка пяти доков + новый файл; контекстная связность падает (потребитель читает один вход вместо пяти) |
| B. Оставить прозу, объявить `Deal.md` домом формулы | правок ноль | прямо противоречит `concept.md` §«Исполнимая форма» («дом формулы — спецификация, а не проза»); пять редакций живут дальше, расхождение обнаружится инцидентом |
| C. Вписать величину в существующий `pnl-reconciliation.json` | нового файла не заводим | смешивает два вопроса: спецификация отвечает «сошлась ли сверка», а не «чему равно число» (`concept.md` §«Один вопрос на файл» действует и на спецификации) |
| D. Крен A **плюс** отдельная величина `crossCcyTerm` с собственным предикатом области и явной веткой `RATE_UNAVAILABLE` | закрывает не только форму, но и направление ошибки при неполученном курсе | требует продуктового решения: недосчитать число или объявить его недоступным — выбор направления ошибки (П1 §3), не вывод машины |

**Ярлык исхода.** `варианты-с-креном` (крен — A с поглощением D).

**Ярлык дефицита.** `работа` — по форме и месту; **плюс `подтверждение`** по
составной половине D (направление ошибки при неполученном курсе).
Оговорка: `backlog.md:1178-1179` уже фиксирует крен («слагаемое не
вносится, курс не подставляется») — если это решение держателя, половина D
закрыта, и находка сводится к переносу решения в дом.

---

## B2. Писатель курса пересчёта не назван ни одним компонентом; обе кросс-ссылки на его дом — битые

**Вопрос.** `DealCashFlow` несёт пять значений, целиком относящихся к
пересчёту чужой валюты (`appliedRate`, `rateStatus` с четырьмя значениями,
три координаты свечи). `writer-named-for-every-value.md` требует назвать
актора и момент **на каждое значение перечисления** и проверить писателя на
достижимость. Фактически писатель назван нарицательно («пересчёт чужой
валюты»), а оба дока, на которые указывают кросс-ссылки, обещанных клауз не
несут. Отсюда cross-ccy-слагаемое (B1) неисчислимо по построению: операнд
некому произвести.

**Цитаты (носители).**
- `docs/models/mapping/DealCashFlow.md:75-82` — таблица «Что проставляет
  вызывающий»: строка `appliedRate, rateStatus, три координаты свечи` →
  «пересчёт чужой валюты». Соседние строки называют акторов (`писатель при
  сохранении`, `резолвер категории`, `предикат линковки`) — эта не называет.
- `docs/models/mapping/DealCashFlow.md:123` — §Связи: «Кто применяет —
  `docs/components/RefreshBillsExecutor.md`».
- `docs/components/RefreshBillsExecutor.md` (весь файл, 83 строки) — слов
  «курс», «свеча», `appliedRate`, `rateStatus` не содержит. **Кросс-ссылка
  битая.** Верифицировано грепом `курс` по всему `docs/components/`:
  единственное вхождение в каталоге — `FinalizeDealExitExecutor.md:32` («по
  применённому курсу»), и это **чтение**, не запись.
- `.claude/work/backlog.md:1167-1179` — CODE-инструкция называет домом
  политики «`docs/components/RefreshBillsExecutor.md` §«Политика отказа
  котировки и догон»» и «`docs/rules/pnl-reconciliation.md` реш.5».
  **Обеих секций в текущих доках нет** (верифицировано полным чтением обоих
  файлов и грепом заголовков `pnl-reconciliation.md`: §«Что сверяется…»,
  §Допуск, §«Реакция на расхождение», §«Разведочный режим допуска», §Связи).
  То есть переписывание корпуса сняло дом политики курса, а инструкция
  продолжает на него указывать.
- `docs/models/domain/other/DealCashFlow.md:27-31` — поля курса;
  `:59-72` — четыре значения `RateStatus` (`NOT_REQUIRED`, `APPLIED`,
  `RATE_UNAVAILABLE`, `SETTLE_CURRENCY_UNAVAILABLE`), ни у одного не назван
  писатель; `:71-72` — «множество строк, **ждущих догона курса**»: переход
  `RATE_UNAVAILABLE → APPLIED` объявлен, актора и момента у него нет.
- `docs/rules/writer-named-for-every-value.md:23-25` («Пассивный залог
  писателя не называет… Нет подлежащего-компонента — требование не
  выполнено») и `:27-38` (§«Писатель обязан быть достижим»).

**Целевой док.** `docs/components/RefreshBillsExecutor.md` — вернуть §резолва
курса (источник свечи, момент, что пишется в каждом из четырёх исходов,
политика догона) **либо** назвать отдельного дозаполнителя; плюс таблица
«тропа → природа → значение → сигнал» в
`docs/models/domain/other/DealCashFlow.md` по
`docs/rules/absent-value-semantics.md:53-60`.

**Ожидаемый владелец.** `solution-designer`; выбор источника курса (пара и
таймфрейм свечи, правило деградации) — хэнд-офф `integrator`, уже заведённый
в `backlog.md:1180-1185`.

**Трассировка.** Пришёл от B1: искал производителя операнда
cross-ccy-слагаемого → `mapping/DealCashFlow.md` назвал «пересчёт чужой
валюты» и отправил в `RefreshBillsExecutor` → открыл целевой док целиком
(правило верификации атрибуции) → клаузы нет → прогрепал `docs/components/`
на «курс» → писателя не называет **ни один** компонент → проверил обратное
направление (модель называет только потребителей) → нашёл в `backlog.md`
указатель на §, которой в доке больше нет.

**Варианты.**

| вариант | что делает | названная цена |
|---|---|---|
| **A. Писатель — `RefreshBillsExecutor`, курс резолвится в момент сохранения строки; догон — повторный проход той же команды по строкам `RATE_UNAVAILABLE`** (крен; совпадает с редакцией, зафиксированной в `backlog.md`) | один актор, один момент, курс воспроизводим по трём координатам свечи | команда добычи получает вторую ответственность (движения + свечи) — проверить против `command-lifecycle.md:12-22` §«один владелец исхода»; свеча на момент операции может быть ещё не загружена, тогда `RATE_UNAVAILABLE` — штатный исход первого прохода |
| B. Отдельный дозаполнитель курса (своё системное действие/звено) | добыча остаётся однозадачной; догон получает собственную строку исполнения и бюджет попыток | новая сущность-компонент — больший след (`.claude/rules/design-simplicity.md`); нужен гейт эмиссии и предикат обязанности |
| C. Курс резолвит финализация выхода в момент расчёта числа | писатель уже существует и уже читает эти поля | на аварийной тропе финализации нет — строки чужой валюты остаются без курса навсегда; курс «на момент операции» пришлось бы поднимать задним числом |
| D. Догона нет: `RATE_UNAVAILABLE` терминален, слагаемое считается без этих строк | минимальный след | направленное смещение числа вниз на всей популяции и молча — запрет П1 §3 и `absent-value-semantics.md:25-39` |

**Ярлык исхода.** `варианты-с-креном` (крен — A; B выписан наравне, потому
что цена A ложится на клаузу атомарности команды).

**Ярлык дефицита.** `работа`.

---

## B3. У настройки сверки дом решён в рабочем файле, но не назван ни одним доком: два носителя ссылаются друг на друга по кругу

**Вопрос.** Шаг 7 обязан прочитать допуск сверки (`relativeShare`,
`omissionMultiplier`, `floor`), режим калибровки и per-exchange список
исключений сверки. В корпусе ни у одной из этих величин носитель не назван:
два дока адресуют её друг другу кольцом, три числа допуска не адресованы
никуда. Решение о доме существует — но живёт CODE-инструкцией в
`backlog.md`, то есть в носителе, который читатель дока не открывает.
`concept.md` §П4: «У каждого факта ровно один носитель-источник истины».

**Цитаты (носители).**
- `docs/rules/pnl-reconciliation.md:88-92` — «**Носитель режима** —
  per-exchange настройка конфигурации, **тот же дом, что у списка исключений
  сверки**».
- `docs/models/mapping/DealCashFlow.md:94-95` — «Список исключений —
  per-exchange настройка конфигурации, **там же, где режим допуска**».
  Кольцо замкнуто; ни одна сторона носитель не называет.
- `.claude/work/backlog.md:1186-1194` — «**Список исключений сверки по бирже
  — дом решён, содержание открыто.** Носитель — `@ConfigurationProperties`
  per-exchange». Решение есть, в корпус не доведено.
- `docs/spec/pnl-reconciliation.json` §operands — `tolerance.relativeShare`,
  `tolerance.omissionMultiplier`, `tolerance.floor` объявлены операндами
  формулы; дома у них нет (у соседних операндов дом назван моделью:
  `DealCashFlow.amount`, `Instrument.settleCurrency`).
- `docs/models/domain/core/Exchange.md:12-22` — структура биржи: `id`,
  `internalId`, `name`, `baseUrl`, `status` плюс аудит. Гипотеза «дом —
  строка биржи» проверена и не подтверждена.
- Верифицировано грепом по всему `docs/` на «список исключений | режим
  допуска | разведочн»: шесть вхождений, все перечислены выше; дока-носителя
  настройки в корпусе нет.
- `docs/rules/pnl-reconciliation.md:96-97` — «**Кто фиксирует** — человек,
  сервисной операцией снятия флага»: актор назван ролью, компонент и точка
  входа — нет.

**Целевой док.** `docs/rules/pnl-reconciliation.md` §«Разведочный режим
допуска» и `docs/models/mapping/DealCashFlow.md` §«Область сверки» — назвать
дом поимённо вместо кольца; при выборе персистентного носителя — модель
настройки контура.

**Ожидаемый владелец.** `solution-designer` (если решение
`@ConfigurationProperties` держательское и окончательное —
`knowledge-curator`: доведение решения до дома).

**Трассировка.** Читал `pnl-reconciliation.md` под шаг → искал, откуда шаг
берёт три числа допуска → §«Допуск» отправляет в спецификацию, спецификация
объявляет их операндами без дома → §«Разведочный режим» отправляет к «дому
списка исключений» → открыл целевой док, он отправляет обратно → проверил
единственного правдоподобного кандидата (`Exchange.md`) → полей нет →
грепнул корпус целиком → нашёл решение в `backlog.md`.

**Варианты.**

| вариант | что делает | названная цена |
|---|---|---|
| **A. Довести до дома уже принятое: `@ConfigurationProperties` per-exchange, названный в `pnl-reconciliation.md`; кольцо разорвать** (крен) | минимальный след, решение не переоткрывается; шаг получает адрес операндов | смена режима остаётся ненаблюдаемым событием: нет строки, нет момента, нет актора — П3 требует «носитель наблюдаемости — персистентная запись, не лог» |
| B. Доменная модель настройки контура (своя таблица, FK на биржу) | режим, три числа и список живут строкой; писатель — сервисная операция с именем; аудит достаётся от `Auditable` | новая сущность + миграция; переоткрывает записанное в `backlog.md` решение — выписан, потому что счётная разница названа (наблюдаемость смены режима) |
| C. Колонки на `Exchange` | ноль новых сущностей | `pnl-reconciliation.md:91` уже отверг колонку («режим — свойство контура, а не строки данных»); выписан, чтобы отказ был виден |
| D. Крен A **плюс** разведение: числа допуска — калибруемые данные, список исключений — статическая карта типов источника | различает калибруемое и контрактное | два носителя вместо одного; но факты **разные**, и совмещение природ само по себе — основание выписать вариант |

**Ярлык исхода.** `варианты-с-креном` (крен — A).

**Ярлык дефицита.** `работа` — по доведению до дома. Величины трёх чисел
предметом находки не являются: они калибруются
(`pnl-reconciliation.md:93-98`, `backlog.md` §«Закрытие фазы 1»), и
некалиброванный выход в `CODE` — решение держателя.

---

## B4. Аварийный терминал: сверка приписана недостижимому актору, полнота разбивки остаётся без писателя, статус сверки пишется условно при заведённом `NOT_RUN`

**Вопрос.** Три сцепленных дефекта, закрываемых одним ходом. (1) Актором
сверки названа «финализация выхода», которой на аварийной тропе не
существует. (2) `breakdownIncomplete` не пишет **никто**, хотя значение
`NOT_ASSESSED` заведено ровно под этот случай. (3) `reconciliationStatus`
пишется «если сверка была обязана», хотя `NOT_RUN` означает именно «не была
обязана», а пустота на `Deal` зарезервирована под другой смысл. Итог:
сделка, закрытая аварийно после реальных операций, уходит в терминал с
признаками отбора в состоянии, которое корпус читает как «тропа закрытия без
входа», — попадает в популяцию отчёта под чужим ярлыком
(`absent-value-semantics.md` §«Благоприятное умолчание»).

**Цитаты (носители).**
- `docs/rules/pnl-reconciliation.md:23-24` — «**Гоняет сверку финализация
  выхода** на штатной тропе; на аварийной — если движения уже добыты».
- `docs/components/SystemActionExecutor.md:56-62` — таблица финализации:
  «аварийная тропа | ребро в ошибку и **аварийный терминал** — два отдельных
  исполнения». Звена «финализация выхода» на аварийной тропе нет ⇒ названный
  актор недостижим (`writer-named-for-every-value.md:27-38`).
- `docs/components/MarkDealEmergencyClosedExecutor.md:31-34` — «Пишет
  торговый исход закрытия… и доступность знаменателя; **статус сверки — если
  сверка была обязана**». Полнота разбивки в перечне отсутствует.
- `docs/components/FinalizeDealExitExecutor.md:67-74` — штатный писатель
  пишет **все четыре** признака безусловно: асимметрия налицо.
- `docs/models/domain/aggregate/Deal.md:55-56` — «Все признаки отбора пусты
  до терминала; **пусто у них означает „признак неприменим“ — тропа закрытия
  без входа**».
- `docs/models/domain/aggregate/Deal.md:85-88` — `BreakdownCompleteness`:
  `NOT_ASSESSED` — «добыча движений не выполнялась — **единственный
  триггер**». Это и есть аварийная тропа
  (`pnl-reconciliation.md:32-33`: «на аварийной тропе звено добычи движений
  не эмитится вовсе»).
- `docs/models/domain/aggregate/Deal.md:73-76` — `ReconciliationStatus`:
  `NOT_RUN` — «сверка **не была обязана**».
- `docs/components/HoldService.md:19-23` — «исполнители терминального ребра»
  во множественном числе: перечень акторов у `HoldService` шире, чем у
  `pnl-reconciliation.md`.

**Целевой док.** `docs/rules/pnl-reconciliation.md` §«Что сверяется, когда,
кем» (назвать актора аварийной тропы поимённо);
`docs/components/MarkDealEmergencyClosedExecutor.md` §«Признаки отбора»
(дописать полноту разбивки и безусловность статуса сверки);
`docs/models/domain/aggregate/Deal.md` (развести природу пустоты признаков
отбора по тропам таблицей `absent-value-semantics.md:53-60`).

**Ожидаемый владелец.** `solution-designer`.

**Трассировка.** Применял требование «писатель назван и достижим» к
величинам, введённым шагом (задание, п. 3) → выписал признаки отбора `Deal`
и сопоставил перечни двух терминалов: штатный пишет четыре, аварийный — два
с половиной → проверил достижимость «финализации выхода» на аварийной тропе
по `SystemActionExecutor.md` §Финализация → проверил обратную посылку («может,
сверка на аварийной тропе недостижима и признаки законно пусты»):
`pnl-reconciliation.md:32-33` и `SystemActionExecutor.md:49-50` показывают,
что движения добываются на выходной тропе, а сделка может уйти в аварию
**после** неё, значит `dutyArisen` достижим — и `MarkDealEmergencyClosedExecutor`
не зря называет статус сверки среди своих записей.

**Варианты.**

| вариант | что делает | названная цена |
|---|---|---|
| **A. Аварийный терминал пишет все четыре признака безусловно; `pnl-reconciliation.md` называет его вторым актором сверки** (крен) | симметрия терминалов, пустота остаётся однозначной, популяция отчёта счётна | аварийный исполнитель получает вычисление сверки — но он уже считает число по той же формуле (`MarkDealEmergencyClosedExecutor.md:16-24`), новой ответственности не появляется |
| B. Оставить условную запись, объявить пустоту признаков третьей природой («аварийная тропа») | правка одной клаузы | пустота получает три смысла на одном поле — перегрузка, запрещённая `absent-value-semantics.md:9-24`; отчёт перестаёт отличать «не было входа» от «была авария» |
| C. Ввести аварийные значения признаков (`ABORTED` в оба перечисления) | различение выражено значением, а не пустотой | больший след при том, что `NOT_ASSESSED` и `NOT_RUN` уже выражают эти случаи; переоткрывает «третьего значения нет» (`Deal.md:75-76`) |
| D. Крен A **плюс** явная клауза, что на аварийной тропе `dutyArisen` достижим только через ранее закрытое окно | снимает посылку, которую сейчас выводит читатель | одна дополнительная клауза в доме |

**Ярлык исхода.** `варианты-с-креном` (крен — A + D).

**Ярлык дефицита.** `дефицита нет` — всё нужное знание в корпусе есть,
требуется приведение записи в соответствие.

---

## B5. Гейт `CODE` шага 7 адресует «реестр предусловий» по номерам в док, где нумерации больше нет

**Вопрос.** Собственный выходной гейт шага перечисляет одиннадцать гейтящих
предусловий **номерами пунктов** внутри `docs/rules/pnl-reconciliation.md`.
Переписанный док нумерованных пунктов не содержит вовсе. Гейт шага
неразрешим: проверить «закрыты ли одиннадцать предусловий» по действующему
корпусу нельзя — адресуемых объектов не существует.

**Цитаты (носители).**
- `.claude/work/backlog.md:591-596` — «**Концепция закрыта:** …
  механика/носители стадий 1-2 и **реестр предусловий `CODE`** —
  `docs/rules/pnl-reconciliation.md`».
- `.claude/work/backlog.md:604-614` — «**одиннадцать** гейтящих предусловий
  (`docs/rules/pnl-reconciliation.md`, пп. 1, 2, 5, 6, 7, 9, 10, 14, 15, 16,
  **17**)» с привязкой кейсов к номерам.
- `.claude/work/backlog.md:625-627` — «реестр предусловий `CODE` п. 13,
  `docs/rules/pnl-reconciliation.md`».
- `docs/rules/pnl-reconciliation.md` — заголовки целиком: §«На какой вопрос
  отвечает этот файл», §«Что сверяется, когда, кем», §«Допуск», §«Реакция на
  расхождение», §«Разведочный режим допуска», §«Связи». Нумерованных пунктов
  нет ни одного (верифицировано полным чтением и грепом на `^[0-9]\.`,
  `предуслови`, `реш\.` — ноль вхождений).
- Тот же класс на соседнем указателе: `backlog.md:1167-1169` адресует
  §«Политика отказа котировки и догон» в `RefreshBillsExecutor.md` —
  секции нет (см. B2).
- Эталон, объясняющий, почему это ожидаемо и почему требует ответа:
  `docs/concept.md` §П4 («Никакой процессной и ссылочной арматуры. Ни
  ссылок на проверочные прогоны и итерации») — реестр предусловий `CODE`
  **правомерно** ушёл из доменного правила; вопрос в том, куда он ушёл.

**Целевой док.** `.claude/work/backlog.md` §«Шаг 7 — исполнительный хвост»
(перенумеровать/переадресовать гейт на действующие носители) либо новый
носитель реестра предусловий в `.claude/work/` — по решению владельца.

**Ожидаемый владелец.** `knowledge-curator` (ссылочная целостность рабочих
файлов после переписывания корпуса), с эскалацией `solution-designer`, если
окажется, что содержание пунктов не переехало никуда.

**Трассировка.** Проверял, не отвечены ли мои находки B1-B3 уже принятыми
решениями → грепнул `backlog.md` по предмету шага → §«Шаг 7» перечислил
одиннадцать предусловий номерами пунктов правила → открыл правило целиком
→ нумерации нет → прогрепал на маркеры нумерации и на «предуслови»/«реш.» —
ноль → проверил тот же класс на втором указателе (`RefreshBillsExecutor`) —
подтвердился.

**Варианты.**

| вариант | что делает | названная цена |
|---|---|---|
| **A. Перечислить предусловия по существу (что должно быть добыто/подтверждено), без опоры на нумерацию дока** (крен) | гейт становится самонесущим и переживает любую ревизию корпуса | нужно восстановить содержание одиннадцати пунктов — работа по истории, если оно нигде не сохранилось |
| B. Вернуть нумерованный реестр в `pnl-reconciliation.md` | указатели чинятся сами | возвращает процессную арматуру в доменное правило — прямо против `concept.md` §П4; и `.claude/rules/policy-home.md` §«Рабочий файл — не носитель политики» действует симметрично: правило — не носитель гейта |
| C. Объявить гейт закрытым, раз реестра нет | шаг выходит в `CODE` немедленно | ровно тот класс, ради которого гейт заведён: непроверенное предусловие становится непроверенным молча; `backlog.md:621-631` прямо называет цену выхода при незакрытых разрывах |

**Ярлык исхода.** `варианты-с-креном` (крен — A).

**Ярлык дефицита.** `работа`, если содержание пунктов восстановимо по
`history/`; **`подтверждение`**, если нет: тогда решение «идём в `CODE`
без реестра» отменяет принятое держателем и выносится ему.

---

# Находки — прочее

## B6. Штатный отказ по незаданным числам конфигурации не имеет наблюдаемого носителя

**Вопрос.** Оба числа конфигурации сейчас не заданы — объявленное штатное
состояние. Отказ описан и прогоняется примерами, но П3 требует, чтобы у
срабатывания в данных были «что сработало, на каком операнде, каково было
значение операнда». Перечень кодов такого значения не содержит, а
переиспользование существующих сделало бы запись **ложной**: `..._EXCEEDED`
утверждает, что потолок превышен, тогда как потолка нет; `..._ABOVE_GLOBAL`
— что стратегия объявила больше глобального, тогда как глобального нет.

**Цитаты (носители).**
- `docs/rules/risk-policy.md:199-205` — «Незаданное число означает
  **отказ**… Отказ штатный и адресный».
- `docs/rules/strategy-validation.md:72-76` — «Незаданное конфигурационное
  число делает сверку невыполнимой — тогда отвергается **создание**».
- `docs/spec/risk-limits.json` §examples — кейсы «незаявленный множитель
  отказывает, а не пропускает действие» и «незаданный максимальный риск на
  сделку отказывает обоими потолками» через `expectRefusal`: спецификация
  отказ **выражает**, кода не назначает.
- `docs/components/models/RiskCheckResult.md:31-54` — полный перечень
  `RiskCheckCode`: значения «число конфигурации не задано» нет.
- `docs/components/RiskValidator.md:52-77` — обе таблицы проверок: строки
  под этот отказ нет; `:25` — «максимальный риск на сделку | конфигурация»
  читается, исход при пустоте не назван.
- `docs/rules/strategy-validation.md:53-66` — четыре кода создания;
  `STRATEGY_CATASTROPHIC_MULTIPLIER_ABOVE_GLOBAL` — код неравенства, не
  код отсутствия операнда.
- `docs/rules/absent-value-semantics.md:25-39` — запрет перегрузки одного
  значения двумя смыслами.

**Целевой док.** `docs/components/models/RiskCheckResult.md` (значение
перечисления) + `docs/components/RiskValidator.md` (строка fail-fast) +
`docs/rules/strategy-validation.md` (код создания).

**Ожидаемый владелец.** `solution-designer`.

**Трассировка.** Задание, п. 4 → нашёл описание отказа в двух правилах и
прогон в спецификации → искал носитель наблюдаемости: прошёл перечень
`RiskCheckCode` целиком, обе таблицы `RiskValidator`, перечень кодов
создания → грепнул корпус на имена обоих чисел (13 вхождений, все учтены) →
кода нет ни у одного отказа.

**Варианты.**

| вариант | что делает | названная цена |
|---|---|---|
| **A. Один код на класс — `RISK_LIMIT_NOT_CONFIGURED`, операнд в `details`** (крен) | одно значение закрывает оба числа; `details` уже предусмотрен (`RiskCheckResult.md:22-23`) | разбор требует заглянуть в `details`, чтобы понять, какое из двух чисел пусто |
| B. Два кода — по числу конфигурации | причина читается прямо из кода | два значения там, где хватает одного с операндом; при равенстве шкала следа отдаёт меньшему |
| C. Переиспользовать `..._EXCEEDED` / `..._ABOVE_GLOBAL` | ноль новых значений | запись становится ложной: причина отказа подменяется несостоявшимся фактом (`absent-value-semantics.md`); выписан ради видимости отказа |
| D. Крен A **плюс** отдельный код создания стратегии — перечни разведены по построению (`RiskCheckResult.md:56-57`) | обе точки контроля наблюдаемы | две записи вместо одной, но перечни и так разные — счётной разницы нет |

**Ярлык исхода.** `варианты-с-креном` (крен — A + D).

**Ярлык дефицита.** `дефицита нет` — назначение величин остаётся за
держателем, но код отказа величиной не является.

---

## B7. Концепция требует невозрастающей базы у «потолка сделки», риск-политика применяет её только к кумулятивному

**Вопрос.** `concept.md` §«Сжатие в просадке» формулирует свойство без
оговорок: «Потолок сделки считается от **невозрастающей** базы: реализованная
просадка режет потолок, рост депозита его не поднимает».
`risk-policy.md` применяет это к одному потолку из четырёх, а три
остальных — включая два, чьи имена содержат `PerDeal`, — считает от текущей
базы. Тогда рост базы в течение жизни сделки поднимает и одновременный, и
катастрофический потолок — то, что концепция называет отменой однократности
потери «по построению».

**Цитаты (носители).**
- `docs/concept.md:360-367` — §«Сжатие в просадке», формулировка без
  исключений; довод — П1, анти-мартингейл.
- `docs/rules/risk-policy.md:43-47` — «**Базы разные.** Поактный,
  одновременный и катастрофический — от **текущей** базы; кумулятивный — от
  `min(снимок, текущая)`».
- `docs/rules/risk-policy.md:30-32` — имена трёх потолков содержат `PerDeal`
  (`strategySimultaneousRiskPerDealPercent`,
  `globalSimultaneousRiskPerDealPercent`,
  `strategyCatastrophicRiskPerDealMultiplier`) — все они «потолки сделки» по
  собственным именам.
- `docs/spec/risk-limits.json` §values — `withinStrategySimultaneous`,
  `withinGlobalSimultaneous`, `withinDealNotional` берут `base`, а не
  `min(base, deal.plannedRiskEquityBase)`: спецификация следует
  риск-политике, не концепции.
- `docs/concept.md:506-509` — «Конфликт концепции с доменным доком читается
  **в пользу концепции**».

**Целевой док.** `docs/concept.md` §«Сжатие в просадке» (сузить) **либо**
`docs/rules/risk-policy.md` §«Базы разные» + `docs/spec/risk-limits.json`
(расширить) — что именно, решает владелец.

**Ожидаемый владелец.** `trading-specialist` (какая база у какого потолка —
торговая развилка), с выносом держателю: исход правит следствие П1.

**Трассировка.** Задание, п. 1. Таблица четырёх потолков сошлась с
концепцией по вопросам, операндам, правым частям, реджектам и по клаузе «кэп
номинала = потолок при худшем ходе 100 %». Разошлась одна ось — база.
Проверил, не сужена ли формулировка концепции контекстом: §«Сжатие в
просадке» стои́т отдельным подразделом §«Риск-контур» и слова «кумулятивный»
не содержит. Проверил спецификацию — следует риск-политике.

**Варианты.**

| вариант | что делает | названная цена |
|---|---|---|
| A. Сузить концепцию: невозрастающая база — свойство кумулятивного потолка | правка одной клаузы, наименьший след; корпус согласуется в текущем виде | анти-мартингейл перестаёт быть свойством **системы** и становится свойством одного контроля: рост базы внутри жизни сделки поднимает три потолка из четырёх |
| B. Расширить невозрастающую базу на все потолки сделки | буквальное исполнение П1; правка правила + трёх предикатов спецификации + примеров | поактный потолок перестаёт быть «поактным» в чистом виде: он привязан к базе на момент **действия**, и заморозка снимком сделки меняет его смысл |
| **C. Привязать ось базы к оси вопроса: невозрастающая — у потолков «за жизнь сделки» (кумулятивный, катастрофический), текущая — у моментных (поактный, одновременный)** (крен) | опирается на разведение, уже проведённое в `risk-policy.md:36-41` («взятое за жизнь» против «под ударом сейчас») | средний след — правка правила, спецификации и клаузы концепции; требует явно назвать катастрофический потолок «пожизненным», хотя его операнд (номинал) моментный |
| D. Оставить как есть, записав расхождение как известное ограничение | ноль правок сейчас | расхождение живёт в **концепции**, то есть в носителе, которым проверяются все прочие доки; каждый следующий обход воспроизведёт находку заново |

**Ярлык исхода.** `варианты-с-креном` (крен — C).

**Ярлык дефицита.** `подтверждение` — в переборе есть варианты,
переоткрывающие следствие ратифицированного П1. Находка **составная**:
держателю — какая база у потолков сделки; владельцу — форма записи после
выбора.

**Зависимость.** Для шага 7 гейтом не является: `resultProfit` и сверка
операндами потолков не пользуются. Гейтит шаги риск-контура, если
разрешится как B или C.

---

## B8. Форма корпуса нарушена самими доками: слой опровержения в семи носителях, один клейм о коде ложен

**Вопрос.** Самоприменимость. `concept.md` §«Опровержение правит на месте»
запрещает «ни „прежняя редакция снята“, ни „клейм ложен“, ни „уточнено“, ни
отсылок к тому, чем знание было раньше»; §«Доменный док» запрещает «какие
были альтернативы». Требование не применено к собственным артефактам.

**Цитаты (носители).**
- `docs/rules/risk-policy.md:163` — «…считается, а не мажорируется обходом
  дерева шагов, **как прежде**».
- `docs/rules/live-risk-protection.md:114-117` — «Уровень сетки ключом
  порядка не служит и **на действии больше не живёт**».
- `docs/rules/strategy-validation.md:82-86` — «**Второе перестало быть
  оценкой сверху и стало равенством.** **Прежде** `N_overlap` мажорировался
  обходом дерева шагов…».
- `docs/processes/risk-evaluation.md:101-102` — «**Прежняя запись**
  «`Deal.status = ERROR`» присваиванием выражала писателя, которого на этой
  тропе нет».
- `docs/components/RetryPolicyService.md:29-30` — «**Альтернатива**
  «авторитет — поле сущности, фиксируется при создании» **отвергнута**».
- `docs/models/domain/aggregate/Strategy.md:146-147` — «**Прежний маркер**
  `level` на действиях… **снят**».
- `docs/models/mapping/TradeFeeRate.md:87` — «**Прежняя атрибуция**
  („сохраняет IGNORE-null“)…».
- `docs/components/models/CalculatedSize.md:20` — «`description` … (целевое
  имя; **legacy — `explanation`**)». **Клейм ложен:**
  `src/main/java/com/example/tradingbot/domain/command/calc/CalculatedSize.java:32`
  объявляет `String description`; идентификатора `explanation` в `src/` нет
  (греп по всем 488 `*.java`). Ложная запись чинится курационно и от
  продуктового выбора не зависит.
- Эталон: `docs/concept.md:431-434` и `:478-488`.

**Целевой док.** Перечисленные семь файлов — вычеркнуть слой опровержения,
оставив действующую редакцию; `CalculatedSize.md:20` — убрать legacy-клейм.

**Ожидаемый владелец.** `knowledge-curator`.

**Трассировка.** Отдельный проход детектора «самоприменимость правил»: взял
требования формы из `concept.md` части IV и прогрепал корпус на маркеры
отсылки к прошлому («прежн», «раньше», «снят», «уточнено», «отменённ»,
«legacy», «альтернатива … отвергнута»), отсеяв законные вхождения («прежняя
защита» как объект предметной области, «раньше снимающих» как порядок).
Клейм о коде проверил грепом по `src/`, как требует правило «клауза дока о
коде верифицируется по коду».

**Варианты.** Развилки нет: требование записано в концепции, носители ему
противоречат, направление правки задано самой концепцией
(`concept.md:506-509`). Единственная развилка внутри —
`CalculatedSize.md:20` — разрешается фактом кода, не выбором.

**Ярлык исхода.** `выводимо`.

**Ярлык дефицита.** `дефицита нет`.

---

## B9. Ссылочная и процессная арматура внутри доменных доков; одна ссылка §Связи указывает на собственный файл

**Вопрос.** `concept.md` §П4: «Никакой процессной и ссылочной арматуры…
Смысловая ссылка на **доменный дом** — допустима и нужна». Доменные доки
ссылаются на пайплайновые решения как на обоснование, переносят корпусные
конвенции внутрь предметного текста, а один док адресует §Связи сам на себя.

**Цитаты (носители).**
- `docs/components/models/RiskValidationResult.md:10-13` — «RVO, не persisted
  (см. `.claude/decisions/runtime-value-object.md`)».
- `docs/components/models/RiskBlockAction.md:13` — то же.
- `docs/components/models/CalculatedSize.md:11` — то же. (Вне охвата линзы
  тот же приём в `CalculatedStrategyAction.md:12`,
  `MarketDataExpirationResult.md:12`, `PositionStatusResolveResult.md:12`,
  `StrategyActionCalculationResult.md:12` — итого семь носителей.)
- `docs/processes/risk-evaluation.md:12-14` — «Процесс — точка композиции…
  (см. `.claude/decisions/process-materialization-criterion.md`)».
- `docs/rules/command-lifecycle.md` §Связи — «Граница «команда — действие» —
  `docs/rules/command-lifecycle.md`»: **ссылка на самого себя**; читатель
  отправлен туда, где уже находится.
- `docs/components/DealOrchestratorJob.md:26-28` — «**Номер шага не адрес:
  ссылаться на шаг следует именем**»: корпусная конвенция ссылок внутри
  предметного дока; её дом — `concept.md` §«Ссылки».
- `docs/rules/persistence-representation.md:34-37` — «Schema-дельта шага —
  сборка-указатель… Расходятся — правится дельта»: правило о пайплайновом
  артефакте внутри доменного правила.
- Находкой **не** являются шапки «Внешний источник правды» в
  `docs/integrations/okx/contracts/*`: они конвенционны
  (`.claude/rules/external-source-sync.md`) и адресуют внешний источник, а не
  довод. Проверено грепом `\.claude/` по `docs/` — 52 файла, разделены.

**Целевой док.** Перечисленные файлы: снять `.claude/`-обоснования (знание
«RVO, не persisted» остаётся, довод уходит), починить самоссылку в
`command-lifecycle.md`, перенести конвенцию ссылок из
`DealOrchestratorJob.md` в её дом.

**Ожидаемый владелец.** `knowledge-curator`.

**Варианты.**

| вариант | что делает | названная цена |
|---|---|---|
| **A. Снять довод, оставить знание; самоссылку заменить фактическим носителем** (крен) | доменные доки перестают зависеть от пайплайновых файлов | нужно найти фактический дом границы «команда — действие» — вероятно `command-lifecycle.md` §«Составное — системное действие, а не команда» + `SystemActionExecutor.md`; если дома нет, находка вырождается в «дом не назван» |
| B. Оставить как есть | ноль правок | `concept.md` §П4 нарушен семью носителями; находка воспроизведётся каждым обходом |

**Ярлык исхода.** `варианты-с-креном` (крен — A).

**Ярлык дефицита.** `дефицита нет`.

---

## B10. «Один дом» на трёх кластерах: предикат линковки прозой при живой спецификации, лестница реакций составом в пяти носителях, четыре числа риска в двух

**Вопрос.** `concept.md` §П4 и §«Исполнимая форма». Проверены кластеры,
названные заданием. Три несут пересказ **состава** политики вне дома.

**Цитаты (носители).**

*Кластер «предикат линковки движений» — дом-спецификация
`docs/spec/cash-flow-linkage.json` существует, предикат переписан прозой:*
- `docs/models/domain/other/DealCashFlow.md:74-78` — «Предикат линковки —
  биржа, инструмент и попадание времени события в окно линковки при
  нетерминальной сделке».
- `docs/components/RefreshBillsExecutor.md:41-48` — тот же предикат третьей
  редакцией.
- `docs/models/mapping/DealCashFlow.md:81` — единственный носитель,
  сделавший правильно: ссылка на спецификацию.

*Кластер «холд / лестница реакций» — дома `instrument-hold.md` и
`exchange-hold.md`:*
- `docs/models/domain/core/Exchange.md:29-38` — обе ступени с составом,
  множествами входа и способом снятия.
- `docs/models/domain/core/Instrument.md:41-57` — то же для инструментной
  пары.
- `docs/rules/error-handling-policy.md:108-120` — §«Перечень реакций»
  повторяет состав обеих лестниц.
- `docs/rules/external-status-resolution.md` §Реакция и
  `docs/rules/controlled-exchange-exceptions.md` §Реакция — один и тот же
  блок «сущность → ERROR, Deal → ERROR, Exchange → TRADE_BLOCKED» в двух
  редакциях.

*Кластер «риск/R»:*
- `docs/rules/risk-policy.md:95-107` §«Заявленное и взятое разведены» и
  `docs/models/domain/aggregate/Deal.md:157-170` §«Четыре числа риска» —
  роли всех четырёх чисел изложены дважды, таблицей в каждом.

*Кластеры, проверенные и **чистые**:* epsilon/допуск (формула — только в
`pnl-reconciliation.json`; повтор в `FinalizeDealExitExecutor.md:49-58`
ограничен составом четырёх пар и трёх конъюнктов и несёт ссылку на дом —
на грани, но не пересказ); свежесть (`market-data-freshness.md` шапкой
отправляет в спецификацию); частичный выход (`no-partial-close.md` — дом;
`SizeCalculator.md:57-72` несёт собственное знание об округлении);
аномалии (`Deal.md:101-106` — короткое упоминание со ссылкой на
`AnomalyJob.md`).

**Целевой док.** `DealCashFlow.md` и `RefreshBillsExecutor.md` — свести
предикат к ссылке на `cash-flow-linkage.json`; `Exchange.md` /
`Instrument.md` — оставить смысл статуса, состав и множества входа увести в
дома; `Deal.md` §«Четыре числа риска» **либо** `risk-policy.md` §«Заявленное
и взятое» — оставить одну таблицу.

**Ожидаемый владелец.** `knowledge-curator`.

**Трассировка.** Задание, п. 2. По каждому кластеру: нашёл дом → грепнул
корпус по характерным операндам/именам → открыл каждый носитель и сравнил не
тему, а **состав** (элементы, границы, исключения) → отделил «короткое
упоминание со ссылкой» от повтора.

**Варианты.**

| вариант | что делает | названная цена |
|---|---|---|
| **A. Свести все три кластера к «дом + ссылка», начиная с предиката линковки** (крен) | предикат перестаёт иметь три редакции, лестница — пять | правка семи-восьми доков; читатель модели статуса теряет самодостаточность (второе измерение цены — контекстная связность растёт) |
| B. Только предикат линковки — он машинно-проверяем, и расхождение редакций даёт неверную выборку движений | минимальный след, закрывает самый дорогой кластер | лестница и числа риска остаются в двух-пяти редакциях |
| C. Признать состав статуса собственным знанием модели, свести только `error-handling-policy.md` §«Перечень реакций» | сохраняет самодостаточность моделей | не снимает класс: множества входа и способ снятия — политика лестницы, а не смысл статуса |

**Ярлык исхода.** `варианты-с-креном` (крен — A, с приоритетом B при
дефиците объёма).

**Ярлык дефицита.** `дефицита нет`.

---

## B11. `RiskCheckCode` несёт семь значений, которых `RiskValidator` не производит; одно объявлено одновременно кодом риск-проверки и контролируемой ошибкой расчёта

**Вопрос.** `RiskCheckResult.md` §Связи объявляет `RiskValidator`
строителем результата, то есть писателем всех значений перечисления.
`writer-named-for-every-value.md:14-21` требует писателя на **каждое**
значение. Семь значений писателя не имеют, восьмое имеет двух — в разных
типах результата.

**Цитаты (носители).**
- `docs/components/models/RiskCheckResult.md:31-54` — перечень
  `RiskCheckCode`; `:61` — «Кто строит — `docs/components/RiskValidator.md`».
- `docs/components/RiskValidator.md:52-77` — обе таблицы проверок. Не
  производит: `BORROW_OR_DEBT_DETECTED`, `BALANCE_NOT_FRESH`,
  `BALANCE_NOT_ENOUGH`, `PARTIAL_EXIT_NOT_REDUCE_ONLY`,
  `PARTIAL_EXIT_INCREASES_POSITION`,
  `DIRECT_PARTIAL_POSITION_CLOSE_FORBIDDEN`,
  `PROTECTION_LADDER_STEP_BELOW_MIN_SIZE`.
- Три из них — `BORROW_OR_DEBT_DETECTED`, `BALANCE_NOT_FRESH`,
  `BALANCE_NOT_ENOUGH` — во всём корпусе встречаются **только** в
  `RiskCheckResult.md` (греп по `docs/` на каждое имя отдельно): ни правила,
  ни компонента, ни тропы.
- `docs/rules/no-partial-close.md:44-46` — три кода частичного выхода как
  «коды нарушения инварианта»; `:41-42` — «Преконтроль риска для reduce-only
  выхода **не вызывается**; обработчик делает минимальные проверки
  инвариантов». Писатель — обработчик, но тип результата объявлен тем,
  который строит валидатор.
- `docs/components/SizeCalculator.md:83-85` — «Ступень меньше минимального
  размера — **контролируемая ошибка расчёта**
  (`PROTECTION_LADDER_STEP_BELOW_MIN_SIZE`)»;
  `docs/components/models/CalculationError.md:12-16` — та же ситуация в
  перечне контролируемых ошибок расчёта; `RiskCheckResult.md:38-39` держит
  тот же код в `RiskCheckCode`.
  `docs/processes/risk-evaluation.md:120-126` разводит эти типы как разные.

**Целевой док.** `docs/components/models/RiskCheckResult.md` (перечень и
писатели) + `docs/components/models/CalculationError.md` (коды своего
перечня) + `docs/rules/no-partial-close.md` (тип результата минимальных
проверок).

**Ожидаемый владелец.** `solution-designer`.

**Трассировка.** Выписал `RiskCheckCode` целиком, сопоставил с обеими
таблицами `RiskValidator`, остаток прогрепал по `docs/` **на каждое имя
отдельно** (каждый названный носитель проверяется по отдельности): три кода
не встречаются нигде больше, четыре встречаются с **другим** производителем.

**Варианты.**

| вариант | что делает | названная цена |
|---|---|---|
| **A. Разнести по типам результата: коды минимальных проверок — свой перечень обработчика, ступень лестницы — `CalculationError`, три бесхозных — удалить либо назначить писателя** (крен) | каждое значение получает один тип-носитель и одного писателя | три перечня вместо одного; но совмещение двух природ (риск-политика против инварианта обработчика) — само по себе основание развести |
| B. Признать `RiskCheckResult` общим носителем исходов проверок, расширить список писателей | минимальный след — правка §Связи | `risk-evaluation.md:120-126` явно разводит `CalculationError` и `RiskValidationResult`; вариант переоткрывает это разведение |
| C. Оставить перечень, дописать «производителя пока нет» у трёх кодов | честно фиксирует состояние | значение перечисления без писателя — «описание состояния, в которое система не умеет попасть» (`writer-named-for-every-value.md:11-12`); фиксация не лечит |

**Ярлык исхода.** `варианты-с-креном` (крен — A).

**Ярлык дефицита.** `дефицита нет`.

---

## B12. `CalculatedSize.notionalUsdt` зашивает валюту в имя поля там, где авторитет валюты — расчётная валюта инструмента

**Вопрос.** Корпус объявляет единственным авторитетом валюты расчётную
валюту инструмента. Поле результата расчёта размера называет конкретный
тикер, фиксируя в имени то, что модель объявляет переменным.

**Цитаты (носители).**
- `docs/components/models/CalculatedSize.md:19` — «`notionalUsdt` |
  `BigDecimal` | Номинал позиции **в USDT**, если рассчитывался».
- `docs/models/domain/core/Instrument.md:24` — `externalSettlementCurrency`
  как «**Авторитет** валюты риска и валюты результата сделки».
- `docs/models/domain/aggregate/Deal.md:151-152` — «**Валюта результата —
  один авторитет:** расчётная валюта инструмента».
- `docs/rules/risk-policy.md:12-15` — база риска в расчётной валюте
  инструмента.
- `docs/rules/trading-constraints.md:9-19` — контур фиксирует линейные SWAP
  (`ctValCcy = baseCcy`) и один инструмент, но расчётную валюту **не**
  фиксирует: `USDT` инвариантом контура не объявлен.
- Клейм о коде: поле `notionalUsdt` в `src/` **существует**
  (`.../calc/CalculatedSize.java:26`, `.../calc/SizeCalculator.java:77`) —
  док коду соответствует; это вопрос к конструкции, а правка — CODE-дельта.

**Целевой док.** `docs/components/models/CalculatedSize.md` (имя и описание
поля) + запись CODE-дельты переименования в `.claude/work/backlog.md`.

**Ожидаемый владелец.** `solution-designer`.

**Варианты.**

| вариант | что делает | названная цена |
|---|---|---|
| **A. `notional` без валюты; валюта читается из инструмента** (крен) | имя перестаёт утверждать факт, которого модель не гарантирует | переименование поля и одного call-site (CODE-дельта, шаг не гейтит) |
| B. `notional` + `notionalCurrency` рядом | величина самодостаточна на любой поверхности потребления | лишнее поле в RVO, дублирующее авторитет инструмента — больший след без счётной разницы: потребитель уже держит инструмент в контексте расчёта |
| C. Оставить, записав «USDT — ограничение фазы 1» | ноль правок | такого ограничения в `trading-constraints.md` нет; его пришлось бы **ввести**, то есть сузить контур ради имени поля |

**Ярлык исхода.** `варианты-с-креном` (крен — A).

**Ярлык дефицита.** `дефицита нет`.

---

# Блокирующие открытые вопросы

Проход по `.claude/work/questions/open-questions.md` целиком.
**Гейтящих шаг 7 нет.** Три пункта касаются охвата линзы и требуют пометки:

- **PNL-Q1 «Измеримость искажений измеряемой ожидаемости»** — не гейтит:
  вопрос про измеримость цены реакции и `triggerPx`, а не про вычислимость
  `resultProfit`. Формулировка соответствует действующим докам
  (`docs/models/domain/aggregate/Deal.md` §CloseOutcome,
  `docs/rules/pnl-reconciliation.md` §«Реакция на расхождение»). Проверено
  открытием обеих целей.
- **ORCH-Q2 «Порядок teardown у kill-switch против инварианта дочистки»** —
  **посылка вопроса снята переписанным корпусом.** Вопрос утверждает, что
  «`docs/components/KillSwitchExecutor.md` §«Порядок исполнения» ставит close
  позиции **первым**». Действующий док
  (`docs/components/KillSwitchExecutor.md:18-24`) говорит противоположное:
  «Порядок остального — инвариант выхода: **сперва живые входные заявки всех
  траншей, потом позиция**», а защита снимается последней. Расхождения с
  `docs/rules/exit-teardown-order.md:9-13` больше нет. Вопрос следует
  переформулировать или закрыть — иначе он произведёт ложную работу.
  Владелец — `knowledge-curator` совместно с `solution-designer`.
- **FEE-Q1 «Слот подгружаемой ставки»** — не гейтит; его довод («финализатор
  `TradeFeeRate` не читает») подтверждается
  `docs/components/FinalizeDealExitExecutor.md:63-65` («справочный навес
  инструмента не читается — все операнды тождества persisted на ноге»).
  Устарел только адрес (`pnl-reconciliation.md` реш.5 — секции нет, см. B5).

---

# Подтверждено-ОК (с уровнем сверки)

- **Четыре потолка ↔ концепция — сверка по составу вопросов, операндам,
  правым частям, реджектам и по клаузе «кэп номинала = потолок».**
  `concept.md` §«Четыре вопроса — четыре лимита» ↔ `risk-policy.md:25-93`:
  четыре вопроса совпадают; катастрофический потолок = максимальный риск на
  сделку × множитель `StrategyDetail`, ограниченный вторым числом
  конфигурации (`risk-policy.md:63-76`, `Strategy.md:97`,
  `strategy-validation.md:61-62`); кэп номинала равен потолку при худшем ходе
  100 % (`risk-policy.md:78-84`); потребитель — `DEAL_NOTIONAL_EXCEEDED`
  (`RiskValidator.md:76`); формула и примеры — `risk-limits.json`
  (`catastrophicLossCeiling`, `withinDealNotional`). Разошлась одна ось —
  база (B7).
- **Вложенность потолков — сверка по обеим парам неравенств и по точке
  контроля.** `risk-policy.md:49-61` ↔ `strategy-validation.md:53-66` (те же
  три неравенства, те же коды) ↔ `risk-limits.json`. `N_overlap` определён
  одинаково в обоих носителях («Σ `levelCount` по объявлениям детали»).
- **Писатели четвёрки чисел риска — сверка по операндам, а не по факту
  упоминания.** Каждый писатель назван компонентом и покрывает свой операнд:
  `CreateOrderExecutor.md:16-27` (появление ноги + write-once база риска),
  `RefreshOrderExecutor.md:34` (филл/терминал ноги),
  `RefreshAlgoOrderExecutor.md:28,35` и `CancelAlgoOrderExecutor.md:32`
  (действующая защита), `RefreshPositionExecutor.md:63-64` (размер живого
  эпизода). Отдельно проверена подозрительная дыра: `CancelOrderExecutor.md`
  чисел не пересчитывает — и не должен, потому что `Deal.md:202-203` отбирает
  по **наблюдённому** состоянию, а подтверждение отмены приходит рефрешем.
- **Писатели окна линковки — сверка по трём границам, все три целевых дока
  открыты.** Нижняя — `SubmitOrderExecutor.md:22-33` («единственный
  писатель… безусловно на всех тропах» + суррогат на тропе восстановления);
  верхняя — `RefreshPositionExecutor.md:25-37`; прогресс добычи —
  `RefreshBillsExecutor.md` (`Deal.md:44`). Однодоменность операндов
  согласована с `time-utc.md:155-161` (`RefreshBillsExecutor.md:38-39`,
  `Auditable.md:29-39`).
- **Обязанность сверки — сверка по трём конъюнктам и по их достижимости.**
  `pnl-reconciliation.md:26-33` ↔ `pnl-reconciliation.json` (`dutyArisen`) ↔
  `FinalizeDealExitExecutor.md:55-58`. Третий конъюнкт проверен на
  достижимость против `SystemActionExecutor.md:49-50`.
- **Формула допуска — сверка по операндам обеих сторон теста.**
  `pnl-reconciliation.md:35-54` ↔ `pnl-reconciliation.json` (`epsilon`,
  `grossTurnover`, `expectedDealFee`, `legFeeRate`): омиссионный член
  действительно взвешен филлами, область суммирования однородна с четырьмя
  парами, пол общий. Клейм `FinalizeDealExitExecutor.md:63-65` («дочитываются
  операнды планового риска по каждой ноге входа») подтверждён телом
  спецификации (`legFeeRate` восстанавливается обращением закрытой формы
  сайзинга).
- **Инвариант покрытия — сверка по предикату, операнду и трём точкам
  проверки.** `live-risk-protection.md` ↔ `protection-coverage.json` ↔
  `RiskValidator.md:92-107` ↔ `risk-validator-scope.md:13-34` ↔
  `strategy-validation.md:64-65`. Траншевая область операнда и
  `coverageAfterRemoval` названы одинаково во всех носителях.
- **Карв-аут исчерпанного бюджета — сверка по перечню кодов.**
  `risk-evaluation.md:58-94` (четыре кода потолков + `PROTECTION_COVERAGE_REDUCED`)
  ↔ `risk-policy.md:180-182` ↔ `RiskValidator.md:72-77` — перечни совпадают.
- **Лестница реакций — сверка по оси разведения и по составу ступеней.**
  `concept.md` §«Лестница реакций» ↔ `error-handling-policy.md:26-47` ↔
  `instrument-hold.md` ↔ `exchange-hold.md`: ось («чему нельзя доверять»),
  четыре уровня, разведение радиуса и силы, ручное снятие у всех блокирующих
  реакций — согласованы. Дефект здесь только формы (B10), не содержания.
- **Ступень 1 на расхождении сверки — сверка по актору, моменту и порядку
  коммитов.** `pnl-reconciliation.md:71-79` ↔ `exchange-hold.md:30-33` ↔
  `MarkDealClosedExecutor.md:50-59` ↔ `HoldService.md:19-23`: подъём после
  коммита терминала, идемпотентность, названная цена окна между коммитами —
  сходятся.
- **Порядок выхода — сверка по обоим уровням и по исключению.**
  `exit-teardown-order.md:9-27` ↔ `KillSwitchExecutor.md:18-24` ↔
  `no-partial-close.md:9-16`: сперва входные заявки, потом экспозиция;
  kill-switch — исключение по построению; полное закрытие законно только при
  выходе всех траншей. (Расхождения, зафиксированного в ORCH-Q2, в
  действующих доках больше нет.)
- **Ставка комиссии — сверка по двум режимам отказа и по владельцу
  гидрации.** `RiskValidator.md:109-120` («ставка была, чтение упало» против
  «ставки не было никогда») ↔ `instrument-hold.md:46-61` (радиус по режиму
  отказа) ↔ `TradeFeeRate.md:56-67` ↔ `InstrumentExternalRulesSyncJob.md:34-45`
  (писатель строки назван) ↔ `InstrumentExternalRulesDataService.md:16-28`
  (владелец гидрации — хранилищный слой навеса, как и заявляет
  `RiskValidator.md:27-31`). Противоречия с
  `CalculationContextFactory.md:14` («правила с гидрированной ставкой») нет:
  фабрика читает уже гидрированное.

# Сводка

- **Гейтящих находок — 5:** B1 (`resultProfit` без исполнимой спецификации;
  арифметика — в рабочем файле), B2 (писатель курса пересчёта не назван,
  обе кросс-ссылки битые), B3 (дом настройки сверки не назван в корпусе —
  кольцевая ссылка), B4 (аварийный терминал: недостижимый актор сверки + два
  признака отбора без корректного писателя), B5 (реестр предусловий `CODE`
  адресуется по номерам в док, где нумерации нет).
- **Прочих — 7:** B6 (нет кода под «число конфигурации не задано»), B7
  (концепция против риск-политики по невозрастающей базе), B8
  (самоприменимость формы: слой опровержения в семи носителях, один клейм о
  коде ложен), B9 (арматура `.claude/` в доменных доках + самоссылка
  §Связи), B10 («один дом» на трёх кластерах), B11 (`RiskCheckCode`: семь
  значений без писателя, одно с двумя), B12 (`notionalUsdt` — валюта в имени
  поля).
- **Итого 12.** Пересчитано по телу: гейтящих — B1, B2, B3, B4, B5 = 5;
  прочих — B6, B7, B8, B9, B10, B11, B12 = 7.
- **По ярлыку исхода:** `варианты-с-креном` — 11 (B1, B2, B3, B4, B5, B6,
  B7, B9, B10, B11, B12); `выводимо` — 1 (B8).
- **По ярлыку дефицита:** `работа` — 4 (B1 в части формы, B2, B3, B5);
  `подтверждение` — 3 (B1 составной половиной — направление ошибки при
  неполученном курсе; B5 — если содержание реестра невосстановимо; B7 —
  какая база у потолков сделки); `дефицита нет` — 7 (B4, B6, B8, B9, B10,
  B11, B12). B1 и B5 учтены в двух строках как составные находки, поэтому
  сумма строк (4 + 3 + 7 = 14) больше числа находок (12) ровно на два.
- **По владельцам:** `solution-designer` — 7 (B1, B2, B3, B4, B6, B11, B12);
  `knowledge-curator` — 4 (B5, B8, B9, B10); `trading-specialist` — 1 (B7),
  плюс совладение по арифметике курса в B1 и B2.
- **Вынос пользователю (дефицит `подтверждение`):** B7 (какая база у потолков
  сделки — исход правит следствие ратифицированного П1); держательская
  половина B1 (входит ли в `resultProfit` строка с неполученным курсом —
  возможно, уже решено, см. `backlog.md:1178-1179`); B5, если содержание
  реестра предусловий невосстановимо и придётся решать «идём в `CODE` без
  реестра». Незаданность двух чисел конфигурации находкой **не** является —
  B6 про наблюдаемость отказа, не про величины.
- **Вердикт:** `GAPS_CLOSE_26` нужен; после него — `DOCS_CHECK_27`. Пять
  гейтящих находок лежат в самом предмете шага: центральная величина без
  единственной редакции (B1), её операнд без производителя (B2), настройка
  контроля без адреса (B3), аварийный терминал без корректных писателей
  признаков (B4) и собственный гейт шага, который нечем проверить (B5).
- **Дисклозура независимости.** Отчёты прошлых прогонов и
  `history/2026-08-29-step-7-docs-check-loop/` не читались. Провенанс
  прошлых итераций частично вшит в рабочие файлы (`backlog.md` цитирует
  `GAPS_CLOSE_18…24`), которые линза читала прицельно; на формулировки
  находок эти маркеры не опирались — каждая находка выведена из доков и
  верифицирована открытием целей (PROC-Q3, вариант A).
