# Линза G — `DOCS_CHECK_30`: остаток корпуса и структурная гигиена

## Охват

**Предмет линзы:** всё в `docs/`, чего не берут линзы A-F (`docs/dictionary/`,
`docs/processes/`, `docs/models/domain/other/`, `docs/models/api/`), форма
корпуса в целом (арматура, адресные перекрёстия, дома политик, уровни
носителей, дерево знаний, «вопрос файла — ровно один») и **сами инструменты
`tools/**` как носители знания**.

**Проверено прогоном (все одиннадцать команд мандата плюс собственные пробы):**

| Команда | Исход |
|---|---|
| `bash tools/spec-run.sh` | rc=0 — 22 спецификации, 448 примеров, «ВСЕ ПРИМЕРЫ СОШЛИСЬ» |
| `bash tools/spec-mutation-check.sh` | rc=0 — 360 величин, 15 переживают прямую нейтрализацию, все 15 — объявленные теоремы с пройденной косвенной пробой |
| `bash tools/spec-mutation-probe.sh` | rc=0 — 7 осей доказаны |
| `python3 tools/spec-scope-check.py docs/spec` | rc=0 — A:0, B:0, C:0 |
| `bash tools/spec-scope-probe.sh` | rc=0 — все оси доказаны |
| `python3 tools/spec-pointer-check.py` | rc=0 — 106 указателей, чужих 0 |
| `bash tools/spec-pointer-probe.sh` | rc=0 — 3 пробы |
| `bash tools/preconditions-check.sh` | rc=0 — «СОШЛОСЬ» |
| `python3 tools/anchor-check.py` | **rc=1** — 272 адреса, 35 неразрешимых |
| `bash tools/anchor-probe.sh` | rc=0 — 4 оси + 3 контроля |
| `mvn -o test-compile` | **rc=127** в форме мандата (`./mvnw`) и в форме бэклога (`mvn`); зелёный rc=0 только через дистрибутив из кэша wrapper'а — см. Г1 |

**Лаунчер Python установлен самостоятельно, до прогонов:** `python3` в этой
среде — настоящий интерпретатор (`Python 3.12.3`, `python3 -c "print(...)"` →
`EXECUTED-OK`, heredoc исполняется); `py` и `python` **отсутствуют**
(`command -v` → пусто). То есть шапка `tools/spec-scope-check.py`, требующая
«ИМЕННО `py`», в этой среде неверна — узел Г1.

**Вне охвата:** торговая корректность величин (D), спека↔док (E), предусловия
`CODE` и рабочие файлы (F), FSM/оркестрация (A), правила `docs/rules/` по
существу (B), граница источника (C). Их носители открывались только как цели
ссылок при верификации атрибуции.

## Стадия остановки

**Прошёл все стадии (стадия 2).** Стадия 0 чиста: гейтящих технических
вопросов скоупа по предмету линзы нет. Стадия 1 (процессы `docs/processes/` —
шесть файлов) пройдена. Стадия 2 (модели `docs/models/domain/other/`,
`docs/models/api/`, словарь, форма корпуса, `tools/**`) пройдена.

---

## Блокирует выход в CODE

### Г1. Факты среды исполнения живут в четырёх носителях с тремя расходящимися редакциями; дома у них нет, и ни одна объявленная команда сборки в этой среде не исполняется

**Вопрос.** Каким носителем исполнитель `CODE` узнаёт, как собрать дерево и
каким лаунчером поднимаются питоновские детекторы, — и почему все объявленные
формы этих команд в действительной среде отказывают?

**Грунт.** Не торговый.

**Счётно и цитаты (носители).**

*Половина «сборка»:*

| Носитель | Что объявляет | Что в среде фактически |
|---|---|---|
| `README.md:26,32` | `./mvnw spring-boot:run`, запасное `mvn spring-boot:run` | `./mvnw` в репозитории нет; `command -v mvn` → пусто |
| `.claude/work/backlog.md:2497,2511` (§«Гейт сборки на закрытиях, правящих `src/`») | закрытие «обязано предъявить зелёный `mvn -o test-compile`» | `mvn -o test-compile` → `mvn: команда не найдена` (rc=127) |
| `tools/boot-test.sh:35` | `exec mvn spring-boot:run -Dspring-boot.run.profiles=test`; шапка (`:9-11`) перечисляет «Preconditions NOT handled here» — Vault, postgres-test, токен | **прогнано:** `bash tools/boot-test.sh` → `line 35: exec: mvn: not found`, rc=127. Блокирующее предусловие (`mvn` на `PATH`) в перечне шапки не названо |
| `.claude/projects/C--Users-RomanKrd-IdeaProjects-VibeTradingBotV5/memory/build-command.md:8-18` | «в репозитории **нет** `mvnw`, `mvn` не на PATH, а `java` на PATH — **JDK 11**»; рабочая команда — PowerShell с `C:\Users\RomanKrd\.jdks\corretto-25.0.3` и `C:\Program Files\JetBrains\IntelliJ IDEA 2026.1\…\mvn.cmd` | `java` на `PATH` **отсутствует вовсе** (не JDK 11); путей `C:\…` в этой среде нет; **сам носитель машинно-мёртв** — ключ каталога памяти `C--Users-RomanKrd-…` соответствует пути Windows-машины, а на этой машине ключ проекта — `-home-romankrdr-IdeaProjects-VibeTradingBotV5`, то есть харнесс этот файл не поднимет |
| `.claude/tests/source-api/okx/code-preconditions.md:364` | «Maven — **есть**, дистрибутив wrapper'а `~/.m2/wrapper/dists/apache-maven-3.9.11-bin/*/apache-maven-3.9.11`; на `PATH` его нет, `mvnw` в репозитории по-прежнему нет» | **верно.** Проверено: `$MD/bin/mvn -v` → `Apache Maven 3.9.11`, `Java version: 25, Amazon.com Inc., /home/romankrdr/.jdks/corretto-25`; `JAVA_HOME=~/.jdks/corretto-25 $MD/bin/mvn -o -q test-compile` → **rc=0** |
| `.claude/agents/code-writer.md` | о сборке **не говорит ничего** (греп `mvn|компил|собир|build` — ноль строк) | — |

*Половина «лаунчер Python»:*

| Носитель | Что объявляет | Что в среде фактически |
|---|---|---|
| `tools/spec-scope-check.py:13-23` (§«КОМАНДА ЗАПУСКА») | `py -3 tools/spec-scope-check.py docs/spec`; «**ИМЕННО `py`**. В этой среде `python` и `python3` в PATH — заглушки Windows Store: печатают строку «Python» и завершаются с кодом 49»; «Настоящий интерпретатор поднимает только лаунчер `py`» | **прогнано:** `py -3 …` → `py: команда не найдена`, rc=127. `python3 tools/spec-scope-check.py docs/spec` → rc=0 с полной итоговой строкой. Заглушек в `PATH` нет |
| `.claude/processes/roadmap-step-execution.md:120-126` | «Класс тот же, что у `python` и `python3` против `py`: **обе заглушки** печатают «Python» и выходят, ничего не выполнив» — безусловно, без указания платформы | то же опровержение; это **процессный дом**, читаемый каждым закрытием |
| `tools/spec-scope-probe.sh:13-14,23-30` | «**ВНИМАНИЕ (Windows):** там детектор поднимается только лаунчером `py`»; «Лаунчер `py` есть только под Windows; под Linux настоящий интерпретатор — `python3`»; выбирает по платформе | **верно и переносимо** |
| `tools/anchor-probe.sh`, `tools/spec-pointer-probe.sh` | `python3` зашит без развилки | работает здесь; под Windows по клейму соседей отказал бы |
| `.claude/rules/structure.md:132` | `python3 tools/anchor-check.py` | работает |
| `.claude/work/backlog.md:2482-2486` | «**Переносимость инструментов — СНЯТА.** `tools/spec-run.sh` и `tools/spec-scope-probe.sh` зашивали … лаунчер `py` в форме Windows и на Linux не запускались вовсе; **обе команды сделаны переносимыми**» | клейм полноты **ложен по периметру**: переносимой сделана *проба*, а сам проверяемый ею детектор `tools/spec-scope-check.py` по-прежнему объявляет Windows-only команду и Windows-only факт о среде |

**Почему гейтит.** `CODE` шага 7 — следующий под-шаг, и он пишет `src/`.
Собственный кейс корпуса (`backlog.md:2501-2507`) показывает исход ровно этой
дыры: `GAPS_CLOSE_27` завело значение енума без ветви `switch`, дерево
перестало компилироваться и пролежало так, пока в среде не появились JDK и
Maven, — ни `DOCS_CHECK_28`, ни `_29` этого не увидели. Гейт, который это
закрывает, уже сформулирован — и **предписывает команду, которая в этой среде
отказывает**. Верная форма в корпусе есть ровно в одном месте, и это артефакт
тест-контура источника, входом `code-writer` не являющийся; носитель, который
харнесс поднял бы автоматически, машинно-мёртв и содержит ложный факт. То
есть исполнитель `CODE` не имеет достижимого верного носителя команды, которой
проверяется его же результат, — и то же самое верно для питоновских
детекторов: процессный дом сообщает закрытию, что рабочего интерпретатора нет,
хотя он есть.

Это не «неудобство инструментария»: клейм о **фактическом** устройстве среды
при расхождении — дефект дока по правилу любой стадии
(`.claude/templates/docs/gap-report.md` §«Классификация и направление правки»),
а «команда, провалившаяся молча, выглядит как команда, не нашедшая нарушений»
(`roadmap-step-execution.md` §«`grep -P` в этой среде требует `LC_ALL=C.UTF-8`»)
— здесь ровно наоборот: команда, которая **не может** провалиться молча, объявлена
единственной рабочей, а рабочая — заглушкой.

**Уровень пробела — носитель.** Истина верна хотя бы в одном месте
(`code-preconditions.md` §«Среда контура» — проверено прогоном и сходится);
дефект в **числе, месте и согласии** носителей: домов среды нет, редакций
три, две из них ложны, одна из них машинно-недостижима. Ниже уровня закрытие
не работает: правка одной шапки оставит три остальных.

**Целевой док.** Завести дом фактов среды исполнения — правило пайплайна
`.claude/rules/` (кандидат: `execution-environment.md`) либо перенести роль
дома на существующий раздел `code-preconditions.md` §«Среда контура» с
указателями из всех потребителей. Правки-спутники: `tools/spec-scope-check.py`
(шапка), `.claude/processes/roadmap-step-execution.md:120-126`,
`tools/boot-test.sh` (шапка + резолв Maven), `README.md`,
`.claude/work/backlog.md` §«Гейт сборки на закрытиях, правящих `src/`»,
`.claude/projects/…/memory/build-command.md`, `.claude/agents/code-writer.md`
(указатель на дом).

**Ожидаемый владелец.** `knowledge-curator` (дом и сведение носителей) с
хвостом на `code-writer` (форма команды сборки в его входе).

**Трассировка.** Открывал: все двенадцать файлов `tools/**` (шапки целиком);
`command -v` по `py/python/python3/mvn/java/cygpath`; `python3 -VV` и heredoc-проба;
прогон `py -3 tools/spec-scope-check.py docs/spec` (rc=127) и `python3 …` (rc=0);
прогон `bash tools/boot-test.sh` (rc=127) и `$MD/bin/mvn -o -q test-compile`
(rc=0); `git ls-files .claude/projects`; сверка ключа каталога памяти с
ключом этой машины; `.claude/tests/source-api/okx/code-preconditions.md`
§«Среда контура» и §«Что среда всё ещё не даёт»; `.claude/work/backlog.md`
§«Средовой дефицит автономного RUN тестов (контур source-api)» и §«Гейт сборки
на закрытиях, правящих `src/`»; `.claude/agents/code-writer.md` (греп по
сборке — пусто). Альтернатива «это уже парковано» проверена и **отвергнута
частично**: парковано отсутствие `mvnw` и отсутствие гейта сборки; **не
парковано** — что объявленные команды не исполняются, что носителей среды
несколько и они расходятся, и что один из них машинно-мёртв.

**Варианты.**

| Вариант | Что делает | Названная цена (правки + контекстная связность) |
|---|---|---|
| A. Достаточность существующего: объявить домом `code-preconditions.md` §«Среда контура», из остальных носителей — указатели | Дом уже существует и уже верен (проверен прогоном) | правок ~6 файлов; **контекстная связность высокая**: дом лежит в артефакте *тест-контура источника*, и `code-writer`, которому нужна команда сборки, обязан знать, что она живёт в реестре предусловий OKX-контура — то есть читателю нужен внешний контекст, чтобы найти дом |
| B. Завести `.claude/rules/execution-environment.md` как дом фактов среды (JDK, Maven, Python-лаунчер, Docker/Vault-контур), остальные — указателями | Один дом на класс «факт среды», нейтральный по предмету; `code-writer` и закрытие адресуются к нему напрямую | правок ~7 файлов + один новый; след больше на одну сущность; **контекстная связность низкая** — носитель самодостаточен |
| C. Сделать команды самодостаточными вместо документирования: `tools/boot-test.sh` и (новый) `tools/mvn.sh` резолвят JDK и Maven из кэша сами; шапки перестают быть носителем факта | Убирает класс «шапка разошлась со средой» целиком — факт добывается прогоном, а не читается | правок 2-3 скрипта; **цена названа счётно**: это не отменяет носителя для `python3`-лаунчера и для не-скриптовых потребителей (backlog-гейт, `code-writer`), то есть B всё равно нужен в урезанном виде |
| D. Крен + отдельная опция: B как дом, C как энфорсер дома | Дом называет факт, скрипт его исполняет; расхождение шапки со средой ловится прогоном, а не вычиткой | сумма цен B и C; **обратимость**: C можно отложить, дом от этого не ломается |
| E. Переоткрыть конструкцию: снять `.claude/projects/…/memory/` из репозитория как носитель знания | Убирает машинно-мёртвый носитель, а не чинит его | правка одна (+ строка дерева); **переоткрывает принятое**: размещение `.claude/projects/` уже припарковано отдельным пунктом курационного захода (`backlog.md` §«Свод адресных перекрёстий `docs/**` к смысловым ссылкам», три артефакта размещения) — вариант выписан, не выбран |

**Крен — D** (B как дом + C как энфорсер, C допускает отсрочку). Довод по
шкале следа: минимальный вариант A даёт **счётную** ошибку исхода — дом
остаётся недостижимым для того потребителя, ради которого узел заведён
(`code-writer` не имеет `code-preconditions.md` во входе, греп по его файлу
даёт ноль строк о сборке), то есть меньший след здесь оставляет ровно ту
тропу, на которой дефект и воспроизвёлся.

**Ярлык исхода — `варианты-с-креном`.**

**Ярлык дефицита — дефицита нет:** выводится из концепции (П3 «носитель
наблюдаемости — персистентная запись», П4 «факт — один дом») и из
`.claude/rules/policy-home.md`; численных величин и продуктового scope не
затрагивает. Закрывает CC.

---

## Прочее

### П1. Энфорсер §-адресов `.claude/**` меряет одну письменную форму из двух, а его дом объявляет полноту

**Вопрос.** На каком основании `.claude/rules/structure.md` утверждает, что
`anchor-check.py` разрешает **каждый** адрес `.claude/**`, если детектор
видит только форму `§«Имя»` и не видит форму `§Имя`?

**Счётно и цитаты.**
- `tools/anchor-check.py:23` — `ADDR_RE = re.compile(r'§«(?P<name>[^»]{2,200})»')`: бескавычечная форма не распознаётся вовсе.
- `.claude/rules/structure.md:132-134` — «`python3 tools/anchor-check.py` **разрешает каждый адрес** `.claude/**` … и падает на неразрешимом».
- `.claude/work/backlog.md:627,633` — «**Долг измерен, а не оценён:** `python3 tools/anchor-check.py` печатает число и перечень».
- Фактика живых `.claude/**` (102 файла, без `history/ library/ progress/ projects/`): **279** вхождений `§«` и **357** бескавычечных `§`-адресов (из них 155 словесных вида `§Персистентность`, `§Енумы`, `§Принципы`, `§Ссылки`; остальные — числовые `§3`, `§AG1`). Детектор отчитался: «адресов проверено: **272**» — 43 % предмета.
- **Мутационная проба (обе стороны).** Во временной копии `target/probe-lensG` файл с двумя битыми адресами — `§«Пассажа такого нет вовсе»` и `§Пассажа-Такого-Нет-Вовсе`: прогон → `адресов проверено: 1; НЕРАЗРЕШИМЫХ: 1`, rc=1. Тот же файл **только с бескавычечным битым адресом**: `адресов проверено: 0; НЕРАЗРЕШИМЫХ: 0`, **rc=0** — зелёный прогон на дефекте. Каталог пробы удалён.
- Расширение логики детектора на словесную бескавычечную форму (та же резолюция, тот же набор кандидатов) даёт **40 неразрешимых из 155** — порядок величины, не единицы.

**Почему не гейтит.** Не меняет поведения кода: форма ссылок и полнота
перечня.

**Уровень пробела — доказательство** (истина в одном доме и согласована;
команда меряет одну письменную форму предмета) с хвостом **носитель**
(клейм полноты в доме правила и в парковке бэклога).

**Целевой док.** `tools/anchor-check.py` (расширить `ADDR_RE`, добавить ось
в `tools/anchor-probe.sh`), `.claude/rules/structure.md:132-134` (снять
«каждый адрес» либо подтвердить его расширенным детектором),
`.claude/work/backlog.md:633` (клейм «измерен»).

**Ожидаемый владелец.** `knowledge-curator`.

**Трассировка.** Класс назван самим корпусом:
`.claude/processes/roadmap-step-execution.md:139-150` §«Измерительная команда
проверяется на полноту формы предмета» — «счёт долга видел `§«Имя»` и не видел
`§Имя` (141 против 201 — треть долга)». Стандарт записан тем же дизайн-проходом,
что ввёл этот энфорсер, и энфорсер воспроизводит названный в стандарте класс.
Встречная половина решения — `docs/concept.md` §Ссылки — форму учла явно
(«Считаются **обе письменные формы** адреса»), с командой, берущей обе;
`.claude`-половина того же решения — нет. Открывал: обе половины, оба
инструмента, `anchor-probe.sh` целиком (4 оси + 3 контроля — ни одна не про
форму записи).

**Варианты.**

| Вариант | Что делает | Названная цена |
|---|---|---|
| A. Расширить `ADDR_RE` на `§Имя` + ось в пробе | Детектор начинает мерить весь предмет | правка 2 скриптов; вскроет ~40 адресов сверх нынешних 35 — рост парковки, не рост дефекта |
| B. Оставить детектор как есть, но снять клейм полноты в доме и в парковке | Клейм перестаёт быть ложным | правка 2 клауз; **цена названа счётно**: бескавычечная форма остаётся неохраняемой навсегда, то есть половина долга не измеряется ничем |
| C. Запретить бескавычечную форму в `.claude/**` и свести её к `§«…»` | Один предмет — одна письменная форма, детектор полон по построению | свод 357 вхождений; **переоткрывает принятое** — `structure.md` §Принципы форму `§Имя` не запрещает |

**Крен — A** (минимальный след, снимает и клейм, и дыру одним ходом; B
оставляет счётную дыру, C дороже на два порядка).

**Ярлык исхода — `варианты-с-креном`. Ярлык дефицита — дефицита нет.**

---

### П2. У трёх новых детекторов нет встречного якоря-гейта, и один из них красен прямо сейчас

**Вопрос.** Что не даёт шагу выйти в `CODE` при красном `anchor-check.py`?

**Счётно и цитаты.** `.claude/skills/update-roadmap-progress.md:118` — единственный
инструмент, прогоняемый гейтом смены статуса, это `bash tools/preconditions-check.sh`.
`roadmap-step-execution.md:80,158,226-227` называет `spec-run.sh` и
`spec-mutation-check.sh` внутри стандарта закрытия. `anchor-check.py`,
`spec-pointer-check.py`, `spec-scope-check.py` не названы **ни одним** гейтом
(греп `tools/` по обоим файлам — вывод выше в «Охвате»). Фактика:
`python3 tools/anchor-check.py` → **rc=1**, 35 неразрешимых.

**Почему не гейтит.** Красный энфорсер адресов не меняет поведения кода.
Сами 35 адресов **припаркованы** (`backlog.md` §«Неразрешимые адреса пассажей
в `.claude/**`», владелец `knowledge-curator`, момент — курационный заход), и
как находка не выносятся. Не парковано — **отсутствие встречного якоря**: та
самая конструкция, которой корпус закрывает «требование записано и не
исполняется» (`roadmap-step-execution.md` §«Исполнение свипа проверяется гейтом
при смене статуса»), к двум новым энфорсерам не применена.

**Уровень пробела — доказательство.** **Целевой док** —
`.claude/skills/update-roadmap-progress.md` §«Гейт выхода из `GAPS_CLOSE_N`».
**Владелец** — `knowledge-curator`. **Варианты:** (A) добавить три прогона в
существующий гейт — цена: гейт станет красным до разбора парковки, то есть
парковка обязана получить срок; (B) добавить только `spec-pointer-check.py` и
`spec-scope-check.py` (зелёные), `anchor-check.py` — после разбора парковки —
цена: класс закрыт наполовину и вторая половина зависит от чужого момента;
(C) не добавлять, снять из `structure.md` слово «энфорсер» — цена: правило
возвращается в состояние «записано и не исполняется», против которого гейты и
заведены. **Крен — B** (меньший след, не блокирует шаг, срок второй половины
— момент парковки). **Ярлык исхода — `варианты-с-креном`. Дефицита нет.**

---

### П3. Единственный инвентарь `tools/**` в корпусе перечисляет 4 инструмента из 12

**Цитаты.** `.claude/rules/structure.md:63` — строка `tools/`: «Исполнимые
проверки и вспомогательные скрипты пайплайна (`spec-run.sh` …,
`preconditions-check.sh` …, `boot-test.sh`, `postman/`). **Не файлы знания:** …
в `knowledge-tree.md` они не регистрируются». Фактика `tools/`: 12 позиций —
`anchor-check.py`, `anchor-probe.sh`, `boot-test.sh`, `postman/`,
`preconditions-check.sh`, `spec-mutation-check.sh`, `spec-mutation-probe.sh`,
`spec-pointer-check.py`, `spec-pointer-probe.sh`, `spec-run.sh`,
`spec-scope-check.py`, `spec-scope-probe.sh`. Восемь отсутствующих —
результат двух последних закрытий.

**Почему не гейтит.** Полнота перечня, поведения не меняет. Но перечень —
**единственный**: по той же строке инструменты в дерево не входят, значит
другого места, где состав `tools/**` виден, в корпусе нет.

**Уровень — гигиена.** **Целевой док** — `.claude/rules/structure.md:63`.
**Владелец** — `knowledge-curator`. **Варианты:** (A) дополнить перечень —
цена: он стареет при каждом новом инструменте; (B) заменить перечень на
формулу «каждый инструмент называет свой предмет в шапке; состав виден
листингом» — цена: состав перестаёт быть виден чтением корпуса; (C)
зарегистрировать `tools/**` в дереве — цена: **переоткрывает принятое**
(строка прямо говорит, что инструменты в дереве не регистрируются). **Крен —
B.** **Ярлык исхода — `варианты-с-креном`. Дефицита нет.**

---

### П4. Клейм «конвенция сведена к суффиксной» в `codestyle.md` пережили две префиксные формы

**Цитаты.** `.claude/rules/codestyle.md` §«Нейминг по слоям»: «**Конвенция была
расколота ровно пополам** — семь классов суффиксом, семь префиксом, и в доках
так же, — а объявлена только одна. `GAPS_CLOSE_29` свёл обе стороны к
объявленной; момент выбран потому, что `CODE` шага 7 заводит новые DTO
источника». Фактика:
- `src/main/java/com/example/tradingbot/integration/model/okx/response/OkxApiResponse.java` — DTO **слоя интеграции** («Сырой DTO источника — за IntegrationService/adapter не выходит», javadoc), имя в префиксной форме;
- `src/main/java/com/example/tradingbot/api/model/request/OkxRawApiRequest.java` — модель **нашего API**, префиксная форма; её доковый дом — `docs/models/api/OkxRawApiRequest.md`.

Остальные 14 DTO источника — суффиксные (`InstrumentOkxResponse`,
`PositionOkxResponse`, …), 12 доков `docs/models/integrations/okx/` — тоже.

**Почему не гейтит.** Именование; поведения не меняет. Значимость в другом:
клейм полноты — это инструкция «дальше не искать», и он адресован ровно
следующему под-шагу («`CODE` шага 7 заводит новые DTO источника»).

**Уровень — носитель** (клейм разошёлся со своим перечнем; правится вместе с
перечнем, а не отдельно — `roadmap-step-execution.md` §«Клейм проверяется по
объектам, а не по строкам»).

**Целевой док.** `.claude/rules/codestyle.md` §«Нейминг по слоям».
**Владелец** — `code-writer` (природа вопроса — конвенция кода).
**Варианты:** (A) переименовать оба и клейм оставить — цена: `OkxApiResponse`
— generic-обёртка без сущности, суффиксная форма для неё (`ApiOkxResponse`)
читается хуже, то есть цена — ухудшение имени ради единообразия; (B) назвать
исключение явно (обёртка ответа и конверт raw-passthrough — не DTO сущности) и
сузить клейм — цена: конвенция получает исключение, которое надо помнить;
(C) снять слово «свёл» и записать остаток долгом — цена: `CODE` шага 7 пишет
новые DTO при незакрытой конвенции, ровно то, чего пассаж хотел избежать.
**Крен — B.** **Ярлык исхода — `варианты-с-креном`. Дефицита нет.**

---

### П5. `README.md` — живой корневой файл вне таблицы размещения и вне дерева, с битой ссылкой и двумя неисполнимыми командами

**Цитаты.** `git ls-files README.md` → отслеживается. В
`.claude/rules/structure.md` строки для него нет; в `.claude/knowledge-tree.md`
узла нет (сверка полными путями: единственные файлы вне дерева —
`.claude/settings.local.json` (гитигнор), `tools/postman/*` (по строке `tools/`)
и `README.md`); раздела «На какой вопрос отвечает этот файл» в нём нет.
`README.md:13` → `docs/ops/vault_local.md` — **каталога `docs/ops` не
существует** (файл живёт только в `.claude-archive/2026-05-21/docs/ops/`).
`README.md:26,32` — `./mvnw` / `mvn` (см. Г1).

Полный свип битых файловых ссылок по живым `docs/**` и `.claude/**`
(29 несуществующих целей) даёт: плейсхолдеры-иллюстрации (`docs/spec/x.json`,
`docs/x/y.md`, `docs/lifecycles/X.md`, `snapshot-vN.md`), намеренные ссылки на
несуществующее (`backlog.md:1131` — «его javadoc ссылается на несуществующий …»),
планируемые файлы (`backlog.md:618`), путь чужого репозитория
(`integration-okx.md:121`), устаревшие заметки `notes/` (перенос в `history/`
**припаркован**: `backlog.md`, курационный заход, «перенос устаревших заметок
`notes/` в `history/`») — и **единственную непарковую битую ссылку в живом
файле: `README.md:13`**.

**Почему не гейтит.** Не меняет поведения кода. **Уровень — гигиена**
(размещение) + **носитель** (ложный факт о команде — входит в Г1).
**Целевой док** — `README.md` + строка в `.claude/rules/structure.md`.
**Владелец** — `knowledge-curator`. **Варианты:** (A) починить ссылку и
команды, завести строку размещения «корневой README» — цена: ещё один тип в
таблице; (B) признать `README.md` техническим файлом (как `.gitignore`) и
записать это исключением — цена: файл содержит инструкции, то есть знание, и
исключение будет ложным; (C) снять `README.md` — цена: репозиторий теряет
входную точку для человека. **Крен — A.** **Ярлык исхода —
`варианты-с-креном`. Дефицита нет.**

---

### П6. «Вопрос файла — ровно один» нарушен в трёх живых носителях

**Цитаты** (механический проход по всем 348 живым файлам знания `docs/**` +
`.claude/**`; раздел «На какой вопрос отвечает этот файл» присутствует у всех
без исключения — это проверено отдельно и **чисто**):
- `.claude/processes/question-delegation.md` — «Как устроен режим автономии: кто решает развилки, что доходит до пользователя **и** как фиксируются решения» — склейка трёх вопросов через двоеточие и «и»; `structure.md` §Принципы требует переформулировки до одного (остальное — оси ответа);
- `.claude/snapshots/snapshot-v95.md` — раздел вопроса несёт, помимо вопроса, статус-блоб текущей итерации (в дереве узел записан коротко и верно: «Где мы сейчас?»); при этом `.claude/rules/snapshot-format.md` о содержании раздела вопроса не высказывается;
- `.claude/work/2026-08-26-step-7-question-flow-analysis.md` — «Какие источники **и в какой пропорции** дали поток вопросов … (числа по итерациям, повторяемость, мета-против-содержания, поверхность доков, происхождение находок, …)». Размещение этого файла **припарковано** (`backlog.md`, три артефакта размещения); форма вопроса — нет.

Сверка вопроса узла дерева с вопросом файла по всем 357 узлам: расхождений с
пересечением ниже 0.30 — семь, из них пять законные сокращения («сокращённая
форма», как объявляет сам дерево-файл), два — перечисленные выше.

**Уровень — гигиена.** **Целевой док** — три названных файла.
**Владелец** — `knowledge-curator`. **Развилки нет** (правило записано, форма
выводится). **Ярлык исхода — `выводимо`. Дефицита нет.**

---

### П7. Процессная арматура в продуктовом корпусе: класс «прежняя редакция» запрещён отдельным пассажем концепции и в парковке не назван

**Цитаты.** `docs/concept.md` §«Опровержение правит на месте»: «В тексте нет
ни «прежняя редакция снята», ни «клейм ложен», ни «уточнено», **ни отсылок к
тому, чем знание было раньше**». Фактика `docs/**` (без `concept.md`):
- **34 вхождения** оборота «прежняя редакция / прежняя посылка / прежняя формулировка / прежняя форма / прежняя запись / прежняя граница» в **24 файлах** — включая `docs/spec/risk-limits.json:34,100,1446`, `docs/spec/order-lifecycle.json:51,61,316`, `docs/spec/deal-lifecycle.json:82,1123`, `docs/spec/loss-streak-halt.json:51,418`, `docs/spec/protection-coverage.json:2124`, `docs/spec/strategy-reference.json:1583`, `docs/rules/risk-policy.md:153`, `docs/rules/error-handling-policy.md:89,125`, `docs/lifecycles/Deal.md:311`, `docs/lifecycles/Order.md:110`, `docs/models/domain/core/Position.md:151,173`;
- 1 вхождение «концепт отменён» (`docs/spec/risk-limits.json:100`);
- **3 упоминания `DOCS_CHECK_29`** (`docs/rules/risk-policy.md:162`, `docs/spec/risk-at-stop.json:26`, `docs/spec/order-sizing.json:27`) — прямое попадание в перечисленный запрет «ссылок на проверочные прогоны и итерации»;
- **105 указателей `docs/** → .claude/**` в 60 файлах** (3 из них — в `concept.md`, где границы концепции названы легально).

**Что уже парковано и находкой не выносится.** `backlog.md:1244` — «**101
вхождение процессной арматуры в 41 доке** `docs/` (номера находок, прогонов,
шагов роадмапа) — свип курации»; `backlog.md:684-686` — ссылки корпус→пайплайн,
«разбирать их **по одной**: часть законна … Дефект — ссылки, объясняющие
**происхождение** знания». Обе парковки покрывают **номера прогонов** и
**указатели в пайплайн**.

**Что не парковано.** Класс «прежняя редакция» — самый массовый из трёх (34
против 3) — ни одной парковкой не назван: он не «номер находки» и не «ссылка
в пайплайн», он **отсылка к тому, чем знание было раньше**, и запрещён
отдельным пассажем концепции. Значительная его часть живёт в `note` примеров
спек в форме «Пример падает при возврате прежней редакции и потому
фальсифицирует правку» — а стандарт, требующий эту сторону предъявить, требует
её **в отчёте закрытия**, не в корпусе
(`roadmap-step-execution.md` §«Пример засчитывается, только если он падает на
прежней форме»: «в **отчёт** идут обе стороны»).

**Почему не гейтит.** Форма записи. Направление вреда, однако, названо
концепцией: слой опровержения — второй носитель того же факта с отрицанием,
он расходится и порождает третий.

**Уровень — носитель.** **Целевой док** — новый пункт парковки в
`.claude/work/backlog.md` (курационный заход) либо расширение пункта
§«101 вхождение процессной арматуры…» на этот класс; правки — 24 файла
`docs/**`. **Владелец** — `knowledge-curator`.
**Варианты:** (A) снять оборот везде, оставив действующее знание — цена: у
части спек нота-«фальсифицирует» потеряет обоснование, придётся переносить его
в отчёт закрытия, где ему и место; (B) объявить `note` примеров спек
исключением из §«Опровержение правит на месте» — цена: **переоткрывает
принятое**, и исключение придётся защищать от расползания на прозу;
(C) оставить как есть, дополнив парковку счётом — цена: класс живёт до
неназванного момента, а он самый массовый. **Крен — A + перенос обоснования
проб в отчёт закрытия.** **Ярлык исхода — `варианты-с-креном`. Дефицита нет.**

**Побочно измерено (клейм-число в парковке).** `backlog.md:1244` называет «101
вхождение в 41 доке». Пересобрать это число по нынешней фактике не удаётся ни
одной формой запроса: `DOCS_CHECK_|GAPS_CLOSE_` даёт 3, «шаг N» — 48,
«прогон/итерация/находка/линза» — 54 (последние два класса содержат законные
употребления: «прогон примеров»). Число в парковке не пересобирается из своего
перечня — тот же класс, что П4.

---

### П8. `docs/rules/risk-policy.md:162` — битый §-адрес, не покрытый парковкой адресного долга

**Цитаты.** `docs/rules/risk-policy.md:162`: «(`DOCS_CHECK_29`, пункт 1
§«Три пункта, которые не закрываются автономно»)». Пассажа с таким именем нет
**ни в одном** файле `docs/**` и `.claude/**` (проверено сборкой индекса всех
заголовков и лид-жирных пассажей); единственное вхождение строки в
репозитории — сама ссылка. Проверено и прогоном: `python3 tools/anchor-check.py docs`
→ rc=1, два дефекта, из них `docs/concept.md → §«Имя пассажа»` — собственная
иллюстрация концепции (ложное срабатывание, см. П10), а
`docs/rules/risk-policy.md → §«Три пункта, которые не закрываются автономно»` —
настоящий.

**Что парковано.** `backlog.md:648-675` §«Свод адресных перекрёстий `docs/**`
к смысловым ссылкам» — **счёт** долга; я подтвердил его командой самого дома
(`docs/concept.md` §Ссылки): `LC_ALL=C.UTF-8 grep -roP '§(?!П[0-9])' docs/
--exclude=concept.md | wc -l` → **62**, ровно как записано в парковке (31
кавычечных + 31 бескавычечных).

**Что не парковано.** Парковка говорит о **форме** («адреса читаются как
долг»), не о **разрешимости**: для `docs/**` разрешимость не проверяет ничто —
`anchor-check.py` объявляет своим предметом `.claude/**`, а гейта на `docs/`
нет. Битый адрес — не долг формы, а битая ссылка.

**Уровень — гигиена.** **Целевой док** — `docs/rules/risk-policy.md:162`
(снять адрес и упоминание прогона одной правкой — та же строка входит в П7).
**Владелец** — `knowledge-curator`. **Развилки нет.**
**Ярлык исхода — `выводимо`. Дефицита нет.**

---

### П9. Три самоссылки в `docs/**` — док отсылает читателя к самому себе

**Цитаты.** `docs/models/domain/other/MarketPhase.md:16` — «… ни одна →
`UNKNOWN`; см. `docs/models/domain/other/MarketPhase.md`» **внутри этого же
файла**; `docs/models/domain/other/MarketStructure.md:109` («fork-A —
`docs/models/domain/other/MarketStructure.md`») и `:190` («см.
`docs/models/domain/other/MarketStructure.md` случай идентичности»). Полный
проход по `docs/**`: ровно три таких вхождения.

`MarketPhase.md` — носитель, заведённый последним закрытием («фаза рынка
внутри сделки»), и самоссылка стоит в первом же смысловом абзаце.

**Уровень — гигиена.** **Целевой док** — три названные строки.
**Владелец** — `knowledge-curator`. **Развилки нет** (либо адресат другой файл
и он называется, либо ссылка снимается). **Ярлык исхода — `выводимо`.
Дефицита нет.**

---

### П10. `anchor-check.py` объявляет дефектом собственные иллюстрации правила

**Цитаты.** Из 35 неразрешимых два — иллюстрации формы в доме самого правила:
`.claude/rules/structure.md → §«Расчёт прибыли и сверка»` и
`→ §«Длинное имя пассажа»` (обе стоят в пассаже «Адрес пишется дословно по
имени пассажа, за вычетом метаданного хвоста», где именно и разбирается,
какое сокращение законно, а какое нет). Встречная половина решения такое
самоисключение имеет явно: `docs/concept.md` §Ссылки — «за вычетом … самого
этого файла, чьи `§` есть иллюстрации формы, а не адреса», и команда несёт
`--exclude=concept.md`. У `.claude`-половины его нет.

**Триаж всех 35** (сверка каждого имени против индекса заголовков и
лид-жирных пассажей всего живого корпуса): **21** — пассажа нет нигде (из них
2 — эти иллюстрации, то есть **19 настоящих битых**); **14** — пассаж
существует, но вне набора кандидатов детектора (цель в пропущенном каталоге
`history/`/`progress/` либо файл в абзаце не назван). Сами адреса —
**припарковано**.

**Уровень — гигиена** (шум детектора). **Целевой док** —
`tools/anchor-check.py` (исключение дома правила, как у команды долга).
**Владелец** — `knowledge-curator`. **Развилки нет** (симметрия с уже принятым
решением по `docs/`). **Ярлык исхода — `выводимо`. Дефицита нет.**

---

### П11. `anchor-probe.sh` засчитывает ось по коду возврата, не различая «поймал дефект» и «интерпретатор не выполнил файл» — но контроли делают батарею громкой

**Проверено пробой, дефекта нет — фиксирую как верифицированный негатив.**
Функция `probe()` в `tools/anchor-probe.sh:8-12` засчитывает ось при любом
`rc != 0`, тогда как стандарт (`roadmap-step-execution.md` §«Оси проверочной
команды доказываются поимённо») требует «ненулевой код/непустой вывод **при
зелёном прогоне на действительном состоянии**». Проба: подставил в `PATH`
заглушку `python3`, печатающую «Python» и выходящую с кодом 49 (форма,
описанная в шапках корпуса), и прогнал батареи:

- `anchor-probe.sh` → все **4 оси** отчитались «падает» **ложно**, но все **3
  контроля ложного срабатывания** дали «ЛОЖНОЕ СРАБАТЫВАНИЕ», итог `НЕ
  ДОКАЗАНО: 3`, **rc=1** — батарея громкая;
- `spec-pointer-probe.sh` → 2 оси ложно «падают», контроль «указатель на дом
  проходит» ловит подмену, `НЕ ДОКАЗАНО: 1`, **rc=1**;
- `spec-scope-probe.sh` и `spec-mutation-probe.sh` резолвят интерпретатор по
  платформе и объявляют код возврата 3 «не найден интерпретатор» — устойчивы
  по построению.

Вывод: ложно-зелёного исхода ни одна батарея не даёт; остаток — форма
(ось засчитывается по `rc`, а не по выводу), и он ничем не гейтит. Правка —
по желанию, при ближайшем касании: сверять вывод пробы, а не только код.

**Уровень — гигиена. Развилки нет. Ярлык исхода — `выводимо`. Дефицита нет.**

---

### П12. `docs/stages/` — пустой каталог вне таблицы размещения

`ls docs/` показывает каталог `stages`; файлов в нём нет (git пустых каталогов
не отслеживает), строки в `.claude/rules/structure.md` и узла в дереве у него
нет. Остаток рабочего дерева. **Уровень — гигиена. Целевой док** — удаление
каталога. **Владелец** — `knowledge-curator`. **Ярлык исхода — `выводимо`.
Дефицита нет.**

---

## Блокирующие открытые вопросы

Проход по `.claude/work/questions/open-questions.md` по предмету линзы:
**гейтящих нет.** Два вопроса имеют форму, задевающую мой предмет
(`§«Ось упущенных возможностей»`, `§«Порядок исполнения»` — оба фигурируют в
перечне неразрешимых адресов, см. П10), но самой механики шага не блокируют:
это адресный долг, а не нерешённый выбор.

## Вынос пользователю

**Пунктов с дефицитом `подтверждение` нет.** Ни одна находка линзы не упирается
в численную величину риск-аппетита или в продуктовый scope; все выводятся из
концепции (П3, П4), из `.claude/rules/policy-home.md`,
`.claude/rules/carrier-levels.md` и `.claude/rules/structure.md`. Закрывает CC
автономно.

## Сводка

- **Находок всего — 13:** гейтящих **1** (Г1), прочих **12** (П1-П12).
- **Гейтящий узел, по строке:**
  - **Г1** — факты среды исполнения (сборка + лаунчер Python) живут в четырёх носителях с тремя расходящимися редакциями, дома нет; `./mvnw`, `mvn -o test-compile`, `tools/boot-test.sh` и `py -3 tools/spec-scope-check.py` — все четыре объявленные команды в этой среде отказывают (rc=127), верная форма есть ровно в одном месте и оно не входит во вход `code-writer`, а машинно-достижимый носитель памяти харнесса мёртв по ключу и ложен по содержанию.
- **Стадия остановки обхода:** прошёл все стадии (стадия 2).
- **Вердикт линзы:** предмет линзы **выход в `CODE` практически не держит** — форма, именование и полнота перечней поведения кода не меняют. Единственный гейт — Г1, и он гейтит не корректность кода, а **проверяемость** его результата: гейт сборки уже сформулирован в бэклоге и предписывает неисполнимую команду, а кейс, на котором он заведён (некомпилирующееся дерево, пережившее два полнокорпусных прогона), воспроизводится тем же механизмом. Остальные 12 позиций — курационный заход; **из них три (П1, П2, П4) — прямые следствия конструкций последнего закрытия** и одна (П9) — дефект в носителе, им же заведённом.
- **Ярлыки исхода** (пересчёт по телу): `варианты-с-креном` — **7** (Г1, П1, П2, П3, П4, П5, П7); `выводимо` — **6** (П6, П8, П9, П10, П11, П12). Сумма 13, сходится с перечнем находок.
- **Ярлыки дефицита:** `подтверждение` — 0; `грунт` — 0; `работа` — 0; «дефицита нет» — 13.
- **Владельцы:** `knowledge-curator` — 12 (Г1 с хвостом на `code-writer`, П1, П2, П3, П5, П6, П7, П8, П9, П10, П11, П12); `code-writer` — 1 (П4).
- **Сколько гейтящих — следствия конструкций последнего закрытия:** **1 из 1**, частично. Питоновская половина Г1 (шапка `spec-scope-check.py` против переносимости, объявленной «СНЯТОЙ» в `backlog.md:2482-2486`, и клейм процессного дома о заглушках) введена/затронута последним закрытием; мавеновская половина (`mvnw`, `boot-test.sh`, память харнесса) старше и последним закрытием только не тронута.
- **Что проверено и оказалось чистым:** раздел «На какой вопрос отвечает этот файл» — у **всех** 348 живых файлов знания `docs/**` и `.claude/**`; `knowledge-tree.md` против фактики — **0** узлов без файла (357 узлов, полными путями) и **0** файлов вне дерева, кроме гитигнорного `settings.local.json` и `tools/postman/*`, выведенных строкой `tools/`; счёт адресного долга `docs/**` (62) сходится с парковкой ровно; девять из одиннадцати команд корпуса зелены, две батареи проб устойчивы к подмене интерпретатора; `mvn -o test-compile` через дистрибутив из кэша — **rc=0**, дерево собирается.
- **Объём.** 38.9 тыс. символов (61 КБ в UTF-8; кириллица — два байта на символ).
  Пересказ снят: полные выводы грепов и прогонов в отчёт не вклеены — названы команда,
  код возврата и итоговая строка. Превышение ориентира составляют два перечня
  носителей узла Г1 (двенадцать строк «носитель — что объявляет — что фактически»):
  это мандатное поле «Цитаты», и по правилу потолка оно ему не уступает.
- **Рекомендация:** `GAPS_CLOSE_30` нужен для Г1; остальное диспозиционируется явно (закрыто либо припарковано с адресатом и носителем) по правилу
  `roadmap-step-execution.md` §«`GAPS_CLOSE_N` диспозиционирует все находки прогона».
