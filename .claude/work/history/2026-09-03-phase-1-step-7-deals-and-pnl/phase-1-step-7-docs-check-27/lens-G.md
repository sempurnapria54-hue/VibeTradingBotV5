# Линза G — остаток корпуса и структурная гигиена (`DOCS_CHECK_27`)

## На какой вопрос отвечает этот файл

Какие пробелы нашла линза G прогона `DOCS_CHECK_27` шага 7 фазы 1.

## Охват

**Часть 1 — остаток корпуса.** `docs/dictionary/` (1 файл),
`docs/models/api/` (1 файл), `docs/models/persistence/` (каталога нет),
`docs/decisions/` (каталога нет), плюс сквозной проход по всем `docs/**`
(199 `.md` + 17 `.json` спеки) на противоречия с `docs/concept.md`.

**Часть 2 — структурная гигиена.** `docs/**` и `.claude/**` — 318 живых
файлов знания (запрещённые пути исключены из выборки на этапе сбора
списка: `.claude/work/history/**`, `.claude/snapshots/**`,
`phase-1-step-7-docs-check-26*`, `phase-1-step-7-gaps-close-26.md`,
`phase-1-step-7-chronicle.md`, `.claude/work/decision-digest.md`,
`.claude-archive/**`).

**Вне охвата:** `src/` (граница `concept-review` §«Граница охвата —
только доки»); содержание запрещённых путей.

## Стадия остановки

**Прошёл все стадии (стадия 2).** Гейтящих вопросов, останавливающих
обход, линза не нашла: весь охват — форма корпуса и целостность
носителей, механику шага 7 ни одна находка не блокирует.

## Находки — блокирует выход в CODE

**Нет.** Ни одна находка линзы G не мешает специфицировать шаг 7: все
они — дефекты формы, размещения и ссылочной целостности, а не пробелы в
спецификации многотраншевой сделки.

## Находки — прочее

### G-1. Провенансные врезки решений живут в доменных доках

- **Вопрос.** Доменные доки несут врезки «кто решил, когда, что
  отклонено, каким закрытием», хотя `docs/concept.md` относит этот класс
  к концепции и истории, а не к корпусу.
- **Цитаты (носители).**
  - `docs/rules/risk-policy.md:101-105` — «*(Решение держателя
    2026-08-29 — база невозрастающая везде… альтернатива «третье число
    конфигурации» отклонена… Закрытие `GAPS_CLOSE_26`.)*»
  - `docs/rules/risk-policy.md:173-175` — «*(Решение держателя
    2026-08-29: шорт равнозначен лонгу… Отклонены — третье число
    риск-аппетита… Закрытие `GAPS_CLOSE_26`.)*»
  - `docs/models/domain/core/AlgoOrder.md:104-106` — «*(Решение
    восстановлено `GAPS_CLOSE_26`: оно было потеряно при переписывании
    корпуса, а не отменено…)*»
  - Нарушаемые клаузы: `docs/concept.md:462-465` («Не содержит: … какие
    были альтернативы; что было записано раньше и почему снято; какая
    проверка это нашла; кто ратифицировал»), `docs/concept.md:195-198`
    (П4: «Никакой процессной и ссылочной арматуры. Ни ссылок на
    проверочные прогоны и итерации»).
- **Целевой док.** Правка трёх врезок на месте (`risk-policy.md`,
  `AlgoOrder.md`); провенанс — в `history/` шага, не в корпусе.
- **Ожидаемый владелец.** `knowledge-curator` (реконсиляция формы
  корпуса).
- **Трассировка.** Греп по `docs/**` на
  `GAPS_CLOSE|решение держателя|восстановлен|прежняя редакция`; открыты
  и прочитаны `docs/concept.md` целиком, `risk-policy.md:96-180`,
  `AlgoOrder.md:90-110`.
- **Варианты.**

  | Вариант | Что делает | Названная цена |
  |---|---|---|
  | Достаточность существующего носителя: вырезать врезки — содержание уже в `history/` шага | 3 правки, знание не теряется | нужно проверить, что каждый отклонённый вариант реально записан в `history/`; иначе довод теряется |
  | Оставить как есть, признав врезку жанром доменного дока | 0 правок | переоткрывает `concept.md:462-465` и П4; врезки будут воспроизводиться каждым `GAPS_CLOSE_N` |
  | Свести врезку к смысловой строке без номера прогона и без «отклонено» | 3 правки, часть довода рядом | половинчато: «кто ратифицировал» остаётся в доке, конфликт с `concept.md:464` не снят |

  **Крен — первый вариант:** концепция названа арбитром явно
  (`docs/concept.md:536-538`), и правка клаузы — меньший след по шкале.
- **Ярлык исхода.** `варианты-с-креном`.
- **Ярлык дефицита.** дефицита нет — выводится из `docs/concept.md`.

### G-2. §-адресация внутри `docs/` против прямого запрета концепции

- **Вопрос.** Концепция запрещает якорные адреса в корпусе; в `docs/` их
  10. Одновременно `.claude/rules/structure.md` §Принципы легитимирует
  §-адресацию, не оговаривая, что на `docs/` она не распространяется —
  это doc↔rule несогласованность, а не только нарушение.
- **Цитаты (носители).**
  - Запрет: `docs/concept.md:502-507` («Ссылка **смысловая, на дом** …
    Не адрес пассажа, не строка, не якорь. Внутрифайловые адресные
    перекрёстья не используются»); `docs/concept.md:195-198` (П4).
  - Нарушители: `docs/rules/risk-policy.md:22`, `:98`, `:287` (три
    внутрифайловых); `docs/rules/loss-streak-halt.md:32`, `:81`, `:92`;
    `docs/rules/pnl-reconciliation.md:49`;
    `docs/models/domain/aggregate/Deal.md:163`;
    `docs/models/domain/other/DealActionState.md:38` («см. §Енумы»);
    `docs/models/domain/core/AlgoOrder.md:96`.
  - Встречная клауза: `.claude/rules/structure.md` §Принципы, пункт про
    §-адресацию именованного пассажа — записан как общий, без исключения
    для `docs/`.
- **Целевой док.** `.claude/rules/structure.md` (оговорка области
  действия) плюс 10 правок в `docs/`.
- **Ожидаемый владелец.** `knowledge-curator`.
- **Трассировка.** `grep -rn '§' docs/` → ровно 10 попаданий; сверено с
  `concept.md:502-507` и с §Принципами `structure.md` (обязательный
  вход линзы).
- **Варианты.**

  | Вариант | Что делает | Названная цена |
  |---|---|---|
  | Снять 10 §-адресов в `docs/`, оставив смысловую ссылку на дом; в `structure.md` §Принципы дописать «область — `.claude/**`» | приводит корпус к концепции и снимает конфликт двух правил | 10 правок + 1; три адреса (`Deal.md:163` §П1, `pnl-reconciliation.md:49` §П2, `loss-streak-halt.md:92` §П1) указывают на принцип концепции — при снятии теряется точка, куда смотреть |
  | Расширить `concept.md` §Ссылки исключением «адрес на принцип концепции допустим» | 1 правка концепции, 7 правок в `docs/` | переоткрывает ратифицированный П4; исключение живёт ровно до следующего адреса |
  | Ничего не менять, признать §-адреса допустимыми в `docs/` | 0 правок | остаются два взаимно противоречащих правила; конфликт читается в пользу концепции (`concept.md:536-538`), то есть `docs/` остаётся дефектным по определению |

  **Крен — первый.** Разведение области действия — меньший след, чем
  правка принципа, и именно его сейчас нет ни в одном носителе.
- **Ярлык исхода.** `варианты-с-креном`.
- **Ярлык дефицита.** дефицита нет — выводится из `docs/concept.md`.

### G-3. Ссылки из `docs/` в рабочие файлы пайплайна

- **Вопрос.** Продуктовый корпус адресует носители исполнительной
  работы; при закрытии или переносе пункта такая ссылка ломается, а
  `curation.md` §Чек-лист п. 6 чинит ссылки только в живых файлах —
  обратное направление никем не охраняется.
- **Цитаты (носители).**
  - `docs/models/domain/core/AlgoOrder.md:96-97` →
    `.claude/work/backlog.md` §«Снятие ограничения `MARK`-only — по
    измеренному базису».
  - `docs/lifecycles/Strategy.md:17` →
    `.claude/work/history/2026-05-27-миграция-торговых-сущностей/tasks-strategy.md`
    (ссылка из живого дока в архив).
  - `docs/integrations/okx/contracts/position.md:90`,
    `docs/models/integrations/okx/AccountBillOkxResponse.md:44`,
    `docs/models/mapping/TradeFeeRate.md:167` →
    `.claude/tests/source-api/okx/plan.md`.
  - Запрет: `docs/concept.md:195-198` (П4).
- **Целевой док.** 5 правок в `docs/`; если указатель на встречный якорь
  нужен, его дом — в `.claude/`, а не в корпусе.
- **Ожидаемый владелец.** `knowledge-curator`.
- **Трассировка.** `grep -rn '\.claude/' docs/` → 95 строк; ссылки на
  устойчивые дома (`decisions|rules|processes|skills`) отделены от
  ссылок на рабочие носители (`work/**`, `tests/**`).
- **Варианты.**

  | Вариант | Что делает | Названная цена |
  |---|---|---|
  | Снять ссылки на `work/**` и `tests/**`; ограничение в доке остаётся, встречный якорь — только в `backlog.md` | корпус чист от арматуры | читатель дока теряет след, где условие снятия ограничения; риск, что ограничение переживёт свой повод |
  | Снять только `work/**`, ссылки на `tests/**` оставить | 2 правки вместо 5 | `tests/**` — тоже пайплайн (`structure.md`: «артефакт проверочной деятельности, не продуктовое знание»); полумера, воспроизводит находку |
  | Завести в `docs/` клаузу «условие снятия названо, дом условия — вне корпуса» без адреса | 5 правок, знание о наличии условия сохраняется | формулировка «где-то есть якорь» слабее адреса: проверить исполнение нельзя |

  **Крен — третий** для `AlgoOrder.md:96` (условие снятия — несущее
  знание, просто убрать нельзя) и **первый** для остальных четырёх.
- **Ярлык исхода.** `варианты-с-креном`.
- **Ярлык дефицита.** дефицита нет — выводится из `docs/concept.md`.

### G-4. Слой опровержения («чем знание было раньше») в трёх доках

- **Вопрос.** Концепция запрещает второй слой того же факта с
  отрицанием; в корпусе он есть в трёх местах, причём один — в
  заголовке.
- **Цитаты (носители).**
  - `docs/integrations/okx/contracts/position-tiers.md:18-23` — заголовок
    `## Уточнение пути (находка прогона 3)` плюс «Сторонний скелет
    указывал `GET /account/position-tiers`; … Манифестная пометка «путь к
    подтверждению» **снята**».
  - `docs/models/integrations/okx/AccountBillOkxResponse.md:35-36` —
    «**Ранее** `fee` **отбрасывался** с доводом «знак несёт уже
    `balChg`» — верным **только** при…».
  - `docs/rules/risk-policy.md:165-171` — «**Почему записано именно
    так.** Клейм «не может потерять больше физически» верен ровно для
    одной стороны, а стои́т в правиле безусловно…».
  - Запрет: `docs/concept.md:509-518` («В тексте нет ни «прежняя
    редакция снята», ни «клейм ложен», ни «уточнено», ни отсылок к тому,
    чем знание было раньше»); `docs/concept.md:464`.
- **Целевой док.** Правка трёх пассажей на месте.
- **Ожидаемый владелец.** `knowledge-curator`; по
  `AccountBillOkxResponse.md` и `position-tiers.md` — первым
  `integrator` (владелец интеграционного знания источника по карте
  `question-delegation.md`): решение «что здесь остаётся фактом»
  касается полей источника.
- **Трассировка.** Грепы `прежняя редакция|снят|восстановлен|Ранее` и
  `^\*\*(Почему|Довод|Альтернатив)` по `docs/**`; три пассажа прочитаны
  целиком, сверены с `concept.md:509-518`.
- **Варианты.**

  | Вариант | Что делает | Названная цена |
  |---|---|---|
  | Переписать три пассажа действующим знанием («путь такой-то»; «`fee` в used, потому что при комбинированном bill компонента не извлекается»; клейм правила сформулировать сразу по стороне) | корпус приходит к `concept.md:509-518` | у `AccountBillOkxResponse.md` рядом лежит разбор гранулярности — при вырезании «Ранее…» его нельзя потерять: это знание, а не история |
  | Оставить как след: провенанс поля источника ценен | 0 правок | переоткрывает §«Опровержение правит на месте»; следующая ревизия добавит четвёртый слой |
  | Вынести провенанс в `docs/integrations/okx/rules/` отдельным носителем истории источника | 3 правки плюс новый файл | новый жанр в корпусе — больший след по шкале; и это ровно «реестр решений», который концепция отвергает (`concept.md:19-21`) |

  **Крен — первый,** с оговоркой по `AccountBillOkxResponse.md`: тест
  вычитанием (`concept.md:13-17`) оставляет разбор гранулярности и
  убирает «Ранее … отбрасывался».
- **Ярлык исхода.** `варианты-с-креном`.
- **Ярлык дефицита.** дефицита нет — выводится из `docs/concept.md`.

### G-5. Обоснование выбора в словарной статье

- **Вопрос.** `docs/dictionary/leg-term.md` — единственный житель типа —
  несёт раздел с альтернативой и её ценой, то есть содержание
  концепции/истории в доке типа «что означает термин».
- **Цитаты (носители).** `docs/dictionary/leg-term.md:26-29` —
  «**Почему разведено именно так.** Слово «нога» в корпусе уже несёт два
  значения; третье сделало бы предложения вида «нога ноги»
  неразличимыми. Транш — слово без прежних значений, поэтому цену
  разведения платит новый уровень, а не 200 существующих упоминаний.»
  Нарушаемые клаузы: `docs/concept.md:462-465`, `docs/concept.md:13-17`
  (тест вычитанием). Встречный дом: `.claude/rules/naming.md`
  §«Разведение уровней абстракции» — этот довод там уже живёт.
- **Целевой док.** `docs/dictionary/leg-term.md` (снять раздел);
  содержание уже есть в `.claude/rules/naming.md`.
- **Ожидаемый владелец.** `knowledge-curator`.
- **Трассировка.** `leg-term.md` прочитан целиком; открыт
  `naming.md` §«Разведение уровней абстракции» (обязательный вход через
  `CLAUDE.md`) и подтверждено, что развилка «уникальные основы против
  систематического маркера» там записана.
- **Варианты.**

  | Вариант | Что делает | Названная цена |
  |---|---|---|
  | Снять раздел: тест вычитанием даёт, что значение термина без него полное | 1 правка, дублирования с `naming.md` нет | теряется конкретика «200 упоминаний» — но это довод разового выбора, `concept.md:19-21` его в корпус не пускает |
  | Оставить: словарь по природе объясняет, а не только определяет | 0 правок | переоткрывает `concept.md:462-465`; и это пересказ политики вне дома (`policy-home.md`), дом — `naming.md` |
  | Свести к строке-указателю «способ разведения — `.claude/rules/naming.md`» | 1 правка, след остаётся | ссылка из `docs/` в `.claude/rules/` — тот же класс, что G-3, хотя и на устойчивый дом |

  **Крен — первый.**
- **Ярлык исхода.** `варианты-с-креном`.
- **Ярлык дефицита.** дефицита нет — выводится из `docs/concept.md` и
  `naming.md`.

### G-6. `okx-coverage-manifest.md` — не тот тип, не тот адрес, нет в дереве

- **Вопрос.** Файл «какова полнота покрытия поверхности OKX REST API»
  лежит в `.claude/processes/` (вопрос типа — «как устроен этот
  методологический процесс?»); девять живых носителей адресуют его как
  `docs/integrations/{name}/coverage-manifest.md`, а такого файла нет;
  в `knowledge-tree.md` файла нет вовсе.
- **Цитаты (носители).**
  - Факт: `.claude/processes/okx-coverage-manifest.md` — раздел-вопрос
    «Какова полнота покрытия поверхности OKX REST API нашими
    интеграционными доками…».
  - Тип: `.claude/rules/structure.md`, строка `.claude/processes/` («Как
    устроен этот методологический процесс? … Один файл = один
    методологический процесс»).
  - Битый адрес в живых носителях:
    `.claude/processes/api-docs-completion.md:58`;
    `.claude/processes/source-api-testing.md:46`, `:107`, `:150`;
    `.claude/skills/test-design.md:31`, `:173`, `:187`;
    `.claude/templates/docs/test-plan.md:39`, `:103`.
  - Дерево: `.claude/knowledge-tree.md:88-95` — узел `processes/`
    перечисляет 7 файлов, восьмого нет.
  - Правило: `.claude/rules/curation.md` §Чек-лист п. 5 и п. 8.
- **Целевой док.** `.claude/rules/structure.md` (если вводится тип
  «манифест покрытия»), либо перенос файла; плюс правка 9 адресов и узел
  дерева.
- **Ожидаемый владелец.** `knowledge-curator`; тип носителя
  интеграционного покрытия — совместно с `integrator`.
- **Трассировка.** Дельта «дерево против фактики» по basename дала
  `okx-coverage-manifest.md` как незарегистрированный; `find` показал
  фактический путь; проверка существования 2231 путевой ссылки дала
  9 обращений к несуществующему
  `docs/integrations/{name}/coverage-manifest.md`.
- **Варианты.**

  | Вариант | Что делает | Названная цена |
  |---|---|---|
  | Перенести в `docs/integrations/okx/coverage-manifest.md` — туда, куда 9 носителей уже указывают | 0 правок адресов, 1 перенос, 1 строка дерева | манифест считает покрытие **нашими доками**, то есть артефакт проверочной деятельности; `.claude/decisions/test-knowledge-type.md` такой класс держит в `.claude/` — перенос переоткрывает это решение |
  | Оставить в `.claude/`, поправить 9 адресов, завести тип в `structure.md` | адрес и тип сходятся с фактом | 9 правок; и `.claude/processes/` перестаёт быть «один файл = один процесс» — нужен новый тип, больший след по шкале |
  | Оставить где лежит, поправить только 9 адресов, тип не заводить | 9 правок, минимальный след | остаётся незарегистрированный артефакт (`curation.md` §5): следующий свип найдёт его снова |

  **Крен — второй.** Счётная разница: файл адресуется девятью
  носителями и один раз попадает в дерево; только вариант с названным
  типом не воспроизводит находку на следующем свипе. Первый дешевле
  правками, но переоткрывает ратифицированное решение о типе
  тест-знания.
- **Ярлык исхода.** `варианты-с-креном`.
- **Ярлык дефицита.** дефицита нет — выводится из `structure.md` и
  `test-knowledge-type.md`.

### G-7. Узел `.claude/work/questions/` отсутствует в `knowledge-tree.md`

- **Вопрос.** Зарегистрированный тип с фиксированным именем файла и
  живым содержанием в дереве не представлен: ни папки, ни
  `open-questions.md`, ни `tasks/`.
- **Цитаты (носители).**
  - `.claude/knowledge-tree.md:147-162` — узел `work/` перечисляет
    `backlog.md`, `decision-digest.md`, датированный аналитический файл,
    `progress/`, `roadmap/`, `history/`; `questions/` нет.
  - `.claude/rules/structure.md` — строки
    `.claude/work/questions/open-questions.md` и
    `.claude/work/questions/tasks/` (оба типа зарегистрированы).
  - Факт: `.claude/work/questions/open-questions.md` (живые вопросы,
    283+ строк), `.claude/work/questions/tasks/`.
  - Правило: `.claude/rules/curation.md` §Чек-лист п. 8 —
    «`knowledge-tree.md` соответствует фактике: каждый файл знания — в
    дереве со своим вопросом».
- **Целевой док.** `.claude/knowledge-tree.md`.
- **Ожидаемый владелец.** `knowledge-curator`.
- **Трассировка.** `grep -n 'open-questions' .claude/knowledge-tree.md`
  → пусто; узел `work/` прочитан целиком (строки 147-162).
- **Варианты.** Развилки нет — п. 8 чек-листа предписывает единственное
  действие: добавить узел с вопросом файла.
- **Ярлык исхода.** `выводимо`.
- **Ярлык дефицита.** дефицита нет — закрыто прогоном.

### G-8. Четырнадцать неразрешимых §-адресов, три — в живых входах ревью

- **Вопрос.** §-адрес, которому в целевом файле не соответствует ни
  заголовок, ни лид-жирный пассаж, — битая ссылка
  (`.claude/rules/structure.md` §Принципы, подпункт «Что остаётся
  дефектом»). Найдено 14; три из них — во входных артефактах ревью, то
  есть ломаются у каждой линзы каждого прогона.
- **Цитаты (носители).** Перечень с проверенной целью:

  | Носитель | Адрес | Что в цели есть на самом деле |
  |---|---|---|
  | `.claude/skills/concept-review.md:184` | `question-delegation.md` §Калибровка | §«Критерий один: выводится ли ответ из концепции» |
  | `.claude/skills/design-fork.md:120` | `question-delegation.md` §Калибровка | то же |
  | `.claude/agents/reviewer.md:14` | `gap-report.md` §Варианты эскалации | поле «**Варианты**» в §«Набор полей находки» |
  | `.claude/skills/design-fork.md:82` | `gap-report.md` §одноимённый | адрес не по имени пассажа — запрещено §«Адрес пишется дословно по имени пассажа» |
  | `.claude/work/backlog.md:1036` | `Deal.md` §«Предикат отбора слагаемых» | нет ни в `lifecycles/Deal.md`, ни в `models/domain/aggregate/Deal.md` |
  | `.claude/work/backlog.md:1352` | `pnl-reconciliation.md` реш.5 §epsilon | §«Допуск» |
  | `.claude/work/backlog.md:1489` | то же | то же |
  | `.claude/work/questions/open-questions.md:234` | то же | то же |
  | `.claude/work/backlog.md:604` | `DealOrchestratorJob.md` §Concurrency-guard | §«Операционная оболочка» |
  | `.claude/work/backlog.md:956` | `replace-not-amend.md` §Решение п. 3 | §«Правило», §«Форма» |
  | `.claude/work/questions/open-questions.md:39` | `replace-not-amend.md` §Следствия | то же |
  | `.claude/work/questions/open-questions.md:274` | `Exchange.md` §Енумы | §«Енум `Status`» |
  | `.claude/tests/source-api/okx/plan.md:1565` | `PositionCloseResult.md` §Validation | §«Структурная валидация — до маппинга» |
  | `.claude/tests/source-api/okx/plan.md:1715` | `contracts/position.md` §«Идентификация записи» | §«История закрытых позиций (источник числа `resultProfit`)» |

- **Целевой док.** 14 правок в носителях-источниках; цели не трогаются.
- **Ожидаемый владелец.** `knowledge-curator`.
- **Трассировка.** Метод и счёт — §Гигиена, строка «§-адреса». Каждый из
  14 подтверждён отдельно: открыт целевой файл, перечислены его
  заголовки уровня 2-3 и прогрепан лид-жирный пассаж по имени адреса.
- **Варианты.** Развилки нет: `structure.md` предписывает починку. Три
  адреса (`design-fork.md:82` и два §Калибровка) требуют выбора **имени**
  цели, но не выбора действия.
- **Ярлык исхода.** `выводимо`.
- **Ярлык дефицита.** дефицита нет — закрыто прогоном.

### G-9. Двадцать пять битых файловых ссылок; двадцать четыре — в четырёх устаревших заметках

- **Вопрос.** `curation.md` §Чек-лист п. 6 требует чинить битые ссылки в
  живых файлах, п. 4 — уводить в `history/` заметки, чья тема закрыта.
  Четыре заметки массово адресуют носители, снятые переписыванием
  корпуса; ещё одна ссылка битая в живом скилле.
- **Цитаты (носители).**
  - `.claude/skills/integration-okx.md:121` →
    `docs/endpointFunctionList.md` (файла нет; **живой скилл**, вход
    `integrator`).
  - `.claude/notes/2026-05-26-обкатка-классификации-торговые-модели.md` —
    19 битых (строки 60, 67, 85, 94, 95, 102, 129, 164, 190, 216, 217,
    235, 258, 292, 293, 318, 391, 419, 575): `docs/models/Position.md`,
    `docs/models/Deal.md`, `docs/rules/exchange-adapter-boundary.md` и
    другие имена доперезаписной раскладки.
  - `.claude/notes/2026-08-23-разрывы-спека-кода-на-тропе-живой-сделки.md:77`,
    `:199`, `:201` → `docs/components/ManagingHandler.md`,
    `docs/rules/risk-creating-entry-protection.md`.
  - `.claude/notes/2026-07-02-code-review-full-codebase.md:54` →
    `docs/rules/risk-creating-entry-protection.md`.
  - `.claude/notes/2026-06-05-аудит-делегируемости-вопросов.md:74` →
    `docs/decisions/strategy-condition-authoring-contract.md` (каталога
    `docs/decisions/` в проекте нет вовсе).
- **Целевой док.** `.claude/skills/integration-okx.md` — правка; четыре
  заметки — решение «чинить или в `history/`».
- **Ожидаемый владелец.** `knowledge-curator`.
- **Трассировка.** Счёт — §Гигиена, строка «файловые ссылки». Проверка
  существования каждой цели; шаблоны-плейсхолдеры (`<X>`, `{name}`,
  `*`, `snapshot-vN`) исключены отдельным проходом и пересчитаны.
- **Варианты.**

  | Вариант | Что делает | Названная цена |
  |---|---|---|
  | Четыре заметки — в `history/` (тема закрыта переписыванием корпуса), `integration-okx.md` починить | 1 правка плюс 4 переноса; п. 4 и п. 6 чек-листа исполнены одним ходом | у заметок могут быть живые входящие ссылки (провенанс правил) — п. 4 такие оставляет; перед переносом нужен проход по входящим |
  | Починить все 25 адресов на новые имена корпуса | ссылки живы, заметки остаются | 25 правок ради артефактов, чья тема закрыта; следующая ревизия корпуса сломает их снова |
  | Починить только `integration-okx.md`, заметки не трогать | 1 правка | 24 битых адреса остаются в живом каталоге; п. 4 и п. 6 не исполнены — находка воспроизводится |

  **Крен — первый.**
- **Ярлык исхода.** `варианты-с-креном`.
- **Ярлык дефицита.** дефицита нет — выводится из `curation.md`.

### G-10. `ClientService` — снятое имя слоя живёт в двух носителях пайплайна

- **Вопрос.** Граница с источником переименована в `Integration`; дом
  правила говорит `IntegrationService`. Два живых носителя пайплайна
  по-прежнему называют слой `ClientService`, причём один — таблица
  размещения, обязательный вход всякой классификации.
- **Цитаты (носители).**
  - `.claude/rules/structure.md:49` — «Единственное, что выходит за
    `ClientService`/adapter (см. `docs/rules/raw-exchange-dto-boundary.md`)».
  - `.claude/decisions/model-layer-ontology.md:59` — «`ClientService` /
    adapter (см. …)».
  - Дом: `docs/rules/raw-exchange-dto-boundary.md:9` — «**Сырой ответ
    источника не выходит за `IntegrationService`.**»
  - Конвенция: `.claude/rules/codestyle.md` §Слои — «Слоя «Client» нет —
    граница с биржей называется **Integration**».
- **Целевой док.** `.claude/rules/structure.md:49`;
  `.claude/decisions/model-layer-ontology.md:59` — по решению владельца.
- **Ожидаемый владелец.** `knowledge-curator`.
- **Трассировка.** `grep -rn 'ClientService'` по `docs/`,
  `.claude/rules`, `.claude/skills`, `.claude/decisions` — 4 попадания,
  из них 2 в живых носителях; открыт
  `raw-exchange-dto-boundary.md:1-25`, сверен с `codestyle` §Слои.
- **Варианты.**

  | Вариант | Что делает | Названная цена |
  |---|---|---|
  | Поправить `structure.md:49` на `IntegrationService`; `model-layer-ontology.md` не трогать (решение описывает своё время) | 1 правка; вход классификации перестаёт называть несуществующий слой | в `decisions/` остаётся снятое имя — читатель решения может принять его за действующее |
  | Поправить оба носителя | 2 правки, снятого имени в живых носителях нет | правка `decisions/` задним числом стирает, что решение принималось при другой номенклатуре |
  | Ничего: имя историческое, читается по контексту | 0 правок | `structure.md` — обязательный вход, и он называет несуществующий слой; это тот же класс, что «клейм о фактическом устройстве» |

  **Крен — первый.** Минимальный след, снимает дефект там, где он
  действует (правило), не трогая архив довода.
- **Ярлык исхода.** `варианты-с-креном`.
- **Ярлык дефицита.** дефицита нет.

### G-11. `docs/models/api/` — единственный житель описывает конверт тест-контура

- **Вопрос.** Тип `docs/models/api/` объявлен как «Request/response DTO
  **нашего API**»; единственный файл описывает конверт generic-прокси на
  OKX, существующий ради контура тестов источника, трижды ссылается на
  `.claude/**` и содержит ложную атрибуцию.
- **Цитаты (носители).**
  - `docs/models/api/OkxRawApiRequest.md:5-6` — «конверт запроса
    generic-эндпоинта `POST /api/proxy/okx/raw`».
  - `docs/models/api/OkxRawApiRequest.md:22` —
    «(`.claude/decisions/source-api-target-rebase.md`,).» — висящая
    запятая; `:28` и `:31` — ещё две ссылки в `.claude/`.
  - Тот же огрызок:
    `docs/models/integrations/okx/TickerOkxResponse.md:23`.
  - `docs/models/api/OkxRawApiRequest.md:31` — «Конвенции api-слоя —
    `.claude/rules/structure.md`»: в `structure.md` конвенций api-слоя
    нет, есть только строка типа («Слой-скаффолд»). Атрибуция проверена
    открытием цели.
  - Тип: `.claude/rules/structure.md`, строка `docs/models/api/`.
- **Целевой док.** `docs/models/api/OkxRawApiRequest.md` (правка трёх
  ссылок и решение о размещении),
  `docs/models/integrations/okx/TickerOkxResponse.md:23`.
- **Ожидаемый владелец.** `integrator` (модель поверхности источника);
  размещение — `knowledge-curator`.
- **Трассировка.** `OkxRawApiRequest.md` прочитан целиком; открыт
  `structure.md` и подтверждено отсутствие «конвенций api-слоя»;
  огрызок найден грепом по `docs/`.
- **Варианты.**

  | Вариант | Что делает | Названная цена |
  |---|---|---|
  | Оставить в типе, починить 3 ссылки (висящие запятые плюс ложная атрибуция `structure.md`) | 3 правки; тип остаётся с одним жителем | тип `docs/models/api/` продолжает называться «наше API», а населён поверхностью тест-контура — расхождение вопроса типа с содержимым остаётся |
  | Перенести в `.claude/tests/source-api/okx/`, где живёт контур | тип освобождается до скаффолда, содержимое рядом с потребителем | `OkxRawApiRequest` — реальный DTO приложения (`@NotBlank`, `@NotNull`, контроллер `OkxProxyController`), а не тест-артефакт: перенос уводит спеку кода из корпуса |
  | Оставить и уточнить вопрос типа в `structure.md` («модель нашей API-поверхности, включая служебные конверты») | 1 правка правила плюс 3 правки ссылок | правка правила ради одного файла — больший след, чем правка файла |

  **Крен — первый:** ссылочные дефекты чинятся безусловно (они не зависят
  от исхода развилки о типе), а сама развилка о типе — при появлении
  второго жителя.
- **Ярлык исхода.** `варианты-с-креном`.
- **Ярлык дефицита.** дефицита нет.

### G-12. «Вопрос ровно один» нарушен в одиннадцати файлах

- **Вопрос.** `structure.md` §Принципы и `concept.md` §«Один вопрос на
  файл» требуют одной фразы; в ряде файлов раздел склеивает несколько
  предикатов через «и»/перечисление либо разросся в абзац с указателями и
  легендой.
- **Цитаты (носители).** Склейка предикатов:
  `.claude/decisions/model-layer-ontology.md` («Как организованы …, куда
  уходит …, как живёт маппинг»);
  `.claude/decisions/source-api-target-rebase.md` («Почему … бьёт в
  сырьё, **и какими вариантами решены сопутствующие развилки**»);
  `.claude/processes/question-delegation.md` («кто решает развилки, что
  доходит до пользователя **и** как фиксируются решения»);
  `.claude/notes/2026-05-29-ростер-тулинга-роадмап.md` («Какой состав …,
  в каком порядке … **и** как устроен каждый»);
  `.claude/notes/2026-06-05-аудит-делегируемости-вопросов.md` («Кто …
  **— и** какие вопросы можно снять»);
  `.claude/notes/2026-07-06-аудит-контекстной-стоимости.md` («Что показал
  аудит … **и** какие предложения … следуют»);
  `docs/integrations/okx/contracts/max-size.md` («контракт операций …
  `max-size` **и** … `max-avail-size`»).
  Раздел-абзац вместо фразы: `.claude/chat/structure-digest.md`;
  `.claude/library/trading/distilled/microstructure.md`,
  `risk-and-sizing.md`, `strategy-patterns.md`, `system-design.md`
  (вопрос плюс легенда указателей, кросс-ссылки и `---`);
  `docs/lifecycles/AlgoOrder.md`, `Deal.md`, `DealTranche.md`,
  `Order.md`, `docs/rules/external-status-resolution.md`,
  `docs/rules/market-data-freshness.md` (вопрос плюс маршрутизация к
  спеке).
- **Целевой док.** По одной правке в каждом носителе, либо клауза о форме
  раздела в `.claude/rules/structure.md`.
- **Ожидаемый владелец.** `knowledge-curator`.
- **Трассировка.** Раздел «На какой вопрос отвечает этот файл» извлечён
  машинно из всех 318 файлов выборки (316 имеют раздел; два исключения —
  §Гигиена H1); отобраны формулировки с соединительными «и / — и / , что
  / ;», затем вручную отделены оси одного ответа («балансов и позиций»,
  «по окружениям и регионам») от склейки предикатов.
- **Варианты.**

  | Вариант | Что делает | Названная цена |
  |---|---|---|
  | Переформулировать 7 склеек до одного вопроса; у 4 lifecycles и 2 правил маршрутизацию к спеке вынести ниже раздела | 13 правок, правило исполнено буквально | у lifecycles маршрутизация («дом матрицы переходов — спека») стоит сразу под вопросом намеренно; вынос ниже снижает шанс, что читатель её увидит |
  | Чинить только 7 склеек предикатов, абзац-с-указателями признать допустимой формой | 7 правок | правило говорит «одной фразой»; допущение надо записать, иначе следующий свип найдёт те же 6 |
  | Записать в `structure.md`, что раздел допускает фразу-вопрос плюс строку маршрутизации, и чинить только склейки | 1 правка правила плюс 7 правок | правка нормы под фактическое состояние — риск подогнать правило под отклонение; но форма у 6 доков единообразна, то есть это уже конвенция без дома |

  **Крен — третий:** шесть доков используют одну форму последовательно;
  записать её дом дешевле тринадцати правок и снимает воспроизводство
  находки.
- **Ярлык исхода.** `варианты-с-креном`.
- **Ярлык дефицита.** дефицита нет — выводится из `structure.md` и
  `concept.md` §«Один вопрос на файл».

## Блокирующие открытые вопросы

Проход по `.claude/work/questions/open-questions.md` в части, относящейся
к охвату линзы (форма корпуса, размещение, целостность носителей):
**блокирующих нет**. Открытые вопросы файла продуктовые и торговые;
механику формы корпуса они не гейтят.

## Вынос пользователю

**Пусто.** Ни одна находка линзы G не несёт дефицит `подтверждение`: все
двенадцать выводятся из `docs/concept.md`, `.claude/rules/structure.md`,
`.claude/rules/curation.md` и `.claude/rules/policy-home.md` и не
затрагивают ни численных величин риск-аппетита, ни продуктового scope
(`question-delegation.md` §«Критерий один: выводится ли ответ из
концепции»).

## Гигиена

### Ссылочная целостность — счёт

| Величина | Число |
|---|---|
| Живых файлов знания в выборке | 318 |
| Извлечено backtick-ссылок вида `*.md` / `*.json` / `*.svg` | 2971 (718 уникальных) |
| Из них путевых (проверяемых на существование) | 2231 |
| Неразрешимо всего | 80 |
| — шаблоны-плейсхолдеры (`<X>`, `{name}`, `*`, `snapshot-vN`) | 55 (не дефект) |
| **Битых файловых ссылок** | **25** (перечень — G-9) |
| Из них в `docs/**` | **0** |
| §-упоминаний извлечено (426 строк-носителей) | 470 |
| — порядковые и короткие (`§4`, `§П1`, `§D`, `§AG1.5`, `§ниже`) — не адреса пассажей | 221 |
| Адресов на именованный пассаж проверено | 249 |
| Помечено автоматикой как неразрешимые | 111 |
| — ложные срабатывания резолвера (адрес разорван переносом строки; цель названа прозвищем без `.md`; пассаж в ячейке таблицы), сняты поштучно | 97 |
| **Битых §-адресов (подтверждены открытием цели)** | **14** (перечень — G-8) |
| Из них в `docs/**` | **0** |

Метод §-проверки: адрес разрешается в заголовок **или** в лид-жирный
пассаж целевого файла (`structure.md` §Принципы, подпункт о §-адресации);
битым считается только то, что не разрешается ни одной формой. Каждый из
14 подтверждён вручную — перечнем заголовков цели плюс грепом имени
пассажа. Отдельно от этих 14 стоит битый адрес-цель
`docs/integrations/{name}/coverage-manifest.md` (9 носителей) — он
проходит по G-6, а не по счёту выше, потому что имеет форму шаблона.

### Прочие позиции гигиены

| # | Позиция | Точка |
|---|---|---|
| H1 | `.claude/projects/**` — тип не зарегистрирован в таблице `structure.md`; при этом `MEMORY.md` проиндексирован в дереве, а раздела «На какой вопрос отвечает» не несёт (единственные два файла выборки без него) | `.claude/projects/C--…/memory/MEMORY.md`, `.claude/projects/C--…/memory/build-command.md`; `.claude/rules/structure.md` таблица |
| H2 | `.claude/work/2026-08-26-step-7-question-flow-analysis.md` лежит в корне `work/` — типа «датированный аналитический файл в `work/`» в таблице нет (в дереве узел есть: `knowledge-tree.md:150`); на файл ссылаются живые правила `policy-home.md` и `design-simplicity.md` | `.claude/work/2026-08-26-step-7-question-flow-analysis.md`; `.claude/rules/structure.md` таблица |
| H3 | Типы `docs/models/persistence/` и `docs/models/externalSnapshot/` зарегистрированы, каталогов нет; persistence-знание фактически живёт секциями `## Персистентность` внутри доменных доков — скаффолд, который может не понадобиться | `.claude/rules/structure.md:49`, `:53`; `docs/models/domain/**` §Персистентность |
| H4 | `knowledge-tree.md:162` — «снапшоты v1–v82» при фактическом v93 в `.claude/snapshots/` (узел `:134` актуален) | `.claude/knowledge-tree.md:162` |
| H5 | Каталог текущего прогона `.claude/work/progress/phase-1-step-7-docs-check-27/` в дереве не заведён (узел `progress/` — `knowledge-tree.md:151-157`); штатно для середины прогона, фиксируется как хвост курации при закрытии | `.claude/knowledge-tree.md:151-157` |
| H6 | Огрызок ссылки — висящая запятая перед закрывающей скобкой, два носителя | `docs/models/api/OkxRawApiRequest.md:22`, `docs/models/integrations/okx/TickerOkxResponse.md:23` |
| H7 | `.gitkeep` лежат в семи каталогах `docs/`, шесть из которых давно наполнены — остаток скаффолда | `docs/{components,components/models,dictionary,lifecycles,models,processes,rules}/.gitkeep` |
| H8 | Пересказ политики вне дома в охвате линзы **не найден**: проверены кластеры «невозрастающая база» (дом `docs/rules/risk-policy.md`, у `Exchange.md:21` — строка-указатель) и «серия убытков» (дом `docs/rules/loss-streak-halt.md`, у `Exchange.md:23`, `:62`, `risk-policy.md:284` — указатели). `policy-home.md` в этой части исполнен | `docs/models/domain/core/Exchange.md:21`, `:23`, `:62`; `docs/rules/risk-policy.md:284` |
| H9 | Процессная арматура в `docs/` — сводный счёт: 3 врезки решений (G-1), 1 заголовок с номером прогона (G-4), 5 ссылок в `work/**` и `tests/**` (G-3), 10 §-адресов (G-2). Ссылки `docs/ → .claude/{decisions,rules,processes,skills}` (26 штук) под находку не сведены: это указатели на устойчивые дома, а не на прогоны — но формулировка П4 их не разрешает, и это ближайший кандидат в следующий свип | `grep -rn '\.claude/' docs/` → 95 строк |

## Сводка

- **Блокирует выход в CODE:** 0.
- **Прочее:** 12 находок (G-1 … G-12).
- **Гигиена:** 9 позиций (H1 … H9) плюс таблица счётов ссылочной
  целостности.
- **Ярлыки исхода:** `выводимо` — 2 (G-7, G-8); `варианты-с-креном` — 10
  (G-1, G-2, G-3, G-4, G-5, G-6, G-9, G-10, G-11, G-12);
  `принято-в-работу` — 0; `неразрешимо-владельцем` — 0.
- **Ярлыки дефицита:** `подтверждение` — 0; `грунт` — 0; `работа` — 0.
  Все двенадцать закрываются владельцем автономно.
- **Владельцы:** `knowledge-curator` — 12 (первый владелец по всем);
  `integrator` — со-владелец на G-4 (часть по источнику), G-6, G-11.
- **Вердикт.** Со стороны линзы G шаг 7 к `CODE` **готов**: гейтящих
  находок нет. Двенадцать находок и девять позиций гигиены — работа
  `GAPS_CLOSE_27`; повторный `DOCS_CHECK_28` по охвату линзы G не нужен,
  достаточно свипа закрытия по списку цитат.
- **Концентрация дефекта.** Пять находок из двенадцати (G-1 … G-5) — один
  класс: **корпус `docs/` не доведён до формы, объявленной
  `docs/concept.md` Частью IV**. Врезки решений, §-адреса, ссылки в
  пайплайн, слой опровержения и обоснования выбора — не пять разных
  дефектов, а пять следов одного: переписывание корпуса по концепции
  прошло по содержанию и не прошло по арматуре. Закрывать их разумно
  одним ходом, и общность фиксируется здесь явно.

## Связи

- Процедура линзы — `.claude/skills/concept-review.md`.
- Формат отчёта и набор полей — `.claude/templates/docs/gap-report.md`.
- Арбитр конфликта дока и концепции — `docs/concept.md`, Часть V.
- Правила размещения и курации — `.claude/rules/structure.md`,
  `.claude/rules/curation.md`, `.claude/rules/policy-home.md`.
