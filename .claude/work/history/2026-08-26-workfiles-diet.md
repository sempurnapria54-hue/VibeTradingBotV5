# Разовая чистка рабочих файлов 2026-08-26

## На какой вопрос отвечает этот файл

Что унесла разовая чистка рабочих файлов 2026-08-26.

Основание — решение держателя («в живом файле — только открытое;
решённое превращается в решение и уходит в историю; рабочий файл —
не носитель политики»): `.claude/rules/closed-work-transfer.md`
§«Диета рабочих файлов», `.claude/rules/policy-home.md`. Тем же днём
введена лестница биржевых safety-состояний
(`docs/rules/exchange-hold.md`,
`docs/decisions/exchange-safety-ladder.md`) — часть правок приводила
упоминания холдов к ней.

## backlog.md (130 → 92 KB)

### Удалено как закрытое

- **«Разнобой имён safety-статуса биржи — сводится здесь»** (п.9
  Cross-cutting): закрыт лестницей `Exchange.HOLD` ↔
  `Exchange.TRADE_BLOCKED` — дом `docs/rules/exchange-hold.md`,
  решение `docs/decisions/exchange-safety-ladder.md`. Вместе с ним
  снята клауза «шаг 7 писателя `Exchange.HOLD` НЕ вводит» —
  отменена лестницей: `Exchange.HOLD` вводится CODE-дельтой шага 7
  (backlog §«Лестница биржевых safety-состояний — CODE-дельта»).
- **Выноска «Множество входа `Exchange.TRADE_BLOCKED` — не
  пересмотрено»** (п.9): закрыта лестницей — множества входа
  биржевых ступеней записаны (`docs/rules/exchange-hold.md`
  §«Границы и эскалация»).
- **Хроника закрытия `ANOM-Q5` и снятия `ANOM-Q3`/`ANOM-Q4`**
  (§Шаг 8): итог — идемпотентность отчёта держится незавершённым
  статусом (`docs/models/domain/other/AnomalyReport.md`
  §Персистентность); живой остаток (влияние радиуса на поиск при
  эскалации scope; `ANOM-Q6` — фаза 3) в backlog сохранён.
- **Хроника «пункт "снять вырожденный `CLOSE_FULL`" снят»**
  (§Шаг 8): итог — оба способа объявить выход законны
  (`docs/rules/no-partial-close.md`).
- **Грунт `integrator`, позиция 8 «Дом конфига списка исключений
  сверки»**: структурная половина закрыта решением пользователя —
  носитель `@ConfigurationProperties` per-exchange, предусловие
  `CODE` п. 12 (`docs/decisions/pnl-finalization-mechanics.md`
  §«Предусловия `CODE`», `docs/models/mapping/DealCashFlow.md`).
  Содержание перечня осталось позицией 5 грунта.
- **Грунт `integrator`, позиция 9 «Ветка AG1.5 — штампуется ли
  entry-fee раньше `cTime` позиции»**: снята (решение пользователя)
  — писатель нижней границы окна один, `Order.externalCreatedAt` не
  позже комиссии исполнения той же ноги ⇒ окно накрывает entry-fee
  по построению (backlog §Шаг 7, пункт «`billsWindowBegin` —
  единственный писатель»). Закрыта, не отложена.
- **«Форма epsilon — закрыта, живёт только калибровка»**: закрытая
  часть убрана, живой остаток слит в пункт «Калибровка epsilon»
  (форма — `docs/rules/pnl-reconciliation.md`,
  `docs/decisions/pnl-finalization-mechanics.md` реш.5 §epsilon;
  итог закрытия — `2026-08-20-curation-sweep-snapshot-v82.md` §2).
- **Снятые (зачёркнутые) пункты CODE-дельты** удалены вместе с
  хроникой снятия: `HoldScope.INSTRUMENT_GROUP` + `fee_group_key`
  (радиус отчёта о несвежести — инструмент,
  `docs/models/domain/other/AnomalyReport.md`); раздельные счётчики
  серии неудач вход-/управление-сайд (ось стороны ноги снята,
  `docs/rules/instrument-hold.md` §«Серия неудач»); target-колонки
  SYSTEM-строк (`docs/models/domain/other/DealActionState.md`
  §Инварианты).
- **Абзац «Регистрация задним числом (свип курации при снапшоте
  v79)»** (§Агентское ревью): хроника регистрации; сами блокеры
  B1/B2 в backlog сохранены.
- **H17 «недоступность обычно временна»** перестал быть отдельным
  пунктом — слит смежной оговоркой в пункт H6 (добор числа на
  `EMERGENCY_CLOSED`).

### Крупно сжато (секция → задачи с указателями на дом)

- **§Шаг 7 (~75 → ~47 KB)**: все CODE-пункты (стадии 1-2, узел
  добычи положения закрытия, R-слот и формулы риска, граница
  «команда ↔ действие», журнальные аномалии, cross-ccy, fee-wiring,
  узел холда, дельта GAPS_CLOSE_10) очищены от провенанс-маркеров
  (`H<N>`/`GAPS_CLOSE_<M>`) и пересказов политики — вместо них
  указатели на дома (`Deal.md`, `Order.md`, `AnomalyReport.md`,
  `DealActionState.md`, `pnl-reconciliation.md`,
  `instrument-hold.md`, `exchange-hold.md`,
  `pnl-finalization-mechanics.md`,
  `persistence-representation.md`). Секция разбита на подсекции:
  «Лестница биржевых safety-состояний — CODE-дельта» (новая
  дельта), «CODE-дельта шага», «Грунт `integrator` для шага 7»,
  «Рантайм-верификация и форвард». Матрица «Искажение измеряемой
  ожидаемости» сжата до клеток с указателями; упоминания холдов
  приведены к лестнице (flatten чужих сделок — только ступень 2
  `Exchange.TRADE_BLOCKED`).
- **§Форвард-материал шагов 7/8 и фазы 3 (~17 → ~14 KB)**: пункты
  шага 8 (переоценка инварианта, `TradeGuardJob`, инвентарь джоб),
  ось упущенных возможностей, перф-форвард — без провенанса и
  пересказов; L4 `fireExchange` привязан к ступени 2 лестницы.
- **§Cross-cutting миграции (~11 → ~7 KB)**: п.9 (Exchange) сведён
  к задаче + указателям; выноска про `AnomalyReport.Severity` — до
  строки (`docs/models/domain/other/AnomalyReport.md` §Енумы).
- **§Агентское ревью**: связка B1→B2 приведена к лестнице
  (требуемая реакция на живой риск без защиты —
  `Exchange.TRADE_BLOCKED`, ступень 2).
- Прочее той же правкой: M2 — без провенанса; добавлены задача M3
  (сведение кластеров дублирования политики к домам,
  `.claude/rules/policy-home.md`) и п.5 гейта закрытия фазы 1
  (пересмотр режима автономии —
  `2026-08-26-autonomy-transition.md`).

## open-questions.md (62 → 27 KB)

Полностью закрытых вопросов не найдено — все 16 живы, удалений нет.
Все 16 сжаты до формы «постановка + статус/блокер +
владелец/горизонт + указатели»: разборы вариантов сведены к
одно-строчным, хроника заходов по итерациям и провенанс-маркеры
прогонов убраны (носители — decisions и архив прогонов в
`history/`), цитаты источников (OKX-Q2, OKX-Q4) убраны — живут в
контракт-доках и архиве.

Закрытые под-части, сведённые к строке-указателю:

- **CMD-Q4** — precheck-часть закрыта (инструмент-скоупный
  exchange-read вне command-layer,
  `docs/components/IntegrationService.md`); жив orphan-скан шага 8.
- **OKX-Q2** — мотив «архив как продолжение runtime-контура fills»
  отпал (runtime фазы 1 fills не читает); жив вопрос
  off-band-аудита.
- **IND-Q1** — книжная часть закрыта
  (`docs/decisions/volume-condition-semantics.md`); жива
  крипто-часть, якорь — фаза 4. В заголовке пометка «(владелец —
  шаг 3)» заменена на «(якорь — фаза 4)»: шаг 3 давно закрыт,
  пометка противоречила телу; ярлык `IND-Q1` сохранён.
- **PNL-Q1** — половина п.1 закрыта (`closeOutcome` доносит
  провенанс ликвидации/ADL, `Deal.md` §«Признаки отбора для
  отчёта»); живы остаток `triggerPx` и пп.2-3.
- **RISK-Q3** — знаменатель R, scope (сетка/добор включены) и
  измеритель взятого риска (`Deal.incurredRiskAmount`) закрыты —
  перечислены строкой «закрыто и не пере-решается»; живо правило
  агрегации лимита + три непройденные оси.
- **ANOM-Q6** — посылка «популяция не названа» снята (предикат
  записан в `AnomalyReport.md` §Персистентность); жива только форма
  частичного уникального индекса (фаза 3).

Упоминания холдов сверены с лестницей: правок не потребовалось —
вопросы держат инструмент-scope статусы (`ENTRY_BLOCKED` /
`Instrument.TRADE_BLOCKED`), биржевой лестницы не касаются.

## Прежние редакции

Полные прежние редакции обоих файлов — git-история
`.claude/work/backlog.md` и
`.claude/work/questions/open-questions.md` (состояние до
2026-08-26).
