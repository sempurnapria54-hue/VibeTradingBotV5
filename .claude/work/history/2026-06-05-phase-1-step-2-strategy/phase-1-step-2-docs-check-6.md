# DOCS_CHECK_6 — шаг 2 Фазы 1 (Стратегия)

## На какой вопрос отвечает этот файл

На каком шаге мы в проверке целостности концепции доков под код
шага 2 (шестая итерация) и какие пробелы найдены.

## Контекст

- Шаг роадмапа: Фаза 1, шаг 2 — «Стратегия (абстракция: объявляет
  нужные индикаторы и условие сигнала; одна реализация)».
- Под-шаг: `DOCS_CHECK_6` (шестая итерация), после `GAPS_CLOSE_5`.
- Тулинг: роль `reviewer`, фокус `concept-review`. Граница охвата —
  **только доки**, код не читается.
- **Scope** (зафиксирован на `GAPS_CLOSE_1`, Э1): шаг 2 строит,
  **персистит** и читает полное монолитное immutable-дерево `Strategy`
  + материализует одну заполненную стратегию через Strategy API.
- **Фокус итерации:** `GAPS_CLOSE_5` закрыл STRAT-Q5 (условие —
  целиком JSONB-поле `condition` на строке `strategy_step`, по дефолту
  правила `docs/rules/persistence-representation.md`) и зачистил
  гигиену М1 (устаревшее «уже принятое» в authoring-contract) и М2
  (мёртвые ссылки на архивированный tasks-файл); М3 ушёл вместе с
  текстом закрытого вопроса. Шестая проверка верифицирует
  согласованность применения и ищет новые doc↔doc хвосты. После
  закрытия STRAT-Q5 известных блокеров не оставалось — итерация
  решает, готов ли шаг к `CODE`.

## Охват

### Проверено (в охвате)

- **Ядро:** `docs/models/domain/aggregate/Strategy.md` целиком, акцент
  на §Персистентность (интро, новый §Условие, §Внутридеревные ссылки,
  §Не зафиксировано), §Условия (контракт), §StrategyIndicatorSetting,
  §IndicatorParams.
- **Decision:** `docs/decisions/strategy-tree-persistence.md` — шапка,
  новый §Условие (`GAPS_CLOSE_5`, STRAT-Q5), §Внутридеревные ссылки,
  §Следствия (новый пункт `GAPS_CLOSE_5`), §Связи.
- **Правило:** `docs/rules/persistence-representation.md` — на
  согласованность формулировки «условие — дефолт правила» с самим
  правилом.
- **М1:** `docs/decisions/strategy-condition-authoring-contract.md` —
  исправленная отвергнутая альтернатива «`type`-строкой».
- **М2:** `docs/models/domain/aggregate/Strategy.md` (2 ссылки),
  `docs/lifecycles/Strategy.md` (1 ссылка) — перенацеленные пути;
  существование целевого файла
  `.claude/work/history/2026-05-27-миграция-торговых-сущностей/tasks-strategy.md`
  подтверждено.
- **Cross-ref-свипы по всем `docs/`:** остаточные упоминания таблиц
  `strategy_condition`/`strategy_condition_rule` (все — в корректном
  контексте «таблиц нет / отвергнутая альтернатива / ревизионная
  запись); все утверждения о реляционности/JSONB
  (правило, оба decision, модель — противоречий нет); метки STRAT-Q5
  вне progress-файлов (только история закрытия в decision, запись в
  шапке `open-questions.md` и неизменяемый `snapshot-v26.md` — все
  легитимны); `strategy-materialization-and-validation.md` — про
  персистентность условия не утверждает (только ссылка на
  authoring-contract).
- **Open-questions:** проход по всем 15 на гейтинг/блокировку шага 2.

### Вне охвата (downstream — ссылки name-level, не пробел)

Без изменений к `DOCS_CHECK_5`: команды/executors (шаг 4), риск
(шаг 5), FSM (шаг 6), runtime сделок/P&L (шаг 7), расчёт
индикаторов/структуры (шаги 3/Фаза 4), `DealActionState`/
`RuntimeTarget`, evaluator (`StrategyConditionEvaluator` —
десериализация условия из JSONB в объектную модель помечена в доках,
сама механика downstream). OKX-mapping вне (Strategy не биржевая
сущность).

## Стадия остановки

**Прошёл все стадии (до стадии 2).** Стадия 0 (гейт scope) — чисто
(scope снят Э1 на `GAPS_CLOSE_1`, гейтящих вопросов нет). Стадия 1
(процессы) — чисто: отдельного процесса у шага 2 нет, материализация
через Strategy API лежит поверх lifecycle
(`docs/lifecycles/Strategy.md`), lifecycle согласован с
`strategy-materialization-and-validation.md`. Стадия 2 (компоненты +
модели) — обход доведён до конца.

## Верификация `GAPS_CLOSE_5` — применено согласованно

- **STRAT-Q5 (условие → JSONB).** Схема идентична в обоих доках:
  JSONB-поле `condition` на строке `strategy_step`, массив `rules`
  (`level`/`ruleType`/`operator` + простые поля), операнды — JSONB
  внутри того же объекта; таблиц `strategy_condition`/
  `strategy_condition_rule` нет; перечень каркасных узлов не пополнен.
  `Strategy.md`: интро §Персистентности дополнено, §Условие добавлен,
  §Внутридеревные ссылки указывает на condition-JSONB шага (фраза
  «строка `strategy_condition_rule`» снята), метка STRAT-Q5 убрана из
  §Не зафиксировано. `strategy-tree-persistence.md`: шапка («настройки
  и условие — JSONB у владельца»), §Условие с обоснованием
  (дефолт правила: FK внутрь условия нет, операнды и так JSONB,
  immutable-агрегат грузится целиком) и отвергнутой альтернативой
  (реляционная пара таблиц = исключение без storage-специфичной
  причины; `strategy_condition` — 1:1 без полезной нагрузки),
  пункт в §Следствиях, STRAT-Q5 убран из §Связей. Обоснование
  согласовано с `persistence-representation.md` дословно по критериям.
  В `open-questions.md` вопрос удалён, запись о закрытии в шапке
  (2026-06-03, ссылка на §Условие). Перекрёстные ссылки
  модель↔decision↔правило взаимны и ведут в существующие разделы.
- **М1** — отвергнутая альтернатива «`type`-строкой» в
  `strategy-condition-authoring-contract.md` теперь ссылается на
  текущую правду («настройки — JSONB-объекты `{ key, type, params }`
  внутри контейнера; ревизия `GAPS_CLOSE_3`») — противоречие с
  актуальной схемой снято.
- **М2** — все три ссылки перенацелены на
  `history/2026-05-27-миграция-торговых-сущностей/tasks-strategy.md`;
  файл существует. Мёртвых ссылок на
  `.claude/work/questions/tasks/strategy.md` в доках не осталось.
- **М3** — фантомная формулировка «обоих доков» ушла вместе с текстом
  STRAT-Q5 при удалении вопроса; отдельная правка не требовалась.

Новых doc↔doc хвостов правки `GAPS_CLOSE_5` не оставили.

## Пробелы по типам

### 1. Несогласованности между доками

Не найдено.

### 2. Name-level без структуры (где структура нужна шагу 2)

Не найдено. Схема персистентности задана для всего дерева, включая
условие. Оставшаяся под-определённость (типы/nullability числовых
полей; per-`ruleType` контракт полей; нейминг поля-ссылки операнда) —
осознанные отложенные пороги, зафиксированные в самих доках
(§Не зафиксировано; «инкрементальная деталь»), не пробелы.

### 3. Неотвеченные вопросы (open-questions)

Проход по `open-questions.md` (15 открытых):

- **Блокирующих шаг 2 — нет.** STRAT-Q5 закрыт на `GAPS_CLOSE_5`.
- **STRAT-Q4** (percent-anchor) — непгейтящий бизнес-инкремент:
  интерпретация якоря — деталь реализации соответствующего
  `ruleType`/placement, entity/миграцию дерева не блокирует.
- DEAL-Q1/Q2/Q3, PROC-Q1, RISK-Q1, INSTR-Q1/Q2, ORCH-Q1, ENUM-Q1,
  CMD-Q1, OKX-Q1..Q4 — downstream (шаги 4-7, Фазы 3/4), шаг 2 не
  блокируют, без изменений.

## Наблюдения (не пробелы)

- **Архивный артефакт со старым путём.** В
  `history/2026-05-27-миграция-торговых-сущностей/progress-strategy.md`
  (стр. 23) остаётся ссылка на старый путь
  `questions/tasks/strategy.md` (файл лежит рядом как
  `tasks-strategy.md`). Это архивный исторический артефакт, не док
  знания шага; история не правится — фиксируется наблюдением, в
  пробелы не идёт. М2 покрывал именно доки — там зачищено.
- **Downstream-хвост `*_setting_id`** (без изменений с
  `DOCS_CHECK_4/5`): доки результатов расчёта (`IndicatorValue.md`,
  `MarketStructure.md`, `IndicatorJob.md`, `MarketStructureJob.md`)
  несут старый `UNIQUE(…, *_setting_id, …)`; переписывается на шагах
  Фаз 3/4 по `market-data-result-identity-keying.md` (§Границы).
  Отслеживаемый хвост, шаг 2 не гейтит.

## Блокирующие открытые вопросы

Нет. Единственный блокер прошлых итераций (STRAT-Q5) закрыт.

## Эскалации

Нет.

## Сводка

- **Несогласованности (doc↔doc):** 0.
- **Name-level без структуры:** 0.
- **Блокирующие открытые вопросы:** 0 (из 15 открытых ни один шаг 2
  не гейтит и не блокирует).
- **Верификация `GAPS_CLOSE_5`:** STRAT-Q5 + М1/М2 применены
  согласованно, М3 снят попутно; новых хвостов нет.
- **Эскалаций:** 0.
- **Итог: чисто.** Шестая проверка новых пробелов не выявила —
  итерации `DOCS_CHECK`/`GAPS_CLOSE` шага 2 завершены. **Шаг 2 готов
  к `CODE`** (entity/Flyway-миграция всего дерева включая условие,
  Strategy API `POST`/`GET`/`PUT`, create-валидация пп. 1-3, 8;
  типы/nullability числовых полей проставляются при написании
  entity/миграции — по §Не зафиксировано).
