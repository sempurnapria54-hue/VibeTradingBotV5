# GAPS_CLOSE_7 — шаг 2 Фазы 1 (Стратегия)

## На какой вопрос отвечает этот файл

Что закрыто в `GAPS_CLOSE_7` шага 2 Фазы 1 (два хвоста
`DOCS_CHECK_7`: апдейт формулировки INSTR-Q2, нейминг runtime-полей
`Position`) и каков результат.

## Контекст

- Шаг роадмапа: Фаза 1, шаг 2 — «Стратегия».
- Под-шаг: `GAPS_CLOSE_7` (`.claude/processes/roadmap-step-execution.md`).
- Вход: gap-отчёт `phase-1-step-2-docs-check-7.md` — верификация
  правки «плечо динамическое» прошла чисто (новых doc↔doc хвостов 0,
  остаточных ссылок на снятый потолок в `docs/` 0, шаг 2 остаётся
  готов к `CODE`); осталось два негейтящих хвоста + решение чата:
  закрыть оба сейчас.

## Что сделано

### 1. INSTR-Q2 — освежена формулировка (вопрос не решён)

Апдейт открытого вопроса в `open-questions.md` по адъюдикации
`DOCS_CHECK_7`:

- **Снята устаревшая премисса** про конфиговый максимум плеча и
  аспект «как соотносится с конфиговым максимумом» — наш потолок
  снят решением «плечо динамическое» (2026-06-04,
  `docs/rules/trading-constraints.md`).
- **Добавлен непокрытый аспект** (напряжение статика↔динамика):
  нужна ли вообще статическому полю `Instrument.leverage` роль
  рабочего плеча, если рабочее плечо выводится динамически на
  каждую сделку; кто и когда выставляет плечо на бирже
  (set-leverage: при онбординге, перед сделкой, на каждую сделку).
- Сохранены актуальные аспекты: роль `externalLeverage` как
  биржевого потолка и соотнесение с rules-полем
  `externalMaxLeverage` (в т.ч. дубль/удаление); `HOLD` в lifecycle
  (нарушение правила плеча теперь — только превышение биржевого
  максимума).
- Заголовок вопроса обновлён: «Роль статического плеча `Instrument`
  при динамическом рабочем плече»; дескриптор в шапке файла — тоже.
- Содержательно вопрос **не решается** на шаге 2 — проработка
  уходит в риск-движок (шаг 5) / торговый совет. Доки Instrument до
  решения не правятся (поле остаётся описанным как рабочее плечо).

### 2. Нейминг runtime-полей `Position` — выровнен

`docs/models/mapping/Position.md` (§`PositionExternalSnapshot` →
`Position`): пять domain-полей приведены к конвенции
`external*`-префикса из `docs/models/domain/core/Position.md` —
`externalAverageEntryPrice`, `externalMarkPrice`,
`externalLiquidationPrice`, `externalMargin`,
`externalUnrealizedProfit` (рядом стоящий `externalSize` уже был с
префиксом). Правка существующего файла под существующую конвенцию,
не новое знание. Других вхождений беспрефиксных имён в `docs/` нет
(свип `Position.averageEntryPrice|markPrice|liquidationPrice|margin|unrealizedProfit`
— чисто).

## Проход по открытым вопросам (15 → 15)

- **Закрыто: 0.** **Добавлено: 0.**
- **Освежён: 1** — INSTR-Q2 (формулировка; вопрос остаётся
  открытым).

## Статус

- Статус шага 2: → `GAPS_CLOSE_7` (`phase-1.md`); ролляп Фазы 1 —
  `IN_PROGRESS` без изменений. Зафиксирован рассинхрон: промежуточные
  статусы `GAPS_CLOSE_6` (правка плеча по решению чата 2026-06-04) и
  `DOCS_CHECK_7` в таблицу не проставлялись (стояло `DOCS_CHECK_6`);
  текущим обновлением таблица приведена к фактическому состоянию.
- **Шаг 2 готов к `CODE`** (вердикт `DOCS_CHECK_7` подтверждён,
  хвосты закрыты). Перед `CODE` по плану — **торговый совет** в
  отдельном чате: сначала спроектировать сам совет (чат vs код,
  источники — книги + интернет, роль/триггер), затем он валидирует
  торговую модель и задаёт вопросы, которые закрываем; только потом
  `CODE`.
- Открытые вопросы (`open-questions.md`): **15** (без изменений).

## Затронутые файлы (staged)

- `.claude/work/questions/open-questions.md`
- `docs/models/mapping/Position.md`
- `.claude/work/roadmap/phase-1.md`
- `.claude/snapshots/snapshot-v27.md`
- `.claude/work/progress/phase-1-step-2-gaps-close-7.md` (этот файл)
