# `DOCS_CHECK_2` — шаг 9 фазы 1 (безопасность)

## На какой вопрос отвечает этот файл

Какие пробелы нашёл второй прогон шага 9.

## Сводка счётом

| Линза | Предмет | Позиций реестра |
|---|---|---|
| A | concept, шаг 9 (следствия `GAPS_CLOSE_1`) | 11 |

**Находок 15:** гейтящих 5 (A1-A4, A6), прочих 7 (A5, A7-A12), гигиены 3
(A13-A15). Позиций реестра — 11: у A6 и A13-A15 предмет — рабочие файлы
пайплайна, продуктового кода они не гейтят.

**Ярлык `подтверждение` — один** (A7, невыводимая половина: сколько
субъектов обязана обслуживать система), с записью самопроверки.

## Доля следствий предыдущего закрытия

**5 из 5 гейтящих** — дефекты текста и конструкций `GAPS_CLOSE_1`.
Закрытие завело **новый дом** политики, и текст этого дома проверялся
впервые.

## Находки

| # | Класс | Суть |
|---|---|---|
| A1 | гейтящая | новый триггер мягкой ступени объявлен вне дома лестницы, чья таблица объявляет себя полным перечнем |
| A2 | гейтящая | ступень 2 у отказа кредов предписывает реакцию, **неисполнимую на её же триггере**; три клейма концепции становятся ложными |
| A3 | гейтящая | п.5 противоречит классификации runtime-ошибок и не называет наблюдателя |
| A4 | гейтящая | область значений актора не резолвится на ручном триггере джобы — критерий даёт ту самую «ложную запись», которую запрещает |
| A6 | гейтящая | реестр гейтов не пересобран под прогон шага 9 |
| A5 | прочая | порог серии отказов отнесён к калибровке, но не назначен |
| A7 | прочая | «принципал один — выводится» — цитируемый пассаж вывода не даёт |
| A8 | прочая | дискриминатор «намерение человека» опровергается ручным полным холдом |
| A9 | прочая | второй исход отказа доступа недостижим по построению самого правила |
| A10 | прочая | перечень точек не покрывает поверхность, которую правило объявляет закрытой |
| A11 | прочая | политика ротации заведена в доме биржевой лестницы |
| A12 | прочая | «единственность вызывающего» названа энфорсером без механизма |
| A13-A15 | гигиена | двойной вердикт `HOLD-Q2`; отсутствие штатных следов закрытия; рост долга адресных перекрёстий |

Разбор и исход каждой — `phase-1-step-9-gaps-close-1.md`.

## Детектор стагнации

| Сигнал | Требуемое окно | Набрано | Замер |
|---|---|---|---|
| С1 — рецидив узла | 2 прогона с закрытием между ними | нет | узлы `_2` — дефекты **текста**, которым закрывались узлы `_1`, а не рецидивы самих узлов |
| С2 — бумеранг закрытия | 2 прогона подряд с долей ≥ ½ | **нет — половина окна** | у `_2` доля **5/5 гейтящих**; у `_1` предыдущего закрытия не было. Сигнал сработает, если `_3` даст ту же долю |
| С3 — стоимость не убывает | 6 прогонов | нет | прогонов два |
| С4 — дефект полноты перебора | 2 закрытия подряд | нет | переборы полны |

**Ни один сигнал не сработал.** Половина окна С2 записана заранее — так,
чтобы следующий прогон не пришлось восстанавливать по памяти.
