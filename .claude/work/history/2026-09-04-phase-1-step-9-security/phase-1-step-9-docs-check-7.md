# `DOCS_CHECK_7` / `_8` — добивка пост-хок гейта шага 9

## На какой вопрос отвечает этот файл

Чем закончились два последних концепт-прогона шага 9.

## `DOCS_CHECK_7`

**Охват** — тот же, что у `_6`, плюс собственный текст `GAPS_CLOSE_6`.

| Линза | Предмет | Позиций реестра |
|---|---|---|
| A | concept, доки шага 9 после `GAPS_CLOSE_6` | 1 |

**Находок 1:** гейтящих 0, прочих 0, гигиены 1. **Прогон не чист.**

**H1. Продуктовый док несёт процессную арматуру — ссылку на шаг роадмапа.**

- *Цитаты:* `docs/rules/api-access-policy.md` §«Инвариант «снятие ступени —
  только вручную»» — «Это и есть предмет шага 9».
- *Целевой док:* тот же. *Владелец:* `knowledge-curator`.
- *Трассировка:* П4 прямо запрещает продуктовому корпусу процессную и
  ссылочную арматуру. Формулировка адресовала **момент** (шаг роадмапа), а
  не носитель, и после закрытия шага читалась бы как отсылка в никуда — тот
  же класс, что был вычищен из javadoc синхронизацией.
- *Варианты:* развилки нет — арматура снимается, клейм остаётся.
- *Ярлык исхода:* `выводимо`. *Дефицита нет.*

### Детектор стагнации `_7`

| Сигнал | Набрано | Замер |
|---|---|---|
| С1 | нет | гейтящих на `_7` нет вовсе — рецидивировать нечему |
| С2 | **нет — вне области определения** | гейтящих **0** при минимальном знаменателе четыре |
| С3 | окно набрано, **не сработал** | `_2`…`_7`: 5, 7, 4 → 2, 2, 0. Среднее первой тройки **5.33**, второй — **1.33**: падение на **75 %** |
| С4 | нет | перебор полон |

### `GAPS_CLOSE_7`

Находок **1**, закрыто **1**, припарковано **0**, вынесено держателю **0**.
Ссылка на шаг роадмапа снята; клейм об энфорсере остался нетронутым.
Политику закрытие не меняло.

## `DOCS_CHECK_8` — чисто

**Охват** — весь периметр и его соседи после `GAPS_CLOSE_7`.

**Находок 0.** Проверено механически и чтением:

| Ось проверки | Команда либо предмет | Итог |
|---|---|---|
| процессная арматура в продуктовом корпусе | `grep -rn "шага 9\|шаге 9\|шагом 9" docs/` | пусто |
| устаревшие обещания в коде | `grep -rn "шаг 9" src/main/java` | пусто |
| счётные клеймы правленых доков | «Обработчиков три», «Все три ловит», «Все три пишут» — против кода | сходятся |
| порядок обработчиков | таблица дока против порядка `catch` в коде | совпадает |
| §-адреса `.claude/**` | `py -3 tools/anchor-check.py` | новых неразрешимых 0 |
| снятые редакции | `py -3 tools/retired-check.py` | дефектов 0 |

**Пост-хок концепт-гейт пройден:** концепт-проверка пост-sync доков прошла
чисто, и шаг вправе уйти в `DONE`
(`.claude/processes/roadmap-step-execution.md` §6a).

### Детектор стагнации `_8`

Прогон **чист** — петля закрыта сверху, а не снизу. Ряд гейтящих находок
шага целиком: **5, 5, 7, 4, 2, 2, 0, 0**.
