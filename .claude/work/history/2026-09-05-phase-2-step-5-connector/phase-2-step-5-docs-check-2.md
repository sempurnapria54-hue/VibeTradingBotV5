# `DOCS_CHECK_2` шага 5 фазы 2 — коннектор OKX

## На какой вопрос отвечает этот файл

Какие пробелы концепции остались после `GAPS_CLOSE_1` шага 5 фазы 2.

## Охват

Те же носители плюс правленные закрытием. Прогон целил в **потребителей
парковки**: кто ещё в корпусе рассчитывает на WS.

**Линза одна** — `concept-review` (W). **Верификации:** V1-V2.

## Слой верификаций

- **V1.** Греп `WS-слушатель|WS-факт` по живым носителям: шесть
  вхождений. Четыре согласованы (правленые `services.md` и `phase-2.md`,
  дом парковки, решение дизайн-прохода — оно **само** говорит «WS-факты
  — ускоритель, а не гарантия», то есть закрытие ему следует). Два
  остались безусловными: `phase-3.md` строка 2 и снапшот v126.
- **V2.** `contracts.md` §События: производитель `ExecutionObserved` —
  коннектор; закрытие его не меняет, меняется только способ наблюдения.
  Согласовано.

## Находки — прочее (гейтящих нет)

### W1. Фаза 3 наследует тот же дефицит, не зная о нём

- **Вопрос.** Строка коннектора Bybit объявляет WS-факты своим предметом.
  Контракта WS-каналов нет ни у OKX, ни у Bybit; шаг фазы 3 упрётся в тот
  же грунт и переоткроет разбор заново.
- **Цитаты.** `.claude/work/roadmap/phase-3.md`, строка 2;
  `.claude/work/backlog.md` §«WS-слушатель фактов исполнения…».
- **Владелец.** `solution-designer`. **Ярлык исхода.** `выводимо`.
  **Дефицита нет.**

**Закрытие.** Строка правлена: наблюдение фактов исполнения, а WS у
**обеих** площадок ждёт добытого контракта каналов, с указателем на дом
парковки.

### W2. Снапшот описывал шаг 5 с WS

- **Цитаты.** `.claude/snapshots/snapshot-v126.md` §«Что дальше».
- **Владелец.** `knowledge-curator`. **Ярлык исхода.** `выводимо`.
  **Дефицита нет.**

**Закрытие.** Абзац правлен: наблюдение опросом, WS припаркован, назван
дом парковки и причина.

## Блокирующие открытые вопросы

Нет.

## Вынос держателю

**Вопросов держателю нет.** Записано явно.

## Детектор стагнации

| Сигнал | Замер | Сработал |
|---|---|---|
| С1 — узел возвращается после собственного закрытия | узел WS вернулся **потребителями парковки**, а не решением; парковка не оспаривалась | нет |
| С2 — закрытие даёт находок столько же, сколько закрывает | `GAPS_CLOSE_1` закрыл 2 и припарковал 1, `DOCS_CHECK_2` дал 2 негейтящих | нет |
| С3 — стоимость прогонов не убывает | 8 → 2 KB; находок 3 → 2, гейтящих 2 → 0 | нет |
| С4 — перебор второй раз подряд неполон | развилок нет | нет |

**Метод — замер, а не перечитывание** (решение Д630): потребители
парковки найдены грепом по терминам, а не чтением. Это то же лекарство,
что сработало на шаге 4.

## Сводка счётом

| Линза | Предмет | Позиций |
|---|---|---|
| W | потребители парковки WS | 2 |

Гейтящих — 0, прочих — 2, обе закрыты здесь же.

**Вывод:** гейтящих находок нет. Следующий — `DOCS_CHECK_3`
подтверждающим проходом, затем `CODE`.
