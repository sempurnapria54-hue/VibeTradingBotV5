# `DOCS_CHECK_5` шага 4 фазы 2 — авторизация (чистый прогон)

## На какой вопрос отвечает этот файл

Остались ли пробелы концепции после `GAPS_CLOSE_4` шага 4 фазы 2.

## Охват

Те же носители. Прогон проверял, сработала ли смена метода, принятая
эскалацией `DOCS_CHECK_4`.

**Линза одна** — `concept-review` (T). **Верификации:** V1-V3.

## Слой верификаций

- **V1 — замер полноты пометок.** Механический разрез
  `docs/models/domain/core/Exchange.md` по заголовкам: **8 разделов,
  непомечен 1** — вводный «На какой вопрос отвечает этот файл», которому
  пометка не нужна по построению. Прежде замер давал 3 непомеченных.
- **V2 — согласованность двух форм.** `Exchange.Availability` (целевая
  площадка) и `ExchangeAccount.Status` (счёт) не пересекаются
  значениями: `AVAILABLE`/`UNAVAILABLE` против
  `ACTIVE`/`HOLD`/`TRADE_BLOCKED`/`CLOSED`. Радиусы разведены явно в обоих
  доках.
- **V3 — граница шагов.** `phase-2.md` строка 4 и `phase-5.md` строка 1
  после закрытий читаются согласованно: поле роли — шаг 4, различение —
  фаза 5; дом (`tenant-and-exchange.md`) говорит то же.

## Находки

**Гейтящих — нет. Прочих — нет.**

## Блокирующие открытые вопросы

Нет.

## Вынос держателю

**Вопросов держателю нет** ни в одном прогоне шага. Записано явно.

## Детектор стагнации

| Сигнал | Замер | Сработал |
|---|---|---|
| С1 — узел возвращается после собственного закрытия | возвратный узел разделения дока дал **ноль** находок | нет |
| С2 — закрытие даёт находок столько же, сколько закрывает | `GAPS_CLOSE_4` закрыл 1, `DOCS_CHECK_5` дал 0 | нет |
| С3 — стоимость прогонов не убывает | 11 → 3 → 2 → 2 → 1 KB; находок 5 → 2 → 1 → 1 → **0** | нет |
| С4 — перебор второй раз подряд неполон | находок нет | нет |

**Эскалация `DOCS_CHECK_4` закрыта исходом.** Смена метода —
перечитывание на замер — сработала: узел, вернувшийся четырежды, дал
ноль. Диагноз подтверждён прогоном, а не рассуждением.

## Сводка счётом

| Линза | Предмет | Позиций |
|---|---|---|
| T | концепция авторизации, подтверждающий проход | 0 |

**Итог прогона: чисто.** Переход к `CODE`.
