# Шаг 3 Фазы 1 — DOCS_CHECK_7 (gap-отчёт)

## На какой вопрос отвечает этот файл

Какие пробелы концепции нашёл седьмой прогон `DOCS_CHECK` шага 3
Фазы 1 (производные рыночные данные) после закрытия `GAPS_CLOSE_5`
(Н11, Н12) и чист ли он для гейта `CODE`.

**Подтверждающий прогон после `GAPS_CLOSE_5`.** `DOCS_CHECK_6` нашёл два
узких пробела условного редизайна фазы — Н11 (`TREND_CHANGED` в whitelist
правил фазы vs stateless-классификатор) и Н12 (деривация `confirmedAt`
условно-классифицированной фазы); `GAPS_CLOSE_5` их закрыл. Этот прогон —
адверсариальный проход по доспецифицированным докам: закрыты ли Н11/Н12 и
не внесли ли правки новых несогласованностей. Обход чистый по процедуре:
целевые файлы открыты и сверены (верификация атрибуции — каждая клауза
прочитана в целевом доке). ID находок (Н1-Н12, ТР1-ТР2) сквозные между
прогонами. Формат — по `.claude/skills/concept-review.md` §Выход.

## Охват

Формулировка шага (роадмап `phase-1.md`): «Производные рыночные данные:
индикаторы + структура рынка (`MarketStructure`) + фаза рынка
(`MarketPhase`) — jobs, модели, сервисы (расчёт/чтение/сохранение
значений, запрошенных стратегией)».

**Проверялись** (фокус — доки, тронутые `GAPS_CLOSE_5`, + их соседи по
ссылкам):

- Модель: `docs/models/domain/aggregate/Strategy.md` §StrategyMarketPhaseRule
  (whitelist `ruleType`/операндов фазы), §StrategyConditionRuleType (полный
  enum); `docs/models/domain/other/MarketPhase.md` (§Структура, §Деривация
  `confirmedAt` — новый, §Правила хранения); `IndicatorValue.md`,
  `MarketStructure.md` (стык как операндов фазы).
- Компоненты: `MarketPhaseClassifier.md` (контракт + `confirmedAt`),
  `MarketPhaseJob.md`, `StrategyConditionEvaluator.md` (привязка
  `TREND_CHANGED`), `MarketStructureResolver.md`,
  `IndicatorService`/`MarketStructureService`/`MarketPhaseService`,
  `MarketDataExpirationChecker.md`.
- Правила/решения: `condition-ruletype-granularity.md`
  (`TREND_CHANGED` — phase-inapplicability),
  `market-phase-conditional-classification.md` (owner-doc, §1 whitelist +
  §3 `confirmedAt`), `strategy-condition-authoring-contract.md`
  §Переиспользование, `market-data-freshness.md`, `market-data-retention.md`,
  `market-data-result-identity-keying.md`,
  `efficiency-ratio-as-catalog-indicator.md`, `volume-condition-semantics.md`.
- Процесс: `docs/processes/market-data-calculation.md`.
- Lifecycle: `docs/lifecycles/Strategy.md` (статусы — кому считать).
- Границы (стык, содержание вне охвата): `candle-loading.md` (шаг 1 DONE),
  `EntryScannerJob.md` (потребитель фазы → detail).

**Вне охвата:** кластеры Deal/Order/Position/команд (шаги 4-7), risk-слой
(шаг 5), интеграционные доки свечей (шаг 1 DONE).

**Торговый фокус** (`trading-review`): грунтовка — дистиллят
`.claude/library/trading/distilled/` (microstructure, strategy-patterns).
Структурных правок торговой модели на `GAPS_CLOSE_5` не было (правки
грамматики/контракта), новых торговых поверхностей не открылось.

## Стадия остановки

Обход прошёл все стадии (стадия 2). Стадия 0 — чиста (механика шага
описана, гейтящих вопросов скоупа нет). Стадия 1 — процесс целостен.
Стадия 2 — компоненты и модели; находок нет (см. ниже).

## Пробелы по типам

- **Несогласованности:** нет.
- **Name-level без структуры:** нет.
- **Неотвеченные вопросы:** ТР1 (= IND-Q1, крипто-часть) — пред-
  существующий, запаркован на фазу 4, **не-гейтящий** (не новый пробел).
- **Торговая категория:** нет новых.

## Проверено — чисто

### Н11 (`TREND_CHANGED` в whitelist правил фазы) — закрыт

Сверено по всем затронутым докам, расхождений нет:

- `Strategy.md` §StrategyMarketPhaseRule — `TREND_CHANGED` убран из
  допустимых структурно-событийных (`RANGE_BREAKOUT_CONFIRMED` /
  `VOLUME_FILTER_PASSED` / `CANDLE_CLOSED` / `MARKET_STRUCTURE_IS`) и
  перечислен в **запрещённых** (рядом с `MARKET_PHASE_IS`) с причиной
  (темпоральное несовместимо со stateless-контрактом; структурные
  переходы — через `RANGE_BREAKOUT_CONFIRMED`/`MARKET_STRUCTURE_IS`);
  дверь на будущее (чтение готовой истории **структуры**) и
  неприкосновенность entry-контекста зафиксированы.
- `strategy-condition-authoring-contract.md` §Переиспользование —
  `TREND_CHANGED` в списке исключений контекста фазы + абзац обоснования +
  дверь на будущее.
- `market-phase-conditional-classification.md` §1 (owner-doc) — whitelist
  приведён к тому же (исключения: lifecycle, `MARKET_PHASE_IS`,
  `TREND_CHANGED`).
- `condition-ruletype-granularity.md` — у `TREND_CHANGED (темпоральное)`
  помечена неприменимость в фазе (со ссылкой на §StrategyMarketPhaseRule);
  при этом как **именованный** (не-алиас) `ruleType` он остаётся
  оправданным — это другая ось, конфликта нет.
- `StrategyConditionEvaluator.md` — пример `TREND_CHANGED → MarketPhase`
  явно помечен **entry/deal-контекстом**; исходная doc↔doc-несогласованность
  (пример vs whitelist) закрыта.
- `Strategy.md` §StrategyConditionRuleType (полный enum) — `TREND_CHANGED`
  в нём **остаётся** (валиден для entry-контекста); это не противоречие —
  enum глобален, ограничение действует в контексте фазы.

### Н12 (деривация `confirmedAt` фазы) — закрыт

- `MarketPhase.md` §Деривация `confirmedAt` (новый раздел) — `confirmedAt`
  = консервативный `max` по гейт-операндам сработавшей клаузы (структура →
  `confirmedAt`, индикатор → `candleTimestamp`, без гейт-операндов →
  `candleTimestamp` бара); роль распущенного `confirmationBars`; дебаунс не
  зашивается (анти-whipsaw операнд-уровневый); арифметика — `CODE`. Поле
  `confirmedAt` в §Структуре ссылается на раздел.
- `MarketPhaseClassifier.md` §Контракт — бул `confirmedAt — производный`,
  та же деривация, ссылка на `MarketPhase.md` §Деривация `confirmedAt`.
- `market-phase-conditional-classification.md` §3 (owner-doc) — отмечена
  деривация поверх распущенного `confirmationBars`.
- Согласованность со свежестью: `confirmedAt` — гейт без look-ahead, **не**
  точка отсчёта (`referencePoint` фазы = `candleTimestamp`); конфликта с
  `market-data-freshness.md` нет.

### Прочее — подтверждено закрытым (без правок на `GAPS_CLOSE_5`)

Находки `GAPS_CLOSE_4` (Н3 структура+фаза, Н6 свежесть, Н8 retention,
Н10 ключевание `MarketPhase`, ТР2 OBV, ТР1-книжная, fork A) на этом
обходе остаются закрытыми — их доки в `GAPS_CLOSE_5` не менялись, стыки с
тронутыми доками целостны (whitelist фазы по-прежнему ссылается на ER
через `INDICATOR_COMPARE`; `MarketStructure` — операнд правил фазы;
ключевание/свежесть/retention не затронуты). Сквозные инварианты («только
закрытые свечи, без look-ahead»; цепочка jobs Indicator → Structure →
Phase; производный checkpoint; реестр конфигураций) — целостны.

## Блокирующие открытые вопросы

Гейтящих (стадия 0) нет. В охвате — ТР1 (= IND-Q1): книжная часть
закрыта, крипто-часть запаркована на фазу 4 (владелец — этот шаг),
**не гейтит** механику расчёта и не является новым пробелом.

## Находки и эскалации

**Новых находок нет.** Активные эскалации прошлых прогонов:

### ТР1 (= IND-Q1, крипто-часть) — без изменений

- **Вопрос.** Крипто-специфика достоверности спот-объёма CEX для OBV /
  `VOLUME_FILTER_PASSED`.
- **Ожидаемый владелец / кто ответил:** `trading-specialist`. Surface —
  `trading-review`. Корпус по крипто-объёму ∅ (подтверждено вычиткой);
  внешний добор — контур дообучения к фазе 4.
- **Ответ.** Не закрываемо на текущем грунте; запарковано на фазу 4.
  В фазе 1 даунсайд прикрыт (ручной выбор инструмента/стратегии + объём =
  подтверждающий фильтр).
- **Ярлык исхода:** `неразрешимо-владельцем`. **Ярлык дефицита:** `грунт`.
- **Флаг действия CC:** `эскалировал` (IND-Q1 уже существует, заново не
  заводится). Не гейтит.

### Сознательные отложенности — маршруты, не пробелы

- Сигнатура контекст-объекта evaluator под фазу — `CODE`
  (`MarketPhaseClassifier.md` §Границы).
- Точная арифметика структуры/ER, форма `breakoutEvent`, warmup-derive
  множители, точная арифметика `confirmedAt`-деривации фазы — `CODE`.
- Численные пороги структуры/фазы, percent-anchor (STRAT-Q4) — хвост
  пользователя / отложенный инкремент контракта.

## Сводка

- **Находок:** 0 новых. Н11, Н12 (`GAPS_CLOSE_5`) подтверждены **закрытыми**
  по всем затронутым докам; правки новых несогласованностей не внесли
  (битых кросс-ссылок нет — `§Деривация confirmedAt` создан и адресуется
  корректно из трёх мест; форбид-листы whitelist фазы согласованы между
  `Strategy.md`/authoring-contract/decision). Прочие закрытые находки
  (Н1-Н10 кроме «живых», ТР2, fork A) — подтверждены закрытыми.
- **По типам:** несогласованности — 0; name-level — 0; неотвеченные
  вопросы — 1 (ТР1 крипто, запаркован, не-гейтящий, не новый).
- **По ярлыкам исхода:** новых нет; активная эскалация — ТР1
  (`неразрешимо-владельцем`).
- **По флагу действия CC:** `финализировал` — 0; `завёл открытый вопрос`
  — 0; `эскалировал` — 1 (ТР1, уже существующий IND-Q1); новых
  `предложил` — 0 (закрытие Н11/Н12 произошло на `GAPS_CLOSE_5`, здесь —
  только верификация). Поведение в рамках — финализации концепта CC не
  делал.
- **Расхождений «ожидаемый владелец vs кто ответил» нет.**
- **Динамика к `DOCS_CHECK_6`:** `DOCS_CHECK_6` был «не чисто» (Н11 +
  Н12). `GAPS_CLOSE_5` оба закрыл; `DOCS_CHECK_7` — **чисто**: новых
  пробелов и расхождений нет, единственный остаток (ТР1 крипто) —
  пред-существующий запаркованный не-гейтящий forward-item. Сдвиг
  поведения пайплайна: петля `DOCS_CHECK`/`GAPS_CLOSE` шага 3 сошлась.

**Рекомендация: чисто.** Концепт-гейт пройден — по правилу
`roadmap-step-execution.md` §«Гейт `CODE` — чистый `DOCS_CHECK`» чистый
прогон есть обязательное условие перехода, и оно выполнено. Открытых
находок/расхождений нет; ТР1 крипто остаётся запаркованной (фаза 4, не
гейтит). Шаг готов к **`CODE`** (включая forward-debt Java step-2 —
ресинк под условную модель фазы + удаление `structureType` + сверка
`MarketStructureParams`). Следующий ход — за пользователем: перевод шага
на `CODE`.
