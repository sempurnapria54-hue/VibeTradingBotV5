# `GAPS_CLOSE_4` шага 8 фазы 2 — стратегии

## На какой вопрос отвечает этот файл

Чем закрыты находки прогона `DOCS_CHECK_4` шага 8 фазы 2.

## Вход и метод

Отчёт прогона — `phase-2-step-8-docs-check-4.md`: 2 находки, гейтящих —
**ноль**, обе с ярлыком `выводимо`. Популяции гейтящих узлов не строятся:
гейтящих узлов нет (`.claude/skills/closure-population.md` §«Когда
применять» — перечень требуется у каждого **гейтящего** узла).

## Диспозиция находок

| Находка | Исход |
|---|---|
| E1 | **закрыта**: вводный абзац §«Что проверяется на активации» переписан под действующий признак — «разрешение, выданное **непроверенному** определению», с указанием, почему часть проверок не считалась на создании. Прежняя формулировка («не способно произвести ни одного действия») снята |
| E2 | **закрыта в обоих носителях**: `docs/models/domain/aggregate/Strategy.md` §Персистентность — прочтение пустоты в строке подраздела; `docs/models/mapping/Strategy.md` §«domain ↔ persistence» — отбрасывание тенанта на стороне копии названо отдельным пунктом |

**Счёт:** находок 2; закрыто 2; припарковано 0. Собственных находок
закрытие не подняло.

## Свипы снятых редакций

E1 **снимал формулировку** — свип применяется.

```bash
LC_ALL=C.UTF-8 grep -rnUP '(?s)не\s+способно\s+произвести\s+\*\*ни\s+одного\*\*' docs/ .claude/ services/ libs/
```

| Файл | Исход |
|---|---|
| `docs/rules/strategy-validation.md` | **правлен**: формулировки в файле больше нет |
| `.claude/work/progress/phase-2-step-8-docs-check-4.md` | **не требует правки**: отчёт цитирует снятую редакцию как находку — это его предмет; каталог `progress/` из области `retired-check` выведен по этой же причине |
| прочих вхождений нет | — |

Записи в реестр снятых редакций **не заводится, и причина названа**: снятая
формулировка была **мотивирующей**, а не действующим правилом — действующий
признак («проверяемо ли определение») введён `GAPS_CLOSE_1` и с тех пор не
менялся; популяции носителей у неё одна, и она правлена. Запись в реестр
охраняла бы фразу, которую никто, кроме этого абзаца, не выражал.

## Записи выноса держателю

Вопросов держателю не было: обе находки `выводимо`.

## Мини-петля критики

Гейтящих узлов нет; обе правки — однопассажные. Раунд один: обе
перечитаны после приземления, прогоны корпуса — в гейте перехода.

## Рекомендация

Шаг идёт на **гейт перехода в `CODE`**: ряд гейтящих находок по шагу
9 → 2 → 2 → 0.
