# Шаг 5 фазы 2 — коннектор OKX

## На какой вопрос отвечает этот файл

Чем закрыт шаг 5 фазы 2.

## Итог

**Коннектор OKX построен и закрыт.** Стейтлесс-сервис без своей базы:
принимает доменные запросы, берёт ключи биржевого счёта из Vault по
идентификатору из запроса, отдаёт общую модель. 86 файлов,
32 операции входного контракта, 32 эндпоинта, 25 тестов.

Тем же ходом закрыта пишущая сторона: `auth` кладёт ключи в хранилище
при регистрации счёта — работа, которую шаг 4 явно отложил «до первого
читателя».

## Что осталось в постоянных носителях

| Что | Где |
|---|---|
| дом перечня операций | `docs/components/IntegrationService.md` §«Входной контракт» |
| дом классов отказа границы | там же, §«Классы отказа на границе — дом здесь» |
| состав секрета счёта | `docs/architecture/tenant-and-exchange.md` §Ключи |
| кто наблюдает опросом | `docs/architecture/services.md` §«Что коннектор не знает» |
| решения Д636-Д644 | `.claude/work/decision-digest.md` (итерация 28) |
| хроника, отчёты фокусов, `SYNC_DOCS`, концепт-гейт | `history/2026-09-05-phase-2-step-5-connector/` |

## Закрытая позиция бэклога

§«Входной контракт коннектора — проектирование операций в доменных
типах» — снята: контракт спроектирован (`GAPS_CLOSE_3`) и закодирован.
При кодировании обнаружилось, что спроектированный перечень был **у́же
донорской границы на шесть операций**; расширен до 32 и сверен
механически — у каждого публичного метода читателя источника есть выход
наружу.

## Парковки, заведённые шагом

| Парковка | Что оживит |
|---|---|
| §«Сброс кэша ключей по событию ротации» | появление события `ExchangeKeysRotated` (заводит шаг `auth`) |
| §«Осиротевшие секреты счетов» | прод-рубеж |
| §«Манифесты развёртывания сервисов» | кластер в среде + выбор реестра образов (ARCH-Q1) |

## Проверки

Десять команд корпуса — код 0. `mvn -o test -DexcludedGroups=source-api-live`
— `BUILD SUCCESS`: `auth` 10, `connector-okx` 25, `tradingbot` 324.
