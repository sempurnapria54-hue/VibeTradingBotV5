# Цена сквозных прогонов — правило и единица работы

## На какой вопрос отвечает этот файл

Что сделано по решению держателя 2026-09-26 о цене закрывающих прогонов
модуля `tests`.

## Итог

Одиночная сессия при остановленном цикле; работа шага 12 не велась. Решения —
Д2218-Д2225 (`.claude/work/decision-digest.md`).

- **Правило:** сессия тестера с одной тестовой дельтой закрывается тронутыми
  классами — `.claude/rules/session-work-unit.md` §«Объём закрывающего
  прогона»; исполнение — режим классов `tools/reactor-test.sh`.
- **Единица работы:** общий стенд на группу сквозных классов —
  `.claude/work/backlog.md` §«Сквозные классы модуля `tests` делят поднятый
  стенд на группу».
- **Постоянный лог реактора** — `target/reactor-test.log`, лента цикла читает
  его давность (`tools/session_feed.py`).

## Каталог проб `target/` в корне уводит `retired-check.py` в код 2

**Закрыта** тем же ходом: постоянный лог реактора сделал корневой `target/`
штатным, и `target` внесён в `OUT_OF_AREA` детектора `tools/retired-check.py` с
причиной. Секция бэклога снята; её условие закрытия — греп `'target'` в
детекторе.
