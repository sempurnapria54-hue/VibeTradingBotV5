# `GAPS_CLOSE_6` шага 10 фазы 2 — поузловая рамка

## На какой вопрос отвечает этот файл

Как закрыты находки прогона `DOCS_CHECK_6` шага 10 фазы 2.

## Рамка закрытия

Та же, что у `GAPS_CLOSE_2`-`GAPS_CLOSE_5`: **поузловые заходы, узел
приземляется в корпус только пройдя мини-петлю критики чисто по носителю**
(`.claude/decisions/per-node-closure-frame.md`, включая поправку `PROC-Q5`).
Порядок — посерийный: следующий узел не открывается, пока текущий не чист
либо явно не остановлен.

**Что действует на этом закрытии:**

- **потолок верификаций — две на узел**, доработка между ними законна; по
  исчерпании узел **останавливается** и поднимается сигнал С5
  (`.claude/decisions/mini-loop-verification-ceiling.md`). Остановленное
  разбирает CC по `.claude/rules/stopped-node-disposition.md`, а не держатель;
- **единица работы сессии — одна** (`.claude/rules/session-work-unit.md`): узел
  целиком, прогон гейта либо дизайн-проход; исход каждого захода пишется в
  отчёт сразу, до начала следующего;
- **границы доковой петли** (`.claude/rules/docs-loop-limits.md`): предмет гейта
  выхода — **корпус**, объём измерения до `CODE` — **ремонт, не усиление**,
  прогонов гейта выхода — **два на закрытие**;
- **обязанности полноты выводятся из рода правки**
  (`.claude/rules/edit-kind-obligations.md`): классификацию по закрытому
  словарю родов проставляет **каждый** корпусный заход, а не единица целиком;
- **форма самоописания** (`.claude/rules/self-description-form.md`): число с
  механическим выводом стои́т порождённым блоком, число без вывода не пишется.

**Заморозка усиления измерения на этом шаге снята входом шага в `CODE`**
(`.claude/rules/docs-loop-limits.md` §«Объём измерения до кода — ремонт, не
усиление»: заморозка вооружается на каждом шаге и снимается входом **этого**
шага в код; шаг 10 в `CODE` вошёл и вышел с аппрувом девятого прогона
фокусов). Ограничение поэтому адресует не эту петлю; закрытие тем не менее
новых **энфорсеров** и новых осей существующих энфорсеров не заводит —
исполнимая форма правила узла 1 кладётся в существующую спеку существующим
механизмом (`populations` + `derive`), которого требует
`.claude/skills/closure-population.md` §5.

Порядок внутри узла: правило узла одной фразой → популяция (до первой правки
каждого корпусного захода) → проект правок → мини-петля критики → приземление
→ обход потребителей и свипы.

## Раскладка находок по узлам

Собрана **встречным счётом от перечней линз**, а не от списка заведённых
узлов (`.claude/processes/roadmap-step-execution.md` §«Раскладка находок по
узлам проверяется встречным счётом от линз, а не от списка запущенных
узлов»): владельца получает каждая находка перечня каждой линзы, и только
потом раскладка сверяется с тем, что запущено.

| Узел | Предмет | Находки | Гейтящих |
|---|---|---|---|
| 1 | причина остановки: два затребователя одного ребра и один резолв | B1 | 1 |
| 2 | писатель факта переехал на ребро: кто он, что гарантирует и чем это стои́т | A1, A2, A3, A8, A9, B3, C2, C4 | 0 |
| 3 | счётчики происшествий: полнота, однократность, указатель, иллюстрация | A4, A5, A12, A13, C1 | 0 |
| 4 | домен значения содержимого и форма пина: дом правила и его популяция | A11, B4, C3 | 0 |
| 5 | актор в таймлайне: на какой вопрос он отвечает и кто его видит | A10, B2 | 0 |
| 6 | копия перечня и счётный клейм форм | A6, A7 | 0 |
| **Итого** | | **21** | **1** |

Строка `Итого` сверяется прогоном `py tools/self-count-check.py` (числовая
колонка — суммой, перечислительная — счётом элементов).

**Ни одна находка перечней линз без владельца не осталась:** A1-A13
(тринадцать), B1-B4 (четыре), C1-C4 (четыре) разложены по шести узлам без
пересечений.

**Дедупликация прогона учтена раскладкой, и одна группа узел ПЕРЕСЕКАЕТ.**
Три пары, названные отчётом `DOCS_CHECK_6` §«Дедупликация — где линзы сошлись
независимо», лежат внутри одного узла каждая (A1↔C2 — узел 2, A12↔C1 — узел 3,
B4↔C3 — узел 4). **Четвёрка «популяция подъёмов ступени» (A3, A4, A5 ↔ B3 ↔
C4) разложена по двум узлам:** A3, B3, C4 — узел 2, A4 и A5 — узел 3.
Прежняя редакция этой строки объявляла все группы внутриузловыми и на четвёрке
была ложной; стык узлов 2 и 3 назван поимённо в
`phase-2-step-10-gaps-close-6-node-2.md` §«Стык с узлом 3», потому что
посерийность межузловых стыков не покрывает
(`.claude/processes/roadmap-step-execution.md` §«Узлы закрытия идут
посерийно»).

## Единицы закрытия

| # | Единица | Состояние |
|---|---|---|
| 1 | узел 1 — причина остановки: два затребователя, один резолв | `закрыт` — `phase-2-step-10-gaps-close-6-node-1.md` §«Исход мини-петли и состояние узла» |
| 2 | концепт-проход по посылке, названной диагнозом С5 узла 1 | `закрыт` — `phase-2-step-10-obligation-subject-pass.md` |
| 3 | приземление узла 1 (после прохода) | `закрыт` — `phase-2-step-10-gaps-close-6-node-1.md` §«Приземление: свипы и обход потребителей» |
| 4 | узел 2 — писатель факта переехал на ребро | `остановлен` — `phase-2-step-10-gaps-close-6-node-2.md` §«Остановка узла: сигнал С5 и выбор реакции» |
| 5 | концепт-проход по посылке, названной диагнозом С5 узла 2 | `закрыт` — `phase-2-step-10-output-disposition-pass.md` |
| 6 | приземление узла 2 (после прохода; тем же ходом Ф2 и Ф3) | `закрыт` — `phase-2-step-10-gaps-close-6-node-2.md` §«Приземление узла» |
| 7 | узел 3 — счётчики происшествий | `закрыт` — `phase-2-step-10-gaps-close-6-node-3.md` §«Исход мини-петли и состояние узла» |
| 8 | узел 4 — домен значения содержимого | `закрыт` — `phase-2-step-10-gaps-close-6-node-4.md` §«Исход мини-петли и состояние узла» |
| 9 | узел 5 — актор в таймлайне | `закрыт` — `phase-2-step-10-gaps-close-6-node-5.md` §«Исход мини-петли и состояние узла» |
| 10 | узел 6 — копия перечня и счётный клейм форм | `закрыт` — `phase-2-step-10-gaps-close-6-node-6.md` §«Исход мини-петли и состояние узла» |
| 11 | первый прогон гейта выхода из `GAPS_CLOSE_6` | `закрыт` — §«Первый прогон гейта выхода из `GAPS_CLOSE_6`»; исход **КРАСНЫЙ по корпусу** (К1, К2) |
| 12 | закрытие К1 и К2 первого прогона гейта | `закрыт` — §«Закрытие К1 и К2 первого прогона гейта» |
| 13 | второй прогон гейта выхода из `GAPS_CLOSE_6` | `закрыт` — §«Второй прогон гейта выхода из `GAPS_CLOSE_6`»; исход **ЗЕЛЁНЫЙ по корпусу** |

## Узел 6

Отчёт — `phase-2-step-10-gaps-close-6-node-6.md`; исход мини-петли, число
раундов и верификаций — там же порождённым блоком.

**Узел ПРИЗЕМЛЁН штатно** (2026-09-11): раунд критики и две верификации, ряд
блокирующих корпусных дефектов **2 → 1 → 0**, вторая верификация чиста по
корпусу — сигнал С5 не срабатывает, потолок не исчерпан. Обе находки (A6, A7)
закрыты по существу.

**Домов узел не заводит и энфорсеров не вводит** — он приводит двух носителей
к уже существующим домам: копия перечня классов с ручной тропой снята из
`docs/architecture/tenant-and-exchange.md` и заменена указателем на прогон по
`docs/spec/event-actor-presence.json`; два счёта форм сняты из
`docs/architecture/contracts.md`, а причина отсутствия счёта названа отдельным
пассажем рядом с тем, который отказывается от счёта построенных классов.

**Сверх находок узел закрыл трёх носителей:** клейм «оставляет запись аудита
с актором» у требования роли (верен у половины про числа риска, ложен у
половины про ключи — у `auth` нет ни резолвера актора, ни производителя
событий), отсутствие дома ожидания у этого остатка (заведена позиция бэклога)
и **вторую копию признака** ручной тропы в
`docs/models/domain/other/Auditable.md` вместе с дублем клаузы внутри одного
пассажа. Первая находка пришла от проб окрестности, третья — от свипа рода
`снятие` при доработке.

**Стык с прогоном гейта выхода назван** в отчёте узла §«Стык с прогоном гейта
выхода»: узел 6 — последний узел закрытия, следующая единица — прогон гейта,
и вести его обязана сессия, закрытий не писавшая.

## Узел 5

Отчёт — `phase-2-step-10-gaps-close-6-node-5.md`; исход мини-петли, число
раундов и верификаций — там же порождённым блоком.

**Узел ПРИЗЕМЛЁН штатно** (2026-09-11): раунд критики и две верификации, ряд
блокирующих корпусных дефектов **4 → 2 → 0**, вторая верификация чиста по
корпусу — сигнал С5 не срабатывает, потолок не исчерпан. Обе находки (A10 и
выводимая половина B2) закрыты по существу.

**Заведён один дом** — признак **состава** формы периметра
(`docs/architecture/contracts.md` §«Состав своей формы периметр объявляет
сам»): умолчание «не едет», довод, названное сегодняшнее ограничение с
оживителем. **Предел актора** («метит ход, а не основание, из-за которого ход
случился») записан у владельца величины
(`docs/models/domain/other/Auditable.md`), а клейм ручной поверхности сведён
с тем, что пишет писатель.

**Вынос держателю исполнен:** продуктовая половина B2 заведена вопросом
`BFF-Q1` в `.claude/work/questions/open-questions.md` с вердиктом гейтинга
«не гейтит, адресат шага 12 фазы 2»; зависящая позиция припаркована там же.

**Сверх своих находок узел закрыл двух носителей:** неполный перечень снятого
в javadoc маппера потока (называл два рода из пяти — снят, а не дополнен) и
ложный клейм припаркованной позиции бэклога о покрытии связки имён
компонентов (утверждала, что связку не мерит ни одна проба ни на одном конце,
— на конце периметра она мерится у классов жизненного цикла определения).
Решения — Д1424-Д1429.

**Стык с узлом 6 назван** в отчёте узла §«Стык с узлом 6»: предметы не
пересекаются, строку счётного клейма «всех пяти его форм» узел 5 не трогал, и
четвёртого носителя перечня классов с ручной тропой не завёл.

## Узел 4

Отчёт — `phase-2-step-10-gaps-close-6-node-4.md`; исход мини-петли, число
раундов и верификаций — там же порождённым блоком.

**Узел ПРИЗЕМЛЁН** (2026-09-11): раунд критики и две верификации, ряд
блокирующих корпусных дефектов **7 → 5 → 3**; все три находки второй
верификации закрыты доработкой 3, которую потолок допускает, — С5 не
срабатывает по своему операнду. Все три находки прогона (A11, B4, C3) закрыты
по существу.

**Заведены три дома:** правило домена значения содержимого
(`docs/architecture/contracts.md` §События), правило формы пина значения,
пересекающего провод (`.claude/rules/codestyle.md`), состав перечня
`StrategyTradeDirection` (`docs/models/domain/aggregate/Strategy.md`).
Популяция из двадцати девяти компонентов пройдена поимённо: двадцать шесть
правлены, три форму уже несли.

**C3 закрыта НЕ по своему крену, и довод предъявлен прогоном инструмента:**
проектная правка (свести четыре ноты операндов к форме указателя) уронила
`python3 tools/population-derive-check.py` — команда вывода популяции читает
именно эту прозу второй стороной сверки. Различие семи операндов оказалось
механически измеримым, состав восстановлен, признак дописан в ноты и в дом
(Д1419, Д1423).

**Сверх своих находок узел закрыл четырёх носителей:** состав перечня
`StrategyTradeDirection` (его не было вовсе), ложное описание `orderType` у
обоих носителей, предмет припаркованной позиции бэклога (был у́же собственного
свипа втрое) и дубль объявления домена, заведённый свипом этого же узла.
Решения — Д1415-Д1423.

**Стык с узлом 5 назван** в отчёте узла §«Стык с узлом 5»: предметы не
пересекаются, пассажи об акторе узел 4 не трогал.

## Узел 3

Отчёт — `phase-2-step-10-gaps-close-6-node-3.md`; исход мини-петли, число
раундов и верификаций — там же порождённым блоком.

**Узел ПРИЗЕМЛЁН штатно** (2026-09-11): раунд критики и две верификации, ряд
блокирующих корпусных дефектов **4 → 2 → 0**, вторая верификация чиста по
корпусу — сигнал С5 не срабатывает, потолок не исчерпан. Все пять находок
(A4, A5, A12, A13, C1) закрыты по существу.

**Сверх своих находок узел закрыл трёх носителей**, которых не называла ни
одна из пяти: ноту `anomalyReports` спеки (проба окрестности О1), javadoc
`IncidentAggregate` (прогон реестра снятых редакций — снятое выражено другим
словом) и два соседних пассажа того же дока, которые формула полноты обходила
(проба О3). Решения — Д1412-Д1414.

**Стык с узлом 4 назван** в отчёте узла §«Стык с узлом 4»: предметы не
пересекаются, ждать узлу 4 нечего; общий носитель один, и §-имена правленной
секции — якоря двух живых указателей.

## Узел 2

Отчёт — `phase-2-step-10-gaps-close-6-node-2.md`.

**Узел ОСТАНОВЛЕН по исчерпании потолка верификаций** (2026-09-11): раунд
критики и две верификации, ряд блокирующих корпусных дефектов 12 → 4 → 3,
вторая верификация красна по корпусу — сигнал **С5**. Предмет узла при этом
снят: все восемь его находок (A1, A2, A3, A8, A9, B3, C2, C4) закрыты по
существу и подтверждены обоими проверяющими; оставшееся — Ф2 (расхождение
дома привязки с домом поведения рёбер о затребователе) и Ф3 (лид секции
парковки).

**Реакцию выбрал CC по `.claude/rules/stopped-node-disposition.md`:** диагноз
**назвал посылку** — обязанность полноты кончается на предъявлении выдачи, а
поимённого исхода на каждый её член не требует ни одна норма, кроме
популяции, — значит идёт **концепт-проход по ней**, отдельной единицей.
Правки узла остаются в дереве **неприземлёнными** и приземляются после
прохода; тем же ходом закрываются Ф2 и Ф3. Решения — Д1400-Д1402.

**Узлы 3-6 прохода НЕ ждут:** сигнал узловой, а посылка адресует форму исхода
свипа — не конструкцию их предметов. Открывать их можно штатно; узел,
открытый до прохода, идёт под прежней формой и знает об этом.

## Узел 1

Отчёт — `phase-2-step-10-gaps-close-6-node-1.md`; исход мини-петли, число
раундов и верификаций — там же порождённым блоком. Состояние единицы ведётся
по факту состоявшихся заходов, а не по ожидаемому исходу.

**Узел ОСТАНОВЛЕН по исчерпании потолка верификаций** (2026-09-10): раунд
критики и две верификации, ряд блокирующих корпусных дефектов 8 → 2 → 3,
вторая верификация красна по корпусу — сигнал **С5**
(`.claude/skills/stagnation-detection.md`). Предмет узла (B1) при этом снят и
предъявлен: конструкция принята обеими верификациями, четыре мутации роняют
объявленные пробы, четырнадцать корпусных инструментов и реактор зелены.

**Реакцию выбрал CC по `.claude/rules/stopped-node-disposition.md`:** диагноз
**назвал посылку** — обязанность полноты исполняется как закрытие находки
предыдущего захода, а не как обязанность собственной правки, — значит идёт
**концепт-проход по ней**, отдельной единицей. Решение — Д1382.

**Проход закрыт, узел приземлён, и узлы 2-6 больше ничего не ждут**
(2026-09-11). Посылка снята правилом
(`.claude/rules/edit-kind-obligations.md` §«Предмет обязанностей — дельта
захода, а не находки, на которые он отвечает»); приземление узла шло уже под
новой редакцией и её же нормой поймало носителя, которого не называла ни одна
находка. Состояние узла — `приземлён`.

**Вынос держателю (продуктовая половина B2) заводится узлом 5**, к которому
находка отнесена; вердикт гейтинга — «не гейтит, адресат шага 12».

## Первый прогон гейта выхода из `GAPS_CLOSE_6`

**2026-09-11.** Ведёт его сессия, ни одного закрытия этой единицы не писавшая
(`.claude/rules/session-work-unit.md` §Правило): прогон гейта назван единицей
работы отдельно именно затем, чтобы отчёт читал тот, кто не помнит, что в него
писал.

### Способ исполнения

**Предмет гейта — корпус** (`.claude/rules/docs-loop-limits.md` §«Предмет гейта
выхода — корпус»). Красным прогон делает дефект носителей, которые читает
будущая работа, либо **отсутствие мандатного поля отчёта**: измерения нет —
значит полнота закрытия не мерилась ничем. Дефект, живущий только в тексте о
работе, записывается и статуса не держит.

Исполнено, а не прочитано:

- **гейт инструментов корпуса** прогнан целиком на приземлённом состоянии — весь
  перечень §«Гейт инструментов корпуса»
  (`.claude/skills/update-roadmap-progress.md`), включая `spec-mutation-check`;
  ненулевых кодов нет, кода 2 нет;
- **отправные метки всех заходов извлечены из отчётов и разрешены в объекты
  git** — висячей метки нет ни у одного захода;
- **исполнимая форма правила узла 1** прогнана своей командой вывода, и ключи
  `rule`/`derive` её спеки предъявлены разбором файла;
- **счётные клеймы носителей, которых касалось закрытие, перепрогнаны** — проза
  против собственной таблицы носителя (§«Красное по корпусу — К2»).

Единицы, которые гейт мерит, — узлы 1-6 и два концепт-прохода (реакции на С5
узлов 1 и 2). Счёта их здесь не стои́т: перечень и есть колонки таблицы ниже
(`.claude/rules/self-description-form.md` §«Что чем закрывается»).

### Исход по шести предметам

| Предмет гейта | Узел 1 | Узел 2 | Узел 3 | Узел 4 | Узел 5 | Узел 6 | Проход 1 | Проход 2 |
|---|---|---|---|---|---|---|---|---|
| предъявленная популяция | зелено — спека и `derive` перепрогнаны | зелено | зелено | зелено | зелено | зелено | зелено | зелено |
| классификация захода по родам | зелено | зелено | зелено | зелено | зелено | зелено | зелено | зелено |
| обязанности сыгранных родов | зелено | зелено | зелено | **КРАСНО — К2** | зелено | зелено | зелено | зелено |
| чек-лист глубины | **КРАСНО — К1** | не применим — гейтящих находок нет | не применим — гейтящих находок нет | не применим — гейтящих находок нет | не применим — гейтящих находок нет | не применим — гейтящих находок нет | не применим — не узел | не применим — не узел |
| исход мини-петли | зелено | зелено | зелено | зелено | зелено | зелено | не применим — мини-петли нет | не применим — мини-петли нет |
| полная диспозиция находок | зелено по существу — К3 | зелено | зелено | зелено | зелено | зелено | не применим — находок прогона нет | не применим — находок прогона нет |
| записи выноса держателю | зелено — К4 | зелено — К4 | зелено — К4 | зелено — К4 | зелено | зелено — К4 | зелено — К4 | зелено — К4 |

### Красное по корпусу — К1

**К1. Чек-листа глубины закрытия нет у гейтящего узла — и ни у одного другого.**

Предмет мандатен и объявлен таковым в трёх домах:
`.claude/processes/roadmap-step-execution.md` §«Гейтящий узел закрывается до
исполнимой глубины, а не до формулировки»,
`.claude/rules/docs-loop-limits.md` §«Чего правило НЕ означает» («популяция,
классификация по родам, свипы, чек-лист глубины, исход мини-петли остаются
мандатными») и сам гейт — `.claude/skills/update-roadmap-progress.md`
§«Гейт выхода из `GAPS_CLOSE_N`» («Нет → статус **не** проставлять;
сигналить, какой пункт какого узла не доведён»).

**Проба — прогоном, а не чтением.** Ни одного пункта чек-листа («политика
сформулирована», «носитель назван и существует, потребители обойдены»,
«механика прописана», «счётный клейм — исполнимый spec-пример либо явное
„клейм не счётный“») в отчётах единицы нет ни в одной письменной форме:

```bash
export LC_ALL=C.UTF-8
PAT='Чек-лист глубины|чек-лист глубины|политика сформулирована|потребители обойдены|механика прописана|клейм не счётный'
grep -rnE "$PAT" .claude/work/progress/phase-2-step-10-gaps-close-6*.md
echo "код=$?"
```

Выдача пуста, код 1.

**Гейтящий узел у закрытия один — узел 1** (§«Раскладка находок по узлам»,
колонка «Гейтящих»), и чек-лист спрашивается у него. У узлов 2-6 предмет не
применим по тому же операнду; у двух концепт-проходов — потому что они не
узлы.

**Почему это корпус, а не текст о работе.** Отсутствие мандатного поля есть
**отсутствие измерения**, а не дефект формулировки: глубина закрытия узла 1 не
мерилась ничем, и код у такого исхода — 2 («не измерялось»), а не «дефектов
нет» (`.claude/rules/docs-loop-limits.md` §«Предмет гейта выхода — корпус»).

**Класс воспроизвёлся с `GAPS_CLOSE_5` дословно.** Его первый прогон гейта
нашёл ровно это (Э1), закрытие проставило чек-листы отдельной единицей, и на
следующем закрытии поле снова не появилось ни у одного узла. Механизм: чек-лист
не производится ни одним заходом узла по построению — он спрашивается **у
отчёта**, то есть после всего, а строки в таблице заходов у него нет.

### Красное по корпусу — К2

**К2. Проза позиции бэклога противоречит собственной таблице позиции — и это
ровно та находка, которую доработка 3 узла 4 объявила закрытой.**

Носитель — `.claude/work/backlog.md` §«Слова провода, по которым ветвится
потребитель, у четырёх компонентов терминала не запинены». Рабочий файл есть
**корпус** (`.claude/rules/docs-loop-limits.md` §«Предмет гейта выхода —
корпус»: «рабочие файлы (`backlog.md`, `open-questions.md`, роадмап,
реестры)»).

**Проба — разбором таблицы, а не чтением:**

```bash
export LC_ALL=C.UTF-8
SEC=.claude/work/backlog.md
SECTXT=$(awk '/^## Слова провода, по которым ветвится потребитель/{f=1;print;next} f&&/^## /{exit} f' "$SEC")
printf 'полей в таблице: %s\n' "$(printf '%s\n' "$SECTXT" | grep -c '^| `')"
printf 'слов провода: %s\n' "$(printf '%s\n' "$SECTXT" | grep '^| `' | cut -d'|' -f3 | tr ',' '\n' | grep -c '`')"
for p in 'шести полях' 'все шесть' 'седьмым' 'каждого из четырёх' 'четырёх новых проб'; do printf '%s: %s\n' "$p" "$(printf '%s\n' "$SECTXT" | grep -c "$p")"; done
```

Выдача прогона:

```
полей в таблице: 7
слов провода: 15
шести полях: 1
все шесть: 1
седьмым: 1
каждого из четырёх: 1
четырёх новых проб: 1
```

**Расхождения поимённо:**

| Место | Что написано | Что даёт таблица |
|---|---|---|
| §«Предмет позиции шире её имени» | «таблица выше — о **шести** полях и пятнадцати словах» | полей **семь**; слов пятнадцать — верно |
| §«Почему не закрыто тем же ходом» | «Пин каждого из **четырёх** требует своей фикстуры» | незапиненных **тринадцать** |
| §«Почему не закрыто тем же ходом» | «то есть **четырёх** новых проб у писателя» | производно от предыдущего — тринадцать |
| §«Почему не закрыто тем же ходом» | «покрывает все **шесть** и не растёт с **седьмым**» | полей семь; седьмое уже есть |

**Почему это обязанность рода, а не гигиена.** Доработка 3 узла 4 объявила
род `клейм` **сыгранным** и записала его исход: «обход носителей с клеймом
полноты о словах провода: единственный — сама позиция, и она переписана
таблицей со сверяемой строкой `Итого`». Обход остановился на таблице и не
дошёл до прозы **той же секции** — притом что находка Вв2, которую доработка
закрывала, была сформулирована как «позиция бэклога противоречит себе».

**Заход, который это ввёл, независимым взглядом не читался, и отчёт это
назвал.** Доработка 3 — третья, а потолок даёт две верификации; её правки
предъявлены гейтом инструментов и собственной пробой окрестности
(`phase-2-step-10-gaps-close-6-node-4.md` §«Доработка по итогам второй
верификации»). Прогон гейта и есть первый независимый читатель этих правок —
ровно то место, где цена, названная узлом, и предъявилась.

**Проба О3 доработки 3 клейм о своей выдаче написала шире исполнения:**
«Проверено, что число слов провода живёт теперь **в одном** носителе: свип даёт
ноль вхождений старого числа и одну таблицу нового». Свип искал старое число
(«шесть литералов»), а прозаические обороты той же секции считают **поля** и
**незапиненные слова** — другой термин, поэтому в выдачу не попали.

### Записанные находки, статуса не держащие

Дефект, живущий только в тексте о работе, идёт находкой прогона и уезжает с
отчётом в `history/` (`.claude/rules/docs-loop-limits.md` §«Предмет гейта
выхода — корпус»).

| # | Находка | Почему не гейтит |
|---|---|---|
| К3 | у узла 1 нет таблицы «Диспозиция находок узла»: исход его единственной находки B1 живёт прозой §«Исход мини-петли и состояние узла» («Предмет B1 снят и предъявлен четырьмя мутациями»), а не клеткой | операнд гейта — «есть находки без исхода» либо «сводка не сходится»; исход у B1 есть и читается, счёт раскладки сходится строкой `Итого`. У остальных двадцати находок исход стои́т клеткой — проверено разбором таблиц шести узлов |
| К4 | записи «выносов держателю не было» нет ни у одного узла, кроме узла 5 (у которого вынос был), и нет общей записи в голове отчёта | пункт 4 предмета адресует форму отчёта; пункты 1-3 зелены: у `BFF-Q1` записаны обе половины самопроверки, вердикт гейтинга и припаркованная позиция с домом, оживителем и причиной |
| К5 | у головного отчёта нет абзаца сводки-счёта «всего / закрыто / припарковано» — он выводится читателем из таблиц шести узлов | тот же операнд, что у К3: счёт сходится, находок без исхода нет. Форма отчёта — не корпус |

### Исход прогона

**КРАСНЫЙ по корпусу: К1 и К2.**

**С6 не сработал:** порог сигнала — красный **второй** прогон по корпусу
(`.claude/skills/stagnation-detection.md` §«С6 — ряд прогонов гейта выхода не
сходится»), а этот первый. Потолок — два прогона на закрытие
(`.claude/rules/docs-loop-limits.md` §«Потолок прогонов гейта выхода — два на
закрытие»).

**Следующая единица — закрытие К1 и К2.** К1: чек-лист глубины проставляется
пунктами поимённо у узла 1 — по каждому пункту названо, чем он доведён. К2:
проза позиции бэклога сводится с её таблицей, и род `клейм` доигрывается по
**прозе** секции, а не только по её таблице. К3-К5 закрываются попутно, если
закрывающий заход их касается; статуса они не держат.

**Второй (и последний) прогон гейта выхода ведёт сессия, этого закрытия не
писавшая** (`.claude/rules/session-work-unit.md`).

## Закрытие К1 и К2 первого прогона гейта

**2026-09-11.** Единица 12 §«Единицы закрытия». **Заход не корпусный:** правятся
отчёты закрытия — этот и `phase-2-step-10-gaps-close-6-node-1.md`, — то есть
текст о работе, а не носители корпуса (`.claude/rules/docs-loop-limits.md`
§«Предмет гейта выхода — корпус»). Род правки обязанностей полноты поэтому не
назначает: корпусным заход делает правка носителя корпуса, а не факт правки
(`.claude/rules/edit-kind-obligations.md`).

Вести второй прогон гейта эта сессия не вправе — его берёт сессия, закрытия не
писавшая (`.claude/rules/session-work-unit.md` §Правило).

### К1 — закрыта

Чек-лист глубины проставлен пунктами поимённо у **гейтящего** узла —
`phase-2-step-10-gaps-close-6-node-1.md` §«Чек-лист глубины». Пять клеток:
политика сформулирована; носитель назван и существует; потребители обойдены
двумя формами; механика прописана; счётный клейм несёт исполнимую форму.

Пункт обхода **перепрогнан, а не пересказан**: обе команды носительской оси
исполнены на нынешнем состоянии дерева, и выдача сверена с таблицей оси
пофайлово. Вторая форма (по прозаической тропе чтения) дала трёх носителей,
которых форма по имени не берёт, и один из трёх — **омоним** («причина
остановки» о прогоне, а не о сделке); он назван в клетке, а не отброшен молча.

Проба, которой находка была предъявлена, на новом состоянии молчит:

```bash
export LC_ALL=C.UTF-8
PAT='Чек-лист глубины|чек-лист глубины|политика сформулирована|потребители обойдены|механика прописана|клейм не счётный'
grep -rlE "$PAT" .claude/work/progress/phase-2-step-10-gaps-close-6*.md
```

Выдача — `phase-2-step-10-gaps-close-6-node-1.md` и этот файл, код 0.

### К2 — снята редакцией носителя, и это предъявлено прогоном

Носитель — `.claude/work/backlog.md` §«Слова провода, по которым ветвится
потребитель, у четырёх компонентов терминала не запинены» — **переписан
внецикловой единицей держателя** (чистка бэклога и ввод машинной формы секций,
2026-09-11; итог — `.claude/work/history/2026-09-11-backlog-diet.md`). Счёт в
прозе заменён указателем на команду, таблица оставлена снимком на момент
парковки, секция несёт маркер формы.

Находка сверена с новой редакцией **той же пробой**, которой была предъявлена:

```bash
export LC_ALL=C.UTF-8
SEC=.claude/work/backlog.md
SECTXT=$(awk '/^## Слова провода, по которым ветвится потребитель/{f=1;print;next} f&&/^## /{exit} f' "$SEC")
printf 'полей в таблице: %s
' "$(printf '%s
' "$SECTXT" | grep -c '^| `')"
printf 'слов провода: %s
' "$(printf '%s
' "$SECTXT" | grep '^| `' | cut -d'|' -f3 | tr ',' '
' | grep -c '`')"
for p in 'шести полях' 'все шесть' 'седьмым' 'каждого из четырёх' 'четырёх новых проб'; do printf '%s: %s
' "$p" "$(printf '%s
' "$SECTXT" | grep -c "$p")"; done
```

Выдача прогона:

```
полей в таблице: 7
слов провода: 15
шести полях: 0
все шесть: 0
седьмым: 0
каждого из четырёх: 0
четырёх новых проб: 0
```

Все четыре расхождения, названные поимённо первым прогоном гейта, дают ноль:
прозаические счёты сняты, а не поправлены — то есть закрыты той формой, которую
правило и предписывает (`.claude/rules/self-description-form.md`: число без
механического вывода не пишется).

**Счёт слов провода в прозе больше не стои́т вовсе** — на его месте команда
`grep -rnE "= '[A-Z_]+'" --include=*.java services/audit-statistics/src/main services/bff/src/main`,
и перечень печатает она.

**Свипа рода `клейм` заход не играет, и это исход, а не пропуск:** обязанность
возникает у правки, а секции заход не правит — находка снята чужой редакцией.
Прочитана секция целиком; единственное оставшееся число о ней самой — «четырёх
компонентов» в **заголовке**, и оно не противоречие, а **объявленное** сужение:
тело секции первой же фразой состояния говорит «предмет шире имени секции» и
называет, чего в имя не попало. Заголовок сохранён дословно потому, что на него
ссылаются `.claude/rules/codestyle.md` §«Пин значения, пересекающего провод:
слово или символ» и реестр гейтов.

### К3 — закрыта попутно

Таблица §«Диспозиция находок узла» проставлена у узла 1
(`phase-2-step-10-gaps-close-6-node-1.md`): исход B1 стои́т клеткой, а не
прозой. Статуса находка не держала и держать не начинает — закрыта потому, что
заход и так правил этот отчёт.

### К4 — закрыта попутно: запись о выносах держателю

**Вынос держателю у закрытия один.** Продуктовая половина B2 (узел 5) заведена
вопросом `BFF-Q1` в `.claude/work/questions/open-questions.md` с вердиктом
гейтинга «**не гейтит**, адресат — шаг 12»; позиция припаркована с домом,
оживителем и причиной (`.claude/rules/parking-address.md`).

**У узлов 1, 2, 3, 4, 6 и у обоих концепт-проходов выносов не было**, и это
записанный исход: ни одна их находка не упиралась в дефицит `подтверждение` —
ни численной величины риск-аппетита, ни продуктового scope, ни намерения среди
них нет (`.claude/processes/question-delegation.md` §«Что доходит до
пользователя»). Развилки, которые в них были, решены владельцами по карте и
записаны строками `.claude/work/decision-digest.md` с правом вето постфактум —
включая обе реакции на С5 (`.claude/rules/stopped-node-disposition.md`).

### К5 — закрыта попутно: сводка диспозиции

Единица счёта — **исход**, а не находка: у B2 половины разведены (узел 5), и
каждая несёт свой, поэтому исходов на единицу больше, чем находок. Перечни
находок по узлам здесь не повторяются — их дом §«Раскладка находок по узлам»,
и его строка `Итого` сверяется тем же прогоном.

| Узел | Закрыто | Припарковано |
|---|---|---|
| 1 | 1 | 0 |
| 2 | 8 | 0 |
| 3 | 5 | 0 |
| 4 | 3 | 0 |
| 5 | 2 | 1 |
| 6 | 2 | 0 |
| **Итого** | **21** | **1** |

Обе строки `Итого` сверяет `py tools/self-count-check.py` (числовая колонка —
суммой). Находок в раскладке 21, закрытых исходов 21, припаркованных 1:
превышение на единицу даёт расщеплённая B2, чья выводимая половина закрыта, а
продуктовая припаркована.

**Незакрытых находок у закрытия нет.** Каждая клетка шести таблиц «Диспозиция
находок узла» несёт исход; единственная позиция без закрытия — припаркованная
половина B2, у которой исход «припаркована» и есть.

### Исход единицы

**К1, К2, К3, К4, К5 закрыты.** Корпус заход не правил: К1, К3, К4, К5 —
правки текста о работе, К2 снята чужой редакцией корпуса и здесь только
предъявлена прогоном.

**Сверх находок заход снял один артефакт**, которого не называл ни один прогон:
пустой файл `phase-2-step-10-gaps-close-6-GATE.tmp` — остаток сессии первого
прогона гейта, нулевого объёма, ни одним носителем не упоминаемый (проверено
грепом по корпусу). Тип `.tmp` таблице размещения не принадлежит, а имя не
отвечает ни одному правилу именования (`.claude/rules/structure.md`,
`.claude/rules/naming.md`), — то есть это ровно пункт 5 чек-листа свипа
курации (`.claude/rules/curation.md`). Удалён из индекса и с диска.

**Следующая единица — второй (и последний) прогон гейта выхода**, и вести его
обязана сессия, этого закрытия не писавшая. Красный по корпусу второй прогон
поднимает **С6**, и реакцию выбирает CC по
`.claude/rules/stopped-node-disposition.md` в рамках заморозки; третьего
прогона нет (`.claude/rules/docs-loop-limits.md` §«Потолок прогонов гейта
выхода — два на закрытие»).

## Второй прогон гейта выхода из `GAPS_CLOSE_6`

**2026-09-11.** Единица 13 §«Единицы закрытия». Ведёт его сессия, ни одного
закрытия этой единицы не писавшая (`.claude/rules/session-work-unit.md`
§Правило): прогон гейта назван единицей отдельно затем, чтобы отчёт читал тот,
кто не помнит, что в него писал. **Прогон последний** — потолок даёт два на
закрытие (`.claude/rules/docs-loop-limits.md` §«Потолок прогонов гейта выхода —
два на закрытие»).

### Способ исполнения второго прогона

**Предмет гейта — корпус.** Предметы «чек-лист глубины» и «обязанности сыгранных
родов» перепроверены на **новой** редакции носителей, а не по вердикту первого
прогона. Прогон исполнял пробы, а не читал отчёты:

- **гейт инструментов корпуса** прогнан целиком — весь перечень
  §«Гейт инструментов корпуса» (`.claude/skills/update-roadmap-progress.md`);
  ненулевых кодов нет, кода 2 нет;
- **обе формы обхода потребителей узла 1 перепрогнаны** на нынешнем дереве, и
  выдача сведена с таблицей носительской оси **машинно**, а не чтением;
- **проба, которой была предъявлена К2,** исполнена на нынешней редакции секции
  бэклога;
- **дома, заведённые закрытием, проверены на существование и на входящие
  указатели** — обязанность рода `дом`, которой не мерит ни один инструмент;
- **перечень находок прогона сведён с перечнями линз** и с клетками диспозиции
  шести узлов.

### Исход второго прогона по шести предметам

| Предмет гейта | Узел 1 | Узел 2 | Узел 3 | Узел 4 | Узел 5 | Узел 6 | Проход 1 | Проход 2 |
|---|---|---|---|---|---|---|---|---|
| предъявленная популяция | зелено — спека, `derive` и обе формы обхода перепрогнаны | зелено | зелено | зелено | зелено | зелено | зелено | зелено |
| классификация захода по родам | зелено | зелено | зелено | зелено | зелено | зелено | зелено | зелено |
| обязанности сыгранных родов | зелено | зелено | зелено | зелено — К2 снята, проба даёт ноль | зелено | зелено | зелено | зелено |
| чек-лист глубины | зелено — К1 закрыта, пункты перепроверены прогоном | не применим — гейтящих находок нет | не применим — гейтящих находок нет | не применим — гейтящих находок нет | не применим — гейтящих находок нет | не применим — гейтящих находок нет | не применим — не узел | не применим — не узел |
| исход мини-петли | зелено | зелено | зелено | зелено | зелено | зелено | не применим — мини-петли нет | не применим — мини-петли нет |
| полная диспозиция находок | зелено | зелено | зелено | зелено | зелено | зелено | не применим — находок прогона нет | не применим — находок прогона нет |
| записи выноса держателю | зелено | зелено | зелено | зелено | зелено — `BFF-Q1` | зелено | зелено | зелено |

### Чем предметы предъявлены

**Чек-лист глубины (бывшая К1).** Пункт обхода потребителей перепрогнан обеими
формами, и выдача сведена с отчётом узла **машинно**: у каждого члена
объединённой выдачи обеих форм проверено наличие исхода в отчёте по имени файла
либо имени класса. Расхождений нет.

```bash
export LC_ALL=C.UTF-8
mkdir -p .tmpgate
grep -rlUzP '(?s)shutdownReason|ShutdownReason' docs/ services/ libs/ .claude/rules .claude/processes .claude/skills .claude/decisions .claude/work/backlog.md --include=*.md --include=*.java --include=*.json | tr '\0' '\n' > .tmpgate/f1.txt
grep -rlUzP '(?s)причин[а-яё]* (выхода из штатного ведения|по радиусу|остановки)' docs/ .claude/rules .claude/processes .claude/skills .claude/work/backlog.md | tr '\0' '\n' > .tmpgate/f2.txt
py -c "
import os
node=open('.claude/work/progress/phase-2-step-10-gaps-close-6-node-1.md',encoding='utf-8').read()
files=set()
for p in ('.tmpgate/f1.txt','.tmpgate/f2.txt'):
    for l in open(p,encoding='utf-8'):
        l=l.strip()
        if l and '/target/' not in l: files.add(l)
print('членов выдачи (обе формы, без target):',len(files))
missing=[f for f in sorted(files) if os.path.basename(f) not in node and os.path.splitext(os.path.basename(f))[0] not in node]
print('без исхода в отчёте узла:',len(missing))
for m in missing: print('   НЕТ ИСХОДА:',m)
"
rm -rf .tmpgate
```

Выдача: `членов выдачи (обе формы, без target): 38`, `без исхода в отчёте узла: 0`.

**`target/` выведен из области исправленной пробой, и это названо.** Форма
обхода, записанная узлом, с `--include=*.java` берёт и порождённые исходники
(`services/*/target/generated-sources/**` — три файла). Носителями корпуса они не
являются: `git check-ignore` относит их к `target/` строки 7 `.gitignore`, их
производит компилятор, и в дельте `CODE` их нет. Правило «дефект предъявленного
поля прогон доводит сам» (`.claude/rules/docs-loop-limits.md` §«Предмет гейта
выхода — корпус») здесь и применено: прогон исполнил **исправленную** пробу и
смотрел на корпус.

**Три носителя, которых форма по имени не берёт, сверены поимённо** — выдача
второй формы за вычетом первой даёт ровно `.claude/skills/update-roadmap-progress.md`
(омоним — «причина остановки» там о прогоне, не о сделке),
`docs/architecture/contracts.md` и `docs/rules/error-handling-policy.md`, как и
записано в клетке чек-листа.

**Исполнимый дом узла 1 предъявлен разбором файла:** `docs/spec/hard-rung-shutdown-reason.json`
несёт величины `hardRungShutdownReason` и `firstMoveShutdownReason` (и
`reactionScopeReason`), две оси `populations`, обе с ключом `derive`, семь
примеров; `bash tools/spec-run.sh` и `py tools/population-derive-check.py` зелены.
Единственный код-читатель и команда вывода существуют; собственный резолв шага
прохода (`shutdownReasonOf`) грепом по деревьям кода не находится.

**К2 снята и на нынешней редакции.** Та же проба, которой находка была
предъявлена, даёт по всем четырём расхождениям ноль (таблица секции — семь полей
и пятнадцать слов; обороты «шести полях», «все шесть», «седьмым», «каждого из
четырёх», «четырёх новых проб» — по нулю). Прозаических счётов о самой секции в
ней больше нет.

**Обязанность рода `дом` проверена на входящих указателях** — её не мерит ни
`anchor-check.py` (мерит разрешимость), ни `doc-pointer-check.py` (мерит
существование файла). У каждого дома, заведённого закрытием, входящие указатели
есть: у `docs/spec/hard-rung-shutdown-reason.json` — шесть носителей, у
`docs/architecture/contracts.md` §«Состав своей формы периметр объявляет сам» —
javadoc маппера потока, бэклог и вопрос держателя, у `.claude/rules/codestyle.md`
§«Пин значения, пересекающего провод: слово или символ» — javadoc блока констант
писателя и бэклог.

**Ловушка пробы названа, потому что она едва не дала ложную находку.** Греп
указателя литеральными пробелами даёт **ложный ноль**: в носителях имя пассажа
разорвано переносом строки, а в java — ещё и звёздочкой продолжения javadoc.
Терпимость к переносу у такой пробы не украшение, а условие её верности — то же
требование, что гейт предъявляет обеим формам обхода потребителей
(`.claude/skills/update-roadmap-progress.md` §«Глубина закрытия гейтящих узлов»).

**Диспозиция сведена машинно:** двадцать одна находка перечней линз
(A1-A13, B1-B4, C1-C4) разложена по шести узлам, и у каждой в таблице
§«Диспозиция находок узла» своего узла стои́т непустая клетка. Сводка отчёта —
закрытых исходов 21, припаркованный 1 — сходится с перечнем линз; строки `Итого`
сверяет `py tools/self-count-check.py`.

**Вынос держателю один и полон:** `BFF-Q1` несёт вердикт гейтинга («не гейтит,
адресат — шаг 12»), обе половины самопроверки и припаркованную позицию с домом,
оживителем и причиной. Основанием выбора концепция в нём не называется — он
говорит обратное: концепция о составе формы показа не высказывается.

### Находка прогона Г1 — ложный счёт пассажей-домов, правлен на месте

**Носитель — `.claude/rules/edit-kind-obligations.md`**, пассаж о словаре родов:
«Родов семь, а пассажей-домов девять — два рода описаны двумя пассажами каждый»
плюс производный буллет «Не отменяет ни одного из девяти пассажей-домов».

**Проба — разбором собственной таблицы носителя:**

```bash
export LC_ALL=C.UTF-8
awk -F'|' '/^## Словарь родов — закрытый/{f=1} f&&/^\| `/{n=gsub(/§«[^»]+»/,"&"); gsub(/[ `]/,"",$2); printf "%s пассажей=%d\n",$2,n} f&&/^## [^С]/{exit}' .claude/rules/edit-kind-obligations.md
```

Выдача: `снятие`, `дом`, `радиус` — по два пассажа; `величина`, `конструкция`,
`кардинальность`, `клейм` — по одному. Итого десять пассажей и **три** рода с
двумя, а не девять и два. Перечисление в скобках при этом называло `снятие` и
`радиус`, то есть расходилось и с собственным числом: род `дом` в него не попал.

**Клейм ложен с рождения.** `git log -S` относит обе строки к вводу файла, и
строка рода `дом` с двумя пассажами введена **тем же** коммитом: числу не с чего
было устареть — оно не совпадало с таблицей никогда.

**Почему находка не гейтит и С6 не поднимает.** Ни один из шести предметов гейта
она не нарушает: клейм введён предыдущим закрытием того же шага, ни один
корпусный заход `GAPS_CLOSE_6` словаря родов не правил (дельта закрытия по этому
файлу — только два новых раздела концепт-проходов), и род `клейм` у них поэтому
не играет. Операнд С6 — **несходимость ряда прогонов по дефектам своего
закрытия** (`.claude/skills/stagnation-detection.md` §«С6 — ряд прогонов гейта
выхода не сходится»); унаследованный дефект соседнего закрытия его не двигает.

**Почему правлен прогоном, а не возвращён закрытию.** Опровергнутый клейм
правится **на месте** (`docs/concept.md` §«Опровержение правит на месте»), а класс
«неразмеченный счётный клейм в прозе» объявлен **названным остатком**, который
держится дисциплиной пишущего и погашается читателем
(`.claude/rules/self-description-form.md` §«Цена принята и названа»). Независимый
взгляд, ради которого прогон ведёт чужая сессия, адресует **гейтящие** находки:
их закрытие ведёт отдельная единица, и её проверяет следующий прогон. Г1 не
гейтит, конструкции у неё нет, а предмет правки — одно число, чья ложность
предъявлена прогоном в этом же разделе.

**Что встало вместо числа.** Счёта пассажей в носителе больше нет: он выводится
из колонки дома таблицы, но порождённой формы под такой вывод в закрытом перечне
генераторов `py tools/self-count-check.py` нет, а написанный рукой рядом с тем,
что он считает, есть запрещённая третья форма. Пассаж называет предмет без числа
и называет снятую редакцию.

### Находка прогона Г2 — ось 9 энфорсера команд слепа к `read` с флагом

**Носитель — `tools/command-carrier-check.py`**, `READ_RE` оси 9. Имя,
связанное циклом чтения **с флагом** (`while IFS= read -r line`), ось считает
несвязанным: шаблон ждёт имя сразу после `read`, а `-r` под `[A-Za-z_]` не
подходит. Контроль 9b на ложное срабатывание у оси есть, но его фикстура
написана без флага, поэтому слепоту он не ловит.

**Предъявлено собственным прогоном**, а не чтением: первая редакция блока пробы
Г1 связывала `line` в том же блоке и получила два `ДЕФЕКТ: имя не связано в
блоке`. Блок переписан без цикла (та же выдача, awk вместо конвейера с чтением),
и прогон даёт ноль.

**Почему не гейтит.** Предмет — инструмент, а не закрытие: ни один из шести
предметов гейта осью 9 не мерится, и ни один узел `GAPS_CLOSE_6` этого
энфорсера не правил. Дефект ложно-положительный — он не пропускает настоящее, а
штрафует законное, — и потому не ставит под сомнение ни один зелёный прогон
корпуса.

**Почему припаркована, а не правлена здесь.** Правка энфорсера требует своей
падающей пробы и контроля на ложное срабатывание (`.claude/processes/roadmap-step-execution.md`
§«Оси проверочной команды доказываются поимённо») — это своя единица, а не
попутная правка внутри прогона, который энфорсер и поймал. Дом, оживитель и
причина — `.claude/work/backlog.md` §«Ось 9 энфорсера команд слепа к `read` с
флагом».

### Правка Г1 — корпусный заход и его обязанности

| Заход | Состояние |
|---|---|
| правка Г1 (корпусный) | `записан` — §«Правка Г1 — корпусный заход и его обязанности» |

**Отправная метка — запасная, и это объявлено.** Метка `git write-tree` до первой
правки отбита не была; восстановлена меткой предыдущего захода — деревом
последнего коммита `8d160f54051a5bafcd0331bf2a46eca223f0daf5`. Перечень поэтому
**шире** собственной дельты: в него попадают носители захода-предшественника
(отчёты закрытия К1/К2 и снапшот). Ошибка допущена в сторону избытка, как и
предписывает запасной вывод (`.claude/rules/edit-kind-obligations.md` §«Цена
принята и названа»).

```bash
git diff --name-only 8d160f54051a5bafcd0331bf2a46eca223f0daf5
git diff -U0 8d160f54051a5bafcd0331bf2a46eca223f0daf5 -- .claude/rules/edit-kind-obligations.md
```

Корпусный носитель в дельте **один** — `.claude/rules/edit-kind-obligations.md`;
точек вставки в нём две (`@@ -148,5 +148,15 @@` и `@@ -235 +245 @@`). Прочие шесть
членов дельты — текст о работе захода-предшественника.

| Род | Что ввёл заход | Исход |
|---|---|---|
| `снятие` | снят ложный счёт пассажей-домов — «родов семь, а пассажей-домов девять, два рода описаны двумя пассажами каждый» — и производный от него буллет «ни одного из девяти» | свип по снятому термину исполнен (блок ниже): единственное корпусное попадание — цитата снятой редакции в самом носителе, то есть предписанная форма записи опровергнутого. **Записи в реестре `tools/retired-check.py` не заводится, и это названный отказ:** снято **число**, а не действующая редакция политики — политика «пассажи держат разные обязанности одного рода, и правило не отменяет ни одного» осталась дословно, и носителю нечем «жить по старой редакции». Тот же класс отказа, что у рода `снятие` узла 6 |
| `дом` | — | **не играет**: дом словаря родов (§«Словарь родов — закрытый» того же файла) и дома семи родов существовали до захода и не переносятся; заход не заводит, не переносит и не делит ни одного дома |
| `величина` | — | **не играет**: ни поля, ни колонки, ни операнда, ни тропы чтения заход не вводит |
| `конструкция` | — | **не играет**: ни формулы, ни предиката, ни инварианта заход не меняет. Закрытость словаря, обязанности родов и признак корпусного захода правкой не тронуты — снято утверждение **о числе пассажей**, а не конструкция |
| `радиус` | — | **не играет**: объём действия правила не двигается. Обязанности по-прежнему адресуют всякий корпусный заход, а словарь — те же семь родов |
| `кардинальность` | — | **не играет**: кардинальность «род → пассажи-дома» заходом не меняется — она и была «у трёх родов по два»; заход приводит **утверждение** о ней к фактике |
| `клейм` | изменён клейм полноты, выведенный из колонки дома таблицы словаря: счёт пассажей снят, а не поправлен | отдельный проход по носителям с клеймом исполнен: свип ниже даёт по корпусу единственное попадание — цитату снятого в самом носителе. Три живых отчёта `work/progress/**` число повторяют — они **текст о работе**, описывают прошлое и ретро-правке не подлежат (`.claude/rules/curation.md` §«Чек-лист свипа») |

**Свип снятого термина и клейма — одна команда на оба рода, термин у них один:**

```bash
export LC_ALL=C.UTF-8
grep -rn -oUzP '(?s)пассажей-домов\s+девять|девяти\s+пассажей|Родов\s+семь' docs/ .claude/rules .claude/processes .claude/skills .claude/decisions .claude/agents .claude/templates .claude/work/backlog.md .claude/work/questions tools/ --include=*.md --include=*.py --include=*.json | tr '\0' '\n'
```

| Член выдачи | Исход |
|---|---|
| `.claude/rules/edit-kind-obligations.md` — `пассажей-домов девять` | **не требует правки**: это цитата снятой редакции внутри пассажа, который её снимает, — предписанная форма записи опровергнутого |
| **Итого** | 1 |

**Полнота по месту правки — четыре пробы окрестности на обеих точках вставки:**

| Точка вставки | О1 окно | О2 анафоры | О3 утверждения о предмете | О4 рамка источника |
|---|---|---|---|---|
| пассаж о словаре родов (строка 148) | абзац до — «Новый род вводится правкой этой таблицы, и такая правка сама рода `клейм`»; абзац после — заголовок §«Форма классификации». Оба прочитаны целиком: первый утверждает обязанность поимённого прохода при вводе рода, второй вводит форму таблицы — вставка не делает ложным ни одно из двух и связки «лид ↔ таблица» не рвёт, потому что стои́т **после** таблицы словаря | выдача по файлу — 10 попаданий; адресность вставка двигает у одного: «колонки дома таблицы **выше**» — анафора введена самой вставкой и разрешается в таблицу словаря над ней. Прочие девять (строки 39, 77, 90, 107, 207, 230, 241, 296, 339) адресуют словарь, предыдущий заход, соседнюю норму и чужие пассажи — ни одно не указывает на правленый пассаж | греп термина `пассаж`-форм по файлу — 5 попаданий: три в новом тексте, одно в буллете «Не отменяет ни одного пассажа-дома» (правится этой же вставкой), одно в §«Что правило снимает» («идёт как написание пассажа») — о жанре правки, не о счёте, и вставкой не двигается | проба пуста: текст правки оборотов черновика не несёт — греп шаблона рамки источника по тексту правки даёт код 1 |
| буллет §«Чего правило НЕ означает» (строка 245) | абзац до — заголовок раздела; абзац после — буллет «Не делает читающий заход корпусным»; оба остаются истинными: правка снимает из буллета число, его утверждения («что обязан сделать род, говорит его дом») не трогая | в буллете анафор нет; выдача по файлу разобрана строкой выше — она общая на обе точки | тот же греп: буллет и есть одно из пяти попаданий, и правится этой вставкой | проба пуста |

**Пробы инструментов после правки:** `py tools/self-count-check.py`,
`py tools/edit-kind-check.py`, `py tools/anchor-check.py`,
`py tools/retired-check.py`, `py tools/command-carrier-check.py` — по нулю.

### Предсостояние статуса: восемь оживителей срабатывают на `DONE`

**Гейт зелен, и это разрешение проставить статус, а не сама простановка.**
Команда гейта с подменой статуса прогнана **до** записи, как и предписано
(`.claude/rules/backlog-section-form.md` §«Что мерит прогон»):

```bash
py tools/backlog-check.py --статус 2-10=DONE
```

Код 1, и это **ожидаемый** исход, а не дефект: восемь секций ждали именно этого
статуса и обязаны получить исход тем же ходом, что и он.

| Секция `.claude/work/backlog.md` | Условие |
|---|---|
| «Команды в клетках таблиц у живых отчётов» | `шаг:2-10:DONE` |
| «Энфорсер конвенции путей внешней поверхности» | `шаг:2-10:DONE` |
| «Энфорсер оси владельца-сервиса» | `шаг:2-10:DONE` |
| «Проза пришедшей редакции в записях реестра снятых редакций» | `шаг:2-10:DONE` |
| «Корневые build-файлы вне области энфорсера снятых редакций» | `шаг:2-10:CODE` |
| «Энфорсер окрестности точки вставки — по снятии заморозки усиления измерения» | `шаг:2-10:CODE` |
| «Снятый термин через javadoc-перенос: энфорсер слеп к кодовому носителю» | `шаг:2-10:CODE` |
| «Энфорсера указателя на дом перечня у компонента содержимого нет» | `шаг:2-10:CODE` |
| **Итого** | 8 |

**Четыре из восьми ждут не `DONE`, а `CODE`, и это не описка условия.** Шаг 10 в
`CODE` входил и вышел с аппрувом; условие читает **нынешний** статус строки шага,
поэтому с переходом в `SYNC_DOCS_FROM_CODE` оно держаться перестало и вернётся
записью `DONE` (`:CODE` держится и на `DONE`). Заморозка усиления измерения, под
которой эти четыре припаркованы, снята **входом шага в код** и сегодня не
действует — то есть задачи открыты, а условие их только и ждало следующего
статуса.

**Одна из восьми несёт срок, привязанный к самому переходу.** «Команды в клетках
таблиц у живых отчётов» требует погашения **до** переноса отчётов шага в
`history/`: область энфорсера — `.claude/work/progress/**`
(`tools/command-carrier-check.py`, `SCAN_REL`), все двадцать одна строка
`tools/command-carrier-debt.txt` адресуют отчёты шага 10, и после переноса прогон
упадёт на строках долга, которым в области больше нет соответствия. Перенос же
предписан тем же ходом, что и `DONE`
(`.claude/rules/closed-work-transfer.md` §Roadmap).

**Отсюда порядок, а не развилка:** погашение долга и исходы восьми секций идут
**раньше** записи статуса и переноса артефактов. Прогон гейта своего разрешения
этим не отзывает — он называет, из чего состоит следующий ход.

### Исход второго прогона

**ЗЕЛЁНЫЙ ПО КОРПУСУ.** Все шесть предметов гейта зелены у шести узлов и двух
концепт-проходов; К1 и К2 первого прогона сняты и перепроверены **пробами на
нынешней редакции**, а не по прошлому вердикту.

**С6 не сработал** — его порог есть красный второй прогон **по корпусу**
(`.claude/skills/stagnation-detection.md` §«С6 — ряд прогонов гейта выхода не
сходится»), а этот зелен. Ряд корпусных находок прогонов гейта — **2 → 0**.

**Находок у прогона две, и ни одна не держит статуса.** **Г1** (ложный счёт
пассажей-домов) закрыта правкой этого же прогона — довод в §«Находка прогона Г1
— ложный счёт пассажей-домов, правлен на месте». **Г2** (ось 9 энфорсера команд
слепа к `read` с флагом) **припаркована** с домом, оживителем и причиной:
`.claude/work/backlog.md` §«Ось 9 энфорсера команд слепа к `read` с флагом».

| Находка | Исход |
|---|---|
| Г1 | **закрыта**: счёт снят, пассаж называет предмет без числа и называет снятую редакцию; свипы обоих сыгранных родов исполнены |
| Г2 | **припаркована**: правка энфорсера требует своей падающей пробы и контроля — своя единица |
| **Итого** | 2 |

**Статус этой единицей не проставляется.** Запись `DONE` тянет за собой
обязанности отдельной единицы: исход восьми сработавших секций бэклога, погашение
долга `tools/command-carrier-debt.txt` **до** переноса отчётов и сам перенос
артефактов шага в `history/`. Единица работы сессии — одна
(`.claude/rules/session-work-unit.md`), и прогон гейта назван ею целиком.

**Следующая единица — переход шага 10 в `DONE`**: погашение долга клеток-носителей,
исход восьми секций, запись статуса, ролляп фазы, перенос артефактов.

## Связи

- Вход — `.claude/work/progress/phase-2-step-10-docs-check-6.md`.
- Процесс — `.claude/processes/roadmap-step-execution.md` §3.
- Популяция — `.claude/skills/closure-population.md`.
- Хроника шага — `.claude/work/progress/phase-2-step-10-chronicle.md`.
