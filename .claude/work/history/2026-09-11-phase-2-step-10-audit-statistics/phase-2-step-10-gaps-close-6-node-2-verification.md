# Верификация доработки узла 2 `GAPS_CLOSE_6`

## На какой вопрос отвечает этот файл

Чиста ли доработка узла 2 прогона `DOCS_CHECK_6` шага 10 фазы 2.

## Вход и режим

Прочитано целиком: отчёт критика
`phase-2-step-10-gaps-close-6-node-2-critique.md` (К1-К16); отчёт закрытия
`phase-2-step-10-gaps-close-6-node-2.md`; `.claude/rules/policy-home.md`,
`.claude/rules/carrier-levels.md`, `.claude/rules/self-description-form.md`,
`.claude/rules/edit-kind-obligations.md`, `.claude/skills/closure-population.md`,
`docs/concept.md` (П1, П3), `.claude/rules/docs-loop-limits.md`,
`.claude/rules/parking-address.md`.

Дельта доработки выведена по метке `1ee7c42a41056cacfa26eec6d60e93bd90e38eb0`
и прочитана **по каждому носителю целиком**:

```bash
git diff --name-only 1ee7c42a41056cacfa26eec6d60e93bd90e38eb0
git diff 1ee7c42a41056cacfa26eec6d60e93bd90e38eb0 -- <файл>
```

Двенадцать носителей: `.claude/work/backlog.md`, отчёт узла,
`docs/architecture/contracts.md`, `docs/components/AnomalyJob.md`,
`docs/components/HoldService.md`, `docs/components/SafetyHoldCoordinator.md`,
`docs/lifecycles/Deal.md`, `docs/rules/error-handling-policy.md`,
`docs/rules/exchange-hold.md`, `docs/rules/manual-halt.md`,
`docs/spec/manual-halt.json`, `tools/retired-check.py`.

Сверх дельты прочитаны исходники `HoldRungEdgeService.java`,
`HoldService.java`, `SafetyHoldCoordinator.java`, `AnomalyReportService.java`,
`DealStatusEdgeService.java`, `DealOrchestratorJob.java`,
`DealActiveHandler.java` и носители `docs/rules/instrument-hold.md`,
`docs/models/domain/aggregate/Deal.md`, `docs/concept.md`.

Режим — **верификация**, предмет у́же раунда критики: закрыта ли каждая из
К1-К12, не внесла ли доработка новых дефектов корпуса, держатся ли клеймы
отчёта. Правок в корпус не вносилось.

Оговорка соблюдена: носитель, помеченный «заводится этим закрытием», по
факту несуществования дефектом не назван.

## Закрытие находок критики

| Находка | Исход | Чем подтверждено |
|---|---|---|
| К1 (различитель ветви A9 ложен) | **закрыта** | `HoldService.md:161-172` — «**По журналу эта пара НЕ РАЗЛИЧИМА, и это названный предел наблюдаемости.** Ту же пару … производит и **поглощение**»; ложный различитель («отличает её по отсутствию факта подъёма … второго различителя у него нет») из строк 153-156 снят. `manual-halt.md:482-487` приведён к тому же исходу и сам называет строку таблицы второй производительницей: «даёт строка таблицы выше «постановка, **повтор той же ступени**, поднятой **автоматикой**»». Наблюдатель припаркован с домом, владельцем и оживителем (`backlog.md:2744`) — состав `parking-address.md` §«Признак верного дома» соблюдён |
| К2 (сосед-образец несёт снимаемую конструкцию) | **закрыта по существу** | `docs/lifecycles/Deal.md:157-158` — колонка «Писатель и ребро» больше не несёт обработчика писателем: «ребро статуса прохода, `ACTIVE → EXIT_PENDING`; затребователь у него один — обработчик активной сделки …». Клейм «уже решён у соседей» из `contracts.md` снят вместе со слоем опровержения. **Правка породила В3** — слова ролей в ней расходятся с закреплёнными |
| К3 («объявленная» тропа никем не объявлена) | **закрыта** | `HoldService.md:61-72` — «**Построенной такой тропы нет ни одной, и объявленной тоже нет ни одной**», развилка названа открытой: «**Пойдёт ли он через ребро или мимо — не решено**», дом развилки заведён (`backlog.md:2789-2801`). `exchange-hold.md:320-322` — «ни одного такого не построено и **ни одного не объявлено**». **Адрес дома развилки при этом сломан В1** |
| К4 (три указателя ведут в пассаж без инварианта) | **закрыта** | все три переведены на `docs/rules/error-handling-policy.md` §«Идемпотентность реакции и идемпотентность отчёта — разные ключи»: `contracts.md:79`, `AnomalyJob.md:24-25`, `SafetyHoldCoordinator.md:163-164`. Целевой пассаж инвариант несёт — новая строка таблицы (`error-handling-policy.md:219`) |
| К5 (инвариант заведён в доке деталей при живом доме) | **закрыта** | инвариант перенесён в дом: `error-handling-policy.md:219` — «**факт подъёма ступени** (событие `HoldRaised`) — тот же анкер … не публикуется»; лид правила (206-210) правлен тем же ходом. В `HoldService.md:189-192` остался указатель («строка таблицы дома называет это наравне с прочими ключами»), формулировка не повторяется |
| К6 (два значения у слова «применитель») | **закрыта в четырёх названных точках** | `contracts.md:88-90` — «**применяет** ребро ровно один класс — сам класс ребра; **затребуют** его тропы»; клетка `HoldRaised` — «и оба **затребуют** одно ребро»; `HoldService.md:111-112` — «ребро **применяет** только его собственный класс, а тропы его **затребуют**»; `HoldService.md:42-43` не менялся и словарю не противоречит. **Пятая точка, заведённая тем же заходом, словарь нарушает — В3** |
| К7 (клейм о колонке ложен на `HoldReleased`) | **закрыта** | клетка переписана: писатель — «**ребро снятия ступени** — заводится ходом, который заведёт класс»; ссылки на `HoldService.md` в колонке писателя больше нет, противоречие с §Границы снято прямой оговоркой «Общего исполнителя блокировки снятие не проходит вовсе». Форма объявлена клаузой `contracts.md:93-97` |
| К8 (популяция записи реестра неполна) | **закрыта** | `tools/retired-check.py` — добавлена строка популяции на `docs/rules/manual-halt.md`; `py tools/retired-check.py` зелен, строк популяции проверено 388, дефектов 0 |
| К9 (слой опровержения в продуктовом корпусе) | **закрыта** | оба названных пассажа сняты: `contracts.md` («Прежняя редакция ставила в колонку затребователей…») и `HoldService.md` («Прежняя редакция повторяла оба примера дома дословно…»). Третье вхождение в `HoldService.md:196` предшествует метке `c4b78ac` (проверено чтением носителя на метке) — предметом узла не является |
| К10 (висячая анафора «названная тем же пассажем») | **закрыта** | `HoldService.md:134-137` — «первым её членом станет тропа, которая ступень запросит мимо ребра, — такой ни построено, ни объявлено (§«Писатель статуса вне этого перечня законен, но в дереве его нет»)»; §-имя возвращено, `anchor-check` разрешает |
| К11 (область объявлена трижды по-разному) | **закрыта** | `error-handling-policy.md:262-283` — один различитель: «**Журнальный носитель — тот, что лежит ВНЕ транзакции решения.** … Признак один, и он механический»; «именованное исключение» снято — «Строка outbox под клаузу не подпадает — **по тому же признаку, а не как исключение из него**». Признак проверен деревом: `AnomalyReportService.journalState` — `@Transactional`, координатор транзакции не открывает. **Правка породила В2** |
| К12 (лид обещает больше, чем даёт список) | **закрыта** | `HoldService.md:113-115`, шаг 3 — «держит её координатор, и **на мягкой тропе шага нет вовсе**: эскалация — предмет полной реакции». Верно по дереву: `HoldService.raiseSoft` — журнал плюс ребро, эскалации нет |
| К13 (свипы С1/С2/С4 объявленного не мерят) | **закрыта частично** | Д1-Д3 пересобраны кириллице-совместимо и выдачу дают. **Д4 класс воспроизводит:** из трёх носителей, объявленных «предметом узла», опубликованная команда находит один — В6. Корпус по исправленному свипу чист |
| К14 («три в донорских копиях») | **не закрыта** | отчёт:395 по-прежнему «пять в `services/` и **три** в донорских копиях»; опубликованная команда даёт **две**. Диспозиция утверждает «число снято» — В5 |
| К15 (прогон реактора без команды) | **закрыта частично** | предъявлена javadoc-only-дельта **доработки** (`git diff --name-only … -- donor/ services/ libs/`, выдача пуста). Клейм «**Реактор** прогнан на состоянии первой правки» по-прежнему без команды и выдачи — В8 |
| К16 (состояние `приземлён` вне словаря) | **закрыта разбором** | разбор верен: `приземлён` — слово словаря **состояний узла** (`.claude/processes/roadmap-step-execution.md:546` — «состояний у узла три, а не два — приземлён, в работе, остановлен»), а закрытый словарь `self-description-form.md` адресует исход захода. Клейм «смешение названо в раскладке явно» не держится: раскладка заходом не тронута — В7 |

**Итог по блокирующим:** К1-К12 закрыты по существу — двенадцать из
двенадцати. Три из закрытий (К2, К6, К11) внесли новые корпусные дефекты, и
они разобраны ниже как самостоятельные находки, а не как незакрытые
предшественники.

## Новые находки доработки

### В1. Две новые `##`-секции вставлены в середину чужой секции бэклога, и хвост её переехал под них — вместе с домом развилки, который тот же заход объявил в `docs/**`

- **Что не так.** Заход вставил в `.claude/work/backlog.md` два раздела
  первого уровня — `## Решётка в первой колонке ограждённого блока читается
  инструментами как заголовок` (строка 2702) и `## Ветвь «отчёт есть,
  ступень не поднята» наблюдателя не имеет` (строка 2744). Точка вставки
  лежит **внутри** `## Шаг 7 (сделки и P&L) — исполнительный хвост` и внутри
  его подраздела `### CODE-дельта шага`. На метке секция занимала строки
  1579-3074 (`git show 1ee7c42…:.claude/work/backlog.md | grep -n '^## '` —
  следующий `##` был 3075); после вставки она кончается на 2701, а весь её
  хвост — **«CODE fee-wiring (N9)» (2770), «CODE узла холда», `### Грунт
  integrator для шага 7` (3043), `### Рантайм-верификация и форвард`
  (3064)** — стои́т под новым `##`-заголовком «Ветвь «отчёт есть, ступень не
  поднята» наблюдателя не имеет».

  Тем же заходом в продуктовый корпус внесён указатель, ставший от этого
  ложным: `docs/components/HoldService.md:69-70` — «дом решения — позиция
  «CODE fee-wiring» в `.claude/work/backlog.md` §«Шаг 7 (сделки и P&L) —
  исполнительный хвост»». Позиция в этой секции больше не лежит.
- **Почему дефект.** `.claude/rules/policy-home.md` §«Заведение и перенос
  дома — не дополнение»: «Носитель, ведший за предметом к прежнему дому,
  после правки ведёт **не туда**, и ни один прогон этого не ловит: старый
  раздел на месте, адрес цел, неверен только адресат». Здесь переехал не
  указатель, а **предмет**, и исход тот же. `py tools/anchor-check.py` зелен
  — он мерит разрешимость заголовка, а не принадлежность позиции разделу.

  Проба О1 (`closure-population.md`, окно точки вставки) заведена ровно
  против этого; отчёт объявляет её сработавшей: «`backlog.md` … окна
  прочитаны, вставки точечные» (§«Окрестность точек вставки доработки»,
  последняя строка). Вставка не точечная — она рвёт связку `##`-раздела с
  373 строками его собственного содержания.

  Второе следствие того же места: `.claude/rules/parking-address.md`
  требует дом, «который её оживит»; парковка К1 заведена соседом хвоста
  шага 7, хотя её оживитель — «заход по проактивной детекции ядра», у
  которого в бэклоге своя живая секция (`### Хвост шага 8 (safety /
  AnomalyJob)`, строка 1679).
- **Блокирует ли приземление** — **да (корпус)**: рабочий файл `.claude/**`
  плюс ложный указатель в `docs/**`.
- **Что сделать.** Перенести обе секции за пределы §«Шаг 7 (сделки и P&L) —
  исполнительный хвост» (после её конца либо в место по оживителю) — тогда
  `HoldService.md:69-70`, `backlog.md:405`, `backlog.md:1451`,
  `.claude/tests/source-api/okx/code-preconditions.md:22` и `plan.md:2335`
  снова ведут к тому, что называют.

### В2. Указатель называет у дома «именованное исключение», которое тот же заход из дома и убрал

- **Что не так.** `docs/components/HoldService.md:147-150`: «Факт подъёма
  выведен из-под клаузы «журнал реакцию не гейтит» — дом клаузы и её
  **именованное исключение** — `docs/rules/error-handling-policy.md`
  §«Отказ журнального носителя реакцию не гейтит»».

  Целевой пассаж после доработки говорит обратное
  (`error-handling-policy.md:279-280`): «**Строка outbox под клаузу не
  подпадает — по тому же признаку, а не как исключение из него.**» Прежняя
  редакция дома («**Именованное исключение одно** — durable-факт, лежащий в
  транзакции решения») снесена этим же заходом при закрытии К11.
- **Почему дефект.** Читатель, пришедший по указателю за «именованным
  исключением», находит пассаж, отрицающий его существование: клейм о
  содержимом соседа ложен. `.claude/rules/carrier-levels.md`: стыковая
  истина, читаемая с двух сторон, живёт один раз — здесь два конца называют
  её по-разному. Свип рода `радиус` (Д3) эту строку в выдаче **имел**
  (`docs/components/HoldService.md:148`, `:150` — проверено прогоном
  команды), но в разбор её не взял: отчёт делит выдачу на «пять javadoc
  ядра» и «два класса чужих смыслов», а строка не попадает ни в один из
  трёх.
- **Блокирует ли приземление** — **да (корпус)**.
- **Что сделать.** Снять слова «и её именованное исключение»: «дом клаузы —
  `docs/rules/error-handling-policy.md` §«Отказ журнального носителя реакцию
  не гейтит»».

### В3. Слово «применяет», закреплённое этим же заходом за классом ребра, в `docs/lifecycles/Deal.md` отдано вызывающему — и роли переставлены местами

- **Что не так.** `docs/lifecycles/Deal.md:157` (внесено доработкой):
  «ребро статуса прохода, `ACTIVE → EXIT_PENDING`; затребователь у него один
  — обработчик активной сделки (`docs/components/DealActiveHandler.md`), а
  **применяет ребро проход оркестратора**
  (`docs/components/DealOrchestratorJob.md`)»; строка 158 — «то же ребро,
  тот же затребователь».

  Расходится с тремя носителями сразу:
  - словарь, закреплённый тем же заходом
    (`docs/architecture/contracts.md:88-89`): «**применяет** ребро ровно
    один класс — сам класс ребра; **затребуют** его тропы, которые до него
    доходят»;
  - строки 3-4 **той же таблицы** (заходом не тронуты): «затребователей у
    него два — первый ход при подъёме ступени (`SafetyHoldCoordinator`) и
    **шаг прохода оркестратора** (`DealOrchestratorJob`)» — тот же
    оркестратор назван затребователем;
  - дерево: `DealOrchestratorJob:377` — `dealStatusEdgeService.applyPassEdge(…)`,
    то есть оркестратор ребро **зовёт**; применяет его
    `DealStatusEdgeService:79`. `DealActiveHandler` ребра не зовёт вовсе —
    он возвращает `DealTransition.collapse(…)` (строки 95, 162; грепом по
    файлу вхождений `dealStatusEdgeService` ноль), значит затребователем в
    смысле словаря он как раз не является.
- **Почему дефект.** Предмет узла — «носитель называет писателя
  подлежащим», и К6 закрывалась вводом одного слова на роль. Заход слово
  ввёл и тут же употребил его в противоположном значении — в носителе,
  который сам же объявлял образцом (К2). Клейм колонки, объявленный
  `contracts.md`, на этой строке не держится
  (`.claude/rules/edit-kind-obligations.md`, род `клейм`: перечень, из
  которого выведен клейм, проходится поимённо).
- **Блокирует ли приземление** — **да (корпус)**.
- **Что сделать.** Привести строку к словарю: применяет ребро
  `DealStatusEdgeService`, затребует — шаг прохода оркестратора
  (`DealOrchestratorJob`), а причину выхода решает обработчик активной
  сделки (`DealActiveHandler`); либо назвать обе роли теми же словами, что
  строки 3-4.

### В4. §-адрес парковки написан без закрывающей кавычки

- **Что не так.** `.claude/work/backlog.md:2756-2757`:
  «`docs/rules/manual-halt.md` §«Наблюдаемость: ручное отличимо и от
  автоматики, / и друг от друга`)» — открывающая кавычка есть, закрывающей
  нет, вместо неё обратная кавычка.
- **Почему дефект.** `.claude/rules/structure.md` §«`§«…»` адресует
  именованный пассаж, а не только заголовок»: формы адреса объявлены, и
  адрес вне объявленных форм детектором не мерится — обещание
  «переименование ловится прогоном» на нём не держится.
  `py tools/anchor-check.py` зелен именно потому, что кавычечную форму здесь
  не опознаёт.
- **Блокирует ли приземление** — **да (корпус)**, слабейшая из четырёх:
  правка в один символ.
- **Что сделать.** Закрыть кавычку.

### В5. К14 объявлена закрытой, число не снято

- **Что не так.** Диспозиция: «К14 — **закрыта**: число снято, форма —
  указатель на команду». Отчёт, строка 395: «доковых носителей у клаузы было
  ноль, кодовых — пять в `services/` и **три** в донорских копиях».
  Опубликованная команда прогнана дословно и даёт **два** донорских файла
  (`DealTerminalFeaturesWriter.java`, `RefreshBalanceExecutor.java`).
- **Блокирует ли приземление** — нет (текст о работе).
- **Что сделать.** Снять число либо поставить два.

### В6. Свип Д4 двух из трёх своих носителей не находит — класс К13 воспроизведён

- **Что не так.** Отчёт, §«Свипы доработки», Д4: «Предмета узла касаются
  **три** вхождения, все правлены: `exchange-hold.md` («всех писателей
  статуса»), `SafetyHoldCoordinator.md` …, `HoldService.md` (квантор)».
  Опубликованная команда прогнана дословно: в выдаче девять строк, и
  `exchange-hold.md` с «всех писателей статуса» в них нет (в носителе стои́т
  жирная разметка внутри оборота — шаблон рвётся), `HoldService.md` нет тоже
  (в носителе «ни одна из» и «них» разделены переносом строки). Из трёх
  объявленных выдача даёт один.
- **Почему это не блокирует.** Проба доведена, как требует
  `.claude/rules/docs-loop-limits.md`: исправленный свип
  `grep -rlUzP '(?s)ни\s+одна\s+из\s*\**\s*них|(?s)\**всех\**\s+писател|(?s)двумя\s+разными\s+носителями|(?s)перечень\s+закрыт' docs/`
  даёт восемь носителей, все прочитаны — `HoldService.md` (квантор сведён с
  соседями), `exchange-hold.md:316` (перечень приведён),
  `SafetyHoldCoordinator.md` (два вхождения), `aggregate/Deal.md` (статус
  **сделки**), `DealActiveHandler.md`, `market-data-freshness.md`,
  `strategy-step-once-per-episode.md`, `spec/pnl-reconciliation.json` — все
  четыре последних о других предметах. Корпусного пропуска нет.
- **Блокирует ли приземление** — нет (текст о работе).
- **Что сделать.** Заменить шаблон Д4 на терпимый к разметке и переносу
  строки и переписать выдачу; клейм «три вхождения, все правлены» вывести из
  неё, а не из памяти.

### В7. Клейм «смешение названо в раскладке явно» не держится

- **Что не так.** Диспозиция К16: «закрыта разбором … смешение названо в
  раскладке явно». `git diff --stat 1ee7c42… -- .claude/work/progress/phase-2-step-10-gaps-close-6.md`
  — выдача пуста, файл заходом не тронут; в нём по-прежнему стои́т голое
  «Состояние узла — `приземлён`» (строка 121) без оговорки о двух словарях.
  Сам разбор при этом верен (`roadmap-step-execution.md:546`).
- **Блокирует ли приземление** — нет (текст о работе).
- **Что сделать.** Либо внести оговорку в раскладку, либо снять клейм.

### В8. К15 закрыта наполовину

- **Что не так.** Отчёт, §«Чем предъявлена доработка»: «**Реактор** прогнан
  на состоянии первой правки …; доработка дерева кода не трогала, и это
  предъявлено:» — предъявлена вторая половина (команда
  `git diff --name-only … -- donor/ services/ libs/`, выдача пуста),
  первая осталась клеймом без команды и выдачи — ровно то, о чём К15.
- **Блокирует ли приземление** — нет (текст о работе).
- **Что сделать.** Привести команду и выдачу реактора либо снять клейм.

### В9. Носительская половина К2 в отчёте не закрыта

- **Что не так.** К2 называла два дефекта: ложный клейм в корпусе и то, что
  носительская ось узла не накрыла `docs/lifecycles/Deal.md`, хотя он
  выпадает в первой же её команде. Корпус правлен; таблица §«Ось
  носительская» отчёта строки на этот носитель по-прежнему не имеет (грепом
  `lifecycles/Deal` по отчёту — вхождения только в §«Уровень пробела»,
  §«Дельта захода», §«Окрестность», §«Свипы», §«Диспозиция»).
- **Блокирует ли приземление** — нет (текст о работе).
- **Что сделать.** Внести строку в таблицу носительской оси.

## Команды отчёта, прогнанные дословно

| Команда отчёта | Совпала ли выдача с тем, что отчёт утверждает |
|---|---|
| Д1 — свип по конструкции поглощения | да — 37 носителей (16 доков, 21 файл дерева кода); клейм «заметно больше пяти» держится, перечень прочитанных доков совпал с доковой половиной выдачи |
| Д2 — свип кардинальности | да — ровно пять файлов, имена совпали дословно (`HoldService.md`, `exchange-hold.md`, `manual-halt.md`, `aggregate/Deal.md`, `strategy-validation.md`) |
| Д3 — свип радиуса | **нет** — выдача содержит `docs/components/HoldService.md:148` и `:150`, которых разбор отчёта не относит ни к «пяти javadoc», ни к «другим смыслам», ни к «носителю наблюдаемости П3»; там живёт В2. Число javadoc (пять) верно |
| Д4 — свип клеймов полноты | **нет** — из трёх носителей, объявленных предметом узла, выдача даёт один (`SafetyHoldCoordinator.md`); В6. Исправленный свип прогнан, корпус чист |
| §«Чем предъявлена доработка» — `git diff --name-only 1ee7c42… -- donor/ services/ libs/` | да — выдача пуста |
| §«Исход раунда критики» — счёт совпадений шаблона с `\w` | да — `0`, код возврата 1 |
| §«Исход раунда критики» — счёт совпадений шаблона с `[а-яё]` | да — `1`, код возврата 0 |
| §«Исход раунда критики» — греп `applyPassEdge` по дереву ядра | да — три строки: javadoc, определение, единственная точка вызова (`DealOrchestratorJob:377`) |
| команда H3 первой правки (донорские копии клаузы) | **нет** — два донорских файла, отчёт говорит три; В5 |
| замер в новой секции бэклога (решётка в ограждённом блоке) | да — 830 вхождений в **34** носителях; «в трёх десятках носителей» держится |
| §«Отправная метка» доработки — `git diff --name-only 1ee7c42…` | да — двенадцать носителей, таблица дельты совпала по именам |

## Гейт инструментов корпуса

| Команда | Код возврата |
|---|---|
| `py tools/anchor-check.py` | 0 |
| `py tools/retired-check.py` | 0 (носителей 2149, записей 126, строк популяции 388, дефектов 0) |
| `py tools/doc-pointer-check.py` | 0 (указателей 2421, битых 0) |
| `py tools/spec-pointer-check.py` | 0 (280 указателей, ведут не в дом — 0) |
| `py tools/population-derive-check.py` | 0 (осей 18, перечни сошлись) |
| `py tools/self-count-check.py` | 0 (носителей 550, блоков 38, дефектов 0) |
| `py tools/edit-kind-check.py` | 0 (корпусных заходов 45, дефектов 0) |
| `py tools/command-carrier-check.py` | 0 (клеток с командой 13, дефектов 0) |
| `py tools/code-gate-check.py` | 0 (позиций 42, критерий пройден) |
| `py tools/deploy-layout-check.py` | 0 (единиц 8, расхождений 0) |
| `py tools/spec-scope-check.py docs/spec` | 0 (A/B/C/D — по нулю) |
| `bash tools/spec-run.sh` | 0 (29 спецификаций, 694 примера, все сошлись) |

Кода 2 («не измерялось») нет ни у одной. Ни одна из четырёх корпусных
находок прогонами не мерится — все три механизма названы в самих правилах
как немеряемые (адресат указателя, слово роли, принадлежность позиции
разделу).

## Что проверено и расхождения не дало

- **Новый признак клаузы журнала верен по дереву.**
  `AnomalyReportService.journalState` помечен `@Transactional` и пишется
  своей транзакцией; `SafetyHoldCoordinator.react*` транзакции не
  открывает — значит `AnomalyReport` действительно лежит вне транзакции
  решения на обеих тропах, и «Признак один, и он механический» держится.
- **Строка таблицы дома для факта подъёма не расходится с соседями.**
  `error-handling-policy.md:219`, `contracts.md:79`, `AnomalyJob.md:22-25`,
  `SafetyHoldCoordinator.md:158-164`, `HoldService.md:189-194`,
  `spec/manual-halt.json` нота `reportProduced` говорят одно и то же;
  `bash tools/spec-run.sh` зелен.
- **Старый признак радиуса из корпуса вычищен полностью:** греп по обороту
  «не существовало бы» даёт два вхождения, оба о другом
  (`manual-halt.md:604`, `ReceptionPairMoments.java:45`).
- **Указатели на дом клаузы разрешимы и ведут в живой раздел:**
  `HoldService.md:150` плюс пять javadoc ядра —
  `error-handling-policy.md:253`.
- **К9 не расширен:** третье «Прежняя редакция» в `HoldService.md:196`
  существовало на метке `c4b78ac` (проверено `git show`), заходом не
  внесено.
- **Парковка К1 несёт все три величины** `parking-address.md`: дом (секция
  бэклога), оживитель («заход по проактивной детекции ядра»), причина
  («разводит их только состояние объекта, которого потребитель журнала не
  видит») — дефект у неё в **месте**, а не в составе (В1).
- **`docs/concept.md` П3 говорит ровно то, на что ссылается новый пассаж**
  (строка 200: «носитель наблюдаемости — персистентная запись, не лог»).
- **Заголовок §«Наблюдаемость: ручное отличимо и от автоматики, и друг от
  друга» существует** (`manual-halt.md:419`) — дефект В4 в форме адреса, не
  в цели.
- **Дефолт минимального следа не нарушен:** новых сущностей, полей и
  механизмов доработка не вводит; два новых раздела бэклога — парковка и
  названный класс, оба с оживителем.
- **Конструкция кода не тронута:** дельта доработки по
  `donor/ services/ libs/` пуста.

## Вердикт

**По корпусу доработка не чиста.** Двенадцать блокирующих находок критика
(К1-К12) закрыты по существу — каждая проверена чтением целевого пассажа, не
словом отчёта. Но три из этих закрытий внесли новые корпусные дефекты, и
ещё один пришёл от парковки:

- **В1** — две `##`-секции вставлены в середину §«Шаг 7 (сделки и P&L) —
  исполнительный хвост» бэклога; её хвост, включая позицию «CODE
  fee-wiring», переехал под новый заголовок, и указатель
  `docs/components/HoldService.md:69-70`, внесённый тем же заходом, ведёт не
  туда;
- **В2** — `docs/components/HoldService.md:148-150` называет у дома
  «именованное исключение», которое тот же заход из дома убрал;
- **В3** — `docs/lifecycles/Deal.md:157` отдаёт слово «применяет»
  вызывающему вопреки словарю, закреплённому этим же заходом в
  `docs/architecture/contracts.md`, вопреки строкам 3-4 собственной таблицы
  и вопреки дереву;
- **В4** — §-адрес парковки в `.claude/work/backlog.md:2757` без
  закрывающей кавычки.

Неблокирующих (текст о работе) — пять: **В5** (К14 объявлена закрытой,
число «три в донорских копиях» на месте), **В6** (свип Д4 двух из трёх
своих носителей не видит — класс К13 воспроизведён; исправленная проба
доведена, корпус чист), **В7** (клейм «смешение названо в раскладке» —
раскладка не тронута), **В8** (клейм о прогоне реактора без команды и
выдачи), **В9** (носительская половина К2 в отчёте не закрыта).

Гейт инструментов корпуса — двенадцать команд, все нули, кода 2 нет.

**Общее у В1-В3 названо:** все три — правка, верная по своему предмету и
ломающая соседа, о котором она знала. В1 знал позицию «CODE fee-wiring» — он
писал на неё указатель; В2 знал пассаж дома — он его и переписывал; В3 знал
словарь — он его и вводил. Это ровно тот класс, ради которого заведена ось
окрестности (`.claude/skills/closure-population.md`) и обязанности по роду
правки (`.claude/rules/edit-kind-obligations.md`): у доработки обе объявлены
исполненными, и на всех трёх точках они дали зелено.
