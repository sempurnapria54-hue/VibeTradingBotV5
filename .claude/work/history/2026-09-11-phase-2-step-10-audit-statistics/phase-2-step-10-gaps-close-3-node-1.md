# Узел 1 закрытия `GAPS_CLOSE_3` — ключ группировки пересчёта (A2, B4)

## На какой вопрос отвечает этот файл

Как закрыт узел 1 прогона `DOCS_CHECK_3` шага 10 фазы 2.

**Узел закрыт: пять правок внесены в корпус 2026-09-07.** Рамка —
`.claude/decisions/per-node-closure-frame.md`; чистота мерена **по
носителю** (поправка держателя по `PROC-Q5`, вариант D), рационал сведён к
проверяемому минимуму. Метод, которым узел вёлся, размещён четырьмя
конвенциями в `.claude/work/progress/phase-2-step-10-chronicle.md`
§«Четыре конвенции, введённые узлом 1 и обязательные для узлов 2-6» — они и
есть провенанс метода; хроники хода мини-петли этот файл не несёт.

## Правило узла

**Ключ, по которому запрос пересчёта группирует строки журнала, равен
ключу зерна той строки агрегата, которую этот запрос собирает.**

## Уровень пробела и род закрывающего действия

Уровень — **носитель** (`.claude/skills/classify-gap-level.md`): истина
верна в своих домах и разошлась с ними в двух пассажах соседнего раздела
**того же файла** (третий пассаж того же раздела верен, но домом не
является). Род действия — свести к домам: пассажи называют ключ **по
имени**, операндов не перечисляют.

**Признак установлен ЧТЕНИЕМ раздела целиком, а не прогоном.** После правок
`docs/rules/statistics-aggregates.md` §«Пересчёт — проекция, а не
накопитель» не содержит перечня операндов **ни одного** зерна **ни в одной
письменной форме**. Полного детектора письменных форм перечня узел не
строит и не объявляет: всякая его редакция — счётная и качественная —
оказывалась неверной. Прогон ниже вспомогательный: он предъявляет
исчезновение того перечня, который снимает правка 2, и о прочих формах не
высказывается.

```bash
PAT='(?s)тенант[а-яё]*[^.]{0,80}сч[ёе]т[а-яё]*[^.]{0,80}(определени|валют|сут)[а-яё]*'
awk 'NR>=288 && NR<=405' docs/rules/statistics-aggregates.md | LC_ALL=C.UTF-8 grep -oUzP "$PAT" | tr '\0' '\n'
LC_ALL=C.UTF-8 grep -oP '§(?!П[0-9])(?!`)' docs/rules/statistics-aggregates.md | wc -l
```

До правок (раздел 288-397) первая команда давала `тенантов, счетов,
определений и валют`; после правок (288-405) — **пустую выдачу**. Вторая
команда — долг адресных перекрёстий правимого файла: **9** до правок и
**9** после, правки его не растят.

**Пять пассажей полного перечня, из них три — дома; все пять установлены
чтением.** В `statistics-aggregates.md` перечень стои́т трижды: в двух
домах (§«Зерно строки» — сделочное; §«Счётчики происшествий — своё зерно,
а не строка сделочного агрегата» — происшествий) и в §Персистентность
(колонками, помеченными «ключ зерна»). Четвёртый —
`docs/architecture/data-ownership.md`, строка базы `statistics`; пятый —
`docs/spec/statistics-aggregates.json`, нота величины `openedDeals`.
**Корпусного прогона под этот перечень не приводится** — он держится
чтением, как и признак выше.

**Исход по двум операндоносным носителям вне дома назван, а не пропущен.**
`data-ownership.md` перечисляет операнды обоих зёрен, тогда как
`docs/architecture/services.md` (строка единицы `audit-statistics`) обходится
упоминанием без операндов, — то есть критерий
`.claude/rules/carrier-levels.md` («деталь несёт упоминание со ссылкой на
дом») применён к одному носителю из двух. **Правилом ЭТОГО узла копия
операндов не покрыта:** правило говорит о ключе **группировки запроса**, а
`data-ownership.md` о запросе не высказывается вовсе. Позиция передана
**узлу 5** («клеймы и копии носителей») явной строкой его перечня; здесь
она не закрывается и не замалчивается. Нота спеки `openedDeals` — в том же
классе и передана туда же.

## Что именно разошлось — три пассажа одного раздела

Все три живут в `docs/rules/statistics-aggregates.md` §«Пересчёт —
проекция, а не накопитель». Границы сняты прогоном:

```bash
awk 'NR>=304 && NR<=322 {printf "%d|%s\n", NR, $0}' docs/rules/statistics-aggregates.md
```

| Строки | Что сказано | Исход |
|---|---|---|
| 305 | «Запрос к `audit` группирует строки окна **по ключам зерна**» | **верно**; домом ключей не является. **Правлено** ради единственности формулировки |
| 310-312 | «объём результата ограничен произведением **тенантов, счетов, определений и валют**» | неполно: суток в произведении нет, второго зерна нет вовсе. **Правлено** |
| 315-321 | «группировка идёт **по четырём колонкам радиуса плюс расчётной валюте** из содержимого» | **ложно**. **Правлено** |

**Пересечение названного ключа с ключом сделочного зерна — три позиции из
пяти.** Четыре колонки идентичности журнала — счёт, инструмент, сделка,
стратегия; ключ сделочного зерна — тенант × счёт × определение × сутки ×
валюта. Общие — счёт, определение, валюта; названный ключ **включает**
инструмент и сделку, которых в зерне нет, и **не включает** тенанта и
суток, без которых зерна не существует.

**Цена односторонняя.** Группировка по сделке даёт строку **на сделку**, а
не на сутки; upsert по ключу зерна (`unique nulls not distinct` —
§Персистентность файла `docs/rules/statistics-aggregates.md`) такие строки
схлопывает — в строке суток останутся числа
последней записанной сделки. Выпущенные из названного ключа **сутки**
складывают разные дни в одну строку — то есть ровно ту ось, которой зерно
и меряется.

**Выпущенный тенант разбиения при этом НЕ меняет.** Счёт **функционально
определяет** тенанта, а счёт в названном ключе есть. Дом зависимости —
`docs/models/domain/core/ExchangeAccount.md` §Персистентность: у таблицы
`exchange_accounts` реестра `auth` `internal_id` объявлен `NOT NULL`
`UNIQUE`, а `tenant_id` — `NOT NULL`, то есть идентификатор счёта глобально
уникален и у каждого счёта ровно один тенант. **Ссылочного ограничения
счёт → тенант не объявляет ни один дом** (в коде оно есть —
`fk_exchange_account_tenant` миграции `auth`, — но
`.claude/rules/parking-address.md` §«Ограничение, живущее только в коде,
домом не является»).

**Схема журнала зависимость НЕ энфорсит.** В `audit_records` колонка
`exchange_account_internal_id` — nullable `varchar(64)` без ограничений
(`docs/models/domain/other/AuditRecord.md` §Персистентность): инвариант
живёт в **чужой базе**. Поэтому арм «выпал тенант» не наблюдаем не потому,
что схема журнала таких данных не допускает — она их допускает, — а потому,
что фикстура, кладущая один счёт под двух тенантов, противоречила бы
реестру `auth`, то есть проверяла бы охрану на состоянии, которого
производитель событий не порождает.

Цена выпущенного тенанта, стало быть, не в смешении тенантов, а в том, что
ключ перестаёт совпадать с объявленным зерном. Обнаружилось бы это не
отказом, а неверными числами у человека.

## Популяция правила

### Ось предметная A — операнды ключа каждого зерна

Члены выведены из домов, а не из правимого пассажа:

```bash
LC_ALL=C.UTF-8 sed -n '/^## Зерно строки/,/^## Что хранится/p' docs/rules/statistics-aggregates.md \
  | LC_ALL=C.UTF-8 grep -oP '^- \*\*\K[^*]+'
LC_ALL=C.UTF-8 grep -oP 'строка происшествий — «\K[^»]+' docs/rules/statistics-aggregates.md
```

```
тенант
счёт
определение стратегии
сутки UTC
расчётная валюта
тенант × биржевой счёт × сутки UTC
```

| Зерно | Операнд | Откуда берётся | Исход |
|---|---|---|---|
| сделочное | тенант | колонка `tenant_id` | **правлено**: пассаж 310-312 операнд называет, пассаж 315-321 его выпускает — правка сводит оба к имени ключа |
| сделочное | биржевой счёт | колонка `exchange_account_internal_id` | выполняется — назван обоими пассажами |
| сделочное | определение стратегии | колонка `strategy_internal_id` | выполняется |
| сделочное | сутки UTC | операнд объявлен §«Зерно строки»; **колонки, из которой он вычисляется, дом операнда не называет**. Соседний пассаж правимого раздела говорит «сутки берутся по моменту происшествия события», но он о **окне пересчёта**, а не о ключе, и домом операнда не является (X7, Y3) | **правлено**: выпущен **обоими** пассажами — 310-312 перечисляет тенантов, счета, определения и валюты без суток, 315-321 суток тоже не называет |
| сделочное | расчётная валюта | `content` события `DealClosed` | выполняется — приходит **из содержимого** (`docs/architecture/contracts.md`: содержимое несёт `internalId` предмета и его контекста). Счётного клейма о прочих операндах клетка не делает: он назначал бы колонку и суткам — назначение, от которого узел трижды отказался (X7, Y3, Z2) |
| происшествий | тенант | колонка `tenant_id` | **правлено**: второго зерна пассаж не упоминал вовсе |
| происшествий | биржевой счёт | колонка `exchange_account_internal_id` | **правлено**: то же |
| происшествий | сутки UTC | как у сделочного зерна: колонки, из которой вычисляется операнд, дом не называет | **правлено**: то же |

### Ось предметная B — все колонки таблицы журнала

Вторая половина находки — «ключ включает лишнее», и её популяция есть
**полный** состав колонок `audit_records`, а не подмножество колонок
идентичности. Перечень выводится из схемы:

```bash
LC_ALL=C.UTF-8 sed -n '/^## Персистентность/,/^### Вторая таблица/p' \
  docs/models/domain/other/AuditRecord.md | LC_ALL=C.UTF-8 grep -oP '^\| `\K[a-z_]+(?=`)'
```

```
id
event_id
tenant_id
event_type
occurred_at
recorded_at
version
trace_context
exchange_account_internal_id
instrument_internal_id
deal_internal_id
strategy_internal_id
content
```

| Колонка | Операнд ключа зерна? | Исход |
|---|---|---|
| `tenant_id` | да, у обоих зёрен | выполняется после правки (ось A) |
| `exchange_account_internal_id` | да, у обоих | выполняется |
| `strategy_internal_id` | да, у сделочного | выполняется |
| `occurred_at` | **колонка окна отбора** — по ней запрос берёт сутки пересчёта (`docs/models/domain/other/AuditRecord.md` §Персистентность, довод индекса `ix_audit_record_occurred`). Операндом ключа является **сутки**; что бакет считается по этой же колонке, **дом операнда не объявляет**, и узел этого не назначает (X7, Y3) | выполняется после правки |
| `content` | несёт расчётную валюту — операнд ключа сделочного зерна. **Счётного клейма о числе операндов из содержимого клетка не делает:** содержимое несёт `internalId` предмета и его контекста, включая счёт (`docs/architecture/contracts.md`) | выполняется |
| `instrument_internal_id` | **нет** | **правлено**: названо поимённо, что в ключ не входит ни у одного зерна |
| `deal_internal_id` | **нет** | **правлено**: названо поимённо; группировка по нему даёт строку на сделку вместо строки на сутки |
| `event_type` | **нет** — это операнд **отбора** каждого счётчика происшествий, а не измерение строки | **правлено**: покрыт общим критерием правки; назван здесь потому, что у зерна происшествий он естественный кандидат в `GROUP BY` — счётчики поклассовые |
| `recorded_at` | **нет** | **правлено**: покрыт общим критерием. Дом уже отдельно запрещает выбирать по нему окно пересчёта («Отметки последнего пересчёта нет намеренно») — группировка по нему тем более |
| `id`, `event_id`, `version`, `trace_context` | **нет** | **правлено**: покрыты общим критерием правки — они не измеряют агрегат |

**Критерий, которым правка отделяет одни от других, — «измеряет ли колонка
агрегат», а не «каркас против радиуса».** Дихотомии «каркас/радиус» не
существует: `exchange_account_internal_id` и `strategy_internal_id` — тоже
колонки идентичности и в ключ входят; `tenant_id` — тоже каркас и в ключ
входит. Критерий един и едет **в текст корпуса**, а не остаётся в рабочем
файле: колонка входит в ключ тогда и только тогда, когда она есть измерение
строки агрегата; всё прочее строку журнала отбирает.

### Ось носительская

Область — `docs/**`, `.claude/**` целиком (включая `CLAUDE.md`), деревья
кода, `tools/`, `deploy/`, `web/`. Шаблоны кириллице-безопасные (`\w`
кириллицу не берёт), терпимые к переносу строки, и включают **именную**
форму предмета:

```bash
for pat in 'ключ[а-яё]*\s+зерна' 'зерн[а-яё]*\s+строки' 'group\s+by' \
           'группиру' 'группировк' 'зёрен'; do
  echo "--- $pat"
  LC_ALL=C.UTF-8 grep -rlUzP "(?s)$pat" docs/ .claude/ CLAUDE.md README.md \
    services/ libs/ donor/ tools/ web/ deploy/ 2>/dev/null \
    | tr '\0' '\n' | grep -v '/target/' | sort -u
done
```

| Носитель | Что несёт | Исход |
|---|---|---|
| `docs/rules/statistics-aggregates.md` §«Зерно строки» | ключ сделочного зерна — **дом** | выполняется |
| `docs/rules/statistics-aggregates.md` §«Счётчики происшествий — своё зерно, а не строка сделочного агрегата» | ключ зерна происшествий — **дом** | выполняется |
| `docs/rules/statistics-aggregates.md` §Персистентность | колонки ключа обеих таблиц, `unique nulls not distinct` | выполняется |
| `docs/rules/statistics-aggregates.md` §«Пересчёт», строки 305, 310-312, 315-321 | три пассажа выше | **правлены** |
| `docs/spec/statistics-aggregates.json`, `operands.rowCurrency` | «расчётная валюта строки агрегата; она в зерне строки, а не в поле» | выполняется |
| `docs/spec/statistics-aggregates.json`, `values[43].note` (`openedDeals`) | «Живёт в строке ВТОРОГО зерна (тенант × счёт × сутки)» | выполняется на правиле узла; **копия операндов** передана узлу 5 |
| `docs/spec/statistics-aggregates.json`, `examples[19].unreachable` | «проекция группирует ПО расчётной валюте — она в зерне строки, поэтому чужая валюта в строку не попадает» | выполняется |
| `docs/architecture/data-ownership.md`, строка базы `statistics` | оба зерна с операндами | выполняется на правиле узла; **копия операндов** передана узлу 5 |
| `docs/architecture/services.md`, строка `audit-statistics` | «агрегаты двух зёрен» без операндов | выполняется — образец верной формы детали |
| `docs/models/domain/other/AuditRecord.md` §«Почему четыре колонки идентичности, а не все» | четыре колонки идентичности — радиус **отбора** | выполняется; предмет другой, правки не требует. Смешение этих двух предметов и породило дефект |
| `.claude/work/decision-digest.md` Д992, Д998, Д1037, Д1041 | зерно строки, второе зерно, `unique nulls not distinct`, агрегирование запросом | выполняется — все четыре согласны с домами |
| `.claude/work/progress/phase-2-step-10-chronicle.md`, дельта `CODE` | позиция 21 («`group by` по ключам зерна») | выполняется; **правлена** отдельно — правка 4 завела позицию 22 (охрана клейма) |
| `tools/retired-check.py` | реестр снятых редакций | **правлен** — правка 5 |
| `docs/models/mapping/Strategy.md`, `donor/**`, `libs/domain-model/**`, `services/strategies/**`, `services/trading-core/**` (совпадения по `группировк`) | «группировка траншей» определения стратегии | **вне предмета** — другой предмет; проверено чтением каждого |
| `docs/integrations/okx/rules/ws-limits.md`, `docs/models/domain/other/MarketStructure.md`, `donor/.../V11__split_deal_and_tranche.sql` | «подписки группируются батчами», «пивоты группируются», «группируются» о траншах | вне предмета |
| `.claude/chat/`, `.claude/decisions/model-layer-ontology.md`, `.claude/tests/source-api/okx/plan.md`, `.claude/work/backlog.md` (по `группировк`) | группировка полей источника, группировка кейсов, группировка траншей | вне предмета — проверено чтением |
| `docs/rules/api-access-policy.md`, `.claude/work/backlog.md` (по `зерн`) | ложное срабатывание: подстрока внутри «бра**узерн**ая» | вне предмета; ловушка названа, чтобы следующий исполнитель её не переоткрыл |
| актуальный снапшот `.claude/snapshots/` | «узел 1 (ключ группировки пересчёта)» — строка состояния сессии | предметом не является: снапшот есть указатель на состояние, а не носитель правила; правится ходом снапшота |
| рабочие файлы и отчёты шага 10, `.claude/work/history/**` | хроника прохода | **классом**: описывают прошлое, задним числом не правятся |

**Носителей ключа группировки в `services/`, `libs/`, `web/`, `deploy/`,
`CLAUDE.md`, `README.md` нет** — джоба пересчёта не написана, а все
совпадения в деревьях кода принадлежат другому предмету (группировка
траншей стратегии).

## Проект правки — пять правок в трёх носителях

**Правка 1** — `docs/rules/statistics-aggregates.md`, строки 305-306.
Было:

>   Запрос к `audit` группирует строки окна по ключам зерна и отдаёт **по
>   строке на ключ**; через границу процесса проходит число строк зерна, а не

Стало:

>   Запрос к `audit` группирует строки окна **по ключу зерна собираемой
>   строки** и отдаёт **по строке на ключ**; через границу процесса проходит
>   число строк зерна, а не

**Правка 2** — там же, строки 310-312. Было:

>   тенантам в память» им прямо запрещено, а объём результата здесь ограничен
>   произведением тенантов, счетов, определений и валют — величиной, которая
>   растёт с бизнесом, а не с потоком событий. Окно ограничено сверху теми же

Стало:

>   тенантам в память» им прямо запрещено, а объём результата здесь ограничен
>   **произведением операндов ключа зерна** — величиной, которая
>   растёт с бизнесом, а не с потоком событий. Окно ограничено сверху теми же

**Правка 3** — там же, буллет 315-321 целиком. Было:

> - **Операнды берутся из содержимого строки, и это не спор с «content
>   реляционно не раскладывается».** То правило — о **схеме**: колонками
>   вынесены только операнды ключа и радиусы отбора. Запрос читать содержимое
>   вправе, и группировка идёт по четырём колонкам радиуса плюс расчётной
>   валюте из содержимого. Всё, чем джоба считает, обязано в содержимом
>   **быть** — иначе величина агрегатом не становится (перечень несчитаемого
>   ниже).

Стало:

> - **Операнды приходят и колонками журнала, и из содержимого, и это не
>   спор с «content реляционно не раскладывается».** То правило — о
>   **схеме**: колонками вынесены только операнды ключа и радиусы отбора.
>   Запрос читать содержимое вправе. **Группировка идёт по ключу зерна
>   собираемой строки, и другого ключа у запроса нет.** Расчётная валюта
>   сделочного зерна приходит **из содержимого**.
>   **Колонка входит в ключ тогда и только тогда, когда она измеряет
>   строку агрегата**, и ни одна другая в него не входит ни у одного зерна:
>   инструмент и сделка отбирают строку журнала, а класс события отбирает
>   строки каждого счётчика происшествий.
>   Группировка по сделке дала бы строку на сделку вместо строки на сутки —
>   ту, которую upsert по ключу зерна схлопнул бы, оставив числа последней
>   записанной. Всё, чем джоба считает, обязано в содержимом **быть** —
>   иначе величина агрегатом не становится (перечень несчитаемого ниже).

**Ни одна из трёх правок не вносит в `docs/**` адресного перекрёстия и не
вносит факта, которого не держит дом.** Ссылка не нужна: оба зерна
объявлены **этим же доком**, и смысловое имя «ключ зерна собираемой строки»
разрешается читателем без адреса; откуда берётся каждый операнд, говорит
§«Зерно строки». Долг адресных перекрёстий файла после правок — прежние 9
(прогон в §«Уровень пробела и род закрывающего действия»).

**Правка 4** — `.claude/work/progress/phase-2-step-10-chronicle.md`,
таблица «Дельта `CODE`, накопленная шагом», новая строка:

> | 22 | **тест джобы пересчёта: число строк результата запроса группировки — ДО записи — равно числу различных ключей зерна в фикстуре.** Состав операндов ключа и состав колонок журнала здесь не перечисляются — их дома: `docs/rules/statistics-aggregates.md` §«Зерно строки» и §«Счётчики происшествий — своё зерно, а не строка сделочного агрегата», `docs/models/domain/other/AuditRecord.md` §Персистентность. Одна проверка ловит обе стороны клейма, но только на фикстуре, где **вариация независима**, и это единый критерий обоих её свойств: (1) каждый операнд ключа зерна принимает больше одного значения **при фиксированных остальных операндах ключа** — иначе выпавший операнд не наблюдаем: он не задваивает строки, а **сливает** их, а операнд, меняющийся заодно с другим, при выпадении подменяется им и разбиения не меняет; (2) внутри хотя бы одной клетки ключа зерна больше одного значения принимает **каждая** не-ключевая колонка журнала, способная различаться внутри отбора запроса — иначе не наблюдаем лишний операнд: он **задваивает** строки, а колонка, постоянная внутри клетки, в `GROUP BY` разбиения не меняет. **Оба квантора несущие, и оба стоя́т вместо «какого-нибудь одного»:** снятая редакция включала в ключ **и инструмент, и сделку**, и фикстура, варьирующая только сделку, лишний инструмент пропускает; фикстура же, где каждый счёт заведён под своим тенантом, теряет **выпавший счёт** — второй случай предъявлен пробой ниже (фикстура A), первый — пробой редакции 6, давшей 32 строки при 32 ключах. **Что независимо варьировать не может — из требования исключено и названо:** тенант определяется счётом, поэтому армом «выпал тенант» охрана не располагает ни при какой допустимой фикстуре; класс события постоянен внутри отбора поклассового счётчика, поэтому армом «лишний класс события» — тоже. Колонка, постоянная внутри отбора (класс события у поклассового счётчика), вариации не требует по тому же доводу. Момент наблюдения несущий: на строках, уже записанных upsert'ом, проверка тавтологична — `unique nulls not distinct` по ключу зерна делает совпадение счёта гарантией схемы | `GAPS_CLOSE_3`, узел 1 (A2, B4) |

Тем же ходом правится **строка счёта под таблицей** — она несёт два числа,
и правка публикуется парой, как всякая правка узла (конвенция 1; Z4 пятой
верификации поймал здесь её отсутствие). Было:

> **Позиций живых — двадцать шесть** (двадцать семь заведённых минус снятая пятая).
> Счёт пересобран из перечня, а не написан рядом с ним.

Стало:

> **Позиций живых — двадцать семь** (двадцать восемь заведённых минус снятая пятая).
> Счёт пересобран из перечня, а не написан рядом с ним.

**Отступ в блоках воспроизводит источник, а не единый стиль:** правки 1 и 2
правят строки внутри буллета (два пробела), правка 4 — абзац верхнего уровня
(без отступа). Выравнивание «заодно» ломает литеральную замену — на этом
редакция 8 споткнулась и проверила обе стороны прогоном.

Оба числа пересобраны из перечня: заведённых позиций двадцать восемь
(двадцать одна нумерованная плюс `7а`, `7б`, `9а`, `9б`, `16а`, `16б` плюс
новая `22`), снятая одна — пятая.

**Почему оба свойства фикстуры названы в самой позиции, а не оставлены
исполнителю.** Проверка одна — равенство счёта, — но каждая из двух сторон
клейма наблюдаема только при своём свойстве фикстуры, и без него тест
проходит на неверном запросе. **Выпавший операнд** сливает строки: на
фикстуре одних суток различия по суткам не существует по построению, и
выпадение суток из `GROUP BY` тест не увидит. **Лишний операнд**
задваивает строки: на фикстуре, где в каждой клетке ключа ровно одна
строка журнала, группировка «ключ зерна плюс сделка» даёт **то же** число
строк, и тест не увидит уже её. Именно эта вторая сторона и есть находка
A2/B4 — названный ключ включал сделку и инструмент.

**Правка 5** — `tools/retired-check.py`, новая запись реестра в конец
`RETIRED`:

```python
    {
        'name': 'группировка пересчёта — по четырём колонкам радиуса плюс валюте',
        # Снято GAPS_CLOSE_3 шага 10 фазы 2, узел 1 (A2, B4). Прежняя
        # редакция называла ключом группировки четыре колонки идентичности
        # журнала плюс расчётную валюту: перечень включал инструмент и
        # сделку, которых в зерне нет, и не включал тенанта и суток, без
        # которых зерна не существует. Группировка по сделке дала бы строку
        # на сделку вместо строки на сутки, а upsert по ключу зерна такие
        # строки схлопывает молча. Пришедшая редакция ключа не перечисляет
        # вовсе: группировка идёт ПО КЛЮЧУ ЗЕРНА собираемой строки, а оба
        # ключа объявлены своими домами в том же доке.
        'pattern': r'по\s+четырём\s+колонкам\s+радиуса\s+плюс\s+расчётной',
        'arrived': r'по\s+ключу\s+зерна\s+собираемой\s+строки',
        'date': '2026-09-06',
        'source': 'GAPS_CLOSE_3 шага 10 фазы 2, узел 1',
        'allowed': ('.claude/work/decision-digest.md',),
        'population': (
            ('docs/rules/statistics-aggregates.md', None),
        ),
    },
```

**`allowed` состоит из одного элемента, и это результат прогона по самому
реестру.** `.claude/work/progress/` и `.claude/work/history/` инертны —
сканер их файлы не читает вовсе (`SKIP`); `tools/retired-check.py` инертен —
он пропускает себя по имени файла у **каждой** записи. Элемент, не меняющий
исхода ни при каком состоянии корпуса, есть лишний носитель
(`.claude/rules/design-simplicity.md`). Остаётся
`.claude/work/decision-digest.md` — единственный, который **сработает**:
решение узла уходит туда строкой, а дайджест в `SKIP` не входит.

**Строка популяции несёт `None`, а не копию `arrived`.** Шапка реестра:
«None во втором поле означает «шаблон записи». Свой шаблон обязателен там,
где на месте снятого встало **РАЗНОЕ**». Здесь у единственного носителя
встало одно и то же. **Единица популяции у `scan()` — путь файла**
(`os.path.exists`), а не пассаж; снятая формулировка стоя́ла ровно в одном
месте, там же появилась пришедшая.

**Дата записи — `2026-09-07`, день фактического снятия редакции из
корпуса**, а не день, которым правка была спроектирована (проект нёс
`2026-09-06`). Поле реестра означает **дату снятия**, и им пользуется
только отчёт прогона.

## Исполнимая форма — чем клейм обеспечен и чем НЕ обеспечен

**Спекой не обеспечен.** Клейм «ключ группировки равен ключу зерна» есть
свойство **запроса**, а спека мерит **строку**: ни одна её величина не
видит, по чему запрос группировал.

**Схема его не ловит, и это хуже, чем «не ловит».** `unique nulls not
distinct` по ключу зерна при upsert'е неверно сгруппированные строки
**схлопывает молча** — маскирует дефект вместо отказа.

**Частично покрыт один операнд из пяти.** `rowCurrencyUniform`
(`over: deals`, `op: all`) мерит, что строка собрана из сделок одной
расчётной валюты. Остальных четырёх не мерит ничто.

**Охрана назначена и получила носителя — правку 4** (позиция 22 дельты
`CODE`). Расширение спеки до `rowGrainUniform` отвергнуто: восемь новых
операндов ради инварианта, который тест выражает одной строкой, — больший
след без выигрыша (`.claude/rules/design-simplicity.md`).

**Область охраны предъявлена пробой на ДВУХ фикстурах, и вторая —
состязательная.** Фикстура A собрана по глобальной вариации каждого
операнда, фикстура B — по вариации при фиксированных остальных. Обе имеют
32 строки и 16 различных ключей зерна:

```
ФИКСТУРА A — каждый счёт под СВОИМ тенантом (глобальная вариация)
  свойство (1) в форме «глобальная вариация»:         выполнено
  свойство (1) в форме «при фиксированных остальных»: tenant=НЕТ, account=НЕТ
  ПРОПУЩЕНО охраной: + event_type, - tenant, - account

ФИКСТУРА B — оба счёта под ОДНИМ тенантом (вариация при фиксированных)
  свойство (1) в форме «глобальная вариация»:         НЕ выполнено
  свойство (1) в форме «при фиксированных остальных»: account=да, tenant=НЕТ
  ПРОПУЩЕНО охраной: + event_type, - tenant
```

**Глобальная форма свойства (1) не просто допускала дырявую фикстуру — она
её ТРЕБОВАЛА.** На фикстуре B она не выполняется (тенант принимает одно
значение на весь набор), то есть исполнитель, читающий эту букву, обязан
был бы взять фикстуру A — ровно ту, на которой теряется `- account`.
Критерий позиции 22 («при фиксированных остальных операндах ключа»)
разводит их механически: на A счёт не варьирует независимо, и фикстура
отвергается.

**На годной фикстуре пропущено ровно два, и оба — не дыры, а следствия
одного критерия.** Арм наблюдаем тогда и только тогда, когда операнд
способен варьировать независимо; `- tenant` и `+ event_type` не способны ни
при какой допустимой фикстуре — первый определяется счётом
(§«Что именно разошлось — три пассажа одного раздела»), второй постоянен
внутри отбора поклассового счётчика. Охраны этих двух армов **не
существует**, и узел объявляет это, а не умалчивает. **Обе стороны находки
A2/B4 при этом покрыты:** снятая редакция включала в ключ инструмент **и**
сделку, и оба варианта проба ловит на обеих фикстурах.

## Исход мини-петли — строкой

**Два раунда критики и семь верификаций; ряд дефектов 13, 10, 8, 6, 7, 7, 4,
5, 4.** Поймано в подавляющей части не в конструкции правки, а в **тексте о
правке** — клеймах проекта о самом себе; отсюда четыре конвенции узла,
размещённые в хронике шага. Доля дефектов, едущих в `docs/**`: 3/8 → 2/6 →
2/7 → 3/7 → **0/4** → **0/5** → **0/4** — три последних захода не нашли ни
одного дефекта в корпусных правках, и два из них **независимо подтвердили
их регресс**. Осталось на момент закрытия: дефекты рационала последней
верификации, закрытые редакцией 10; восьмая верификация не назначалась — по
поправке держателя она условием приземления не является. Финальная правка
верифицирована: блоки «Было» сверены с корпусом машинно и применены
литеральной заменой без нормализации пробелов.

Отчёты заходов — `…-node-1-critique.md`, `…-node-1-critique-round2.md`,
`…-node-1-verification.md` … `…-node-1-verification-7.md`; замер, на котором
стои́т поправка держателя, — хроника шага
§«Четыре конвенции, введённые узлом 1 и обязательные для узлов 2-6».

## Приземление — что внесено и чем проверено

Пять правок внесены 2026-09-07 литеральной заменой без нормализации
пробелов; каждый блок «Было» найден в цели ровно один раз.

| Правка | Носитель | Исход |
|---|---|---|
| 1 | `docs/rules/statistics-aggregates.md`, строки 305-307 | внесена |
| 2 | там же, строки 311-313 | внесена |
| 3 | там же, буллет 316-329 | внесена |
| 4 | `…/phase-2-step-10-chronicle.md`: строка 22 дельты `CODE` плюс строка счёта | внесена |
| 5 | `tools/retired-check.py`, запись реестра | внесена |

**Прогоны после приземления.**

- Признак узла: перечень операндов в §«Пересчёт» — **пустая выдача**; долг
  адресных перекрёстий файла — **9**, не вырос.
- `tools/retired-check.py` — код 0: живых носителей 1950, записей реестра
  **67**, строк популяции **234**, дефектов 0. Запись узла предъявляет и
  снятую редакцию (её в корпусе нет), и пришедшую (она в единственном
  носителе популяции).
- Остальной гейт инструментов корпуса — `.claude/skills/update-roadmap-progress.md`
  §«Гейт инструментов корпуса»; исход прогона — хроника шага.

**Что узел не закрывает и куда передал.** Две позиции — копия операндов
обоих зёрен в `docs/architecture/data-ownership.md` (строка базы
`statistics`) и в `docs/spec/statistics-aggregates.json`, `values[43].note`
(`openedDeals`) — правилом этого узла не покрыты и переданы **узлу 5**
(«клеймы и копии носителей») явной строкой его перечня в
`.claude/work/progress/phase-2-step-10-gaps-close-3.md`.
