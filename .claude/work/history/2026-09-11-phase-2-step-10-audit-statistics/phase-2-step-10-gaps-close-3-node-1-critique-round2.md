# Критика узла 1 `GAPS_CLOSE_3` шага 10 фазы 2 — раунд 2

## На какой вопрос отвечает этот файл

Какие дефекты найдены во второй редакции закрытия узла 1 (A2, B4) до его
приземления в корпус.

## Рамка

Предмет — раздел «Узел 1 — ключ группировки пересчёта (A2, B4)» файла
`.claude/work/progress/phase-2-step-10-gaps-close-3.md`, **редакция вторая**,
оба предмета критики: конструкция и текст правки
(`.claude/processes/roadmap-step-execution.md` §«У критики два предмета, и
второй — текст правки»). Проект — план до приземления: неправленность
носителя, помеченного «правится этим закрытием», дефектом не считается.

Вторая обязанность раунда — проверить исход тринадцати дефектов раунда 1
(`phase-2-step-10-gaps-close-3-node-1-critique.md`) и наличие новых дефектов
**того же класса**, который доработка снимала.

## Что прогнано

### 1. Опубликованные командой предметной оси — три команды, дословно

```bash
LC_ALL=C.UTF-8 sed -n '/^## Зерно строки/,/^## Что хранится/p' docs/rules/statistics-aggregates.md \
  | LC_ALL=C.UTF-8 grep -oP '^- \*\*\K[^*]+'
LC_ALL=C.UTF-8 grep -oP 'строка происшествий — «\K[^»]+' docs/rules/statistics-aggregates.md
LC_ALL=C.UTF-8 sed -n '/^## Персистентность/,/^### Вторая таблица/p' \
  docs/models/domain/other/AuditRecord.md | LC_ALL=C.UTF-8 grep -oP '^\| `\K[a-z_]+_internal_id(?=`)'
```

Вывод дословно (все три — код 0):

```
тенант
счёт
определение стратегии
сутки UTC
расчётная валюта
---
тенант × биржевой счёт × сутки UTC
---
exchange_account_internal_id
instrument_internal_id
deal_internal_id
strategy_internal_id
```

Совпадает с опубликованным в проекте, включая порядок.

### 2. Опубликованная команда носительской оси — цикл из пяти шаблонов, дословно

```bash
for pat in 'ключ[а-яё]*\s+зерна' 'зерн[а-яё]*\s+строки' 'group\s+by' 'группиру' 'зёрен'; do
  echo "--- $pat"
  LC_ALL=C.UTF-8 grep -rlUzP "(?s)$pat" docs/ .claude/rules/ .claude/processes/ \
    .claude/decisions/ .claude/skills/ .claude/work/ services/ libs/ donor/ tools/ 2>/dev/null \
    | tr '\0' '\n' | grep -v '/target/' | sort -u
done
```

Вывод (объединение — 29 файлов). Живые носители предмета:

```
--- ключ[а-яё]*\s+зерна
.claude/work/progress/phase-2-step-10-chronicle.md
.claude/work/progress/phase-2-step-10-docs-check-2-lens-a.md
.claude/work/progress/phase-2-step-10-docs-check-2-lens-b.md
.claude/work/progress/phase-2-step-10-docs-check-3-lens-a.md
.claude/work/progress/phase-2-step-10-docs-check-3-lens-b.md
.claude/work/progress/phase-2-step-10-docs-check-3.md
.claude/work/progress/phase-2-step-10-gaps-close-1-critique.md
.claude/work/progress/phase-2-step-10-gaps-close-1-part-2.md
.claude/work/progress/phase-2-step-10-gaps-close-1.md
.claude/work/progress/phase-2-step-10-gaps-close-2.md
.claude/work/progress/phase-2-step-10-gaps-close-3-node-1-critique.md
.claude/work/progress/phase-2-step-10-gaps-close-3.md
docs/rules/statistics-aggregates.md
--- зерн[а-яё]*\s+строки
.claude/work/decision-digest.md
.claude/work/progress/phase-2-step-10-gaps-close-1.md
.claude/work/progress/phase-2-step-10-gaps-close-3-node-1-critique.md
.claude/work/progress/phase-2-step-10-gaps-close-3.md
docs/spec/statistics-aggregates.json
--- group\s+by
.claude/work/history/2026-08-26-policy-home-split/Deal-full.md
.claude/work/history/2026-08-29-step-7-docs-check-loop/phase-1-step-7-docs-check-13.md
.claude/work/history/2026-08-29-step-7-docs-check-loop/phase-1-step-7-docs-check-14.md
.claude/work/history/2026-08-29-step-7-docs-check-loop/phase-1-step-7-gaps-close-14.md
.claude/work/progress/phase-2-step-10-chronicle.md
.claude/work/progress/phase-2-step-10-gaps-close-2.md
.claude/work/progress/phase-2-step-10-gaps-close-3-node-1-critique.md
.claude/work/progress/phase-2-step-10-gaps-close-3.md
--- группиру
.claude/work/history/2026-05-26-обкатка-классификации-процессы.md
.claude/work/history/2026-06-10-phase-1-step-3-derived-market-data/phase-1-step-3-gaps-close-4-design.md
.claude/work/history/2026-06-10-phase-1-step-3-derived-market-data/phase-1-step-3-n3-structure-rerun.md
.claude/work/history/2026-09-03-phase-1-step-7-deals-and-pnl/phase-1-step-7-docs-check-28/lens-A.md
.claude/work/progress/phase-2-step-10-docs-check-3-lens-b.md
.claude/work/progress/phase-2-step-10-docs-check-3-lens-c.md
.claude/work/progress/phase-2-step-10-gaps-close-1.md
.claude/work/progress/phase-2-step-10-gaps-close-3-node-1-critique.md
.claude/work/progress/phase-2-step-10-gaps-close-3.md
docs/integrations/okx/rules/ws-limits.md
docs/models/domain/other/MarketStructure.md
docs/rules/statistics-aggregates.md
docs/spec/statistics-aggregates.json
donor/src/main/resources/db/migration/V11__split_deal_and_tranche.sql
--- зёрен
.claude/work/progress/phase-2-step-10-docs-check-3-lens-a.md
.claude/work/progress/phase-2-step-10-docs-check-3-lens-b.md
.claude/work/progress/phase-2-step-10-gaps-close-3-node-1-critique.md
.claude/work/progress/phase-2-step-10-gaps-close-3.md
docs/architecture/data-ownership.md
docs/architecture/services.md
docs/spec/statistics-aggregates.json
```

Замечание к сверке: строка таблицы проекта про ложное срабатывание
(`api-access-policy.md:49`, `backlog.md:1198,3791`) в этом выводе **не
появляется** — новые шаблоны «браузерн» не берут; строки в таблице
оставлены как предупреждение следующему исполнителю. Оба адреса проверены
поимённо и верны: `:49` — «браузерная», `:1198` — «браузерной», `:3791` —
«браузерный».

### 3. Встречный контроль охвата — по каталогу и по письменной форме

```bash
{ for pat in '(?i)зерн' '(?i)зёрн' '(?i)group\s*by' '(?i)grain' '(?i)группир'; do
  LC_ALL=C.UTF-8 grep -rlUzP "(?s)$pat" docs/ .claude/rules/ .claude/processes/ \
    .claude/decisions/ .claude/skills/ .claude/work/ services/ libs/ donor/ tools/ 2>/dev/null | tr '\0' '\n'
done; } | grep -v '/target/' | sort -u
```

Опубликованное объединение — **29** файлов, контрольное — **104**; в
контроле и не в опубликованном — 78 файлов. Разбор: `docs/`-часть разницы
— `contracts.md` («Браузерный `EventSource`»), `api-access-policy.md`
(«браузерная»), `Deal.md` («Группировка по траншам»), `Strategy.md`,
`mapping/Strategy.md`, `strategy-reference.json` («сгруппированные по
статусу»); кодовая часть — «ключ группировки» шагов стратегии. **Ни одного
носителя предмета среди них нет**, но причина разницы несущая: шаблон
`группиру` не берёт **именной формы** предмета — «группировк» (см. Н6).

Отдельно — сегменты каталога, которых в опубликованной команде нет:

```bash
for pat in 'ключ[а-яё]*\s+зерна' 'зерн[а-яё]*\s+строки' 'group\s+by' 'группиру' 'зёрен' 'группировк'; do
  echo "--- $pat"
  LC_ALL=C.UTF-8 grep -rlUzP "(?s)$pat" .claude/agents/ .claude/templates/ .claude/tests/ \
    .claude/notes/ .claude/snapshots/ .claude/knowledge-tree.md CLAUDE.md web/ deploy/ README.md \
    2>/dev/null | tr '\0' '\n' | sort -u
done
```

Единственное попадание — `.claude/tests/source-api/okx/plan.md:2803`
(«стоимость по квоте и группировка» — движения биржи, вне предмета).
Пропущенных носителей предмета нет.

### 4. Точные границы трёх правимых пассажей

```bash
awk 'NR>=304 && NR<=322 {printf "%d|%s\n", NR, $0}' docs/rules/statistics-aggregates.md
```

```
304|- **Агрегирование выполняется В БАЗЕ ЖУРНАЛА, а не в памяти джобы.**
305|  Запрос к `audit` группирует строки окна по ключам зерна и отдаёт **по
306|  строке на ключ**; через границу процесса проходит число строк зерна, а не
...
310|  тенантам в память» им прямо запрещено, а объём результата здесь ограничен
311|  произведением тенантов, счетов, определений и валют — величиной, которая
312|  растёт с бизнесом, а не с потоком событий. Окно ограничено сверху теми же
...
315|- **Операнды берутся из содержимого строки, и это не спор с «content
316|  реляционно не раскладывается».** То правило — о **схеме**: колонками
317|  вынесены только операнды ключа и радиусы отбора. Запрос читать содержимое
318|  вправе, и группировка идёт по четырём колонкам радиуса плюс расчётной
319|  валюте из содержимого. Всё, чем джоба считает, обязано в содержимом
320|  **быть** — иначе величина агрегатом не становится (перечень несчитаемого
321|  ниже).
322|- **Разъезд на два процесса шов не проходит сам собой**, и ограничение об
```

Буллет занимает **315-321**, не 315-320; хвост фразы правки 2 стои́т на
**312**, не на 311.

### 5. Тело спеки — цель трёх атрибуций таблицы носителей

```bash
C:/Users/.../python.exe -c "<обход всех строк docs/spec/statistics-aggregates.json,
печать тех, что содержат 'зерн' либо 'группир', с путём в дереве JSON>"
```

```
/operands/rowCurrency :: расчётная валюта строки агрегата; она в зерне строки, а не в поле. Пусто — строка нерезолвленной валюты
/operands/records :: строки журнала суток одной строки агрегата второго зерна; ...
/values/42/note :: Охранный инвариант зерна: строка агрегата собрана из сделок ОДНОЙ расчётной валюты ...
/values/43/note :: Счётчик происшествий суток: заведённых сделок. Живёт в строке ВТОРОГО зерна (тенант × счёт × сутки): ...
/examples/19/unreachable :: проекция группирует ПО расчётной валюте — она в зерне строки, поэтому чужая валюта в строку не попадает
```

Имена: `values[42].name = rowCurrencyUniform`, `values[43].name =
openedDeals`. Нота операнда `currency` (дословно): «расчётная валюта
результата сделки — Deal.resultProfitCurrency, дом формы
docs/spec/deal-result.json. Пусто достижимо: расчётная валюта инструмента
не резолвилась, и у числа нет единицы измерения».

`rowCurrencyUniform` дословно: `over: deals`, `op: all`,
`of: (isNull(currency) && isNull(rowCurrency)) || currency == rowCurrency`.

Операндов сделки, разноимённых по тенанту/счёту/стратегии/суткам, в спеке
нет — счёт «восемь новых операндов» проекта сходится.

### 6. Энфорсеры на текущем дереве (базовое состояние, до приземления)

```
python tools/anchor-check.py            → код 0; адресов 768; неразрешимых 54 (все — объявленный долг);
                                           НОВЫХ НЕРАЗРЕШИМЫХ 0
bash   tools/spec-run.sh                → код 0; спецификаций 27, примеров 656 (контрпримеров 40);
                                           популяций 11, объявленных членов 117
python tools/population-derive-check.py → код 0; осей популяций 11; ПЕРЕЧНИ СОШЛИСЬ С ВЫВЕДЕННЫМИ
python tools/retired-check.py           → код 0; живых носителей 1950; записей реестра 66;
                                           строк популяции 233; ДЕФЕКТОВ 0
```

`retired-check` раунд 1 не прогонял — он энфорсер именно той оси, которую
редакция 2 достраивает (§«Снятая редакция»).

### 7. Разрешимость адресов самого проекта — воспроизведено на энфорсере

`tools/anchor-check.py` держит `/progress/` в `SKIP`, поэтому адреса
проекта им не мерятся. Прогон той же логики на временной фикстуре:

```
trim_tail('Уровень пробела и род закрывающего действия') = 'уровень пробела и род закрывающего действия'
norm('Уровень пробела')                                  = 'уровень пробела'
совпадает: False
scan(фикстура) → bad = [('src.md', 3, '«Уровень пробела»'),
                        ('src.md', 5, '«Счётчики происшествий…»')]
```

Контроль на законном хвосте: `trim_tail('Исполнимая форма — чем клейм
обеспечен и чем НЕ обеспечен') == norm('Исполнимая форма')` → `True`.

### 8. Шаблон по секции-доводу — счёт проверен

```bash
LC_ALL=C.UTF-8 sed -n '/^### Почему четыре колонки идентичности, а не все/,/^### Почему поля аудита/p' \
  docs/models/domain/other/AuditRecord.md | LC_ALL=C.UTF-8 grep -oP '^\| `\K[a-z_]+_internal_id(?=`)'
LC_ALL=C.UTF-8 sed -n '56,96p' docs/models/domain/other/AuditRecord.md \
  | LC_ALL=C.UTF-8 grep -oP '[a-z_]+_internal_id'
```

Обе — пустой вывод, код 1. Расширенно (`grep -noPi '[a-z_]*internal_?id'`)
секция даёт три вхождения camelCase (`internalId` ×2, `strategyInternalId`),
имён колонок — **ноль**.

### 9. Долг адресных перекрёстий в корпусе (команда `docs/concept.md` §Ссылки)

```bash
LC_ALL=C.UTF-8 grep -roP '§(?!П[0-9])(?!`)' docs/ --exclude=concept.md | wc -l
LC_ALL=C.UTF-8 grep -oP '§(?!П[0-9])(?!`)' docs/rules/statistics-aggregates.md | wc -l
```

```
610
9
```

### 10. Дельта `CODE` шага — состав позиций

```bash
awk 'NR>=362 && NR<=396 {printf "%d|%s\n", NR, $0}' .claude/work/progress/phase-2-step-10-chronicle.md
LC_ALL=C.UTF-8 grep -noP '.{0,90}тест.{0,90}' .claude/work/progress/phase-2-step-10-chronicle.md
```

Единственное вхождение «тест» во всей хронике — позиция 19 («охрана состава
— тест у писателя каждой формы»). Позиции о **тесте джобы пересчёта** в
дельте нет. Позиция 2 дельты («состав `DealClosedContent`: идентичность
определения, …») присутствует — операнд «определение стратегии» ключа
обеспечен.

### 11. Прочитаны целями атрибуций

`docs/rules/statistics-aggregates.md` §«Зерно строки» (20-56), §«Что
хранится», §«Счётчики происшествий — своё зерно, а не строка сделочного
агрегата» (214-241), §«Пересчёт — проекция, а не накопитель» (288-336),
§Персистентность (431-487); `docs/models/domain/other/AuditRecord.md`
§«Почему четыре колонки идентичности, а не все», §Персистентность;
`docs/architecture/data-ownership.md:38` и §«Разделение по тенанту» (236);
`docs/architecture/services.md:54`; `docs/architecture/contracts.md:100-135`;
`.claude/work/decision-digest.md` Д992, Д998, Д1037, Д1041;
`tools/retired-check.py` (шапка, `RETIRED`, `registry_incomplete`, `scan`);
`libs/domain-model/.../event/DealClosedContent.java`; отчёты линз A2 и B4.

---

## Исход дефектов раунда 1

| # | Исход | Чем именно не до конца |
|---|---|---|
| К1 | **снят** | правка 3 ключа не выписывает, оба дома получили указатель; крен A2 (вариант 3) исполнен |
| К2 | **снят** | квантор «каждого» убран; «ровно один операнд — расчётная валюта **сделочного** зерна» верно по обоим зёрнам |
| К3 | **снят частично** | каталог расширен и найденные носители внесены с исходами; но объявленная область «живые `.claude/**`» шире прогнанной (6 подкаталогов из 13; нет `CLAUDE.md`), а пять шаблонов не берут именной формы предмета — Н6 |
| К4 | **снят** | `:310-311` внесены третьей строкой таблицы расхождений и получили правку 2 |
| К5 | **снят** | ложный клейм убран; названо, чего не мерит спека и что схема дефект маскирует; отказ от `rowGrainUniform` обоснован счётом, который сходится (восемь операндов). Носитель назначенной охраны — отдельный дефект Н5 |
| К6 | **снят частично** | ось сделана двусторонней; но команда второй половины по построению выводит только `*_internal_id`, и класс «колонка журнала, не измеряющая агрегат» представлен двумя членами из возможных — Н10 |
| К7 | **снят** | «три позиции из пяти» — проверено: счёт, определение стратегии, расчётная валюта |
| К8 | **снят частично** | лишняя обратная кавычка убрана; начало диапазона исправлено (316→315), но конец теперь короче буллета (315-320 при 315-321), и у правки 2 та же ошибка — Н1. В самом проекте появились два неразрешимых §-адреса — Н8 |
| К9 | **снят частично** | две ноты названы верно и дословно; третья строка приписана «ноте операнда `currency`», а это `examples[19].unreachable` — Н2 |
| К10 | **снят частично** | критерий «измеряет агрегат / отбирает строку журнала» попал **в текст правки** — это главное. Остаток: лид правки по-прежнему называет включённые операнды «колонками каркаса», а исключённые — «колонками радиуса», то есть воспроизводит ровно ту пару терминов, которую сам проект объявляет не-дихотомией; абзац-различение живёт в рабочем файле, в корпус не едет |
| К11 | **снят** | таблица говорит «верно; **домом не является**», правка 1 ставит указатель |
| К12 | **снят** | повтор довода снят, правка ссылается на пассаж ниже |
| К13 | **снят частично** | вопрос отвечен, §«Снятая редакция» заведён; но предписанная запись реестра неисполнима — Н3 |

Итого: **7 снято полностью, 6 снято частично, 0 не снято**. Все шесть
частичных — новые дефекты **того же класса**, который доработка снимала
(диапазон пассажа, атрибуция строки спеки, охват команды, полнота оси,
исполнимость записи реестра).

---

## Новые дефекты

### Н1. Диапазоны правок 2 и 3 короче текста, который правки воспроизводят: замена по названному адресу оставит в корпусе осколок

**Что не так.** Правка 3 объявлена как «пришедшая редакция пассажа» с
адресом **стр. 315-320**. Буллет занимает **315-321**: строка 321 — «  ниже).»
— хвост той же фразы, которую пришедшая редакция воспроизводит целиком
(«…иначе величина агрегатом не становится (перечень несчитаемого ниже).»).
Замена ровно 315-320 оставит в корпусе висящую строку «ниже).» сразу за
законченным предложением.

То же у правки 2: адрес — **стр. 310-311**, а воспроизводимый текст
кончается словами «растёт с бизнесом, а не с потоком событий», которые
стоя́т на **312**. Замена 310-311 оставит их дубль.

Это буквально класс К8 («замена по названному диапазону оставила бы первую
строку жирного лида на месте»), объявленного в таблице проекта как
«исправлено оба»: начало диапазона поправлено, конец — не перепроверен.

**Чем предъявлено.** Проект, §«Что именно разошлось», §«Проект правки»
(правки 2 и 3); вывод `awk` п. 4 §«Что прогнано».

**Предмет.** Текст правки. **Тяжесть.** Текстовый (механический на
приземлении: чинится двумя цифрами).

---

### Н2. Третья строка спеки приписана «ноте операнда `currency`» — на деле это `examples[19].unreachable`; рецидив К9

**Что не так.** Строка таблицы носителей: «`docs/spec/statistics-aggregates.json`,
нота операнда `currency` | «проекция группирует ПО расчётной валюте — она в
зерне строки, поэтому чужая валюта в строку не попадает» | выполняется».

Цитата дословно верна, но принадлежит **не ноте операнда**: это значение
ключа `unreachable` у примера `examples[19]` — то есть **контрпример**,
объявляющий, чем состояние исключено
(`.claude/processes/roadmap-step-execution.md` §«Охранный инвариант
доказывается предъявленным контрпримером»). Нота операнда `currency`
говорит другое: «расчётная валюта результата сделки —
`Deal.resultProfitCurrency`, дом формы `docs/spec/deal-result.json`. Пусто
достижимо…».

Различение не косметическое дважды. Во-первых, `currency` — операнд
**сделки**, и клейма о зерне он не несёт вовсе (ровно то, за что К9 и
поймал раунд 1). Во-вторых, носитель другого **рода**: у контрпримера свой
режим правки и свой прогон, и «нота, согласная с домом» описывает не тот
артефакт, который придётся перечитать при следующей правке.

Раунд 1 закрывался фразой «таблица носителей несёт три ноты спеки поимённо,
каждая со своим текстом» — две из трёх названы верно, третья заново
приписана не тому носителю.

**Чем предъявлено.** Дамп спеки, п. 5 §«Что прогнано»; проект, таблица
§«Ось носительская».

**Предмет.** Текст правки. **Тяжесть.** Текстовый.

---

### Н3. Предписанная запись реестра снятых редакций неисполнима: популяция названа пассажами, реестр держит файлы, и названные пассажи пришедшей редакции не предъявят

**Что не так.** §«Снятая редакция» предписывает: «запись о снятии вносится в
реестр `tools/retired-check.py`: пришедшая редакция — «группировка идёт по
ключу зерна собираемой строки»; **популяция носителей — два пассажа-дома,
где ключи объявлены**».

Реестр устроен иначе, и это проверяется по его коду:

- **единица популяции — путь файла**, а не пассаж: `scan()` делает
  `os.path.exists(member)` и на несуществующем пути даёт находку «строка
  популяции указывает на носитель, которого нет» (ось 5 шапки);
- **смысл популяции другой**: «места, где снятая редакция стоя́ла и где
  **пришедшая обязана появиться**». Снятая формулировка стоя́ла в одном
  месте — §«Пересчёт»; пришедшая появится там же. §«Зерно строки» и
  §«Счётчики происшествий — своё зерно, а не строка сделочного агрегата» —
  дома **ключа**, а не клаузы о группировке: пришедшая редакция в них не
  появится и появиться не должна. Названные популяцией, они дадут находку
  «пришедшая редакция не дошла до носителя популяции» (ось 4);
- **обязательные ключи записи не названы**: `pattern` (шаблон снятой),
  `date`, `source`, `allowed`. Первые три `registry_incomplete()`
  проверяет либо читает напрямую; `entry['allowed']` читается **без
  `.get`**, то есть запись без него роняет прогон исключением, а не
  находкой.

Прогон реестра на текущем дереве зелен (66 записей, 233 строки популяции,
дефектов 0) — то есть дефект придёт **после** приземления, у следующего
исполнителя, и будет выглядеть отказом инструмента, а не ошибкой закрытия.

Довесок: живых вхождений снятой формулировки вне `progress/` нет (проверено
`грепом` по `docs/`, `.claude/`, `tools/`, `CLAUDE.md`, `README.md`) — то
есть `allowed` может быть пуст, но, если решение поедет в
`.claude/work/decision-digest.md` (он **не** в `SKIP` реестра, и прецедент
узла 1 `GAPS_CLOSE_2` именно таков), дайджест обязан попасть в `allowed`.
Проект дайджест не упоминает вовсе.

**Чем предъявлено.** Проект, §«Снятая редакция»; `tools/retired-check.py`
шапка (ФОРМЫ ПРЕДМЕТА, РЕЕСТР), `RETIRED[0]` как образец формы,
`registry_incomplete()`, `scan()`; прогон п. 6 §«Что прогнано».

**Предмет.** Текст правки + конструкция. **Тяжесть.** Несущий.

---

### Н4. Проверяемый признак уровня «носитель» — счётный и ложный: полный перечень операндов ключа стои́т не в двух пассажах, а минимум в пяти носителях

**Что не так.** §«Уровень пробела и род закрывающего действия» вводит
признак, которым доказывает соблюдение уровня: «после правки полный перечень
операндов ключа стои́т **ровно в тех же двух пассажах**, что и до неё
(§«Зерно строки» — сделочное; §«Счётчики происшествий…» — происшествий)».

Полный перечень операндов стои́т, помимо этих двух:

- **§Персистентность того же файла** — по колонке на операнд, каждая
  помечена «ключ зерна»: `deal_aggregates` (пять) и `incident_aggregates`
  (три). Сам проект это признаёт строкой таблицы носителей («колонки ключа
  обеих таблиц»), а раунд 1 в К1 считал её носителем ключа явно;
- **`docs/architecture/data-ownership.md:38`** — «агрегаты двух зёрен —
  сделочного (тенант × счёт × определение × сутки × валюта) и происшествий
  (тенант × счёт × сутки)»;
- **`docs/spec/statistics-aggregates.json`, `values[43].note`** — «Живёт в
  строке ВТОРОГО зерна (тенант × счёт × сутки)»;
- **§«Зерно строки»:24** несёт ключ происшествий вторым экземпляром рядом с
  указателем на его дом.

Признак, введённый ради ответа на К1, сам оказался ложным счётным клеймом —
то есть класс К7, воспроизведённый в новом месте. Рядом второе
самопротиворечие: тот же абзац говорит «истина … **разошлась** с ним в трёх
пассажах», тогда как таблица двумя абзацами ниже про первый из трёх пишет
«верно».

Отдельный хвост того же счёта: клетка `data-ownership.md:38` получает исход
«выполняется — оба зерна с операндами», хотя соседняя строка таблицы
засчитывает `services.md:54` верной **именно за отсутствие операндов**
(«упоминание со ссылкой на дом»). Критерий у закрытия есть и применён к
одному носителю из двух; исхода по `.claude/rules/policy-home.md` у
операндоносного носителя не дано ни «правится», ни «не требует правки —
почему».

**Чем предъявлено.** Проект, §«Уровень пробела и род закрывающего
действия», таблица §«Ось носительская»;
`docs/rules/statistics-aggregates.md:440-444`, `:453-455`, `:24`;
`docs/architecture/data-ownership.md:38`; `docs/architecture/services.md:54`;
дамп спеки, п. 5 §«Что прогнано».

**Предмет.** Конструкция + текст правки. **Тяжесть.** Текстовый (счётный
клейм; исправляется числом и одной клеткой).

---

### Н5. Охрана счётного клейма назначена артефакту, которого нет: позиция дельты `CODE` объявлена внесённой, её текст не задан, и §«Проект правки» объявляет ровно три правки в одном файле

**Что не так.** §«Исполнимая форма» закрывает пункт глубины «если клейм
счётный — исполнимая форма» так: «охрана назначена и получает носителя —
тест джобы пересчёта… **Позиция внесена в дельту `CODE` шага**».

Три несовместимых факта:

1. **в дельте позиции нет.** Единственное вхождение слова «тест» во всей
   хронике шага — позиция 19 («тест у писателя каждой формы»); позиции о
   тесте джобы пересчёта в таблице «Дельта `CODE`, накопленная шагом» (26
   живых позиций) не существует;
2. **§«Проект правки» объявляет: «Три правки в одном разделе
   `docs/rules/statistics-aggregates.md`».** Четвёртая правка — в другом
   файле — этим объявлением исключена, а её **содержание** (текст позиции)
   нигде не задано: исполнителю нечего вносить;
3. **клетка популяции противоречит обоим**: строка таблицы носителей про
   хронику даёт исход «**выполняется** — согласна с домом», а не
   «правится».

`.claude/skills/closure-population.md` §6 требует ровно обратного:
предписание исполнителю, которое пишет само закрытие, — член популяции
наравне с наличными носителями, читается как текст и прогоняется своим
инструментом. Здесь единственная охрана счётного клейма узла не имеет ни
клетки, ни текста, ни носителя; пункт глубины
(`.claude/processes/roadmap-step-execution.md` §«Гейтящий узел закрывается
до исполнимой глубины, а не до формулировки»: «носитель назван и
**существует** — не обещан будущим ходом») на ней не закрывается.

Оговорка о плане до приземления сюда не распространяется: дефект не в том,
что позиции ещё нет, а в том, что закрытие называет её внесённой, содержания
её не даёт и собственным §«Проект правки» её исключает.

**Чем предъявлено.** Проект, §«Исполнимая форма», §«Проект правки», таблица
§«Ось носительская»; `.claude/work/progress/phase-2-step-10-chronicle.md:362-396`
и грep «тест» по ней (п. 10 §«Что прогнано»).

**Предмет.** Конструкция + текст правки. **Тяжесть.** Несущий.

---

### Н6. Носительская ось объявляет область шире прогнанной — и по каталогу, и по письменной форме предмета

**Что не так.** Проект объявляет: «Область — `docs/**`, живые `.claude/**`,
деревья кода». Команда греп ит **шесть** подкаталогов `.claude/` из
тринадцати: `rules`, `processes`, `decisions`, `skills`, `work` (плюс
`tools/`). Вне прогона остались `agents/`, `chat/`, `notes/`, `snapshots/`,
`templates/`, `tests/`, `knowledge-tree.md`, а также `CLAUDE.md`.

Вторая ось того же: ни один из пяти шаблонов не берёт **именной формы**
предмета — «группировк». Узел называется «ключ **группировки** пересчёта»,
и `группиру` матчит только глагольные формы («группирует», «группируются»),
но не «группировка / группировки / сгруппированные». Именно на этой форме
`services/` и `libs/` попадания **дают** — то есть отрицательный клейм «в
деревьях кода носителей нет: `services/` и `libs/` совпадений не дают»
держится на форме, слепой к имени предмета. Это ровно класс
`.claude/processes/roadmap-step-execution.md` §«Измерительная команда
проверяется на полноту формы предмета» и §«Полнота письменных форм
предмета».

**Смягчающее названо честно и проверено мной встречно:** пропущенных
носителей предмета нет ни в одном из непокрытых сегментов и ни на именной
форме (все попадания — группировка траншей, шагов стратегии и подписок).
Дефект — в **предъявленности** клейма
(`.claude/skills/closure-population.md` §«Признак предъявленного перечня»
п. 2), а не в найденном пропуске.

Третий хвост того же: файл самого проекта в вывод команды попадает, а строки
в таблице носителей не получает (`.claude/skills/closure-population.md` §6).

**Чем предъявлено.** Проект, §«Ось носительская»; вывод пп. 2-3 §«Что
прогнано»; `ls -d .claude/*/` — тринадцать подкаталогов.

**Предмет.** Конструкция. **Тяжесть.** Текстовый.

---

### Н7. Правки вносят в `docs/**` три новых адресных перекрёстия — форму, которую `docs/concept.md` объявляет расхождением с правилом и названным долгом

**Что не так.** Правка 1 добавляет в корпус два §-адреса — «(§«Зерно
строки» — сделочное; §«Счётчики происшествий — своё зерно, а не строка
сделочного агрегата» — происшествий)»; правка 3 добавляет относительное
указание «(по моменту происшествия — **довод ниже в этом же разделе**)».

`docs/concept.md` П4: «Никакой процессной и ссылочной арматуры. Ни ссылок на
проверочные прогоны и итерации, ни строчных якорей, ни **адресных
перекрёстий на внутренние пассажи**». §Ссылки: «Ссылка **смысловая, на
дом**… Не адрес пассажа, не строка, не якорь. **Область правила —
продуктовый корпус `docs/**`**», и стоящие в корпусе перекрёстия названы
долгом, который гасится отдельной задачей. Замер долга командой самого
концепта — 610 по `docs/`, из них 9 в правимом файле; закрытие доводит файл
до 11.

Требование `.claude/rules/carrier-levels.md` («упоминание со ссылкой на
дом») этим не нарушалось бы: дом ключа лежит **в том же файле**, и
смысловая формулировка без адреса («по ключу зерна собираемой строки» —
зёрна объявлены этим же доком) закрывает уровень «носитель» тем же следом,
не наращивая долга. Развилка не названа: закрытие выбрало форму, о цене
которой корпус высказался, и цену не упомянуло.

**Чем предъявлено.** Проект, §«Проект правки» (правки 1 и 3);
`docs/concept.md` П4, §Ссылки; счёт п. 9 §«Что прогнано».

**Предмет.** Текст правки. **Тяжесть.** Текстовый.

---

### Н8. Два §-адреса самого проекта неразрешимы объявленным правилом; рецидив К8

**Что не так.**

- §«Уровень пробела» (строка таблицы разбора К1) — заголовок раздела
  называется «Уровень пробела **и род закрывающего действия**». Сокращение
  проходит **не по границе метаданного хвоста**: `.claude/rules/structure.md`
  разрешает опускать хвост, начинающийся скобкой либо « — », и объявляет
  всякое иное сокращение битым адресом. Воспроизведено на энфорсере:
  `trim_tail` заголовка не равен адресу, `scan` фикстуры даёт находку;
- §«Счётчики происшествий…» (строка 305 таблицы расхождений) — форма с
  многоточием тем же прогоном не разрешается.

Оба адреса живут в `progress/`, который `tools/anchor-check.py` держит в
`SKIP`, — то есть прогон их не поймает, ровно как и в раунде 1.

**Чем предъявлено.** Проект, таблица §«Что именно разошлось» (строка 305) и
таблица разбора К1; `.claude/rules/structure.md` §Принципы (адрес пишется
дословно за вычетом метаданного хвоста); воспроизведение п. 7 §«Что
прогнано».

**Предмет.** Текст правки. **Тяжесть.** Текстовый.

---

### Н9. «Шаблон по секции-доводу дал бы одно совпадение из четырёх» — по факту ноль

**Что не так.** Довод, которым проект объясняет выбор §Персистентность
источником третьей команды, счётный: «Шаблон по секции-доводу дал бы
**одно** совпадение из четырёх — то есть перечень, молчащий о трёх членах».

Прогон опубликованного шаблона по §«Почему четыре колонки идентичности, а не
все» даёт **ноль** совпадений (код 1); свободный `[a-z_]+_internal_id` по
той же секции — тоже ноль; имён колонок в секции нет вовсе (есть три
camelCase-вхождения, из них одно — компонент содержимого
`strategyInternalId`, а не колонка).

Вывод довода от этого крепнет, а число — ложно. Класс тот же, что у К7:
счётный клейм, который следующий читатель проверит и не воспроизведёт.

**Чем предъявлено.** Проект, §«Ось предметная»; прогон п. 8 §«Что
прогнано».

**Предмет.** Текст правки. **Тяжесть.** Текстовый.

---

### Н10. Вторая половина предметной оси объявлена шире, чем её команда способна вывести

**Что не так.** Ось объявлена как «операнды ключа **И колонки, в ключ не
входящие**». Команда вывода второй половины —
`grep -oP '^\| \`\K[a-z_]+_internal_id(?=\`)'` — по построению выводит
**только** колонки идентичности: `event_type`, `recorded_at`, `version`,
`trace_context`, `event_id` попасть в перечень не могут никогда, хотя все
они — «колонки, в ключ не входящие».

Пропуск не праздный: `recorded_at` — та самая колонка, против группировки и
отсечки по которой тот же док держит отдельный пассаж («Отметка по моменту
приёма выглядит дешевле окна и вносит дефект, который не наблюдается»), а
`event_type` — естественный кандидат в `GROUP BY` у зерна происшествий, где
счётчики именно поклассовые. Ни одного из двух ни перечень, ни текст правки
поимённо не называет.

Смягчающее: `.claude/skills/closure-population.md` §«Что перечень не обязан
делать» разрешает свести неразличимые правилом состояния в один член, и
текст правки даёт **общий критерий** («отбирают строку журнала, а не
измеряют агрегат»), которым читатель закроет и остальные колонки. Поэтому
дефект — в происхождении перечня (§3: член добывается командой, читающей
предмет), а не в тексте корпуса: команда выводит подмножество объявленной
оси, и полнота второй половины держится на памяти автора, а не на прогоне.

**Чем предъявлено.** Проект, §«Ось предметная» (объявление оси и третья
команда); `docs/models/domain/other/AuditRecord.md:187-201` (двенадцать
колонок таблицы); `docs/rules/statistics-aggregates.md:338-341`.

**Предмет.** Конструкция. **Тяжесть.** Текстовый.

---

## Что проверено и дефекта не дало

1. **Правило узла сформулировано верно и покрывает оба зерна** — «ключу
   зерна той строки агрегата, которую этот запрос собирает».
2. **Уровень пробела выбран верно** — «носитель»
   (`.claude/skills/classify-gap-level.md`): истина цела в §«Зерно строки»,
   §«Счётчики происшествий…» и §Персистентность, разошлась копия. Род
   действия («свести к дому, ключ не переписывать») исполнен: правка 3
   перечня ключей не заводит — это главный исход раунда.
3. **Крен линз исполнен.** A2 §Варианты, вариант 3 («убрать перечень из
   §«Пересчёт», оставив «группировка по ключам зерна»») и вывод B4 («обе
   клаузы обязаны на него ссылаться, а не переобъявлять ключ») выполнены
   обоими правками. Два экземпляра клаузы в разделе (стр. 305 и 318)
   дефектом **не** являются: B4 прямо требует, чтобы ссылались **обе**
   клаузы.
4. **Закрытие прошло по спискам цитат обеих находок.** Все пять носителей
   B4 (`:305`, `:318`, `AuditRecord` §«Почему четыре колонки…», §«Зерно
   строки», §Персистентность) и все четыре A2 (`:22-24`, `:316-320`,
   `:436-470`, `AuditRecord:56-70`) имеют строку с исходом.
5. **Диагноз по фактике верен.** Три пассажа существуют дословно; счёт
   «три позиции из пяти» сходится; «названный ключ включает инструмент и
   сделку, не включает тенанта и суток» — верно по `AuditRecord.md`;
   `tenant_id` в четвёрку колонок идентичности не входит.
6. **Счётное следствие ошибки описано верно** — группировка по
   `deal_internal_id` даёт строку на сделку, upsert
   `unique nulls not distinct` её схлопывает (`:468-479`); отсутствие
   тенанта ломает радиус владения, и §«Разделение по тенанту» существует
   (`data-ownership.md:236`).
7. **Абзац «Различение, на котором держится правка» верен по домам
   терминов**: `exchange_account_internal_id` и `strategy_internal_id` —
   тоже колонки радиуса и в ключ входят; `tenant_id` — тоже каркас и в ключ
   входит. Признак «измеряет агрегат» разделяет классы правильно.
8. **Отказ от `rowGrainUniform` обоснован счётом, который сходится.** В
   спеке нет ни одного посделочного операнда по тенанту, счёту, стратегии и
   суткам — значит, расширение стоит восемь операндов, как и сказано;
   `.claude/rules/design-simplicity.md` применён по назначению, а не как
   отговорка: альтернатива названа и оценена.
9. **Клейм «спека клейма не мерит» верен.** Ни одна из 49 величин не
   принимает ключа группировки; `rowCurrencyUniform` мерит один операнд из
   пяти — ровно как сказано (`over: deals`, `op: all`).
10. **Клейм «схема дефект маскирует, а не ловит» верен** и не противоречит
    доводу §Персистентность, ради которого клауза заведена.
11. **Четыре записи дайджеста согласны с домом** — Д992 (валюта в зерне),
    Д998 (второе зерно), Д1037 (`unique nulls not distinct`), Д1041
    (агрегирование запросом); ни одна ключа группировки не переобъявляет.
12. **Позиция 21 дельты `CODE` согласна с домом** («`group by` по ключам
    зерна за окно суток»), исход «выполняется» для неё верен.
13. **Операнд «определение стратегии» достижим.** `DealClosedContent` в
    коде его сегодня не несёт, но позиция **2** дельты `CODE` («состав
    `DealClosedContent`: идентичность определения, …») его вводит, а
    `docs/architecture/contracts.md:119` требует. Клетка «выполняется»
    законна.
14. **Операнд «расчётная валюта» достижим** — `resultCurrency` уже есть в
    `DealClosedContent`; «`content` события `DealClosed`» в таблице
    популяции верно.
15. **Строки «вне предмета» верны дословно** — `ws-limits.md:40`
    («подписки группируются батчами»), `MarketStructure.md:130` («пивоты
    группируются в ценовые уровни»), `V11__split_deal_and_tranche.sql:72`
    (шаги стратегии). Ловушка «зерн» ⊂ «браузерн» названа верно, три адреса
    подтверждены построчно.
16. **`services.md:54` — образцовая форма упоминания** («агрегаты двух
    зёрен» без операндов, со ссылкой на дом), исход «выполняется» верен.
17. **Клейм «в деревьях кода носителей нет» верен по существу** —
    `audit-statistics` в `services/` не заведён, джоба пересчёта не
    написана (претензия Н6 — к форме доказательства, не к выводу).
18. **Раскладка находок по узлам сходится встречным счётом от линз**:
    A1-A12 (12) + B1-B7 (7) + C1-C9 (9) = 28, гейтящих 2+3+2+1+2 = 10 —
    как в таблице.
19. **Все четыре энфорсера зелены до правки** (`anchor-check`, `spec-run`,
    `population-derive-check`, `retired-check`) — базовое состояние
    зафиксировано, регресс после приземления будет отличим от наличного
    долга.
20. **Раунд 1 отчитан честно**: «ни один дефект не найден инструментами» —
    подтверждено повторным прогоном; таблица разбора К1-К13 не приписывает
    себе несделанного, каждая строка соответствует тому, что в редакции 2
    действительно изменилось.

## Вердикт

**Узел 1 не чист.**

Из тринадцати дефектов раунда 1: **снято 7, снято частично 6, не снято 0**.
Новых дефектов — **10**, из них **несущих — 2** (Н3, Н5).

До приземления обязано быть доработано:

1. **Н5** — назвать четвёртый артефакт закрытия (позицию дельты `CODE` про
   тест джобы) правкой: дать её текст, внести в §«Проект правки», сменить
   исход клетки хроники на «правится»; либо назвать иную существующую
   охрану счётного клейма.
2. **Н3** — переписать §«Снятая редакция» под фактическую форму реестра:
   популяция — файлами (`docs/rules/statistics-aggregates.md`), плюс
   `pattern`, `arrived`, `date`, `source`, `allowed` (с дайджестом, если
   решение туда поедет).
3. **Н1** — поправить два диапазона: правка 3 — 315-321, правка 2 — 310-312.
4. **Н4** — переписать признак уровня по фактическому числу носителей и дать
   клетке `data-ownership.md:38` исход по `.claude/rules/policy-home.md`
   (правится или не требует правки — почему); снять расхождение «разошлась в
   трёх пассажах» с собственной таблицей.
5. **Н2** — переатрибутировать третью строку спеки на `examples[19]`,
   ключ `unreachable`.
6. **Н6, Н9, Н10** — привести клеймы полноты и счётные клеймы в
   соответствие с тем, что команды действительно меряют.

Н7 и Н8 чинятся тем же ходом: форма ссылки в корпусе и два адреса в самом
проекте.
