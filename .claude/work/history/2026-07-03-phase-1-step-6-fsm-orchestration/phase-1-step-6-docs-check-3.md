# DOCS_CHECK_3 — шаг 6 фазы 1 (FSM + живая оркестрация)

## На какой вопрос отвечает этот файл

На каком под-шаге мы в исполнении шага 6 фазы 1 и что показал узкий
подтверждающий прогон сквозной проверки концепции после `GAPS_CLOSE_2`.

## Контекст прогона

- **Шаг:** 6 фазы 1 — «FSM + живая оркестрация».
- **Под-шаг:** `DOCS_CHECK_3` — **узкий подтверждающий прогон** после
  `GAPS_CLOSE_2`. `GAPS_CLOSE_2` закрыл единственную находку `DOCS_CHECK_2`
  — **R1** (реконсиляция формулировки `deal-management.md` под
  DEAL-Q2-контракт). Задача прогона: верифицировать, что R1 закрыт чисто, и
  что в `docs/` не осталось **других** непроброшенных копий безусловного
  инварианта `resultProfit` (sweep).
- **Как прогнан:** один независимый ревьюер-субагент (concept-фокус, не автор
  правки `GAPS_CLOSE_2`), адверсариальная стойка. Торговый фокус не нужен —
  правка чисто доковая (реконсиляция формулировки); торговая состоятельность
  терминального контракта подтверждена на `DOCS_CHECK_2`.

## Охват

Узкий: `docs/processes/deal-management.md` (правленый док) +
`docs/lifecycles/Deal.md` (авторитет терминального контракта) + sweep по
`docs/` на другие копии инварианта `resultProfit` (executor-доки,
`DealFinalizationState` модель+lifecycle, `Deal` модель, `account-bills`,
правила терминальных инвариантов).

## Стадия остановки

**Стадия 2 пройдена** (гейтящих стадии-0 нет). Узкий прогон, обход без
останова.

## Результат верификации

- **R1 — ЗАКРЫТО ЧИСТО.** `deal-management.md` §«Статусная механика и
  recovery»: обязательность `resultProfit`/`resultProfitCurrency` теперь
  ограничена **чистым** terminal `CLOSED`; ошибочный `EMERGENCY_CLOSED` —
  по терминальному контракту (`docs/lifecycles/Deal.md` §«Терминальный
  контракт финализации», DEAL-Q2). Безусловное «для terminal обязательны…»
  (ошибочно включавшее `EMERGENCY_CLOSED` по группировке `Terminal: CLOSED,
  EMERGENCY_CLOSED`) устранено. Формулировка дословно согласуется с
  авторитетом `Deal.md` (:38-48, :91-97, §«Терминальный контракт финализации»
  :114-134); кросс-ссылка верифицирована (целевая клауза присутствует).

- **Sweep по `docs/` — других стале-копий не найдено.** Проверены все доки,
  упоминающие `resultProfit`/обязательность/terminal/`EMERGENCY_CLOSED`: по
  каждому — корректный скоуп (привязка к чистому `CLOSED` / явное освобождение
  `EMERGENCY_CLOSED`) либо отсутствие claim (`Deal.md` модель,
  `MarkDealClosedExecutor`, `MarkDealErrorExecutor`, `ErrorHandler`,
  `FinalizeDeal*Executor`, `ServiceCommandExecutor`, `account-bills`,
  `DealFinalizationState` модель+lifecycle). Единственное co-mention
  `EMERGENCY_CLOSED`+инвариант (`Deal.md:92`) констатирует **освобождение**,
  не обязательность. Паразитных групповых парафразов нет.

- **Новой ряби нет.** Однострочная правка целостна, ссылка валидна, новых
  doc↔doc расхождений не вносит.

## Пробелы по типам

Нет — ни несогласованностей, ни name-level, ни неотвеченных гейтящих
вопросов.

## Эскалации

Нет.

## Сводка

- **Находок:** 0. Эскалаций: 0. Торговых блокеров: 0.
- **R1** подтверждён закрытым чисто; других стале-копий инварианта в `docs/`
  нет; новой ряби нет.
- **Гейт `CODE`:** **чисто.** `DOCS_CHECK_3` прошёл без открытых находок и
  расхождений — обязательное условие гейта концепции
  (`roadmap-step-execution.md` §«Гейт `CODE` — чистый `DOCS_CHECK`»)
  удовлетворено по обоим фокусам (concept — этот прогон; trading — чисто на
  `DOCS_CHECK_2`, поверхность не менялась).
- **Торговый гейт:** **чисто** (без изменений против `DOCS_CHECK_2`).

## Рекомендация

Концепция шага 6 целостна; **гейт `CODE` чист**. Шаг 6 готов к под-шагу
`CODE` (написание `code-writer` + ревью-фокусы `conventions`/`performance`/
`disaster` + аппрув). Перевод в `CODE` — за пользователем.

**Напоминание о хвостах на `CODE`/`DONE`:**
- **Жёсткие гейты `DONE` шага 6** (петлю нельзя включать, пока не закрыты):
  **D-B3** (SUBMIT recovery-by-clientId) и **D-M1** (реализация
  concurrency-guard — БД-блок на весь проход; спека закрыта в доках,
  реализация — на `CODE`).
- **Форварды на свои шаги:** расчёт `resultProfit`/PnL → шаг 7; CMD-Q4
  orphan-скан → шаг 8; INSTR-Q2 остаток (CODE-представление write
  set-leverage); HTTP-коды/409-политика — провизорный хвост; TR2-TR4 —
  cross-cutting forward.
