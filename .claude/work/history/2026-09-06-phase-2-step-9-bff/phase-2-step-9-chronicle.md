# Хроника шага 9 фазы 2 — BFF

## На какой вопрос отвечает этот файл

Что сделано на шаге 9 фазы 2.

## Статус

`DONE`. Шаг открыт и закрыт 2026-09-06: **семь прогонов, шесть
закрытий**, аппрув четырьмя фокусами, синхронизация доков и пост-хок
концепт-гейт.

**Доковая петля сошлась** — ряд гейтящих 8 → 7 → 2 → 1 → **0**, ни один
узел не возвращался, детектор стагнации не срабатывал ни разу за все
семь прогонов. Пять компонентов периметра в реестре
(`.claude/work/code-gate-ledger.json`, заход «шаг 9 фазы 2»), все
«закодирован».

## Вход шага

- Формулировка — `.claude/work/roadmap/phase-2.md`, строка 9: «BFF:
  проверка токена, контекст тенанта, агрегация, SSE живых данных».
- Доки по оси владельца
  (`.claude/rules/knowledge-ownership-by-service.md`, строки `bff`):
  `docs/rules/api-access-policy.md` (делится с `auth`),
  `docs/models/domain/other/AccessDenial.md` (делится с `auth`),
  `docs/models/api/` (сырая поверхность источника — с оговоркой о
  переходе к коннектору).
- Источник порта — **отсутствует**: у донора периметра нет, `bff`
  строится с нуля по `docs/architecture/`. Это первый шаг фазы 2, у
  которого источника порта нет вовсе.
- Смежное: `docs/architecture/services.md` (ярус периметра),
  `contracts.md` §«Контекст тенанта в вызове», §«Живые данные в
  браузер», §«Синхронные вызовы», §События;
  `docs/architecture/tenant-and-exchange.md` §«Пользователи и роли»;
  `docs/architecture/platform.md` §Безопасность (ингресс, исходящий
  периметр).

## Под-шаг 1 — `TOOLING`

**Пройден без артефактов.** Новых агентов, скиллов и процессов шаг не
требует: линзы (`concept-review`, `trading-review`), формат отчёта
(`.claude/templates/docs/gap-report.md`), машина итераций и все
одиннадцать команд гейта инструментов существуют и применимы к материалу
шага без правок.

**Проверено, а не предположено:** предмет шага — периметр, и торговых
решений он не принимает (`docs/architecture/services.md`, ярус
периметра), поэтому торговая линза работает на нём **суженно** — по
одному вопросу: не создаёт ли форма живых данных ложной картины у того,
кто по ней принимает решения руками. Отдельного инструмента этот вопрос
не требует.

## Гейт инструментов корпуса на входе шага

Прогнан до первой правки — одиннадцать команд перечня, все код 0
(десять корпусных плюс `code-gate-check`), плюс `bash tools/reactor-test.sh`
(1377 файлов, 915 тестов, 0 дефектов). Дерево кода прогоном не тронуто.

## Реестр прогонов

| Прогон | Находок | Гейтящих | Узлы гейтящих | Доля следствий предыдущего закрытия | Объём отчёта | Дефект перебора в закрытии |
|---|---|---|---|---|---|---|
| `DOCS_CHECK_1` | 9 | 8 | A1-A8 | — (первый прогон шага) | 17 KB | — |
| `DOCS_CHECK_2` | 10 | 7 | маршрут периметра (C1), поверхность членств `auth` (C2), граница «проксирование ↔ своя форма» (C3), добытчик тенанта (C4), производитель событий `auth` (C5), браузерный клиент провайдера (C6), живость потока (D1) | 29 % (C3, C5 — следствия `GAPS_CLOSE_1`; сигнал С2 требует двух прогонов подряд) | 25 KB | нет |
| `DOCS_CHECK_3` | 6 | 2 | якорь посылки одного субъекта (E1), повтор мутирующих запросов периметром (F1) | знаменатель 2 — ниже минимального (четыре); окно С2 обрывается. Обе гейтящие — следствия `GAPS_CLOSE_2` (C6 и C1) | 15 KB | нет |
| `DOCS_CHECK_4` | 2 | 1 | предъявление токена на тропе потока (G1) | знаменатель 1 — ниже минимального; G1 следствием закрытия не является (ось, не тронутая ни одним прогоном) | 11 KB | нет |
| `DOCS_CHECK_5` | 2 | **0** | — (гейтящих нет) | доли нет; обе прочие — следствия текста `GAPS_CLOSE_4`, класс `НОСИТЕЛЬ` | 9 KB | нет |
| `DOCS_CHECK_6` *(пост-хок гейт)* | 3 | 3 | двусмысленность значащего молчания (J1), исключение исходящего периметра (J2), исполнение оси хоста (J3) | все три — следствия инкрементов `CODE`; знаменатель 3 ниже минимального | 10 KB | нет |
| `DOCS_CHECK_7` *(пост-хок гейт)* | **0** | **0** | — | — | 4 KB | нет |

**Закрытие `GAPS_CLOSE_1`:** восемь гейтящих узлов, все чисты; закрыто 9
из 9 находок, припарковано 0. Кодовых правок 0 — все находки о доках.
Три редакции сняты и **зарегистрированы** в `tools/retired-check.py`
(право операции; смещение подписки; «состояния не держит» без оговорки о
кэше). Отчёт — `phase-2-step-9-gaps-close-1.md`.

**Что решено этим закрытием — семь конструктивных выборов:** проверку
права держит домен, а не периметр (и сегодня её нет ни у кого — состояние
с условием выхода); периметр **проксирует**, а не заводит экранные
контракты; позиция чтения потока выражается идентичностью события, а не
смещением темы; форма на проводе к браузеру — api-модель периметра, не
доменный класс; у каждой реплики своя группа потребителя, дедуплицирует
клиент; кэш членств эфемерен со сроком 60 с; след отказа на периметре —
лог и метрика, а не строка в чужой базе.

**`DOCS_CHECK_2` — проверочный прогон по следствиям.** Десять находок,
семь гейтящих, все класса `КОД`; дефицита `подтверждение` нет.
Детектор стагнации: **ни один сигнал не сработал** — узлы прогона с
узлами `DOCS_CHECK_1` не пересекаются. Характер прогона тот же, что у
первого: предмет строится не портом, и семь из семи гейтящих находок —
«носителя нет» либо «два носителя расходятся», а не ошибка модели.
Четыре из семи найдены **верификацией клеймов доков по коду и
манифестам** (границу линзы это не двигает: проверяется цитата, а не
корректность кода). Отчёт — `phase-2-step-9-docs-check-2.md`.

**Закрытие `GAPS_CLOSE_2`:** семь гейтящих узлов, все чисты; закрыто 8
из 10 находок, припарковано 2 (ход по событиям `auth`; клиенты реалма) —
обе с домом и условием возврата. Кодовых правок 0; назначена **дельта
`CODE` из семи позиций**, включая первую на этом шаге правку чужого
сервиса (точка резолва членства у `auth`) с записанным доводом
сводимости. Одна редакция снята и зарегистрирована («все формы к
браузеру — своя api-модель периметра»). **Мини-петля критики поймала
четыре дефекта в собственном тексте закрытия**, из них несущий —
противоречие двух клауз, написанных одним заходом: точка резолва
заводит тенанта при первом предъявлении, а соседний абзац объявлял ноль
членств отказом. Отчёт — `phase-2-step-9-gaps-close-2.md`.

**`DOCS_CHECK_3` — второй проверочный прогон.** Шесть находок, две
гейтящие; абсолютный ряд гейтящих по шагу — **8 → 7 → 2**. Детектор:
ни один сигнал не сработал; узлы прогона с узлами `DOCS_CHECK_2` не
пересекаются. Обе гейтящие — оси, которых прошлые прогоны не касались:
якорь посылки одного субъекта (браузерная тропа впервые делает её
проверяемой) и повтор мутирующих запросов периметром. **Три из шести
находок — дефекты собственного артефакта прошлого закрытия**, пойманные
чтением текста правки, а не конструкции. Отчёт —
`phase-2-step-9-docs-check-3.md`.

**Закрытие `GAPS_CLOSE_3`:** два гейтящих узла, оба чисты; закрыто 6 из
6 находок, припарковано 0. Редакций не снималось — обе правки
**добавляют** клаузы. Одна правка манифеста
(`identity-realm.yaml`: `registrationAllowed: false`) сделана тем же
ходом, потому что дом посылки объявляет факт о манифесте, и отложенный
факт сделал бы клейм ложным в момент написания. **Мини-петля критики
поймала два дефекта**, оба — воспроизведённые заново классы этого же
прогона: док утверждал несуществующий факт о манифесте, и довод
дублировался между двумя домами. Отчёт —
`phase-2-step-9-gaps-close-3.md`.

**`DOCS_CHECK_4` — третий проверочный прогон.** Две находки, одна
гейтящая; ряд гейтящих — **8 → 7 → 2 → 1**. Детектор: ни один сигнал не
сработал, пересечения узлов нет. Гейтящая — ось, которой не касался ни
один прогон: тропа потока объявлена SSE и закрыта bearer-токеном, а
браузерный `EventSource` заголовка не ставит. Отчёт —
`phase-2-step-9-docs-check-4.md`.

**Закрытие `GAPS_CLOSE_4`:** один гейтящий узел, чист; закрыто 2 из 2
находок, припарковано 0. Редакций не снималось. **Мини-петля критики
поймала два дефекта**: срок билета в первой редакции рвал бы живой
поток, и обязанность клиента передавать идентичность события при
пересоздании подписки возникала молча. Отчёт —
`phase-2-step-9-gaps-close-4.md`.

**`DOCS_CHECK_5` — прогон чист по гейтящим.** Две находки, обе класса
`НОСИТЕЛЬ`: клейм о задержке отзыва не различал REST-тропу и
установленный поток; перечень величин билета расходился со своим же
пояснением. Детектор: ни один сигнал не сработал.  Отчёт —
`phase-2-step-9-docs-check-5.md`.

**Закрытие `GAPS_CLOSE_5`:** гейтящих узлов нет — чек-листы глубины и
популяции не применяются, и это записано явно. Закрыто 2 из 2;
припаркована форвард-часть H1 (предел жизни установленного потока —
`.claude/work/backlog.md` §«Второй субъект поверхности — пересмотр
посылки о единственном принципале», пункт 5). Политики закрытие не
меняло, гейт свипов по этому основанию не применялся. Отчёт —
`phase-2-step-9-gaps-close-5.md`.

**Под-шаг `CODE`.** Построены пять компонентов периметра плюс правки
`auth` (точка резолва членства, корень пути по конвенции) и переезд пяти
форм событий ядра в `domain-model` — их первый читатель приехал этим
шагом, и правило §«Общие артефакты монорепозитория» до него нарушалось
молча. Реактор: **1423 файла, 948 тестов** (+46 файлов, +33 теста к
входу шага), 0 дефектов; компиляция с показом deprecation — 0
предупреждений в нашем коде.

**Аппрув `CODE`:** четыре фокуса, **9 находок**, все закрыты. Несущая —
`disaster` D1: пульс бился по расписанию независимо от живости
потребителя тем, то есть доказывал работу таймера и отрицал собственный
предмет. Закрыто операндом живости (слушатели запущены и держат
назначенные партиции) плюс правилом класса в `codestyle` §Джобы. Отчёт
— `phase-2-step-9-code-focuses.md`.

**`SYNC_DOCS_FROM_CODE`:** фокус `divergence` — **6 расхождений** (2
change, 4 add, 0 remove), все реконсилированы. Отчёт —
`phase-2-step-9-sync-docs.md`.

**Пост-хок концепт-гейт (§6a)** применён — на `CODE` вошли два
контрактных инкремента. Ряд **3 → 0**: `DOCS_CHECK_6` дал три гейтящих
находки, `GAPS_CLOSE_6` закрыл все три, `DOCS_CHECK_7` прошёл чисто.
Несущая из них — J3: ось «имя хоста ингресса» была объявлена, но её
первый потребитель приехал только этим шагом, и без патчей оверлеев
`stage` и `prod` получили бы ингресс и клиента провайдера с хостом
`dev`.

## Дельта `CODE`, накопленная шагом

Позиции назначены закрытиями и приезжают под-шагом `CODE`; здесь —
сводный перечень, чтобы он не жил только в отчёте закрытия.

| # | Что | Откуда |
|---|---|---|
| 1 | корень `ExchangeAccountController` (`auth`) → `/api/v1/auth/exchange-accounts`; call-site `AuthReadClient.java:49` тем же ходом | `GAPS_CLOSE_2`, C1 |
| 2 | поверхность резолва членства у `auth`, заводящая тенанта с членством `OWNER` при первом предъявлении | `GAPS_CLOSE_2`, C2 |
| 3 | вывод тенанта контекста из членств: одно — контекст, ноль — заведение, больше одного — недостижимо | `GAPS_CLOSE_2`, C4 |
| 4 | подписка периметра на `trading-core.facts` и `strategies.facts`; тем `auth` не заводить | `GAPS_CLOSE_2`, C5 |
| 5 | клиенты реалма: публичный браузерный. **Служебного клиента у периметра НЕ оказалось нужно** — к владельцам он ходит под токеном пользователя; расхождение с формулировкой закрытия снято на `CODE` | `GAPS_CLOSE_2`, C6 |
| 6 | `Ingress` окружения и выдача сертификата | `platform.md` §Безопасность (V7 прогона `DOCS_CHECK_2`) |
| 7 | пульс потока; показ его отсутствия — вход шага 12 | `GAPS_CLOSE_2`, D1 |
| 8 | заголовок роли `X-Tenant-Role` — константа и её чтение | `GAPS_CLOSE_1`, A4 |
| 9 | выдача и проверка билета подписки: точка выдачи под bearer, подпись на общем секрете реплик, срок как калибровочная величина | `GAPS_CLOSE_4`, G1 |

**Перечень — дополнение к работе шага, а не её план.** Сборка модуля,
конфигурация ресурс-сервера, манифест развёртывания и тесты в него не
входят и входить не обязаны: здесь только то, что **назначено
закрытиями** и иначе жило бы лишь в их отчётах.

## Связи

- Отчёты прогонов — `phase-2-step-9-docs-check-1.md`,
  `phase-2-step-9-docs-check-2.md`, `phase-2-step-9-docs-check-3.md`,
  `phase-2-step-9-docs-check-4.md`, `phase-2-step-9-docs-check-5.md`.
- Отчёты закрытий — `phase-2-step-9-gaps-close-1.md`,
  `phase-2-step-9-gaps-close-2.md`, `phase-2-step-9-gaps-close-3.md`,
  `phase-2-step-9-gaps-close-4.md`, `phase-2-step-9-gaps-close-5.md`.
- Процесс — `.claude/processes/roadmap-step-execution.md`.
