# Секции бэклога шага 12, закрытые кодовыми заходами 2026-09-24

## На какой вопрос отвечает этот файл

Какие секции рабочего бэклога закрыли кодовые заходы шага 12 фазы 2 за
2026-09-24.

## Снято

- **§«Снимок определения несёт числовые ключи базы владельца, и копия у ядра
  не заводится»** (находка `F6`, владелец `code-writer`). Контракт Д2079
  исполнен с обеих сторон: маппер формы `StrategyActivatedMessage` у
  `strategies` строит копию дерева без ключей узлов, а
  `StrategyMapper#domainToPersistence` у ядра ключей ни одного узла не
  переносит. Пробы стыка — `StrategyEventFormTest` у производителя и
  `StrategyTreeCopyTest` у ядра; обе падают на прежней форме. Пять клеток `E2`
  сквозного набора прошли зелёными вместе с меткой `debt`. Форма перехода —
  `docs/models/mapping/Strategy.md`.
- **§«Числовой ключ базы владельца едет в снимке определения»** (находка F-5
  ящика `strategies`, владелец `code-writer`) — сторона производителя того же
  дефекта, закрыта тем же ходом: клетка `B6.2` зелена, метка `debt` с неё
  снята. Соседняя величина F-7 (колонки аудита узлов в снимке) дефектом не
  является и осталась наблюдением — §«Колонки аудита каждого узла едут в
  снимке определения».
- **§«Донорский числовой ключ инструмента у определения в общей библиотеке»**
  (владелец `code-writer`) — поле `instrumentId` снято с `Strategy`; мапперы
  сервисов резолвят ключ строки из `instrumentInternalId`, как и прежде.
- **§«Пробы формы снимка определения у производителя нет»** (владелец
  `tester`) — предмет построен тем же ходом: проба производителя пишет снимок
  формой провода и читает его обратно (`StrategyEventFormTest`).

- **§«Налив входной ноги на отправленном входе не наблюдается ничем»**
  (находка `F-13` ящика ядра, владелец `code-writer`) — заход 150.
  Обработчик отправленного входа при молчащем рабочем блоке отдаёт добычу
  живой ноги вместе с позицией, налитой — одну позицию; звенья названы явно.
  Добыча едет наблюдением, отдельным от работы, и работу уровня сделки не
  занимает (`docs/processes/fsm-execution-layering.md` §«Добыча не занимает
  проход»).
- **§«На штатном выходе сделки защитный стоп снимается раньше, чем закрыта
  позиция»** и **§«Дочистка транша под каскадом не доходит до звена,
  наблюдающего её же результат»** (владелец `trading-core`) — тем же
  заходом, одним ходом, как требовали обе: транш снимает защиты только при
  нулевой экспозиции, под сворачиванием при живом риске позиции молчит,
  закрытие нетто-экспозиции уровня сделки идёт раньше снятия защит; каждое
  снятие едет с добычей своей сущности, стоящее намерение не повторяется
  (`docs/rules/exit-teardown-order.md` §«Защиты снимаются последними»).
- **§«Две команды выходного контура ядра написаны исполнителем и не
  эмитятся ничем»** (находка `F4` сквозного набора, владелец
  `trading-core`) — тем же заходом: снятие встроенной защиты эмитит
  дочистка транша (причина `CANCELED_BY_STRATEGY`, новое значение перечня
  встроенной защиты), добычу движений — обработчик координированного выхода
  после терминальности траншей и записей закрытия
  (`docs/components/DealExitPendingHandler.md` §«Рабочая логика»).

- **§«Пустое намерение reduce-only делает ногу входа невидимой своему
  траншу»** (владелец `code-writer`) — заход 151. Валидатор определения
  отвергает действие-заявку без объявленного намерения кодом
  `STRATEGY_ACTION_REDUCE_ONLY_NOT_DECLARED`; счётчик —
  `docs/spec/strategy-reference.json` §`reduceOnlyIntentNotDeclared`, дом —
  `docs/rules/strategy-validation.md`; эталон определения объявляет
  намерение отрицанием. Клетки `U33` валидатора.
- **§«Пустой тип закрытия на добытой записи роняет терминальное ребро
  отказом»** (находка `F6` кейсов расчёта ядра) — заход 151: счёт исхода
  эпизода приводит пустое значение к пустой строке до проверки
  принадлежности; клетка `U6.7` зелена, метка `debt` снята.
- **§«Инвариант `reduce-only` объявлен домом, а писателя у него нет»**
  (находка `F-2` ящика коннектора) — заход 151, **снятием клейма**: дом
  `docs/integrations/okx/rules/reduce-only-invariant.md` объявляет, что
  посылочной сверки нет, и называет, чем держится последствие
  неисполненного намерения (сверка экспозиции, второй эпизод). Клетка
  `B2.12` ящика коннектора переписана и зелена.
- **§«Принятое закрытие позиции, оставившее остаток, не повторяется»** —
  заход 151: закрытие со стоящим намерением повторяется за добычей того же
  прохода, исполнитель закрытия плоскую позицию не закрывает
  (`docs/rules/exit-teardown-order.md` §«Защиты снимаются последними»).
- **§«Сверка экспозиции траншей с нетто-размером поднимает ступень на
  гонке двух добыч»** — заход 151: проход при расхождении добывает обе
  стороны заново и ступени не просит; ступень поднимает детектор `A11` с
  гистерезисом в два тика (`docs/models/domain/aggregate/Deal.md`
  §«Экспозиция сделки и сверка с биржей»).
- **§«Исчерпание бюджета у звена без классификации реакции не
  производит»** (находка `F6` кейсов выхода) — заход 151: учёт бросает
  исчерпание на обоих повторяемых классах, и перехватчик прохода уводит
  системную строку ошибочной тропой (`docs/components/ServiceCommandExecutor.md`
  §«Исходов исполнения три, а не два»).

- **§«Чтение заявки с площадки отказывает на переводе рода заявки»** и
  **§«Тип заявки: снапшот несёт тип исполнения площадки под именем доменного
  бизнес-типа»** (находка `F-7` ящика коннектора, `M-8` кейсов маппинга) —
  заход 152: тип исполнения площадки (`ordType`) в снапшот не приземляется и
  в род заявки не переводится — род заявки наш, его пишет создатель ноги
  (`docs/models/mapping/Order.md` §«`OrderExternalSnapshot` → `Order`»).
  Клетки `B3.1`, `B3.2`, `B3.4`, `B3.5`, `B3.7`, `B3.8`, `B10.4`, `B10.5`
  зелены, метка `debt` снята; юнит `U21.18` красен на прежней форме.
- **§«Юнит-проба перехода снапшота в заявку стои́т на недостижимом
  состоянии»** (находка `F-8`, владелец `tester`) — тем же заходом, после
  снятия дефекта, как требовал порядок секции: гашение `ordType` снято у обеих
  проб, вход стои́т на реальном типе исполнения.
- **§«Отказ разбора ответа площадки уезжает ядру классом «негодный вход»»**
  (находка `F-1`) — заход 152: разбор ответа у шлюза коннектора идёт под
  сетью, и значение вне формы контракта уезжает
  `EXTERNAL_INVARIANT_VIOLATION`; контур секрета вне перечня —
  `CREDENTIALS_UNAVAILABLE` (`docs/components/IntegrationService.md`
  §«Классы отказа на границе — дом здесь»). Класс `INVALID_REQUEST` остался
  только негодному операнду вызывающего — машинное условие секции поэтому не
  держится намеренно. Клетки `B1.6`, `B6.4`, `B6.5` зелены.
- **§«Классы отказа границы разъехались со своими тропами»** (находка `F-9`)
  — тем же заходом: транспортный отказ площадки уезжает
  `EXCHANGE_UNREACHABLE`, отказ соединения с хранилищем —
  `SECRET_STORE_UNAVAILABLE`. Клетки `B1.7`, `B7.5`, `B7.6`, `B7.8` зелены.

- **§«Ручная полная остановка на паре снятия риска не производит: контекст
  поверхности сделки не несёт»** (находка `F4` сквозного набора, владелец
  `trading-core`) и **§«Ручная полная постановка инструментного радиуса
  роняет снятие риска»** (находка `F-14` ящика ядра) — один механизм, закрыт
  заходом 153: инструментная половина снятия риска читает нетерминальные
  сделки пары сама, а сделка, поднявшая сигнал проходом, идёт своим
  контекстом (`docs/components/KillSwitchService.md` §«Два радиуса»). Клетки
  `U8.6`, `U8.8`, ящик — `B6.14`, красная на прежней форме.
- **§«Счётный kill-switch подтверждает снятие риска, не видя позиций без
  сделки»** (владелец `trading-core`) — тем же заходом: исполнитель закрывает
  позицию на инструменте без нетерминальной сделки и подтверждает радиус
  срезом позиций; позиция вне контура закрыта быть не может и оставляет отчёт
  незакрытым (`docs/components/KillSwitchExecutor.md` §«Риск вне графа
  сделок»). Группа `U15`, клетки `U8.9`, `U8.10`, ящик — `B6.15`; `B7.2`
  переписана под новый исход.

- **§«Позицию инструмента вне контура снятие риска закрыть не может»**
  (владелец `code-writer`) — закрыта заходом 154: валюта расчёта у входа
  закрытия коннектора стала необязательной (площадка её для изолированной
  маржи не требует), и снятие риска закрывает позицию вне контура без неё
  (`docs/components/KillSwitchExecutor.md` §«Риск вне графа сделок»). Клетки
  `U15.3`, ящик ядра — `B7.2`, ящик коннектора — `B2.15`; все три красны на
  прежней форме.

- **§«Структурная валидация записи закрытия построена на одну проверку из
  трёх»** (находка `F-3` ящика коннектора) — тем же заходом: читатель
  источника отвергает запись с пустым готовым net либо пустой его валютой
  классом `EXTERNAL_INVARIANT_VIOLATION`; прочие проверки дома уже стояли там,
  где значение разбирается (`docs/models/mapping/PositionCloseResult.md`
  §«Структурная валидация — до маппинга»). Клетка `B5.4` зелена с четырьмя
  вариантами, метка `debt` снята.

- **§«Единый error-DTO у поверхностей соседних сервисов»** (клетки `B8.2`,
  `B8.6` коннектора и долговые клетки формы тела у `auth`, `market-data`,
  `trading-core`) — тем же заходом у всех четырёх: глобальный обработчик
  наследует обработку отказов контейнера и ловит непредусмотренное, обе точки
  входа фильтр-цепочки делегируют в общий энфорсер отказа
  (`docs/rules/error-handling-policy.md` §«Внешняя поверхность», §«Отказ
  доступа — тот же контракт, что и прочие ошибки»). Метка `debt` снята с
  тридцати трёх клеток; `B11.7` ядра, закреплявшая долг отрицанием,
  перевёрнута; у `B12.6` ядра исправлено ожидание ступени вне словаря.
- **§«У периметра обработчик отказа авторизации не проведён в
  ресурс-сервер»** (находка `F7`, `U13.3`) — тем же заходом: у `bff`
  обработчик отказа авторизации проведён и в ресурс-сервер.

- **§«Javadoc коннектора называет снятые конструкции донора»** (находка `F-4`)
  — тем же заходом: javadoc точки отправки больше не называет потребителями
  код-тесты, коллекцию и эндпоинт `/raw`, читатель источника — несуществующий
  класс. Машинное условие секции не сработало бы никогда: подстрока `/raw`
  стои́т в пути `docs/rules/raw-exchange-dto-boundary.md`, на который ссылается
  javadoc коннектора; секция закрыта по предмету.

- **Кластер маппинга коннектора — заход 155** (находки `M-1`, `M-2`, `M-3`,
  `M-5`, `M-7` документа `.claude/tests/cases/okx-mapping.md` и `F9`
  `.claude/tests/cases/jsonb-overlay-roundtrip.md`), шесть секций:
  - §«Размер встроенной защиты из тела родителя в снапшот не переносится» —
    перенос построен (`U8.8`);
  - §«Отказ разбора ставки комиссии проглочен маппером вопреки контракту
    границы» — маппер отказ не глотает, пустые ставки и ключ группы
    отвергает структурная валидация читателя (`U16.6`-`U16.8`, ящик `B5.9`);
  - §«Снапшот рыночного среза не держит конвенцию пустой строки источника» —
    маппер среза разбирает конвертером границы (`U20.3`-`U20.5`);
  - §«Ветка секундного бара маппера таймфрейма: дом объявляет бросок, код
    отдаёт строку» — дом приведён к контракту источника,
    `docs/models/mapping/TimeFrame.md` (`U31.9`); код не менялся;
  - §«Перегрузка материализации рыночной цены с ключом не вызывается ничем» —
    перегрузка снята (`U19.9`), `docs/models/mapping/MarketPriceData.md`
    выровнен;
  - §«Javadoc маппера коннектора объявляет границу персистентности, которой у
    него нет» — снята у четырёх мапперов коннектора, а не у одного.

- **Кластер преконтроля ядра и реджект источника цены — заход 156**
  (фокус `trading`, `T9`, `T10`; пробел `G2` ящика `strategies`), три секции:
  - §«Перенос в безубыток не охраняется от постановки до прохода цены за
    уровень» — перенос уровня на прибыльную сторону обязан лечь за марк-ценой
    живой позиции, иначе откладывается кодом `STOP_LOSS_BEYOND_MARK_PRICE`
    (член карв-аута);
    `docs/rules/risk-policy.md`, `docs/spec/stop-distance.json`
    (`transferStopBehindMark`); кейсы `U30`, `U22.9`, `U25.12`;
  - §«Первый вход не охраняется от ликвидации, пустое плечо проходит
    преконтроль» — пустое плечо у действия, создающего риск, отвергается
    кодом `LEVERAGE_NOT_CONFIGURED`; писатель плеча — поверхность ядра
    `PUT …/pair-settings/{счёт}/{инструмент}` (`docs/rules/trading-constraints.md`);
    кейсы `U2.15`, `U2.18`, ящик `B4.14`, `B11.12`, `B11.13`. Расчётная
    ликвидация первого входа остаётся секцией A13;
  - §«Источник цены `MARK_PRICE` / `INDEX_PRICE` не отвергается создáнием» —
    реджект `STRATEGY_PRICE_SOURCE_UNAVAILABLE` на всех трёх носителях —
    размещение, операнд шага, операнд клаузы классификации фазы (`U22.8`,
    `U22.11`, `U27.16`, `U27.17`, `U26.12`).

- **База срабатывания защиты — заход 157**, две секции:
  - §«Защитный трейлинг «только по MARK» не исполняет, а пустая база
    срабатывания уходит `last`» — пустую собственную базу защиты триггерной
    ветки создатель определения записывает `MARK`
    (`Strategy#applyProtectiveTriggerDefaults`, Д2114); у трейлинга база до
    площадки не доезжает — ограничение названо в
    `docs/models/domain/core/AlgoOrder.md`; кейсы `U16.30`-`U16.33`, `U15.29`,
    ящик `B1.21`;
  - §«Встроенная защита входа проходит создание с базой срабатывания `LAST` /
    `INDEX`» — область ограничения `MARK` включает встроенную защиту входа
    (Д2115, `docs/rules/strategy-validation.md`); кейс `U19.9`.
- **Читатель исхода округления выхода — заход 157**, одна секция:
  §«Журнальные отчёты исходов округления выхода — читателя `exitOutcome` в
  ядре нет» — `ExitRoundingReader` у исполнителей создания заявки и условной
  заявки: `SKIPPED` — строка исполнения в `SKIPPED` без команды, `FULL` —
  команда уходит; оба пишут отчёт происшествия по строке исполнения с
  операндами округления (Д2116); пропущенная строка исчерпывает пакет шага
  (Д2117, `docs/rules/strategy-step-once-per-episode.md`). Кейсы — `U16`,
  `U9.20`-`U9.22` документа `trading-core-safety.md`, `U23.9`
  `trading-core-fsm.md`.
- **Три ожидания кейсов по риску и якорь себестоимости — заход 157**, две
  секции:
  - §«Три ожидания кейсов по риску ждут сверки с кодом» — подтвердились
    пункты 1 и 2 и два подпункта из трёх пункта 3: переключение защиты
    читает предикат нарушения покрытия
    по дому (`trancheViolated`), а не упрощённую копию (`U19.12`, `U19.17`);
    отрицательный риск акта обрезается нулём (`U10.6`); пустая стоимость
    контракта — отказ `INSTRUMENT_RULES_MISSING` (`U10.3`, `U13.7`, `U15.9`),
    необъявленная толерантность возраста снимка средств — снимок несвеж
    (`U17.13`). Пустой заявленный риск сделки (`U12.4`) оставлен — Д2120;
    якорь от средней цены эпизода — объявленный выбор дома;
  - §«Якорь себестоимости преконтроля резолвится мимо предиката живого
    эпизода» (R-6) — якорь — ветвь по предикату живого эпизода дома, без
    отката к плановой цене; пустой якорь у риск-создающего акта — отказ
    `CALCULATED_ACTION_INVALID` (`U4.3`-`U4.5`, `U6.8`, `U10.5`). Условие
    закрытия секции было неисполнимо (плановая ветвь якоря законно читает
    ту же цену) — закрыта по предмету.

- **§«Четыре решения кейсов, торгово спорные, ждут перебора владельца»**
  (владелец `trading-specialist`) — заходы 157-158, все четыре пункта:
  подтяжка стопа под ступенью инструмента разрешена — Д2122 (величина
  `actStopKeepsProtection` спеки покрытия, транш проведён в преконтроль);
  отказ отмены либо закрытия без строки исполнения объявляется журнальным
  отчётом `ANCHORLESS_COMMAND_REFUSED` — Д2123
  (`docs/components/ServiceCommandExecutor.md`); пропуск частичного выхода
  ниже минимума оставлен — Д2121; риск на стопе без проскальзывания и спреда
  оставлен, дом дописан знаком смещения — Д2124
  (`docs/rules/risk-policy.md` §«Что называется риском»).

- **§«Прогон реактора не отказывает кодом 2 без демона Docker»** (владелец
  `code-writer`) — заход 158. `tools/reactor-test.sh` спрашивает демон
  (`docker info`, клиент переопределяется `REACTOR_DOCKER`) до сноса классов
  и отказывает кодом 2; ось 6 доказана батареей той же команды (клетки 9-11 —
  демон молчит, отвечает, клиента нет). Норма — `.claude/skills/test-code.md`
  §«Детерминизм»; довод — `.claude/decisions/test-contour-design-pass.md`
  §«9. Docker — предусловие реактора, а не условие пропуска тестов».

## Где остальное

Записи заходов — хроника шага 12 фазы 2
(`.claude/work/progress/phase-2-step-12-chronicle.md`), заходы 149-158.
