# Шаг 4 фазы 1: концепт-ревью закрыто (TOOLING → DOCS_CHECK_5)

## На какой вопрос отвечает этот файл

Что мы уже сделали: итог концепт-ревью шага 4 фазы 1 («Команды и их
жизненный цикл») — от тулинга интегратора до чистого `DOCS_CHECK_5`
и пройденного гейта `CODE`.

## Итог (2026-06-11)

Концепт-ревью шага 4 закрыто; **гейт `CODE` пройден**. Сам шаг не
`DONE` — впереди CODE-фаза (написание кода + ревью + аппрув,
SYNC_DOCS_FROM_CODE).

- **Тулинг + прогоны интегратора 1-3:** материализован `integrator`
  (роль, процесс `api-docs-completion`, скилл `integration-okx`);
  канал чтения офдока — самообслуживание (статический HTML, сырой
  fetch + локальный парсинг; решение B — только дистиллят); **весь
  продуктовый периметр OKX докачан до поле-уровневых доков
  офдок-grade** (18 новых контрактов, манифест без пробелов,
  sync-шапки по `external-source-sync`).
- **И-1 закрыт исходом (а)** (ветвление cancel-пути по семье algo;
  продуктовый факт trailing-защиты — `Strategy.md`); **И-2**
  (cancel-advance-algos выведен из офдока) — провалидирован,
  runtime-снятие в demo trading ждёт кредов; **И-3** (advance не
  амендится) — следствие закрыто решением REPLACE-only.
- **К-1/Т-1 (`DOCS_CHECK_4`) → REPLACE-only:** пользователь расширил
  развилку вариантом (г); проработка `solution-designer` (крен
  (г)/(г-2)) провалидирована **чисто** (1-я чистая валидация гейта
  роли, счёт 1 из 3); `GAPS_CLOSE_3` разнёс дельту (~30 доков:
  enum 19→17, `StrategyActionType` CREATE/REPLACE/CANCEL,
  identity-цепочка `replacesInternalId` + `REPLACED_BY_STRATEGY`,
  порядок ног по риск-классу, риск-скоуп на place-ноге); решение —
  `docs/decisions/replace-not-amend.md`.
- **`DOCS_CHECK_5` — чисто** (греп-верификация без dangling,
  link-integrity, закрытия прежних итераций интактны, торговый гейт
  без блокеров) → концепт-гейт `CODE` вновь пройден.

## Детальные артефакты

Подпапка `2026-06-11-phase-1-step-4-concept-review/`: gap-отчёты
`DOCS_CHECK_1-5`, отчёты интегратора (прогоны 1-3), проработка К-1
(`phase-1-step-4-k1-replace-design.md`). Хронология под-шагов — в
пометках `.claude/work/roadmap/phase-1.md`.

## Хвост в CODE-фазу

Код-дельта REPLACE-only (Java-енумы, amend-исполнители, пример
`trend-following-ema.json`, валидатор); рантайм-проверка Flyway
`V1`-`V5` + тик-джоб (из v43, за пользователем); И-2
runtime-проверка (ждёт кредов demo trading); CMD-Q4 — форвард шага
6/8 с заметкой об окне двойной reduce-only защиты при REPLACE.
