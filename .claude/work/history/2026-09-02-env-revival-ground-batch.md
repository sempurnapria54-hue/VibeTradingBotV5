# Оживление среды: снятие срока ожидания и добор грунта

## На какой вопрос отвечает этот файл

Что было сделано 2026-09-02 при оживлении demo-контура (снятие ENV-Q1,
добор грунта).

## Итог

- **Среда ожила до срока (2026-09-04): решение
  `.claude/decisions/env-wait-deadline.md` исполнено оживлением** —
  состав исхода в §«Исход решения» самого решения. Очередь расчёта
  разобралась сама 03:18–05:16 UTC (ответа по тикету 38426982 не было):
  четыре замороженные заявки исполнились (открыв позиции ETH/BTC/SOL),
  две отменились. Проба из кода подтвердила терминал фактом состояния
  (лимитная заявка → отмена → `state=canceled` ~2 с); одноразовый
  probe-класс удалён после прогона (Д375).
- **Позиции-хвосты закрыты руками** (`close-position` ETH/BTC/SOL, счёт
  flat); их записи закрытия несут ненулевой `fundingFee` — побочное
  подкрепление п. 7. Класс «уборка кейса на ветви отказа — руками»
  остаётся (Д378).
- **Сняты носители срока и запрета:** оси `срок`/`по-сроку` обоих
  компонентов реестра гейтов, раздел бэклога «Срок ожидания среды
  (2026-09-04)» (перенесён сюда), запрет живых заявок. Дом среды
  приведён к факту снятой записью
  (`.claude/tests/source-api/okx/code-preconditions.md` §«Что среда всё
  ещё не даёт»).
- **Слот п. 2 × `AG6.2` закрыт** первым прогоном фикстуры (8 кейсов
  зелёные): 177 кандидатов — `observations/AG6_2.md`; ответная половина
  была закрыта 2026-09-01. `DealCashFlow` грунтом больше не гейтится.
- **Позиция грунта A4а закрыта** (раздел бэклога «Позиция грунта»
  перенесён сюда): ts bill'а входного филла **равен** `cTime` эпизода
  (0 мс), посылка однородности покрытий подтверждена включающим `>=` —
  `observations/AG1_5.md`, реестр гейтов A4а §«чем» (Д380).
- **Слот п. 7 × `AG1.7`** — прогон в окне границы 20:00 UTC исполнился
  целиком и **добыл предмет пункта** (эпизод пережил расчёт, запись
  закрытия несёт ненулевой `fundingFee` сразу после закрытия), но
  сверка кейса с bills разошлась операндом и принесла **новый факт
  источника**: на isolated-марже расчёт финансирования ложится в
  `posBalChg` при нулевом `balChg` (равен `fundingFee` записи до
  последнего знака). Кейс правлен на Σ(`balChg`+`posBalChg`), `posBalChg`
  проведён сырым фактом через слои (V19), раннер перевзведён на границу
  00:00 UTC (Д387, Д388). **Перепрогон 2026-09-03 закрыл слот**:
  `BUILD SUCCESS`, сверка сошлась точно, оба знака `fundingFee`
  предъявлены двумя прогонами, Verify.end чист — **открытых слотов
  грунта не осталось**. Ложная тревога
  «молчание раннера» 2026-09-02 ~20:30 разобрана: часы хоста сверены с
  сервером OKX (0 с), проверка в UI прошла до открытия позиции —
  смешение локального времени с UTC.
- **`RefreshBillsExecutor` и `DealCashFlow` закодированы** (решение
  держателя: исполнитель не ждёт funding-кейс — привязка слота к
  компоненту снята, примечание в реестре гейтов). Состав и названные
  ограничения — `.claude/work/progress/phase-1-step-7-code-tail.md`
  §Т9; 173 юнит-теста зелёные, подъём с V16-V19 пройден.
- **Средовые починки попутно:** контур поднят с нуля (Docker, Vault
  распечатан держателем), системные часы хоста ресинкнуты — отставали от
  серверного времени OKX на ~3 ч, подпись не проходила (Д376).

## Указатели

- Исход решения о сроке — `.claude/decisions/env-wait-deadline.md`.
- Дом фактов среды — `.claude/tests/source-api/okx/code-preconditions.md`.
- Наблюдения — `.claude/tests/source-api/okx/observations/{AG1_5,AG6_2}.md`.
- Решения Д375–Д380 — `.claude/work/decision-digest.md`, итерация
  2026-09-02 (11).
- Хроника — `.claude/work/progress/phase-1-step-7-chronicle.md`.
