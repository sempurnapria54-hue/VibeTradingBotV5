# DOCS_CHECK_6 — шаг 7 фазы 1 «Сделки и P&L»

## На какой вопрос отвечает этот файл

Каков исход подтверждающего прогона концепции (`concept-review` ×2 +
`trading-review`) после `GAPS_CLOSE_5` — чисто ли доспецифицирован материал
шага под CODE, или остались пробелы.

## Контекст

- **Под-шаг:** `DOCS_CHECK_6` (процесс `roadmap-step-execution.md`),
  подтверждающий прогон после `GAPS_CLOSE_5` (материализована только
  свалидированная политика; структурные развилки withhold — тестовый
  материал этого прогона).
- **Прогон:** три независимых ревьюер-субагента — concept линза-A
  (механика / холды / командный слой / компоненты / правила / джобы),
  concept линза-B (модели / mapping / native / персистентность,
  field-level), линза-C (торговая корректность, грунт — дистиллят корпуса).
  Линзам запрещены отчёты прошлых итераций / снапшоты / заметки
  (свежий взгляд). **Первый прогон по обновлённой машине:** формат
  `templates/docs/gap-report.md` (обязательные цитаты на находке),
  ось структурной свободы, верификация код-клауз по `src/`.
- **Верификация CC:** несущие атрибуции гейтящих находок перепроверены
  чтением целей и грепом (V1-V7, §ниже) независимо от субагентов.
- **Нумерация `H#` — локальная для этого отчёта.** Ссылки на находки
  субагентов: `F-A#` / `F-B#` / `F-C#`.

## Охват

Аппарат холдов (instrument-hold, exchange-hold, error-handling-policy,
risk-validator-scope, risk-creating-entry-protection,
controlled-exchange-exceptions, runtime-error-classification,
SafetyHoldCoordinator, KillSwitchService/Executor, HoldSignal, AnomalyReport
×2); fee-путь (TradeFeeRate ×3, InstrumentExternalRules ×3, sync-job,
DataService, trade-fee contract); риск-слой (RiskValidator, SizeCalculator,
RiskCheckResult, per-trade-risk-policy, CalculationContextFactory);
bills-путь (DealCashFlow ×2, OkxAccountBillResponse, account-bills);
positions-history (PositionCloseResult mapping, OkxPositionsHistoryResponse,
Refresh*/Finalize*/Mark*-executor'ы); ядро (Instrument, Exchange, Deal ×2,
DealFinalizationState ×2, Auditable); решения (pnl-finalization-mechanics,
result-profit-source, source-model-change-absorption,
controlled-violation-exchange-wide-hold); trading-constraints,
persistence-representation, coverage-manifest; процессы (deal-management,
risk-evaluation); рабочие файлы (open-questions.md, backlog.md §9 / §Шаг 7);
test-план §AG-якоря. `src/` — только верификация код-клауз доков.

## Стадия остановки

**Прошли все стадии; НЕ чист.** Стадия 0 чиста (все три линзы независимо:
ни один открытый вопрос механику шага 7 не гейтит). Стадия 1 чиста.
Дефекты — стадии 2 + стейл-носители решений/backlog.

**Ключевой итог:** политика `GAPS_CLOSE_5` (мягкая реакция, радиус
per-режим, блок-сет по направлению риска) **подтверждена всеми линзами как
торгово корректная и согласованная в правилах** — но (1) два носителя
канона не реконсилированы (среди них **CODE-инструкция backlog**), и
(2) у мягкой реакции не специфицированы снятие и enforcement — ось
постановки проработана богаче оси снятия.

---

## Узел 1 — Стейл-носители реакции на несвежесть

### H1 — `pnl-finalization-mechanics.md` реш.4 и CODE-пункт backlog предписывают снятую реакцию (kill-switch + ручное снятие) [F-A1 + F-B1 + F-C1 — все три линзы независимо]. Тип: несогласованность. **ГЕЙТИТ.**

- **Цитаты (носители дефекта):** `docs/decisions/pnl-finalization-mechanics.md:211-222`
  («Реакция — полная, уровня 4: … + **kill-switch** по нему + `AnomalyReport`,
  снятие **вручную**… Более слабого класса холда ("гейт входа" без
  kill-switch…) **не заводим**») — верифицировано CC (V1);
  `.claude/work/backlog.md:308-310` (CODE-пункт fee-wiring: «kill-switch +
  `AnomalyReport` + ручное снятие») — верифицировано CC (V2).
- **Канон (не требует правки):** `instrument-hold.md:52-56`,
  `error-handling-policy.md:91-101, :111`, `TradeFeeRate.md:207-211`,
  `InstrumentExternalRulesSyncJob.md:124-128`,
  `per-trade-risk-policy.md:128-131`, `coverage-manifest.md:139`; сам
  backlog в другом месте фиксирует снятие (`backlog.md:350-354`).
- **Почему гейтит:** backlog-пункт — прямая инструкция исполнителю CODE;
  риск — CODE реализует отвергнутый вариант. Посылка установлена
  (канон ратифицирован, 6 согласных носителей).
- **Провенанс дефекта:** остаток закрытия H2 `GAPS_CLOSE_5` — обход по
  списку цитат находки выполнен, но оба носителя в списке цитат H2
  отсутствовали, а свип «сверх списка» шёл грепом по `docs/` и не покрыл
  ни хвост реш.4, ни рабочий файл backlog (см. §Пайплайн).
- *Владелец:* `knowledge-curator` (реконсиляция; содержание ратифицировано,
  ре-решать нечего). *Ярлык исхода:* `выводимо-Предложение`. *Дефицит:*
  `работа`. *Флаг CC:* `предложил`.

---

## Узел 2 — Снятие и enforcement мягкого холда

### H2 — Снятие мягкого запрета входов не задано нигде; enforcement для радиусов «группа»/«аккаунт» не специфицирован [F-A2 + F-B2 + F-C2]. Тип: name-level без структуры. **ГЕЙТИТ.**

- **Цитаты:** `instrument-hold.md:118-132` (§Снятие явно ограничен
  kill-switch-холдами; про мягкий — ни слова);
  `error-handling-policy.md:111-112` (в таблице реакций у «холд с
  kill-switch» есть «снятие вручную», у мягкой — оси снятия нет);
  `InstrumentExternalRulesSyncJob.md:86-88` («**выставляет** холд» — кто
  снимает, не сказано); `instrument-hold.md:134-142` (enforcement =
  статус инструмента, «полная модель — backlog п.9»; для радиуса «аккаунт»
  механика массового статуса не задана); `TradeFeeRate.md:155-160`
  (измеритель самоизлечим по построению — напряжение с «выставляет» как
  sticky-записью).
- **Грунт (линза C):** шаблон риск-процесса — «что измеряем / уровень /
  действие / **условие отката действия**» [Carver AFTS, тактика 4, PDF
  с.636-637] — пункт «откат» отсутствует.
- **Варианты (собраны из всех линз; ось структурной свободы применена):**
  1. **авто-снятие по измерителю** — возраст < порога → входы открыты
     (симметрично постановке; риск флип-флопа на мигающей интеграции);
  2. **ручное снятие** — единая конвенция с kill-switch-классом; против
     H2-довода (реакция действует при нулевом радиусе ущерба; заморозка
     дольше отказа, забытый холд);
  3. **переструктурировать: холд — не state, а вычисляемый предикат**
     гейта входа (`TradeFeeRate.isFresh()` уже задан) — «выставлять/
     снимать» нечего, авто-снятие и радиус «аккаунт» бесплатно (предикат
     ложен у всех групп при отказе вызова), `AnomalyReport` дедупится
     отдельно (не на тик). Снимает и H3-зависимость (писателя статуса
     нет). **Крен всех трёх линз независимо**;
  4. гибрид: авто-снятие + гистерезис / K подряд успешных чтений
     (вклад C: порог восстановления доверия после длинного отказа —
     policy-выбор, не выводится).
- *Владельцы:* `solution-designer` (механика) + `trading-specialist`
  (семантика восстановления доверия к интеграции, цена паузы входов);
  policy-остаток — пользователь. *Ярлык исхода:* `варианты-с-креном`
  (крен — вариант 3). *Дефицит:* `работа`. *Флаг CC:* `эскалировал`.

### H3 — Доковые инвентари статусов стейл против кода: `TRADE_BLOCKED` материализован в обоих енумах [F-A3 + F-B3]. Тип: несогласованность (док↔код). **ЗАВИСИМАЯ от H2.**

- **Цитаты:** `instrument-hold.md:136-139» («`Instrument.Status = HOLD`,
  енум уже есть» — указывает на **онбординговый** статус);
  `Instrument.md:49-52» (7 членов, `TRADE_BLOCKED` нет);
  `Exchange.md:36-37, :55-56, :62-66» («в енуме нет ни того, ни другого»,
  «материализация отложена на п.9»).
- **Код (верифицировано линзами и ранее CC):**
  `Instrument.java:94-104` — `HOLD` («не вовлекается в онбординг») **и**
  `TRADE_BLOCKED` («safety-холд… Отдельный смысл от онбординг-HOLD»);
  `Exchange.java:42-66` — `TRADE_BLOCKED` в енуме + `isTradeBlocked()`.
  Побочный отказ буквального чтения правила: писатель выставил бы
  онбординг-`HOLD` и вывел инструмент из scope синка
  (`InstrumentExternalRulesSyncJob.md:92-94`).
- **Запись зависимости:** гейтит, если H2 разрешится вариантами 1/2
  (писатель обязан знать константу; в `TRADE_BLOCKED` вшито «снятие —
  вручную» ⇒ мягкому классу может понадобиться третье имя). При
  варианте 3 — не применима как гейт; остаётся не-гейтящая правка
  инвентарей `Instrument.md`/`Exchange.md` и клаузы правила.
- *Владелец:* `knowledge-curator` (инвентари) + `solution-designer`
  (хвост имени при H2-исходе 1/2). *Ярлык исхода:*
  `выводимо-Предложение`. *Дефицит:* `работа`. *Флаг CC:* `предложил`.

### H4 — `AnomalyReport.scope`: ярлыки L3/L4 остались (H6-снятие не доехало), носителя радиусов «группа»/«аккаунт» нет [F-A5 + F-C6]. Тип: несогласованность + name-level. **ЗАВИСИМАЯ от H1/H2.**

- **Цитаты:** `AnomalyReport.md:30` («`INSTRUMENT` (L3) / `EXCHANGE`
  (L4)») — верифицировано CC (V5); `:64-67` (то же в инвариантах) —
  против `HoldSignal.md:50-53` (ярлыки сняты, H6 `GAPS_CLOSE_5`) и
  `error-handling-policy.md:114-116` (перечень радиусов трёхчлен).
- **Запись зависимости:** ярлыки-часть — выводимая правка (не гейтит);
  носитель радиуса гейтит, если состав CODE включает журналирование
  мягкого холда с радиусом группа/аккаунт (по текущему backlog-пункту —
  включает, но сам пункт — H1).
- **Варианты (вклад C):** (а) расширить `HoldScope`
  (`INSTRUMENT_GROUP`/`ACCOUNT`); (б) `scope=INSTRUMENT` + перечень
  инструментов в слепке; (в) код аномалии несёт радиус, scope двучлен.
  Крена нет — сцеплено с моделью enforcement (H2/п.9).
- *Владелец:* `knowledge-curator` (ярлыки) + `solution-designer`
  (носитель радиуса). *Ярлык исхода:* `выводимо-Предложение` (ярлыки) +
  открытый выбор носителя. *Дефицит:* `работа`. *Флаг CC:* `предложил`.

---

## Узел 3 — Cross-ccy guard

### H5 — Guard мёртв: `ccy=USDT` в запросе и `ccy` в линковке отсекают нарушение до guard'а, вопреки трём декларациям «не отбрасывается молча» [F-A6 + F-B6]. Тип: несогласованность (счётом). **ГЕЙТИТ.**

- **Цитаты:** `account-bills.md:88` (query `ccy=USDT`) против `:107-108`
  («не отбрасывается молча фильтром — помечается») — **в одном файле»;
  верифицировано CC (V6); `:90-93` (линковка по `ccy`);
  `RefreshBillsExecutor.md:14-15, :29-31`; `mapping/DealCashFlow.md:233-236`;
  `DealCashFlow.md:103-109`; `trading-constraints.md:76-84` (guard —
  **единственный** детект инварианта, «проактивного рычага нет»).
- **Счёт (обе линзы независимо):** bill с `ccy=OKB` не приходит в ответ ⇒
  строка не возникает ⇒ guard не срабатывает никогда; расхождение сверки
  тоже не возникает (OKB-комиссия не входит ни в net settle-ccy, ни в
  Σ USDT-bills) — нарушение невидимо полностью. Рантайм-кейс AG3.4/RQ-4
  наблюдает инвариант только на тестовой фикстуре, runtime-guard не
  заменяет.
- **Варианты (из обеих линз; ось структурной свободы):** (а) убрать `ccy`
  из query и из match-критериев; ветка на записи: совпала → штатно; нет →
  персист + линковка + `AnomalyReport`, из Σ`amount` и разбивки
  исключается (крен обеих линз); (б) второй контрольный запрос без `ccy`
  при финализации; (в) признать детект test-only и снять клаузу
  (ослабляет инвариант); (г) **переструктурировать:** наблюдатель —
  отдельный периодический скан bills аккаунта вне окна сделки (зона
  `AnomalyJob`, шаг 8; в фазе 1 детекта нет). Хвост при (а): судьба
  несматченных записей, валюта в области Σ`amount`,
  `DealCashFlow.dealId` «null до матчинга» недостижим в БД при текущем
  потоке (вклад B).
- *Владелец:* `solution-designer` + `integrator` (`ccy` в query
  опционален по контракту). *Ярлык исхода:* `варианты-с-креном`.
  *Дефицит:* `работа`. *Флаг CC:* `эскалировал`.

---

## Узел 4 — Онтология AnomalyReport

### H6 — Lifecycle не вмещает журнальные аномалии шага 7 (без kill-switch) и не знает актуальных управляющих [F-A4]. Тип: name-level + несогласованность. **ГЕЙТИТ.**

- **Цитаты (верифицировано CC, V4):** `lifecycles/AnomalyReport.md:10-22`
  (управляющие: TradeRuleValidator / AnomalyJob / KillSwitchExecutor —
  ни `SafetyHoldCoordinator` (реактивная тропа шага 6,
  `SafetyHoldCoordinator.md:36-47`), ни производители шага 7);
  `:44-53` (матрица: единственный путь к `COMPLETED` — через
  `KILL_SWITCH_EXECUTED`). Производители шага 7 без kill-switch:
  `FinalizeDealExitExecutor.md:34-40` (сверка), `pnl-finalization-mechanics.md:277-282`
  («severity умеренная — не холд»), `InstrumentExternalRulesSyncJob.md:122-126`
  (несвежесть), `MarkDealEmergencyClosedExecutor.md:33-34`.
  `AnomalyReport.md:47-51` — `Severity` бинарна, оба значения в
  kill-switch-терминах.
- **Варианты (ось структурной свободы):** (а) журнальная тропа
  `CREATED → COMPLETED` (слепки при создании) + уточнить `Severity` вне
  kill-switch-терминов; (б) **переструктурировать:** развести
  «журнальную/аудит-аномалию» и «инцидент реактивной обработки»
  (дискриминатор kind / отдельная сущность); (в) объявить шаги
  опциональными по severity (матрица перестаёт быть проверяемой).
  Крен — (а) для шага 7; (б) — если аудит-класс вырастет на шаге 8.
- *Владелец:* `solution-designer`. *Ярлык исхода:* `варианты-с-креном`.
  *Дефицит:* `работа`. *Флаг CC:* `эскалировал`.

---

## Не гейтят / зависимые (прочее)

### H7 — Принцип «радиус ограничивает реакцию» не прогнан по L3-триггеру «серия неудач»; класс неудач не определён [F-C3]. **НЕ ГЕЙТИТ.**
Цитаты: `instrument-hold.md:26-31, :94-99`; `error-handling-policy.md:91-99,
:149-153` (провизорен только порог, не класс). Entry-side неудачи → живая
сделка вне радиуса, kill-switch платит спред+хвост [Harris гл.4 с.71-72;
Tharp гл.6 с.158-159]; management-side → kill-switch адресен [Vince, введ.,
с.6]. Варианты: (а) единая как есть; (б) различить классы (крен C);
(в) записать цену явно. *Владелец:* `trading-specialist`. *Исход:*
`варианты-с-креном`. *Дефицит:* `работа`. *Флаг:* `предложил`.

### H8 — Две «пустые» клетки матрицы искажений непусты [F-C4]. **НЕ ГЕЙТИТ** (форвард, фаза ожидаемости).
Цитаты: `backlog.md:350-354, :364`. (a) L4-flatten чужих здоровых сделок
(`controlled-violation-exchange-wide-hold.md:37-42` + каскад
`KillSwitchService.md:31-37`) → «исходы × пессимистично» непуста;
(b) окно несвежести 0-24 ч двусторонне: даунгрейд тира в окне → сайзинг по
заниженной ставке → позиция больше положенной → «возможности ×
оптимистично» непуста [Vince гл.1 с.9,18 — перебор хуже недобора].
*Владелец:* `trading-specialist`. *Исход:* `выводимо-Предложение`.
*Дефицит:* `работа`. *Флаг:* `предложил`.

### H9 — Свежесть меряется у значения ставки, не у ключа резолва: протухший `externalFeeGroupId` резолвится в чужую свежую строку [F-C5]. **НЕ ГЕЙТИТ.**
Цитаты: `InstrumentExternalRulesSyncJob.md:111-114` (изоляция источников
без реакции на отказ instruments); `TradeFeeRate.md:155-160, :199-205`;
`InstrumentExternalRules.md:79, :190-199`. Класс отказа — «молча неверная
ставка» (сам проект считает болезнью: `InstrumentExternalRulesSyncJob.md:52-59`).
Крен: записать довод «громкость прочих отказов покрывает» + расширить RQ-3
на mismatch ключа. *Владелец:* `trading-specialist` + `solution-designer`.
*Исход:* `выводимо-Предложение`. *Дефицит:* `подтверждение` (частота смены
`groupId` — рантайм-датчик). *Флаг:* `предложил`.

### H10 — Посылка «`fee` — компонента `amount` той же записи» не записана; под-случай fee-эха ломает Σкомиссии и realized [F-B7]. **ЗАВИСИМАЯ от AG3.5.**
Цитаты: `mapping/DealCashFlow.md:72-73, :90-93, :166-178`;
`DealCashFlow.md:41`; `plan.md:1395-1409` (AG3.5 вопрос эха не заявлен).
Счёт: раздельная гранулярность + информационное `fee=−f` на trade-записи ⇒
Σ`externalFee` двоит `f`, realized = `pnl + f` ≠ `pnl`; Σ`amount` сходится —
сверка слепа. Гейтит, если AG3.5 покажет эхо; прогон и так до CODE. Крен:
записать посылку-инвариант + расширить AG3.5 (инспекция `fee` на
pnl-записи). *Владелец:* `solution-designer` + tester (AG3.5). *Исход:*
`варианты-с-креном`. *Дефицит:* `подтверждение` + `работа`. *Флаг:*
`предложил`.

### H11 — AG3.5 несёт неограниченный суперсет-claim (H15 не доехал до плана); кросс-ссылки доков на гранулярность указывают §AG1.5 вместо §AG3.5 [F-B8]. **НЕ ГЕЙТИТ.**
Цитаты: `plan.md:1398-1404`; `mapping/DealCashFlow.md:74-75`;
`DealCashFlow.md:69-70`; `OkxAccountBillResponse.md:60-62»; `plan.md:1303`
(AG1.5 = агрегация N11), `:1388` (гранулярность = AG3.5). *Владелец:*
`knowledge-curator` + tester. *Исход:* `выводимо-Предложение`. *Дефицит:*
`работа`. *Флаг:* `предложил`.

### H12 — Резолв «по паре» у читателей против тройки ключа/индекса (`exchangeId` опущен) [F-B9]. **НЕ ГЕЙТИТ** (SWAP-only, одна биржа).
Цитаты: `TradeFeeRate.md:150-152, :246-249` (ключ и индекс — тройка);
`InstrumentExternalRulesDataService.md:32-35`, `instrument-hold.md:45-48`,
`mapping/TradeFeeRate.md:122-124` (формулировки «по паре»). Свести к
«(`exchangeId`, пара)» — DataService знает биржу через инструмент.
*Владелец:* `knowledge-curator`. *Исход:* `выводимо-Предложение`.
*Дефицит:* `работа`. *Флаг:* `предложил`.

### H13 — Носитель `PositionCloseResult`-снапшота между `REFRESH_POSITIONS_HISTORY` и `FINALIZE_DEAL_EXIT` не назначен (параллель N7) [F-B10]. **НЕ ГЕЙТИТ.**
Цитаты: `mapping/PositionCloseResult.md:49-52` («транзитный — не
персистится»); `RefreshPositionsHistoryExecutor.md:14-16, :38-39`
(лексика хранения); `FinalizeDealExitExecutor.md:12-23` («на биржу сам не
ходит»); `pnl-finalization-mechanics.md:47-60` (durable-слот решён для
числа, не для снапшота). Крен: (а) контракт оркестрации — refresh+finalize
в одном проходе, снапшот in-memory, рестарт → повторная эмиссия
идемпотентного refresh. *Владелец:* `solution-designer`. *Исход:*
`варианты-с-креном`. *Дефицит:* `работа`. *Флаг:* `предложил`.

### H14 — Операнды окна сделки (`begin`/`end`) не названы; в перечне носителей — невводимый `TradeFill` [F-B13]. **НЕ ГЕЙТИТ.**
Цитаты: `RefreshBillsExecutor.md:26-28`, `account-bills.md:83-87` (формула
окна без полей-носителей); `Deal.md:139-141` («через
`Order`/`Position`/**`TradeFill`**/audit») против `Deal.md:122-123`
(«persisted `TradeFill` не вводится»). *Владелец:* `solution-designer`
(носители окна) + `knowledge-curator` (фантом). *Исход:*
`принято-в-работу`. *Дефицит:* `работа`. *Флаг:* `предложил`.

### H15 — «`REFRESH_FILLS` снят: убран из енума, удалён executor» — прошедшее время против кода [F-B4]. **НЕ ГЕЙТИТ.**
Цитаты: `result-profit-source.md:92-95`; `pnl-finalization-mechanics.md:355-358`;
`coverage-manifest.md:101`. Код (верифицировано CC, V3):
`ServiceCommandType.java:48` — `REFRESH_FILLS` есть;
`RefreshFillsExecutor.java` существует; `REFRESH_POSITIONS_HISTORY`/
`REFRESH_BILLS`/`MARK_DEAL_EMERGENCY_CLOSED` в енуме отсутствуют. Дельта —
целевая (CODE шага 7), формулировать целевым временем, не свершившимся.
*Владелец:* `knowledge-curator`. *Исход:* `выводимо-Предложение`.
*Дефицит:* `работа`. *Флаг:* `предложил`.

### H16 — `MarkDealClosedExecutor` несёт до-N8 клаузу «число всё равно проставляется… не ноль/null» [F-A7]. **НЕ ГЕЙТИТ.**
Цитаты: `MarkDealClosedExecutor.md:50-52` против `lifecycles/Deal.md:139-143`,
`MarkDealEmergencyClosedExecutor.md:31-34`, `pnl-finalization-mechanics.md:80-84`.
*Владелец:* `knowledge-curator`. *Исход:* `выводимо-Предложение`.
*Дефицит:* `работа`. *Флаг:* `предложил`.

### H17 — `BALANCE_NOT_FRESH`: валидатор «возвращает», реестр — «не эмитится» [F-A8 + F-C гипотеза 5]. **НЕ ГЕЙТИТ.**
Цитаты: `RiskValidator.md:137-139` против `RiskCheckResult.md:77-79`,
`risk-evaluation.md:37-39`. *Владелец:* `knowledge-curator`. *Исход:*
`выводимо-Предложение`. *Дефицит:* `работа`. *Флаг:* `предложил`.

### H18 — Мета-клауза `controlled-violation-exchange-wide-hold` об офдоке устарела (офдок уже выровнен) [F-A9]. **НЕ ГЕЙТИТ.**
Цитаты: `controlled-violation-exchange-wide-hold.md:93-100` против
`controlled-exchange-exceptions.md:51-56`. *Владелец:* `knowledge-curator`.
*Исход:* `выводимо-Предложение`. *Дефицит:* `работа`. *Флаг:* `предложил`.

### H19 — `triggerPx`: три версии применимости по `type` в трёх носителях [F-B5, расширено CC]. **НЕ ГЕЙТИТ.**
Цитаты (верифицировано CC, V7): `contracts/position.md:82` («type 3/4/5»);
`OkxPositionsHistoryResponse.md:42` («3–6»);
`result-profit-source.md:43` («3/4/5/6»). Влияет на структурную валидацию
погранслучая ADL; число не затрагивает. *Владелец:* `integrator` (сверка с
офдоком — внешний источник правды). *Исход:* `неразрешимо-владельцем` без
офдока. *Дефицит:* `грунт`. *Флаг:* `предложил`.

### H20 — `externalTakerFeeRate`: `external*`-имя несёт не-сырое значение; механика `× −1` на строковом носителе не названа [F-B11]. **НЕ ГЕЙТИТ.**
Цитаты: `InstrumentExternalRules.md:82-83` (конвенция «сырое») против
`TradeFeeRate.md:69-70`; `mapping/TradeFeeRate.md:38, :115-117`;
`OkxTradeFeeResponse.md:88-89`. Девиация знака сознательна и помечена;
оговорка конвенции и носитель негации — мелкие хвосты. *Владелец:*
`knowledge-curator` / `code-writer` (носитель — CODE). *Исход:*
`выводимо-Предложение`. *Дефицит:* `работа`. *Флаг:* `предложил`.

### H21 — Легенда coverage-manifest не определяет метку «⚪ не-runtime» [F-B12]. **НЕ ГЕЙТИТ.**
Цитаты: `coverage-manifest.md:50-58` против `:101-102`. *Владелец:*
`knowledge-curator`. *Исход:* `выводимо-Предложение`. *Дефицит:* `работа`.
*Флаг:* `предложил`.

---

## Верификация CC (V1-V7)

- **V1:** `pnl-finalization-mechanics.md:211-222` — стейл-реакция
  подтверждена чтением (H1).
- **V2:** `backlog.md:308-310` — CODE-пункт с kill-switch подтверждён (H1).
- **V3:** греп `ServiceCommandType.java` — `REFRESH_FILLS` жив (:48),
  новых команд нет; `RefreshFillsExecutor.java` существует (H15).
- **V4:** `lifecycles/AnomalyReport.md` — управляющие и матрица
  подтверждены чтением (H6).
- **V5:** `AnomalyReport.md:30` — ярлыки L3/L4 подтверждены (H4).
- **V6:** `account-bills.md:88 + :90-93 + :107-108` — противоречие
  query-фильтра и guard-декларации в одном файле подтверждено (H5).
- **V7:** греп `triggerPx` — три версии в трёх носителях; находка F-B5
  расширена третьим носителем (`result-profit-source.md:43`) (H19).
- Ранее верифицировано CC (инспекция `GAPS_CLOSE_5`): `Instrument.java`
  — `HOLD` и `TRADE_BLOCKED` оба в енуме (H3).

## Подтверждено-ОК (сводно; ≥2 линз и/или верификация CC)

- **Политика `GAPS_CLOSE_5` в правилах согласована и торгово корректна:**
  мягкая реакция с радиус-доводом (C: цена пропуска входов не принята за
  ноль — зарегистрирована с корпусными указателями); радиус per-режим +
  «синк выключен — не источник» идентичны в трёх носителях; блок-сет =
  граница `risk-validator-scope` (взаимные ссылки сходятся); таблица
  scope × reaction на месте.
- **Ядро P&L-цепочки согласовано во всех носителях** (источник → добыча →
  staged-число → терминал; tx-связка `FINALIZE` пишет / `MARK` ассертит;
  null="неисчислимо" ≠ 0; два провенанса EMERGENCY_CLOSED) — единственный
  выброс H16.
- **Знаковый контур ставки end-to-end** (× −1 одним местом; `+ commissions`
  верно в обе стороны; асимметрия с `externalFee` помечена с обеих сторон);
  **арифметика трёх областей суммирования сходится счётом** (funding вне
  realized, в сверке); ветки null/0 заданы.
- **Сырая пара как ось группы — на всех носителях; механизм свежести
  `refreshCount` замкнут** (встречные ссылки `Auditable` ↔ `TradeFeeRate`);
  fee-seam единообразен в восьми носителях; null-политика
  реджект-vs-несвежесть разведена одинаково в трёх местах.
- **Kill-switch контур связен с обеих сторон** (риск-минимизирующий порядок
  teardown; эскалация `EXCHANGE_KILL_SWITCH_RESIDUAL`; per-deal best-effort
  каскада); стоп-ликвидационная геометрия и реджект-vs-fallback — торгово
  выверены [C].
- **Все §AG-якоря плана существуют** (AG1.5/AG3.4/AG3.5/AG12.4/AG12.5/M1.7,
  PENDING до CODE); N11 везде помечен предположением; исполнимость реш.1
  (обе refresh-команды имеют эмитентов и executor'ы) подтверждена.
- **Стадия 0 чиста** — три линзы независимо: открытые вопросы механику
  шага 7 не гейтят.

## Гипотезы, не подтвердившиеся (сводно)

«HoldScope без GROUP — дыра» (мягкая тропа не идёт через HoldSignal);
«двойная реакция на отсутствие ставки» (случаи разведены в трёх местах);
«гонка двух писателей навеса» (писатель один); «двойной счёт ребейта»
(знаковое суммирование неттует); «funding втягивается в realized» (защита
есть); «`groupId` unused» (в used с офдок-цитатой); «bills-матчинг присвоит
чужие движения» (инвариант одной активной сделки); «L3-kill-switch смещает
R-выборку» (для истинных риск-триггеров — часть системы-с-safety; остаток
ушёл в H8); «манипуляция извне наводит холд» (триггеры не ценовые);
«`externalTs` — дубль `Auditable`» (семантика различена).

---

## Тест правки B — структурные развилки из withhold: подняты ли линзами сами

**Да — все гейтящие переоткрыты независимо, со структурными вариантами в
наборах** (линзы прошлых отчётов не читали):

- H5-имя (`HOLD` vs `TRADE_BLOCKED`) → **H3**: переоткрыт двумя линзами,
  причём **по коду** (правка «код-клауза верифицируется по коду»
  сработала — прежний прогон верифицировал только по докам) + расширен на
  `Exchange`.
- H9-снятие → **H2**: переоткрыт всеми тремя линзами; структурный вариант
  «предикат вместо статуса» — **крен всех трёх независимо**.
- H6-радиус → **H4**: вариант расширения `HoldScope` в наборе.
- H17-cross-ccy → **H5**: переоткрыт двумя линзами счётом, в наборе
  структурный вариант «отдельный скан-компонент».
- H11-ключ → **H12** (пара vs тройка переоткрыта на оси формулировок).
- H1-тропа — **сузился по построению**: мягкая реакция сняла нужду в
  kill-switch-тропе; остаток (enforcement/писатель) вошёл в H2.

**Не переоткрыты** — только не-гейтящие хвосты прежнего прогона: H13
(обратный резолв группа→инструменты; частично поглощён H2-вариантом 3),
H16 (`OTHER` роняет комиссию из разбивки), H19 (критерий «значение
группы»). Держать в поле зрения при сверке; гейта не несут.

## Пайплайн — кандидат в правку

**Свип «сверх списка цитат» после политикоменяющего закрытия — не
специфицирован.** Кейс H1: закрытие H2 `GAPS_CLOSE_5` прошло по списку
цитат находки (правка A выполнена), но носители вне списка выпали из
свипа — греп шёл по одному термину и только по `docs/` (рабочие файлы —
backlog CODE-пункт — не покрыты; хвост реш.4 лежал за пределами читаного
окна). Кандидат: закрытие, меняющее политику, обязано завершаться
свип-грепом по терминам старой политики (напр. «kill-switch», «ручное
снятие») по **всем живым носителям** (docs + рабочие файлы), с отметкой
каждого попадания. Решение — за пользователем.

---

## Сводка

**21 находка (H1-H21). Гейтят — 4:** H1 (стейл-носители реакции, вкл.
CODE-инструкцию backlog), H2 (снятие/enforcement мягкого холда), H5
(cross-ccy guard мёртв), H6 (lifecycle AnomalyReport). **Зависимых — 3:**
H3 (от H2), H4 (от H1/H2), H10 (от AG3.5). **Не гейтят — 14.**

**Агрегация по ярлыкам исхода:** `выводимо-Предложение` — 13;
`варианты-с-креном` — 6 (H2, H5, H6, H7, H10, H13); `принято-в-работу` — 1
(H14); `неразрешимо-владельцем` — 1 (H19). **По дефициту:** `работа` — 18;
`подтверждение` — 2 (H9, H10); `грунт` — 1 (H19). **По владельцу:**
`knowledge-curator` — 11; `solution-designer` — 7; `trading-specialist` — 4;
`integrator` — 2; tester (соучастие AG3.5) — 2.

**Узлы:** (1) стейл-носители реакции — H1; (2) снятие/enforcement мягкого
холда — H2+H3+H4 (центр тяжести; настоящая конструкторская развилка —
state vs предикат); (3) cross-ccy — H5; (4) онтология AnomalyReport — H6.

**Характер прогона:** политика `GAPS_CLOSE_5` подтверждена; остаток — (а)
незакрытая реконсиляция двух носителей (H1 — выводимая правка), (б)
незаданная ось снятия/enforcement (H2 — развилка к пользователю через
владельцев), (в) два независимых от `GAPS_CLOSE_5` пробела (H5 cross-ccy,
H6 lifecycle) и хвост гигиены (H11-H21, преимущественно курация).

**Исход: `DOCS_CHECK_6` НЕ чист → `GAPS_CLOSE_6`.** Гейт `CODE` не снят.
Эскалаций к пользователю — 4 узла (тяжёлая — узел 2). Рантайм-фикстура
§AG1.5 — по-прежнему после чистого прогона (H10 расширяет её вопросник —
AG3.5).
