# GAPS_CLOSE_10 — шаг 7 фазы 1 «Сделки и P&L»

## На какой вопрос отвечает этот файл

Каков исход закрытия находок `DOCS_CHECK_10`.

## Контекст

Под-шаг исполнен **в две части**.

- **Часть 1** — слепой проход **прерван на четвёртом вопросе**: вскрылось,
  что несущий принцип механики рефреша существует устно и в носители как
  класс не попал. Содержание: запись принципа, переклассификация находок
  против него, решения пользователя по узлам B и C, правки процесса (§1-4).
- **Часть 2** — закрытие всего остатка: решения пользователя по H8, H10,
  H14, H17, H18, H26 плюс находки, ушедшие владельцам без участия
  пользователя; правки процесса; инвентарь джоб (§6-10).

**Все 27 находок `DOCS_CHECK_10` закрыты.** Нумерация парная
(`DOCS_CHECK_N` → `GAPS_CLOSE_N`), поэтому закрытие идёт под тем же
номером второй частью; следующая пара — `DOCS_CHECK_11`.

---

## 1. Принцип команд рефреша: был записан как случай, обобщён до класса

**Формулировка пользователя:** команда рефреша ходит несколькими запросами
к источнику и останавливается, когда нашла нужную информацию. Это общий
принцип класса, не свойство отдельной команды.

**Что нашлось в носителях** (греп + чтение, не предположение):

| Носитель | Что несёт | Чего не несёт |
|---|---|---|
| `docs/rules/command-lifecycle.md:52-59` | правило есть, дом верный (`rules/` = «какое правило действует») | формулировка **разрешительная** («может обойти несколько эндпоинтов»), лестницы перечислены **per-command**, **предиката остановки нет** |
| `docs/decisions/refresh-evidence-cycle-ownership.md:29-33` | предикат остановки дословно: «обрываются на **первом успешном эндпоинте**, полный обход — только при не-найдено» | это `decisions/` («почему решили так»), и клауза прикреплена к **перечню трёх команд**, из которых `REFRESH_FILLS` снят |

**Диагноз — записан случай, а не класс.** Решающий симптом: каждая новая
многоэндпоинтная команда потребовала **отдельной ноты-обновления** в
решении — одна под `REFRESH_BILLS_COMMAND` (пагинация 7d → архив), вторая
под `REFRESH_POSITION_COMMAND` (live → positions-history, с оговоркой «а
терминал эта не выносит»). Класс-правило таких нот не требует: новая
команда просто ему подчиняется.

**Записано** (`docs/rules/command-lifecycle.md` §Правило, клауза «Принцип
обхода — общий для класса `REFRESH_*`») — три следствия, применимые к любой
refresh-команде, существующей и будущей:

1. **Предикат остановки — «факт добыт», не «эндпоинт опрошен».** Лестница
   эндпоинтов конкретной команды — деталь её компонент-дока, в реестр правил
   не выносится.
2. **Факт не добыт ⇒ команда не завершена.** Отсутствие искомого — не
   успешное завершение с пустым результатом; промежуточные записи по пути
   завершением не являются.
3. **Исчерпанный цикл — самостоятельный исход, а не отказ.** Он **не
   инкрементирует счётчик отказов** и не питает серию неудач: отказ — это
   невозможность спросить, а не отрицательный ответ источника.

Плюс следствие для стадии составного действия: «звено сделано» выводится из
**добытого факта**, а не из дельты значения и не из памяти прохода;
отдельной метки «команда отработала» класс не требует.

`refresh-evidence-cycle-ownership.md` получил шапку-указатель: ноты
сохранены как провенанс, новые per-command ноты туда не добавляются.

**Почему это первопричина класса находок.** Линзы читают доки; устный
принцип для них не существует. Отсутствие **записи** механики читается как
отсутствие **механики**, и находка формулируется как «предикат не
определён» — с полным набором вариантов, которые предлагают выбрать уже
выбранное.

---

## 2. Переклассификация 27 находок против принципа

Список кандидатов чата (H1, H2, H4) проверен по носителям, а не принят;
H9 подтверждён; H20 закрыт попутно как выводимый из правки процесса.

### Снято принципом — развилкой не является (3)

| # | Было | Стало |
|---|---|---|
| **H1** | «подтверждённый факт звена для не-bills `REFRESH_*` не определён; предикат эмиссии финализации невычислим»; ⚠-клауза гейтила механику меткой узла 7 | **Гейт снят.** Он вырос из прочтения «звено сделано» как **дельты значения** («refresh не меняет данных, если факт не изменился»), тогда как предикат класса — **добыт ли факт**. Предикаты выписаны по звеньям выходной тропы, все durable и без отдельной метки: `Position.status = CLOSED` + положение закрытия; окно закрыто и залинковано; терминальный статус ордера/algo; `externalUpdatedAt`, отданный самим источником (для баланса собственный измеритель не нужен). Узел 7 (измеритель свежести) как **самостоятельный** вопрос жив — но предикат завершённости звена больше не гейтит |
| **H9** | «сделка без наблюдавшейся `Position` не получает числа никогда»; три варианта, включая «резервную ногу» | **Решение пользователя: закрывается принципом.** Запрос к истории позиций по инструменту и окну — не нога, которую надо добавить, а поведение, уже следующее из «факт не добыт ⇒ продолжаем спрашивать». Снято ограничение «нога 2 только при наличии локальной `Position`»: недоставало не ноги, а **оси адресации**. Записано явно (этого не было ни в одном доке): позиция, впервые увиденная **уже закрытой**, заводится записью из истории и **тем же проходом** финализирует сделку, заполняя обе границы окна. **Хэнд-офф `integrator`** остаётся: какие оси запроса принимает история позиций и как ведёт себя при нескольких записях в окне |
| **H20** | индекс `AnomalyReport` по несуществующей колонке `hold_scope`; `anomaly_reports` вне «полной schema-дельты» | **Закрыто.** `hold_scope`/`holdScope` → `scope` в трёх клаузах (§Инварианты, реестр кодов, §Персистентность); в перечень schema-дельты добавлены `anomaly_reports` (индекс) и `instruments` (бэкфилл под H21). Правка выводима из записанного правила (§4) |

### Переформулированы — принцип снял часть, остаток живой (2)

| # | Что снял принцип | Что осталось |
|---|---|---|
| **H2** | Обе несущие части. (а) «терминальный исход пагинации не durable» — предикат переписан на durable: окно закрыто **и** движения залинкованы; (в) «звено завершилось до появления `end`» — невозможно: факт не добыт ⇒ звено не завершено, команда повторяется. (б) конфликт модели и исполнителя снят: `null` в `deal_id` **транзиторно наблюдаем**, а не «не наблюдаемое состояние фазы 1»; осиротевших строк ветка не производит, потому что сделка удерживает слот, пока факт не добыт | **Узкий выбор:** персистить ли прочитанные страницы **до** закрытия окна. Сохранено существующее поведение (персистит + до-линковка UPDATE'ом на последующем исполнении) — это статус-кво дока, не новый выбор; упрощение линзы B «не персистить до наличия `end`» остаётся живой альтернативой владельцу |
| **H4** | Ветку «легитимно пусто» целиком: исчерпанный цикл — самостоятельный исход, **счётчик отказов не трогает** и серию неудач не питает. Это снимает половину коллизии с общим учётом отказов | **Живое:** §5a приравнивает к «недоступно» **жёсткий отказ чтения** (ошибка вызова, отказ прав) — это настоящий отказ, принципом не покрыт. Компонент, применяющий приравнивание на аварийной тропе, по-прежнему не назван; приоритет между двумя живыми SYSTEM-исполнениями в одном проходе — тоже |

### Сужены принципом, но остаются (2)

- **H11** (носитель курса cross-ccy): структурное возражение снято —
  дополнительный запрос за котировкой **внутри** команды принципом
  легитимен, отдельной команды не требует. Содержательный пробел цел:
  эндпоинт, ось запроса, метод границы, политика отказа.
- **H17** («серия неудач» без носителя): область сузилась — легитимно
  пустой ответ в серию больше не входит, считаются только отказы. Носителя,
  владельца, точки инкремента и порога по-прежнему нет; довод «cleanup без
  анкера — его серия уже считается» по-прежнему опирается на счётчик,
  которого нет.

### Решены пользователем (2) — §3

**H6**, **H7**.

### Остались без изменений (18)

H3, H5, H8, H10, H12, H13, H14, H15, H16, H18, H19, H21, H22, H23, H24,
H25, H26, H27.

Принцип их не касается: это топология собственности (H3), транспорт
метрики (H5), риск-посылки (H8, H18), валюта и состав числа (H10, H12,
H13), границы линковки и допуска (H14, H15), идемпотентность журнала
(H16), схема и миграции (H19, H21, H22, H23, H24), форвард (H25, H26,
H27).

**Итог переклассификации: 27 → снято 3, переформулировано 2 (+2 сужены),
решено пользователем 2, осталось 18** (из них гейтящих — 8: H3, H5, H10,
H11, H14, H17, H19 и переформулированная H4).

---

## 3. Решения пользователя — узлы B и C

Дефицит у всех трёх — `подтверждение`, то есть пользовательский хвост, а не
адъюдикация чата. Внесены; финализация владельцем — на продолжении прохода.

### H6 — знаменатель `R`: вариант (2)

**`R` остаётся плановым**, но персистятся его операнды — reference-цена
входа и заявленный размер; разрыв «заявлено ↔ взято» становится измеримым,
поправка живёт в аналитике.

Крен линзы был **(1) + (2) как обязательное дополнение** — совпало
наполовину, разошлось в том, какое число живёт в поле.

**Контекст, вскрытый в разборе и записанный как довод:** стоп — абсолютный
ценовой уровень, рассчитанный от рантайм-цены и политики риска, на бирже не
плывёт; цена исполнения и объём входа уже читаются финализацией входа.
Значит фактически взятый риск **вычислим из имеющегося** без новых полей —
и reference-цена нужна не варианту (1), а именно варианту (2): как вход
решения, **против которого решение принималось**. Не хватало второй
половины сравнения, а не самого риска.

**Внесено:** `Deal.md` §«Плановый риск» — поля `plannedEntryPrice` /
`plannedSizeContracts` (имена предварительные), довод, явный отказ от
«писать в поле риск по факту входа» с ценой альтернативы;
`CreateOrderExecutor.md` — писатель той же транзакцией + предупреждение,
что `plannedEntryPrice` **нельзя брать с `Order.price`** (при market-входе
executor заполняет его только при `sendPriceToExchange`, то есть
reference-цена в сущности ордера не остаётся — верифицировано по коду
`CreateOrderExecutor.java:65-67`).

### H7 — `CLOSED` без числа и рамка R-выборки: вариант (4)

**Вариант, которого в отчёте не было:** на тропах закрытия без входа
пишется `0`, **но перед записью проверяется факт операций на бирже**; если
операции были — сделка идёт обычным путём финализации и получает реальное
число.

Крен линзы был **(1)** (переформулировать инвариант через факт входа,
оставить `null`) — разошлось.

**Основание:** тропа из `ENTRY_SUBMITTED` достижима из состояния, где
заявка уже на бирже и могла **частично исполниться**; чистый `0` там —
неверное число у состоявшейся сделки, и `closeReason` его не разоблачает
(код уже переиспользует `ENTRY_CONDITION_EXPIRED` на обеих тропах —
верификация V12). Инвариант чистого `CLOSED` при этом **не смягчается**:
число обязательно всегда, меняется лишь то, что на тропах без входа оно
равно нулю **по факту проверки**, а не по умолчанию.

**Рамка R-выборки — тем же предикатом.** Пользователь квалифицировал это
как вопрос статистики, не структуры: полей достаточно, нужно определение.
Записано: R-выборку образуют сделки со **состоявшимися операциями на
бирже**; несостоявшиеся входы — отдельный счётчик частоты (ось «частота
возможностей», не исход системы); неисчислимые исключаются как `unknown` и
считаются поштучно. Явно записано, почему предикат — не `closeReason` и не
непустота `plannedRiskAmount`: последняя истинна уже у входа, отменённого
до исполнения.

**Сцепление зафиксировано:** корректность исключения «как unknown» держится
на том, что число unknown'ов **известно**, то есть на H16 (схлопывание
маркеров анкером). Разбирать вместе.

**Внесено:** `trading-constraints.md` §«Гейт открытия сделки» (три тропы,
клауза проверки, почему обязательна); `Deal.md` §«Рамка R-выборки» (новый);
`PrecheckHandler.md`, `EntrySubmittedHandler.md` — клауза проверки на своих
тропах.

---

## 4. Правки процесса

| Правка | Носитель |
|---|---|
| **«Механика не определена» — сперва проверить на незаписанный принцип.** Признаки (однотипность у нескольких артефактов; накопленные per-случайные ноты; формулировка в `decisions/` при пустоте в `rules/`); сработало — находка идёт владельцу **запросом подтверждения**, а не набором вариантов | `.claude/skills/concept-review.md` §«Детекторы свипа» |
| **Кейсы в скилл-файлах — обезличенно**, без номеров находок и итераций: скилл — обязательный вход линзы, а ограничение каталога от него не защищает. Применено тем же ходом к `concept-review.md` (5 кейсов) и `trading-review.md` (1) | `.claude/rules/structure.md` §Принципы |
| **Свип политикоменяющего закрытия покрывает и док уровня, на который переехала собственность** — свип «по терминам старой политики» второй конец переноса не находит, там старых терминов может не быть вовсе | `.claude/processes/roadmap-step-execution.md` §3 |
| **Невалидированный крен переживает закрытие итерации** — переносится в отчёт следующей явной строкой с пометкой «носители не правились» | `.claude/processes/roadmap-step-execution.md` §3 |
| **Имена операндов индекса сверяются с фактической схемой** (по миграциям, не по доменному имени и не по соседнему доку); новая колонка шага обязана попадать в перечень schema-дельты | `docs/rules/persistence-representation.md` (новый §) |
| **Оценка цены варианта — проверяемая величина**: «цена нулевая» / «≈ 0» принимается, только если названо, из чего складывается и чем проверяется. Плюс: **названное предпочтение — это крен**, даже если записано «крена не требуется» (ярлык H24 занижен) | `.claude/templates/docs/gap-report.md` §«Набор полей находки» |
| **Сбор внешнего грунта не ждёт чистого прогона** для находок, у которых `грунт` — **единственный** ярлык дефицита | `.claude/processes/roadmap-step-execution.md` §4 |

**Про петлю «грунт после чистого прогона» — уточнение фактики.** Порядок
«рантайм-фикстура после чистого `DOCS_CHECK`, затем `CODE`» **нигде в
процессе не был записан**: он накопился практикой и жил только в хрониках и
снапшотах. То есть чинить было нечего — правило вводится **вместе с
исключением**, сразу в нужной редакции. Затронутые находки: H11, H13, H14 и
адресная часть H9 — у всех `грунт` без сопутствующей `работы`, значит по
новому правилу они уходят на сбор грунта, не дожидаясь чистоты остальных.

**Про H24.** Ярлык исправляется по новому правилу шаблона:
`выводимо-Предложение` → `варианты-с-креном` с двумя выставленными ветками
(SYSTEM-строки несут `target_entity_type = DEAL` + `target_entity_id` и
ловятся первым частичным индексом, либо оставляют цель пустой и ловятся
вторым). Предпочтение определяет, что пишет миграция, — значит это выбор.

---

## 5. Что уходило на продолжение прохода

**Гейтящих — 8:** H3, H4, H5, H10, H11, H14, H17, H19. **Мягко — 6:** H8,
H12, H16, H18, H21, H22. **Не гейтят — 3:** H13, H15, H23 (+ H24 с
исправленным ярлыком). **Форвард — 3:** H25, H26, H27.

Весь список закрыт частью 2 (§6-7).

**Сцепления, зафиксированные для разбора вместе:** H7 ↔ H16 (счётность
unknown'ов — **закрыто**, §7); H8 ↔ `RISK-Q3` (агрегат между сделками и
внутри одной — вопрос жив); H10 ↔ `CCY-Q2`; H21 ↔ `FEE-Q1`; H26 расширяет
`PNL-Q1` вторым потребителем.

---

# Часть 2 — закрытие остатка (2026-08-03)

Закрыт **весь** остаток §5: решения пользователя (H8, H10, H14, H17, H18,
H26) плюс находки, уходившие владельцам без участия пользователя. Открытых
находок `DOCS_CHECK_10` не осталось.

## 6. Решения пользователя

### H8 — агрегатная экспозиция: чинится посылка, не довод

**В фазе 1 торгуется один инструмент.** Крен линзы был **(3)** (оставить
отложенным, переписав довод) — расхождение существенное: пользователь
отверг саму посылку крена. После ограничения контура «агрегатной экспозиции
нет» становится **истиной**, а не допущением, и отсрочка уровней 2-3
перестаёт стоять на операционной дисциплине.

**Проведённое следствие (владельческое, пользователю не выносилось).** Раз
скан по докам уже умеет перебирать инструменты, «один инструмент» — не
констатация, а **ограничение с местом применения**. Выбран **гейт входа**:
`EntryScannerJob`/`DealOpeningService` к проверке «нет активной сделки по
этому инструменту» добавляют контурную — «нет активной сделки ни по
одному». Конфиг контура отвергнут: отбор торгуемого уже выражен статусом
инструмента, конфиг задвоил бы источник истины; гейт вдобавок
**фальсифицируем** — посылка проверяется кодом. Цена названа: DB-инварианта
нет (у `deals` нет колонки биржи), гонку тиков закрывает
`JobExecutionGuard`, на мультиинстансе гейт остаётся app-only — тот же
горизонт (фаза 3), что у прочей мультиинстанс-сериализации.

**Внесено:** `per-trade-risk-policy.md` §«Трёхуровневая модель риска»
(посылка + следствие + горизонт снятия) и §Альтернативы;
`trading-constraints.md` §Инструменты; `EntryScannerJob.md` §Шаги п.7;
`RiskCheckResult.md` §«Риск на сделку и экспозиция».
**Сцепление:** `RISK-Q3` не закрыт — тот же класс с радиусом внутри сделки
(записано в вопросе).

### H10 — валюта результата: авторитет один

**Вариант (1): авторитет — расчётная валюта инструмента.** Совпало с креном
линзы A; крен линзы B (не блокировать, помечать) выбором покрыт.
`Position.externalResultCurrency` остаётся **проверяемым признаком**: не
совпало → `AnomalyReport` `RESULT_CURRENCY_MISMATCH`, расчёт не
блокируется.

**Ветка «операнд пуст» — по образцу `FEE_RATE_UNAVAILABLE`,** градуированно:
реджект `SETTLE_CURRENCY_UNAVAILABLE` там, где риск ещё не взят (вход);
пометка тем же кодом там, где отказывать нельзя (запись движения,
финализация — валюта берётся с `Position` как явная деградация). Реджект на
входе делает нижние ветки достижимыми только для сделок, живых на момент
ввода.

**Внесено:** `Deal.md` §«Валюта результата: один авторитет» (новый) +
поле-описание + §«Итоговый PnL»; `mapping/PositionCloseResult.md` (строка
таблицы `Position → Deal` переписана, Validation); `FinalizeDealExitExecutor.md`;
`AnomalyReport.md` (два кода); `RiskCheckResult.md` (новый
`RiskCheckCode`); `pnl-finalization-mechanics.md` реш.5;
`OkxPositionsHistoryResponse.md` (used-строка `ccy`).
**Сцепление:** `CCY-Q2` (имя поля) остаётся открытым, имена предварительны.

### H14 — область сверки: список исключений по бирже

**Вариант (2) с уточнением от противного.** Линкуется всё; область
Σ-сверки задаётся перечнем **исключаемых** типов, конфигурируемым **по
бирже**. Совпало с креном и **усилило** его: при списке исключений новый
тип от биржи по умолчанию попадает в расчёт и, если он посторонний,
обнаруживается **сам** через непустую корзину `OTHER` — а не выпадает
молча, как в отвергнутом allow-list-варианте (1). Непустой `OTHER` даёт
`AnomalyReport` `UNCLASSIFIED_CASH_FLOW`.

**Хэнд-офф `integrator`** (штатная работа интегратора): конфиг по бирже —
новый носитель настроек; определить, где он живёт и кто правит его при
добавлении биржи. До назначения дома список для OKX **пуст** — честный
старт: посторонние типы проявятся через `OTHER` и расхождение сверки.

**Внесено:** `mapping/DealCashFlow.md` §«Область сверки задаётся списком
исключений по бирже» (новый) + §«Область суммирования»;
`domain/other/DealCashFlow.md` (енум `OTHER`); `account-bills.md`
§Использование п.5 + §«Область суммирования»; `FinalizeDealExitExecutor.md`;
`AnomalyReport.md` (код); backlog (хэнд-офф).

### H17 — серия неудач: блокировка работает, счётчик форвардится

**Блокировка инструмента работает в шаге 7**, поднимается по исчерпанию
бюджета попыток исполнения, **порог серии = 1**. Совпало с креном линзы (3)
с добавкой владельца: настоящий счётчик серии по инструменту **поверх
сделок** ведёт **`TradeGuardJob`** — новая джоба, проектирование за рамками
шага 7; клаузы про настоящую серию помечены форвардом на неё.

**Уточнение фактики:** `attemptCount` на строке исполнения считает попытки
**одного исполнения** и инструмент-scope не покрывает — это разные
счётчики, а не один с двух сторон.

**Довод «cleanup без анкера» переписан на бюджет.** Прежнее «его серия уже
считается на инструмент-scope, второй счётчик дал бы двойной учёт» —
**снято**: счётчика не существовало. Верная запись: анкера нет, потому что
нет исполнения-действия; следствие названо честно — отказы cleanup холд не
поднимают, учёт форвардится на `TradeGuardJob`.

**Внесено:** `instrument-hold.md` §«Носитель серии» (новый) + §Триггеры +
§«Серия неудач»; `SystemActionExecutor.md` §«Что не входит»;
`command-action-boundary.md` §2; `fsm-execution-layering.md`;
`ServiceCommand.md`; `AnomalyJob.md` §Смежное (граница с `TradeGuardJob`);
backlog §Шаг 8.

### H18 — переоценка инварианта «ликвидация за стопом»

**Вариант (1): проверять и реагировать.** Совпало с креном линзы. Проверку
ведёт существующая **`AnomalyJob`**; нарушение у ведомой позиции ведёт к
**перестановке защиты или контролируемому выходу**, не только к фиксации
аномалии. `RiskValidator` scope не расширяем — переоценка ведомой позиции
риска не создаёт.

**Возражение «ордер без защиты не создаётся» находку не снимает** (записано
доводом): защита есть, но цена ликвидации **едет после входа** — на
изолированной марже funding подъедает маржу, и стоп может перестать быть
первым на пути.

**Внесено:** `AnomalyJob.md` §«Переоценка инварианта» (новый);
`per-trade-risk-policy.md` §«Роль плеча»; `risk-validator-scope.md`
§«Переоценка вне создания риска» (новый); backlog §Шаг 8 (такт, гистерезис,
выбор реакции — детальная проработка на своём шаге).

### H26 — калибровка запаса на проскок

**Вариант (а):** фактическая цена выхода сохраняется, потребитель назван.
Совпало с креном. Симметрично H6: обе половины сравнения — заявленное
против фактического — оседают в базе.

**Внесено:** `Position.md` §«Цена фактического выхода» (новый) + поле
`externalCloseAveragePrice` + §Персистентность + §«Что не хранит»;
`mapping/PositionCloseResult.md` (снапшот, оба маппинга, native-перечень);
`OkxPositionsHistoryResponse.md` (`closeAvgPx` возвращён в used);
`per-trade-risk-policy.md` §«Без поправки на проскок»;
`pnl-finalization-mechanics.md` §Следствия (колонка); `PNL-Q1` (второй
потребитель записан, пункт 1 вопроса **не** закрыт).

## 7. Закрыто владельцами (без пользовательского прохода)

| # | Исход | Носители |
|---|---|---|
| **H2** (остаток) | Персист страниц до закрытия окна **сохранён как выбор**, а не умолчание: цена альтернативы — перечитывание страниц на каждом повторе (квота 5 req/s, растёт с длиной окна); дедуп `UNIQUE(external_bill_id)` делает персист безопасным | `RefreshBillsExecutor.md` |
| **H3** | Состав цикла **выводится из `DealContext`** (статус + runtime graph) — вариант (2), не крен (3). Довод против разведения на типы действия: троп-контекстов ≥3 (сопровождение — третий), а дискриминатор уже существует durable-полем `Deal.status`; новая ось enum'а его дублировала бы. Стейл-клауза «задаёт handler-контекст» снята | `SystemActionExecutor.md`, `command-action-boundary.md` §2 |
| **H4** (остаток) | Приравнивание жёсткого отказа применяет **`ServiceCommandExecutor`** (единственная ветка учёта), предикат — `Deal.status = ERROR`; попыток до приравнивания **ноль**; на аварийной тропе исчерпание бюджета **не поднимает `HoldSignal`**, а разрешает терминал; приоритет — **добыча раньше терминала**, порядок держит `ErrorHandler` | `pnl-finalization-mechanics.md` §5a, `SystemActionExecutor.md` |
| **H5** | Канал — **поля `CreateOrderCommandPayload`** (крен 3). Записана ось, которую смешивал прежний довод: «durable-слот не нужен» отвечает на «где живёт между проходами», а не «чем доезжает внутри прохода» | `CreateOrderExecutor.md` §«Канал доставки», `Deal.md` §«Плановый риск» |
| **H12** | **Симметрия** (крен обеих линз): аварийный терминал применяет то же cross-ccy-слагаемое. Цена проверяема: чтение `deal_cash_flows` по `deal_id` (индекс есть), вызовов биржи и миграций не добавляется | `MarkDealEmergencyClosedExecutor.md` §«Состав числа», `Deal.md` |
| **H13** | Посылка «net не содержит издержку вне settle-ccy» помечена **предположением**; вопросник §AG3.4 расширен инспекцией `realizedPnl` на сделке с cross-ccy-издержкой (фикстура та же) | `pnl-finalization-mechanics.md` реш.5, `tests/source-api/okx/plan.md` |
| **H15** | **Двухчастный epsilon** (крен 1): `min(композиционный по обороту, омиссионный по ожидаемой комиссии)`. Побочно **закрыт форвард** «форма epsilon слепнет с ростом оборота» — омиссионный член от числа движений не растёт | `pnl-finalization-mechanics.md` реш.5 §epsilon, `mapping/DealCashFlow.md`, backlog |
| **H16** | **Анкеры разведены по природе производителя** (крен линзы C, 3) — новая колонка `kind` (`STATE`/`EVENT`); индекс `where kind = 'STATE'`. Перечень кодов в предикате индекса отвергнут (хардкод реестра в схеме). `STATE`-отчёт остаётся открытым до ручного снятия холда — иначе анкер не срабатывает никогда. **Сцепление H7↔H16 закрыто:** счётность `unknown`'ов обеспечена структурно | `AnomalyReport.md` §Енумы/§Инварианты/§Персистентность, `lifecycles/AnomalyReport.md`, `Deal.md` §«Рамка R-выборки» |
| **H19** | **(2) для строк финализации + (1) для `target`-бэкфилла** (крен). Строки не переносятся (`DELETE` + `DROP TABLE`): перенос 2:1 конструировал бы состояние исполнения, которого не было, вместе с фальшивой историей попыток; фаза 1 не в проде. `target` (jsonb) STRATEGY-строк бэкфиллится в колонки + `DROP COLUMN` | `DealActionState.md` §«Правило переноса» (новый), `pnl-finalization-mechanics.md` §Следствия |
| **H21** | **Крен (3) отклонён.** `NULL` = «ключ не подтверждён» ⇒ инструмент **не попадает в entry-скан** (предусловие, не холд; самоизлечимо первым тиком). Бэкфилл отвергнут: он проставил бы метке значение, которого измерение не производило — болезнь fallback'а, дважды отвергнутая проектом. **`instruments` из schema-дельты убран.** Клауза «писателей ровно два» снята: писатель **один** — синк | `InstrumentExternalRules.md` §«Свежесть ключа группы», `InstrumentExternalRulesSyncJob.md`, `EntryScannerJob.md`, `pnl-finalization-mechanics.md` §Следствия |
| **H22** | Пометка «`INSTRUMENT_GROUP` — целевой, вводится на `CODE` шага 7» + **`scope` → `varchar(32)`** (крен б); CODE-пункт на javadoc `HoldScope` (снятые ярлыки уровня) | `HoldSignal.md`, `pnl-finalization-mechanics.md` §Следствия, backlog |
| **H23** | **`varchar`** (крен 1) с записанным исключением из конвенции численных колонок: модель сознательно допускает непарсящееся значение, `numeric` переносит отказ парсинга с чтения на запись | `TradeFeeRate.md` §Персистентность, `persistence-representation.md` §«Численные колонки» (новый) |
| **H24** | SYSTEM-строки несут `target_entity_type = DEAL` + `target_entity_id = deal_id` ⇒ первый частичный индекс; формулировки выровнены | `SystemActionExecutor.md` §Контракт, `DealActionState.md` §Инварианты |
| **H25** | Направление (а) принято: R-мультипликатор от `resultProfit − Σ FUNDING`; реализация — шаг ожидаемости. От шага 7 требуется только носитель, и `CashFlowCategory.FUNDING` его уже даёт — это подтверждение исполнимости, не работа шага | `per-trade-risk-policy.md` §N13-форвард |
| **H27** | Дом альтернативного писателя заведён **условно**: `SubmitOrderExecutor` пишет `billsWindowBegin` только при отрицательном ответе §AG1.5. Ветка записана превентивно — сам дефолт был зафиксирован заранее, исполнителя не имел | `SubmitOrderExecutor.md` §«Нижняя граница окна линковки bills» (новый), `Deal.md` |
| **H11** | Снята с пользовательского прохода (§8). Политика отказа задана владельцем: `appliedRate = null`, слагаемое не вносится, `AnomalyReport` `CROSS_CCY_RATE_UNAVAILABLE`; курс не подставляется. **Эндпоинт и ось запроса — хэнд-офф `integrator`** | `RefreshBillsExecutor.md` §«Носитель курса», `coverage-manifest.md` (обе строки-кандидата), `AnomalyReport.md`, backlog |

## 8. Правки процесса (3)

| Правка | Носитель |
|---|---|
| **Фильтр выноса на слепой проход.** Находка без пользовательского хвоста (дефицит `работа`/`грунт`, владелец штатный) на проход **не выносится** — идёт владельцу, к пользователю возвращается результатом. Ярлык `варианты-с-креном` сам по себе основанием не является; пользовательской находку делает дефицит `подтверждение`. Названа цена ошибки в **обе** стороны. Кейсы: H11 (вынесена ошибочно — пользователь ответил догадкой, а не решением), H19 и H23 (отсеяны фильтром верно) | `.claude/processes/roadmap-step-execution.md` §3 |
| **Смешанный предикат — штатный третий вариант** на развилке «писать пусто или писать ноль»: значение по **проверенному факту**, а не по умолчанию. Двоичная постановка теряет его систематически. Кейс H7: ни одна из трёх линз не совместила пользу нуля для статистики с тем, что частичное исполнение делает ноль ложью | `.claude/templates/docs/gap-report.md` §«Набор полей находки» |
| **Продуктовый scope не оценивается агентом как цена варианта.** Довод «режет функциональность, которую система уже умеет» подставляет ответ пользователя вместо запроса. Такой вариант выписывается **без** оценки цены по продуктовой оси. Кейс H8: пользователь выбрал именно отвергнутый этим доводом вариант | там же |

## 9. Инвентарь периодических джоб

Собран по требованию: **два механизма, всплывшие за один проход и менявшие
ответ, в разборе отсутствовали** — `TradeGuardJob` (новая) и `AnomalyJob`
(существует в докax). Это тот же класс, что принцип рефреша из части 1:
механизм есть, но не записан — и линза находит «механизма нет» там, где он
есть, либо принимает на веру то, чего нет.

| Джоба | Док | Код (`@Scheduled`) | Состояние |
|---|---|---|---|
| `CandleJob` | есть | есть | работает |
| `IndicatorJob` | есть | есть | работает |
| `MarketStructureJob` | есть | есть | работает |
| `InstrumentExternalRulesSyncJob` | есть | есть | работает; шаг 7 добавляет чтение `trade-fee` и запись метки ключа |
| `EntryScannerJob` | есть | есть | работает; шаг 7 добавляет контурный гейт (H8) и предусловие ключа (H21) |
| `DealOrchestratorJob` | есть | есть | работает |
| **`AnomalyJob`** | есть | **нет** | материализация — шаг 8; **все ссылки на него — целевым временем** |
| `ReconciliationJob` | нет | нет | только название (backlog п.7) |
| **`TradeGuardJob`** | нет | нет | введена H17; проектирование — шаг 8 |
| `MarketPhaseJob` | снят | снят | `market-phase-stateless.md` |
| `TradeFeeSyncJob` | — | — | отвергнута (`pnl-finalization-mechanics.md` реш.4, вариант B) |

**Сверка с клаузами шага 7 — одно расхождение и оно закрыто пометкой.**
Клаузы `trading-constraints.md` §Первоисточник («детектит `AnomalyJob`») и
новая клауза H18 опираются на компонент, **которого в коде нет**. Пометка
«Состояние носителей» поставлена в `AnomalyJob.md`; остальные джобы клаузам
шага 7 соответствуют.

**Отдельного инвентарь-дока не заводим:** типа под кросс-компонентный
инвентарь в `structure.md` нет, а вводить его ради одной таблицы
преждевременно. Durable-форма — пометка состояния **в самом
компонент-доке** (образец — `AnomalyJob.md` §«Состояние носителей»); сама
таблица живёт здесь и в backlog §Шаг 8 как позиция «держать сверенным».

## 10. Итог

**27 находок `DOCS_CHECK_10` закрыты полностью.** Часть 1: снято принципом
3, переформулировано 2, сужено 2, решено пользователем 2. Часть 2: решений
пользователя 6, закрыто владельцами 16 (включая остатки H2 и H4).
Открытых находок прогона не осталось; гейт `CODE` снимается **чистым
`DOCS_CHECK_11`**, не этим закрытием.

**Хвосты, ушедшие владельцам и на рантайм (не находки, а работа):**

- **`integrator`:** эндпоинт и ось запроса котировки cross-ccy (H11); дом
  конфига исключений сверки по бирже (H14); оси запроса истории позиций
  (H9, часть 1).
- **Рантайм §AG1.5/AG3.4/AG3.5** — до `CODE`: инвариант агрегации (N11),
  гранулярность bills, `ccy` fee-bills, **состав `realizedPnl` при
  cross-ccy-издержке** (H13, добавлено), entry-fee против `cTime` (ветка
  H27).
- **Шаг 8:** проектирование `TradeGuardJob` (H17), переоценка инварианта
  ликвидации (H18), перекрытие анкеров по scope.
- **Шаг ожидаемости / фаза 2:** funding в знаменателе R (H25), добор числа
  на `EMERGENCY_CLOSED`, калибровка запаса на проскок по накопленным
  `externalCloseAveragePrice` (H26).

**Открытые вопросы:** `RISK-Q3` (H8 не закрывает — другой радиус), `CCY-Q2`
(имена полей предварительны), `PNL-Q1` (пункт 1 жив; второй потребитель
записан), `FEE-Q1` — без изменений.

## Связи

- Отчёт прогона — `phase-1-step-7-docs-check-10.md`.
- Принцип рефреша — `docs/rules/command-lifecycle.md` §Правило.
- Хроника шага — `phase-1-step-7-chronicle.md`.
