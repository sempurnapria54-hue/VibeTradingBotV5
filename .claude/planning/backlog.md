# Backlog

Единый список открытых задач, ADR-кандидатов и тем для проработки.

**Не путать с:**
- `../adr/` — принятые архитектурные решения (append-only).
- `docs/planning/` (прочие файлы) — roadmap, milestones, вехи.
- `../pipeline-evolution-log.md` — история изменений пайплайна работы.

## Как использовать

- Добавляй сюда задачу, когда она всплыла, но не берётся сейчас.
- Закрытая задача — не удаляется, а помечается `✅ DONE (дата, ссылка)`. История остаётся.
- Переоценка приоритетов — на ретроспективе или по факту изменения контекста.
- Если задача стала ADR-кандидатом — оставить ссылку на номер ADR в строке.

## Приоритеты

- **Now** — в работе или следующее в очереди.
- **Next** — ближайшее, понятно зачем и когда.
- **Later** — нужно, но не сейчас. Зависит от чего-то.
- **Someday** — может, никогда. Идеи, гипотезы, "хорошо бы".

---

## Документация и миграция

### Now

- **Миграция `docs/domain/processes/` в `docs/spec/`.**
  Все актуальные документы движка пересматриваются под единый формат, оптимизированный
  под текущий флоу (агенты, source hierarchy, связные обновления). Перед миграцией —
  выработка стандарта структуры spec-документов. Возможно с привлечением агентов.
  *Источник: чат ADR-0001, 2026-05-14.*

### Next

- **Стандарт структуры `docs/spec/`.**
  Минимальное правило ("models/ с именами = именам классов") добавлено в `../working-with-claude.md`
  в рамках ADR-0001. Полный стандарт — отдельная задача, должна быть выработана **до**
  массовой миграции из `docs/domain/processes/`. Кандидат на ADR.

- **Стандарт описания персистентности доменных моделей.**
  AnomalyReport.md использован как первый кейс — раздел «Персистентность»
  для jsonb-хранения. На основе этого кейса закрепить формат стандарта:
  граница тривиально/нетривиально, взаимодействие с Flyway, возможный
  пересмотр решения про отсутствие жанра `schema/`.
  *Источник: ADR-0002 §1 + миграция AnomalyReport (закрытая ниже).*

- **Создание `docs/conventions/terminology.md`.**
  Терминологические соглашения проекта: «доменная модель», «aggregate
  root», «core vs runtime», «хранимая модель» и подобные. Не зависит
  от миграции. Создаётся в Этапе 4 подготовки. По жанру — convention,
  соседствует с CodeStyle.md и TechRadar.md.
  *Источник: расщепление функций глоссария по ADR-0007 §3 и ADR-0009 §9.*

- **Создание `docs/spec/MODELS.md`.**
  Реестр канонических имён бизнес-моделей (13 core + 3 runtime).
  Стартовое содержание — заглушки; наполняется ссылками на
  model-документы по мере миграции кластеров. Используется Curator
  для идентификации забегающих ссылок. Создаётся в начале фазы 1 миграции.
  *Источник: расщепление функций глоссария по ADR-0007 §3.*

- **Создание `.claude/planning/migration-tracker.md`.**
  Рабочий артефакт процесса: чеклист прогресса миграции по 8
  кластерам + 6 сквозным концепциям со ссылками на целевые
  spec-документы и коммиты завершения. Создаётся в начале фазы 1.
  *Источник: расщепление функций глоссария по ADR-0009 §9.*

- **Пересмотреть необходимость `docs/spec/MODELS.md` и
  `.claude/planning/migration-tracker.md` после завершения фазы 5.**
  По завершении миграции (фаза 5 — цикл ревью смигрированной
  документации) оценить: остаются ли эти артефакты полезны как
  навигация / прогресс-индикатор, нужно ли реструктурировать,
  упразднять или оставить как есть. terminology.md этим триггером
  не затрагивается (живёт независимо от миграции).
  *Источник: расщепление функций глоссария — артефакты с явно
  ограниченной во времени миграционной ролью требуют пересмотра
  по завершении.*

- **Миграция `codex/CodeStyle.md` и `codex/TechRadar.md` в `docs/conventions/`.**
  С доработкой. Папка `codex/` уже удалена в стартовом аудите — нужно проверить, где
  именно лежат актуальные версии.
  *Источник: setup-strategy-summary.md.*

- **Починка висячих ссылок** (Main.md удалён, Code style.md и т.д.).
  Адресуется при миграции conventions.
  *Источник: setup-strategy-summary.md.*

### Later

- **Создание `docs/ROADMAP.md`.**
  Сведение двух линий планирования. Имеет смысл после того, как новая spec-структура устаканится.
  *Источник: setup-strategy-summary.md.*

- **Оформить миграцию документов как отдельный Сценарий в playbook.**
  Сейчас миграция выполняется через автоактивацию скилла
  `spec-document-migration`, без выделенного Сценария в
  playbook. После 2-3 миграций — оценить устойчивость
  паттерна и оформить как Сценарий 9 (или дополнить Сценарий 7).
  *Источник: создание скилла spec-document-migration,
  2026-05-15.*

- **Стандарт описания внутреннего маппинга (snapshot ↔ доменная ↔ персистентная).**
  Где и как описывать слоистый поток данных внутри продукта
  (ExternalSnapshot, MapStruct). Возможный артефакт — отдельный ADR про
  паттерн ExternalSnapshot + документ в `references/data-flow-layers.md`.
  Активируется при первой задаче интеграции с реальной потребностью.
  *Источник: ADR-0002, чат принятия.*

- **Оценить необходимость отдельного агента spec-curator.**
  Скилл `spec-document-workflow` обеспечивает процедурные проверки
  стандарта spec. Если станет видно, что нужен агент для контроля более
  высокого уровня — выделить spec-curator в самостоятельного агента.
  *Источник: ADR-0002, чат принятия.*

- **Прецедент цепочки ADR в одной сессии (наблюдение).** ADR-0004 → ADR-0005 (2026-05-15) — родительский ADR явно предусмотрел дочерний «при необходимости», дочерний принят в той же сессии без правки родительского. Это наблюдённая практика, не закреплённое правило. Накопить 2-3 подобных кейса и решить: фиксировать как явный паттерн в `working-with-claude.md` / playbook, или это всегда останется частным случаем. Не торопиться.

- **Определение «принятия» ADR.** Правило append-only в `.claude/adr/README.md` ссылается на «момент принятия», но не определяет, что это: согласование решения в чате или коммит. Действующая (по практике) модель — «принят = согласован в чате». Уточнить формулировку в `.claude/adr/README.md`, добавив однозначное определение. Прецедент возник в сессии ADR-0004/0005 (2026-05-15), когда возникло разночтение. Маленькая правка, но отдельный ADR не требуется — это уточнение существующего правила, не пересмотр.

---

## Содержательные ADR (TBD из ADR-0001)

### Next

- **ADR-0002 (?): AnomalyJob — полная спецификация.**
  Оркестрация: момент глобального снимка биржи, последовательность per-instrument
  обработки, обработка orphan, политика ретраев `ERROR`-репортов, отношения с
  `DealOrchestratorJob`, защита от конкурентного исполнения.
  Связано с `docs/domain/processes/Открытые вопросы по движку.md`, п. 3, 9.
  *Источник: ADR-0001, TBD-1.*

- **ADR-0004 (?): Каталог аномалий.**
  Перечень `code`-ов, привязка к `severity`, политика обработки по каждому типу
  (всегда kill-switch / только блокировка / только запись report). Связано с
  поиском существующих материалов в репо.
  *Источник: ADR-0001, TBD-2. Переномерован с ADR-0003 (?) после принятия ADR-0003 (разделение docs/ vs .claude/).*

### Later

- **ADR: KillSwitchExecutor — детальная спецификация.**
  Идемпотентность, обработка частичных сбоев, формат `KillSwitchResult`,
  разделение action-state vs report-snapshot. Возможно, новых документов движка
  достаточно — проверять после миграции.
  *Источник: ADR-0001, TBD-3.*

- **ADR: политика блокировки торговли при orphan-аномалии.**
  На уровне инструмента vs биржи, способ снятия блокировки, ручной разбор.
  *Источник: ADR-0001, TBD-4.*

- **ADR: конкурентность DealOrchestratorJob vs AnomalyJob.**
  Связано с `docs/domain/processes/Открытые вопросы по движку.md`, п. 9.
  *Источник: ADR-0001, TBD-5.*

---

## Архитектура и инфраструктура

### Next

- **Решение по Flyway: `flyway.enabled: true` vs внешнее применение миграций.**
  Кандидат на ADR.
  *Источник: setup-strategy-summary.md.*

- **План миграции `RestTemplate → RestClient`.**
  Кандидат на ADR. Текущий код использует RestTemplate, Tech Radar требует RestClient.
  *Источник: setup-strategy-summary.md.*

### Later

- **Решение по устаревшим `codex/tasks/05A..05E` + запись в `../pipeline-evolution-log.md`.**
  Папка `codex/` удалена в стартовом аудите. Фиксация в логе не сделана. Низкий
  приоритет, поскольку фактически уже сделано.
  *Источник: setup-strategy-summary.md.*

---

## Тесты

### Next

- **Стартовое покрытие тестами критичных частей:**
  - `DealStateMachine`
  - `KillSwitchService` (после переименования — `KillSwitchExecutor`)
  - `OkxAuthSigner`
  - `TradeRuleValidator`

  Сейчас `src/test/java/` пуст. Тестовая стратегия — кандидат на ADR.
  *Источник: setup-strategy-summary.md.*

---

## Документы под пересмотр размера

Сюда автоматически попадают spec-документы, превысившие мягкий cap в
1000 строк. Не задача для AI — сигнал для пользователя пересмотреть
декомпозицию.

Формат записи:

- `<путь к файлу>` — `<количество>` строк (на `<дата>`).
  Возможные направления декомпозиции: `<короткое предложение>`.

*Источник: ADR-0002.*

---

## Закрытые

- ✅ **ADR-0001: модель reconcile через AnomalyReport.** Закрыто 2026-05-14.
  Ссылка: `../adr/0001-reconcile-via-anomaly-report.md`.
- ✅ **ADR-0003: Разделение продуктовой документации и работы с Claude.**
  Закрыто 2026-05-15. Ссылка: `../adr/0003-meta-docs-relocation.md`.
- ✅ **[2026-05-15] Перенос шаблонов spec-документов в `.claude/`** —
  принят ADR-0004 (`../adr/0004-spec-templates-relocation.md`).
- ✅ **[2026-05-15] Перенос шаблона ADR в `.claude/templates/documents/adr.md`** —
  принят ADR-0005 (`../adr/0005-adr-template-relocation.md`). Семантика
  `documents/` расширена до «шаблоны markdown-документов проекта».
- ✅ **[2026-05-15] Привести `docs/spec/models/AnomalyReport.md` в
  соответствие со стандартом ADR-0002 и выделить динамическую часть в
  `docs/spec/lifecycle/AnomalyReport.md`.** Java-блоки заменены таблицами,
  добавлен frontmatter, разделение по жанрам (Вариант A). См. коммит.
- ✅ **[2026-05-15] Создание скилла `spec-document-migration`.**
  Стартовая версия после пилотного кейса миграции AnomalyReport.
  Помечен как первая версия — ожидается итерация после 2-3
  миграций. См. коммит.
- ✅ **[2026-05-17] Перенос `docs/spec/models/AnomalyReport.md` →
  `docs/spec/models/core/AnomalyReport.md`.** Закрытие структурной
  части задачи из ADR-0006 §Consequences (раскладка по подкаталогам
  по ADR-0007 §1). Содержательная часть (frontmatter, таблицы,
  разделение на model + lifecycle) закрыта 2026-05-15. Обновлены
  ссылки в `docs/spec/lifecycle/AnomalyReport.md` (2 места) и
  `docs/spec/MODELS.md` (запись AnomalyReport дополнена ссылкой на
  model-документ). Технические примечания добавлены в ADR-0001,
  ADR-0002, ADR-0006, ADR-0009.
  *Источник: B-2 анализа dry-run миграции (`.claude/notes/migration-dryrun-analysis-2026-05-17.md`).*

---

## История изменений документа

- 2026-05-14 — создан по итогам чата ADR-0001. Перенесены все открытые пункты
  из `setup-strategy-summary.md`, добавлены TBD из ADR-0001, добавлена задача
  миграции `docs/domain/processes/` в `docs/spec/`.
- 2026-05-15 — закрыты пункты ADR-0004 (перенос шаблонов spec-документов) и
  ADR-0005 (перенос шаблона ADR); в «Документация → Later» добавлены два
  наблюдения из сессии ADR-0004/0005 (прецедент цепочки ADR, определение
  «принятия» ADR).
- 2026-05-15 — закрыт пункт «Привести AnomalyReport.md к стандарту ADR-0002»;
  пункт «Стандарт описания персистентности доменных моделей» активирован
  и перенесён из Later в Next с переформулировкой (AnomalyReport — первый
  кейс).
- 2026-05-15 — закрыт пункт «Создание скилла
  spec-document-migration»; в Later добавлен пункт об
  оформлении миграции как отдельного Сценария в playbook.
- 2026-05-16 — пункт «Создание `docs/GLOSSARY.md`» переформулирован
  в три пункта (terminology.md, MODELS.md, migration-tracker.md)
  + добавлен пункт пересмотра после фазы 5. По решению о
  расщеплении функций глоссария (правки в Proposed-ADR-0007 §3
  и ADR-0009 §9).
- 2026-05-17 — закрыт пункт «Перенос
  `docs/spec/models/AnomalyReport.md` → `docs/spec/models/core/AnomalyReport.md`»
  (структурная часть задачи из ADR-0006 §Consequences по дисциплине
  ADR-0007 §1).
