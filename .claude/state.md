# Текущее состояние пайплайна

**Дата обнуления:** 2026-05-21.

## Что произошло

Старая инфраструктура пайплайна (карта 5×2+5×2, скиллы, агенты,
ADR, snapshots, playbook) перемещена в `.claude-archive/2026-05-21/`.
`.claude/` и `CLAUDE.md` пересобираются с нуля.

Причина: накопленная инфраструктура перестала помогать. Контекст
терялся от чата к чату, артефактов стало больше, чем человек
может удерживать. Точечные правки не решали корневую проблему.

## Над чем работаем сейчас

Заполняем новое основание по мере появления конкретных потребностей.
Никаких заранее спроектированных слоёв, ролей, шаблонов.

## Что брать из архива

По запросу, не по умолчанию. Если возникает задача, для которой
в архиве есть наработка — рассматриваем как материал. Решение
взять / переработать / выбросить — каждый раз отдельное.

В архиве есть, в порядке убывания вероятной полезности:
- Java-код проекта (в `src/`, не в архиве — не трогался).
- Продуктовая документация в `docs/` (не в архиве — не трогалась).
- Старый CLAUDE.md и `.claude/working-with-claude.md` —
  накопленные конвенции работы. Большая часть может быть актуальна.
- ADR — архитектурные решения. Часть про продукт (ADR-0001
  AnomalyReport), часть про пайплайн (всё остальное).
- Скиллы, агенты, snapshots, playbook — методология. Самая
  спорная часть, разрослась больше остальных.

## Конвенции работы

Минимальные, дополняем по необходимости:

- Чат в claude.ai — для обсуждений, проектирования, решений.
- Claude Code — для правки файлов в репозитории.
- Коммиты — вручную через IDEA после ревью. Claude Code не
  коммитит.
- После правки файла — `git add` (staging).
- Project Knowledge содержит: `CLAUDE.md` и `.claude/state.md`.
  Всё. Обновляется вручную после изменений.

## Project Knowledge: что должно быть

После обнуления — только два файла:
- `CLAUDE.md`.
- `.claude/state.md` (этот файл).

Если PK содержит что-то ещё из старого набора — удалить.