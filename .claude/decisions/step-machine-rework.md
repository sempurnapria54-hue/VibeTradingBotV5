# Переработка машины шага

## На какой вопрос отвечает этот файл

Почему машина шага роадмапа заменена с docs-first на
`CONCEPT → CONCEPT_REVIEW → CODE → DOCS → REVIEW → DONE`.

## Решение держателя (2026-09-15)

**Концепция — держателя, код и документация по факту — CC, апрув —
держателя.** Доковая петля до кода (`DOCS_CHECK_N` / `GAPS_CLOSE_N`) снята
вместе с машинерией под ней; концепция проверяется на **достаточность**,
а не на полноту по полю.

### Термин «концепция»

Концепция — примерное описание намерения держателя по предмету (шаг
роадмапа, сервис, сквозная тема): что и зачем; сервисы и высокоуровневая
архитектура, если держатель их видит; границы — чего не делает, что
отложено и до какого триггера. Компоненты, данные, поведение в деталях,
схемы, API, код, правила, тесты — **не** концепция: это CC дорабатывает
сам в `CODE`.

Достаточность концепции — единственный предмет её ревью: нет открытых
вопросов `подтверждение` (цель, scope, риск-аппетит, приоритеты, сроки) и
нет конфликта с принятыми решениями. Всё остальное — не находка ревью
концепции.

### Машина шага

`CONCEPT` → `CONCEPT_REVIEW` → `CODE` → `DOCS` → `REVIEW` → `DONE`

- **`CONCEPT`** — держатель с чатом. Если концепция уже есть, статус
  проходится сразу.
- **`CONCEPT_REVIEW`** — CC: достаточность для старта. Вопросы
  `подтверждение` и конфликты с решениями — держателю; апрув CC = вопросов
  к держателю нет.
- **`CODE`** — CC: дизайн и код целиком, реактор и гейты зелены. Риски и
  развилки держателя выносит по ходу; прочие развилки решает сам, в
  дайджест.
- **`DOCS`** — CC: документация по факту построенного — как это работает,
  так, чтобы держатель мог прочитать и понять.
- **`REVIEW`** — сначала адверсариальные фокусы CC, затем просмотр
  держателя: код и доки вместе. Держатель спрашивает «как это работает»
  — CC объясняет; объяснение, которого нет в доках, — пробел доков,
  закрывается до апрува. Апрув — держателя.
- **`DONE`** — закрытие, история, снапшот, граница шага.

### Что снимается

`TOOLING` как статус — агенты и скиллы появляются от потребности внутри
любого статуса. `DOCS_CHECK_N`, `GAPS_CLOSE_N`, выходной гейт, пост-хок
концепт-гейт, `SYNC_DOCS_FROM_CODE` как отдельный статус — и машинерия под
них: сквозная проверка концепции «по полю», классификация пробелов,
популяции и глубина закрытия, потолки мини-петель.

Остаются: энфорсеры и свипы (теперь единственное, что держит доки от
дрейфа после факта), реактор, единица работы сессии, отчёты и снапшоты,
дайджест решений.

### Следствия, названные держателем

- Шаг 11 фазы 2 уже `DONE` — новая машина действует с шага 12 («Тесты»).
- Тесты — отдельный шаг в конце каждой фазы, той же машиной. Его
  `CONCEPT` — концепция тестирования держателя: **чёрный ящик сервиса,
  юниты по критерию чистой логики, сквозные**; для фазы 2 шаг входит
  сразу в `CONCEPT_REVIEW`. Его `CODE` — три под-шага в этом порядке:
  тест-кейсы (вход, все ожидаемые выходы включая отсутствие,
  предусловия; без кода) → ревью кейсов другим агентом, адверсариально →
  код тестов по утверждённым кейсам.
- Оживители бэклога, привязанные к снятым статусам, переназначаются.
- Знание старой машины (процессы, скиллы, решения, роли) переписывается
  или снимается; снятое — в историю по правилам.
- Скиллы `test-design` / `test-review` / `test-code`, роль `tester` и
  фокус `test` переводятся с контура API источника на новый предмет под
  три под-шага; параллельный набор не заводится.

## Что разобрано CC

| Предмет | Исход |
|---|---|
| Процесс исполнения шага | переписан под машину; дом — `.claude/processes/roadmap-step-execution.md` |
| Скилл ведения прогресса | переписан: гейты по статусам, перечень команд гейта без снятых энфорсеров |
| Ревью концепции | `.claude/skills/concept-review.md` переписан на достаточность: две проверки, отчёт с вердиктом |
| Единица работы сессии | одна единица = один заход одного статуса; таблица единиц по статусам (`.claude/rules/session-work-unit.md`) |
| Стоячий промпт цепочки | ходы держателя (концепция, гейтящий вопрос, апрув в `REVIEW`) — статус `holder_decision` |
| Свипы и нормы измерения | пассажи процесса разнесены по домам: свипы по роду правки — `.claude/rules/edit-kind-obligations.md` (без классификационной таблицы и её энфорсера), нормы проверочных команд — `.claude/rules/measurement-commands.md` |
| Тестовый контур | три скилла, роль и фокус переведены на три под-шага; специфика контура источника перенесена в `.claude/processes/source-api-testing.md` |
| Реестр контакта с кодом | `code-gate-ledger.json` и `code-gate-check.py` сняты: его позиции гейтили компоненты по находкам доковых прогонов, которых больше нет; четыре припаркованные позиции уже живут своими секциями бэклога |
| Снятые энфорсеры отчётной формы | `edit-kind-check.py`, `insertion-neighborhood-check.py` — мерили таблицы отчётов узлов закрытия |
| Снятые скиллы, правила, шаблон | `closure-population`, `stagnation-detection`, `classify-gap-level`, `classify-code-blocking`; `docs-loop-limits`, `stopped-node-disposition`; `gap-report` |
| Снятые решения | тринадцать решений о машине доковой петли — в `history/` |
| Бэклог | маркеров с условиями по снятым статусам не было; переписаны тела секций, опиравшихся на заморозку доковой петли и реестр гейтов |
| Открытый вопрос PROC-Q3 (независимость линз) | закрыт как беспредметный: линз больше нет |
| Шаг 12 | `DOCS_CHECK_1` → `CONCEPT_REVIEW`; концепция тестирования записана в хронику |

Полный перечень перенесённого и переписанного —
`.claude/work/history/2026-09-15-step-machine-rework.md`.

## Что было на развилке у CC

| Вариант | Почему не он |
|---|---|
| оставить реестр контакта с кодом как носитель парковок ревью | реестр гейтил компоненты по классу находки доковой петли; без петли остаётся секция бэклога с владельцем и условием — тот же исход меньшим следом (`.claude/rules/design-simplicity.md`) |
| оставить свипы в процессе исполнения шага | процесс стал описанием машины; норма, читаемая всяким корпусным заходом, а не только шагом, живёт в правиле (`.claude/rules/policy-home.md`) |
| завести отдельный набор тестовых скиллов под тестовый шаг | запрещено самим решением; общая форма кейса одна, различия контура — в его процессе |
| переписать снятые решения под новую машину | решение описывает прошлое; правка сделала бы его ложным (`.claude/rules/curation.md` §«Чек-лист свипа») |

## Связи

- Машина шага — `.claude/processes/roadmap-step-execution.md`.
- Скилл прогресса и гейты — `.claude/skills/update-roadmap-progress.md`.
- Итог переработки и перечень снятого —
  `.claude/work/history/2026-09-15-step-machine-rework.md`.
- Роадмап — `.claude/work/roadmap/phase-2.md`.
