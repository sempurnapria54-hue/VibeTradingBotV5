# Пересборка роадмапа под сервисную платформу

## На какой вопрос отвечает этот файл

Почему роадмап пересобран так: фаза 1 растворена, рубеж перенесён до
прода, фазы 2-8 и их порядок.

## Решения держателя (2026-09-04), не пере-решаются

- Архитектурный рубеж переносится до прода и расширяется: сервисы,
  облако с CI/CD, наблюдаемость, мультитенант с правами, полноценный
  фронт, вторая биржа, много инструментов.
- Фаза 1 по старому гейту не закрывается: шаги 10-11 растворяются в
  новом роадмапе.
- Мониторинг, советник, детектор разгона-и-слива — вне рубежа. Платёжный
  шлюз и бухгалтерия — место зарезервировано, не проектируется.

## Развилки владельца

### Фаза 1 — статус `FOLDED`

**Исход:** новый статус фазы `FOLDED` — «растворена в новом роадмапе». Его
смысл живёт в `.claude/work/roadmap/roadmap.md` §«Примечания».

**Альтернативы.** (а) `DONE` — отвергнуто: ролляп в `DONE` исполняет гейт
закрытия фазы, который держатель велел не проходить; кроме того, `DONE` с
двумя снятыми шагами лжёт о выполненном. (б) Оставить `IN_PROGRESS` с
пустыми шагами — отвергнуто: статус означал бы работу, которой нет, и
ролляп из девяти `DONE` дал бы `DONE` при первом же пересчёте.
(в) Перенумеровать так, чтобы старые шаги стали шагами новой фазы —
отвергнуто: история девяти шагов ссылается на «шаг N фазы 1» в сотнях
мест.

### Порт первым, а не «strangler» по способностям

**Исход:** фаза 2 переносит существующую способность в сервисную форму
целиком, до новых способностей. **Альтернатива** — строить сигнал,
портфель и мультитенант на монолите, а сервисы вводить по одному
(«strangler») — отвергнута ценой: каждая новая способность писалась бы
дважды, а модель тенанта пронизывает все таблицы и не вводится
частично. Цена порта названа: фаза 2 не даёт новой торговой способности;
её ценность — та же торговля в форме продукта, из которой растёт всё
остальное.

### Порядок фаз 3-6

**Исход (решение держателя 2026-09-04, ARCH-Q2):** Bybit (3) → сигнал и
бэктест-гейт (4) → мультитенант и фронт (5) → портфель и аллокатор (6).

**Отвергнутое предложение машины:** сигнал (3) → портфель (4) →
мультитенант (5) → Bybit (6); довод предложения — сигнал предпосылка
аллокатора, аллокатор предпосылка риска на тенанта, права и фронт
оболочка над готовым содержанием, а Bybit последним проверяет модель, к
которой уже добавлены тенант и сигнал. Довод не ложен, но проигрывает
цене ошибки: если вторая площадка потребует править **существующие** поля
`domain-model` (критерий устойчивости —
`docs/architecture/services.md`), то правка застанет сигнал, аллокатор и
права **уже написанными** против неверной модели, и цена перепишется на
все три. Проверка «раньше была бы неполной» этой ценой не окупается:
неполная проверка ловит бо́льшую часть расхождений формы, а поздняя
проверка ловит их после того, как на форму наросло.

Порядок между направлениями — класс «приоритеты», решение держателя;
альтернативы были названы равными по силе, и выбор сделан им.

### Прод-рубеж — гейт, не фаза

**Исход:** прод-рубеж — гейт, а не фаза. Устройство гейта живёт в
`.claude/work/roadmap/prod-gate.md` §«Назначение».

### Тесты и фронт — шаги в конце каждой фазы

Не меняется (`.claude/decisions/product-roadmap-type.md`). Зрелость фронта
по фазам живёт в `.claude/work/roadmap/roadmap.md` §«Примечания».

### Фазы 7-8 — вне рубежа

Мониторинг и советник (7), модель на шве сигнала (8) — записаны фазами,
чтобы резервирование мест (`docs/architecture/reserved-extension-points.md`)
имело адресата; детализация — при взятии в работу.

## Переякоренные условия

Условия, чей якорь сменился с «конца фазы 1» на прод-рубеж либо на новые
фазы, держат новый якорь сами: `.claude/rules/pre-launch-schema-changes.md`,
`.claude/work/prod-checks.md`, `.claude/processes/question-delegation.md`,
`.claude/work/backlog.md`, `.claude/rules/tech-radar.md`,
`docs/rules/trading-constraints.md`.

## Связи

- Дизайн-проход — `.claude/decisions/service-architecture-design-pass.md`.
- Реструктуризация репозитория — `.claude/decisions/monorepo-restructuring-in-place.md`.
- Роадмап — `.claude/work/roadmap/roadmap.md`.
- Тип роадмапа — `.claude/decisions/product-roadmap-type.md`.
