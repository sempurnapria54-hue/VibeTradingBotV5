---
name: build-command
description: Where the acting build command and environment facts live (single home; do not duplicate)
metadata:
  type: reference
---

Команда сборки, пути JDK и Maven, лаунчер Python — **один дом**:
`.claude/skills/environment-commands.md` §«Факты среды — проверено
прогоном, не выведено» (таблица + §«Воспроизводимые команды»).
Факты там **проверены прогоном**, а не выведены, и обновляются вместе со
средой.

Здесь редакция не держится намеренно: расходящихся копий было три на
четыре носителя, и две предписывали команды, которых в среде того момента
не существовало. Имена таких команд здесь тоже не приводятся — они живут
до следующей смены среды и затем противоречат дому: копия в этом файле
стареет первой, её читает тот, кто до дома не дошёл.
