# Структура размещения знания

## На какой вопрос отвечает этот файл

Какое у нас правило размещения знания.

## Базовый принцип

Тип знания определяется вопросом, на который оно отвечает.
Один тип вопроса — одно место в структуре каталогов.

Если фрагмент не отвечает ни на один из перечисленных
вопросов — заводим открытый вопрос в
`.claude/work/questions/open-questions.md`, обсуждаем, фиксируем
решение в `decisions/`. Решение может ввести новый тип,
переразложить фрагмент на существующие, или что-то ещё в
зависимости от случая.

## Таблица размещения

| Путь | На какой вопрос отвечает | Имя файла | Комментарий |
|---|---|---|---|
| `CLAUDE.md` | Каковы базовые правила проекта? | фиксировано | В корне репо. Читается Claude Code автоматически. |
| `.claude/snapshots/` | Где мы сейчас и как сюда пришли? | `snapshot-vN.md` | Старые не правятся. Последний по номеру — актуальный. |
| `.claude/rules/` | Какое у нас правило? | kebab-case по теме | Один файл = одно правило. |
| `.claude/templates/code/` | Каков абстрактный паттерн/шаблон кода для X? | `<Язык>/PascalCase.md` по паттерну | Код-шаблоны (плейсхолдеры `<Model>`) — вход для написания кода на под-шаге `CODE`. См. `.claude/decisions/code-templates-vs-examples.md`. |
| `.claude/templates/docs/` | Каков формат докового файла такого-то типа? | kebab-case по типу файла | Доковые шаблоны. Один файл = один шаблон. Пока пусто; наполняется по потребности. |
| `.claude/skills/` | Как именно делать X? | kebab-case по операции | Один файл = один скилл. |
| `.claude/agents/` | Что делает Claude в роли X? | kebab-case по роли | Один файл = одна роль. |
| `.claude/decisions/` | Почему мы решили так, а не иначе? (пайплайн) | kebab-case по сути | Один файл = одно решение с альтернативами и обоснованием. |
| `.claude/notes/` | Что я наблюдал / разведал? | `YYYY-MM-DD-тема.md` | Разовые заметки, не правила. |
| `.claude/processes/` | Как устроен этот методологический процесс? | kebab-case по теме | Один файл = один методологический процесс. |
| `.claude/work/backlog.md` | Что мы планируем сделать? | фиксировано | Единый файл-список планируемой исполнительной работы. |
| `.claude/work/roadmap/` | Куда движется продукт по бизнес-ценности и в каком статусе каждый шаг? | `roadmap.md` (главный) + `phase-N.md` (детальный по фазе) | Продуктовый роадмап: верхний уровень — фазы, детальный — шаги фазы. Scope — только продукт; пайплайн вне. См. `.claude/decisions/product-roadmap-type.md`. |
| `.claude/work/progress/` | На каком шаге мы в этой активной операции? | kebab-case по задаче | Один файл = одна активная задача. По завершении: удаляется (короткое summary в `history/`), либо при ценных детальных артефактах — архивируется в подпапку `history/YYYY-MM-DD-<задача>/` (см. `.claude/decisions/forward-notes-after-task-closure.md`). |
| `.claude/work/questions/open-questions.md` | Что мы ещё не решили? (общие) | фиксировано | Единый файл со всеми общими вопросами (пайплайн + продукт). |
| `.claude/work/questions/tasks/` | Что неясно по этой конкретной активной задаче? | kebab-case, совпадает с именем в `progress/` | Один файл = одна задача со списком её локальных вопросов / форвард-заметок. По завершении архивируется вместе с прогрессом; указатели на форвард-заметки переезжают в `backlog.md` (см. `.claude/decisions/forward-notes-after-task-closure.md`). |
| `.claude/work/history/` | Что мы уже сделали? | `YYYY-MM-DD-kebab-case.md` | Один файл = одна завершённая задача, короткое summary. Ценные детальные артефакты задачи — в одноимённой подпапке `YYYY-MM-DD-<задача>/`. |
| `.claude-archive/` | (Архив.) | — | Не место для новых файлов. |
| `docs/decisions/` | Почему мы решили так, а не иначе? (продукт) | kebab-case по сути | Один файл = одно решение. |
| `docs/models/integrations/{name}/` | Какие поля у нативной модели источника `{name}`? | PascalCase (совпадает с DTO источника) | Инвентарь полей источника (used/unused). Один источник = один `{name}`. См. `.claude/decisions/model-layer-ontology.md`. |
| `docs/models/externalSnapshot/` | Какая структура нормализованного граничного объекта `*ExternalSnapshot`? | PascalCase | Единственное, что выходит за `ClientService`/adapter (см. `docs/rules/raw-exchange-dto-boundary.md`). Файл — только при самостоятельном содержании. |
| `docs/models/domain/core/` | Что это за торговая модель с биржевым воплощением? | PascalCase | Имя совпадает с именем Java-класса. |
| `docs/models/domain/aggregate/` | Что это за сущность без биржевой привязки, нужная для торговли? | PascalCase | Имя совпадает с именем Java-класса. |
| `docs/models/domain/other/` | Что это за прочая хранимая модель? | PascalCase / kebab-case | Имя совпадает с Java-классом, если применимо; для свечей, индикаторов, аудита и иных не-классов — по теме. |
| `docs/models/persistence/` | Что это за модель хранимого слоя? | PascalCase / kebab-case | Слой-скаффолд; наполняется по мере появления persistence-проекций. |
| `docs/models/api/` | Что это за модель API нашего сервиса? | PascalCase / kebab-case | Request/response DTO нашего API. Слой-скаффолд. |
| `docs/models/mapping/` | Как сущность переходит между слоями (источник↔snapshot↔domain↔request) и как резолвится её статус? | PascalCase (совпадает с доменной сущностью) | Один файл = одна сущность; источники подразделами (`## OKX`, ...). См. `.claude/decisions/model-layer-ontology.md`. |
| `docs/lifecycles/` | Через какие состояния проходит этот объект? Кто управляет? | PascalCase (совпадает с моделью) | Один файл = lifecycle одного объекта. |
| `docs/processes/` | Как устроен этот процесс? | kebab-case по теме | Например, загрузка свечей, расчёт индикаторов. |
| `docs/components/` | Кто выполняет? | PascalCase | Имя совпадает с именем Java-класса. |
| `docs/components/models/` | Что это за runtime-объект? | PascalCase | Имя совпадает с именем Java-класса. См. `.claude/decisions/runtime-value-object.md`. |
| `docs/rules/` | Какое правило действует в системе? | kebab-case по теме | Инварианты + бизнес-правила. Тип уточняется внутри файла при необходимости. |
| `docs/dictionary/` | Что означает этот термин? | kebab-case | Для терминов-классов — короткая статья со ссылкой на `docs/models/domain/...`. |
| `docs/integrations/{name}/contracts/` | Каков контракт и какие лимиты у этой операции источника `{name}`? | kebab-case по теме | Endpoints, permissions, rate limits, ACK-семантика, пагинация. См. `.claude/decisions/model-layer-ontology.md`. |
| `docs/integrations/{name}/rules/` | Какое правило источника `{name}`? | kebab-case по теме | Инварианты, adapter-константы, конвенции, специфичные для источника. |
| `src/` | (Код.) | — | Не место для документации. |

## Принципы

- **Один файл = одна тема.** Что считать темой — определяется
  типом знания (см. таблицу). Для моделей, lifecycles,
  заметок, истории, прогресса задач — строго один файл на
  сущность. Для правил, скиллов, решений, процессов — обычно
  одно правило / один скилл / одно решение / один процесс,
  но связные группы по теме допускаются (например,
  `naming.md` — все правила именования вместе). Исключения
  для списков: `open-questions.md` и файлы в `questions/tasks/`.
  Для `docs/models/mapping/<Сущность>.md` — один файл на сущность,
  per-source детали подразделами (`## OKX`, …), чтобы не
  дублировать source-agnostic ядро.
- **Скиллы самодостаточны.** Содержат внутри себя знание,
  нужное для работы (перечни, критерии, признаки). Не
  ссылаются на справочники и на другие скиллы. Логику
  последовательности и оркестрации держит вызывающая
  сторона (агент или процесс). Зависимости направлены
  сверху вниз: оркестратор знает про инструменты; инструменты
  не знают друг про друга и про оркестратора; справочники не
  знают про того, кто их использует.
- **Тип знания определяется вопросом**, не предметом или
  происхождением.
- **Когда фрагмент не ложится в один тип** — не угадывать.
  Подсветить развилку: разнести по местам, положить в основной
  тип со ссылками, или завести открытый вопрос.
- **Каждый файл знания указывает вопрос, на который отвечает.**
  В начале файла — раздел «На какой вопрос отвечает этот файл»
  с одной фразой. Без исключений среди файлов знания.
  Технические файлы (`.gitkeep`, `.gitignore`, build-конфиги
  и т. п.) — не файлы знания, правило к ним не применяется.

## Связи между типами

- Открытый вопрос (`questions/`) может породить решение
  (`decisions/`).
- Решение (`decisions/`) может породить задачу (`backlog.md`).
- Задача переходит из `backlog.md` в `progress/` при старте; в
  `history/` при завершении.
- Задача в работе может породить локальные вопросы
  (`questions/tasks/<имя-задачи>.md`) или новые общие вопросы
  (`open-questions.md`).
- Сознательный методологический сдвиг оформляется как decision
  с альтернативами и обоснованием.
- Наблюдение без решения — в `notes/`.

## Правила именования

См. `.claude/rules/naming.md`.

## Процедура размещения

См. `.claude/skills/place-knowledge.md`.
