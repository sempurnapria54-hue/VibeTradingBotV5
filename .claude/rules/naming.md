# Правила именования файлов

## На какой вопрос отвечает этот файл

Какое у нас правило именования файлов в репозитории.

## Правила

- **Модели и lifecycles:** `PascalCase.md`. Совпадает с именем
  Java-класса (например, `Position.md`, `Order.md`).
- **Решения, правила, скиллы, агенты, процессы,
  словарные статьи:** `kebab-case.md` по теме или сути.
- **Шаблоны:** доковые (`.claude/templates/docs/`) —
  `kebab-case.md` по типу файла; код-шаблоны
  (`.claude/templates/code/`) — `<Язык>/PascalCase.md` по паттерну
  (совпадает с именем кодового конструкта, например
  `Java/Controller.md`).
- **Заметки:** `YYYY-MM-DD-тема.md`. Дата обязательна.
- **История задач:** `YYYY-MM-DD-kebab-case.md`. Дата завершения
  задачи.
- **Снапшоты:** `snapshot-vN.md`, где N — целое число, последний
  по номеру — актуальный.
- **Фиксированные имена:** `CLAUDE.md`, `backlog.md`,
  `open-questions.md`.

## Разведение уровней абстракции

Когда в модели появляется **новый уровень абстракции** над уже
существующим (действие над командой, шаг над операцией и т. п.), имена
уровней разводятся **систематическим маркером-суффиксом**, а не
уникальными основами: основа имени у соседних уровней может совпадать,
уровень читается по суффиксу. Так уровень виден в любом имени, а не
угадывается по словарю.

**Способ разведения — самостоятельная развилка.** Он выбирается **до**
подбора конкретных имён, и в набор вариантов обязаны попасть обе оси:
уникальные основы против систематического маркера. Уже принятое единичное
имя **не считается установленным ограничением** на способ: из того, что
одно имя оказалось непохожим на соседнее, ограничение «имена уровней
обязаны различаться основой» не следует.

Смена способа разведения задним числом — переименование по всем
потребителям (значения enum, персистентные значения, ключи конфигурации,
внешние контракты), поэтому развилка проходится до имён, а не после.

### Исключение: имя, ловящее ошибочные предикаты

**Чистота оси уступает разделяющему значению, когда имя константы
доказанно ловит ошибочные предикаты.** Требование «одна ось на енум» —
довод **за** переименование в пользу чистоты; он **перевешивается**, если
у ложного имени есть **зафиксированные** случаи, где на него сел неверный
предикат.

**Признак применимости — счётный, а не оценочный:** названы **конкретные
места в коде**, где предикат построен на имени и неверен. Не «имя может
запутать», а «вот здесь запутало».

**Почему так.** Переименование делает имя честным, но **предикат
по-прежнему нечего построить**: если у роли нет собственного значения,
код продолжит различать роль другим полем — и всякий, кто прочтёт имя как
роль, ошибётся снова. Разделяющее значение убирает саму возможность
ошибки; переименование убирает только подсказку к ней.

**Цена исключения называется и закрывается, а не замалчивается.** У енума
появляется вторая ось, а с ней — рассогласуемая пара носителей
(`Type = X` при `флаг = false`). Пара закрывается **инвариантом с
валидацией**, и валидация — часть того же решения, а не хвост: без неё
исключение не применяется.

*Кейс, на котором записано:* `Order.Type` различал наличие встроенной
защиты, но обе константы носили в имени «ENTRY»; на это имя сели два
ошибочных предиката (`isEntryType`, `entryOrder` — оба считали входной
всякую ногу, включая reduce-only, а на результате второго стоял гейт
защиты). CC выбрал переименование, сославшись на чистоту оси — ссылка
была **законной**, и без этого исключения следующая итерация
воспроизвела бы выбор и была бы права. Держатель выбрал третье значение
`REDUCE_ONLY`; пара закрыта инвариантом
`Type = REDUCE_ONLY ⇔ positionReducingOnly = true`
(`docs/models/domain/core/Order.md` §Енумы).

## Чего НЕ используем

- Номера в стиле `ADR-0001`, `RFC-002` и т. п. для решений.
- Даты в именах решений, моделей, процессов, правил, скиллов,
  агентов, шаблонов, файлов в `progress/`.
- Префиксы по статусу (`draft-`, `wip-`, `final-`) — статус
  виден из расположения (backlog vs progress vs history).
