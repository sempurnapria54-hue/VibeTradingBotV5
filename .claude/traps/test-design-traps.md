# Ловушки дома `.claude/skills/test-design.md`

## На какой вопрос отвечает этот файл

Как узнать и обойти ловушку, которую собрал дом `.claude/skills/test-design.md`?

Тела ловушек; оглавление — в самом доме, раздел «Ловушки и обходы». Адрес тела — заголовок с номером; форма и правило чтения — `.claude/rules/structure.md`, строка `.claude/traps/`.

## Счёт меток кейсов

### TD-001

- **Счёт кейсов шаблоном `^| U<n>.<m> ` двои́т** — перекрёстные таблицы
  (недостижимость, непрогоняемость) повторяют метку в первой колонке. Верное
  число даёт `| sort -u | wc -l`; так же сверяется покрытие таблицы
  самопроверки — `diff` двух дедуплицированных списков. Дубль метки
  проверяется номерами строк, а не счётом. *(с 2026-09-18)*
