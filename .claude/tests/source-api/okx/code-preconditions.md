# Гейтящие предусловия `CODE` шага 7

## На какой вопрос отвечает этот файл

Какие факты источника обязаны быть добыты до входа шага 7 в `CODE`.

## Почему реестр здесь, а не в `docs/`

Прежний дом реестра — `docs/rules/pnl-reconciliation.md`, и это было
ошибкой размещения: реестр отвечает на вопрос **проверочной
деятельности** («что обязано быть измерено до того, как писать код»), а
не на продуктовый вопрос («как сверяется результат сделки»). Переписывание
корпуса по `docs/concept.md` вычистило процессную арматуру из `docs/` — и
вместе с ней снесло реестр, **не перенеся** его: четыре дока остались
ссылаться на несуществующие номера.

Дом восстановлен здесь по `.claude/rules/structure.md` — строка
`.claude/tests/{testType}/{source}/`: «Планы — артефакт проверочной
деятельности (пайплайн), не продуктовое знание об источнике».

Восстановлено `GAPS_CLOSE_26`; содержание собрано из
`.claude/work/backlog.md` §«Шаг 7 (сделки и P&L) — исполнительный хвост»
и меток «ГЕЙТИТ `CODE`» в `plan.md`.

## Правило гейта

**Шаг 7 не входит в `CODE`, пока хотя бы один слот реестра не закрыт.**

- **Слот** — пара «пункт × кейс». Пункт закрыт, когда закрыты **все** его
  слоты: у двух пунктов носителей факта два (п. 1 — `AG1.5` и `AG1.9`;
  п. 2 — `AG6.1` и `AG6.2`), и наблюдение одного кейса второй слот не
  закрывает.
- **Слот закрывается прогоном своего кейса** в контуре
  `.claude/processes/source-api-testing.md` — **наблюдением**.
- **Исход «событие не наступило» слот не закрывает** (решение держателя
  2026-08-30, дословно: «гейт исходом „не наступило“ не закрывается»).
  Прежняя редакция приравнивала к наблюдению исход `OBSERVED_ABSENT`
  (решение П18 валидации `GAPS_CLOSE_17`); эта клауза её **снимает**.
  Механика исхода как записи наблюдения сохраняется — §«Закрытие исходом
  `OBSERVED_ABSENT`» ниже переименовано в §«Исход „не наступило“ —
  запись, не закрытие»; закрывающей силы у неё больше нет.
- **Фикстура, которую собрать не удалось, оставляет слот `PENDING`** — с
  названной причиной в колонке статуса. «Не собралась фикстура» и «событие
  не наступило» — разные вещи, и ни одна из них слот не закрывает.

Предусловие несёт дефицит **`грунт`**: не хватает внешнего факта, а не
проектирования и не решения держателя. По
`.claude/processes/roadmap-step-execution.md` §4 такие пункты уходят на
сбор **сразу**, не дожидаясь чистого `DOCS_CHECK`, — иначе образуется
петля: факт ждёт чистого прогона, а прогон без факта чистым не станет.

**Ярлык `грунт` описывает дефицит, но не блокеры сбора.** Факта не
хватает — это `грунт`, и он таким и остаётся; но собрать его сегодня
нельзя не поэтому, а из-за двух блокеров: непройденного этапа 3 контура
(аппрув пользователя — **действие**, не дефицит знания) и среды.
Владельцы и адресаты обоих — §«Что это значит для гейта». Прежде дом и
владелец были только у второго, и первый не доходил ни до кого
(F4 `DOCS_CHECK_28`).

**Этот файл — дом правила гейта грунта.** Встречный якорь со стороны
события — `.claude/skills/update-roadmap-progress.md` §«Гейт грунта»
(пункт `CODE` в §«Гейт-предусловие перехода (`CODE` / `DONE`)» на неё
ссылается): проверка **исполняется** в момент простановки статуса
`CODE`, а не тогда, когда кто-то вспомнит открыть реестр.

**Скилл несёт исполнимую процедуру, и это не пересказ политики.** Шаги
§«Гейт грунта» повторяют клаузы дома **намеренно**: гейт заведён для
того, кто дом не открыл, и обязан отработать без его открытия. Правило
при этом одно, и живёт оно здесь — при расхождении формулировок верен
дом (`.claude/rules/policy-home.md`: упоминание вне дома не подменяет
дом). Прежняя редакция обоих носителей заявляла «скилл правило не
пересказывает» и тут же его пересказывала — клейм снят как неверный, не
процедура (F12 `DOCS_CHECK_28`).

**Проверка полноты этой таблицы — отдельный шаг гейта, и он идёт раньше
чтения статусов.** Гейт читает колонку статуса, а слот, в таблицу **не
заведённый**, статуса не имеет и пропускается молча; ловит это только
`tools/preconditions-check.sh` (§«Сверка реестра с планом — исполнимой
проверкой»). Поэтому скрипт — инструмент гейта, а не иллюстрация
реестра: `.claude/skills/update-roadmap-progress.md` §«Гейт грунта»
прогоняет его **до** чтения колонки статуса и при ненулевом коде
возврата статус не проставляет (F2 `DOCS_CHECK_28`: до этого ни одной
входящей ссылки на скрипт из `.claude/skills/**` и
`.claude/processes/**` не было).

## Реестр

| Пункт | Что должно быть установлено | Кейс | Носитель прогона | Статус наблюдения |
|---|---|---|---|---|
| 1 | Агрегирует ли источник partial-закрытия в **одну** финализированную запись на `posId`, и в какой момент запись финализирована | `AG1.5` | код-тест `Ag1DealFixtureLiveTest`.ag1_5_partialCloseAggregation | ✅ собрано (2026-08-30) |
| 1 | То же с обратной стороны: несколько записей в окне — это эпизоды разных `posId`, а не партиалы одного | `AG1.9` | код-тест `Ag1DealFixtureLiveTest`.ag1_9_episodesInWindow | ✅ собрано (2026-08-30) — **посылка ложна**, см. §«Что добыто» |
| 2 | Справочник типов движений: полный перечень `type`/`subType`, встречающихся на счёте | `AG6.1` | код-тест `Ag6BillSubtypesLiveTest.ag6_1_directDictionary` + запрос коллекции | ✅ собрано (2026-08-30) — перечнем, 32 типа / 179 пар |
| 2 | То же, вторая половина: какие типы справочника **не** принадлежат экономике сделки — список исключений сверки | `AG6.2` | код-тест `Ag1DealFixtureLiveTest`.ag6_2_typesOutsideDealEconomics | ✅ собрано (2026-08-30) |
| 5 | Есть ли у пар котировки бар секундного разрешения — носитель курса cross-ccy | `MG7.5` | код-тест `Mg7IndexCandlesLiveTest.mg7_5_crossCcyRateCarrier` | ✅ собрано (2026-08-30) |
| 6 | Оси адресации записи истории позиций, у которой нет `posId` | `AG1.6` | код-тест `Ag1DealFixtureLiveTest`.ag1_6_addressingAxes | ✅ собрано (2026-08-30) |
| 7 | Горизонт доступности `fundingFee` в записи закрытия | `AG1.7` | код-тест `Ag1DealFixtureLiveTest`.ag1_7_numericFieldSemantics | ⏳ `PENDING` — фикстуру собрать не удалось: границу funding-интервала за прогон не пересекали |
| 9 | Приходят ли комиссии в расчётной валюте инструмента | `AG3.4` | код-тест `Ag1DealFixtureLiveTest`.ag3_4_feeCurrency | ✅ собрано (2026-08-30) |
| 10 | След автоделевериджа в движениях счёта | `AG1.8` | код-тест `Ag1DealFixtureLiveTest`.ag1_8_adlTrace | ⏳ `PENDING` — эпизод инициирует биржа, на demo не заказывается |
| 14 | Форма пустого значения числовых полей записи (пустая строка / `"0"` / отсутствие ключа) | `AG1.7` | код-тест `Ag1DealFixtureLiveTest`.ag1_7_numericFieldSemantics | ✅ собрано (2026-08-30) |
| 15 | Отдаёт ли окно с несколькими эпизодами **отдельную** запись на каждый `posId` | `AG1.9` | код-тест `Ag1DealFixtureLiveTest`.ag1_9_episodesInWindow | ✅ собрано (2026-08-30) — **посылка ложна**, см. §«Что добыто» |
| 16 | Отображение `type`/`subType` источника в экономические категории движения | `AG6.1` | тот же код-тест — сохраняет перечень пар | ✅ собрано (2026-08-30) |
| 17 | Поведение встроенной защиты при отмене родительской заявки | `M17.5` | код-тест `M17CancelOrderLiveTest.m17_5_attachedProtectionOnParentCancel` | ⏳ `PENDING` — C17a наблюдён; несущая C17b/C17c недостижима под потолком риска |

**Слотов тринадцать, различных пунктов одиннадцать (1, 2, 5, 6, 7, 9, 10,
14, 15, 16, 17), различных кейсов десять; закрыто десять слотов из
тринадцати (2026-08-30), открыты три — пп. 7, 10, 17.** Пункт закрыт,
когда закрыты все его слоты: закрыты пп. 1, 2, 5, 6, 9, 14, 15, 16 —
**восемь пунктов из одиннадцати**. Три кейса несут
по два пункта (`AG1.7` — пп. 7 и 14; `AG1.9` — пп. 1 и 15; `AG6.1` —
пп. 2 и 16), два пункта закрываются парами кейсов (п. 1, п. 2) — этими
двумя перекрытиями тринадцать слотов и сворачиваются в одиннадцать
пунктов. Счёт пересобирается **из таблицы**, а не правится отдельно.

### Сверка реестра с планом — исполнимой проверкой

Проверку несёт скрипт **`tools/preconditions-check.sh`** (запуск из корня
репозитория, без аргументов — умолчания контура OKX):

```bash
bash tools/preconditions-check.sh
# СОШЛОСЬ: слоты реестра == гейт-пометки плана     → код возврата 0
```

Он меряет **три** оси, каждая поднимает дефект до кода возврата, и
первые две — ровно та конструкция, которую реестр ввёл:

1. **Слот-сверка.** Множество пар «пункт × кейс» реестра обязано совпадать
   с множеством пар, объявленных гейт-заголовками плана
   (`**ГЕЙТИТ `CODE`** (предусловия п. N …)`). Сравниваются **пары**, а не
   множества кейсов: расхождение «кейс в реестре есть, но не на том
   пункте» множеством кейсов не ловится вовсе. Плановая сторона разбирает
   формы «п. N», «пп. N и M» и «пп. N, M и K» (номера принимаются только
   в списке, открытом маркером «п.»/«пп.»).
2. **Разведённость слота.** В строке реестра — ровно один кейс. Два кейса
   в одной ячейке означают, что статус наблюдения нельзя назначить
   покейсно, и это отдельный `ДЕФЕКТ`, а не расхождение множеств.
3. **Носители прогона — сверкой клейма с фактикой.** По каждому кейсу
   клейм колонки «Носитель прогона» (называет ли она код-тест / запрос
   коллекции; по кейсу — объединение его строк) сверяется с грепом по
   каталогу код-тестов и по коллекции. Расхождение в **обе** стороны —
   `ДЕФЕКТ`: носитель объявлен, но не найден, либо носитель существует,
   а колонка говорит «нет». Перечень кейсов берётся **из колонки «Кейс»
   той же таблицы**, не хардкодом: реестр пополнится — проверка увидит
   новый кейс сама. Кейс ищется **как идентификатор, а не как
   регулярка**: точка экранируется, границы стоят с **обеих** сторон,
   поэтому `M17.5` не совпадает ни с `M1755`, ни с `M17.55`, `AG6.1` — ни
   с `AG6X1` (левая граница), ни с `AG6.1b` (правая). Обе границы
   доказаны своими пробами; прежняя редакция закрывала только левую, и
   клейм «как идентификатор» был шире доказанного (F11 `DOCS_CHECK_28` и
   Н1 мини-петли критики). Дефект был латентным — на действительном
   состоянии обе формы шаблона давали один результат, и «носитель есть»
   могло подтвердиться **чужим** кейсом.

**Колонки берутся по именам шапки** («Пункт»/«№», «Кейс», «Носитель
прогона»), не по позиции, и при неразобранной шапке таблицы скрипт
**падает громко** (код возврата 3, `ОШИБКА: шапка таблицы реестра не
разобрана`), а не меняет смысл вывода молча. Тот же контракт — при
нулевом разборе гейт-заголовков плана и при отсутствии входных файлов
или каталога код-тестов. Клейм проверен пробами: переименованная колонка
шапки и историческая редакция без колонки «Носитель прогона» дают `rc=3`.

**Каждая ось доказана падающей пробой** — по стандарту
`.claude/processes/roadmap-step-execution.md` §«Оси проверочной команды
доказываются поимённо» (решение держателя 2026-08-29): падение на одной
оси не устанавливает остальных, поэтому проб — по одной на ось и сторону.
Прежняя редакция скрипта была «доказана» падением на одной оси и на двух
других молчала: `ДЕФЕКТ` разведённости уходил в stderr при коде возврата
0, форма «пп. N, M и K» терялась разбором, ось носителей на код возврата
не влияла (F1 `DOCS_CHECK_28`; ещё раньше проверка-предшественник
сравнивала множества кейсов и была зелена на породившем её дефекте — B2
мини-петли критики `GAPS_CLOSE_27`).

Батарея проб (мутации во временных копиях; рабочие файлы не трогаются).
Проба засчитана, только если **падает**; зелёная проба означает, что
мутация перестала попадать в носитель (изменилась опорная строка), и
проба переякоривается:

```bash
t=$(mktemp -d); P=.claude/tests/source-api/okx/plan.md; R=.claude/tests/source-api/okx/code-preconditions.md

# ось 1, плановая сторона (форма «пп. N, M и K»): гейт-пометке добавлены несуществующие пункты
sed 's/(предусловие п\. 1)/(предусловия пп. 1, 20 и 21)/' "$P" > "$t/plan.md"
bash tools/preconditions-check.sh "$t/plan.md" "$R"   # → «< AG1.5 20», «< AG1.5 21», rc=1

# ось 1, реестровая сторона: строка слота AG1.9-п.1 удалена
grep -v 'обратной стороны' "$R" > "$t/reg.md"
bash tools/preconditions-check.sh "$P" "$t/reg.md"    # → «< AG1.9 1», rc=1

# ось 2: два кейса в одной строке
sed '/Агрегирует ли источник/ s/`AG1.5`/`AG1.5`, `AG1.9`/' "$R" > "$t/reg.md"
bash tools/preconditions-check.sh "$P" "$t/reg.md"    # → «ДЕФЕКТ: пункт 1 — в одной строке 2 кейса…», rc=1

# ось 3, «объявлен — не найден»: AG1.5 объявляет код-тест, которого нет
sed '/`AG1.5`/ s/| нет |/| код-тест `AgX` |/' "$R" > "$t/reg.md"
bash tools/preconditions-check.sh "$P" "$t/reg.md"    # → «реестр объявляет код-тест, грепом … не найден», rc=1

# ось 3, «существует — не назван»: носители AG6.1 скрыты за «нет»
sed '/AG6.1/ s/| [^|]*код-тест[^|]*|/| нет |/' "$R" > "$t/reg.md"
bash tools/preconditions-check.sh "$P" "$t/reg.md"    # → два «ДЕФЕКТ … колонка … не называет», rc=1

# ось 3, «идентификатор, а не регулярка»: носители подменены чужим кейсом AG6X1
mkdir -p "$t/tests"; echo 'class X { void x() { observe("AG6X1"); } }' > "$t/tests/X.java"
echo '{"item":[{"name":"AG6X1 шумовой запрос"}]}' > "$t/coll.json"
bash tools/preconditions-check.sh "$P" "$R" "$t/tests/" "$t/coll.json"
                                                      # → два «ДЕФЕКТ … объявляет … не найден», rc=1
                                                      # прежняя форма (греп кейса регуляркой) на той же
                                                      # фикстуре: «СОШЛОСЬ», rc=0 — точка совпадала с «X»

# ось 3, правая граница идентификатора: чужой кейс с буквенным хвостом
mkdir -p "$t/t8"; echo 'class X { void x() { observe("AG6.1b"); } }' > "$t/t8/X.java"
echo '{"item":[{"name":"AG6.1b чужой кейс"}]}' > "$t/c8.json"
bash tools/preconditions-check.sh "$P" "$R" "$t/t8/" "$t/c8.json"
                                                      # → два «ДЕФЕКТ … объявляет … не найден», rc=1
                                                      # прежняя (асимметричная) правая граница `([^0-9]|$)`
                                                      # на той же фикстуре: «СОШЛОСЬ», rc=0 — «AG6.1b»
                                                      # засчитывался носителем кейса «AG6.1»

# контракт формы: переименованная колонка шапки
sed 's/| Кейс |/| Case |/' "$R" > "$t/reg.md"
bash tools/preconditions-check.sh "$P" "$t/reg.md"    # → «ОШИБКА: шапка … не разобрана», rc=3

bash tools/preconditions-check.sh                     # действительное состояние → «СОШЛОСЬ», rc=0
```

Историческая редакция, породившая F1 `DOCS_CHECK_27`, адресуется
**неподвижно** — редакция `GAPS_CLOSE_26`, `git show d132a37:<файл>`
(относительный адрес `git show HEAD` протух в момент коммита — F3
`DOCS_CHECK_28`). Нынешний скрипт на ней падает громко с `rc=3`: та
редакция реестра ещё не несла колонку «Носитель прогона». Канон
доказательства — батарея проб выше: она не зависит от истории git.

Расхождение слотов означает одно из двух: пара «пункт × кейс» помечена
гейтящей в плане и не заведена в реестр (гейт не сработает) либо названа
в реестре и снята с гейта в плане (гейт держит шаг без основания).
Правится **вместе** — план и реестр несут одно множество пар, а не два.

### Колонка «Носитель прогона» — почему она отдельная

Колонка «Статус наблюдения» отвечает только на вопрос «факт добыт?».
Вопрос «а есть ли чем добывать?» она не выражает вовсе, и потому
одиннадцать «⏳ не собрано» читались как «осталось нажать кнопку», тогда
как прогонять нечего: код-тест и запрос коллекции есть у **одного** кейса
из десяти, и тот производит не то наблюдение (F2, F3 `DOCS_CHECK_27`).

Цена решения названа: колонка в одной таблице, связность нулевая,
проверяемость — ось 3 `tools/preconditions-check.sh`: клейм колонки
сверяется с фактикой (перечень кейсов берётся из этой же таблицы,
грепается по каталогу код-тестов и по коллекции; расхождение клейма с
фактикой — `ДЕФЕКТ`, а не справка).

Отвергнуто: расширять статус кейса в плане (правка конвенции по 17
заголовкам и шаблону `templates/docs/test-plan.md` при той же
разрешающей способности) и «читать состояние носителя из плана» — план
его тоже не различает: `⏳ PENDING` стоит и у кейса с код-тестом, и без.

### Исход «не наступило» — запись, не закрытие

Механику исходов держит `.claude/tests/source-api/okx/plan.md` §«Исходы
содержательного кейса — их три, и третий гейт не закрывает». **Закрывающей
силы у третьего исхода больше нет** — решение держателя 2026-08-30
(§«Правило гейта» выше). Исход остаётся **записью наблюдения**: кейс
прогнан, событие за прогон не наступило; слот при этом остаётся
`PENDING` с этой причиной в колонке статуса.

Что изменилось и почему это важно: п. 10 (`AG1.8`, след автоделевериджа)
был назван первым кандидатом на закрытие этим исходом. Прогон 2026-08-30
кейс исполнил, эпизода ADL не наблюдал — и по новой редакции слот
**остался открытым**. Так и записано в таблице.

Заголовок плана §«Исходы содержательного кейса — их три, и третий
закрывает гейт» на сегодня своей второй половиной противоречит этому
дому; правка плана — в отчёте прогона.

## Что в реестр НЕ входит

- **Пункт 8 — калибратор допуска сверки** — гейтом быть перестал решением
  держателя (`GAPS_CLOSE_19`): в `CODE` выходим с некалиброванным
  допуском, он калибруется до боевой торговли
  (`.claude/work/backlog.md` §«Закрытие фазы 1 — снятие разведочного
  режима допуска сверки»). Номер назван явно: пока он молчал, ссылка
  «предусловие `CODE` п. 8» жила в рабочем файле как действующая
  (F8 `DOCS_CHECK_27`).
- **Пункт 13 — цена доведения шага до `CODE` при незакрытых разрывах** —
  решён держателем 2026-08-23 и гейтом не является: клеймы полноты шага 7
  непроверяемы до реализации системного слоя, и «чисто» на `DOCS_CHECK`
  означает «доки не противоречат друг другу», а не «спецификация
  реализуема». Отчёт прогона обязан различать эти два утверждения явно.
- **Номера 3, 4, 11, 12** — снятые редакции реестра до его переноса; их
  предмет ни в одном живом носителе гейтящим не значится. Номера не
  переиспользуются и не сдвигаются (`.claude/rules/closed-work-transfer.md`
  §«Ссылочная целостность»); действующее множество гейтящих пунктов
  задаётся **таблицей выше** и сверяется с планом командой §«Сверка
  реестра с планом».
- **`M15.7` (семантика `actualPx`)** — гейтом не размечен ни планом, ни
  реестром.
- **Знаки трёх операндов записи и bill'а штрафа** (`AG1.7`, пункт 7
  §«Грунт `integrator`» backlog) — гейта не образуют: знак выясняется
  прогоном и правится по факту. Кейс общий с пп. 7 и 14, предусловие —
  нет.

## Что добыто и что осталось

Состояние на **2026-08-30**: среда поднята, контур прогнан автономно,
**закрыто десять слотов из тринадцати**. Оба блокера редакции 2026-08-29
сняты — носители прогона написаны, среда работает; открытыми остались три
слота, и причина у каждого своя, названная фактом, а не догадкой.

### Среда контура — проверено прогоном, не выведено

| Что нужно контуру | Состояние | Чем проверено |
|---|---|---|
| JDK 25 | **есть** — `~/.jdks/corretto-25`, Corretto 25.0.0.36.2 | `~/.jdks/corretto-25/bin/java -version` |
| Maven | **есть** — дистрибутив wrapper'а `~/.m2/wrapper/dists/apache-maven-3.9.11-bin/*/apache-maven-3.9.11`; на `PATH` его нет, `mvnw` в репозитории по-прежнему нет | `$MAVEN_HOME/bin/mvn -v` → 3.9.11 на JDK 25 |
| Docker + контур `docker-compose.yml` | **есть** — три контейнера подняты | `docker ps` → `vibetradingbotv5-{vault,postgres,postgres-test}` |
| Vault `127.0.0.1:8200` | **распечатан** (`sealed:false`), `secret/tradingbot/okx-test` и `secret/tradingbot/postgres-test` читаются | `curl /v1/sys/health`; чтение путей токеном из `.env.vault.test.local` |
| Postgres-test `5441` | **слушает**, Flyway накатывает схему при подъёме профиля `test` | подъём `@SpringBootTest` профиля `test` → `Database version: 16.13` |
| Сеть до биржи | **есть** | `GET /api/v5/public/time` → `HTTP 200` |
| Демо-контур кредов | **доказан отрицанием** | тот же ключ **без** заголовка `x-simulated-trading` → `50101 APIKey does not match current environment`; с заголовком → `code=0`, `label=BotV5-test`, `acctLv=2`, `posMode=net_mode` |

**Проверка демо-контура — падающая проба, а не флаг конфигурации.**
Клейм «ключ демо-контура» доказывается тем, что боевой контур этот ключ
**отвергает**, а не тем, что профиль выставляет `okx.simulated=1`.
Второе — заявление о нашей настройке, первое — свойство самого ключа.

### Воспроизводимые команды

```bash
export JAVA_HOME="$HOME/.jdks/corretto-25"
export MAVEN_HOME="$HOME/.m2/wrapper/dists/apache-maven-3.9.11-bin/6mqf5t809d9geo83kj4ttckcbc/apache-maven-3.9.11"
export PATH="$JAVA_HOME/bin:$MAVEN_HOME/bin:$PATH"
source .env.vault.test.local                 # VAULT_TOKEN профиля test
docker compose up -d vault postgres-test     # если контур опущен

# джобы гасятся, чтобы состояние счёта за прогон меняли только кейсы
export CANDLE_JOB_ENABLED=false INDICATOR_JOB_ENABLED=false \
       MARKET_STRUCTURE_JOB_ENABLED=false INSTRUMENT_RULES_SYNC_ENABLED=false \
       ENTRY_SCANNER_ENABLED=false DEAL_ORCHESTRATOR_ENABLED=false

mvn -o test -Dgroups=source-api-live -Dtest=Ag1DealFixtureLiveTest    # пп. 1, 2, 6, 9, 14, 15 (+ 7, 10 — не наблюдены)
mvn -o test -Dgroups=source-api-live -Dtest=Ag6BillSubtypesLiveTest   # пп. 2, 16
mvn -o test -Dgroups=source-api-live -Dtest=Mg7IndexCandlesLiveTest   # п. 5
mvn -o test -Dgroups=source-api-live -Dtest=M17CancelOrderLiveTest    # п. 17 (C17a наблюдён, C17b/C17c — нет)

bash tools/preconditions-check.sh            # сверка реестра с планом и с фактикой носителей
```

### Что добыто

**П. 1 и 15 — агрегация и адресация записи (`AG1.5`, `AG1.9`).** Источник
**агрегирует** слайсы частичного закрытия в **одну** финализированную
запись эпизода: у эпизода «открыть 0.02 → закрыть 0.01 → закрыть остаток»
запись одна, `closeTotalPos=0.02` (полный закрытый объём, не последний
слайс), `realizedPnl` кумулятивен и **точно** равен
`pnl + fee + fundingFee + liqPenalty`. Сверка с движениями счёта сходится
без остатка: Σ`pnl` и Σ`fee` торговых bills окна равны суммам по записям.

**Посылка «один эпизод ↔ один `posId`» ложна.** Переоткрытая позиция по
тому же инструменту получает **тот же** `posId`: два эпизода в одном окне
дали две записи с одинаковым `posId` и разными `cTime`; запрос
`positions-history` с фильтром `posId` вернул **все** записи этого
инструмента, а не одну. Наблюдено дважды независимо (ручной прогон и
код-тест). Адресуемая единица — **пара (`posId`, `cTime`)**, а не `posId`.
Что это ломает — названо в плане §AG1.9 «Что решает вторая половина
кейса» (B11 `DOCS_CHECK_20`): дискриминатор смены эпизода
(`docs/lifecycles/Position.md`) и ключ `uk_position_deal_external`.
Правка обоих — продуктовая работа закрытия, не реестра.

**П. 6 — оси адресации (`AG1.6`).** `positions-history` принимает `instId`
**без** `instType`; окно по `uTime` (`before`/`after`) отбирает ровно
записи окна сделки; каждая запись несёт `cTime`, `direction`, `openAvgPx`
— операнды материализации `Position` на create-тропе. Порядок записей в
окне — по убыванию `uTime`.

**П. 2 и 16 — справочник типов (`AG6.1`, `AG6.2`).** Справочник —
**32 верхних типа и 179 пар `type`/`subType`**; перечень сохранён
код-тестом построчно. Прежнее наблюдение «`data.size=32`» было счётом
**верхних типов**, а не пар, — то есть даже как мощность оно измеряло не
ту величину. В окне сделки фактически появились **две** пары: `2/1` (Trade
/ Buy) и `2/2` (Trade / Sell); **маржинальных движений `type=6` в окне
нет** — при isolated-марже перевод маржи идёт полем `posBalChg` **внутри**
торгового bill'а, отдельной записи не порождая. Список исключений сверки
непуст по построению: 177 пар справочника в экономику сделки не входят.

**П. 9 — валюта комиссии (`AG3.4`).** У всех записей окна `ccy="USDT"` —
инвариант settle-ccy держится. Комиссия приходит **полем `fee` внутри
торгового bill'а**, отдельной fee-записи нет. Движений вне settle-ccy в
окне не наблюдалось, поэтому вторая проверка (состав `realizedPnl` при
cross-ccy-издержке) этой фикстурой **не разрешена** — она и не гейтит.

**П. 14 — форма пустого значения (`AG1.7`).** Ожидание подтверждено:
несобытийные `fundingFee` и `liqPenalty` приходят строковым **`"0"`**,
ключ присутствует. Контраст наблюдён и записан: неприменимые к записи
поля (`settledPnl`, `nonSettleAvgPx`, `triggerPx`) приходят **пустой
строкой** — то есть форма пустого значения у источника **различается по
полю**, и конвенция «пусто = 0» верна для числовых слагаемых, а не для
всех полей записи.

**П. 5 — носитель курса cross-ccy (`MG7.5`).** Секундный бар у пары
котировки **есть**: `index-candles(ETH-USDT, bar=1s)` отдаёт данные,
строка — `[ts, o, h, l, c, confirm]`, close по индексу 4. Одним запросом
берётся **300** строк, то есть диапазон читается разом, а не построчно.
Глубина: свежий `index-candles` окно **в прошлом не обслуживает вовсе**
(`n=0` на любом `after` в прошлом) — носитель глубины
`history-index-candles`; `bar=1s` достаёт назад до **~150 дней** (на 180
уже пусто), `bar=1m` — не менее **365 дней**. Отсюда правило деградации:
1s в пределах ~5 месяцев, дальше — 1m.

**П. 17 — частично (`M17.5`, цепочка C17a).** Встроенная защита
представлена у живого родителя элементом `attachAlgoOrds[0]` с
`attachAlgoClOrdId` и присвоенным биржей `attachAlgoId`; самостоятельной
записью в `orders-algo-pending` она **не видна** (ни до отмены, ни после).
После отмены родителя без налива `attachAlgoOrds[0]` **сохраняется в
теле отменённого родителя** с прежними значениями и пустыми
`failCode`/`failReason`; `cancelSource=1` («Order was canceled by you»).
Несущая половина вопроса — судьба защиты при **непустом наливе** — не
наблюдена (ниже).

### Что осталось открытым — три слота

**П. 7 (`AG1.7`, горизонт `fundingFee`) — `PENDING`, фикстуру собрать не
удалось.** Слагаемое наблюдаемо только у позиции, пережившей расчёт
фандинга; за окно прогона границу интервала не пересекали
(`fundingBillsInWindow = 0`), ближайший расчёт был более чем через десять
часов. Что нужно: удержать позицию через границу funding-интервала
(00:00 / 08:00 / 16:00 UTC) и перепрогнать `ag1_7_numericFieldSemantics` —
кейс уже несёт ветвь сверки Σ`balChg` funding-bills с Σ`fundingFee`
записей. Отдельная оговорка: вопрос сформулирован как «за всю жизнь
`posId`», а `posId` переиспользуется — при перепрогоне вопрос читается
как «за жизнь **эпизода**».

**П. 10 (`AG1.8`, след автоделевериджа) — `PENDING`, событие не
заказуемо.** Кейс прогнан: bills типов 5 (Forced liquidation) и 9 (ADL) в
окне сделки и за весь доступный семидневный период — ноль. Эпизод ADL
инициирует биржа, на demo заказать его нельзя. Прежняя редакция реестра
закрыла бы слот исходом `OBSERVED_ABSENT`; по решению держателя
2026-08-30 исход «не наступило» гейт не закрывает, поэтому слот открыт.

**П. 17 (`M17.5`, судьба защиты при непустом наливе) — `PENDING`,
фикстуру собрать не удалось.** Обе несущие цепочки требуют **частичного**
налива под потолком риска 200 USDT. На demo `ETH-USDT-SWAP` потолок
эквивалентен **0.81 контракта**, тогда как объём лучшего уровня книги —
десятки и тысячи контрактов (наблюдено 55.6, 14.82, 3043.85). Отсюда
любая заявка под потолком наливается **целиком**: C17b пропускает попытку
по правилу «объём лучшего аска больше половины потолка», а единственная
прошедшая попытка (лучший аск 0.02) выродилась в `filled` — книга
сменилась между чтением и постановкой. C17c (`ioc`) недостижим по тому же
счёту: размер «2 × объём лучшего уровня» выходит за потолок на два-три
порядка. Три попытки исчерпаны, кейс остаётся `PENDING`; подмена этого
исхода на «не наступило» закрыла бы гейт ложно.

Что могло бы его снять — развилка держателя, не автономный выбор:
поднять потолок риска цепочки; взять инструмент с тонкой книгой (правка
плана, кейс перестаёт быть про `ETH-USDT-SWAP`); либо ловить частичный
налив пассивной заявкой в очереди, что делает цепочку недетерминированной.

### Что среда всё ещё не даёт

- **`mvnw` в репозитории нет.** Прогон опирается на дистрибутив Maven,
  лежащий в кэше wrapper'а пользователя, и на ручной `PATH`. Состав работ
  — `.claude/work/backlog.md` §«Средовой дефицит автономного RUN тестов
  (контур source-api)».
- **Токен `.env.vault.test.local` читает и prod-пути.** Проверено: тем же
  токеном отвечают `secret/tradingbot/okx` и `secret/tradingbot/postgres`,
  тогда как `.env.vault.test.local.example` требует токена, ограниченного
  тестовыми путями. Это расхождение среды с собственным требованием, не
  дефект кода; адресат — шаг 9 «Безопасность».
- **`tools/spec-run.sh` не переносим на Linux:** путь JDK по умолчанию —
  `C:/Users/...`, разделитель classpath — `;`. Переменные `SPEC_JDK`/`SPEC_M2`
  первое обходят, второе — нет.


## Связи

- Кейсы и их прогон — `.claude/tests/source-api/okx/plan.md`.
- Процесс контура — `.claude/processes/source-api-testing.md`.
- Дом гейта `CODE` (оба условия) — `.claude/processes/roadmap-step-execution.md`
  §Гейты; встречный якорь гейта грунта —
  `.claude/skills/update-roadmap-progress.md` §«Гейт грунта».
- Исполнительный хвост шага — `.claude/work/backlog.md`.
