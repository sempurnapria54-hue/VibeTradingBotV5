# Гейтящие предусловия `CODE` шага 7

## На какой вопрос отвечает этот файл

Какие факты источника обязаны быть добыты до входа шага 7 в `CODE`.

## Почему реестр здесь, а не в `docs/`

Прежний дом реестра — `docs/rules/pnl-reconciliation.md`, и это было
ошибкой размещения: реестр отвечает на вопрос **проверочной
деятельности** («что обязано быть измерено до того, как писать код»), а
не на продуктовый вопрос («как сверяется результат сделки»). Переписывание
корпуса по `docs/concept.md` вычистило процессную арматуру из `docs/` — и
вместе с ней снесло реестр, **не перенеся** его: четыре дока остались
ссылаться на несуществующие номера.

Дом восстановлен здесь по `.claude/rules/structure.md` — строка
`.claude/tests/{testType}/{source}/`: «Планы — артефакт проверочной
деятельности (пайплайн), не продуктовое знание об источнике».

Восстановлено `GAPS_CLOSE_26`; содержание собрано из
`.claude/work/backlog.md` §«Шаг 7 (сделки и P&L) — исполнительный хвост»
и меток «ГЕЙТИТ `CODE`» в `plan.md`.

## Правило гейта

**Открытый слот гейтит свой компонент, а не шаг** (редакция 2026-09-01,
`.claude/decisions/code-contact-as-gate.md` §«Покомпонентное чтение гейта
грунта»). Компонент, которому нужен добытый факт, не кодируется, пока его
слот открыт; прочие компоненты слотом не гейтятся, и статус шага он не
держит. Привязка «слот → компонент» живёт величиной `ждёт-грунта` в
реестре гейтов (`.claude/work/code-gate-ledger.json`) и проверяется
прогоном `python3 tools/code-gate-check.py` (ось 15).

**Снятая редакция — «шаг 7 не входит в `CODE`, пока хотя бы один слот
реестра не закрыт».** Она снята вместе с пороговой формой первого условия
гейта: обе мерили состояние **шага**, тогда как поправка держателя мерит
**компонент**. Что слот закрывает и чем — не изменилось ничем из
перечисленного ниже.

- **Слот** — пара «пункт × кейс». Пункт закрыт, когда закрыты **все** его
  слоты: у двух пунктов носителей факта два (п. 1 — `AG1.5` и `AG1.9`;
  п. 2 — `AG6.1` и `AG6.2`), и наблюдение одного кейса второй слот не
  закрывает.
- **Слот закрывается прогоном своего кейса** в контуре
  `.claude/processes/source-api-testing.md` — **наблюдением**.
- **Кроме слотов, объявивших ДВУХШАГОВЫЙ контракт.** Часть предусловий
  добывается прогоном лишь наполовину: кейс приносит **операнд**
  (перечень, справочник), а сам ответ строится **семантической разметкой**
  этого операнда — работой, которой тест не делает и делать не должен
  (тест не принимает продуктовых решений). Такой слот закрывается
  **двумя** шагами, и оба обязаны быть названы в его строке:
  1. **грунтовая половина** — прогон кейса, записавший операнд в
     `observations/`;
  2. **ответная половина** — **названный артефакт**, зафиксированный
     **названным писателем** (хэнд-офф, файл и ключ).

  Слот с двухшаговым контрактом закрыт, только когда предъявлены **оба**.
  Прогон, давший операнд, закрывает первую половину и **не закрывает
  слот**: без второй половины сверка уехала бы в `CODE` по
  непостроенному ответу.

  **Класс введён потому, что правило его не знало, а таблица уже
  использовала** (F2 `DOCS_CHECK_34`): строка `AG6.2` объявляла закрытие
  «составом в конфиге исключений», тогда как дом говорил безусловно
  «закрывается наблюдением», а исполнимая процедура §«Гейт грунта»
  реализовывала безусловную редакцию — и пропускала слот после
  перепрогона, давшего одни кандидаты.
- **Исход «событие не наступило» слот не закрывает** (решение держателя
  2026-08-30, дословно: «гейт исходом „не наступило“ не закрывается»).
  Исход `OBSERVED_ABSENT` остаётся **записью наблюдения** — что кейс
  прогнан и событие в окне не пришло, — и разбирается в §«Исход „не
  наступило“ — запись, не закрытие»; закрывающей силы у него нет.
- **Фикстура, которую собрать не удалось, оставляет слот `PENDING`** — с
  названной причиной в колонке статуса. «Не собралась фикстура» и «событие
  не наступило» — разные вещи, и ни одна из них слот не закрывает.

Предусловие несёт дефицит **`грунт`**: не хватает внешнего факта, а не
проектирования и не решения держателя. По
`.claude/processes/roadmap-step-execution.md` §4 такие пункты уходят на
сбор **сразу**, не дожидаясь чистого `DOCS_CHECK`, — иначе образуется
петля: факт ждёт чистого прогона, а прогон без факта чистым не станет.

**Ярлык `грунт` описывает дефицит, но не блокеры сбора.** Факта не
хватает — это `грунт`, и он таким и остаётся; но собрать его сегодня
нельзя не поэтому, а из-за двух блокеров: непройденного этапа 3 контура
(аппрув пользователя — **действие**, не дефицит знания) и среды.
Владельцы и адресаты обоих — §«Что это значит для гейта». Прежде дом и
владелец были только у второго, и первый не доходил ни до кого
(F4 `DOCS_CHECK_28`).

**Этот файл — дом правила гейта грунта.** Встречный якорь со стороны
события — `.claude/skills/update-roadmap-progress.md` §«Гейт грунта»
(пункт `CODE` в §«Гейт-предусловие перехода (`CODE` / `DONE`)» на неё
ссылается): проверка **исполняется** в момент простановки статуса
`CODE`, а не тогда, когда кто-то вспомнит открыть реестр.

**Скилл несёт исполнимую процедуру, и это не пересказ политики.** Шаги
§«Гейт грунта» повторяют клаузы дома **намеренно**: гейт заведён для
того, кто дом не открыл, и обязан отработать без его открытия. Правило
при этом одно, и живёт оно здесь — при расхождении формулировок верен
дом (`.claude/rules/policy-home.md`: упоминание вне дома не подменяет
дом). Прежняя редакция обоих носителей заявляла «скилл правило не
пересказывает» и тут же его пересказывала — клейм снят как неверный, не
процедура (F12 `DOCS_CHECK_28`).

**Проверка полноты этой таблицы — отдельный шаг гейта, и он идёт раньше
чтения статусов.** Гейт читает колонку статуса, а слот, в таблицу **не
заведённый**, статуса не имеет и пропускается молча; ловит это только
`tools/preconditions-check.sh` (§«Сверка реестра с планом — исполнимой
проверкой»). Поэтому скрипт — инструмент гейта, а не иллюстрация
реестра: `.claude/skills/update-roadmap-progress.md` §«Гейт грунта»
прогоняет его **до** чтения колонки статуса и при ненулевом коде
возврата статус не проставляет (F2 `DOCS_CHECK_28`: до этого ни одной
входящей ссылки на скрипт из `.claude/skills/**` и
`.claude/processes/**` не было).

## Реестр

| Пункт | Что должно быть установлено | Кейс | Носитель прогона | Статус наблюдения |
|---|---|---|---|---|
| 1 | Агрегирует ли источник partial-закрытия в **одну** финализированную запись на `posId`, и в какой момент запись финализирована | `AG1.5` | код-тест `Ag1DealFixtureLiveTest`.ag1_5_partialCloseAggregation | ✅ собрано (2026-08-30) |
| 1 | То же с обратной стороны: несколько записей в окне — это эпизоды разных `posId`, а не партиалы одного | `AG1.9` | код-тест `Ag1DealFixtureLiveTest`.ag1_9_episodesInWindow | ✅ собрано (2026-08-30) — **посылка ложна**, см. §«Что добыто» |
| 2 | Справочник типов движений: полный перечень `type`/`subType`, встречающихся на счёте | `AG6.1` | код-тест `Ag6BillSubtypesLiveTest.ag6_1_directDictionary` | ✅ собрано (2026-09-01) — перепрогон записал перечень в `observations/AG6_1.md`: **32 верхних типа, 179 пар**. Носитель персистентный, не лог |
| 2 | То же, вторая половина: какие типы справочника **не** принадлежат экономике сделки — список исключений сверки | `AG6.2` | код-тест `Ag1DealFixtureLiveTest`.ag6_2_typesOutsideDealEconomics | ✅ собрано (2026-09-02) — **двухшаговый контракт, обе половины предъявлены**: грунтовая — перечень 177 кандидатов (пары справочника вне окна сделки) в `observations/AG6_2.md`, прогон первым заходом после оживления среды; ответная — состав в секции «Типы вне экономики сделки» `docs/integrations/okx/rules/cash-flow-categories.md` (2026-09-01) |
| 5 | Есть ли у пар котировки бар секундного разрешения — носитель курса cross-ccy | `MG7.5` | код-тест `Mg7IndexCandlesLiveTest.mg7_5_crossCcyRateCarrier` | ✅ собрано (2026-08-30) |
| 6 | Оси адресации записи истории позиций, у которой нет `posId` | `AG1.6` | код-тест `Ag1DealFixtureLiveTest`.ag1_6_addressingAxes | ✅ собрано (2026-08-30) |
| 7 | Горизонт доступности `fundingFee` в записи закрытия | `AG1.7` | код-тест `Ag1FundingHorizonLiveTest` — сам читает интервал инструмента и пишет исход в `observations/AG1_7.md` на всех трёх ветвях (пропуск — 2026-09-01; «сверка не сошлась» — 2026-09-02; закрытие — 2026-09-03) | ✅ собрано (2026-09-03) — запись закрытия эпизода, пережившего расчёт, несёт ненулевой `fundingFee` **сразу после закрытия**; оба знака предъявлены двумя прогонами (+0.0103… доход, −0.0101… расход); сверка Σ(`balChg`+`posBalChg`) == `fundingFee` сходится точно. Попутный факт источника: на isolated-марже расчёт ложится в `posBalChg` при нулевом `balChg`. Разбор — §«Что осталось открытым» |
| 9 | Приходят ли комиссии в расчётной валюте инструмента | `AG3.4` | код-тест `Ag1DealFixtureLiveTest`.ag3_4_feeCurrency | ✅ собрано (2026-08-30) |
| 14 | Форма пустого значения числовых полей записи (пустая строка / `"0"` / отсутствие ключа) | `AG1.7` | код-тест `Ag1DealFixtureLiveTest`.ag1_7_numericFieldSemantics | ✅ собрано (2026-08-30) |
| 15 | Отдаёт ли окно с несколькими эпизодами **отдельную** запись на каждый `posId` | `AG1.9` | код-тест `Ag1DealFixtureLiveTest`.ag1_9_episodesInWindow | ✅ собрано (2026-08-30) — **посылка ложна**, см. §«Что добыто» |
| 16 | Отображение `type`/`subType` источника в экономические категории движения | `AG6.1` | код-тест `Ag6BillSubtypesLiveTest.ag6_1_directDictionary` — сохраняет перечень пар в `observations/AG6_1.md` | ✅ собрано (2026-09-01) — **двухшаговый контракт**, обе половины предъявлены: грунтовая — перечень 179 пар в `observations/AG6_1.md`; ответная — отображение «пара источника → `CashFlowCategory`» в `docs/integrations/okx/rules/cash-flow-categories.md` §«Отображение пар источника в категории» |
| 17 | Поведение встроенной защиты при отмене родительской заявки | `M17.5` | код-тест `M17CancelOrderLiveTest.m17_5_attachedProtectionOnParentCancel` | ✅ собрано (2026-08-30) — все три цепочки; **защита переживает терминал родителя**, см. §«Что добыто» |

**Числа состояния здесь не хранятся: единственный носитель — колонка
статуса таблицы выше.** Сколько слотов открыто и какие — читается
таблицей, а не сводной строкой: сводка есть второй носитель того же
факта, и он расходится с таблицей при первой же смене статуса. Так уже
было: два слота вернулись в `PENDING`, а сводка продолжала утверждать,
что открыт один.

**Структура перекрытий — не счёт, и она записана.** Слот — пара «пункт ×
кейс»; пункт закрыт, когда закрыты **все** его слоты. Три кейса несут по
два пункта (`AG1.7` — пп. 7 и 14; `AG1.9` — пп. 1 и 15; `AG6.1` — пп. 2 и
16), два пункта закрываются **парами** кейсов (п. 1 — `AG1.5` и `AG1.9`;
п. 2 — `AG6.1` и `AG6.2`). Этими перекрытиями слоты и сворачиваются в
пункты; сами числа берутся прогоном
`bash tools/preconditions-check.sh`, а не отсюда.

**П. 10 из гейтящей таблицы снят** решением держателя 2026-08-30 — не
закрыт наблюдением, а выведен из-под гейта: см. §«Что в реестр НЕ
входит».

### Сверка реестра с планом — исполнимой проверкой

Проверку несёт скрипт **`tools/preconditions-check.sh`** (запуск из корня
репозитория, без аргументов — умолчания контура OKX):

```bash
bash tools/preconditions-check.sh
# СОШЛОСЬ: слоты реестра == гейт-пометки плана     → код возврата 0
```

Он меряет **четыре** оси, каждая поднимает дефект до кода возврата, и
первые две — ровно та конструкция, которую реестр ввёл:

1. **Слот-сверка.** Множество пар «пункт × кейс» реестра обязано совпадать
   с множеством пар, объявленных гейт-заголовками плана
   (`**ГЕЙТИТ `CODE`** (предусловия п. N …)`). Сравниваются **пары**, а не
   множества кейсов: расхождение «кейс в реестре есть, но не на том
   пункте» множеством кейсов не ловится вовсе. Плановая сторона разбирает
   формы «п. N», «пп. N и M» и «пп. N, M и K» (номера принимаются только
   в списке, открытом маркером «п.»/«пп.»).
2. **Разведённость слота.** В строке реестра — ровно один кейс. Два кейса
   в одной ячейке означают, что статус наблюдения нельзя назначить
   покейсно, и это отдельный `ДЕФЕКТ`, а не расхождение множеств.
3. **Носители прогона — сверкой клейма с фактикой, и клейм читается
   поимённо.** По каждому кейсу проверяются **две** вещи:
   - **фактика по кейсу** — есть ли в каталоге код-тестов **исполнимая**
     строка с идентификатором кейса. Строки комментария (javadoc, `//`)
     носителем не считаются: упоминание кейса в шапке базового класса
     засчитывалось покрытием у кейса, тест-метода не имеющего вовсе;
   - **клейм колонки** — названные ею **класс и метод** обязаны
     существовать (`<Class>LiveTest` в каталоге, метод — объявлением в
     этом классе). Прежняя редакция оси грепала **идентификатор кейса**,
     то есть колонку не читала вовсе: подмена имени класса на
     несуществующий проверку не роняла, и падающей пробы у оси не было
     (батарея проб ниже даёт `rc=1` на подмене класса и на подмене метода).

   Расхождение в **обе** стороны — `ДЕФЕКТ`: носитель объявлен, но не
   найден, либо носитель существует, а колонка говорит «нет». Перечень
   кейсов берётся **из колонки «Кейс»** той же таблицы, не хардкодом.
   Кейс ищется **как идентификатор, а не как регулярка**: точка
   экранируется, границы стоят с **обеих** сторон, поэтому `M17.5` не
   совпадает ни с `M1755`, ни с `M17.55`, `AG6.1` — ни с `AG6X1` (левая
   граница), ни с `AG6.1b` (правая). Обе границы доказаны своими пробами.

   **Коллекция из оси выведена:** она не аппрув-артефакт и не исполнитель
   прогона (`.claude/decisions/collection-not-approval-artifact.md`),
   поэтому клейм о ней измерял бы третью запись того же знания.

4. **Разобранность каждого гейт-заголовка.** Каждый заголовок плана,
   помеченный `**ГЕЙТИТ `CODE`**`, обязан дать **хотя бы одну** пару
   «пункт × кейс». Охрана `[ -s plan-pairs ]` этого не ловила: она
   срабатывает только при **нулевом** разборе, а потерянный **один** из
   многих заголовков оставлял `СОШЛОСЬ` и код возврата 0. Порог ошибки
   при этом не «смена конвенции», а опечатка: форму `(п.18)` — маркер
   `п.` без пробела перед номером — теряет тот же разбор, что и
   непредусмотренную `(предусловие 18)`. Обе формы доказаны пробами
   ниже.

**Колонки берутся по именам шапки** («Пункт»/«№», «Кейс», «Носитель
прогона»), не по позиции, и при неразобранной шапке таблицы скрипт
**падает громко** (код возврата 2 — «проверка не проводилась», `ОШИБКА:
шапка таблицы реестра не разобрана`), а не меняет смысл вывода молча. Тот же контракт — при
нулевом разборе гейт-заголовков плана и при отсутствии входных файлов
или каталога код-тестов. Клейм проверен пробами: переименованная колонка
шапки и историческая редакция без колонки «Носитель прогона» дают `rc=2`.

**Каждая ось доказана падающей пробой** — по стандарту
`.claude/processes/roadmap-step-execution.md` §«Оси проверочной команды
доказываются поимённо» (решение держателя 2026-08-29): падение на одной
оси не устанавливает остальных, поэтому проб — по одной на ось и сторону.
Прежняя редакция скрипта была «доказана» падением на одной оси и на двух
других молчала: `ДЕФЕКТ` разведённости уходил в stderr при коде возврата
0, форма «пп. N, M и K» терялась разбором, ось носителей на код возврата
не влияла (F1 `DOCS_CHECK_28`; ещё раньше проверка-предшественник
сравнивала множества кейсов и была зелена на породившем её дефекте — B2
мини-петли критики `GAPS_CLOSE_27`).

Батарея проб (мутации во временных копиях; рабочие файлы не трогаются).
Проба засчитана, только если **падает**; зелёная проба означает, что
мутация перестала попадать в носитель (изменилась опорная строка), и
проба переякоривается:

```bash
t=$(mktemp -d); P=.claude/tests/source-api/okx/plan.md; R=.claude/tests/source-api/okx/code-preconditions.md

# ось 1, плановая сторона (форма «пп. N, M и K»): гейт-пометке добавлены несуществующие пункты
sed 's/(предусловие п\. 1)/(предусловия пп. 1, 20 и 21)/' "$P" > "$t/plan.md"
bash tools/preconditions-check.sh "$t/plan.md" "$R"   # → «< AG1.5 20», «< AG1.5 21», rc=1

# ось 1, реестровая сторона: строка слота AG1.9-п.1 удалена
grep -v 'обратной стороны' "$R" > "$t/reg.md"
bash tools/preconditions-check.sh "$P" "$t/reg.md"    # → «< AG1.9 1», rc=1

# ось 2: два кейса в одной строке
sed '/Агрегирует ли источник/ s/`AG1.5`/`AG1.5`, `AG1.9`/' "$R" > "$t/reg.md"
bash tools/preconditions-check.sh "$P" "$t/reg.md"    # → «ДЕФЕКТ: пункт 1 — в одной строке 2 кейса…», rc=1

# ось 3, клейм колонки: названный КЛАСС не существует
sed 's|`M17CancelOrderLiveTest\.m17_5_attachedProtectionOnParentCancel`|`AgXNoSuchLiveTest.m17_5_attachedProtectionOnParentCancel`|' "$R" > "$t/reg.md"
bash tools/preconditions-check.sh "$P" "$t/reg.md"    # → «колонка называет носителя, которого нет: класс AgXNoSuchLiveTest», rc=1
                                                      # прежняя редакция оси на той же мутации: «СОШЛОСЬ», rc=0 —
                                                      # она грепала идентификатор КЕЙСА и колонку не читала

# ось 3, клейм колонки: названный МЕТОД не существует
sed 's/m17_5_attachedProtectionOnParentCancel/m17_5_noSuchMethod/' "$R" > "$t/reg.md"
bash tools/preconditions-check.sh "$P" "$t/reg.md"    # → «…которого нет: метод M17CancelOrderLiveTest.m17_5_noSuchMethod», rc=1

# ось 3, клейм строки неизмерим: строка объявляет код-тест и не именует его
sed 's/| код-тест `Ag6BillSubtypesLiveTest.ag6_1_directDictionary` — сохраняет перечень пар/| тот же код-тест — сохраняет перечень пар/' "$R" > "$t/reg.md"
bash tools/preconditions-check.sh "$P" "$t/reg.md"    # → «слот п. 16 × AG6.1 — колонка объявляет код-тест,
                                                      #    но не именует носителя разбираемой формой», rc=1
                                                      # клейм читается ПОСТРОЧНО: прежняя редакция сливала
                                                      # строки одного кейса в общий клейм, и эта строка
                                                      # проходила зелёной на соседней строке того же кейса

# ось 3, «существует — не назван»: носители AG6.1 скрыты за «нет»
sed '/AG6.1/ s/| [^|]*код-тест[^|]*|/| нет |/' "$R" > "$t/reg.md"
bash tools/preconditions-check.sh "$P" "$t/reg.md"    # → два «ДЕФЕКТ … колонка … не называет», rc=1

# Изолированная фикстура осей носителя: ПОЛНАЯ копия каталога тестов с
# вычисткой исполнимых вхождений ОДНОГО кейса (AG6.1). Однофайловая
# фикстура ось не изолировала: на ней отсутствовали носители всех девяти
# кейсов, rc=1 был гарантирован независимо от проверяемой оси, и падение
# не атрибутировалось ей (F-1 `DOCS_CHECK_33`).
TESTS=src/test/java/com/example/tradingbot/integration/sourceapi/okx/
mkdir -p "$t/iso"; cp -r "$TESTS"* "$t/iso/"
find "$t/iso" -name '*.java' -exec sed -i 's/AG6\.1\b/AG6_RET/g' {} +

# ось 3, «идентификатор, а не регулярка»: вместо AG6.1 — чужой кейс AG6X1
printf 'class ProbeX { void x() { observe("AG6X1"); } }\n' > "$t/iso/ProbeX.java"
bash tools/preconditions-check.sh "$P" "$R" "$t/iso/"
                                                      # → «ДЕФЕКТ: кейс AG6.1 … исполнимой строки … нет», rc=1;
                                                      # прочие кейсы «СОШЛИСЬ» — падение атрибутировано оси.
                                                      # Прежняя форма (греп кейса регуляркой): AG6X1
                                                      # засчитывался — точка совпадала с «X»
rm "$t/iso/ProbeX.java"

# ось 3, правая граница идентификатора: чужой кейс с буквенным хвостом
printf 'class ProbeX { void x() { observe("AG6.1b"); } }\n' > "$t/iso/ProbeX.java"
bash tools/preconditions-check.sh "$P" "$R" "$t/iso/"
                                                      # → тот же «ДЕФЕКТ: кейс AG6.1 …», rc=1
                                                      # прежняя (асимметричная) правая граница `([^0-9]|$)`
                                                      # засчитывала «AG6.1b» носителем «AG6.1»
rm "$t/iso/ProbeX.java"

# ось 3, комментарная слепота — обе письменные формы комментария:
# однострочный javadoc и trailing-`//`
printf 'class ProbeX {\n/** кейс AG6.1 покрыт */\nvoid x() { }\n}\n' > "$t/iso/ProbeX.java"
bash tools/preconditions-check.sh "$P" "$R" "$t/iso/"  # → «ДЕФЕКТ: кейс AG6.1 …», rc=1
printf 'class ProbeX {\nvoid x() { int a = 1; } // кейс AG6.1 покрыт\n}\n' > "$t/iso/ProbeX.java"
bash tools/preconditions-check.sh "$P" "$R" "$t/iso/"  # → тот же ДЕФЕКТ, rc=1
                                                      # прежний фильтр (отбрасывание строк, НАЧИНАЮЩИХСЯ с
                                                      # маркера) обе формы засчитывал носителем: «СОШЛОСЬ», rc=0
# контроль изоляции: исполнимая строка возвращает зелёный
printf 'class ProbeX {\n/** кейс AG6.1 */\nvoid ag61() { observe("AG6.1"); }\n}\n' > "$t/iso/ProbeX.java"
bash tools/preconditions-check.sh "$P" "$R" "$t/iso/"  # → «СОШЛОСЬ», rc=0

# ось 4, непредусмотренная форма хвоста: маркер «п.» потерян
sed 's/(предусловие п\. 5)/(предусловие 5)/' "$P" > "$t/plan.md"
bash tools/preconditions-check.sh "$t/plan.md"          # → «ДЕФЕКТ: гейт-заголовок кейса MG7.5
                                                        #    не дал ни одной пары…», rc=1

# ось 4, каноническая форма без пробела: та же потеря от опечатки
sed 's/(предусловие п\. 5)/(п.5)/' "$P" > "$t/plan.md"
bash tools/preconditions-check.sh "$t/plan.md"          # → тот же ДЕФЕКТ, rc=1
                                                        # прежняя редакция на обеих: «СОШЛОСЬ», rc=0

# контракт формы: переименованная колонка шапки
sed 's/| Кейс |/| Case |/' "$R" > "$t/reg.md"
bash tools/preconditions-check.sh "$P" "$t/reg.md"    # → «ОШИБКА: шапка … не разобрана», rc=2

bash tools/preconditions-check.sh                     # действительное состояние → «СОШЛОСЬ», rc=0
```

Историческая редакция, породившая F1 `DOCS_CHECK_27`, адресуется
**неподвижно** — редакция `GAPS_CLOSE_26`, `git show d132a37:<файл>`
(относительный адрес `git show HEAD` протух в момент коммита — F3
`DOCS_CHECK_28`). Нынешний скрипт на ней падает громко с `rc=2`: та
редакция реестра ещё не несла колонку «Носитель прогона». Канон
доказательства — батарея проб выше: она не зависит от истории git.

Расхождение слотов означает одно из двух: пара «пункт × кейс» помечена
гейтящей в плане и не заведена в реестр (гейт не сработает) либо названа
в реестре и снята с гейта в плане (гейт держит шаг без основания).
Правится **вместе** — план и реестр несут одно множество пар, а не два.

### Колонка «Носитель прогона» — почему она отдельная

Колонка «Статус наблюдения» отвечает только на вопрос «факт добыт?».
Вопрос «а есть ли чем добывать?» она не выражает вовсе, и потому
одиннадцать «⏳ не собрано» читались как «осталось нажать кнопку», тогда
как прогонять нечего: код-тест и запрос коллекции есть у **одного** кейса
из десяти, и тот производит не то наблюдение (F2, F3 `DOCS_CHECK_27`).

Цена решения названа: колонка в одной таблице, связность нулевая,
проверяемость — ось 3 `tools/preconditions-check.sh`: клейм колонки
сверяется с фактикой (перечень кейсов берётся из этой же таблицы и
грепается по каталогу код-тестов; расхождение клейма с фактикой —
`ДЕФЕКТ`, а не справка). **Коллекция в ось не входит** и носителем
прогона не объявляется: аппрув-артефактом контура она не является и
исполнителем прогона тоже, поэтому клейм о ней мерил бы третью запись
того же знания (`.claude/decisions/collection-not-approval-artifact.md`).

Отвергнуто: расширять статус кейса в плане (правка конвенции по 17
заголовкам и шаблону `templates/docs/test-plan.md` при той же
разрешающей способности) и «читать состояние носителя из плана» — план
его тоже не различает: `⏳ PENDING` стоит и у кейса с код-тестом, и без.

### Исход «не наступило» — запись, не закрытие

Механику исходов держит `.claude/tests/source-api/okx/plan.md` §«Исходы
содержательного кейса — их три, и третий гейт не закрывает». **Закрывающей
силы у третьего исхода больше нет** — решение держателя 2026-08-30
(§«Правило гейта» выше). Исход остаётся **записью наблюдения**: кейс
прогнан, событие за прогон не наступило; слот при этом остаётся
`PENDING` с этой причиной в колонке статуса.

Что изменилось и почему это важно: п. 10 (`AG1.8`, след автоделевериджа)
был назван первым кандидатом на закрытие этим исходом. Прогон 2026-08-30
кейс исполнил, эпизода ADL не наблюдал — и по новой редакции слот
**остался открытым**. Так и записано в таблице.

**Заголовок плана приведён в соответствие** (`GAPS_CLOSE_29`): он читается
«…и третий гейт **не** закрывает». Прежняя редакция второй половиной
противоречила этому дому, и противоречие стояло в **имени** пассажа — то
есть воспроизводилось каждой ссылкой на него.

## Что в реестр НЕ входит

- **Пункт 8 — калибратор допуска сверки** — гейтом быть перестал решением
  держателя (`GAPS_CLOSE_19`): в `CODE` выходим с некалиброванным
  допуском, он калибруется до боевой торговли
  (`.claude/work/backlog.md` §«Закрытие фазы 1 — снятие разведочного
  режима допуска сверки»). Номер назван явно: пока он молчал, ссылка
  «предусловие `CODE` п. 8» жила в рабочем файле как действующая
  (F8 `DOCS_CHECK_27`).
- **Пункт 13 — цена доведения шага до `CODE` при незакрытых разрывах** —
  решён держателем 2026-08-23 и гейтом не является: клеймы полноты шага 7
  непроверяемы до реализации системного слоя, и «чисто» на `DOCS_CHECK`
  означает «доки не противоречат друг другу», а не «спецификация
  реализуема». Отчёт прогона обязан различать эти два утверждения явно.
- **Номера 3, 4, 11, 12** — снятые редакции реестра до его переноса; их
  предмет ни в одном живом носителе гейтящим не значится. Номера не
  переиспользуются и не сдвигаются (`.claude/rules/closed-work-transfer.md`
  §«Ссылочная целостность»); действующее множество гейтящих пунктов
  задаётся **таблицей выше** и сверяется с планом командой §«Сверка
  реестра с планом».
- **Пункт 10 — след автоделевериджа (`AG1.8`)** — гейтящий статус снят
  решением держателя 2026-08-30
  (`.claude/decisions/unorderable-fact-substitutes.md`). Основание:
  эпизод ADL инициирует биржа, на demo он не заказуем, и по клаузе
  «исход „не наступило“ гейт не закрывает» слот держал бы `CODE`
  **бессрочно** — то есть гейт перестал бы мерить готовность и стал бы
  мерить везение. Дефицит остаётся `грунт`: факта нет, и это записано;
  снято только его право держать `CODE`. Кейс закрывается **тремя
  заменителями**, и каждый имеет носитель:
  1. **синтетическая фикстура по документированному payload'у** — три
     примера `docs/spec/pnl-reconciliation.json` («ФИКСТУРА п. 10 (ADL)»)
     на форме, взятой из собственного справочника источника
     (`docs/integrations/okx/contracts/account-bills.md` §«Семейство
     принудительного закрытия — строки справочника», прогон 2026-08-30);
  2. **защитная обработка неизвестного источника сокращения** — третий из
     этих примеров: односторонний след штрафа даёт `mismatched`, а не
     тихое схождение; плюс отказ от типового умолчания у гетерогенного
     типа `5` (там же), плюс `UNDETERMINED > NORMAL_EXIT` на резолве
     исхода (`docs/models/mapping/PositionCloseResult.md`);
  3. **пункт живого наблюдения на запуске** — `.claude/work/backlog.md`
     §«Живое наблюдение на запуске».

  Кейс `AG1.8` из плана **не удаляется** и остаётся прогоняемым: если
  эпизод наступит, наблюдение заместит фикстуру.

- **`M15.7` (семантика `actualPx`)** — гейтом не размечен ни планом, ни
  реестром.
- **Знаки трёх операндов записи и bill'а штрафа** (`AG1.7`, пункт 7
  §«Грунт `integrator`» backlog) — гейта не образуют: знак выясняется
  прогоном и правится по факту. Кейс общий с пп. 7 и 14, предусловие —
  нет.

## Что добыто и что осталось

Состояние на **2026-08-31**: среда поднята, контур прогнан автономно,
носители прогона написаны. **Сколько слотов открыто — читается колонкой
статуса таблицы выше и прогоном `bash tools/preconditions-check.sh`,
здесь это число не хранится.** У каждого открытого слота причина названа
фактом, а не догадкой; разбор — ниже. Слот п. 10 не закрыт, а **выведен
из-под гейта** (§«Что в реестр НЕ входит»).

### Среда контура — проверено прогоном, не выведено

**Это дом фактов среды.** Команда сборки, лаунчер Python и пути JDK/Maven
живут здесь одной редакцией; прочие носители ссылаются, а не
пересказывают. Правило заведено потому, что расходящихся редакций было
три на четыре носителя, и две из них предписывали команды, которых в
среде **того момента** не существовало, — падение громкое, но дефект
предписания остаётся дефектом.

**Пример нарочно не называется здесь именами команд.** Всякое имя,
приведённое как «несуществующее», живёт ровно до следующей смены среды и
затем противоречит таблице ниже: так и вышло — эта строка объявляла
`py -3` несуществующим, тогда как таблица того же файла называет его
единственным рабочим лаунчером. Действующие имена — **только** в таблице
и в §«Воспроизводимые команды»; проза о них не высказывается.

**Среда сменилась 2026-08-31:** хост — Windows 11 (`MINGW64_NT-10.0-26200`;
IDEA 2026.1, Git Bash / PowerShell), Vault пересоздан в не-dev режиме,
демо-ключ OKX сменился. Таблица и команды ниже — редакция, проверенная
прогоном из новой среды; строки прежней среды сняты этой же правкой.

| Что нужно контуру | Состояние | Чем проверено |
|---|---|---|
| JDK 25 | **есть** — `~/.jdks/corretto-25.0.3`, Corretto 25.0.3.9.1; прежнего пути `~/.jdks/corretto-25` больше нет | `~/.jdks/corretto-25.0.3/bin/java -version` |
| Maven | **есть** — встроенный в IDEA: `C:/Program Files/JetBrains/IntelliJ IDEA 2026.1/plugins/maven/lib/maven3`, 3.9.11; на `PATH` нет, `mvnw` в репозитории по-прежнему нет, кэш `~/.m2/wrapper/dists` прежней записи исчез; `~/.m2/repository` жив | `"$MAVEN_HOME/bin/mvn" -v` → 3.9.11 на JDK 25.0.3 |
| Docker + контур `docker-compose.yml` | **есть** — три контейнера подняты | `docker ps` → `vibetradingbotv5-{vault,postgres,postgres-test}` |
| Vault `127.0.0.1:8200` | **не-dev режим**: file storage, shamir 1/1 (`vault.hcl`); listener в контейнере — `18200`, наружу проброшен `8200`; после каждого рестарта контейнера **запечатан** — нужен `vault operator unseal` (ключ у держателя); сейчас распечатан, `secret/tradingbot/okx-test` и `secret/tradingbot/postgres-test` заведены и читаются | `vault status` → `sealed:false`, `storage:file`, shamir 1/1; `vault kv get secret/tradingbot/okx-test` токеном CLI контейнера (CLI внутри контейнера требует `VAULT_ADDR=http://127.0.0.1:18200`) |
| Postgres-test `5441` | **слушает** (БД `tradingbot_test`); профиль `test` из IDEA поднимается: Vault-импорт отработал, Flyway накатил 10 миграций на чистую базу | запуск приложения профиля `test` из IDEA, 2026-08-31 |
| Сеть до биржи | **есть** | `GET /api/v5/public/time` → `HTTP 200`, `code=0` |
| Лаунчер Python | **`py -3`** → Python 3.11.9; `python3` и `python` в Git Bash — заглушки WindowsApps: печатают `Python` без версии, код не исполняют. Ровно инверсия прежней записи этой строки | `py -3 -c "print(1)"` → `1`; `python3 -c "print(1)"` → исполнения нет |
| Сборка дерева | `JAVA_HOME=~/.jdks/corretto-25.0.3 "$MAVEN_HOME/bin/mvn" -o test-compile` (`MAVEN_HOME` — maven3 из IDEA, см. строку Maven); `./mvnw` и `mvn` на `PATH` **отсутствуют** | прогон команды 2026-08-31 → `BUILD SUCCESS` |
| Прогон unit-тестов | **есть** — весь unit-набор одной командой: `mvn -o test -DexcludedGroups=source-api-live` → **85 тестов, 0 падений, `BUILD SUCCESS`** (прогон 2026-09-01). Точечно — `mvn -o test -Dtest='<Класс>[,<Класс>]' -Dsurefire.failIfNoSpecifiedTests=false`. Разделитель классов — **запятая**; `+` surefire не понимает и отвечает «No tests matching pattern». Офлайн-прогон стал возможен 2026-09-01: `surefire-junit-platform:3.5.4` отсутствовал в `~/.m2` и был докачан **одним онлайн-прогоном** (сеть до `repo.maven.apache.org` есть, проверено `curl` → 200). До этого `-o test` падал «could not be resolved … in offline mode» — то есть офлайн не запускался ни один unit-тест, включая уже написанные | прогон `DealTrancheStateMachineTest` и `DealTerminalGateTest` 2026-09-01: сначала онлайн (4 теста, 2 падения — дефекты данных пробы), после починки офлайн → `BUILD SUCCESS`, 9 тестов |
| Демо-контур кредов | **доказан отрицанием** — перепроверено из новой среды 2026-08-31; ключ сменился: `label=TestKeyW` (был `BotV5-test`) | тот же ключ **без** заголовка `x-simulated-trading` → `HTTP 401`, `50101 APIKey does not match current environment`; с заголовком → `code=0` на `account/balance` и `account/config` (`label=TestKeyW`, `acctLv=2`, `posMode=net_mode`) |

**Проверка демо-контура — падающая проба, а не флаг конфигурации.**
Клейм «ключ демо-контура» доказывается тем, что боевой контур этот ключ
**отвергает**, а не тем, что профиль выставляет `okx.simulated=1`.
Второе — заявление о нашей настройке, первое — свойство самого ключа.

### Воспроизводимые команды

```bash
export JAVA_HOME="$HOME/.jdks/corretto-25.0.3"
export MAVEN_HOME="/c/Program Files/JetBrains/IntelliJ IDEA 2026.1/plugins/maven/lib/maven3"
export PATH="$JAVA_HOME/bin:$MAVEN_HOME/bin:$PATH"
# ВНИМАНИЕ: MAVEN_HOME здесь — в POSIX-форме (/c/...), и это обязательно.
# Драйв-буквенная форма («C:/Program Files/…») в PATH не работает: разделитель
# PATH в Git Bash — двоеточие, поэтому «C:» отрезается как отдельный элемент и
# `mvn` не находится («mvn: command not found»). Прежняя редакция этого блока
# несла драйв-буквенную форму и как записана не исполнялась; для `-v` и прямого
# вызова «$MAVEN_HOME/bin/mvn» она годилась — потому дефект и не был замечен.
source .env.vault.test.local                 # VAULT_TOKEN профиля test — действует, см. §«Что среда всё ещё не даёт» (ограничение — область токена, не срок)
docker compose up -d vault postgres-test     # если контур опущен
docker exec -e VAULT_ADDR=http://127.0.0.1:18200 vibetradingbotv5-vault \
  vault operator unseal                      # после каждого рестарта контейнера Vault; unseal-ключ у держателя

# джобы гасятся, чтобы состояние счёта за прогон меняли только кейсы
export CANDLE_JOB_ENABLED=false INDICATOR_JOB_ENABLED=false \
       MARKET_STRUCTURE_JOB_ENABLED=false INSTRUMENT_RULES_SYNC_ENABLED=false \
       ENTRY_SCANNER_ENABLED=false DEAL_ORCHESTRATOR_ENABLED=false

mvn -o test -Dgroups=source-api-live -Dtest=Ag1DealFixtureLiveTest    # пп. 1, 6, 9, 14, 15; п. 2 — перепрогоном (перечень пишется персистентно)
mvn -o test -Dgroups=source-api-live -Dtest=Ag6BillSubtypesLiveTest   # пп. 2, 16
mvn -o test -Dgroups=source-api-live -Dtest=Mg7IndexCandlesLiveTest   # п. 5
mvn -o test -Dgroups=source-api-live -Dtest=M17CancelOrderLiveTest    # п. 17 (все три цепочки наблюдены — §«Что добыто»)
mvn -o test -Dgroups=source-api-live -Dtest=Ag1FundingHorizonLiveTest # п. 7 — сам выбирает инструмент по интервалу; вне окна пишет исход пропуска

bash tools/preconditions-check.sh            # сверка реестра с планом и с фактикой носителей
```

### Что добыто

**П. 1 и 15 — агрегация и адресация записи (`AG1.5`, `AG1.9`).** Источник
**агрегирует** слайсы частичного закрытия в **одну** финализированную
запись эпизода: у эпизода «открыть 0.02 → закрыть 0.01 → закрыть остаток»
запись одна, `closeTotalPos=0.02` (полный закрытый объём, не последний
слайс), `realizedPnl` кумулятивен и **точно** равен
`pnl + fee + fundingFee + liqPenalty`. Сверка с движениями счёта сходится
без остатка: Σ`pnl` и Σ`fee` торговых bills окна равны суммам по записям.

**Посылка «один эпизод ↔ один `posId`» ложна.** Переоткрытая позиция по
тому же инструменту получает **тот же** `posId`: два эпизода в одном окне
дали две записи с одинаковым `posId` и разными `cTime`; запрос
`positions-history` с фильтром `posId` вернул **все** записи этого
инструмента, а не одну. Наблюдено дважды независимо (ручной прогон и
код-тест). Адресуемая единица — **пара (`posId`, `cTime`)**, а не `posId`.
Что это ломает — названо в плане §AG1.9 «Что решает вторая половина
кейса» (B11 `DOCS_CHECK_20`): дискриминатор смены эпизода
(`docs/lifecycles/Position.md`) и ключ `uk_position_deal_external`.
Правка обоих — продуктовая работа закрытия, не реестра.

**П. 6 — оси адресации (`AG1.6`).** `positions-history` принимает `instId`
**без** `instType`; окно по `uTime` (`before`/`after`) отбирает ровно
записи окна сделки; каждая запись несёт `cTime`, `direction`, `openAvgPx`
— операнды материализации `Position` на create-тропе. Порядок записей в
окне — по убыванию `uTime`.

**П. 2 и 16 — справочник типов (`AG6.1`, `AG6.2`) — закрыты: п. 16 —
2026-09-01, п. 2 — 2026-09-02 (перечень кандидатов `AG6.2` добыт первым
прогоном после оживления среды; хроника ниже — путь к закрытию).**
Прогон 2026-08-30 наблюдал 32 верхних типа и 179 пар `type`/`subType`, но
перечень ушёл **в лог**: `observeValue` печатает строку в консоль и ничего
не сохраняет. Носителем наблюдаемости лог не является — `docs/concept.md`
прямо говорит, что сослаться на него при разборе нельзя, — поэтому слоты
закрыты не были и возвращены в `PENDING`. Кейс правлен: перечень пишется в
`.claude/tests/source-api/okx/observations/AG6_1.md` и коммитится вместе с
реестром. **Перепрогон закрывает слот п. 2 × `AG6.1` целиком** (его
предмет — сам перечень) и **только грунтовую половину слота п. 16**: там
предмет — ОТОБРАЖЕНИЕ пар в экономические категории, а оно строится
семантической разметкой перечня, а не прогоном (двухшаговый контракт —
§«Правило гейта»).

Что из прогона **сохранилось** и остаётся верным: в окне сделки
фактически появились **две** пары — `2/1` (Trade / Buy) и `2/2` (Trade /
Sell); **маржинальных движений `type=6` в окне нет** — при isolated-марже
перевод маржи идёт полем `posBalChg` **внутри** торгового bill'а,
отдельной записи не порождая.

**Клейм «177 пар справочника в экономику сделки не входят» снят.**
Наблюдение его не производило: оно дало мощность справочника и две пары
окна, а принадлежность остальных 177 экономике сделки не проверялась —
и как минимум три из них в неё входят: финансирование `8/173`, `8/174` и
штраф `5/116` (правые операнды 3-й и 4-й пар сверки,
`docs/rules/pnl-reconciliation.md`). Сохранённый перечень — **кандидаты**;
состав списка исключений строится **семантической разметкой справочника**
по нему — шаг «кандидаты → состав», писатель — хэнд-офф `integrator`,
тем же ходом, что отображение категорий п. 16 (носитель хэнд-оффа —
`.claude/work/backlog.md`, пункт списка исключений).

**П. 9 — валюта комиссии (`AG3.4`).** У всех записей окна `ccy="USDT"` —
инвариант settle-ccy держится. Комиссия приходит **полем `fee` внутри
торгового bill'а**, отдельной fee-записи нет. Движений вне settle-ccy в
окне не наблюдалось, поэтому вторая проверка (состав `realizedPnl` при
cross-ccy-издержке) этой фикстурой **не разрешена** — она и не гейтит.

**П. 14 — форма пустого значения (`AG1.7`).** Ожидание подтверждено:
несобытийные `fundingFee` и `liqPenalty` приходят строковым **`"0"`**,
ключ присутствует. Контраст наблюдён и записан: неприменимые к записи
поля (`settledPnl`, `nonSettleAvgPx`, `triggerPx`) приходят **пустой
строкой** — то есть форма пустого значения у источника **различается по
полю**, и конвенция «пусто = 0» верна для числовых слагаемых, а не для
всех полей записи.

**П. 5 — носитель курса cross-ccy (`MG7.5`).** Секундный бар у пары
котировки **есть**: `index-candles(ETH-USDT, bar=1s)` отдаёт данные,
строка — `[ts, o, h, l, c, confirm]`, close по индексу 4. Одним запросом
берётся **300** строк, то есть диапазон читается разом, а не построчно.
Глубина: свежий `index-candles` окно **в прошлом не обслуживает вовсе**
(`n=0` на любом `after` в прошлом) — носитель глубины
`history-index-candles`; `bar=1s` достаёт назад до **~150 дней** (на 180
уже пусто), `bar=1m` — не менее **365 дней**. Отсюда правило деградации:
1s в пределах ~5 месяцев, дальше — 1m.

**П. 17 — судьба встроенной защиты (`M17.5`), все три цепочки.** У живого
родителя защита представлена элементом `attachAlgoOrds[0]` с
`attachAlgoClOrdId` и присвоенным биржей `attachAlgoId`; самостоятельной
записью в `orders-algo-pending` она **не видна**. После отмены родителя
**без налива** `attachAlgoOrds[0]` сохраняется в теле отменённого
родителя с прежними значениями и пустыми `failCode`/`failReason`.

**Несущее: при непустом наливе защита ПЕРЕЖИВАЕТ терминал родителя.** Она
материализуется **самостоятельной живой** conditional-заявкой
(`state=live`, `reduceOnly=true`, `side=sell`) и в
`orders-algo-history(canceled)` не появляется. Наблюдено обеими
цепочками: C17b — наша отмена (`cancelSource=1`, `accFillSz=0.01`), C17c
— снятие остатка биржей (`ordType=ioc`, `cancelSource=14`,
`accFillSz=2.03`). Четыре следствия:

1. **От инициатора терминала судьба защиты не зависит** — исходы C17b и
   C17c совпали; расхождения, которого опасался кейс, нет.
2. **`sz` живой защиты равен `accFillSz` родителя**, не исходному `sz`:
   покрытие ровно на налитый объём.
3. **`algoId` живой записи не равен `attachAlgoId` родителя.** Матч по
   `attachAlgoId` не сойдётся; единственный сходящийся операнд —
   клиентский `algoClOrdId == attachAlgoClOrdId`.
4. **Обратной ссылки на родителя нет:** `ordId=""`,
   `linkedOrd.ordId=""`.

Что это ломает: исход `CANCEL_BY_PARENT` (`docs/lifecycles/Order.md`)
помечает **живую** защиту снятой, покрытие недосчитывается, и гейт
покрытия уводит штатную сделку в ступень 2 по ложному факту. Правка
носителей — продуктовая работа закрытия.

**Фикстурный потолок — поднят** (решение держателя 2026-08-30): прежние
200 USDT давали 0.81 контракта, меньше любого наблюдённого верхнего
уровня книги, и частичный налив был недостижим **по счёту**. Действующее
значение — 2500 USDT (≈10 контрактов), дом константы —
`M17CancelOrderLiveTest.FIXTURE_RISK_CEILING_USDT`; величина помечена
фикстурной, живёт только в `src/test` и продуктовыми путями не читается.

### Что осталось открытым

**Заполненность `slTriggerPxType` в эхе встроенной защиты — ДОБЫТА
(замер 2026-09-02, после постановки Т5).** Вход со встроенной защитой
поставлен ровно формой Т5-маппинга (`attachAlgoClOrdId`, `slTriggerPx`,
`slTriggerPxType="mark"`, `slOrdPx="-1"`, без `sz`); родитель налит
мгновенно. Эхо **обеих** форм несёт поле **непустым и равным
отправленному**:

- **элемент `attachAlgoOrds[0]` родителя** (`GET /trade/order`,
  `state=filled`): `slTriggerPxType="mark"`, `attachAlgoId` присвоен,
  `failCode` пуст, `sz` пуст (не отправляли);
- **самостоятельная запись** (`orders-algo-pending`, `conditional`):
  `slTriggerPxType="mark"`, `state=live`, `side=sell`,
  `reduceOnly=true`, `sz=0.01` — равен наливу родителя.

Следствие: сверка объявленной базы `MARK`
(`docs/models/domain/core/Order.md`) получает операнд — источник не
молчит, ограничение «сверка молчит, где источник промолчал» остаётся
правилом пустоты, но фактическая ветвь наблюдена непустой. Попутно
подтверждены три факта `M17.5` на свежем эпизоде: матч только по
клиентскому id, `algoId` записи ≠ `attachAlgoId` родителя, размер
записи равен наливу. Уборка замера: `close-position(autoCxl)` → счёт
flat, algo-pending и orders-pending пусты.

**Класс дефицита был исправлен на `грунт` решением держателя
(2026-09-01)** — поле требовало наблюдения, не санкции; блокеров было
два (Т5 наш, отказ демо-контура средовой), и оба сняты 2026-09-02: Т5
поставлен (постановка шлёт `attachAlgoOrds`), среда ожила.

**П. 2 × `AG6.2` (типы вне экономики сделки) — закрыт целиком
(2026-09-02), обе половины предъявлены.** Первый прогон фикстуры после
оживления среды прошёл все восемь кейсов зелёными; перечень 177
кандидатов (пары справочника вне окна сделки) записан персистентно в
`observations/AG6_2.md`. Ответная половина была закрыта раньше
(2026-09-01) — состав списка исключений в
`docs/integrations/okx/rules/cash-flow-categories.md` §«Типы вне
экономики сделки».

**Уборка кейса на ветви отказа — по-прежнему руками.** `Verify.end`
фикстуры стои́т **ниже** упавшего ожидания, поэтому на ветви отказа
состояние не восстанавливается — тот же класс, что назван прочей
находкой П2 линзы F для `Ag1FundingHorizonLiveTest`; класс не починен и
при живой среде: упавшая фикстура оставляет хвост, снимаемый вручную.
Случай предъявлен первым же прогоном 2026-09-02: `Snapshot.start` упал о
позиции, оставшиеся от разбора очереди расчёта, и уборка шла руками
(`close-position` по трём инструментам) до перепрогона.

**П. 16 × `AG6.1` — закрыт целиком (2026-09-01), обе половины
предъявлены.** Перечень 179 пар добыт (`observations/AG6_1.md`);
отображение пар в экономические категории зафиксировано —
`docs/integrations/okx/rules/cash-flow-categories.md` §«Отображение пар
источника в категории». Прежняя редакция этого абзаца объявляла ответную
половину открытой и **противоречила собственной строке реестра** (п. 16,
`✅ собрано`): абзац писался до постройки артефакта и не был сведён с
таблицей тем же ходом.

**П. 7 (`AG1.7`, горизонт `fundingFee`) — ЗАКРЫТ перепрогоном
2026-09-03 (граница 00:00 UTC).** Правленый кейс прошёл целиком,
`BUILD SUCCESS`, запись — `observations/AG1_7.md`: запись закрытия
эпизода, пережившего расчёт, несёт ненулевой `fundingFee` **сразу после
закрытия** (−0.0101…, расход; прогон 2026-09-02 наблюдал доход +0.0103…
— оба знака предъявлены), сверка Σ(`balChg`+`posBalChg`) == `fundingFee`
сошлась точно, композиция `realizedPnl` сходится, Verify.end чист.
Хроника пути к закрытию — ниже.

**Замер 2026-09-01 добавил ко времени вторую ось, и она была слепой.**
Инструмент фикстуры выбирался **только** по интервалу расчёта, а
торгуемость пары аккаунтом не проверялась ничем: `ACT-USDT-SWAP` отвечает
`51155` («local compliance restrictions»), то есть кейс не собрал бы
фикстуру и в идеальном окне. Инструмент заменён на `AVAX-USDT-SWAP` —
тот же четырёхчасовой интервал, торгуемость проверена пробой, не
создающей заявки (§«Что среда всё ещё не даёт»). **Окно прогона от этого
не расширилось:** ближайшая граница расчёта у всех четырёхчасовых
инструментов общая (00/04/08/12/16/20 UTC), и кейс исполняется только
тогда, когда прогон стартует за 6-50 минут до неё. Это свойство
**прогона**, а не источника.

**Прогон 2026-09-02 в окне границы 20:00 UTC добыл предмет пункта и
принёс новый факт источника; слот закрыл перепрогон правленого
кейса.** Кейс исполнился целиком: позиция `AVAX-USDT-SWAP` (69.6
контракта) открыта за 5 мин до границы, пережила расчёт, закрыта двумя
слайсами; запись закрытия эпизода несёт **ненулевой
`fundingFee = +0.0103373078764316` сразу после закрытия**, композиция
`realizedPnl = pnl + fee + fundingFee` сходится. Упала только сверка
шага 8 — и её расхождение оказалось **фактом источника, а не сбоя**: у
funding-bill'а окна `balChg = 0`, а сумма расчёта лежит в
**`posBalChg`** — равном `fundingFee` записи до последнего знака. **На
isolated-марже расчёт финансирования ложится в маржу позиции, не в
баланс** — та же механика, что у перевода маржи торговых bills
(наблюдение `AG6.1`). Кейс суммировал только `balChg` и разошёлся ровно
на весь расчёт; правлен на Σ(`balChg`+`posBalChg`), исход «сверка не
сошлась» записан персистентно (третья ветвь, `observations/AG1_7.md`).
Перепрогон — раннером в окне границы 00:00 UTC.

Следствие для потребителей: поле `posBalChg` переведено в used
(`docs/models/integrations/okx/AccountBillOkxResponse.md`), строка
разбивки несёт его сырым фактом (`positionBalanceChange`); как оно
входит в пары сверки — решает дом сверки.

**Предмет п. 7 подкреплён и побочным наблюдением уборки 2026-09-02, но
слот им не закрыт.** Три позиции, открытые разбором очереди расчёта и
пережившие границу 16:00 UTC, при ручном закрытии дали записи
`positions-history` с ненулевым `fundingFee` (ETH `+0.0158`, BTC
`+0.0158`, SOL `−0.000199`). Слот закрывается **прогоном своего кейса**
(§«Правило гейта») — наблюдением `Ag1FundingHorizonLiveTest` с записью в
`observations/AG1_7.md`, не побочным фактом.

**Персистентность обеих ветвей — проверена, а не объявлена** (прогон
2026-09-01 18:06 UTC, вне окна): кейс исполнен, `Skipped: 1`,
`BUILD SUCCESS`, запись легла в
`.claude/tests/source-api/okx/observations/AG1_7.md` (исход
`OBSERVED_ABSENT`, инструмент `AVAX-USDT-SWAP`, интервал 4 ч, до границы
113 мин). До этого прогона файла наблюдений у кейса **не существовало**, и
клейм реестра «пишет исход на обеих ветвях» держался только на чтении
кода.

**Клейм «ближайший расчёт более чем через десять часов» был ложен, и
почему — важно.** Он выведен из расписания **восьмичасового** интервала
(00:00 / 08:00 / 16:00 UTC), а не из фактов источника: интервал у
инструмента определяется **разницей `fundingTime` и `nextFundingTime`**
собственного ответа — типично восемь часов, но биржа сжимает до 6/4/2/1 ч
(`docs/integrations/okx/contracts/funding-rate.md` — доковый дом, он
говорил верно). Замер публичных эндпоинтов 2026-08-30: на demo из 142
живых USDT-свопов **89 считают фандинг каждые четыре часа**, 53 — каждые
восемь; на production 200 из 443 — четырёхчасовые. То есть слот
достижим внутри сессии на любом четырёхчасовом инструменте, а держался
открытым по выводу, верному только для восьмичасового `ETH-USDT-SWAP`.

Что нужно: удержать позицию через **ближайшую границу интервала того
инструмента, который взят фикстурой** (границы читаются из его же ответа,
не из расписания). Кейс заведён — `Ag1FundingHorizonLiveTest`: он сам
читает интервал разницей `fundingTime`/`nextFundingTime` и **пишет исход
в персистентный носитель** (`observations/AG1_7.md`) **на обеих ветвях** —
и когда окно подошло, и когда прогон пропущен assumption'ом. Вторая ветвь
несущая: без неё «кейс не гонялся» и «гонялся, окно не подошло» в данных
не различаются, а это и есть тот дефект, из-за которого два слота
вернулись в `PENDING`. Альтернатива — перепрогон
`ag1_7_numericFieldSemantics`, —
кейс уже несёт ветвь сверки Σ`balChg` funding-bills с Σ`fundingFee`
записей. Отдельная оговорка: вопрос сформулирован как «за всю жизнь
`posId`», а `posId` переиспользуется — при перепрогоне вопрос читается
как «за жизнь **эпизода**».

**П. 10 (`AG1.8`, след автоделевериджа) — наблюдения нет и не будет по
расписанию, гейта нет.** Кейс прогнан: bills типов 5 (Forced liquidation)
и 9 (ADL) в окне сделки и за весь доступный семидневный период — ноль.
Эпизод ADL инициирует биржа, на demo заказать его нельзя. Слот выведен
из-под гейта решением держателя 2026-08-30 и закрывается заменителями —
состав, носители и цена в §«Что в реестр НЕ входит» и в
`.claude/decisions/unorderable-fact-substitutes.md`. **Слотом реестра п.
10 больше не является**, поэтому в счёт открытых не входит.

### Что среда всё ещё не даёт

- **`mvnw` в репозитории нет.** Прогон опирается на maven3, встроенный в
  IDEA (кэш wrapper'а `~/.m2/wrapper/dists` прежней записи исчез), и на
  ручной `PATH`. Состав работ
  — `.claude/work/backlog.md` §«Средовой дефицит автономного RUN тестов
  (контур source-api)».
- **Голый `mvn -o test` не означает «unit-прогон» — и это не поломка
  контура.** Прогон без фильтра даёт `Tests run: 296, Errors: 210`, все
  210 — `ApplicationContext failure`. Разобрано 2026-09-01: в `pom.xml`
  **нет** конфигурации surefire, то есть исключения группы по умолчанию
  тоже нет, — и вместе с unit-набором запускаются 211 живых кейсов
  `@Tag("source-api-live")`, чей `@SpringBootTest` поднимает профиль
  `test` → Vault → требует `VAULT_TOKEN`. Переменной в окружении нет,
  пока не выполнен `source .env.vault.test.local`, — отсюда отказ
  контекста у каждого.

  **Обе половины проверены прогоном**, и живой контур в отказе не
  участвует: `mvn -o test -DexcludedGroups=source-api-live` → **85
  тестов, 0 падений**; с сорснутым токеном `mvn -o test
  -Dgroups=source-api-live -Dtest=Pg7ServerTimeLiveTest` → **2 теста, 0
  падений**. То есть «210 ошибок контекста» — свойство **команды**, а не
  среды, и читать его как «live-контур не бутается» неверно.

  Чего среда не даёт: чтобы `mvn test` означал unit-прогон без флага.
  Состав правки (одна строка `excludedGroups` в surefire-конфигурации
  `pom.xml`) — `.claude/work/backlog.md` §«Средовой дефицит автономного
  RUN тестов (контур source-api)»; здесь она не делается, потому что
  меняет умолчание сборки для всех потребителей (IDEA, будущий CI), а
  состав этого захода — четыре доменных компонента.
- **Токен `.env.vault.test.local` действует, но тестовыми путями не
  ограничен.** Держатель обновил токен 2026-08-31; прогон 2026-09-01
  подтверждает: `auth/token/lookup-self` → `200`,
  `secret/data/tradingbot/{okx-test,postgres-test}` → `200`, приложение
  профиля `test` поднимается и живые кейсы контура исполняются. Чего среда
  не даёт: `policies=["root"]`, `ttl=0` — то есть токен корневой и
  бессрочный, а требование `.env.vault.test.local.example` — токен, узкий
  до тестовых путей. Prod-пути этого инстанса отвечают `404` (их в нём
  нет), поэтому проба «читает ли токен prod» на этом инстансе ничего не
  различает. Перевыпуск scoped-токена и политики — адресат шага 9
  «Безопасность».
- **Команды корпуса, записанные как `python3 tools/…`, напрямую не
  исполняются:** `python3` на этой машине — заглушка (строка «Лаунчер
  Python» таблицы выше). Исполнение — `py -3 tools/…`; носители команд
  не переписываются, дом факта — эта таблица.
- **СНЯТО 2026-09-02: отказ терминальной стадии demo-контура
  (2026-08-30 15:23 UTC → 2026-09-02 ~03:18 UTC).** Запись оставлена
  снятой, потому что на ней держались срок ожидания, запрет живых заявок
  и `PENDING` двух слотов; действующей истиной она больше не является.

  **Что было.** Заявка аккаунта принималась (`code=0`, `sCode=0`) и
  навсегда оставалась `state=live`; не завершался **всякий терминальный
  переход** — дом отказа была стадия расчёта (settlement), не матчинг:
  отмена принималась в очередь (повторная — `51410`) и не исполнялась.
  Тип заявки (`market`/`ioc`/`limit`), инструмент (ETH/BTC/SOL) и тропа
  отмены были сняты замерами как незначащие; отказ был подтверждён вне
  нашей стороны (веб-UI биржи, бот поддержки; **тикет 38426982 от
  2026-09-01**). Хвост дорос до шести `live`-заявок. Полный разбор того
  периода — `.claude/decisions/env-wait-deadline.md` и хроника шага
  (`.claude/work/progress/phase-1-step-7-chronicle.md`).

  **Развязка.** Содержательного ответа по тикету не было; очередь
  расчёта разобралась **сама** — 2026-09-02 между 03:18 и 05:16 UTC
  (`uTime` записей `orders-history`). Из шести замороженных заявок
  **четыре исполнились** (market 0.02 ETH, ioc 0.01 ETH/BTC/SOL — открыли
  три позиции), **две отменились** (limit 0.01 ETH — нашей очередью
  отмены, `cancelSource=1`; ioc 0.01 ETH — авто-отменой IOC,
  `cancelSource=14`). То есть «пять заявок отменились», наблюдённые
  держателем в UI, на стороне счёта были разбором очереди с исполнением
  части заявок, и хвост превратился из заявок в позиции.

  **Оживление подтверждено пробой из кода — фактом состояния, не
  принятием (2026-09-02 17:07 UTC).** Хвостов в `orders-pending` ноль;
  минимальная лимитная заявка вне рынка принята → отменена → терминальный
  `state=canceled` за ~2 с → из pending ушла. Исполнение подтверждено
  отдельно: рыночные заявки фикстуры и уборки исполняются до `filled` в
  секунды. Позиции-хвосты разбора закрыты руками тем же днём
  (`close-position` ETH/BTC/SOL, счёт flat, pending пуст).

  **Что снято вместе с отказом:** запрет живых заявок (стоял по просьбе
  поддержки и ради ненаращивания хвоста — оба довода исчерпаны); клеймы
  «фикстурные кейсы неисполнимы» и «хвост снять нечем»; срок ожидания
  2026-09-04 — решение `.claude/decisions/env-wait-deadline.md` исполнено
  оживлением до наступления срока, оси `срок`/`по-сроку` у обоих
  компонентов сняты из `.claude/work/code-gate-ledger.json` той же
  правкой (§Исход решения).
- **`ACT-USDT-SWAP` этим аккаунтом не торгуется** — `51155` («local
  compliance restrictions»). Пара была инструментом фикстуры кейса
  `AG1.7`, то есть кейс не собрал бы фикстуру и при работающем совпадении.
  Заменён на `AVAX-USDT-SWAP` (тот же четырёхчасовой интервал, торгуемость
  проверена). **Проба торгуемости заявки не создаёт:** лимитная заявка
  заведомо вне ценового лимита даёт `51006` у торгуемой пары и `51155` у
  рестриктнутой — проверка соответствия идёт раньше ценовой.
- **`order-precheck` этому аккаунту недоступен** — `51010` («under your
  current account mode»): эндпоинт требует режима portfolio-margin, а у
  контура `acctLv=2`. Пробой торгуемости он служить не может.
**Что среда теперь даёт (проверено прогоном 2026-09-01, `GAPS_CLOSE_33`
узел 9):** все команды проверочной машинерии корпуса исполняются из этой
среды голым запуском (Python-инструменты — через `py -3`, см. строку
«Лаунчер Python») и возвращают задокументированные коды: `spec-run.sh` 0,
`spec-mutation-check.sh` 0, `population-derive-check.py` 0,
`spec-pointer-check.py` 0, `anchor-check.py` 0, `retired-check.py` 0,
`spec-scope-check.py` 0, `preconditions-check.sh` 0,
`code-gate-check.py` — содержательный код по реестру диспозиции (не
средовой отказ), `mvn -o test-compile` — `BUILD SUCCESS` (строка
«Сборка дерева» таблицы выше). Починка (оснастка
`cygpath` для mktemp-путей, `LC_ALL=C.UTF-8` в derive-командах,
нормализация `\`-путей в SKIP-фильтрах и индексе, UTF-8-печать на
cp1251-консоли, маппинг незапуска JVM в код 2) — итог узла 9
`GAPS_CLOSE_33` (`.claude/decisions/measurement-repair-not-extension.md`).
Прежние клеймы этого раздела «`anchor-check.py` из этой среды не
прогоняем» и «`spec-run.sh` не переносим на Linux» сняты: первый —
починкой, второй был опровергнут чтением оснастки (она ветвится по
платформе — `cygpath`, разделитель, дефолтный JDK `$HOME/.jdks/corretto-25`
на Linux).


## Связи

- Кейсы и их прогон — `.claude/tests/source-api/okx/plan.md`.
- Процесс контура — `.claude/processes/source-api-testing.md`.
- Дом гейта `CODE` (оба условия) — `.claude/processes/roadmap-step-execution.md`
  §Гейты; встречный якорь гейта грунта —
  `.claude/skills/update-roadmap-progress.md` §«Гейт грунта».
- Исполнительный хвост шага — `.claude/work/backlog.md`.
