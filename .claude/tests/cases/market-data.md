# Тест-кейсы: `market-data`

## На какой вопрос отвечает этот файл

Какие тест-кейсы у сервиса `market-data`.

## Предмет

**Чёрный ящик единицы развёртывания `market-data`** — третий предмет
перечня под-шага 1 `CODE` шага 12 фазы 2
(`.claude/decisions/test-contour-design-pass.md`
§«Перечень предметов и порядок работы»). Новая ось формы у него одна —
**тик как вход**: у предмета пять проходов по расписанию, и почти всё, что
сервис производит, производят они, а не вызов поверхности. Всё остальное
наследует форму, проверенную на `auth` и `connector-okx`.

Сервис — **владелец рыночной картины**: каталог инструментов и их биржевые
правила, ряды (свечи, срезы стакана, тикер), производные (индикаторы,
структура) и фичи на момент решения
(`docs/architecture/services/market-data.md` §«Что он делает и зачем»).
Глубину ряда заказывает **потребитель** синхронной командой, а не
конфигурация. Границы — §«Чего не делает намеренно» того же дока: у границы
кейсов нет, и ниже они не заводятся.

Уровень у всех кейсов ниже один — `B`; мишень у предмета одна, поэтому
суффикса мишени метки не несут
(`.claude/skills/test-design.md` §«Форма — предмет → кейсы → таблица»).

## Чем достаются выходы

Контур — `.claude/decisions/test-contour-design-pass.md`, здесь не
пересказывается (`.claude/rules/policy-home.md`). Что из него следует
**для этого предмета**:

- **субстрат** — один контейнер базы (решение 2), и образ у него тот же,
  что в `deploy/base`: миграция `V1` заводит **гипертаблицы**, и на стоковом
  `postgres` они не поднимаются вовсе;
- **сосед подменяется WireMock** (решение 3): коннектор адресуется свойством
  `connector.base-url` через `@DynamicPropertySource` — тем же свойством,
  которым его подаёт манифест окружения. Это единственный сосед сервиса;
- **провайдер идентичности — JWKS-стаб** с локальной парой ключей (решение 4).
  У предмета он служит **обеим** сторонам: проверкой входящего токена
  (`issuer-uri`) и выдачей исходящей служебной идентичности
  (`client_credentials` на `token-uri` того же стаба);
- **Vault не поднимается**: ключей биржевого счёта у сервиса нет ни в каком
  виде — все его чтения площадки публичные
  (`docs/architecture/services/market-data.md` §«Какие вызовы делает и какие
  принимает»);
- **брокер не поднимается**: клиента брокера у сервиса нет, и отсутствие
  событий проверяется отсутствием его тропы, а не пустой темой (группа B10);
- **ассерт по базе** идёт через поверхность всюду, где поверхность есть
  (решение 7): объявленного читателя вне кода `market-data` у его таблиц нет
  — ряды и каталог читаются через его API. Где поверхности нет (статус
  единицы сбора, границы загруженного, курсор обхода, инварианты схемы),
  кейс называет ассерт прямым — и это его цена, а не умолчание.

## Тик — вход, и это новая ось формы

У предмета **пять** проходов по расписанию — синк каталога, загрузка свечей,
расчёт индикаторов, расчёт структуры, сбор невосполнимых срезов, — и у
каждого есть ручной асинхронный фасад
(`services/market-data/.../domain/jobs/`, `JobController`). Отсюда форма
кейса тика:

- **расписание в прогоне выключено** (`*.enabled=false` либо CRON, до
  которого прогон не доживает), а тик подаёт сам тест — **ручным фасадом**,
  потому что у этого сервиса фасад есть у всех пяти
  (`.claude/decisions/test-contour-design-pass.md` §«6. Время: тик — вход,
  возраст — данные, ожидание — `Awaitility`»);
- **фасад асинхронен**, поэтому след тика ждётся `Awaitility` с ограниченным
  таймаутом, а не `Thread.sleep`;
- **возраст данных ставится в данных**: свеча с нужным `openTimestamp`,
  значение индикатора с нужным `candleTimestamp`, срез с нужной биржевой
  меткой. Часы процесса не двигаются ни в одном кейсе;
- **срок свежести приезжает операндом чтения** и потому входом является
  **дважды**: и как параметр запроса, и как расстояние между меткой данных и
  моментом прогона.

## Ожидание берётся из дома, даже когда сегодня оно не исполнено

Часть кейсов ниже ожидает то, что дом объявляет, а дерево кода ещё не
несёт. Такие ожидания **не ослабляются под текущий факт**: кейс проверяет
контракт, а не сегодняшнее поведение, и его красный прогон и есть
предъявление названного долга. У каждого такого кейса в поле «чем
подтверждается» стои́т дом **и** секция бэклога, в которой долг припаркован:

| Долг | Где припаркован |
|---|---|
| единый error-DTO у отказов контейнера и отказа доступа | `.claude/work/backlog.md` §«Единый error-DTO у поверхностей соседних сервисов» |
| строка `access_denials` у сервиса со своей базой | `.claude/work/backlog.md` §«Таблица отказов доступа у сервисов со своей базой» |
| события каталога и тема `market-data.facts` | `.claude/work/backlog.md` §«Темы Kafka по производителям — заводятся шагами сервисов» |
| счётчик попыток докачки переживает рестарт | `.claude/work/backlog.md` §«Дефекты донора, воспроизведённые портом» |
| дедуп вставки — ключом, а не проверкой существования | `.claude/work/backlog.md` §«Дедуп проверкой существования перед вставкой — построенный класс, а не точка» |
| политика сжатия и хранения гипертаблиц | `.claude/work/backlog.md` §«Политики сжатия и хранения гипертаблиц — по пину образа TimescaleDB» |

## Поверхность предмета

| Операция | Метод и путь | Кто зовёт | Дом ожидания |
|---|---|---|---|
| требование свечей | `POST /api/v1/market-data/requirements/candles` | `trading-core`, `strategies` | `docs/architecture/market-data-collection.md` §«Как потребность доходит до сбора» |
| требование индикатора | `POST /api/v1/market-data/requirements/indicators` | то же | там же; ключевание — `docs/models/domain/other/IndicatorValue.md` §«Ключевание — идентичностью вычисления» |
| требование структуры рынка | `POST /api/v1/market-data/requirements/market-structures` | то же | там же |
| действующий листинг | `GET /api/v1/market-data/instruments` | `trading-core`, `bff` | `docs/architecture/contracts.md` §«Синхронные вызовы» |
| инструмент каталога | `GET /api/v1/market-data/instruments/{internalId}` | то же | `docs/models/domain/core/Instrument.md` |
| справочные правила инструмента | `GET …/{internalId}/rules` | `trading-core` | `docs/models/domain/other/InstrumentExternalRules.md` |
| единицы сбора инструмента | `GET …/{internalId}/candle-groups` | `bff` | `docs/models/domain/other/CandleGroup.md` |
| история свечей окном | `GET …/{internalId}/candles` | `strategies` (бэктест) | `docs/architecture/contracts.md` §«Синхронные вызовы» |
| последнее свежее значение индикатора | `GET …/{internalId}/indicator-values/latest` | `bff` | `docs/components/IndicatorService.md` §Контракт |
| последняя свежая структура | `GET …/{internalId}/market-structures/latest` | `bff` | `docs/components/MarketStructureService.md` §Контракт |
| фичи на момент решения | `POST …/{internalId}/features` | `trading-core` | `docs/architecture/market-data-collection.md` §«Фаза: клаузы приезжают операндом вызова» |
| цены момента | `GET …/{internalId}/prices` | `trading-core`, `bff` | `docs/components/MarketPriceDataService.md` §Назначение |
| последний срез книги заявок | `GET …/{internalId}/order-book/latest` | `bff` | `docs/models/domain/other/MarketOrderBook.md` |
| последний срез цен | `GET …/{internalId}/ticker/latest` | `bff` | `docs/models/domain/other/MarketTicker.md` |
| ручной запуск пяти тиков | `POST /api/v1/market-data/jobs/{тик}` | эксплуатация | `.claude/rules/codestyle.md` §Джобы |
| проба живости | `GET /actuator/health` | развёртывание | `docs/rules/api-access-policy.md` §«Вся поверхность закрыта; открытое — перечислено» |

## B1 — Требования потребителя

### B1.1 — Требование свечей заводит единицу сбора

| Поле | Содержание |
|---|---|
| Предусловия | каталог несёт инструмент `I1` в статусе `SYNC`; единиц сбора нет |
| Вход | `POST /requirements/candles` с `instrumentInternalId` = `I1`, `timeframe` = `H1`, `depthBars` = `500` |
| Ожидаемые выходы — все | `200`; тело — единица сбора со `status` = `CREATED`, `internalId` = `<internalId инструмента>:H1`; в базе ровно одна строка `candle_groups` с `planned_first_utc_millis`, выведенным из глубины и длительности бара, и `count` = 0; **статус инструмента не менялся**; к стабу коннектора обращений нет; событий нет |
| Чем подтверждается | `docs/architecture/market-data-collection.md` §«Как потребность доходит до сбора» (команда называет три вещи, из них выводится единица сбора и горизонт); `docs/processes/candle-loading.md` §«Кто заводит группу» |
| Факт | |

### B1.2 — Повтор того же требования второй единицы не создаёт

| Поле | Содержание |
|---|---|
| Предусловия | единица сбора заведена кейсом B1.1 |
| Вход | тот же запрос второй раз |
| Ожидаемые выходы — все | `200`; `internalId` тот же; число строк `candle_groups` не изменилось; горизонт не сдвинулся |
| Чем подтверждается | `docs/architecture/market-data-collection.md` §«Как потребность доходит до сбора» («повтор команды безопасен»); `docs/rules/idempotency-via-unique.md`; ключ `uk_candle_group_instrument_timeframe` |
| Факт | |

### B1.3 — Требование мельче стоящего горизонта не сужает

| Поле | Содержание |
|---|---|
| Предусловия | у группы стои́т горизонт на 500 баров назад; статус `ACTIVE` |
| Вход | требование того же инструмента и таймфрейма с `depthBars` = `50` |
| Ожидаемые выходы — все | `200`; `plannedFirstUtcMillis` **не изменился**; статус остался `ACTIVE`; собранных свечей не убавилось |
| Чем подтверждается | `docs/architecture/market-data-collection.md` §«Как потребность доходит до сбора» («отзыва требования нет; собранное остаётся»); `docs/rules/market-data-retention.md` §«Правило — по классу ряда» |
| Факт | |

### B1.4 — Требование глубже стоящего возвращает готовую группу к бэкфиллу

| Поле | Содержание |
|---|---|
| Предусловия | группа в `ACTIVE`, горизонт — 50 баров назад |
| Вход | требование с `depthBars` = `500` |
| Ожидаемые выходы — все | `200`; `plannedFirstUtcMillis` углублён; **статус стал `BACKFILL`**; собранные свечи на месте |
| Чем подтверждается | `docs/lifecycles/CandleGroup.md` §«Возврат к `BACKFILL` по углублённому требованию» |
| Факт | |

### B1.5 — То же ребро срабатывает из `SYNC`, `CHECK` и `REPAIR`

| Поле | Содержание |
|---|---|
| Предусловия | три группы, по одной в каждом из статусов `SYNC`, `CHECK`, `REPAIR`; горизонт у всех мелкий |
| Вход | углубляющее требование по каждой |
| Ожидаемые выходы — все | все три — в `BACKFILL`; ни одна не осталась в прежнем статусе. Ребро **шире, чем «из `ACTIVE`»**: дотягивать нижнюю границу умеет только `BACKFILL`, и из прочих живых статусов группа дошла бы до `ACTIVE` с непокрытым горизонтом |
| Чем подтверждается | `docs/lifecycles/CandleGroup.md` §«Возврат к `BACKFILL` по углублённому требованию» |
| Факт | |

### B1.6 — Терминальная группа требованием не оживляется

| Поле | Содержание |
|---|---|
| Предусловия | группа в `ERROR`; вторая — в `DELETED` |
| Вход | углубляющее требование по каждой |
| Ожидаемые выходы — все | `200`; **горизонт расширен у обеих**, статус у обеих **не изменился**; тик загрузки их не берёт |
| Чем подтверждается | `docs/lifecycles/CandleGroup.md` §«Возврат к `BACKFILL` по углублённому требованию» («терминальные статусы требованием не оживляются; горизонт при этом всё равно расширяется — он величина группы, а не её статуса») |
| Факт | |

### B1.7 — Требование без глубины означает всю доступную историю

| Поле | Содержание |
|---|---|
| Предусловия | каталог несёт `I1`; единиц сбора нет |
| Вход | требование без `depthBars` |
| Ожидаемые выходы — все | `200`; строка заведена, `plannedFirstUtcMillis` **пуст**; бэкфилл такой группы завершится только пустым ответом площадки, а не достижением горизонта |
| Чем подтверждается | `docs/lifecycles/CandleGroup.md` §«Глубина и покрытие (`BACKFILL`)» («горизонт не назван — выкачка идёт до конца истории площадки») |
| Факт | |

### B1.8 — Требование по несуществующему инструменту

| Поле | Содержание |
|---|---|
| Предусловия | каталог пуст |
| Вход | требование свечей с неизвестным `instrumentInternalId` |
| Ожидаемые выходы — все | `400` с error-DTO (`code` = `INVALID_REQUEST`); строки `candle_groups` нет; инструмент **не заводится** — каталог наполняет листинг, а не команда |
| Чем подтверждается | `docs/components/InstrumentSyncJob.md` §«Каталог наполняет листинг, а не человек»; `@ApiResponses` контроллера требований |
| Факт | |

### B1.9 — Требование индикатора идемпотентно по канонической форме параметров

| Поле | Содержание |
|---|---|
| Предусловия | реестр идентичностей пуст |
| Вход | два требования `ATR` на `H1` с одними и теми же параметрами, записанными **разным порядком ключей** |
| Ожидаемые выходы — все | оба `200`; `internalId` совпадает; в `indicator_configs` **одна** строка; вторая тропа расчёта не заводится |
| Чем подтверждается | `docs/models/domain/other/IndicatorValue.md` §«Ключевание — идентичностью вычисления»; ключ `uk_indicator_config_identity` |
| Факт | |

### B1.10 — Параметры, не разбирающиеся под заявленный тип

| Поле | Содержание |
|---|---|
| Предусловия | реестр пуст |
| Вход | требование `ATR` с параметрами `{"fastPeriod": 12}` (поля чужого типа) |
| Ожидаемые выходы — все | `400` с error-DTO; строки в реестре нет; идентичность не заводится «наполовину» |
| Чем подтверждается | `docs/rules/persistence-representation.md` (подтип восстанавливается по типу строки-владельца, тег в теле не дублируется); `@ApiResponses` точки требования индикатора |
| Факт | |

### B1.11 — Идентичность структуры включает идентичности её входов

| Поле | Содержание |
|---|---|
| Предусловия | заведены две идентичности `ATR` — `A1` и `A2` |
| Вход | два требования структуры с одинаковым таймфреймом и параметрами, но с разными `atrConfigInternalId` (`A1`, затем `A2`) |
| Ожидаемые выходы — все | оба `200`; `internalId` **различаются**; в `market_structure_configs` две строки |
| Чем подтверждается | `docs/models/domain/other/MarketStructure.md` (входы входят в идентичность результата); ключ `uk_market_structure_config_identity` |
| Факт | |

### B1.12 — Требование структуры с несуществующим входом

| Поле | Содержание |
|---|---|
| Предусловия | реестр идентичностей индикаторов пуст |
| Вход | требование структуры с `efficiencyRatioConfigInternalId`, которого нет |
| Ожидаемые выходы — все | `400` с error-DTO; строки структуры в реестре нет |
| Чем подтверждается | `@ApiResponses` точки требования структуры («названного входа нет либо параметры не разбираются») |
| Факт | |

### B1.13 — Требование вычисления ряда свечей не влечёт

| Поле | Содержание |
|---|---|
| Предусловия | каталог несёт `I1`; единиц сбора нет |
| Вход | требование индикатора `ATR` на `H1` — и больше ничего |
| Ожидаемые выходы — все | `200`; идентичность заведена; **ни одной строки `candle_groups`**; тик расчёта по этой идентичности ничего не пишет, потому что групп её таймфрейма нет |
| Чем подтверждается | `docs/architecture/market-data-collection.md` §«Как потребность доходит до сбора» («требование ВЫЧИСЛЕНИЯ не влечёт ряда свечей») |
| Факт | |

### B1.14 — Отзыва требования у поверхности нет

| Поле | Содержание |
|---|---|
| Предусловия | заведены единица сбора и обе идентичности |
| Вход | `DELETE` по каждому из трёх путей требований |
| Ожидаемые выходы — все | `405` у всех трёх (метод не поддержан), тело — error-DTO; ни одна строка не удалена. Отсутствие операции — **намеренная граница**, а не пропуск |
| Чем подтверждается | `docs/architecture/market-data-collection.md` §«Как потребность доходит до сбора» («отзыва требования нет»); долг формы тела — `.claude/work/backlog.md` §«Единый error-DTO у поверхностей соседних сервисов» |
| Факт | |

## B2 — Синк каталога: тик как вход

### B2.1 — Первый тик зеркалит листинг в каталог

| Поле | Содержание |
|---|---|
| Предусловия | каталог пуст; `connector.instrument-types` = `SWAP`; стаб коннектора отдаёт листинг из трёх инструментов и правила по каждому |
| Вход | ручной запуск синка (`POST /jobs/instrument-sync`) |
| Ожидаемые выходы — все | `202` немедленно; после тика в `instruments` три строки со `status` = `SYNC`, непустым `internal_id` и `exchange_code` из конфигурации; навес правил материализован; к стабу ушли один запрос листинга на тип и по запросу правил на инструмент; событий нет |
| Чем подтверждается | `docs/components/InstrumentSyncJob.md` §«Что делает», §«Листинг и правила идут разным охватом»; `docs/lifecycles/Instrument.md` §«Координация `Instrument.Status` ↔ `CandleGroup.Status`» (групп нет — инструмент остаётся в `SYNC`) |
| Факт | |

### B2.2 — Повторный тик вторых строк не заводит и навес не затирает

| Поле | Содержание |
|---|---|
| Предусловия | каталог заведён кейсом B2.1; стаб отдаёт тот же листинг с изменённым биржевым статусом одного инструмента |
| Вход | второй тик |
| Ожидаемые выходы — все | строк по-прежнему три; `internal_id` у каждой прежний; биржевой статус обновлён; **колонка навеса правил не обнулилась**; `status` онбординга из листинга не переписан |
| Чем подтверждается | `docs/components/InstrumentSyncJob.md` §«Каталог наполняет листинг, а не человек»; ключ `uk_instrument_exchange_external_id` |
| Факт | |

### B2.3 — Пустое поле ответа не затирает известного

| Поле | Содержание |
|---|---|
| Предусловия | у инструмента заполнены биржевые поля спецификации |
| Вход | тик, на котором стаб отдаёт тот же инструмент с частью полей пустыми |
| Ожидаемые выходы — все | известные значения на месте; пустота ответа их не переписала |
| Чем подтверждается | `docs/models/domain/core/Instrument.md` §Структура; стратегия обновления из листинга (`InstrumentMapper.updateFromListing`, `nullValuePropertyMappingStrategy = IGNORE`) — ассерт прямой по базе: поверхности у этого инварианта нет |
| Факт | |

### B2.4 — Отказ доступа на листинге прекращает тик целиком

| Поле | Содержание |
|---|---|
| Предусловия | каталог несёт инструменты; стаб отвечает на листинг кодом `429` |
| Вход | тик синка |
| Ожидаемые выходы — все | **ни одного** запроса правил после отказа; каталог не изменился; в журнале — запись об остановке тика; отказ наружу не транслируется (фасад уже ответил `202`) |
| Чем подтверждается | `docs/components/InstrumentSyncJob.md` §«Отказ доступа прекращает тик»; `docs/processes/snapshot-collection.md` §«Отказ на проходе» (общий довод: под исчерпанным лимитом теряется и следующий проход) |
| Факт | |

### B2.5 — Рядовой отказ чтения тик не роняет

| Поле | Содержание |
|---|---|
| Предусловия | `connector.instrument-types` = `SWAP,FUTURES`; стаб отвечает `500` на один тип и штатно на другой |
| Вход | тик синка |
| Ожидаемые выходы — все | инструменты отвечавшего типа заведены; отказ второго типа записан в журнал; обход правил состоялся |
| Чем подтверждается | `docs/components/InstrumentSyncJob.md` §«Отказ доступа прекращает тик» («рядовой отказ чтения стои́т один инструмент») |
| Факт | |

### B2.6 — Правила обходятся окном за курсором и по кругу

| Поле | Содержание |
|---|---|
| Предусловия | каталог несёт пять инструментов; `instrument-sync.rules-batch-size` = `2` |
| Вход | четыре тика подряд |
| Ожидаемые выходы — все | тики берут инструменты окнами `[1,2]`, `[3,4]`, `[5]`, затем пустое окно возвращает курсор в начало и четвёртый тик снова берёт `[1,2]`; **ни один инструмент не остаётся без обновления правил**; запросов правил за тик не больше размера окна |
| Чем подтверждается | `docs/components/InstrumentSyncJob.md` §«Листинг и правила идут разным охватом» («окно идёт ЗА курсором, а не первыми N») |
| Факт | |

### B2.7 — Отказ доступа на обходе правил прекращает обход, а сделанное остаётся

| Поле | Содержание |
|---|---|
| Предусловия | каталог несёт три инструмента; стаб отдаёт правила первому и отвечает `401` на втором |
| Вход | тик синка |
| Ожидаемые выходы — все | навес первого обновлён; запроса по третьему **нет**; навес второго и третьего не менялся |
| Чем подтверждается | `docs/components/InstrumentSyncJob.md` §«Отказ доступа прекращает тик» |
| Факт | |

### B2.8 — Выключатель гасит и расписание, и ручной тик

| Поле | Содержание |
|---|---|
| Предусловия | `instrument-sync.enabled` = `false` |
| Вход | ручной запуск синка |
| Ожидаемые выходы — все | `202` (фасад отвечает за запуск, не за работу); **ни одного** запроса к стабу; каталог не изменился |
| Чем подтверждается | `.claude/rules/codestyle.md` §Джобы («при `false` запланированный и ручной тик ничего не делают»); §«Обработка ошибок» (async-фасад отвечает только за запуск) |
| Факт | |

### B2.9 — Перекрывающий запуск пропускается молча

| Поле | Содержание |
|---|---|
| Предусловия | стаб отдаёт листинг с задержкой, превышающей паузу между двумя запусками |
| Вход | два ручных запуска синка подряд |
| Ожидаемые выходы — все | оба ответа `202`; работа выполнена **один** раз — число запросов листинга к стабу равно числу типов инструментов, а не удвоено |
| Чем подтверждается | `.claude/rules/codestyle.md` §Джобы (защита от конкурентного выполнения — `JobExecutionGuard`); `JobController` javadoc (перекрытие гасится молча) |
| Факт | |

### B2.10 — Валюты инструмента синк не пишет

| Поле | Содержание |
|---|---|
| Предусловия | инструмент заведён с валютами `BTC`/`USDT`/`USDT` |
| Вход | тик синка, на котором стаб отдаёт тот же инструмент с **другими** валютами |
| Ожидаемые выходы — все | значения валют в базе **не изменились**: они добываются один раз при заведении и подтверждения не требуют. Ожидание взято из дома; сегодняшнее дерево его не несёт — см. находку F-2 |
| Чем подтверждается | `docs/components/InstrumentSyncJob.md` §Границы («валюты инструмента синк не пишет»); `docs/lifecycles/Instrument.md` §«Переходы и триггеры» («ежечасный `InstrumentSyncJob` валют не касается») |
| Факт | |

## B3 — Загрузка свечей: цикл единицы сбора

### B3.1 — Заведённая группа уходит в бэкфилл

| Поле | Содержание |
|---|---|
| Предусловия | группа в `CREATED`; стаб коннектора отдаёт историю |
| Вход | тик загрузки свечей (`POST /jobs/candles`) |
| Ожидаемые выходы — все | статус стал `BACKFILL`; **чтения площадки на этом тике нет** — первый тик только открывает цикл; свечей не прибавилось |
| Чем подтверждается | `docs/lifecycles/CandleGroup.md` §«Общий поток»; ассерт прямой по базе — поверхности у статуса единицы сбора, кроме чтения `candle-groups`, нет |
| Факт | |

### B3.2 — Бэкфилл тянет историю назад и фиксирует границы

| Поле | Содержание |
|---|---|
| Предусловия | группа в `BACKFILL`, горизонт — 300 баров назад; стаб отдаёт страницу закрытых свечей на каждый запрос истории |
| Вход | серия тиков |
| Ожидаемые выходы — все | свечи записаны; `actual_first_utc_millis`, `actual_last_utc_millis` и `count` на группе совпадают с записанным рядом; запрос к стабу идёт с `afterMillis`, равным нижней границе загруженного, и `limit` из конфигурации |
| Чем подтверждается | `docs/lifecycles/CandleGroup.md` §«Глубина и покрытие (`BACKFILL`)»; `docs/processes/candle-loading.md` §«Оркестрация: `CandleJob` и цикл `CandleGroup`» |
| Факт | |

### B3.3 — Пустой ответ площадки завершает бэкфилл

| Поле | Содержание |
|---|---|
| Предусловия | группа в `BACKFILL` **без** горизонта (требование без глубины, B1.7) |
| Вход | тик, на котором стаб отдаёт пустую страницу |
| Ожидаемые выходы — все | статус стал `CHECK`; нижняя граница зафиксирована по фактически загруженному; повторных запросов вглубь нет |
| Чем подтверждается | `docs/lifecycles/CandleGroup.md` §«Глубина и покрытие (`BACKFILL`)» («до пустого ответа биржи — начало истории биржи по инструменту») |
| Факт | |

### B3.4 — Достигнутый горизонт завершает бэкфилл

| Поле | Содержание |
|---|---|
| Предусловия | группа в `BACKFILL` с горизонтом; загруженная нижняя граница ещё выше горизонта |
| Вход | тики, пока нижняя граница не дойдёт до горизонта |
| Ожидаемые выходы — все | как только `actualFirst` ≤ `plannedFirst`, статус становится `CHECK`; дальше вглубь не читается, хотя площадка отдаёт ещё |
| Чем подтверждается | `docs/lifecycles/CandleGroup.md` §«Глубина и покрытие (`BACKFILL`)»; `docs/components/CandleJob.md` §«Горизонт берётся у группы, а не у инструмента» |
| Факт | |

### B3.5 — Плотный ряд на проверке даёт готовность

| Поле | Содержание |
|---|---|
| Предусловия | группа в `CHECK`; ряд плотен — `count` равен ожидаемому по density-инварианту на фактических границах |
| Вход | тик |
| Ожидаемые выходы — все | статус стал `ACTIVE`; чтения площадки на проверке нет |
| Чем подтверждается | `docs/lifecycles/CandleGroup.md` §«Проверка целостности (`CHECK`)»; `docs/models/domain/other/CandleGroup.md` §«Целостность по count (density-инвариант)» |
| Факт | |

### B3.6 — Дефицит на проверке уводит в починку

| Поле | Содержание |
|---|---|
| Предусловия | группа в `CHECK`; в середине ряда вырезано несколько баров |
| Вход | тик |
| Ожидаемые выходы — все | статус стал `REPAIR`; дыра ещё не локализована — чтения на этом тике нет |
| Чем подтверждается | `docs/lifecycles/CandleGroup.md` §«Проверка целостности (`CHECK`)» |
| Факт | |

### B3.7 — Починка локализует дыру и возвращает ряд к плотности

| Поле | Содержание |
|---|---|
| Предусловия | группа в `REPAIR`; дыра внутри ряда; стаб отдаёт недостающие бары на запрос окна |
| Вход | серия тиков |
| Ожидаемые выходы — все | запросы истории идут **окнами, сужающимися вдвое**, а не полным перечитыванием ряда; после докачки статус проходит `CHECK` и становится `ACTIVE`; дыры в ряду нет |
| Чем подтверждается | `docs/lifecycles/CandleGroup.md` §«Докачка дыр (`REPAIR`)» (бинарный поиск по count) |
| Факт | |

### B3.8 — Неустранимая дыра исчерпывает попытки и даёт `ERROR`

| Поле | Содержание |
|---|---|
| Предусловия | группа в `REPAIR`; стаб на запрос окна отдаёт пусто (площадка действительно не имеет баров); `candle-loading.max-repair-attempts` = `2` |
| Вход | три тика |
| Ожидаемые выходы — все | после превышения числа попыток статус стал `ERROR`; цикл группу больше не ведёт; требование потребителя её не оживляет (B1.6) |
| Чем подтверждается | `docs/lifecycles/CandleGroup.md` §«Докачка дыр (`REPAIR`)» («после исчерпания попыток → `ERROR`; реестра известных пропусков нет») |
| Факт | |

### B3.9 — Счётчик попыток не переживает рестарт

| Поле | Содержание |
|---|---|
| Предусловия | группа в `REPAIR` с неустранимой дырой; часть попыток израсходована |
| Вход | перезапуск контекста ящика, затем тики |
| Ожидаемые выходы — все | **по дому**: гарантия «N попыток → `ERROR`» переживает рестарт — счётчик лежит на группе. **Сегодня**: счётчик в памяти, и после рестарта отсчёт начинается заново — кейс красный, и это предъявление названного долга |
| Чем подтверждается | `docs/models/domain/other/CandleGroup.md` §Структура; `.claude/work/backlog.md` §«Дефекты донора, воспроизведённые портом» |
| Факт | |

### B3.10 — Докачка хвоста уводит готовую группу в синхронизацию

| Поле | Содержание |
|---|---|
| Предусловия | группа в `ACTIVE`; последняя загруженная свеча старше момента прогона больше чем на две длительности бара |
| Вход | тик |
| Ожидаемые выходы — все | группа переведена в `SYNC` и **тем же тиком** прочитана у площадки; новые закрытые бары записаны; после проверки — снова `ACTIVE` |
| Чем подтверждается | `docs/lifecycles/CandleGroup.md` §«Регулярная докачка хвоста (`SYNC`)»; `docs/components/CandleJob.md` §«Что делает» |
| Факт | |

### B3.11 — Готовность инструмента считается по его группам

| Поле | Содержание |
|---|---|
| Предусловия | у инструмента две группы: одна `ACTIVE`, одна `SYNC` |
| Вход | тик |
| Ожидаемые выходы — все | инструмент — в `CANDLES_LOADING`; после доведения второй группы до `ACTIVE` следующий тик переводит инструмент в `ACTIVE` |
| Чем подтверждается | `docs/lifecycles/Instrument.md` §«Координация `Instrument.Status` ↔ `CandleGroup.Status`»; `docs/components/CandleJob.md` §«Пересчёт готовности идёт этим же тиком» |
| Факт | |

### B3.12 — Инструмент без единой группы остаётся в `SYNC`

| Поле | Содержание |
|---|---|
| Предусловия | инструмент заведён синком; требований по нему не предъявлено |
| Вход | тик загрузки свечей |
| Ожидаемые выходы — все | статус остался `SYNC`; в `CANDLES_LOADING` он не уходит и в `ACTIVE` не попадает. Это **не дефект онбординга**: спецификация известна, собирать нечего |
| Чем подтверждается | `docs/lifecycles/Instrument.md` §«Координация `Instrument.Status` ↔ `CandleGroup.Status`» |
| Факт | |

### B3.13 — Популяция пересчёта готовности двусторонняя

| Поле | Содержание |
|---|---|
| Предусловия | инструмент в `CANDLES_LOADING`, **все** его группы уже `ACTIVE` (незавершённых нет ни одной) |
| Вход | тик |
| Ожидаемые выходы — все | инструмент переведён в `ACTIVE`. Односторонняя популяция («есть незавершённая группа») его бы не нашла, и он остался бы в `CANDLES_LOADING` навсегда |
| Чем подтверждается | `docs/lifecycles/Instrument.md` §«Координация `Instrument.Status` ↔ `CandleGroup.Status`» («пересчёт готовности идёт двусторонней популяцией»); `docs/components/CandleJob.md` §«Пересчёт готовности идёт этим же тиком» |
| Факт | |

### B3.14 — Отказ доступа прекращает тик, рядовой отказ — одну группу

| Поле | Содержание |
|---|---|
| Предусловия | три рабочих группы; стаб отвечает `429` на чтении второй |
| Вход | тик |
| Ожидаемые выходы — все | первая группа продвинута; **запроса по третьей нет**; при отказе `500` вместо `429` — третья группа обрабатывается, а отказ второй уходит в журнал |
| Чем подтверждается | `docs/components/CandleJob.md` §«Отказ доступа прекращает тик»; `docs/processes/snapshot-collection.md` §«Отказ на проходе» |
| Факт | |

### B3.15 — Повтор страницы вторых свечей не создаёт

| Поле | Содержание |
|---|---|
| Предусловия | группа в `SYNC`; стаб на двух тиках подряд отдаёт **одну и ту же** страницу |
| Вход | два тика |
| Ожидаемые выходы — все | число свечей после второго тика не выросло; `count` группы совпадает с числом строк; перекрытие страниц дублей не даёт |
| Чем подтверждается | `docs/rules/idempotency-via-unique.md`; ключ `pk_candle` (группа + открытие бара); долг формы дедупа — `.claude/work/backlog.md` §«Дедуп проверкой существования перед вставкой — построенный класс, а не точка» |
| Факт | |

### B3.16 — Незакрытый бар в ряд не попадает

| Поле | Содержание |
|---|---|
| Предусловия | группа в `SYNC`; стаб отдаёт только закрытые бары — признак закрытия виден коннектору и наружу не едет |
| Вход | тик |
| Ожидаемые выходы — все | в ряду нет бара, чьё открытие лежит ближе одной длительности к моменту прогона; второго фильтра по признаку, которого в ответе нет, у сервиса **не заводится** |
| Чем подтверждается | `docs/components/CandleJob.md` §«Правило закрытых свечей»; `docs/models/domain/other/Candle.md` |
| Факт | |

## B4 — Производные: индикаторы и структура

### B4.1 — Идентичность считается по всем группам своего таймфрейма

| Поле | Содержание |
|---|---|
| Предусловия | заведена одна идентичность `ATR` на `H1`; три инструмента имеют группы `H1` в рабочих статусах, четвёртый — только `M15` |
| Вход | тик расчёта индикаторов (`POST /jobs/indicators`) |
| Ожидаемые выходы — все | значения записаны по трём инструментам; по четвёртому — ни одного; одна заказанная идентичность покрыла весь листинг своего таймфрейма; чтений площадки на тике нет |
| Чем подтверждается | `docs/components/IndicatorJob.md` §«Инструменты приносит сбор, а не заказ», §«Обходятся идентичности вычисления, а не настройки стратегий» |
| Факт | |

### B4.2 — Повторный тик вторых значений не пишет

| Поле | Содержание |
|---|---|
| Предусловия | значения посчитаны кейсом B4.1; свечей не прибавилось |
| Вход | второй тик |
| Ожидаемые выходы — все | число строк `indicator_values` не изменилось; новых значений нет; чекпойнт отдельным состоянием не хранится |
| Чем подтверждается | `docs/processes/market-data-calculation.md` §«Условия запуска и инварианты»; ключ `uk_indicator_value_identity` |
| Факт | |

### B4.3 — Зона разгона наружу не выходит

| Поле | Содержание |
|---|---|
| Предусловия | группа несёт ровно столько баров, сколько нужно на разгон плюс три; идентичность — `RSI` с периодом `14` |
| Вход | тик |
| Ожидаемые выходы — все | записаны только значения на барах **после** прогрева; значений зоны разгона в ряду нет; величина прогрева в самом значении не хранится |
| Чем подтверждается | `docs/components/IndicatorJob.md` §Прогрев |
| Факт | |

### B4.4 — Недостаточная история не даёт значений

| Поле | Содержание |
|---|---|
| Предусловия | у группы `H1` баров меньше периода идентичности; группа в рабочем статусе |
| Вход | тик |
| Ожидаемые выходы — все | значений не записано **ни одного**; тик не падает; остальные идентичности посчитаны |
| Чем подтверждается | `docs/processes/market-data-calculation.md` §«Условия запуска и инварианты» («при отсутствии свежих входных данных job не создаёт новый result») |
| Факт | |

### B4.5 — Идентичность без калькулятора остальных не роняет

| Поле | Содержание |
|---|---|
| Предусловия | реестр несёт две идентичности; для одной калькулятора в контексте нет |
| Вход | тик |
| Ожидаемые выходы — все | вторая идентичность посчитана; отказ первой — запись в журнале; тик завершился |
| Чем подтверждается | `docs/components/IndicatorJob.md` §Границы; ассерт по журналу — поверхности у этой ветви нет |
| Факт | |

### B4.6 — Объявленный, но не готовый вход даёт `UNKNOWN`

| Поле | Содержание |
|---|---|
| Предусловия | идентичность структуры объявляет вход `ATR`; значений этого `ATR` по инструменту нет |
| Вход | тик расчёта структуры (`POST /jobs/market-structures`) |
| Ожидаемые выходы — все | строка структуры **записана** с типом `UNKNOWN`, пустым перечнем уровней и границами окна по фактическим свечам; пропуска строки нет, приближения нет |
| Чем подтверждается | `docs/components/MarketStructureJob.md` §«Объявленный вход не готов — консервативный исход» |
| Факт | |

### B4.7 — Необъявленный вход отсутствием не является

| Поле | Содержание |
|---|---|
| Предусловия | идентичность структуры входов не объявляет вовсе |
| Вход | тик |
| Ожидаемые выходы — все | структура посчитана резолвером и записана **с реальным типом**, а не `UNKNOWN`: «вход не объявлен» и «вход не готов» — разные факты |
| Чем подтверждается | `docs/components/MarketStructureJob.md` §«Объявленный вход не готов — консервативный исход»; `MarketStructureConfigApiRequest` (пусто — вход не объявлен) |
| Факт | |

### B4.8 — Производитель свежесть входа не гейтит

| Поле | Содержание |
|---|---|
| Предусловия | идентичность структуры объявляет вход `ATR`; значение `ATR` есть, но его метка много старше окна |
| Вход | тик |
| Ожидаемые выходы — все | структура посчитана **на этом значении**, а не пропущена и не обнулена; годность посчитанного оценит читатель по своему сроку (B6.2) |
| Чем подтверждается | `docs/components/MarketStructureJob.md` §«Свежесть входа здесь не гейтится»; `docs/rules/market-data-freshness.md` §Правило |
| Факт | |

### B4.9 — Идентичность структуры без окна пропускается с записью в журнал

| Поле | Содержание |
|---|---|
| Предусловия | в реестре есть идентичность структуры, у параметров которой нет `lookbackBars` |
| Вход | тик |
| Ожидаемые выходы — все | строк по этой идентичности не записано; остальные идентичности посчитаны; отказ — в журнале |
| Чем подтверждается | `docs/models/domain/other/MarketStructure.md`; ассерт по журналу — поверхности у ветви нет |
| Факт | |

### B4.10 — Повторный тик структуры вторых строк не пишет

| Поле | Содержание |
|---|---|
| Предусловия | структура посчитана; свечей не прибавилось |
| Вход | второй тик |
| Ожидаемые выходы — все | число строк `market_structures` не изменилось; ключ — инструмент, идентичность и конец окна |
| Чем подтверждается | `docs/processes/market-data-calculation.md` §«Условия запуска и инварианты»; ключ `uk_market_structure_identity` |
| Факт | |

### B4.11 — Фазы ни один тик не считает и не хранит

| Поле | Содержание |
|---|---|
| Предусловия | заведены идентичности индикатора и структуры; посчитаны значения |
| Вход | все пять тиков подряд |
| Ожидаемые выходы — все | таблицы фазы в схеме нет; ни один тик строки фазы не пишет; фаза появляется только в ответе чтения фич с клаузами (B6.7) |
| Чем подтверждается | `docs/architecture/market-data-collection.md` §Производные («фаза рынка в этот класс не входит»); `docs/rules/market-data-retention.md` §«Правило — по классу ряда» |
| Факт | |

### B4.12 — Выключатели тиков производных независимы

| Поле | Содержание |
|---|---|
| Предусловия | `market-data.indicator.enabled` = `false`, `market-data.structure.enabled` = `true` |
| Вход | ручной запуск обоих тиков |
| Ожидаемые выходы — все | оба ответа `202`; значений индикаторов не прибавилось; структура посчитана на том, что есть |
| Чем подтверждается | `.claude/rules/codestyle.md` §Джобы |
| Факт | |

## B5 — Невосполнимые срезы: проход как единица работы

### B5.1 — Проход снимает тикер агрегатно, а книгу поинструментно

| Поле | Содержание |
|---|---|
| Предусловия | каталог несёт три инструмента действующего листинга; стаб отдаёт тикеры, марк-цены, индексы и книги |
| Вход | ручной запуск прохода (`POST /jobs/snapshots`) |
| Ожидаемые выходы — все | к стабу ушли: один запрос тикеров и один марк-цен **на тип инструмента**, по запросу индексов **на котировочную валюту** и по запросу книги **на инструмент**; в `ticker_snapshots` и `order_book_snapshots` по три строки; у каждой — биржевая метка времени и своя метка наблюдения |
| Чем подтверждается | `docs/processes/snapshot-collection.md` §«Два ряда снимаются по-разному, и это не деталь реализации»; `docs/architecture/market-data-collection.md` §«Невосполнимые срезы» |
| Факт | |

### B5.2 — Нерезолвившиеся марк-цена и индекс остаются пустыми

| Поле | Содержание |
|---|---|
| Предусловия | стаб отдаёт тикеры, но пустые карты марк-цен и индексов |
| Вход | проход |
| Ожидаемые выходы — все | строки тикер-среза записаны; поля марк-цены и индекса **пусты**; последняя цена в них **не подставлена** — подстановка молча меняла бы ценовой домен |
| Чем подтверждается | `docs/architecture/market-data-collection.md` §«Невосполнимые срезы»; `docs/components/models/MarketPriceData.md` §«Правила использования» |
| Факт | |

### B5.3 — Инструмент без валют индекса не получает

| Поле | Содержание |
|---|---|
| Предусловия | у инструмента пусты базовая либо котировочная валюта; стаб отдаёт полную карту индексов |
| Вход | проход |
| Ожидаемые выходы — все | срез записан; поле индекса пусто; чужая цена по похожему ключу не подставлена |
| Чем подтверждается | `docs/architecture/market-data-collection.md` §«Невосполнимые срезы»; `docs/models/domain/other/MarketTicker.md` §Структура |
| Факт | |

### B5.4 — Повтор момента площадки второй строкой не ложится

| Поле | Содержание |
|---|---|
| Предусловия | проход состоялся; стаб на втором проходе отдаёт **тот же** биржевой момент |
| Вход | второй проход |
| Ожидаемые выходы — все | число строк обоих рядов не изменилось; первая запись **не переписана** — иначе исказился бы ряд задержки; второго факта не произошло |
| Чем подтверждается | `docs/rules/idempotency-via-unique.md`; ключи `pk_ticker_snapshot`, `pk_order_book_snapshot`; долг формы дедупа — `.claude/work/backlog.md` §«Дедуп проверкой существования перед вставкой — построенный класс, а не точка» |
| Факт | |

### B5.5 — Отказ чтения книги по одному инструменту проход не роняет

| Поле | Содержание |
|---|---|
| Предусловия | три инструмента; стаб отвечает `500` на книге второго |
| Вход | проход |
| Ожидаемые выходы — все | книги первого и третьего записаны; строки второго **нет вовсе** — «не снято» отличается от «снято и пусто» отсутствием строки; отказ — в журнале |
| Чем подтверждается | `docs/processes/snapshot-collection.md` §«Отказ на проходе» |
| Факт | |

### B5.6 — Отказ ПИСЬМА одной строки — тоже отказ по одному инструменту

| Поле | Содержание |
|---|---|
| Предусловия | три инструмента; стаб отдаёт тикер второго **без биржевой метки времени** (половина естественного ключа) |
| Вход | проход |
| Ожидаемые выходы — все | тикеры первого и третьего записаны; строки второго нет; **книги всех трёх сняты** — незаписанная строка не унесла с собой ещё не снятые книги; отказ письма — в журнале |
| Чем подтверждается | `docs/processes/snapshot-collection.md` §«Отказ на проходе» («отказ ПИСЬМА одной строки — тоже отказ по одному инструменту») |
| Факт | |

### B5.7 — Отказ доступа на тикерах прекращает проход до книг

| Поле | Содержание |
|---|---|
| Предусловия | стаб отвечает `429` на чтении тикеров |
| Вход | проход |
| Ожидаемые выходы — все | **ни одного** запроса книги; строк обоих рядов не прибавилось; остановка записана в журнал |
| Чем подтверждается | `docs/processes/snapshot-collection.md` §«Отказ на проходе» («продолжать обход под исчерпанным лимитом — способ потерять и следующий проход») |
| Факт | |

### B5.8 — Рядовой отказ тикеров книги не отменяет

| Поле | Содержание |
|---|---|
| Предусловия | стаб отвечает `500` на чтении тикеров и штатно на книгах |
| Вход | проход |
| Ожидаемые выходы — все | тикер-срез этого прохода пропущен; **книги сняты по всем инструментам**; отказ — в журнале предупреждением, а не остановкой |
| Чем подтверждается | `docs/processes/snapshot-collection.md` §«Отказ на проходе» |
| Факт | |

### B5.9 — Отказ доступа на книгах прекращает обход

| Поле | Содержание |
|---|---|
| Предусловия | три инструмента; стаб отвечает `401` на книге второго |
| Вход | проход |
| Ожидаемые выходы — все | книга первого записана; запроса по третьему **нет**; тикер-срез прохода записан целиком |
| Чем подтверждается | `docs/processes/snapshot-collection.md` §«Отказ на проходе» |
| Факт | |

### B5.10 — Порядок обхода стабилен, а окно прохода ограничено

| Поле | Содержание |
|---|---|
| Предусловия | каталог несёт пять инструментов; `snapshot-collection.pass-limit` = `3` |
| Вход | два прохода подряд |
| Ожидаемые выходы — все | оба прохода взяли **одни и те же** три инструмента в одном и том же порядке; усечённым оказался один и тот же хвост листинга, а не случайные инструменты |
| Чем подтверждается | `docs/processes/snapshot-collection.md` §«Оркестрация: один проход по листингу за интервал» («порядок обхода — стабильный») |
| Факт | |

### B5.11 — В проход входит только действующий листинг

| Поле | Содержание |
|---|---|
| Предусловия | каталог несёт инструмент в `CREATED` и инструмент в `ACTIVE` |
| Вход | проход |
| Ожидаемые выходы — все | срез снят только по действующему; по инструменту вне действующих статусов запросов нет и строк нет |
| Чем подтверждается | `docs/architecture/market-data-collection.md` §«Правило — по невосполнимости» («весь листинг каждой подключённой площадки»); `docs/lifecycles/Instrument.md` §«`Instrument.Status` (онбординг-путь шага 1)» |
| Факт | |

### B5.12 — Перекрывающий проход не догоняется

| Поле | Содержание |
|---|---|
| Предусловия | стаб отдаёт книги с задержкой, превышающей паузу между запусками |
| Вход | два ручных запуска прохода подряд |
| Ожидаемые выходы — все | оба `202`; работа выполнена **один** раз — расход запросов к стабу не удвоился; пропущенный проход не догоняется, потому что момент прошёл |
| Чем подтверждается | `docs/processes/snapshot-collection.md` §«Оркестрация: один проход по листингу за интервал»; `.claude/rules/codestyle.md` §Джобы |
| Факт | |

### B5.13 — Глубина книги — величина конфигурации и уезжает в запрос

| Поле | Содержание |
|---|---|
| Предусловия | `snapshot-collection.order-book-depth` = `5` |
| Вход | проход |
| Ожидаемые выходы — все | в запросе к стабу глубина равна объявленной; в записанном срезе уровней каждой стороны не больше неё |
| Чем подтверждается | `docs/architecture/market-data-collection.md` §«Невосполнимые срезы» (величина, её писатель и вывод значения) |
| Факт | |

## B6 — Фичи на момент решения и свежесть

### B6.1 — Связка фич снимается одним чтением по авторским именам

| Поле | Содержание |
|---|---|
| Предусловия | посчитаны значения двух идентичностей и одна структура; их метки свежи под срок читателя |
| Вход | `POST …/features` с привязками `{"быстрый": <ATR>, "медленный": <EMA>}` и одной структурной привязкой; `priceRequired` = `false` |
| Ожидаемые выходы — все | `200`; раскладки ключуются **авторскими именами**, а не идентичностями; отданы и последние, и предыдущие значения; структура отдана; цены нет; фазы нет; **к стабу коннектора обращений нет** |
| Чем подтверждается | `docs/architecture/market-data-collection.md` §«Фаза: клаузы приезжают операндом вызова»; `docs/components/MarketPhaseService.md` §«Клаузы и привязки приносит потребитель» |
| Факт | |

### B6.2 — Устаревшее значение места в раскладке не занимает

| Поле | Содержание |
|---|---|
| Предусловия | значение идентичности есть; его `candleTimestamp` старше момента прогона больше, чем на срок привязки |
| Вход | чтение фич с этой привязкой |
| Ожидаемые выходы — все | `200`; ключа привязки в раскладке **нет вовсе**; ошибки нет; предыдущее значение при этом отдаётся — оно свежестью не гейтится |
| Чем подтверждается | `docs/rules/market-data-freshness.md` §Правило; `docs/components/IndicatorService.md` §«Поведение при отсутствии / устаревании» |
| Факт | |

### B6.3 — Отсутствующее и устаревшее отвечают одинаково

| Поле | Содержание |
|---|---|
| Предусловия | по одной идентичности значений нет вовсе, по второй — значение устарело |
| Вход | одно чтение фич с обеими привязками |
| Ожидаемые выходы — все | `200`; обоих ключей в раскладке нет; различить две пустоты по ответу нельзя — и читателю не нужно |
| Чем подтверждается | `docs/spec/market-data-freshness.json`, величина `freshnessState`; `docs/components/MarketStructureService.md` §«Поведение при отсутствии / устаревании» |
| Факт | |

### B6.4 — Один и тот же ряд двум читателям с разными сроками

| Поле | Содержание |
|---|---|
| Предусловия | одно значение идентичности с меткой возрастом в пять минут |
| Вход | два чтения фич одной и той же привязки: срок `PT10M` и срок `PT1M` |
| Ожидаемые выходы — все | первое чтение значение отдаёт, второе — нет; **строка результата не менялась** и срока не несёт: он операнд вызова |
| Чем подтверждается | `docs/rules/market-data-freshness.md` §Правило («срок принадлежит ПОТРЕБИТЕЛЮ, а не строке результата»); `docs/components/MarketDataExpirationChecker.md` §«Вычисление свежести (на чтение)» |
| Факт | |

### B6.5 — Цена читается у площадки, только когда её спрашивают

| Поле | Содержание |
|---|---|
| Предусловия | стаб коннектора считает запросы цен |
| Вход | два чтения фич: одно без `priceRequired` и без клауз с ценовым операндом, второе — с `priceRequired` = `true` |
| Ожидаемые выходы — все | у первого — **ни одного** запроса к стабу и пустая цена в ответе; у второго — ровно один запрос цен и цена в ответе |
| Чем подтверждается | `docs/components/MarketPhaseService.md` §«Цена читается, только если её спрашивает клауза» |
| Факт | |

### B6.6 — Отказ площадки на цене даёт пустой операнд, а не отказ чтения

| Поле | Содержание |
|---|---|
| Предусловия | стаб отвечает `500` на чтении цен |
| Вход | чтение фич с `priceRequired` = `true` |
| Ожидаемые выходы — все | `200`, а не `502`; цена в ответе пуста; значения индикаторов и структуры отданы; отказ — в журнале. Семантика входа одна для всех: недоступный операнд в контекст не попадает |
| Чем подтверждается | `docs/components/MarketPhaseService.md` §«Поведение при отсутствии / устаревании входов»; `docs/rules/market-data-freshness.md` §Правило |
| Факт | |

### B6.7 — Клауз не передано — ответа о фазе нет

| Поле | Содержание |
|---|---|
| Предусловия | значения и структуры посчитаны и свежи |
| Вход | чтение фич **без** клауз фазы |
| Ожидаемые выходы — все | `200`; поле фазы пусто; `UNKNOWN` в нём **не стои́т** — классифицировать было нечем, а не нечего |
| Чем подтверждается | `docs/architecture/market-data-collection.md` §«Фаза: клаузы приезжают операндом вызова» («клауз не передано — ответа нет (не `UNKNOWN`)») |
| Факт | |

### B6.8 — Недоступный вход клаузы даёт `UNKNOWN`

| Поле | Содержание |
|---|---|
| Предусловия | клаузы фазы ссылаются на операнд, чьё значение устарело либо не посчитано |
| Вход | чтение фич с клаузами |
| Ожидаемые выходы — все | `200`; фаза отдана типом `UNKNOWN`; строки фазы в базе нет — она не персистируется |
| Чем подтверждается | `docs/components/MarketPhaseService.md` §«Поведение при отсутствии / устаревании входов»; `docs/architecture/market-data-collection.md` §«Фаза: клаузы приезжают операндом вызова» |
| Факт | |

### B6.9 — Одиночное чтение значения индикатора

| Поле | Содержание |
|---|---|
| Предусловия | значение посчитано и свежо под `PT10M` |
| Вход | `GET …/indicator-values/latest` с `configInternalId` и `tolerance` = `PT10M`, затем то же с `tolerance` = `PT1S` |
| Ожидаемые выходы — все | первое — `200` с телом, в котором стоят `internalId` инструмента и идентичности; второе — `204` **без тела**; ошибкой второе не является |
| Чем подтверждается | `docs/components/IndicatorService.md` §Контракт, §«Поведение при отсутствии / устаревании»; `@ApiResponses` точки |
| Факт | |

### B6.10 — Одиночное чтение структуры

| Поле | Содержание |
|---|---|
| Предусловия | структура посчитана; её конец окна старше срока читателя |
| Вход | `GET …/market-structures/latest` |
| Ожидаемые выходы — все | `204`; при сроке шире возраста — `200` с уровнями и обеими границами окна |
| Чем подтверждается | `docs/components/MarketStructureService.md` §Контракт; точка отсчёта — `docs/rules/market-data-freshness.md` §Правило (у структуры это конец окна, не момент подтверждения) |
| Факт | |

### B6.11 — Неизвестная идентичность в привязке

| Поле | Содержание |
|---|---|
| Предусловия | реестр пуст |
| Вход | чтение фич с привязкой к несуществующему `configInternalId` |
| Ожидаемые выходы — все | `400` с error-DTO; частичной раскладки не отдаётся; к стабу обращений нет |
| Чем подтверждается | `@ApiResponses` точки чтения фич; `.claude/rules/codestyle.md` §«Обработка ошибок» |
| Факт | |

### B6.12 — Чтение фич состояния не меняет

| Поле | Содержание |
|---|---|
| Предусловия | снимок всех таблиц снят до вызова |
| Вход | десять чтений фич подряд, включая ценовые |
| Ожидаемые выходы — все | снимок после совпадает с исходным: `POST` у чтения — форма запроса, а не запись; событий нет; строк не прибавилось |
| Чем подтверждается | `MarketFeatureController` javadoc («состояния вызов не меняет»); `docs/architecture/contracts.md` §«Синхронные вызовы» |
| Факт | |

### B6.13 — Пустой запрос фич законен

| Поле | Содержание |
|---|---|
| Предусловия | инструмент существует |
| Вход | `POST …/features` с телом `{}` |
| Ожидаемые выходы — все | `200`; раскладки пусты; цены нет; фазы нет; отказа нет — требовать непустоты значило бы запрещать законное чтение |
| Чем подтверждается | `FeatureReadApiRequest` javadoc; `docs/architecture/market-data-collection.md` §«Фаза: клаузы приезжают операндом вызова» |
| Факт | |

## B7 — Чтения каталога и истории

### B7.1 — Листинг отдаёт только действующее

| Поле | Содержание |
|---|---|
| Предусловия | каталог несёт инструменты в `CREATED`, `SYNC`, `CANDLES_LOADING`, `ACTIVE` |
| Вход | `GET /instruments` |
| Ожидаемые выходы — все | в ответе три инструмента — `SYNC`, `CANDLES_LOADING`, `ACTIVE`; инструмента в `CREATED` в ответе нет |
| Чем подтверждается | `docs/lifecycles/Instrument.md` §«`Instrument.Status` (онбординг-путь шага 1)»; `InstrumentController` (перечень действующих статусов) |
| Факт | |

### B7.2 — Навес правил: «правил нет» отличается от «правила пусты»

| Поле | Содержание |
|---|---|
| Предусловия | у первого инструмента навес материализован, у второго — нет |
| Вход | `GET …/rules` по каждому |
| Ожидаемые выходы — все | у первого `200` с телом правил; у второго `204` **без тела**; пустого объекта со `200` не отдаётся |
| Чем подтверждается | `InstrumentController` javadoc; `docs/models/domain/other/InstrumentExternalRules.md` §Персистентность |
| Факт | |

### B7.3 — Единицы сбора инструмента

| Поле | Содержание |
|---|---|
| Предусловия | у инструмента две группы разных таймфреймов |
| Вход | `GET …/candle-groups` |
| Ожидаемые выходы — все | обе группы со статусом, таймфреймом, `count` и фактическими границами; числового `id` ни своей строки, ни инструмента в теле нет |
| Чем подтверждается | `docs/models/domain/other/CandleGroup.md` §Структура; `.claude/rules/codestyle.md` §«Идентичность наружу — `internalId`, не id из БД» |
| Факт | |

### B7.4 — История читается окном, и окно обязательно

| Поле | Содержание |
|---|---|
| Предусловия | группа `H1` несёт тысячу свечей |
| Вход | `GET …/candles` без `limit`; затем с `timeframe`, `fromMillis` и `limit` = `100` |
| Ожидаемые выходы — все | первый — `400` (обязательный операнд не предъявлен); второй — `200` со ста свечами по возрастанию от нижней границы; безлимитного чтения истории нет ни одной тропой |
| Чем подтверждается | `CandleHistoryApiQuery` javadoc; `.claude/rules/codestyle.md` §«Выборка данных: не тянем сущность ради одного поля» |
| Факт | |

### B7.5 — У окна есть потолок

| Поле | Содержание |
|---|---|
| Предусловия | группа несёт больше свечей, чем потолок страницы |
| Вход | чтение истории с `limit` = `50000` |
| Ожидаемые выходы — все | `400` с error-DTO; выборки к базе **не производится**; глубже потолка история читается страницами, сдвигая нижнюю границу |
| Чем подтверждается | `CandleHistoryApiQuery` (потолок страницы объявлен полем); `.claude/rules/codestyle.md` §«Выборка данных: не тянем сущность ради одного поля» |
| Факт | |

### B7.6 — Несуществующая единица сбора у существующего инструмента

| Поле | Содержание |
|---|---|
| Предусловия | у инструмента есть группа `H1` и нет `M1` |
| Вход | чтение истории с `timeframe` = `M1` |
| Ожидаемые выходы — все | `400` с error-DTO; пустого списка со `200` не отдаётся — «группы нет» и «свечей нет» различаются |
| Чем подтверждается | `@ApiResponses` точки истории; `.claude/rules/codestyle.md` §«Обработка ошибок» |
| Факт | |

### B7.7 — Несуществующий инструмент на любой тропе чтения

| Поле | Содержание |
|---|---|
| Предусловия | каталог пуст |
| Вход | по одному вызову каждой из точек, адресующих инструмент путём |
| Ожидаемые выходы — все | у каждой — `400` с error-DTO и `code` = `INVALID_REQUEST`; ни одна не отвечает `500` и ни одна не отдаёт пустое тело со `200` |
| Чем подтверждается | `GlobalExceptionHandler` (`IllegalArgumentException` → `400`); `@ApiResponses` точек |
| Факт | |

### B7.8 — Последние срезы отдаются по одному правилу

| Поле | Содержание |
|---|---|
| Предусловия | по первому инструменту срезы сняты, по второму — нет |
| Вход | `GET …/order-book/latest` и `GET …/ticker/latest` по каждому |
| Ожидаемые выходы — все | у первого — `200` с последним по **биржевой** метке срезом и проставленным `internalId` инструмента; у второго — `204` у обеих точек |
| Чем подтверждается | `docs/models/domain/other/MarketTicker.md` §Назначение; `docs/models/domain/other/MarketOrderBook.md` |
| Факт | |

### B7.9 — Наружу уходит идентичность, а не ключ базы

| Поле | Содержание |
|---|---|
| Предусловия | заведены инструмент, группа, идентичность и значение |
| Вход | по одному вызову каждой читающей точки |
| Ожидаемые выходы — все | ни одно тело ответа не несёт числового `id` — ни своей строки, ни связанной; ссылки на связанные сущности выражены их `internalId` |
| Чем подтверждается | `.claude/rules/codestyle.md` §«Идентичность наружу — `internalId`, не id из БД»; `docs/architecture/data-ownership.md` §Идентификаторы |
| Факт | |

## B8 — Контур доступа и отказы поверхности

### B8.1 — Вызов без предъявленного принципала

| Поле | Содержание |
|---|---|
| Предусловия | контекст поднят |
| Вход | `GET /instruments` без заголовка `Authorization` |
| Ожидаемые выходы — все | `401`; тело — **тот же error-DTO**, что у всякой ошибки поверхности, без причины сверх класса; к стабу коннектора обращений нет |
| Чем подтверждается | `docs/rules/error-handling-policy.md` §«Отказ доступа — тот же контракт, что и прочие ошибки»; долг формы — `.claude/work/backlog.md` §«Единый error-DTO у поверхностей соседних сервисов» |
| Факт | |

### B8.2 — Токен, подписанный чужим ключом, просроченный и чужого издателя

| Поле | Содержание |
|---|---|
| Предусловия | стаб отдаёт JWKS с ключом `K1`; `issuer-uri` указывает на него |
| Вход | три вызова: токен, подписанный `K2`; токен с `exp` в прошлом; токен с чужим `iss` |
| Ожидаемые выходы — все | у всех трёх `401` с error-DTO; подпись проверена **локально** по ключам провайдера — за подтверждением сервис никуда не ходит |
| Чем подтверждается | `docs/architecture/contracts.md` §«Контекст тенанта в вызове»; `.claude/decisions/test-contour-design-pass.md` §«4. Провайдер идентичности — JWKS-стаб с локальной парой ключей и `issuer-uri` на стаб» |
| Факт | |

### B8.3 — Служебная идентичность соседа поверхность принимает

| Поле | Содержание |
|---|---|
| Предусловия | стаб выдаёт токен служебной идентичности кластера |
| Вход | `GET /instruments` и `POST …/features` под ним |
| Ожидаемые выходы — все | оба `200`: у поверхности `market-data` пер-операционных проверок нет намеренно, а вызывающие — соседи, а не человек |
| Чем подтверждается | `services/market-data/.../config/SecurityConfig.java` javadoc; `docs/rules/api-access-policy.md` §«Принципал один — посылка фазы 1» |
| Факт | |

### B8.4 — Проба живости открыта, остальное закрыто

| Поле | Содержание |
|---|---|
| Предусловия | контекст поднят |
| Вход | `GET /actuator/health` без токена; затем `GET /actuator/metrics` без токена |
| Ожидаемые выходы — все | у первой — `200`; у второй — отказ, а не данные: открытое перечислено поимённо, умолчание закрыто |
| Чем подтверждается | `docs/rules/api-access-policy.md` §«Вся поверхность закрыта; открытое — перечислено»; `application.yaml` (экспозиция актуатора — только проба живости) |
| Факт | |

### B8.5 — Неизвестный путь не предъявившему себя

| Поле | Содержание |
|---|---|
| Предусловия | контекст поднят |
| Вход | `GET /api/v1/market-data/whatever` без токена |
| Ожидаемые выходы — все | `401`, не `404`: не предъявивший себя не узнаёт, существует ли путь |
| Чем подтверждается | `docs/rules/error-handling-policy.md` §«Отказ доступа — тот же контракт, что и прочие ошибки» |
| Факт | |

### B8.6 — Неподдержанный метод и неразбираемое тело

| Поле | Содержание |
|---|---|
| Предусловия | принципал предъявлен; инструмент заведён |
| Вход | `GET /requirements/candles`; затем `POST /requirements/candles` с телом `{` |
| Ожидаемые выходы — все | `405` и `400` соответственно; тело у обоих — **тот же** error-DTO, что у прочих ошибок; строк не заведено |
| Чем подтверждается | `docs/rules/error-handling-policy.md` §«Отказ, произведённый контейнером, — тот же контракт»; долг — `.claude/work/backlog.md` §«Единый error-DTO у поверхностей соседних сервисов» |
| Факт | |

### B8.7 — Отказ доступа площадки наружу — отказ зависимости

| Поле | Содержание |
|---|---|
| Предусловия | стаб отвечает `429` на чтении цен |
| Вход | `GET …/prices` |
| Ожидаемые выходы — все | `503` с `code` = `EXCHANGE_ACCESS_REFUSED`; не `500` и не `400`: читатель не виноват в том, что площадка отказала, и ответ говорит «повтори позже» |
| Чем подтверждается | `GlobalExceptionHandler` javadoc; `docs/rules/error-handling-policy.md`. Код `503` у этой точки `@ApiResponses` не объявлен — находка F-4 |
| Факт | |

### B8.8 — Рядовой отказ чтения площадки наружу

| Поле | Содержание |
|---|---|
| Предусловия | стаб отвечает `500` на чтении цен |
| Вход | `GET …/prices` |
| Ожидаемые выходы — все | `502` с `code` = `EXCHANGE_READ_FAILED`; тело — тот же error-DTO |
| Чем подтверждается | `GlobalExceptionHandler`; `@ApiResponses` точки цен |
| Факт | |

### B8.9 — Тикера на площадке нет — это пустота, а не отказ

| Поле | Содержание |
|---|---|
| Предусловия | стаб отвечает на чтение цен пустым телом |
| Вход | `GET …/prices` |
| Ожидаемые выходы — все | `204` без тела; `502` не отдаётся — отсутствие тикера отказом чтения не является |
| Чем подтверждается | `docs/components/MarketPriceDataService.md` §Поведение; `@ApiResponses` точки цен |
| Факт | |

### B8.10 — Тенанта поверхность не принимает вовсе

| Поле | Содержание |
|---|---|
| Предусловия | принципал предъявлен |
| Вход | вызовы читающих точек с заголовком контекста тенанта и без него |
| Ожидаемые выходы — все | ответы **совпадают**: ни отбор, ни содержимое от заголовка не зависят; третьего звена радиуса у сервиса нет по построению предмета — его данные не тенантные |
| Чем подтверждается | `docs/architecture/contracts.md` §«Контекст тенанта в вызове» («`market-data` не принимает его вовсе»); `docs/architecture/tenant-and-exchange.md` §Инструменты |
| Факт | |

### B8.11 — След отказа доступа

| Поле | Содержание |
|---|---|
| Предусловия | у сервиса есть своя база; таблицы `access_denials` в схеме `market_data` **нет** |
| Вход | вызов закрытой точки без токена (B8.1) |
| Ожидаемые выходы — все | **сегодня**: отказ оставляет запись в журнале приложения; строки в базе нет, потому что таблицы нет. **По дому**: строка заводится до ответа. Кейс описан и **не прогоняется** в части строки |
| Чем подтверждается | `docs/rules/api-access-policy.md` §«След отказа пишет тот, у кого есть база»; `docs/models/domain/other/AccessDenial.md` §Персистентность; `.claude/work/backlog.md` §«Таблица отказов доступа у сервисов со своей базой» |
| Факт | |

### B8.12 — Исходящий токен наружу не выходит

| Поле | Содержание |
|---|---|
| Предусловия | стаб провайдера выдаёт токен служебной идентичности; лог прогона собирается |
| Вход | полный набор троп, ходящих к коннектору (B5.1, B6.5, B8.7) |
| Ожидаемые выходы — все | значение токена не встречается **ни в одном** теле ответа, ни в одной строке лога, ни в одном сообщении отказа; ключей биржевого счёта сервис не запрашивает ни у кого |
| Чем подтверждается | `.claude/rules/codestyle.md` §Логирование; `docs/architecture/services/market-data.md` §«Какие вызовы делает и какие принимает» |
| Факт | |

### B8.13 — Недоступная служебная идентичность — отказ, а не анонимный вызов

| Поле | Содержание |
|---|---|
| Предусловия | стаб провайдера отвечает отказом на выдачу токена |
| Вход | `GET …/prices` |
| Ожидаемые выходы — все | отказ класса чтения с error-DTO; запрос к коннектору **без** заголовка идентичности не уходит: иначе причина отказа на его стороне стала бы неотличима от «ключи отвергнуты» |
| Чем подтверждается | `services/market-data/.../integration/internal/api/ServiceTokenProvider.java` javadoc; `docs/architecture/contracts.md` §«Контекст тенанта в вызове» |
| Факт | |

## B9 — Конфигурация и схема как вход

### B9.1 — Ненастроенный контур доступа не поднимает поверхности

| Поле | Содержание |
|---|---|
| Предусловия | база поднята |
| Вход | контекст с пустым `spring.security.oauth2.resourceserver.jwt.issuer-uri` |
| Ожидаемые выходы — все | контекст **не поднимается**; поверхность не отвечает ни на одной точке — в частности, закрытые точки не становятся открытыми |
| Чем подтверждается | `services/market-data/src/main/resources/application.yaml` (ось объявлена комментарием: незаданное означает отказ); `docs/concept.md` П1 |
| Факт | |

### B9.2 — Незаданный адрес коннектора отказывает на вызове, а не на подъёме

| Поле | Содержание |
|---|---|
| Предусловия | `connector.base-url` пуст; контекст поднят |
| Вход | `GET …/prices`; затем тик синка |
| Ожидаемые выходы — все | чтение отвечает отказом класса границы с error-DTO; тик записывает отказ в журнал и каталога не меняет; ни один запрос не уходит на случайный адрес |
| Чем подтверждается | `application.yaml`; `docs/architecture/services/market-data.md` §«Какие вызовы делает и какие принимает» |
| Факт | |

### B9.3 — Типы инструментов — вход обоих обходов площадки

| Поле | Содержание |
|---|---|
| Предусловия | `connector.instrument-types` = `SWAP,FUTURES` |
| Вход | тик синка, затем проход срезов |
| Ожидаемые выходы — все | листинг читается **по разу на каждый тип**; тикеры и марк-цены — тоже; каталог полон по обоим типам. Настройка говорит, что **зеркалится в каталог**, а не что собирается: сбор определяет требование потребителя |
| Чем подтверждается | `application.yaml` (комментарий оси); `docs/architecture/market-data-collection.md` §«Как потребность доходит до сбора» |
| Факт | |

### B9.4 — Котировочные валюты — вход чтения индексов

| Поле | Содержание |
|---|---|
| Предусловия | `connector.index-quote-currencies` = `USDT,USD` |
| Вход | проход срезов |
| Ожидаемые выходы — все | к стабу ушли **два** запроса индексов — по одному на валюту; индексы обеих карт доступны при заполнении срезов |
| Чем подтверждается | `docs/architecture/market-data-collection.md` §«Невосполнимые срезы» (чтение индексов идёт по валюте, а не по инструменту) |
| Факт | |

### B9.5 — Такт каждого тика приезжает конфигурацией

| Поле | Содержание |
|---|---|
| Предусловия | контекст поднят с заданными выражениями расписания всех пяти тиков |
| Вход | подъём контекста |
| Ожидаемые выходы — все | все пять тиков объявляют такт **выражением из конфигурации**, а не литералом в коде; у каждого есть свой выключатель; число `@Scheduled`-методов дерева сервиса равно пяти |
| Чем подтверждается | `.claude/rules/codestyle.md` §Джобы («период тика — из конфигурации, не хардкод; плюс булев флаг `enabled`») |
| Факт | |

### B9.6 — Схема накатывается миграциями и сверяется с отображением

| Поле | Содержание |
|---|---|
| Предусловия | пустая база в контейнере на образе с расширением временных рядов |
| Вход | подъём контекста |
| Ожидаемые выходы — все | миграции применены; **гипертаблицы созданы** (свечи, срезы книги, срезы цен); отображение сущностей проходит проверку — колонка, объявленная моделью и не заведённая миграцией, роняет подъём, а не обнаруживается в проде |
| Чем подтверждается | `application.yaml` (`ddl-auto: validate`); `docs/architecture/data-ownership.md` §«Временные ряды»; `.claude/rules/codestyle.md` §«Применённая миграция неизменяема — включая комментарии» |
| Факт | |

### B9.7 — Стоковый образ базы прогон не проходит

| Поле | Содержание |
|---|---|
| Предусловия | контейнер поднят на образе **без** расширения временных рядов |
| Вход | подъём контекста |
| Ожидаемые выходы — все | подъём **падает** на миграции гипертаблиц — и падает громко, а не пропускается: тест на неподходящем субстрате не скипается |
| Чем подтверждается | `.claude/decisions/test-contour-design-pass.md` §«2. Субстрат — Testcontainers, образ — тот же, что в `deploy/base`», §«9. Docker — предусловие реактора, а не условие пропуска тестов» |
| Факт | |

### B9.8 — Хранилища секретов сервису не нужно

| Поле | Содержание |
|---|---|
| Предусловия | контейнера Vault в прогоне нет вовсе |
| Вход | подъём контекста и полный набор троп прогона |
| Ожидаемые выходы — все | контекст поднимается; все тропы проходят; ни одна не обращается к хранилищу секретов — ключей биржевого счёта у сервиса нет ни в каком виде |
| Чем подтверждается | `docs/architecture/services/market-data.md` §«Какие вызовы делает и какие принимает»; `docs/architecture/tenant-and-exchange.md` §Ключи |
| Факт | |

## B10 — Отсутствие выходов

### B10.1 — Сервис не публикует событий

| Поле | Содержание |
|---|---|
| Предусловия | контекст поднят; клиента брокера в дереве сервиса нет |
| Вход | полный набор мутирующих троп: все три требования, все пять тиков |
| Ожидаемые выходы — все | **ни одного** сообщения никуда; темы `market-data.facts` не существует; таблицы outbox у сервиса нет вовсе; бинов публикации в контексте нет |
| Чем подтверждается | `docs/architecture/services/market-data.md` §«Что потребляет и что публикует» («событий сегодня нет ни в одну сторону», названный долг и его условие возврата); `.claude/work/backlog.md` §«Темы Kafka по производителям — заводятся шагами сервисов» |
| Факт | |

### B10.2 — Сервис не потребляет событий

| Поле | Содержание |
|---|---|
| Предусловия | контекст поднят |
| Вход | подъём контекста |
| Ожидаемые выходы — все | слушателей брокера в контексте нет ни одного; сервис не значится потребителем определения стратегии и о её активации не узнаёт — требование приезжает синхронной командой |
| Чем подтверждается | `docs/architecture/market-data-collection.md` §«Как потребность доходит до сбора»; `docs/architecture/contracts.md` §События |
| Факт | |

### B10.3 — Исходящие обращения ограничены двумя адресами

| Поле | Содержание |
|---|---|
| Предусловия | подняты стабы соседних поверхностей — ядра, стратегий, периметра, авторизации — и каждый считает запросы |
| Вход | полный набор троп прогона |
| Ожидаемые выходы — все | стабы соседей не получили **ни одного** запроса; исходящие обращения ограничены коннектором и точками провайдера идентичности |
| Чем подтверждается | `docs/architecture/services/market-data.md` §«Какие вызовы делает и какие принимает» («делает — вниз: публичные чтения площадки у коннектора») |
| Факт | |

### B10.4 — Сервис не ходит в чужие базы

| Поле | Содержание |
|---|---|
| Предусловия | поднят только контейнер базы `market_data` |
| Вход | полный набор троп прогона |
| Ожидаемые выходы — все | все тропы проходят при единственной доступной базе; второго источника данных контекст не объявляет; настроек стратегий сервис не читает |
| Чем подтверждается | `docs/architecture/data-ownership.md` §Раскладка; `docs/components/IndicatorJob.md` §«Обходятся идентичности вычисления, а не настройки стратегий» |
| Факт | |

### B10.5 — Ряды не чистятся ни одним тиком

| Поле | Содержание |
|---|---|
| Предусловия | в рядах лежат строки много старше любой объявленной глубины |
| Вход | все пять тиков подряд |
| Ожидаемые выходы — все | **ни одна** строка не удалена — ни невосполнимая, ни восполнимая; чистка сбором не производится, а потребителя глубины ещё нет. Джобы чистки у сервиса нет вовсе, и это **граница**, а не пропуск |
| Чем подтверждается | `docs/processes/snapshot-collection.md` §«Что процесс не делает»; `docs/rules/market-data-retention.md` §«Глубина свечей — величина потребителя, и потребитель назван» («до появления потребителей восполнимое не чистится») |
| Факт | |

### B10.6 — Торговых решений сервис не принимает

| Поле | Содержание |
|---|---|
| Предусловия | посчитаны фичи, дающие «сигнал» по любому разумному правилу |
| Вход | все пять тиков и полный набор чтений |
| Ожидаемые выходы — все | ни заявки, ни сделки, ни команды площадке; к коннектору уходят **только** публичные чтения — ни одного пути приватных операций; статуса стратегий сервис не меняет |
| Чем подтверждается | `docs/architecture/services/market-data.md` §«Чего не делает намеренно»; `docs/rules/market-data-freshness.md` §Правило («джобы рыночных данных статус стратегии не меняют») |
| Факт | |

## Чего кейсы не мерят намеренно

- **Математику индикаторов, классификацию структуры и резолвер фазы.**
  Это предмет уровня 2 (`market-data-indicators` в
  `.claude/decisions/test-contour-design-pass.md` §«Перечень предметов и
  порядок работы»): у калькуляторов и резолверов нет ввода-вывода, и
  таблица «вход → выход» по границам области определения дешевле и полнее
  через ящик. Ящику остаются **оркестрация** расчёта (что по чему считается,
  что записывается, что пропускается) и **прогрев** как наблюдаемый исход
  (B4.3).
- **Ликвидации.** Канала у них нет вовсе: площадка отдаёт их потоком по WS,
  а WS припаркован по грунту
  (`docs/architecture/market-data-collection.md` §«Невосполнимые срезы»).
  Кейса на несуществующий исполнитель не заводится.
- **Открытый интерес и funding.** Класс объявлен восполнимым, требования на
  них никто не предъявил, и механизма сбора у сервиса нет — это граница
  (`docs/architecture/market-data-collection.md` §Восполнимое).
- **Соблюдение лимитов площадки.** Лимиты держит коннектор; стаб их не
  имеет. Ящик мерит **реакцию** на объявленный отказ лимита (B2.4, B3.14,
  B5.7), а не сам лимит.
- **Конфигурация развёртывания.** Переменные окружения манифеста, сетевые
  политики, образ сервиса — предмет развёртывания, а не ящика
  (`.claude/decisions/test-contour-design-pass.md` §«1. Ящик — реальный
  контекст сервиса в JVM теста; подменяется только то, что за границей
  процесса»). Кейсы B9 мерят **чтение** конфигурации процессом, а не её
  доставку.

## Кейсы, не прогоняемые сегодня

| Кейс | Почему | Что сделает прогоняемым |
|---|---|---|
| B8.11 (часть о строке) | таблицы `access_denials` в схеме `market_data` нет | закрытие `.claude/work/backlog.md` §«Таблица отказов доступа у сервисов со своей базой» |
| ребро `CREATED → SYNC` жизненного цикла инструмента | статус `CREATED` не пишет ни одна тропа: заведение из листинга ставит `SYNC` сразу | исход находки F-3 — либо писатель, либо правка охвата lifecycle |
| сжатие и глубина хранения гипертаблиц | политики нет ни в одной миграции | закрытие `.claude/work/backlog.md` §«Политики сжатия и хранения гипертаблиц — по пину образа TimescaleDB» |

Кейсы, чьё ожидание взято из дома, а дерево кода его пока не несёт
(B1.14, B2.10, B3.9, B8.1, B8.6, B8.7), прогоняемы **полностью** — они и
предъявляют названные долги.

## Находки владельцам

Найдены при выводе кейсов; ожиданиями кейсов не подменяются
(`.claude/agents/tester.md` §«Взаимодействие с владельцами предмета»).

| # | Находка | Владелец | Где припаркована |
|---|---|---|---|
| F-1 | доменные модели уходят наружу без api-слоя: `GET …/rules` отдаёт `InstrumentExternalRules`, `GET …/prices` — `MarketPriceData`, и та же доменная форма вложена в связку фич. Цепочка `api → domain` обязательна даже при 1:1, а каждое поле api-модели несёт своё описание — у обеих форм его нет | `code-writer` | `.claude/work/backlog.md` §«Доменные модели уходят наружу с поверхности `market-data`» |
| F-2 | два дома объявляют, что синк валют инструмента не пишет; построенный маппер обновления из листинга их **переписывает** при каждом тике, когда ответ их несёт. Расхождение разрешимо в обе стороны — игнором поля либо правкой обоих домов, — и выбор принадлежит владельцу предмета | `solution-designer` | `.claude/work/backlog.md` §«Клейм о том, что синк валют не пишет, опровергнут маппером обновления из листинга» |
| F-3 | статус `Instrument.Status.CREATED` не пишет ни одна тропа `market-data`: заведение из листинга ставит `SYNC`. Lifecycle при этом материализует ребро `CREATED → SYNC` и описывает `CREATED` как состояние онбординга — ребро не имеет писателя, а статус читается только как кандидат пересчёта готовности | `solution-designer` | `.claude/work/backlog.md` §«Ребро `CREATED → SYNC` жизненного цикла инструмента писателя не имеет» |
| F-4 | `@ApiResponses` чтения цен объявляет `200`, `204` и `502`; отказ доступа либо исчерпанный лимит та же тропа отдаёт кодом `503` — объявленный перечень кодов точки у́же производимого | `code-writer` | `.claude/work/backlog.md` §«Перечень кодов у чтения цен `market-data` у́же производимого» |
| F-5 | действующий листинг отдаётся наружу **без окна**: чтение каталога тянет все строки действующих статусов, тогда как та же популяция внутри сервиса берётся ограниченным окном прохода. Потолок листинга — величина площадки, а не наша | `code-writer` | `.claude/work/backlog.md` §«Чтение действующего листинга `market-data` идёт без окна» |

## Связи

- Форма кейса и три уровня — `.claude/skills/test-design.md`.
- Контур: чем достаются выходы — `.claude/decisions/test-contour-design-pass.md`.
- Концепция шага — `.claude/work/progress/phase-2-step-12-chronicle.md` §«Концепция».
- Роль — `.claude/agents/tester.md`; ревью кейсов — `.claude/skills/test-review.md`.
- Предмет — `docs/architecture/services/market-data.md`,
  `docs/architecture/market-data-collection.md`.
