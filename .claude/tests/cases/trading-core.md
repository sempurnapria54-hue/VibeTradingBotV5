# Тест-кейсы: `trading-core`

## На какой вопрос отвечает этот файл

Какие тест-кейсы у сервиса `trading-core`.

## Предмет

**Чёрный ящик единицы развёртывания `trading-core`** — четвёртый предмет
перечня под-шага 1 `CODE` шага 12 фазы 2
(`.claude/decisions/test-contour-design-pass.md`
§«Перечень предметов и порядок работы»). Новая ось формы у него одна —
**объём**: FSM, команды, риск, холды, outbox. Всё остальное наследует форму,
проверенную на `auth`, `connector-okx` и `market-data`.

Сервис — **единственный писатель принятого риска**: ведёт сделку от решения о
входе до терминала, собирает и отправляет команды площадке через коннектор,
проверяет каждое действие риск-гейтом, поднимает и снимает ступени защиты,
заводит отчёты аномалий и публикует свои факты через outbox
(`docs/architecture/services/trading-core.md` §«Что он делает и зачем»).
Работа идёт **проходами по расписанию**, а не от внешнего запроса. Границы —
§«Чего не делает намеренно» того же дока: у границы кейсов нет, и ниже они не
заводятся.

Уровень у всех кейсов ниже один — `B`; мишень у предмета одна, поэтому
суффикса мишени метки не несут
(`.claude/skills/test-design.md` §«Форма — предмет → кейсы → таблица»).

## Объём — новая ось формы, и режет его единица работы, а не класс

Предмет несёт проходы по расписанию, слушателя темы, поверхность чтения и
ручных операций, статусные машины сделки и транша, исполнителей команд и две
лестницы ступеней — перечни каждого живут у своих домов и здесь не
пересчитываются. Перечислить это кейсами «по ребру» значило бы написать
документ, который никто не прочтёт, и написать его **не про тот уровень**.
Отсюда правило нарезки, действующее по всему документу:

- **единица кейса — одно наблюдаемое следствие одной единицы работы**: тик,
  вызов поверхности, принятое сообщение. Внутренние переходы кейс называет
  только там, где они видны снаружи — статусом в ответе поверхности, строкой
  в базе, вызовом к стабу, опубликованной строкой outbox;
- **рёбра FSM, неравенства риск-гейта, лестница ступеней и арифметика
  сайзинга ящику не принадлежат.** Их предмет — уровень 2
  (`trading-core-fsm`, `trading-core-risk`, `trading-core-safety` в том же
  перечне): у них нет ввода-вывода, и таблица «вход → выход» по границам
  области определения дешевле и полнее. Ящику остаётся **оркестрация**: кто
  кого запускает, в каком порядке, что уходит соседу, что ложится в базу, что
  публикуется — и чего не происходит;
- **кейс формулируется в терминах контракта сервиса**, а не его классов:
  «сделка ушла в ошибочное состояние и получила причину», а не «звено
  `MARK_DEAL_ERROR_COMMAND` вернуло успех».

## Чем достаются выходы

Контур — `.claude/decisions/test-contour-design-pass.md`, здесь не
пересказывается (`.claude/rules/policy-home.md`). Что из него следует
**для этого предмета**:

- **субстрат — контейнер базы и контейнер брокера** (решения 1 и 2). Брокер
  нужен **обеими** сторонами: ядро и публикует через outbox, и потребляет
  тему определений;
- **соседи подменяются WireMock — все, без исключения** (решение 3):
  `neighbours.connector.base-url`, `neighbours.auth.base-url`,
  `neighbours.market-data.base-url` — те же свойства, которыми их подаёт
  манифест окружения;
- **провайдер идентичности — JWKS-стаб** с локальной парой ключей
  (решение 4), и служит он обеим сторонам: проверке входящего токена
  (`issuer-uri`) и выдаче исходящей служебной идентичности
  (`client_credentials` на `token-uri` того же стаба);
- **Vault не поднимается**: ключей биржевого счёта у ядра нет ни в каком
  виде — оно адресует счёт идентичностью, а ключи держит коннектор
  (`docs/architecture/services/trading-core.md`
  §«Чего не делает намеренно»);
- **ассерт по базе идёт через поверхность там, где поверхность есть**
  (решение 7): объявленного читателя вне кода ядра у его таблиц **нет ни
  одной** — сделки, транши, зеркала заявок, outbox, навесы читаются через его
  API либо не читаются никем. Сделки, транши, торговое состояние счёта и
  числа риск-аппетита кейс проверяет поверхностью; строки исполнения, ступени
  пары «счёт, инструмент», outbox, inbox, отчёты аномалий, проекции чужих
  реестров и ставки комиссии поверхности не имеют, и кейс называет ассерт
  **прямым** — это его цена, а не умолчание.

## Тик — вход, и форма его та же, что у третьего предмета

Проходы по расписанию перечислены таблицей ниже, и у каждого из них есть
ручной асинхронный фасад (`domain/jobs/facade/`, `JobController`):

| Тик | Ключ такта | Что делает |
|---|---|---|
| сопровождение сделок | `deal-orchestrator.cron` | проход по нетерминальным сделкам окна |
| отбор входа | `entry-scanner.cron` | ищет вход по активным определениям и создаёт **только** сделку |
| проактивная детекция | `anomaly-job.cron` | сравнивает живые факты биржи со строками ядра |
| реле outbox | `outbox-relay.cron` | публикует накопленное и помечает опубликованное |
| синк проекций | `projection-sync.cron` | реестр счетов у `auth`, каталог у `market-data` |
| объявление потребности | `strategy-demand.cron` | требования рядов и идентичностей у `market-data` |
| синк ставок комиссии | `trade-fee-rate-sync.cron` | приватное чтение ставок у коннектора |

- **расписание в прогоне выключено** (`*.enabled=false`), тик подаёт сам тест
  **ручным фасадом** — он есть у всех семи;
- **фасад асинхронен**, поэтому след тика ждётся `Awaitility` с ограниченным
  таймаутом, а не `Thread.sleep`;
- **возраст данных ставится в данных**: снимок средств с нужным
  `externalUpdatedAt`, строка проекции с нужным моментом снимка, отчёт с
  нужным моментом заведения, движение с нужной биржевой меткой. Часы процесса
  не двигаются ни в одном кейсе;
- **порядок тиков — вход**: часть кейсов подаёт два тика подряд (сканер, затем
  сопровождение) и проверяет, что второй видит следствие первого.

## Ожидание берётся из дома, даже когда сегодня оно не исполнено

Часть кейсов ниже ожидает то, что дом объявляет, а дерево кода ещё не несёт.
Такие ожидания **не ослабляются под текущий факт**: кейс проверяет контракт, а
не сегодняшнее поведение, и его красный прогон и есть предъявление названного
долга. У каждого такого кейса в поле «чем подтверждается» стои́т дом **и**
секция бэклога, в которой долг припаркован:

| Долг | Где припаркован |
|---|---|
| автоконфигурация брокера: бина `KafkaTemplate` нет, `@KafkaListener` не обрабатывается | `.claude/work/backlog.md` §«Тропа к брокеру у построенных соседей не объявлена ни правилом, ни автоконфигурацией» |
| единый error-DTO у отказов контейнера и отказа доступа | `.claude/work/backlog.md` §«Единый error-DTO у поверхностей соседних сервисов» |
| строка `access_denials` у сервиса со своей базой | `.claude/work/backlog.md` §«Таблица отказов доступа у сервисов со своей базой» |
| событие `HoldReleased` на ручном снятии ступени | `.claude/work/backlog.md` §«Класс события `HoldReleased` и его ручная тропа» |
| ступень `ENTRY_BLOCKED` по несвежести ставки комиссии | `.claude/work/backlog.md` §«Ступень `ENTRY_BLOCKED` по несвежести ставки комиссии — писателя нет» |
| читатель журнальных отчётов исхода округления выхода | `.claude/work/backlog.md` §«Журнальные отчёты исходов округления выхода — читателя `exitOutcome` в ядре нет» |
| дедуп вставки — ключом, а не проверкой существования | `.claude/work/backlog.md` §«Дедуп проверкой существования перед вставкой — построенный класс, а не точка» |
| отчёт ручной операции не поглощается отказом журнала | `.claude/work/backlog.md` §«Отчёт ручной операции поглощается вместе с отказом журнала» |
| реджект создания по источнику цены `MARK_PRICE` / `INDEX_PRICE` | `.claude/work/backlog.md` §«Источник цены `MARK_PRICE` / `INDEX_PRICE` не отвергается создáнием» |

## Поверхность предмета

| Операция | Метод и путь | Кто зовёт | Дом ожидания |
|---|---|---|---|
| сделки счёта окном | `GET /api/v1/trading-core/deals` | `bff` | `docs/architecture/contracts.md` §«Синхронные вызовы» |
| сделка со своими траншами | `GET …/deals/{internalId}` | `bff` | `docs/models/domain/aggregate/Deal.md` §Структура |
| торговое состояние счёта | `GET …/safety/states/{exchangeAccountInternalId}` | `bff`, держатель | `docs/rules/manual-halt.md` §«Наблюдаемость: ручное отличимо и от автоматики, и друг от друга» |
| числа риск-аппетита тенанта | `GET …/risk-appetites/{tenantInternalId}` | `bff`, `strategies` | `docs/rules/risk-policy.md` §«Числа назначает держатель; пустое место — отказ» |
| назначение чисел риск-аппетита | `PUT …/risk-appetites/{tenantInternalId}` | `bff` | там же |
| проверка пары «счёт × инструмент» | `GET …/pair-checks` | `strategies` | `docs/rules/strategy-validation.md` |
| ручная постановка ступени | `POST …/safety/halts` | держатель | `docs/rules/manual-halt.md` §«Постановка: режимы» |
| ручное снятие ступени | `POST …/safety/halt-clearances` | держатель | `docs/rules/manual-halt.md` §«Снятие: что во что переходит и при каком предусловии» |
| ручной запуск семи тиков | `POST …/jobs/{тик}` | эксплуатация | `.claude/rules/codestyle.md` §Джобы |
| проба живости | `GET /actuator/health` | развёртывание | `docs/rules/api-access-policy.md` §«Вся поверхность закрыта; открытое — перечислено» |

**Событийная поверхность.** Потребляет — тему `strategies.facts` durable-группой
`trading-core.strategy-facts`, три класса определения. Публикует — через outbox
своей базы в тему `trading-core.facts`: `OrderDecided`, `DealOpened`,
`DealShutdownInitiated`, `DealClosed`, `HoldRaised`, `AnomalyReported`
(`docs/architecture/contracts.md` §События).

**Исходящие вызовы идут по адресам `neighbours.*` и никуда больше:** коннектор (команды площадке и
добыча факта), `auth` (реестр биржевых счетов), `market-data` (фичи, листинг,
биржевые правила, требования рядов и идентичностей).

## B1 — Отбор входа: тик сканера

### B1.1 — Активное определение на свободной паре заводит сделку и её транши

| Поле | Содержание |
|---|---|
| Предусловия | проекция несёт счёт `A1` в рабочем состоянии и инструмент `I1`; копия активного определения на паре `A1 × I1` с входным шагом, чьё условие выполнено; фич стаб `market-data` отдаёт свежими; активных сделок на счёте нет |
| Вход | ручной тик отбора входа |
| Ожидаемые выходы — все | `GET /deals?exchangeAccountInternalId=A1` отдаёт **одну** сделку в `ACTIVE` с причиной заведения «по стратегии»; у неё транши по объявлениям детали, все в `PRECHECK`; к стабу коннектора ушёл **один** вызов — чтение биржевого момента; заявки не создано ни одной; в outbox ровно одна строка класса `DealOpened` |
| Чем подтверждается | `docs/components/EntryScannerJob.md` §«Что делает», §«Что передаёт при создании»; `docs/components/DealOpeningService.md` §«Входная тропа — вход по стратегии»; `docs/architecture/contracts.md` §«У каждого класса события назван писатель, и он же писатель решения» |
| Факт | |

### B1.2 — Заведя сделку, обход счёта прекращается

| Поле | Содержание |
|---|---|
| Предусловия | на счёте `A1` два инструмента с активными определениями, условия выполнены у обоих |
| Вход | один ручной тик отбора входа |
| Ожидаемые выходы — все | заведена **ровно одна** сделка; второй инструмент не тронут; фичи второго инструмента у стаба `market-data` не запрашивались |
| Чем подтверждается | `docs/components/EntryScannerJob.md` §«Гейт входа» (контурная проверка «нет активной сделки ни по одной паре счёта»); `docs/rules/trading-constraints.md` §Контур (торгуется один инструмент на биржевой счёт) |
| Факт | |

### B1.3 — Активная сделка на счёте закрывает вход по всему контуру счёта

| Поле | Содержание |
|---|---|
| Предусловия | на счёте `A1` есть активная сделка по `I1`; по `I2` стои́т активное определение с выполненным условием |
| Вход | ручной тик отбора входа |
| Ожидаемые выходы — все | новой сделки нет; фич по `I2` не запрашивалось; счёт из обхода выпал целиком |
| Чем подтверждается | `docs/rules/trading-constraints.md` §«Гейт открытия сделки»; `docs/components/EntryScannerJob.md` §«Гейт входа» |
| Факт | |

### B1.4 — Ошибочное состояние слот держит

| Поле | Содержание |
|---|---|
| Предусловия | единственная сделка счёта стои́т в `ERROR` |
| Вход | ручной тик отбора входа |
| Ожидаемые выходы — все | новой сделки нет: `ERROR` — активный статус; ответ поверхности по счёту его и показывает |
| Чем подтверждается | `docs/lifecycles/Deal.md` §Группы («ошибочное состояние активно»); `docs/rules/trading-constraints.md` §«Гейт открытия сделки» |
| Факт | |

### B1.5 — Счёт под мягкой ступенью из выборки выпадает

| Поле | Содержание |
|---|---|
| Предусловия | `A1` в `safetyRung = HOLD`; определение активно, условие выполнено, сделок нет |
| Вход | ручной тик отбора входа |
| Ожидаемые выходы — все | сделки нет; к стабу `market-data` обращений по этому счёту нет; торговое состояние счёта поверхностью читается как `HOLD` |
| Чем подтверждается | `docs/rules/exchange-hold.md` §«Ступень 1 — мягкий холд» (счёт выпадает из выборки entry-скана); `docs/components/EntryScannerJob.md` §«Гейт входа» |
| Факт | |

### B1.6 — Пара под стоящей ступенью инструмента пропускается, соседняя нет

| Поле | Содержание |
|---|---|
| Предусловия | на `A1` два инструмента с активными определениями; на паре `A1 × I1` стои́т ступень, на `A1 × I2` — нет |
| Вход | ручной тик отбора входа |
| Ожидаемые выходы — все | сделка заведена по `I2`; по `I1` — нет; торговое состояние счёта перечисляет `I1` среди инструментов со стоящей ступенью |
| Чем подтверждается | `docs/rules/instrument-hold.md` §Enforcement (гейт выборки entry-скана); носитель ступени — таблица пары, `docs/rules/instrument-hold.md` §Правило |
| Факт | |

### B1.7 — Нерезолвенная фаза рынка вход не открывает

| Поле | Содержание |
|---|---|
| Предусловия | стаб `market-data` отдаёт связку фич **без** фазы |
| Вход | ручной тик отбора входа |
| Ожидаемые выходы — все | сделки нет; отказа нет; деталь стратегии не выбрана — пустота консервативна, отбор молчит, а не берёт деталь наугад |
| Чем подтверждается | `EntryScannerJob` javadoc; `docs/rules/market-data-freshness.md` (гейт входа читает свежесть операндом) |
| Факт | |

### B1.8 — Шаг, чьи операнды не покрыты свежей раскладкой, до оценки условия не доходит

| Поле | Содержание |
|---|---|
| Предусловия | стаб отдаёт связку фич, в которой ключа операнда входного шага **нет** (устаревшее и отсутствующее места в раскладке не занимают) |
| Вход | ручной тик отбора входа |
| Ожидаемые выходы — все | сделки нет; условие шага не вычислялось; следующий шаг детали с покрытыми операндами при этом оценивается |
| Чем подтверждается | `docs/rules/market-data-freshness.md`; `docs/components/EntryScannerJob.md` §«Что делает» |
| Факт | |

### B1.9 — Невыполненное условие входа сделки не заводит

| Поле | Содержание |
|---|---|
| Предусловия | фичи свежие и покрывают операнды; условие входного шага ложно |
| Вход | ручной тик отбора входа |
| Ожидаемые выходы — все | сделки нет; строк не заведено ни одной; к коннектору обращений нет |
| Чем подтверждается | `docs/components/EntryScannerJob.md` §«Шаги»; `docs/rules/strategy-condition-contract.md` |
| Факт | |

### B1.10 — Определение не в `ACTIVE` входом не пользуется

| Поле | Содержание |
|---|---|
| Предусловия | копия определения на паре стои́т в `INACTIVE`; условие выполнимо |
| Вход | ручной тик отбора входа |
| Ожидаемые выходы — все | сделки нет; фич по паре не запрашивалось |
| Чем подтверждается | `docs/lifecycles/Strategy.md` §Статусы; `docs/components/EntryScannerJob.md` §«Что делает» |
| Факт | |

### B1.11 — Отказ отбора по одному счёту отбор по остальным не отменяет

| Поле | Содержание |
|---|---|
| Предусловия | два счёта с активными определениями; стаб `market-data` отвечает отказом только на инструменте первого счёта |
| Вход | ручной тик отбора входа |
| Ожидаемые выходы — все | по второму счёту сделка заведена; тик завершился; в журнале — строка об отказе по первому счёту; повторный тик пробует первый счёт снова |
| Чем подтверждается | `EntryScannerJob` javadoc («счета независимы, и общая ветка — не выход из тика»); `docs/rules/runtime-error-classification.md` §«Отказ соседа по ярусу — свой класс, и сделку в ошибку он не уводит» |
| Факт | |

### B1.12 — Окно выборки инструментов ограничено, и упор в него молчалив

| Поле | Содержание |
|---|---|
| Предусловия | в проекции каталога инструментов больше, чем `entry-scanner.instrument-window`; определение стои́т на инструменте **за** окном |
| Вход | ручной тик отбора входа |
| Ожидаемые выходы — все | сделки нет; отказа нет; величина окна читается конфигурацией, и её расширение вход открывает. Кейс предъявляет **названное** ограничение: окно обязано быть шире каталога контура |
| Чем подтверждается | `application.yaml`, ключ `entry-scanner` (комментарий величины); `.claude/rules/codestyle.md` §«Выборка данных: не тянем сущность ради одного поля» |
| Факт | |

### B1.13 — Выключатель гасит и расписание, и ручной тик

| Поле | Содержание |
|---|---|
| Предусловия | `entry-scanner.enabled=false`; условия входа выполнены |
| Вход | ручной тик отбора входа |
| Ожидаемые выходы — все | `202` на фасаде; сделки нет; обращений к соседям нет |
| Чем подтверждается | `.claude/rules/codestyle.md` §Джобы (выключатель гасит запланированный и ручной тик) |
| Факт | |

### B1.14 — Сканер заявок не создаёт и риска не проверяет

| Поле | Содержание |
|---|---|
| Предусловия | те же, что у B1.1 |
| Вход | ручной тик отбора входа |
| Ожидаемые выходы — все | к стабу коннектора **не** уходило ни одной команды размещения; строк исполнения нет; событий класса `OrderDecided` в outbox нет — сканер создаёт **только** сделку |
| Чем подтверждается | `docs/components/EntryScannerJob.md` §Границы |
| Факт | |

## B2 — Проход сопровождения: порядок шагов и перехваты

### B2.1 — Проход берёт нетерминальные сделки окна и терминальных не трогает

| Поле | Содержание |
|---|---|
| Предусловия | на счёте есть сделка в `ACTIVE`, сделка в `EXIT_PENDING`, сделка в `ERROR` и две терминальные (`CLOSED`, `EMERGENCY_CLOSED`) |
| Вход | ручной тик сопровождения |
| Ожидаемые выходы — все | контекст собран у трёх нетерминальных; у терминальных — ни чтения графа, ни вызова к коннектору, ни правки строки: терминальные статусы обработчиков не имеют |
| Чем подтверждается | `docs/components/DealOrchestratorJob.md` §«Цикл прохода» (шаг выборки); `docs/lifecycles/Deal.md` §«Инварианты переходов» |
| Факт | |

### B2.2 — Окно выборки ограничено, не попавшее подбирает следующий тик

| Поле | Содержание |
|---|---|
| Предусловия | нетерминальных сделок больше, чем `deal-orchestrator.batch-size` |
| Вход | два ручных тика подряд |
| Ожидаемые выходы — все | первый тик прошёл ровно `batch-size` сделок, второй — следующие; порядок выборки детерминирован идентификатором, то есть повтор тика на неизменном состоянии даёт тот же набор |
| Чем подтверждается | `application.yaml`, ключ `deal-orchestrator`; `docs/components/DealOrchestratorJob.md` §«Цикл прохода» |
| Факт | |

### B2.3 — Энфорсмент жёсткой ступени стои́т раньше сборки контекста

| Поле | Содержание |
|---|---|
| Предусловия | на счёте стои́т `TRADE_BLOCKED`; активная сделка по паре есть; стаб коннектора отвечает **отказом кредов** на любое чтение |
| Вход | ручной тик сопровождения |
| Ожидаемые выходы — все | сделка уведена в `ERROR` с причиной остановки `EXCHANGE_HOLD`; в outbox — строка `DealShutdownInitiated`; увод **состоялся, несмотря на** отказ добычи: энфорсмент, стоящий после сборки контекста, съедался бы этим отказом |
| Чем подтверждается | `docs/components/DealOrchestratorJob.md` §«Цикл прохода» (порядок шагов); `docs/rules/exchange-hold.md` §«Ступень 2 — сворачивание», п. 5; `docs/lifecycles/Deal.md` §«Причина выхода из штатного ведения» |
| Факт | |

### B2.4 — Жёсткая ступень энфорсится каждым проходом, а не один раз

| Поле | Содержание |
|---|---|
| Предусловия | на счёте стои́т `TRADE_BLOCKED`; сделка заведена **после** подъёма ступени (восстановительной тропой) |
| Вход | ручной тик сопровождения |
| Ожидаемые выходы — все | сделка уведена в `ERROR` этим проходом: каскад подъёма — первый ход энфорсмента, а не весь |
| Чем подтверждается | `docs/rules/error-handling-policy.md` §«Жёсткая ступень энфорсится непрерывно, а не одним ходом»; `docs/components/DealOrchestratorJob.md` §«Цикл прохода» |
| Факт | |

### B2.5 — При обоих стоящих радиусах причиной пишется биржевая

| Поле | Содержание |
|---|---|
| Предусловия | на счёте стои́т `TRADE_BLOCKED` **и** на паре «счёт, инструмент» стои́т `TRADE_BLOCKED`; сделка активна |
| Вход | ручной тик сопровождения |
| Ожидаемые выходы — все | причина остановки — `EXCHANGE_HOLD`, не `RISK_POLICY`: биржевой радиус старше; ровно одна строка `DealShutdownInitiated` |
| Чем подтверждается | `docs/lifecycles/Deal.md` §«Причина выхода из штатного ведения»; исполнимая форма — `docs/spec/hard-rung-shutdown-reason.json` (`hardRungShutdownReason`) |
| Факт | |

### B2.6 — Сделка без стоящей ступени шагом энфорсмента не трогается

| Поле | Содержание |
|---|---|
| Предусловия | ступеней не стои́т ни на одном радиусе сделки |
| Вход | ручной тик сопровождения |
| Ожидаемые выходы — все | статус не меняется шагом энфорсмента; причина остановки пуста; строки `DealShutdownInitiated` нет |
| Чем подтверждается | `docs/components/DealOrchestratorJob.md` §«Цикл прохода»; `docs/lifecycles/Deal.md` §«Причина выхода из штатного ведения» |
| Факт | |

### B2.7 — Повторный проход по уведённой сделке причины не переписывает

| Поле | Содержание |
|---|---|
| Предусловия | сделка уже уведена в `ERROR` с `EXCHANGE_HOLD`; ступень стои́т |
| Вход | два ручных тика сопровождения подряд |
| Ожидаемые выходы — все | вторая строка `DealShutdownInitiated` **не** заводится: ребра у сделки, уже стоящей в `ERROR`, больше нет; поле причины не меняется |
| Чем подтверждается | `docs/lifecycles/Deal.md` §«Причина выхода из штатного ведения» (гард ребра; перезапись — только на ребре из `EXIT_PENDING`) |
| Факт | |

### B2.8 — Перезапись причины на ребре из `EXIT_PENDING` даёт вторую строку журнала

| Поле | Содержание |
|---|---|
| Предусловия | сделка в `EXIT_PENDING` с причиной `STRATEGY_DELETED`; на счёте поднята жёсткая ступень |
| Вход | ручной тик сопровождения |
| Ожидаемые выходы — все | статус — `ERROR`, причина заменена на `EXCHANGE_HOLD`; в outbox **две** строки `DealShutdownInitiated` по одной сделке — событие публикуется на ребре, а не «однажды на сделку» |
| Чем подтверждается | `docs/lifecycles/Deal.md` §«Причина выхода из штатного ведения»; `docs/architecture/contracts.md` §События |
| Факт | |

### B2.9 — Удаление определения уводит сделку в координированный выход

| Поле | Содержание |
|---|---|
| Предусловия | активная сделка с закреплённой деталью; копия определения переведена в `DELETED` событием владельца |
| Вход | ручной тик сопровождения |
| Ожидаемые выходы — все | сделка в `EXIT_PENDING` с причиной `STRATEGY_DELETED`; строка `DealShutdownInitiated` одна; транши идут своим штатным выходом, заявок агрегат сам не снимает |
| Чем подтверждается | `docs/lifecycles/Deal.md` §«Координация траншей», п. 5; §«Причина выхода из штатного ведения» |
| Факт | |

### B2.10 — Ошибочное состояние FSM траншей не гоняет

| Поле | Содержание |
|---|---|
| Предусловия | сделка в `ERROR`, у неё два нетерминальных транша |
| Вход | ручной тик сопровождения |
| Ожидаемые выходы — все | статусы траншей не меняются; команд стратегии не порождается; разрешены только safety, восстановление и добыча фактов — к стабу коннектора уходят чтения, но не команды размещения |
| Чем подтверждается | `docs/processes/fsm-execution-layering.md` §«Кто прогоняет FSM транша — дом ответа»; `docs/components/ErrorHandler.md` |
| Факт | |

### B2.11 — Координированный выход FSM траншей гоняет

| Поле | Содержание |
|---|---|
| Предусловия | сделка в `EXIT_PENDING`, два нетерминальных транша с живой экспозицией |
| Вход | ручные тики сопровождения до исчерпания |
| Ожидаемые выходы — все | каждый транш проходит свой выход и доходит до `CLOSED`; после терминальности всех траншей и доказанного отсутствия живого риска сделка уходит в `CLOSED`; без прогона FSM траншей выходная проверка не наступила бы никогда |
| Чем подтверждается | `docs/processes/fsm-execution-layering.md` §«Кто прогоняет FSM транша — дом ответа»; `docs/components/DealExitPendingHandler.md` |
| Факт | |

### B2.12 — Отказ одной сделки проход по остальным не отменяет

| Поле | Содержание |
|---|---|
| Предусловия | две активные сделки; сборка контекста первой роняется |
| Вход | ручной тик сопровождения |
| Ожидаемые выходы — все | первая уведена в `ERROR` перехватом — **без** радиусной реакции: ступеней не поднято ни на одном радиусе, отчёта аномалии нет; вторая прошла штатно; в журнале — строка об отказе |
| Чем подтверждается | `docs/components/DealOrchestratorJob.md` §«Перехват реакции: выделенный обработчик до общего»; `docs/processes/fsm-execution-layering.md` §«Ребро в `ERROR`: два механизма по природе тропы» |
| Факт | |

### B2.13 — Перехват петлёй причины остановки не пишет

| Поле | Содержание |
|---|---|
| Предусловия | те же, что у B2.12 |
| Вход | ручной тик сопровождения |
| Ожидаемые выходы — все | у уведённой сделки статус `ERROR`, а причина остановки **пуста**: писателя у тропы перехвата нет по построению; строки `DealShutdownInitiated` о ней нет |
| Чем подтверждается | `docs/lifecycles/Deal.md` §«Причина выхода из штатного ведения» (третья клетка перебора рёбер); `docs/processes/fsm-execution-layering.md` §«Ребро в `ERROR`: два механизма по природе тропы» |
| Факт | |

### B2.14 — Отказ ребра присвоения причины в ошибку не перехватывается

| Поле | Содержание |
|---|---|
| Предусловия | на счёте стои́т жёсткая ступень; запись ребра энфорсмента роняется |
| Вход | ручной тик сопровождения |
| Ожидаемые выходы — все | сделка остаётся в прежнем статусе — **не** уводится в `ERROR` без причины; в журнале строка об отказе ребра; следующий тик применяет ребро снова и причину записывает |
| Чем подтверждается | `DealOrchestratorJob` javadoc (несущее исключение ребра остановки); `docs/lifecycles/Deal.md` §«Причина выхода из штатного ведения» |
| Факт | |

### B2.15 — Диспетчеризация прерывается на первом неуспехе

| Поле | Содержание |
|---|---|
| Предусловия | сделка с двумя траншами, у каждого своя команда перехода; стаб коннектора отвечает отказом на команду первого транша |
| Вход | ручной тик сопровождения |
| Ожидаемые выходы — все | команда второго транша в этом проходе **не** отправлялась; переход не применён; статусы обоих траншей прежние; следующий тик подбирает оба |
| Чем подтверждается | `docs/components/DealOrchestratorJob.md` §«Цикл прохода» (шаг диспетчеризации и его названная цена) |
| Факт | |

### B2.16 — Перекрывающий запуск прохода пропускается молча

| Поле | Содержание |
|---|---|
| Предусловия | стаб коннектора отвечает с задержкой, превышающей паузу между двумя запусками |
| Вход | два ручных тика сопровождения подряд |
| Ожидаемые выходы — все | оба ответа `202`; работа выполнена **один** раз — число команд к стабу не удвоено; в журнале строка о пропуске перекрытия. **Дом при этом обещает `409` на отказе блокировки** — см. находку F-2 |
| Чем подтверждается | `.claude/rules/codestyle.md` §Джобы (защита от конкурентного выполнения); `docs/rules/error-handling-policy.md` §«Внешняя поверхность» (форма ответа фасада) |
| Факт | |

### B2.17 — Ревизия живых системных исполнений идёт раз за проход и до обработчика

| Поле | Содержание |
|---|---|
| Предусловия | у сделки есть строка системного исполнения, чьё действие стало неактуальным |
| Вход | ручной тик сопровождения |
| Ожидаемые выходы — все | строка закрыта **до** передачи управления обработчику, своей транзакцией; повторный тик её не трогает. Ассерт прямой по базе: поверхности у строк исполнения нет |
| Чем подтверждается | `docs/components/DealOrchestratorJob.md` §«Цикл прохода» (шаг ревизии); `docs/models/domain/other/DealActionState.md` |
| Факт | |

### B2.18 — Частичное применение перехода наблюдаемо и рёбра траншей не откатывает

| Поле | Содержание |
|---|---|
| Предусловия | переход несёт одобренные рёбра траншей и статусное ребро сделки; запись статусного ребра роняется |
| Вход | ручной тик сопровождения |
| Ожидаемые выходы — все | рёбра траншей применены, статус сделки прежний, факта о нём в outbox нет; в журнале строка на каждом тике, пока причина стои́т; следующий проход считает уже другое состояние графа |
| Чем подтверждается | `docs/components/DealOrchestratorJob.md` §«Цикл прохода» (две записи разными транзакциями, названная цена) |
| Факт | |

## B3 — Команды площадке: жизненный цикл, повторы, классы отказа

### B3.1 — Создание заявки на биржу не ходит и пишет решение с событием

| Поле | Содержание |
|---|---|
| Предусловия | транш в `PRECHECK`, условие входа выполнено, преконтроль разрешил |
| Вход | ручной тик сопровождения |
| Ожидаемые выходы — все | к стабу коннектора команда размещения **не** уходила; локальная заявка заведена с клиентским идентификатором; в outbox — строка `OrderDecided`, лежащая **той же** транзакцией, что и строка заявки |
| Чем подтверждается | `docs/rules/command-lifecycle.md` §Правило (создание на биржу не ходит); `docs/architecture/contracts.md` §«У каждого класса события назван писатель, и он же писатель решения» |
| Факт | |

### B3.2 — Отправка идёт следующим проходом и переводит транш во вход

| Поле | Содержание |
|---|---|
| Предусловия | заявка создана кейсом B3.1; стаб коннектора подтверждает размещение |
| Вход | второй ручной тик сопровождения |
| Ожидаемые выходы — все | к стабу ушла **одна** команда размещения с тем же клиентским идентификатором; транш — в `ENTRY_SUBMITTED`; подтверждение приёма состоянием не считается: открытие экспозиции подтверждённым не объявлено |
| Чем подтверждается | `docs/rules/ack-not-runtime-truth.md` §Правило; `docs/lifecycles/DealTranche.md` §Статусы; `docs/rules/command-lifecycle.md` §Правило |
| Факт | |

### B3.3 — Повторная отправка ищет сущность по клиентскому идентификатору, а не шлёт вторую

| Поле | Содержание |
|---|---|
| Предусловия | отправка состоялась, но ответ до нас не дошёл: стаб отвечает таймаутом и заявку помнит |
| Вход | следующий ручной тик сопровождения |
| Ожидаемые выходы — все | перед повтором к стабу ушёл **поиск** по клиентскому идентификатору; второй заявки на стабе не появилось; факт отправки восстановлен |
| Чем подтверждается | `docs/components/ServiceCommandExecutor.md` §«Общая семантика групп» (группа отправки); `docs/rules/idempotency-via-unique.md` §Правило |
| Факт | |

### B3.4 — Добыча обрывается на первом ответе, несущем искомый факт

| Поле | Содержание |
|---|---|
| Предусловия | стаб коннектора отдаёт заявку уже на чтении живых заявок |
| Вход | ручной тик сопровождения |
| Ожидаемые выходы — все | к стабу ушёл один запрос лестницы, история заявок **не** запрашивалась; строка исполнения завершена |
| Чем подтверждается | `docs/rules/command-lifecycle.md` §Правило (предикат остановки — добытый факт, а не опрошенный эндпоинт) |
| Факт | |

### B3.5 — Недобытый факт звена не завершает и повторяется по бюджету

| Поле | Содержание |
|---|---|
| Предусловия | стаб отдаёт пусто на всех ногах лестницы добычи; предел попыток политики — три |
| Вход | ручные тики сопровождения до исчерпания бюджета |
| Ожидаемые выходы — все | строка исполнения остаётся живой первые два прохода; исход «звено не завершено» отказом **не** является: радиусной реакции нет, ступеней не поднято; на третьем — исчерпание бюджета своим исходом, и сделка уходит штатной ошибочной тропой |
| Чем подтверждается | `docs/components/ServiceCommandExecutor.md` §«Исходов исполнения три, а не два»; `docs/rules/command-lifecycle.md` §Правило |
| Факт | |

### B3.6 — Предел повторов читается живьём из политики, а не со строки

| Поле | Содержание |
|---|---|
| Предусловия | строка исполнения накопила одну попытку при пределе три; предел правится на два перед следующим тиком |
| Вход | ручной тик сопровождения |
| Ожидаемые выходы — все | новый предел действует немедленно: бюджет исчерпан на втором проходе, а не на третьем — авторитет предела у политики, а не у снимка на строке |
| Чем подтверждается | `docs/components/RetryPolicyService.md` §«Авторитет предела — политика, читается живьём»; `application.yaml`, ключ `service-command-retry` |
| Факт | |

### B3.7 — Контролируемое исключение границы поднимает жёсткую биржевую ступень

| Поле | Содержание |
|---|---|
| Предусловия | стаб коннектора отвечает классом контролируемого отказа на добыче заявки |
| Вход | ручной тик сопровождения |
| Ожидаемые выходы — все | счёт — в `TRADE_BLOCKED`; заведён отчёт аномалии; сделка уведена в `ERROR`; отметка исхода на сущности **пережила** бросок — в базе она есть, хотя транзакция звена завершилась отказом |
| Чем подтверждается | `docs/rules/controlled-exchange-exceptions.md` §«Реакция — безусловная биржевая ступень 2»; `docs/components/ServiceCommandExecutor.md` §«Контракт броска» |
| Факт | |

### B3.8 — Отказ источника в наших кредах — свой класс, и повтора он не получает

| Поле | Содержание |
|---|---|
| Предусловия | стаб коннектора отвечает опознанным кодом отказа кредов |
| Вход | ручной тик сопровождения |
| Ожидаемые выходы — все | счёт — в `TRADE_BLOCKED` с кодом причины `EXCHANGE_CREDENTIALS_REJECTED`; повтора команды **нет** ни одного; эскалации «бюджет кончился» этот класс не порождает |
| Чем подтверждается | `docs/rules/runtime-error-classification.md` §«Отказ источника в наших кредах изъят из ретраибельного»; `docs/rules/exchange-hold.md` §«Ступень 2 — сворачивание», п. 5 |
| Факт | |

### B3.9 — Отказ соседа по ярусу сделку в ошибку не уводит

| Поле | Содержание |
|---|---|
| Предусловия | стаб `market-data` отвечает недоступностью на чтении фич |
| Вход | ручной тик сопровождения |
| Ожидаемые выходы — все | сделка остаётся в своём статусе; ступеней не поднято; отчёта аномалии нет; следующий тик повторяет чтение |
| Чем подтверждается | `docs/rules/runtime-error-classification.md` §«Отказ соседа по ярусу — свой класс, и сделку в ошибку он не уводит» |
| Факт | |

### B3.10 — Замещение идёт двумя командами, и порядок ног зависит от класса

| Поле | Содержание |
|---|---|
| Предусловия | транш в сопровождении с живой защитной заявкой; шаг стратегии объявляет замещение защиты |
| Вход | ручные тики сопровождения до исчерпания секвенса |
| Ожидаемые выходы — все | к стабу ушли **новая заявка**, затем — после подтверждения фактом — **отмена старой**; команды «заменить» коннектору не уходило ни одной; старая переведена в отменённые с причиной замещения; у новой стои́т указатель на предшественника |
| Чем подтверждается | `docs/rules/replace-not-amend.md` §«Порядок ног — по риск-классу действия», §«Идентичность и след»; `docs/architecture/contracts.md` §«Синхронные вызовы» |
| Факт | |

### B3.11 — Входное замещение идёт обратным порядком ног

| Поле | Содержание |
|---|---|
| Предусловия | транш с живой входной заявкой; шаг объявляет замещение входа |
| Вход | ручные тики сопровождения |
| Ожидаемые выходы — все | первой ушла **отмена старой**, и только после подтверждения её терминала — новая: перекрытие двойного входного риска исключено |
| Чем подтверждается | `docs/rules/replace-not-amend.md` §«Порядок ног — по риск-классу действия» |
| Факт | |

### B3.12 — Полное закрытие идёт командой закрытия позиции, частичное — reduce-only заявкой

| Поле | Содержание |
|---|---|
| Предусловия | два прогона: (а) выходят все транши сделки; (б) выходит один транш из двух |
| Вход | ручные тики сопровождения |
| Ожидаемые выходы — все | (а) к стабу ушла команда закрытия позиции; (б) команды закрытия позиции **не** уходило — ушла reduce-only заявка размером не больше экспозиции выходящего транша |
| Чем подтверждается | `docs/rules/no-partial-close.md` §Правило, §«Механизм частичного выхода»; `docs/rules/exit-teardown-order.md` §«Два уровня выхода» |
| Факт | |

### B3.13 — Порядок выхода: сперва живые входные заявки, потом позиция

| Поле | Содержание |
|---|---|
| Предусловия | у транша живая входная заявка и ненулевая экспозиция; объявлен выход |
| Вход | ручные тики сопровождения |
| Ожидаемые выходы — все | порядок вызовов к стабу — отмена входной заявки, затем закрытие экспозиции; обратный порядок оставил бы окно долива в момент выхода |
| Чем подтверждается | `docs/rules/exit-teardown-order.md` §«Правило (инвариант системы)» |
| Факт | |

### B3.14 — В окне сворачивания нового риска не берёт ни один транш

| Поле | Содержание |
|---|---|
| Предусловия | сделка в `EXIT_PENDING`; у одного транша объявлен шаг добора |
| Вход | ручной тик сопровождения |
| Ожидаемые выходы — все | добора не исполнено: преконтроль отверг его кодом `RISK_CREATING_UNDER_COLLAPSE`; сделка **не** уведена в `ERROR` — реджект в карв-ауте; выход остальных траншей продолжается |
| Чем подтверждается | `docs/rules/exit-teardown-order.md` §«Окно сворачивания: нового риска не берёт ни один транш»; `docs/processes/risk-evaluation.md` §«Карв-аут исчерпанного бюджета сделки» |
| Факт | |

### B3.15 — Ожидающие команды рестарт не переживают

| Поле | Содержание |
|---|---|
| Предусловия | команда создана, но не отправлена; контекст перезапущен |
| Вход | ручной тик сопровождения после перезапуска |
| Ожидаемые выходы — все | очереди команд в базе нет: FSM пересобрал нужную команду по актуальным фактам графа, контекста и строк исполнения; двойной отправки не произошло |
| Чем подтверждается | `docs/rules/command-lifecycle.md` §Правило; `docs/processes/deal-management.md` §«Статусная механика и recovery» |
| Факт | |

## B4 — Риск-гейт: когда спрашивается и что делает вердикт

### B4.1 — Преконтроль спрашивается у создающего риск действия

| Поле | Содержание |
|---|---|
| Предусловия | транш в `PRECHECK`; числа риск-аппетита тенанта назначены; снимок средств свеж |
| Вход | ручной тик сопровождения |
| Ожидаемые выходы — все | заявка создана; в контекст преконтроля вошли числа тенанта, состояние счёта и ставка комиссии — то есть без ставки, снимка средств и чисел действие не исполняется |
| Чем подтверждается | `docs/rules/risk-validator-scope.md` §Вызывается; `docs/components/RiskValidator.md` §«Что читает» |
| Факт | |

### B4.2 — Преконтроль не спрашивается у добычи, выхода, дочистки и safety

| Поле | Содержание |
|---|---|
| Предусловия | сделка с живой экспозицией; числа риск-аппетита тенанта **не** назначены |
| Вход | ручной тик сопровождения на тропе выхода |
| Ожидаемые выходы — все | выход исполняется: reduce-only выходу преконтроль не нужен, и пустые числа его не гейтят; отказа `RISK_APPETITE_NOT_CONFIGURED` на этой тропе нет |
| Чем подтверждается | `docs/rules/risk-validator-scope.md` §«Не вызывается»; `docs/rules/no-partial-close.md` §«Механизм частичного выхода» |
| Факт | |

### B4.3 — Несвежий снимок средств заказывает добычу, а не отказ

| Поле | Содержание |
|---|---|
| Предусловия | снимок средств старше `deal-context.balance-freshness` (возраст ставится **в данных**) |
| Вход | ручной тик сопровождения |
| Ожидаемые выходы — все | на этой итерации преконтроль не вызывался и заявка не создавалась; к стабу коннектора ушло чтение средств; следующий тик на свежем снимке действие исполняет |
| Чем подтверждается | `docs/processes/risk-evaluation.md` §«Когда вызывается»; `application.yaml`, ключ `deal-context` |
| Факт | |

### B4.4 — Бессрочный вердикт на `PRECHECK` закрывает транш, а не сделку в аварию

| Поле | Содержание |
|---|---|
| Предусловия | транш в `PRECHECK`, живого риска у него нет; преконтроль отвергает действие бессрочным кодом |
| Вход | ручной тик сопровождения |
| Ожидаемые выходы — все | транш — в `CLOSED` с причиной `RISK_CONTROL`; сделка **не** в `ERROR`; когда так закрылись все транши — сделка уходит в терминал с нулём по проверке «операций не было» |
| Чем подтверждается | `docs/processes/risk-evaluation.md` §«Реакция на результат»; `docs/rules/trading-constraints.md` §«Закрытие без входа: ноль как результат проверки» |
| Факт | |

### B4.5 — Временный вердикт действие откладывает, а транш оставляет ждать

| Поле | Содержание |
|---|---|
| Предусловия | транш в `PRECHECK`; преконтроль отвергает действие временным кодом |
| Вход | два ручных тика сопровождения |
| Ожидаемые выходы — все | после первого — транш всё ещё в `PRECHECK`, заявки нет, терминала нет; после снятия причины вторым тиком действие исполняется |
| Чем подтверждается | `docs/processes/risk-evaluation.md` §«Реакция на результат»; `docs/components/RiskBlockResolver.md` §«Карта «вердикт → действие»» |
| Факт | |

### B4.6 — Код карв-аута на стадии с живым риском в аварию не уводит

| Поле | Содержание |
|---|---|
| Предусловия | транш в сопровождении с живой защитой; преконтроль отвергает действие кодом исчерпанного бюджета сделки |
| Вход | ручной тик сопровождения |
| Ожидаемые выходы — все | действие не исполнено; сделка **остаётся** в своём статусе; живой риск не снимается; ступеней не поднято; следующий тик пробует снова |
| Чем подтверждается | `docs/processes/risk-evaluation.md` §«Карв-аут исчерпанного бюджета сделки» |
| Факт | |

### B4.7 — Код вне карв-аута на стадии с живым риском уводит сделку в ошибку

| Поле | Содержание |
|---|---|
| Предусловия | транш в сопровождении; преконтроль отвергает действие кодом рассогласования учёта средств |
| Вход | ручной тик сопровождения |
| Ожидаемые выходы — все | сделка — в `ERROR`; дальше её ведёт аварийная тропа; причина остановки при этом **пуста** — ребро решением обработчика её не пишет |
| Чем подтверждается | `docs/processes/risk-evaluation.md` §«Реакция на результат»; `docs/lifecycles/Deal.md` §«Причина выхода из штатного ведения» |
| Факт | |

### B4.8 — Неполный граф — не вердикт риск-политики

| Поле | Содержание |
|---|---|
| Предусловия | разбивка движений сделки упирается в `deal-context.cash-flow-window-limit` — контекст предъявлен неполным |
| Вход | ручной тик сопровождения |
| Ожидаемые выходы — все | расчёт запрещён; транш **не** уходит в `CLOSED` с причиной `RISK_CONTROL` ни на какой стадии — идёт тропа ошибки сделки: «риск не позволил» и «контекст не загрузился» в данных различимы |
| Чем подтверждается | `docs/processes/risk-evaluation.md` §«Отказ по неполноте графа реакцию не делит со схемой»; `docs/components/DealContextService.md` §«Полнота контекста» |
| Факт | |

### B4.9 — Незаданные числа риск-аппетита отказывают на действии, а не на старте

| Поле | Содержание |
|---|---|
| Предусловия | строка чисел риск-аппетита тенанта заведена пустой |
| Вход | подъём контекста, затем ручной тик сопровождения на входном действии |
| Ожидаемые выходы — все | контекст поднялся — стартового обхода чисел нет вовсе; действие отвергнуто кодом `RISK_APPETITE_NOT_CONFIGURED`; сделка **не** в `ERROR` — код в карв-ауте; отказ называет, какое именно число пусто |
| Чем подтверждается | `docs/rules/risk-policy.md` §«Числа назначает держатель; пустое место — отказ»; `docs/processes/risk-evaluation.md` §«Карв-аут исчерпанного бюджета сделки» |
| Факт | |

### B4.10 — Стоящая мягкая ступень пары блокирует создание риска, но не фиксацию прибыли

| Поле | Содержание |
|---|---|
| Предусловия | на паре «счёт, инструмент» стои́т `ENTRY_BLOCKED`; у транша живая экспозиция; два прогона — действие добора и постановка уровня фиксации прибыли |
| Вход | ручные тики сопровождения |
| Ожидаемые выходы — все | добор отвергнут кодом `INSTRUMENT_SAFETY_HOLD`, сделка остаётся в статусе; постановка уровня фиксации прибыли **проходит** — она риска не создаёт и контроля не ослабляет |
| Чем подтверждается | `docs/rules/instrument-hold.md` §Enforcement; `docs/rules/risk-validator-scope.md` §«Граница общая с блок-сетом холдов» |
| Факт | |

### B4.11 — Заблокированное действие шага останавливает остальные действия того же шага

| Поле | Содержание |
|---|---|
| Предусловия | шаг несёт два действия, первое — создающее риск и заблокированное |
| Вход | ручной тик сопровождения |
| Ожидаемые выходы — все | второе действие шага не исполнялось; команд к стабу нет; шаг остаётся неисполненным до следующего прохода |
| Чем подтверждается | `docs/processes/risk-evaluation.md` §«Несколько actions в одном StrategyStep» |
| Факт | |

### B4.12 — За проход исполняется одно действие пакета шага

| Поле | Содержание |
|---|---|
| Предусловия | шаг несёт три исполнимых действия, все разрешены |
| Вход | три ручных тика сопровождения подряд |
| Ожидаемые выходы — все | на каждом тике к стабу уходит **одна** команда; порядок — устанавливающие защиту раньше снимающих; после третьего пакет исчерпан |
| Чем подтверждается | `docs/processes/fsm-execution-layering.md` §«Порядок выбора действия в пакете шага»; `docs/rules/live-risk-protection.md` §Правило |
| Факт | |

### B4.13 — Отказ расчёта по стороне уровня — отказ шага, не авария

| Поле | Содержание |
|---|---|
| Предусловия | рыночная структура даёт уровень остановки на прибыльной стороне; транш в сопровождении |
| Вход | ручной тик сопровождения |
| Ожидаемые выходы — все | шаг не исполнен; транш и сделка остаются в своих статусах; ступеней не поднято; повтор приходит следующим проходом |
| Чем подтверждается | `docs/processes/risk-evaluation.md` §«Отказ расчёта по стороне уровня — отказ шага, не авария» |
| Факт | |

## B5 — Ступени защиты: автоматика, каскад, снятие риска

### B5.1 — Порядок полной реакции: статус, отчёт, снятие риска, терминал отчёта, каскад

| Поле | Содержание |
|---|---|
| Предусловия | счёт в рабочем состоянии, активная сделка с живой экспозицией и живой защитой; стаб коннектора подтверждает закрытие |
| Вход | тик проактивной детекции, нашедший признак жёсткой биржевой ступени |
| Ожидаемые выходы — все | счёт — в `TRADE_BLOCKED`; в outbox строка `HoldRaised`, лежащая **той же** транзакцией, что и перестановка ступени; отчёт заведён **до** снятия риска и несёт снимок «до»; к стабу ушли отмены живых заявок и закрытие позиции; отчёт завершён снимком «после»; активная сделка уведена в `ERROR` с причиной `EXCHANGE_HOLD` |
| Чем подтверждается | `docs/components/SafetyHoldCoordinator.md` §Последовательность; `docs/rules/error-handling-policy.md` §«Идемпотентность реакции и идемпотентность отчёта — разные ключи» |
| Факт | |

### B5.2 — Защита снимается последней и только после подтверждённого закрытия позиции

| Поле | Содержание |
|---|---|
| Предусловия | у сделки живая входная заявка, живая позиция и живая условная заявка защиты |
| Вход | подъём жёсткой ступени |
| Ожидаемые выходы — все | порядок вызовов к стабу: отмена входных заявок → закрытие позиции → подтверждение факта → снятие защиты; позиция не оголяется ни на одном шаге |
| Чем подтверждается | `docs/components/KillSwitchExecutor.md` §Порядок; `docs/rules/exit-teardown-order.md` §«Правило (инвариант системы)» |
| Факт | |

### B5.3 — Встроенная защита родителя снимается наравне с отдельной

| Поле | Содержание |
|---|---|
| Предусловия | родительская заявка терминальна с непустым наливом; её встроенная защита жива на стабе отдельной условной заявкой |
| Вход | подъём жёсткой ступени |
| Ожидаемые выходы — все | к стабу ушло снятие **и** отдельной условной заявки, **и** материализованной встроенной; после хода живых сущностей радиуса на стабе не осталось; предикат «риск снят» живую встроенную защиту не пропускает |
| Чем подтверждается | `docs/components/KillSwitchExecutor.md` §Порядок, §Подтверждение |
| Факт | |

### B5.4 — Неподтверждённое снятие риска повторяется ограниченно и кончается отказом

| Поле | Содержание |
|---|---|
| Предусловия | стаб коннектора принимает закрытие, но позицию продолжает отдавать живой; `kill-switch.max-teardown-attempts = 3` |
| Вход | подъём жёсткой ступени и последующие тики |
| Ожидаемые выходы — все | попыток ровно три; после исчерпания счёт остаётся в `TRADE_BLOCKED`, отчёт — **незакрытым**; автоматических повторов сверх предела нет; выход даёт только доведение по вызову держателя |
| Чем подтверждается | `docs/components/KillSwitchExecutor.md` §Подтверждение; `application.yaml`, ключ `kill-switch` |
| Факт | |

### B5.5 — Повторный сигнал по стоящей ступени реакции не гоняет

| Поле | Содержание |
|---|---|
| Предусловия | счёт уже в `TRADE_BLOCKED`; детектор находит тот же признак снова |
| Вход | второй тик проактивной детекции |
| Ожидаемые выходы — все | статус не переставляется; снятия риска и каскада нет; строки `HoldRaised` второй нет — событие лежит в транзакции перестановки, а её не было |
| Чем подтверждается | `docs/rules/error-handling-policy.md` §«Идемпотентность реакции и идемпотентность отчёта — разные ключи»; `docs/rules/exchange-hold.md` §«Границы и эскалация» |
| Факт | |

### B5.6 — Поглощение реакции отчёт не гасит, а второе основание заводит свою строку

| Поле | Содержание |
|---|---|
| Предусловия | счёт в `TRADE_BLOCKED` по одному коду; детектор находит признак с **другим** машинным кодом в том же окне наблюдения |
| Вход | тик проактивной детекции |
| Ожидаемые выходы — все | статус не переставлен, реакции нет; отчёт по **новому** коду заведён — он журнальный, снятия риска у него нет; второй отчёт по **тому же** коду в окне не заводится |
| Чем подтверждается | `docs/rules/error-handling-policy.md` §«Идемпотентность реакции и идемпотентность отчёта — разные ключи»; `docs/models/domain/other/AnomalyReport.md` §Инварианты |
| Факт | |

### B5.7 — Мягкий запрос на объекте под сворачиванием поглощается

| Поле | Содержание |
|---|---|
| Предусловия | счёт в `TRADE_BLOCKED` |
| Вход | автоматический сигнал мягкой ступени того же радиуса (расхождение сверки) |
| Ожидаемые выходы — все | статус остаётся `TRADE_BLOCKED` — понижения нет; реакции нет; понижение делает только снятие |
| Чем подтверждается | `docs/rules/exchange-hold.md` §«Границы и эскалация» |
| Факт | |

### B5.8 — Эскалация с мягкой на жёсткую реакцию не пропускает

| Поле | Содержание |
|---|---|
| Предусловия | счёт в `HOLD` |
| Вход | сигнал жёсткой ступени того же радиуса |
| Ожидаемые выходы — все | счёт — в `TRADE_BLOCKED`; полный состав реакции отработал: отчёт, снятие риска, каскад; мягкая ступень анкером идемпотентности не является |
| Чем подтверждается | `docs/rules/exchange-hold.md` §«Границы и эскалация» |
| Факт | |

### B5.9 — Инструментный сигнал на счёте под биржевой ступенью ничего не делает

| Поле | Содержание |
|---|---|
| Предусловия | счёт в `TRADE_BLOCKED`; детектор находит признак инструментного радиуса |
| Вход | тик проактивной детекции |
| Ожидаемые выходы — все | ступени пары не переставляются; каскада и снятия риска нет: биржевые ступени доминируют инструментные реакции |
| Чем подтверждается | `docs/rules/exchange-hold.md` §«Границы и эскалация»; `docs/rules/instrument-hold.md` §Enforcement |
| Факт | |

### B5.10 — Мягкая ступень живых сделок не трогает

| Поле | Содержание |
|---|---|
| Предусловия | активная сделка с живой экспозицией |
| Вход | подъём мягкой ступени счёта |
| Ожидаемые выходы — все | сделка остаётся в `ACTIVE`; ремодел защиты, трейлинг и выход продолжают исполняться; новых сделок сканер не создаёт; заморозки уровня остановки не произошло |
| Чем подтверждается | `docs/rules/exchange-hold.md` §«Ступень 1 — мягкий холд» |
| Факт | |

### B5.11 — Серия убыточных закрытий доводит счёт до мягкой ступени

| Поле | Содержание |
|---|---|
| Предусловия | предел серии назначен числом риск-аппетита тенанта; на счёте закрываются подряд убыточные сделки |
| Вход | ручные тики сопровождения до терминалов |
| Ожидаемые выходы — все | на сделке, доводящей счёт до предела: счёт — в `HOLD`, заведён отчёт с кодом `LOSS_STREAK_LIMIT_REACHED`, в outbox — `HoldRaised`; живой риск **не** снимается; прибыльное закрытие до предела счётчик обнуляет, нулевой результат его не двигает, недоступный — тоже |
| Чем подтверждается | `docs/rules/loss-streak-halt.md` §Правило, §«Что двигает счётчик», §«Чем выражается остановка» |
| Факт | |

### B5.12 — Снятие ступени счётчик серии не обнуляет

| Поле | Содержание |
|---|---|
| Предусловия | счёт в `HOLD` по достигнутому пределу серии |
| Вход | ручное снятие мягкой ступени, затем ещё одно убыточное закрытие |
| Ожидаемые выходы — все | после снятия счёт в рабочем состоянии; следующее убыточное закрытие снова доводит до предела — счётчик снятием не тронут |
| Чем подтверждается | `docs/rules/loss-streak-halt.md` §Носители; `docs/rules/exchange-hold.md` §«Ступень 1 — мягкий холд» |
| Факт | |

### B5.13 — Расхождение сверки в разведочном режиме лестницу не триггерит

| Поле | Содержание |
|---|---|
| Предусловия | `exchange-contour.exchanges.OKX.reconciliation-exploratory = true`; терминал сделки даёт расхождение сверх допуска |
| Вход | ручной тик сопровождения до терминала |
| Ожидаемые выходы — все | признак расхождения на сделке проставлен, отчёт заведён; счёт **остаётся** в рабочем состоянии; при выключенном разведочном режиме тот же вход даёт `HOLD` с кодом `PNL_RECONCILIATION_MISMATCH` |
| Чем подтверждается | `docs/rules/pnl-reconciliation.md` §«Разведочный режим допуска», §«Реакция на расхождение»; `application.yaml`, ключ `exchange-contour` |
| Факт | |

### B5.14 — Отказ журнального носителя реакцию не отменяет

| Поле | Содержание |
|---|---|
| Предусловия | запись отчёта аномалии роняется |
| Вход | сигнал жёсткой ступени |
| Ожидаемые выходы — все | ступень поднята, риск снят, каскад отработал; в журнале приложения — строка об отказе записи отчёта; реакция от отказа наблюдателя не зависит |
| Чем подтверждается | `docs/rules/error-handling-policy.md` §«Отказ журнального носителя реакцию не гейтит» |
| Факт | |

### B5.15 — Kill-switch петле не подчинён

| Поле | Содержание |
|---|---|
| Предусловия | тик сопровождения выключен (`deal-orchestrator.enabled=false`); активная сделка с живым риском |
| Вход | подъём жёсткой ступени ручной постановкой |
| Ожидаемые выходы — все | снятие риска состоялось: аварийный исполнитель доводит свой ход сам и от петли не зависит; каскад сделок при этом ждёт своего тика |
| Чем подтверждается | `docs/processes/fsm-execution-layering.md` §«Kill-switch стоит вне слоёв»; `docs/rules/execution-hierarchy.md` |
| Факт | |

## B6 — Ручная поверхность остановки

### B6.1 — Полная постановка асинхронна, мягкая синхронна

| Поле | Содержание |
|---|---|
| Предусловия | счёт в рабочем состоянии |
| Вход | два вызова: `POST /safety/halts` с полным классом и он же с мягким |
| Ожидаемые выходы — все | полный — `202` (ход пошёл фасадом и гоняет снятие риска с повторами); мягкий — `204`; формы разведены счётно: мягкая постановка **может отказать**, а асинхронный отказ невидим вызывающему |
| Чем подтверждается | `SafetyController` javadoc; `docs/rules/error-handling-policy.md` §«Внешняя поверхность» (форма ответа фасада) |
| Факт | |

### B6.2 — Четыре допустимые пары «класс × радиус», прочие — отказ при запуске

| Поле | Содержание |
|---|---|
| Предусловия | счёт и инструмент существуют |
| Вход | четыре законных вызова (полный на счёт, полный на пару, мягкий на пару, мягкий на счёт-класс) плюс два негодных: мягкий инструментный класс **без** инструмента и мягкий биржевой класс **с** инструментом |
| Ожидаемые выходы — все | законные применены; негодные — `400` с error-DTO, ступени не тронуты, отчёта нет: перечень пар задан лестницами, а не поверхностью |
| Чем подтверждается | `docs/rules/manual-halt.md` §«Радиус — оба, и выбирается вызовом»; `ManualHaltService` javadoc |
| Факт | |

### B6.3 — Неизвестный класс вмешательства — негодный вход вызова

| Поле | Содержание |
|---|---|
| Предусловия | контекст поднят |
| Вход | `POST /safety/halts` со значением класса вне перечня |
| Ожидаемые выходы — все | `400` с тем же error-DTO, что у прочих негодных входов; ступени не тронуты |
| Чем подтверждается | `SafetyController` javadoc (перевод api → domain делает контроллер); `docs/rules/error-handling-policy.md` §«Отказ, произведённый контейнером, — тот же контракт» |
| Факт | |

### B6.4 — Ручная постановка идёт тем же механизмом и тем же составом реакции

| Поле | Содержание |
|---|---|
| Предусловия | счёт в рабочем состоянии, активная сделка с живым риском |
| Вход | `POST /safety/halts`, полный класс, радиус счёта |
| Ожидаемые выходы — все | тот же состав, что у автоматики: `TRADE_BLOCKED`, `HoldRaised` в outbox, отчёт с кодом `MANUAL_HALT_REQUESTED`, снятие живого риска, каскад сделок в `ERROR`; второго механизма остановки не наблюдается |
| Чем подтверждается | `docs/rules/manual-halt.md` §Правило, §«Постановка: режимы»; `docs/rules/exchange-hold.md` §«Состав реакции» |
| Факт | |

### B6.5 — Мягкая постановка отказывает по статусу, полная — нет

| Поле | Содержание |
|---|---|
| Предусловия | счёт стои́т в `TRADE_BLOCKED` |
| Вход | мягкая постановка на этом счёте; затем полная на нём же |
| Ожидаемые выходы — все | мягкая — отказ при запуске (`400`), ступень не понижена; полная — принята: множество входа жёсткой ступени — любое состояние, авария застаёт объект в любом |
| Чем подтверждается | `docs/rules/exchange-hold.md` §«Границы и эскалация»; `ManualHaltService` javadoc |
| Факт | |

### B6.6 — Снятие называет ступень явно, и повтор попадает в холостой ход

| Поле | Содержание |
|---|---|
| Предусловия | счёт в `HOLD` |
| Вход | два одинаковых вызова `POST /safety/halt-clearances` с мягким классом |
| Ожидаемые выходы — все | оба `204`; после первого счёт в рабочем состоянии; второй — холостой: строки журнала он не заводит, по лестнице дальше не шагает. Без явной ступени двойное нажатие вернуло бы торговлю |
| Чем подтверждается | `docs/rules/manual-halt.md` §«Идемпотентность наследуется, а не обходится»; `SafetyController` javadoc |
| Факт | |

### B6.7 — Прыжка через ступень нет

| Поле | Содержание |
|---|---|
| Предусловия | счёт в `TRADE_BLOCKED` |
| Вход | `POST /safety/halt-clearances` с **мягким** классом |
| Ожидаемые выходы — все | `400`: снимать мягкую нечего, пока над ней стои́т жёсткая; статус не изменился |
| Чем подтверждается | `docs/rules/manual-halt.md` §«Снятие: что во что переходит и при каком предусловии» |
| Факт | |

### B6.8 — Снятие сворачивания ведёт в мягкую ступень, а не в рабочее состояние

| Поле | Содержание |
|---|---|
| Предусловия | счёт в `TRADE_BLOCKED`, живого риска на радиусе нет |
| Вход | `POST /safety/halt-clearances` с полным классом |
| Ожидаемые выходы — все | `204`; торговое состояние счёта читается поверхностью как `HOLD`, **не** как рабочее: две ступени — два хода |
| Чем подтверждается | `docs/rules/manual-halt.md` §«Снятие: что во что переходит и при каком предусловии»; `docs/rules/exchange-hold.md` §«Снятие — вручную и только в `HOLD`» |
| Факт | |

### B6.9 — Непогашенный живой риск снятие сворачивания отвергает

| Поле | Содержание |
|---|---|
| Предусловия | счёт в `TRADE_BLOCKED`; на стабе жива позиция по инструменту сделки счёта |
| Вход | `POST /safety/halt-clearances` с полным классом |
| Ожидаемые выходы — все | `400`; ступень стои́т; предусловие машинное, а не заявляемое: снятие поверх непогашенного риска вернуло бы вход в торговлю |
| Чем подтверждается | `docs/rules/manual-halt.md` §«Снятие: что во что переходит и при каком предусловии»; `docs/rules/api-access-policy.md` (раздел об энфорсере инварианта ручного снятия ступени) |
| Факт | |

### B6.10 — Выборка предусловия берёт и терминальные сделки недавним окном

| Поле | Содержание |
|---|---|
| Предусловия | счёт в `TRADE_BLOCKED`; нетерминальных сделок нет вовсе; на **терминальной** сделке недавнего окна остался живой хвост на стабе |
| Вход | `POST /safety/halt-clearances` с полным классом |
| Ожидаемые выходы — все | `400`: выборка только по нетерминальным была бы пуста ровно на мотивирующей тропе. Кейс-спутник: хвост на сделке, закрытой **раньше** окна, предусловием не виден, и снятие проходит — это названное ограничение, а не дефект |
| Чем подтверждается | `docs/rules/manual-halt.md` §«Снятие: что во что переходит и при каком предусловии» |
| Факт | |

### B6.11 — Повторный полный вызов гоняет снятие риска заново

| Поле | Содержание |
|---|---|
| Предусловия | счёт в `TRADE_BLOCKED`, снятие риска не подтвердилось, отчёт не закрыт |
| Вход | `POST /safety/halts` с полным классом на том же счёте |
| Ожидаемые выходы — все | `202`; снятие риска гоняется снова — анкер это **не** поглощает: право на доведение недоделанного есть только у явного вызова держателя; строки `HoldRaised` при этом нет — перехода не было |
| Чем подтверждается | `docs/rules/manual-halt.md` §«Идемпотентность наследуется, а не обходится»; `ManualHaltService` javadoc; `docs/components/SafetyHoldCoordinator.md` §Последовательность |
| Факт | |

### B6.12 — Автоматический сигнал доведения не получает

| Поле | Содержание |
|---|---|
| Предусловия | те же, что у B6.11 |
| Вход | тик проактивной детекции, находящий тот же признак |
| Ожидаемые выходы — все | снятие риска **не** гоняется: автоматический сигнал поглощается анкером, иначе бюджет попыток перестал бы что-либо ограничивать |
| Чем подтверждается | `docs/components/KillSwitchExecutor.md` §Подтверждение; `docs/components/SafetyHoldCoordinator.md` §Последовательность |
| Факт | |

### B6.13 — Ручное снятие на биржу не ходит и события не производит

| Поле | Содержание |
|---|---|
| Предусловия | счёт в `HOLD` |
| Вход | `POST /safety/halt-clearances` с мягким классом |
| Ожидаемые выходы — все | к стабу коннектора обращений нет; в журнале — строка о снятии. **По дому** факт снятия ступени обязан уезжать классом `HoldReleased`; сегодня писателя у класса нет, и строки в outbox не появляется — кейс предъявляет названный долг |
| Чем подтверждается | `docs/architecture/contracts.md` §События (клетка `HoldReleased`); `.claude/work/backlog.md` §«Класс события `HoldReleased` и его ручная тропа» |
| Факт | |

## B7 — Проактивная детекция: что ищет и чем отвечает

### B7.1 — Активная позиция без объясняющей сделки заводит сделку восстановлением

| Поле | Содержание |
|---|---|
| Предусловия | на стабе жива позиция по управляемому инструменту; активной сделки по паре нет |
| Вход | ручной тик проактивной детекции |
| Ожидаемые выходы — все | заведена сделка с причиной заведения «восстановление», **без** закреплённой детали, с одним траншем сразу в сопровождении; в outbox — `DealOpened`; ступеней не поднято — это не чужая сущность, а принятие в модель |
| Чем подтверждается | `docs/components/AnomalyJob.md` §«Что ищет» (A1); `docs/components/DealOpeningService.md` §«Восстановительная тропа — сделка вокруг уже живого риска» |
| Факт | |

### B7.2 — Живой риск по инструменту вне контура сворачивает счёт

| Поле | Содержание |
|---|---|
| Предусловия | на стабе жива позиция по инструменту, строки которого в каталоге ядра нет вовсе |
| Вход | ручной тик проактивной детекции |
| Ожидаемые выходы — все | счёт — в `TRADE_BLOCKED`; отчёт с кодом `EXCHANGE_FOREIGN_INSTRUMENT_RISK`; полный состав реакции отработал |
| Чем подтверждается | `docs/components/AnomalyJob.md` §«Что ищет» (A2); `docs/rules/exchange-hold.md` §«Ступень 2 — сворачивание», п. 3 |
| Факт | |

### B7.3 — Чужая заявка опознаётся нашим маркером, а не отсутствием строки

| Поле | Содержание |
|---|---|
| Предусловия | два прогона: (а) на стабе жива заявка **без** нашего клиентского маркера; (б) жива заявка **с** маркером, строки которой в базе нет |
| Вход | ручной тик проактивной детекции |
| Ожидаемые выходы — все | (а) счёт — в `TRADE_BLOCKED`, отчёт с кодом `EXCHANGE_FOREIGN_ORDER`; (б) ступени **не** поднимаются: наша заявка, пережившая рестарт между отправкой и коммитом, чужой не объявляется |
| Чем подтверждается | `docs/components/AnomalyJob.md` §«Что ищет» (A7 и разбор дискриминатора) |
| Факт | |

### B7.4 — Больше одной позиции на инструмент — нарушение режима счёта

| Поле | Содержание |
|---|---|
| Предусловия | стаб отдаёт две позиции по одному инструменту |
| Вход | ручной тик проактивной детекции |
| Ожидаемые выходы — все | счёт — в `TRADE_BLOCKED`; отчёт с кодом `EXCHANGE_POSITION_MODE_VIOLATION` |
| Чем подтверждается | `docs/components/AnomalyJob.md` §«Что ищет» (A3); `docs/models/domain/core/Position.md` |
| Факт | |

### B7.5 — Хвосты заявок без позиции дают мягкую ступень пары

| Поле | Содержание |
|---|---|
| Предусловия | по инструменту позиций на стабе нет, живые заявки есть; активной сделки по паре нет |
| Вход | ручной тик проактивной детекции |
| Ожидаемые выходы — все | на паре «счёт, инструмент» — `ENTRY_BLOCKED`; отчёт с кодом `INSTRUMENT_ORPHAN_ORDERS`; живой риск **не** снимается — снимать нечего |
| Чем подтверждается | `docs/components/AnomalyJob.md` §«Что ищет» (A9); `docs/rules/instrument-hold.md` §Триггеры |
| Факт | |

### B7.6 — Локально терминальная сущность, живая на бирже, даёт только отчёт

| Поле | Содержание |
|---|---|
| Предусловия | заявка в базе терминальна, на стабе жива |
| Вход | ручной тик проактивной детекции |
| Ожидаемые выходы — все | отчёт с кодом `LOCAL_TERMINAL_ALIVE_ON_EXCHANGE`; ступеней не поднято ни на одном радиусе; блокировки нет |
| Чем подтверждается | `docs/components/AnomalyJob.md` §«Что ищет» (A8); `docs/rules/error-handling-policy.md` §«Перечень реакций» |
| Факт | |

### B7.7 — Расхождение суммы экспозиций с нетто-размером сворачивает счёт

| Поле | Содержание |
|---|---|
| Предусловия | сумма экспозиций траншей не сходится с нетто-размером живого эпизода на стабе |
| Вход | ручной тик проактивной детекции |
| Ожидаемые выходы — все | счёт — в `TRADE_BLOCKED`; отчёт с кодом `EXCHANGE_EXPOSURE_MISMATCH`; живой риск снимается: он есть, но не приписан ни одному траншу |
| Чем подтверждается | `docs/components/AnomalyJob.md` §«Что ищет» (A11); `docs/models/domain/aggregate/Deal.md` §«Экспозиция сделки и сверка с биржей» |
| Факт | |

### B7.8 — Неполный срез заставляет прочие детекторы молчать

| Поле | Содержание |
|---|---|
| Предусловия | стаб отдаёт два счёт-широких среза из трёх, на третьем — отказ; в базе при этом стои́т состояние, на котором сработал бы детектор чужой сущности |
| Вход | ручной тик проактивной детекции |
| Ожидаемые выходы — все | ступеней по прочим детекторам **не** поднято: на неполном срезе расхождение производит сама неполнота; заведён отчёт с кодом `ANOMALY_PASS_INCOMPLETE`; счёт слепоты увеличен на единицу |
| Чем подтверждается | `docs/components/AnomalyJob.md` §«Гейт полноты среза» |
| Факт | |

### B7.9 — Серия ненаблюдённых проходов поднимает мягкую ступень счёта

| Поле | Содержание |
|---|---|
| Предусловия | `anomaly-job.blind-pass-limit = 3`; стаб отказывает на одном из срезов |
| Вход | три ручных тика проактивной детекции подряд |
| Ожидаемые выходы — все | после третьего счёт — в `HOLD` с кодом `ANOMALY_PASS_INCOMPLETE`; ступень **мягкая**: биржевая защита стои́т и исполняется, снимать нечего; наблюдённый проход до предела счёт обнуляет |
| Чем подтверждается | `docs/components/AnomalyJob.md` §«Гейт полноты среза»; `docs/rules/exchange-hold.md` §«Ступень 1 — мягкий холд» |
| Факт | |

### B7.10 — Счёт слепоты двигают только состоявшиеся проходы

| Поле | Содержание |
|---|---|
| Предусловия | `anomaly-job.enabled=false`; счёт слепоты равен нулю |
| Вход | три интервала, в которые тик не исполнялся вовсе |
| Ожидаемые выходы — все | счёт слепоты остался нулевым; ступень не поднята: молчание читается как «наблюдение идёт, аномалий нет». Кейс предъявляет **названное** ограничение предела, а не дефект |
| Чем подтверждается | `docs/components/AnomalyJob.md` §«Гейт полноты среза»; `.claude/work/backlog.md` §«Слепота считается состоявшимися проходами, а тропа входа её не спрашивает» |
| Факт | |

### B7.11 — Свежая строка подтверждением признака не служит

| Поле | Содержание |
|---|---|
| Предусловия | `anomaly-job.confirmation-min-age = 30s`; стоящая строка заведена секундой раньше (момент ставится **в данных**) |
| Вход | ручной тик проактивной детекции |
| Ожидаемые выходы — все | признак не подтверждён, ступень не поднята; на строке старше порога тот же вход даёт реакцию |
| Чем подтверждается | `docs/components/AnomalyJob.md` §«Такт и гистерезис»; `application.yaml`, ключ `anomaly-job` |
| Факт | |

### B7.12 — Упор в окно обхода контура засчитывается неполнотой прохода

| Поле | Содержание |
|---|---|
| Предусловия | инструментов контура больше, чем `anomaly-job.contour-window` |
| Вход | ручной тик проактивной детекции |
| Ожидаемые выходы — все | проход помечен неполным тем же ходом, что и неполученный срез: отчёт `ANOMALY_PASS_INCOMPLETE`, счёт слепоты увеличен, прочие детекторы молчат |
| Чем подтверждается | `application.yaml`, ключ `anomaly-job`; `docs/components/AnomalyJob.md` §«Гейт полноты среза» |
| Факт | |

### B7.13 — Жёсткая ступень без энфорсмента даёт отчёт, но не вторую реакцию

| Поле | Содержание |
|---|---|
| Предусловия | на счёте стои́т `TRADE_BLOCKED`, а на стабе живы сущности радиуса |
| Вход | ручной тик проактивной детекции |
| Ожидаемые выходы — все | заведён отчёт с кодом `SAFETY_RUNG_NOT_ENFORCED` некритической тяжести; ступень **не** запрашивается — она уже стои́т; снятие риска этой тропой не гоняется |
| Чем подтверждается | `docs/components/AnomalyJob.md` §«Что ищет» (A6); `docs/components/SafetyHoldCoordinator.md` §«Гейт терминала отчёта» |
| Факт | |

## B8 — Приём фактов владельца определений

### B8.1 — Активация заводит копию дерева и делает её активной

| Поле | Содержание |
|---|---|
| Предусловия | копии определения в базе ядра нет; в теме `strategies.facts` лежит сообщение активации со снимком дерева и полным конвертом в заголовках |
| Вход | подъём контекста с подпиской на тему |
| Ожидаемые выходы — все | копия заведена деревом целиком и стои́т в `ACTIVE`; отметка обработанного события легла **той же** транзакцией; повторная доставка того же сообщения второй копии не создаёт и дерева не переписывает |
| Чем подтверждается | `docs/architecture/data-ownership.md` §«Копии чужих данных»; `StrategyDefinitionApplier` javadoc; `docs/rules/idempotency-via-unique.md` §Правило |
| Факт | |

### B8.2 — Повторная активация двигает только статус

| Поле | Содержание |
|---|---|
| Предусловия | копия заведена и стои́т в `INACTIVE`; закреплённая деталь у живой сделки ссылается на её узлы |
| Вход | новое сообщение активации того же определения |
| Ожидаемые выходы — все | статус копии — `ACTIVE`; узлы дерева не переписаны: закрепление детали у живой сделки не оборвано |
| Чем подтверждается | `docs/models/domain/aggregate/Strategy.md` §«Что у копии неизменяемо, а что состояние»; `StrategyDefinitionApplier` javadoc |
| Факт | |

### B8.3 — Деактивация и удаление двигают статус, строку не удаляя

| Поле | Содержание |
|---|---|
| Предусловия | копия активна; живая сделка держит её деталь |
| Вход | сообщение деактивации, затем сообщение удаления |
| Ожидаемые выходы — все | статусы копии — `INACTIVE`, затем `DELETED`; строка **не** удалена: деталь нужна сделке до самого терминала, в том числе на выходе, который удаление и запускает |
| Чем подтверждается | `docs/lifecycles/Strategy.md`; `StrategyDefinitionApplier` javadoc |
| Факт | |

### B8.4 — Событие о определении без копии — штатный исход

| Поле | Содержание |
|---|---|
| Предусловия | копии определения в базе нет вовсе |
| Вход | сообщение удаления этого определения, затем сообщение активации **другого** определения |
| Ожидаемые выходы — все | первое отмечено обработанным без следствия и обработку не роняет; второе применено: отказ на первом занял бы партию и остановил следующие |
| Чем подтверждается | `StrategyDefinitionApplier` javadoc; `docs/architecture/contracts.md` §«Идемпотентность и повтор» |
| Факт | |

### B8.5 — Сообщение без конверта пропускается с записью

| Поле | Содержание |
|---|---|
| Предусловия | в теме лежит запись без заголовка идентичности события либо без заголовка класса |
| Вход | подъём контекста с подпиской |
| Ожидаемые выходы — все | копия не тронута; в журнале — строка со смещением записи; обработка следующих записей продолжается |
| Чем подтверждается | `StrategyDefinitionConsumer` javadoc; `docs/architecture/contracts.md` §«Конверт события» |
| Факт | |

### B8.6 — Неизвестный класс события пропускается, а неразбираемое содержимое — нет

| Поле | Содержание |
|---|---|
| Предусловия | два прогона: запись с классом события вне перечня; запись знакомого класса с телом, которое не разбирается в объявленную форму |
| Вход | подъём контекста с подпиской |
| Ожидаемые выходы — все | первая пропущена с записью в журнал — производитель вправе завести класс раньше читателя; вторая **роняет** обработку: пропуск оставил бы копию в прежнем состоянии молча, а событие — потерянным без следа |
| Чем подтверждается | `StrategyDefinitionConsumer` javadoc; `.claude/work/backlog.md` §«Потерянное событие при неразбираемом содержимом — наблюдаемости нет» |
| Факт | |

### B8.7 — Чтение темы начинается с начала, а не с конца

| Поле | Содержание |
|---|---|
| Предусловия | сообщения активации опубликованы **до** подъёма контекста |
| Вход | подъём контекста с подпиской |
| Ожидаемые выходы — все | копия заведена: пропущенная активация оставила бы ядро без определения, по которому оно торгует |
| Чем подтверждается | `application.yaml`, ключ `spring.kafka` (`auto-offset-reset`); `docs/architecture/data-ownership.md` §«Outbox и доставка» |
| Факт | |

### B8.8 — Тропа приёма сегодня не поднимается вовсе

| Поле | Содержание |
|---|---|
| Предусловия | дерево кода без автоконфигурации брокера |
| Вход | подъём контекста ящика |
| Ожидаемые выходы — все | **по дому**: контекст поднялся, подписка установлена, публикатор получил свой клиент. **Сегодня**: бина клиента брокера в контексте нет, обработка подписки не включена — кейсы B8 и B9 красны целиком, и это предъявление названного долга, а не ослабление ожидания |
| Чем подтверждается | `.claude/rules/tech-radar.md` (запись Kafka: автоконфигурации в дереве нет); `.claude/work/backlog.md` §«Тропа к брокеру у построенных соседей не объявлена ни правилом, ни автоконфигурацией» |
| Факт | |

## B9 — Outbox и реле

### B9.1 — Решение и его событие ложатся одной транзакцией

| Поле | Содержание |
|---|---|
| Предусловия | запись строки сделки проходит, запись строки outbox роняется (либо наоборот) |
| Вход | ручной тик сопровождения на ребре, несущем факт |
| Ожидаемые выходы — все | в базе **нет ни одной из двух**: решение без факта и факт без решения одинаково недостижимы; следующий тик применяет ребро снова |
| Чем подтверждается | `docs/architecture/contracts.md` §«У каждого класса события назван писатель, и он же писатель решения»; `docs/architecture/data-ownership.md` §«Outbox и доставка» |
| Факт | |

### B9.2 — Реле публикует окном и в порядке записи

| Поле | Содержание |
|---|---|
| Предусловия | в outbox накоплено больше строк, чем `outbox-relay.batch-size`, у двух тенантов вперемешку |
| Вход | два ручных тика реле |
| Ожидаемые выходы — все | первый тик опубликовал ровно окно, второй — остаток; внутри каждого тенанта порядок публикации совпал с порядком записи; упор в окно неполнотой прохода не считается |
| Чем подтверждается | `docs/components/OutboxRelayJob.md` §«Состав содержимого по классам»; `docs/architecture/contracts.md` §«Конверт события» |
| Факт | |

### B9.3 — Ключ сообщения — тенант, конверт едет заголовками

| Поле | Содержание |
|---|---|
| Предусловия | в outbox строка любого класса ядра |
| Вход | ручной тик реле |
| Ожидаемые выходы — все | опубликованная запись несёт ключом идентичность тенанта; заголовками — идентичность события, класс, версию формы и момент происшествия; телом — содержимое; пустой контекст трассировки заголовка **не** занимает |
| Чем подтверждается | `EventPublisher` javadoc; `docs/architecture/contracts.md` §«Конверт события» |
| Факт | |

### B9.4 — Отметка публикации ставится после подтверждения брокером

| Поле | Содержание |
|---|---|
| Предусловия | брокер принимает запись с задержкой |
| Вход | ручной тик реле |
| Ожидаемые выходы — все | момент публикации у строки проставлен **после** подтверждения; при отказе подтверждения строка остаётся непомеченной; ассерт прямой по базе — поверхности у outbox нет |
| Чем подтверждается | `EventPublisher` javadoc; `application.yaml`, ключ `spring.kafka` (подтверждение от всех синхронных реплик) |
| Факт | |

### B9.5 — Недоступный брокер проход прекращает, а решения не трогает

| Поле | Содержание |
|---|---|
| Предусловия | брокер отвечает отказом; в outbox три непомеченные строки |
| Вход | ручной тик реле |
| Ожидаемые выходы — все | ни одна строка не помечена; статусов сделок проход не двигает; ступеней не поднимает; в журнале — строка об отказе; следующий тик на поднятом брокере публикует все три |
| Чем подтверждается | `docs/components/OutboxRelayJob.md` §«Брокер недоступен — проход пропускается, строки копятся» |
| Факт | |

### B9.6 — Опубликованная, но непомеченная строка публикуется повторно

| Поле | Содержание |
|---|---|
| Предусловия | публикация прошла, отметка упала |
| Вход | следующий ручной тик реле |
| Ожидаемые выходы — все | запись уходит в тему второй раз с **той же** идентичностью события; дубль снимает потребитель своей отметкой обработанного, а не реле |
| Чем подтверждается | `docs/components/OutboxRelayJob.md` §«Идемпотентность публикации — на стороне потребителя»; `docs/rules/idempotency-via-unique.md` §Правило |
| Факт | |

### B9.7 — Повторная запись того же события дублем не становится

| Поле | Содержание |
|---|---|
| Предусловия | строка события с данной идентичностью уже есть |
| Вход | повторная запись той же идентичности |
| Ожидаемые выходы — все | второй строки нет — уникальность идентичности события держит инвариант базы; отказ не роняет тропу решения |
| Чем подтверждается | `docs/components/OutboxRelayJob.md` §«Персистентность outbox — место истины схемы»; `docs/rules/idempotency-via-unique.md` §Правило |
| Факт | |

### B9.8 — Реле строк не удаляет и тем не заводит

| Поле | Содержание |
|---|---|
| Предусловия | опубликованных строк накоплено больше окна |
| Вход | ручной тик реле |
| Ожидаемые выходы — все | помеченные строки остаются в базе; создания темы реле не запрашивает; накопление ограничения сверху не имеет — это названное ограничение |
| Чем подтверждается | `docs/components/OutboxRelayJob.md` §Границы; `.claude/work/backlog.md` §«Размеры томов данных — назначение по наблюдаемому потреблению» |
| Факт | |

### B9.9 — Каждый публикуемый класс приходит со своего ребра, и сверх них нет ничего

| Поле | Содержание |
|---|---|
| Предусловия | прогон, проходящий тропы всех классов ядра: решение о заявке, открытие сделки, затребование остановки, закрытие, подъём ступени, отчёт аномалии |
| Вход | ручные тики сопровождения, детекции и реле |
| Ожидаемые выходы — все | в теме — ровно классы, названные контрактом у производителя `trading-core`, и ни одного сверх; версия формы содержимого у всех одна |
| Чем подтверждается | `docs/architecture/contracts.md` §События; `CoreEventWriter` javadoc |
| Факт | |

### B9.10 — Поглощённая реакция факта подъёма не производит

| Поле | Содержание |
|---|---|
| Предусловия | счёт уже в `TRADE_BLOCKED` |
| Вход | повторный сигнал жёсткой ступени, затем ручной тик реле |
| Ожидаемые выходы — все | второй строки `HoldRaised` в теме нет: событие лежит в транзакции перестановки ступени, а перестановки не было |
| Чем подтверждается | `docs/rules/error-handling-policy.md` §«Идемпотентность реакции и идемпотентность отчёта — разные ключи»; `docs/architecture/contracts.md` §События |
| Факт | |

## B10 — Проекции чужих реестров, ставки комиссии, объявление потребности

### B10.1 — Синк проекций сводит реестр счетов и каталог одним моментом снимка

| Поле | Содержание |
|---|---|
| Предусловия | стаб `auth` отдаёт два счёта, стаб `market-data` — два инструмента со своими правилами |
| Вход | ручной тик синка проекций |
| Ожидаемые выходы — все | в базе четыре строки проекций; момент снимка у строк одного тика **один**; правила инструмента прочитаны в том же проходе, что и спецификация; под тенантов появившихся счетов заведено место для чисел риск-аппетита |
| Чем подтверждается | `RegistryProjectionJob` javadoc; `RegistryProjectionService` javadoc; `docs/architecture/data-ownership.md` §«Копии чужих данных» |
| Факт | |

### B10.2 — Половины тика независимы

| Поле | Содержание |
|---|---|
| Предусловия | стаб `auth` недоступен, стаб `market-data` исправен |
| Вход | ручной тик синка проекций |
| Ожидаемые выходы — все | каталог сведён; в журнале — строка об отказе половины счетов; перезапуск соседа не стал причиной устаревания чужой проекции |
| Чем подтверждается | `RegistryProjectionJob` javadoc |
| Факт | |

### B10.3 — Исчезнувшая у владельца строка из проекции не удаляется

| Поле | Содержание |
|---|---|
| Предусловия | инструмент, на который ссылаются строки сделок, пропал из листинга владельца |
| Вход | ручной тик синка проекций |
| Ожидаемые выходы — все | строка проекции на месте со **старым** моментом снимка; ссылки торговых строк целы; вход по этому инструменту отвергает гейт свежести, а не удаление |
| Чем подтверждается | `RegistryProjectionService` javadoc; `docs/rules/market-data-freshness.md` |
| Факт | |

### B10.4 — Отказ по одному инструменту метку не двигает и проход не роняет

| Поле | Содержание |
|---|---|
| Предусловия | стаб `market-data` отвечает отказом на правилах одного инструмента из трёх |
| Вход | ручной тик синка проекций |
| Ожидаемые выходы — все | два инструмента сведены с новым моментом; у третьего момент прежний; тик завершился; повтор пробует третий снова |
| Чем подтверждается | `RegistryProjectionService` javadoc |
| Факт | |

### B10.5 — Недоступность владельца каталога прекращает проход целиком

| Поле | Содержание |
|---|---|
| Предусловия | стаб `market-data` отвечает недоступностью начиная со второго инструмента |
| Вход | ручной тик синка проекций |
| Ожидаемые выходы — все | первый сведён, обход прекращён — следующие четыреста вызовов дали бы тот же отказ; сделок в ошибку не уводится |
| Чем подтверждается | `RegistryProjectionService` javadoc; `docs/rules/runtime-error-classification.md` §«Отказ соседа по ярусу — свой класс, и сделку в ошибку он не уводит» |
| Факт | |

### B10.6 — Ставка комиссии читается одним вызовом на пару «счёт, тип инструмента»

| Поле | Содержание |
|---|---|
| Предусловия | в проекции каталога пять инструментов **одного** типа; торгующих счетов два |
| Вход | ручной тик синка ставок |
| Ожидаемые выходы — все | к стабу коннектора ушло **два** вызова, а не десять; счёт-владелец проставлен ядром — коннектор числового ключа не знает; перечень типов взят из данных проекции, а не из константы |
| Чем подтверждается | `TradeFeeRateSyncService` javadoc; `docs/models/domain/other/TradeFeeRate.md` |
| Факт | |

### B10.7 — Отказ одной пары стоит одну пару

| Поле | Содержание |
|---|---|
| Предусловия | стаб коннектора отказывает по одному счёту из двух |
| Вход | ручной тик синка ставок |
| Ожидаемые выходы — все | ставки второго счёта записаны; в журнале — строка об отказе первого; ставки соседних типов и счетов не устарели |
| Чем подтверждается | `TradeFeeRateSyncService` javadoc |
| Факт | |

### B10.8 — Пустая проекция каталога синк ставок пропускает

| Поле | Содержание |
|---|---|
| Предусловия | в проекции каталога ни одной строки |
| Вход | ручной тик синка ставок |
| Ожидаемые выходы — все | к стабу коннектора обращений нет; отказа нет; записей не появилось |
| Чем подтверждается | `TradeFeeRateSyncService` javadoc |
| Факт | |

### B10.9 — Потребность объявляется по копиям с непривязанным объявлением

| Поле | Содержание |
|---|---|
| Предусловия | копия определения заведена; идентичности вычисления её условий не привязаны |
| Вход | ручной тик объявления потребности |
| Ожидаемые выходы — все | к стабу `market-data` ушли требования рядов, затем идентичностей индикаторов, затем структур — **в этом порядке**; полученные идентичности привязаны к объявлениям копии; второй тик по той же копии обращений не делает |
| Чем подтверждается | `StrategyDemandService` javadoc; `docs/architecture/market-data-collection.md` §«Как потребность доходит до сбора» |
| Факт | |

### B10.10 — Недоступность владельца данных приём определения не роняет

| Поле | Содержание |
|---|---|
| Предусловия | стаб `market-data` недоступен; приходит сообщение активации |
| Вход | приём сообщения, затем ручной тик объявления потребности |
| Ожидаемые выходы — все | копия заведена и активна; потребность не объявлена, идентичности не привязаны; торговать копия не начинает — её условия не адресуются; следующий тик на поднятом соседе объявление доводит |
| Чем подтверждается | `StrategyDemandService` javadoc; `docs/architecture/data-ownership.md` §«Копии чужих данных» |
| Факт | |

### B10.11 — Объявленная глубина ряда едет как есть

| Поле | Содержание |
|---|---|
| Предусловия | копия несёт два объявления индикаторов одного таймфрейма с разной глубиной |
| Вход | ручной тик объявления потребности |
| Ожидаемые выходы — все | требование ряда одно, глубина — **бо́льшая** из двух; своего числа прогрева ядро не подставляет; пустая объявленная глубина означает всю доступную историю |
| Чем подтверждается | `StrategyDemandService` javadoc; `.claude/work/backlog.md` §«Глубина ряда свечей выводится потребителем, а не владельцем вычисления» |
| Факт | |

## B11 — Поверхность чтения и числа риск-аппетита

### B11.1 — Сделки счёта отдаются окном, от новых

| Поле | Содержание |
|---|---|
| Предусловия | на счёте больше сделок, чем окно чтения |
| Вход | `GET /deals?exchangeAccountInternalId=A1` |
| Ожидаемые выходы — все | `200`; отдано ровно окно, от новых; идентичности инструментов резолвены **одной** раскладкой на всю выборку; наружу уходят идентичности, а не числовые ключи базы |
| Чем подтверждается | `TradingSurfaceService` javadoc; `.claude/rules/codestyle.md` §«Идентичность наружу — `internalId`, не id из БД» |
| Факт | |

### B11.2 — Сделка отдаётся со своими траншами

| Поле | Содержание |
|---|---|
| Предусловия | сделка с двумя траншами |
| Вход | `GET /deals/{internalId}` |
| Ожидаемые выходы — все | `200`; в теле — сделка и оба транша; идентичности счёта и инструмента резолвены; числового ключа базы в теле нет ни у одной сущности |
| Чем подтверждается | `docs/models/domain/aggregate/Deal.md` §Структура; `.claude/rules/codestyle.md` §«Идентичность наружу — `internalId`, не id из БД» |
| Факт | |

### B11.3 — Несуществующая идентичность — негодный вход вызова

| Поле | Содержание |
|---|---|
| Предусловия | контекст поднят |
| Вход | три вызова: сделки несуществующего счёта, сделка с несуществующей идентичностью, торговое состояние несуществующего счёта |
| Ожидаемые выходы — все | у всех трёх `400` с error-DTO, не `500`: отвечать пятисотым значило бы сказать «чини сервер» тому, кто чинить обязан запрос |
| Чем подтверждается | `GlobalExceptionHandler` javadoc; `docs/rules/error-handling-policy.md` §«Внешняя поверхность» |
| Факт | |

### B11.4 — Торговое состояние счёта показывает ступень и инструменты со стоящей ступенью

| Поле | Содержание |
|---|---|
| Предусловия | счёт в `HOLD`; на одной паре «счёт, инструмент» стои́т ступень, на другой нет |
| Вход | `GET /safety/states/{exchangeAccountInternalId}` |
| Ожидаемые выходы — все | `200`; в теле — ступень счёта и **идентичность** инструмента со стоящей ступенью; второй инструмент не назван; ключей базы в теле нет |
| Чем подтверждается | `TradingSurfaceService` javadoc; `docs/rules/manual-halt.md` §«Наблюдаемость: ручное отличимо и от автоматики, и друг от друга» |
| Факт | |

### B11.5 — Числа риск-аппетита назначаются снимком намерения целиком

| Поле | Содержание |
|---|---|
| Предусловия | строка чисел тенанта заведена и заполнена |
| Вход | `PUT /risk-appetites/{tenantInternalId}` с телом, в котором одно из полей **не передано** |
| Ожидаемые выходы — все | `200`; непереданное поле **стёрто**, а не сохранено: тело есть снимок намерения держателя целиком; следующее чтение отдаёт пустое значение |
| Чем подтверждается | `TradingSurfaceController` javadoc; `docs/rules/risk-policy.md` §«Числа назначает держатель; пустое место — отказ» |
| Факт | |

### B11.6 — Отсутствие строки чисел отличается от пустых чисел

| Поле | Содержание |
|---|---|
| Предусловия | тенанта, о котором ядро ещё не знает, в проекции нет |
| Вход | `GET /risk-appetites/{tenantInternalId}` |
| Ожидаемые выходы — все | `400`: строки нет — ядро о тенанте не знает; у тенанта с заведённой пустой строкой тот же вызов отдаёт `200` с пустыми числами |
| Чем подтверждается | `TradingSurfaceService` javadoc; `docs/rules/absent-value-semantics.md` |
| Факт | |

### B11.7 — Негодное тело назначения чисел отвергается валидацией

| Поле | Содержание |
|---|---|
| Предусловия | строка тенанта заведена |
| Вход | `PUT /risk-appetites/{tenantInternalId}` с телом, нарушающим объявленные ограничения полей |
| Ожидаемые выходы — все | `400` с error-DTO; числа в базе не изменились |
| Чем подтверждается | `.claude/rules/codestyle.md` §«Контроллеры / API»; `docs/rules/error-handling-policy.md` §«Отказ, произведённый контейнером, — тот же контракт» |
| Факт | |

### B11.8 — Проверка пары отвечает признаками, а не отказом

| Поле | Содержание |
|---|---|
| Предусловия | счёт `A1` принадлежит тенанту `T1`; инструмент `I1` в проекции есть, `I2` нет |
| Вход | четыре вызова `GET /pair-checks`: «всё сходится»; чужой тенант; несуществующий счёт; несуществующий инструмент |
| Ожидаемые выходы — все | у всех четырёх `200`; различаются **признаки** в теле: найденность счёта, принадлежность тенанту, найденность инструмента; ненайденность здесь — ответ, а не исключение: вопрос вызывающего и есть «существует ли» |
| Чем подтверждается | `TradingSurfaceService` javadoc; `docs/rules/strategy-validation.md` |
| Факт | |

### B11.9 — Проверка пары отвечает проекциями со своим моментом снимка

| Поле | Содержание |
|---|---|
| Предусловия | у владельца реестра счёт уже заведён, синк проекций ещё не проходил |
| Вход | `GET /pair-checks` по этому счёту |
| Ожидаемые выходы — все | признак «счёт найден» ложен; ошибка направлена в **запрещающую** сторону: устаревшая проекция может лишь не знать о новом |
| Чем подтверждается | `TradingSurfaceService` javadoc; `docs/rules/strategy-validation.md` |
| Факт | |

### B11.10 — Поверхность чтения состояния не двигает

| Поле | Содержание |
|---|---|
| Предусловия | активная сделка, ступень на паре, числа тенанта |
| Вход | все читающие точки подряд, дважды |
| Ожидаемые выходы — все | ответы совпали; статусов сделок, ступеней и строк исполнения не изменилось; к стабу коннектора обращений нет: поверхность торговых решений не принимает |
| Чем подтверждается | `TradingSurfaceService` javadoc; `docs/architecture/services/trading-core.md` §«Чего не делает намеренно» |
| Факт | |

### B11.11 — Тенант операндом на читающих тропах не принимается

| Поле | Содержание |
|---|---|
| Предусловия | счёт `A1` принадлежит тенанту `T1` |
| Вход | `GET /deals?exchangeAccountInternalId=A1` с заголовком контекста тенанта `T2` и без заголовка вовсе |
| Ожидаемые выходы — все | **сегодня**: оба ответа одинаковы — тенант в отборе не участвует; **по дому**: домен применяет полученного тенанта к своим строкам, то есть третье звено радиуса обязано существовать. Кейс описан и предъявляет находку F-1 |
| Чем подтверждается | `docs/architecture/contracts.md` §«Контекст тенанта в вызове»; `.claude/work/backlog.md` §«Тенант на читающих тропах ядра операндом не принимается» |
| Факт | |

## B12 — Контур доступа и отказы поверхности

### B12.1 — Вызов без предъявленного принципала

| Поле | Содержание |
|---|---|
| Предусловия | контекст поднят |
| Вход | `GET /deals?exchangeAccountInternalId=A1` без заголовка `Authorization` |
| Ожидаемые выходы — все | `401`; тело — **тот же** error-DTO, что у всякой ошибки поверхности, без причины сверх класса; к соседям обращений нет |
| Чем подтверждается | `docs/rules/error-handling-policy.md` §«Отказ доступа — тот же контракт, что и прочие ошибки»; долг формы — `.claude/work/backlog.md` §«Единый error-DTO у поверхностей соседних сервисов» |
| Факт | |

### B12.2 — Токен, подписанный чужим ключом, просроченный и чужого издателя

| Поле | Содержание |
|---|---|
| Предусловия | стаб отдаёт JWKS с ключом `K1`; `issuer-uri` указывает на него |
| Вход | три вызова: токен, подписанный `K2`; токен с истёкшим сроком; токен чужого издателя |
| Ожидаемые выходы — все | у всех трёх `401` с error-DTO; подпись проверена **локально** по ключам провайдера — за подтверждением сервис никуда не ходит |
| Чем подтверждается | `docs/architecture/contracts.md` §«Контекст тенанта в вызове»; `.claude/decisions/test-contour-design-pass.md` §«4. Провайдер идентичности — JWKS-стаб с локальной парой ключей и `issuer-uri` на стаб» |
| Факт | |

### B12.3 — Пер-операционных проверок права нет намеренно

| Поле | Содержание |
|---|---|
| Предусловия | стаб выдаёт токен служебной идентичности кластера |
| Вход | под ним — чтение сделок, ручная постановка ступени, назначение чисел риск-аппетита, запуск тика |
| Ожидаемые выходы — все | все четыре приняты: при одном субъекте различать некого, и проверка права приезжает со вторым субъектом |
| Чем подтверждается | `SecurityConfig` javadoc; `docs/rules/api-access-policy.md` §«Принципал один — посылка фазы 1» |
| Факт | |

### B12.4 — Проба живости открыта, остальное закрыто

| Поле | Содержание |
|---|---|
| Предусловия | контекст поднят |
| Вход | `GET /actuator/health` без токена; затем `GET /actuator/metrics` без токена |
| Ожидаемые выходы — все | у первой `200`; у второй — отказ, а не данные: открытое перечислено поимённо, умолчание закрыто, а экспозиция актуатора ограничена пробой живости |
| Чем подтверждается | `docs/rules/api-access-policy.md` §«Вся поверхность закрыта; открытое — перечислено»; `application.yaml`, ключ `management` |
| Факт | |

### B12.5 — Неизвестный путь не предъявившему себя

| Поле | Содержание |
|---|---|
| Предусловия | контекст поднят |
| Вход | `GET /api/v1/trading-core/whatever` без токена |
| Ожидаемые выходы — все | `401`, не `404`: не предъявивший себя не узнаёт, существует ли путь |
| Чем подтверждается | `docs/rules/error-handling-policy.md` §«Отказ доступа — тот же контракт, что и прочие ошибки» |
| Факт | |

### B12.6 — Неподдержанный метод и неразбираемое тело

| Поле | Содержание |
|---|---|
| Предусловия | принципал предъявлен |
| Вход | `GET /safety/halts`; затем `POST /safety/halts` с телом, которое не разбирается |
| Ожидаемые выходы — все | `405` и `400` соответственно; тело у обоих — **тот же** error-DTO, что у прочих ошибок; ступени не тронуты |
| Чем подтверждается | `docs/rules/error-handling-policy.md` §«Отказ, произведённый контейнером, — тот же контракт»; долг — `.claude/work/backlog.md` §«Единый error-DTO у поверхностей соседних сервисов» |
| Факт | |

### B12.7 — Отказ соседа наружу — отказ зависимости, а не наша ошибка

| Поле | Содержание |
|---|---|
| Предусловия | стаб `auth` недоступен; читающая точка, которой нужен реестр |
| Вход | вызов такой точки |
| Ожидаемые выходы — все | `503` с кодом отказа соседа и тем же error-DTO; не `500`: наша сторона исправна, и повтор имеет смысл позже |
| Чем подтверждается | `GlobalExceptionHandler` javadoc; `docs/rules/runtime-error-classification.md` §«Отказ соседа по ярусу — свой класс, и сделку в ошибку он не уводит» |
| Факт | |

### B12.8 — Исходящий токен наружу не выходит

| Поле | Содержание |
|---|---|
| Предусловия | стаб провайдера выдаёт токен служебной идентичности; лог прогона собирается |
| Вход | полный набор троп, ходящих к соседям |
| Ожидаемые выходы — все | значение токена не встречается ни в одном теле ответа, ни в одной строке лога, ни в одном сообщении отказа; ключей биржевого счёта сервис не запрашивает ни у кого |
| Чем подтверждается | `.claude/rules/codestyle.md` §Логирование; `docs/architecture/services/trading-core.md` §«Какие вызовы делает и какие принимает» |
| Факт | |

### B12.9 — Недоступная служебная идентичность — отказ, а не анонимный вызов

| Поле | Содержание |
|---|---|
| Предусловия | стаб провайдера отвечает отказом на выдачу токена |
| Вход | ручной тик синка проекций |
| Ожидаемые выходы — все | запрос к соседу **без** заголовка идентичности не уходит; проход отказывает своим классом; сделок в ошибку не уводится |
| Чем подтверждается | `ServiceTokenProvider` javadoc; `docs/architecture/contracts.md` §«Контекст тенанта в вызове» |
| Факт | |

### B12.10 — След отказа доступа

| Поле | Содержание |
|---|---|
| Предусловия | у сервиса есть своя база; таблицы отказов доступа в схеме `trading_core` **нет** |
| Вход | вызов закрытой точки без токена (B12.1) |
| Ожидаемые выходы — все | **сегодня**: отказ оставляет запись в журнале приложения; строки в базе нет, потому что таблицы нет. **По дому**: строка заводится до ответа. Кейс описан и **не прогоняется** в части строки |
| Чем подтверждается | `docs/rules/api-access-policy.md` §«След отказа пишет тот, у кого есть база»; `.claude/work/backlog.md` §«Таблица отказов доступа у сервисов со своей базой» |
| Факт | |

## B13 — Конфигурация и схема как вход

### B13.1 — Ненастроенный контур доступа не поднимает поверхности

| Поле | Содержание |
|---|---|
| Предусловия | база и брокер подняты |
| Вход | контекст с пустым `issuer-uri` |
| Ожидаемые выходы — все | контекст не поднялся; поверхность не отвечает ни на одной точке: пустое означает, что контур доступа не настроен, и это отказ, а не открытая поверхность |
| Чем подтверждается | `application.yaml`, ключ `spring` (комментарий величины); `docs/rules/api-access-policy.md` §«Вся поверхность закрыта; открытое — перечислено» |
| Факт | |

### B13.2 — Незаданный адрес соседа отказывает на вызове, а не на подъёме

| Поле | Содержание |
|---|---|
| Предусловия | `neighbours.market-data.base-url` пуст |
| Вход | подъём контекста, затем ручной тик синка проекций |
| Ожидаемые выходы — все | контекст поднялся; половина каталога отказала своим классом; половина счетов отработала; поход в чужое окружение не состоялся |
| Чем подтверждается | `application.yaml`, ключ `neighbours` (комментарий величины) |
| Факт | |

### B13.3 — Размер пула планировщика выведен из числа джоб и охраняется пробой

| Поле | Содержание |
|---|---|
| Предусловия | размер пула планировщика объявлен конфигурацией сервиса; число методов по расписанию в дереве ему равно |
| Вход | подъём контекста и прогон пробы вместимости планировщика |
| Ожидаемые выходы — все | счёт методов сходится с объявленным числом; лишняя джоба, заведённая без правки пула, роняет сборку — иначе тик-измеритель голодал бы молча |
| Чем подтверждается | `application.yaml`, ключ `spring` (комментарий размера пула); `.claude/rules/codestyle.md` §Джобы |
| Факт | |

### B13.4 — Схема накатывается миграциями и сверяется с отображением

| Поле | Содержание |
|---|---|
| Предусловия | чистая база |
| Вход | подъём контекста |
| Ожидаемые выходы — все | все миграции применены по порядку; отображение сущностей сверено со схемой на старте; колонка, разошедшаяся с миграцией, роняет подъём, а не доживает до первого запроса |
| Чем подтверждается | `application.yaml`, ключ `spring` (комментарий сверки отображения); `.claude/rules/pre-launch-schema-changes.md` |
| Факт | |

### B13.5 — Ключевые инварианты схемы стоя́т и отказывают

| Поле | Содержание |
|---|---|
| Предусловия | контекст поднят |
| Вход | попытка завести вторую активную сделку по паре «счёт, инструмент»; попытка завести вторую строку outbox с той же идентичностью события; попытка завести вторую строку чисел риск-аппетита того же тенанта |
| Ожидаемые выходы — все | все три отвергнуты инвариантом базы, а не проверкой приложения: гейт входа и идемпотентность записи держатся ключами |
| Чем подтверждается | `docs/components/EntryScannerJob.md` §«Гейт входа»; `docs/rules/idempotency-via-unique.md` §Правило; `docs/components/OutboxRelayJob.md` §«Персистентность outbox — место истины схемы» |
| Факт | |

### B13.6 — Отображение сырого типа движения в категорию — вход конфигурации

| Поле | Содержание |
|---|---|
| Предусловия | стаб коннектора отдаёт движение с типом **вне** отображения контура |
| Вход | ручной тик сопровождения на тропе добычи движений |
| Ожидаемые выходы — все | движение садится в принимающую корзину и заводит журнальный отчёт: разведочное состояние видно в данных, а не подразумевается; тип из отображения ложится в свою категорию |
| Чем подтверждается | `application.yaml`, ключ `exchange-contour`; `docs/models/mapping/DealCashFlow.md` |
| Факт | |

### B13.7 — Числа допуска сверки и режим — вход, а не константа

| Поле | Содержание |
|---|---|
| Предусловия | три числа допуска заданы конфигурацией |
| Вход | терминал сделки с расхождением внутри допуска и с расхождением сверх него |
| Ожидаемые выходы — все | первое расхождения не даёт; второе даёт признак и отчёт; правка чисел меняет границу без правки кода |
| Чем подтверждается | `docs/rules/pnl-reconciliation.md` §Допуск; `application.yaml`, ключ `pnl-reconciliation` |
| Факт | |

### B13.8 — Имя окружения — объявленная величина

| Поле | Содержание |
|---|---|
| Предусловия | `platform.environment.name` задан значением тестового контура |
| Вход | подъём контекста и прогон троп, читающих имя окружения |
| Ожидаемые выходы — все | значение читается как объявлено; пустое значение не подменяется умолчанием боевого контура |
| Чем подтверждается | `application.yaml`, ключ `platform`; `docs/spec/environment-contour.json` |
| Факт | |

## B14 — Отсутствие выходов

### B14.1 — Ядро не говорит с площадкой напрямую

| Поле | Содержание |
|---|---|
| Предусловия | прогон, проходящий все тропы, ходящие на площадку |
| Вход | тики сопровождения, детекции, синка ставок |
| Ожидаемые выходы — все | исходящих HTTP-обращений на адреса вне объявленных `neighbours.*` нет ни одного; подписи запросов ядро не строит; ключей биржевого счёта не читает |
| Чем подтверждается | `docs/architecture/services/trading-core.md` §«Чего не делает намеренно»; `docs/architecture/contracts.md` §«Синхронные вызовы» |
| Факт | |

### B14.2 — Определением стратегии ядро не владеет

| Поле | Содержание |
|---|---|
| Предусловия | копия определения в базе ядра есть |
| Вход | полный набор вызовов поверхности |
| Ожидаемые выходы — все | точки, правящей определение, у ядра нет; статус копии двигает только приём события; собственных событий о определении ядро не публикует |
| Чем подтверждается | `docs/architecture/services/trading-core.md` §«Чего не делает намеренно»; `docs/architecture/data-ownership.md` §«Копии чужих данных» |
| Факт | |

### B14.3 — Рыночных данных ядро не считает

| Поле | Содержание |
|---|---|
| Предусловия | стаб `market-data` отдаёт фичи |
| Вход | ручные тики сопровождения и отбора входа |
| Ожидаемые выходы — все | своих рядов, индикаторов и структур ядро не заводит и не пишет; читает уже посчитанное; сбой сбора у соседа исполнение не роняет |
| Чем подтверждается | `docs/architecture/services/trading-core.md` §«Чего не делает намеренно» |
| Факт | |

### B14.4 — Журнал истории ядро не читает

| Поле | Содержание |
|---|---|
| Предусловия | прогон, проходящий тропы принятия решений |
| Вход | тики сопровождения, отбора входа, детекции |
| Ожидаемые выходы — все | обращений к сервисам истории нет ни одного: журнал есть след, а не источник состояния; строки этих пар нет и в таблице синхронных вызовов |
| Чем подтверждается | `docs/rules/audit-not-runtime-source.md` §Правило; `docs/architecture/contracts.md` §«Синхронные вызовы» |
| Факт | |

### B14.5 — В чужие базы ядро не ходит

| Поле | Содержание |
|---|---|
| Предусловия | прогон целиком |
| Вход | все тики и все точки поверхности |
| Ожидаемые выходы — все | подключение одно — к своей базе; чужих схем в конфигурации нет; проекции наполняются **вызовом** к владельцу, а не чтением его базы |
| Чем подтверждается | `docs/architecture/data-ownership.md` §Раскладка, §«Копии чужих данных» |
| Факт | |

### B14.6 — Ручного перевода сделки в терминал мимо FSM нет

| Поле | Содержание |
|---|---|
| Предусловия | активная сделка |
| Вход | полный набор точек поверхности |
| Ожидаемые выходы — все | точки, ставящей статус сделки, на поверхности нет; терминал достигается только тропой FSM с подтверждённым отсутствием живого риска |
| Чем подтверждается | `docs/rules/trading-constraints.md` §«Гейт открытия сделки»; `docs/lifecycles/Deal.md` §«Инварианты переходов» |
| Факт | |

### B14.7 — Событие, которого ядро не публикует

| Поле | Содержание |
|---|---|
| Предусловия | прогон, проходящий ручное снятие ступени и приём фактов определений |
| Вход | ручное снятие ступени; приём активации |
| Ожидаемые выходы — все | строки о снятии ступени в теме **нет** (долг класса `HoldReleased`); событий о чужих определениях ядро не переопубликовывает: потребление следствия в теме не порождает |
| Чем подтверждается | `docs/architecture/contracts.md` §События; `.claude/work/backlog.md` §«Класс события `HoldReleased` и его ручная тропа» |
| Факт | |

## Чего кейсы не мерят намеренно

- **Рёбра FSM сделки и транша, неравенства риск-гейта, лестницу ступеней и
  арифметику сайзинга.** Это предмет уровня 2 — `trading-core-fsm`,
  `trading-core-risk`, `trading-core-safety`, `strategy-engine-calculation`
  (`.claude/decisions/test-contour-design-pass.md` §«Перечень предметов и
  порядок работы»): у них нет ввода-вывода, и таблица «вход → выход» по
  границам области определения дешевле и полнее, чем прогон через ящик.
  Ящику остаётся **оркестрация** и наблюдаемый исход.
- **Расчёт результата сделки и сверку движений поэлементно.** Формулы живут
  в спеках (`docs/spec/deal-result.json`, `docs/spec/pnl-reconciliation.json`)
  и мерятся прогоном спек; ящик мерит **реакцию** на исход сверки (B5.13) и
  то, что терминал без подсчитанных движений не наступает.
- **Наблюдение фактов исполнения по каналу событий.** Потребителя класса
  `ExecutionObserved` у ядра нет: производитель припаркован по грунту
  (`.claude/work/backlog.md` §«WS-слушатель фактов исполнения — по добытому
  контракту каналов»). Кейса на несуществующий исполнитель не заводится;
  наблюдение опросом кейсами B3 покрыто.
- **Приостановку тенанта.** Потребление названо контрактом и не построено —
  производителя у события нет
  (`.claude/work/backlog.md` §«События `auth` — производителя и темы нет»).
  Это граница, а не пропуск.
- **Мультиинстансную сериализацию прохода.** Замок джобы — in-memory, на
  инстанс, и это названное ограничение; распределённый лок ждёт своего
  горизонта (`.claude/work/backlog.md` §«Унификация инфраструктуры джоб —
  горизонт масштабирование ядра после прод-рубежа»). Ящик мерит защиту от
  перекрытия **в одном** процессе (B2.16).
- **Лимиты площадки и её поведение.** Лимиты держит коннектор; стаб их не
  имеет. Ящик мерит **реакцию** на объявленный класс отказа (B3.7, B3.8), а
  не сам лимит.
- **Конфигурацию развёртывания.** Переменные окружения манифеста, сетевые
  политики, образ сервиса — предмет развёртывания, а не ящика
  (`.claude/decisions/test-contour-design-pass.md` §«1. Ящик — реальный
  контекст сервиса в JVM теста; подменяется только то, что за границей
  процесса»). Кейсы B13 мерят **чтение** конфигурации процессом, а не её
  доставку.
- **Существующие модульные тесты сервиса.** Их ревизия критерием чистой
  логики — свой предмет не-кейсовой формы того же перечня; ни один
  существующий тест этим документом не снимается.

## Кейсы, не прогоняемые сегодня

| Кейс | Почему | Что сделает прогоняемым |
|---|---|---|
| B8 и B9 целиком | автоконфигурации брокера в дереве нет: бина клиента нет, обработка подписки не включена — контекст с публикатором не поднимается | закрытие `.claude/work/backlog.md` §«Тропа к брокеру у построенных соседей не объявлена ни правилом, ни автоконфигурацией» |
| B12.10 (часть о строке) | таблицы отказов доступа в схеме `trading_core` нет | закрытие `.claude/work/backlog.md` §«Таблица отказов доступа у сервисов со своей базой» |
| B6.13 и B14.7 (часть о `HoldReleased`) | писателя у класса нет: ступень снимает тропа поверхности, своего ребра у неё нет | закрытие `.claude/work/backlog.md` §«Класс события `HoldReleased` и его ручная тропа» |
| B11.11 (часть по дому) | тенант на читающих тропах операндом не принимается | исход находки F-1 — либо третье звено радиуса, либо правка клейма контракта |

Кейсы, чьё ожидание взято из дома, а дерево кода его пока не несёт (B2.16 в
части кода ответа), прогоняемы **полностью** — они и предъявляют названный
долг.

## Находки владельцам

Найдены при выводе кейсов; ожиданиями кейсов не подменяются
(`.claude/agents/tester.md` §«Взаимодействие с владельцами предмета»).

| # | Находка | Владелец | Где припаркована |
|---|---|---|---|
| F-1 | контракт называет три звена радиуса тенанта, и третье — «домен применяет полученного тенанта к своим строкам». У ядра тенант принимается только там, где он **и есть** предмет вызова (числа риск-аппетита, проверка пары); на чтении сделок, сделки и торгового состояния счёта он не принимается **вовсе** — отбор идёт идентичностью счёта. Названная цена контракта адресует «принимает и не сверяет», а здесь звена нет совсем | `solution-designer` | `.claude/work/backlog.md` §«Тенант на читающих тропах ядра операндом не принимается» |
| F-2 | дом внешней поверхности объявляет у асинхронного фасада джобы два исхода — `202` на чистом запуске и `409` на проблеме при запуске, включая отказ блокировки. Второй недостижим **по построению**: фасад асинхронен, ответ уходит раньше, чем замок вообще берётся, и перекрытие гасится молча уже в потоке джобы. Клейм верен ровно для синхронных операций той же поверхности | `solution-designer` | `.claude/work/backlog.md` §«Обещанный `409` на отказе блокировки у асинхронного фасада недостижим» |
| F-3 | у синка проекций две половины, и терпимость к отказу одной строки объявлена только у половины каталога («отказ по одному инструменту проход не роняет»). Половина реестра счетов такой терпимости не имеет: отказ записи одного счёта прекращает обход остальных и заодно пропускает заведение мест под числа риск-аппетита — то есть один негодный счёт останавливает проекцию всех следующих, и наблюдается это одной строкой лога | `code-writer` | `.claude/work/backlog.md` §«Половина синка проекций по счетам отказ одной строки не терпит»|

## Связи

- Форма кейса и три уровня — `.claude/skills/test-design.md`.
- Контур: чем достаются выходы — `.claude/decisions/test-contour-design-pass.md`.
- Концепция шага — `.claude/work/progress/phase-2-step-12-chronicle.md` §«Концепция».
- Роль — `.claude/agents/tester.md`; ревью кейсов — `.claude/skills/test-review.md`.
- Предмет — `docs/architecture/services/trading-core.md`,
  `docs/processes/deal-management.md`, `docs/processes/fsm-execution-layering.md`,
  `docs/processes/risk-evaluation.md`.
