# Тест-кейсы: `strategy-engine-condition`

## На какой вопрос отвечает этот файл

Какие тест-кейсы у интерпретатора условий движка стратегий.

## Предмет

**Слой условий общего артефакта `services/common/strategy-engine`** — второй
предмет **уровня 2** перечня под-шага 1 `CODE` шага 12 фазы 2
(`.claude/decisions/test-contour-design-pass.md`
§«Перечень предметов и порядок работы»). Уровень у всех кейсов ниже один —
`U`; мишень у предмета одна, поэтому суффикса мишени метки не несут
(`.claude/skills/test-design.md` §«Форма — предмет → кейсы → таблица»).

Признак уровня выполнен механически: **у предмета нет ввода-вывода**. Всё
состояние приезжает аргументом — `ConditionEvaluationContext`, — и
интерпретатор по свечам ничего не считает, в хранилище не ходит и часов не
читает (`docs/components/StrategyConditionEvaluator.md` §Назначение).

Единственный публичный вход предмета:

| Вход | Что отдаёт | Дом поведения |
|---|---|---|
| `StrategyConditionEvaluator.evaluate(condition, context)` | `Boolean` — применимо ли условие | `docs/components/StrategyConditionEvaluator.md`, `docs/spec/deal-condition.json`, `docs/spec/market-phase-condition.json` |

**Чего предмет не включает, и границы названы, а не подразумеваются:**

- **расчётный слой** (`PriceCalculator`, `SizeCalculator`,
  `StrategyActionCalculator`) — предмет
  `strategy-engine-calculation`, сосед по перечню, чьи кейсы написаны прежде
  (`.claude/tests/cases/strategy-engine-calculation.md`);
- **резолвер фазы** (`MarketPhaseResolver`, `market-data`) — предмет
  `market-data-indicators`: перечень уровня 2 отдаёт ему «фазу» вместе с
  индикаторами и структурой. Здесь проверяются **два предиката фазы**,
  потому что они типы правил грамматики; first-match по клаузам —
  его работа, и второго набора кейсов на неё не заводится
  (`docs/components/MarketPhaseResolver.md`);
- **предикаты доменных моделей**, которые интерпретатор зовёт
  (`Position.hasLiveRisk`, `Order.isFilled`, `DealTranche.hasStandaloneProtection`,
  `MarketStructure.hasConfirmedBreakout`) — предмет `domain-model-predicates`:
  они живут в `common/model/domain`, и их границы мерит свой набор. Здесь
  проверяется, что интерпретатор спрашивает **тот** предикат и на **той**
  модели, а не сама его граница;
- **сборка контекста** (`DealContext.conditionContext`,
  `MarketFeatures.conditionOperands`, `MarketFeatureService.readFeatures`) —
  она ходит в базу и к соседу, то есть признак уровня 2 у неё ложен по
  построению; её кейсы принадлежат ящикам `trading-core` и `market-data`;
- **выбор шага и решение его применить** (`StrategyStepSelector`, обработчики
  FSM) — предмет `trading-core-fsm`: интерпретатор отвечает `true`/`false` и
  ни шага, ни действия не выбирает
  (`docs/components/StrategyConditionEvaluator.md` §Границы).

## Новая ось формы: выход булев, и ложь у него одна на все причины

Форму уровня 2 — группа с объявленной базовой сборкой, кейс строкой таблицы
из пяти колонок — ввёл соседний документ, и здесь она не пересказывается
(`.claude/rules/policy-home.md`;
`.claude/tests/cases/strategy-engine-calculation.md` §«Форма уровня 2 выбрана
здесь»). Предмет добавляет к ней **ровно одну** ось, и она несущая.

**У расчётного слоя выход составной**, и «отсутствие выхода» выражалось
пустым полем: у стопа пусты тейк и трейлинг. **У интерпретатора выход —
одно булево значение**, и пустого поля у него нет вовсе. Зато у **лжи**
причин много, и все они дают ровно одно и то же значение:

- предикат посчитан и ложен («фаза `RANGE`, объявлена `BULL_TREND`»);
- операнд недоступен — раскладка ключа не несёт, цена не наблюдена, фактов
  сделки контекст не собирал (`docs/rules/absent-value-semantics.md`);
- тип правила либо оператор интерпретатором не исполняется вовсе.

**Отсюда единица кейса здесь — ПАРА строк, а не строка.** Кейс, называющий
только «ложь», зелен у интерпретатора, который ложен **всегда**; поэтому у
каждой ложной строки в группе стои́т соседняя, отличающаяся **ровно одним**
операндом и дающая истину. Разводит их не значение, а то, что между ними
поменяли: пара и есть доказательство, что предикат считается, а не
возвращается.

**Третью причину лжи по значению отличить нельзя, и наблюдается она
единственным следом — записью лога.** Ветвь неисполнимого типа пишет
предупреждение; ветви недоступного операнда и посчитанной лжи не пишут
ничего. Поэтому строки групп U12 называют **лог** предметом ожидания
наравне с булевым значением: без него «тип не исполняется» неотличимо от
«предикат честно ложен», а именно это различение и есть предмет группы.

**Достижимость на уровне 2 значит то же, что у соседа:** недостижимых
**входов** у предмета нет — аргумент собирается прямо. Пометку «не
прогоняется» получает кейс, у которого нет **ожидания**: два носителя
называют разное либо дом молчит.

## Чем достаются выходы

Контур — `.claude/decisions/test-contour-design-pass.md`, здесь не
пересказывается. Что из него следует **для этого предмета**:

- **субстрата нет вовсе** — ни контейнеров, ни контекста Spring, ни стабов:
  предмет конструируется `new StrategyConditionEvaluator()`. Аннотация
  `@Service` на классе — деталь потребителя, конструктор пуст;
- **состояние собирается настоящими полями доменных моделей**, а не
  подменёнными предикатами (`.claude/rules/codestyle.md` §«Тесты доменных
  моделей»): живой риск эпизода вытекает из статуса и наблюдённого размера,
  налив входной ноги — из статуса и причины закрытия, встроенная защита — из
  состава заявок транша, подтверждённый пробой — из непустоты события
  структуры. Подменённый предикат проверял бы интерпретатор против модели,
  которой в проде не существует;
- **моков нет ни одного**: коллабораторов у предмета нет вовсе — ни поля, ни
  конструкторного аргумента;
- **временем управлять нечем**: ни одна ветвь не читает часов, а поле
  момента оценки в контексте не читает никто (находка F-6);
- **лог наблюдается захватом вывода логгера** — это единственный выход,
  кроме булева значения, и снимается он на уровне предмета, а не сервиса.

## Ожидание берётся из дома, даже когда сегодня оно не исполнено

Часть кейсов ниже ожидает то, что дом объявляет, а дерево кода несёт иначе.
Такие ожидания **не ослабляются под текущий факт** — их красный прогон и
есть предъявление названного долга. У кейса, где **два носителя называют
разное**, ожидания нет вовсе: он стои́т описанным и помечен непрогоняемым,
пока владелец не назовёт дом.

| Расхождение | Где припарковано |
|---|---|
| порог процентов объявляется плоским полем правила, а читается из константного операнда | `.claude/work/backlog.md` §«Порог хода читается из операнда, а объявляется плоским полем правила» |
| источник цены у ценового операнда: грамматика его объявляет, интерпретатор берёт последнюю цену всегда | `.claude/work/backlog.md` §«Источник цены у ценового операнда условия интерпретатором не читается» |
| кроссовер с ценовым операндом: предыдущей цены в контексте нет ни у одного сборщика | `.claude/work/backlog.md` §«Кроссовер с ценовым операндом ложен всегда: предыдущей цены в контексте нет» |
| предикат фазы объявлен равенством, а создание требует у правила оператор и до равенства его не сужает | `.claude/work/backlog.md` §«Предикат фазы игнорирует оператор, а создание его требует» |
| тип «свеча закрыта» объявлен защитой от look-ahead и константно истинен | `.claude/work/backlog.md` §«Тип `CANDLE_CLOSED` константно истинен, а объявлен защитой от look-ahead» |
| шесть операторов и шесть типов источника объявлены перечнями, проходят создание и молча ложны | `.claude/work/backlog.md` §«Элементы грамматики условий, объявленные перечнями и не исполняемые интерпретатором» |
| пустое условие истинно, и дома у этого в корпусе нет | `.claude/work/backlog.md` §«Пустое условие истинно, и у клаузы фазы это даёт безусловную фазу» |
| консервативная ложь объявлена домом, а интерпретатор на двух тропах бросает | `.claude/work/backlog.md` §«Интерпретатор бросает там, где объявлена консервативная ложь» |
| гейт покрытия операндов у ядра мерит только последнюю половину, а пересечение читает обе | `.claude/work/backlog.md` §«Гейт покрытия операндов ядра не мерит предыдущую половину» |

## Поверхность предмета

| Ось | Что её задаёт | Дом ожидания |
|---|---|---|
| конъюнкция правил и её вырождения | `StrategyCondition.rules` | `docs/models/domain/aggregate/Strategy.md` §Условия |
| сравнение значений | `operator` плюс два операнда | `docs/rules/strategy-condition-contract.md` §«Правило и операнды» |
| разрешение индикаторного операнда | `indicatorKey`, `indicatorComponent` | `docs/rules/strategy-condition-contract.md` §«Настройка индикатора» |
| разрешение ценового операнда | `priceSource` | `docs/models/domain/aggregate/Strategy.md` §Условия |
| кроссовер: две половины времени | `previousIndicators` контекста | `docs/components/StrategyConditionEvaluator.md` §Данные |
| объёмный фильтр | левый операнд против своего прошлого | `docs/models/domain/aggregate/Strategy.md` §Условия |
| структура рынка: равенство типа и пробой | `structureKey`, событие пробоя | `docs/models/domain/other/MarketStructure.md` |
| фаза рынка: два предиката | фаза прохода и фаза входа сделки | `docs/spec/market-phase-condition.json`, величины `marketPhaseIs`, `trendChanged` |
| факты сделки: эпизод, вход, защиты | `activePosition`, `tranche` | `docs/spec/deal-condition.json` |
| пороги хода | якорь, цена момента, направление, порог | `docs/spec/deal-condition.json`, величина `signedMovePercents` |
| whitelist контекста | пустота операндов, а не перечень типов | `docs/components/StrategyConditionEvaluator.md` §Назначение |
| типы без исполнения | журнальная ветвь и константная истина | `docs/rules/strategy-condition-contract.md` §«Источник истины — объектная модель настроек» |

## U1 — Условие как конъюнкция и её вырождения

**Базовая сборка.** Контекст: раскладки индикаторов и структур пусты, цены
нет, фазы нет, фактов сделки нет. Условие — одно правило
`MARKET_PHASE_IS` с константой `BULL_TREND`, фаза прохода `BULL_TREND`
(правило истинно).

**Дом группы** — `docs/models/domain/aggregate/Strategy.md` §Условия;
`docs/components/StrategyConditionEvaluator.md` §Назначение.

| Метка | Вход | Ожидаемый выход — весь | Чем подтверждается | Факт |
|---|---|---|---|---|
| U1.1 | базовая сборка | истина; лога не пишется | `docs/spec/market-phase-condition.json`, величина `marketPhaseIs` | |
| U1.2 | два правила, оба истинны | истина | `docs/models/domain/aggregate/Strategy.md` §Условия («шаг применим, когда истинны все») | |
| U1.3 | два правила, первое истинно, второе ложно | ложь | там же | |
| U1.4 | два правила, первое ложно, второе истинно | ложь: конъюнкция порядка не знает | там же | |
| U1.5 | те же два правила, порядок в списке обратный | тот же исход, что у U1.4: **значение от порядка не зависит**. Поле `level` интерпретатор не читает вовсе, и это безвредно ровно потому, что связка конъюнктивна | там же | |
| U1.6 | условие пусто (`null`), контекст тоже `null` | истина; ни один операнд контекста не читается — падения нет | **ожидания в `docs/**` нет**: «пустое условие истинно» живёт только в javadoc — находка F-7, `.claude/work/backlog.md` §«Пустое условие истинно, и у клаузы фазы это даёт безусловную фазу» | |
| U1.7 | условие есть, перечень правил пуст | истина — та же ветвь | там же | |
| U1.8 | условие есть, перечень правил `null` | истина — та же ветвь, третья её форма | там же | |
| U1.9 | правило одно, его тип `null` | **ожидания нет**: ветвь выбора по типу на пустом типе падает, а дом на этот случай молчит. Кейс описан, не прогоняется — вход отвергается созданием, и второго носителя у ответа нет | `docs/rules/strategy-validation.md` (тип правила обязателен) | |

## U2 — Сравнение: операторы и их область

**Базовая сборка.** Раскладка последних значений несёт ключ `ema_fast` со
значением `EMA = 12`; правило `INDICATOR_COMPARE`, левый операнд —
индикатор `ema_fast`, правый — константа `10` типа `NUMBER`, оператор `GT`.

**Дом группы** — `docs/rules/strategy-condition-contract.md` §«Правило и
операнды»; `docs/models/domain/aggregate/Strategy.md` §Условия.

| Метка | Вход | Ожидаемый выход — весь | Чем подтверждается | Факт |
|---|---|---|---|---|
| U2.1 | базовая сборка | истина | `docs/rules/strategy-condition-contract.md` §«Правило и операнды» | |
| U2.2 | константа `14` | ложь — вторая сторона той же границы | там же | |
| U2.3 | константа `12`, оператор `GT` | ложь: сравнение строгое | там же | |
| U2.4 | константа `12`, оператор `GTE` | истина: граница включающая | там же | |
| U2.5 | оператор `LT`, константа `14` | истина | там же | |
| U2.6 | оператор `LTE`, константа `12` | истина | там же | |
| U2.7 | оператор `EQ`, константа `12.0` | истина: сравнение по **значению**, а не по масштабу записи | `docs/rules/decimal-arithmetic.md` | |
| U2.8 | оператор `NE`, константа `12.0` | ложь — та же граница с другой стороны | там же | |
| U2.9 | константы на обеих сторонах: `12` против `10` | истина: любой источник допустим на любой стороне | `docs/models/domain/aggregate/Strategy.md` §Условия | |
| U2.10 | индикатор слева, индикатор справа (`ema_fast` `12` против `ema_slow` `10`) | истина: сравнение индикатора с индикатором — базовая форма грамматики | там же | |
| U2.11 | оператор `null` | ложь, а не падение | `docs/rules/absent-value-semantics.md` | |
| U2.12 | оператор `BETWEEN` | **ожидания нет**: перечень операторов его объявляет, создание сравнивающего правила пропускает, а интерпретатор молча ложен; второй границы диапазона правилу не на чем нести — операндов у него два. Кейс описан, не прогоняется — находка F-6 | `.claude/work/backlog.md` §«Элементы грамматики условий, объявленные перечнями и не исполняемые интерпретатором» | |
| U2.13 | оператор `CROSSED_ABOVE` у типа `INDICATOR_COMPARE` | ложь: пересечение исполняет **своя** ветвь, а в сравнивающем правиле оператор не разрешается ни во что. **Охрана второго рубежа**: создание отдаёт пересечение только типу `CROSSOVER` | `docs/rules/strategy-validation.md`; находка F-6 | |
| U2.14 | правый операнд отсутствует | ложь | `docs/rules/absent-value-semantics.md` | |
| U2.15 | у правого операнда тип источника пуст | ложь | там же | |
| U2.16 | правый операнд — константа типа `ENUM` со значением `BULL_TREND` | ложь: в сравнение числовых значений перечисление не разрешается | `docs/rules/condition-ruletype-granularity.md` §«Границы применения» («равенство перечисления и числовое сравнение — разные контракты») | |
| U2.17 | правый операнд — константа типа `BOOLEAN` со значением `true` | ложь — та же ветвь | там же | |
| U2.18 | правый операнд — константа типа `PERCENT` со значением `10` | истина: процент читается тем же числом, что и `NUMBER` | `docs/rules/strategy-condition-contract.md` §«Правило и операнды» | |
| U2.19 | правый операнд — константа типа `NUMBER` со значением `abc` | ложь, а не бросок: неразбираемый литерал есть недоступный операнд. Код бросает исключение разбора — красный прогон предъявляет находку F-8 | `docs/rules/absent-value-semantics.md`; `.claude/work/backlog.md` §«Интерпретатор бросает там, где объявлена консервативная ложь» | |
| U2.20 | тип правила `PRICE_COMPARE`, левый операнд — цена, цена момента `12` | истина: тип правила на разрешение операндов не влияет — обе ветви ведут в одно сравнение | `docs/rules/strategy-condition-contract.md` §«Правило и операнды» | |

## U3 — Индикаторный операнд: ключ, раскладка, компонент

**Базовая сборка.** Та же, что у U2; раскладки последних значений несут
`ema_fast` (`EMA = 12`) и `macd` (`MACD`: линия `2`, сигнальная `1`,
гистограмма `1`).

**Дом группы** — `docs/rules/strategy-condition-contract.md` §«Настройка
индикатора»; `docs/models/domain/other/IndicatorValue.md`.

| Метка | Вход | Ожидаемый выход — весь | Чем подтверждается | Факт |
|---|---|---|---|---|
| U3.1 | базовая сборка | истина: ключ операнда резолвится в раскладку **авторским именем**, а не идентичностью вычисления | `docs/rules/strategy-condition-contract.md` §«Настройка индикатора» | |
| U3.2 | ключ операнда `ema_slow`, в раскладке его нет | ложь: отсутствующий ключ есть недоступный операнд, а не ноль | `docs/rules/absent-value-semantics.md` | |
| U3.3 | ключ операнда пуст | ложь | там же | |
| U3.4 | однокомпонентный индикатор, компонент не задан | истина: единственное значение подразумевается | `docs/rules/strategy-condition-contract.md` §«Настройка индикатора» | |
| U3.5 | операнд `macd`, компонент `MACD_LINE`, константа `1` | истина: сравнивается линия `2` | там же | |
| U3.6 | операнд `macd`, компонент `SIGNAL_LINE`, константа `1` | ложь: сигнальная `1` строго больше `1` не является — **пара к U3.5**, и различает их ровно компонент | там же | |
| U3.7 | операнд `macd`, компонент `HISTOGRAM`, константа `0` | истина | там же | |
| U3.8 | операнд `macd`, компонент не задан | истина при константе `1`: у многокомпонентного берётся **линия** по умолчанию. **Охрана второго рубежа**: создание требует компонент у многокомпонентных | `docs/rules/strategy-validation.md`; `docs/rules/strategy-condition-contract.md` §«Настройка индикатора» | |
| U3.9 | стохастик в раскладке (`%K = 80`, `%D = 70`), компонент `STOCH_D`, константа `75` | ложь: берётся `%D` | `docs/models/domain/other/IndicatorValue.md` | |
| U3.10 | тот же стохастик, компонент `STOCH_K`, константа `75` | истина — **пара к U3.9** | там же | |
| U3.11 | тот же стохастик, компонент не задан | истина при константе `75`: у стохастика по умолчанию `%K`. Охрана второго рубежа | там же | |
| U3.12 | полосы Боллинджера в раскладке, компонент не задан, константа `0.5` | сравнивается `%B` — умолчание своё у каждого многокомпонентного типа, и общего умолчания у грамматики нет | там же | |
| U3.13 | полосы Боллинджера, компонент `BANDWIDTH` | сравнивается ширина полос, а не `%B` | там же | |
| U3.14 | стохастик в раскладке, компонент `UPPER_BAND` (чужой типу) | сравнивается `%K`: чужой компонент **молча** уходит в умолчание типа. **Охрана второго рубежа**: создание отвергает компонент, не разрешённый типу | `docs/rules/strategy-validation.md` | |

## U4 — Ценовой операнд

**Базовая сборка.** Цена момента в контексте `100`; правило
`PRICE_COMPARE`, левый операнд — цена с источником `LAST_PRICE`, правый —
константа `90`, оператор `GT`.

**Дом группы** — `docs/models/domain/aggregate/Strategy.md` §Условия;
`docs/components/StrategyConditionEvaluator.md` §Данные.

| Метка | Вход | Ожидаемый выход — весь | Чем подтверждается | Факт |
|---|---|---|---|---|
| U4.1 | базовая сборка | истина | `docs/models/domain/aggregate/Strategy.md` §Условия | |
| U4.2 | константа `110` | ложь — вторая сторона границы | там же | |
| U4.3 | цены момента в контексте нет | ложь: цена — единственный операнд грамматики, который читается у площадки, и её недоступность консервативна | `docs/components/StrategyConditionEvaluator.md` §Границы; `docs/rules/market-data-freshness.md` | |
| U4.4 | источник операнда `BEST_BID_PRICE` | **ожидания нет**: грамматика объявляет у ценового операнда источник из перечня размещения, а интерпретатор берёт последнюю цену при любом источнике; второй носитель объявляет скалярный ценовой операнд грамматики **одним**. Кейс описан, не прогоняется — находка F-2 | `docs/models/domain/aggregate/Strategy.md` §Условия против `docs/components/models/DealContext.md`; `.claude/work/backlog.md` §«Источник цены у ценового операнда условия интерпретатором не читается» | |
| U4.5 | источник операнда `MARK_PRICE` | то же отсутствие ожидания, и вход к тому же достижим: отказ создания по неподдержанному источнику сегодня стои́т только у размещения | `.claude/work/backlog.md` §«Источник цены `MARK_PRICE` / `INDEX_PRICE` не отвергается создáнием» | |
| U4.6 | источник операнда не задан | **ожидания нет** по той же причине: источник не читается ни в одной форме | там же | |
| U4.7 | цена слева, цена справа | истина при операторе `GTE`: обе стороны берут один и тот же скаляр | `docs/models/domain/aggregate/Strategy.md` §Условия | |

## U5 — Кроссовер: две половины времени

**Базовая сборка.** Раскладка последних значений: `ema_fast = 12`,
`ema_slow = 10`; раскладка предыдущих: `ema_fast = 9`, `ema_slow = 10`.
Правило `CROSSOVER`, левый операнд `ema_fast`, правый `ema_slow`, оператор
`CROSSED_ABOVE`.

**Дом группы** — `docs/components/StrategyConditionEvaluator.md` §Данные;
`docs/rules/condition-ruletype-granularity.md` (темпоральная семантика —
основание именованного типа).

| Метка | Вход | Ожидаемый выход — весь | Чем подтверждается | Факт |
|---|---|---|---|---|
| U5.1 | базовая сборка | истина: прежде не выше, теперь строго выше | `docs/components/StrategyConditionEvaluator.md` §Данные | |
| U5.2 | предыдущее `ema_fast = 11` (уже было выше) | ложь: пересечения не произошло — **пара к U5.1**, различает прошлое | там же | |
| U5.3 | предыдущие значения равны (`ema_fast = 10`) | истина: касание с последующим уходом вверх есть пересечение, граница прошлого включающая | там же | |
| U5.4 | последние равны (`ema_fast = 10`) | ложь: граница настоящего строгая | там же | |
| U5.5 | оператор `CROSSED_BELOW`, последние `ema_fast = 9`, предыдущие `ema_fast = 11` | истина — зеркальная форма | там же | |
| U5.6 | оператор `CROSSED_BELOW`, предыдущие равны | истина: та же включающая граница с другой стороны | там же | |
| U5.7 | оператор `GT` у типа `CROSSOVER` | ложь. **Охрана второго рубежа**: создание требует у пересечения один из двух его операторов | `docs/rules/strategy-validation.md` | |
| U5.8 | раскладки предыдущих значений нет ключа `ema_fast` | ложь: без второй половины предикат консервативно ложен, и шаг не исполняется | `docs/components/StrategyConditionEvaluator.md` §Данные (предыдущее значение отдаётся вместе с последним) | |
| U5.9 | раскладка предыдущих пуста целиком | ложь — та же ветвь | там же | |
| U5.10 | правый операнд — константа `10`, последние `ema_fast = 12`, предыдущие `ema_fast = 9` | истина: у константы прошлое равно настоящему, и пересечение с уровнем выразимо | `docs/rules/strategy-condition-contract.md` §«Правило и операнды» | |
| U5.11 | левый операнд — цена (`100` сейчас), правый — индикатор | ложь **при любом** прошлом: предыдущей цены контекст не несёт ни одним полем, и форма «цена пересекла среднюю» невыразима. Ожидание — истина на пересечении; красный прогон предъявляет находку F-3 | `docs/models/domain/aggregate/Strategy.md` §Условия (любой источник на любой стороне); `.claude/work/backlog.md` §«Кроссовер с ценовым операндом ложен всегда: предыдущей цены в контексте нет» | |

## U6 — Объёмный фильтр

**Базовая сборка.** Раскладка последних: `obv = 120`; предыдущих:
`obv = 100`. Правило `VOLUME_FILTER_PASSED`, левый операнд — индикатор
`obv`.

**Дом группы** — `docs/models/domain/aggregate/Strategy.md` §Условия
(объёмный операнд допускает только относительные формы).

| Метка | Вход | Ожидаемый выход — весь | Чем подтверждается | Факт |
|---|---|---|---|---|
| U6.1 | базовая сборка | истина: накопительный объём растёт | `docs/models/domain/aggregate/Strategy.md` §Условия | |
| U6.2 | предыдущее `obv = 140` | ложь — **пара к U6.1** | там же | |
| U6.3 | предыдущее равно последнему | ложь: сравнение строгое | там же | |
| U6.4 | предыдущего значения в раскладке нет | ложь, а не пропуск фильтра | `docs/rules/absent-value-semantics.md` | |
| U6.5 | правый операнд — константа `50`, оператор `GT` | исход тот же, что у U6.1: **правая сторона и оператор не читаются вовсе**. Абсолютное сравнение объёма грамматикой и не разрешено | `docs/models/domain/aggregate/Strategy.md` §Условия («сравнение накопительного объёма с константой не допускается») | |
| U6.6 | левый операнд — цена | ложь при любом прошлом: у цены прошлого в контексте нет — та же половина, что у U5.11 | находка F-3 | |

## U7 — Структура рынка: равенство типа и подтверждённый пробой

**Базовая сборка.** Раскладка структур несёт ключ `range_h1` со структурой
типа `RANGE` без события пробоя. Правило `MARKET_STRUCTURE_IS`, левый
операнд — структура `range_h1`, правый — константа `RANGE` типа `ENUM`,
оператор `EQ`.

**Дом группы** — `docs/models/domain/other/MarketStructure.md`;
`docs/rules/condition-ruletype-granularity.md` §«Границы применения».

| Метка | Вход | Ожидаемый выход — весь | Чем подтверждается | Факт |
|---|---|---|---|---|
| U7.1 | базовая сборка | истина | `docs/models/domain/other/MarketStructure.md` | |
| U7.2 | константа `UPTREND` | ложь — **пара к U7.1** | там же | |
| U7.3 | оператор `NE`, константа `UPTREND` | истина: отрицание равенства читается | там же | |
| U7.4 | оператор `GT` | ложь: над перечислением упорядочения нет | `docs/rules/condition-ruletype-granularity.md` §«Границы применения» | |
| U7.5 | ключа `range_h1` в раскладке структур нет | ложь | `docs/rules/absent-value-semantics.md` | |
| U7.6 | структура есть, тип у неё пуст | ложь | там же | |
| U7.7 | константа `MOONWALK` (не значение перечня) | ложь, а не падение. **Охрана второго рубежа**: создание отвергает неизвестное значение типа структуры | `docs/rules/strategy-validation.md` | |
| U7.8 | структурный операнд стои́т **справа**, константа слева | истина: сторона операнда безразлична обеим половинам разбора | `docs/models/domain/aggregate/Strategy.md` §Условия | |
| U7.9 | тип правила `RANGE_BREAKOUT_CONFIRMED`, у структуры есть событие пробоя | истина: предвычисленное событие читается **готовым**, интерпретатор его не детектирует | `docs/models/domain/other/MarketStructure.md`; `docs/models/domain/aggregate/Strategy.md` §Условия | |
| U7.10 | тот же тип, события пробоя у структуры нет | ложь — **пара к U7.9** | там же | |
| U7.11 | тот же тип, у правила объявлен процент буфера `0.5`, события нет | ложь: **буфер правилом не читается** — он параметр резолвера структуры, а не поле условия | `docs/rules/strategy-condition-contract.md` §«Правило и операнды» | |
| U7.12 | тот же тип, структурного операнда нет вовсе | ложь. Охрана второго рубежа: создание требует у пробоя структурный операнд | `docs/rules/strategy-validation.md` | |
| U7.13 | тот же тип, оператор `NE` | истина: пробой оператора не знает, и объявленный не меняет исхода | `docs/models/domain/other/MarketStructure.md` | |

## U8 — Фаза рынка: два предиката

**Базовая сборка.** Фаза прохода `BULL_TREND`; фазы входа нет; фактов
сделки нет. Правило `MARKET_PHASE_IS` с константой `BULL_TREND` типа
`ENUM`.

**Дом группы** — `docs/spec/market-phase-condition.json`, величины
`marketPhaseIs`, `trendChanged`; смысл — `docs/models/domain/other/MarketPhase.md`
§«Фаза внутри прохода сделки — носитель и предикаты».

| Метка | Вход | Ожидаемый выход — весь | Чем подтверждается | Факт |
|---|---|---|---|---|
| U8.1 | базовая сборка | истина | `docs/spec/market-phase-condition.json`, величина `marketPhaseIs` | |
| U8.2 | фаза прохода `RANGE` | ложь — **пара к U8.1** | там же | |
| U8.3 | фаза прохода `UNKNOWN`, объявлена `UNKNOWN` | ложь: встреча двух незнаний совпадением не является, и разрешающее умолчание открыло бы вход по ненаблюдённой фазе | там же, величина `phaseKnown` | |
| U8.4 | фазы прохода нет вовсе | ложь — вторая форма неустановленности | там же | |
| U8.5 | константный операнд у правила отсутствует | ложь | `docs/rules/absent-value-semantics.md` | |
| U8.6 | константа `MOONWALK` | ложь. Охрана второго рубежа: создание отвергает неизвестную фазу | `docs/rules/strategy-validation.md` | |
| U8.7 | оператор `NE`, фаза прохода `BULL_TREND`, объявлена `BULL_TREND` | **ложь** — объявлено «фаза не равна», и предикат обязан ответить отрицанием. Код оператор не читает и отвечает истиной; красный прогон предъявляет находку F-4 | `docs/models/domain/aggregate/Strategy.md` §Условия против `docs/spec/market-phase-condition.json`; `.claude/work/backlog.md` §«Предикат фазы игнорирует оператор, а создание его требует» | |
| U8.8 | тип правила `TREND_CHANGED`, фаза прохода `BEAR_TREND`, фаза входа `BULL_TREND` | истина: уход от фазы, при которой входили | `docs/spec/market-phase-condition.json`, величина `trendChanged` | |
| U8.9 | тот же тип, обе фазы `BULL_TREND` | ложь — **пара к U8.8** | там же | |
| U8.10 | тот же тип, фаза прохода `UNKNOWN`, фаза входа `BULL_TREND` | ложь: без конъюнкта установленности сделка выходила бы по пропаже операнда | там же, величина `phaseKnown` | |
| U8.11 | тот же тип, фазы входа нет (восстановленная сделка) | ложь: сравнивать не с чем | там же, величина `entryPhaseKnown` | |
| U8.12 | тот же тип, фаза входа `UNKNOWN` | ложь — вторая форма неустановленности фазы входа | там же | |
| U8.13 | тот же тип, константный операнд объявлен `RANGE` | ложь при совпавших фазах и истина при разошедшихся: **объявленная фаза смене тренда безразлична** — предикат сравнивает две наблюдённые, а не наблюдённую с объявленной | там же | |

## U9 — Факты сделки: эпизод, вход, защиты

**Базовая сборка.** Живой эпизод: статус `ACTIVE`, наблюдённый размер `1`,
средняя цена входа `100`. Транш без заявок и без отдельных защит. Правило
`POSITION_OPENED`.

**Дом группы** — `docs/spec/deal-condition.json`; модели —
`docs/models/domain/core/Position.md`, `docs/models/domain/core/Order.md`,
`docs/models/domain/aggregate/DealTranche.md`.

| Метка | Вход | Ожидаемый выход — весь | Чем подтверждается | Факт |
|---|---|---|---|---|
| U9.1 | базовая сборка | истина | `docs/spec/deal-condition.json`, величина `positionOpened` | |
| U9.2 | эпизод `ACTIVE`, наблюдённый размер `0` | ложь: строка эпизода без положительного размера открытой позицией не считается — она факт наблюдения, а не экспозиция | там же | |
| U9.3 | эпизод `CLOSED`, наблюдённый размер `1` | ложь: вторая половина предиката живого риска | `docs/models/domain/core/Position.md` | |
| U9.4 | эпизода в контексте нет | ложь | `docs/spec/deal-condition.json`, величина `positionRiskBearing` | |
| U9.5 | тип правила `NO_OPEN_POSITION`, базовая сборка | ложь — **строгое отрицание** U9.1 | там же, величина `noOpenPosition` | |
| U9.6 | тот же тип, эпизода нет | истина | там же | |
| U9.7 | тот же тип, эпизод `ACTIVE` с размером `0` | истина: обе величины читают **один** предикат, и расхождения между ними не бывает по построению | там же | |
| U9.8 | тип правила `ENTRY_ORDER_FINALIZED`, у транша входная нога `COMPLETED` с причиной `FILLED` | истина | там же, величина `entryOrderFinalized` | |
| U9.9 | тот же тип, входная нога `PARTIALLY_COMPLETED` | ложь: частичный налив финализацией не является — экспозиция ещё меняется | там же | |
| U9.10 | тот же тип, нога `COMPLETED` с причиной `CANCELED` | ложь: завершённость без налива финализацией не является | `docs/models/domain/core/Order.md` | |
| U9.11 | тот же тип, у транша только reduce-only заявка `COMPLETED`/`FILLED` | ложь: входной ногой она не является — признак берётся у намерения заявки, а не у её статуса | `docs/models/domain/aggregate/DealTranche.md` | |
| U9.12 | тот же тип, у транша две входные ноги, налита та, у которой идентификатор больше | истина: входная нога — **последняя**, и замещение входа заводит новую заявку | там же | |
| U9.13 | тот же тип, транша в контексте нет | ложь | `docs/rules/absent-value-semantics.md` | |
| U9.14 | тип правила `ATTACHED_STOP_LOSS_EXISTS`, у входной ноги живая встроенная защита | истина; правило `MAIN_PROTECTION_EXISTS` на той же сборке — **ложь** | `docs/spec/deal-condition.json`, величины `attachedStopLossExists`, `mainProtectionExists` | |
| U9.15 | у транша отдельная защита `ACTIVE` с действующим уровнем, встроенной нет | `ATTACHED_STOP_LOSS_EXISTS` — ложь; `MAIN_PROTECTION_EXISTS` — истина. **Пара к U9.14**, и на этом различении стои́т сценарий переключения защиты | там же | |
| U9.16 | встроенная защита есть, но её статус терминальный | `ATTACHED_STOP_LOSS_EXISTS` — ложь: предикат спрашивает **живую** защиту | там же | |
| U9.17 | отдельная защита жива, действующего уровня остановки убытка не несёт (тейк) | `MAIN_PROTECTION_EXISTS` — ложь: вопрос ровно в том, встала ли основная | `docs/spec/protection-coverage.json` | |
| U9.18 | оба типа правил, транша в контексте нет | оба ложны | `docs/rules/absent-value-semantics.md` | |

## U10 — Пороги хода: знак, направление, граница

**Базовая сборка.** Живой эпизод: `ACTIVE`, размер `1`, средняя цена входа
`100`; цена момента `103`; направление `LONG`. Правило
`PROFIT_PERCENTS_REACHED`, порог `2` — **константным операндом** (форма,
которую читает интерпретатор сегодня) либо плоским полем правила (форма,
которую объявляет контракт авторинга); различие предъявляет U10.13.

**Дом группы** — `docs/spec/deal-condition.json`, величины
`signedMovePercents`, `profitPercentsReached`, `lossPercentsReached`.

| Метка | Вход | Ожидаемый выход — весь | Чем подтверждается | Факт |
|---|---|---|---|---|
| U10.1 | базовая сборка | истина: ход `+3` процента взял порог `2` | `docs/spec/deal-condition.json`, величина `profitPercentsReached` | |
| U10.2 | цена момента `101` | ложь — **пара к U10.1** | там же | |
| U10.3 | цена момента `102` | истина: граница **включающая**, строгое сравнение сдвинуло бы срабатывание на тик | там же | |
| U10.4 | цена момента `97` | ложь: ход отрицателен | там же, величина `signedMovePercents` | |
| U10.5 | направление `SHORT`, цена момента `97` | истина: тот же ход вниз у короткой стороны есть **прибыль** | там же (ветвь направления) | |
| U10.6 | тип правила `LOSS_PERCENTS_REACHED`, цена момента `97`, порог `2` | истина: порог объявлен положительной величиной убытка и сравнивается с отрицанием хода | там же, величина `lossPercentsReached` | |
| U10.7 | тот же тип, цена момента `103` | ложь — **пара к U10.6**; без отрицания порога правило сработало бы в прибыли | там же | |
| U10.8 | тот же тип, цена момента `98` | истина: граница включающая с той же стороны | там же | |
| U10.9 | цены момента нет | ложь, а не срабатывание: вычисление на пустом операнде объявило бы полный убыток | там же, величина `moveOperandsAvailable` | |
| U10.10 | средней цены входа у эпизода нет | ложь — вторая половина той же доступности; плановой ценой якорь **не подменяется** | там же, величина `entryAnchor` | |
| U10.11 | эпизода нет вовсе | ложь | там же | |
| U10.12 | средняя цена входа `0` | ложь, а не деление на ноль | там же, величина `moveOperandsAvailable` | |
| U10.13 | порог объявлен **плоским полем** правила (`percents = 2`), константного операнда нет | истина: контракт авторинга кладёт порог в плоское поле, создание его там и требует, и эталонное определение объявляет его так. Код читает только константный операнд и отвечает ложью — красный прогон предъявляет находку F-1 | `docs/models/domain/aggregate/Strategy.md` §Условия; `docs/rules/strategy-condition-contract.md` §«Правило и операнды»; `.claude/work/backlog.md` §«Порог хода читается из операнда, а объявляется плоским полем правила» | |
| U10.14 | порога нет ни в одной форме | ложь: порог, прочитанный нулём, срабатывал бы на любом положительном ходе | `docs/spec/deal-condition.json`, величина `profitPercentsReached` | |
| U10.15 | направления в контексте нет, цена момента `103` | ход читается как `−3` — короткой стороной, и порог прибыли ложен. **Охрана второго рубежа**: перечень направлений сделки закрыт двумя значениями, и пустого среди них нет | там же, операнд `dealDirection`; `docs/models/domain/aggregate/Strategy.md` §Действия | |
| U10.16 | средняя цена входа `3`, цена момента `10` | ход `233.33…` процента — деление идёт в доменном десятичном контексте, а не в целых | `docs/rules/decimal-arithmetic.md` | |

## U11 — Whitelist контекста выражен пустотой операндов

**Базовая сборка.** Контекст классификации фазы: раскладки пусты, цены нет,
фазы нет, **фактов сделки нет ни одного** — ни эпизода, ни транша, ни фазы
входа.

**Дом группы** — `docs/components/StrategyConditionEvaluator.md` §Назначение;
`docs/spec/deal-condition.json`.

| Метка | Вход | Ожидаемый выход — весь | Чем подтверждается | Факт |
|---|---|---|---|---|
| U11.1 | правило `POSITION_OPENED` | ложь | `docs/components/StrategyConditionEvaluator.md` §Назначение | |
| U11.2 | правило `ENTRY_ORDER_FINALIZED` | ложь | там же | |
| U11.3 | правило `MAIN_PROTECTION_EXISTS` | ложь | там же | |
| U11.4 | правило `ATTACHED_STOP_LOSS_EXISTS` | ложь | там же | |
| U11.5 | правило `PROFIT_PERCENTS_REACHED` с порогом `2` | ложь | там же | |
| U11.6 | правило `TREND_CHANGED` | ложь | там же | |
| U11.7 | правило `NO_OPEN_POSITION` | **истина**: строгое отрицание отсутствующего эпизода консервативным не является, и это названное следствие формы whitelist'а — типа в перечне разрешённых у классификации фазы нет, и запрещает его создание, а не интерпретатор | `docs/rules/strategy-validation.md` (перечень типов, разрешённых клаузе фазы); `docs/spec/deal-condition.json`, величина `noOpenPosition` | |
| U11.8 | правило `POSITION_OPENED`, контекст сделки с живым эпизодом | истина — **пара к U11.1**: различает их ровно наличие факта в контексте, а не перечень типов | `docs/components/StrategyConditionEvaluator.md` §Назначение | |
| U11.9 | контекст собран билдером **без раскладок вовсе** (индикаторы и структуры не заданы), правило `INDICATOR_COMPARE` | ложь: несобранная раскладка есть недоступный операнд. Код обращается к пустой раскладке и падает — красный прогон предъявляет находку F-8 | `docs/rules/absent-value-semantics.md`; `.claude/work/backlog.md` §«Интерпретатор бросает там, где объявлена консервативная ложь» | |

## U12 — Типы без исполнения: журнальная ветвь и константная истина

**Базовая сборка.** Контекст сделки со всеми фактами; захват вывода
логгера включён.

**Дом группы** — `docs/rules/strategy-condition-contract.md` §«Источник
истины — объектная модель настроек» (перечень типов без дома).

| Метка | Вход | Ожидаемый выход — весь | Чем подтверждается | Факт |
|---|---|---|---|---|
| U12.1 | правило `NO_ACTIVE_DEAL` | ложь **и запись лога, называющая тип**: операнд типа — реестр сделок пары, и приносит его отбор входа, а не проход сделки; молчаливой ложью тип не оборачивается | `docs/rules/strategy-condition-contract.md` §«Источник истины — объектная модель настроек» | |
| U12.2 | правило `POSITION_OPENED` на той же сборке | ложь **без записи лога**: посчитанная ложь и неисполнимый тип различаются только этим следом — **пара к U12.1** | там же | |
| U12.3 | правило `CANDLE_CLOSED` с объявленным таймфреймом, свеча этого таймфрейма не закрыта | **ложь**: тип объявлен защитой от look-ahead, и создание требует у него таймфрейм. Код отвечает истиной при любом входе — таймфрейма он не читает, а операнда закрытия свечи в контексте нет вовсе; красный прогон предъявляет находку F-5 | `docs/models/domain/aggregate/Strategy.md` §Условия; `.claude/work/backlog.md` §«Тип `CANDLE_CLOSED` константно истинен, а объявлен защитой от look-ahead» | |
| U12.4 | правило `CANDLE_CLOSED` без таймфрейма | ложь по той же причине. Охрана второго рубежа: создание таймфрейм требует | `docs/rules/strategy-validation.md`; находка F-5 | |
| U12.5 | правило `INDICATOR_COMPARE`, левый операнд типа источника `TIME`, правый — константа | ложь **и без записи лога**: тип источника объявлен перечнем, разрешён создание́м в клаузе фазы, а интерпретатор его не резолвит и о том не сообщает. Поле момента оценки в контексте не читает при этом никто — находка F-6 | `.claude/work/backlog.md` §«Элементы грамматики условий, объявленные перечнями и не исполняемые интерпретатором» | |
| U12.6 | то же с типом источника `BALANCE` | ложь без записи лога — вторая форма того же класса | там же | |
| U12.7 | то же с типом источника `MARKET_PHASE` | ложь без записи лога: как **скаляр сравнения** фаза не резолвится, хотя свой именованный тип правила у неё есть | там же | |
| U12.8 | правило `MARKET_STRUCTURE_IS`, структурный операнд есть, а второго операнда нет | ложь без записи лога | `docs/rules/absent-value-semantics.md` | |

## U13 — Отсутствие выходов у интерпретатора

**Дом группы** — `docs/components/StrategyConditionEvaluator.md` §Границы;
`docs/architecture/services.md` §«Что в библиотеку НЕ уезжает».

| Метка | Вход | Ожидаемый выход — весь | Чем подтверждается | Факт |
|---|---|---|---|---|
| U13.1 | любая оценка | ни базы, ни соседа, ни брокера, ни часов: у класса нет ни одного поля-коллаборатора, а конструктор пуст | `docs/architecture/services.md` §«Что в библиотеку НЕ уезжает» | |
| U13.2 | оценка прошла | контекст и его модели **не изменены**: ни эпизод, ни транш, ни заявки, ни раскладки не получили ни одного нового значения | `docs/components/StrategyConditionEvaluator.md` §Назначение (готовые данные, интерпретатор ничего не считает) | |
| U13.3 | оценка прошла | в ответе нет ни шага, ни действия, ни решения о применении: интерпретатор отвечает одним булевым значением | там же §Границы | |
| U13.4 | оценка на устаревших данных | своего предиката свежести интерпретатор не имеет и его не применяет: отсутствующий операнд и устаревший для него **неразличимы**, и различать их незачем | `docs/rules/market-data-freshness.md`; `docs/components/StrategyConditionEvaluator.md` §Границы | |
| U13.5 | оценка правила, читающего факты сделки | журнальных отчётов, событий и метрик интерпретатор не заводит; единственный его след, кроме ответа, — предупреждение о неисполнимом типе (U12.1) | `docs/components/StrategyConditionEvaluator.md` §Границы | |
| U13.6 | два вызова с одним и тем же контекстом | ответ один и тот же: состояния между вызовами интерпретатор не держит, полей у него нет | там же §Назначение | |

## Кейсы, не прогоняемые сегодня

У уровня 2 недостижимых **входов** нет: аргумент собирается прямо. Ниже —
кейсы, у которых нет **ожидания**: два носителя называют разное либо дом
молчит.

| Кейс | Почему не прогоняется | Что сделает прогоняемым |
|---|---|---|
| U1.6, U1.7, U1.8 | «пустое условие истинно» объявлено только javadoc'ом; в продуктовом корпусе у семантики пустого условия дома нет, а у клаузы фазы она даёт безусловную фазу | исход парковки F-7 — заведение дома либо отказ создания |
| U1.9 | правило с пустым типом отвергается созданием, и что обязан ответить интерпретатор, не называет ни один носитель | решение владельца о поведении на невалидном правиле |
| U4.4, U4.5, U4.6 | грамматика объявляет у ценового операнда источник, а второй носитель объявляет ценовой скаляр грамматики одним; обе редакции внутренне непротиворечивы | исход парковки F-2 — правка одной из двух сторон |
| U2.12 | оператор объявлен перечнем, но правилу не на чем нести вторую границу диапазона: форма невыразима структурой, а не только не реализована | исход парковки F-6 — реализация с расширением структуры правила либо снятие оператора из перечня |

**Охраны второго рубежа прогоняемы, и это записано отдельно.** Кейсы U2.13,
U3.8, U3.11, U3.14, U5.7, U7.7, U7.12, U8.6, U10.15 и U12.4 подают вход,
который до интерпретатора не доезжает: его отвергает создание стратегии
(`docs/rules/strategy-validation.md`) либо исключает закрытый перечень
доменного значения. Охрана на то и охрана — кейс прогоняется, — но его
достижимость в проде исключена **чужим** носителем, и красный прогон такого
кейса означает снятую охрану, а не сломанную тропу.

**Кейсы, чьё ожидание взято из дома, а дерево кода несёт иначе** (U2.19,
U5.11, U6.6, U8.7, U10.13, U11.9, U12.3), прогоняемы полностью: их красный
прогон и есть предъявление находок F-1, F-3, F-4, F-5 и F-8.

## Существующий набор предмета

У предмета уже есть тест, идущий с кодом: `DealConditionEvaluationTest`
(девять проверок — фаза, эпизод, налив входа, две защиты, два порога,
граница, пустые операнды, whitelist контекста). Перечень выше выведен **от
предмета**, а не от него: кейс не пропускается потому, что тест на него уже
есть, и не заводится потому, что теста нет.

**Ревизия существующего набора — отдельный предмет** перечня под-шага 1
(`.claude/decisions/test-contour-design-pass.md` §«Перечень предметов и
порядок работы»), и здесь она не делается. Что у неё будет на входе от этого
документа, названо двумя фразами: подмен доменных моделей у набора нет ни
одной (`.claude/rules/codestyle.md` §«Тесты доменных моделей»); зато оба его
порога хода объявлены **константным операндом**, то есть набор закрепляет
форму, которой контракт авторинга не производит, — и именно этим находка F-1
осталась непойманной.

## Находки владельцам

Каждая получила секцию бэклога тем же заходом
(`.claude/rules/parking-address.md`); здесь — только указатель.

| Находка | Предмет | Где припаркована |
|---|---|---|
| F-1 | порог обоих правил хода объявляется плоским полем правила — так велит контракт авторинга, так требует создание и так написано эталонное определение, — а интерпретатор читает его только из константного операнда: оба порога ложны всегда, и шаг перевода защиты в безубыток не срабатывает ни разу | `.claude/work/backlog.md` §«Порог хода читается из операнда, а объявляется плоским полем правила» |
| F-2 | ценовой операнд условия объявлен несущим источник цены из перечня размещения, а интерпретатор берёт последнюю цену при любом источнике; второй носитель объявляет ценовой скаляр грамматики одним | `.claude/work/backlog.md` §«Источник цены у ценового операнда условия интерпретатором не читается» |
| F-3 | предыдущей цены контекст не несёт ни одним полем, и пересечение с участием ценового операнда ложно при любом входе: классическая форма «цена пересекла среднюю» невыразима, а создание её пропускает | `.claude/work/backlog.md` §«Кроссовер с ценовым операндом ложен всегда: предыдущей цены в контексте нет» |
| F-4 | предикат фазы объявлен равенством и оператора не знает, а создание оператор у правила **требует** и до равенства не сужает: объявленное «фаза не равна» исполняется как «равна» | `.claude/work/backlog.md` §«Предикат фазы игнорирует оператор, а создание его требует» |
| F-5 | тип «свеча закрыта» объявлен защитой от look-ahead, создание требует у него таймфрейм, а интерпретатор константно истинен и таймфрейма не читает: единственная защита от заглядывания вперёд не мерит ничего, и эталонное определение на неё опирается | `.claude/work/backlog.md` §«Тип `CANDLE_CLOSED` константно истинен, а объявлен защитой от look-ahead» |
| F-6 | шесть операторов и шесть типов источника объявлены перечнями грамматики, проходят создание и интерпретатором молча ложны; поле момента оценки в контексте не читает ни одна ветвь | `.claude/work/backlog.md` §«Элементы грамматики условий, объявленные перечнями и не исполняемые интерпретатором» |
| F-7 | «пустое условие истинно» дома в корпусе не имеет, а у клаузы классификации фазы это даёт безусловную фазу на каждом проходе: создание пустое условие клаузы пропускает, а резолвер берёт первую совпавшую | `.claude/work/backlog.md` §«Пустое условие истинно, и у клаузы фазы это даёт безусловную фазу» |
| F-8 | интерпретатор бросает там, где объявлена консервативная ложь: неразбираемый литерал константного операнда проходит создание и роняет весь тик отбора входа, а контекст без раскладок роняет оценку | `.claude/work/backlog.md` §«Интерпретатор бросает там, где объявлена консервативная ложь» |
| F-9 | гейт покрытия операндов у ядра мерит только последние значения, а пересечение и объёмный фильтр читают предыдущие: шаг проходит гейт свежести и молча ложен. **Предмет находки лежит у ядра**, и кейса в этом документе у неё поэтому нет — здесь она видна половиной U5.8 и U5.9, где ложь на несобранной второй половине верна и ожидаема | `.claude/work/backlog.md` §«Гейт покрытия операндов ядра не мерит предыдущую половину» |

## Связи

- Форма кейса и три уровня — `.claude/skills/test-design.md`; роль —
  `.claude/agents/tester.md`.
- Контур и его развилки — `.claude/decisions/test-contour-design-pass.md`.
- Предмет — `docs/components/StrategyConditionEvaluator.md`,
  `docs/rules/strategy-condition-contract.md`,
  `docs/rules/condition-ruletype-granularity.md`,
  `docs/models/domain/aggregate/Strategy.md` §Условия.
- Исполнимые формы — `docs/spec/deal-condition.json`,
  `docs/spec/market-phase-condition.json`.
- Сосед по перечню, чьи кейсы написаны прежде —
  `.claude/tests/cases/strategy-engine-calculation.md` (первый предмет
  уровня 2).
