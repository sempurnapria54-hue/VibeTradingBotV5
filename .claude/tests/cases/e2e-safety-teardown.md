# Тест-кейсы: сквозная тропа `e2e-safety-teardown`

## На какой вопрос отвечает этот файл

Какие тест-кейсы у сквозной тропы «жёсткая ступень → снятие живого риска →
отчёт и событие».

## Предмет

**Тропа «расхождение замечено → ступень поднята → живой риск снят у площадки
→ отчёт закрыт → сделки радиуса уведены → факты у журнала и статистики»** —
третий предмет **уровня 3** перечня под-шага 1 `CODE` шага 12 фазы 2
(`.claude/decisions/test-contour-design-pass.md` §«Перечень предметов и
порядок работы»). Уровень у всех кейсов ниже один — `E`; мишень у тропы одна
(стаб площадки), поэтому суффикса мишени метки не несут
(`.claude/skills/test-design.md` §«Форма — предмет → кейсы → таблица»).

**Стороны тропы**: `trading-core`, `connector-okx`, `audit`, `statistics`,
`market-data`. Сосед, чей след кейс не называет, стороной не является и
поднимается стабом: площадка OKX, `auth` и провайдер идентичности.

**`market-data` стороной стал первым кругом ревью, и довод механический:**
раскладку фич читает **сборка контекста прохода** — безусловно, на каждое
построение у сделки с закреплённой деталью (`DealContextService#build` →
`MarketFeatureService#readForEvaluation`), — а эта тропа строит контекст
в четырёх местах: на каждом тике детекции по каждой нетерминальной сделке
(детекторы инварианта сделки, `DealInvariantDetectors#detect`), в счёт-широком
снятии риска по каждой нетерминальной сделке
(`KillSwitchService#fireExchangeAccount`), на каждом тике сопровождения и в
предикате снятия ступени (`ManualHaltService#riskProvenAbsentOnScope`)
(**прогоном поправлено на месте** 2026-09-26: прежде мест было названо три —
без детекции).
Прежняя редакция объявляла его молчащим соседом, и то же отрицание стоя́ло
ожиданием E8.2. `strategies`
поднимается **ради пролога** и на собственных отрезках этой тропы следа не
имеет — её отсутствие кейс называет прямо (E8.2); стороной от подъёма он не
становится. Периметр `bff` на этой тропе не стои́т — класс подъёма ступени он
получает своей тропой (`e2e-perimeter-read`).

**Форма уровня 3 введена первыми двумя предметами и здесь не изобретается
заново**: сторона и её след, стык как предмет кейса, порядок как ассерт,
отрезок как наименьшая единица, тик поверхностью —
`.claude/tests/cases/e2e-strategy-to-deal.md` §«Новая ось формы — СТОРОНА
ТРОПЫ И ЕЁ СЛЕД»; пролог в чужом документе, неповторение его следов, разрыв
между тропами, два конца — `.claude/tests/cases/e2e-exit-and-close.md`
§«Новая ось формы — ПРОЛОГ ЧУЖОЙ ТРОПЫ». Ниже — ровно то, чего у них не
было.

## Новая ось формы — ВХОД ТРОПЫ ЕСТЬ РАСХОЖДЕНИЕ, А НЕ ХОД

У первых двух предметов тропа начиналась **ходом**: создание определения,
шаг `EXIT`, удаление у владельца. Здесь её начинает **состояние чужой
стороны, которое система обязана заметить сама**, и из этого следует четыре
вещи.

- **Ход теста — расстановка среза, а не вызов.** Вход подаётся тем, что
  стаб площадки начинает отвечать срезом с расхождением; сторона об этом не
  извещается ничем. Отсюда у кейса **два** входных поля по существу: чем
  отличается срез и сколько тиков подано. Кейс, ставящий расхождение прямо в
  базе ядра, проверяет не стык, а собственную расстановку — ровно тот запрет,
  который уровень объявил первым предметом.
- **Число тиков — часть входа, а не подробность прогона.** Признак
  большинства детекторов обязан держаться **подряд идущие** такты, прежде
  чем реакция запрашивается (`docs/components/AnomalyJob.md` §«Такт и
  гистерезис»), и носителем подтверждения служит **стоящая строка отчёта**, а
  не память процесса. Поэтому кейс называет число тиков явно, а кейс, которому
  нужен неподтверждённый первый взгляд, подаёт **один** тик и проверяет
  отсутствие реакции как положительный исход.
- **Входов в один стык два — автоматический и ручной, — и различают их не
  состав реакции, а два наблюдаемых операнда.** Ручная поверхность заходит в
  ту же точку и той же последовательностью
  (`docs/components/HoldService.md` §«Ручная постановка объявлена той же
  тропой, только решение принимает человек»); различают тропы **машинный код**
  причины и произведённый им разрез счётчика у статистики. Кейс поэтому
  называет, какой вход подан, и ждёт **одинакового** состава реакции при
  различающихся коде и разрезе.
- **Незавершённость — объявленный ИСХОД, а не отсутствие следа.** Отчёт,
  оставшийся незакрытым при неподтверждённом снятии риска, есть требуемый
  результат тропы (`docs/lifecycles/AnomalyReport.md` §«Гейт терминала
  критичной тропы — подтверждённое снятие риска»), и проверяется он
  **положительным** ассертом на промежуточный статус, а не отсутствием
  терминала. Тем же образом читается «эскалировать некуда» на счётном
  радиусе: второй реакции нет не потому, что что-то не сработало.

## Чем достаются выходы

Контур — `.claude/decisions/test-contour-design-pass.md`, здесь не
пересказывается (`.claude/rules/policy-home.md`); что из него следует для
уровня 3 — `.claude/tests/cases/e2e-strategy-to-deal.md` §«Чем достаются
выходы»: сторона есть процесс своего boot-jar'а, база у каждой своя в общем
контейнере, стаб один на все виды чужой поверхности, адреса сторон друг другу
— реальные порты, исходящая идентичность — у стаба провайдера, момент
ставится в данных, асинхронный след ждётся `Awaitility`, ассерт по базе
разведён признаком решения 7. Своё у этой тропы — пять вещей.

- **Стаб площадки отвечает тремя счёт-широкими срезами, и они же — вход
  тропы.** Живые позиции, pending-заявки и pending algo-заявки по трём
  семьям (условные, OCO, трейлинг); поштучного обхода по инструментам у
  детекции нет ни одного (`docs/components/AnomalyJob.md` §«Состояние
  носителя»). **Срезов три, а запросов у стаба пять:** `ordType` у эндпоинта
  algo-заявок обязателен, и срез собирается тремя вызовами по семьям
  (`OkxSourceReader#getAllPendingAlgoOrders`). Кейс ведёт счёт по **стабу**,
  а не по вызовам ядра, поэтому число в ожидании — пять. Сверх срезов стабу нужны команды снятия риска — отмена заявки,
  отмена algo-заявки, снятие встроенной защиты, закрытие позиции — и чтения
  подтверждения по ним.
- **Порядок команд у стаба — наблюдаемая величина**, как и на тропе выхода:
  стаб ведёт журнал принятых запросов с их моментом, и ассерт порядка
  читается им. Инвариант снятия риска объявлен **порядком**
  (`docs/rules/exit-teardown-order.md`), и состояние после тропы одинаково при
  любом.
- **Тик детекции подаётся ручным фасадом ядра** — `POST
  /api/v1/trading-core/jobs/anomaly-detection`; тик прохода сопровождения —
  своим
  фасадом. Расписание в прогоне выключено, поэтому два подряд поданных тика и
  есть гистерезис. **Минимальный возраст стоящей строки** при этом остаётся
  величиной конфигурации процесса: кейс, которому нужно подтверждение, ставит
  его нулевым, а кейс, проверяющий саму границу, — ненулевым и подаёт тики
  подряд.
- **Числа калибровки — вход конфигурации процесса ядра**, а не данные: предел
  слепоты, окно наблюдения отчёта, минимальный возраст подтверждения, предел
  попыток снятия риска. Переключаются они перезапуском стороны с другим
  значением.
- **Ступень пары «счёт, инструмент» ассертится ПРЯМЫМ чтением базы**, и это
  названная цена набора, а не умолчание: поверхность торгового состояния
  отдаёт по паре только идентичности со стоящей ступенью, а не саму ступень
  (`.claude/work/backlog.md` §«Поверхность торгового состояния отдаёт факт
  стоящей ступени пары, а не саму ступень»). Ступень **счёта**, серия убытков
  и счёт слепоты читаются поверхностью.

## Стороны тропы и их поверхность

| Сторона | Чем на неё подаётся вход | Чем читается её след |
|---|---|---|
| `trading-core` | `POST /api/v1/trading-core/jobs/{anomaly-detection,deal-orchestrator,outbox-relay}`; `POST /api/v1/trading-core/safety/halts`; `POST /api/v1/trading-core/safety/halt-clearances` | `GET /api/v1/trading-core/safety/states/{exchangeAccountInternalId}`, `GET /api/v1/trading-core/deals`, `GET …/deals/{internalId}`; отчёты аномалий, ступень пары, строки исполнения и outbox — колонками |
| `connector-okx` | зовёт его ядро, а не тест | запрос, дошедший до стаба площадки, его момент и порядок; ответ, вернувшийся ядру |
| `market-data` | зовёт его ядро сборкой контекста прохода, а не тест | запрос раскладки фич, дошедший до стаба, и его число |
| `audit` | тема `trading-core.facts` | `GET /api/v1/audit/journal/records`; `reception_states` — колонками |
| `statistics` | тема `trading-core.facts` | `GET /api/v1/statistics/aggregates/rows`; `incident_facts`, `deal_facts`, `reception_states` — колонками |

## Предусловия тропы — что лежит до первого хода

**Пролог собирается из ДВУХ чужих документов**, и это не новая ось, а
применение объявленной: предусловие называется чужим кейсом поимённо и
исполняется его ходами (`.claude/tests/cases/e2e-exit-and-close.md` §«Новая
ось формы — ПРОЛОГ ЧУЖОЙ ТРОПЫ»). Своих ходов постановки у этой тропы нет.

| Что | Чем ставится |
|---|---|
| определение активировано, копия у ядра есть, сделка заведена, входная нога налилась | ходы `.claude/tests/cases/e2e-strategy-to-deal.md` до §«E5.1 — Добыча факта: наполнение заявки доезжает до зеркала и двигает транш» |
| в зеркале ядра живой эпизод позиции, у транша ненулевая экспозиция | ход `.claude/tests/cases/e2e-exit-and-close.md` §«E1.1 — Наблюдение позиции у стаба даёт зеркалу эпизод, а траншу — экспозицию» |
| стаб площадки отвечает тремя счёт-широкими срезами: позиции, pending-заявки, pending algo-заявки трёх семей | `GET /api/v5/account/positions`, `GET /api/v5/trade/orders-pending`, `GET /api/v5/trade/orders-algo-pending` |
| стаб площадки принимает отмену заявки, отмену algo-заявки, снятие встроенной защиты и закрытие позиции | `POST /api/v5/trade/cancel-order`, `.../cancel-algos`, `.../cancel-advance-algos`, `.../close-position` |
| стаб площадки отдаёт состояние заявки и algo-заявки по запросу подтверждения | `GET /api/v5/trade/order`, `GET /api/v5/trade/order-algo` |
| стаб `market-data` отвечает на чтение раскладки фич инструмента | `POST /api/v1/market-data/instruments/{internalId}/features` |
| каталог инструментов ядра несёт инструмент сделки, и он в рабочем состоянии | синк проекций пролога |
| ступеней не стои́т ни на счёте, ни на паре; счёт слепоты нулевой; серия убытков нулевая | старт сторон с чистыми базами |
| гистерезис, окно наблюдения, минимальный возраст подтверждения, предел слепоты и предел попыток снятия риска заданы | конфигурация процесса ядра |
| расписание джоб сторон, у которых есть фасад, выключено | конфигурация процесса |

**Расхождения в срезе предусловия НЕ содержат** — его вносит первый отрезок
каждой группы: тропа начинается там, где стаб начинает отвечать иначе, и это
её собственный ход, а не обвязка.

## Тропа сегодня не прогоняется целиком, и причина та же

**Прежняя причина — реле ядра к брокеру не ходит — снята прогоном
2026-09-26:** клиент брокера у ядра объявлен явно, и события подъёма ступени и
отчёта группы `E1` доехали до журнала и до статистики (`E1.1`, `E1.2`, `E1.7`).
Открытой у оси остаётся только встречное сетевое правило на стенде — в
прогоне набора его нет (`.claude/work/backlog.md` §«Тропа к брокеру у
построенных соседей не объявлена ни правилом, ни автоконфигурацией»).

**Пометка «не прогоняется» на кейсы от этого не ставится** по тому же доводу,
что у первых двух троп: недостижимо не предусловие, а исправность предмета, и
красный прогон есть предъявление названного долга
(`.claude/tests/cases/e2e-strategy-to-deal.md` §«Тропа сегодня не прогоняется
целиком, и причин две»).

## Ожидание берётся из дома, даже когда сегодня оно не исполнено

| Долг | Где припаркован |
|---|---|
| встречного сетевого правила к брокеру на стенде нет | `.claude/work/backlog.md` §«Тропа к брокеру у построенных соседей не объявлена ни правилом, ни автоконфигурацией» |
| у durable-группы ядра нет ни строки состояния приёма, ни рядов | `.claude/work/backlog.md` §«Экспорт метрик из сервисов — приёмник есть, отправителя нет» |
| события `HoldReleased` на ручном снятии ступени нет | `.claude/work/backlog.md` §«Класс события `HoldReleased` и его ручная тропа» |
| статус подтверждённого снятия риска ставится и на неподтверждённом | `.claude/work/backlog.md` §«Статус подтверждённого снятия риска ставится и на неподтверждённом» |
| биржевая ступень не доминирует над инструментной реакцией | `.claude/work/backlog.md` §«Доминирование биржевых ступеней над инструментными реакциями энфорсера не имеет» |
| отчёт ручной операции поглощается вместе с отказом журнала | `.claude/work/backlog.md` §«Отчёт ручной операции поглощается вместе с отказом журнала» |
| возврат инструмента в онбординговый статус неисполним | `.claude/work/backlog.md` §«Карв-аут возврата инструмента в онбординговый статус неисполним и писателя не имеет» |
| ступень пары в поверхности торгового состояния не видна | `.claude/work/backlog.md` §«Поверхность торгового состояния отдаёт факт стоящей ступени пары, а не саму ступень» |
| снимки отчёта счёт-широкой реакции несут только поля счёта: ни экспозиции до, ни остатка после в них нет | `.claude/work/backlog.md` §«Снимки отчёта счёт-широкой реакции несут только поля счёта» |

## E1 — Детекция расхождения и гистерезис

### E1.1 — Чужая живая заявка в срезе: первый тик даёт отчёт наблюдения и ни одной команды

| Поле | Содержание |
|---|---|
| Предусловия | общие предусловия тропы целиком; минимальный возраст подтверждения нулевой |
| Вход | стаб площадки начинает отдавать в срезе pending-заявок живую заявку по инструменту сделки, чей клиентский идентификатор нашего маркера не несёт; **один** тик проактивной детекции |
| Ожидаемые выходы — все | **`trading-core`:** ступени нет ни на счёте, ни на паре; заведена **одна** строка отчёта некритичной критичности кодом чужой заявки, счётного радиуса; строка outbox класса подъёма ступени отсутствует, строка класса отчёта одна. **`connector-okx`:** к стабу ушли **пять** чтений среза — позиции, pending-заявки и три семьи pending algo-заявок — и **ни одной** команды: ни отмены, ни закрытия. **`audit`:** строка журнала об отчёте есть. **`statistics`:** счётчик отчётов вырос на единицу, счётчик критичных — нет; счётчик поднятых ступеней не двигался |
| Чем подтверждается | `docs/components/AnomalyJob.md` §«Такт и гистерезис» (таблица гистерезиса, `A7`); `docs/components/AnomalyJob.md` §«У детектора с жёсткой ступенью строк получается две, и это ратифицированная семантика»; `docs/rules/statistics-aggregates.md` §«Счётчики происшествий — своё зерно, а не строка сделочного агрегата» |
| Факт | зелено 2026-09-26 |

### E1.2 — Второй тик с тем же признаком поднимает жёсткую ступень счёта и рождает событие подъёма

| Поле | Содержание |
|---|---|
| Предусловия | состояние E1.1: строка наблюдения стои́т в окне и старше минимального возраста |
| Вход | второй тик проактивной детекции при том же срезе стаба |
| Ожидаемые выходы — все | **`trading-core`:** ступень счёта — сворачивание; заведена **вторая** строка отчёта, критичная, тем же кодом; строка outbox класса подъёма ступени одна, и в содержимом — идентичности тенанта и счёта, ступень сигнала, код причины и актор; инструмента в содержимом **нет** — радиус счётный. **`connector-okx`:** у стаба появились команды снятия риска (состав и порядок — E2). **`audit`:** строки журнала об обоих классах. **`statistics`:** счётчик поднятых ступеней и счётчик жёстких выросли на единицу каждый, счётчик ручных — **нет**; счётчик отчётов вырос на единицу, критичных — на единицу |
| Чем подтверждается | `docs/components/AnomalyJob.md` §«Такт и гистерезис»; `docs/architecture/contracts.md` §«Содержимое несёт идентичности и то, чего потребитель не дочитает сам»; `docs/components/HoldService.md` §«Инструмент в содержимом читается по РАДИУСУ, а не по тому, есть ли он в контексте» |
| Факт | зелено 2026-09-26 — тенант едет конвертом (колонка `tenant_id` строки outbox), а не полем содержимого |

### E1.3 — Признак исчез ко второму тику: ступени нет, у площадки команд нет

| Поле | Содержание |
|---|---|
| Предусловия | состояние E1.1 |
| Вход | стаб убирает чужую заявку из среза; второй тик детекции |
| Ожидаемые выходы — все | **`trading-core`:** ступени нет ни на одном радиусе; второй строки отчёта нет; строк outbox не прибавилось. **`connector-okx`:** команд у стаба нет ни одной; чтения срезов повторились. **`audit`, `statistics`:** новых строк и фактов нет. **Стоящая строка наблюдения при этом остаётся** — она описывает прошлый тик, и снимать её некому |
| Чем подтверждается | `docs/components/AnomalyJob.md` §«Такт и гистерезис»; `docs/rules/exchange-hold.md` §«Границы и эскалация» (автоматического снятия нет ни у одной ступени) |
| Факт | зелено 2026-09-26 |

### E1.4 — Строка подтверждения моложе минимального возраста ступени не поднимает

| Поле | Содержание |
|---|---|
| Предусловия | общие предусловия тропы; минимальный возраст подтверждения задан **ненулевым** |
| Вход | расхождение в срезе; два тика детекции подряд, без паузы между ними |
| Ожидаемые выходы — все | **`trading-core`:** ступени нет; строк отчёта по коду ровно **одна** — вторая не заводится, потому что ключ дедупа тот же и обе критичности некритичны; строки outbox класса подъёма нет. **`connector-okx`:** команд нет ни одной. **`statistics`:** счётчик отчётов вырос на единицу за оба тика |
| Чем подтверждается | `docs/components/AnomalyJob.md` §«Границ у окна две, и обе обязательны»; `docs/models/domain/other/AnomalyReport.md` §«Ключ дедупа у состояния» |
| Факт | зелено 2026-09-26 |

### E1.5 — Живой риск по инструменту вне контура поднимает ступень первым же тиком

| Поле | Содержание |
|---|---|
| Предусловия | общие предусловия тропы |
| Вход | стаб отдаёт в срезе позиций живую позицию по биржевому имени, которому строки каталога у ядра нет **вовсе**; **один** тик детекции |
| Ожидаемые выходы — все | **`trading-core`:** ступень счёта — сворачивание уже на первом тике; строка отчёта одна и **сразу критичная** — тропы первого взгляда у этого детектора нет; событие подъёма одно. **`connector-okx`:** команды снятия риска у стаба есть. **`statistics`:** счётчик жёстких подъёмов вырос; счётчик критичных отчётов вырос; некритичной строки по этому коду нет ни одной |
| Чем подтверждается | `docs/components/AnomalyJob.md` §«Такт и гистерезис» (гистерезис в один тик у `A2`, `A3`); `docs/components/AnomalyJob.md` §«Что ищет» |
| Факт | зелено 2026-09-26 — на ядре `E1.4`: минимальный возраст подтверждения детектору без гистерезиса безразличен |

### E1.6 — Неполный срез: прочие детекторы молчат, и молчание записано

| Поле | Содержание |
|---|---|
| Предусловия | общие предусловия тропы; в срезе pending-заявок лежит чужая заявка — признак, который на полном срезе дал бы реакцию |
| Вход | стаб отвечает отказом на чтение среза algo-заявок; один тик детекции |
| Ожидаемые выходы — все | **`trading-core`:** ступени нет ни на одном радиусе — на неполном срезе детекторы молчат; строка отчёта одна, кодом неполного прохода, некритичная; счёт слепоты счёта равен единице; отчёта по чужой заявке **нет**. **`connector-okx`:** команд снятия риска нет ни одной. **`audit`:** строка журнала об отчёте неполного прохода |
| Чем подтверждается | `docs/components/AnomalyJob.md` §«Гейт полноты среза»; `docs/components/AnomalyJob.md` §«Слепота счётна» |
| Факт | зелено 2026-09-26 — отказ одной семьи условных заявок кодом словаря площадки (`50001`) делает срез неполным; на ядре `E1.3` |

### E1.7 — Третий подряд неполный проход поднимает МЯГКУЮ ступень счёта, и снятия риска в ней нет

| Поле | Содержание |
|---|---|
| Предусловия | состояние E1.6, повторённое до счёта слепоты, равного пределу минус один |
| Вход | ещё один тик детекции при том же отказе стаба |
| Ожидаемые выходы — все | **`trading-core`:** ступень счёта — **мягкая**, а не сворачивание; счёт слепоты равен пределу; строка outbox класса подъёма одна, ступень в содержимом мягкая, код — код неполного прохода; отдельной журнальной строки этот тик не пишет — ключ тот же. **`connector-okx`:** команд снятия риска нет **ни одной**: в составе мягкой ступени его нет. **`statistics`:** счётчик поднятых ступеней вырос на единицу, счётчик **жёстких** — нет |
| Чем подтверждается | `docs/components/AnomalyJob.md` §«У слепоты есть предел, и он мягкий»; `docs/components/AnomalyJob.md` §«Ступень мягкая, и это следствие оси, а не смягчение»; `docs/rules/exchange-hold.md` §«Ступень 1 — мягкий холд» |
| Факт | зелено 2026-09-26 |

### E1.8 — Наблюдённый проход обнуляет счёт слепоты и ступени не снимает

| Поле | Содержание |
|---|---|
| Предусловия | состояние E1.7: мягкая ступень счёта стои́т, счёт слепоты равен пределу |
| Вход | стаб снова отвечает на все три среза; один тик детекции |
| Ожидаемые выходы — все | **`trading-core`:** счёт слепоты обнулён; ступень счёта **осталась** мягкой — автоматического снятия нет ни у одной ступени; строк outbox не прибавилось. **`audit`, `statistics`:** новых строк и фактов нет |
| Чем подтверждается | `docs/rules/exchange-hold.md` §«Границы и эскалация»; `docs/components/AnomalyJob.md` §«Операнд счёта — исход НАБЛЮДЕНИЯ, а не полнота среза, и отметка идёт после обхода» |
| Факт | зелено 2026-09-26 — срез снова полон и без чужой заявки |

## E2 — Снятие живого риска у площадки: порядок и подтверждение

### E2.1 — Порядок снятия: входные ноги, потом позиция, защиты последними

| Поле | Содержание |
|---|---|
| Предусловия | состояние E1.1; у транша сверх налившейся ноги живы вторая входная нога и отдельная условная заявка защиты; стаб подтверждает каждую команду |
| Вход | второй тик детекции — тот же, что в E1.2 |
| Ожидаемые выходы — все | **`connector-okx`:** по журналу стаба момент отмены входной ноги **раньше** момента закрытия позиции, а момент снятия защиты — **позже** подтверждённого закрытия; все команды несут идентификатор счёта. **`trading-core`:** живых заявок и живых защит у траншей не осталось; зеркало эпизода позиции закрыто. **`audit`, `statistics`:** следа сверх классов того же тика — подъёма и отчёта (E1.2) и остановки каскада (E3.1) — нет (**прогоном поправлено на месте**: прежде «сверх классов E1.2», а каскад того же тика даёт свой класс) |
| Чем подтверждается | `docs/components/KillSwitchExecutor.md` §Порядок; `docs/rules/exit-teardown-order.md` |
| Факт | зелено 2026-09-26 — пролог с добором тропы выхода; отмену второй ноги площадка отражает её чтением |

### E2.2 — Встроенная защита снимается тоже, и перечень собирается по родителям

| Поле | Содержание |
|---|---|
| Предусловия | состояние E1.1; налившаяся входная нога несла встроенную защиту, и её родитель локально **терминален** |
| Вход | второй тик детекции |
| Ожидаемые выходы — все | **`connector-okx`:** у стаба есть команда снятия встроенной защиты, и её аргумент — защита **терминального** родителя; перечень живых отдельных заявок её не содержал. **`trading-core`:** подтверждение отсутствия живого риска выдано только после того, как судьба встроенной защиты резолвлена добычей родителя |
| Чем подтверждается | `docs/components/KillSwitchExecutor.md` §Порядок (снятие в обеих формах); `docs/components/KillSwitchExecutor.md` §Подтверждение |
| Факт | зелено 2026-09-26 |

### E2.3 — Подтверждение читается фактами, а не подтверждением приёма команды

| Поле | Содержание |
|---|---|
| Предусловия | состояние E1.1 |
| Вход | стаб отвечает на все команды успешным подтверждением приёма, но срез позиций после них **по-прежнему** отдаёт живую позицию |
| Ожидаемые выходы — все | **`trading-core`:** снятие риска **не** считается подтверждённым, хотя подтверждение приёма получено по каждой команде; отчёт терминала не получил. **`connector-okx`:** после команд к стабу ушли чтения добычи — состояние заявок и срез счёта. **`audit`:** строки о завершении отчёта нет |
| Чем подтверждается | `docs/rules/ack-not-runtime-truth.md`; `docs/components/KillSwitchExecutor.md` §Подтверждение |
| Факт | зелено 2026-09-26 — закрытие принимается и не исполняется: позиция читается живой |

### E2.4 — Неподтверждённое снятие повторяется по бюджету и кончается штатным отказом

| Поле | Содержание |
|---|---|
| Предусловия | состояние E2.3; предел попыток снятия риска задан числом конфигурации |
| Вход | тики детекции сверх предела попыток при том же неподтверждающем срезе |
| Ожидаемые выходы — все | **`connector-okx`:** число проходов снятия риска у стаба **не превысило** предела: повторный автоматический сигнал на стоящей ступени поглощается анкером и второго снятия не гоняет. **`trading-core`:** ступень счёта стои́т; отчёт остался в промежуточном статусе; строк outbox класса подъёма ступени **не прибавилось** — перестановки не было. **`statistics`:** счётчик поднятых ступеней стои́т на прежнем значении |
| Чем подтверждается | `docs/components/AnomalyJob.md` §«Повторное снятие риска джоба не запускает»; `docs/components/SafetyHoldCoordinator.md` §«Чем продолжается исчерпанный предел попыток» |
| Факт | зелено 2026-09-26 — на ядре `E2.3`; предел попыток — ключ `kill-switch.max-teardown-attempts`, три |

### E2.5 — Инструментный радиус: снятие идёт по сделке пары, обхода счёта нет

| Поле | Содержание |
|---|---|
| Предусловия | общие предусловия тропы; у счёта **две** активные сделки на разных инструментах, обе с живой экспозицией |
| Вход | ручная постановка полного класса на паре «счёт, инструмент» первой сделки |
| Ожидаемые выходы — все | **`connector-okx`:** команды снятия риска у стаба несут инструмент **только первой** сделки; по инструменту второй нет ни одной команды. **`trading-core`:** ступень стои́т на паре, ступени счёта нет; вторая сделка осталась активной, её заявки живы |
| Чем подтверждается | `docs/components/KillSwitchService.md` §«Два радиуса»; `docs/rules/instrument-hold.md`. Сделку пары снятие риска берёт популяцией радиуса, а не из контекста вызова (долг `F4` закрыт заходом 153) |
| Факт | не прогоняется — кода клетки нет: две активные сделки на разных инструментах требуют пролога двух инструментов, которого у набора ещё нет |

### E2.6 — Счётный радиус: обход всех нетерминальных сделок, и одна неподтверждённая делает исход неподтверждённым

| Поле | Содержание |
|---|---|
| Предусловия | те же две активные сделки; стаб подтверждает снятие по первой и **не** подтверждает по второй |
| Вход | ручная постановка полного класса на счёте |
| Ожидаемые выходы — все | **`connector-okx`:** команды ушли по **обеим** сделкам — обход пройден до конца, а не оборван на первой неудаче. **`trading-core`:** исход снятия риска — не подтверждено; отчёт терминала не получил; ступень счёта стои́т. **`audit`:** строки о завершении отчёта нет |
| Чем подтверждается | `docs/components/KillSwitchService.md` §«Два радиуса»; `docs/components/SafetyHoldCoordinator.md` §«Гейт терминала отчёта» |
| Факт | не прогоняется — кода клетки нет: то же предусловие, что у `E2.5` |

## E3 — Каскад радиуса и остановка сделок

### E3.1 — Каскад уводит активные сделки радиуса в ошибочное состояние по сделке за раз

| Поле | Содержание |
|---|---|
| Предусловия | состояние E1.1; у счёта две активные сделки на разных инструментах |
| Вход | второй тик детекции, поднимающий жёсткую ступень счёта |
| Ожидаемые выходы — все | **`trading-core`:** обе сделки в ошибочном состоянии; у каждой причина выхода из штатного ведения — биржевая; строк outbox класса «остановка сделки» **две**, по одной на сделку, каждая со своей идентичностью. **`audit`:** две строки журнала о остановке, по одной на сделку. **`statistics`:** сделочных фактов нет — терминала ещё не было; счётчика остановленных сделок не существует, и ни одно число агрегата этим классом не двигается |
| Чем подтверждается | `docs/components/SafetyHoldCoordinator.md` §«Каскад идёт ТЕМ ЖЕ ребром, что и шаг прохода, по сделке за раз»; `docs/lifecycles/Deal.md` §«Причина выхода из штатного ведения»; `docs/rules/statistics-aggregates.md` §«Счётчика остановленных сделок нет, и довод — объявленный предмет счёта» |
| Факт | не прогоняется — кода клетки нет: две активные сделки на разных инструментах требуют пролога двух инструментов, которого у набора ещё нет |

### E3.2 — Сделка под стоящей биржевой ступенью при инструментном сигнале получает биржевую причину

| Поле | Содержание |
|---|---|
| Предусловия | ступень счёта — сворачивание, поднятая ранее; сделка на паре активна |
| Вход | ручная постановка полного класса на паре «счёт, инструмент» этой сделки |
| Ожидаемые выходы — все | **`trading-core`:** причина выхода у сделки — **биржевая**, а не причина риск-политики: каскад резолвит её по стоящим ступеням, а не по радиусу своего сигнала; ступень пары при этом переставлена. **`audit`:** строка журнала об остановке несёт ту же причину |
| Чем подтверждается | `docs/components/SafetyHoldCoordinator.md` §«Причину каскад НЕ выводит из радиуса своего сигнала»; `docs/spec/hard-rung-shutdown-reason.json`, величина `hardRungShutdownReason` |
| Факт | зелено 2026-09-26 — сделка, активная под ступенью счёта, заведена восстановлением после аварийного терминала первой |

### E3.3 — Каскад начинается после терминала отчёта, а не параллельно ему

| Поле | Содержание |
|---|---|
| Предусловия | состояние E1.1; снятие риска у стаба подтверждается |
| Вход | второй тик детекции |
| Ожидаемые выходы — все | **`trading-core`:** момент ребра остановки первой сделки каскада **позже** момента терминала отчёта, а момент открытия отчёта — **раньше** первой команды у стаба. **`connector-okx`:** между терминалом отчёта и ребром остановки к стабу не ушло **ни одного** чтения: внешний снимок собирается по инструменту контекста, а у счёт-широкой реакции инструмента нет — снимок остаётся пустым (`AnomalyReportService#externalSnapshot`, долг ниже). **`audit`:** порядок строк журнала повторяет порядок решений: подъём, отчёт, остановки |
| Чем подтверждается | `docs/components/SafetyHoldCoordinator.md` §Последовательность; `docs/components/SafetyHoldCoordinator.md` §«Порядок фактов в теме следует порядку решений» |
| Факт | зелено 2026-09-26 |

### E3.4 — Сделка, ставшая активной после каскада, подбирается шагом прохода

| Поле | Содержание |
|---|---|
| Предусловия | ступень счёта поднята каскадом; вторая сделка заведена восстановительной тропой **после** каскада |
| Вход | тик прохода сопровождения |
| Ожидаемые выходы — все | **`trading-core`:** вторая сделка уведена в ошибочное состояние тем же ребром, с биржевой причиной; строка outbox класса остановки по ней одна. **`audit`:** своя строка журнала. **`connector-okx`:** снятие риска по ней гоняется проходом, а не каскадом — второго прогона реакции ступени нет |
| Чем подтверждается | `docs/components/SafetyHoldCoordinator.md` §Последовательность (шаг 5 — первый ход энфорсмента, а не весь); `docs/components/DealOrchestratorJob.md` |
| Факт | красно 2026-09-26 — находка `F7`: сделка уведена в ошибку шагом прохода с биржевой причиной, второго прогона реакции нет, но команд снятия её живой позиции проходы не шлют; метка `debt` |

### E3.5 — Ошибочное состояние не терминал: сделка доходит до аварийного терминала следующими проходами

| Поле | Содержание |
|---|---|
| Предусловия | состояние E3.1; снятие риска подтверждено |
| Вход | тики прохода сопровождения до терминальности сделки |
| Ожидаемые выходы — все | **`trading-core`:** сделка перешла в аварийный терминал, причина закрытия — аварийная; строка outbox терминального класса одна. **`audit`:** строка журнала о терминале. **`statistics`:** сделочный факт появился, и сутки зерна взяты моментом терминала. **Содержимое терминального события здесь не переспрашивается** — оно предмет `.claude/tests/cases/e2e-exit-and-close.md` §«E5 — Терминал сделки и его событие» |
| Чем подтверждается | `docs/components/ErrorHandler.md` §«Выходные проверки»; `docs/lifecycles/Deal.md` |
| Факт | зелено 2026-09-26 — сутки зерна проверены моментом сделочного факта, равным моменту терминала |

### E3.6 — Терминала нет, пока отсутствие живого риска не доказано

| Поле | Содержание |
|---|---|
| Предусловия | состояние E3.1; срез стаба продолжает отдавать живую позицию |
| Вход | два тика прохода сопровождения подряд |
| Ожидаемые выходы — все | **`trading-core`:** сделка осталась в ошибочном состоянии; терминального события нет ни одного. **`connector-okx`:** к стабу ушли чтения добычи фактов. **`statistics`:** сделочного факта нет, число закрытых сделок не двигалось |
| Чем подтверждается | `docs/components/ErrorHandler.md` §«Выходные проверки»; `docs/spec/deal-lifecycle.json`, величина `riskProvenAbsent` |
| Факт | зелено 2026-09-26 — закрытие принимается и не исполняется, как в `E2.3` |

## E4 — Отчёт аномалии: снимки, дедуп и терминал

### E4.1 — Отчёт открыт до первой команды снятия риска, и снимок «до» несёт живой риск

| Поле | Содержание |
|---|---|
| Предусловия | состояние E1.1; снятие риска у стаба подтверждается |
| Вход | второй тик детекции |
| Ожидаемые выходы — все | **`trading-core`:** момент заведения критичной строки отчёта **раньше** момента первой команды у стаба; локальный снимок «до» несёт поля счёта — идентичности и стоящую ступень — и **не** несёт ни идентичности сделки, ни ног траншей, ни полей инструмента: контекст счёт-широкой реакции собирается из счёта и находки, а сделки в нём нет ни одной (`AnomalyReaction#apply`). Внешнего снимка у строки **нет вовсе** — он собирается по инструменту контекста. **`connector-okx`:** перед первой командой к стабу **не** ушло чтения состояния: собирать снимок не из чего. **`audit`:** строка журнала об отчёте с моментом заведения. **Названная цена:** самая тяжёлая реакция контура оставляет отчёт без экспозиции — долг ниже |
| Чем подтверждается | `docs/components/SafetyHoldCoordinator.md` §Последовательность (шаг 2); `docs/lifecycles/AnomalyReport.md` §«Две тропы обработки» |
| Факт | зелено 2026-09-26 — тот же тик, что у `E4.4` |

### E4.2 — Снимок «после» читается после снятия риска, и остаточный риск виден в нём

| Поле | Содержание |
|---|---|
| Предусловия | состояние E4.1; стаб после команд отдаёт плоский срез |
| Вход | тот же тик |
| Ожидаемые выходы — все | **`trading-core`:** отчёт завершён, и момент терминала **позже** последней команды снятия; локальный снимок «после» несёт те же поля счёта, что и «до», внешнего снимка у строки **нет** — по тому же признаку, что в E4.1. Отсутствие живой позиции читается **срезом стаба**, а не снимком отчёта. **`connector-okx`:** чтений после команд ушло ровно столько, сколько их делает подтверждение снятия риска — `REFRESH`-добыча по инструменту сделки и срез позиций счёта, которым исполнитель подтверждает отсутствие риска вне графа сделок (`docs/components/KillSwitchExecutor.md` §«Риск вне графа сделок»), — и ни одного сверх них: срезов заявок и условных заявок счёта после команд нет (**прогоном поправлено на месте**: прежде подтверждение называло одну добычу по инструменту). **`audit`:** строки журнала об отчёте по-прежнему одна на класс — терминал отчёта своего класса события не имеет |
| Чем подтверждается | `docs/components/SafetyHoldCoordinator.md` §Последовательность (шаг 4); `docs/architecture/contracts.md` §События |
| Факт | зелено 2026-09-26 — тот же тик, что у `E4.4` |

### E4.3 — Дедуп по стоящей строке: повтор того же основания второй строки не даёт и события не рождает

| Поле | Содержание |
|---|---|
| Предусловия | состояние E1.2: жёсткая ступень стои́т, критичная строка отчёта заведена и стои́т в окне |
| Вход | ещё два тика детекции при том же расхождении в срезе |
| Ожидаемые выходы — все | **`trading-core`:** строк отчёта по ключу столько же, сколько было; строк outbox класса отчёта по этому ключу не прибавилось — прибавилась одна, некритичная, детектора непроэнфорсенной ступени `A6`: чужая заявка под стоящей ступенью жива, а снятие риска её не трогает; строк класса подъёма ступени не прибавилось — ступень стои́т, перестановки не было. **`connector-okx`:** команд снятия риска нет ни одной. **`statistics`:** счётчик подъёмов стои́т; счётчик отчётов вырос ровно на строку `A6`, критичных — нет (**прогоном поправлено на месте**: прежде «строк outbox класса отчёта не прибавилось» и «счётчик отчётов стои́т» — вопреки строке `A6` таблицы детекторов `docs/components/AnomalyJob.md`) |
| Чем подтверждается | `docs/models/domain/other/AnomalyReport.md` §«Ключ дедупа у состояния»; `docs/architecture/contracts.md` §«У каждого класса события назван писатель, и он же писатель решения» (клетка `AnomalyReported`) |
| Факт | зелено 2026-09-26 |

### E4.4 — Эскалация критичности заводит свою строку: запуск снятия риска виден в журнале

| Поле | Содержание |
|---|---|
| Предусловия | стои́т некритичная строка наблюдения кодом чужой заявки (E1.1) |
| Вход | второй тик детекции, поднимающий жёсткую ступень |
| Ожидаемые выходы — все | **`trading-core`:** строк отчёта по этому коду **две** — некритичная первого тика и критичная второго; ключ дедупа не совпал по критичности. **`audit`:** две строки журнала об отчётах, с разной критичностью. **`statistics`:** счётчик отчётов вырос на два за обе тика, счётчик критичных — на один |
| Чем подтверждается | `docs/models/domain/other/AnomalyReport.md` §«Ключ дедупа у состояния»; `docs/components/AnomalyJob.md` §«У детектора с жёсткой ступенью строк получается две, и это ратифицированная семантика» |
| Факт | зелено 2026-09-26 |

### E4.5 — Незакрытый отчёт при неподтверждённом снятии — объявленный исход, а не зависание

| Поле | Содержание |
|---|---|
| Предусловия | состояние E2.3: снятие риска не подтверждается ни одним проходом |
| Вход | тики детекции сверх предела попыток |
| Ожидаемые выходы — все | **`trading-core`:** критичная строка отчёта стои́т в **промежуточном** статусе — не завершённом и не ошибочном; ступень счёта стои́т; снимка «после» у строки нет. **`audit`:** строка журнала об отчёте есть, строки о его завершении нет по построению — терминал отчёта класса события не имеет. **Ассерт положительный:** проверяется именно промежуточный статус, а не отсутствие терминала |
| Чем подтверждается | `docs/lifecycles/AnomalyReport.md` §«Гейт терминала критичной тропы — подтверждённое снятие риска»; `docs/components/SafetyHoldCoordinator.md` §«Гейт терминала отчёта»; `.claude/work/backlog.md` §«Статус подтверждённого снятия риска ставится и на неподтверждённом» |
| Факт | зелено 2026-09-26 — промежуточный статус проверен перечнем незавершённых и неошибочных |

### E4.6 — Отказ записи отчёта реакцию не гейтит, и число статистики его не досчитывается

| Поле | Содержание |
|---|---|
| Предусловия | состояние E1.1; база ядра отвергает вставку строки отчёта на втором тике |
| Вход | второй тик детекции |
| Ожидаемые выходы — все | **`trading-core`:** ступень счёта поднята; строка outbox класса подъёма ступени есть; строки отчёта нет, строки outbox класса отчёта нет. **`connector-okx`:** команды снятия риска ушли — реакция журналом не гейтится. **`statistics`:** счётчик подъёмов вырос, счётчик отчётов — **нет**; расхождение объявлено пределом полноты числа, а не дефектом прогона |
| Чем подтверждается | `docs/rules/error-handling-policy.md` §«Отказ журнального носителя реакцию не гейтит»; `docs/rules/statistics-aggregates.md` §«Полнота чисел, складывающих обе тропы, условна, и условия у них разные» |
| Факт | зелено 2026-09-26 — отказ вставки критичной строки ставится неисправностью базы ядра (`Database#refuseInserts`) |

### E4.7 — Счёт-широкая тропа: локальный снимок несёт только поля счёта

| Поле | Содержание |
|---|---|
| Предусловия | общие предусловия тропы; активных сделок у счёта нет |
| Вход | расхождение, дающее счётный радиус (чужая позиция вне контура); тик детекции |
| Ожидаемые выходы — все | **`trading-core`:** строка отчёта заведена; локальный снимок несёт поля счёта и **не** несёт идентичности сделки и инструмента; ступень счёта поднята; снятие подтверждено срезом позиций, и отчёт доведён до терминала. **`connector-okx`:** к стабу ушли чтения среза позиций и одно закрытие чужой позиции — без валюты расчёта: строки проекции у инструмента вне контура нет, и контракт закрытия её не требует. **`audit`:** строка журнала об отчёте есть; о его завершении строки нет по построению — терминал отчёта своего класса события не имеет, как в E4.2 (**прогоном поправлено на месте**: прежде «и о его завершении есть» — вопреки собственному звену документа) |
| Чем подтверждается | `docs/lifecycles/AnomalyReport.md` §«Две тропы обработки»; `docs/components/KillSwitchService.md` §«Два радиуса»; `docs/components/KillSwitchExecutor.md` §«Риск вне графа сделок» |
| Факт | зелено 2026-09-26 — ядро без сделок |

## E5 — Эскалация радиуса и исчерпанный предел

### E5.1 — Неподтверждённое снятие на инструментном радиусе поднимает счётную ступень своим кодом

| Поле | Содержание |
|---|---|
| Предусловия | общие предусловия тропы; ступеней не стои́т ни одной; стаб не подтверждает снятие риска |
| Вход | ручная постановка полного класса на паре «счёт, инструмент» |
| Ожидаемые выходы — все | **`trading-core`:** ступень пары стои́т; **сверх неё** поднята жёсткая ступень счёта; строк outbox класса подъёма **две**, и у второй радиус счётный, а код — код остатка снятия риска, а не код исходного сигнала; отчёт инструментной реакции терминала не получил. **`audit`:** две строки журнала о подъёме, с разными радиусами и кодами. **`statistics`:** счётчик подъёмов вырос на два, счётчик жёстких — на два, счётчик **ручных** — на один: эскалация ручной тропой не является |
| Чем подтверждается | `docs/components/SafetyHoldCoordinator.md` §«Гейт терминала отчёта»; `docs/components/SafetyHoldCoordinator.md` §«Свой факт подъёма эскалация пишет тем же ребром, что и всякая другая тропа»; `docs/rules/statistics-aggregates.md` §«Счётчики происшествий — своё зерно, а не строка сделочного агрегата». Долг `F4`, который гасил развилку эскалации на этой тропе, закрыт заходом 153 |
| Факт | зелено 2026-09-26 — конец реакции читается записью ядра об эскалации |

### E5.2 — Каскад инструментного радиуса идёт после всей счётной реакции эскалации

| Поле | Содержание |
|---|---|
| Предусловия | состояние E5.1 |
| Вход | тот же ход |
| Ожидаемые выходы — все | **`trading-core`:** момент ребра остановки сделки инструментного радиуса **позже** момента подъёма счётной ступени и позже её каскада; вложенность порядка наблюдаема по моментам строк outbox; строк остановки **не больше**, чем сделок радиуса, и ни одна сделка не уведена дважды. **`audit`:** порядок строк журнала повторяет тот же порядок, и строк чужих классов между ними нет |
| Чем подтверждается | `docs/components/SafetyHoldCoordinator.md` §Последовательность; `docs/components/SafetyHoldCoordinator.md` §«Порядок фактов в теме следует порядку решений». Долг `F4`, из-за которого счётной реакции эскалации на этой тропе не наступало, закрыт заходом 153 |
| Факт | зелено 2026-09-26 — тот же ход, что у `E5.1`: сделку уводит каскад счётной реакции, каскад пары её не переуводит |

### E5.3 — Эскалация на счёте со стоящей счётной ступенью поглощается, и события подъёма нет

| Поле | Содержание |
|---|---|
| Предусловия | ступень счёта — сворачивание, уже стои́т; стаб не подтверждает снятие риска |
| Вход | ручная постановка полного класса на паре «счёт, инструмент» |
| Ожидаемые выходы — все | **`trading-core`:** ступень пары переставлена, и её факт подъёма один; **второго** факта — счётного — нет: эскалация поглощена анкером, перестановки не было. **`statistics`:** счётчик подъёмов вырос ровно на единицу |
| Чем подтверждается | `docs/components/SafetyHoldCoordinator.md` §«Эскалация — автоматический ход, и права на доведение недоделанного у неё нет»; `docs/components/HoldService.md` §«Факт подъёма поглощение гасит вместе со статусом» |
| Факт | зелено 2026-09-26 — на ядре `E5.4` |

### E5.4 — На счётном радиусе эскалировать некуда: второй реакции нет, отчёт остаётся незакрытым

| Поле | Содержание |
|---|---|
| Предусловия | общие предусловия тропы; стаб не подтверждает снятие риска |
| Вход | ручная постановка полного класса на **счёте** |
| Ожидаемые выходы — все | **`trading-core`:** строка outbox класса подъёма **одна**; ступень счёта — сворачивание; отчёт в промежуточном статусе; второго прогона снятия риска нет. **`connector-okx`:** число проходов снятия риска у стаба равно одному. **`audit`:** одна строка журнала о подъёме |
| Чем подтверждается | `docs/components/SafetyHoldCoordinator.md` §«Гейт терминала отчёта»; `docs/components/KillSwitchExecutor.md` §«На биржевом радиусе эскалировать некуда — и это терминал, а не тупик» |
| Факт | зелено 2026-09-26 |

## E6 — Ручная пара: постановка, доведение и снятие

### E6.1 — Ручная полная постановка идёт тем же составом, а различают её код и разрез счётчика

| Поле | Содержание |
|---|---|
| Предусловия | общие предусловия тропы; ступеней не стои́т ни одной; стаб подтверждает снятие риска |
| Вход | `POST /api/v1/trading-core/safety/halts` полного класса на счёте |
| Ожидаемые выходы — все | **`trading-core`:** состав и порядок те же, что у автоматической тропы E1.2 — подъём, отчёт, снятие риска, терминал отчёта, каскад; код причины — код ручной постановки; критичность отчёта критичная. **`connector-okx`:** те же команды в том же порядке. **`audit`:** строки журнала тех же классов, и код причины в содержимом — ручной. **`statistics`:** счётчик подъёмов и счётчик жёстких выросли на единицу, и **сверх них** вырос счётчик ручных; счётчик отчётов и критичных выросли на единицу, и сверх них — счётчик отчётов ручной операции; счётчик заведённых сделок и счётчик решений о заявке **не двигались** — ручная остановка ни того, ни другого класса не производит |
| Чем подтверждается | `docs/components/HoldService.md` §«Ручная постановка объявлена той же тропой, только решение принимает человек»; `docs/rules/manual-halt.md` §«Наблюдаемость: ручное отличимо и от автоматики, и друг от друга»; `docs/rules/statistics-aggregates.md` §«Счётчики происшествий — своё зерно, а не строка сделочного агрегата» |
| Факт | зелено 2026-09-26 — состав сверен списком команд у стаба с автоматической тропой `E6.2` того же класса |

### E6.2 — Ручной повтор той же ступени, поднятой автоматикой, заводит строку и реакции не гоняет

| Поле | Содержание |
|---|---|
| Предусловия | состояние E1.2: жёсткая ступень счёта поднята автоматической тропой |
| Вход | ручная постановка полного класса на том же счёте; стаб к этому моменту подтверждает плоский срез |
| Ожидаемые выходы — все | **`trading-core`:** строка отчёта ручным кодом **заводится** — по ключу её ещё нет; ступень не переставлена, и строки outbox класса подъёма не прибавилось; снятия риска нет — права на доведение у этого вызова нет, риск погашен. **`connector-okx`:** команд у стаба нет ни одной. **`statistics`:** счётчик подъёмов не двигался; счётчик отчётов, счётчик **критичных** и счётчик отчётов ручной операции выросли на единицу каждый — строка поглощённого сигнала заводится критичной, потому что критичность производна от состава затребованной реакции, а не от того, состоялась ли перестановка |
| Чем подтверждается | `docs/rules/manual-halt.md` §«Наблюдаемость: ручное отличимо и от автоматики, и друг от друга» (строка «постановка, **повтор той же ступени**, поднятой **автоматикой**»); `docs/components/HoldService.md` §«Поглощение по уже стоящей ступени гасит шаги 2-4, но не шаг 1» |
| Факт | зелено 2026-09-26 |

### E6.3 — Ручное доведение при непогашенном риске гоняет снятие заново, а события подъёма не рождает

| Поле | Содержание |
|---|---|
| Предусловия | состояние E4.5: жёсткая ступень счёта стои́т, снятие риска автоматикой не подтвердилось, срез стаба по-прежнему с живой позицией |
| Вход | ручная постановка полного класса на том же счёте; стаб на этот раз подтверждает команды и отдаёт плоский срез |
| Ожидаемые выходы — все | **`connector-okx`:** команды снятия риска ушли к стабу **заново**, полным составом и в том же порядке. **`trading-core`:** ступень не переставлена, строки outbox класса подъёма не прибавилось; отчёт ручным кодом заведён и завершён по подтверждённому снятию; автоматическая строка осталась незакрытой. **`statistics`:** счётчик подъёмов не двигался; счётчик ручных отчётов вырос |
| Чем подтверждается | `docs/rules/manual-halt.md` §«Идемпотентность наследуется, а не обходится»; `docs/components/SafetyHoldCoordinator.md` §«Анкер и доведение недоделанного» |
| Факт | зелено 2026-09-26 — на ядре `E4.5`; площадка снова исполняет закрытие |

### E6.4 — Автоматический сигнал права на доведение не имеет

| Поле | Содержание |
|---|---|
| Предусловия | то же состояние E4.5 |
| Вход | тик детекции, чей признак держится именно потому, что снятие не подтвердилось |
| Ожидаемые выходы — все | **`connector-okx`:** команд снятия риска у стаба **не прибавилось** — автоматический сигнал поглощён анкером. **`trading-core`:** ступень стои́т, строк outbox не прибавилось, отчёт остался незакрытым |
| Чем подтверждается | `docs/components/SafetyHoldCoordinator.md` §«Почему исключение не распространяется на автоматический сигнал»; `docs/components/AnomalyJob.md` §«Повторное снятие риска джоба не запускает» |
| Факт | зелено 2026-09-26 — на ядре `E4.5` |

### E6.5 — Снятие сворачивания при живом риске отвергается, и до площадки вызов не доходит

| Поле | Содержание |
|---|---|
| Предусловия | жёсткая ступень счёта стои́т; у счёта есть сделка с недоказанным отсутствием живого риска |
| Вход | `POST /api/v1/trading-core/safety/halt-clearances` полного класса на счёте |
| Ожидаемые выходы — все | **`trading-core`:** синхронный отказ поверхности; ступень счёта не изменилась; строки отчёта **нет** — отказ при запуске строки не заводит. **`connector-okx`:** к стабу не ушло ни одного запроса: снятие на площадку не ходит вовсе. **`audit`, `statistics`:** следа нет |
| Чем подтверждается | `docs/rules/manual-halt.md` §«Снятие: что во что переходит и при каком предусловии»; `docs/spec/manual-halt.json`, величина `clearanceRefused` |
| Факт | зелено 2026-09-26 — на ядре `E4.5`; отказ — `400` |

### E6.6 — Снятие сворачивания при погашенном риске переводит счёт в мягкую ступень, а не в рабочее состояние

| Поле | Содержание |
|---|---|
| Предусловия | жёсткая ступень счёта стои́т; сделки радиуса терминальны, живого риска нет |
| Вход | снятие полного класса на счёте |
| Ожидаемые выходы — все | **`trading-core`:** ступень счёта — **мягкая**; строка отчёта заведена кодом снятия, природа — происшествие, некритичная; строки outbox класса подъёма нет, класса снятия **нет тоже** — событие снятия не производит никто (долг). **`connector-okx`:** запросов нет ни одного. **`audit`:** строка журнала об отчёте снятия. **`statistics`:** счётчик отчётов и счётчик отчётов ручной операции выросли на единицу; счётчик подъёмов не двигался |
| Чем подтверждается | `docs/rules/manual-halt.md` §«Снятие: что во что переходит и при каком предусловии»; `docs/spec/manual-halt.json`, величина `clearanceTarget`; `.claude/work/backlog.md` §«Класс события `HoldReleased` и его ручная тропа» |
| Факт | зелено 2026-09-26 — после аварийного терминала сделки ядра `E4.5`; природа строки колонкой не несётся — проверены код и критичность |

### E6.7 — Второе снятие той же ступени холостое, и строки не даёт

| Поле | Содержание |
|---|---|
| Предусловия | состояние E6.6: счёт в мягкой ступени |
| Вход | повтор того же вызова снятия полного класса |
| Ожидаемые выходы — все | **`trading-core`:** ступень счёта осталась мягкой — по лестнице вниз вызов не шагает; второй строки отчёта нет; отказа тоже нет. **`audit`, `statistics`:** новых строк и фактов нет |
| Чем подтверждается | `docs/rules/manual-halt.md` §«Ступень называется явно и у снятия тоже»; `docs/spec/manual-halt.json`, величина `clearanceNoop` |
| Факт | зелено 2026-09-26 |

### E6.8 — Снятие мягкого класса под стоящей жёсткой ступенью — прыжок, и он отвергается

| Поле | Содержание |
|---|---|
| Предусловия | жёсткая ступень счёта стои́т; живого риска на радиусе нет |
| Вход | снятие **мягкого** класса на счёте |
| Ожидаемые выходы — все | **`trading-core`:** синхронный отказ; ступень счёта не изменилась; строки отчёта нет. **`audit`, `statistics`:** следа нет. **Названная цена:** держатель, читающий торговое состояние поверхностью, ступень **пары** не видит и на инструментном радиусе выбирает имя вслепую (`.claude/work/backlog.md` §«Поверхность торгового состояния отдаёт факт стоящей ступени пары, а не саму ступень») |
| Чем подтверждается | `docs/spec/manual-halt.json`, величина `clearanceRefused`; `docs/rules/manual-halt.md` §«Две ступени — два хода» |
| Факт | зелено 2026-09-26 — на ядре `E6.2`; класс снятия на счёте — `FREEZE`, отказ — `400` |

## E7 — Журнал и статистика на этой тропе

### E7.1 — Классы тропы ложатся строками журнала одной группы в порядке происшествий

| Поле | Содержание |
|---|---|
| Предусловия | тропа пройдена от E1.1 до E3.5 с фиксацией моментов |
| Вход | `GET /api/v1/audit/journal/records` окном тропы |
| Ожидаемые выходы — все | **`audit`:** строки классов «отчёт о происшествии», «подъём ступени», «остановка сделки» и «терминал сделки» лежат в порядке их моментов происшествия; у каждой — идентичности тенанта и счёта из конверта и момент, а актор — у классов с ручной тропой; у терминала сделки его нет (**прогоном поправлено на месте**: прежде актор ждался у каждой строки — вопреки дому его присутствия); строк чужих классов в окне нет. **`trading-core`:** число опубликованных строк outbox равно числу строк журнала окна |
| Чем подтверждается | `docs/architecture/contracts.md` §«Конверт события»; `docs/models/domain/other/AuditRecord.md` §«Как журнал читается»; `docs/spec/event-actor-presence.json`, величина `actorTravelsInContent` |
| Факт | зелено 2026-09-26 — окно несёт и повторы `E4.3`; актор поправлен по дому (выше) |

### E7.2 — Счётчики происшествий растут по классам и разрезам, а не по предметам

| Поле | Содержание |
|---|---|
| Предусловия | тропа пройдена автоматической тропой (E1.1-E1.2) и ручной (E6.1) на одном счёте в одни сутки |
| Вход | тик пересчёта агрегатов; `GET /api/v1/statistics/aggregates/rows` |
| Ожидаемые выходы — все | **`statistics`:** в строке зерна «тенант × счёт × сутки» счётчик подъёмов равен числу **подъёмов**, а не числу объектов под ступенью; счётчик жёстких — их жёсткой доле; счётчик ручных — доле ручной тропы, и эскалация в него не входит; счётчик отчётов складывает обе тропы, счётчик критичных — критичную долю, счётчик отчётов ручной операции — ручную. **`audit`:** числа журнала и числа агрегата собраны из **разных** рядов и совпадать не обязаны — клейм полноты у каждого свой |
| Чем подтверждается | `docs/rules/statistics-aggregates.md` §«Счётчики происшествий — своё зерно, а не строка сделочного агрегата»; `docs/rules/statistics-aggregates.md` §«У подъёма ступени расхождение то же, а счётчик есть и верен» |
| Факт | зелено 2026-09-26 — сторона `audit` ассерта не несёт: «совпадать не обязаны» есть отсутствие требования, а не выход |

### E7.3 — Разрез ручной тропы читается СЛОВОМ провода, и слово это у писателя не запинено

| Поле | Содержание |
|---|---|
| Предусловия | тропа пройдена ручной постановкой (E6.1) и ручным снятием (E6.6) |
| Вход | тик пересчёта агрегатов |
| Ожидаемые выходы — все | **`statistics`:** счётчик ручных подъёмов и счётчик отчётов ручной операции отобрали свои строки по коду в содержимом события, а не по актору: у тика детекции, запущенного ручным фасадом, актор равен принципалу, и по актору автоматическая остановка попала бы в число ручных. **`trading-core`:** в содержимом обоих классов лежит ровно тот код, по которому отбирает потребитель. **Названное ограничение:** совпадение двух слов держится дисциплиной пишущего — пин у писателя символьный, а перечень слов провода выводится командой, которая слова-параметры не печатает (`.claude/work/backlog.md` §«Третья ось того же стыка — команда перечня слепа к слову, уезжающему параметром») |
| Чем подтверждается | `docs/rules/manual-halt.md` §«Наблюдаемость: ручное отличимо и от автоматики, и друг от друга»; `.claude/rules/codestyle.md` §«Пин значения, пересекающего провод: слово или символ» |
| Факт | зелено 2026-09-26 — актор автоматического подъёма и ручного один и тот же принципал |

### E7.4 — Поглощённый повтор ни одного числа не двигает

| Поле | Содержание |
|---|---|
| Предусловия | состояние E4.3: ступень стои́т, повторы поглощены |
| Вход | тик пересчёта агрегатов |
| Ожидаемые выходы — все | **`statistics`:** счётчик подъёмов равен числу состоявшихся перестановок, а не числу сигналов; счётчик отчётов равен числу заведённых строк. **`audit`:** строк журнала столько же, сколько классов было опубликовано |
| Чем подтверждается | `docs/rules/statistics-aggregates.md` §«Поглощённый повтор ступени неполнотой не является»; `docs/components/HoldService.md` §«Факт подъёма поглощение гасит вместе со статусом» |
| Факт | зелено 2026-09-26 — повторы поданы до аварийного терминала сделки: после него чужая заявка сиротская, и детектор поднимает мягкую ступень пары — иное основание, а не повтор |

### E7.5 — Остановка сделки числа не даёт ни одного

| Поле | Содержание |
|---|---|
| Предусловия | состояние E3.1, но одна из двух сделок к моменту каскада стоя́ла в координированном выходе со своей причиной — каскад переписал её на ребре в ошибочное состояние |
| Вход | тик пересчёта агрегатов |
| Ожидаемые выходы — все | **`statistics`:** фактов происшествия класса остановки **нет ни одного** — класс признаку несомого не отвечает объявленным зерном, и счётчика под него не заведено; число закрытых сделок двигает только терминал. **`audit`:** строки журнала об остановках есть, и их три — по числу рёбер присвоения причины |
| Чем подтверждается | `docs/rules/statistics-aggregates.md` §«Счётчика остановленных сделок нет, и довод — объявленный предмет счёта»; `docs/models/domain/other/StatisticsFact.md` §«Признак несомого класса» |
| Факт | не прогоняется — кода клетки нет: две сделки счёта к моменту каскада требуют пролога двух инструментов, которого у набора ещё нет |

### E7.6 — Полнота приёма обеих групп доходит до конца тропы

| Поле | Содержание |
|---|---|
| Предусловия | тропа пройдена целиком; обе durable-группы прочитали тему до конца |
| Вход | тики состояния приёма у обеих сторон |
| Ожидаемые выходы — все | **`audit`:** строка состояния приёма несёт обе величины полноты, предикат непрерывности истинен, разрывов нет. **`statistics`:** своя строка состояния приёма, своя нижняя граница одним операндом, предикат истинен; выборка агрегатов отдаёт границу рядом с числами. **Обе группы двигают свои смещения независимо**, и здоровье одной о другой не свидетельствует |
| Чем подтверждается | `docs/rules/durable-consumer-reception.md`; `docs/models/domain/other/StatisticsFact.md` §«Своя группа — не то же, что своя строка, и обе несущие» |
| Факт | зелено 2026-09-26 |

### E7.7 — Второй такт пересчёта даёт те же числа

| Поле | Содержание |
|---|---|
| Предусловия | состояние E7.2 |
| Вход | ещё один тик пересчёта агрегатов без новых событий |
| Ожидаемые выходы — все | **`statistics`:** числа строки зерна те же; строк агрегата столько же — пересчёт есть проекция, а не накопитель; фактов не прибавилось. **`audit`:** строк журнала столько же |
| Чем подтверждается | `docs/rules/statistics-aggregates.md` §«Пересчёт — проекция, а не накопитель» |
| Факт | зелено 2026-09-26 |

## E8 — Порядок, отсутствие следов и повтор тропы

### E8.1 — След стороны не появляется раньше причины на предыдущей

| Поле | Содержание |
|---|---|
| Предусловия | тропа пройдена от E1.1 до E7.2 с фиксацией моментов |
| Вход | разбор моментов у всех сторон |
| Ожидаемые выходы — все | **Порядок сквозной:** расхождение в срезе стаба раньше первой строки отчёта; строка наблюдения раньше подтверждения; ребро подъёма ступени раньше открытия критичного отчёта; открытие отчёта раньше первой команды снятия риска; отмена входных ног раньше закрытия позиции; закрытие раньше снятия защит; подтверждение плоского среза раньше терминала отчёта; терминал отчёта раньше первого ребра каскада; ребро каскада раньше строки журнала о нём; факт раньше строки агрегата. **Ни один след не предшествует своей причине** |
| Чем подтверждается | `docs/components/SafetyHoldCoordinator.md` §Последовательность; `docs/rules/exit-teardown-order.md`; `docs/architecture/data-ownership.md` |
| Факт | зелено 2026-09-26 — пролог с добором; ручная часть `E7.2` звеньев цепочке не добавляет, и тропа идёт автоматической |

### E8.2 — Сторона вне тропы следа не оставляет ни одного

| Поле | Содержание |
|---|---|
| Предусловия | тропа пройдена целиком до E3.5 |
| Вход | разбор состояния стабов и сторон |
| Ожидаемые выходы — все | **`strategies`:** его база не менялась и строк outbox не прибавилось — определение аварией не трогается, и стороной этой тропы он не является. **`market-data`:** к стабу ушло **ровно по одному** чтению раскладки на каждое построение контекста прохода — по нетерминальной сделке на каждом тике детекции (детекторы инварианта сделки), в счёт-широком снятии риска и на каждом тике сопровождения — и **ни одного сверх них**: контекст собирается вместе с раскладкой безусловно (**прогоном поправлено на месте**: прежде операнды детекции объявлялись взятыми только из среза площадки и строк ядра — вопреки детекторам инварианта сделки, строки `A4` и соседние таблицы детекторов `docs/components/AnomalyJob.md`). **`auth`:** запросов сверх пролога нет. **Провайдер идентичности:** запросов сверх выдачи токенов нет. **`bff`:** стороной не является и не поднимается вовсе |
| Чем подтверждается | `docs/architecture/contracts.md` §«Синхронные вызовы»; `docs/architecture/services.md`; `docs/architecture/data-ownership.md` |
| Факт | зелено 2026-09-26 — чтения раскладки: тик наблюдения — одно, тик реакции — два, проход — по одному; неподъём `bff` — свойство раскладки набора (`Trail#open`), а не ассерт |

### E8.3 — Повтор тропы целиком даёт то же состояние

| Поле | Содержание |
|---|---|
| Предусловия | тропа пройдена целиком; набор поднят заново на тех же базах |
| Вход | те же ходы тропы в том же порядке |
| Ожидаемые выходы — все | **`trading-core`:** ступень та же и одна, вторых строк отчёта по тем же ключам нет, каскад сделок не переприменён, терминал не переприменён. **`audit`:** строк журнала столько же. **`statistics`:** фактов столько же, числа агрегата те же. **`connector-okx`:** повторные чтения срезов у стаба есть, повторных **команд** нет |
| Чем подтверждается | `docs/rules/idempotency-via-unique.md`; `docs/components/SafetyHoldCoordinator.md` §«Анкер и доведение недоделанного» |
| Факт | красно 2026-09-26 — долг доминирования (`.claude/work/backlog.md` §«Доминирование биржевых ступеней над инструментными реакциями энфорсера не имеет»): после терминала сделки чужая заявка сиротская, и мягкий сигнал пары поднимает её ступень под стоящим сворачиванием счёта; метка `debt` |

### E8.4 — Отсутствие выходов у тропы целиком

| Поле | Содержание |
|---|---|
| Предусловия | тропа пройдена целиком до состояния E7.2 |
| Вход | разбор состояния всех сторон |
| Ожидаемые выходы — все | **Новых сделок на счёте нет** — счёт выпал из выборки входа стоящей ступенью, и тик отбора входа их не заводит. **Живой позиции и живых заявок контура у счёта нет** — срез позиций стаба плоский; чужая заявка стои́т — её снятие риска не трогает, и она предмет детектора `A6` (`E4.3`; **прогоном поправлено на месте**: прежде «живых заявок у счёта нет» — вопреки собственному звену документа). **Ни одной торговой команды после снятия риска к стабу не ушло**: блок-сет ступени держит счёт, а чтения остаются открытыми. **Строк отказа доступа нет** — все вызовы прошли с валидным токеном. **Ни одна сторона не писала в чужую базу**: имя владельца каждой изменённой таблицы совпадает со стороной, которая её изменила. **Наружу за пределы стабов не ушло ни одного запроса.** **Серия убытков не двигалась** — её двигает только ценовой результат закрытой сделки, а снятие ступени не трогает вовсе |
| Чем подтверждается | `docs/rules/exchange-hold.md` §«После teardown на площадку не уходит ни одна торговая команда счёта»; `docs/rules/loss-streak-halt.md` §«Что двигает счётчик»; `docs/architecture/data-ownership.md`; `docs/models/domain/other/AccessDenial.md` |
| Факт | зелено 2026-09-26 — отказы доступа читаются у сторон с базой отказов (`audit`, `statistics`); запись в чужую базу и выход за стабы набором не наблюдаемы: база у стороны своя по конфигурации процесса, а адреса соседей — стабы |

## Где живёт код кейсов

Модуль сквозного набора `tests/`, пакет `com.example.tests.e2e.safetyteardown`,
класс на группу: `TeardownDetectionPathTest` (`E1`; ядер три — `E1.1`-`E1.2`,
`E1.3` с тропой слепоты `E1.6`-`E1.8`, `E1.4` с `E1.5`, — каждое поднято заново
прологом; минимальный возраст подтверждения и предел слепоты — ключи процесса
ядра `anomaly-job.confirmation-min-age`, `anomaly-job.blind-pass-limit`),
`TeardownOrderPathTest` (`E2`; каждая клетка на своём ядре, `E2.4` — на ядре
`E2.3`; пролог с добором — `ExitTrail#walkToScaledIn`, отмену второй ноги
площадка отражает чтением её одной — `Stub#answersWhereInState`; закрытие без
исполнения — `TeardownTrail#exchangeAcknowledgesCloseWithoutEffect`),
`TeardownCascadePathTest` (`E3`; `E3.3`, `E3.5`, `E3.4` — одно ядро, `E3.2` и
`E3.6` — свои; сделка, активная под стоящей ступенью счёта, заводится
восстановлением после аварийного терминала первой —
`TeardownTrail#recoverAfterCascade`; ручная постановка —
`TeardownTrail#halt`), `TeardownReportPathTest` (`E4`; `E4.4`, `E4.1`,
`E4.2` читают один тик эскалации, `E4.3` продолжает ядро, `E4.5`, `E4.6` и
`E4.7` — свои, `E4.6` раньше `E4.7`; чужая позиция без сделок —
`TeardownTrail#exchangeHoldsOnlyForeignPosition`; отказ вставки —
`Database#refuseInserts`, неисправность субстрата, а не поставленное состояние),
`TeardownEscalationPathTest` (`E5`; `E5.1`-`E5.2` — один ход, `E5.3` — на
ядре `E5.4`; конец реакции ручной постановки читается записью ядра),
`TeardownManualPathTest` (`E6`; ядер три — `E6.2` с `E6.8` на автоматической
ступени, `E6.1` на чистом счёте, `E6.5`, `E6.4`, `E6.3`, `E6.6`, `E6.7` на ядре
`E4.5`; команды автоматического снятия `E6.2` — эталон состава ручных троп;
полная постановка ждётся записью фасада — `TeardownTrail#haltFully`, снятие —
`TeardownTrail#clear`, возврат исполнения закрытия —
`TeardownTrail#exchangeConfirmsClose`),
`TeardownJournalPathTest` (`E7`; ядер два при одной статистике — автоматическое
ведёт `E7.4` и `E7.1` до аварийного терминала, ручное — `E7.2`, `E7.3`, `E7.7`,
`E7.6`; числа первого ядра сняты до его пересоздания),
`TeardownTracePathTest` (`E8`; ядро одно, пролог с добором, журнал стаба не
забывается от расхождения до конца каскада, проходы сопровождения подаются по
одному; `E8.4` идёт раньше повтора `E8.3`). Ходы
тропы — `TeardownTrail`: пролог — `ExitTrail#walkToExposure` тропы выхода,
открытый этой тропе; расхождение ставится ответом стаба по параметру
счёт-широкого чтения (тип инструмента у позиций, семья у условных заявок), и
штатных чтений по инструменту оно не трогает; тик детекции — ручным фасадом;
числа статистики читаются строкой зерна происшествий, сложенной после приёма
каждого опубликованного события несомых классов (`TeardownTrail#incidents`).

## Кейсы, не прогоняемые сегодня

**Код есть у всех кейсов групп `E1`, `E4`, `E5`, `E6` и `E8`, у `E2.1`-`E2.4`,
`E3.2`-`E3.6` и у группы `E7` без `E7.5`; `E2.5`, `E2.6`, `E3.1` и `E7.5` ждут
пролога двух инструментов. Красны две клетки — `E3.4`
(находка `F7`) и `E8.3` (долг доминирования биржевых ступеней), обе меткой
`debt`.** Их
пролог — ходы `.claude/tests/cases/e2e-strategy-to-deal.md` до `E5.1` и ход
`E1.1` тропы `e2e-exit-and-close`, — с 2026-09-24 достижим: находка `F6`
первой тропы, на которой он останавливался, закрыта кодом, и ходы первой тропы
написаны (`.claude/tests/cases/e2e-strategy-to-deal.md` §«Где живёт код
кейсов»). Код клеток пишется по группам — секция `.claude/work/backlog.md`
§«Клетки тропы активации за копией определения ждут закрытия находки `F6`».

**Кейса, чьё предусловие недостижимо в среде прогона, у этой тропы нет ни
одного.** Пролог ставится ходами двух чужих троп, субстрат и стабы — обвязкой
набора, расхождение вносится ответом стаба, а живой площадки тропа не
касается: её мишень — стаб.

**Недостижима не среда, а исправность предмета** — §«Тропа сегодня не
прогоняется целиком, и причина та же». Красный прогон кейса, чей след лежит
за публикацией, есть предъявление названного долга, а не пометка
недостижимости.

## Контрольный прогон на нейтрализованной сборке

Прогон 2026-09-26 по сборкам, заведённым заходом группы `E1` (чужая заявка в
срезе — `TeardownTrail#exchangeHoldsForeignOrder`; отказ среза условных заявок —
`TeardownTrail#exchangeRefusesAlgoSlice`); база — класс
`TeardownDetectionPathTest`, восемь клеток. Мутация ставилась и снималась
восстановлением файла из индекса.

| Ось | Мутация | Предсказано | Измерено |
|---|---|---|---|
| 1 | клиентский идентификатор заявки несёт маркер контура — заявка своя | красны `E1.1`, `E1.2`, `E1.3` на предусловии и `E1.4` — строки наблюдения нет; `E1.5`-`E1.8` зелены | так — сошлось |
| 2 | семья условных заявок отвечает успехом — срез полон | красны `E1.6`, `E1.7` на предусловии и `E1.8`; прочие пять зелены | так — сошлось; `E1.6` красна раньше предсказанного места — ступенью счёта: чужая заявка, вернувшаяся после чистого тика `E1.3`, подтвердилась стоящей строкой сразу (находка `F6` этого документа) |

Прогон 2026-09-26 по сборкам, заведённым заходом группы `E2` (отмена второй
ноги у площадки — `TeardownTrail#walkToScaledIn` на `Stub#answersWhereInState`;
закрытие без исполнения — `TeardownTrail#exchangeAcknowledgesCloseWithoutEffect`);
база — класс `TeardownOrderPathTest`, четыре клетки.

| Ось | Мутация | Предсказано | Измерено |
|---|---|---|---|
| 1 | принятая отмена второй ноги её чтения не меняет | красна одна `E2.1` — вторая нога остаётся живой; прочие три пролога с добором не зовут | так — сошлось |
| 2 | закрытие исполняется — ответ пролога не перекрыт | красны `E2.3` и `E2.4` — отчёт завершён; `E2.1`, `E2.2` зелены | так — сошлось |

Прогон 2026-09-26 по сборкам, заведённым заходом группы `E3` (восстановление
после каскада — `TeardownTrail#recoverAfterCascade`; ручная постановка —
`TeardownTrail#halt`); база — класс `TeardownCascadePathTest`, пять клеток,
`E3.4` до мутации красна своим последним ожиданием.

| Ось | Мутация | Предсказано | Измерено |
|---|---|---|---|
| 1 | восстановление не сбрасывает сценарий площадки — позиция читается плоской | красны `E3.4` раньше долгового ожидания и `E3.2` — предусловие не ставится; `E3.3`, `E3.5`, `E3.6` зелены | так — сошлось |
| 2 | постановка не называет инструмент — повтор ступени счёта | красна одна `E3.2` — ступень пары не переставлена; прочие зелены (`E3.4` — вне умолчания) | так — сошлось |

Прогон 2026-09-26 по сборке, заведённой заходом группы `E4` (чужая позиция
без сделок — `TeardownTrail#exchangeHoldsOnlyForeignPosition`); база — класс
`TeardownReportPathTest`, шесть клеток.

| Ось | Мутация | Предсказано | Измерено |
|---|---|---|---|
| 1 | принятое закрытие чужой позиции площадка не отражает | красна одна `E4.7` — снятие не подтверждено; прочие пять ход не зовут | так — сошлось; статус строки — снятие риска выполнено, хотя не подтверждено (долг `.claude/work/backlog.md` §«Статус подтверждённого снятия риска ставится и на неподтверждённом») |

Прогон 2026-09-26 по сборке, заведённой клеткой `E4.6` (отказ вставки —
`Database#refuseInserts`); база — класс `TeardownReportPathTest`, семь клеток.

| Ось | Мутация | Предсказано | Измерено |
|---|---|---|---|
| 1 | отказ вставки не ставится — метод возвращается сразу | красна одна `E4.6` — критичная строка отчёта заведена; прочие шесть неисправности базы не зовут | так — сошлось |

Заход группы `E5` базовых сборок не заводил — только ожидание конца реакции
по записи ядра; постановка на паре и закрытие без исполнения измерены осями
групп `E2` и `E3`, и контрольного прогона у `E5` нет.

Прогон 2026-09-26 по сборкам, заведённым заходом группы `E6` (возврат
исполнения закрытия — `TeardownTrail#exchangeConfirmsClose`; снятие —
`TeardownTrail#clear`); база — класс `TeardownManualPathTest`, восемь клеток.

| Ось | Мутация | Предсказано | Измерено |
|---|---|---|---|
| 1 | возврат исполнения закрытия ничего не заводит — площадка по-прежнему закрытие не исполняет | красны `E6.3` — команды доведения повторяют закрытие по бюджету, — `E6.6` — сделка до аварийного терминала не доходит — и `E6.7` — отказ по живому риску; прочие пять зелены | так — сошлось |
| 2 | снятие называет полный класс, какой бы ни был передан | красна одна `E6.8` — снятие проходит и опускает ступень; прочие семь зовут снятие полным классом | так — сошлось; подстановка задела и постановку — она зовётся только полным классом, и мутация на ней эквивалентна |

Заход группы `E7` базовых сборок тропы не заводил — ходы постановки и снятия
измерены осями группы `E6`, — и контрольного прогона у `E7` нет. Тем же
основанием его нет и у группы `E8`: её ходы — ходы групп `E1`, `E2` и `E3`.

## Находки владельцам

| Метка | Находка | Где припаркована |
|---|---|---|
| `F1` | перечень слов провода выводится командой, слепой к слову, уезжающему в запрос параметром: коды ручной операции ветвят два счётчика статистики, а пин у них символьный | `.claude/work/backlog.md` §«Третья ось того же стыка — команда перечня слепа к слову, уезжающему параметром» |
| `F2` | поверхность торгового состояния отдаёт по паре «счёт, инструмент» только идентичности со стоящей ступенью, а снятие обязано назвать ступень | `.claude/work/backlog.md` §«Поверхность торгового состояния отдаёт факт стоящей ступени пары, а не саму ступень» |
| `F3` | дом перечня детекторов разорван прозой на две таблицы, и у семи строк из двенадцати колонки читателю не подписаны | `.claude/work/backlog.md` §«Таблица детекторов аномалий разорвана прозой на две, и вторая без шапки» |
| `F4` | ручная полная остановка на паре «счёт, инструмент» живого риска не снимает ни одной командой: единственный её вызывающий строит контекст без сделки, а исполнитель снятия обходит граф **триггерной** сделки — обход падает до первой команды, реакция уходит в ошибочный статус отчёта и до развилки эскалации не доходит | закрыта кодом (заход 153) — `.claude/work/history/2026-09-24-step-12-sections-closed-by-code.md` |
| `F5` | снимки «до» и «после» у счёт-широкой реакции несут только идентичности и ступень счёта: сделки в контексте нет, инструмента нет, внешний снимок пуст — то есть у самой тяжёлой реакции контура ни экспозиция до, ни остаток после не наблюдаемы, хотя дом объявляет снимок «после» носителем остаточного риска и называет ровно одно исключение (отказ в кредах) | `.claude/work/backlog.md` §«Снимки отчёта счёт-широкой реакции несут только поля счёта» |
| `F6` | гистерезис объявлен числом **подряд идущих** тиков, а носитель подтверждения — стоящая строка наблюдения в окне — чистым тиком между наблюдениями не прерывается: признак, замеченный, пропавший и вернувшийся в пределах окна, подтверждается сразу и поднимает жёсткую ступень счёта со снятием риска (наблюдение контрольного прогона группы `E1`, ось 2) | `.claude/work/backlog.md` §«Подтверждение гистерезиса не прерывается чистым тиком» |
| `F7` | жёсткая ступень объявлена снятием принятого риска, а у сделки, ставшей активной после каскада, построен только её перевод в ошибку: команд снятия живой позиции не шлёт ни шаг прохода, ни обработчик ошибочного состояния, анкер поглощает повтор реакции, детектор `A6` только журналит (клетка `E3.4`) | `.claude/work/backlog.md` §«Живой риск сделки, ставшей активной после каскада, под жёсткой ступенью не снимается никем» |

## Пробелы покрытия

Метки `G`, их исход и форма раздела — `.claude/skills/test-review.md`
§«Что апрув не блокирует».

| Метка | Что не покрыто | Почему кейса нет сегодня | Исход |
|---|---|---|---|
| `G1` | инструментная реакция, пришедшая **проходом FSM** (`INSTRUMENT_MARKET_DATA_EXPIRED` у обработчика активной сделки и у прохода транша): её контекст сделку несёт, и снятие риска по графу триггерной сделки на ней достижимо | весь инструментный радиус документа подан **ручной** поверхностью — это второй эмитент того же радиуса с другим контекстом, то есть другая тропа с другими предусловиями | под-шаг 3: кейс `E2.7` с несвежей раскладкой на шаге, чья эскалация объявлена снятием риска, — единственная сегодня тропа, где инструментное снятие доходит до команд |
| `G2` | детекторы «больше одной позиции на инструмент» и «локально терминальная сущность жива у площадки»: два признака среза из трёх у биржевой стороны и вся группа учётных детекторов | E1 ведёт гистерезис и предел слепоты — оси, ради которых документ и заведён; каждый второй детектор есть свой срез стаба и свой набор предусловий, а перебор признаков берёт ящик своего сервиса | ящик `trading-core` плюс юнит `trading-core-safety`: перебор детекторов — их предмет, сквозная тропа берёт по представителю на класс реакции |
| `G3` | снятие ступени **пары** ручной поверхностью (`ManualHaltClass.SOFT` и полный класс на паре): E6 целиком идёт счётным радиусом | ступень пары поверхностью торгового состояния не читается (`F2`), и ассерт исхода снятия пришлось бы ставить прямым чтением базы по каждому кейсу группы | под-шаг 3: добирается вместе с `F2`, если поверхность ступень отдаст; иначе кейсы `E6.9`-`E6.10` прямым чтением базы |
| `G4` | отказ **самого ребра** подъёма (отказ записи факта откатывает и переход): ветвь объявлена домом как названное исключение из best-effort | ветвь требует отказа записи строки outbox, а не отказа базы целиком — расстановка её стабом площадки недостижима, нужен отказ на конкретной вставке | юнит `trading-core-safety`: предмет уровня 2, там подмена носителя записи законна |

## Связи

- Форма кейса и три уровня — `.claude/skills/test-design.md`.
- Контур тестов и его развилки — `.claude/decisions/test-contour-design-pass.md`.
- Пролог этой тропы — `.claude/tests/cases/e2e-strategy-to-deal.md`,
  `.claude/tests/cases/e2e-exit-and-close.md`.
- Ящики сторон — `.claude/tests/cases/trading-core.md`,
  `.claude/tests/cases/connector-okx.md`, `.claude/tests/cases/audit.md`,
  `.claude/tests/cases/statistics.md`.
- Юнит того же предмета — `.claude/tests/cases/trading-core-safety.md`.
- Лестницы и их составы — `docs/rules/exchange-hold.md`,
  `docs/rules/instrument-hold.md`; ручная пара — `docs/rules/manual-halt.md`;
  последовательность полной реакции — `docs/components/SafetyHoldCoordinator.md`.
- Роль — `.claude/agents/tester.md`; ревью кейсов — `.claude/skills/test-review.md`.
