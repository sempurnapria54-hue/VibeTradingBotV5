# Тест-кейсы: `bff`

## На какой вопрос отвечает этот файл

Какие тест-кейсы у сервиса `bff`.

## Предмет

**Чёрный ящик единицы развёртывания `bff`** — восьмой и последний предмет
уровня 1 перечня под-шага 1 `CODE` шага 12 фазы 2
(`.claude/decisions/test-contour-design-pass.md`
§«Перечень предметов и порядок работы»). Уровень у всех кейсов ниже один —
`B`; мишень у предмета одна, поэтому суффикса мишени метки не несут
(`.claude/skills/test-design.md` §«Форма — предмет → кейсы → таблица»).

Сервис — **единственная дверь периметра**: он проверяет токен предъявителя,
выводит тенанта из его членств, пересылает запрос владельцу данных и раздаёт
браузеру живые данные потоком (`docs/architecture/services/bff.md`). Границы —
§«Чего не делает намеренно» того же дока: у границы кейсов нет, и ниже они не
заводятся.

## Новая ось формы — ПОТОК В БРАУЗЕР, БИЛЕТ И ОТСУТСТВИЕ СВОЕЙ БАЗЫ

Предмет стои́т восьмым ради трёх осей, которых у первых семи не было, и каждая
меняет форму кейса, а не только его содержание:

- **Выход — не ответ и не строка, а ПРОВОД, который держится открытым.**
  У семи предшественников ассерт снимался с ответа, из базы либо из темы —
  все три однократны. Здесь наблюдаемое есть **последовательность записей на
  живом соединении**, и у неё, сверх содержимого, есть две собственные оси:
  **порядок** (клиент восстановить его не может — порядкового номера у записи
  нет) и **момент** (запись, пришедшая после закрытия подписки, не приходит
  никуда). Отсюда всякий кейс потока называет не только «что уехало», но и
  **чем оно кончилось**: подписка закрыта штатно, оборвана, оставлена открытой.
- **Форм предъявления две, и вторая живёт ТОЛЬКО на одной тропе.** Билет —
  не второй контур доступа и не открытая точка
  (`docs/rules/api-access-policy.md` §«Формы предъявления две, и вторая
  исключением не является»), поэтому кейсы билета делятся надвое: группа B2
  мерит **сам билет** как значение (подпись, состав, срок), группа B3 — что
  предъявление им открывает ровно тропу потока и ничего сверх неё. Склеенная
  группа была бы зелена при билете, годном и на прокси.
- **Своей базы нет ни одной, и потому «ассерт по базе» здесь не применим
  вовсе.** Признак решения 7 дизайн-прохода (`.claude/decisions/test-contour-design-pass.md`)
  различает таблицы с внешним читателем и без; у периметра таблиц нет ни тех,
  ни других. Всё его состояние — **эфемерное, в памяти реплики**: кэш членств,
  открытые подписки, окно переигрывания. Читается оно **только поверхностью и
  проводом**, и это не послабление, а ужесточение: состояние, наблюдаемое лишь
  косвенно, требует кейса, который ставит его **чужим** ходом и читает
  **своим** — так группа B4 предъявляет содержимое окна, ни разу к окну не
  обращаясь.

Четвёртое следствие общее для всех трёх осей: **отсутствие выхода у периметра
почти всегда означает «запись не уехала», а не «строки нет».** Пропущенное
событие у клиента неотличимо от «событий не было»
(`docs/architecture/contracts.md` §«Цена пропуска названа»), и потому кейс,
ожидающий пропуска, обязан предъявить **второе**, заведомо доезжающее событие:
иначе зелёным окажется и мёртвая раздача.

## Чем достаются выходы

Контур — `.claude/decisions/test-contour-design-pass.md`, здесь не
пересказывается (`.claude/rules/policy-home.md`). Что из него следует **для
этого предмета**:

- **субстрат — контейнер брокера, и только он.** Базы у периметра нет, Vault
  ему не нужен: ключей площадки он не касается, а единственный его секрет —
  общий у реплик секрет подписи билета — приезжает осью конфигурации. Это
  первый предмет перечня, чей ящик поднимается **без контейнера базы**;
- **провайдер идентичности** — JWKS-стаб с локальной парой ключей,
  `issuer-uri` на стаб; токен подписывает сам тест (решение 4). Исходящей
  идентичности у периметра нет: к владельцам он ходит под токеном
  **пользователя**, а не под служебной
  (`docs/architecture/contracts.md` §«Контекст тенанта в вызове»);
- **соседи — все шесть, и подменяются они одним механизмом.** WireMock на
  случайном порту отвечает и за владельца членств, и за пятерых владельцев
  данных; адрес подаётся **не шестью свойствами, а одним** —
  `perimeter.owner-url-template` с плейсхолдером имени. Это само по себе
  предмет кейса B8.10: перечня владельцев у периметра нет, и новый владелец
  правки периметра не требует;
- **тик как вход — у единственной джобы, и ручного фасада у неё нет
  намеренно.** Пульс потока работы не производит, догонять ему нечего, а такт
  `fixedDelay` следующий запуск до конца текущего не пускает
  (`.claude/rules/codestyle.md` §Джобы). Поэтому расписание гасится
  выключателем `perimeter.stream.pulse-enabled`, а тик подаётся **прямым
  вызовом метода джобы** — решение 6 называет эту точку единственной, где кейс
  уровня 1 касается бина, и объявляет её входом, а не подменой;
- **возраст ставится в данных и в конфигурации.** Срок билета, срок записи
  кэша, срок соединения и ширина окна — оси конфигурации, и кейс выбирает их
  значениями прогона, а не ожиданием. Часы процесса не двигаются ни в одном
  кейсе; момент происшествия записи ставится **заголовком конверта**;
- **асинхронный след** (появление записи в проводе после положенной в тему
  записи, молчание пульса, закрытие подписки по сроку) ждётся `Awaitility` с
  ограниченным таймаутом. Ответ поверхности асинхронным следом не является;
- **ассерт по базе не идёт ни в одном кейсе, и это не умолчание.** Его нет,
  потому что базы нет (`docs/architecture/services/bff.md` §«Что держит
  истиной»); эфемерное состояние читается тем же проводом и той же
  поверхностью, которыми оно и наполняется. Единственное исключение из
  «только поверхность» — счёт `@Scheduled`-методов дерева в B5.10: он мерит
  **инвариант вывода числа**, а не поведение, и форма его задана общей пробой
  (`.claude/rules/codestyle.md` §Джобы).

## Ожидание берётся из дома, даже когда сегодня оно не исполнено

Часть кейсов ниже ожидает то, что дом объявляет, а дерево кода ещё не несёт.
Такие ожидания **не ослабляются под текущий факт**: кейс проверяет контракт, а
не сегодняшнее поведение, и его красный прогон и есть предъявление названного
долга. У каждого такого кейса в поле «чем подтверждается» стои́т дом **и**
секция бэклога, в которой долг припаркован:

| Долг | Где припаркован |
|---|---|
| срок билета короче срока соединения — штатный разрыв попадает вне его | `.claude/work/backlog.md` §«Срок билета подписки короче срока соединения, и штатный разрыв попадает вне его» |
| неразбираемый момент происшествия роняет раздачу вместо пропуска | `.claude/work/backlog.md` §«Неразбираемый момент происшествия роняет раздачу вместо пропуска» |
| окно переигрывания копится по тенантам без подписок и не вытесняется | `.claude/work/backlog.md` §«Окно переигрывания периметра копится по тенантам без подписок и не вытесняется» |
| переигрывание идёт после регистрации подписки | `.claude/work/backlog.md` §«Переигрывание идёт после регистрации подписки, и параллельный факт обгоняет хвост» |
| отказ доступа не оставляет ни лога, ни метрики | `.claude/work/backlog.md` §«Отказ доступа у периметра не оставляет ни лога, ни метрики» |
| переговорные заголовки обещаны шире, чем пересылаются | `.claude/work/backlog.md` §«Переговорные заголовки содержимого периметр обещает шире, чем пересылает» |
| перехватчик `Exception` съедает отказ по правам | `.claude/work/backlog.md` §«Перехватчик `Exception` съедает отказ по правам у `bff` и `strategies`» |
| api-формы периметра дока не имеют | `.claude/work/backlog.md` §«Api-формы периметра без дока — хвост шага 9 фазы 2» |

## Поверхность предмета

| Операция | Метод и путь | Кто зовёт | Дом ожидания |
|---|---|---|---|
| контекст предъявителя | `GET /api/v1/bff/context` | браузер | `docs/architecture/contracts.md` §«Контекст тенанта в вызове» |
| выдача билета подписки | `POST /api/v1/bff/stream-tickets` | браузер | `docs/architecture/contracts.md` §«Подписку открывает билет, а не сам токен» |
| открытие подписки на поток | `GET /api/v1/bff/stream` | браузер (`EventSource`) | `docs/architecture/contracts.md` §«Живые данные в браузер» |
| пересылка владельцу | любой глагол на `/api/v1/{owner}/**` | браузер | `docs/architecture/contracts.md` §«Периметр: что `bff` отдаёт и чего не делает» |
| проба живости | `GET /actuator/health` | развёртывание | `docs/rules/api-access-policy.md` §«Вся поверхность закрыта; открытое — перечислено» |

**Входящее сверх поверхности — записи тем, у которых есть производитель**
(`trading-core.facts`, `strategies.facts`), и **один такт расписания** — тик
пульса потока. **Исходящее — шесть владельцев**: `auth` за членствами,
`strategies`, `trading-core`, `market-data` за чтениями и командами, `audit` и
`statistics` только за чтением
(`docs/architecture/contracts.md` §«Синхронные вызовы»).

**Съёма метрик у периметра нет вовсе** — экспозиция актуатора объявлена одной
пробой живости. Это состояние, а не решение, и оно предъявлено находкой F-5
(группа B9).

## B1 — Контекст тенанта: вывод из членств предъявителя

### B1.1 — Ровно одно членство и есть контекст

| Поле | Содержание |
|---|---|
| Предусловия | стаб провайдера идентичности поднят, пара ключей у теста; стаб `auth` отвечает на `POST /api/v1/auth/memberships/self` списком из одного членства (`T1`, роль `OWNER`); кэш пуст |
| Вход | `GET /api/v1/bff/context` с bearer-токеном, подписанным ключом стаба, субъект `S1` |
| Ожидаемые выходы — все | `200`; тело несёт `T1` и `OWNER` и **ничего сверх**; к стабу `auth` ушёл ровно один запрос, глаголом `POST`, по пути `/api/v1/auth/memberships/self`; к прочим стабам не ушло ни одного запроса; в темы не опубликовано ничего; ни одна подписка не открыта |
| Чем подтверждается | `docs/architecture/contracts.md` §«Контекст тенанта в вызове», п. 2 (ровно одно членство — оно и есть контекст) |
| Факт | |

### B1.2 — Резолв идёт под токеном ПОЛЬЗОВАТЕЛЯ, а не под служебной идентичностью

| Поле | Содержание |
|---|---|
| Предусловия | те же |
| Вход | тот же запрос |
| Ожидаемые выходы — все | заголовок предъявления в запросе к `auth` равен `Bearer ` плюс **принятый** токен предъявителя, байт в байт; исходящей идентичности периметр у стаба провайдера не запрашивает — точки `token` стаб не видит ни разу; второго заголовка предъявления в запросе нет |
| Чем подтверждается | `docs/architecture/contracts.md` §«Контекст тенанта в вызове» (резолв обязан отвечать про предъявителя, и подставить его может только его же токен) |
| Факт | |

### B1.3 — Больше одного членства — отказ, а не молчаливый выбор

| Поле | Содержание |
|---|---|
| Предусловия | стаб `auth` отвечает списком из двух членств (`T1`, `T2`) |
| Вход | `GET /api/v1/bff/context` под принятым токеном |
| Ожидаемые выходы — все | `409`; тело — единый error-DTO; ни `T1`, ни `T2` в ответ не попадают; контекст не кэшируется — повторный запрос снова зовёт владельца; ни одного вызова к владельцам данных не ушло |
| Чем подтверждается | `docs/architecture/contracts.md` §«Контекст тенанта в вызове» (больше одного недостижимо до второго субъекта; молчаливый выбор дал бы контекст, о котором никто не решал) |
| Факт | |

### B1.4 — Пустой ответ владельца членств периметр тенанта не заводит

| Поле | Содержание |
|---|---|
| Предусловия | стаб `auth` отвечает пустым списком |
| Вход | тот же запрос |
| Ожидаемые выходы — все | ответ — отказ, а не заведённый контекст; периметр **сам** тенанта не заводит: второго запроса к `auth` (создающего) не уходит, и ни одного значения тенанта периметр не порождает; в кэше записи не появляется |
| Чем подтверждается | `docs/architecture/contracts.md` §«Контекст тенанта в вызове» (заведение — работа точки резолва у владельца, поэтому пустой ответ сюда не доходит вовсе); `docs/architecture/services/bff.md` §«Что держит истиной» |
| Факт | |

### B1.5 — Отказ транспорта у владельца членств — `503`, и резолв не повторяется

| Поле | Содержание |
|---|---|
| Предусловия | стаб `auth` не отвечает (соединение отвергается либо держится дольше таймаута) |
| Вход | тот же запрос |
| Ожидаемые выходы — все | `503`, код `PEER_UNAVAILABLE`; к стабу ушёл **ровно один** запрос — резолв глагол мутирующий и бюджету повторов чтения не подлежит; в кэш ничего не положено; следующий запрос зовёт владельца заново |
| Чем подтверждается | `docs/architecture/contracts.md` §«Периметр повторяет только чтения»; `docs/architecture/contracts.md` §«Синхронные вызовы», строка `bff → auth` (глагол мутирующий, потому что точка заводит тенанта) |
| Факт | |

### B1.6 — Присланный браузером тенант не читается ни в одном поле

| Поле | Содержание |
|---|---|
| Предусловия | стаб `auth` отвечает одним членством `T1` |
| Вход | `GET /api/v1/bff/context` с заголовками `X-Tenant-Id: T9` и `X-Tenant-Role: OWNER`, плюс параметром запроса `tenantId=T9` |
| Ожидаемые выходы — все | ответ несёт `T1`; `T9` не появляется ни в ответе, ни в запросе к владельцу; вывод контекста от присланных значений не зависит ни в одной ветви |
| Чем подтверждается | `docs/architecture/contracts.md` §«Контекст тенанта в вызове» (значение, присланное браузером, добытчиком не является ни при каком варианте) |
| Факт | |

### B1.7 — Годная запись кэша владельца не спрашивает

| Поле | Содержание |
|---|---|
| Предусловия | срок годности кэша прогона — заведомо больше длительности кейса; стаб `auth` отвечает одним членством |
| Вход | два запроса контекста подряд под токенами **одного** субъекта `S1` |
| Ожидаемые выходы — все | оба ответа одинаковы; к стабу `auth` ушёл **ровно один** запрос; второй ответ отдан без сетевого вызова |
| Чем подтверждается | `docs/architecture/contracts.md` §«Состояния периметр не держит» (кэш членств эфемерен и со сроком) |
| Факт | |

### B1.8 — Просроченная запись перечитывается у владельца

| Поле | Содержание |
|---|---|
| Предусловия | срок годности кэша прогона — заведомо меньше паузы между запросами; стаб `auth` отвечает членством `T1`, затем `T2` |
| Вход | запрос контекста; пауза больше срока; повторный запрос того же субъекта |
| Ожидаемые выходы — все | первый ответ несёт `T1`, второй — `T2`; к стабу ушло **два** запроса; устаревшая запись наружу второй раз не отдаётся |
| Чем подтверждается | `docs/architecture/contracts.md` §«Состояния периметр не держит» (холодный под перечитывает контекст у владельца; срок — страховка на случай пропущенного события смены членства) |
| Факт | |

### B1.9 — Ключ кэша — субъект, и ошибка направлена в разрешающую сторону

| Поле | Содержание |
|---|---|
| Предусловия | стаб `auth` отвечает членством `T1`; после первого запроса стаб перенастроен на пустой список (членство «отозвано») |
| Вход | запрос контекста субъектом `S1`; сразу же — второй запрос того же субъекта **другим** токеном той же идентичности |
| Ожидаемые выходы — все | второй ответ по-прежнему несёт `T1` — запись кэша годна, и отзыв виден с задержкой до срока годности; к стабу ушёл один запрос; по истечении срока второй запрос отвечает отказом |
| Чем подтверждается | `docs/architecture/contracts.md` §«Состояния периметр не держит» (направление ошибки названо: устаревшая запись ошибается в разрешающую сторону, поэтому срок короткий) |
| Факт | |

### B1.10 — Контекст ничего не пишет и ничего не публикует

| Поле | Содержание |
|---|---|
| Предусловия | те же, что у B1.1 |
| Вход | десяток запросов контекста подряд |
| Ожидаемые выходы — все | в темы не опубликовано ни одной записи; к владельцам данных не ушло ни одного запроса; подключения к какой-либо базе в контексте нет ни одного; единственный побочный след — запросы к `auth` числом промахов кэша |
| Чем подтверждается | `docs/architecture/services/bff.md` §«Что потребляет и что публикует» (периметр — читатель шины, не писатель) |
| Факт | |

## B2 — Билет подписки как значение

### B2.1 — Выдача под токеном отдаёт билет и момент его негодности

| Поле | Содержание |
|---|---|
| Предусловия | секрет подписи задан осью конфигурации; стаб `auth` отвечает одним членством `T1` |
| Вход | `POST /api/v1/bff/stream-tickets` под принятым токеном субъекта `S1` |
| Ожидаемые выходы — все | `200`; тело несёт билет и момент, после которого он негоден; момент отстои́т от текущего на объявленный срок; телом не уезжают ни секрет, ни субъект, ни тенант отдельными полями; подписки ни одной не открыто; в темы ничего не опубликовано |
| Чем подтверждается | `docs/architecture/contracts.md` §«Подписку открывает билет, а не сам токен» (билет получают обычным REST-вызовом под bearer-токеном) |
| Факт | |

### B2.2 — Выданный билет проверяется обратно в субъект и тенант

| Поле | Содержание |
|---|---|
| Предусловия | те же |
| Вход | билет, выданный B2.1, предъявляется точке подписки |
| Ожидаемые выходы — все | подписка открывается; поток несёт факты **того** тенанта, который был в контексте выдачи; факты иных тенантов в него не идут ни разу |
| Чем подтверждается | `docs/architecture/contracts.md` §«Подписку открывает билет, а не сам токен» (билет несёт субъекта и тенанта — и только их) |
| Факт | |

### B2.3 — Подделанный билет отвергается

| Поле | Содержание |
|---|---|
| Предусловия | билет выдан; тест меняет один символ его значимой части, подпись оставляет прежней |
| Вход | открытие подписки этим билетом |
| Ожидаемые выходы — все | `401`, код `ACCESS_UNAUTHENTICATED`; поток не открывается — ответ не имеет типа `text/event-stream`; ни одной записи в провод не уходит; тело отказа не сообщает, какая именно проверка не прошла |
| Чем подтверждается | `docs/architecture/contracts.md` §«Подписку открывает билет, а не сам токен» (проверяется подписью); `docs/rules/error-handling-policy.md` §«Отказ доступа — тот же контракт, что и прочие ошибки» |
| Факт | |

### B2.4 — Билет чужого секрета отвергается

| Поле | Содержание |
|---|---|
| Предусловия | тест собирает билет верного состава, подписав его **другим** секретом |
| Вход | открытие подписки этим билетом |
| Ожидаемые выходы — все | `401`, тот же код и то же тело, что у B2.3; поток не открывается; реплика, не выдававшая билета, но знающая общий секрет, тот же билет принимает (обратная сторона того же свойства) |
| Чем подтверждается | `docs/architecture/contracts.md` §«Подписку открывает билет, а не сам токен» (секрет общий у реплик; подписка может прийти не на ту реплику, что выдала билет) |
| Факт | |

### B2.5 — Просроченный билет отвергается при ОТКРЫТИИ

| Поле | Содержание |
|---|---|
| Предусловия | срок билета прогона — заведомо меньше паузы кейса |
| Вход | билет выдан; пауза больше срока; открытие подписки |
| Ожидаемые выходы — все | `401`; поток не открывается; **установленная ранее тем же билетом подписка при этом не рвётся** — проверка стои́т на открытии, а не на каждой записи |
| Чем подтверждается | `docs/architecture/contracts.md` §«Подписку открывает билет, а не сам токен» (проверяется при открытии; установленный поток по истечении срока не рвётся) |
| Факт | |

### B2.6 — Негодность наружу не различается

| Поле | Содержание |
|---|---|
| Предусловия | четыре билета: отсутствующий (параметра нет), неразбираемый (не Base64), с испорченной подписью, просроченный |
| Вход | четыре открытия подписки |
| Ожидаемые выходы — все | все четыре ответа совпадают статусом и кодом отказа; тексты пояснений наружу различий контура не выдают; по ответу нельзя заключить, существовал ли билет, знали ли секрет и когда он истёк |
| Чем подтверждается | `docs/rules/error-handling-policy.md` §«Отказ доступа — тот же контракт, что и прочие ошибки» (наружу не уходит ничего сверх класса) |
| Факт | |

### B2.7 — Незаданный секрет есть отказ, а не разрешение

| Поле | Содержание |
|---|---|
| Предусловия | контекст поднят с пустым `perimeter.ticket.secret` |
| Вход | выдача билета; открытие подписки с любым значением билета и без него |
| Ожидаемые выходы — все | **обе** операции отвечают отказом; подписка не открывается ни с билетом, ни без него; незаданное значение не даёт ни пустой подписи, ни пропуска проверки |
| Чем подтверждается | `docs/architecture/contracts.md` §«Подписку открывает билет, а не сам токен»; `docs/rules/absent-value-semantics.md` (умолчание — отказ, а не открытая поверхность) |
| Факт | |

### B2.8 — В пределах срока билет переиспользуем

| Поле | Содержание |
|---|---|
| Предусловия | билет выдан; срок заведомо больше длительности кейса |
| Вход | подписка открывается тем же билетом, закрывается, открывается снова |
| Ожидаемые выходы — все | оба открытия успешны; второе открытие отказа не получает; таблицы использованных билетов нет — повторное предъявление не помечается нигде |
| Чем подтверждается | `docs/architecture/contracts.md` §«Подписку открывает билет, а не сам токен» (штатное переподключение `EventSource` идёт тем же адресом; одноразовость без таблицы не выразима, а таблица запрещена) |
| Факт | |

### B2.9 — Роли билет не несёт

| Поле | Содержание |
|---|---|
| Предусловия | стаб `auth` отвечает членством с ролью `TRADER` |
| Вход | выдача билета; разбор его значимой части тестом |
| Ожидаемые выходы — все | в билете два значения — субъект и тенант; роли нет; поведение потока от роли не зависит ни в одной ветви |
| Чем подтверждается | `docs/architecture/contracts.md` §«Подписку открывает билет, а не сам токен» (роль на тропе потока не читается никем, и класть её в билет значило бы завести величину раньше предмета) |
| Факт | |

### B2.10 — Выдача билета без токена не проходит

| Поле | Содержание |
|---|---|
| Предусловия | контекст поднят штатно |
| Вход | `POST /api/v1/bff/stream-tickets` без заголовка предъявления и с токеном чужой подписи |
| Ожидаемые выходы — все | оба ответа — `401` от фильтр-цепочки с единым error-DTO и заголовком `WWW-Authenticate`; билета не выдано; к стабу `auth` не ушло ни одного запроса — резолв до отказа не доходит |
| Чем подтверждается | `docs/rules/api-access-policy.md` §«Вся поверхность закрыта; открытое — перечислено» (умолчание закрыто) |
| Факт | |

### B2.11 — Повторная выдача прежний билет не отменяет

| Поле | Содержание |
|---|---|
| Предусловия | билет `A` выдан |
| Вход | выдаётся билет `B` тому же субъекту; подписка открывается билетом `A` |
| Ожидаемые выходы — все | подписка билетом `A` открывается; отзыва билетов у периметра нет ни в каком виде; число выданных билетов нигде не хранится |
| Чем подтверждается | `docs/architecture/services/bff.md` §«Что держит истиной» (персистентного состояния у периметра нет); `docs/architecture/contracts.md` §«Подписку открывает билет, а не сам токен» |
| Факт | |

## B3 — Открытие подписки и радиус тенанта

### B3.1 — Годный билет открывает поток, и факт тенанта доезжает

| Поле | Содержание |
|---|---|
| Предусловия | брокер прогона поднят, темы `trading-core.facts` и `strategies.facts` заведены; слушатель периметра получил назначение партиций; билет тенанта `T1` выдан |
| Вход | открытие подписки билетом; затем в тему положена запись: ключ `T1`, заголовки `eventId`, `eventType = DEAL_OPENED`, `occurredAt`, тело — содержимое события |
| Ожидаемые выходы — все | ответ имеет тип `text/event-stream`; в проводе появляется ровно одна запись; её идентичность равна `eventId`, имя события — `DEAL_OPENED`, момент равен `occurredAt`, содержимое — форма периметра; второй записи о том же событии нет; в темы периметр ничего не публикует |
| Чем подтверждается | `docs/architecture/contracts.md` §«Живые данные в браузер»; §«Форма на проводе к браузеру — своя, а не доменный класс» того же дока |
| Факт | |

### B3.2 — Факт чужого тенанта в провод не идёт

| Поле | Содержание |
|---|---|
| Предусловия | подписка тенанта `T1` открыта |
| Вход | в тему кладутся две записи подряд: ключ `T2`, затем ключ `T1` |
| Ожидаемые выходы — все | в проводе ровно одна запись — та, чей ключ `T1`; записи `T2` нет ни в каком виде; **вторая запись предъявлена намеренно**: без неё зелёным был бы и мёртвый провод |
| Чем подтверждается | `docs/architecture/contracts.md` §«Живые данные в браузер» (фильтруя по тенанту сессии) |
| Факт | |

### B3.3 — Без билета подписка не открывается

| Поле | Содержание |
|---|---|
| Предусловия | контекст поднят штатно |
| Вход | `GET /api/v1/bff/stream` без параметра билета |
| Ожидаемые выходы — все | `401`; тип ответа — не `text/event-stream`; соединение не удерживается открытым; последующая запись в тему в это соединение не уходит |
| Чем подтверждается | `docs/rules/api-access-policy.md` §«Формы предъявления две, и вторая исключением не является» (билет — не открытая точка) |
| Факт | |

### B3.4 — Тропа подписки вне bearer-цепочки, но открытой не становится

| Поле | Содержание |
|---|---|
| Предусловия | контекст поднят штатно |
| Вход | `GET /api/v1/bff/stream` с **валидным bearer-токеном** и без билета; затем — с билетом и без токена |
| Ожидаемые выходы — все | первый ответ — отказ: токен на этой тропе добытчиком не является; второй — поток открыт; то есть исключение из цепочки заменяет форму предъявления, а не снимает её |
| Чем подтверждается | `docs/rules/api-access-policy.md` §«Формы предъявления две, и вторая исключением не является» |
| Факт | |

### B3.5 — Потолок подписок тенанта: сверх него — отказ «слишком много запросов»

| Поле | Содержание |
|---|---|
| Предусловия | потолок подписок прогона задан малым числом; билет `T1` выдан |
| Вход | открывается столько подписок, сколько допускает потолок, затем ещё одна |
| Ожидаемые выходы — все | последнее открытие отвечает `429`; ранее открытые подписки не рвутся и продолжают получать записи; отказ — часть контракта, а не отказ доступа: код отличается от `ACCESS_UNAUTHENTICATED` |
| Чем подтверждается | `docs/architecture/contracts.md` §«Число одновременных подписок тенанта ограничено, и отказ — часть контракта» |
| Факт | |

### B3.6 — Потолок считается по тенанту, а не по субъекту и не по процессу

| Поле | Содержание |
|---|---|
| Предусловия | потолок мал; выданы билеты тенантов `T1` и `T2` |
| Вход | потолок подписок открыт тенантом `T1`; затем открывается подписка тенанта `T2` |
| Ожидаемые выходы — все | подписка `T2` открывается штатно; счёт `T1` на неё не влияет; исчерпание одного тенанта не закрывает поток другому |
| Чем подтверждается | `docs/architecture/contracts.md` §«Число одновременных подписок тенанта ограничено, и отказ — часть контракта» |
| Факт | |

### B3.7 — Закрытая подписка освобождает место под потолком

| Поле | Содержание |
|---|---|
| Предусловия | потолок исчерпан тенантом `T1` |
| Вход | одна из открытых подписок закрывается клиентом; открывается новая |
| Ожидаемые выходы — все | новая подписка открывается; отказа нет; закрытая в рассылку больше не входит — записи в неё не пишутся, и отказ записи в неё не роняет рассылку остальным |
| Чем подтверждается | `docs/architecture/contracts.md` §«Число одновременных подписок тенанта ограничено, и отказ — часть контракта»; §«Дедуплицирует клиент, и это часть внешнего контракта» того же дока (событие обязано дойти до всех открытых сессий) |
| Факт | |

### B3.8 — Оборванная подписка не лишает данных остальные

| Поле | Содержание |
|---|---|
| Предусловия | две подписки тенанта `T1` открыты; одна из них обрывается на стороне клиента без штатного закрытия |
| Вход | в тему кладётся запись `T1` |
| Ожидаемые выходы — все | вторая подписка запись получает; приём события не роняется; оборванная выбывает из набора; последующие записи в неё не пишутся |
| Чем подтверждается | `docs/architecture/contracts.md` §«Дедуплицирует клиент, и это часть внешнего контракта» |
| Факт | |

### B3.9 — Соединение закрывается по своему сроку штатно

| Поле | Содержание |
|---|---|
| Предусловия | срок жизни соединения прогона задан малым |
| Вход | подписка открыта и оставлена без записей |
| Ожидаемые выходы — все | по истечении срока соединение закрывается **штатно**, а не отказом; место под потолком освобождается; записей об ошибке в провод не уходит |
| Чем подтверждается | `docs/architecture/contracts.md` §«Подписку открывает билет, а не сам токен» (внутри срока билета автоматическое переподключение `EventSource` работает штатно, тем же адресом) |
| Факт | |

### B3.10 — Штатный разрыв по сроку соединения обязан попадать ВНУТРЬ срока билета

| Поле | Содержание |
|---|---|
| Предусловия | контекст поднят на **умолчаниях** сервиса, без переопределения сроков прогоном |
| Вход | читаются два объявленных умолчания: срок билета и срок жизни соединения |
| Ожидаемые выходы — все | срок билета **не меньше** срока соединения; следствие проверяется поведением: подписка, открытая билетом и дожившая до планового закрытия соединения, переоткрывается **тем же адресом и тем же билетом** без отказа. Сегодня оба ожидания красны: срок соединения объявлен вчетверо больше срока билета, и переподключение `EventSource` после планового закрытия отвечает `401` **всегда** |
| Чем подтверждается | `docs/architecture/contracts.md` §«Подписку открывает билет, а не сам токен» (срок билета выбирается больше типичного разрыва); долг F-1 — `.claude/work/backlog.md` §«Срок билета подписки короче срока соединения, и штатный разрыв попадает вне его» |
| Факт | |

### B3.11 — Просроченный билет закрытую подписку не воскрешает и открытую не рвёт

| Поле | Содержание |
|---|---|
| Предусловия | срок билета мал; подписка открыта им в пределах срока |
| Вход | пауза больше срока билета; в тему кладётся запись тенанта |
| Ожидаемые выходы — все | открытая подписка запись получает — проверка стои́т на открытии; новая подписка тем же билетом не открывается; клиент обязан взять новый билет |
| Чем подтверждается | `docs/architecture/contracts.md` §«Подписку открывает билет, а не сам токен» (установленный поток по истечении срока не рвётся; обязанность клиента названа) |
| Факт | |

## B4 — Окно переигрывания и явный разрыв

### B4.1 — Известная позиция продолжает поток с неё

| Поле | Содержание |
|---|---|
| Предусловия | подписка `T1` открыта; в тему положены три записи `E1`, `E2`, `E3`; все три доехали; подписка закрыта |
| Вход | подписка открывается заново тем же билетом с заголовком `Last-Event-ID: E1` |
| Ожидаемые выходы — все | в провод уходят `E2` и `E3` в этом порядке; `E1` повторно не уходит; записи разрыва нет; после переигрывания новые записи продолжают идти в тот же провод |
| Чем подтверждается | `docs/architecture/contracts.md` §«Позиция чтения в браузер не уходит» (нашлась идентичность в окне — поток продолжается с неё) |
| Факт | |

### B4.2 — Неизвестная позиция даёт ЯВНЫЙ разрыв, а не молчаливое продолжение

| Поле | Содержание |
|---|---|
| Предусловия | те же; окно тенанта наполнено |
| Вход | подписка открывается с `Last-Event-ID` выдуманного значения |
| Ожидаемые выходы — все | первой в провод уходит запись класса `PERIMETER_GAP`; хвоста окна за ней не идёт; дальше идут только новые записи; молчаливого продолжения нет ни в одной ветви |
| Чем подтверждается | `docs/architecture/contracts.md` §«Позиция чтения в браузер не уходит» (не нашлась — поток начинается с текущего момента и сообщает разрыв отдельным событием) |
| Факт | |

### B4.3 — Первое подключение разрыва не получает

| Поле | Содержание |
|---|---|
| Предусловия | окно тенанта наполнено предыдущими записями |
| Вход | подписка открывается **без** `Last-Event-ID` |
| Ожидаемые выходы — все | записи разрыва в проводе нет; хвоста окна нет тоже — терять первому подключению нечего, и история ему не переигрывается; идут только записи, положенные после открытия |
| Чем подтверждается | `docs/architecture/contracts.md` §«Позиция чтения в браузер не уходит»; `docs/architecture/services/bff.md` §«Что потребляет и что публикует» (браузеру нужно происходящее сейчас, а не полный ряд с начала времён) |
| Факт | |

### B4.4 — Позиция, вытесненная из окна, даёт разрыв

| Поле | Содержание |
|---|---|
| Предусловия | ширина окна прогона задана малым числом; в тему положено записей больше ширины окна |
| Вход | подписка открывается с `Last-Event-ID` **первой** записи — уже вытесненной |
| Ожидаемые выходы — все | в провод уходит запись разрыва; вытесненные записи не восстанавливаются ничем; окно держит ровно объявленную ширину, не больше |
| Чем подтверждается | `docs/architecture/contracts.md` §«Позиция чтения в браузер не уходит» (окно ушло вперёд); §«Цена пропуска названа» того же дока |
| Факт | |

### B4.5 — Окно принадлежит тенанту: чужая позиция не находится

| Поле | Содержание |
|---|---|
| Предусловия | записи тенанта `T2` положены и доехали его подписке; выдан билет тенанта `T1` |
| Вход | подписка `T1` открывается с `Last-Event-ID` записи тенанта `T2` |
| Ожидаемые выходы — все | в провод уходит разрыв; ни одной записи `T2` не переигрывается; содержимое чужого окна не наблюдается ни в каком виде |
| Чем подтверждается | `docs/architecture/contracts.md` §«Живые данные в браузер» (фильтруя по тенанту сессии); §«Позиция чтения в браузер не уходит» того же дока |
| Факт | |

### B4.6 — Записи периметра идентичности не несут и в окно не кладутся

| Поле | Содержание |
|---|---|
| Предусловия | подписка открыта; тик пульса подан; разрыв получен кейсом B4.2 |
| Вход | подписка закрывается и открывается с `Last-Event-ID` последнего **факта**; отдельно — попытка назвать позицией класс записи периметра |
| Ожидаемые выходы — все | у записей `PERIMETER_PULSE` и `PERIMETER_GAP` поля идентичности нет вовсе — ни в проводе, ни в теле; ни одна из них не попадает в окно переигрывания: хвост после известной позиции их не содержит; продолжить поток «с пульса» нечем по построению |
| Чем подтверждается | `docs/architecture/contracts.md` §«Позиция чтения в браузер не уходит» (клиент сообщает идентичность последнего полученного **события**) |
| Факт | |

### B4.7 — Смещение темы наружу не уходит ни в одном поле

| Поле | Содержание |
|---|---|
| Предусловия | подписка открыта; записи доезжают |
| Вход | полный обход: запись потока, ответ контекста, ответ выдачи билета, ответ отказа |
| Ожидаемые выходы — все | смещения, номера партиции и имени темы нет ни в одном поле ни одного ответа; позиция чтения выражается **только** идентичностью события; запросить произвольную позицию клиент не может ничем |
| Чем подтверждается | `docs/architecture/contracts.md` §«Позиция чтения в браузер не уходит» (смещение — внутренняя величина транспорта) |
| Факт | |

### B4.8 — Переигрывание предшествует новым записям того же провода

| Поле | Содержание |
|---|---|
| Предусловия | окно тенанта `T1` наполнено записями `E1`…`E3`; подписка закрыта |
| Вход | подписка открывается с `Last-Event-ID: E1`, и **одновременно** с открытием в тему кладётся запись `E4` того же тенанта |
| Ожидаемые выходы — все | порядок записей в проводе — `E2`, `E3`, `E4`; `E4` не обгоняет хвоста окна; восстановить порядок клиент не может ничем — порядкового номера у записи нет. Сегодня ожидание красно: подписка регистрируется в наборе **до** переигрывания, и запись, пришедшая в этот промежуток, уходит в провод раньше хвоста |
| Чем подтверждается | `docs/architecture/contracts.md` §«Позиция чтения в браузер не уходит» (поток продолжается **с** названной позиции); долг F-4 — `.claude/work/backlog.md` §«Переигрывание идёт после регистрации подписки, и параллельный факт обгоняет хвост» |
| Факт | |

### B4.9 — Окно тенанта без подписок не наполняется

| Поле | Содержание |
|---|---|
| Предусловия | подписок тенанта `T2` нет ни одной; билет `T2` выдан |
| Вход | в тему кладутся три записи ключом `T2`; затем открывается подписка `T2` с `Last-Event-ID` первой из них |
| Ожидаемые выходы — все | в провод уходит **разрыв**, а не хвост: записей, положенных до существования подписки, окно не держит — держать их не для кого. Сегодня ожидание красно: окно наполняется для всякого тенанта, чья запись доехала, и переигрывается тому, кто на неё не подписывался; вытеснения по тенантам нет ни одного, и число окон растёт числом тенантов системы |
| Чем подтверждается | `docs/architecture/services/bff.md` §«Что держит истиной» (оба эфемерных предмета — со сроком годности); долг F-3 — `.claude/work/backlog.md` §«Окно переигрывания периметра копится по тенантам без подписок и не вытесняется» |
| Факт | |

### B4.10 — Перезапуск процесса теряет окно, кэш и подписки — и это законно

| Поле | Содержание |
|---|---|
| Предусловия | подписка открыта, окно наполнено, кэш членств горяч |
| Вход | контекст поднимается заново (холодная реплика) |
| Ожидаемые выходы — все | подписок нет ни одной; окно пусто — открытие с прежним `Last-Event-ID` даёт разрыв; членства перечитываются у владельца; ни одной строки, переживающей процесс, не существует; восстановления состояния с диска не происходит |
| Чем подтверждается | `docs/architecture/services/bff.md` §«Что держит истиной»; `docs/architecture/contracts.md` §«Состояния периметр не держит» |
| Факт | |

## B5 — Пульс потока

### B5.1 — Живой потребитель пропускает пульс

| Поле | Содержание |
|---|---|
| Предусловия | подписка тенанта `T1` открыта; слушатель запущен и держит назначенные партиции; выключатель тика включён |
| Вход | тик пульса подан прямым вызовом метода джобы |
| Ожидаемые выходы — все | в провод уходит ровно одна запись класса `PERIMETER_PULSE`; момент её — момент отправки; идентичности и содержимого у неё нет; в окно она не попадает; в темы ничего не публикуется |
| Чем подтверждается | `docs/architecture/contracts.md` §«Поток несёт пульс, и молчание без пульса — наблюдаемый отказ» |
| Факт | |

### B5.2 — Остановленный слушатель пульс гасит

| Поле | Содержание |
|---|---|
| Предусловия | подписка открыта; слушатель остановлен |
| Вход | тик пульса |
| Ожидаемые выходы — все | в провод не уходит ничего; молчание — честное: потребитель мёртв, и показывать живую картину нечем |
| Чем подтверждается | `docs/architecture/contracts.md` §«Пульс отражает живость ПОТРЕБИТЕЛЯ, а не планировщика» |
| Факт | |

### B5.3 — Слушатель без назначенных партиций пульс гасит

| Поле | Содержание |
|---|---|
| Предусловия | подписка открыта; слушатель запущен, но назначения партиций у него нет (связь с брокером потеряна либо группа развалилась) |
| Вход | тик пульса |
| Ожидаемые выходы — все | в провод не уходит ничего; запущенность сама по себе живости не доказывает |
| Чем подтверждается | `docs/architecture/contracts.md` §«Пульс отражает живость ПОТРЕБИТЕЛЯ, а не планировщика» (потеря связи с брокером снимает назначение) |
| Факт | |

### B5.4 — Слушателя нет вовсе — пульса нет

| Поле | Содержание |
|---|---|
| Предусловия | подписка открыта; ни одного слушателя в реестре |
| Вход | тик пульса |
| Ожидаемые выходы — все | в провод не уходит ничего; пустой перечень слушателей читается как «не живо», а не как «нарушений нет» |
| Чем подтверждается | `docs/architecture/contracts.md` §«Пульс отражает живость ПОТРЕБИТЕЛЯ, а не планировщика»; `docs/rules/absent-value-semantics.md` |
| Факт | |

### B5.5 — Без подписок пульса нет

| Поле | Содержание |
|---|---|
| Предусловия | подписок нет ни одной; слушатель жив |
| Вход | тик пульса |
| Ожидаемые выходы — все | в провод не уходит ничего — писать некуда; исключения тик не бросает; следующий тик после открытия подписки пульс даёт |
| Чем подтверждается | `docs/architecture/contracts.md` §«Поток несёт пульс, и молчание без пульса — наблюдаемый отказ» (пульс — свойство потока, а не процесса) |
| Факт | |

### B5.6 — Выключатель тика гасит пульс целиком

| Поле | Содержание |
|---|---|
| Предусловия | подписка открыта; слушатель жив; выключатель выключен |
| Вход | тик пульса |
| Ожидаемые выходы — все | в провод не уходит ничего; прочая раздача продолжается — факт, положенный в тему, доезжает; то есть выключатель гасит **измеритель**, а не поток |
| Чем подтверждается | `.claude/rules/codestyle.md` §Джобы (выключатель у всякой джобы); `docs/architecture/contracts.md` §«Выключатель тика — диагностический инструмент, а не рабочий режим, и цена его названа» (цена названа: выключенный пульс делает молчание двусмысленным) |
| Факт | |

### B5.7 — Пульс идёт всем тенантам с открытыми подписками

| Поле | Содержание |
|---|---|
| Предусловия | открыты подписки тенантов `T1` и `T2` |
| Вход | один тик пульса |
| Ожидаемые выходы — все | пульс уходит в оба провода; содержимого тенанта он не несёт, поэтому радиуса не нарушает; тенанту без подписок не уходит ничего |
| Чем подтверждается | `docs/architecture/contracts.md` §«Поток несёт пульс, и молчание без пульса — наблюдаемый отказ» |
| Факт | |

### B5.8 — Пульс не подменяет факта и факта не вытесняет

| Поле | Содержание |
|---|---|
| Предусловия | подписка открыта; окно наполнено |
| Вход | десяток тиков пульса подряд, затем открытие подписки с известной позицией |
| Ожидаемые выходы — все | хвост окна отдан целиком — пульсы его не вытеснили; ширина окна считается по фактам; ни один пульс в хвосте не появляется |
| Чем подтверждается | `docs/architecture/contracts.md` §«Позиция чтения в браузер не уходит» |
| Факт | |

### B5.9 — Ручного запуска у тика нет, и это не пропуск

| Поле | Содержание |
|---|---|
| Предусловия | контекст поднят штатно |
| Вход | обход поверхности в поисках точки ручного запуска джоб |
| Ожидаемые выходы — все | точки ручного запуска нет ни одной; `POST` на всякий путь под `/api/v1/bff/…`, кроме выдачи билета, отвечает отказом, а не запуском; защиты от перекрытия у тика тоже нет — и обе обязанности сняты названными доводами, а не умолчанием |
| Чем подтверждается | `.claude/rules/codestyle.md` §Джобы (тик живости: догонять нечего, а `fixedDelay` следующий тик до конца текущего не запускает) |
| Факт | |

### B5.10 — Размер пула планировщика выведен из числа `@Scheduled`-методов

| Поле | Содержание |
|---|---|
| Предусловия | контекст поднят на умолчаниях сервиса |
| Вход | счёт `@Scheduled`-методов дерева сервиса; чтение объявленного размера пула |
| Ожидаемые выходы — все | числа равны; размер пула плейсхолдера окружения не несёт — он выводится из дерева, а не калибруется манифестом; вторая джоба, заведённая без правки числа, роняет пробу |
| Чем подтверждается | `.claude/rules/codestyle.md` §Джобы, клауза «Операнда живости мало: такт обязан ДОСТАТЬСЯ тику»; `docs/architecture/contracts.md` §«Пульс — измеритель, поэтому такт обязан ему достаться» |
| Факт | |

## B6 — Приём события в запись потока

### B6.1 — Конверт читается заголовками, тенант — ключом, содержимое — телом

| Поле | Содержание |
|---|---|
| Предусловия | подписка тенанта `T1` открыта; слушатель держит партиции |
| Вход | запись: ключ `T1`, заголовки `eventId`, `eventType`, `occurredAt`, `version`, `traceContext`; тело — содержимое класса |
| Ожидаемые выходы — все | в проводе одна запись; её идентичность и класс равны заголовкам; момент равен `occurredAt`; тенант радиуса взят из **ключа**, а не из заголовка и не из тела; `version` и `traceContext` наружу не уходят ни в одном поле |
| Чем подтверждается | `docs/architecture/contracts.md` §«Как конверт лежит на проводе — поимённо»; §«Обязательных у периметра именно три, а не шесть» того же дока |
| Факт | |

### B6.2 — Неполный конверт пропускается, а раздача продолжается

| Поле | Содержание |
|---|---|
| Предусловия | подписка `T1` открыта |
| Вход | три записи подряд: без ключа, без заголовка `eventId`, без заголовка `eventType`; следом — полная запись `T1` |
| Ожидаемые выходы — все | ни одна из трёх неполных в провод не уходит; **четвёртая доезжает** — раздача не остановлена; смещение продвинулось по всем четырём; группа не встала; отказ каждой из трёх виден строкой лога |
| Чем подтверждается | `docs/architecture/contracts.md` §«Неполный конверт и неразбираемое содержимое периметр пропускает, а раздачу не останавливает» |
| Факт | |

### B6.3 — Неизвестный класс события ведёт себя как пропуск, а не как отказ

| Поле | Содержание |
|---|---|
| Предусловия | те же |
| Вход | запись с `eventType`, которого нет ни в одном перечне производителя, телом произвольной формы; следом — полная запись известного класса |
| Ожидаемые выходы — все | первой записи в проводе нет; вторая доезжает; приём не останавливается; в лог уходит строка о пропуске |
| Чем подтверждается | `docs/architecture/contracts.md` §«Неполный конверт и неразбираемое содержимое периметр пропускает, а раздачу не останавливает» (производитель вправе завести класс раньше, чем появится читатель) |
| Факт | |

### B6.4 — Неразбираемое содержимое пропускается тем же исходом

| Поле | Содержание |
|---|---|
| Предусловия | те же |
| Вход | запись известного класса, чьё тело не разбирается объявленной формой; следом — полная запись того же класса |
| Ожидаемые выходы — все | первой в проводе нет; вторая доезжает; частично разобранная запись в провод не уходит — полупустой формы клиент не получает; смещение продвигается |
| Чем подтверждается | `docs/architecture/contracts.md` §«Неполный конверт и неразбираемое содержимое периметр пропускает, а раздачу не останавливает» |
| Факт | |

### B6.5 — Момента происшествия нет — периметр показывает момент раздачи

| Поле | Содержание |
|---|---|
| Предусловия | подписка открыта |
| Вход | полная запись **без** заголовка `occurredAt` |
| Ожидаемые выходы — все | запись доезжает; её момент лежит между моментом подачи и моментом чтения провода; по потоку подставленный момент от настоящего не отличить — и это названная цена, а не дефект |
| Чем подтверждается | `docs/architecture/contracts.md` §«Обязательных у периметра именно три, а не шесть» (момент происшествия раздачу не гейтит; подстановка названа, потому что она видна клиенту) |
| Факт | |

### B6.6 — Неразбираемый момент происшествия раздачу НЕ останавливает

| Поле | Содержание |
|---|---|
| Предусловия | подписка открыта |
| Вход | полная запись, чей заголовок `occurredAt` несёт значение, не разбирающееся как момент (пустая строка, число, текст); следом — полная запись того же класса с верным моментом |
| Ожидаемые выходы — все | первая запись **пропускается** — как неполный конверт и как неразобранное тело; вторая доезжает; группа не встаёт, смещение продвигается, повторных доставок первой нет. Сегодня ожидание красно: разбор момента не защищён, отказ уходит из слушателя наружу, и запись доставляется повторно, пока обработчик не откажется от неё |
| Чем подтверждается | `docs/architecture/contracts.md` §«Неполный конверт и неразбираемое содержимое периметр пропускает, а раздачу не останавливает» (поток решений не порождает — он показывает); долг F-2 — `.claude/work/backlog.md` §«Неразбираемый момент происшествия роняет раздачу вместо пропуска» |
| Факт | |

### B6.7 — Группа своя у реплики, и дедупа на сервере нет

| Поле | Содержание |
|---|---|
| Предусловия | подписка открыта; та же запись подаётся дважды с одним `eventId` |
| Вход | две доставки одной записи |
| Ожидаемые выходы — все | в провод уходят **обе**; дедуплицирует клиент по идентичности — это часть внешнего контракта; inbox-таблицы у периметра нет и быть не может: базы нет |
| Чем подтверждается | `docs/architecture/contracts.md` §«Дедуплицирует клиент, и это часть внешнего контракта»; `docs/architecture/data-ownership.md` §«Outbox и доставка» |
| Факт | |

### B6.8 — Группа начинается с текущего момента, истории не переигрывает

| Поле | Содержание |
|---|---|
| Предусловия | в тему положены записи `T1` **до** подъёма контекста |
| Вход | контекст поднимается; открывается подписка `T1`; затем кладётся новая запись |
| Ожидаемые выходы — все | записи, положенные до подъёма, в провод не уходят ни одна; новая доезжает; сброс позиции группы на начало темы не происходит ни при каком рестарте |
| Чем подтверждается | `docs/architecture/services/bff.md` §«Что потребляет и что публикует» (эфемерная группа читает с конца, отставания не накапливает и в счёт durable-групп не входит) |
| Факт | |

### B6.9 — Подписка идёт только на темы, у которых есть производитель

| Поле | Содержание |
|---|---|
| Предусловия | в брокере прогона заведены и темы производителей, и тема без производителя |
| Вход | подъём контекста; обход назначенных слушателю тем |
| Ожидаемые выходы — все | подписка накрывает ровно объявленные осью окружения темы; темы классов `auth` среди них нет ни одной; на несуществующую тему подписки не создаётся |
| Чем подтверждается | `docs/architecture/contracts.md` §«Подписка идёт на темы, у которых есть производитель»; `.claude/work/backlog.md` §«События `auth` — производителя и темы нет» |
| Факт | |

### B6.10 — Приём ничего не пишет и ничего не публикует

| Поле | Содержание |
|---|---|
| Предусловия | подписок нет ни одной; слушатель жив |
| Вход | в темы кладётся десяток записей разных тенантов и классов |
| Ожидаемые выходы — все | к стабам владельцев не ушло ни одного запроса; в темы не опубликовано ничего; строк не заведено нигде — базы нет; единственный след приёма — наполнение окна (и оно предмет находки F-3, кейс B4.9) |
| Чем подтверждается | `docs/architecture/services/bff.md` §«Что потребляет и что публикует» (не публикует ничего: событие порождает тот, кто принимает решение) |
| Факт | |

## B7 — Форма на проводе к браузеру

### B7.1 — Доменного класса в проводе нет ни в одном поле

| Поле | Содержание |
|---|---|
| Предусловия | подписка открыта |
| Вход | по одной записи каждого из восьми несомых классов |
| Ожидаемые выходы — все | у каждой записи содержимое — форма периметра; производных полей, порождённых предикатами доменной модели, нет ни в одной; поля, которых форма периметра не объявила, не уезжают, сколько бы их ни несло содержимое владельца |
| Чем подтверждается | `docs/architecture/contracts.md` §«Форма на проводе к браузеру — своя, а не доменный класс»; §«Состав своей формы периметр объявляет сам» того же дока |
| Факт | |

### B7.2 — Активация везёт идентичности и имя, а дерева определения не везёт

| Поле | Содержание |
|---|---|
| Предусловия | подписка открыта |
| Вход | запись класса активации определения, чьё содержимое несёт снимок дерева целиком |
| Ожидаемые выходы — все | в проводе — идентичности радиуса, взятые с **верхнего уровня** содержимого, и имя определения из снимка; самого дерева (условий, шагов, действий) в записи нет ни одного узла; дочитывает его браузер обычным запросом к владельцу |
| Чем подтверждается | `docs/architecture/contracts.md` §«Форма на проводе к браузеру — своя, а не доменный класс»; долг доков форм — `.claude/work/backlog.md` §«Api-формы периметра без дока — хвост шага 9 фазы 2» |
| Факт | |

### B7.3 — Классы жизненного цикла определения едут одной формой

| Поле | Содержание |
|---|---|
| Предусловия | подписка открыта |
| Вход | записи классов деактивации и удаления определения |
| Ожидаемые выходы — все | обе едут одной формой периметра; класс различается **именем записи**, а не формой содержимого; полей, которых у формы нет, ни одна не приобретает |
| Чем подтверждается | `docs/architecture/contracts.md` §«Состав своей формы периметр объявляет сам» |
| Факт | |

### B7.4 — Компонент, формой не объявленный, не едет

| Поле | Содержание |
|---|---|
| Предусловия | подписка открыта |
| Вход | запись, чьё содержимое несёт **дополнительный** компонент, которого форма периметра не объявляла |
| Ожидаемые выходы — все | компонент в провод не уходит; отказа разбора при этом нет — запись доезжает без него; умолчание у периметра — «не едет» |
| Чем подтверждается | `docs/architecture/contracts.md` §«Состав своей формы периметр объявляет сам» (умолчание маппера гасит предупреждение о непокрытой цели, а решение принимает форма) |
| Факт | |

### B7.5 — Разошедшееся имя компонента даёт пустое поле, и охраны у этого нет

| Поле | Содержание |
|---|---|
| Предусловия | подписка открыта |
| Вход | запись, у которой объявленный формой компонент назван в содержимом **другим** именем |
| Ожидаемые выходы — все | объявленный компонент приезжает браузеру **пустым**; отказа не происходит ни при сборке, ни в рантайме; наблюдаемость расхождения — только этот кейс. Ожидание здесь — предъявление названного предела, а не требование поведения: механической охраны у связки имён нет, и дом её называет |
| Чем подтверждается | `docs/architecture/contracts.md` §«Состав своей формы периметр объявляет сам» (объявленность — условие необходимое, а не достаточное, и предел назван) |
| Факт | |

### B7.6 — Восемь несомых классов переводятся, и каждый — своей формой

| Поле | Содержание |
|---|---|
| Предусловия | подписка открыта |
| Вход | по записи на каждый класс двух перечней производителей |
| Ожидаемые выходы — все | доезжают все; класс записи равен имени значения перечня; формы содержимого у классов разные там, где различаются их предметы; ни одна запись не приезжает с пустым содержимым |
| Чем подтверждается | `docs/architecture/contracts.md` §События (перечень классов и их производители); §«Форма на проводе к браузеру — своя, а не доменный класс» того же дока |
| Факт | |

## B8 — Проксирование владельцу

### B8.1 — Чтение уходит владельцу с путём и строкой запроса как есть

| Поле | Содержание |
|---|---|
| Предусловия | стаб владельца отвечает `200` с телом; стаб `auth` отвечает членством `T1` |
| Вход | `GET /api/v1/trading-core/deals?status=OPEN&limit=20` под принятым токеном |
| Ожидаемые выходы — все | к стабу владельца ушёл `GET` того же пути со **строкой запроса целиком**; ответ владельца вернулся браузеру как есть — статус, тело и заголовки содержимого; тела периметр не переписывал; в темы ничего не опубликовано |
| Чем подтверждается | `docs/architecture/contracts.md` §«Периметр: что `bff` отдаёт и чего не делает» (контракт остаётся у владельца и версионируется вместе с ним) |
| Факт | |

### B8.2 — Заголовки уходят закрытым перечнем

| Поле | Содержание |
|---|---|
| Предусловия | те же |
| Вход | `POST /api/v1/strategies/definitions` с телом и типом содержимого |
| Ожидаемые выходы — все | в запросе к владельцу ровно четыре заголовка предмета: предъявление (`Bearer ` плюс **принятый** токен), `X-Tenant-Id`, `X-Tenant-Role`, тип содержимого; транспортных заголовков исходного соединения (длина, кодировка, хост, соединение) среди них нет ни одного |
| Чем подтверждается | `docs/architecture/contracts.md` §«Контекст тенанта в вызове», п. 3 (имена заголовков контекста объявлены контрактом, а не сервисом) |
| Факт | |

### B8.3 — Присланный браузером контекст не пересылается владельцу

| Поле | Содержание |
|---|---|
| Предусловия | стаб `auth` отвечает членством `T1` |
| Вход | тот же запрос с заголовками `X-Tenant-Id: T9`, `X-Tenant-Role: OWNER`, добавленными браузером |
| Ожидаемые выходы — все | к владельцу уходит `T1`, выведенный из членств; `T9` не уходит ни в одном заголовке; двух значений одного заголовка в запросе нет; подменить контекст со стороны браузера нельзя ничем |
| Чем подтверждается | `docs/architecture/contracts.md` §«Контекст тенанта в вызове», п. 2 (браузер тенанта не называет) |
| Факт | |

### B8.4 — Ответ владельца, каким бы он ни был, есть его решение

| Поле | Содержание |
|---|---|
| Предусловия | стаб владельца отвечает `409` с телом-отказом, затем `500` |
| Вход | два чтения подряд |
| Ожидаемые выходы — все | оба ответа доходят до браузера **как есть** — статус и тело владельца; периметр их не переписывает и не подменяет своим error-DTO; повтора ни одного не происходит: отказ владельца — не отказ транспорта |
| Чем подтверждается | `docs/architecture/contracts.md` §«Периметр повторяет только чтения» (ответ владельца есть его решение и повтору не подлежит); §«Агрегация не превращает несколько отказов в один» того же дока |
| Факт | |

### B8.5 — Чтение повторяется в пределах объявленного бюджета

| Поле | Содержание |
|---|---|
| Предусловия | бюджет повторов чтения прогона задан; стаб владельца отвергает соединение на первых попытках и отвечает на последней |
| Вход | `GET` к владельцу |
| Ожидаемые выходы — все | число попыток равно бюджету плюс одна; браузер получает успешный ответ; между попытками к иным владельцам не уходит ничего |
| Чем подтверждается | `docs/architecture/contracts.md` §«Периметр повторяет только чтения» (бюджет повторов вызывающего) |
| Факт | |

### B8.6 — Мутирующий запрос не повторяется никогда

| Поле | Содержание |
|---|---|
| Предусловия | бюджет повторов задан положительным; стаб владельца отвергает соединение всегда |
| Вход | `POST`, `PUT`, `PATCH`, `DELETE` к владельцу |
| Ожидаемые выходы — все | к стабу по каждому глаголу ушла **ровно одна** попытка; бюджет на них не действует; браузеру возвращается `503` с кодом `PEER_UNAVAILABLE` |
| Чем подтверждается | `docs/architecture/contracts.md` §«Периметр повторяет только чтения» (повторённая команда стоит выполненного дважды действия, и обезвредить её нечем) |
| Факт | |

### B8.7 — Исчерпанный бюджет отвечает `503`, а не своим отказом

| Поле | Содержание |
|---|---|
| Предусловия | стаб владельца не отвечает ни на одной попытке |
| Вход | `GET` к владельцу |
| Ожидаемые выходы — все | `503`, код `PEER_UNAVAILABLE`; отказ не выдаётся за отказ автора запроса (`4xx` не отвечается); тело — единый error-DTO; в лог уходит строка с именем владельца и глаголом |
| Чем подтверждается | `docs/rules/runtime-error-classification.md` §«Отказ соседа по ярусу — свой класс, и сделку в ошибку он не уводит»; `docs/rules/error-handling-policy.md` §«Внешняя поверхность» |
| Факт | |

### B8.8 — Адресат выводится первым сегментом; таблицы маршрутов нет

| Поле | Содержание |
|---|---|
| Предусловия | стаб отвечает по адресу **любого** имени владельца, подставляемого в шаблон адреса |
| Вход | запросы к шести именам владельцев подряд и к седьмому, которого в инвентаре ещё нет |
| Ожидаемые выходы — все | все семь уходят по адресу, собранному из шаблона подстановкой имени; перечня владельцев периметр не сверяет ни с чем; новый владелец правки периметра не потребовал — это и есть проверяемое свойство конвенции |
| Чем подтверждается | `docs/architecture/contracts.md` §«Владельца называет путь, а не таблица маршрутов»; `docs/architecture/services/bff.md` §«Что он делает и зачем» |
| Факт | |

### B8.9 — Периметр себя не проксирует

| Поле | Содержание |
|---|---|
| Предусловия | контекст поднят штатно |
| Вход | `GET /api/v1/bff/unknown-point` под принятым токеном |
| Ожидаемые выходы — все | `404` с единым error-DTO; к стабам не ушло ни одного запроса; исходящего вызова на самого себя не происходит; собственная точка по этому адресу либо заведена контроллером, либо не существует вовсе |
| Чем подтверждается | `docs/architecture/contracts.md` §«Периметр: что `bff` отдаёт и чего не делает» (собственные точки периметра живут на `/api/v1/bff/…` по той же общей фразе) |
| Факт | |

### B8.10 — Имя не той формы адресом не становится

| Поле | Содержание |
|---|---|
| Предусловия | контекст поднят штатно |
| Вход | запросы, чей первый сегмент после версии — `../auth`, `AUTH`, `auth:8080`, `a_uth`, пустой сегмент |
| Ожидаемые выходы — все | каждый отвечает `404`; к стабам не ушло ни одного запроса; ни одно из значений не попало в адрес исходящего вызова; охрана формы имени стои́т у самого периметра, а сетевая политика остаётся вторым рубежом, а не единственным |
| Чем подтверждается | `docs/architecture/contracts.md` §«Владельца называет путь, а не таблица маршрутов» (роутер периметра — объявленное исключение прогона, а не дыра в нём) |
| Факт | |

### B8.11 — Тело пересылается байтами и не разбирается

| Поле | Содержание |
|---|---|
| Предусловия | стаб владельца отвечает `200` |
| Вход | `POST` с телом, которое не разбирается как JSON, и с типом содержимого `application/json` |
| Ожидаемые выходы — все | тело ушло владельцу байт в байт; периметр его не разбирал — отказа разбора не произошло; ответ владельца вернулся как есть; второй записи контракта владельца у периметра не появилось |
| Чем подтверждается | `docs/architecture/contracts.md` §«Периметр: что `bff` отдаёт и чего не делает» (контракт остаётся у владельца) |
| Факт | |

### B8.12 — Пустое тело есть отсутствие тела, а не тело нулевой длины

| Поле | Содержание |
|---|---|
| Предусловия | стаб владельца отвечает `200` |
| Вход | `GET` без тела и `POST` с телом нулевой длины |
| Ожидаемые выходы — все | к владельцу в обоих случаях уходит запрос **без** тела; заголовка длины периметр не выдумывает; ответ владельца доходит до браузера |
| Чем подтверждается | `docs/rules/absent-value-semantics.md`; `docs/architecture/contracts.md` §«Периметр: что `bff` отдаёт и чего не делает» |
| Факт | |

### B8.13 — Переговорные заголовки содержимого пересылаются владельцу

| Поле | Содержание |
|---|---|
| Предусловия | стаб владельца различает ответы по заголовку `Accept` |
| Вход | `GET` с заголовком `Accept`, названным браузером |
| Ожидаемые выходы — все | к владельцу уходят **оба** переговорных заголовка, которые у запроса были, — и тип содержимого, и принимаемое; владелец согласует ответ с тем, что просил браузер. Сегодня ожидание красно: перечень пересылаемого несёт только тип содержимого, а `Accept` не уходит вовсе, хотя javadoc перечня называет переговорные заголовки во множественном числе |
| Чем подтверждается | `docs/architecture/contracts.md` §«Периметр: что `bff` отдаёт и чего не делает» (пересылаемое идёт формой владельца); долг F-6 — `.claude/work/backlog.md` §«Переговорные заголовки содержимого периметр обещает шире, чем пересылает» |
| Факт | |

### B8.14 — Без принятого токена пересылки не происходит

| Поле | Содержание |
|---|---|
| Предусловия | контекст поднят штатно |
| Вход | `GET /api/v1/trading-core/deals` без заголовка предъявления; затем — с токеном чужой подписи; затем — с просроченным |
| Ожидаемые выходы — все | все три отвечают `401`; к стабу владельца не ушло ни одного запроса; к стабу `auth` не ушло ни одного — резолв контекста до отказа не доходит |
| Чем подтверждается | `docs/rules/api-access-policy.md` §«Вся поверхность закрыта; открытое — перечислено» |
| Факт | |

### B8.15 — Отказ резолва контекста владельца не тревожит

| Поле | Содержание |
|---|---|
| Предусловия | стаб `auth` отвечает двумя членствами |
| Вход | `GET /api/v1/trading-core/deals` под принятым токеном |
| Ожидаемые выходы — все | `409`; к стабу владельца данных не ушло ни одного запроса — контекст выводится **до** пересылки; тело — единый error-DTO |
| Чем подтверждается | `docs/architecture/contracts.md` §«Контекст тенанта в вызове», п. 3 (домен принимает контекст операндом вызова) |
| Факт | |

### B8.16 — Агрегации сегодня нет ни одной, и слитого отказа нет тоже

| Поле | Содержание |
|---|---|
| Предусловия | стабы двух владельцев подняты, один отвечает отказом |
| Вход | обход поверхности периметра в поисках точки, отвечающей из двух источников |
| Ожидаемые выходы — все | такой точки нет ни одной: каждый запрос браузера идёт ровно одному владельцу; экран из нескольких владельцев делает столько вызовов, сколько владельцев показывает — и это названная цена; слитого отказа поэтому не существует по построению |
| Чем подтверждается | `docs/architecture/contracts.md` §«Периметр: что `bff` отдаёт и чего не делает» (агрегирует только там, где экран иначе делает несколько вызовов; названная цена); §«Агрегация не превращает несколько отказов в один» того же дока |
| Факт | |

## B9 — Контур доступа и единый формат отказа

### B9.1 — Умолчание контура закрыто

| Поле | Содержание |
|---|---|
| Предусловия | контекст поднят штатно |
| Вход | обход всех путей поверхности без заголовка предъявления: контекст, выдача билета, пересылка каждому владельцу, описание поверхности |
| Ожидаемые выходы — все | каждый отвечает `401`; открытым не оказывается ни один путь, кроме названных исключением; заголовок `WWW-Authenticate` со схемой предъявления присутствует |
| Чем подтверждается | `docs/rules/api-access-policy.md` §«Вся поверхность закрыта; открытое — перечислено» (обратный дефолт запрещён) |
| Факт | |

### B9.2 — Открытых точек ровно две, и вторая — тропа билета

| Поле | Содержание |
|---|---|
| Предусловия | контекст поднят штатно |
| Вход | `GET /actuator/health` без предъявления; `GET /api/v1/bff/stream` без предъявления и без билета; `GET /actuator/prometheus` без предъявления |
| Ожидаемые выходы — все | проба живости отвечает `200` и не сообщает ничего о состоянии контура; тропа подписки без билета отвечает `401` — из bearer-цепочки она изъята, но открытой не стала; съём метрик **не существует вовсе**: экспозиция актуатора объявлена одной пробой живости, и путь отвечает `404`, а не отказом доступа |
| Чем подтверждается | `docs/rules/api-access-policy.md` §«Вся поверхность закрыта; открытое — перечислено»; §«Съём метрик — второе исключение» того же дока (у периметра рядов нет, и исключение приезжает вместе с ними) |
| Факт | |

### B9.3 — Сессии контур не заводит

| Поле | Содержание |
|---|---|
| Предусловия | контекст поднят штатно |
| Вход | последовательность запросов под принятым токеном |
| Ожидаемые выходы — все | `Set-Cookie` не приходит ни в одном ответе; идентификатора сессии не заводится; второй запрос без токена не проходит, сколько бы первых ни прошло |
| Чем подтверждается | `docs/rules/api-access-policy.md` §«Вся поверхность закрыта; открытое — перечислено» (контур stateless) |
| Факт | |

### B9.4 — Отказ фильтр-цепочки отвечает единым error-DTO

| Поле | Содержание |
|---|---|
| Предусловия | контекст поднят штатно |
| Вход | запрос без предъявления |
| Ожидаемые выходы — все | тело — единый error-DTO с классом отказа, пояснением и моментом; **пустого тела нет**; класс отказа — «принятого принципала нет»; ни существования объекта, ни текста исключения наружу не уходит |
| Чем подтверждается | `docs/rules/error-handling-policy.md` §«Отказ доступа — тот же контракт, что и прочие ошибки» |
| Факт | |

### B9.5 — Отказ билета отвечает тем же форматом и тем же классом

| Поле | Содержание |
|---|---|
| Предусловия | контекст поднят штатно |
| Вход | открытие подписки с испорченным билетом; запрос контекста без предъявления |
| Ожидаемые выходы — все | оба тела совпадают формой и классом отказа; второго формата отказа у поверхности не появляется — а появился бы он ровно на той тропе, которую периметр и заводит |
| Чем подтверждается | `docs/rules/error-handling-policy.md` §«Отказ доступа — тот же контракт, что и прочие ошибки»; `docs/architecture/contracts.md` §«Подписку открывает билет, а не сам токен» |
| Факт | |

### B9.6 — Отказы контейнера отвечают нашим телом при своём статусе

| Поле | Содержание |
|---|---|
| Предусловия | контекст поднят штатно; токен принят |
| Вход | путь вне `/api` (`GET /unknown`); собственная точка периметра несуществующим адресом; выдача билета глаголом `GET`; тело, не разбирающееся контейнером |
| Ожидаемые выходы — все | каждый отвечает единым error-DTO при своём статусе; пустого тела нет ни у одного; пояснение берётся из того, что собрал контейнер, а не выдумывается; своего текста о внутреннем устройстве в ответе нет |
| Чем подтверждается | `docs/rules/error-handling-policy.md` §«Внешняя поверхность» (единый глобальный обработчик и единый error-DTO, не per-endpoint) |
| Факт | |

### B9.7 — Непредусмотренный отказ наружу устройства не рассказывает

| Поле | Содержание |
|---|---|
| Предусловия | стаб владельца отвечает телом, которое роняет обработку на стороне периметра |
| Вход | запрос, доводящий тропу до непредусмотренного отказа |
| Ожидаемые выходы — все | `500` с единым error-DTO; текста исключения, имени класса и стека в теле нет; в лог отказ уходит целиком |
| Чем подтверждается | `docs/rules/error-handling-policy.md` §«Внешняя поверхность»; `docs/rules/error-handling-policy.md` §«Что отказ НЕ сообщает» |
| Факт | |

### B9.8 — Отказ доступа оставляет наблюдаемый след

| Поле | Содержание |
|---|---|
| Предусловия | контекст поднят штатно |
| Вход | серия из десяти запросов без предъявления по разным путям |
| Ожидаемые выходы — все | строки в базе не заводится ни одной — базы у периметра нет, и это верно; **но след остаётся**: каждый отказ виден строкой лога с путём и глаголом, а частота отказов видна рядом, который снимает наблюдатель окружения. Сегодня оба ожидания красны: точка входа отказа при отсутствующем писателе не пишет **ни строки лога**, реестра рядов у сервиса нет вовсе, и серия отказов не наблюдаема ничем |
| Чем подтверждается | `docs/rules/api-access-policy.md` §«След отказа пишет тот, у кого есть база» (что остаётся вместо строки: лог отказа и метрика их частоты); долг F-5 — `.claude/work/backlog.md` §«Отказ доступа у периметра не оставляет ни лога, ни метрики» |
| Факт | |

### B9.9 — Пер-операционных проверок права у периметра нет

| Поле | Содержание |
|---|---|
| Предусловия | стаб `auth` отвечает членством с ролью `VIEWER`; стаб владельца отвечает `200` |
| Вход | мутирующий запрос к владельцу под этим токеном |
| Ожидаемые выходы — все | периметр запрос **пересылает**: право операции проверяет владелец, а не он; роль уходит заголовком контекста; собственного отказа по праву периметр не порождает ни на одном пути |
| Чем подтверждается | `docs/rules/api-access-policy.md` §«Где проверяется право операции: у домена, а не у периметра» |
| Факт | |

### B9.10 — Отказ по праву внутри диспетчера отвечает своим статусом

| Поле | Содержание |
|---|---|
| Предусловия | пер-операционная проверка права заведена хотя бы на одной точке периметра |
| Вход | запрос, которому право не даётся |
| Ожидаемые выходы — все | `403` с единым error-DTO, а не `500`; перехватчик непредусмотренного отказа его не съедает. Кейс предъявляет объявленный долг и сегодня недостижим: проверок права нет ни одной |
| Чем подтверждается | `docs/rules/error-handling-policy.md` §«Отказ доступа — тот же контракт, что и прочие ошибки»; долг — `.claude/work/backlog.md` §«Перехватчик `Exception` съедает отказ по правам у `bff` и `strategies`» |
| Факт | |

## B10 — Конфигурация как вход

### B10.1 — Незаданная точка провайдера идентичности контекст не поднимает

| Поле | Содержание |
|---|---|
| Предусловия | `spring.security.oauth2.resourceserver.jwt.issuer-uri` пуст |
| Вход | подъём контекста |
| Ожидаемые выходы — все | контекст не поднимается; поверхность не становится открытой ни на одном пути; отказ наступает при старте, а не на первом запросе |
| Чем подтверждается | `docs/rules/api-access-policy.md` §«Вся поверхность закрыта; открытое — перечислено»; `docs/rules/absent-value-semantics.md` (незаданное есть отказ, а не разрешение) |
| Факт | |

### B10.2 — Подпись токена проверяется локально по ключам провайдера

| Поле | Содержание |
|---|---|
| Предусловия | стаб провайдера отдаёт диспетчер и набор ключей; стаб `auth` поднят |
| Вход | десяток запросов под принятым токеном |
| Ожидаемые выходы — все | к стабу провайдера уходит не более чем добыча набора ключей, и **не на каждый запрос**; вызова к `auth` за проверкой подписи нет ни одного — членства он отвечает, подпись не проверяет; токен, подписанный чужим ключом, отвергается без сетевого вызова |
| Чем подтверждается | `docs/architecture/contracts.md` §«Контекст тенанта в вызове», п. 2 (подпись проверяется локально, без вызова `auth`) |
| Факт | |

### B10.3 — Выдающая сторона токена сверяется

| Поле | Содержание |
|---|---|
| Предусловия | стаб провайдера поднят; тест подписывает токен верным ключом, но с **другой** выдающей стороной |
| Вход | запрос контекста |
| Ожидаемые выходы — все | `401`; верной подписи мало — выдающая сторона сверяется тоже; тот же токен с верной выдающей стороной принимается |
| Чем подтверждается | `docs/rules/api-access-policy.md` §«Вся поверхность закрыта; открытое — перечислено»; решение 4 дизайн-прохода `.claude/decisions/test-contour-design-pass.md` (сервисы объявили именно точку выдающей стороны, а не набор ключей) |
| Факт | |

### B10.4 — Оси окружения приезжают конфигурацией, а не хардкодом

| Поле | Содержание |
|---|---|
| Предусловия | контекст поднимается с переопределёнными значениями каждой оси |
| Вход | шаблон адреса владельца, бюджет повторов чтения, срок кэша членств, перечень тем, ширина окна, период и выключатель пульса, срок соединения, потолок подписок, секрет и срок билета |
| Ожидаемые выходы — все | каждое переопределённое значение наблюдаемо меняет поведение соответствующего кейса групп выше; ни одна из величин не зашита в код; умолчание каждой выбрано так, что незаданное означает отказ либо безопасную сторону |
| Чем подтверждается | `docs/architecture/platform.md` §«Чем различаются окружения»; `docs/architecture/services/bff.md` §«Что держит истиной» |
| Факт | |

### B10.5 — Размер пула планировщика осью окружения НЕ является

| Поле | Содержание |
|---|---|
| Предусловия | контекст поднят на умолчаниях |
| Вход | чтение объявления размера пула |
| Ожидаемые выходы — все | плейсхолдера переменной окружения у него нет; значение стои́т числом; сколько у сервиса джоб, знает его дерево, а не манифест |
| Чем подтверждается | `.claude/rules/codestyle.md` §Джобы (размер пула выводится из числа `@Scheduled`-методов и объявляется величиной конфигурации сервиса) |
| Факт | |

### B10.6 — Перечень тем подписки — ось окружения, и пустой он подписки не создаёт

| Поле | Содержание |
|---|---|
| Предусловия | контекст поднимается с одной темой в перечне, затем — с пустым |
| Вход | подъём в обеих конфигурациях; подача записи в неназванную тему |
| Ожидаемые выходы — все | при одной теме записи второй темы в провод не уходят; при пустом перечне слушателя не создаётся, и пульс молчит — живости потребления нет; поверхность при этом поднимается и отвечает |
| Чем подтверждается | `docs/architecture/contracts.md` §«Подписка идёт на темы, у которых есть производитель»; §«Пульс отражает живость ПОТРЕБИТЕЛЯ, а не планировщика» того же дока |
| Факт | |

## B11 — Границы: чего периметр не делает

### B11.1 — Своей базы нет ни одной

| Поле | Содержание |
|---|---|
| Предусловия | контейнер базы прогоном **не поднимается** |
| Вход | полный цикл: подъём контекста, контекст, билет, подписка, приём записи, пересылка |
| Ожидаемые выходы — все | контекст поднимается без базы; подключения к какой-либо базе в нём нет ни одного; миграций не накатывается; ни один кейс выше базы не требовал |
| Чем подтверждается | `docs/architecture/services/bff.md` §«Что держит истиной» (персистентного состояния периметр не держит принципиально) |
| Факт | |

### B11.2 — В темы периметр не публикует ничего

| Поле | Содержание |
|---|---|
| Предусловия | брокер поднят; подписка открыта; записи приняты |
| Вход | полный цикл, включая отказы каждого рода |
| Ожидаемые выходы — все | ни в одну тему не опубликовано ни одной записи; строки outbox у периметра нет — базы нет; реле у него не существует |
| Чем подтверждается | `docs/architecture/services/bff.md` §«Что потребляет и что публикует» (периметр — читатель шины, не писатель) |
| Факт | |

### B11.3 — Durable-группой периметр не принимает

| Поле | Содержание |
|---|---|
| Предусловия | контекст поднят |
| Вход | обход групп потребителя процесса; обход поверхности в поисках величин полноты приёма |
| Ожидаемые выходы — все | группа у реплики одна и она эфемерна — имя её уникально на процесс; строки состояния приёма нет ни одной; величин полноты приёма поверхность не отдаёт; в счёт durable-потребителей периметр не входит; тика состояния приёма у него нет |
| Чем подтверждается | `docs/rules/durable-consumer-reception.md` §«Кто durable-потребитель»; `docs/architecture/services/bff.md` §«Что потребляет и что публикует» |
| Факт | |

### B11.4 — К ярусу интеграции периметр не ходит

| Поле | Содержание |
|---|---|
| Предусловия | стаб коннектора поднят и доступен по адресу шаблона |
| Вход | полный цикл, включая пересылку по пути с первым сегментом `connector-okx` |
| Ожидаемые выходы — все | к стабу коннектора не ушло ни одного запроса **сверх** того, который браузер адресовал явно; собственной тропы к ярусу интеграции у периметра нет; строки `bff → connector-<биржа>` в таблице вызовов нет, и по ярусам быть не может |
| Чем подтверждается | `docs/architecture/services/bff.md` §«Какие вызовы делает и какие принимает»; `docs/architecture/contracts.md` §«Периметр: что `bff` отдаёт и чего не делает» (ярус интеграции в область конвенции не входит) |
| Факт | |

### B11.5 — Торговых решений периметр не принимает

| Поле | Содержание |
|---|---|
| Предусловия | подписка открыта; в тему кладутся записи подъёма ступени и отчёта о происшествии |
| Вход | полный цикл |
| Ожидаемые выходы — все | ни одной команды владельцу по своей инициативе не уходит; ни одна ступень защиты периметром не поднимается и не снимается; содержимое принятых записей на его поведение не влияет ничем, кроме показа |
| Чем подтверждается | `docs/architecture/services/bff.md` §«Чего не делает намеренно»; `docs/rules/audit-not-runtime-source.md` |
| Факт | |

### B11.6 — Контракта «на экран» периметр не заводит

| Поле | Содержание |
|---|---|
| Предусловия | стаб владельца отвечает своей формой |
| Вход | пересылка чтения; приём события |
| Ожидаемые выходы — все | пересланное идёт формой **владельца** — периметр её не переписывает и полей не добавляет; порождённое им (запись потока, билет, контекст, отказ) идёт его формой; третьего рода содержимого, отдаваемого браузеру, у него нет |
| Чем подтверждается | `docs/architecture/contracts.md` §«Форму задаёт происхождение: порождённое — своё, пересланное — чужое»; `docs/architecture/services/bff.md` §«Что потребляет и что публикует» |
| Факт | |

### B11.7 — Членств периметр не пишет и своей копии не держит

| Поле | Содержание |
|---|---|
| Предусловия | стаб `auth` отвечает членством |
| Вход | десяток запросов контекста разных субъектов |
| Ожидаемые выходы — все | к `auth` уходят только чтения-резолвы; мутирующих вызовов сверх точки резолва нет; после перезапуска контекста членства перечитываются заново — копии, переживающей процесс, не существует |
| Чем подтверждается | `docs/architecture/contracts.md` §«Контекст тенанта в вызове», п. 2 (периметр членств не пишет и своей копии не держит, кроме эфемерного кэша) |
| Факт | |

### B11.8 — Отказы источников периметр в один не сливает

| Поле | Содержание |
|---|---|
| Предусловия | стабы двух владельцев подняты; один отвечает отказом, другой — успехом |
| Вход | два запроса браузера, каждый своему владельцу |
| Ожидаемые выходы — все | исход каждого возвращается **отдельно**, со своим статусом и телом; общего «частично не получилось» периметр не порождает; решение о повторе остаётся у вызывающего |
| Чем подтверждается | `docs/architecture/contracts.md` §«Агрегация не превращает несколько отказов в один» |
| Факт | |

## Кейсы, не прогоняемые сегодня

| Кейс | Почему не прогоняется | Что сделает прогоняемым |
|---|---|---|
| B4.8 | одновременность открытия и публикации на одном процессе воспроизводима лишь вероятностно: детерминированной точки между регистрацией подписки и переигрыванием снаружи нет | правка порядка (находка F-4) — тогда кейс становится проверкой инварианта, а не гонки; либо проба уровня 2 на самом реестре подписок |
| B6.7 | вторая реплика процесса в ящике не поднимается: контекст у кейса один | сквозной набор уровня 3 либо прогон на стенде с двумя репликами периметра |
| B7.5 | расхождение имени компонента требует правки формы **владельца**, то есть чужого артефакта; охраны у связки имён нет ни одной | вторая ось того же стыка — позиция об именах компонентов (`.claude/work/backlog.md`) |
| B9.10 | пер-операционных проверок права нет ни одной, и отказ по правам внутри диспетчера недостижим | второй субъект поверхности (`.claude/work/backlog.md` §«Второй субъект поверхности — пересмотр посылки о единственном принципале») |
| B10.1 | контекст с пустой точкой провайдера не поднимается, и обычной формой кейса ящика это не наблюдается — предмет наблюдения есть **отказ подъёма** | отдельная форма кейса: попытка подъёма контекста с ожидаемым отказом, как у соседей по перечню |

## Находки владельцам

Каждая получила секцию бэклога тем же заходом
(`.claude/rules/parking-address.md`); здесь — только указатель.

| Находка | Предмет | Где припаркована |
|---|---|---|
| F-1 | срок билета вчетверо короче срока соединения: плановое закрытие подписки **всегда** попадает вне срока билета, и штатное переподключение `EventSource` отвечает отказом | `.claude/work/backlog.md` §«Срок билета подписки короче срока соединения, и штатный разрыв попадает вне его» |
| F-2 | неразбираемый момент происшествия роняет проход слушателя, хотя правило требует пропускать запись и раздачу не останавливать | `.claude/work/backlog.md` §«Неразбираемый момент происшествия роняет раздачу вместо пропуска» |
| F-3 | окно переигрывания заводится для всякого тенанта, чья запись доехала, и не вытесняется никогда: единственное состояние периметра, растущее числом тенантов системы | `.claude/work/backlog.md` §«Окно переигрывания периметра копится по тенантам без подписок и не вытесняется» |
| F-4 | подписка регистрируется в наборе до переигрывания: запись, пришедшая в промежутке, обгоняет хвост окна, а восстановить порядок клиенту нечем | `.claude/work/backlog.md` §«Переигрывание идёт после регистрации подписки, и параллельный факт обгоняет хвост» |
| F-5 | отказ доступа у периметра не оставляет ни строки лога, ни ряда метрики — обе объявленные замены персистентной строке отсутствуют | `.claude/work/backlog.md` §«Отказ доступа у периметра не оставляет ни лога, ни метрики» |
| F-6 | перечень пересылаемых заголовков назван «переговорными заголовками содержимого», а несёт только тип содержимого: принимаемое владельцу не уходит | `.claude/work/backlog.md` §«Переговорные заголовки содержимого периметр обещает шире, чем пересылает» |

## Связи

- Форма кейса и три уровня — `.claude/skills/test-design.md`; роль —
  `.claude/agents/tester.md`.
- Контур и его развилки — `.claude/decisions/test-contour-design-pass.md`.
- Предмет — `docs/architecture/services/bff.md`,
  `docs/architecture/contracts.md` §«Периметр: что `bff` отдаёт и чего не
  делает», §«Живые данные в браузер», §«Контекст тенанта в вызове».
- Контур доступа — `docs/rules/api-access-policy.md`; формат отказа —
  `docs/rules/error-handling-policy.md`.
- Сосед по форме, чьи кейсы написаны прежде — `.claude/tests/cases/statistics.md`.
