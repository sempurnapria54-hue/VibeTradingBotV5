# Тест-кейсы: `market-data-indicators`

## На какой вопрос отвечает этот файл

Какие тест-кейсы у расчётного слоя рыночных данных.

## Предмет

**Расчётный слой сервиса `market-data`** — седьмой предмет **уровня 2**
перечня под-шага 1 `CODE` шага 12 фазы 2
(`.claude/decisions/test-contour-design-pass.md`
§«Перечень предметов и порядок работы»). Уровень у всех кейсов ниже один —
`U`; мишень у предмета одна, поэтому суффикса мишени метки не несут
(`.claude/skills/test-design.md` §«Форма — предмет → кейсы → таблица»).

Признак уровня выполнен механически: **у предмета нет ввода-вывода**.
Команда воспроизводима, и попадание у неё одно — в тексте javadoc, а не в
коде:

```bash
grep -n 'DataService\b\|Repository\b\|Instant\.now\|KafkaTemplate\|RestClient\|@Autowired' \
  services/market-data/src/main/java/com/example/marketdata/domain/service/indicator/*.java \
  services/market-data/src/main/java/com/example/marketdata/domain/service/structure/*.java \
  services/market-data/src/main/java/com/example/marketdata/domain/service/phase/*.java \
  services/market-data/src/main/java/com/example/marketdata/util/IndicatorMath.java
```

Всё состояние приезжает аргументом: ряд закрытых свечей, параметры
идентичности вычисления, готовые скаляры входов, клаузы классификации.
Ни один вход не ходит в базу, к соседу, в брокер и к часам — ровно то, на
чём стои́т делимость расчёта между живой торговлей и бэктестом
(`docs/processes/market-data-calculation.md` §«Главная идея»).

Единицы предмета и их поверхность:

| Единица | Вход → выход | Дом поведения |
|---|---|---|
| вычислители `IndicatorCalculator`, по одному на тип | ряд закрытых свечей плюс параметры → ряд значений после прогрева | `docs/models/domain/other/IndicatorValue.md`, `docs/components/IndicatorJob.md` §Прогрев |
| умолчания интерфейса вычислителя | override и выведенное → эффективный прогрев; свеча → отметка времени значения | `docs/rules/strategy-condition-contract.md` §«Прогрев выводится, override возможен» |
| `IndicatorMath` | ряд чисел плюс окно → серия EMA, SMA, популяционное СКО | `docs/models/domain/other/IndicatorValue.md` §Наследники |
| `MarketStructureResolver` | свечи окна, готовые скаляры, параметры → тип, уровни, событие пробоя, границы окна | `docs/models/domain/other/MarketStructure.md` §«Семантика классификации (как считается)», `docs/components/MarketStructureResolver.md` |
| `MarketPhaseResolver` | клаузы «условие → фаза» плюс контекст → тип фазы | `docs/components/MarketPhaseResolver.md`, `docs/models/domain/other/MarketPhase.md` |

**Чего предмет не включает, и границы названы, а не подразумеваются:**

- **джобы, раздающие сервисы и реестр идентичностей** (`IndicatorJob`,
  `MarketStructureJob`, `IndicatorService`, `MarketStructureService`,
  `MarketPhaseService`, `MarketDataExpirationChecker`) — они ходят в базу
  и к соседу, то есть признак уровня 2 у них ложен по построению; их
  поведение — предмет чёрного ящика `.claude/tests/cases/market-data.md`;
- **выбор окна и глубины ряда, идемпотентность записи и чекпойнт** —
  там же: вычислитель получает ряд, а чем он ограничен, решает джоба
  (`docs/components/IndicatorJob.md` §«Что делает»);
- **грамматика условий** (`StrategyConditionEvaluator` и его операнды) —
  предмет `.claude/tests/cases/strategy-engine-condition.md`. Резолвер
  фазы её потребляет и не держит: здесь проверяется **порядок клауз и
  консервативный дефолт**, а не истинность условия;
- **десятичный контекст** (`DomainMath.CONTEXT`) — предмет
  `domain-model-predicates`, следующий в перечне. Здесь он операнд: точность
  промежуточного деления объявлена им, а не вычислителем;
- **свежесть результата** — ни один вычислитель её не мерит: точка отсчёта
  объявлена у модели, а срок приносит потребитель
  (`docs/rules/market-data-freshness.md`).

## Новая ось формы: вход и выход — РЯДЫ, и ожидание бывает свойством

Форма уровня 2 объявлена в
`.claude/tests/cases/strategy-engine-calculation.md` §«Форма уровня 2
выбрана здесь» и здесь не переоткрывается: группа несёт базовую сборку и
дом, кейс — строка таблицы с пятью колонками. Этот предмет добавляет к ней
**свою** ось, и она о том, чем выход вообще является.

**У соседей по уровню выход был точкой:** посчитанное число, булев
вердикт, ребро матрицы, протокол вызовов. Здесь выход — **ряд, выровненный
по входному ряду**, и кейс, называющий только значение, не проверяет двух
третей предмета. Поэтому строка ожидания называет **три величины ряда**:

- **с какого бара ряд начинается** — зона прогрева пропущена, и её граница
  есть наблюдаемый исход, а не внутренняя деталь
  (`docs/components/IndicatorJob.md` §Прогрев);
- **сколько в ряду членов** — в том числе «ни одного», когда истории
  короче порога;
- **чему равен член** — значение на названном баре.

Пропуск любой из трёх оставляет предмет непроверенным: ряд, сдвинутый на
бар, несёт верные числа на неверных отметках времени, и читатель получает
look-ahead, которого дом запрещает.

**Вторая половина оси: ожидание бывает СВОЙСТВОМ, а не числом.** Деление
идёт с точностью, объявленной доменным контекстом, и переписывать в
ожидание тридцать четыре разряда значило бы вклеить в кейс ответ
реализации вместо ответа дома. Признак выбора механический:

- **число** — когда вход сконструирован так, что **деления точны**:
  окно, чья сумма делится на длину нацело; ряд, где множитель
  сглаживания равен половине; вырожденный ряд, на котором формула
  схлопывается в константу. Такие входы ниже названы прямо;
- **свойство** — когда деление приблизительно: границы области значений
  (осциллятор в своих пределах, нормированная величина в своих), знак,
  монотонность по параметру, равенство двух путей расчёта, инвариант
  «верх ≥ середина ≥ низ».

**Свойство не слабее числа.** Ожидание «RSI ∈ [0,100]» падает на
перепутанных местами приросте и убытке ровно так же, как падает точное
число, и не падает на смене последнего разряда деления — то есть мерит
предмет, а не арифметику библиотеки.

**Третья половина оси называется отдельно, потому что она о дефолтах:
вырожденное окно.** Плоский ряд (все цены равны) и нулевой знаменатель —
это не «редкий вход», а штатное состояние неликвидного бара, и каждый
вычислитель отвечает на него **своей** константой. Дом об этом молчит, и
поэтому ожидания у таких кейсов нет: они стоя́т описанными и помечены
непрогоняемыми (§«Кейсы, не прогоняемые сегодня»).

## Чем достаются выходы

Контур — `.claude/decisions/test-contour-design-pass.md`, здесь не
пересказывается (`.claude/rules/policy-home.md`). Что из него следует
**для этого предмета**:

- **субстрата нет вовсе** — ни контейнеров, ни контекста Spring, ни
  стабов: вычислитель конструируется `new AtrCalculator()`, резолвер —
  `new MarketStructureResolver()`. Аннотации `@Component` и `@Service` на
  классах — деталь потребителя, конструктор пуст у всех, кроме резолвера
  фазы;
- **моков нет ни одного, и у резолвера фазы это НЕ послабление, а тот же
  механический признак.** Его единственный коллаборатор — вычислитель
  условий, и ввода-вывода у него нет: собственных полей у класса нет
  вовсе, состояние приезжает контекстом. Признак мокания, введённый на
  третьем предмете уровня, требует подмены там, где у коллаборатора
  ввод-вывод **есть**; здесь его нет, и коллаборатор берётся настоящим.
  Цена названа: кейс группы клауз строит настоящее условие, и его
  истинность обеспечивается простейшим сравнением констант — грамматику
  мерит соседний предмет, здесь она средство;
- **состояние собирается настоящими полями доменных моделей**
  (`.claude/rules/codestyle.md` §«Тесты доменных моделей»): свеча несёт
  открытие, максимум, минимум, закрытие и объём; параметры — свои поля;
  клауза — свой тип и своё условие. Подменённый вычислитель условий у
  резолвера фазы проверял бы порядок против модели, которой в проде не
  существует;
- **временем управлять нечем**: ни один вход не читает часов. Отметка
  времени значения выводится из момента открытия бара, то есть из данных;
- **отметки времени наблюдаются как значения**: кейс сравнивает момент
  значения с моментом бара, а не с «сейчас»;
- **лог наблюдается захватом вывода логгера** ровно там, где он и есть
  весь наблюдаемый эффект, — а у этого предмета такого места нет ни
  одного: ни один вычислитель и ни один резолвер не пишет в лог.
  Отсутствие лога тоже ожидание, и оно названо в §«U17 — Отсутствие
  выходов: чего слой не делает».

## Ожидание берётся из дома, даже когда сегодня оно не исполнено

Часть кейсов ниже ожидает то, что дом объявляет, а дерево кода несёт
иначе. Такие ожидания **не ослабляются под текущий факт** — их красный
прогон и есть предъявление названного долга. У кейса, где **два носителя
называют разное** либо дом молчит, ожидания нет вовсе: он стои́т описанным
и помечен непрогоняемым.

| Расхождение | Где припарковано |
|---|---|
| подтверждённый пробой структуру не переклассифицирует, а событие считается и на типе «неизвестно» | `.claude/work/backlog.md` §«Подтверждённый пробой структуру не переклассифицирует и считается на любом типе» |
| подтверждение уровня касаниями к выдаваемым уровням не применяется: наружу идут сырые пивоты и экстремумы окна | `.claude/work/backlog.md` §«Подтверждение уровня касаниями к выдаваемым уровням структуры не применяется» |
| момент подтверждения структуры равен концу окна всегда, хотя дом разводит его с границей окна | `.claude/work/backlog.md` §«Момент подтверждения структуры равен концу окна всегда» |
| кумулятивный объёмный индикатор считается от начала окна прохода, а не истории | `.claude/work/backlog.md` §«Кумулятивный OBV считается от начала окна прохода, а не истории» |
| от чего берётся процент буфера пробоя, два носителя называют по-разному | `.claude/work/backlog.md` §«От чего процент у буфера пробоя, два носителя называют по-разному» |
| константа вырожденного окна у вычислителей своя у каждого, и дома у неё нет — тем же носителем, что у формул | `.claude/work/backlog.md` §«Формулы и вырожденные дефолты индикаторов дома в корпусе не имеют» |
| параметры требования доезжают до вычислителя непроверенными, а их дефекты гасятся логом джобы | `.claude/work/backlog.md` §«Параметры требования вычисления доезжают до вычислителя непроверенными» |
| пустое условие клаузы истинно, и фаза становится безусловной | `.claude/work/backlog.md` §«Пустое условие истинно, и у клаузы фазы это даёт безусловную фазу» |
| непроставленный объём свечи читается нулём, а дом благоприятное умолчание на недобытом операнде запрещает | `.claude/work/backlog.md` §«Непроставленный объём свечи читается нулём, и исключения этой арифметике дом не заводил» |
| пробой ищется только в хвосте окна: удержание, завершившееся раньше последнего бара, событием не становится вовсе | `.claude/work/backlog.md` §«Пробой обнаруживается только хвостом окна» |

## Поверхность предмета

| Ось | Что её задаёт | Дом ожидания |
|---|---|---|
| эффективный прогрев | override настройки и выведенное реализацией | `docs/rules/strategy-condition-contract.md` §«Прогрев выводится, override возможен» |
| граница выдачи ряда | зона прогрева пропускается вычислителем | `docs/components/IndicatorJob.md` §Прогрев |
| отметка времени значения | момент открытия бара, UTC | `docs/models/domain/other/IndicatorValue.md` §Структура |
| идентичности на результате | инструмент и идентичность вычисления | `docs/models/domain/other/IndicatorValue.md` §«Ключевание — идентичностью вычисления» |
| состав полей значения | наследник по типу индикатора | `docs/models/domain/other/IndicatorValue.md` §«Наследники (значения по типу)» |
| нормировка величины | область значений своего типа | `docs/models/domain/other/IndicatorValue.md` §«Енум `Type`» |
| ограничение операнда кумулятивного индикатора | нестабильность абсолютного уровня | `docs/models/domain/other/IndicatorValue.md` §«Енум `Type`» |
| точность промежуточного деления | доменный арифметический контекст | `docs/rules/decimal-arithmetic.md` |
| свинг-пивот | локальный экстремум в окне с обеих сторон | `docs/models/domain/other/MarketStructure.md` §«Семантика классификации (как считается)» |
| подтверждение уровня | число касаний в пределах толеранса | там же |
| толеранс кластеризации | готовый скаляр волатильности либо доля цены | `docs/components/MarketStructureResolver.md` §«Потребляет готовые скаляры, не пересчитывает» |
| дискриминатор тренда и шума | готовый скаляр эффективности либо внутренний прокси | там же |
| тип структуры | геометрия пивотов, ширина полосы, касания | `docs/models/domain/other/MarketStructure.md` §«Семантика классификации (как считается)» |
| типы выдаваемых уровней | тип структуры | там же |
| событие пробоя | буфер и число баров удержания | `docs/models/domain/other/MarketStructure.md` §«MarketBreakoutEvent (раздел)» |
| границы окна и момент подтверждения | первый и последний бар окна | `docs/models/domain/other/MarketStructure.md` §«Енум `Type`» |
| консервативный исход структуры | неполные параметры, пустое окно, противоречивая геометрия | `docs/models/domain/other/MarketStructure.md` §«Семантика классификации (как считается)» |
| порядок клауз фазы | позиция в списке | `docs/components/MarketPhaseResolver.md` §«First-match по позиции» |
| консервативный дефолт фазы | ни одна клауза не истинна | там же |
| безгосударственность резолвера фазы | решение есть функция клауз и входов | `docs/components/MarketPhaseResolver.md` §Stateless |

## Четвёртая половина оси: у арифметики шести вычислителей дома нет

Осью формы это не является, но состав кейсов определяет сильнее всего, и
потому названо здесь, а не в находках.

**Формула индикатора в корпусе не объявлена ни одним носителем.** Доменный
док называет поля наследников и область значений
(`docs/models/domain/other/IndicatorValue.md` §«Наследники (значения по
типу)»); формула, вид сглаживания и вид среднеквадратичного отклонения живут
**только в javadoc реализации**. Исключений два: эффективность хода и
знаковая сумма объёма — их дом называет формулой и словами (§«Енум `Type`»
того же дока).

**Прогрев под этот клейм НЕ попадает, и это названо здесь, потому что
прежняя редакция раздела числила его в javadoc вместе с формулами.** Дом у
выведения есть: `docs/rules/strategy-condition-contract.md` §«Прогрев
выводится, override возможен» задаёт его **классом** — «оконные — период,
рекурсивные — кратно периоду, составные — от старшего периода». Отсюда у
оконных типов (полосы Боллинджера, эффективность хода) выведенное значение
проверяемо прямо и кейс прогоняем (U7.8, U10.8); у рекурсивных и составных
дом задаёт только форму, а множитель и слагаемые живут в реализации — там
кейс остаётся непрогоняемым; у кумулятивного типа класса в доме нет вовсе
(U9.9).

Отсюда прямое следствие для этого документа: **кейс, чьё ожидание можно
взять только из реализации, ожидания не имеет.** Такой кейс описан, помечен
непрогоняемым и снимет пометку заведённой спекой
(§«Кейсы, не прогоняемые сегодня»; находка — M-1).

**Это не отказ от проверки, а её условие.** Прогон, чьё ожидание списано с
реализации, зелен по построению и охраняет ноль: он падает ровно тогда,
когда кто-то правит реализацию, — и не отличает правку-починку от
правки-поломки. Дом формулы — спецификация, а не проза и не javadoc
(`docs/concept.md` §«Исполнимая форма»).

**Что при этом ПРОГОНЯЕМО у каждого вычислителя, и это не остаток.** Дом
называет состав полей значения, область значений величины, отметку времени,
идентичности на результате, границу зоны прогрева, порядок ряда и поведение
на истории короче порога. Это и есть те оси, на которых живут дефекты
выравнивания ряда и заглядывания вперёд, — и именно они не проверены
сегодня ничем.

## U1 — Общий контракт вычислителя: прогрев, отметка времени, идентичности

**Базовая сборка.** Ряд закрытых свечей по возрастанию момента открытия;
параметры своего типа с объявленным таймфреймом; идентификатор инструмента
и идентификатор идентичности вычисления — различимые числа. Коллабораторов
нет: вычислитель конструируется на месте.

**Дом группы** — `docs/components/IndicatorJob.md` §Прогрев,
`docs/rules/strategy-condition-contract.md` §«Прогрев выводится, override
возможен», `docs/models/domain/other/IndicatorValue.md` §Структура.

| Метка | Вход | Ожидаемый выход — весь | Чем подтверждается | Факт |
|---|---|---|---|---|
| U1.1 | override прогрева объявлен и больше выведенного | эффективный прогрев равен override; первое значение ряда стои́т на баре с номером override; выведенное не применяется | `docs/rules/strategy-condition-contract.md` §«Прогрев выводится, override возможен» | зелено 2026-09-19 |
| U1.2 | override пуст | эффективный прогрев равен выведенному реализацией; ряд начинается с него | там же | зелено 2026-09-19 |
| U1.3 | override меньше выведенного | принимается как есть, встречной проверки вычислитель не делает; ряд начинается раньше выведенной границы | там же (эффективное значение есть override либо выведенное) | зелено 2026-09-19 |
| U1.4 | базовая сборка | отметка времени значения равна моменту **открытия** своего бара в UTC — не закрытия, не момента расчёта | `docs/models/domain/other/IndicatorValue.md` §Структура | зелено 2026-09-19 |
| U1.5 | базовая сборка | на каждом значении ряда проставлены инструмент и идентичность вычисления, поданные аргументом, — и ничего сверх них | `docs/models/domain/other/IndicatorValue.md` §«Ключевание — идентичностью вычисления» | зелено 2026-09-19 |
| U1.6 | базовая сборка | технический идентификатор и audit-поля не проставлены ни на одном значении: их ставит персистентность | `docs/models/domain/other/Auditable.md` | зелено 2026-09-19 |
| U1.7 | базовая сборка | объявленный тип вычислителя совпадает с типом значений, которые он производит; по нему и только по нему его выбирает джоба | `docs/components/IndicatorJob.md` §«Что делает» | зелено 2026-09-19 |
| U1.8 | базовая сборка | ряд отдан по возрастанию отметки времени, тем же порядком, что вход; пропусков внутри ряда нет | `docs/models/domain/other/IndicatorValue.md` §«Правила хранения» (индекс чтения берёт окно назад от последнего) | зелено 2026-09-19 |
| U1.9 | эффективный прогрев больше длины ряда свечей | ряд пуст; исключения нет; частичных значений нет | `docs/components/IndicatorJob.md` §Прогрев | зелено 2026-09-19 |
| U1.10 | ряд свечей пуст | ряд пуст; исключения нет | там же | зелено 2026-09-19 |
| U1.11 | эффективный прогрев равен нулю | ряд начинается с первого бара, на котором формула определена, — граница задаётся не прогревом, а разгоном самой формулы | `docs/components/IndicatorJob.md` §Прогрев | зелено 2026-09-19 |
| U1.12 | период параметров пуст | ожидания нет: непроверенный параметр доезжает до вычислителя, и дом поведения на нём молчит | находка M-7 | не прогоняется — §«Кейсы, не прогоняемые сегодня» |

## U2 — Числовые помощники: серия EMA, окно SMA, популяционное отклонение

**Базовая сборка.** Ряд чисел и длина окна; утилита статическая, состояния
не держит.

**Дом группы** — дома у формул нет (M-1); прогоняемы здесь только
**структурные** ожидания серии: длина, положение разгона, поведение на
короткой истории.

| Метка | Вход | Ожидаемый выход — весь | Чем подтверждается | Факт |
|---|---|---|---|---|
| U2.1 | ряд короче окна | серия той же длины, что вход, и **вся** пуста; исключения нет | структурное ожидание: серия выровнена по входу | зелено 2026-09-19 |
| U2.2 | ряд длиной ровно в окно | заполнен единственный член — последний; предшествующие пусты | там же | зелено 2026-09-19 |
| U2.3 | окно равно единице | заполнены все члены серии, разгона нет вовсе | там же | зелено 2026-09-19 |
| U2.4 | ряд из равных чисел, окно любое | все заполненные члены серии равны этому числу: сглаживание постоянного ряда постоянно | свойство, не зависящее от вида сглаживания | зелено 2026-09-19 |
| U2.5 | ряд, на котором деление точно (сумма окна делится на его длину нацело, множитель сглаживания равен половине) | член разгона равен среднему первых чисел окна; следующий — предыдущий плюс половина приращения | ожидания нет: формула дома не имеет (M-1) | не прогоняется — §«Кейсы, не прогоняемые сегодня» |
| U2.6 | окно SMA по отрезку, чья сумма делится нацело | среднее отрезка; соседние члены ряда в расчёт не входят | ожидания нет (M-1) | не прогоняется — §«Кейсы, не прогоняемые сегодня» |
| U2.7 | окно из двух чисел, отстоящих на равное расстояние от середины | отклонение равно половине их разности — то есть **популяционное**, а не выборочное | ожидания нет (M-1); ровно эта развилка и делает находку несущей | не прогоняется — §«Кейсы, не прогоняемые сегодня» |
| U2.8 | окно из равных чисел | отклонение равно нулю; корень из нуля берётся без отказа | свойство | зелено 2026-09-19 |
| U2.9 | окно негодное — ноль либо отрицательное | ожидания нет: помощники делят на длину окна и на разгон, ветвь отказа у них своя, и дом её не называет — ровно как у вычислителей (`U1.12`) | находка M-7; пробел `G2`, добран под-шагом 3 | не прогоняется — §«Кейсы, не прогоняемые сегодня» |

## U3 — EMA: выравнивание ряда и разгон

**Базовая сборка.** Параметры EMA с объявленным периодом; ряд закрытых
свечей, различающихся ценой закрытия.

**Дом группы** — `docs/models/domain/other/IndicatorValue.md` §«Наследники
(значения по типу)» (состав поля); арифметика дома не имеет (M-1).

| Метка | Вход | Ожидаемый выход — весь | Чем подтверждается | Факт |
|---|---|---|---|---|
| U3.1 | базовая сборка | у каждого значения заполнено одно поле — сглаженная цена; иных полей у наследника нет | `docs/models/domain/other/IndicatorValue.md` §«Наследники (значения по типу)» | зелено 2026-09-19 |
| U3.2 | ряд короче периода | ряд пуст; исключения нет (порога длины у вычислителя нет — пустоту даёт разгон серии) | структурное ожидание | зелено 2026-09-19 |
| U3.3 | период равен единице, прогрев переопределён нулём | значение на каждом баре равно цене закрытия этого бара | ожидания нет (M-1) | не прогоняется — §«Кейсы, не прогоняемые сегодня» |
| U3.4 | ряд из равных цен закрытия | все значения равны этой цене | свойство | зелено 2026-09-19 |
| U3.5 | прогрев не переопределён | первое значение стои́т на баре с номером, равным удвоенному периоду | ожидания нет: выведение прогрева живёт в реализации (M-1) | не прогоняется — §«Кейсы, не прогоняемые сегодня» |
| U3.6 | базовая сборка | учитываются только цены закрытия; максимум, минимум и объём на результат не влияют | javadoc реализации — ожидания нет (M-1) | не прогоняется — §«Кейсы, не прогоняемые сегодня» |

## U4 — ATR: истинный диапазон и сглаживание

**Базовая сборка.** Параметры ATR с объявленным периодом; ряд свечей, у
которых максимум, минимум и закрытие различимы.

**Дом группы** — состав поля у `docs/models/domain/other/IndicatorValue.md`
§«Наследники (значения по типу)»; область значений — там же
(волатильность моделируется этим значением); арифметика дома не имеет
(M-1).

| Метка | Вход | Ожидаемый выход — весь | Чем подтверждается | Факт |
|---|---|---|---|---|
| U4.1 | базовая сборка | у каждого значения заполнено одно поле — средний истинный диапазон | `docs/models/domain/other/IndicatorValue.md` §«Наследники (значения по типу)» | зелено 2026-09-19 |
| U4.2 | любой ряд | все значения неотрицательны | свойство: диапазон есть расстояние | зелено 2026-09-19 |
| U4.3 | ряд короче периода | ряд пуст; исключения нет | структурное ожидание | зелено 2026-09-19 |
| U4.4 | ряд, где у каждого бара размах равен одному и тому же числу, а разрывов между барами нет | все значения равны этому числу | свойство: сглаживание постоянного ряда постоянно | зелено 2026-09-19 |
| U4.5 | бар с разрывом вверх: минимум выше закрытия предыдущего | истинный диапазон бара больше его собственного размаха | ожидания нет (M-1) | не прогоняется — §«Кейсы, не прогоняемые сегодня» |
| U4.6 | первый бар ряда | его истинный диапазон равен размаху: предыдущего закрытия нет | ожидания нет (M-1); ветвь названа отдельно, потому что она о **краевом** баре | не прогоняется — §«Кейсы, не прогоняемые сегодня» |
| U4.7 | прогрев не переопределён | первое значение стои́т на баре с номером, равным удвоенному периоду, даже если значение посчиталось раньше | ожидания нет (M-1) | не прогоняется — §«Кейсы, не прогоняемые сегодня» |
| U4.8 | ряд длиной ровно в период | единственное значение на последнем баре периода — и только если прогрев его не отсёк | структурное ожидание вместе с U1.9 | зелено 2026-09-19 |

## U5 — RSI: область значений и края

**Базовая сборка.** Параметры RSI с объявленным периодом; ряд закрытых
свечей.

**Дом группы** — `docs/models/domain/other/IndicatorValue.md` §«Наследники
(значения по типу)»; арифметика дома не имеет (M-1).

| Метка | Вход | Ожидаемый выход — весь | Чем подтверждается | Факт |
|---|---|---|---|---|
| U5.1 | базовая сборка | у каждого значения заполнено одно поле — значение осциллятора | `docs/models/domain/other/IndicatorValue.md` §«Наследники (значения по типу)» | зелено 2026-09-19 |
| U5.2 | произвольный ряд | каждое значение лежит в пределах от нуля до ста включительно | свойство нормированного осциллятора | зелено 2026-09-19 |
| U5.3 | ряд, растущий на каждом баре | все значения равны ста: убытка в окне нет | свойство границы | зелено 2026-09-19 |
| U5.4 | ряд, падающий на каждом баре | все значения равны нулю | свойство границы | зелено 2026-09-19 |
| U5.5 | ряд длиной ровно в период | ряд пуст: порог длины строгий — периода мало, нужен бар сверх него | структурное ожидание | зелено 2026-09-19 |
| U5.6 | ряд из равных цен закрытия (прироста нет, убытка нет) | ожидания нет: вычислитель отвечает верхней границей, и дом этой конвенции не имеет | находка M-1 (вырожденное окно) | не прогоняется — §«Кейсы, не прогоняемые сегодня» |
| U5.7 | прогрев не переопределён | первое значение стои́т на баре с номером, равным удвоенному периоду | ожидания нет (M-1) | не прогоняется — §«Кейсы, не прогоняемые сегодня» |
| U5.8 | ряд, где половина баров растёт, половина падает одинаковыми шагами | значение строго между границами; точное число не проверяется | свойство | зелено 2026-09-19 |

## U6 — MACD: три поля одного значения и порядок периодов

**Базовая сборка.** Параметры MACD с быстрым, медленным и сигнальным
периодами; ряд закрытых свечей длиннее суммы медленного и сигнального.

**Дом группы** — `docs/models/domain/other/IndicatorValue.md` §«Наследники
(значения по типу)» и §«Адресный компонент в условии (D1)»; арифметика дома
не имеет (M-1).

| Метка | Вход | Ожидаемый выход — весь | Чем подтверждается | Факт |
|---|---|---|---|---|
| U6.1 | базовая сборка | у каждого значения заполнены **три** поля — линия, сигнальная и гистограмма; пустых среди них нет | `docs/models/domain/other/IndicatorValue.md` §«Наследники (значения по типу)» | зелено 2026-09-19 |
| U6.2 | базовая сборка | гистограмма равна разности линии и сигнальной на том же баре — инвариант, проверяемый без знания формулы линий | `docs/models/domain/other/IndicatorValue.md` §«Адресный компонент в условии (D1)» (компоненты адресуются порознь и обязаны быть согласованы) | зелено 2026-09-19 |
| U6.3 | ряд короче медленного периода | ряд пуст; исключения нет | структурное ожидание | зелено 2026-09-19 |
| U6.4 | ряд из равных цен закрытия | линия, сигнальная и гистограмма равны нулю на всех барах | свойство | зелено 2026-09-19 |
| U6.5 | базовая сборка | ряд начинается не раньше бара, на котором определена **сигнальная** линия: значения с пустой сигнальной в ряд не идут | структурное ожидание (три поля обязательны — U6.1) | зелено 2026-09-19 |
| U6.6 | прогрев не переопределён | первое значение стои́т на баре с номером, равным сумме медленного и сигнального периодов | ожидания нет (M-1) | не прогоняется — §«Кейсы, не прогоняемые сегодня» |
| U6.7 | быстрый период **больше** медленного | ожидания нет: порядок периодов не охраняет ни создание определения, ни требование вычисления, ни сам вычислитель; исход — отказ разыменования, который джоба гасит в лог | находка M-7 | не прогоняется — §«Кейсы, не прогоняемые сегодня» |
| U6.8 | быстрый период равен медленному | ожидания нет: линия вырождается в ноль на всех барах, и дом не говорит, законна ли такая идентичность | находка M-7 | не прогоняется — §«Кейсы, не прогоняемые сегодня» |

## U7 — Полосы Боллинджера: инвариант порядка полос и производные

**Базовая сборка.** Параметры полос с периодом и множителем отклонения; ряд
закрытых свечей.

**Дом группы** — `docs/models/domain/other/IndicatorValue.md` §«Наследники
(значения по типу)»; арифметика дома не имеет (M-1).

| Метка | Вход | Ожидаемый выход — весь | Чем подтверждается | Факт |
|---|---|---|---|---|
| U7.1 | базовая сборка | у каждого значения заполнены **пять** полей — верх, середина, низ, ширина и положение цены; пустых среди них нет | `docs/models/domain/other/IndicatorValue.md` §«Наследники (значения по типу)» | зелено 2026-09-19 |
| U7.2 | положительный множитель отклонения | на каждом баре верх не ниже середины, а середина не ниже низа | свойство: инвариант порядка полос | зелено 2026-09-19 |
| U7.3 | базовая сборка | ширина равна отношению расстояния между границами к середине — инвариант, проверяемый из самих полей значения | свойство: производная величина согласована со своими операндами | зелено 2026-09-19 |
| U7.4 | цена закрытия равна верхней границе | положение цены равно единице; равна нижней — нулю | свойство нормировки | зелено 2026-09-19 |
| U7.5 | ряд короче периода | ряд пуст; исключения нет | структурное ожидание | зелено 2026-09-19 |
| U7.6 | ряд из равных цен закрытия | полосы схлопываются в середину; ширина равна нулю по инварианту U7.3 — её делитель середина, и он не нулевой; **у положения цены ожидания нет**: его знаменатель нулевой, и константы вырожденного окна дом не называет | ширина — свойство (U7.3); положение цены — находка M-1 | зелено 2026-09-19 по четырём полям; положение цены не проверяется (M-1) |
| U7.7 | множитель отклонения равен нулю | все три полосы совпадают с серединой; ширина равна нулю; **у положения цены ожидания нет** — ширина нулевая, и оно отдаётся той же константой вырожденного окна, что у U7.6: кейс прогоняется по четырём полям из пяти | свойство; положение цены — находка M-1 | зелено 2026-09-19 по четырём полям; положение цены не проверяется (M-1) |
| U7.8 | прогрев не переопределён | первое значение стои́т на баре с номером, равным периоду | `docs/rules/strategy-condition-contract.md` §«Прогрев выводится, override возможен» (оконные — период) | зелено 2026-09-19 |
| U7.9 | окно из двух цен, отстоящих на равное расстояние от середины, множитель равен единице | границы равны этим двум ценам | ожидания нет (M-1): исход зависит от вида отклонения, а он в корпусе не объявлен | не прогоняется — §«Кейсы, не прогоняемые сегодня» |
| U7.10 | весь ряд закрытий нулевой: середина полосы равна нулю | три полосы равны нулю; ширина отдана нулём, а не делением на ноль; отказа нет. **Само число ширины ожиданием не является** — дом константы вырожденного окна не называет (M-1); наблюдается отсутствие отказа и схлопывание полос | свойство (схлопывание) плюс отсутствие отказа; пробел `G1`, добран под-шагом 3 | зелено 2026-09-19 |

## U8 — Стохастик: две линии и их выравнивание

**Базовая сборка.** Параметры стохастика с окном, сглаживанием и окном
второй линии; ряд свечей с различимыми максимумом, минимумом и закрытием.

**Дом группы** — `docs/models/domain/other/IndicatorValue.md` §«Наследники
(значения по типу)» и §«Адресный компонент в условии (D1)»; арифметика дома
не имеет (M-1).

| Метка | Вход | Ожидаемый выход — весь | Чем подтверждается | Факт |
|---|---|---|---|---|
| U8.1 | базовая сборка | у каждого значения заполнены **два** поля — быстрая и медленная линии; пустых среди них нет | `docs/models/domain/other/IndicatorValue.md` §«Наследники (значения по типу)» | зелено 2026-09-19 |
| U8.2 | произвольный ряд | обе линии лежат в пределах от нуля до ста включительно | свойство нормированного осциллятора | зелено 2026-09-19 |
| U8.3 | ряд, где закрытие каждого бара равно его максимуму, **а максимумы не убывают** — то есть закрытие бара есть максимум своего окна; окна сглаживания единичны | быстрая линия равна ста на каждом баре ряда | свойство границы: числитель равен знаменателю | зелено 2026-09-19 |
| U8.4 | ряд, где закрытие каждого бара равно его минимуму, **а минимумы не возрастают** — то есть закрытие бара есть минимум своего окна; окна сглаживания единичны | быстрая линия равна нулю на каждом баре ряда | свойство границы: числитель нулевой | зелено 2026-09-19 |
| U8.5 | ряд короче окна | ряд пуст; исключения нет | структурное ожидание | зелено 2026-09-19 |
| U8.6 | базовая сборка | значение попадает в ряд только когда определены **обе** линии: бара, где медленная ещё пуста, в ряду нет | структурное ожидание (оба поля обязательны — U8.1) | зелено 2026-09-19 |
| U8.7 | окно, у которого **все** бары несут один и тот же максимум и тот же минимум, равные между собой: размах окна нулевой. Одного плоского бара мало — экстремумы берутся по окну, а не по бару | ожидания нет: вычислитель отвечает серединой шкалы, и дом этой конвенции не имеет | находка M-1 (вырожденное окно) | не прогоняется — §«Кейсы, не прогоняемые сегодня» |
| U8.8 | прогрев не переопределён | первое значение стои́т на баре с номером, равным сумме трёх окон | ожидания нет (M-1) | не прогоняется — §«Кейсы, не прогоняемые сегодня» |
| U8.9 | сглаживание поверх ряда с пустотой внутри окна: окно 3, сглаживание 3, вторая линия 1 | член результата остаётся пустым, пока окно сглаживания неполно: ряд начинается с бара 4, а бары 2 и 3 — где сырое значение уже есть — в него не входят; членов 8 | структурное ожидание; пробел `G3`, добран под-шагом 3 | зелено 2026-09-19 |

## U9 — Знаковая сумма объёма: единственная формула с домом

**Базовая сборка.** Параметры знаковой суммы объёма; ряд закрытых свечей с
объявленным объёмом.

**Дом группы** — `docs/models/domain/other/IndicatorValue.md` §«Енум
`Type`»: дом называет формулу словами — прирост объёма при росте цены
закрытия, убыль при падении, ноль при равенстве, — и там же объявляет
нестабильность абсолютного уровня.

| Метка | Вход | Ожидаемый выход — весь | Чем подтверждается | Факт |
|---|---|---|---|---|
| U9.1 | бар, чьё закрытие выше предыдущего | накопленное значение выросло ровно на объём этого бара | `docs/models/domain/other/IndicatorValue.md` §«Енум `Type`» | зелено 2026-09-19 |
| U9.2 | бар, чьё закрытие ниже предыдущего | накопленное значение упало ровно на объём этого бара | там же | зелено 2026-09-19 |
| U9.3 | бар, чьё закрытие равно предыдущему | накопленное значение не изменилось; значение на этот бар всё равно выдано | там же | зелено 2026-09-19 |
| U9.4 | ряд из двух баров, прогрев переопределён единицей | ряд содержит одно значение — на втором баре: первому бару предыдущего закрытия не даёт никто, и знака у его объёма нет | `docs/models/domain/other/IndicatorValue.md` §«Енум `Type`» (знак берётся сравнением с предыдущим закрытием); override — `docs/rules/strategy-condition-contract.md` §«Прогрев выводится, override возможен» | зелено 2026-09-19 |
| U9.5 | ряд из одного бара | ряд пуст; исключения нет | структурное ожидание | зелено 2026-09-19 |
| U9.6 | бар с непроставленным объёмом | ожидания нет: вычислитель читает пустой объём нулём, а дом благоприятное умолчание на недобытом операнде **запрещает** и своего перечня исключений этой арифметикой не пополнял | находка M-8; дом — `docs/rules/absent-value-semantics.md` §«Благоприятное умолчание запрещено» и §«Названные исключения» | не прогоняется — §«Кейсы, не прогоняемые сегодня» |
| U9.7 | один и тот же бар в составе **двух разных по длине** рядов | значения на нём **различаются**: уровень зависит от начала ряда, и дом это объявляет прямо | `docs/models/domain/other/IndicatorValue.md` §«Енум `Type`» (абсолютный уровень нестабилен) | зелено 2026-09-19 |
| U9.8 | ряд, где рост и падение чередуются равными объёмами | накопленное значение возвращается к исходному через пару баров | свойство | зелено 2026-09-19 |
| U9.9 | прогрев не переопределён | ожидания нет: выведенный прогрев кумулятивного типа домом не задан — класс «оконные / рекурсивные / составные» его не покрывает, а число живёт в javadoc реализации | находка M-1 | не прогоняется — §«Кейсы, не прогоняемые сегодня» |

## U10 — Эффективность хода: вторая формула с домом

**Базовая сборка.** Параметры эффективности хода с объявленным периодом;
ряд закрытых свечей.

**Дом группы** — `docs/models/domain/other/IndicatorValue.md` §«Енум
`Type`»: дом называет формулу как отношение модуля чистого хода к сумме
модулей побарных ходов окна и объявляет нормировку.

| Метка | Вход | Ожидаемый выход — весь | Чем подтверждается | Факт |
|---|---|---|---|---|
| U10.1 | ряд, монотонно растущий равными шагами | значение равно единице: чистый ход равен сумме побарных | `docs/models/domain/other/IndicatorValue.md` §«Енум `Type`» | зелено 2026-09-19 |
| U10.2 | ряд, монотонно падающий равными шагами | значение равно единице: в числителе модуль, направление на него не влияет | там же | зелено 2026-09-19 |
| U10.3 | ряд, вернувшийся к исходной цене внутри окна | значение равно нулю | там же | зелено 2026-09-19 |
| U10.4 | произвольный ряд | значение лежит в пределах от нуля до единицы включительно | там же (нормирован по определению) | зелено 2026-09-19 |
| U10.5 | у каждого значения ряда | заполнено одно поле — отношение; иных полей у наследника нет | `docs/models/domain/other/IndicatorValue.md` §«Наследники (значения по типу)» | зелено 2026-09-19 |
| U10.6 | ряд длиной ровно в период | ряд пуст: порог длины строгий — периода мало, нужен бар сверх него | структурное ожидание | зелено 2026-09-19 |
| U10.7 | ряд из равных цен закрытия | ожидания нет: знаменатель нулевой, и дом не называет исхода на нём | находка M-1 (вырожденное окно) | не прогоняется — §«Кейсы, не прогоняемые сегодня» |
| U10.8 | прогрев не переопределён | первое значение стои́т на баре с номером, равным периоду | `docs/rules/strategy-condition-contract.md` §«Прогрев выводится, override возможен» (оконные — период) | зелено 2026-09-19 |
| U10.9 | окно, где чистый ход вдвое меньше суммы побарных | значение равно половине | `docs/models/domain/other/IndicatorValue.md` §«Енум `Type`» (деление точно по построению входа) | зелено 2026-09-19 |

## U11 — Структура: окно, свинг-пивоты и их моменты

**Базовая сборка.** Ряд закрытых свечей окна по возрастанию открытия;
параметры структуры со **всеми** объявленными числами; готовые скаляры
входов не поданы (их ветвь мерит U12). Коллабораторов нет.

**Дом группы** — `docs/models/domain/other/MarketStructure.md`
§«Семантика классификации (как считается)» (пункты о пивотах и о моменте
подтверждения) и §Структура (поля каркаса).

| Метка | Вход | Ожидаемый выход — весь | Чем подтверждается | Факт |
|---|---|---|---|---|
| U11.1 | базовая сборка | начало окна равно моменту открытия первого бара, конец — последнего; оба в UTC | `docs/models/domain/other/MarketStructure.md` §Структура | зелено 2026-09-19 |
| U11.2 | бар, чей максимум выше максимумов всех соседей в пределах глубины поиска с обеих сторон | в уровнях есть свинг-максимум с ценой этого максимума | там же §«Семантика классификации (как считается)» | зелено 2026-09-19 |
| U11.3 | бар, чей минимум ниже минимумов соседей в той же окрестности | в уровнях есть свинг-минимум с ценой этого минимума | там же | зелено 2026-09-19 |
| U11.4 | бар, отстоящий от края окна меньше чем на глубину поиска | пивотом не становится: подтверждающих баров с одной стороны нет | там же (свинг подтверждается барами с **каждой** стороны) | зелено 2026-09-19 |
| U11.5 | пивот в середине окна | момент обнаружения равен моменту его бара; момент подтверждения — моменту бара, отстоящего вперёд на глубину поиска | там же (момент подтверждения есть гейт использования без заглядывания вперёд) | зелено 2026-09-19 |
| U11.6 | окно короче удвоенной глубины поиска | пивотов нет вовсе; тип структуры — «неизвестно»; отказа нет | там же (консервативный дефолт) | зелено 2026-09-19 |
| U11.7 | два соседних бара с **равными** максимумами, оба выше окрестности | ожидания нет: дом говорит «локальный максимум» и о равенстве соседей не высказывается | дом молчит | не прогоняется — §«Кейсы, не прогоняемые сегодня» |
| U11.8 | глубина поиска равна нулю | ожидания нет: параметр не проверен ни одним носителем, а дом такого окна не называет | находка M-7 | не прогоняется — §«Кейсы, не прогоняемые сегодня» |

## U12 — Структура: классификация типа

**Базовая сборка.** Параметры со всеми числами; ряд, дающий не менее двух
свинг-максимумов и двух свинг-минимумов; скаляр эффективности подан явно.

**Дом группы** — `docs/models/domain/other/MarketStructure.md`
§«Семантика классификации (как считается)»; ветви входов —
`docs/components/MarketStructureResolver.md` §«Потребляет готовые скаляры,
не пересчитывает».

| Метка | Вход | Ожидаемый выход — весь | Чем подтверждается | Факт |
|---|---|---|---|---|
| U12.1 | последние два максимума растут, последние два минимума растут, скаляр эффективности не ниже порога | тип — восходящий тренд | `docs/models/domain/other/MarketStructure.md` §«Семантика классификации (как считается)», пункт о тренде | зелено 2026-09-19 |
| U12.2 | последние два максимума падают, последние два минимума падают, скаляр не ниже порога | тип — нисходящий тренд | там же | зелено 2026-09-19 |
| U12.3 | геометрия тренда есть, скаляр **ниже** порога | тренд не признаётся; дальше проверяется диапазон | там же (чистый ход обязан доминировать над шумом) | зелено 2026-09-19 |
| U12.4 | скаляр равен порогу | тренд признаётся: граница включена | там же (порог записан нестрогим неравенством) | зелено 2026-09-19 |
| U12.5 | геометрии тренда нет, ширина полосы между крайними пивотами в объявленных пределах, касаний к каждой границе не меньше требуемого | тип — диапазон | там же, пункт о диапазоне | зелено 2026-09-19 |
| U12.6 | ширина полосы **уже** минимальной | тип — «неизвестно» | там же, пункт о консервативном дефолте | зелено 2026-09-19 |
| U12.7 | ширина полосы **шире** максимальной | тип — «неизвестно» | там же | зелено 2026-09-19 |
| U12.8 | касаний к одной из границ меньше требуемого | тип — «неизвестно» | там же | зелено 2026-09-19 |
| U12.9 | свинг-пивотов с одной из сторон меньше двух | тип — «неизвестно»: геометрия не определена | там же (противоречивая геометрия) | зелено 2026-09-19 |
| U12.10 | скаляр эффективности **не подан** | расчёт идёт внутренним прокси по ценам закрытия окна, отказа нет; при монотонном ряде прокси даёт единицу и тренд признаётся | `docs/models/domain/other/MarketStructure.md` §«Семантика классификации (как считается)» (вход не объявлен — внутренний прокси) | зелено 2026-09-19 |
| U12.11 | порог эффективности параметрами не задан | применяется провизорный дефолт резолвера, расчёт идёт, отказа нет; число дефолта ожиданием не является — дом объявляет его провизорным | там же (численные пороги — хвост пользователя, значения провизорны) | зелено 2026-09-19 |
| U12.12 | одновременно выполнены и геометрия тренда, и признаки диапазона | ожидания нет: дом перечисляет оба исхода и старшинства между ними не называет | дом молчит | не прогоняется — §«Кейсы, не прогоняемые сегодня» |

## U13 — Структура: уровни выхода и их типы

**Базовая сборка.** Та же, что у U12; наблюдается **состав уровней**
результата.

**Дом группы** — `docs/models/domain/other/MarketStructure.md`
§«MarketPriceLevel (раздел)» и §«Семантика классификации (как
считается)».

| Метка | Вход | Ожидаемый выход — весь | Чем подтверждается | Факт |
|---|---|---|---|---|
| U13.1 | классификация дала диапазон | среди уровней есть верхняя и нижняя границы диапазона; свинги при этом сохранены | `docs/models/domain/other/MarketStructure.md` §«Семантика классификации (как считается)», пункт о диапазоне | зелено 2026-09-19 |
| U13.2 | классификация дала тренд | среди уровней есть сопротивление и поддержка; свинги сохранены | там же, пункт о тренде | зелено 2026-09-19 |
| U13.3 | классификация дала «неизвестно» | граничных уровней нет вовсе: в составе только свинги, если они были | там же (уровни названы у диапазона и у тренда, у консервативного исхода не названы) | зелено 2026-09-19 |
| U13.4 | базовая сборка | цена верхней границы — цена **подтверждённого** уровня-потолка, то есть кластера пивотов с числом касаний не меньше требуемого; нижней — подтверждённого уровня-пола. Крайний пивот окна ценой границы сам по себе не становится | там же, пункт о кластеризации; находка M-3 | красно 2026-09-19 — ожидание из дома, метка `debt` (M-3) |
| U13.5 | базовая сборка | у граничного уровня момент обнаружения — бар пивота, образовавшего уровень; момент подтверждения — бар, на котором достигнуто требуемое число касаний. Концу окна тождественно не равен ни один из двух | `docs/models/domain/other/MarketStructure.md` §«MarketPriceLevel (раздел)» (найден / подтверждён) и §«Семантика классификации (как считается)», пункт о моменте подтверждения; находка M-4 | красно 2026-09-19 — ожидание из дома, метка `debt` (M-4) |
| U13.6 | тренд, у чьей границы касаний **меньше** требуемого | границы среди уровней нет: дом объявляет поддержку и сопротивление **подтверждёнными** уровнями | там же, пункт о кластеризации («уровень с касаниями не меньше требуемого — подтверждён»); находка M-3 | красно 2026-09-19 — ожидание из дома, метка `debt` (M-3) |
| U13.7 | несколько пивотов в пределах толеранса друг друга | они собраны в **один** уровень: дом объявляет группировку пивотов в ценовые уровни | там же; находка M-3 | красно 2026-09-19 — ожидание из дома, метка `debt` (M-3) |
| U13.8 | базовая сборка | ни на одном уровне не проставлены идентификатор, инструмент и идентичность вычисления — их ставит джоба | `docs/components/MarketStructureResolver.md` §«Что делает» (сам не персистит) | зелено 2026-09-19 |

## U14 — Структура: событие подтверждённого пробоя

**Базовая сборка.** Параметры со всеми числами, включая буфер и число
баров удержания; ряд, дающий подтверждённые границы.

**Дом группы** — `docs/models/domain/other/MarketStructure.md`
§«MarketBreakoutEvent (раздел)» и пункт о пробое в §«Семантика
классификации (как считается)».

| Метка | Вход | Ожидаемый выход — весь | Чем подтверждается | Факт |
|---|---|---|---|---|
| U14.1 | хвост окна длиной в число баров удержания, каждый бар закрыт выше сопротивления с запасом буфера | событие есть: сломанный уровень — сопротивление, направление вверх, цена уровня равна цене сопротивления, момент подтверждения проставлен | `docs/models/domain/other/MarketStructure.md` §«MarketBreakoutEvent (раздел)» | зелено 2026-09-19 |
| U14.2 | тот же хвост ниже поддержки | событие есть: сломанный уровень — поддержка, направление вниз | там же | зелено 2026-09-19 |
| U14.3 | один из баров хвоста за уровнем не удержался | события нет; прочие поля структуры не меняются | там же (удержание требуется на **каждом** баре) | зелено 2026-09-19 |
| U14.4 | хвост закрыт за уровнем, но запаса буфера не хватает | события нет | там же (запас обязателен наравне с удержанием) | зелено 2026-09-19 |
| U14.5 | окно короче числа баров удержания | события нет; отказа нет | там же | зелено 2026-09-19 |
| U14.6 | число баров удержания меньше единицы | события нет; отказа нет | там же (удержание требует хотя бы одного бара) | зелено 2026-09-19 |
| U14.7 | подтверждённый пробой на структуре, классифицированной диапазоном | тип структуры переклассифицирован: подтверждённый пробой уводит из диапазона | там же, пункт о пробое («подтверждённый пробой переклассифицирует структуру»); находка M-2 | красно 2026-09-19 — ожидание из дома, метка `debt` (M-2) |
| U14.8 | подтверждённый пробой при типе «неизвестно» | события нет: подтверждённых уровней у консервативного исхода не объявлено, ломать нечего | там же (пробит бывает **уровень**, а уровень — подтверждённый); находка M-2 | красно 2026-09-19 — ожидание из дома, метка `debt` (M-2) |
| U14.9 | удержание за уровнем завершилось **раньше** конца окна, и дальше цена вернулась внутрь | событие есть, его момент подтверждения равен моменту последнего бара удержания, а не концу окна, и сломанный уровень перестал быть живым | там же (момент подтверждения — бар, на котором удержание завершилось; уровень жив, пока его не сломал подтверждённый пробой); находки M-4 и M-9 — детекция читает только хвост окна, поэтому сегодня события не будет вовсе | красно 2026-09-19 — ожидание из дома, метка `debt` (M-4 и M-9) |
| U14.10 | буфер задан, ширина полосы и цена уровня различаются в разы | ожидания нет: от чего берётся процент — от ширины полосы или от цены уровня — два носителя называют по-разному | находка M-5 | не прогоняется — §«Кейсы, не прогоняемые сегодня» |
| U14.11 | пивотов нет вовсе, а окно и параметры в порядке (строго растущий ряд) | границ нет, события нет, тип «неизвестно», границы окна проставлены, отказа нет | `docs/models/domain/other/MarketStructure.md` §«MarketBreakoutEvent (раздел)» (пробит бывает уровень); пробел `G4`, добран под-шагом 3 | зелено 2026-09-19 |

## U15 — Структура: вырожденные входы и консервативный исход

**Базовая сборка.** Отклонения подаются от сборки U12; наблюдается весь
результат целиком, включая то, что осталось пустым.

**Дом группы** — `docs/models/domain/other/MarketStructure.md`
§«Семантика классификации (как считается)» (консервативный дефолт) и
`docs/components/MarketStructureResolver.md` §Границы.

| Метка | Вход | Ожидаемый выход — весь | Чем подтверждается | Факт |
|---|---|---|---|---|
| U15.1 | окно свечей пусто | тип — «неизвестно»; уровней нет; границы окна и момент подтверждения не проставлены; события нет; отказа нет | `docs/models/domain/other/MarketStructure.md` §«Семантика классификации (как считается)» | зелено 2026-09-19 |
| U15.2 | параметры не поданы вовсе | тип — «неизвестно»; границы окна **проставлены** (они выводятся из свечей, а не из параметров); уровней нет; события нет | там же | зелено 2026-09-19 |
| U15.3 | одно из обязательных чисел параметров пусто | тот же исход, что у U15.2: расчёт не определён — значит «неизвестно», а не частичный результат | там же | зелено 2026-09-19 |
| U15.4 | скаляр волатильности не подан | толеранс кластеризации берётся долей цены; расчёт идёт, отказа нет | `docs/models/domain/other/MarketStructure.md` §«Семантика классификации (как считается)» (вход не объявлен — откат на долю цены) | зелено 2026-09-19 |
| U15.5 | множитель толеранса параметрами не задан, скаляр волатильности подан | применяется провизорный дефолт множителя; расчёт идёт | там же (значения порогов провизорны) | зелено 2026-09-19 |
| U15.6 | середина полосы равна нулю | диапазон не признаётся; деления на ноль не происходит | там же (консервативный дефолт) | зелено 2026-09-19 |
| U15.7 | базовая сборка | на каркасе не проставлены ни идентификатор, ни инструмент, ни идентичность вычисления | `docs/components/MarketStructureResolver.md` §Границы (результат не сохраняет) | зелено 2026-09-19 |
| U15.8 | объявленный вход подан пустым, потому что устарел | резолвер идёт той же веткой, что при необъявленном входе: различие «объявлен, но не готов» держит джоба, а не он | `docs/components/MarketStructureJob.md` §«Объявленный вход не готов — консервативный исход» | зелено 2026-09-19 |
| U15.9 | базовая сборка | резолвер свежести не проверяет и своих часов не читает: момент подтверждения выводится из бара | `docs/components/MarketStructureResolver.md` §Границы | зелено 2026-09-19 |
| U15.10 | кластер пивотов разнесён: потолок на 110 и 109; одна и та же сборка прогоняется по двум ветвям толеранса | ветви дают **разный счёт касаний и разный исход**: толеранс долей цены (0.55) второго пивота не захватывает — тип «неизвестно»; толеранс от скаляра волатильности 4 при дефолтном множителе (2) захватывает оба — тип «диапазон» | `docs/components/MarketStructureResolver.md` §«Потребляет готовые скаляры, не пересчитывает»; пробел `G5`, добран под-шагом 3 | зелено 2026-09-19 |

## U16 — Фаза: порядок клауз и консервативный дефолт

**Базовая сборка.** Упорядоченный список клауз «условие → фаза»; контекст
оценки с готовыми значениями индикаторов и структуры; вычислитель условий
— **настоящий**, и истинность клаузы обеспечивается простейшим сравнением,
чья грамматика мерится соседним предметом.

**Дом группы** — `docs/components/MarketPhaseResolver.md` §«First-match по
позиции» и §Stateless; консервативный дефолт —
`docs/models/domain/other/MarketPhase.md` §«Енум `Type`».

| Метка | Вход | Ожидаемый выход — весь | Чем подтверждается | Факт |
|---|---|---|---|---|
| U16.1 | единственная клауза, её условие истинно | тип — тип этой клаузы | `docs/components/MarketPhaseResolver.md` §«First-match по позиции» | зелено 2026-09-19 |
| U16.2 | истинны и вторая, и четвёртая клаузы | тип — тип **второй**: выбирает позиция в списке, а не порядок проверки условий и не «сила» клаузы | там же | зелено 2026-09-19 |
| U16.3 | истинна только последняя клауза | тип — её тип: первая истинная, а не первая в списке | там же | зелено 2026-09-19 |
| U16.4 | ни одна клауза не истинна | тип — «неизвестно» | `docs/models/domain/other/MarketPhase.md` §«Енум `Type`» | зелено 2026-09-19 |
| U16.5 | список клауз пуст | тип — «неизвестно»; отказа нет | `docs/components/MarketPhaseResolver.md` §«First-match по позиции» | зелено 2026-09-19 |
| U16.6 | список клауз не подан вовсе | тип — «неизвестно»; отказа нет | там же (консервативный исход) | зелено 2026-09-19 |
| U16.7 | у первой клаузы условие пусто | она истинна безусловно и задаёт тип, а последующие недостижимы — названное следствие, а не свойство резолвера | `.claude/work/backlog.md` §«Пустое условие истинно, и у клаузы фазы это даёт безусловную фазу» | зелено 2026-09-19 |
| U16.8 | у истинной клаузы тип фазы не проставлен | ожидания нет: дом клаузы без типа не называет | дом молчит | не прогоняется — §«Кейсы, не прогоняемые сегодня» |
| U16.9 | нужный клаузе вход в контексте отсутствует | клауза ложна, и если прочие тоже ложны — тип «неизвестно»; отказа нет | `docs/models/domain/other/MarketPhase.md` §«Вычисление и свежесть (на лету)» | зелено 2026-09-19 |
| U16.10 | два вызова подряд с одними и теми же клаузами и контекстом | тип одинаков; результат второго вызова не зависит от первого | `docs/components/MarketPhaseResolver.md` §Stateless | зелено 2026-09-19 |
| U16.11 | базовая сборка | резолвер ничего не сохраняет и момента подтверждения не выставляет: у фазы его нет вовсе | `docs/models/domain/other/MarketPhase.md` §Структура | зелено 2026-09-19 |

## U17 — Отсутствие выходов: чего слой не делает

**Базовая сборка.** Любая из групп выше; наблюдается **отсутствие**
эффектов за пределами возвращённого значения.

**Дом группы** — `docs/components/IndicatorJob.md` §Границы,
`docs/components/MarketStructureResolver.md` §Границы,
`docs/components/MarketPhaseResolver.md` §Границы.

| Метка | Вход | Ожидаемый выход — весь | Чем подтверждается | Факт |
|---|---|---|---|---|
| U17.1 | любой вход любого вычислителя | в базу не пишется ничего: результат только возвращается | `docs/components/MarketStructureResolver.md` §Границы; `docs/components/IndicatorJob.md` §«Что делает» (сохраняет джоба) | зелено 2026-09-19 |
| U17.2 | любой вход | соседу не звонят, в брокер не публикуют, событий не производят | `docs/architecture/data-ownership.md` (публикация — дело реле владельца) | зелено 2026-09-19 |
| U17.3 | любой вход вычислителя либо резолвера структуры; у резолвера фазы — клаузы, все правила которых в этом контексте оцениваемы | в лог не пишется ничего: ни один вычислитель и ни один резолвер логгера не держит. Оговорка названа: настоящий коллаборатор резолвера фазы — вычислитель условий — пишет предупреждение на неоцениваемом типе правила, и такого входа кейс не подаёт (грамматику мерит `.claude/tests/cases/strategy-engine-condition.md`) | `docs/components/MarketStructureResolver.md` §Границы; звено `Z2` | зелено 2026-09-19 |
| U17.4 | ряд любой длины | ожидания нет, и причина не в молчании дома: **такого входа не существует** — доменная свеча признака закрытости не несёт вовсе, поэтому «закрытости не проверяет» наблюдать нечем. Инвариант обеспечен загрузкой, а не расчётом | `docs/processes/market-data-calculation.md` §«Свечи как вход»; форма свечи — звено `Z1` | не прогоняется — §«Кейсы, не прогоняемые сегодня» |
| U17.5 | значение, чей бар старше любого мыслимого срока | свежесть не проверяется и не помечается: её мерит потребитель своим сроком | `docs/rules/market-data-freshness.md` | зелено 2026-09-19 |
| U17.6 | любой вход | идентичностей вычисления не заводится и решения «что считать» не принимается | `docs/components/IndicatorJob.md` §Границы | зелено 2026-09-19 |
| U17.7 | структура любого типа | торгового решения не принимается и сигнала не производится: тип есть факт | `docs/components/MarketStructureResolver.md` §Границы | зелено 2026-09-19 |
| U17.8 | фаза любого типа | значений индикаторов и структуры резолвер фазы не считает | `docs/components/MarketPhaseResolver.md` §Границы | зелено 2026-09-19 |
| U17.9 | любой вход | фаза не персистится и истории не накапливает | `docs/models/domain/other/MarketPhase.md` §«Вычисление и свежесть (на лету)» | зелено 2026-09-19 |

## Кейсы, не прогоняемые сегодня

Недостижимых **входов** у уровня 2 нет: состояние собирается прямо. Один
кейс, однако, стои́т здесь не по молчанию дома, а потому что его входа **не
существует в форме модели** — он назван в таблице отдельной строкой. Прочие —
кейсы, у которых нет **ожидания**.

| Кейс | Почему не прогоняется | Что сделает прогоняемым |
|---|---|---|
| U1.12, U6.7, U6.8, U11.8, U2.9 | параметр доезжает до вычислителя непроверенным ни одним носителем, и дом поведения на нём не называет: исход — отказ, который джоба гасит в лог | владелец называет дом: либо проверка требования на входе сервиса, либо объявленное поведение вычислителя на негодном параметре (M-7) |
| U2.5, U2.6, U2.7, U3.3, U3.6, U4.5, U4.6, U7.9 | формула, вид сглаживания и вид отклонения живут только в javadoc реализации; ожидание, списанное с неё, зелено по построению | спека величины в `docs/spec/` с примерами, и ссылка на неё из доменного дока (M-1) |
| U3.5, U4.7, U5.7, U6.6, U8.8 | дом задаёт выведение прогрева **классом** («рекурсивные — кратно периоду», «составные — от старшего периода»), а множитель и слагаемые живут в реализации: проверяемо «кратно», не само число | та же спека (M-1) — либо дом называет множитель класса поимённо |
| U9.9 | у кумулятивного типа класса выведения нет ни в одном носителе: ни оконный, ни рекурсивный, ни составной | владелец вносит класс кумулятивных в `docs/rules/strategy-condition-contract.md` §«Прогрев выводится, override возможен» (M-1) |
| U5.6, U8.7, U10.7 целиком; U7.6 и U7.7 — только клеткой положения цены (по четырём остальным полям оба прогоняемы) | константа вырожденного окна у каждого вычислителя своя, и дома у неё нет: это торговое решение, а не деталь формулы | тот же дом, что у M-1, с явной нотой о вырожденном окне |
| U11.7 | дом говорит «локальный максимум» и о равных соседях не высказывается; строгость сравнения меняет число пивотов на плато | владелец называет строгость сравнения в доме семантики |
| U12.12 | дом перечисляет исход диапазона и исход тренда и старшинства между ними не называет; вход, удовлетворяющий обоим, законен | владелец называет порядок проверки в доме семантики |
| U14.10 | от чего берётся процент буфера, два носителя называют по-разному | владелец сводит два носителя к одному (M-5) |
| U14.11 | пивотов нет вовсе, а окно и параметры в порядке (строго растущий ряд) | границ нет, события нет, тип «неизвестно», границы окна проставлены, отказа нет | `docs/models/domain/other/MarketStructure.md` §«MarketBreakoutEvent (раздел)» (пробит бывает уровень); пробел `G4`, добран под-шагом 3 | зелено 2026-09-19 |
| U16.8 | дом клаузы без объявленного типа фазы не называет | владелец называет исход в доме клаузы либо запрещает её созданием |
| U9.6 | вычислитель читает пустой объём нулём; дом благоприятное умолчание на недобытом операнде запрещает, а своего перечня исключений этой арифметикой не пополнял — два носителя говорят разное | владелец выбирает сторону: либо запись исключения в `docs/rules/absent-value-semantics.md` §«Названные исключения», либо явное «не определено» плюс сигнал у вычислителя (M-8) |
| U17.4 | **входа не существует**: доменная свеча признака закрытости не несёт вовсе (звено `Z1`), и «закрытости не проверяет» наблюдать нечем — это единственная строка таблицы не про молчание дома | появление признака закрытости у формы свечи; сегодня инвариант держит загрузка, и предъявлять его этому предмету нечем |

**Кейсы, чьё ожидание взято из дома, а дерево кода несёт иначе,
прогоняемы, и их красный прогон и есть предъявление находки.** Это U13.4,
U13.6 и U13.7 (находка M-3), U14.7 и U14.8 (M-2), U13.5 и U14.9 (M-4;
U14.9 предъявляет сверх того M-9).

**Охран второго рубежа у предмета нет ни одной, и это следствие
раскладки.** Соседний сервис проверяет параметры определения при его
создании, но вычислитель получает их **не оттуда**: требование приезжает
на поверхность рыночных данных свободной картой, и между картой и
вычислителем проверки не стои́т ни одной (M-7). Поэтому кейсы негодных
параметров здесь — не охраны, а достижимые в проде состояния без
объявленного исхода.

## Существующий набор предмета

**Тестов у предмета нет ни одного.** Ни один вычислитель, ни числовые
помощники, ни резолвер структуры не упоминаются ни в одном тестовом классе
дерева; резолвер фазы упоминается в единственном тестовом классе — как **настоящий**
коллаборатор в тесте раздающего сервиса, где предмет проверки другой (команда
ниже печатает четыре файла: три попадания в дереве движка дают его
собственные вычислители цены и размера, к этому предмету не относящиеся).
Команда воспроизводима:

```bash
grep -rln 'Calculator\|MarketStructureResolver\|MarketPhaseResolver\|IndicatorMath' \
  services/market-data/src/test services/common/strategy-engine/src/test
```

**Под-шаг 3 это подтвердил и снимать ничего не стал** (2026-09-19): прежних
проб у предмета не было ни одной, поэтому вопрос второго носителя одного
ожидания (`.claude/rules/carrier-levels.md`) здесь не возникает вовсе.
Единственная точка соприкосновения — `MarketFeatureReadTest` раздающего
сервиса — осталась на месте: её предмет другой, и резолвер фазы она берёт
настоящим.

**Ревизия существующего набора — отдельный предмет** перечня под-шага 1
(`.claude/decisions/test-contour-design-pass.md` §«Перечень предметов и
порядок работы»), и здесь она не делается. Что у неё будет на входе от
этого документа, названо двумя фразами. Первое: у расчётного слоя рыночных
данных набора нет, поэтому «тест закрепляет структуру» здесь неприменимо —
закреплять нечем, и ревизии останется только принять перечень выше как
план. Второе: единственная точка соприкосновения — тест раздающего
сервиса, и он **не** подменяет резолвер фазы, то есть под критерий чистой
логики попадает уже сейчас; снимать его нечего.

## Где живёт код кейсов

**Под-шаг 3 исполнен 2026-09-19**; колонка «Факт» каждой клетки заполнена
исходом того прогона. Класс на группу, метод на кейс, метка кейса — в имени
метода и в `@DisplayName`; пакет уровня — `unit` дерева своего сервиса
(`.claude/skills/test-code.md`).

**Пакет один на весь слой, и это решение, а не умолчание.** Предмет — один
расчётный слой, его классы лежат тремя пакетами дерева кода
(`indicator`, `structure`, `phase`), а группа `U17` утверждает обо **всех**
десяти классах сразу: разложенная по трём пакетам, она не смогла бы взять
общую базовую сборку, не сделав её публичной.

| Группа | Код кейсов (`services/market-data/src/test/java/com/example/marketdata/unit/calculation/`) |
|---|---|
| `U1` | `CalculatorContractTest.java` |
| `U2` | `IndicatorMathSeriesTest.java` |
| `U3` | `EmaSeriesTest.java` |
| `U4` | `AtrSeriesTest.java` |
| `U5` | `RsiRangeTest.java` |
| `U6` | `MacdComponentsTest.java` |
| `U7` | `BollingerBandsTest.java` |
| `U8` | `StochasticLinesTest.java` |
| `U9` | `ObvSignedVolumeTest.java` |
| `U10` | `EfficiencyRatioTest.java` |
| `U11` | `StructureSwingPivotsTest.java` |
| `U12` | `StructureClassificationTest.java` |
| `U13` | `StructureLevelsTest.java` |
| `U14` | `StructureBreakoutTest.java` |
| `U15` | `StructureConservativeOutcomeTest.java` |
| `U16` | `PhaseClauseOrderTest.java` |
| `U17` | `CalculationLayerBoundariesTest.java` |
| — (базовая сборка, общая всем группам) | `CalcFixture.java` |

**Класс на группу, а не на класс предмета** — форма объявлена у соседнего
предмета (`.claude/tests/cases/strategy-engine-calculation.md` §«Где живёт код
кейсов») и здесь не переоткрывается. Здесь она подтверждена вторично:
классов предмета одиннадцать, групп семнадцать, и `MarketStructureResolver`
один несёт пять из них.

### Красное ожидание живёт меткой `debt`, а не ослабленным ассертом

Форма и её довод — дом у соседнего предмета
(`.claude/tests/cases/platform-shared-logic.md` §«Красное ожидание живёт
меткой `debt`, а не ослабленным ассертом»), здесь не пересказывается.
Свойство `test.excluded.groups` у `services/market-data/pom.xml` уже было
заведено прежним заходом — седьмым модулем оно не становится.

```bash
mvn -o -am -pl services/market-data test -Dtest.excluded.groups=   -Dtest='com/example/marketdata/unit/calculation/*Test'
```

### Контрольный прогон на нейтрализованной сборке

Ось взята **узкая и проверяемая**: монотонность цен закрытия у окна
восходящего тренда — то, на чём стои́т ветвь внутреннего прокси. Предсказание
названо **до** прогона поимённо: падают ровно `U12.10` и `U15.8` — две
клетки, чьё ожидание опирается на прокси, — а прочие остаются зелёными.
Замер сошёлся: из 129 прогоняемых клеток красными стали 9 = 7 помеченных
`debt` плюс ровно эти две.

```bash
# в CalcFixture#monotoneUptrendWindow закрытия 90,96,97,98,99,100,101,102,103
# заменены зигзагом 90,96,90,100,90,100,90,105,96 (прокси падает с 1 до 0.079)
mvn -o -am -pl services/market-data test -Dtest.excluded.groups=   -Dtest='com/example/marketdata/unit/calculation/*Test'
```

## Звенья кода, названные один раз

Ожидание, у которого дом молчит, а факт даёт дерево кода, опирается на
названное звено; звено называется здесь один раз и в клетках не
переписывается.

| Метка | Звено | Что из него берётся |
|---|---|---|
| `Z1` | `services/common/model/domain/src/main/java/com/example/tradingbot/domain/model/trade/candle/Candle.java`, javadoc класса: «Признак закрытия (confirm) не хранится» | у доменной свечи нет поля закрытости, поэтому вход «бар не закрыт» не строится ничем (U17.4) |
| `Z2` | `services/common/strategy-engine/src/main/java/com/example/strategy/engine/condition/StrategyConditionEvaluator.java`, ветвь `default` разбора типа правила | единственный писатель в лог на всей тропе резолвера фазы — коллаборатор, а не сам резолвер (U17.3) |

## Пробелы покрытия

Перечень единиц §Предмета перебран поимённо: у восьми вычислителей, трёх
числовых помощников, резолвера структуры и резолвера фазы прямые кейсы есть.
Непокрытыми остались пять ветвей, у всех исход — **под-шаг 3** (добираются по
ходу кода тестов), секции бэклога не требует ни одна.

**Все пять добраны под-шагом 3 (2026-09-19), и каждая стала строкой своей
группы:** `G1` — `U7.10`, `G2` — `U2.9`, `G3` — `U8.9`, `G4` — `U14.11`,
`G5` — `U15.10`. Четыре из пяти прогоняемы и зелены; `U2.9` ожидания не
имеет по той же причине, что `U1.12`, — дом ветви отказа помощников не
называет.

- **`G1`** — ширина полос при **нулевой середине** (весь ряд закрытий
  нулевой): ветвь отдаёт ноль, не деля. U7.6 берёт равные **ненулевые**
  закрытия, а U15.6 мерит нулевую середину у структуры, не у полос.
- **`G2`** — числовые помощники на негодном окне (ноль либо отрицательное):
  делят на длину окна и разгон, и ветвь отказа у них своя. У вычислителей она
  названа (U1.12, U6.7, U6.8), у самих помощников прямого кейса нет.
- **`G3`** — сглаживание стохастика поверх ряда с **пустотой внутри окна**:
  член результата остаётся пустым, пока окно неполно. Сегодня это наблюдается
  только косвенно, итоговым рядом (U8.6).
- **`G4`** — событие пробоя при полном отсутствии пивотов: границы пусты,
  события нет. U11.6 наблюдает тип, U15.1 и U15.2 — пустое окно и пустые
  параметры; ветвь «границ нет, а окно и параметры в порядке» не взята.
- **`G5`** — влияние толеранса кластеризации на **число касаний**: U15.4 и
  U15.5 проверяют, что расчёт идёт по каждой из двух ветвей толеранса, но не
  то, что ветви дают разный толеранс и разный счёт касаний — то есть разный
  исход классификации.

**Два кандидата проверены и сняты, и это названо, потому что снятие здесь
дороже заведения.** Первый — «у семи вычислителей из восьми нет своего кейса
override прогрева»: override исполняет умолчание интерфейса, одно на всех, и
его мерит группа U1. Второй — «не всякое значение перечня типов фазы получает
кейс»: резолвер типом не ветвится, он переносит тип сработавшей клаузы, а
ветвление по типу фазы — предмет грамматики условий
(`.claude/tests/cases/strategy-engine-condition.md`).

## Находки владельцам

Каждая получила секцию бэклога тем же заходом
(`.claude/rules/parking-address.md`); здесь — только указатель.

| Находка | Предмет | Где припаркована |
|---|---|---|
| M-1 | формулы шести вычислителей дома в корпусе не имеют: доменный док называет поля наследников и область значений, а формула, вид сглаживания, вид отклонения, выведение прогрева и константы вырожденного окна живут только в javadoc реализации. Дом формулы — спецификация, а не проза; у соседних расчётных слоёв — сайзинга, дистанции стопа, риска — спеки заведены, у рыночных данных нет ни одной. Следствие названо: ожидание, списанное с реализации, зелено по построению, и кейсы арифметики стоя́т непрогоняемыми | `.claude/work/backlog.md` §«Формулы и вырожденные дефолты индикаторов дома в корпусе не имеют» |
| M-2 | подтверждённый пробой структуру **не переклассифицирует**, хотя дом объявляет переклассификацию прямо; сверх того событие считается **после** классификации и на любом её исходе, включая «неизвестно», где подтверждённых уровней не объявлено вовсе. Потребитель читает событие готовым, и признак «диапазон сломан» у него расходится с типом структуры того же расчёта | `.claude/work/backlog.md` §«Подтверждённый пробой структуру не переклассифицирует и считается на любом типе» |
| M-3 | подтверждение уровня касаниями к **выдаваемым** уровням не применяется: наружу идут сырые свинг-пивоты плюс экстремумы окна, а число касаний читает только предикат диапазона. Отсюда поддержка и сопротивление тренда объявлены домом подтверждёнными уровнями и подтверждения не имеют, а кластеризация пивотов в уровень не производится вовсе — хотя толеранс для неё посчитан | `.claude/work/backlog.md` §«Подтверждение уровня касаниями к выдаваемым уровням структуры не применяется» |
| M-4 | момент подтверждения структуры равен концу окна **всегда** — и у каркаса, и у события пробоя. Дом разводит эти два момента прямо: конец окна есть точка отсчёта свежести, момент подтверждения — гейт использования без заглядывания вперёд. Пока они равны тождественно, гейт не мерит ничего, а различие, на котором стои́т правило свежести, не наблюдаемо | `.claude/work/backlog.md` §«Момент подтверждения структуры равен концу окна всегда» |
| M-5 | от чего берётся процент буфера пробоя, два носителя называют по-разному: объявление параметра говорит «от ширины диапазона», расчёт берёт процент от цены уровня, а дом семантики не уточняет вовсе. На инструменте, чья полоса узка относительно цены, эти две величины расходятся на порядок — то есть расходится сам порог, отсекающий ложный пробой | `.claude/work/backlog.md` §«От чего процент у буфера пробоя, два носителя называют по-разному» |
| M-6 | кумулятивная сумма объёма считается от начала **окна прохода**, а не истории: джоба подаёт вычислителю последние бары, а запись уже существующих строк не переписывает. Ряд в таблице поэтому склеен из отрезков с разной базой, и на стыке отрезков стои́т скачок, которого в объёме не было. Дом ограничил операнд относительными формами именно из-за нестабильности уровня — но скачок базы эти формы читают как движение | `.claude/work/backlog.md` §«Кумулятивный OBV считается от начала окна прохода, а не истории» |
| M-7 | параметры требования вычисления доезжают до вычислителя непроверенными: поверхность рыночных данных принимает их свободной картой, и между картой и вычислителем не стои́т ни одной проверки. Пустой период даёт отказ разыменования, нулевой — отказ деления, быстрый период больше медленного — отказ разыменования; все три джоба гасит в лог, и заказанная идентичность молча не считается никогда. Проверка соседа на создании определения сюда не дотягивается: у требования свой вход | `.claude/work/backlog.md` §«Параметры требования вычисления доезжают до вычислителя непроверенными» |
| M-8 | непроставленный объём свечи вычислитель кумулятивной суммы читает **нулём**, то есть подставляет благоприятное умолчание на недобытом операнде. Дом такую форму запрещает прямо, а два его названных исключения ограничены одним полем в одном выражении и прецедентом объявлены не служащими; этой арифметики в перечне нет. Цена не «неточность»: бар без объёма уходит в ряд как состоявшийся бар без сделок, и относительные формы операнда, которыми дом OBV и ограничил, читают его наравне с измеренными | `.claude/work/backlog.md` §«Непроставленный объём свечи читается нулём, и исключения этой арифметике дом не заводил» |
| M-9 | подтверждённый пробой ищется **только в хвосте окна**: проверяются последние `breakoutConfirmationBars` баров, и удержание, завершившееся раньше последнего бара, событием не становится вовсе. Дом объявляет уровень сломанным, как только цена закрылась за ним с запасом и продержалась требуемое число баров, и живым — пока его не сломал подтверждённый пробой. Отсюда пробой с возвратом внутрь пропускается целиком, а не приезжает с неверным моментом: потребитель читает «уровень жив» там, где дом называет его сломанным | `.claude/work/backlog.md` §«Пробой обнаруживается только хвостом окна» |

## Связи

- Форма кейса и три уровня — `.claude/skills/test-design.md`; роль —
  `.claude/agents/tester.md`.
- Контур и его развилки — `.claude/decisions/test-contour-design-pass.md`.
- Предмет — `docs/models/domain/other/IndicatorValue.md`,
  `docs/models/domain/other/MarketStructure.md`,
  `docs/models/domain/other/MarketPhase.md`,
  `docs/components/IndicatorJob.md`,
  `docs/components/MarketStructureResolver.md`,
  `docs/components/MarketStructureJob.md`,
  `docs/components/MarketPhaseResolver.md`,
  `docs/processes/market-data-calculation.md`,
  `docs/rules/strategy-condition-contract.md`,
  `docs/rules/market-data-freshness.md`,
  `docs/rules/decimal-arithmetic.md`.
- Соседи по перечню, чьи кейсы написаны прежде —
  `.claude/tests/cases/strategy-engine-calculation.md`,
  `.claude/tests/cases/strategy-engine-condition.md`,
  `.claude/tests/cases/trading-core-fsm.md`,
  `.claude/tests/cases/trading-core-risk.md`,
  `.claude/tests/cases/trading-core-safety.md`,
  `.claude/tests/cases/okx-mapping.md`.
- Чёрный ящик того же сервиса — `.claude/tests/cases/market-data.md`.
