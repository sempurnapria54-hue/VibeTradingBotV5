# Шаблон: тест-план интеграции

## На какой вопрос отвечает этот файл

Каков формат докового файла тест-плана контура тестов API источника.

## Назначение

Универсальный шаблон тест-плана и кейсов контура тестов API
источника, **один на все источники**. Вход для `tester` на этапе
DESIGN (`.claude/skills/test-design.md`). Готовый план кладётся в
`.claude/tests/{testType}/{source}/`.

**Стаб.** Известен скелет (форма плана + форма кейса). Детальные
поля **не фиксируются** — форма снимается с первого пилотного плана
(`.claude/tests/source-api/okx/`). Спекулятивные поля не вносим.

## Форма плана (скелет)

- **На какой вопрос отвечает этот файл** — «Как проверяем интеграцию
  с источником `{name}` (такой-то скоуп)?».
- **Скоуп** — что покрывает план (эндпоинты / цепочки), триггер
  (новая биржа / новый эндпоинт / изменение контракта / разовый).
- **Среда** — demo (test-профиль, demo-креды) для write; prod
  read-only — для чтений; явно, что без write-эффектов на prod.
- **Негативный слой (без состояния)** — дешёвые кейсы, гоняются
  первыми.
- **Цепочки (по графу предусловий)** — связные последовательности;
  выход кейса = предусловие следующего.
- **Teardown** — как прогон возвращает биржу в чистое состояние (нет
  висящих ордеров / позиций).

## Форма кейса (скелет)

- **Объект** — эндпоинт или цепочка.
- **Предусловие-состояние** — что должно быть на бирже до кейса
  (исполненный ордер / живой неисполненный / отсутствие ордера / —).
- **Шаги** — что вызываем (операция клиентского слоя + параметры).
- **Ожидаемый результат** — из контракта источника (ACK-семантика,
  код ошибки, факт состояния). Незадокументированное не
  выдумывается — помечается пробелом.
- **Среда** — demo / prod read-only.
- **Фактический результат + наблюдения** — заполняется на RUN
  (`.claude/skills/test-run.md`); в плане до прогона пусто.

## Связи

- Скилл построения — `.claude/skills/test-design.md`.
- Скилл прогона — `.claude/skills/test-run.md`.
- Роль-автор — `.claude/agents/tester.md`.
- Процесс контура — `.claude/processes/source-api-testing.md`.
- Тип знания (где живут планы) —
  `.claude/decisions/test-knowledge-type.md`.
