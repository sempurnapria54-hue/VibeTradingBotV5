---
name: knowledge-placement
description: Use when a new piece of knowledge (decision, definition, rule, fact, observation) appears in chat or work and needs to be persisted in a file, but the right file is not obvious. Classifies the fragment by dimension (PIPELINE / PRODUCT) and process layer or tracker, proposes a concrete storage location (existing file with section, or a new file with proposed path), surfaces connected updates across related artifacts (including conflicts with existing ADRs), and asks the user clarifying questions only for unresolved forks. Not for routine edits inside an already-known file, and not for pure plan items (those go to T-BL / T-MIG directly).
---

# Скилл: размещение нового знания по карте артефактов

Принимает фрагмент знания и определяет, где он должен быть зафиксирован. Скилл — процедура, не справочник карты. Сама карта живёт в `CLAUDE.md` (разделы Process layers + Trackers) и `.claude/working-with-claude.md`.

Скилл работает в два измерения и две оси:

- **Измерения:** PIPELINE (методология работы над проектом, артефакты в `.claude/`) и PRODUCT (содержание самой системы, артефакты в `docs/`).
- **Оси:** пять процессных слоёв (L-SPEC, L-ADR, L-CONV, L-SKILL, L-AGENT) и пять трекеров (T-SNAP, T-Q, T-BL, T-MIG, T-EVO).

Матрица — 5×2 + 5×2 = 20 потенциальных ячеек. Часть ячеек легитимно пустые. Текущее наполнение — см. `.claude/notes/artifact-question-mapping-2026-05-18.md` (референс, не источник истины: фактическое состояние репозитория важнее).

## Когда активируется

- В работе появилось новое знание (решение, определение, правило, факт, наблюдение, договорённость), которое требует фиксации в файле.
- Неочевидно, в какой именно файл и раздел его записать.
- Фрагмент потенциально затрагивает несколько мест и нужно убедиться, что связные обновления не упущены.

Скилл вызывается **явно** при фиксации нового знания. Это не автоматический фильтр на весь поток сообщений.

`knowledge-curator` может вызывать этот скилл в своих сценариях проверки в конце сессии.

## Когда НЕ активируется

- Правка опечатки или формулировки в уже определённом файле.
- Обновление статуса entry (Q-N, backlog-item, ADR) в уже известном артефакте.
- Добавление строки в существующую таблицу или список, чьё место известно.
- Фиксация информации, для которой уже есть устоявшийся скилл с собственным правилом размещения (`open-questions-workflow`, `spec-document-workflow`, `spec-models-registry`, `spec-document-migration`). В таких случаях вызывается **тот** скилл, а не этот.
- Фрагмент описывает только план действий («создать в таком порядке», «выполнить на этапе X», «следующий шаг — сделать Y») без обоснования и без альтернатив. Такие фрагменты — пункты `T-BL` или `T-MIG`, идут туда напрямую без процедуры классификации. Если у плана есть обоснование с альтернативами — это уже не чистый план (см. тест «знание vs план» в Шаге 1).

## Процедура

Четыре последовательных шага. На каждом шаге может возникнуть развилка — она переносится в Шаг 4 (разрешение неоднозначностей).

### Шаг 1. Классификация: измерение и ось

Цель шага — определить ячейку матрицы (измерение × слой/трекер).

**(а) Измерение.** Ответить на вопрос: фрагмент про продукт (торговый бот: модели, процессы, риски, интеграции, операционка инфраструктуры) или про пайплайн (методология работы, артефакты процесса, инструменты Claude, эволюция пайплайна)?

- → **PRODUCT** — артефакт ляжет в `docs/`.
- → **PIPELINE** — артефакт ляжет в `.claude/`.
- → **развилка** — переход к Шагу 4 с конкретным вопросом пользователю.

**(б) Слой или трекер.** Определить, на какой характеристический вопрос отвечает фрагмент:

Процессные слои:
- «что есть в [системе / пайплайне]?» → **L-SPEC**
- «почему выбрано это, а не другое?» → **L-ADR**
- «как принято оформлять / именовать / писать в проекте?» → **L-CONV**
- «как выполняется эта операция?» → **L-SKILL**
- «какая роль смотрит на это?» → **L-AGENT**

Трекеры:
- «есть ли в проекте вопрос без ответа?» → **T-Q**
- «где мы сейчас (срез на дату)?» → **T-SNAP**
- «что в работе?» → **T-BL**
- «где мы в миграции?» → **T-MIG**
- «как менялся пайплайн?» → **T-EVO**

**Критерии границ.** Близкие слои и трекеры различаются по трём тестам:

— **L-ADR vs L-CONV.** Фрагмент содержит обоснование выбора с альтернативами → **L-ADR**. Фрагмент содержит правило применения (что писать, как именовать, какой формат, какая политика) → **L-CONV**. Если и то и другое — фиксируется как **два связных артефакта**: L-ADR (решение) + L-CONV (правило, ссылающееся на ADR). Скилл предлагает оба места.

— **L-ADR vs T-BL / T-MIG.** Есть альтернативы и обоснование → **L-ADR**. Описание состояния работы или порядка действий со сроком жизни до закрытия задачи или завершения миграции → **T-BL** или **T-MIG**. Если присутствуют оба аспекта — два связанных артефакта (решение + пункт исполнения со ссылкой на решение).

— **Знание vs пункт плана.** Утверждение о мире («есть», «является», «должно», «не нужно») → знание, идёт в L-* (или T-Q, если это незакрытый вопрос). Императив с порядком/сроками («сделать», «создать в таком-то порядке», «удалить на этапе X») без обоснования и без альтернатив → план, идёт в **T-BL / T-MIG**. Скилл не натягивает чистый план на L-ADR.

**(в) Один или несколько основных аспектов.** Фрагмент может иметь:

- *Один основной аспект.* Зафиксировать его и при необходимости — сопутствующие (по которым потребуются связные обновления, Шаг 3).
- *Два или более равноправных аспекта одного решения.* Если фрагмент содержит несколько сцепленных утверждений (например, классификация артефакта + правило его применения; решение о структуре + список её элементов) — фиксировать **все** как основные. В выходном формате — поле «Основные категории» (множественное), и для каждой — отдельное предложение места. Это сигнал, что фрагмент порождает несколько связанных артефактов.

Не вынуждать выбор «одного основного», если их объективно два — это искажает фрагмент.

**(г) Неклассифицируемые.** Если фрагмент не ложится чисто ни в один слой и ни в один трекер — фиксировать как «развилка карты» и нести в Шаг 4. Не пытаться силой втиснуть в ближайший слой.

### Шаг 2. Подбор места хранения

Для определённой на Шаге 1 ячейки (измерение + слой/трекер):

**(а) Открыть карту.** Источник правды — текущее состояние репозитория, не note `artifact-question-mapping`. Карту читать по `CLAUDE.md` (разделы Process layers + Trackers) и по фактическому содержимому соответствующих каталогов.

**(б) Подходящий файл существует.** Предложить конкретный файл. Если файл крупный — указать конкретный раздел внутри файла (по заголовкам). Проверить, не дублирует ли фрагмент уже зафиксированное в файле — если дублирует, фиксация не нужна (это не новое знание).

**(в) Подходящего файла нет — один новый файл.** Предложить создание нового, с конкретным именем и путём по конвенциям слоя:

- L-ADR → `.claude/adr/NNNN-<kebab-slug>.md` (нумерация по индексу `adr/README.md`).
- L-SKILL → `.claude/skills/<kebab-name>/SKILL.md`.
- L-AGENT → `.claude/agents/<kebab-name>.md`.
- L-SPEC (PRODUCT) → `docs/spec/<жанровый-каталог>/<Name>.md` по ADR-0002 / ADR-0007. Для новой модели — отдельный документ + запись в `docs/spec/MODELS.md` (см. скилл `spec-models-registry`).
- L-CONV (PRODUCT) → раздел в `docs/conventions/terminology.md` или новый файл в `docs/conventions/` по типу конвенции.
- L-CONV (PIPELINE) → раздел в `CLAUDE.md` (Key project conventions) или в `working-with-claude.md` — по адресату правила.

Для трекеров — добавление entry в существующий файл трекера; новые трекерные файлы — редкое событие, требует ADR.

**(г) Целевой артефакт планируется в будущем пакете.** Если предложенный файл — часть пакета артефактов, который будет создан позже (например, ADR в составе будущего пакета рефакторинга, метамодель в составе ещё не созданного слоя L-SPEC PIPELINE), фиксировать **промежуточное хранение**: текущий strategy-summary, рабочая нота в `.claude/notes/`, или раздел в `.claude/planning/backlog.md` как «знание, ожидающее фиксации в файле X». В отчёте скилла явно отметить:

```
Место хранения:
- Целевой файл: [планируется: <путь>] (в составе <пакета / события>)
- Промежуточное хранение: <текущий путь>
```

При создании целевого пакета фрагмент мигрирует в целевой файл; промежуточная запись помечается ссылкой на целевой артефакт.

**(д) Фрагмент порождает не файл, а каталог или слой.** Если из фрагмента следует создание нового каталога, новой ячейки карты или системы из нескольких связанных артефактов (например, новый слой L-SPEC PIPELINE с несколькими метамоделями; новый каталог `.claude/conventions/`) — это **расширение карты**, требующее отдельного решения о структуре. Скилл не предлагает путь файла, а:

- Фиксирует пункт в «Открытые расширения карты» выходного формата.
- Даёт черновое предложение структуры каталога (имена будущих файлов, разделение по жанрам) как материал для отдельного решения.
- Промежуточное хранение фрагмента — по пункту (г).

**(е) Ячейка пустая и без устоявшихся конвенций.** Фиксировать как развилку «первый файл в ячейке X — какие договорённости об именовании / структуре?» и нести в Шаг 4 или в открытые расширения карты.

**(ж) Конвенция размещения для категории отсутствует.** Если
фрагмент классифицирован чисто (Шаг 1 дал однозначную категорию),
но в (б)–(е) нет правила, куда конкретно класть фрагмент этой
категории в этом случае — фиксировать как **развилку конвенции**
и нести в Шаг 4 (или в «Открытые расширения карты», если
развилка системная, а не разовая).

Типичные случаи (на материале корпус-прогона 2026-05-19):

- Фрагмент — не файл и не каталог, а **семантическая пометка
  существующего артефакта** (разметка раздела внутри файла,
  append-only как policy для существующего журнала, двойная
  семантика существующего трекера). Конвенций такой пометки в
  скилле нет.
- Промежуточное хранение естественно ложится в `.claude/notes/`,
  но статус категории `.claude/notes/` (L-SCRATCH? рабочее место
  без категории?) не зафиксирован.

Скилл **не выбирает место наугад**. Указывает развилку явно:
«категория X классифицирована, конвенция размещения для случая Y
не определена — требуется решение о конвенции до фиксации
фрагмента».

Различие с (е) «пустая ячейка без устоявшихся конвенций»:
- (е) — категория пустая, файлов в ней нет, и непонятно даже,
  как именовать первый файл.
- (ж) — категория не пустая, конвенция для **типового случая**
  есть, но конкретный случай (пометка существующего, промежуточное
  без статуса) выпадает.

### Шаг 3. Связные обновления

Проверить, не затрагивает ли фрагмент несколько мест. Типовые случаи:

**(а) Новое имя бизнес-модели (PRODUCT).** Создание model-документа в `docs/spec/models/...` и обновление реестра `docs/spec/MODELS.md`. Делегировать скиллу `spec-models-registry`.

**(б) Закрытие открытого вопроса.** Проверить `.claude/questions/open-questions.md` (PIPELINE) и при наличии аналогичного журнала для PRODUCT — соответствующий журнал. Если есть Q-N, ссылающийся на эту тему, — предложить перевод в `Resolved` через скилл `open-questions-workflow`.

**(в) Изменение convention.** Проверить, не нарушает ли фрагмент существующие артефакты этой convention. Если нарушает — это сигнал не просто записать новое правило, а пересмотреть применимость старого (потенциально ADR с alternative «оставить как есть»).

**(г) Изменение метамодели артефакта процесса (Q-N, ADR, spec-документ, MIGRATED-маркер, заглушка).** Перечислить места, где её свойства фигурируют: шаблоны в `.claude/templates/documents/`, ссылающиеся скиллы, агенты с инструкциями по проверке. Согласно Принципу 7 working-with-claude — правила применения живут в скиллах/агентах, ADR — обоснование.

**(д) Изменение пайплайна.** Запись в `.claude/pipeline-evolution-log.md` (T-EVO). Если меняется состав Project Knowledge в claude.ai — отметить необходимость ручного обновления PK (см. working-with-claude, раздел про PK).

**(е) Закрытие backlog-item.** Если фиксация знания закрывает пункт из `.claude/planning/backlog.md` — пункт переносится в «Закрытые» со ссылкой на новый артефакт.

**(ж) Конфликт с существующим ADR или convention.** Проверить, не пересматривает ли фрагмент уже принятое решение. Если фрагмент противоречит, дополняет или отменяет существующее ADR или convention — это сигнал, что фиксация знания не сводится к одному месту:

- Нужно либо **новое ADR со статусом Supersedes** старое (по разделу «ADR — архитектурные решения» в working-with-claude);
- Либо явное **примечание к старому ADR** (по типам примечаний из `.claude/adr/README.md`);
- Либо переоценка границ старого правила (если фрагмент не отменяет, а уточняет).

В отчёте скилла — явно перечислить затрагиваемые ADR/convention в разделе «Связные обновления». Конфликт не разрешается внутри скилла; скилл его подсвечивает, решение — отдельным действием.

**(ж') Паттерн-конфликт: правило тиражировано или раздвоено.**
Когда фрагмент классифицирован и место хранения определено —
выполнить позитивный тест на структурный паттерн-конфликт:

**Подвид (β) — тиражирование.** Существует ли это правило (или
его существенная часть) в **≥2 артефактах** — точным дублем или
в переформулировке? Если да — фиксировать как паттерн-конфликт.
Это сигнал, что Принцип 7 working-with-claude нарушен: правило
должно жить в одном источнике, остальные ссылаются.

Признаки подвида (β):
- В нескольких артефактах (агенты, скиллы, ADR, CLAUDE.md,
  working-with-claude, playbook) фигурирует одно и то же
  содержательное правило.
- Формулировки могут быть точными дублями (один в один) или
  переформулировками (разный текст, тот же смысл).
- При изменении правила нужно править все места одновременно.

**Подвид (γ) — раздвоение по адресату.** Существует ли
семантически одно правило в **разных формулировках для разных
адресатов Claude** — Claude Code (CLAUDE.md, working-with-claude,
playbook, скиллы, агенты) и чат (project-instructions)? Если да
— фиксировать как паттерн-конфликт подтипа «раздвоение по
адресату».

Признаки подвида (γ):
- Одна и та же мысль (правило стиля, процедура, граница
  применимости) сформулирована отдельно для двух адресатов.
- Формулировки сознательно разные (CC и чат — разные среды,
  разные действия).
- При изменении смысла правила нужно менять обе формулировки.

**Различение с обычным Шагом 3(ж).** Обычный конфликт — два
правила противоречат друг другу. Паттерн-конфликт — одно
правило сидит в нескольких местах (β) или раздвоено по
адресату (γ), даже если все формулировки согласованы между
собой.

**Позитивный тест «согласованный дубль» vs «refactor-кандидат».**
Подвид (β) делится на две качественно разные ситуации:

- **β-согласованный-дубль** — дублирование сознательное и
  неустранимое, обусловлено различием ролей артефактов.
- **β-refactor-кандидат** — дублирование несфокусированное,
  нарушение Принципа 7 (правило должно жить в одном источнике).

Различение до v5 было субъективным. v5 вводит **позитивный
тест**:

> **«Содержит ли каждое из мест уникальную часть правила
> (обоснование / процедура / применение / навигация / границы
> применимости), не покрытую другими местами?»**

**Если ДА** — это β-согласованный-дубль. Дублирование оправдано
разделением ролей артефактов. Refactor не требуется.

**Если НЕТ** — это β-refactor-кандидат. Одно и то же содержание
сидит в N местах без уникальной части. Нарушение Принципа 7.
Решение — вынести в один источник, остальные сослаться.

**Типичные роли артефактов** (для теста):
- **ADR** — обоснование (почему так решено).
- **skill** — процедура (как делать).
- **agent** — применение (что проверять при ревью).
- **template** — инструкция (что писать в новом артефакте).
- **CLAUDE.md / working-with-claude / playbook** — навигация
  (где что лежит, как пользоваться).
- **model / lifecycle / process spec** — спецификация (что
  есть).

Если правило сформулировано в N артефактах разных ролей и
каждая формулировка отражает свою роль (одна — почему, другая —
как, третья — что проверять) — это **согласованный дубль**.
Если в N артефактах одного типа (например, в 3 агентах) одна и
та же мысль без раздела ролей — это **refactor-кандидат**.

**Пограничные случаи** (фиксировать в поле выходного формата):

- Несколько артефактов **одной роли** содержат **разные части**
  правила (например, два агента, в каждом своя часть процедуры
  ревью). Это **согласованный дубль по части правила**, не по
  ролям — нужно явно перечислить части в выходном формате.
- Артефакт содержит **уникальную часть + дублирующую часть**.
  Это **гибрид**: уникальное — обосновано, дублирующее — кандидат
  на сокращение. В выходном формате — отметить обе части.

**Что фиксирует скилл:** для каждого артефакта в списке —
**уникальная часть** (если есть) и **дублирующая часть** (если
есть). Это превращает поле «Артефакты, где правило фигурирует»
из плоского списка в структурированный.

**Что фиксирует скилл:** список артефактов, где правило
фигурирует; тип повтора (точный дубль / переформулировка);
кандидат на единственный источник по Принципу 7. Решение о
структурной правке — отдельное действие, не часть фиксации
фрагмента.

**Что НЕ покрыто (v5 кандидат).** Подвид (α) «ADR содержит
фрагмент чужого слоя» (фрагмент при размещении выявляет, что
чужой слой уже сидит в ADR). Это обратная диагностика — скилл
работает «фрагмент → проверить чужие слои в существующих ADR»,
а не «фрагмент → место». В v4 не реализуется.

Список не исчерпывающий. Идея шага — не пропустить тривиальные связные обновления, которые иначе оставят рассинхрон.

### Шаг 4. Разрешение неоднозначностей

Если на Шаге 1 или 2 возникла развилка — сформулировать уточняющие вопросы пользователю.

**(а) Один вопрос — одна развилка.** Не объединять несвязанные вопросы.

**(б) Вопрос с конкретными вариантами.** «Это PIPELINE или PRODUCT?» с явными опциями, не «как тебе видится». Если вариантов больше двух — перечислить все.

**(в) Минимум вопросов.** Если на развилку можно ответить чтением репозитория (фактический контент файла, индекс ADR, состав каталога) — отвечать самостоятельно, не выносить пользователю.

**(г) Развилка карты vs развилка фрагмента.** Если неоднозначность — пробел в самой карте (новая ячейка, новый жанр, нет конвенций именования для пустого слоя) — фиксировать как **расширение карты** (см. секцию «Открытые расширения карты» выходного формата), не как вопрос пользователю. Решение о расширении карты — отдельное событие пайплайна, не часть фиксации одного фрагмента.

**(д) Развилка конвенции размещения.** Если Шаг 2 уперся в (ж) —
формулировать вопрос пользователю как «нужна конвенция для X,
варианты: …». Не как «куда положить этот конкретный фрагмент»,
а как **решение об общем правиле**, под которое потом ляжет
фрагмент. Конкретное место хранения определяется как следствие
принятой конвенции, не выбирается ad hoc.

Если развилка конвенции системная (повторится на других
фрагментах того же типа) — нести её в «Открытые расширения
карты», не в вопрос пользователю.

## Выходной формат

После прохождения четырёх шагов скилл выдаёт структурированный ответ:

```
Фрагмент: <цитата фрагмента>

Классификация:
- Измерение: PIPELINE / PRODUCT / [развилка: <вопрос пользователю>]
- Слой/трекер: L-XXX / T-XXX / [развилка: <вопрос>]
- Основные категории (если несколько равноправных): L-XXX + L-YYY / L-XXX + T-YYY
- Основной вопрос: <формулировка характеристического вопроса>
- Сопутствующие (если есть): <список>
- Тег (если применимо): negative-decision / temporary-rule / precedent

Место хранения:
- Файл: <путь> или [новый: <предложение пути>] или [планируется: <путь> в составе <пакета>]
- Промежуточное хранение (если целевой файл планируется): <путь>
- Раздел (если применимо): <название раздела>

Связные обновления (если есть):
- <список конкретных файлов / артефактов и что в них меняется>
- <затрагиваемые ADR / convention — отдельно, с пометкой "конфликт" / "уточнение" / "supersede">

Паттерн-конфликт (если Шаг 3(ж') сработал):
- Подвид: β-тиражирование / γ-раздвоение по адресату
- Тип повтора (если β): точный дубль / переформулировка
- Тип согласованности (если β): β-согласованный-дубль / β-refactor-кандидат / гибрид
- Артефакты, где правило фигурирует (с разбивкой по уникальной/дублирующей части):
  - <артефакт 1> (роль: <ADR/skill/agent/template/navigation/spec>):
    - уникальная часть: <что>
    - дублирующая часть (если есть): <что>
  - <артефакт 2>: ...
- Источник, который должен быть единственным (если refactor-кандидат): <предложение>
- Решение: оставить согласованный дубль / refactor (вынести в источник + ссылки) / отдельная развилка / гибрид (сократить дублирующее, оставить уникальное)

Развилка конвенции (если Шаг 2(ж)):
- Категория классифицирована: <L-XXX / T-XXX>
- Конвенция размещения для случая <описание случая> отсутствует.
- Варианты решения конвенции (если применимо): <список>

Уточняющие вопросы пользователю (если есть):
- <список>

Открытые расширения карты (если есть):
- <список развилок, которые отражают пробел в карте, а не во фрагменте>
- <черновое предложение структуры нового каталога / слоя, если фрагмент его порождает>
```

Поля без содержания — опускаются (не печатать «нет», «—»).

**Жанровые теги** (опционально, в поле «Тег»):

- `negative-decision` — решение «не делать X», отказ от опции (например: «жанр format не нужен»). Формулировка основного вопроса при этом — «почему отвергнута альтернатива?», а не «почему выбрано это?».
- `temporary-rule` — правило с известным сроком действия (например: «append-only временно не действует на Этапе 1»). Связные обновления должны включать триггер возврата к постоянному правилу после истечения срока.
- `precedent` — методологическое решение, создающее основу для будущих похожих случаев. Сигнал, что фрагмент тянет на ADR, даже если кажется частной задачей.

Теги не взаимоисключающие — фрагмент может быть и `negative-decision`, и `precedent`.

## Принципы применения

- Скилл не записывает файлы сам. Он **предлагает место**; запись — отдельное действие после согласования с пользователем.
- Скилл не создаёт ADR-уровневые решения. Если фрагмент тянет на ADR (значимое решение с альтернативами) — скилл указывает на это и направляет к процедуре создания ADR.
- Скилл не пытается охватить все связные обновления автоматически — он перечисляет очевидные на основе содержимого фрагмента. Глубокая проверка консистентности — задача `knowledge-curator`.
- Если фрагмент явно принадлежит зоне другого скилла (Q-N → `open-questions-workflow`, spec-документ → `spec-document-workflow`, реестр моделей → `spec-models-registry`, миграция legacy → `spec-document-migration`) — текущий скилл указывает на это и передаёт управление.

## Связи

- `CLAUDE.md` — карта Process layers + Trackers; иерархия источников правды; разделение `docs/` vs `.claude/`.
- `.claude/working-with-claude.md` — концепция пайплайна; Принцип 4 (связное обновление, new term routing); Принцип 7 (скиллы — источник применения, ADR — обоснование).
- **Критерии границ слоёв** — формализованы (или будут формализованы) в ADR-PIPELINE-NNNN, планируемом на Этапе 1 рефакторинга, либо в `working-with-claude.md` в разделе о слоях. До формальной фиксации границы L-ADR/L-CONV и L-ADR/T-BL действуют в формулировке Шага 1(б) этого скилла.
- `.claude/notes/artifact-question-mapping-2026-05-18.md` — референс по текущему наполнению ячеек (не источник истины).
- `.claude/notes/knowledge-placement-debug-run-2026-05-19.md` — отчёт отладочного прогона v1; материал, на основе которого построена v2.
- `.claude/notes/knowledge-placement-debug-run-v2-2026-05-19.md` —
  сравнительный прогон v2 на тех же 12 фрагментах; материал
  для оценки эффективности v2.
- `.claude/notes/knowledge-placement-debug-run-v2-corpus-2026-05-19.md` —
  прогон v2 на корпусе трёх нот (18 подозрительных фрагментов,
  4 чистых); материал, на основе которого построена v3.
- `.claude/notes/knowledge-placement-debug-run-v3-clusterA-2026-05-19.md` —
  регрессионный прогон v3 на 4 фрагментах Кластера A (4/4
  чисто); подтверждение, что правка v3 адресует Кластер A.
- `.claude/notes/knowledge-placement-debug-run-v3-full-2026-05-19.md` —
  широкий прогон v3 на полном корпусе (30 подозрительных
  фрагментов, 9 чистых); материал, на основе которого построена
  v4.
- `.claude/notes/knowledge-placement-debug-run-v4-clusterC-2026-05-19.md` —
  регрессионный прогон v4 на 5 фрагментах Кластера C (5/5
  чисто); подтверждение, что правка v4 адресует Кластер C.
- `.claude/notes/knowledge-placement-debug-run-v4-full-2026-05-19.md` —
  широкий прогон v4 на полном корпусе (32 подозрительных
  фрагмента, 9 чистых); материал, на основе которого построена
  v5.
- `.claude/agents/knowledge-curator.md` — потенциальный вызывающий: curator проверяет конец сессии и может вызывать этот скилл для каждого незафиксированного фрагмента.
- ADR-PIPELINE-0006 (планируется, Этап 1 рефакторинга) — формальная фиксация двойной оси (5 слоёв + 5 трекеров) и двух измерений (PIPELINE / PRODUCT). До его принятия скилл работает в режиме as-if-Accepted на материале strategy-summary v7 и note `artifact-question-mapping-2026-05-18`.
- Связанные скиллы по конкретным жанрам артефактов: `open-questions-workflow`, `spec-document-workflow`, `spec-models-registry`, `spec-document-migration`, `spec-cluster-migration`.

## Открытые развилки v5

История эволюции открытых развилок:

- v1 → v2: см. `.claude/notes/knowledge-placement-debug-run-2026-05-19.md`
  (12 фрагментов из strategy-summary v7) и
  `.claude/notes/knowledge-placement-debug-run-v2-2026-05-19.md`
  (сравнительный прогон тех же 12 фрагментов на v2). v2 адресует
  P1–P5, P8; P6, P7 остаются как границы применимости.
- v2 → v3: см. `.claude/notes/knowledge-placement-debug-run-v2-corpus-2026-05-19.md`
  (18 подозрительных фрагментов из трёх нот, не пересекающихся
  с v1/v2). v3 адресует Кластер A.
- v3 → v4: см. `.claude/notes/knowledge-placement-debug-run-v3-clusterA-2026-05-19.md`
  (регрессия Кластера A, 4/4 чисто) и
  `.claude/notes/knowledge-placement-debug-run-v3-full-2026-05-19.md`
  (широкий прогон, 30 подозрительных + 9 чистых фрагментов).
  v4 адресует Кластер C, подвиды (β) тиражирование и (γ)
  раздвоение по адресату; подвид (α) ADR-содержит-чужой-слой
  откладывается на v5 как обратная диагностика.
- v4 → v5: см. `.claude/notes/knowledge-placement-debug-run-v4-clusterC-2026-05-19.md`
  (регрессия Кластера C, 5/5 чисто) и
  `.claude/notes/knowledge-placement-debug-run-v4-full-2026-05-19.md`
  (широкий прогон, 32 подозрительных фрагмента). v5 адресует
  сигнал 14 — позитивный тест на «согласованный дубль» внутри
  подвида (β) Шага 3(ж').

**Адресовано в v5 (по результатам широкого прогона v4):**

- Сигнал 14 — позитивный тест на «согласованный дубль».
  Подвид (β) Шага 3(ж') теперь делится на β-согласованный-дубль
  и β-refactor-кандидат по позитивному тесту «каждое место
  содержит уникальную часть правила». Выходной формат расширен:
  поле «Артефакты» структурировано по уникальной/дублирующей
  части и по ролям артефактов. Пограничные случаи (согласованный
  дубль по частям правила, гибрид) явно покрыты.
- Доминирование β-согласованного-дубля (56% фрагментов v4-full)
  превратило субъективную оценку в процедурную. Это снимает
  главное ограничение применимости Шага 3(ж') на ADR-богатом
  корпусе.

**Адресовано в v4 (по результатам широкого прогона):**

- Кластер C (паттерн-конфликт), подвиды:
  - (β) тиражирование — добавлен Шаг 3(ж') с позитивным
    тестом «правило фигурирует в ≥2 артефактах» + новое поле
    «Паттерн-конфликт» в выходном формате.
  - (γ) раздвоение по адресату Claude (CC vs чат) — встроено
    в Шаг 3(ж') как подвид. Это также адресует Сигнал 3
    («два адресата Claude» из широкого прогона).
- Подвид (α) — ADR содержит фрагменты чужого слоя — отложен
  на v5: требует обратной диагностики (скилл проверяет
  существующие ADR на наличие чужих слоёв), это другой режим
  работы скилла, чем «фрагмент → место».

**Адресовано в v3 (по результатам корпус-прогона):**

- Кластер A (семантическая пометка существующего артефакта;
  `.claude/notes/` как промежуточное хранение без статуса
  категории) — добавлен Шаг 2(ж) «конвенция размещения
  отсутствует» + Шаг 4(д) «развилка конвенции размещения» +
  поле «Развилка конвенции» в выходном формате. Скилл теперь
  явно подсвечивает отсутствие конвенции вместо выбора места
  наугад.
- Тег `precedent` сработал на 9/18 (50%) после ужесточения в v2;
  по сравнению с 9/12 (75%) v2 динамика правильная, но
  абсолютная частота всё ещё высокая. Перенесено в «Не
  адресовано в v3» как сигнал к следующей итерации.

**Адресовано в v2 (по результатам отладки):**

- P1 (целевой файл не существует, относится к будущему пакету) — добавлен Шаг 2(г) про промежуточное хранение.
- P2 (L-ADR vs T-BL / T-MIG) и P3 (L-ADR vs L-CONV) — добавлены критерии границ в Шаге 1(б).
- P4 (конфликт с существующим ADR не подсвечивается) — добавлен Шаг 3(ж).
- P5 (многокомпонентные фрагменты) — переписан Шаг 1(в); разрешены несколько основных категорий.
- P8 (негативные решения, временные правила, прецеденты) — добавлены жанровые теги в выходной формат.
- Фильтр на план-уровневые фрагменты — добавлен пункт в «Когда НЕ активируется» + критерий «знание vs план» в Шаге 1(б).

**Не адресовано полностью (остаются как сигналы границ применимости скилла):**

1. **P6 — фрагмент порождает целый каталог / слой, а не один файл.** В v2 добавлен Шаг 2(д) (расширение карты, черновое предложение структуры), но **сам акт расширения карты** скилл не выполняет — это отдельное событие пайплайна. Скилл подсвечивает потребность, но не закрывает её. Это **граница применимости**, не дефект: размещение знания и проектирование структуры карты — разные задачи.

2. **P7 — циркулярность: фрагмент описывает саму карту.** Если фрагмент задаёт характеристические вопросы слоёв, границы измерений, состав ячеек — скилл при классификации использует ровно эти слои. Чистый ответ от скилла в таком случае — «расширение / уточнение карты», без выбора конкретного файла до фиксации новой версии карты. v2 не пытается выйти из циркулярности; она признаётся явной границей.

3. **Расширенные кандидаты-вопросы (M-NAV, M-PIPE, M-USER, M-FORM, M-META, M-PROCDESIGN, L-SCRATCH)** из note `artifact-question-mapping`. По букве задачи v2 список характеристических вопросов не расширяется — это решение карты, не скилла. Фрагменты, ложащиеся в эти категории, по-прежнему уходят в «Открытые расширения карты». Решение о расширении основного списка — отдельное событие, не правка скилла.

4. **Делегирование специализированным скиллам.** В v2 список делегатов тот же (4 скилла). Если появятся новые специализированные — нужно ручное обновление. Автоматического реестра нет.

5. **Декомпозиция на несколько скиллов.** В v2 все четыре шага по-прежнему внутри одного файла. Если на втором прогоне выяснится, что некоторые шаги вызываются независимо (например, Шаг 3 как отдельная проверка консистентности по уже зафиксированному знанию) — кандидат на выделение в отдельный скилл.

6. **Выходной формат.** В v2 добавлены поля (Основные категории, Тег, Промежуточное хранение) и расширены пометки в «Связных обновлениях» и «Открытых расширениях карты». Если на практике окажется, что добавленные поля систематически пусты — пересмотреть в v3.

**Не адресовано в v5 (сигналы к v6, по результатам широкого прогона v4):**

7. **Кластер B (расширение Шага 1: классификация).** На корпус-
   прогоне выявлены две неразличаемые v2/v3 развилки: (а) открытый
   вопрос со списком альтернатив без выбора уходит в T-Q «по
   отрицательному тесту» (нужен позитивный); (б) «открытое
   расширение карты» — крупная свалка, внутри ≥5 подвидов (новый
   слой, новое измерение/перпендикулярная ось, отсутствующая
   половина измерения PIPELINE/PRODUCT, мини-циркулярность,
   самореферентность). v4 кандидат: уточнение Шага 1(б) и
   декомпозиция «открытых расширений карты».
   Условие применимости: после фиксации ADR-PIPELINE-0006
   (состав категорий карты).

8. **Калибровка тегов.** На широком прогоне v4 тег `precedent`
   применяется на 20/32 фрагментов (63%). Динамика по итерациям:
   75% (v2) → 50% (v2-corpus) → 40% (v3-full) → 63% (v4-full).
   Регресс к v2-corpus объясняется спецификой v4-full корпуса
   (много фундаментальных ADR-фрагментов). v6 кандидат: явное
   различение «фундаментальный precedent» (на ADR-уровне, по
   умолчанию оправдан) и «прикладной precedent» (на skill/
   agent-уровне, применять строго). `negative-decision` 12/32 =
   38% (рост с 0% v3-full из-за ADR Alternatives sections) —
   устойчивый, оправданный. `temporary-rule` 1/32 = 3% —
   удерживать как редкий устойчивый.

9. **T-EVO как основная категория.** На корпус-прогоне впервые
   зафиксирован T-EVO как одна из основных категорий
   (фрагмент 18). Характеристический вопрос T-EVO «как менялся
   пайплайн?» допускает двойное толкование: (а) куда писать о
   новом изменении; (б) носитель исторического контекста из
   других мест. v4 кандидат: уточнить формулировку
   характеристического вопроса T-EVO.

10. **L-SCRATCH (статус `.claude/notes/`).** v3 явно подсвечивает
    отсутствие конвенции через Шаг 2(ж), но не решает: считать
    ли `.claude/notes/` шестой категорией (L-SCRATCH) или
    «рабочим местом без категории». Это решение карты, не скилла.
    v4 ждёт ADR-PIPELINE про статус нот.

11. **Подвид (α) Кластера C — обратная диагностика.** В широком
    прогоне зафиксированы случаи, когда фрагмент при размещении
    выявляет, что чужой слой уже сидит в ADR (corpus-фрагменты
    4-6: ADR содержит элементы L-CONV/L-AGENT/L-SKILL). v3(ж')
    адресует прямое тиражирование (β) и раздвоение (γ), но
    не покрывает (α). Скилл сейчас работает «фрагмент → место»;
    для (α) нужно «фрагмент → проверить чужие слои в
    существующих ADR» — обратный обход. v5 кандидат: добавить
    в Шаг 3(ж') режим обратной диагностики или вынести в
    отдельную процедуру / скилл.

12. **γ-asymmetric: подвид раздвоения по адресату с
    отсутствующей формулировкой.** Формальное определение
    подвида (γ) в Шаге 3(ж') v4 предполагает «правило
    сформулировано отдельно для двух адресатов Claude (Claude
    Code vs чат)». На регрессионном прогоне v4 (фрагменты 17+18
    Rule 13/14) встречена асимметрия: правило сформулировано
    только для одного адресата, для другого — подразумевается
    или не зафиксировано. Это не точное раздвоение, но и не
    отсутствие проблемы — у адресата, для которого формулировки
    нет, поведение ad hoc. v5 кандидат: явный подвид
    `γ-asymmetric` внутри (γ) с собственным признаком и
    собственным решением (зафиксировать формулировку для
    второго адресата, не выносить в единый источник).

13. **Метапаттерн «семья правил с общим паттерном».** На
    регрессионном прогоне обнаружена пара фрагментов (8 «MODELS.md
    — отражение, не гейт» и 14 «Целевые spec-документы в трекере
    не предзаявляются»), которые применяют общий паттерн
    «реестр vs гейт» к разным правилам. Это не (β) тиражирование
    одного правила в N мест, а **семья правил с общим паттерном**
    применения. v4 фиксирует каждый случай отдельно как (β), но
    не маркирует их как членов семьи. v5 кандидат: тест на
    «общий паттерн применён к N разным правилам» (упомянут как
    «семья реестров» в v3-full сигналах). Это пересекается с
    Сигналом 10 «согласованный дубль отличный от конфликта»
    из v3-full, но конкретизирует его: согласованность не на
    уровне одного правила, а на уровне паттерна, применённого
    к нескольким правилам.

14. **(δ) Омоним: один термин — N семантик в разных артефактах.**
    Новый сигнал из v4-full (фрагмент 17 «hard gates»: один
    термин обозначает два разных набора проверок в ADR-0009 и
    в скилле `spec-cluster-migration`). Это не (β) и не (γ) —
    одно имя, две семантики. v6 кандидат: добавить (δ) как
    новый подвид паттерн-конфликта в Шаге 3(ж'). Тест: «один
    термин используется в ≥2 артефактах с разным денотатом».

15. **β-семейный: общая структура, разное содержимое.** Новый
    сигнал из v4-full (фрагмент 24 «KPI блок в каждом агенте»).
    Это не β-точный-дубль (содержание разное) и не β-переформулировка
    (нет общего правила, только общая структура). v6 кандидат:
    явный подтип β-семейный внутри (β). Тест: «каждый артефакт
    содержит уникальное содержимое в общей структуре». Граница
    с сигналом 13 «семьи правил» — β-семейный про **структуру
    внутри одного артефакта**, семьи правил про **общий паттерн
    применения к N разным правилам**.

16. **γ-multiplex: раздвоение по N адресатам (N ≥ 3).** Новый
    сигнал из v4-full (фрагмент 29 working-with-claude
    Принцип 1 — правило о прикреплении файлов для CC, чата
    claude.ai и человека M-USER). Расширение (γ) с пары CC vs
    чат до N адресатов. v6 кандидат: явное различение γ-binary
    (текущая γ) и γ-multiplex; либо расширение определения (γ)
    до N адресатов с N ≥ 2.

17. **Измерение «both PIPELINE+PRODUCT».** Подтверждено пятью
    случаями на v4-full (фрагменты 1, 3, 9, 18, 25, 27;
    исходный сигнал из v3-full 1 «Принцип 6 — дока ведёт код»).
    Шаг 1(а) v4 заставляет выбирать одно измерение, но
    некоторые фрагменты структурно принадлежат обоим. v6
    обязательное: добавить опцию «измерение: both» в Шаге 1(а).
