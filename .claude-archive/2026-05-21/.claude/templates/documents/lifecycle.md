---
status: draft
last_review: 2026-MM-DD
related_adrs: []
related_models: [<Имя класса>]
related_processes: []
---

# <Имя класса> — жизненный цикл

<Короткое описание: что это за модель, как живёт в системе. 1-3 предложения.>

## Жизненный цикл

<Текстом или mermaid-диаграммой — от появления до завершения.
Основные фазы.>

## Статусы и переходы

<Таблица допустимых переходов между состояниями. Если модель имеет
несколько FSM — отдельный раздел для каждой.>

| От | К | Условие |
|---|---|---|
| NEW | LIVE | <Условие перехода> |

## Кто триггерит переходы

<Список процессов с указанием, какие переходы инициирует каждый.
Ссылки на документы в processes/.>

- **<Имя процесса>** — триггерит NEW → LIVE при
  успешном ответе биржи.

## Доменные запреты переходов

<Опционально. Перечисляются ТОЛЬКО доменные запреты с осмысленным
обоснованием. Структурные запреты («из терминального в терминальный»,
«в тот же статус», «обратные переходы») НЕ пишутся — отсутствие
перехода в таблице выше эквивалентно запрету. Раздел нужен, когда
запрет неочевиден из таблицы переходов и требует объяснения «почему».
Если таких запретов нет — раздел отсутствует.>

- **<От_статус> → <К_статус>** — запрещён, потому что
  <содержательное обоснование, какое доменное правило нарушается;
  например: «созданный Order не может стать активным минуя
  отправку на биржу, потому что биржа должна знать об ордере»>.

## Инварианты поведения

- <Правила, которые держатся на уровне жизни модели. Например: «после
  ERROR — только CLOSED, никаких возвратов».>

## Обработка сбоев

<Что происходит с моделью при отказе биржи, рестарте, рассинхроне.
Не реализация (она в процессах) — а что состояние модели в этих
случаях.>
