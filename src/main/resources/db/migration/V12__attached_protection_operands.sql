-- Операнды разбора встроенной защиты: код отказа источника и ценовая база
-- триггера. Оба жили только в граничном снапшоте прохода и исчезали вместе
-- с ним, поэтому по данным не восстанавливались ни тропа потери покрытия
-- (три тропы писали одну причину), ни применённая биржей база срабатывания.
-- См. docs/models/domain/core/Order.md §«Персистентность встроенной защиты».
--
-- Обе колонки nullable по существу модели, а не ради переноса данных:
-- пустое эхо источника — недобытый факт, а не разрешение подставить
-- умолчание (docs/rules/absent-value-semantics.md). Обязательность базы
-- триггера держится на стороне ПОСТАНОВКИ: вход не выпускается без неё.
--
-- Бэкфилла нет: до конца фазы 1 таблицы пусты
-- (.claude/rules/pre-launch-schema-changes.md).

alter table attached_algo_orders
    add column fail_code          varchar(64),
    add column trigger_price_type varchar(32);
