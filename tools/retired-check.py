# -*- coding: utf-8 -*-
"""Предмет проверки: снятая редакция не живёт в носителях, а пришедшая — дошла.

ЗАЧЕМ. Правило `.claude/rules/carrier-levels.md` («стыковая истина живёт один
раз; копия в доке деталей — дефект носителя») и `.claude/rules/policy-home.md`
(«один дом на политику») энфорсера не имели ни одного: механической проверки,
что правка политики ДОШЛА ДО ПОТРЕБИТЕЛЕЙ, не существовало. Измерено: 27 из
39 гейтящих находок одного прогона — непройденный обход потребителей, причём
дом правила противоречил сам себе через тридцать строк. Правка верна
поодиночке и не сходится вместе.

ЗАЧЕМ ВТОРАЯ ОСЬ (2026-08-31). Свип по ФОРМЕ ТЕРМИНА не видит носителя,
выражающего снятую редакцию ДРУГИМИ СЛОВАМИ, — и это не редкий случай: снятая
редакция дедупа отчёта жила в шести носителях при зелёном прогоне этой
команды, а канон гейта держал снятый исход глаголом закрытия ПЕРЕД термином.
Поэтому запись реестра называет ещё и ПОПУЛЯЦИЮ НОСИТЕЛЕЙ — места, где снятая
редакция стоя́ла и где ПРИШЕДШАЯ обязана появиться. Свип идёт по популяции, а
не только по форме: носитель, названный в популяции и не предъявивший
пришедшей редакции, — дефект, как бы он ни был написан.

ФОРМЫ ПРЕДМЕТА, КОТОРЫЕ ДЕТЕКТОР ВИДИТ (объявлено, доказано осями батареи):
  1. дословный термин снятой редакции в живом носителе;
  2. тот же термин, разорванный переносом строки (свип идёт по плоскому
     тексту: построчный проход фразу через перенос не видит);
  3. вхождение в РАБОЧЕМ файле (`backlog.md`, `open-questions.md`, roadmap) —
     оно опаснее докового: рабочий файл адресован исполнителю следующего
     под-шага;
  4. носитель ПОПУЛЯЦИИ, не предъявивший пришедшей редакции, — включая
     носитель, выражающий снятое другими словами (встречная форма);
  5. строка популяции, указывающая на носитель, которого в корпусе нет;
  6. свой шаблон пришедшей редакции у отдельного носителя популяции — там, где
     на месте снятого встало разное (контрпример у одного, нота величины-дома
     с переехавшим клеймом у другого);
  7. ОТРИЦАНИЕ рядом с термином (ключ `unless`): «слот не закрывает»,
     «закрывающей силы у него нет» — это ДЕЙСТВУЮЩАЯ редакция, а не снятая.
     Без этой формы дом действующей редакции попадал бы в находки, и гасить
     его пришлось бы allow-листом — то есть allow-лист работал бы подавителем
     ложных срабатываний, а не разрешением называть снятое.
  8. ПРОЗА САМОЙ ЗАПИСИ, пересказывающая пришедшую редакцию. Дом пришедшей
     редакции один — ключ `arrived`; комментарий, объявляющий её ещё и словами,
     есть второй носитель той же истины, и стареет он первым. Измерено:
     комментарий новой записи назвал пришедшей редакцию, которую верификация
     ОТВЕРГЛА, — механическая часть записи была верна, шаблоны совпадали,
     прогон был зелен, соврала проза. Правило действует на НОВЫЕ записи;
     накопленное перечислено в `tools/retired-prose-debt.txt` и не растёт:
     прогон падает на новой записи с прозой и на строке долга, которой в
     реестре больше нет. Механика долга — та же, что у `tools/anchor-debt.txt`.
     Дом формы — `.claude/rules/self-description-form.md`.
Чего детектор НЕ мерит: носитель, который выражает снятую редакцию другими
словами и в популяции НЕ НАЗВАН. Против этого класса стои́т предмет 2 свипа
закрытия («свип идёт по конструкции, а не только по снимаемым терминам») и
построение популяции до правки (`.claude/skills/closure-population.md`), а не
эта команда: перечислить то, о чём закрытие не знает, механически нельзя.

РЕЕСТР. Одна запись — одна снятая редакция: опознавательный шаблон снятой
редакции, шаблон ПРИШЕДШЕЙ, дата снятия, решение-источник, перечень мест, где
снятый термин стоя́ть ВПРАВЕ, и ПОПУЛЯЦИЯ носителей. Запись живёт, пока живо
знание о снятии; снимается вместе с закрытием шага. Запись без шаблона
пришедшей редакции или без популяции ОТКАЗЫВАЕТ проверку целиком (код 2):
неполная запись давала бы зелёный прогон на неизмеренной оси.

ЧТО МОЖЕТ ПОПАСТЬ В РАЗРЕШЁННЫЕ МЕСТА. Носитель, где снятая редакция
называется затем, чтобы сказать, чем она заменена. Перечня родов здесь нет:
место называется у СВОЕЙ записи ключом `allowed`, и это объявление и есть
перечень — родовой список стареет молча, а ключ проверяется прогоном. Файл
продуктового корпуса (`docs/**`) в разрешённые места НЕ попадает никогда —
`docs/concept.md` §«Опровержение правит на месте» запрещает слой опровержения
в корпусе: знание, оказавшееся неверным, переписывается, а не сопровождается
пометкой «прежняя редакция снята».

ЧЕГО ЭТА КОМАНДА НЕ ЛОВИТ В ДОМЕ. Внутри разрешённого места вхождения снятого
термина не проверяются вовсе, поэтому дом, который на одной строке фиксирует
снятие, а через тридцать строк утверждает снятое, оси 1-3 не поймают: их
предмет — ПОТРЕБИТЕЛИ. Но дом, названный в популяции, обязан предъявить
пришедшую редакцию, и это ось 4.

ОБЛАСТЬ СВИПА. Живые носители: `CLAUDE.md`, `README.md` и `*/README.md`
(указатель каждого дерева монорепозитория — файл знания, Д582), `docs/**`
(md, json), деревья кода (java, json, sql, yml, yaml, properties) — `donor/`,
`services/*`, `libs/*`, — манифесты `deploy/**` (yml, yaml, json),
`tools/**` (py, sh, txt) и `.claude/**` (md) за вычетом архива, истории,
библиотеки и отчётов прогонов (`progress/` цитирует снятые редакции как
находки — это их предмет, а не рецидив). РАБОЧИЕ РЕЕСТРЫ `.claude/work/*.json`
в области ЕСТЬ: реестр компонентов и цитирует снятые редакции как закрытые
находки, и несёт живые клеймы. Первое объявляется у своих записей ключом
`allowed` — так же, как объявляется дом решения; второе свипается. Исключить
реестр целиком значило бы вывести из-под свипа носитель, который гейтит
`CODE` и адресован исполнителю следующего под-шага. Число просмотренных носителей
печатает сам прогон: область, которая у́же, чем полагает автор, — тот же
ложный зелёный.

ОБЛАСТЬ ОБЪЯВЛЕНА ИСЧЕРПЫВАЮЩЕ (ось 20). Перечень шаблонов сам по себе от
потери дерева не защищает: дерево, которого в перечне нет, не портит зелёного
прогона и потому невидимо — ровно тот механизм, ради которого перечень
переписан префиксами, и он повторился внутри той же правки (`web/`, `deploy/`
и пять `README.md` деревьев остались вне области). Поэтому область сверяется
с ФАКТИЧЕСКИМ корнем репозитория: каждый каталог верхнего уровня либо покрыт
шаблоном с ЛИТЕРАЛЬНЫМ первым сегментом, либо назван в `OUT_OF_AREA` с
причиной. Непокрытый и неназванный каталог ⇒ код 2 «ПРОВЕРКА НЕ ПРОВОДИЛАСЬ»:
свип по неполной области ничего не удостоверяет. Шаблон с подстановкой в
первом сегменте (`*/README.md`) покрытием дерева НЕ считается — иначе всякое
новое дерево покрывалось бы им молча, и ось не срабатывала бы никогда.

Запуск (из корня репозитория):  python3 tools/retired-check.py
Код возврата: 0 — снятых редакций в живых носителях нет и популяции
предъявили пришедшую редакцию; 1 — есть дефекты; 2 — ПРОВЕРКА НЕ ПРОВОДИЛАСЬ
(ось не доказана, реестр пуст либо неполон, корпус пуст).
"""
import glob
import io
import os
import re
import sys
import tempfile

# Печать не зависит от кодировки консоли вызывающего: на cp1251-консоли
# объявленной среды вывод падал UnicodeEncodeError (класс описан в backlog
# у anchor-check; тот же ремонт исполнимости, DOCS_CHECK_33 узел 9).
if hasattr(sys.stdout, "reconfigure"):
    sys.stdout.reconfigure(encoding="utf-8")
if hasattr(sys.stderr, "reconfigure"):
    sys.stderr.reconfigure(encoding="utf-8")

SKIP = ('/.claude-archive/', '/.claude/work/history/', '/.claude/work/progress/',
        '/.claude/library/', '/target/', '/.git/',
        # Файл долга прозы ПЕРЕЧИСЛЯЕТ снятые редакции по именам — это его
        # предмет, а не рецидив, ровно как у отчётов `progress/`. Свипать его
        # значило бы объявлять дефектом каждую строку собственного долга.
        '/tools/retired-prose-debt.txt')

# Область приведена к ОБЪЯВЛЕННОЙ: прежний перечень не брал миграции,
# конфиги (yaml/yml), реестры инструментов (txt) и сам план запросов
# (json коллекции) — то есть шапка обещала «src/**, tools/**», а свип шёл
# по трём расширениям из семи. Носитель, не попавший в область, зелёного
# прогона не портит и потому невидим.
#
# Деревья кода перечислены ПРЕФИКСАМИ, а не одним `src/**`: после
# реструктуризации в монорепозиторий (шаг 1 фазы 2) код донора лежит в
# `donor/src`, а сервисы и общие артефакты появятся в `services/<имя>/src` и
# `libs/<имя>/src`. Перечень, привязанный к одному корню, молча терял бы
# каждое новое дерево — тот самый невидимый зелёный, о котором абзац выше.
CODE_TREES = ('donor', 'services/*', 'libs/*')
CODE_SUFFIXES = ('java', 'json', 'sql', 'yml', 'yaml', 'properties')
DEPLOY_SUFFIXES = ('yml', 'yaml', 'json')
# СБОРОЧНЫЕ ФАЙЛЫ В ОБЛАСТИ, и это решение, а не расширение по инерции.
# Комментарий `pom.xml` — носитель предписания наравне с доком: им объясняют,
# почему модуль объявлен так, а не иначе, и снятая редакция живёт там ровно так
# же незаметно. Три уровня — корень реактора, дерево донора и модули
# `services/<имя>`, `libs/<имя>`; глубже модулей у реактора ничего нет.
BUILD_FILES = ('pom.xml', '*/pom.xml', '*/*/pom.xml')

# ДЕРЕВО ИНСТРУМЕНТОВ — ШЕСТЬ РАСШИРЕНИЙ, И ТРИ ИЗ НИХ ДОБАВЛЕНЫ ЭТИМ ХОДОМ.
# Шаблоны выписаны литералами, а не собраны из перечня суффиксов: их читает не
# только эта строка, но и условие возврата задачи, которая их потребовала, —
# собранный в цикле шаблон в тексте файла не встречается вовсе.
# `tools/**/*.md` — стоячий промпт цикла (`tools/session-prompt.md`) есть живой
# носитель ПРЕДПИСАНИЙ, а не оформление: снятая редакция в нём действует на
# каждую сессию. `tools/**/*.ps1` — оболочечный близнец `session-loop.sh`,
# несущий ту же прозу. `tools/**/*.yaml` — манифест стенда
# (`tools/stand/kind-cluster.yaml`) с комментариями о выборе формы кластера.
TOOL_PATTERNS = ('tools/**/*.py', 'tools/**/*.sh', 'tools/**/*.txt',
                 'tools/**/*.md', 'tools/**/*.ps1', 'tools/**/*.yaml')

ROOTS = (('CLAUDE.md', 'README.md', '*/README.md',
          'docs/**/*.md', 'docs/**/*.json',
          '.claude/**/*.md', '.claude/work/*.json')
         + BUILD_FILES
         + TOOL_PATTERNS
         + tuple('%s/src/**/*.%s' % (tree, suf)
                 for tree in CODE_TREES for suf in CODE_SUFFIXES)
         + tuple('deploy/**/*.%s' % suf for suf in DEPLOY_SUFFIXES))

# Каталоги верхнего уровня, сознательно оставленные ВНЕ области, с причиной.
# Ось 20a требует, чтобы каждый каталог корня был либо покрыт шаблоном с
# литеральным первым сегментом, либо назван здесь: молчаливо потерянного
# дерева не бывает — бывает названное.
OUT_OF_AREA = {
    '.git': 'служебный каталог git — не носитель знания',
    '.idea': 'настройки IDE, локальные для машины',
    '.m2': 'локальный кэш Maven',
    '.claude-archive': 'архив: материал, не источник истины',
    '.codebase-archive': 'архив: материал, не источник истины',
    'web': ('SPA: исходников ещё нет, суффиксы фронта заводит шаг 12 фазы 2; '
            'указатель web/README.md свипается как файл знания'),
}

# Файлы верхнего уровня вне области, с причиной (ось 20b). Общее у всех —
# машинная настройка без прозы о политике: их читает инструмент, а не человек,
# ищущий обоснование.
OUT_OF_AREA_FILES = {
    '.gitignore': 'перечень шаблонов игнорирования — прозы о политике не несёт',
    '.env.prod.local': 'локальный секрет машины держателя, вне репозитория',
    '.env.test.local': 'локальный секрет машины держателя, вне репозитория',
    '.env.vault.test.local': 'локальный секрет машины держателя, вне репозитория',
    '.env.vault.test.local.example': 'образец файла секретов — пары «ключ = значение»',
    'code-style.xml': 'выгрузка настроек форматтера IDE — машинная, не прозаическая',
    'docker-compose.yml': 'локальный подъём зависимостей разработчика, вне контура развёртывания',
    'lombok.config': 'настройка процессора аннотаций — пары «ключ = значение»',
    'vault.hcl': 'конфигурация локального Vault стенда — пары «ключ = значение»',
}

# Расширения внутри покрытых деревьев вне области, с причиной (ось 20c). Ключ —
# «дерево.расширение».
OUT_OF_AREA_SUFFIXES = {
    'docs.gitkeep': 'пустышка git для пустого каталога — содержимого не несёт',
    '.claude.gitkeep': 'пустышка git для пустого каталога — содержимого не несёт',
    '.claude.json': ('локальные настройки харнесса и выгрузки плана запросов: '
                     'формат без синтаксиса комментария, прозу нести не может'),
    '.claude.svg': 'иллюстрация дерева знания — растр, а не текст',
    'donor.iml': 'дескриптор модуля IDE — машинный, локальный для машины',
    'tools.json': 'выгрузки коллекций Postman — формат без синтаксиса комментария',
    'tools.pyc': 'байт-код кэша Python — производное, не носитель',
    'deploy.gitkeep': 'пустышка git для пустого каталога — содержимого не несёт',
}

# --- РЕЕСТР СНЯТЫХ РЕДАКЦИЙ ---------------------------------------------------
# Ключи записи:
#   name       — как редакция называется в отчёте;
#   pattern    — опознавательный шаблон СНЯТОЙ редакции (плоский текст);
#   arrived    — опознавательный шаблон ПРИШЕДШЕЙ редакции;
#   date       — дата снятия;
#   source     — решение-источник;
#   allowed    — где снятый термин стоя́ть вправе (носители пайплайна);
#   population — носители, где снятая редакция стоя́ла и где пришедшая обязана
#                стоять. Пара «носитель, свой шаблон пришедшей»; None во втором
#                поле означает «шаблон записи». Свой шаблон обязателен там, где
#                на месте снятого встало РАЗНОЕ: у одного носителя — контрпример,
#                у другого — нота величины-дома, несущая переехавший клейм.
#                Пустой популяции не бывает: редакция, не стоявшая нигде, не
#                снималась.
RETIRED = [
    {
        'name': 'следующий шаг берёт следующая сессия',
        # Снято решением держателя 2026-09-11 (остановка цикла на границе
        # шага). Прежняя редакция вела цикл через границу закрытого шага
        # дальше: момент, когда держателю показывают закрытое, зависел от
        # того, на какой сессии кончится предел запуска, а не от того, что
        # шаг закрыт. Что действует теперь — объявляет ключ `arrived`.
        'pattern': r'следующий\s+шаг\s+бер[её]т\s+следующая\s+сессия',
        'arrived': r'границ[а-яё]*\s+закрытого\s+шага\s+останавливает\s+цикл',
        'date': '2026-09-11',
        'source': 'решение держателя 2026-09-11 (цикл сессий: остановка на границе шага)',
        'allowed': ('.claude/skills/session-chain.md',
                    '.claude/work/decision-digest.md'),
        'population': (
            ('.claude/skills/session-chain.md', None),
            ('tools/session-prompt.md', None),
            ('tools/session-loop.sh', None),
        ),
    },
    {
        'name': 'связку имён компонентов не мерит ни одна проба ни на одном конце',
        # Снято первой верификацией узла 5 GAPS_CLOSE_6 шага 10 фазы 2
        # (2026-09-11, находка В2). Прежняя редакция выводила клейм о покрытии
        # из одной мутации — класса, у которого пробы периметра нет, — и была
        # шире замера. Что действует теперь — объявляет ключ `arrived`.
        'pattern': r'у\s+прочих\s+классов\s+имя\s+не\s+связывает',
        'arrived': r'На\s+конце\s+периметра\s+охрана\s+есть',
        'date': '2026-09-11',
        'source': 'узел 5 GAPS_CLOSE_6, шаг 10 фазы 2 (верификация 1, В2)',
        'allowed': ('.claude/work/decision-digest.md',),
        'population': (
            ('.claude/work/backlog.md', None),
        ),
    },
    {
        'name': 'на «кто остановил торговлю» в таймлайне отвечает содержимое события',
        # Снято узлом 5 GAPS_CLOSE_6 шага 10 фазы 2 (2026-09-11, закрытие A10).
        # Прежняя редакция приписывала актору содержимого вопрос шире того, на
        # который отвечает писатель: ребро остановки сделки затребуют двое, и у
        # сделок одной и той же ручной ступени значение расходится. Что
        # действует теперь — объявляет ключ `arrived`.
        'pattern': r'кто\s+остановил\s+торговлю',
        'arrived': (r'(кем\s+порождён\s+этот\s+ход'
                    r'|Инициатор\s+—\s+инициатор\s+ЭТОГО\s+хода)'),
        'date': '2026-09-11',
        'source': 'узел 5 GAPS_CLOSE_6, шаг 10 фазы 2 (закрытие A10)',
        'allowed': ('.claude/work/decision-digest.md',),
        'population': (
            ('docs/rules/manual-halt.md', r'кем\s+порождён\s+этот\s+ход'),
            ('docs/models/domain/other/Auditable.md',
             r'Инициатор\s+—\s+инициатор\s+ЭТОГО\s+хода'),
        ),
    },
    {
        'name': 'состав формы периметра объявлен перечнем снятого в javadoc маппера',
        # Снято узлом 5 GAPS_CLOSE_6 шага 10 фазы 2 (2026-09-11, закрытие B2).
        # Прежняя редакция держала решение о составе только в javadoc маппера,
        # перечнем из двух родов снятого, а указатель вела в раздел, такой
        # клаузы не содержавший; форма при этом снимала и то, чего перечень не
        # называл. Что действует теперь — объявляет ключ `arrived`.
        'pattern': (r'(потребител[яь]\s+у\s+них\s+на\s+той\s+стороне'
                    r'|Операнды\s+отчёта\s+терминала\s+и\s+актор)'),
        'arrived': (r'(Состав\s+своей\s+формы\s+периметр\s+объявляет\s+сам'
                    r'|Состав\s+формы\s+задаёт\s+сама\s+форма'
                    r'|КРИТЕРИЙ\s+состава\s+из\s+позиции\s+выведен)'),
        'date': '2026-09-11',
        'source': 'узел 5 GAPS_CLOSE_6, шаг 10 фазы 2 (закрытие B2)',
        'allowed': ('.claude/work/decision-digest.md',),
        'population': (
            ('docs/architecture/contracts.md',
             r'Состав\s+своей\s+формы\s+периметр\s+объявляет\s+сам'),
            ('services/bff/src/main/java/com/example/bff/mapping/'
             'StreamEventMapper.java', r'Состав\s+формы\s+задаёт\s+сама\s+форма'),
            ('.claude/work/backlog.md', r'КРИТЕРИЙ\s+состава\s+из\s+позиции\s+выведен'),
        ),
    },
    {
        'name': 'исход свипа предъявляется ПОФАЙЛОВОЙ таблицей независимо от выдачи',
        # Снято концепт-проходом по разбору добытой выдачи (2026-09-11, реакция
        # на С5 узла 2 GAPS_CLOSE_6 шага 10 фазы 2). Прежняя редакция задавала
        # единицу клетки разбора СЛОВОМ — «пофайловая», — а не командой: при
        # выдаче попаданиями файл стоя́л в таблице с исходом, верным для одного
        # своего места, и прочие попадания разбору не подлежали. Что действует
        # теперь — объявляет ключ `arrived`.
        'pattern': r'пофайлов[а-яё]+\s+таблиц',
        'arrived': r'соразмерн[а-яё]+\s+(выдаче|единице)',
        'date': '2026-09-11',
        'source': 'концепт-проход по разбору выдачи, шаг 10 фазы 2 (Д1403-Д1407)',
        'allowed': ('.claude/work/decision-digest.md',),
        'population': (
            ('.claude/processes/roadmap-step-execution.md', None),
            ('.claude/skills/update-roadmap-progress.md', None),
            ('.claude/skills/closure-population.md', None),
            ('.claude/rules/edit-kind-obligations.md', None),
        ),
    },
    {
        'name': 'причина каскада — по радиусу поднятого сигнала; читателей соответствия двое',
        # Снято узлом 1 GAPS_CLOSE_6 шага 10 фазы 2 (2026-09-10, закрытие B1).
        # Прежняя редакция позволяла каждому затребователю ребра энфорсмента
        # резолвить причину самостоятельно: первый ход брал её из радиуса
        # ПОДНИМАЕМОГО сигнала, шаг прохода — из стоящих ступеней, и на сделке
        # под обеими ступенями они писали в одну durable-колонку разное. Что
        # действует теперь — объявляет ключ `arrived`.
        'pattern': (r'(причину\s+выхода\s+из\s+штатного\s+ведения\s+по\s+радиусу'
                    r'|Читателей\s+у\s+соответствия\s+двое'
                    r'|шаг\s+читает\s+счёт\s+первым'
                    r'|порядком\s+чтения\s+шага\s+энфорсмента)'),
        'arrived': (r'(Резолв\s+причины\s+у\s+обоих\s+затребователей'
                    r'|Причину\s+каскад\s+НЕ\s+выводит'
                    r'|Читатель\s+у\s+соответствия\s+ОДИН'
                    r'|Порядок\s+чтения\s+свойством\s+этого\s+шага)'),
        'date': '2026-09-10',
        'source': 'узел 1 GAPS_CLOSE_6, шаг 10 фазы 2 (закрытие B1)',
        'allowed': ('.claude/work/decision-digest.md',),
        'population': (
            ('docs/lifecycles/Deal.md', r'Резолв\s+причины\s+у\s+обоих\s+затребователей'),
            ('docs/components/SafetyHoldCoordinator.md', r'Причину\s+каскад\s+НЕ\s+выводит'),
            ('docs/components/DealOrchestratorJob.md',
             r'Порядок\s+чтения\s+свойством\s+этого\s+шага'),
            ('services/trading-core/src/main/java/com/example/tradingcore/domain/safety/'
             'HoldScope.java', r'Читатель\s+у\s+соответствия\s+ОДИН'),
        ),
    },
    {
        'name': 'писатель события подъёма ступени — сама точка входа блокировки',
        # Снято кодовым заходом K19 шага 10 фазы 2 (2026-09-10). Прежняя
        # редакция называла писателем класса `HoldRaised` точку входа
        # блокировки, а та транзакции не открывает: ступень коммитилась
        # своей транзакцией, событие — третьей, уже после внешних вызовов
        # снятия риска и каскада. Что действует теперь — объявляет ключ
        # `arrived`.
        'pattern': (r'(переставляет\s+ступень,\s+а\s+он\s+здесь'
                    r'|этот\s+механизм\s+и\s+есть\s+писатель\s+класса)'),
        'arrived': r'ребр[а-яё]*\s+подъёма\s+ступени',
        'date': '2026-09-10',
        'source': 'кодовый заход K19, шаг 10 фазы 2 (закрытие Ф9 и Ф10)',
        'allowed': ('.claude/work/decision-digest.md',),
        'population': (
            ('docs/components/HoldService.md', None),
            ('docs/components/SafetyHoldCoordinator.md', None),
            ('docs/architecture/contracts.md', None),
            ('docs/rules/manual-halt.md', None),
        ),
    },
    {
        'name': 'полная реакция отдаёт исход подъёма ступени',
        # Снято тем же заходом. Возврат координатора был заведён операндом
        # писателя, стоявшего НАД переходом; писатель переехал на сам
        # переход, и читателя у возврата не осталось ни одного. Что
        # действует теперь — объявляет ключ `arrived`.
        'pattern': r'возврат\s+координатора',
        'arrived': r'[Ии]схода\s+реакция[^.]*не\s+отдаёт',
        'date': '2026-09-10',
        'source': 'кодовый заход K19, шаг 10 фазы 2 (закрытие Ф9)',
        'allowed': ('.claude/work/decision-digest.md',),
        'population': (
            ('docs/rules/manual-halt.md', None),
            ('services/trading-core/src/main/java/com/example/tradingcore/domain/safety/'
             'SafetyHoldCoordinator.java', None),
            # Третий носитель: javadoc поверхности ручной остановки называл
            # возврат координатора операндом своей мимо-исполнительской
            # ветви. В популяции захода-источника его не было, и ось 1 его
            # не показывала — форма разорвана переносом javadoc
            # (.claude/work/backlog.md §«Снятый термин через
            # javadoc-перенос: энфорсер слеп к кодовому носителю»). Строка
            # популяции держит носитель независимо от того, видит ли форму
            # ось 1.
            ('services/trading-core/src/main/java/com/example/tradingcore/domain/safety/'
             'ManualHaltService.java', None),
        ),
    },
    {
        'name': 'каскад ступени — свой писатель статуса, причину пишет только шаг прохода',
        # Снято кодовым заходом K18 шага 10 фазы 2 (2026-09-10). Прежняя
        # редакция давала каскаду собственный радиусный `update` статуса, а
        # писателем причины называла ТОЛЬКО шаг прохода — и гард ребра
        # причины требует активного статуса, который каскад уже сменил.
        # Что действует теперь — объявляет ключ `arrived`.
        'pattern': (r'(Одним\s+запросом,\s+а\s+не\s+выборкой\s+с\s+обходом'
                    r'|Этот\s+шаг\s+—\s+писатель\s+.Deal\.shutdownReason.'
                    r'|Он\s+же\s+—\s+писатель\s+причины\s+выхода)'),
        'arrived': r'Затребователей\s+у\s+ребра\s+два',
        'date': '2026-09-10',
        'source': 'кодовый заход K18, шаг 10 фазы 2 (закрытие Ф7 и Ф8)',
        'allowed': ('.claude/work/decision-digest.md',),
        'population': (
            ('docs/models/domain/aggregate/Deal.md',
             r'Каскад\s+ступени\s+отдельным\s+писателем\s+не\s+является'),
            ('docs/lifecycles/Deal.md',
             r'он\s+применяет\s+ТО\s+ЖЕ\s+ребро'),
            ('docs/components/SafetyHoldCoordinator.md',
             r'Каскад\s+идёт\s+ТЕМ\s+ЖЕ\s+ребром'),
            ('docs/components/DealOrchestratorJob.md',
             r'Затребователей\s+у\s+ребра\s+два'),
            ('docs/rules/error-handling-policy.md',
             r'Оба\s+хода\s+применяют\s+ОДНО\s+ребро'),
            ('docs/architecture/contracts.md',
             r'Рёбер\s+два,\s+а\s+затребователей\s+три'),
        ),
    },
    {
        'name': 'у класса «сделка перестала вестись штатно» ручной тропы нет',
        # Снято тем же заходом. Прежняя редакция стоя́ла на том, что обе
        # тропы присвоения причины — собственный проход; первый ход
        # энфорсмента, который запускает и держатель, довёл команду
        # пользователя до писателя класса. Что действует теперь —
        # объявляет ключ `arrived`.
        'pattern': r'[Аа]ктора\s+содержимое\s+не\s+несёт',
        'arrived': r'Актор\s+едет\s+содержимым',
        'date': '2026-09-10',
        'source': 'кодовый заход K18, шаг 10 фазы 2 (закрытие Ф8)',
        'allowed': ('.claude/work/decision-digest.md',),
        'population': (
            ('libs/domain-model/src/main/java/com/example/tradingbot/domain/event/'
             'DealShutdownInitiatedContent.java',
             r'Актор\s+едет\s+содержимым,\s+потому\s+что\s+у\s+класса\s+есть\s+ручная\s+тропа'),
            ('docs/spec/event-actor-presence.json',
             r'ручная\s+тропа\s+есть,\s+и\s+актор\s+едет\s+содержимым'),
            ('docs/rules/manual-halt.md',
             r'ручную\s+тропу\s+имеет\s+и\s+.DealShutdownInitiated.'),
        ),
    },
    {
        'name': 'разведение пары «детекция x проход сделки» посылкой о вставке',
        'pattern': r'A\s+в\s+сделки\s+только\s+ВСТАВЛЯЕТ',
        'arrived': r'точечн[а-яё]+\s+гардированн[а-яё]+\s+запрос',
        'date': '2026-09-10',
        'source': 'кодовый заход K17, шаг 10 фазы 2 (закрытие Ф6)',
        'allowed': ('.claude/work/decision-digest.md',
                    'services/trading-core/src/main/resources/application.yaml'),
        'population': (
            ('services/trading-core/src/test/java/com/example/tradingcore/'
             'SchedulerCapacityTest.java',
             r'разведена\s+переводом\s+писателей\s+существующей\s+строки'),
            ('docs/models/domain/aggregate/Deal.md',
             r'Существующая\s+строка\s+правится\s+ТОЛЬКО\s+точечным'),
            ('docs/components/DealOrchestratorJob.md',
             r'ТОЛЬКО\s+точечными\s+гардированными\s+запросами'),
        ),
    },
    {
        'name': 'признак измерителя приложением через операнд алерта',
        # Снято кодовым заходом K16 шага 10 фазы 2 (2026-09-10). Прежняя
        # редакция называла первый встреченный случай — экспортируемый
        # наблюдателю окружения ряд возраста, — и на сервисе, у которого
        # экспорта метрик не построено вовсе, давала «измерителя нет» ПО
        # ПОСТРОЕНИЮ. Производный от неё клейм стоя́л в рабочем файле
        # (позиция о пуле планировщика ядра) и снят вместе с позицией. Что
        # действует теперь — объявляет ключ `arrived`.
        'pattern': r'чей\s+исход\s+есть\s+операнд\s+алерта',
        'arrived': r'Выход\s+тика\s+есть\s+измерение\s+о\s+ЧУЖОМ\s+состоянии',
        'date': '2026-09-10',
        'source': 'кодовый заход K16, шаг 10 фазы 2',
        'allowed': ('.claude/work/decision-digest.md',),
        'population': (
            ('.claude/rules/codestyle.md',
             r'Выход\s+тика\s+есть\s+измерение\s+о\s+ЧУЖОМ\s+состоянии'),
        ),
    },
    {
        'name': 'счётчики происшествий не несут ни определения, ни расчётной валюты',
        # Снято приземлением узла 5 `GAPS_CLOSE_5` (2026-09-10). Прежняя
        # редакция довода была написана по двум классам перечня из четырёх:
        # у заведения сделки и решения о заявке идентичность определения в
        # составе содержимого объявлена, и на них довод был ложен. Что
        # действует теперь — объявляет ключ `arrived`.
        'pattern': r'не\s+несут\s+\*{0,2}ни\s+того,\s+ни\s+другого',
        'arrived': r'расчётной\s+валюты\s+не\s+несёт\s+ни\s+одно\s+из',
        'date': '2026-09-10',
        'source': 'приземление узла 5 `GAPS_CLOSE_5`, шаг 10 фазы 2',
        'allowed': (),
        'population': (
            ('docs/rules/statistics-aggregates.md',
             r'расчётной\s+валюты\s+не\s+несёт\s+ни\s+одно\s+из'),
            # У ноты спеки своя форма снятого и своя пришедшая: она говорила
            # о том же доводе словами величины, а не дома (ось 6).
            ('docs/spec/statistics-aggregates.json',
             r'результата\s+у\s+него\s+ещё\s+нет'),
        ),
    },
    {
        'name': 'потребителей у темы фактов пока нет',
        # Снято приземлением узла 5 `GAPS_CLOSE_5` (2026-09-10). Прежняя
        # редакция клейма была общей по темам, а у `strategies.facts`
        # durable-потребитель построен шагом 8 фазы 2. Что действует теперь —
        # объявляет ключ `arrived`.
        'pattern': r'потребител[а-яё]+\s+у\s+тем[ыи]\s+пока\s+нет',
        'arrived': r'Состояние\s+это\s+потемное,\s+а\s+не\s+общее',
        'date': '2026-09-10',
        'source': 'приземление узла 5 `GAPS_CLOSE_5`, шаг 10 фазы 2',
        'allowed': (),
        'population': (
            ('docs/architecture/data-ownership.md',
             r'Состояние\s+это\s+потемное,\s+а\s+не\s+общее'),
            # У якоря калибровки своя пришедшая редакция: он называет
            # оживитель по теме, а не общее состояние темы.
            ('.claude/work/backlog.md',
             r'Что\s+оживит\s+—\s+по\s+теме,\s+а\s+не\s+одним\s+событием'),
        ),
    },
    {
        'name': 'пара сетевой политики — вид стойки',
        # Снято приземлением узла 5 `GAPS_CLOSE_5` (2026-09-10). Прежняя
        # редакция перечня видов стойки называла элемент, а не конструкцию, и
        # на построенном давала оба прочтения сразу. Шаблон намеренно узкий —
        # по строке перечня: сама фраза «пара сетевой политики» законна всюду,
        # где речь о конкретной паре. Что действует теперь — ключ `arrived`.
        'pattern': r'реле,\s+тема,\s+пара\s+сетевой\s+политики',
        'arrived': r'Стойка\s+меряется\s+конструкцией,\s+а\s+не\s+её\s+элементом',
        'date': '2026-09-10',
        'source': 'приземление узла 5 `GAPS_CLOSE_5`, шаг 10 фазы 2',
        'allowed': (),
        'population': (
            ('.claude/processes/roadmap-step-execution.md',
             r'Стойка\s+меряется\s+конструкцией,\s+а\s+не\s+её\s+элементом'),
        ),
    },
    {
        'name': 'пустота колонки радиуса однозначна одной ветвью',
        # Снято приземлением узла 1 `GAPS_CLOSE_5` (2026-09-09). Прежняя
        # редакция инварианта имела одну ветвь и после оживителя состава
        # содержимого стала бы ложной: у сделки, заведённой восстановительной
        # тропой, класс определение производит, а предмет строки его не
        # имеет. Что действует теперь — объявляет ключ `arrived`.
        'pattern': r'пустота\s+здесь\s+однозначна:\s+класс\s+события\s+её\s+не\s+производит',
        'arrived': r'либо\s+предмет\s+строки\s+её\s+не\s+имеет',
        'date': '2026-09-09',
        'source': 'приземление узла 1 `GAPS_CLOSE_5`, шаг 10 фазы 2',
        'allowed': (),
        'population': (
            ('docs/models/domain/other/AuditRecord.md',
             r'либо\s+предмет\s+строки\s+её\s+не\s+имеет'),
        ),
    },
    {
        'name': 'требование контекста входа стои́т на обоих классах без различения половин',
        # Снято приземлением узла 1 `GAPS_CLOSE_5` (2026-09-09). Прежняя
        # редакция говорила «на обоих классах» о паре целиком, а перечень
        # состава терминального события в том же доме называет из пары одну
        # половину: два прочтения клаузы были одинаково правдоподобны.
        'pattern': r'не\s+только\s+на\s+терминальном',
        'arrived': r'половины\s+пары\s+на\s+них\s+разные',
        'date': '2026-09-09',
        'source': 'приземление узла 1 `GAPS_CLOSE_5`, шаг 10 фазы 2',
        'allowed': (),
        'population': (
            ('docs/architecture/contracts.md',
             r'половины\s+пары\s+на\s+них\s+разные'),
        ),
    },
    {
        'name': 'разрыв у требования о пустом значении — состояние кода, а не названное ограничение',
        # Снято приземлением узла 1 `GAPS_CLOSE_5` (2026-09-09). Прежняя
        # редакция несла из трёх обязательных величин названного ограничения
        # одну — ход, — и вдобавок ручной счёт точек публикации, у которого
        # механического вывода нет.
        'pattern': r'это\s+состояние\s+кода,\s+а\s+не\s+спор\s+о\s+правиле',
        'arrived': r'это\s+названное\s+ограничение,\s+а\s+не\s+спор\s+о\s+правиле',
        'date': '2026-09-09',
        'source': 'приземление узла 1 `GAPS_CLOSE_5`, шаг 10 фазы 2',
        'allowed': (),
        'population': (
            # Ограничение закрыто кодовым заходом K12 шага 10 фазы 2: на месте
            # названного ограничения стои́т исполнение, и шаблон носителя ведёт
            # к нему. Запись живёт дальше — она сторожит ВОЗВРАТ снятой
            # редакции, а не наличие промежуточной.
            ('docs/architecture/contracts.md',
             r'Требование\s+исполнено,\s+и\s+форма\s+его\s+исполнения\s+названа'),
        ),
    },
    {
        'name': 'осей у узла две — предметная и носительская',
        # Снято проходом по окрестности точки вставки (2026-09-09). Прежняя
        # редакция называла обязательной одну ось сверх предметной, и обе
        # перебирались по ПРАВИЛУ; текст, стоящий рядом с приземляемым, в
        # перебор не входил ни по одной из них — по построению, а не по
        # невнимательности. Что действует теперь — объявляет ключ `arrived`,
        # а не этот комментарий (`.claude/rules/self-description-form.md`).
        'pattern': r'Осей\s+у\s+узла\s+обычно\s+две',
        'arrived': r'Осей\s+у\s+корпусного\s+захода\s+три',
        'date': '2026-09-09',
        'source': 'проход по окрестности точки вставки, шаг 10 фазы 2',
        'allowed': (
            '.claude/work/decision-digest.md',
            '.claude/work/history/2026-09-11-phase-2-step-10-audit-statistics/phase-2-step-10-insertion-neighborhood-pass.md',
        ),
        'population': (
            ('.claude/skills/closure-population.md',
             r'Осей\s+у\s+корпусного\s+захода\s+три'),
            ('.claude/skills/update-roadmap-progress.md',
             r'обязательную\s+носительскую\s+и'),
            ('.claude/processes/roadmap-step-execution.md',
             r'ось\s+наравне\s+с\s+предметной\s+и\s+носительской'),
        ),
    },
    {
        'name': 'команда вывода окрестностной оси — только четыре пробы',
        # Снято проходом по предмету обязанностей полноты (2026-09-11).
        # Прежняя редакция называла командой вывода окрестностной оси одни
        # пробы, и перечень ТОЧЕК, на которых они исполняются, оставался
        # единственным членом без источника: заход брал его из находок,
        # которые закрывал. Что действует теперь — объявляет ключ `arrived`,
        # а не этот комментарий (`.claude/rules/self-description-form.md`).
        'pattern': r'команд[а-яё]*\s+вывода\s+—\s+четыре\s+пробы',
        'arrived': r'команда\s+вывода\s+\*\*двойная\*\*',
        'date': '2026-09-11',
        'source': 'проход по предмету обязанностей полноты, шаг 10 фазы 2',
        'allowed': (
            '.claude/work/decision-digest.md',
            '.claude/work/history/2026-09-11-phase-2-step-10-audit-statistics/phase-2-step-10-obligation-subject-pass.md',
        ),
        'population': (
            ('.claude/skills/closure-population.md',
             r'команда\s+вывода\s+\*\*двойная\*\*'),
            ('.claude/skills/update-roadmap-progress.md',
             r'команда\s+вывода\s+\*\*двойная\*\*'),
            ('.claude/processes/roadmap-step-execution.md',
             r'выводит\s+из\s+своей\s+дельты\s+по\s+отправной\s+метке'),
            ('.claude/rules/edit-kind-obligations.md',
             r'Предмет\s+обязанностей\s+—\s+дельта\s+захода'),
        ),
    },
    {
        'name': 'заморозка усиления измерения снята по своему условию',
        # Снято решением держателя 2026-09-08 о границах доковой петли.
        # Прежняя редакция была разовой: заморозка 2026-08-31 действовала
        # «до входа в `CODE`» с одним-единственным входом и снялась своим
        # условием 2026-09-01, когда в код вошёл шаг 7 фазы 1, — после чего
        # механизм движущейся линейки вернулся на следующем же шаге. Что
        # действует теперь — объявляет ключ `arrived`, а не этот комментарий
        # (`.claude/rules/self-description-form.md`).
        'pattern': r'заморозк[а-яё]{0,3}\s+усиления\s+измерения\s+снята\s+по\s+сво',
        'arrived': r'вооружается\s+заново\s+на\s+каждом\s+шаге',
        'date': '2026-09-08',
        'source': 'решение держателя о границах доковой петли, шаг 10 фазы 2',
        'allowed': ('.claude/work/decision-digest.md',),
        'population': (
            ('.claude/rules/docs-loop-limits.md',
             r'вооружается\s+заново\s+на\s+каждом\s+шаге'),
            ('.claude/processes/roadmap-step-execution.md',
             r'вооружается\s+заново\s+на\s+каждом\s+шаге'),
            ('.claude/decisions/population-origin-and-code-gate.md',
             r'вооружается\s+заново\s+на\s+каждом\s+шаге'),
            ('.claude/decisions/code-contact-as-gate.md',
             r'вооружается\s+заново\s+на\s+каждом\s+шаге'),
            ('.claude/work/backlog.md',
             r'вооружается\s+заново\s+на\s+каждом\s+шаге'),
        ),
    },
    {
        'name': 'формы единицы сессии — узел, прогон DOCS_CHECK, дизайн-проход',
        # Снято тем же решением. Прежний перечень форм единицы прогона гейта
        # выхода не называл, хотя сессии цепочки брали его единицей и
        # независимость требует, чтобы гейт вёл не тот, кто писал закрытие.
        # Что действует теперь — объявляет ключ `arrived`.
        'pattern': r'узел\s+.GAPS_CLOSE.,\s+(один\s+)?прогон\s+.DOCS_CHECK.\s+либо\s+(один\s+)?дизайн-проход',
        'arrived': r'прогон\s+гейта\s+выхода\s+либо\s+(один\s+)?дизайн-проход',
        'date': '2026-09-08',
        'source': 'решение держателя о границах доковой петли, шаг 10 фазы 2',
        'allowed': ('.claude/work/decision-digest.md',),
        'population': (
            ('.claude/rules/session-work-unit.md',
             r'один\s+прогон\s+гейта\s+выхода\s+либо\s+один\s+дизайн-проход'),
            ('tools/session-prompt.md',
             r'один\s+прогон\s+гейта\s+выхода\s+либо\s+один\s+дизайн-проход'),
            ('.claude/skills/session-chain.md',
             r'прогон\s+гейта\s+выхода\s+либо\s+дизайн-проход'),
        ),
    },
    {
        'name': 'разбор остановленного адресует только С5',
        # Снято тем же решением. Прежняя редакция ограничивала область
        # правила разбора одним сигналом С5, потому что второго сигнала
        # петли внутри закрытия не существовало; условие возврата,
        # записанное в самой области, адресовало С1-С4 и на новый сигнал не
        # сработало бы. Что действует теперь — объявляет ключ `arrived`.
        'pattern': r'(Правило\s+адресует\s+только\s+С5|Область\s+—\s+только\s+С5|Область\s+правила\s+—\s+только\s+С5)',
        'arrived': r'(Правило\s+адресует\s+С5\s+и\s+С6|Область\s+—\s+С5\s+и\s+С6|Область\s+правила\s+—\s+сигналы\s+петли|правилу\s+\(С5\s+и\s+С6)',
        'date': '2026-09-08',
        'source': 'решение держателя о границах доковой петли, шаг 10 фазы 2',
        'allowed': ('.claude/work/decision-digest.md',),
        'population': (
            ('.claude/rules/stopped-node-disposition.md',
             r'Правило\s+адресует\s+С5\s+и\s+С6'),
            ('.claude/skills/update-roadmap-progress.md',
             r'Область\s+—\s+С5\s+и\s+С6'),
            ('.claude/processes/roadmap-step-execution.md',
             r'Область\s+правила\s+—\s+сигналы\s+петли'),
            ('.claude/skills/stagnation-detection.md',
             r'правилу\s+\(С5\s+и\s+С6'),
        ),
    },
    {
        'name': 'заходы узла — критика, верификация, приземление',
        # Снято проходом по обязанностям полноты у правки (2026-09-08). Прежний
        # перечень называл поимённо только те заходы, что заканчиваются отчётом
        # соседа или статусом; заходы, которыми единица корпус МЕНЯЕТ, в него не
        # входили — и обязанностей полноты у них не спрашивал никто. Что пришло
        # взамен — объявляет ключ `arrived`, а не этот комментарий
        # (`.claude/rules/self-description-form.md`).
        'pattern': r'заходы\s+названы\s+поимённо:\s+раунд\s+критика',
        'arrived': r'заходы\s+названы\s+поимённо:\s+построение\s+популяции',
        'date': '2026-09-08',
        'source': 'проход по обязанностям полноты у правки, шаг 10 фазы 2',
        'allowed': ('.claude/work/decision-digest.md',),
        'population': (
            ('.claude/rules/session-work-unit.md',
             r'заходы\s+названы\s+поимённо:\s+построение\s+популяции'),
        ),
    },
    {
        'name': 'популяция строится до первой правки узла',
        # Снято проходом по обязанностям полноты у правки (2026-09-08). Прежняя
        # редакция называла моментом построения перечня первую правку УЗЛА;
        # доработка мини-петли — правка того же узла, приходящая позже, — под
        # неё не подводилась, и перечень отставал ровно на число доработок.
        # Что пришло взамен — объявляет ключ `arrived`, а не этот комментарий
        # (`.claude/rules/self-description-form.md`).
        'pattern': r'до\s+первой\s+правки\s+узла',
        'arrived': r'до\s+первой\s+правки\s+корпусного\s+захода',
        'date': '2026-09-08',
        'source': 'проход по обязанностям полноты у правки, шаг 10 фазы 2',
        'allowed': (
            '.claude/work/decision-digest.md',
            '.claude/skills/closure-population.md',
            '.claude/work/history/2026-09-11-phase-2-step-10-audit-statistics/phase-2-step-10-edit-kind-pass.md',
        ),
        'population': (
            ('.claude/processes/roadmap-step-execution.md',
             r'до\s+первой\s+правки\s+корпусного\s+захода'),
            ('.claude/skills/closure-population.md',
             r'До\s+первой\s+правки\s+каждого\s+корпусного\s+захода'),
        ),
    },
    {
        'name': 'гейт свипов не применяется, когда закрытие политики не меняло',
        # Снято тем же проходом. Прежняя редакция гейта выхода спрашивала один
        # вопрос — меняло ли закрытие политику, — и на отрицательный ответ не
        # применялась вовсе: правка, заведшая дом или введшая величину,
        # политики не меняла и мимо гейта проходила целиком. Что пришло
        # взамен — объявляет ключ `arrived`.
        'pattern': r'закрытие\s+меняло\s+политику\s+\(снимало\s+или\s+заменяло',
        'arrived': r'проставлена\s+классификация\s+по\s+родам',
        'date': '2026-09-08',
        'source': 'проход по обязанностям полноты у правки, шаг 10 фазы 2',
        'allowed': ('.claude/work/decision-digest.md',),
        'population': (
            ('.claude/skills/update-roadmap-progress.md',
             r'проставлена\s+классификация\s+по\s+родам'),
        ),
    },
    {
        'name': 'статистика — потребитель событий ядра',
        # Снято GAPS_CLOSE_4 шага 10 фазы 2 (узел 1, A6). Прежняя редакция
        # называла статистику потребителем событий в двух клаузах
        # contracts.md, тогда как её дом (services.md) и раскладка данных
        # объявляют обратное и выводят из этого несущее следствие: у события
        # ровно ОДНО следствие, и отметкой обработанного служит сама строка
        # журнала. Что пришло взамен — объявляет ключ `arrived`, а не этот
        # комментарий (`.claude/rules/self-description-form.md`).
        'pattern': r'аудит,\s+статистика,\s+`bff`'
                   r'|Потребитель\s+события\s+—\s+аудит,\s+статистика\s+и',
        'arrived': r'Статистики\s+в\s+этом\s+перечне\s+нет',
        'date': '2026-09-08',
        'source': 'GAPS_CLOSE_4 шага 10 фазы 2, узел 1 (A6)',
        'allowed': ('.claude/work/decision-digest.md',),
        'population': (
            ('docs/architecture/contracts.md', r'Статистики\s+в\s+этом\s+перечне\s+нет'),
        ),
    },
    {
        'name': 'результат до финансирования — объявленное поле терминального события',
        # Снято GAPS_CLOSE_4 шага 10 фазы 2 (узел 1, B5). Прежняя редакция
        # требовала от содержимого DealClosed поле «результат до
        # финансирования наравне с итогом» и объявляла все члены перечня
        # невыводимыми — при том, что величина равна сумме двух других членов
        # того же перечня, а её дом (loss-streak-halt.md) прямо объявляет, что
        # своего поля у неё не заводится. Что пришло взамен — объявляет ключ
        # `arrived`, а не этот комментарий: прежде здесь стоя́л пересказ, и он
        # называл пришедшей редакцию, которую верификация ОТВЕРГЛА, при верных
        # шаблонах и зелёном прогоне (`.claude/rules/self-description-form.md`).
        'pattern': r'результат\s+до\s+финансирования\s+наравне\s+с\s+итогом',
        'arrived': r'Результата\s+до\s+финансирования\s+в\s+перечне\s+нет'
                   r'|Ценовой\s+результат\s+полем\s+не\s+заводится'
                   r'|СОДЕРЖИМЫМ\s+СОБЫТИЯ\s+не\s+приезжает',
        'date': '2026-09-08',
        'source': 'GAPS_CLOSE_4 шага 10 фазы 2, узел 1 (B5)',
        'allowed': ('.claude/work/decision-digest.md',),
        'population': (
            ('docs/architecture/contracts.md', r'Результата\s+до\s+финансирования\s+в\s+перечне\s+нет'),
            ('docs/spec/statistics-aggregates.json', r'СОДЕРЖИМЫМ\s+СОБЫТИЯ\s+не\s+приезжает'),
            ('.claude/work/history/2026-09-11-phase-2-step-10-audit-statistics/phase-2-step-10-chronicle.md', r'Ценовой\s+результат\s+полем\s+не\s+заводится'),
        ),
    },
    {
        'name': 'право операции проверяет сервис на каждом вызове',
        # Снято GAPS_CLOSE_1 шага 9 фазы 2 (узел A3). Прежняя редакция
        # объявляла проверку права действующей («сервис проверяет право
        # операции по роли на каждом вызове», «права проверяются на сервере на
        # каждом вызове») — при том, что ни один сервис её не делает и
        # api-access-policy объявляет её отсутствие намеренным. Три носителя
        # читались как противоречащие. Пришедшая редакция: проверку ДЕРЖИТ
        # владелец операции, а не периметр; сегодня её нет ни у кого, и это
        # названо состоянием с условием выхода (второй субъект).
        'pattern': r'Сервис\s+проверяет\s+право\s+операции\s+по\s+роли\s+на\s+каждом\s+вызове'
                   r'|Права\s+проверяются\s+на\s+сервере\s+на\s+каждом\s+вызове',
        'arrived': r'Где\s+проверяется\s+право\s+операции'
                   r'|у\s+владельца\s+операции|сервис-владелец\s+операции',
        'date': '2026-09-06',
        'source': 'GAPS_CLOSE_1 шага 9 фазы 2, узел A3',
        'allowed': ('.claude/work/decision-digest.md',),
        'population': (
            ('docs/rules/api-access-policy.md', r'Где\s+проверяется\s+право\s+операции'),
            ('docs/architecture/contracts.md', r'сервис-владелец\s+операции'),
            ('docs/architecture/tenant-and-exchange.md', r'у\s+владельца\s+операции'),
        ),
    },
    {
        'name': 'смещение подписки сообщает клиент',
        # Снято GAPS_CLOSE_1 шага 9 фазы 2 (узел A2). Прежняя редакция отдавала
        # браузеру ВНУТРЕННЮЮ величину транспорта — смещение темы, — не
        # ограничивая её ничем: клиент становился хранителем позиции чтения и
        # мог заказать чтение истории темы одним запросом. Пришедшая редакция:
        # клиент сообщает идентичность последнего события (Last-Event-ID), а не
        # найденная идентичность даёт поток с текущего момента и явный разрыв.
        'pattern': r'смещения,\s+которое\s+сообщает\s+клиент',
        'arrived': r'Last-Event-ID|Позиция\s+чтения\s+в\s+браузер\s+не\s+уходит',
        'date': '2026-09-06',
        'source': 'GAPS_CLOSE_1 шага 9 фазы 2, узел A2',
        'allowed': ('.claude/work/decision-digest.md',),
        'population': (
            ('docs/architecture/contracts.md', r'Last-Event-ID'),
        ),
    },
    {
        'name': 'состояния bff не держит — без оговорки о кэше',
        # Снято GAPS_CLOSE_1 шага 9 фазы 2 (узел A6). Прежняя редакция
        # объявляла периметр вовсе без состояния и двумя строками ниже —
        # с кэшем членств и сбросом по событию: кэш есть состояние, и обе
        # стороны стояли одновременно. Пришедшая редакция: персистентного
        # состояния нет, кэш эфемерен и со сроком годности; направление ошибки
        # устаревшей записи названо (разрешающее), отсюда короткий срок.
        'pattern': r'Состояния\s+.bff.\s+не\s+держит',
        'arrived': r'персистентного\s+состояния|\*\*персистентного\*\*\s+состояния'
                   r'|Состояния\s+периметр\s+не\s+держит\s+—\s+persistent',
        'date': '2026-09-06',
        'source': 'GAPS_CLOSE_1 шага 9 фазы 2, узел A6',
        'allowed': ('.claude/work/decision-digest.md',),
        'population': (
            ('docs/architecture/contracts.md', r'Состояния\s+периметр\s+не\s+держит'),
            ('docs/architecture/services.md', r'\*\*персистентного\*\*\s+состояния'),
        ),
    },
    {
        'name': 'все формы к браузеру — своя api-модель периметра',
        # Снято GAPS_CLOSE_2 шага 9 фазы 2 (узел C3). Прежняя редакция строки
        # карты владельцев объявляла СВОЕЙ api-моделью периметра всё, что он
        # отдаёт браузеру, — и читалась против §«Периметр: что bff отдаёт и
        # чего не делает», где периметр ПРОКСИРУЕТ и собственных контрактов не
        # заводит. Два клейма на одной тропе предписывали противоположное:
        # завести форму на каждый проксируемый ответ либо не заводить ни одной.
        # Пришедшая редакция: форму задаёт ПРОИСХОЖДЕНИЕ — порождённое
        # периметром идёт его api-моделью, пересылаемое — формой владельца.
        'pattern': r'формы,\s+которые\s+периметр\s+отдаёт\s+браузеру',
        'arrived': r'периметр\s+\*\*порождает\*\*\s+сам'
                   r'|Форму\s+задаёт\s+происхождение',
        'date': '2026-09-06',
        'source': 'GAPS_CLOSE_2 шага 9 фазы 2, узел C3',
        'allowed': ('.claude/work/decision-digest.md',),
        'population': (
            ('.claude/rules/knowledge-ownership-by-service.md',
             r'периметр\s+\*\*порождает\*\*\s+сам'),
            ('docs/architecture/contracts.md',
             r'Форму\s+задаёт\s+происхождение'),
        ),
    },
    {
        'name': 'тенант строкой — исключение ровно одно, конверт события',
        # Снято GAPS_CLOSE_2 шага 8 фазы 2 (узел C1+C2). Прежняя редакция
        # объявляла у правила «тенант отдельным полем торговой строки не
        # заводится» РОВНО ОДНО исключение — конверт события, — и её посылка
        # («резолвится по счёту») была неявной: она исполнима только там, где
        # строка счёта локальна. У `strategies` проекции счетов нет вовсе.
        # Пришедшая редакция: признак применимости механический (держит ли
        # сервис строку счёта), случаев два — конверт и сервис без счёта.
        'pattern': r'Исключение\s+одно\s+и\s+названо:\s+\*\*конверт',
        'arrived': r'Два\s+случая,\s+где\s+тенант\s+несётся\s+строкой'
                   r'|счёт\s+резолвится\s+локально'
                   r'|Несётся\s+строкой\s+у\s+владельца\s+определений',
        'date': '2026-09-06',
        'source': 'GAPS_CLOSE_2 шага 8 фазы 2, узел C1+C2',
        'allowed': ('.claude/work/decision-digest.md',),
        'population': (
            ('docs/architecture/tenant-and-exchange.md',
             r'счёт\s+резолвится\s+локально'),
            ('docs/models/domain/aggregate/Strategy.md',
             r'Несётся\s+строкой\s+у\s+владельца\s+определений'),
        ),
    },
    {
        'name': 'поле источника у черновика стратегии — существующее сегодня',
        # Снято GAPS_CLOSE_1 шага 8 фазы 2 (узел 8). Шов советника объявлял
        # среди «что соблюдается уже сегодня» поле источника черновика;
        # такого поля не было ни в составе корня определения, ни в перечне
        # статусов, ни в доменном классе. Пришедшая редакция: источник
        # черновика различает уже существующий createdBy (два класса актора —
        # «имя принципала» и «собственный проход»), а отдельного поля не
        # заводится, пока нет советника: место резервируется швом, не
        # колонкой.
        'pattern': r'у\s+черновика\s+есть\s+поле\s+источника'
                   r'|советник\s+—\s+тем\s+же\s+вызовом\s+с\s+признаком\s+источника',
        'arrived': r'createdBy',
        'date': '2026-09-06',
        'source': 'GAPS_CLOSE_1 шага 8 фазы 2, узел 8',
        'allowed': ('.claude/work/decision-digest.md',),
        'population': (
            ('docs/architecture/reserved-extension-points.md', r'createdBy'),
            ('docs/lifecycles/Strategy.md', r'createdBy'),
        ),
    },
    {
        'name': 'состав strategy-engine — детекторы и интерпретатор, расчёт в ядре',
        # Снято GAPS_CLOSE_2 шага 7 фазы 2 (узел C3). Дом состава артефакта
        # перечислял детекторы, интерпретатор и симулированный коннектор, тогда
        # как карта владельцев отдавала библиотеке ещё пять расчётных
        # компонентов. Пришедшая редакция: расчётный слой — в библиотеке, и это
        # следствие довода, ради которого движок вынесен (бэктест и живая
        # торговля исполняют один расчёт).
        'pattern': r'детекторы,\s*интерпретатор\s+определения\s+стратегии,\s*симулированный',
        'arrived': r'расчётн\w+\s+слой'
                   r'|контекст\s+расчёта,\s*цена\s+размещения,\s*размер',
        'date': '2026-09-05',
        'source': 'GAPS_CLOSE_2 шага 7 фазы 2, узел C3',
        'allowed': ('.claude/work/decision-digest.md',),
        'population': (
            ('docs/architecture/services.md', r'расчётн\w+\s+слой'),
        ),
    },
    {
        'name': 'радиус торговой строки — инструмент и биржа, а не счёт',
        # Снято GAPS_CLOSE_1 шага 7 фазы 2 (узел 1). Монолит имел ровно одну
        # строку биржи, и она молча служила площадкой, счётом и тенантом:
        # радиусы «биржа» и «инструмент» были одним радиусом. Пришедшая
        # редакция: корневая торговая строка называет БИРЖЕВОЙ СЧЁТ, а
        # инвариант радиуса «инструмент» читается на паре «счёт × инструмент»
        # (docs/architecture/tenant-and-exchange.md §«Торговая строка называет
        # счёт, и радиусы читаются от него»).
        # Разрешённые места — ЗАМОРОЖЕННЫЕ файлы донора: он остаётся рабочим
        # монолитом до удаления (donor/README.md §«Портированная область
        # замораживается»), и форма монолита в нём законна. Файла docs/** в
        # перечне нет ни одного.
        'pattern': r'Exchange\.(riskBase|riskBaseCurrency|consecutiveLossCount|blindPassCount)'
                   r'|uk_deal_active_instrument'
                   r'|одн[аоу]\s+(активн|незакрыт)\w*\s+сделк\w*\s+на\s+инструмент'
                   r'|одн[аоу]\s+активн\w*\s+стратеги\w*\s+на\s+инструмент'
                   # Форма, проскочившая мимо шаблона до GAPS_CLOSE_1 шага 8 фазы 2:
                   # дом цикла писал инвариант ЗНАЧЕНИЕМ статуса, а не словом
                   # «стратегия», и свип его не видел.
                   r'|одн[аоу]\s+.{0,2}ACTIVE.{0,2}\s+на\s+инструмент'
                   r'|HoldScope\.EXCHANGE\b'
                   r'|\(EXCHANGE,\s*(SOFT|HARD|FREEZE|FULL)\)',
        'arrived': r'ExchangeAccount\.(riskBase|consecutiveLossCount|blindPassCount|HOLD|status)'
                   r'|uk_deal_active_account_instrument'
                   r'|exchangeAccountId'
                   r'|EXCHANGE_ACCOUNT'
                   r'|сделк\w*\s+на\s+паре\s+счёт'
                   r'|стратеги\w*\s+на\s+пару\s+«биржевой\s+счёт'
                   r'|Торговая\s+строка\s+называет\s+счёт',
        'date': '2026-09-05',
        'source': 'GAPS_CLOSE_1 шага 7 фазы 2, узел 1 (A1 + B2)',
        'allowed': (
            '.claude/work/decision-digest.md',
            '.claude/notes/2026-08-23-разрывы-спека-кода-на-тропе-живой-сделки.md',
            'donor/src/main/resources/db/migration/V9__create_deal_finalization_states.sql',
            'donor/src/main/java/com/example/tradingbot/domain/command/executor/RefreshBalanceExecutor.java',
            'donor/src/main/java/com/example/tradingbot/domain/safety/AnomalyReaction.java',
            'donor/src/main/java/com/example/tradingbot/domain/safety/DealInvariantDetectors.java',
            'donor/src/main/java/com/example/tradingbot/domain/safety/ExchangeSideDetectors.java',
            'donor/src/main/java/com/example/tradingbot/domain/safety/HoldService.java',
            'donor/src/main/java/com/example/tradingbot/domain/safety/HoldSignal.java',
            'donor/src/main/java/com/example/tradingbot/domain/safety/SafetyHoldCoordinator.java',
            'donor/src/test/java/com/example/tradingbot/domain/safety/ExchangeSideDetectorsTest.java',
            # Найдены расширением шаблона (GAPS_CLOSE_1 шага 8 фазы 2): форма
            # «одна ACTIVE на инструмент» стои́т в замороженном доноре, где
            # радиус монолита законен.
            'donor/src/main/java/com/example/tradingbot/domain/service/strategy/StrategyService.java',
            'donor/src/main/java/com/example/tradingbot/persistence/service/StrategyDataService.java',
            # Тот же замороженный донор и тот же предмет, что у двух строк
            # выше: persistence-проекция называет схему монолита. Детектор
            # его не показывал, потому что форма разорвана переносом
            # javadoc, а свип идёт по плоскому тексту, где между словами
            # остаётся звёздочка (.claude/work/backlog.md §«Снятый термин
            # через javadoc-перенос: энфорсер слеп к кодовому носителю»).
            'donor/src/main/java/com/example/tradingbot/persistence/model/strategy/'
            'StrategyEntity.java',
        ),
        'population': (
            ('docs/architecture/tenant-and-exchange.md',
             r'Торговая\s+строка\s+называет\s+счёт'),
            ('docs/models/domain/aggregate/Deal.md',
             r'uk_deal_active_account_instrument'),
            # Форма пришедшей редакции у стратегии сменилась GAPS_CLOSE_1 шага 8
            # фазы 2 (узел 1): корень определения называет счёт ИДЕНТИЧНОСТЬЮ,
            # а не числовым ключом базы ядра — числовые ключи баз границу
            # сервиса не пересекают. Счёт по-прежнему НАЗВАН, менялась только
            # его форма, поэтому запись остаётся той же.
            ('docs/models/domain/aggregate/Strategy.md',
             r'exchangeAccountInternalId'),
            ('docs/rules/strategy-validation.md',
             r'стратеги\w*\s+на\s+пару\s+«биржевой\s+счёт'),
            ('docs/lifecycles/Strategy.md', r'на\s+\*\*паре\*\*\s+счёт'),
            ('docs/components/EntryScannerJob.md',
             r'uk_deal_active_account_instrument'),
            ('docs/components/RefreshPositionExecutor.md',
             r'uk_deal_active_account_instrument'),
            ('docs/rules/loss-streak-halt.md',
             r'ExchangeAccount\.consecutiveLossCount'),
            ('docs/rules/risk-policy.md', r'ExchangeAccount\.riskBase'),
            ('docs/rules/exchange-hold.md', r'ExchangeAccount\.safetyRung'),
            ('docs/rules/manual-halt.md', r'EXCHANGE_ACCOUNT'),
            ('docs/rules/error-handling-policy.md', r'EXCHANGE_ACCOUNT'),
            ('docs/components/AnomalyJob.md', r'EXCHANGE_ACCOUNT'),
            ('docs/components/HoldService.md', r'ступень\s+счёта'),
            ('docs/components/models/HoldSignal.md', r'EXCHANGE_ACCOUNT'),
            ('docs/components/models/DealContext.md', r'биржевой\s+счёт'),
            ('docs/models/domain/other/AnomalyReport.md', r'exchangeAccountId'),
            ('docs/models/domain/other/DealCashFlow.md', r'exchangeAccountId'),
            ('docs/models/domain/other/TradeFeeRate.md', r'exchangeAccountId'),
            ('docs/models/domain/core/BalanceContainer.md', r'exchangeAccountId'),
            ('docs/spec/cash-flow-linkage.json', r'exchangeAccountId'),
            ('docs/spec/manual-halt.json', r'EXCHANGE_ACCOUNT'),
            ('docs/spec/loss-streak-halt.json', r'ExchangeAccount\.'),
            ('docs/spec/risk-limits.json', r'ExchangeAccount\.'),
        ),
    },
    {
        'name': 'owner-ключевание производных рыночных данных настройкой стратегии',
        # Снято GAPS_CLOSE_5 шага 6 фазы 2 (находка D1): результат
        # индикатора и структуры ключевался настройкой стратегии и держал
        # на неё внешний ключ. В сервисной конструкции это невыразимо по
        # двум независимым причинам: настройка живёт в базе другого
        # сервиса (FK через границу не бывает), а у фич по всему листингу
        # для детекторов советника владельца нет вовсе. Пришедшая
        # редакция — ключевание идентичностью вычисления (тип, таймфрейм,
        # канонические параметры), реестр идентичностей у market-data.
        # Имя КОЛОНКИ донора в шаблон не входит намеренно: применённая
        # миграция неизменяема и описывает прошлое, а не действующее
        # правило (.claude/rules/codestyle.md §«Применённая миграция
        # неизменяема»).
        'pattern': r'owner-ключевание'
                   r'|ключуется\s+настройкой-владельцем'
                   r'|strategyMarketStructureSettingId'
                   r'|strategyIndicatorSettingId',
        'arrived': r'идентичностью\s+вычисления'
                   r'|indicator_config_id'
                   r'|market_structure_config_id'
                   r'|толерантность\s+читателя',
        'date': '2026-09-05',
        'source': 'GAPS_CLOSE_5 шага 6 фазы 2, находка D1',
        'allowed': ('.claude/work/decision-digest.md',
                    'tools/retired-check.py',
                    # Применённая миграция донора неизменяема и описывает
                    # ПРОШЛОЕ, а не действующее правило: правка её байта
                    # роняет валидацию Flyway у того, кто миграций не
                    # трогал (.claude/rules/codestyle.md §«Применённая
                    # миграция неизменяема — включая комментарии»).
                    'donor/src/main/resources/db/migration/V5__market_data_owner_keying.sql'),
        'population': (('docs/models/domain/other/IndicatorValue.md',
                        r'идентичностью\s+вычисления'),
                       ('docs/models/domain/other/MarketStructure.md',
                        r'market_structure_config_id'),
                       ('docs/rules/market-data-freshness.md',
                        r'толерантность\s+читателя'),
                       ('docs/components/MarketDataExpirationChecker.md',
                        r'толерантность\s+читателя'),
                       ('docs/processes/market-data-calculation.md',
                        r'идентичность\s+вычисления')),
    },
    {
        'name': 'наблюдение фактов исполнения ОПРОСОМ как работа коннектора',
        # Снято закрытием шага 5 фазы 2 (находка Z1): опрос требует ПАМЯТИ
        # (отличить новое состояние от уже виденного) и РЕЕСТРА наблюдаемого
        # (какие счета и заявки живы) — ни того, ни другого у стейтлесс-
        # коннектора без базы нет по построению. Опрашивает ЯДРО своим
        # REFRESH-контуром через синхронные чтения коннектора; у коннектора
        # единственный механизм публикации — WS-слушатель, и он припаркован.
        # Клейм стоя́л в трёх рабочих носителях и в инвентаре сервисов, причём
        # обосновывался ссылкой «как донор» — а в доноре опрашивают джобы
        # ядра, не граница интеграции.
        'pattern': r'наблюдени\w*\s+фактов\s+исполнения\s*\((?:\s*|[^)]{0,20})опросом'
                   r'|факты\s+исполнения.{0,40}наблюдением\s+опросом'
                   r'|[Кк]оннектор\s+наблюдает\s+исполнение\s+\*\*опросом\*\*',
        'arrived': r'опрос\s+—\s+механизм\s+ЯДРА'
                   r'|[Оо]просом\s+коннектор\s+не\s+наблюдает'
                   r'|опрашивает\s+\*\*ядро\*\*'
                   r'|публикаци\w+\s+фактов\s+исполнения\s+по\s+WS',
        'date': '2026-09-05',
        'source': 'закрытие шага 5 фазы 2, находка Z1',
        'allowed': ('.claude/work/decision-digest.md',
                    'tools/retired-check.py'),
        'population': (('.claude/work/roadmap/phase-2.md',
                        r'опрос\s+—\s+механизм\s+ЯДРА'),
                       ('docs/architecture/services.md',
                        r'[Оо]просом\s+коннектор\s+не\s+наблюдает'),
                       ('.claude/work/backlog.md',
                        r'опросом\s+наблюдает\s+\*\*ядро\*\*')),
    },
    {
        'name': 'сторона заявки как литерал источника в поле String',
        # Снято GAPS_CLOSE_1 шага 3 фазы 2 (находка H3): Order переехал в общую
        # библиотеку domain-model, чей объявленный критерий устойчивости поле с
        # словарём одной площадки нарушает по построению. Условие возврата
        # долга («ближайший ход, трогающий модель заявки») сработало дословно.
        # Ловятся две письменные формы — тип поля и прозаический клейм долга.
        'pattern': r'`side`\s*\|\s*`String`'
                   r'|Order\.side.{0,40}литерал\s+источника'
                   r'|CreateOrderActionExecutor\.toSide',
        'arrived': r'`side`\s*\|\s*`Side`'
                   r'|доменн\w+\s+перечень\s+`?Order\.Side`?'
                   r'|[Оо]бъявленного\s+долга\s+на\s+этой\s+границе\s+больше\s+нет',
        'date': '2026-09-05',
        'source': 'GAPS_CLOSE_1 шага 3 фазы 2, находка H3 (Д612)',
        'allowed': ('.claude/work/decision-digest.md',
                    'tools/retired-check.py'),
        'population': (('docs/models/domain/core/Order.md',
                        r'`side`\s*\|\s*`Side`'),
                       ('.claude/rules/codestyle.md',
                        r'[Оо]бъявленного\s+долга\s+на\s+этой\s+границе\s+больше\s+нет'),
                       ('docs/models/mapping/Order.md',
                        r'перевод\s+словаря\s+площадки\s+в\s+доменный\s+перечень')),
    },
    {
        'name': 'синхронные вызовы «только вниз по ярусам» как правило яруса',
        # Снято GAPS_CLOSE_4 шага 2 фазы 2 (находка D1): формулировка была ШИРЕ
        # собственного довода — довод охраняет только ВОСХОДЯЩИЙ вызов, а
        # «только вниз» запрещало заодно три живых внутриярусных вызова из
        # contracts.md. Пришедшая редакция: ярус запрещает восходящее,
        # разрешение даёт поимённая строка контракта. Ловятся две письменные
        # формы — дословная и через тире-перечисление ярусов.
        # Третья форма (`вниз по ярусам` без «только») добавлена прогоном
        # DOCS_CHECK_5: свип закрытия искал дословную формулировку и не видел
        # её же без наречия — ровно тот класс, против которого стои́т
        # требование полноты письменных форм у детектора.
        'pattern': r'только\s+вниз\s+по\s+ярусам'
                   r'|вызовы\s+идут\s+только\s+вниз'
                   r'|чтением\s+вниз\s+по\s+ярусам'
                   r'|синхронно\s+вниз(?!\s+и)',
        'arrived': r'вверх\s+по\s+ярусам\s+не\s+идёт'
                   r'|[Яя]рус\s+запрещает\s+восходящее'
                   r'|не\s+идёт\s+никогда',
        'date': '2026-09-05',
        'source': 'GAPS_CLOSE_4 шага 2 фазы 2, находка D1 (Д600)',
        'allowed': ('.claude/work/decision-digest.md',
                    'tools/retired-check.py'),
        'population': (('docs/concept.md',
                        r'[Сс]инхронный\s+вызов\s+вверх\s+по\s+ярусам\s+не\s+идёт'),
                       ('docs/architecture/services.md',
                        r'[Яя]рус\s+запрещает\s+восходящее'),
                       ('docs/architecture/contracts.md',
                        r'вверх\s+по\s+ярусам\s+не\s+идёт'),
                       ('docs/architecture/platform.md',
                        r'восходящих\s+путей\s+нет\s+вовсе')),
    },
    {
        'name': 'удержание темы Kafka до подтверждения всеми группами как свойство брокера',
        # Снято GAPS_CLOSE_1 шага 2 фазы 2 (находка A3): Kafka удерживает по
        # сроку/размеру либо компактит по ключу, смещения групп на удаление не
        # влияют ни в одной конфигурации. Клейм объявлял свойством брокера то,
        # чего у брокера нет, и был невыразим в манифесте темы. Ловятся две
        # письменные формы — дословная и прозаическая («свойство брокера, а не
        # надежда на потребителя»).
        'pattern': r'без\s+автоудаления\s+до\s+подтверждения'
                   r'|до\s+подтверждения\s+всеми\s+группами'
                   r'|свойство\s+брокера,\s+а\s+не\s+надежда',
        'arrived': r'[Тт]ема\s+хранится\s+так,\s+как\s+брокер\s+умеет'
                   r'|темы\s+фактов'
                   r'|компакция\s+им\s+запрещена',
        'date': '2026-09-05',
        'source': 'GAPS_CLOSE_1 шага 2 фазы 2, находка A3 (Д591)',
        'allowed': ('.claude/work/decision-digest.md',
                    'tools/retired-check.py'),
        'population': (('docs/architecture/data-ownership.md',
                        r'[Тт]ема\s+хранится\s+так,\s+как\s+брокер\s+умеет'),),
    },
    {
        'name': 'поле `kind` как носитель природы факта отчёта',
        # Снято сверкой SYNC_DOCS_FROM_CODE шага 8: поля kind и енума Kind не
        # существовало ни на одном слое ни одного дня, а читателя, которому
        # природа нужна ПОСТФАКТУМ по данным, в корпусе нет ни одного.
        # Дискриминатор живёт на тропе писателя: journalState против journal.
        # Ловятся три письменные формы — имя поля, обе константы енума и
        # прозаический клейм «дискриминатор идемпотентности».
        'pattern': r'`kind`\s*\|\s*`Kind`'
                   r'|`kind`\s+`(?:STATE|EVENT)`'
                   r'|`?kind`?\s*=\s*`?(?:STATE|EVENT)`?'
                   r'|(?:природ\w+|дедуп\w*)\s+`?(?:STATE|EVENT)`?'
                   r'|(?:STATE|EVENT)-(?:отчёт|факт)'
                   r'|Дискриминатор\s+идемпотентности',
        'arrived': r'[Пп]рирода\s+факта\s+—\s+свойство\s+тропы'
                   r'|природа\s+—\s+(?:\*\*)?(?:состояние|происшествие)'
                   r'|природа\s+\|\s+\*\*(?:состояние|происшествие)\*\*'
                   r'|та\s+же\s+природа',
        'date': '2026-09-04',
        'source': 'SYNC_DOCS_FROM_CODE шага 8, расхождение R1',
        'allowed': ('.claude/work/decision-digest.md',
                    '.claude/work/history/2026-09-04-phase-1-step-8-anomaly-job/phase-1-step-8-closure.md',
                    '.claude/knowledge-tree.md',
                    'tools/retired-check.py'),
        'population': (('docs/models/domain/other/AnomalyReport.md',
                        r'[Пп]рирода\s+факта\s+—\s+свойство\s+тропы'),
                       ('docs/lifecycles/AnomalyReport.md',
                        r'[Пп]рироду\s+несёт\s+тропа'),
                       ('docs/rules/manual-halt.md',
                        r'природа\s+—\s+\*\*(?:состояние|происшествие)\*\*'),
                       ('docs/rules/instrument-hold.md',
                        r'природа\s+—\s+состояние'),
                       ('docs/models/mapping/DealCashFlow.md',
                        r'природа\s+\|\s+\*\*состояние\*\*'),
                       ('docs/components/RefreshBalanceExecutor.md',
                        r'природа\s+\|\s+\*\*состояние\*\*'),
                       ('docs/components/MarkDealClosedExecutor.md',
                        r'природа\s+—\s+происшествие'),
                       ('docs/components/MarkDealEmergencyClosedExecutor.md',
                        r'природа\s+—\s+происшествие'),
),
    },
    {
        'name': 'перечень детекторов построен на один из тринадцати',
        # Снято кодовой фазой шага 8: построены одиннадцать детекторов из
        # двенадцати живых, кода нет только у A13 — она форвард шага
        # сопровождения, и это объявлено в её собственном разборе.
        'pattern': r'кода\s+ниже\s+—\s+один\s+детектор'
                   r'|построен\s+один\s+детектор\s+из\s+тринадцати',
        'arrived': r'[Пп]еречень\s+построен'
                   r'|одиннадцать\s+детекторов',
        'date': '2026-09-03',
        'source': 'кодовая фаза шага 8, заходы CODE_1..CODE_5',
        'allowed': ('.claude/work/decision-digest.md',
                    '.claude/knowledge-tree.md',
                    'tools/retired-check.py'),
        'population': (('docs/components/AnomalyJob.md', None),
                       ('donor/src/main/java/com/example/tradingbot/domain/jobs/AnomalyJob.java',
                        r'одиннадцать\s+детекторов')),
    },
    {
        'name': 'мягкая биржевая ступень не построена',
        # Снято постройкой Т11: пара (EXCHANGE, SOFT) поднимает Exchange.HOLD,
        # фабрика exchangeSoft заведена, снятие сворачивания стало двухходовым.
        # Ловятся три письменные формы довода: «сюда не приходит» у исполнителя,
        # «исполнения не имеет вовсе» у поверхности и «фабрику имеет, а
        # исполнения — нет» у сигнала.
        'pattern': r'[Мм]ягк\w+\s+биржев\w+\s+ступень[^.\n]{0,60}(?:не\s+постро|сюда\s+не\s+приход)'
                   r'|исполнени\w+\s+не\s+имеет\s+вовсе'
                   r'|фабрику\s+имеет,\s+а\s+исполнения\s+—\s+нет',
        'arrived': r'[Мм]ягк\w+\s+ступень\s+исполняется\s+на\s+обоих\s+радиусах'
                   r'|[Ии]сполнитель\s+у\s+этой\s+пары\s+есть'
                   r'|пару\s+`?\(EXCHANGE(?:_ACCOUNT)?,\s*SOFT\)`?\s+поднимает'
                   r'|[Пп]ара\s+`?\(EXCHANGE(?:_ACCOUNT)?,\s*SOFT\)`?\s+исполняется',
        'date': '2026-09-03',
        'source': 'закрытие Т11 при выводе шага 8 в CODE',
        'allowed': ('.claude/work/decision-digest.md',
                    '.claude/knowledge-tree.md',
                    'tools/retired-check.py'),
        'population': (('docs/components/HoldService.md', None),
                       ('docs/components/models/HoldSignal.md',
                        r'[Пп]ара\s+`?\(EXCHANGE(?:_ACCOUNT)?,\s*SOFT\)`?\s+исполняется'),
                       ('docs/rules/manual-halt.md',
                        r'пару\s+`?\(EXCHANGE(?:_ACCOUNT)?,\s*SOFT\)`?\s+поднимает'),
                       ('docs/components/AnomalyJob.md',
                        r'[Ии]сполнитель\s+у\s+этой\s+пары\s+есть')),
    },
    {
        'name': 'перечень детекторов неполон, потому что реакцию корпус не выбрал',
        # Снято на открытии шага 8: ступень и радиус каждого детектора
        # ВЫВОДЯТСЯ тремя ратифицированными осями (уровень — «чему нельзя
        # доверять», радиус — ущерб, ступень — судьба принятого риска), а
        # такт и гистерезис объявлены домом перечня. Неполнота осталась
        # кодовой. Ловятся обе письменные формы довода — «реакцию корпус
        # не выбрал» и «у каждого свой такт, свой гистерезис и своя
        # классификация» как ОСНОВАНИЕ не заводить перечень.
        'pattern': r'значило\s+бы\s+назначить\s+реакцию,\s+котор\w+\s+корпус'
                   r'|перечень\s+детекторов\s+неполон\b(?![^.\n]{0,40}код)',
        'arrived': r'ступень\s+и\s+радиус\s+(?:у\s+кажд\w+\s+)?выводятся'
                   r'|перечень\s+объявлен\s+целиком'
                   r'|неполнота\s+КОДОВАЯ|[Нн]еполнота\s+остал\w+\s+\*\*кодовой',
        'date': '2026-09-03',
        'source': 'открытие шага 8 фазы 1, DOCS_CHECK_1',
        'allowed': ('.claude/work/decision-digest.md',
                    '.claude/knowledge-tree.md',
                    'tools/retired-check.py'),
        'population': (('docs/components/AnomalyJob.md', None),
                       ('donor/src/main/java/com/example/tradingbot/domain/jobs/AnomalyJob.java',
                        r'ВЫВОДЯТСЯ\s+тремя\s+ратифицированными')),
    },
    {
        'name': 'ReactionClass как построенный енум ступени сигнала',
        # Снято при закрытии шага 7 фокусом `divergence`: в системе ступень
        # выражена HoldRung с ДВУМЯ значениями (SOFT/HARD), поля
        # `reactionClass` у сигнала нет, а перечень фабрик — пять, не четыре.
        # Ловятся ДВЕ письменные формы: строка структуры с полем сигнала и
        # клейм полноты «перечень фабрик … их четыре».
        'pattern': r'`reactionClass`\s*\|\s*`ReactionClass`'
                   r'|[Пп]еречень\s+фабрик[^.\n]{0,80}[Ии]х\s+четыре',
        # Проектное имя ReactionClass/FREEZE остаётся ЗАКОННЫМ там, где
        # описывается непостроенная поверхность ручной остановки: клауза
        # отображения на HoldRung стои́т в её доме.
        'unless': r'HoldRung|проектн\w+\s+имен\w+|в\s+код\s+не\s+(?:вошёл|вошел)',
        'arrived': r'`rung`\s*\|\s*`HoldRung`'
                   r'|`?HoldRung`?\s+с\s+двумя\s+значениями'
                   r'|`?ReactionClass`?[^.\n]{0,90}в\s+код\s+не\s+(?:вошёл|вошел)',
        'date': '2026-09-03',
        'source': 'фокус divergence при закрытии шага 7 фазы 1',
        'allowed': ('.claude/work/decision-digest.md',
                    '.claude/knowledge-tree.md'),
        'population': (('docs/components/models/HoldSignal.md', None),
                       ('docs/rules/manual-halt.md',
                        r'в\s+код\s+не\s+(?:вошёл|вошел)')),
    },
    {
        'name': 'невозрастающая база риска',
        'pattern': r'невозраста\w+\s*(?:баз\w*)?|база\s+риска\s+не\s+(?:растёт|растет|возрастает)'
                   r'|риск-баз\w*\s+не\s+возрастает|база\s+расти\s+сама\s+не\s+должна'
                   r'|вверх\s+баз\w*\s+автоматически\s+не\s+ходит',
        'arrived': r'следует\s+за\s+(?:свободным\s+остатком|балансом)\s+в\s+обе\s+стороны'
                   r'|в\s+обе\s+стороны',
        'date': '2026-08-30',
        'source': 'решение держателя: база риска следует за балансом в обе стороны',
        'allowed': ('.claude/decisions/risk-base-follows-balance.md',
                    '.claude/work/decision-digest.md',
                    '.claude/knowledge-tree.md'),
        # Форма «невозраста-ЕМОСТЬ» шаблоном «невозраста-ЮЩ» не ловилась, и
        # носитель-исполнитель в популяции не стоя́л вовсе: зелёный прогон
        # означал «форм из перечня нет», а не «снятой редакции нет».
        # Кодовый носитель добавлен закрытием шага 10 фазы 2: javadoc пробы
        # приземления снимка обосновывал её снятой формулировкой —
        # поведение пробы верно, а довод утверждал снятое. Ось 1 его не
        # показывала: форма разорвана переносом javadoc
        # (.claude/work/backlog.md §«Снятый термин через javadoc-перенос:
        # энфорсер слеп к кодовому носителю»).
        'population': (('docs/rules/risk-policy.md', None),
                       ('docs/models/domain/core/Exchange.md', None),
                       ('docs/spec/risk-limits.json',
                        r'вверх\s+автоматически\s+не\s+ходит'),
                       ('docs/components/RefreshBalanceExecutor.md',
                        r'решает\s+её\s+дом|risk-base-follows-balance'),
                       ('services/trading-core/src/test/java/com/example/tradingcore/'
                        'BalanceRefreshTest.java', None)),
    },
    {
        'name': 'закрывающая сила исхода OBSERVED_ABSENT',
        # Шаблон ПОРЯДКО-НЕЗАВИСИМ: снятая редакция встретилась формой
        # «глагол закрытия ПЕРЕД термином» («слоты закрыты … либо исходом
        # OBSERVED_ABSENT»), которой прежний порядок слов не видел.
        # Шаблон ПОРЯДКО-НЕЗАВИСИМ: снятая редакция встречается и формой
        # «глагол закрытия ПЕРЕД термином». Отрицание снимается ключом
        # `unless` — действующая редакция («слот не закрывает»,
        # «закрывающей силы у него нет») снятой не является, и гасить её
        # allow-листом значило бы использовать allow-лист подавителем.
        'pattern': r'закрыт\w*[^.\n]{0,90}OBSERVED_ABSENT'
                   r'|OBSERVED_ABSENT[^.\n]{0,90}закрыва\w+',
        'unless': r'не\s+закрыва|закрывающ\w+\s+сил\w+|не\s+наблюдение\s+факта'
                  r'|неприменим|гейт\s+не\s+закрыт',
        'arrived': r'OBSERVED_ABSENT[^.\n]{0,120}(?:не\s+закрыва|закрывающей\s+силы\s+не)'
                   r'|закрывающ\w+\s+сил\w+\s+(?:исхода\s+)?«?не\s+наступило»?\s+снят\w*'
                   r'|исходом\s+«не\s+наступило»\s+не\s+закрыва\w+',
        'date': '2026-08-30',
        'source': 'решение держателя: гейт исходом «не наступило» не закрывается',
        # Носители пайплайна, где снятая редакция названа затем, чтобы
        # сказать, что её больше нет: скилл-гейт (действующая редакция),
        # дайджест решений (запись самого снятия) и план контура (хроника
        # двух снятий подряд).
        # Разрешённые места — носители пайплайна, где снятая редакция названа
        # затем, чтобы сказать, чем заменена: дайджест фиксирует само снятие,
        # план контура ведёт хронику двух снятий подряд. Дом ДЕЙСТВУЮЩЕЙ
        # редакции (скилл-гейт) сюда не входит: его гасит `unless`, а не
        # allow-лист.
        'allowed': ('.claude/work/decision-digest.md',
                    '.claude/tests/source-api/okx/plan.md'),
        # На месте снятого встало РАЗНОЕ: скилл-гейт держит действующую
        # редакцию дословно; канон процесса перечень способов закрытия
        # СНЯЛ и делегировал реестру; дом-реестр формулирует своими
        # словами. Отсюда свой шаблон у двух носителей из трёх.
        'population': (('.claude/skills/update-roadmap-progress.md', None),
                       ('.claude/processes/roadmap-step-execution.md',
                        r'Чем\s+именно\s+слот\s+закрывается\s+—\s+предмет\s+реестра'),
                       ('.claude/tests/source-api/okx/code-preconditions.md',
                        r'закрывающей\s+силы\s+у\s+него\s+нет')),
    },
    {
        'name': 'величина-теорема с ключом provenBy',
        'pattern': r'provenBy|величин\w*-теорем\w*|косвенн\w+\s+проб\w+',
        'arrived': r'unreachable',
        'date': '2026-08-30',
        'source': 'дизайн-проход приёмки: класс упразднён, охранный инвариант '
                  'доказывается предъявленным контрпримером',
        'allowed': ('.claude/decisions/acceptance-by-measurement.md',
                    '.claude/work/decision-digest.md',
                    '.claude/processes/roadmap-step-execution.md',
                    'donor/src/test/java/com/example/tradingbot/spec/Spec.java',
                    'donor/src/test/java/com/example/tradingbot/spec/SpecMutation.java',
                    'tools/retired-check.py'),
        # Популяция добыта по состоянию корпуса ДО снятия (git show HEAD:<файл>
        # | grep -c provenBy): пять спек и одно правило. На месте снятого встало
        # РАЗНОЕ, поэтому у половины носителей свой шаблон: где инвариант ложен
        # на достижимом состоянии — контрпример `unreachable`; где он оказался
        # переобъявлением — нота величины-дома с носителями клейма.
        'population': (('.claude/rules/structure.md', None),
                       ('docs/spec/protection-coverage.json', None),
                       ('docs/spec/loss-streak-halt.json', r'носител\w*\s+клейма'),
                       ('docs/spec/manual-halt.json', r'носител\w*\s+клейма'),
                       ('docs/spec/order-sizing.json', r'носител\w*\s+клейма'),
                       ('docs/spec/strategy-walkthrough.json', r'носител\w*\s+клейма')),
    },
    {
        'name': 'дедуп STATE-отчёта поиском незавершённого',
        # Шаблон берёт УТВЕРДИТЕЛЬНУЮ форму: «дедуп/идемпотентность держится
        # незавершённым», «ключ/индекс поиска незавершённого». Отрицание
        # («индекс незавершённых строк ей не нужен») снятой редакцией не
        # является и в шаблон не попадает.
        'pattern': r'поиска?\s+незавершённого'
                   r'|держится\s+незавершённым\s+статусом'
                   r'|индекс\s+незавершённых\s+`?STATE`?',
        'arrived': r'стоящ\w+\s+состояни\w+\s+объекта|состояни\w+\s+ОБЪЕКТА|ключу\s+состояния',
        'date': '2026-08-31',
        'source': 'закрытие GAPS_CLOSE_31: операнд дедупа STATE — стоящее '
                  'состояние объекта радиуса, а не статус отчёта',
        # Разрешённых мест нет: оба дома выражают ДЕЙСТВУЮЩУЮ редакцию, а
        # шаблон снятой в них не встречается. Файл docs/** в allowed не
        # попадает никогда — это проверяет базовый гейт ниже.
        'allowed': (),
        'population': (('docs/models/domain/other/AnomalyReport.md', None),
                       ('docs/rules/error-handling-policy.md', None),
                       ('docs/rules/loss-streak-halt.md', r'дедуп\w*\s+по\s+стоящему\s+состоянию'),
                       ('docs/rules/manual-halt.md', None),
                       ('docs/spec/manual-halt.json', None),
                       ('.claude/work/questions/open-questions.md', None)),
    },
    {
        'name': 'эпизод адресуется одним externalId',
        'pattern': r'наблюдение\s+того\s+же\s+идентификатора\s+обновляет\s+свою\s+строку'
                   r'|эпизод\w*\s+адресу\w+\s+одним\s+`?externalId`?',
        'arrived': r'externalCreatedAt',
        'date': '2026-08-30',
        'source': 'закрытие: адресуемая единица эпизода — пара externalId + externalCreatedAt',
        'allowed': (),
        'population': (('docs/models/domain/core/Position.md', None),
                       ('docs/models/mapping/Position.md', None),
                       ('docs/lifecycles/Position.md', None)),
    },
    {
        'name': 'величина-счётчик покрытия популяции',
        # Механизм такой величины не производит и никогда не производил:
        # покрытие считает раннер по ключу populations. Предписание жило в
        # ТРЁХ носителях сразу и было неисполнимо в каждом.
        'pattern': r'величин\w*-счётчик\w*\s+покрытия|счётчик\w*\s+покрытия',
        'arrived': r'ключ\w*\s+`?populations`?|`rule`|проверк\w+\s+правила\s+на\s+нём',
        'date': '2026-08-31',
        'source': 'решение держателя: происхождение перечня и критерий покрытия '
                  '(.claude/decisions/population-origin-and-code-gate.md)',
        'allowed': ('.claude/decisions/population-origin-and-code-gate.md',
                    '.claude/work/decision-digest.md'),
        'population': (('.claude/processes/roadmap-step-execution.md', None),
                       ('.claude/skills/closure-population.md', None),
                       ('.claude/skills/update-roadmap-progress.md', None),
                       ('.claude/decisions/closure-completeness-by-population.md', None)),
    },
    {
        'name': 'гейт CODE — чистый DOCS_CHECK',
        # Утвердительная форма: «гейт CODE — чистый прогон», «прогон прошёл
        # чисто ⇒ статус CODE». Действующая редакция говорит о КЛАССАХ
        # находок и о нуле незакрытых в двух из них.
        'pattern': r'[Гг]ейт\s+`?CODE`?\s*[—:-]\s*чист\w+'
                   r'|чист\w+\s+`?DOCS_CHECK`?[^.\n]{0,40}(?:услови\w+|гейт)'
                   r'|до\s+чистого\s+прогона\s+статус\s+`?CODE`?',
        'arrived': r'классы?\s+«?`?КОД`?»?\s+и\s+«?`?РИСК`?»?|code-gate-check|в\s+ноль\s+незакрытых',
        'date': '2026-08-31',
        'source': 'решение держателя: исполнимый критерий выхода в CODE по классам '
                  '(.claude/decisions/population-origin-and-code-gate.md)',
        'allowed': ('.claude/decisions/population-origin-and-code-gate.md',
                    '.claude/work/decision-digest.md'),
        'population': (('.claude/processes/roadmap-step-execution.md',
                        r'в\s+ноль\s+незакрытых'),
                       ('.claude/skills/update-roadmap-progress.md',
                        r'code-gate-check'),
                       ('.claude/skills/classify-code-blocking.md', None)),
    },
    {
        'name': 'пятый источник цикла добычи',
        # Закрытие развело добычу заявки и добычу материализованной защиты на
        # ДВА цикла; редакция «пятая нога первого» жила в трёх носителях, а
        # счёт «пять источников» стоя́л над таблицей из четырёх строк.
        'pattern': r'пят\w+\s+(?:источник\w*|ног\w+)\s+цикла\s+добычи|пять\s+источников',
        'unless': r'не\s+пят\w+\s+ног\w+|а\s+не\s+пятая',
        'arrived': r'цикл\w*\s+добычи\s+материализованной\s+защиты|четыре\s+источника|второй\s+цикл',
        'date': '2026-08-31',
        'source': 'закрытие GAPS_CLOSE_32: цикл добычи материализованной защиты — '
                  'второй цикл, а не пятая нога первого',
        'allowed': ('.claude/work/decision-digest.md',),
        'population': (('docs/models/mapping/Order.md', None),
                       ('docs/components/AttachedAlgoOrderStateResolver.md', None),
                       ('docs/models/integrations/okx/AlgoOrderOkxResponse.md', None)),
    },
    {
        'name': 'пункт 10 реестра предусловий гейтит CODE',
        # Гейтящий статус снят решением держателя 2026-08-30: эпизод ADL
        # инициирует биржа, на demo он не заказуем. Слот выведен из-под гейта
        # и строки в таблице реестра не имеет.
        'pattern': r'п\.\s*10[^.\n]{0,60}(?:гейт\w+|предуслови\w+)\s+`?CODE`?'
                   r'|предусловие\s+`?CODE`?\s+п\.\s*10'
                   r'|пп\.\s*[\d,\s]*\b10\b[\d,\s]*\)',
        'arrived': r'выведен\w*\s+из-под\s+гейта|гейтящ\w+\s+статус\s+снят',
        'date': '2026-08-30',
        'source': 'решение держателя: неупорядочиваемый факт закрывается заменителями '
                  '(.claude/decisions/unorderable-fact-substitutes.md)',
        'allowed': ('.claude/decisions/unorderable-fact-substitutes.md',
                    '.claude/work/decision-digest.md'),
        'population': (('.claude/tests/source-api/okx/plan.md',
                        r'выведен\w*\s+из-под\s+гейта'),
                       ('.claude/tests/source-api/okx/coverage-manifest.md',
                        r'выведен\w*\s+из-под\s+гейта'),
                       ('.claude/tests/source-api/okx/code-preconditions.md', None)),
    },
    {
        'name': 'Postman-коллекция — обязательный аппрув-артефакт контура',
        # Утвердительная форма: аппрув на ПАРУ, ревью плана И коллекции,
        # коллекция как ревью/аппрув-артефакт. Действующая редакция:
        # аппрув-артефакт — план, коллекция необязательна.
        'pattern': r'пар\w+\s+«?план\s*\+\s*коллекц\w+'
                   r'|план\s*\+\s*коллекци\w+\s+(?:прошли|уход\w+|аппрув\w+)'
                   r'|коллекци\w+\s+—\s+ревью/аппрув-\s*артефакт'
                   r'|аппрув\w*\s+плана\s*\+\s*коллекции',
        'arrived': r'аппрув-артефакт\w*\s+(?:контура\s+)?—\s+план|коллекци\w+\s+необязательн\w+'
                   r'|аппрув-артефактом[^.\n]{0,24}не\s+(?:являетс|служ)\w+'
                   r'|необязательн\w+\s+справк\w+',
        'date': '2026-08-31',
        'source': 'решение держателя: коллекция не аппрув-артефакт '
                  '(.claude/decisions/collection-not-approval-artifact.md)',
        'allowed': ('.claude/decisions/collection-not-approval-artifact.md',
                    '.claude/work/decision-digest.md'),
        # Реестра предусловий в популяции НЕ БЫЛО, и он единственный нёс
        # роль коллекции в клейме ИЗМЕРЕНИЯ («ось 3 грепает и по коллекции»),
        # а не в клейме аппрува: свип по форме снятой редакции такую фразу не
        # видит, и прогон был зелен при ложном клейме дома реестра
        # (F5а DOCS_CHECK_34).
        'population': (('.claude/processes/source-api-testing.md', None),
                       ('.claude/skills/test-collection.md', None),
                       ('.claude/skills/test-review.md', None),
                       ('.claude/skills/test-code.md', None),
                       ('.claude/skills/test-design.md', None),
                       ('.claude/agents/tester.md', None),
                       ('.claude/tests/source-api/okx/plan.md', None),
                       ('.claude/tests/source-api/okx/code-preconditions.md', None)),
    },
    {
        'name': 'две ноги истории цикла добычи защиты',
        # Перечень ног был собран из пары state, которые имелись в виду,
        # а не из контракта эндпоинта, у которого терминальных state ТРИ:
        # order_failed не опрашивался, разбор «исчерпывался» на
        # существующем факте (сработала и не исполнилась). Встречная
        # форма «идёт двумя вызовами» у самостоятельной условной заявки
        # (mapping/AlgoOrder.md) законна, потому что третий терминал там
        # предъявляет точечная нога details, — негативный просмотр
        # пропускает носителя, называющего order_failed рядом.
        'pattern': r'ног\s+у\s+истории\s+две'
                   r'|истори\w+\s+ид[её]т\s+двумя\s+вызовами(?!.{0,400}order_failed)',
        'arrived': r'order_failed',
        'date': '2026-09-01',
        'source': 'C2 DOCS_CHECK_33: перечень ног выводится из контракта '
                  'эндпоинта (терминалов три), узел 1 GAPS_CLOSE_33',
        'allowed': ('.claude/work/code-gate-ledger.json',),
        'population': (('docs/models/mapping/Order.md', None),
                       ('docs/models/mapping/AlgoOrder.md', None),
                       ('docs/lifecycles/Order.md', None),
                       ('docs/components/RefreshOrderExecutor.md', None)),
    },
    {
        'name': 'суррогат границы — биржевой момент создания сделки',
        # Снятая редакция: нижняя граница окна (линковки движений и
        # адресации истории позиций) при пустой колонке — «биржевой
        # момент создания сделки» БЕЗ оговорки тропы. Пришедшая: суррогат
        # берётся значением externalCreatedAt, куда восстановительная
        # тропа пишет биржевое время ОТКРЫТИЯ наблюдённой позиции
        # (решение держателя 2026-09-01). Фраза «биржевой момент создания
        # сделки» сама по себе законна у штатной тропы — шаблон ловит
        # только связку с пустой колонкой/суррогатом в снятом порядке.
        'pattern': r'пуст\w+\s+колонк\w+[^.]{0,80}биржевы?м?\s+момент\w*\s+создания\s+сделки'
                   r'|суррогат[^.]{0,60}биржев\w+\s+момент\w*\s+создания\s+сделки',
        'arrived': r'открыти[яе]\s+наблюдённой\s+позиции|ОТКРЫТИЯ\s+наблюдённой',
        'date': '2026-09-01',
        'source': 'решение держателя: граница — время открытия наблюдённой позиции '
                  '(.claude/decisions/recovered-deal-linkage-window-bound.md)',
        'allowed': ('.claude/decisions/recovered-deal-linkage-window-bound.md',
                    '.claude/work/decision-digest.md',
                    '.claude/work/code-gate-ledger.json'),
        # Дом адресуемой единицы эпизода в популяции НЕ СТОЯ́Л, и это была
        # ровно та дыра, которую ось 4 закрывает: он выражал снятую редакцию
        # ДРУГИМИ СЛОВАМИ («операнда нижней границы нет — адресация на
        # восстановительной подтропе не определена»), поэтому оси 1-3 его не
        # видели, а прогон был зелен при доме, противоречащем трём
        # потребителям (A1/A2 DOCS_CHECK_34). Свой шаблон пришедшей у него
        # потому, что дом деталей истину стыка не пересказывает, а
        # ссылается на дом окна (`.claude/rules/carrier-levels.md`).
        'population': (('docs/models/domain/aggregate/Deal.md', None),
                       ('docs/components/DealOpeningService.md', None),
                       ('docs/components/RefreshBillsExecutor.md', None),
                       ('docs/components/RefreshPositionExecutor.md', None),
                       ('docs/lifecycles/Deal.md', None),
                       ('docs/components/SubmitOrderExecutor.md', None),
                       ('docs/integrations/okx/contracts/server-time.md', None),
                       ('docs/models/domain/core/Position.md',
                        r'подтроп\w+\s+исключением\s+не\s+явля'),
                       ('docs/spec/cash-flow-linkage.json', None)),
    },
    {
        'name': 'верхняя граница окна линковки — поле сделки',
        # Снятая редакция: поле сделки объявлено ВЕРХНЕЙ ГРАНИЦЕЙ окна
        # линковки движений. Дом предиката (docs/spec/cash-flow-linkage.json
        # §inWindow) держит «окно открыто сверху до времени источника
        # прохода», и поле в предикате не участвует вовсе; реализация по
        # снятому имени не прилинковала бы у ЖИВОЙ сделки ни одного движения
        # (поле у неё пусто). Пришедшая: поле несёт ПОРОГ ДОКАЗАННОГО
        # ПОКРЫТИЯ и переименовано под свою роль — coverageProvenThrough,
        # потребитель один (обязанность сверки, duty.coverageProven).
        'pattern': r'billsWindowEnd|bills_window_end|windowClosed'
                   r'|верхн\w+\s+границ\w+\s+окна\s+линковки',
        'arrived': r'порог\w*\s+доказанн\w+\s+покрыти\w*|доказанного\s+покрытия'
                   r'|coverageProvenThrough|coverage_proven_through|coverageProven',
        'date': '2026-09-01',
        'source': 'A3 DOCS_CHECK_34: поле не участвует в предикате линковки, '
                  'кодовый заход 5 шага 7',
        'allowed': ('.claude/work/decision-digest.md',
                    '.claude/work/code-gate-ledger.json',
                    'tools/retired-check.py'),
        'population': (('docs/models/domain/aggregate/Deal.md', None),
                       ('docs/components/RefreshPositionExecutor.md', None),
                       ('docs/models/domain/core/Position.md', None),
                       ('docs/lifecycles/Position.md', None),
                       ('docs/models/mapping/PositionCloseResult.md', None),
                       ('docs/rules/pnl-reconciliation.md', None),
                       ('docs/spec/pnl-reconciliation.json', None),),
    },
    {
        'name': 'радиус шире инструмента — набором строк',
        # Встречная форма снятой редакции «групповой И БИРЖЕВОЙ радиусы
        # выражаются набором строк»: «шире инструмента» покрывает и
        # биржу, для которой клауза ложна — у неё своё значение
        # HoldScope.EXCHANGE, своя строка статуса и своя строка отчёта
        # (а на ручной поверхности — один вызов, не набор). Пришедшая
        # редакция разводит: набором строк/вызовов — только групповой,
        # биржевой — своим значением, своей строкой, одним вызовом.
        # Шаблон ловит и форму «набор вызовов» — четвёртый носитель нашла
        # мини-петля после свипа по «набором строк».
        'pattern': r'радиус\w*\s+шире\s+инструмента\s+выража\w+\s+набором'
                   r'|шире\s+инструмента\s+—\s+набор\w*\s+(строк|вызовов)',
        'arrived': r'групповой\s+радиус[^.]{0,120}набор\w*\s+(строк|вызовов)'
                   r'|(?:биржев\w+|счётн\w+)\s+радиус\s+сюда\s+не\s+относится',
        'date': '2026-09-01',
        'source': 'B1 DOCS_CHECK_33: встречная форма снятой редакции, '
                  'узел 5 GAPS_CLOSE_33',
        'allowed': ('.claude/work/code-gate-ledger.json',),
        'population': (('docs/rules/error-handling-policy.md', None),
                       ('docs/rules/instrument-hold.md', None),
                       ('docs/components/models/HoldSignal.md', None),
                       ('docs/models/domain/other/AnomalyReport.md', None),
                       ('docs/rules/manual-halt.md', None)),
    },
    {
        'name': 'после FAILED новая строка законна безусловно',
        # Снятая редакция: отказавшая надобность повторяется новой строкой
        # без гейта («новая строка законна») — петля «N попыток -> FAILED ->
        # новая строка» без предела. Пришедшая: повтор гейтится стоящей
        # ступенью радиуса и возобновляется её снятием.
        # Ограничение негативного просмотра объявлено: возрождение,
        # упоминающее «ступень»/«гейт» в радиусе 200 символов в ЛЮБОМ
        # качестве («…законна, ступень ни при чём»), шаблон пропустит —
        # его держит популяция носителей (пришедшая редакция обязана
        # стоять в каждом), как у записей «двух ног» и суррогата.
        'pattern': r'нов\w+\s+строк\w+\s+законн(?!.{0,200}(ступен|гейт))',
        'arrived': r'гейт\w*\s+по\s+стоящей\s+ступени|стоящ\w+\s+ступень\w*|stepRetryGated'
                   r'|после\s+снятия\s+ступени',
        'date': '2026-09-01',
        'source': 'B3 DOCS_CHECK_33: судьба надобности после FAILED — гейт '
                  'по стоящей ступени, узел 5 GAPS_CLOSE_33',
        'allowed': ('.claude/work/code-gate-ledger.json',),
        'population': (('docs/rules/strategy-step-once-per-episode.md', None),
                       ('docs/components/StrategyActionOrchestrator.md', None),
                       ('docs/rules/instrument-hold.md',
                        r'стоящ\w+\s+ступень\w*|гейт\w*\s+по\s+стоящей'),
                       ('docs/lifecycles/DealActionState.md', None),
                       # Носитель, который отказ и ЛОВИТ. В популяцию не входил,
                       # и свип его не видел: ячейка выделенного обработчика
                       # называла оба класса броска одной развязкой «уводит
                       # сделку ошибочной тропой» (B5 DOCS_CHECK_34).
                       ('docs/components/DealOrchestratorJob.md',
                        r'стоящ\w+\s+ступень\w*|остаётся\s+в\s+своём\s+статусе'),
                       ('docs/spec/strategy-walkthrough.json', None)),
    },
    {
        'name': 'популяция предиката стороны — только после входа',
        # Снятая редакция: primaryStopOnLossSide охраняет «первичную
        # защитную заявку ПОСЛЕ входа», а «на входной тропе первым
        # отказывает сайзинг» безусловно; рубежи «охраняют РАЗНЫЕ
        # популяции». В полосе между якорем и безубытком (ширина
        # ~2f*P/(1-f)) знаменатель положителен, сайзинг проходит, и
        # исключённый из популяции вход не охранял никто. Пришедшая:
        # популяция предиката — вся первичная постановка с объявленным
        # уровнем, вход включая; рубежи различаются предметом.
        # Широта шаблона объявлена: альтернации — родовые фразы, и
        # будущий законный текст о ДРУГОМ предмете может их произнести
        # без возрождения снятой редакции; ложное срабатывание тогда
        # разбирается вручную (та же цена, что у записей «двух ног»,
        # суррогата и гейта повтора, — популяция носителей важнее
        # точности шаблона).
        'pattern': r'защитн\w+\s+заявк\w+\s+после\s+входа'
                   r'|охраня\w+\s+РАЗНЫЕ\s+популяции'
                   r'|разные\s+популяции\s+действий',
        'arrived': r'вход\w*\s+включая|включает\s+\**вход|полос\w+\s+(между|у)\s+якор'
                   r'|клетку\s+«вход',
        'date': '2026-09-01',
        'source': 'D-G1 DOCS_CHECK_33: клетка «вход × полоса» без охраны, '
                  'узел 6 GAPS_CLOSE_33',
        'allowed': ('.claude/work/code-gate-ledger.json',),
        'population': (('docs/rules/risk-policy.md', None),
                       ('docs/spec/order-sizing.json', None),
                       ('docs/components/SizeCalculator.md', None),
                       ('docs/spec/stop-distance.json',
                        r'ПОЛОСЕ\s+между\s+якорем|полос\w+\s+между\s+якорем')),
    },
    {
        'name': 'якорь уровня назван только у безубытка',
        # Снятая редакция: якорь фактической средней цены объявлялся ТОЛЬКО у
        # безубытка и объявлялся контрастом («а не плановая цена ноги»), из чего
        # у стопа, тейка и активации трейлинга читалась плановая; проза дома
        # сужала ветвь до «всякий перенос уровня». Пришедшая: один якорь на все
        # уровни, потребители перечислены поимённо, ветвление по наличию живого
        # эпизода.
        'pattern': r'всякий\s+перенос\s+уровня'
                   r'|якор\w+\s+назван\s+только\s+у\s+безубытка',
        'arrived': r'один\s+на\s+все\s+уровни|у\s+ЛЮБОГО\s+уровня|от\s+ЯКОРЯ,\s+а\s+не\s+от\s+плановой',
        'date': '2026-09-01',
        'source': 'D4 DOCS_CHECK_34: якорь расчёта уровня, кодовый заход 3',
        'allowed': ('.claude/work/code-gate-ledger.json',),
        'population': (('docs/components/PriceCalculator.md', r'один\s+на\s+все\s+уровни'),
                       ('docs/rules/risk-policy.md', r'у\s+ЛЮБОГО\s+уровня|от\s+ЯКОРЯ,\s+а\s+не\s+от\s+плановой')),
    },
    {
        'name': 'ступень радиуса объекта — любая',
        # Снятая редакция: гейт повтора читал «на радиусе объекта действия стои́т
        # safety-ступень (любая)» — область не названа, биржевой радиус не
        # исключён, и под мягкой биржевой ступенью сопровождение частично
        # замирало бы. Пришедшая: safety-ступень ИНСТРУМЕНТА действия.
        'pattern': r'ступень\s+радиуса\s+объекта|радиус\w*\s+объекта\s+действия'
                   r'|чужая\s+ступень\s+того\s+же\s+радиуса',
        'arrived': r'safety-ступень\s+ИНСТРУМЕНТА|ступень\s+инструмента|instrumentSafetyRungStanding',
        'date': '2026-09-01',
        'source': 'B7 DOCS_CHECK_34: область операнда стоящей ступени, кодовый заход 3',
        'allowed': ('.claude/work/code-gate-ledger.json',),
        'population': (('docs/rules/strategy-step-once-per-episode.md', None),
                       ('docs/spec/strategy-walkthrough.json', r'instrumentSafetyRungStanding')),
    },
    {
        'name': 'диспозиция BLOCKED на PRECHECK — терминал безусловно',
        # Снятая редакция: отказ преконтроля на PRECHECK читался терминалом
        # транша безусловно (по стадии), при том что покодовое правило вело
        # часть кодов в ERROR, а два дока несли квалификатор «бессрочно» без
        # определения. Пришедшая: род реакции даёт стадия, бессрочность —
        # свойство кода, свёртка многокодового вердикта — конъюнкция.
        'pattern': r'сайзинг\s+обязан\s+был\s+уложить\s+действие\s+в\n?\s*поактный\s+потолок\s+—\s+расхождение\s+расчёта',
        'arrived': r'род\s+реакции\s+даёт\s+СТАДИЯ|Бессрочность\s+отказа|бессрочност\w+\s+вердикта',
        'date': '2026-09-01',
        'source': 'D1/D2/D3 DOCS_CHECK_34: развязка исходов преконтроля, кодовый заход 3',
        'allowed': ('.claude/work/code-gate-ledger.json',),
        'population': (('docs/processes/risk-evaluation.md', r'род\s+реакции\s+даёт\s+СТАДИЯ'),
                       ('docs/components/RiskBlockResolver.md', r'Карта\s+«вердикт'),
                       ('docs/components/models/RiskCheckResult.md', r'Бессрочность\s+отказа')),
    },
    {
        'name': 'пустой разбор истории даёт благоприятный терминал',
        # Снятая редакция замыкала ветвь «ни одна нога не дала» на
        # CANCELED/PARENT_ORDER_CANCELED — благоприятный терминал по
        # НЕДОБЫТОМУ факту, ровно шаблон нарушения из дома природы пустоты.
        # Пришедшая: терминал не ставится, исход не определён, поднимается
        # сигнал; оснований пустоты названо четыре, а не одно.
        'pattern': r'следов\s+материализации\s+нет'
                   r'|защита\s+читается\s+ушедшей\s+с\s+родителем'
                   r'|на\s+\*\*пустом\s+разборе\s+истории\*\*',
        'arrived': r'терминал\s+не\s+ставится|исход\s+\*\*не\s+определ|не\s+определ\w+,\s+поднимается\s+сигнал'
                   r'|производитель\s+\*\*один\*\*',
        'date': '2026-09-01',
        'source': 'C6 DOCS_CHECK_34: благоприятное умолчание в ветви «иначе», РИСК-заход шага 7',
        'allowed': ('.claude/work/code-gate-ledger.json',),
        'population': (('docs/lifecycles/Order.md', None),
                       ('docs/models/domain/core/Order.md', r'производитель\s+\*\*один\*\*'),
                       ('docs/components/RefreshOrderExecutor.md', r'терминала\s+не\s+даёт'),
                       ('docs/models/mapping/Order.md', r'терминал\s+не\s+ставится|«не\s+определено»')),
    },
    {
        'name': 'вторая ступень читает живую позицию уровня сделки',
        # Снятая редакция: PROTECTION_LOST по паре операндов РАЗНОГО уровня
        # (живая позиция сделки против защиты того же транша). Пришедшая:
        # обе стороны предиката траншевые — экспозиция транша.
        'pattern': r'позиция\s+жива,\s+отдельной\s+основной\s+защиты\s+нет'
                   r'|нога\s+живых\s+пуста\s+при\s+живой\s+позиции',
        'arrived': r'экспозиция\s+\*\*ТРАНША\*\*|экспозиции\s+ТРАНША|tranche\.exposure',
        'date': '2026-09-01',
        'source': 'C1 DOCS_CHECK_34: операнд второй ступени приведён к потраншевому инварианту',
        'allowed': ('.claude/work/code-gate-ledger.json',),
        'population': (('docs/lifecycles/Order.md', r'экспозиция\s+ТРАНША\s+положительна'),
                       ('docs/spec/order-lifecycle.json', r'tranche\.exposure'),
                       ('docs/models/domain/core/Order.md', r'экспозиции\s+ТРАНША')),
    },
    {
        'name': 'правило однократности не адресует шаги уровня сделки',
        # Снятая редакция выводила агрегатную поверхность (EXIT, FAIL_SAFE)
        # из-под правила целиком: отбор шёл номером эпизода транша, которого
        # у агрегатной строки нет. Пришедшая: область — эпизод ОБЪЕКТА шага,
        # у сделки он один, правило покрывает оба уровня.
        'pattern': r'[Нн]е\s+адресует\s+шаги\s+уровня\s+сделки'
                   r'|правил\w+\s+шагов\s+уровня\s+сделки\s+не\s+адресует',
        'arrived': r'эпизод\w*\s+объекта\s+шага|объект\s+шага|вырождается\s+в\s+саму\s+сделку',
        'date': '2026-09-01',
        'source': 'B2 DOCS_CHECK_34: гейт повтора не покрывал шаги уровня сделки, '
                  'кодовый заход 2 шага 7',
        'allowed': ('.claude/work/code-gate-ledger.json',),
        'population': (('docs/rules/strategy-step-once-per-episode.md', None),
                       ('docs/spec/strategy-walkthrough.json',
                        r'эпизод\w*\s+ОБЪЕКТА\s+ШАГА|объекта\s+шага')),
    },
    {
        'name': 'КОД и РИСК в ноль незакрытых до перевода статуса',
        # Пороговая форма первого условия гейта CODE: классы КОД и РИСК
        # обязаны быть в ноль ДО простановки статуса. Пришедшая форма мерит
        # компонент: КОД закрывается до или в момент кодирования своего
        # компонента, РИСК — прежде любого компонента торговой тропы.
        # Шаблон снятой намеренно узок и опирается на выделение: широкая
        # форма «в ноль незакрытых» законно стои́т в носителях, которые
        # НАЗЫВАЮТ редакцию снятой. Полноту здесь держит не шаблон, а
        # популяция: у каждого носителя проверяется ПРИШЕДШАЯ редакция.
        'pattern': r'обязан\w+\s+быть\s+в\s+\*{0,2}ноль\s+незакрытых'
                   r'|—\s*\*\*в\s+ноль\s+незакрытых\*\*'
                   r'|в\s+ноль\s+незакрытых,\s+прочие',
        'arrived': r'покомпонентн|до\s+или\s+в\s+момент\s+кодирования|торгов\w+\s+троп',
        'date': '2026-09-01',
        'source': 'решение держателя: контакт с кодом вместо доковых итераций '
                  '(.claude/decisions/code-contact-as-gate.md)',
        'allowed': ('.claude/decisions/code-contact-as-gate.md',
                    '.claude/work/decision-digest.md',
                    # Решение-источник СНЯТОЙ редакции: журнал решений описывает
                    # принятое на свой момент и правится только шапкой-статусом.
                    '.claude/decisions/population-origin-and-code-gate.md'),
        'population': (('.claude/processes/roadmap-step-execution.md', None),
                       ('.claude/skills/update-roadmap-progress.md', None),
                       ('.claude/skills/classify-code-blocking.md',
                        r'до\s+или\s+в\s+момент\s+кодирования'),
                       ('tools/code-gate-check.py', r'покомпонентн'),
                       ('.claude/rules/structure.md', r'статусом\s+кодирования')),
    },
    {
        'name': 'открытый слот грунта гейтит шаг',
        # Второе условие гейта CODE мерило состояние ШАГА: пока открыт хотя
        # бы один слот реестра предусловий, шаг в CODE не входит. Пришедшая
        # форма — та же, что у первого условия: слот гейтит СВОЙ компонент,
        # привязка живёт величиной «ждёт-грунта» реестра гейтов.
        # Шаблон опирается на выделение по той же причине, что и запись
        # выше: дом называет снятую редакцию дословно и вправе это делать.
        'pattern': r'\*\*Шаг\s+7\s+не\s+входит\s+в\s+`?CODE`?'
                   r'|все\s+его\s+слоты\s+«пункт\s+×\s+кейс»\s+закрыты'
                   r'|хотя\s+бы\s+один\s+слот\s+открыт\s+→\s+статус',
        'arrived': r'гейтит\s+свой\s+компонент|ждёт-грунта|покомпонентн',
        'date': '2026-09-01',
        'source': 'вывод CC из поправки держателя (.claude/decisions/code-contact-as-gate.md '
                  '§«Покомпонентное чтение гейта грунта»)',
        'allowed': ('.claude/decisions/code-contact-as-gate.md',
                    '.claude/work/decision-digest.md'),
        'population': (('.claude/tests/source-api/okx/code-preconditions.md',
                        r'гейтит\s+свой\s+компонент'),
                       ('.claude/skills/update-roadmap-progress.md',
                        r'гейтит\s+свой\s+компонент'),
                       ('.claude/processes/roadmap-step-execution.md',
                        r'[Гг]ейт\s+грунта\s+—\s+тоже\s+покомпонентный')),
    },
    {
        'name': 'отмена родителя переводит встроенную защиту в терминал',
        # Посылка опровергнута наблюдением 2026-08-30 (цепочки C17a-C17c):
        # при НЕПУСТОМ наливе источник разворачивает защиту в самостоятельную
        # живую условную заявку ровно на налитый объём, и она переживает
        # терминал родителя — в том числе когда инициатор терминала МЫ.
        # На снятой посылке стоя́ла отдельная строка порядка ног
        # «входной, филл непуст» с обратным порядком (place-new первым);
        # вместе с посылкой уходит и строка.
        # Отрицание и оговорка нулевого налива — ДЕЙСТВУЮЩАЯ редакция:
        # при нулевом наливе защита действительно уходит с родителем.
        'pattern': r'отмена\s+родител\w*[^.\n]{0,60}(?:переводит|уводит)[^.\n]{0,60}'
                   r'(?:встроенн\w+\s+защит\w*|защит\w*)[^.\n]{0,40}терминал'
                   r'|attached-защита\s+уходит\s+вместе\s+с\s+родителем'
                   r'|филл\s+непуст[^.\n]{0,80}place-new',
        'unless': r'нулев\w+\s+налив|налива\s+нет|TERMINAL_EMPTY|не\s+переводит'
                  r'|[Сс]нятая\s+редакция',
        'arrived': r'ровно\s+на\s+налитый\s+объём|переживает\s+терминал\s+родителя'
                   r'|по\s+филлу\s+НЕ\s+ветвится|входной\*?\*?\s*\(любой\s+налив\)',
        'date': '2026-09-01',
        'source': 'кодовый заход 4 шага 7, закрытие C5 DOCS_CHECK_34 '
                  '(наблюдение цепочек C17a-C17c)',
        'allowed': ('.claude/work/decision-digest.md',
                    'tools/retired-check.py',
                    '.claude/work/code-gate-ledger.json',),
        # На месте снятого встало РАЗНОЕ: правило порядка ног держит
        # объединённую строку и называет снятие; компонент-док отсылает к
        # дому судьбы защиты; рабочий файл несёт инструкцию исполнителю.
        'population': (('docs/rules/replace-not-amend.md', None),
                       ('docs/components/CancelAlgoOrderExecutor.md',
                        r'судьбу\s+её\s+встроенной\s+защиты\s+решает\s+налив'),
),
    },
    {
        'name': 'в биржевом эхе тип триггера не наблюдается',
        # Опровергается собственным инвентарём источника: slTriggerPxType
        # объявлен у ОБЕИХ форм — у элемента attachAlgoOrds[*] родителя и у
        # самостоятельной записи. Вместе с посылкой терялся операнд
        # подтверждения объявленной базы MARK.
        'pattern': r'тип\s+триггера\s+не\s+наблюдается'
                   r'|где\s+тип\s+триггера\s+не\s+набл\w+'
                   r'|эхо[^.\n]{0,40}не\s+нес[её]т[^.\n]{0,40}тип\w*\s+триггера',
        'unless': r'[Сс]нятая\s+посылка',
        'arrived': r'slTriggerPxType\s+объявлен\s+у\s+\*\*обеих\*\*\s+форм'
                   r'|операнд\s+сверки\s+объявленной\s+базы'
                   r'|эхо\s+`?slTriggerPxType`?',
        'date': '2026-09-01',
        'source': 'кодовый заход 4 шага 7, закрытие C3 DOCS_CHECK_34',
        'allowed': ('.claude/work/decision-digest.md',
                    'tools/retired-check.py',
                    '.claude/work/code-gate-ledger.json',),
        'population': (('docs/models/domain/core/Order.md', None),
                       ('docs/models/mapping/Order.md',
                        r'операнд\s+сверки\s+объявленной\s+базы')),
    },
    {
        'name': 'PROTECTION_LOST — одно значение на три тропы',
        # Отказ постановки, исчерпание цикла и order_failed писали ОДНУ
        # причину: по данным тропа не восстанавливалась. Разведены значением
        # (PROTECTION_PLACEMENT_FAILED / PROTECTION_LOST /
        # PROTECTION_TRIGGER_FAILED). Шаблон ловит связку старого значения с
        # чужой теперь тропой, а не само значение: у второй тропы оно живо.
        'pattern': r'PROTECTION_LOST[^.\n]{0,120}(?:отказ\w*\s+постановки|order_failed)'
                   r'|(?:отказ\w*\s+постановки|order_failed)[^.\n]{0,120}PROTECTION_LOST',
        'unless': r'PROTECTION_PLACEMENT_FAILED|PROTECTION_TRIGGER_FAILED'
                  r'|[Тт]роп\w*\s+потери\s+покрытия\s+три',
        'arrived': r'PROTECTION_PLACEMENT_FAILED|PROTECTION_TRIGGER_FAILED',
        'date': '2026-09-01',
        'source': 'кодовый заход 4 шага 7, закрытие C2 DOCS_CHECK_34',
        'allowed': ('.claude/work/decision-digest.md',
                    'tools/retired-check.py'),
        'population': (('docs/models/domain/core/Order.md', None),
                       ('docs/lifecycles/Order.md', None),
                       ('docs/models/mapping/Order.md', None)),
    },
    {
        'name': 'покодовый карв-аут объявлен неразрешимым и не применялся',
        # Карта резолвера объявляла покодовый карв-аут неразрешимым («обе
        # однородные свёртки ошибочны») и уводила в ERROR ЛЮБОЙ вердикт при
        # живом риске — включая реджект защитного действия, которым живая
        # позиция как раз и сохраняется. Между тем перечень карв-аута
        # (docs/processes/risk-evaluation.md) объявляет ровно обратное, и его
        # членство названо «единственным носителем». Пришедшая редакция:
        # карв-аут применяется, свёртка КОНЪЮНКТИВНАЯ и только внутри ветви
        # живого риска.
        'pattern': r'свёртка\s+не\s+покодовая|[Пп]окодов\w+\s+карв-аут[^.\n]{0,60}неразрешим',
        'arrived': r'членство\s+в\s+карв-ауте\s+—\s+тоже\s+конъюнкция'
                   r'|вердикт\s+\*\*целиком\s+из\s+кодов\s+карв-аута\*\*'
                   r'|карв-аут[^.\n]{0,80}конъюнкц',
        'date': '2026-09-01',
        'source': 'кодовый заход по преконтролю шага 7: карта резолвера '
                  'противоречила дому перечня карв-аута',
        'allowed': ('.claude/work/decision-digest.md',
                    'tools/retired-check.py'),
        'population': (('docs/components/RiskBlockResolver.md', None),),
    },
    # --- дизайн-проход периметра безопасности, шаг 9 фазы 1 (2026-09-04) -----
    {
        'name': 'у инварианта ручного снятия два энфорсера',
        # Дом доступа объявлял «два энфорсера» лидом и «энфорсер один»
        # текстом ниже; поверхность ручной остановки цитировала лид. Пришедшая
        # редакция: энфорсер один — аутентифицированный принципал, а
        # единственность вызывающего изнутри есть НАЗВАННОЕ ОГРАНИЧЕНИЕ, не
        # обеспечение (смешение заявления с обеспечением запрещает П1-2).
        'pattern': r'[Уу]\s+инварианта\s+два\s+энфорсера'
                   r'|названы\s+оба\s+энфорсера',
        'arrived': r'энфорсер\s+один'
                   r'|назван\s+\*\*энфорсер\*\*',
        'date': '2026-09-04',
        'source': 'дизайн-проход периметра безопасности шага 9, узел A8',
        'allowed': ('.claude/work/decision-digest.md',
                    '.claude/work/progress/phase-1-step-9-design-pass.md',
                    'tools/retired-check.py'),
        'population': (('docs/rules/api-access-policy.md', r'энфорсер\s+один'),
                       ('docs/rules/manual-halt.md',
                        r'назван\s+\*\*энфорсер\*\*')),
    },
    {
        'name': 'ступень отказа кредов выводится вне оси лестницы',
        # Дом лестницы объявлял п.5 ВНЕ единственной оси («не судьбой принятого
        # риска, а тем, что у контура не остаётся ни одной тропы к нему») — и
        # тем делал ложным клейм единственности оси в концепции и в доме
        # сигнала. Пришедшая редакция: ступень выводится ТОЙ ЖЕ осью —
        # покрытие принятого риска стало неподтверждаемым и невосстановимым.
        'pattern': r'не\s+остаётся\s+ни\s+одной\s+тропы\s+к\s+нему'
                   r'|[Сс]тупень\s+выводится\s+не\s+«судьбой',
        'arrived': r'выводится\s+той\s+же\s+осью',
        'date': '2026-09-04',
        'source': 'дизайн-проход периметра безопасности шага 9, узел A1',
        'allowed': ('.claude/work/decision-digest.md',
                    'tools/retired-check.py'),
        'population': (('docs/rules/exchange-hold.md', None),),
    },
    {
        'name': 'сделки доходят до терминала локально на тропе отказа кредов',
        # Клейм опровергался исполнимой спекой: оба терминала гейтятся
        # riskProvenAbsent, а он стои́т на добываемых ЧТЕНИЕМ фактах. Пришедшая
        # редакция: сделки радиуса до терминала НЕ доходят и остаются в ERROR
        # до восстановления кредов; выход из состояния внешний.
        'pattern': r'[Сс]делки\s+доходят\s+до\s+терминала\s+локально'
                   r'|терминал\w*\s+пишется\s+без\s+подтверждения\s+фактом',
        'arrived': r'до\s+терминала\s+не\s+доходят',
        'date': '2026-09-04',
        'source': 'дизайн-проход периметра безопасности шага 9, узел A2',
        'allowed': ('.claude/work/decision-digest.md',
                    '.claude/work/progress/phase-1-step-9-design-pass.md',
                    '.claude/work/progress/phase-1-step-9-docs-check-3.md',
                    'tools/retired-check.py'),
        'population': (('docs/rules/exchange-hold.md', None),
                       ('docs/lifecycles/Deal.md',
                        r'пока\s+у\s+контура\s+есть\s+тропа\s+к\s+бирже'),
                       ('docs/concept.md',
                        r'ждут\s+восстановления\s+доступа'),
                       ('docs/components/ErrorHandler.md',
                        r'подтверждение\s+не\s+придёт\s+по\s+построению')),
    },
    {
        'name': 'изъятие отказа кредов из ретраибельного живёт в доме лестницы',
        # Изъятие есть правило КЛАССИФИКАЦИИ: оно говорит, что делает с
        # классом граница исполнения. Стоя́ в доме лестницы, оно оставляло
        # правило «EXCHANGE_ERROR ретраибелен» безусловным в собственном доме.
        'pattern': r'[Кк]ласс\s+выведен\s+из\s+общего\s+ретраибельного',
        'arrived': r'изъят\w*\s+из\s+ретраибельного'
                   r'|дом\s+изъятия\s+—\s+не\s+здесь',
        'date': '2026-09-04',
        'source': 'дизайн-проход периметра безопасности шага 9, узел A3',
        'allowed': ('.claude/work/decision-digest.md',
                    'tools/retired-check.py'),
        'population': (('docs/rules/runtime-error-classification.md',
                        r'изъят\w*\s+из\s+ретраибельного'),
                       ('docs/rules/exchange-hold.md',
                        r'дом\s+изъятия\s+—\s+не\s+здесь')),
    },
    {
        'name': 'тропа запроса ступени у отказа кредов — тропа контролируемых отказов',
        # Атрибуция вела в дом, чей перечень ЗАКРЫТ и отказа кредов не
        # содержит. Пришедшая редакция: у трёх категорий предмет — сущность и
        # её closeReason; отказ кредов сущности не касается, ступень
        # запрашивает граница исполнения прохода.
        'pattern': r'та\s+же\s+тропа,\s+что\s+и\s+у\s+прочих\s+контролируемых\s+отказов',
        'arrived': r'ступень\s+запрашивает\s+\*\*граница\s+исполнения\s+прохода\*\*'
                   r'|[Пп]еречень\s+закрыт,\s+и\s+отказ\s+источника',
        'date': '2026-09-04',
        'source': 'дизайн-проход периметра безопасности шага 9, узел A4',
        'allowed': ('.claude/work/decision-digest.md',
                    'tools/retired-check.py'),
        'population': (('docs/rules/exchange-hold.md',
                        r'ступень\s+запрашивает\s+\*\*граница\s+исполнения\s+прохода\*\*'),
                       ('docs/rules/controlled-exchange-exceptions.md',
                        r'[Пп]еречень\s+закрыт,\s+и\s+отказ\s+источника')),
    },
    {
        'name': 'перечень «вне иерархии» закрыт одним признаком — механикой контура',
        # Клейм был ложен: механики контура вне перечня хватает (постановка
        # ступени, резолверы, entry-скан), и у каждого класса СВОЯ причина
        # невхождения. Пришедшая редакция: три конъюнкта — доходит до биржи,
        # порождено не стратегией, исполняется механикой контура.
        'pattern': r'закрыт\s+над\s+механикой\s+контура'
                   r'|закрыт\s+над\s+МЕХАНИКОЙ\s+КОНТУРА',
        'arrived': r'не\s+по\s+стратегии|НЕ\s+ПО\s+СТРАТЕГИИ',
        'date': '2026-09-04',
        'source': 'дизайн-проход периметра безопасности шага 9, узел A11',
        'allowed': ('.claude/work/decision-digest.md',
                    'tools/retired-check.py'),
        'population': (('docs/rules/execution-hierarchy.md',
                        r'не\s+по\s+стратегии'),
                       ('docs/concept.md', r'НЕ\s+ПО\s+СТРАТЕГИИ')),
    },
    {
        'name': 'число субъектов поверхности не задано — вопрос вынесен держателю',
        # SEC-Q1 закрыт держателем 2026-09-04: к поверхности бота в фазе 1
        # обращается ОДИН субъект. Снята редакция «число не задано, вопрос
        # открыт, контур строится одинаково при любом ответе»; пришедшая —
        # НАЗВАННАЯ ПОСЫЛКА ФАЗЫ 1 с параметром снятия (появление второго
        # субъекта поверхности) и встречным якорем со стороны события.
        # У каждого носителя свой шаблон: на месте снятого встало разное —
        # посылка с параметром, привязка недостижимости, следствие для
        # области значений актора.
        'pattern': r'[Вв]опрос\s+вынесен\s+держателю'
                   r'|ответом\s+на\s+.?SEC-Q1'
                   r'|механику\s+шага\s+он\s+не\s+гейтит',
        'arrived': r'посылка\s+фазы\s+1|посылке\s+фазы\s+1',
        'date': '2026-09-04',
        'source': 'решение держателя по SEC-Q1: один принципал',
        'allowed': ('.claude/work/decision-digest.md',
                    '.claude/work/progress/phase-1-step-9-gaps-close-5.md',
                    'tools/retired-check.py'),
        'population': (('docs/rules/api-access-policy.md',
                        r'посылка\s+фазы\s+1'),
                       ('docs/models/domain/other/AccessDenial.md',
                        r'вместе\s+с\s+посылкой'),
                       ('docs/rules/error-handling-policy.md',
                        r'при\s+посылке\s+фазы\s+1'),
                       ('docs/models/domain/other/Auditable.md',
                        r'следствие\s+посылки')),
    },
    {
        'name': 'форма реестра гейтов переключается фактом идущих прогонов',
        # Гейт входа в GAPS_CLOSE_N нёс снятую формулировку: на шаге, чьи
        # прогоны идут ПОСЛЕ остановки петли, она требовала обеих форм разом.
        # Пришедшая редакция: форму задаёт решение держателя.
        'pattern': r'[Нн]орма\s+действует,\s+пока\s+идут\s+прогоны',
        'arrived': r'которую\s+назвало\s+решение\s+держателя',
        'date': '2026-09-04',
        'source': 'дизайн-проход периметра безопасности шага 9, узел A10',
        'allowed': ('.claude/work/decision-digest.md',
                    'tools/retired-check.py'),
        'population': (('.claude/skills/update-roadmap-progress.md', None),),
    },
    {
        'name': 'отметка обработанного у потребителя — всегда таблица inbox',
        # Снято GAPS_CLOSE_1 шага 10 фазы 2 (узел A7). Прежняя редакция
        # объявляла ФОРМУ отметки безусловной: «дедуплицирует таблицей inbox».
        # У второго построенного потребителя (audit-statistics) следствие
        # приёма — сама строка журнала, и её ключ служит отметкой; вторая
        # таблица была бы вторым носителем той же истины. Пришедшая редакция:
        # отметка обработанного, форму которой задаёт следствие потребителя.
        'pattern': r'дедуплицирует\s+по\s+\S*eventId\S*\s+таблицей\s+inbox'
                   r'|отсекается\s+inbox-таблицей\s+потребителя'
                   r'|дубль\s+закрыт\s+inbox\W*ом\s+потребителя',
        'arrived': r'отметк\w*\s+обработанного',
        'date': '2026-09-06',
        'source': 'GAPS_CLOSE_1 шага 10 фазы 2, узел A7',
        'allowed': ('.claude/work/decision-digest.md',
                    'tools/retired-check.py'),
        'population': (
            ('docs/architecture/data-ownership.md', None),
            ('docs/architecture/contracts.md', None),
            ('docs/components/OutboxRelayJob.md', None),
            ('libs/domain-model/src/main/java/com/example/tradingbot/domain/event/EventEnvelope.java', None),
            ('services/strategies/src/main/java/com/example/strategies/domain/jobs/JobExecutionGuard.java', None),
        ),
    },
    {
        'name': 'ярусы запрещают восходящий вызов у потребителя события',
        # Снято GAPS_CLOSE_1 шага 10 фазы 2 (узел A5). Прежняя редакция
        # закрывала ярусом законный вариант «завести строку синхронного
        # чтения» для audit-statistics, который стои́т в ТОМ ЖЕ ярусе домена.
        # Пришедшая редакция: довод — отсутствие поимённой строки.
        'pattern': r'синхронно\s+его\s+не\s+читает\s+\(ярусы\s+запрещают',
        'arrived': r'потому,?\s+что\s+поимённой\s+строки\s+на\s+это',
        'date': '2026-09-06',
        'source': 'GAPS_CLOSE_1 шага 10 фазы 2, узел A5',
        'allowed': ('.claude/work/decision-digest.md',
                    'tools/retired-check.py'),
        'population': (('docs/architecture/contracts.md', None),),
    },
    {
        'name': 'аудит по тенанту полон уже сегодня',
        # Снято GAPS_CLOSE_1 шага 10 фазы 2 (узел A14). Прежняя редакция
        # объявляла полноту журнала ДОСТИГНУТОЙ у сервиса, которого не
        # существует, и клейм полноты запрещал писателю искать дальше.
        # Пришедшая редакция: полнота обеспечивается конструкцией, а из
        # двадцати классов производитель построен у восьми.
        'pattern': r'аудит\s+по\s+тенанту\s+полон',
        'arrived': r'обеспечивается\s+конструкцией|полнот\w*\s+журнала\s+по\s+тенанту',
        'date': '2026-09-06',
        'source': 'GAPS_CLOSE_1 шага 10 фазы 2, узел A14',
        'allowed': ('.claude/work/decision-digest.md',
                    'tools/retired-check.py'),
        'population': (('docs/architecture/reserved-extension-points.md', None),),
    },
    {
        'name': 'журнал хранится целиком и навсегда',
        # Снято GAPS_CLOSE_1 шага 10 фазы 2, часть 2 (серия 8, узлы A9+A19).
        # Прежняя редакция объявляла клейм полноты АБСОЛЮТНЫМ («целиком и
        # навсегда») — неверный дважды: в непроизводственных окружениях журнал
        # чистится, а непрерывности он не обещает вовсе. Пришедшая редакция:
        # полнота — ДВЕ величины, нижняя граница (минимум момента приёма) и
        # непрерывность, которую журнал не обещает, а делает наблюдаемой.
        'pattern': r'целиком\s+и\s+навсегда',
        'arrived': r'нижн[а-яё]+\s+границ|произведённым\s+после',
        'date': '2026-09-06',
        'source': 'GAPS_CLOSE_1 шага 10 фазы 2, часть 2, серия 8 (A9+A19)',
        # Рабочие файлы прогона несут снятую формулировку как ПРЕДМЕТ находки;
        # без allowed запись упала бы на собственных отчётах этого же шага.
        'allowed': ('.claude/work/decision-digest.md',
                    'tools/retired-check.py',
                    '.claude/snapshots/',
                    '.claude/work/progress/'),
        'population': (('docs/models/domain/other/AuditRecord.md', None),),
    },
    {
        'name': 'исполнение команды рефреша попадает в историю',
        # Снято GAPS_CLOSE_1 шага 10 фазы 2, часть 2 (серия 9, узел B13).
        # Прежняя редакция утверждала попадание в историю как СОСТОЯВШЕЕСЯ —
        # при том, что класса события на переход строки исполнения нет ни
        # одного. Пришедшая редакция: клейм ВЕРЕН и не обеспечен, дефицит
        # назван и припаркован; ложным клейм не объявляется — durable-носитель
        # у исполнения есть.
        'pattern': r'попадает\s+в\s+историю',
        'arrived': r'верен\s+и\s+.{0,20}не\s+обеспечен'
                   r'|класса\s+события\s+на\s+переход\s+строки',
        'date': '2026-09-06',
        'source': 'GAPS_CLOSE_1 шага 10 фазы 2, часть 2, серия 9 (B13)',
        'allowed': ('.claude/work/decision-digest.md',
                    'tools/retired-check.py',
                    '.claude/work/progress/'),
        'population': (
            ('docs/rules/audit-not-runtime-source.md',
             r'верен\s+и\s+.{0,20}не\s+обеспечен'),
            ('docs/components/RefreshBalanceExecutor.md',
             r'класса\s+события\s+на\s+переход\s+строки'),
        ),
    },
    {
        'name': 'периметр шлёт audit-statistics команды пользователя',
        # Снято GAPS_CLOSE_1 шага 10 фазы 2, часть 2 (серия 11, узел A11).
        # Прежняя редакция держала четыре сервиса в одной ячейке синхронных
        # вызовов и давала периметру команды к сервису, у которого входящего
        # REST на запись нет по инвентарю: две строки об одной поверхности
        # расходились. Пришедшая редакция: строка расщеплена, у аудита —
        # только чтение.
        # Популяция ОДНА, а не «все носители оси»: пришедшую редакцию несёт
        # ровно один живой носитель, и буквально записанная ось уронила бы
        # прогон на файлах, которые снятой редакции никогда не несли.
        'pattern': r'audit-statistics.{0,40}команды\s+пользователя',
        'arrived': r'только\s+чтение',
        'date': '2026-09-06',
        'source': 'GAPS_CLOSE_1 шага 10 фазы 2, часть 2, серия 11 (A11)',
        'allowed': ('.claude/work/decision-digest.md',
                    'tools/retired-check.py',
                    '.claude/work/progress/',
                    '.claude/work/history/'),
        'population': (('docs/architecture/contracts.md', None),),
    },
    {
        'name': 'счётный клейм о построенности (классы, потребители, группы)',
        # Снято GAPS_CLOSE_2 шага 10 фазы 2, узел 1 (A1, A11, C1, C3, V7).
        # Прежняя редакция держала число построенного прозой сразу в пяти
        # носителях («Построены девять», «производитель построен у девяти»,
        # «девять из двадцати одного», «построены восемь», «потребителей
        # теперь три», «групп сегодня две»). Число стареет МОЛЧА при каждом
        # заведённом классе, потребителе и группе, а живя копиями — расходится:
        # к DOCS_CHECK_2 копии дали 9/21, 8/20 и 3 потребителя при фактических
        # 8/21 и 2. Пришедшая редакция: счётного клейма нет ни в одном
        # носителе; живёт ПРИЧИНА непокрытия и УСЛОВИЕ ВОЗВРАТА у владельца
        # журнала, а число читатель получает прогоном.
        'pattern': r'Построены\s+\*\*девять\*\*'
                   r'|производитель\s+построен\s+у\s+\*\*девяти\*\*'
                   r'|девять\s+из\s+двадцати\s+одного'
                   r'|построены\s+\*\*восемь\*\*'
                   r'|Групп\s+сегодня\s+две'
                   r'|теперь\s+их\s+три\s+—\s+ядро',
        'arrived': r'Счётного\s+клейма',
        'date': '2026-09-06',
        'source': 'GAPS_CLOSE_2 шага 10 фазы 2, узел 1',
        'allowed': ('.claude/work/decision-digest.md',
                    'tools/retired-check.py',
                    '.claude/work/progress/',
                    '.claude/work/history/'),
        'population': (
            ('docs/models/domain/other/AuditRecord.md', None),
            ('docs/architecture/contracts.md', None),
            ('docs/architecture/reserved-extension-points.md', None),
            ('docs/architecture/data-ownership.md', None),
        ),
    },
    {
        'name': 'актор едет содержимым сегодня; ручная тропа только у классов определения',
        # Снято GAPS_CLOSE_2 шага 10 фазы 2, узел 2 (A3, B6, C2). Прежняя
        # редакция утверждала как факт сегодняшнего дня, что актор едет полем
        # содержимого у трёх классов жизненного цикла определения, — при том
        # что поля нет ни в одной построенной форме; и объявляла подъём
        # safety-ступени «автоматическим по построению», при том что держатель
        # ставит ту же ступень тем же механизмом. Операнд спеки при этом мерил
        # ПОСТРОЕННОСТЬ СЕРВИСА (outbox, реле, тема) и потому давал двум
        # классам одного производителя в одинаковом состоянии противоположные
        # исходы. Пришедшая редакция: правило действует, а построенность поля
        # названа поимённо — ограничение с домом, оживителем и причиной;
        # ручная тропа у подъёма ступени объявлена; операнд мерит
        # построенность КЛАССА (eventClassBuilt). Кодовый заход K11 шага 10
        # фазы 2 построил поле у классов определения, и формулировка
        # ограничения сузилась с «нет ни у одного» до «нет у классов ядра» —
        # обе формы стоя́т в шаблоне пришедшей редакции.
        'pattern': r'актор\s+есть\s+сегодня\s+у\s+трёх\s+классов'
                   r'|Ручные\s+тропы\s+среди\s+построенных\s+классов\s+есть\s+уже\s+сегодня'
                   r'|подъём\s+автоматический\s+по\s+построению'
                   r'|producerBuilt',
        'arrived': r'Поля\s+актора\s+сегодня\s+нет'
                   r'|Поля\s+актора\s+у\s+них\s+сегодня\s+нет'
                   r'|актора\s+сегодня\s+нет\s+ни\s+у\s+одного'
                   r'|Поле\s+актора\s+построено'
                   r'|Актор\s+в\s+журнале\s+есть\s+у\s+обоих'
                   r'|eventClassBuilt'
                   r'|Поля\s+актора\s+у\s+`HoldRaised`\s+нет'
                   r'|Актор\s+едет\s+содержимым\s+`HoldRaised`',
        'date': '2026-09-06',
        'source': 'GAPS_CLOSE_2 шага 10 фазы 2, узел 2',
        'allowed': ('.claude/work/decision-digest.md',
                    'tools/retired-check.py',
                    '.claude/work/progress/',
                    '.claude/work/history/'),
        'population': (
            ('docs/architecture/contracts.md', None),
            ('docs/models/domain/other/Auditable.md', None),
            ('docs/architecture/tenant-and-exchange.md', None),
            ('docs/spec/event-actor-presence.json', r'eventClassBuilt'),
            # Обе формулировки ограничения сменились ИСПОЛНЕНИЕМ (кодовый заход
            # K12 шага 10 фазы 2): поле актора построено у классов ядра тоже.
            ('docs/rules/manual-halt.md', r'Актор\s+едет\s+содержимым\s+`HoldRaised`'),
        ),
    },
    {
        'name': 'состав агрегата: ценовая сумма, gross-итог, знаменатель R и молчаливый UNIQUE',
        # Снято GAPS_CLOSE_2 шага 10 фазы 2, узел 3 (B1-B4, B7, B8, A8, М5, М6,
        # М10). Прежняя редакция: (1) звала «ценовой» сумму, из которой комиссия
        # и штраф ликвидации уже вычтены, и «gross» — тот носитель, который
        # корпус зовёт net, приглашая вычесть издержки второй раз; (2) держала
        # знаменатель R на популяции, шире числителя; (3) называла двенадцать
        # слагаемых прозой без колонок и типов; (4) обещала «перезаписывает, а
        # не задваивает» при молчаливом UNIQUE поверх двух nullable-колонок
        # ключа; (5) называла счётчики происшествий прозой, без имён и формы.
        # Пришедшая редакция: имена по составу, знаменатель на популяции
        # числителя, колонки с типами, unique nulls not distinct, шесть
        # именованных счётчиков происшествий с двумя разрезами.
        'pattern': r'сумма\s+ценовых\s+результатов\s+доступных'
                   r'|двенадцать\s+хранимых\s+слагаемых'
                   r'|сумма\s+плановых\s+рисков\s+тех\s+принявших\s+риск'
                   r'|повторный\s+пересчёт\s+её\s+перезаписывает,\s+а\s+не\s+задваивает'
                   r'|счётчики\s+происшествий:\s+заведённых\s+сделок',
        'arrived': r'unique\s+nulls\s+not\s+distinct'
                   r'|resultBeforeFundingSum|netResultSum|rDenominatorDeals|openedDeals',
        'date': '2026-09-06',
        'source': 'GAPS_CLOSE_2 шага 10 фазы 2, узел 3',
        'allowed': ('.claude/work/decision-digest.md',
                    'tools/retired-check.py',
                    '.claude/work/progress/',
                    '.claude/work/history/'),
        'population': (
            ('docs/rules/statistics-aggregates.md', r'unique\s+nulls\s+not\s+distinct'),
            ('docs/spec/statistics-aggregates.json', r'resultBeforeFundingSum'),
        ),
    },
    {
        'name': 'ветвь отказа журнала по трём полям; непрерывность без писателя и без живости измерителя',
        # Снято GAPS_CLOSE_2 шага 10 фазы 2, узел 4 (A5, A9, B5, C6, М1).
        # Прежняя редакция: (1) называла ветвь отказа по ТРЁМ полям конверта,
        # тогда как схема объявляет обязательными шесть входных значений —
        # отказ приходил бы из базы, а не из механизма, который его объявляет;
        # (2) объявляла «обработка останавливается» без подлежащего-механизма,
        # притом что умолчание технологии даёт молчаливый пропуск; (3) держала
        # предикат непрерывности на двух операндах без писателя и без носителя,
        # то есть в метрике, которую сам корпус объявил непригодной для разбора.
        # Пришедшая редакция: механизм остановки назван через нефиксацию
        # смещения, обязательное равно схеме, операнды предиката живут durable-
        # строкой состояния приёма, и третий конъюнкт мерит живость измерителя.
        'pattern': r'нет\s+`eventId`,\s+`tenantId`\s+либо\s+`eventType`'
                   r'|неполный\s+конверт\s+\(нет\s+eventId'
                   r'|обработка\s+останавливается,\s+и\s+отставание\s+группы',
        'arrived': r'Механизм\s+остановки\s+назван|receptionStateFresh|journal_reception_state',
        'date': '2026-09-06',
        'source': 'GAPS_CLOSE_2 шага 10 фазы 2, узел 4',
        'allowed': ('.claude/work/decision-digest.md',
                    'tools/retired-check.py',
                    '.claude/work/progress/',
                    '.claude/work/history/'),
        'population': (
            ('docs/models/domain/other/AuditRecord.md', r'Механизм\s+остановки\s+назван'),
            ('docs/spec/audit-journal.json', r'receptionStateFresh'),
        ),
    },
    {
        'name': 'множественность событий на сделку — самостоятельное основание отказа классу',
        # Снято GAPS_CLOSE_2 шага 10 фазы 2, узел 5 (C5). Прежняя редакция
        # соединяла две посылки отказа союзом «а», и клауза читалась как одна
        # посылка: «класс дал бы несколько событий на сделку». Прочитанная так,
        # она объявляла дефектным DealShutdownInitiated — класс, принятый тем
        # же закрытием и обладающий ровно этим признаком: причина остановки
        # присваивается на двух рёбрах, и событий бывает два. Пришедшая
        # редакция: посылки соединены «и», отказ держит КОНЪЮНКЦИЯ —
        # множественность при отсутствии потребителя, — а множественность сама
        # по себе объявлена свойством класса с обязательством у потребителя.
        'pattern': r'дал\s+бы\s+несколько\s+событий\s+на\s+сделку,\s+а\s+метрики',
        'arrived': r'Посылки\s+соединены|БОЛЬШЕ\s+ОДНОГО\s+`DealShutdownInitiated`',
        'date': '2026-09-06',
        'source': 'GAPS_CLOSE_2 шага 10 фазы 2, узел 5',
        'allowed': ('.claude/work/decision-digest.md',
                    'tools/retired-check.py',
                    '.claude/work/progress/',
                    '.claude/work/history/'),
        'population': (
            ('docs/rules/audit-not-runtime-source.md', r'Посылки\s+соединены'),
            ('docs/architecture/contracts.md', r'БОЛЬШЕ\s+ОДНОГО\s+`DealShutdownInitiated`'),
        ),
    },
    {
        'name': 'группа журнала читает одну тему; правило алерта — на группу; состав точек чтения без дома',
        # Снято GAPS_CLOSE_2 шага 10 фазы 2, узел 6 (A6, A7, A12). Прежняя
        # редакция: (1) говорила о теме журнала в единственном числе, при том
        # что журнал несёт классы нескольких производителей, — часть
        # объявленных классов не доходила бы, а перечень непокрытого расходился
        # бы с фактикой молча; (2) писала правило алерта на ГРУППУ, и сумма по
        # темам маскировала бы остановку на одной молчанием другой; (3) держала
        # решение «состав точек не специфицируется» без дома, то есть видимым
        # только тому, кто открыл этот док. Пришедшая редакция: подписка на
        # тему КАЖДОГО производителя несомых классов, единица правила — пара
        # «группа × тема» с потемными операндами, у решения о составе точек
        # назван дом в бэклоге с оживителем и причиной.
        'pattern': r'Группа\s+журнала\s+читает\s+тему\s+с\s+самой\s+ранней'
                   r'|Правило\s+пишется\s+по\s+КАЖДОЙ\s+durable-группе'
                   r'|Состав\s+точек\s+здесь\s+не\s+специфицируется,\s+и\s+это\s+решение\.',
        'arrived': r'тему\s+КАЖДОГО\s+производителя|Единица\s+правила\s+—\s+ПАРА|Дом\s+позиции',
        'date': '2026-09-06',
        'source': 'GAPS_CLOSE_2 шага 10 фазы 2, узел 6',
        'allowed': ('.claude/work/decision-digest.md',
                    'tools/retired-check.py',
                    '.claude/work/progress/',
                    '.claude/work/history/'),
        'population': (
            ('docs/models/domain/other/AuditRecord.md', r'тему\s+КАЖДОГО\s+производителя'),
            ('docs/architecture/data-ownership.md', r'Единица\s+правила\s+—\s+ПАРА'),
        ),
    },
    {
        'name': 'группировка пересчёта — по четырём колонкам радиуса плюс валюте',
        # Снято GAPS_CLOSE_3 шага 10 фазы 2, узел 1 (A2, B4). Прежняя
        # редакция называла ключом группировки четыре колонки идентичности
        # журнала плюс расчётную валюту: перечень включал инструмент и
        # сделку, которых в зерне нет, и не включал тенанта и суток, без
        # которых зерна не существует. Группировка по сделке дала бы строку
        # на сделку вместо строки на сутки, а upsert по ключу зерна такие
        # строки схлопывает молча. Пришедшая редакция ключа не перечисляет
        # вовсе: группировка идёт ПО КЛЮЧУ ЗЕРНА собираемой строки, а оба
        # ключа объявлены своими домами в том же доке.
        'pattern': r'по\s+четырём\s+колонкам\s+радиуса\s+плюс\s+расчётной',
        'arrived': r'по\s+ключу\s+зерна\s+собираемой\s+строки',
        'date': '2026-09-07',
        'source': 'GAPS_CLOSE_3 шага 10 фазы 2, узел 1',
        'allowed': ('.claude/work/decision-digest.md',),
        'population': (
            ('docs/rules/statistics-aggregates.md', None),
        ),
    },
    {
        'name': 'нижняя граница полноты — одно min(recorded_at) по всему журналу',
        # Снято GAPS_CLOSE_3 шага 10 фазы 2, узел 2 (A1, A5, B3). Прежняя
        # редакция считала границу одним минимумом по журналу и на этом
        # основании отказывалась объявлять её полем (П4). Потемного разреза
        # журнал не несёт — колонки темы в audit_records нет, — поэтому
        # тема, присоединённая к подписке позже начала производства,
        # оставляла границу РАНЬШЕ фактического начала покрытия, и ни одна
        # ветвь разрыва этого не ловила. Пришедшая редакция: граница —
        # свёртка двух операндов, позднейший из минимума по журналу и
        # позднейшего момента наблюдения темы; второй хранится полем
        # observed_since строки состояния приёма пары.
        'pattern': r'Нижняя\s+граница\*\*\s+—\s+самый\s+ранний\s+момент\s+приёма'
                   r'|журнал\s+полон\s+по\s+событиям,\s+произведённым\s+после'
                   r'\s+самого\s+раннего\s+момента\s+приёма',
        'arrived': r'свёртка\s+по\s+парам|предмет\s+владельца\s+журнала',
        'date': '2026-09-07',
        'source': 'GAPS_CLOSE_3 шага 10 фазы 2, узел 2',
        'allowed': ('.claude/work/decision-digest.md',),
        'population': (
            ('docs/models/domain/other/AuditRecord.md', r'свёртка\s+по\s+парам'),
            ('docs/architecture/reserved-extension-points.md', r'предмет\s+владельца\s+журнала'),
        ),
    },
    {
        'name': 'выведенный из знаменателя риск виден суммой на строке без валюты',
        # Снято GAPS_CLOSE_3 шага 10 фазы 2, узел 3 (B2). Прежняя редакция
        # объявляла денежные суммы строки нерезолвленной валюты нулевыми по
        # построению и тут же называла одну из них носителем видимости этой
        # популяции. Область plannedRiskExcludedSum есть ДОПОЛНЕНИЕ области
        # прочих сумм, поэтому на строке с пустым ключом валюты считалась
        # ровно она — сумма без единицы измерения, то есть та операция, ради
        # запрета которой валюта вынесена в ключ зерна. Пришедшая редакция:
        # величина считается только на строке известной валюты, а популяция
        # видна счётчиком.
        'pattern': r'суммой\s+выведенного\s+риска',
        'arrived': r'ключ\s+зерна\*\*:\s+единицу\s+строке\s+даёт\s+он',
        'date': '2026-09-07',
        'source': 'GAPS_CLOSE_3 шага 10 фазы 2, узел 3',
        'allowed': ('.claude/work/decision-digest.md',),
        'population': (
            ('docs/rules/statistics-aggregates.md', None),
            ('docs/spec/statistics-aggregates.json', r'не\s+тождество\s+двух\s+валют'),
        ),
    },
    {
        'name': 'операнд итога сделки зовётся gross',
        # Снято GAPS_CLOSE_3 шага 10 фазы 2, узел 3 (C5). Операнд, из
        # которого складывается netResultSum, звался grossResult — тем самым
        # именем, которое нота этой величины запрещает («назвать это число
        # gross значило бы звать gross ровно то, что корпус зовёт net»).
        # Домового имени он при этом не носил: дом зовёт число
        # Deal.resultProfit. Пришедшая редакция — домовое имя, как у
        # соседнего операнда priceResult.
        'pattern': r'"grossResult"',
        'arrived': r'"resultProfit"',
        'date': '2026-09-07',
        'source': 'GAPS_CLOSE_3 шага 10 фазы 2, узел 3',
        'allowed': ('.claude/work/decision-digest.md',),
        'population': (
            ('docs/spec/statistics-aggregates.json', None),
        ),
    },
    {
        'name': 'природу подъёма ступени различает актор строки журнала',
        # Снято GAPS_CLOSE_3 шага 10 фазы 2, узел 3 (B5). Актор отвечает на
        # вопрос «кем порождён ход», и у джобы, запущенной ручным триггером,
        # он равен принципалу (перенос контекста в порождённый тред —
        # требование docs/models/domain/other/Auditable.md). По актору
        # автоматическая остановка, найденная в ручном прогоне детекции,
        # считалась бы остановкой держателя — то есть разрез давал бы ровно
        # ту ошибку, против которой заведён. Пришедшая редакция: различитель
        # природы — КОД тропы, которым ручная поверхность метит свои
        # операции, и он же различитель у отчёта о происшествии.
        'pattern': r'actorIsPrincipal',
        'arrived': r'codeIsManualOperation',
        'date': '2026-09-07',
        'source': 'GAPS_CLOSE_3 шага 10 фазы 2, узел 3',
        'allowed': ('.claude/work/decision-digest.md',),
        'population': (
            ('docs/spec/statistics-aggregates.json', None),
            ('docs/rules/statistics-aggregates.md', r'различает\s+её\s+КОДОМ'),
        ),
    },
    {
        'name': 'разрез ручных счётчиков зовётся по природе и считает держателя',
        # Снято GAPS_CLOSE_3 шага 10 фазы 2, узел 3 (B5, верификация финальной
        # правки). Слово «природа» у отчёта о происшествии занято своей осью —
        # «состояние против происшествия» (docs/models/domain/other/AnomalyReport.md),
        # и разрез, названный так же, давал одному слову два несовместимых
        # смысла на одной сущности. Популяция же у обоих ручных счётчиков —
        # ТРОПА, а не человек: код одинаков у человека и у скрипта, дошедшего
        # до той же поверхности (docs/models/domain/other/Auditable.md).
        'pattern': r'разрез\s+по\s+природе'
                   r'|Природу\s+различает'
                   r'|природы\s+подъёма\s+ступени'
                   r'|природы\s+отчёта\s+о\s+происшествии'
                   r'|поднятых\s+командой\s+держателя'
                   r'|заведённых\s+операцией\s+держателя',
        'arrived': r'разрез\s+по\s+тропе',
        'date': '2026-09-07',
        'source': 'GAPS_CLOSE_3 шага 10 фазы 2, узел 3',
        'allowed': ('.claude/work/decision-digest.md',),
        'population': (
            ('docs/spec/statistics-aggregates.json', None),
            ('docs/rules/statistics-aggregates.md', None),
        ),
    },
    {
        'name': 'счётчик значению нужен, когда оно достижимо в денежных суммах',
        # Снято GAPS_CLOSE_3 шага 10 фазы 2, узел 3 (верификация захода 5).
        # Критерий исключал ровно то значение, которому счётчик всё же нужен:
        # «полнота разбивки не оценивалась» в денежные суммы недостижимо по
        # собственному охранному инварианту, а числа его не хранило бы ничто,
        # и здоровое значение той же оси переставало быть выводимым остатком.
        # Пришедшая редакция: счётчик — у всякого НЕЗДОРОВОГО значения.
        'pattern': r'достижимо\s+у\s+сделки,\s+вошедшей\s+в\s+денежные\s+суммы',
        'arrived': r'у\s+всякого\s+нездорового\s+значения',
        'date': '2026-09-07',
        'source': 'GAPS_CLOSE_3 шага 10 фазы 2, узел 3',
        'allowed': ('.claude/work/decision-digest.md',),
        'population': (
            ('docs/rules/statistics-aggregates.md', None),
        ),
    },
    {
        'name': 'поверхность ручных операций ядра не построена',
        # Снято GAPS_CLOSE_3 шага 10 фазы 2, узел 3 (свип стыка B5).
        # Поверхность построена: SafetyController несёт постановку и снятие
        # ступени, обе доходят до писателя отчёта о происшествии и через него
        # до outbox. Не построен КЛАСС события HoldReleased — значения
        # перечня и содержимого у него нет. Прежняя редакция путала эти две
        # вещи в четырёх носителях и выводила из них, что журнал не
        # фиксирует возобновление торговли вовсе.
        'pattern': r'приезжает\s+поверхностью\s+ручных\s+операций'
                   r'|тропы\s+снятия\s+ещё\s+нет'
                   r'|ход,\s+строящий\s+тропу\s+снятия'
                   r'|а\s+её\s+не\s+построено',
        'arrived': r'построенн\w*\s+поверхност\w*\s+ручных\s+операций'
                   r'|поверхность\s+ручных\s+операций\s+ядра\s+построена',
        'date': '2026-09-07',
        'source': 'GAPS_CLOSE_3 шага 10 фазы 2, узел 3',
        'allowed': ('.claude/work/decision-digest.md',),
        'population': (
            ('docs/spec/event-actor-presence.json', None),
            ('docs/models/domain/other/Auditable.md',
             r'ручная\s+тропа\s+\*\*построена\*\*'),
            ('docs/models/domain/other/AuditRecord.md', None),
            ('.claude/work/backlog.md', r'уже\s+построенную\*\*\s+поверхность'),
            ('docs/architecture/contracts.md',
             r'поверхность\s+ручных\s+операций\s+ядра\s+построена'),
        ),
    },
    {
        'name': 'домен сверяет контекст тенанта с собственным кэшем членств auth',
        # Снято GAPS_CLOSE_3 шага 10 фазы 2, узел 4 (A3). Прежняя редакция
        # объявляла у домена сверку контекста с кэшем членств: механизм
        # неисполним (строки «домен → auth» за членствами в таблице
        # синхронных вызовов нет, а точка резолва мутирует по построению и
        # объявлена тропой периметра) и ложен о коде — кэша членств нет ни у
        # одного домена, а `strategies` принимает заголовок как радиус отбора
        # без всякой сверки. Пришедшая редакция: домен принимает тенанта
        # операндом вызова и не сверяет; радиус держат три звена (вывод
        # тенанта периметром, сетевая политика, отбор по полученному
        # тенанту), цена названа, оживитель — второй субъект.
        # ТРИ ПИСЬМЕННЫЕ ФОРМЫ, и одной было мало: первая редакция шаблона
        # видела только глагольную («сверяет контекст с …») и давала
        # ЗЕЛЁНЫЙ на двух живых javadoc'ах — «сверка контекста с членствами
        # идёт по заголовку» и «сверяется с членствами». Отсюда явные
        # альтернативы под отглагольное существительное и под возвратную
        # форму; `unless` гасит места, где термин стои́т рядом с ОТРИЦАНИЕМ
        # (действующая редакция: «сверки нет ни у одного домена»).
        'pattern': r'сверя[а-яё]*\s+контекст[а-яё]*\s+с\s+(собственным\s+)?(кэшем\s+)?членств'
                   r'|сверк[а-яё]*\s+контекст[а-яё]*\s+с\s+(кэшем\s+)?членств'
                   r'|сверя[а-яё]*\s+с\s+членств'
                   r'|проверкой\s+контекста\s+на\s+каждом\s+вызове'
                   r'|только\s+для\s+сверки\s+тенанта\s+с\s+членствами',
        # `unless` СУЖЕН до одной альтернативы намеренно. Первая редакция
        # несла ещё `не сверяет|не распространён|названное ограничение` — и
        # гасила НАСТОЯЩИЙ дефект у четырёх носителей популяции из девяти:
        # окно гашения ±160 знаков, а `arrived` этой же записи содержит
        # «не сверяет», то есть возле удостоверяемого пассажа снятая
        # редакция становилась недетектируемой по построению. Замер: корпус
        # зелен и с одной альтернативой — три прочие не гасят ничего
        # сегодня и погасили бы дефект завтра.
        'unless': r'нет\s+ни\s+у\s+одного',
        'arrived': r'принимает\s+операндом\s+вызова\s+и\s+не\s+сверяет',
        'date': '2026-09-07',
        'source': 'GAPS_CLOSE_3 шага 10 фазы 2, узел 4',
        'allowed': ('.claude/work/decision-digest.md',),
        'population': (
            ('docs/architecture/contracts.md', None),
            ('docs/rules/api-access-policy.md',
             r'Сверки\s+контекста\s+с\s+членствами\s+нет'),
            ('docs/architecture/platform.md',
             r'верки\s+контекста\s+на\s+вызове[^.]{0,24}нет'),
            ('docs/models/domain/other/AuditRecord.md',
             r'доверие\s+к\s+самому\s+значению\s+тенанта\s+здесь'),
            ('docs/models/domain/aggregate/Strategy.md',
             r'\*\*вторым\*\*\s+его\s+объявлением'),
            ('.claude/work/backlog.md',
             r'добытчик\s+доверенного\s+тенанта\s+у\s+домена'),
            ('.claude/decisions/service-architecture-design-pass.md',
             r'принимает\s+операндом\s+вызова\s+и\s+не\s+сверяет'),
            ('services/strategies/src/main/java/com/example/strategies/util/Constants.java',
             r'ВТОРЫМ\s+объявлением'),
            ('services/strategies/src/main/java/com/example/strategies/api/model/'
             'request/CreateStrategyApiRequest.java',
             r'ВТОРЫМ\s+объявлением'),
        ),
    },
    {
        'name': 'роль базы — на сервис (манифест кластера)',
        # Снято GAPS_CLOSE_3 шага 10 фазы 2, узел 4 (A7). Комментарий
        # манифеста нёс формулу «база на сервис», которую дом раскладки снял
        # своим текстом: счёт идёт по ВЛАДЕЛЬЦУ данных, и у процесса с двумя
        # владельцами баз две, а ролей тоже две. Манифест — тот носитель, по
        # которому заводятся ресурсы, и формула давала одну роль вместо двух.
        'pattern': r'Роли-владельцы\s+баз\.\s+База\s+на\s+сервис',
        'arrived': r'Роль\s+—\s+на\s+ВЛАДЕЛЬЦА\s+ДАННЫХ',
        'date': '2026-09-07',
        'source': 'GAPS_CLOSE_3 шага 10 фазы 2, узел 4',
        'allowed': ('.claude/work/decision-digest.md',),
        'population': (
            ('deploy/base/data/postgres-cluster.yaml', None),
            ('docs/architecture/data-ownership.md',
             r'Роль\s+базы\s+—\s+на\s+владельца\s+данных'),
        ),
    },
    {
        'name': 'у процесса аудита и статистики два источника данных',
        # Снято GAPS_CLOSE_3 шага 10 фазы 2, узел 4 (A7). Разделение
        # писателей двух баз одного процесса восстановлено грантами, и это
        # меняет счёт: подключений три, а не два, — своя база у каждого
        # владельца под своей ролью плюс чтение журнала под ролью статистики.
        # Прежнее число писалось при одной роли на процесс.
        'pattern': r'двумя\s+источниками\s+данных',
        'arrived': r'держит\s+это\s+\*\*грант\*\*',
        'date': '2026-09-07',
        'source': 'GAPS_CLOSE_3 шага 10 фазы 2, узел 4',
        'allowed': ('.claude/work/decision-digest.md',),
        'population': (
            ('docs/rules/statistics-aggregates.md', None),
            ('docs/architecture/data-ownership.md',
             r'подключений\s+к\s+базам\s+у\s+процесса\s+три'),
        ),
    },
    {
        'name': 'состав каталога равен инвентарю',
        # Снято GAPS_CLOSE_3 шага 10 фазы 2, узел 5 (C9). Клейм обещал
        # ДВУСТОРОННЮЮ проверяемость, а энфорсер мерит одно вложение
        # (`services/` ⊆ инвентарь, `libs/` ⊆ таблица артефактов). Обратное
        # вложение сегодня ложно ровно на единице, которую строит этот шаг.
        # Пришедшая редакция называет измеряемое вложение и объявляет
        # непокрытость обратного. ПОПУЛЯЦИЯ ШИРЕ ОДНОГО ДОМА: та же формула
        # жила в README каталога, в комментарии корневого pom.xml и ДВАЖДЫ
        # в шапке самого энфорсера — свип по одному носителю оставил бы
        # снятую редакцию в трёх местах, причём одно из них цитирует её как
        # свой предмет.
        'pattern': r'состав\s+каталога\s+равен\s+инвентарю'
                   r'|Состав\s+каталога\s+равен\s+таблице'
                   r'|каталог\s+services/\s+равен\s+инвентарю'
                   r'|равенстве\s+держится\s+проверяемость',
        'arrived': r'нет\s+единиц\w*,\s+котор\w+\s+нет\s+в\s+инвентаре'
                   r'|нет\s+артефакта,\s+которого\s+нет\s+в\s+таблице',
        'date': '2026-09-07',
        'source': 'GAPS_CLOSE_3 шага 10 фазы 2, узел 5',
        'allowed': ('.claude/work/decision-digest.md',),
        'population': (
            ('.claude/rules/structure.md', None),
            ('tools/deploy-layout-check.py', None),
            ('services/README.md', None),
            # pom.xml лежит ВНЕ области свипа (ROOTS корневых файлов не
            # берёт, ось 20 мерит каталоги) — и потому назван популяцией:
            # ось 4 читает носитель по пути, а не по области. Пробел области
            # назван в `.claude/work/backlog.md`.
            ('pom.xml', r'нет\s+единицы,\s+которой\s+нет\s+в\s+инвентаре'),
        ),
    },
    {
        'name': 'у правила представления ровно два исключения',
        # Снято GAPS_CLOSE_3 шага 10 фазы 2, узел 5 (A8). Модель журнала
        # ссылалась на «ровно два объявленных исключения того же правила»,
        # тогда как правило объявляло другие два, и радиус отбора не был
        # покрыт ни одним. Введено третье исключение — операнд, по которому
        # строку ищут, — а счёт снят у ОБЕИХ сторон: перечень пополняется,
        # и число старело бы молча на следующем исключении.
        'pattern': r'ровно\s+два\s+объявленных\s+исключения'
                   r'|\*\*Два\s+исключения:\*\*',
        'arrived': r'по\s+объявленным\s+там\s+исключениям'
                   r'|\*\*Исключения\*\*\s+\(счёта\s+у\s+перечня\s+нет',
        'date': '2026-09-07',
        'source': 'GAPS_CLOSE_3 шага 10 фазы 2, узел 5',
        'allowed': ('.claude/work/decision-digest.md',),
        'population': (
            ('docs/rules/persistence-representation.md',
             r'операнд,\s+по\s+которому\s+строку\s+ИЩУТ'),
            ('docs/models/domain/other/AuditRecord.md',
             r'по\s+объявленным\s+там\s+исключениям'),
        ),
    },
    {
        'name': 'построенных durable-групп уже больше одной',
        # Снято GAPS_CLOSE_3 шага 10 фазы 2, узел 5 (C2). Довод сквозного
        # правила фиксации смещения стоял на счёте построенных групп;
        # прогон по конфигурациям даёт ОДНУ (`trading-core.strategy-facts`;
        # группа периметра эфемерна по построению). Довод при этом верен и
        # держится на РОДЕ потребителя — им и заменён.
        'pattern': r'построенных\s+durable-групп\s+уже\s+больше\s+одной',
        'arrived': r'сквозное\s+по\s+\*\*роду\s+потребителя\*\*',
        'date': '2026-09-07',
        'source': 'GAPS_CLOSE_3 шага 10 фазы 2, узел 5',
        'allowed': ('.claude/work/decision-digest.md',),
        'population': (('docs/architecture/data-ownership.md', None),),
    },
    {
        'name': 'пара сетевой политики у поверхности audit-statistics уже есть',
        # Снято GAPS_CLOSE_3 шага 10 фазы 2, узел 5 (A6). Рабочий файл
        # дважды утверждал, что пара существует; манифест периметра
        # объявляет её отсутствие прямым текстом. Клейм исключал пару из
        # перечня того, что обязан завести `CODE` шага, а кластер работает
        # при запрете по умолчанию — обнаружилось бы первым живым прогоном.
        'pattern': r'пара\s+сетевой\s+политики\s+у\s+поверхности\s+уже\s+есть'
                   r'|строка\s+вызова\s+и\s+пара\s+политики\s+существуют',
        # Редакция сменилась ещё раз ходом `CODE` шага 10 (2026-09-11, чистка
        # бэклога): носитель пришедшего — манифест периметра, не рабочий файл.
        # Что действует теперь — объявляет ключ `arrived`.
        'arrived': r'audit-statistics-ingress-from-bff',
        'date': '2026-09-07',
        'source': 'GAPS_CLOSE_3 шага 10 фазы 2, узел 5',
        'allowed': ('.claude/work/decision-digest.md',),
        'population': (
            ('deploy/base/services/bff.yaml', None),
            ('.claude/work/history/2026-09-11-phase-2-step-10-audit-statistics/phase-2-step-10-chronicle.md',
             r'встречная\s+ПАРА\s+сетевой\s+политики'),
        ),
    },
    {
        'name': 'непокрытая доля trading-core — два класса',
        # Снято GAPS_CLOSE_3 шага 10 фазы 2, узел 5 (C1). Таблица причин
        # объявлена ключуемой производителем, а несла строку-псевдо-
        # производителя «фаза 4» и счёт «два класса» при четырёх; строки
        # `strategies` не было вовсе. Пришедшая редакция: ключ строго по
        # производителю, срок появления предмета — причина внутри строки,
        # счётов не осталось ни одного.
        'pattern': r'не\s+вся\s+доля,\s+а\s+два\s+класса'
                   r'|Классы\s+именует\s+ровно\s+одна\s+строка',
        'arrived': r'причина\s+названа\s+у\s+каждого\s+непокрытого\s+класса'
                   r'|Классы\s+именует\s+не\s+всякая\s+строка',
        'date': '2026-09-07',
        'source': 'GAPS_CLOSE_3 шага 10 фазы 2, узел 5',
        'allowed': ('.claude/work/decision-digest.md',),
        'population': (
            ('docs/models/domain/other/AuditRecord.md', None),
        ),
    },
    {
        'name': 'перечень обязательного входа равен колонкам not null',
        # Снято GAPS_CLOSE_3 шага 10 фазы 2, узел 5 (C8). Клейм заведён как
        # ОХРАНА от расхождения ветви отказа со схемой и был ложен на два
        # члена: `id` присваивает база, `recorded_at` ставит код приёма —
        # во входящем сообщении их нет по построению. Пришедшая редакция
        # оставляет равенство механическим, вычитая колонки стороны приёма.
        'pattern': r'равен\s+множеству\s+колонок\s*\n?\s*`?not\s+null`?\s+схемы\s+строки,\s+и',
        'arrived': r'за\s+вычетом\s+тех,\s+которые\s+производит\s+сама\s+сторона',
        'date': '2026-09-07',
        'source': 'GAPS_CLOSE_3 шага 10 фазы 2, узел 5',
        'allowed': ('.claude/work/decision-digest.md',),
        'population': (('docs/models/domain/other/AuditRecord.md', None),),
    },
    {
        'name': 'верификация финальной правки без права новой доработки',
        # Снято ответом держателя по PROC-Q7 (2026-09-07, вариант C). Снятая
        # редакция: финальная доработка возвращается критику на верификацию
        # БЕЗ ПРАВА НОВОЙ ДОРАБОТКИ — то есть на блокирующую находку
        # верификации узел приземлялся с остатком. Пришедшая: доработка по
        # блокирующим находкам верификации законна, но верификаций у узла не
        # больше двух, а по исчерпании потолка узел останавливается сигналом
        # С5 детектора. Клауза стоя́ла в двух носителях — доме машины
        # мини-петли и гейте простановки статуса, — и оба обязаны предъявить
        # потолок: гейт, оставшийся со снятой клаузой, запрещал бы ровно ту
        # доработку, которую дом разрешил.
        'pattern': r'без\s+права\s+новой\s+доработки',
        'arrived': r'верификаци\w+\s+у\s+узла\s+(?:не\s+)?больше\s+двух',
        'date': '2026-09-07',
        'source': 'ответ держателя PROC-Q7 = C '
                  '(.claude/decisions/mini-loop-verification-ceiling.md)',
        'allowed': ('.claude/decisions/mini-loop-verification-ceiling.md',),
        'population': (('.claude/processes/roadmap-step-execution.md', None),
                       ('.claude/skills/update-roadmap-progress.md', None),
                       ('.claude/skills/stagnation-detection.md',
                        r'С5\s+—\s+мини-петля\s+узла\s+не\s+сходится'),
                       ('.claude/decisions/per-node-closure-frame.md',
                        r'потолк\w*\s+верификаций')),
    },
    {
        'name': 'величину строки состояния приёма пишет проход опроса',
        # Снято дизайн-проходом по форме исполнителя приёма (2026-09-08).
        # Снятая редакция назначала писателем «проход опроса», в том числе
        # холостой, — момент, который существует у опрашивающего цикла и не
        # существует у слушателя контейнера: его метод вызывается сообщением.
        # Пришедшая редакция: величины состава и живости пишет тик
        # ReceptionStateJob, и тик МОЛЧИТ, когда приём не жив.
        'pattern': r'проход\w*\s+опроса',
        'arrived': r'ReceptionStateJob|такт\w*\s+тика',
        'date': '2026-09-08',
        'source': 'дизайн-проход шага 10 фазы 2, позиция 2 '
                  '(.claude/decisions/reception-executor-design-pass.md)',
        'allowed': ('.claude/work/decision-digest.md',
                    '.claude/decisions/reception-executor-design-pass.md',
                    '.claude/work/history/2026-09-11-phase-2-step-10-audit-statistics/phase-2-step-10-design-pass.md'),
        'population': (('docs/models/domain/other/AuditRecord.md',
                        r'ReceptionStateJob'),
                       ('docs/spec/audit-journal.json', r'ReceptionStateJob'),
                       ('docs/components/ReceptionStateJob.md', None)),
    },
    {
        'name': 'у остановки приёма состояния не заводится',
        # Снято дизайн-проходом (2026-09-08), находка A4 DOCS_CHECK_4. Снятая
        # редакция выводила остановку приёма из-под клаузы «снятие только
        # вручную» посылкой «состояния у неё нет» — при том, что дом механизма
        # заводит not null-флаг с отдельной транзакцией и автоматическим
        # снятием. Пришедшая редакция: состояние есть, а исключение стои́т на
        # РОДЕ реакции — она не блокирующая, она свидетельствует.
        'pattern': r'потому\s+что\s+состояния\s+не\s+заводится',
        'arrived': r'не\s+блокирующая\s+реакция',
        'date': '2026-09-08',
        'source': 'дизайн-проход шага 10 фазы 2, позиция 1 '
                  '(.claude/decisions/reception-executor-design-pass.md)',
        'allowed': ('.claude/work/decision-digest.md',
                    '.claude/work/history/2026-09-11-phase-2-step-10-audit-statistics/phase-2-step-10-design-pass.md'),
        'population': (('docs/rules/error-handling-policy.md',
                        r'не\s+блокирующая\s+реакция'),),
    },
    {
        'name': 'признак разрыва по сроку хранения без писателя снятия',
        # Снято дизайн-проходом (2026-09-08), находки A3 и B3 DOCS_CHECK_4.
        # Снятая редакция держала признак булевой колонкой
        # lag_exceeded_retention: ни начального значения, ни писателя снятия,
        # ни операнда обнаружения (позиция earliest сбрасывает вышедшее за
        # пределы смещение молча). Пришедшая: колонка lag_gap_at — МОМЕНТ,
        # ставит слушатель по разрыву в смещениях, гасит JournalCleanupJob.
        # Шаблон — ПРОЗА снятой редакции, а не имя колонки: код ветви разрыва
        # `LAG_EXCEEDED_RETENTION` остаётся действующим (его разбирает
        # tools/derive/journal-continuity-branches.sh), а свип детектора
        # регистр не различает.
        'pattern': r'обнаружив,\s+что\s+зафиксированное\s+смещение\s+вышло',
        'arrived': r'lag_gap_at',
        'date': '2026-09-08',
        'source': 'дизайн-проход шага 10 фазы 2, позиция 3 '
                  '(.claude/decisions/reception-executor-design-pass.md)',
        'allowed': ('.claude/work/decision-digest.md',
                    '.claude/work/history/2026-09-11-phase-2-step-10-audit-statistics/phase-2-step-10-design-pass.md'),
        'population': (('docs/models/domain/other/AuditRecord.md', r'lag_gap_at'),
                       ('docs/spec/audit-journal.json', r'lag_gap_at'),
                       ('docs/components/AuditEventListener.md', None),
                       ('docs/components/JournalCleanupJob.md', None)),
    },
    {
        'name': 'отказ продвинуть смещение обеспечен умолчанием клиента',
        # Снято дизайн-проходом (2026-09-08), находка B1 DOCS_CHECK_4. Снятая
        # редакция опирала на «умолчание клиента» ДВА разных клейма: порядок
        # «следствие → фиксация смещения» (верно) и отказ продвинуть смещение
        # на отказе обработки (ложно: умолчание восстанавливает запись и
        # смещение ПРОДВИГАЕТ). Пришедшая: отказ обеспечен обработчиком ошибок
        # контейнера без ограничения числа попыток.
        'pattern': r'умолчание\s+клиента,\s+и\s+обратного',
        'arrived': r'обработчик\w*\s+ошибок\s+контейнера',
        'date': '2026-09-08',
        'source': 'дизайн-проход шага 10 фазы 2, позиция 1 '
                  '(.claude/decisions/reception-executor-design-pass.md)',
        'allowed': ('.claude/work/history/2026-09-11-phase-2-step-10-audit-statistics/phase-2-step-10-design-pass.md',),
        'population': (('docs/models/domain/other/AuditRecord.md',
                        r'обработчик\w*\s+ошибок\s+контейнера'),
                       ('docs/components/AuditEventListener.md', None)),
    },
    {
        'name': 'дом ограничений отбора у поверхности чтения audit-statistics один',
        # Снято приземлением узла 2 GAPS_CLOSE_4 (2026-09-08), находка W1
        # верификации 2. Доработка узла завела ВТОРОЙ дом — у владельца
        # агрегатов, — а указатели потребителей остались вести за
        # ограничениями ОБЕИХ выборок к дому журнала; один из них сверх того
        # пересказывал состав. Три письменные формы одной редакции собраны
        # альтернацией шаблона. Что встало взамен — объявляет ключ `arrived`,
        # а не этот комментарий (`.claude/rules/self-description-form.md`).
        'pattern': (r'(?:Ограничения\s+отбора\s+\(радиус\s+тенанта'
                    r'|состав\s+здесь\s+не\s+пересказывается,\s+дом\s+—'
                    r'|поверхность\s+чтения\s+—\s+.?docs/models)'),
        'arrived': r'statistics-aggregates\.md.{0,3}§«Что это за числа и кто их читает»',
        'date': '2026-09-08',
        'source': 'приземление узла 2 GAPS_CLOSE_4 шага 10 фазы 2, находка W1',
        'allowed': (),
        'population': (
            ('docs/architecture/contracts.md', None),
            ('docs/architecture/services.md',
             r'ограничения\s+отбора\s+агрегатной\s+вместе\s+с'),
            ('.claude/work/backlog.md', None),
            ('.claude/rules/knowledge-ownership-by-service.md', None),
        ),
    },
    {
        'name': 'вопрос файла-носителя агрегатов — состав и способ счёта',
        # Снято тем же приземлением, находка W3. Дом объявлял вопросом
        # «состав и способ счёта», а доработка внесла в него ограничения
        # отбора выборки, которой строки читаются: ни состав агрегата, ни
        # способ его счёта. Дом при этом выбран верно — расходился
        # объявленный вопрос носителя (`.claude/rules/structure.md`
        # §Принципы). Что встало взамен — объявляет ключ `arrived`.
        'pattern': r'состав\s+и\s+способ\s+счёта\s+агрегатов',
        'arrived': r'Какое\s+правило\s+действует\s+для\s+агрегатов\s+статистики',
        'date': '2026-09-08',
        'source': 'приземление узла 2 GAPS_CLOSE_4 шага 10 фазы 2, находка W3',
        'allowed': (),
        'population': (
            ('docs/rules/statistics-aggregates.md', None),
            ('.claude/knowledge-tree.md',
             r'Какое правило действует для агрегатов статистики\?'),
        ),
    },
    {
        'name': 'отложенное шагом 10 — «состав точек» поверхности чтения',
        # Снято правками 1-2 узла 2 GAPS_CLOSE_4 (2026-09-08), находка A7 и
        # V1 верификации 1; запись заведена приземлением узла (находка Н3
        # гейта выхода: свипов в предписанной форме закрытие не предъявило).
        # Под именем «состав точек» откладывалась и выборка, чей состав
        # корпусом уже задан. Что встало взамен — объявляет ключ `arrived`
        # (`.claude/rules/self-description-form.md`).
        'pattern': r'состав\s+точек',
        'arrived': r'состав\s+экранных\s+операций',
        'date': '2026-09-08',
        'source': 'узел 2 GAPS_CLOSE_4 шага 10 фазы 2, правки 1-2 (A7, V1)',
        'allowed': ('.claude/work/roadmap/phase-2.md',),
        'population': (
            ('docs/models/domain/other/AuditRecord.md', None),
            ('.claude/work/backlog.md', None),
        ),
    },
    {
        'name': 'заголовок тенанта читается сегодня одним владельцем',
        # Снято правкой 5 узла 2 GAPS_CLOSE_4 (2026-09-08), находка D2;
        # запись заведена приземлением узла по той же находке Н3. Счёт
        # членов ЧУЖОГО перечня стоя́л в носителе, который тем перечнем не
        # владеет, и устаревал при каждом новом принимающем домене. Что
        # встало взамен — объявляет ключ `arrived`.
        'pattern': r'сегодня\s+одним\s+владельцем',
        'arrived': r'теми\s+доменами,\s+которые\s+его\s+принимают',
        'date': '2026-09-08',
        'source': 'узел 2 GAPS_CLOSE_4 шага 10 фазы 2, правка 5 (D2)',
        'allowed': (),
        'population': (
            ('docs/rules/api-access-policy.md', None),
        ),
    },
    {
        'name': 'число форм невоспроизводимости у энфорсера носителя команды',
        # Снято закрытием Ш1-Ш3 шестого прогона гейта выхода из GAPS_CLOSE_4
        # (2026-09-08) вводом оси 9. Прежние редакции называли ЧИСЛО форм,
        # которые энфорсер мерит; число выводится из состава осей инструмента и
        # потому стареет при каждом его расширении. Что встало взамен —
        # объявляет ключ `arrived`.
        'pattern': (r'(?:две\s+статически\s+различимые\s+форм[а-яё]{0,2}'
                    r'|одну\s+форм[а-яё]{0,2}\s+невоспроизводимости)'),
        'arrived': r'три\s+статически\s+различимые',
        'date': '2026-09-08',
        'source': 'закрытие Ш1-Ш3 шестого прогона гейта GAPS_CLOSE_4 шага 10 фазы 2',
        'allowed': (),
        'population': (
            ('.claude/processes/roadmap-step-execution.md',
             r'Форм\s+невоспроизводимости\s+у\s+энфорсера\s+три'),
            ('.claude/skills/update-roadmap-progress.md', None),
        ),
    },
    {
        'name': 'момент писателя пересчёта агрегатов — тик плюс ручной триггер',
        # Снято приземлением узла 2 `GAPS_CLOSE_5` (2026-09-09). Прежняя
        # редакция давала писателю второй момент — внерасписанный запуск через
        # фасад, — которого у джоб этого сервиса не бывает: поверхность модуля
        # объявлена только читающей. Что действует теперь — объявляет ключ
        # `arrived`.
        'pattern': r'плюс\s+ручной\s+триггер',
        'arrived': r'и\s+другого\s+момента\s+у\s+неё\s+нет',
        'date': '2026-09-09',
        'source': 'приземление узла 2 `GAPS_CLOSE_5`, шаг 10 фазы 2',
        'allowed': (),
        'population': (
            ('docs/rules/statistics-aggregates.md',
             r'и\s+другого\s+момента\s+у\s+неё\s+нет'),
        ),
    },
    {
        'name': 'полный пересчёт — вторая форма со своим моментом',
        # Снято приземлением узла 2 `GAPS_CLOSE_5` (2026-09-09). Прежняя
        # редакция объявляла полный пересчёт отдельной формой, чей момент —
        # ручной триггер держателя; момента этого у сервиса не существует, и
        # форма оставалась без достижимого писателя. Что действует теперь —
        # объявляет ключ `arrived`.
        'pattern': r'та\s+же\s+джоба\s+по\s+всему\s+журналу',
        'arrived': r'«полный»\s+есть\s+значение\s+операнда\s+окна',
        'date': '2026-09-09',
        'source': 'приземление узла 2 `GAPS_CLOSE_5`, шаг 10 фазы 2',
        'allowed': (),
        'population': (
            ('docs/rules/statistics-aggregates.md',
             r'«полный»\s+есть\s+значение\s+операнда\s+окна'),
        ),
    },
    {
        'name': 'строки агрегатов восполнимы полностью и безусловно',
        # Снято приземлением узла 2 `GAPS_CLOSE_5` (2026-09-09). Прежняя
        # редакция клейма условия не несла, а чистка непроизводственного
        # окружения делала его ложным: пересчёт по усечённому журналу заменил
        # бы верные числа граничных суток частичными при свежем моменте
        # сборки. Носителей у клейма было три, и один выражал его другими
        # словами. Что действует теперь — объявляет ключ `arrived`.
        'pattern': r'восполнимы\s+полностью',
        'arrived': r'на\s+всём,\s+что\s+журнал\s+несёт',
        'date': '2026-09-09',
        'source': 'приземление узла 2 `GAPS_CLOSE_5`, шаг 10 фазы 2',
        'allowed': (),
        'population': (
            ('docs/rules/statistics-aggregates.md',
             r'на\s+всём,\s+что\s+журнал\s+несёт'),
            ('docs/models/domain/other/AuditRecord.md',
             r'на\s+всём,\s+что\s+журнал\s+несёт'),
            ('docs/components/JournalCleanupJob.md',
             r'Косвенно\s+он\s+область\s+пересчёта\s+сужает'),
        ),
    },
    {
        'name': 'фильтры поверхности чтения отложены целиком до фронта',
        # Снято приземлением узла 3 `GAPS_CLOSE_5` (2026-09-09). Прежняя
        # редакция откладывала до шага 12 «срезы, фильтры и агрегации»
        # целиком — то есть и отборы по колонкам радиуса, чьё назначение
        # объявлено домом представления в БД и под чьи две тропы миграция
        # шага 10 уже заводит индексы. Что действует теперь — объявляет ключ
        # `arrived`.
        'pattern': r'срезы,\s+фильтры\s+и\s+агрегации',
        'arrived': r'сверх\s+объявленных',
        'date': '2026-09-09',
        'source': 'приземление узла 3 `GAPS_CLOSE_5`, шаг 10 фазы 2',
        'allowed': (),
        'population': (
            ('docs/models/domain/other/AuditRecord.md', None),
            ('.claude/work/backlog.md', None),
        ),
    },
    {
        'name': 'единственный межбазовый читатель журнала — джоба пересчёта',
        # Снято приземлением узла 3 `GAPS_CLOSE_5` (2026-09-09). Прежняя
        # редакция называла читателем третьего подключения одну джобу
        # пересчёта; обязательство отдавать нижнюю границу полноты вместе со
        # строками агрегатов даёт тому же подключению второго читателя. Что
        # действует теперь — объявляет ключ `arrived`.
        'pattern': r'джоб\w*\s+пересчёта\s+(?:статистики\s+)?читает',
        'arrived': r'джоб\w+\s+пересчёта\s+и\s+агрегатн\w+\s+выборк\w+',
        'date': '2026-09-09',
        'source': 'приземление узла 3 `GAPS_CLOSE_5`, шаг 10 фазы 2',
        'allowed': (),
        'population': (
            ('docs/architecture/data-ownership.md', None),
            ('docs/architecture/services.md', None),
            ('docs/spec/owner-role-grants.json', None),
        ),
    },
    {
        'name': 'строка состояния приёма — «вторая таблица базы»',
        # Снято приземлением узла 4 `GAPS_CLOSE_5` (2026-09-09). Счёт в имени
        # раздела расходился с раскладкой владения: у базы `audit` есть и
        # предмет, к журналу отношения не имеющий, и строит его тот же шаг.
        'pattern': r'Втор[а-яё]+\s+таблица\s+базы',
        'arrived': r'Строка\s+состояния\s+приёма\s+—\s+таблица',
        'date': '2026-09-09',
        'source': 'приземление узла 4 `GAPS_CLOSE_5`, шаг 10 фазы 2',
        'allowed': (),
        'population': (
            ('docs/models/domain/other/AuditRecord.md', None),
            ('docs/components/ReceptionStateJob.md',
             r'раздел\s+о\s+строке\s+состояния\s+приёма'),
        ),
    },
    {
        'name': 'у чистки журнала нет ни ручного фасада, ни защиты от перекрытия',
        # Снято приземлением узла 4 `GAPS_CLOSE_5` (2026-09-09). Экземпция
        # соседнего тика была взята целиком, при том что вторая её половина
        # стои́т на форме такта, а у чистки такт другой.
        'pattern': r'по\s+тем\s+же\s+двум\s+доводам',
        'arrived': r'Защита\s+от\s+перекрытия\s+у\s+него\s+ЕСТЬ',
        'date': '2026-09-09',
        'source': 'приземление узла 4 `GAPS_CLOSE_5`, шаг 10 фазы 2',
        'allowed': (),
        'population': (
            ('docs/components/JournalCleanupJob.md', None),
            ('.claude/rules/codestyle.md',
             r'Экземпция\s+снимает\s+две\s+обязанности'),
            ('.claude/skills/conventions-review.md',
             r'Экземпции\s+две,\s+и\s+снимают\s+их\s+разные'),
        ),
    },
    {
        'name': 'глубина чистки журнала приезжает осью окружения',
        # Снято приземлением узла 4 `GAPS_CLOSE_5` (2026-09-09). Ось несёт
        # профиль, а предикату исполнителя нужно число; носителя перевода не
        # держал ни один файл.
        'pattern': (r'Глубина\s+приезжает\s+(своей\s+)?осью\s+окружения'
                    r'|Глубина\s+хранения\s+журнала\s+аудита:\s+СВОЯ\s+ось'),
        'arrived': r'лубина\s+в\s+сутках\s+—\s+\*{0,2}величина\s+конфигурации',
        'date': '2026-09-09',
        'source': 'приземление узла 4 `GAPS_CLOSE_5`, шаг 10 фазы 2',
        'allowed': (),
        'population': (
            ('docs/models/domain/other/AuditRecord.md', None),
            ('docs/components/JournalCleanupJob.md', None),
            ('deploy/dev/env.yaml', r'профиль,\s+не\s+число'),
            ('deploy/stage/env.yaml', r'профиль,\s+не\s+число'),
            ('deploy/prod/env.yaml', r'профиль,\s+не\s+число'),
            ('docs/architecture/platform.md',
             r'глубину\s+в\s+сутках\s+несёт\s+величина\s+конфигурации'),
        ),
    },
    {
        'name': 'писатель ступени вне ребра — наличный, а не объявленный',
        # Снято узлом 2 GAPS_CLOSE_6 шага 10 фазы 2 (2026-09-11, закрытие B3
        # и A3). Прежняя редакция писала настоящим временем, что журнальная
        # тропа синка ставок ставит мягкий запрет входов сама, и на этом
        # примере стояли и цена единственного писателя факта, и перечень
        # «всех писателей статуса» дома монотонности. В дереве такой тропы
        # нет: подъём применяет только ребро. Что действует теперь —
        # объявляет ключ `arrived`.
        'pattern': (r'Писатели\s+статуса\s+вне\s+этого\s+перечня\s+существуют'
                    r'|тропа\s+синка\s+ставок\s+ставит\s+мягкий\s+запрет'
                    r'|сервис\s+блокировки\s+и\s+самостоятельных\s+писателей'),
        'arrived': (r'в\s+дереве\s+его\s+нет'
                    r'|Построенный\s+писатель\s+подъёма\s+сегодня\s+один'),
        'date': '2026-09-11',
        'source': 'узел 2 GAPS_CLOSE_6, шаг 10 фазы 2 (закрытие B3 и A3)',
        'allowed': ('.claude/work/decision-digest.md',),
        'population': (
            ('docs/components/HoldService.md', r'в\s+дереве\s+его\s+нет'),
            ('docs/rules/exchange-hold.md',
             r'Построенный\s+писатель\s+подъёма\s+сегодня\s+один'),
            ('docs/rules/manual-halt.md', r'в\s+дереве\s+его\s+нет'),
        ),
    },
    {
        'name': 'дом привязки называет писателем затребователя ребра',
        # Снято узлом 2 GAPS_CLOSE_6 шага 10 фазы 2 (2026-09-11, закрытие A1
        # и C2). Прежняя редакция ставила в колонку «Писатель» компонент-доки
        # тех, кого клетка момента тут же называла затребователями, — при том
        # что у соседних строк та же колонка несла настоящих писателей. Что
        # действует теперь — объявляет ключ `arrived`.
        'pattern': (r'HoldRaised`\s*\|\s*`docs/components/HoldService\.md`'
                    r'|DealShutdownInitiated`\s*\|\s*`docs/components/DealActiveHandler\.md`'),
        'arrived': r'В\s+колонке\s+назван\s+ПИСАТЕЛЬ',
        'date': '2026-09-11',
        'source': 'узел 2 GAPS_CLOSE_6, шаг 10 фазы 2 (закрытие A1 и C2)',
        'allowed': ('.claude/work/decision-digest.md',),
        'population': (
            ('docs/architecture/contracts.md', r'В\s+колонке\s+назван\s+ПИСАТЕЛЬ'),
        ),
    },
    {
        'name': 'затребователь ребра определён вызовом, а не решением',
        # Снято приземлением узла 2 GAPS_CLOSE_6 шага 10 фазы 2 (2026-09-11,
        # закрытие Ф2). Снятая редакция определяла затребователя тропой,
        # доходящей до ребра ВЫЗОВОМ: под ней затребователем оказывался
        # проход-передатчик, а семь носителей корпуса, называющих
        # затребователем решающего, становились ложны разом. Той же редакцией
        # в таблицу причин остановки завелась третья роль («решающий»),
        # которой словарь дома не знает. Что действует теперь — объявляют
        # ключи `arrived` и `population`.
        'pattern': (r'затребуют\s+его\s+тропы'
                    r'|причину\s+выхода\s+решает\s+обработчик'
                    r'|тот\s+же\s+решающий'),
        'arrived': r'чьим\s+решением\*\*\s+ребро\s+объяснено',
        'date': '2026-09-11',
        'source': 'приземление узла 2 GAPS_CLOSE_6, шаг 10 фазы 2 (закрытие Ф2)',
        'allowed': (),
        'population': (
            ('docs/architecture/contracts.md',
             r'чьим\s+решением\*\*\s+ребро\s+объяснено'),
            ('docs/lifecycles/Deal.md',
             r'затребователь\s+у\s+него\s+один\s+—\s+\*\*обработчик\s+активной\s+сделки\*\*'),
            ('docs/components/DealActiveHandler.md',
             r'Событие\s+остановки\s+пишет\s+не\s+он'),
        ),
    },
    {
        'name': 'форма счётчика происшествий верна по однократности события на предмете',
        # Снято узлом 3 GAPS_CLOSE_6 шага 10 фазы 2 (2026-09-11, закрытие A5).
        # Снятая редакция обосновывала отбор строк журнала по классу события
        # квантором «у классов, над которыми стоя́т счётчики, событие
        # однократно на своём предмете», и называла среди них подъём ступени.
        # Квантор ложен: предмет HoldRaised — объект блокировки, а эскалация
        # мягкой ступени в жёсткую на том же объекте достижима и пишет второй
        # факт того же класса. Что действует теперь — объявляют ключи
        # `arrived` и `population`.
        'pattern': (r'однократно\s+на\s+своём\s+предмете'
                    r'|верная\s+лишь\s+тогда,\s+когда\s+у\s+предмета\s+событие\s+ровно\s+одно'
                    r'|[Сс]чётчик\s+над\s+классом,\s+который\s+бывает\s+многократным'),
        'arrived': r'совпадать\s+с\s+тем,\s+ЧТО\s+счётчик\s+объявлен\s+считать'
                   r'|считает\s+СОБЫТИЯ,\s+а\s+не\s+их\s+предметы',
        'date': '2026-09-11',
        'source': 'A5 DOCS_CHECK_6: довод формы счётчиков происшествий, узел 3 GAPS_CLOSE_6',
        # Дайджест называет снятую формулировку как ПРЕДМЕТ решения — разбор
        # альтернативы (б) без неё не читается; без allowed запись падала бы на
        # носителе, который её и объявляет снятой. Отчёты `progress/` в allowed
        # не перечисляются: они изъяты из области списком SKIP. Каталог
        # снапшотов тоже не перечисляется, и это не пропуск — сверка идёт
        # ТОЧНЫМ совпадением пути, а имя снапшота меняется с каждой версией:
        # запись-префикс была бы объявленным, но неработающим разрешением.
        'allowed': ('.claude/work/decision-digest.md',),
        'population': (
            ('docs/rules/statistics-aggregates.md',
             r'совпадать\s+с\s+тем,\s+ЧТО\s+счётчик\s+объявлен\s+считать'),
            ('services/audit-statistics/src/main/java/com/example/auditstatistics/'
             'persistence/repository/journalread/IncidentGrainRow.java',
             r'считает\s+СОБЫТИЯ,\s+а\s+не\s+их\s+предметы'),
            ('services/audit-statistics/src/main/resources/db/migration/statistics/'
             'V1__statistics_baseline.sql',
             r'считает\s+СОБЫТИЯ,\s+а\s+не\s+их\s+предметы'),
            ('services/audit-statistics/src/main/java/com/example/auditstatistics/'
             'domain/model/IncidentAggregate.java',
             r'считает\s+СОБЫТИЯ,\s+а\s+не\s+их\s+предметы'),
        ),
    },
]


# ПРОДОЛЖЕНИЕ КОММЕНТАРИЯ в java: звёздочка javadoc, её закрывающая пара и две
# косые черты строчного комментария. Сведение носителя к плоскому тексту меняет
# перенос с пробелами на пробел — в markdown этого довольно, а в java между
# словами остаётся маркер продолжения, и шаблон записи реестра не совпадает,
# хотя деревья кода объявлены областью свипа. Термин, разорванный переносом
# внутри javadoc, был детектору невидим.
#
# ОБЛАСТЬ НОРМАЛИЗАЦИИ — ТОЛЬКО `.java`, и это не осторожность, а признак:
# маркер продолжения существует там, где его вводит синтаксис комментария. В
# markdown `*` начинает пункт списка, и снятие его склеивало бы соседние пункты
# в одну строку — то есть заводило бы ЛОЖНЫЕ срабатывания там, где сегодня их
# нет. Ось контроля на это в батарее стои́т.
JAVA_CONTINUATION = re.compile(r'\n[ \t]*(?:\*/|\*|//)[ \t]?')


def flatten(text, suffix):
    """Плоский текст носителя: перенос с продолжением комментария — в пробел."""
    if suffix == '.java':
        text = JAVA_CONTINUATION.sub('\n', text)
    return re.sub(r'\s*\n\s*', ' ', text)


def flat_text(path):
    with io.open(path, encoding='utf-8', errors='replace') as handle:
        return flatten(handle.read(), os.path.splitext(path)[1])


def carriers(roots):
    found = []
    for pattern in roots:
        for path in glob.glob(pattern, recursive=True):
            if any(skip in '/' + path.replace(os.sep, '/') for skip in SKIP):
                continue
            found.append(path)
    return sorted(set(found))


def area_incomplete(roots, out_of_area, root='.',
                    out_of_area_files=None, out_of_area_suffixes=None):
    """Ось 20: область объявлена исчерпывающе против фактического корня.

    ТРИ ПОДОСИ, И ВСЕ ТРИ НУЖНЫ. Прежняя редакция мерила только первую, и
    молчаливый пропуск возвращался через две другие — класс предъявлен дважды,
    на носителях разного типа: файл ВНЕ покрытых деревьев (комментарий корневого
    `pom.xml`) и расширение ВНЕ покрытых ВНУТРИ дерева (`tools/session-prompt.md`).
    Оба были закрыты поимённой строкой популяции конкретной записи — то есть
    средством, работающим лишь там, где закрытие о носителе уже знает.

      20a. КАТАЛОГ верхнего уровня обязан быть либо покрыт шаблоном с
           ЛИТЕРАЛЬНЫМ первым сегментом, либо назван в `out_of_area` с
           причиной. Шаблон с подстановкой в первом сегменте покрытием не
           считается: иначе `*/README.md` покрывал бы всякое новое дерево
           молча, и ось не срабатывала бы никогда.
      20b. ФАЙЛ верхнего уровня обязан быть либо покрыт шаблоном без
           разделителя пути, либо назван в `out_of_area_files` с причиной.
           Корень — не только каталоги: `pom.xml` и `vault.hcl` лежат рядом с
           `CLAUDE.md` и в область прежде не попадали ни одним шаблоном.
      20c. РАСШИРЕНИЕ, встреченное внутри покрытого дерева, обязано быть либо
           достижимо шаблоном этого дерева, либо названо в
           `out_of_area_suffixes` парой «дерево.расширение» с причиной.

    Гранулярность 20c — дерево, а не путь: `.claude/work/*.json` делает
    расширение `json` достижимым во всём `.claude/**`, хотя шаблон покрывает
    один каталог. Это названное огрубление — подось отвечает на вопрос «есть ли
    у расширения хоть один читатель в дереве», а не «покрыт ли каждый файл».
    Пути, снятые `SKIP` (архивы, история, библиотека, `target/`), в обходе
    20c не участвуют: они вне свипа по построению, и требовать объявления их
    расширений значило бы объявлять область того, что не свипается.
    """
    out_of_area_files = out_of_area_files or {}
    out_of_area_suffixes = out_of_area_suffixes or {}
    covered = set()
    covered_files = set()
    for pattern in roots:
        flat = pattern.replace(os.sep, '/')
        head = flat.split('/')[0]
        if '/' not in flat and not glob.has_magic(flat):
            covered_files.add(flat)
            continue
        if head and not glob.has_magic(head):
            covered.add(head)
    try:
        entries = sorted(os.listdir(root))
    except OSError as failure:
        return 'корень репозитория не прочитан (%s) — область не сверить' % failure

    undeclared = [name for name in entries
                  if os.path.isdir(os.path.join(root, name))
                  and name not in covered and name not in out_of_area]
    if undeclared:
        return ('каталоги верхнего уровня вне объявленной области и не названные '
                'причиной: %s — свип по неполной области ничего не удостоверяет'
                % ', '.join(undeclared))

    stray = [name for name in entries
             if os.path.isfile(os.path.join(root, name))
             and name not in covered_files and name not in out_of_area_files]
    if stray:
        return ('файлы корня вне объявленной области и не названные причиной: %s '
                '— свип по неполной области ничего не удостоверяет'
                % ', '.join(stray))

    reachable = set()
    for pattern in roots:
        for path in glob.glob(os.path.join(root, pattern), recursive=True):
            reachable.add(os.path.relpath(path, root).replace(os.sep, '/'))
    for tree in sorted(covered):
        tree_path = os.path.join(root, tree)
        if not os.path.isdir(tree_path):
            continue
        for directory, _, names in os.walk(tree_path):
            relative = os.path.relpath(directory, root).replace(os.sep, '/')
            flat_dir = '/' + ('' if relative == '.' else relative + '/')
            if any(skipped in flat_dir for skipped in SKIP):
                continue
            for name in names:
                if '.' not in name:
                    continue
                suffix = name.rsplit('.', 1)[-1]
                relative_file = (relative + '/' + name) if relative != '.' else name
                if relative_file in reachable:
                    continue
                if '%s.%s' % (tree, suffix) in out_of_area_suffixes:
                    continue
                return ('расширение `.%s` внутри покрытого дерева `%s` не достижимо '
                        'ни одним шаблоном области и не названо причиной (пример: '
                        '%s) — свип по неполной области ничего не удостоверяет'
                        % (suffix, tree,
                           os.path.relpath(os.path.join(directory, name),
                                           root).replace(os.sep, '/')))
    return None


def registry_incomplete(registry):
    """Базовый гейт реестра: запись без пришедшей редакции или без популяции
    оставляет ось 4 неизмеренной, а прогон — зелёным. Это ложный зелёный."""
    for entry in registry:
        for key in ('pattern', 'arrived', 'population'):
            if not entry.get(key):
                return 'запись «%s» не несёт «%s» — ось популяции не измерима' % (
                    entry.get('name', '?'), key)
        for place in entry['allowed']:
            if place.replace(os.sep, '/').startswith('docs/'):
                return ('запись «%s» держит в разрешённых местах файл корпуса «%s»: '
                        'docs/concept.md запрещает слой опровержения в корпусе, '
                        'и allow-лист там означал бы подавитель, а не разрешение'
                        % (entry.get('name', '?'), place))
    return None


PROSE_DEBT = os.path.join(os.path.dirname(os.path.abspath(__file__)),
                          'retired-prose-debt.txt')
PROSE_MARK = re.compile(r'Пришедш[а-яё]*')


def record_comments(source):
    """Комментарий каждой записи реестра, в порядке самих записей."""
    start = source.index('RETIRED = [')
    chunks = re.split(r'\n    \{\n', source[start:])[1:]
    out = []
    for chunk in chunks:
        lines = [line.strip().lstrip('#').strip()
                 for line in chunk.split('\n') if line.strip().startswith('#')]
        out.append(' '.join(lines))
    return out


def prose_hits(source, registry):
    """Записи, чей комментарий пересказывает пришедшую редакцию."""
    comments = record_comments(source)
    hits = []
    for index, entry in enumerate(registry):
        comment = comments[index] if index < len(comments) else ''
        if PROSE_MARK.search(comment):
            hits.append(entry.get('name', '?'))
    return hits


def prose_debt():
    if not os.path.isfile(PROSE_DEBT):
        return None
    names = []
    with io.open(PROSE_DEBT, encoding='utf-8') as handle:
        for line in handle:
            line = line.strip()
            if line and not line.startswith('#'):
                names.append(line)
    return names


def scan(registry, roots):
    if not registry:
        return None, 'реестр снятых редакций пуст — проверять нечего'
    incomplete = registry_incomplete(registry)
    if incomplete:
        return None, incomplete
    files = carriers(roots)
    if not files:
        return None, 'ни одного живого носителя не найдено — проверять нечего'
    hits = []
    self_name = os.path.basename(__file__)
    # --- оси 1-3: снятая редакция жива в потребителе
    for path in files:
        normalized = path.replace(os.sep, '/')
        if os.path.basename(normalized) == self_name:
            # Сам реестр — разрешённое место для КАЖДОЙ своей записи: он их и
            # объявляет. Перечислять его в allowed у каждой строки значило бы
            # держать одно и то же знание в N местах.
            continue
        text = flat_text(path)
        for entry in registry:
            if normalized in entry['allowed']:
                continue
            unless = entry.get('unless')
            for match in re.finditer(entry['pattern'], text, re.I):
                if unless:
                    window = text[max(0, match.start() - 160):match.end() + 160]
                    if re.search(unless, window, re.I):
                        # Действующая редакция: термин стои́т рядом с отрицанием.
                        continue
                hits.append((path, entry['name'], 'снятая редакция жива',
                             match.group(0)[:60], entry['date'], entry['source']))
    # --- оси 4-5: популяция носителей предъявляет пришедшую редакцию
    population_checked = 0
    for entry in registry:
        for member, own_arrived in entry['population']:
            population_checked += 1
            expected = own_arrived or entry['arrived']
            if not os.path.exists(member):
                hits.append((member, entry['name'],
                             'строка популяции указывает на носитель, которого нет',
                             '', entry['date'], entry['source']))
                continue
            text = flat_text(member)
            if not re.search(expected, text, re.I):
                hits.append((member, entry['name'],
                             'пришедшая редакция не дошла до носителя популяции',
                             expected[:40], entry['date'], entry['source']))
    return (len(files), population_checked, hits), None


# --- батарея осей ------------------------------------------------------------

def battery():
    axes = []
    with tempfile.TemporaryDirectory() as work:
        def page(name, body):
            path = os.path.join(work, name)
            with io.open(path, 'w', encoding='utf-8') as handle:
                handle.write(body)
            return os.path.join(work, '*.md')

        def path_of(name):
            return os.path.join(work, name).replace(os.sep, '/')

        def probe(population, allowed=None):
            return [{
                'name': 'проба',
                'pattern': r'снят\w+\s+редакци\w+\s+живёт',
                'arrived': r'пришедш\w+\s+редакци\w+\s+стои́т',
                'date': '2026-08-31',
                'source': 'проба',
                'allowed': allowed if allowed is not None else (path_of('дом.md'),),
                'population': population,
            }]

        # Носитель популяции, всегда предъявляющий пришедшую редакцию: чтобы
        # оси 1-3 мерились без побочного срабатывания оси 4.
        page('популяция-ок.md', '# ПО\n\nЗдесь пришедшая редакция стои́т.\n')
        ok_population = ((path_of('популяция-ок.md'), None),)

        roots = page('потребитель.md', '# П\n\nЗдесь снятая редакция живёт до сих пор.\n')
        result, _ = scan(probe(ok_population), [roots])
        axes.append(('1. дословный термин в живом носителе',
                     any(kind == 'снятая редакция жива' for _, _, kind, _, _, _ in result[2]),
                     'дефектов: %d' % len(result[2])))

        os.remove(os.path.join(work, 'потребитель.md'))
        roots = page('перенос.md', '# П\n\nЗдесь снятая\nредакция живёт до сих пор.\n')
        result, _ = scan(probe(ok_population), [roots])
        axes.append(('2. термин, разорванный переносом строки',
                     any(kind == 'снятая редакция жива' for _, _, kind, _, _, _ in result[2]),
                     'дефектов: %d' % len(result[2])))

        os.remove(os.path.join(work, 'перенос.md'))
        roots = page('дом.md', '# Д\n\nПрежняя снятая редакция живёт — здесь она названа '
                               'затем, чтобы сказать, что её больше нет.\n')
        result, _ = scan(probe(ok_population), [roots])
        axes.append(('3. контроль: дом, фиксирующий снятие, разрешён', not result[2],
                     'дефектов: %d' % len(result[2])))

        os.remove(os.path.join(work, 'дом.md'))
        roots = page('чисто.md', '# Ч\n\nДействующая редакция и ничего больше.\n')
        result, _ = scan(probe(ok_population), [roots])
        axes.append(('4. контроль: чистый носитель находки не даёт', not result[2],
                     'дефектов: %d' % len(result[2])))

        # --- ось популяции: встречная форма
        page('встречная.md', '# В\n\nТо же самое, сказанное совершенно другими словами: '
                             'незавершённое ищется поиском.\n')
        result, _ = scan(probe(((path_of('встречная.md'), None),)), [roots])
        axes.append(('5. носитель популяции без пришедшей редакции (встречная форма)',
                     any(kind.startswith('пришедшая') for _, _, kind, _, _, _ in result[2]),
                     'дефектов: %d' % len(result[2])))

        page('перенос-пришедшей.md', '# ПП\n\nЗдесь пришедшая\nредакция стои́т.\n')
        result, _ = scan(probe(((path_of('перенос-пришедшей.md'), None),)), [roots])
        axes.append(('6. контроль: пришедшая редакция через перенос строки — не дефект',
                     not result[2], 'дефектов: %d' % len(result[2])))

        result, _ = scan(probe(((path_of('нет-такого-носителя.md'), None),)), [roots])
        axes.append(('7. строка популяции указывает на несуществующий носитель',
                     any(kind.startswith('строка популяции') for _, _, kind, _, _, _ in result[2]),
                     'дефектов: %d' % len(result[2])))

        result, _ = scan(probe(ok_population), [roots])
        axes.append(('8. контроль: носитель популяции с пришедшей редакцией — не дефект',
                     not result[2], 'дефектов: %d' % len(result[2])))

        page('свой-шаблон.md', '# СШ\n\nЗдесь стои́т переехавший клейм: носители клейма — '
                                'примеры ниже.\n')
        result, _ = scan(probe(((path_of('свой-шаблон.md'), None),)), [roots])
        own_missing = bool(result[2])
        result, _ = scan(probe(((path_of('свой-шаблон.md'), r'носител\w*\s+клейма'),)), [roots])
        axes.append(('9. свой шаблон носителя перекрывает шаблон записи',
                     own_missing and not result[2],
                     'по шаблону записи дефект есть, по своему — нет: %s'
                     % (own_missing and not result[2])))

        page('отрицание.md', '# О\n\nЗдесь снятая редакция живёт до сих пор — впрочем, нет: '
                              'действующая редакция отрицает её прямо.\n')
        neg = probe(ok_population)
        neg[0]['unless'] = r'действующая\s+редакция\s+отрицает'
        result, _ = scan(neg, [os.path.join(work, '*.md')])
        axes.append(('14. отрицание рядом с термином находкой не считается',
                     not any(k == 'снятая редакция жива' for _, _, k, _, _, _ in result[2]),
                     'дефектов: %d' % len(result[2])))
        result, _ = scan(probe(ok_population), [os.path.join(work, '*.md')])
        axes.append(('15. контроль оси 14: без unless тот же текст — находка',
                     any(k == 'снятая редакция жива' for _, _, k, _, _, _ in result[2]),
                     'дефектов: %d' % len(result[2])))
        os.remove(os.path.join(work, 'отрицание.md'))

        # --- отказы
        _, refusal = scan([], [roots])
        axes.append(('16. пустой реестр — проверка отказывает', bool(refusal),
                     refusal or 'проверка отчиталась'))
        _, refusal = scan(probe(ok_population), [os.path.join(work, 'нет-такого', '*.md')])
        axes.append(('17. корпус пуст — проверка отказывает', bool(refusal),
                     refusal or 'проверка отчиталась'))
        broken = probe(ok_population)
        broken[0]['population'] = ()
        _, refusal = scan(broken, [roots])
        axes.append(('18. запись без популяции — проверка отказывает', bool(refusal),
                     refusal or 'проверка отчиталась'))
        broken = probe(ok_population)
        broken[0]['arrived'] = ''
        _, refusal = scan(broken, [roots])
        axes.append(('19. запись без шаблона пришедшей редакции — проверка отказывает',
                     bool(refusal), refusal or 'проверка отчиталась'))

        # --- ось 20: полнота ОБЛАСТИ против фактического корня.
        # Корень проб — СВОЙ каталог, а не общая рабочая папка батареи:
        # подось 20b мерит файлы корня, а в общей папке лежат фикстуры
        # прочих осей, и проба ловила бы их вместо своего предмета.
        area = os.path.join(work, 'корень-области')
        os.makedirs(os.path.join(area, 'новое-дерево'), exist_ok=True)
        refusal = area_incomplete(('дом/**/*.md',), {}, area)
        axes.append(('20. дерево корня вне области и не названное причиной — '
                     'проверка отказывает', bool(refusal),
                     refusal or 'проверка отчиталась'))
        refusal = area_incomplete(('*/README.md',), {}, area)
        axes.append(('21. подстановка в первом сегменте покрытием не считается',
                     bool(refusal), refusal or 'проверка отчиталась'))
        refusal = area_incomplete(('новое-дерево/**/*.md',), {}, area)
        axes.append(('22. контроль: дерево покрыто литеральным шаблоном — '
                     'отказа нет', not refusal, refusal or 'проверка отчиталась'))
        refusal = area_incomplete(('дом/**/*.md',),
                                  {'новое-дерево': 'проба: названо причиной'}, area)
        axes.append(('23. контроль: дерево названо в OUT_OF_AREA — отказа нет',
                     not refusal, refusal or 'проверка отчиталась'))

        # --- ось 20b: ФАЙЛ корня вне области. Класс предъявлен комментарием
        # корневого `pom.xml`: он лежал рядом с `CLAUDE.md` и не попадал в
        # область ни одним шаблоном, а ось 20a мерила только каталоги.
        with open(os.path.join(area, 'сборка.xml'), 'w', encoding='utf-8') as handle:
            handle.write('<!-- проба -->\n')
        refusal = area_incomplete(('новое-дерево/**/*.md',), {}, area)
        axes.append(('20b. файл корня вне области и не названный причиной — '
                     'проверка отказывает', bool(refusal),
                     refusal or 'проверка отчиталась'))
        refusal = area_incomplete(('новое-дерево/**/*.md', 'сборка.xml'), {}, area)
        axes.append(('20b-контроль: файл корня покрыт шаблоном — отказа нет',
                     not refusal, refusal or 'проверка отчиталась'))
        refusal = area_incomplete(('новое-дерево/**/*.md',), {}, area,
                                  {'сборка.xml': 'проба: названо причиной'})
        axes.append(('20b-контроль: файл корня назван причиной — отказа нет',
                     not refusal, refusal or 'проверка отчиталась'))
        os.remove(os.path.join(area, 'сборка.xml'))

        # --- ось 20c: РАСШИРЕНИЕ внутри покрытого дерева. Класс предъявлен
        # `tools/session-prompt.md`: дерево объявлено областью, а расширение в
        # неё не входило — пропуск был молчаливым ровно так же.
        with open(os.path.join(area, 'новое-дерево', 'промпт.md'), 'w',
                  encoding='utf-8') as handle:
            handle.write('проба\n')
        refusal = area_incomplete(('новое-дерево/**/*.py',), {}, area)
        axes.append(('20c. расширение внутри покрытого дерева вне области — '
                     'проверка отказывает', bool(refusal),
                     refusal or 'проверка отчиталась'))
        refusal = area_incomplete(('новое-дерево/**/*.py', 'новое-дерево/**/*.md'),
                                  {}, area)
        axes.append(('20c-контроль: расширение достижимо шаблоном дерева — '
                     'отказа нет', not refusal, refusal or 'проверка отчиталась'))
        refusal = area_incomplete(('новое-дерево/**/*.py',), {}, area, {},
                                  {'новое-дерево.md': 'проба: названо причиной'})
        axes.append(('20c-контроль: расширение названо причиной — отказа нет',
                     not refusal, refusal or 'проверка отчиталась'))
        os.remove(os.path.join(area, 'новое-дерево', 'промпт.md'))
        os.rmdir(os.path.join(area, 'новое-дерево'))
        # Ось 8 — проза записи, пересказывающая пришедшую редакцию. Мерится
        # на СИНТЕТИЧЕСКОМ исходнике: детектор читает текст реестра, а не
        # только разобранный список, и проба обязана это доказать.
        source_with = ('RETIRED = [\n    {\n'
                       '        # Снято пробой. Пришедшая редакция: нечто иное.\n'
                       "        'name': 'проба',\n    },\n]\n")
        source_without = ('RETIRED = [\n    {\n'
                          '        # Снято пробой; довод — почему снято.\n'
                          "        'name': 'проба',\n    },\n]\n")
        entry = [{'name': 'проба'}]
        axes.append(('24. проза записи пересказывает пришедшую редакцию',
                     prose_hits(source_with, entry) == ['проба'],
                     'запись с прозой опознана'))
        axes.append(('25. контроль: комментарий без пересказа — не дефект',
                     prose_hits(source_without, entry) == [],
                     'запись без прозы молчит'))

    # --- ось 26: ПРОДОЛЖЕНИЕ КОММЕНТАРИЯ в java. Термин, разорванный переносом
    # внутри javadoc, прежде был невидим: между словами оставалась звёздочка, и
    # шаблон записи не совпадал, хотя дерево кода объявлено областью свипа.
    javadoc = ('/**\n'
               ' * Текст про одну ACTIVE\n'
               ' * на инструмент — разорван переносом.\n'
               ' */\n')
    pattern = r'одну\s+ACTIVE\s+на\s+инструмент'
    axes.append(('26. термин, разорванный переносом внутри javadoc, опознан',
                 bool(re.search(pattern, flatten(javadoc, '.java'), re.I)),
                 'плоский текст: %r' % (flatten(javadoc, '.java').strip(),)))
    axes.append(('26-контроль: без нормализации тот же термин невиден',
                 not re.search(pattern, flatten(javadoc, '.md'), re.I),
                 'плоский текст: %r' % (flatten(javadoc, '.md').strip(),)))

    # --- ось 27: контроль на ЛОЖНОЕ срабатывание. В markdown `*` начинает пункт
    # списка, а не продолжает комментарий; снятие его склеило бы соседние пункты
    # и завело бы находку там, где терминов нет.
    bullet = '* одну ACTIVE\n* на инструмент\n'
    axes.append(('27. контроль: пункты markdown-списка не склеиваются',
                 not re.search(pattern, flatten(bullet, '.md'), re.I),
                 'плоский текст: %r' % (flatten(bullet, '.md').strip(),)))

    return axes


def main():
    axes = battery()
    print('--- батарея осей детектора (исполняется той же командой)')
    for title, passed, observed in axes:
        print('  %s: %s — %s' % ('доказана' if passed else 'НЕ ДОКАЗАНА', title, observed))
    broken = [title for title, passed, _ in axes if not passed]
    if broken:
        print('ПРОВЕРКА НЕ ПРОВОДИТСЯ: недоказанных осей %d — чистый свип '
              'ничего не удостоверял бы' % len(broken))
        return 2

    refusal = area_incomplete(ROOTS, OUT_OF_AREA, '.',
                              OUT_OF_AREA_FILES, OUT_OF_AREA_SUFFIXES)
    if refusal:
        print('ПРОВЕРКА НЕ ПРОВОДИТСЯ: ' + refusal)
        return 2

    declared = prose_debt()
    if declared is None:
        print('ПРОВЕРКА НЕ ПРОВОДИТСЯ: файла долга прозы '
              '`tools/retired-prose-debt.txt` нет — ось 8 неизмерима')
        return 2
    with io.open(os.path.abspath(__file__), encoding='utf-8') as handle:
        prose = prose_hits(handle.read(), RETIRED)
    fresh = [name for name in prose if name not in declared]
    stale = [name for name in declared if name not in prose]
    print('записей с прозой о пришедшей редакции: %d (в долге — %d, '
          'новых — %d, строк долга без записи — %d)'
          % (len(prose), len(declared), len(fresh), len(stale)))
    for name in fresh:
        print('  ДЕФЕКТ [проза записи]: «%s» — пришедшую редакцию объявляет '
              'ключ `arrived`, а не комментарий '
              '(.claude/rules/self-description-form.md)' % name)
    for name in stale:
        print('  ДЕФЕКТ [долг прозы]: строка долга «%s» — записи с такой прозой '
              'в реестре больше нет; строку снять' % name)

    result, refusal = scan(RETIRED, ROOTS)
    if refusal:
        print('ПРОВЕРКА НЕ ПРОВОДИТСЯ: ' + refusal)
        return 2
    checked, population_checked, hits = result
    print('живых носителей просмотрено: %d; записей реестра: %d; '
          'строк популяции проверено: %d; ДЕФЕКТОВ: %d'
          % (checked, len(RETIRED), population_checked, len(hits)))
    for path, name, kind, sample, date, source in hits:
        tail = ('; вхождение: «%s…»' % sample) if sample else ''
        print('  ДЕФЕКТ [%s]: %s — «%s» (снято %s: %s)%s'
              % (kind, path, name, date, source, tail))
    return 1 if (hits or fresh or stale) else 0


if __name__ == '__main__':
    sys.exit(main())
