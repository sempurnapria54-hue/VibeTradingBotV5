# Долг прозы записей реестра снятых редакций.
#
# Дом пришедшей редакции — ключ `arrived`; комментарий, объявляющий её ещё
# и словами, есть второй носитель той же истины и стареет первым
# (`.claude/rules/self-description-form.md`). Перечисленные записи заведены
# ДО правила; прогон `retired-check.py` падает на НОВОЙ записи с прозой и на
# строке долга, которой в реестре больше нет. Долг не растёт.
#
# Две записи узла 1 GAPS_CLOSE_4 шага 10 фазы 2 в долг НЕ внесены: пересказ
# снят с них тем же проходом, который завёл правило, — на одной из них он и
# соврал (находка W1, из-за которой узел остановлен сигналом С5).
#
# Погашение — `.claude/work/backlog.md` §«Проза пришедшей редакции в записях
# реестра снятых редакций».
#
# Одна строка — имя записи (ключ `name`).
право операции проверяет сервис на каждом вызове
смещение подписки сообщает клиент
состояния bff не держит — без оговорки о кэше
все формы к браузеру — своя api-модель периметра
тенант строкой — исключение ровно одно, конверт события
поле источника у черновика стратегии — существующее сегодня
состав strategy-engine — детекторы и интерпретатор, расчёт в ядре
радиус торговой строки — инструмент и биржа, а не счёт
owner-ключевание производных рыночных данных настройкой стратегии
синхронные вызовы «только вниз по ярусам» как правило яруса
суррогат границы — биржевой момент создания сделки
верхняя граница окна линковки — поле сделки
радиус шире инструмента — набором строк
после FAILED новая строка законна безусловно
популяция предиката стороны — только после входа
якорь уровня назван только у безубытка
ступень радиуса объекта — любая
диспозиция BLOCKED на PRECHECK — терминал безусловно
пустой разбор истории даёт благоприятный терминал
вторая ступень читает живую позицию уровня сделки
правило однократности не адресует шаги уровня сделки
КОД и РИСК в ноль незакрытых до перевода статуса
открытый слот грунта гейтит шаг
покодовый карв-аут объявлен неразрешимым и не применялся
у инварианта ручного снятия два энфорсера
ступень отказа кредов выводится вне оси лестницы
сделки доходят до терминала локально на тропе отказа кредов
тропа запроса ступени у отказа кредов — тропа контролируемых отказов
перечень «вне иерархии» закрыт одним признаком — механикой контура
форма реестра гейтов переключается фактом идущих прогонов
отметка обработанного у потребителя — всегда таблица inbox
ярусы запрещают восходящий вызов у потребителя события
аудит по тенанту полон уже сегодня
журнал хранится целиком и навсегда
исполнение команды рефреша попадает в историю
периметр шлёт audit-statistics команды пользователя
счётный клейм о построенности (классы, потребители, группы)
актор едет содержимым сегодня; ручная тропа только у классов определения
состав агрегата: ценовая сумма, gross-итог, знаменатель R и молчаливый UNIQUE
ветвь отказа журнала по трём полям; непрерывность без писателя и без живости измерителя
множественность событий на сделку — самостоятельное основание отказа классу
группа журнала читает одну тему; правило алерта — на группу; состав точек чтения без дома
группировка пересчёта — по четырём колонкам радиуса плюс валюте
нижняя граница полноты — одно min(recorded_at) по всему журналу
выведенный из знаменателя риск виден суммой на строке без валюты
операнд итога сделки зовётся gross
природу подъёма ступени различает актор строки журнала
счётчик значению нужен, когда оно достижимо в денежных суммах
домен сверяет контекст тенанта с собственным кэшем членств auth
состав каталога равен инвентарю
непокрытая доля trading-core — два класса
перечень обязательного входа равен колонкам not null
верификация финальной правки без права новой доработки
величину строки состояния приёма пишет проход опроса
у остановки приёма состояния не заводится
признак разрыва по сроку хранения без писателя снятия
отказ продвинуть смещение обеспечен умолчанием клиента
