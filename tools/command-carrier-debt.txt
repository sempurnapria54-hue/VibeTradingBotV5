# Объявленный долг клеток-носителей команд — команды, стоящие в клетке
# markdown-таблицы с экранированной альтернацией, и строки ограждённого блока,
# читающие имя, которое блок не связывает. Механизм и оси — шапка
# tools/command-carrier-check.py; дом правила —
# .claude/processes/roadmap-step-execution.md, пассаж «Команда, объявленная
# воспроизводимой, стоит блоком кода, а не клеткой таблицы».
#
# 2026-09-11 отсюда сняты все строки, стоявшие на тот момент: каждая адресовала
# отчёт шага 10 фазы 2, и переход шага в DONE увёз их носители в
# .claude/work/history/. Область энфорсера — живые отчёты
# .claude/work/progress/**; носитель, уехавший в history/, из области выходит, и
# строка долга без соответствия сама становится дефектом (ось 3).
#
# ПОГАШЕНИЕМ ПРАВКОЙ это не было, и подмены тут нет. Правка перечисленных клеток
# означала бы переписывание отчётов УЖЕ ЗАКРЫТЫХ заходов, а их правка делает их
# ложными, а не верными (.claude/rules/self-description-form.md §«Датированный
# замер клеймом не является»; .claude/rules/curation.md §«Чек-лист свипа»).
# Разбор — .claude/work/progress/phase-2-step-10-done-transition.md
# §«Долг клеток-носителей: снят переездом носителей, а не правкой».
#
# Прогон падает на НОВОЙ дефектной клетке или строке блока и на строке долга,
# которой в корпусе больше нет. Долг не растёт.
#
# Формат: <путь от .claude/work/progress/><TAB><начало команды, как его печатает
# прогон>. Номеров строк нет намеренно: номер дрейфует от любой правки.
